var x5 = Object.defineProperty;
var E5 = (i, t, e) => t in i ? x5(i, t, { enumerable: !0, configurable: !0, writable: !0, value: e }) : i[t] = e;
var h = (i, t, e) => E5(i, typeof t != "symbol" ? t + "" : t, e);
import * as w from "react";
import Se, { useState as Ne, useEffect as Ee, memo as Qy, forwardRef as Tg, useRef as at, useContext as hr, useLayoutEffect as T5, useCallback as $n, useMemo as bt, createContext as dc, useReducer as Sg, cloneElement as S5 } from "react";
import Xa, { unstable_batchedUpdates as Oa, createPortal as A5 } from "react-dom";
function P5(i, t) {
  for (var e = 0; e < t.length; e++) {
    const n = t[e];
    if (typeof n != "string" && !Array.isArray(n)) {
      for (const r in n)
        if (r !== "default" && !(r in i)) {
          const s = Object.getOwnPropertyDescriptor(n, r);
          s && Object.defineProperty(i, r, s.get ? s : {
            enumerable: !0,
            get: () => n[r]
          });
        }
    }
  }
  return Object.freeze(Object.defineProperty(i, Symbol.toStringTag, { value: "Module" }));
}
function Ag(i) {
  return i && i.__esModule && Object.prototype.hasOwnProperty.call(i, "default") ? i.default : i;
}
var Ff = { exports: {} }, Va = {};
/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var db;
function I5() {
  if (db) return Va;
  db = 1;
  var i = Se, t = Symbol.for("react.element"), e = Symbol.for("react.fragment"), n = Object.prototype.hasOwnProperty, r = i.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner, s = { key: !0, ref: !0, __self: !0, __source: !0 };
  function o(a, l, c) {
    var u, d = {}, f = null, m = null;
    c !== void 0 && (f = "" + c), l.key !== void 0 && (f = "" + l.key), l.ref !== void 0 && (m = l.ref);
    for (u in l) n.call(l, u) && !s.hasOwnProperty(u) && (d[u] = l[u]);
    if (a && a.defaultProps) for (u in l = a.defaultProps, l) d[u] === void 0 && (d[u] = l[u]);
    return { $$typeof: t, type: a, key: f, ref: m, props: d, _owner: r.current };
  }
  return Va.Fragment = e, Va.jsx = o, Va.jsxs = o, Va;
}
var Ma = {};
/**
 * @license React
 * react-jsx-runtime.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hb;
function R5() {
  return hb || (hb = 1, process.env.NODE_ENV !== "production" && function() {
    var i = Se, t = Symbol.for("react.element"), e = Symbol.for("react.portal"), n = Symbol.for("react.fragment"), r = Symbol.for("react.strict_mode"), s = Symbol.for("react.profiler"), o = Symbol.for("react.provider"), a = Symbol.for("react.context"), l = Symbol.for("react.forward_ref"), c = Symbol.for("react.suspense"), u = Symbol.for("react.suspense_list"), d = Symbol.for("react.memo"), f = Symbol.for("react.lazy"), m = Symbol.for("react.offscreen"), g = Symbol.iterator, p = "@@iterator";
    function b(C) {
      if (C === null || typeof C != "object")
        return null;
      var P = g && C[g] || C[p];
      return typeof P == "function" ? P : null;
    }
    var v = i.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    function y(C) {
      {
        for (var P = arguments.length, q = new Array(P > 1 ? P - 1 : 0), ge = 1; ge < P; ge++)
          q[ge - 1] = arguments[ge];
        x("error", C, q);
      }
    }
    function x(C, P, q) {
      {
        var ge = v.ReactDebugCurrentFrame, Oe = ge.getStackAddendum();
        Oe !== "" && (P += "%s", q = q.concat([Oe]));
        var ue = q.map(function(M) {
          return String(M);
        });
        ue.unshift("Warning: " + P), Function.prototype.apply.call(console[C], console, ue);
      }
    }
    var S = !1, k = !1, I = !1, A = !1, L = !1, R;
    R = Symbol.for("react.module.reference");
    function O(C) {
      return !!(typeof C == "string" || typeof C == "function" || C === n || C === s || L || C === r || C === c || C === u || A || C === m || S || k || I || typeof C == "object" && C !== null && (C.$$typeof === f || C.$$typeof === d || C.$$typeof === o || C.$$typeof === a || C.$$typeof === l || // This needs to include all possible module reference object
      // types supported by any Flight configuration anywhere since
      // we don't know which Flight build this will end up being used
      // with.
      C.$$typeof === R || C.getModuleId !== void 0));
    }
    function N(C, P, q) {
      var ge = C.displayName;
      if (ge)
        return ge;
      var Oe = P.displayName || P.name || "";
      return Oe !== "" ? q + "(" + Oe + ")" : q;
    }
    function le(C) {
      return C.displayName || "Context";
    }
    function te(C) {
      if (C == null)
        return null;
      if (typeof C.tag == "number" && y("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), typeof C == "function")
        return C.displayName || C.name || null;
      if (typeof C == "string")
        return C;
      switch (C) {
        case n:
          return "Fragment";
        case e:
          return "Portal";
        case s:
          return "Profiler";
        case r:
          return "StrictMode";
        case c:
          return "Suspense";
        case u:
          return "SuspenseList";
      }
      if (typeof C == "object")
        switch (C.$$typeof) {
          case a:
            var P = C;
            return le(P) + ".Consumer";
          case o:
            var q = C;
            return le(q._context) + ".Provider";
          case l:
            return N(C, C.render, "ForwardRef");
          case d:
            var ge = C.displayName || null;
            return ge !== null ? ge : te(C.type) || "Memo";
          case f: {
            var Oe = C, ue = Oe._payload, M = Oe._init;
            try {
              return te(M(ue));
            } catch {
              return null;
            }
          }
        }
      return null;
    }
    var me = Object.assign, ce = 0, Ae, Fe, be, re, B, X, he;
    function Q() {
    }
    Q.__reactDisabledLog = !0;
    function Y() {
      {
        if (ce === 0) {
          Ae = console.log, Fe = console.info, be = console.warn, re = console.error, B = console.group, X = console.groupCollapsed, he = console.groupEnd;
          var C = {
            configurable: !0,
            enumerable: !0,
            value: Q,
            writable: !0
          };
          Object.defineProperties(console, {
            info: C,
            log: C,
            warn: C,
            error: C,
            group: C,
            groupCollapsed: C,
            groupEnd: C
          });
        }
        ce++;
      }
    }
    function we() {
      {
        if (ce--, ce === 0) {
          var C = {
            configurable: !0,
            enumerable: !0,
            writable: !0
          };
          Object.defineProperties(console, {
            log: me({}, C, {
              value: Ae
            }),
            info: me({}, C, {
              value: Fe
            }),
            warn: me({}, C, {
              value: be
            }),
            error: me({}, C, {
              value: re
            }),
            group: me({}, C, {
              value: B
            }),
            groupCollapsed: me({}, C, {
              value: X
            }),
            groupEnd: me({}, C, {
              value: he
            })
          });
        }
        ce < 0 && y("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
      }
    }
    var ye = v.ReactCurrentDispatcher, Ce;
    function J(C, P, q) {
      {
        if (Ce === void 0)
          try {
            throw Error();
          } catch (Oe) {
            var ge = Oe.stack.trim().match(/\n( *(at )?)/);
            Ce = ge && ge[1] || "";
          }
        return `
` + Ce + C;
      }
    }
    var F = !1, H;
    {
      var pe = typeof WeakMap == "function" ? WeakMap : Map;
      H = new pe();
    }
    function W(C, P) {
      if (!C || F)
        return "";
      {
        var q = H.get(C);
        if (q !== void 0)
          return q;
      }
      var ge;
      F = !0;
      var Oe = Error.prepareStackTrace;
      Error.prepareStackTrace = void 0;
      var ue;
      ue = ye.current, ye.current = null, Y();
      try {
        if (P) {
          var M = function() {
            throw Error();
          };
          if (Object.defineProperty(M.prototype, "props", {
            set: function() {
              throw Error();
            }
          }), typeof Reflect == "object" && Reflect.construct) {
            try {
              Reflect.construct(M, []);
            } catch (Be) {
              ge = Be;
            }
            Reflect.construct(C, [], M);
          } else {
            try {
              M.call();
            } catch (Be) {
              ge = Be;
            }
            C.call(M.prototype);
          }
        } else {
          try {
            throw Error();
          } catch (Be) {
            ge = Be;
          }
          C();
        }
      } catch (Be) {
        if (Be && ge && typeof Be.stack == "string") {
          for (var $ = Be.stack.split(`
`), Z = ge.stack.split(`
`), ne = $.length - 1, oe = Z.length - 1; ne >= 1 && oe >= 0 && $[ne] !== Z[oe]; )
            oe--;
          for (; ne >= 1 && oe >= 0; ne--, oe--)
            if ($[ne] !== Z[oe]) {
              if (ne !== 1 || oe !== 1)
                do
                  if (ne--, oe--, oe < 0 || $[ne] !== Z[oe]) {
                    var se = `
` + $[ne].replace(" at new ", " at ");
                    return C.displayName && se.includes("<anonymous>") && (se = se.replace("<anonymous>", C.displayName)), typeof C == "function" && H.set(C, se), se;
                  }
                while (ne >= 1 && oe >= 0);
              break;
            }
        }
      } finally {
        F = !1, ye.current = ue, we(), Error.prepareStackTrace = Oe;
      }
      var ve = C ? C.displayName || C.name : "", Ge = ve ? J(ve) : "";
      return typeof C == "function" && H.set(C, Ge), Ge;
    }
    function We(C, P, q) {
      return W(C, !1);
    }
    function Ye(C) {
      var P = C.prototype;
      return !!(P && P.isReactComponent);
    }
    function Ze(C, P, q) {
      if (C == null)
        return "";
      if (typeof C == "function")
        return W(C, Ye(C));
      if (typeof C == "string")
        return J(C);
      switch (C) {
        case c:
          return J("Suspense");
        case u:
          return J("SuspenseList");
      }
      if (typeof C == "object")
        switch (C.$$typeof) {
          case l:
            return We(C.render);
          case d:
            return Ze(C.type, P, q);
          case f: {
            var ge = C, Oe = ge._payload, ue = ge._init;
            try {
              return Ze(ue(Oe), P, q);
            } catch {
            }
          }
        }
      return "";
    }
    var Je = Object.prototype.hasOwnProperty, pt = {}, Rn = v.ReactDebugCurrentFrame;
    function rn(C) {
      if (C) {
        var P = C._owner, q = Ze(C.type, C._source, P ? P.type : null);
        Rn.setExtraStackFrame(q);
      } else
        Rn.setExtraStackFrame(null);
    }
    function $t(C, P, q, ge, Oe) {
      {
        var ue = Function.call.bind(Je);
        for (var M in C)
          if (ue(C, M)) {
            var $ = void 0;
            try {
              if (typeof C[M] != "function") {
                var Z = Error((ge || "React class") + ": " + q + " type `" + M + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof C[M] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                throw Z.name = "Invariant Violation", Z;
              }
              $ = C[M](P, M, ge, q, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
            } catch (ne) {
              $ = ne;
            }
            $ && !($ instanceof Error) && (rn(Oe), y("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", ge || "React class", q, M, typeof $), rn(null)), $ instanceof Error && !($.message in pt) && (pt[$.message] = !0, rn(Oe), y("Failed %s type: %s", q, $.message), rn(null));
          }
      }
    }
    var zt = Array.isArray;
    function bn(C) {
      return zt(C);
    }
    function On(C) {
      {
        var P = typeof Symbol == "function" && Symbol.toStringTag, q = P && C[Symbol.toStringTag] || C.constructor.name || "Object";
        return q;
      }
    }
    function Vt(C) {
      try {
        return Ht(C), !1;
      } catch {
        return !0;
      }
    }
    function Ht(C) {
      return "" + C;
    }
    function hi(C) {
      if (Vt(C))
        return y("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", On(C)), Ht(C);
    }
    var jt = v.ReactCurrentOwner, Wt = {
      key: !0,
      ref: !0,
      __self: !0,
      __source: !0
    }, Yt, ti, sn;
    sn = {};
    function un(C) {
      if (Je.call(C, "ref")) {
        var P = Object.getOwnPropertyDescriptor(C, "ref").get;
        if (P && P.isReactWarning)
          return !1;
      }
      return C.ref !== void 0;
    }
    function Le(C) {
      if (Je.call(C, "key")) {
        var P = Object.getOwnPropertyDescriptor(C, "key").get;
        if (P && P.isReactWarning)
          return !1;
      }
      return C.key !== void 0;
    }
    function U(C, P) {
      if (typeof C.ref == "string" && jt.current && P && jt.current.stateNode !== P) {
        var q = te(jt.current.type);
        sn[q] || (y('Component "%s" contains the string ref "%s". Support for string refs will be removed in a future major release. This case cannot be automatically converted to an arrow function. We ask you to manually fix this case by using useRef() or createRef() instead. Learn more about using refs safely here: https://reactjs.org/link/strict-mode-string-ref', te(jt.current.type), C.ref), sn[q] = !0);
      }
    }
    function _e(C, P) {
      {
        var q = function() {
          Yt || (Yt = !0, y("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", P));
        };
        q.isReactWarning = !0, Object.defineProperty(C, "key", {
          get: q,
          configurable: !0
        });
      }
    }
    function Ue(C, P) {
      {
        var q = function() {
          ti || (ti = !0, y("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", P));
        };
        q.isReactWarning = !0, Object.defineProperty(C, "ref", {
          get: q,
          configurable: !0
        });
      }
    }
    var $e = function(C, P, q, ge, Oe, ue, M) {
      var $ = {
        // This tag allows us to uniquely identify this as a React Element
        $$typeof: t,
        // Built-in properties that belong on the element
        type: C,
        key: P,
        ref: q,
        props: M,
        // Record the component responsible for creating this element.
        _owner: ue
      };
      return $._store = {}, Object.defineProperty($._store, "validated", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: !1
      }), Object.defineProperty($, "_self", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: ge
      }), Object.defineProperty($, "_source", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: Oe
      }), Object.freeze && (Object.freeze($.props), Object.freeze($)), $;
    };
    function ze(C, P, q, ge, Oe) {
      {
        var ue, M = {}, $ = null, Z = null;
        q !== void 0 && (hi(q), $ = "" + q), Le(P) && (hi(P.key), $ = "" + P.key), un(P) && (Z = P.ref, U(P, Oe));
        for (ue in P)
          Je.call(P, ue) && !Wt.hasOwnProperty(ue) && (M[ue] = P[ue]);
        if (C && C.defaultProps) {
          var ne = C.defaultProps;
          for (ue in ne)
            M[ue] === void 0 && (M[ue] = ne[ue]);
        }
        if ($ || Z) {
          var oe = typeof C == "function" ? C.displayName || C.name || "Unknown" : C;
          $ && _e(M, oe), Z && Ue(M, oe);
        }
        return $e(C, $, Z, Oe, ge, jt.current, M);
      }
    }
    var ft = v.ReactCurrentOwner, Jt = v.ReactDebugCurrentFrame;
    function it(C) {
      if (C) {
        var P = C._owner, q = Ze(C.type, C._source, P ? P.type : null);
        Jt.setExtraStackFrame(q);
      } else
        Jt.setExtraStackFrame(null);
    }
    var rt;
    rt = !1;
    function dt(C) {
      return typeof C == "object" && C !== null && C.$$typeof === t;
    }
    function Mt() {
      {
        if (ft.current) {
          var C = te(ft.current.type);
          if (C)
            return `

Check the render method of \`` + C + "`.";
        }
        return "";
      }
    }
    function Dt(C) {
      return "";
    }
    var Lt = {};
    function Nt(C) {
      {
        var P = Mt();
        if (!P) {
          var q = typeof C == "string" ? C : C.displayName || C.name;
          q && (P = `

Check the top-level render call using <` + q + ">.");
        }
        return P;
      }
    }
    function on(C, P) {
      {
        if (!C._store || C._store.validated || C.key != null)
          return;
        C._store.validated = !0;
        var q = Nt(P);
        if (Lt[q])
          return;
        Lt[q] = !0;
        var ge = "";
        C && C._owner && C._owner !== ft.current && (ge = " It was passed a child from " + te(C._owner.type) + "."), it(C), y('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', q, ge), it(null);
      }
    }
    function Pt(C, P) {
      {
        if (typeof C != "object")
          return;
        if (bn(C))
          for (var q = 0; q < C.length; q++) {
            var ge = C[q];
            dt(ge) && on(ge, P);
          }
        else if (dt(C))
          C._store && (C._store.validated = !0);
        else if (C) {
          var Oe = b(C);
          if (typeof Oe == "function" && Oe !== C.entries)
            for (var ue = Oe.call(C), M; !(M = ue.next()).done; )
              dt(M.value) && on(M.value, P);
        }
      }
    }
    function vn(C) {
      {
        var P = C.type;
        if (P == null || typeof P == "string")
          return;
        var q;
        if (typeof P == "function")
          q = P.propTypes;
        else if (typeof P == "object" && (P.$$typeof === l || // Note: Memo only checks outer props here.
        // Inner props are checked in the reconciler.
        P.$$typeof === d))
          q = P.propTypes;
        else
          return;
        if (q) {
          var ge = te(P);
          $t(q, C.props, "prop", ge, C);
        } else if (P.PropTypes !== void 0 && !rt) {
          rt = !0;
          var Oe = te(P);
          y("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", Oe || "Unknown");
        }
        typeof P.getDefaultProps == "function" && !P.getDefaultProps.isReactClassApproved && y("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
      }
    }
    function wn(C) {
      {
        for (var P = Object.keys(C.props), q = 0; q < P.length; q++) {
          var ge = P[q];
          if (ge !== "children" && ge !== "key") {
            it(C), y("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", ge), it(null);
            break;
          }
        }
        C.ref !== null && (it(C), y("Invalid attribute `ref` supplied to `React.Fragment`."), it(null));
      }
    }
    var yn = {};
    function ni(C, P, q, ge, Oe, ue) {
      {
        var M = O(C);
        if (!M) {
          var $ = "";
          (C === void 0 || typeof C == "object" && C !== null && Object.keys(C).length === 0) && ($ += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.");
          var Z = Dt();
          Z ? $ += Z : $ += Mt();
          var ne;
          C === null ? ne = "null" : bn(C) ? ne = "array" : C !== void 0 && C.$$typeof === t ? (ne = "<" + (te(C.type) || "Unknown") + " />", $ = " Did you accidentally export a JSX literal instead of a component?") : ne = typeof C, y("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", ne, $);
        }
        var oe = ze(C, P, q, Oe, ue);
        if (oe == null)
          return oe;
        if (M) {
          var se = P.children;
          if (se !== void 0)
            if (ge)
              if (bn(se)) {
                for (var ve = 0; ve < se.length; ve++)
                  Pt(se[ve], C);
                Object.freeze && Object.freeze(se);
              } else
                y("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
            else
              Pt(se, C);
        }
        if (Je.call(P, "key")) {
          var Ge = te(C), Be = Object.keys(P).filter(function(Ut) {
            return Ut !== "key";
          }), Et = Be.length > 0 ? "{key: someKey, " + Be.join(": ..., ") + ": ...}" : "{key: someKey}";
          if (!yn[Ge + Et]) {
            var dn = Be.length > 0 ? "{" + Be.join(": ..., ") + ": ...}" : "{}";
            y(`A props object containing a "key" prop is being spread into JSX:
  let props = %s;
  <%s {...props} />
React keys must be passed directly to JSX without using spread:
  let props = %s;
  <%s key={someKey} {...props} />`, Et, Ge, dn, Ge), yn[Ge + Et] = !0;
          }
        }
        return C === n ? wn(oe) : vn(oe), oe;
      }
    }
    function zi(C, P, q) {
      return ni(C, P, q, !0);
    }
    function ii(C, P, q) {
      return ni(C, P, q, !1);
    }
    var qn = ii, Si = zi;
    Ma.Fragment = n, Ma.jsx = qn, Ma.jsxs = Si;
  }()), Ma;
}
process.env.NODE_ENV === "production" ? Ff.exports = I5() : Ff.exports = R5();
var _ = Ff.exports;
class O5 {
  constructor(t) {
    this.label = t;
  }
}
class Pd extends O5 {
  constructor(t) {
    super(
      t.label
    );
  }
}
class V5 {
  constructor(t, e) {
    this.render = t, this.validation = e;
  }
}
class zn extends V5 {
  constructor(t) {
    super(
      t.render,
      t.validation
    );
  }
}
class M5 {
  constructor(t, e, n, r, s, o, a, l, c) {
    this.form = t, this.settings = e, this.type = n, this.heading = r, this.description = s, this.hidden = o, this.icon = a, this.data = l, this.group = c, this.group || (this.group = new Pd({
      label: "None"
    }));
  }
}
let ps = class extends M5 {
  constructor(t) {
    super(
      t.form,
      t.settings,
      t.type,
      t.heading,
      t.description,
      t.hidden,
      t.icon,
      t.data,
      t.group
    );
  }
};
class Id {
  constructor(t = null, e = []) {
    this.group = t, this.items = e;
  }
  registerItem(t, e) {
    e ? t.group = e : this.group && (t.group = this.group), this.items.push(t);
  }
  addRegistry(t) {
    for (const e of t)
      this.items.push(e);
  }
  getByItem(t) {
    if (t)
      return this.items.find((e) => e.type === t.type);
  }
  //
  // getByType(type: string): Item | undefined {
  //     return this.items.find(item => item.type === type);
  // }
  *[Symbol.iterator]() {
    for (const t of this.items)
      yield t;
  }
  map(t) {
    return this.items.map((e, n, r) => t(e, n, r));
  }
}
let D5 = class {
  constructor(t = [], e = []) {
    this.external_data = t, this.forms = e;
  }
};
const L5 = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
let br = (i = 21) => {
  let t = "", e = crypto.getRandomValues(new Uint8Array(i));
  for (; i--; )
    t += L5[e[i] & 63];
  return t;
};
const Ko = {
  id: br(),
  type: "content-html",
  label: "",
  value: "",
  placeholder: ""
}, N5 = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...Ko, ...i });
  Ee(() => {
    n({ ...Ko, ...i });
  }, [i]);
  const r = Math.random().toString(36).substring(2, 15);
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { htmlFor: r, children: e.label }),
    /* @__PURE__ */ _.jsx("div", { dangerouslySetInnerHTML: { __html: e.value } })
  ] }) }) });
}, B5 = (i) => ({});
var fb;
function $f() {
  return $f = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, $f.apply(null, arguments);
}
var F5 = function(t) {
  return /* @__PURE__ */ w.createElement("svg", $f({
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 640 512"
  }, t), fb || (fb = /* @__PURE__ */ w.createElement("path", {
    d: "M392.8 1.2c-17-4.9-34.7 5-39.6 22l-128 448c-4.9 17 5 34.7 22 39.6s34.7-5 39.6-22l128-448c4.9-17-5-34.7-22-39.6m80.6 120.1c-12.5 12.5-12.5 32.8 0 45.3l89.3 89.4-89.4 89.4c-12.5 12.5-12.5 32.8 0 45.3s32.8 12.5 45.3 0l112-112c12.5-12.5 12.5-32.8 0-45.3l-112-112c-12.5-12.5-32.8-12.5-45.3 0zm-306.7 0c-12.5-12.5-32.8-12.5-45.3 0l-112 112c-12.5 12.5-12.5 32.8 0 45.3l112 112c12.5 12.5 32.8 12.5 45.3 0s12.5-32.8 0-45.3L77.3 256l89.4-89.4c12.5-12.5 12.5-32.8 0-45.3"
  })));
}, zf = { exports: {} }, Ic = { exports: {} }, lt = {};
/** @license React v16.13.1
 * react-is.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var mb;
function $5() {
  if (mb) return lt;
  mb = 1;
  var i = typeof Symbol == "function" && Symbol.for, t = i ? Symbol.for("react.element") : 60103, e = i ? Symbol.for("react.portal") : 60106, n = i ? Symbol.for("react.fragment") : 60107, r = i ? Symbol.for("react.strict_mode") : 60108, s = i ? Symbol.for("react.profiler") : 60114, o = i ? Symbol.for("react.provider") : 60109, a = i ? Symbol.for("react.context") : 60110, l = i ? Symbol.for("react.async_mode") : 60111, c = i ? Symbol.for("react.concurrent_mode") : 60111, u = i ? Symbol.for("react.forward_ref") : 60112, d = i ? Symbol.for("react.suspense") : 60113, f = i ? Symbol.for("react.suspense_list") : 60120, m = i ? Symbol.for("react.memo") : 60115, g = i ? Symbol.for("react.lazy") : 60116, p = i ? Symbol.for("react.block") : 60121, b = i ? Symbol.for("react.fundamental") : 60117, v = i ? Symbol.for("react.responder") : 60118, y = i ? Symbol.for("react.scope") : 60119;
  function x(k) {
    if (typeof k == "object" && k !== null) {
      var I = k.$$typeof;
      switch (I) {
        case t:
          switch (k = k.type, k) {
            case l:
            case c:
            case n:
            case s:
            case r:
            case d:
              return k;
            default:
              switch (k = k && k.$$typeof, k) {
                case a:
                case u:
                case g:
                case m:
                case o:
                  return k;
                default:
                  return I;
              }
          }
        case e:
          return I;
      }
    }
  }
  function S(k) {
    return x(k) === c;
  }
  return lt.AsyncMode = l, lt.ConcurrentMode = c, lt.ContextConsumer = a, lt.ContextProvider = o, lt.Element = t, lt.ForwardRef = u, lt.Fragment = n, lt.Lazy = g, lt.Memo = m, lt.Portal = e, lt.Profiler = s, lt.StrictMode = r, lt.Suspense = d, lt.isAsyncMode = function(k) {
    return S(k) || x(k) === l;
  }, lt.isConcurrentMode = S, lt.isContextConsumer = function(k) {
    return x(k) === a;
  }, lt.isContextProvider = function(k) {
    return x(k) === o;
  }, lt.isElement = function(k) {
    return typeof k == "object" && k !== null && k.$$typeof === t;
  }, lt.isForwardRef = function(k) {
    return x(k) === u;
  }, lt.isFragment = function(k) {
    return x(k) === n;
  }, lt.isLazy = function(k) {
    return x(k) === g;
  }, lt.isMemo = function(k) {
    return x(k) === m;
  }, lt.isPortal = function(k) {
    return x(k) === e;
  }, lt.isProfiler = function(k) {
    return x(k) === s;
  }, lt.isStrictMode = function(k) {
    return x(k) === r;
  }, lt.isSuspense = function(k) {
    return x(k) === d;
  }, lt.isValidElementType = function(k) {
    return typeof k == "string" || typeof k == "function" || k === n || k === c || k === s || k === r || k === d || k === f || typeof k == "object" && k !== null && (k.$$typeof === g || k.$$typeof === m || k.$$typeof === o || k.$$typeof === a || k.$$typeof === u || k.$$typeof === b || k.$$typeof === v || k.$$typeof === y || k.$$typeof === p);
  }, lt.typeOf = x, lt;
}
var ct = {};
/** @license React v16.13.1
 * react-is.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var gb;
function z5() {
  return gb || (gb = 1, process.env.NODE_ENV !== "production" && function() {
    var i = typeof Symbol == "function" && Symbol.for, t = i ? Symbol.for("react.element") : 60103, e = i ? Symbol.for("react.portal") : 60106, n = i ? Symbol.for("react.fragment") : 60107, r = i ? Symbol.for("react.strict_mode") : 60108, s = i ? Symbol.for("react.profiler") : 60114, o = i ? Symbol.for("react.provider") : 60109, a = i ? Symbol.for("react.context") : 60110, l = i ? Symbol.for("react.async_mode") : 60111, c = i ? Symbol.for("react.concurrent_mode") : 60111, u = i ? Symbol.for("react.forward_ref") : 60112, d = i ? Symbol.for("react.suspense") : 60113, f = i ? Symbol.for("react.suspense_list") : 60120, m = i ? Symbol.for("react.memo") : 60115, g = i ? Symbol.for("react.lazy") : 60116, p = i ? Symbol.for("react.block") : 60121, b = i ? Symbol.for("react.fundamental") : 60117, v = i ? Symbol.for("react.responder") : 60118, y = i ? Symbol.for("react.scope") : 60119;
    function x(W) {
      return typeof W == "string" || typeof W == "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
      W === n || W === c || W === s || W === r || W === d || W === f || typeof W == "object" && W !== null && (W.$$typeof === g || W.$$typeof === m || W.$$typeof === o || W.$$typeof === a || W.$$typeof === u || W.$$typeof === b || W.$$typeof === v || W.$$typeof === y || W.$$typeof === p);
    }
    function S(W) {
      if (typeof W == "object" && W !== null) {
        var We = W.$$typeof;
        switch (We) {
          case t:
            var Ye = W.type;
            switch (Ye) {
              case l:
              case c:
              case n:
              case s:
              case r:
              case d:
                return Ye;
              default:
                var Ze = Ye && Ye.$$typeof;
                switch (Ze) {
                  case a:
                  case u:
                  case g:
                  case m:
                  case o:
                    return Ze;
                  default:
                    return We;
                }
            }
          case e:
            return We;
        }
      }
    }
    var k = l, I = c, A = a, L = o, R = t, O = u, N = n, le = g, te = m, me = e, ce = s, Ae = r, Fe = d, be = !1;
    function re(W) {
      return be || (be = !0, console.warn("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.")), B(W) || S(W) === l;
    }
    function B(W) {
      return S(W) === c;
    }
    function X(W) {
      return S(W) === a;
    }
    function he(W) {
      return S(W) === o;
    }
    function Q(W) {
      return typeof W == "object" && W !== null && W.$$typeof === t;
    }
    function Y(W) {
      return S(W) === u;
    }
    function we(W) {
      return S(W) === n;
    }
    function ye(W) {
      return S(W) === g;
    }
    function Ce(W) {
      return S(W) === m;
    }
    function J(W) {
      return S(W) === e;
    }
    function F(W) {
      return S(W) === s;
    }
    function H(W) {
      return S(W) === r;
    }
    function pe(W) {
      return S(W) === d;
    }
    ct.AsyncMode = k, ct.ConcurrentMode = I, ct.ContextConsumer = A, ct.ContextProvider = L, ct.Element = R, ct.ForwardRef = O, ct.Fragment = N, ct.Lazy = le, ct.Memo = te, ct.Portal = me, ct.Profiler = ce, ct.StrictMode = Ae, ct.Suspense = Fe, ct.isAsyncMode = re, ct.isConcurrentMode = B, ct.isContextConsumer = X, ct.isContextProvider = he, ct.isElement = Q, ct.isForwardRef = Y, ct.isFragment = we, ct.isLazy = ye, ct.isMemo = Ce, ct.isPortal = J, ct.isProfiler = F, ct.isStrictMode = H, ct.isSuspense = pe, ct.isValidElementType = x, ct.typeOf = S;
  }()), ct;
}
var pb;
function e_() {
  return pb || (pb = 1, process.env.NODE_ENV === "production" ? Ic.exports = $5() : Ic.exports = z5()), Ic.exports;
}
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/
var Eh, bb;
function H5() {
  if (bb) return Eh;
  bb = 1;
  var i = Object.getOwnPropertySymbols, t = Object.prototype.hasOwnProperty, e = Object.prototype.propertyIsEnumerable;
  function n(s) {
    if (s == null)
      throw new TypeError("Object.assign cannot be called with null or undefined");
    return Object(s);
  }
  function r() {
    try {
      if (!Object.assign)
        return !1;
      var s = new String("abc");
      if (s[5] = "de", Object.getOwnPropertyNames(s)[0] === "5")
        return !1;
      for (var o = {}, a = 0; a < 10; a++)
        o["_" + String.fromCharCode(a)] = a;
      var l = Object.getOwnPropertyNames(o).map(function(u) {
        return o[u];
      });
      if (l.join("") !== "0123456789")
        return !1;
      var c = {};
      return "abcdefghijklmnopqrst".split("").forEach(function(u) {
        c[u] = u;
      }), Object.keys(Object.assign({}, c)).join("") === "abcdefghijklmnopqrst";
    } catch {
      return !1;
    }
  }
  return Eh = r() ? Object.assign : function(s, o) {
    for (var a, l = n(s), c, u = 1; u < arguments.length; u++) {
      a = Object(arguments[u]);
      for (var d in a)
        t.call(a, d) && (l[d] = a[d]);
      if (i) {
        c = i(a);
        for (var f = 0; f < c.length; f++)
          e.call(a, c[f]) && (l[c[f]] = a[c[f]]);
      }
    }
    return l;
  }, Eh;
}
var Th, vb;
function Pg() {
  if (vb) return Th;
  vb = 1;
  var i = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
  return Th = i, Th;
}
var Sh, wb;
function t_() {
  return wb || (wb = 1, Sh = Function.call.bind(Object.prototype.hasOwnProperty)), Sh;
}
var Ah, yb;
function j5() {
  if (yb) return Ah;
  yb = 1;
  var i = function() {
  };
  if (process.env.NODE_ENV !== "production") {
    var t = Pg(), e = {}, n = t_();
    i = function(s) {
      var o = "Warning: " + s;
      typeof console < "u" && console.error(o);
      try {
        throw new Error(o);
      } catch {
      }
    };
  }
  function r(s, o, a, l, c) {
    if (process.env.NODE_ENV !== "production") {
      for (var u in s)
        if (n(s, u)) {
          var d;
          try {
            if (typeof s[u] != "function") {
              var f = Error(
                (l || "React class") + ": " + a + " type `" + u + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof s[u] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`."
              );
              throw f.name = "Invariant Violation", f;
            }
            d = s[u](o, u, l, a, null, t);
          } catch (g) {
            d = g;
          }
          if (d && !(d instanceof Error) && i(
            (l || "React class") + ": type specification of " + a + " `" + u + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof d + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument)."
          ), d instanceof Error && !(d.message in e)) {
            e[d.message] = !0;
            var m = c ? c() : "";
            i(
              "Failed " + a + " type: " + d.message + (m ?? "")
            );
          }
        }
    }
  }
  return r.resetWarningCache = function() {
    process.env.NODE_ENV !== "production" && (e = {});
  }, Ah = r, Ah;
}
var Ph, _b;
function W5() {
  if (_b) return Ph;
  _b = 1;
  var i = e_(), t = H5(), e = Pg(), n = t_(), r = j5(), s = function() {
  };
  process.env.NODE_ENV !== "production" && (s = function(a) {
    var l = "Warning: " + a;
    typeof console < "u" && console.error(l);
    try {
      throw new Error(l);
    } catch {
    }
  });
  function o() {
    return null;
  }
  return Ph = function(a, l) {
    var c = typeof Symbol == "function" && Symbol.iterator, u = "@@iterator";
    function d(B) {
      var X = B && (c && B[c] || B[u]);
      if (typeof X == "function")
        return X;
    }
    var f = "<<anonymous>>", m = {
      array: v("array"),
      bigint: v("bigint"),
      bool: v("boolean"),
      func: v("function"),
      number: v("number"),
      object: v("object"),
      string: v("string"),
      symbol: v("symbol"),
      any: y(),
      arrayOf: x,
      element: S(),
      elementType: k(),
      instanceOf: I,
      node: O(),
      objectOf: L,
      oneOf: A,
      oneOfType: R,
      shape: le,
      exact: te
    };
    function g(B, X) {
      return B === X ? B !== 0 || 1 / B === 1 / X : B !== B && X !== X;
    }
    function p(B, X) {
      this.message = B, this.data = X && typeof X == "object" ? X : {}, this.stack = "";
    }
    p.prototype = Error.prototype;
    function b(B) {
      if (process.env.NODE_ENV !== "production")
        var X = {}, he = 0;
      function Q(we, ye, Ce, J, F, H, pe) {
        if (J = J || f, H = H || Ce, pe !== e) {
          if (l) {
            var W = new Error(
              "Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types"
            );
            throw W.name = "Invariant Violation", W;
          } else if (process.env.NODE_ENV !== "production" && typeof console < "u") {
            var We = J + ":" + Ce;
            !X[We] && // Avoid spamming the console because they are often not actionable except for lib authors
            he < 3 && (s(
              "You are manually calling a React.PropTypes validation function for the `" + H + "` prop on `" + J + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details."
            ), X[We] = !0, he++);
          }
        }
        return ye[Ce] == null ? we ? ye[Ce] === null ? new p("The " + F + " `" + H + "` is marked as required " + ("in `" + J + "`, but its value is `null`.")) : new p("The " + F + " `" + H + "` is marked as required in " + ("`" + J + "`, but its value is `undefined`.")) : null : B(ye, Ce, J, F, H);
      }
      var Y = Q.bind(null, !1);
      return Y.isRequired = Q.bind(null, !0), Y;
    }
    function v(B) {
      function X(he, Q, Y, we, ye, Ce) {
        var J = he[Q], F = Ae(J);
        if (F !== B) {
          var H = Fe(J);
          return new p(
            "Invalid " + we + " `" + ye + "` of type " + ("`" + H + "` supplied to `" + Y + "`, expected ") + ("`" + B + "`."),
            { expectedType: B }
          );
        }
        return null;
      }
      return b(X);
    }
    function y() {
      return b(o);
    }
    function x(B) {
      function X(he, Q, Y, we, ye) {
        if (typeof B != "function")
          return new p("Property `" + ye + "` of component `" + Y + "` has invalid PropType notation inside arrayOf.");
        var Ce = he[Q];
        if (!Array.isArray(Ce)) {
          var J = Ae(Ce);
          return new p("Invalid " + we + " `" + ye + "` of type " + ("`" + J + "` supplied to `" + Y + "`, expected an array."));
        }
        for (var F = 0; F < Ce.length; F++) {
          var H = B(Ce, F, Y, we, ye + "[" + F + "]", e);
          if (H instanceof Error)
            return H;
        }
        return null;
      }
      return b(X);
    }
    function S() {
      function B(X, he, Q, Y, we) {
        var ye = X[he];
        if (!a(ye)) {
          var Ce = Ae(ye);
          return new p("Invalid " + Y + " `" + we + "` of type " + ("`" + Ce + "` supplied to `" + Q + "`, expected a single ReactElement."));
        }
        return null;
      }
      return b(B);
    }
    function k() {
      function B(X, he, Q, Y, we) {
        var ye = X[he];
        if (!i.isValidElementType(ye)) {
          var Ce = Ae(ye);
          return new p("Invalid " + Y + " `" + we + "` of type " + ("`" + Ce + "` supplied to `" + Q + "`, expected a single ReactElement type."));
        }
        return null;
      }
      return b(B);
    }
    function I(B) {
      function X(he, Q, Y, we, ye) {
        if (!(he[Q] instanceof B)) {
          var Ce = B.name || f, J = re(he[Q]);
          return new p("Invalid " + we + " `" + ye + "` of type " + ("`" + J + "` supplied to `" + Y + "`, expected ") + ("instance of `" + Ce + "`."));
        }
        return null;
      }
      return b(X);
    }
    function A(B) {
      if (!Array.isArray(B))
        return process.env.NODE_ENV !== "production" && (arguments.length > 1 ? s(
          "Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z])."
        ) : s("Invalid argument supplied to oneOf, expected an array.")), o;
      function X(he, Q, Y, we, ye) {
        for (var Ce = he[Q], J = 0; J < B.length; J++)
          if (g(Ce, B[J]))
            return null;
        var F = JSON.stringify(B, function(pe, W) {
          var We = Fe(W);
          return We === "symbol" ? String(W) : W;
        });
        return new p("Invalid " + we + " `" + ye + "` of value `" + String(Ce) + "` " + ("supplied to `" + Y + "`, expected one of " + F + "."));
      }
      return b(X);
    }
    function L(B) {
      function X(he, Q, Y, we, ye) {
        if (typeof B != "function")
          return new p("Property `" + ye + "` of component `" + Y + "` has invalid PropType notation inside objectOf.");
        var Ce = he[Q], J = Ae(Ce);
        if (J !== "object")
          return new p("Invalid " + we + " `" + ye + "` of type " + ("`" + J + "` supplied to `" + Y + "`, expected an object."));
        for (var F in Ce)
          if (n(Ce, F)) {
            var H = B(Ce, F, Y, we, ye + "." + F, e);
            if (H instanceof Error)
              return H;
          }
        return null;
      }
      return b(X);
    }
    function R(B) {
      if (!Array.isArray(B))
        return process.env.NODE_ENV !== "production" && s("Invalid argument supplied to oneOfType, expected an instance of array."), o;
      for (var X = 0; X < B.length; X++) {
        var he = B[X];
        if (typeof he != "function")
          return s(
            "Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + be(he) + " at index " + X + "."
          ), o;
      }
      function Q(Y, we, ye, Ce, J) {
        for (var F = [], H = 0; H < B.length; H++) {
          var pe = B[H], W = pe(Y, we, ye, Ce, J, e);
          if (W == null)
            return null;
          W.data && n(W.data, "expectedType") && F.push(W.data.expectedType);
        }
        var We = F.length > 0 ? ", expected one of type [" + F.join(", ") + "]" : "";
        return new p("Invalid " + Ce + " `" + J + "` supplied to " + ("`" + ye + "`" + We + "."));
      }
      return b(Q);
    }
    function O() {
      function B(X, he, Q, Y, we) {
        return me(X[he]) ? null : new p("Invalid " + Y + " `" + we + "` supplied to " + ("`" + Q + "`, expected a ReactNode."));
      }
      return b(B);
    }
    function N(B, X, he, Q, Y) {
      return new p(
        (B || "React class") + ": " + X + " type `" + he + "." + Q + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + Y + "`."
      );
    }
    function le(B) {
      function X(he, Q, Y, we, ye) {
        var Ce = he[Q], J = Ae(Ce);
        if (J !== "object")
          return new p("Invalid " + we + " `" + ye + "` of type `" + J + "` " + ("supplied to `" + Y + "`, expected `object`."));
        for (var F in B) {
          var H = B[F];
          if (typeof H != "function")
            return N(Y, we, ye, F, Fe(H));
          var pe = H(Ce, F, Y, we, ye + "." + F, e);
          if (pe)
            return pe;
        }
        return null;
      }
      return b(X);
    }
    function te(B) {
      function X(he, Q, Y, we, ye) {
        var Ce = he[Q], J = Ae(Ce);
        if (J !== "object")
          return new p("Invalid " + we + " `" + ye + "` of type `" + J + "` " + ("supplied to `" + Y + "`, expected `object`."));
        var F = t({}, he[Q], B);
        for (var H in F) {
          var pe = B[H];
          if (n(B, H) && typeof pe != "function")
            return N(Y, we, ye, H, Fe(pe));
          if (!pe)
            return new p(
              "Invalid " + we + " `" + ye + "` key `" + H + "` supplied to `" + Y + "`.\nBad object: " + JSON.stringify(he[Q], null, "  ") + `
Valid keys: ` + JSON.stringify(Object.keys(B), null, "  ")
            );
          var W = pe(Ce, H, Y, we, ye + "." + H, e);
          if (W)
            return W;
        }
        return null;
      }
      return b(X);
    }
    function me(B) {
      switch (typeof B) {
        case "number":
        case "string":
        case "undefined":
          return !0;
        case "boolean":
          return !B;
        case "object":
          if (Array.isArray(B))
            return B.every(me);
          if (B === null || a(B))
            return !0;
          var X = d(B);
          if (X) {
            var he = X.call(B), Q;
            if (X !== B.entries) {
              for (; !(Q = he.next()).done; )
                if (!me(Q.value))
                  return !1;
            } else
              for (; !(Q = he.next()).done; ) {
                var Y = Q.value;
                if (Y && !me(Y[1]))
                  return !1;
              }
          } else
            return !1;
          return !0;
        default:
          return !1;
      }
    }
    function ce(B, X) {
      return B === "symbol" ? !0 : X ? X["@@toStringTag"] === "Symbol" || typeof Symbol == "function" && X instanceof Symbol : !1;
    }
    function Ae(B) {
      var X = typeof B;
      return Array.isArray(B) ? "array" : B instanceof RegExp ? "object" : ce(X, B) ? "symbol" : X;
    }
    function Fe(B) {
      if (typeof B > "u" || B === null)
        return "" + B;
      var X = Ae(B);
      if (X === "object") {
        if (B instanceof Date)
          return "date";
        if (B instanceof RegExp)
          return "regexp";
      }
      return X;
    }
    function be(B) {
      var X = Fe(B);
      switch (X) {
        case "array":
        case "object":
          return "an " + X;
        case "boolean":
        case "date":
        case "regexp":
          return "a " + X;
        default:
          return X;
      }
    }
    function re(B) {
      return !B.constructor || !B.constructor.name ? f : B.constructor.name;
    }
    return m.checkPropTypes = r, m.resetWarningCache = r.resetWarningCache, m.PropTypes = m, m;
  }, Ph;
}
var Ih, Cb;
function U5() {
  if (Cb) return Ih;
  Cb = 1;
  var i = Pg();
  function t() {
  }
  function e() {
  }
  return e.resetWarningCache = t, Ih = function() {
    function n(o, a, l, c, u, d) {
      if (d !== i) {
        var f = new Error(
          "Calling PropTypes validators directly is not supported by the `prop-types` package. Use PropTypes.checkPropTypes() to call them. Read more at http://fb.me/use-check-prop-types"
        );
        throw f.name = "Invariant Violation", f;
      }
    }
    n.isRequired = n;
    function r() {
      return n;
    }
    var s = {
      array: n,
      bigint: n,
      bool: n,
      func: n,
      number: n,
      object: n,
      string: n,
      symbol: n,
      any: n,
      arrayOf: r,
      element: n,
      elementType: n,
      instanceOf: r,
      node: n,
      objectOf: r,
      oneOf: r,
      oneOfType: r,
      shape: r,
      exact: r,
      checkPropTypes: e,
      resetWarningCache: t
    };
    return s.PropTypes = s, s;
  }, Ih;
}
if (process.env.NODE_ENV !== "production") {
  var q5 = e_(), G5 = !0;
  zf.exports = W5()(q5.isElement, G5);
} else
  zf.exports = U5()();
var Rs = zf.exports;
const Ie = /* @__PURE__ */ Ag(Rs);
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md.
 */
function K5() {
  const i = {
    resolve: null,
    promise: null
  };
  return i.promise = new Promise((t) => {
    i.resolve = t;
  }), i;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md.
 */
function Z5(i) {
  let t = null;
  return (...e) => (t || (t = {
    current: i(...e)
  }), t.current);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md.
 */
const Ln = new Array(256).fill("").map((i, t) => ("0" + t.toString(16)).slice(-2));
function Y5() {
  const [i, t, e, n] = crypto.getRandomValues(new Uint32Array(4));
  return "e" + Ln[i >> 0 & 255] + Ln[i >> 8 & 255] + Ln[i >> 16 & 255] + Ln[i >> 24 & 255] + Ln[t >> 0 & 255] + Ln[t >> 8 & 255] + Ln[t >> 16 & 255] + Ln[t >> 24 & 255] + Ln[e >> 0 & 255] + Ln[e >> 8 & 255] + Ln[e >> 16 & 255] + Ln[e >> 24 & 255] + Ln[n >> 0 & 255] + Ln[n >> 8 & 255] + Ln[n >> 16 & 255] + Ln[n >> 24 & 255];
}
var J5 = Object.defineProperty, X5 = (i, t, e) => t in i ? J5(i, t, { enumerable: !0, configurable: !0, writable: !0, value: e }) : i[t] = e, Gn = (i, t, e) => X5(i, typeof t != "symbol" ? t + "" : t, e);
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md.
 */
const n_ = class i_ {
  constructor(t, e) {
    Gn(this, "_lifecycle"), Gn(this, "_element"), Gn(this, "_releaseLock", null), Gn(this, "_value", null), Gn(this, "_afterMountCallbacks", []), Gn(this, "_state", {
      destroyedBeforeInitialization: !1,
      mountingInProgress: null
    }), Gn(this, "release", Z5(() => {
      const { _releaseLock: n, _state: r, _element: s, _lifecycle: o } = this;
      r.mountingInProgress ? r.mountingInProgress.then(() => o.unmount({
        element: s,
        // Mount result might be overridden by watchdog during restart so use instance variable.
        mountResult: this.value
      })).catch((a) => {
        console.error("Semaphore unmounting error:", a);
      }).then(n.resolve).then(() => {
        this._value = null;
      }) : (r.destroyedBeforeInitialization = !0, n.resolve());
    })), this._element = t, this._lifecycle = e, this._lock();
  }
  /**
   * Getter for {@link #_value}.
   */
  get value() {
    return this._value;
  }
  /**
   * Occasionally, the Watchdog restarts the editor instance, resulting in a new instance being assigned to the semaphore.
   * In terms of race conditions, it's generally safer to simply override the semaphore value rather than recreating it
   * with a different one.
   */
  unsafeSetValue(t) {
    this._value = t, this._afterMountCallbacks.forEach((e) => e(t)), this._afterMountCallbacks = [];
  }
  /**
   * This registers a callback that will be triggered after the editor has been successfully mounted.
   *
   * 	* If the editor is already mounted, the callback will be executed immediately.
   *	* If the editor is in the process of mounting, the callback will be executed upon successful mounting.
  * 	* If the editor is never mounted, the passed callback will not be executed.
  * 	* If an exception is thrown within the callback, it will be re-thrown in the semaphore.
  */
  runAfterMount(t) {
    const { _value: e, _afterMountCallbacks: n } = this;
    e ? t(e) : n.push(t);
  }
  /**
   * This method is used to inform other components that the {@link #_element} will be used by the editor,
   * which is initialized by the {@link #_lifecycle} methods.
   *
   * 	* If an editor is already present on the provided element, the initialization of the current one
   * 	  will be postponed until the previous one is destroyed.
   *
   * 	* If the element is empty and does not have an editor attached to it, the currently locked editor will
   * 	  be mounted immediately.
   *
   * After the successful initialization of the editor and the assignment of the {@link #_value} member,
   * the `onReady` lifecycle method is called.
   *
   * *Important note:*
   *
   * It’s really important to keep this method *sync*. If we make this method *async*, it won’t work well because
   * it will cause problems when we’re trying to set up the {@link LifeCycleEditorElementSemaphore#_semaphores} map entries.
   */
  _lock() {
    const { _semaphores: t } = i_, { _state: e, _element: n, _lifecycle: r } = this, s = t.get(n) || Promise.resolve(null), o = K5();
    this._releaseLock = o;
    const a = s.then(() => e.destroyedBeforeInitialization ? Promise.resolve(void 0) : (e.mountingInProgress = r.mount().then((l) => (l && this.unsafeSetValue(l), l)), e.mountingInProgress)).then(async (l) => {
      l && r.afterMount && await r.afterMount({
        element: n,
        mountResult: l
      });
    }).then(() => o.promise).catch((l) => {
      console.error("Semaphore mounting error:", l);
    }).then(() => {
      t.get(n) === a && t.delete(n);
    });
    t.set(n, a);
  }
};
Gn(n_, "_semaphores", /* @__PURE__ */ new Map());
let Q5 = n_;
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md.
 */
const eT = "$__CKEditorReactContextMetadata";
function tT(i, t) {
  return {
    ...t,
    [eT]: i
  };
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md.
 */
const nT = Se.createContext(null), iT = (i) => !!i && typeof i == "object" && "status" in i && ["initializing", "initialized", "error"].includes(i.status), r_ = (i) => (t) => iT(t) && t.status === i, kb = r_("initializing"), rT = (i) => r_("initialized")(i) && i.watchdog.state === "ready";
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md.
 */
const Rh = "Lock from React integration (@ckeditor/ckeditor5-react)";
class Ig extends Se.Component {
  constructor(t) {
    super(t), Gn(this, "domContainer", Se.createRef()), Gn(this, "editorSemaphore", null), this._checkVersion();
  }
  /**
   * Checks if the CKEditor version used in the application is compatible with the component.
   */
  _checkVersion() {
    const { CKEDITOR_VERSION: t } = window;
    if (!t)
      return console.warn('Cannot find the "CKEDITOR_VERSION" in the "window" scope.');
    const [e] = t.split(".").map(Number);
    e >= 42 || t.startsWith("0.0.0") || console.warn("The <CKEditor> component requires using CKEditor 5 in version 42+ or nightly build.");
  }
  get _semaphoreValue() {
    const { editorSemaphore: t } = this;
    return t ? t.value : null;
  }
  /**
   * An watchdog instance.
   */
  get watchdog() {
    const { _semaphoreValue: t } = this;
    return t ? t.watchdog : null;
  }
  /**
   * An editor instance.
   */
  get editor() {
    const { _semaphoreValue: t } = this;
    return t ? t.instance : null;
  }
  /**
   * The CKEditor component should not be updated by React itself.
   * However, if the component identifier changes, the whole structure should be created once again.
   */
  shouldComponentUpdate(t) {
    const { props: e, editorSemaphore: n } = this;
    return t.id !== e.id || t.disableWatchdog !== e.disableWatchdog ? !0 : (n && (n.runAfterMount(({ instance: r }) => {
      this._shouldUpdateEditorData(e, t, r) && r.data.set(t.data);
    }), "disabled" in t && n.runAfterMount(({ instance: r }) => {
      t.disabled ? r.enableReadOnlyMode(Rh) : r.disableReadOnlyMode(Rh);
    })), !1);
  }
  /**
   * Initialize the editor when the component is mounted.
   */
  componentDidMount() {
    kb(this.context) || this._initLifeCycleSemaphore();
  }
  /**
   * Re-render the entire component once again. The old editor will be destroyed and the new one will be created.
   */
  componentDidUpdate() {
    kb(this.context) || this._initLifeCycleSemaphore();
  }
  /**
   * Destroy the editor before unmounting the component.
   */
  componentWillUnmount() {
    this._unlockLifeCycleSemaphore();
  }
  /**
   * Async destroy attached editor and unlock element semaphore.
   */
  _unlockLifeCycleSemaphore() {
    this.editorSemaphore && (this.editorSemaphore.release(), this.editorSemaphore = null);
  }
  /**
   * Unlocks previous editor semaphore and creates new one..
   */
  _initLifeCycleSemaphore() {
    this._unlockLifeCycleSemaphore(), this.editorSemaphore = new Q5(this.domContainer.current, {
      mount: async () => this._initializeEditor(),
      afterMount: ({ mountResult: t }) => {
        const { onReady: e } = this.props;
        e && this.domContainer.current !== null && e(t.instance);
      },
      unmount: async ({ element: t, mountResult: e }) => {
        const { onAfterDestroy: n } = this.props;
        try {
          await this._destroyEditor(e), t.innerHTML = "";
        } finally {
          n && n(e.instance);
        }
      }
    });
  }
  /**
   * Render a <div> element which will be replaced by CKEditor.
   */
  render() {
    return /* @__PURE__ */ Se.createElement("div", { ref: this.domContainer });
  }
  /**
   * Initializes the editor by creating a proper watchdog and initializing it with the editor's configuration.
   */
  async _initializeEditor() {
    if (this.props.disableWatchdog)
      return {
        instance: await this._createEditor(this.domContainer.current, this._getConfig()),
        watchdog: null
      };
    const t = rT(this.context) ? new sT(this.context.watchdog) : new this.props.editor.EditorWatchdog(this.props.editor, this.props.watchdogConfig), e = {
      current: 0
    };
    return t.setCreator(async (n, r) => {
      var s;
      const { editorSemaphore: o } = this, { onAfterDestroy: a } = this.props;
      e.current > 0 && a && ((s = o == null ? void 0 : o.value) != null && s.instance) && a(o.value.instance);
      const l = await this._createEditor(n, r);
      return o && e.current > 0 && (o.unsafeSetValue({
        instance: l,
        watchdog: t
      }), setTimeout(() => {
        this.props.onReady && this.props.onReady(t.editor);
      })), e.current++, l;
    }), t.on("error", (n, { error: r, causesRestart: s }) => {
      (this.props.onError || console.error)(r, { phase: "runtime", willEditorRestart: s });
    }), await t.create(this.domContainer.current, this._getConfig()).catch((n) => {
      (this.props.onError || console.error)(n, { phase: "initialization", willEditorRestart: !1 });
    }), {
      watchdog: t,
      instance: t.editor
    };
  }
  /**
   * Creates an editor from the element and configuration.
   *
   * @param element The source element.
   * @param config CKEditor 5 editor configuration.
   */
  _createEditor(t, e) {
    const { contextItemMetadata: n } = this.props;
    return n && (e = tT(n, e)), this.props.editor.create(t, e).then((r) => {
      if ("disabled" in this.props) {
        /* istanbul ignore else -- @preserve */
        this.props.disabled && r.enableReadOnlyMode(Rh);
      }
      const s = r.model.document, o = r.editing.view.document;
      return s.on("change:data", (a) => {
        /* istanbul ignore else -- @preserve */
        this.props.onChange && this.props.onChange(a, r);
      }), o.on("focus", (a) => {
        /* istanbul ignore else -- @preserve */
        this.props.onFocus && this.props.onFocus(a, r);
      }), o.on("blur", (a) => {
        /* istanbul ignore else -- @preserve */
        this.props.onBlur && this.props.onBlur(a, r);
      }), r;
    });
  }
  /**
   * Destroys the editor by destroying the watchdog.
   */
  async _destroyEditor(t) {
    const { watchdog: e, instance: n } = t;
    return new Promise((r, s) => {
      /* istanbul ignore next -- @preserve */
      setTimeout(async () => {
        try {
          if (e)
            return await e.destroy(), r();
          if (n)
            return await n.destroy(), r();
          r();
        } catch (o) {
          console.error(o), s(o);
        }
      });
    });
  }
  /**
   * Returns true when the editor should be updated.
   *
   * @param prevProps Previous react's properties.
   * @param nextProps React's properties.
   * @param editor Current editor instance.
   */
  _shouldUpdateEditorData(t, e, n) {
    return !(t.data === e.data || n.data.get() === e.data);
  }
  /**
   * Returns the editor configuration.
   */
  _getConfig() {
    const t = this.props.config || {};
    return this.props.data && t.initialData && console.warn(
      "Editor data should be provided either using `config.initialData` or `content` property. The config value takes precedence over `content` property and will be used when both are specified."
    ), {
      ...t,
      initialData: t.initialData || this.props.data || ""
    };
  }
}
Gn(Ig, "contextType", nT);
Gn(Ig, "propTypes", {
  editor: Ie.func.isRequired,
  data: Ie.string,
  config: Ie.object,
  disableWatchdog: Ie.bool,
  watchdogConfig: Ie.object,
  onChange: Ie.func,
  onReady: Ie.func,
  onFocus: Ie.func,
  onBlur: Ie.func,
  onError: Ie.func,
  disabled: Ie.bool,
  id: Ie.any
});
class sT {
  /**
   * @param contextWatchdog The context watchdog instance that will be wrapped into editor watchdog API.
   */
  constructor(t) {
    Gn(this, "_contextWatchdog"), Gn(this, "_id"), Gn(this, "_creator"), this._contextWatchdog = t, this._id = Y5();
  }
  /**
   *  @param creator A watchdog's editor creator function.
   */
  setCreator(t) {
    this._creator = t;
  }
  /**
   * Adds an editor configuration to the context watchdog registry. Creates an instance of it.
   *
   * @param sourceElementOrData A source element or data for the new editor.
   * @param config CKEditor 5 editor config.
   */
  create(t, e) {
    return this._contextWatchdog.add({
      sourceElementOrData: t,
      config: e,
      creator: this._creator,
      id: this._id,
      type: "editor"
    });
  }
  /**
   * Creates a listener that is attached to context watchdog's item and run when the context watchdog fires.
   * Currently works only for the `error` event.
   */
  on(t, e) {
    this._contextWatchdog.on("itemError", (n, { itemId: r, error: s }) => {
      r === this._id && e(null, { error: s, causesRestart: void 0 });
    });
  }
  destroy() {
    return this._contextWatchdog.state === "ready" ? this._contextWatchdog.remove(this._id) : Promise.resolve();
  }
  /**
   * An editor instance.
   */
  get editor() {
    return this._contextWatchdog.getItem(this._id);
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md.
 */
function s_(...i) {
  return (t) => {
    i.forEach((e) => {
      typeof e == "function" ? e(t) : e != null && (e.current = t);
    });
  };
}
const oT = Qy(Tg(({ id: i, semaphore: t, rootName: e }, n) => {
  const r = at(null);
  return Ee(() => {
    let s, o;
    return t.runAfterMount(({ instance: a }) => {
      if (!r.current)
        return;
      o = a;
      const { ui: l, model: c } = o, u = c.document.getRoot(e);
      u && o.ui.getEditableElement(e) && o.detachEditable(u), s = l.view.createEditable(e, r.current), l.addEditable(s), a.editing.view.forceRender();
    }), () => {
      if (o && o.state !== "destroyed" && r.current) {
        const a = o.model.document.getRoot(e);
        /* istanbul ignore else -- @preserve */
        a && o.detachEditable(a);
      }
    };
  }, [t.revision]), /* @__PURE__ */ Se.createElement(
    "div",
    {
      key: t.revision,
      id: i,
      ref: s_(n, r)
    }
  );
}));
oT.displayName = "EditorEditable";
const aT = Tg(({ editor: i }, t) => {
  const e = at(null);
  return Ee(() => {
    const n = e.current;
    if (!i || !n)
      return;
    const r = i.ui.view.toolbar.element;
    return n.appendChild(r), () => {
      n.contains(r) && n.removeChild(r);
    };
  }, [i && i.id]), /* @__PURE__ */ Se.createElement("div", { ref: s_(e, t) });
});
aT.displayName = "EditorToolbarWrapper";
var o_ = typeof global == "object" && global && global.Object === Object && global, lT = typeof self == "object" && self && self.Object === Object && self, tr = o_ || lT || Function("return this")(), fr = tr.Symbol, a_ = Object.prototype, cT = a_.hasOwnProperty, uT = a_.toString, Da = fr ? fr.toStringTag : void 0;
function dT(i) {
  var t = cT.call(i, Da), e = i[Da];
  try {
    i[Da] = void 0;
    var n = !0;
  } catch {
  }
  var r = uT.call(i);
  return n && (t ? i[Da] = e : delete i[Da]), r;
}
var hT = Object.prototype, fT = hT.toString;
function mT(i) {
  return fT.call(i);
}
var gT = "[object Null]", pT = "[object Undefined]", xb = fr ? fr.toStringTag : void 0;
function bs(i) {
  return i == null ? i === void 0 ? pT : gT : xb && xb in Object(i) ? dT(i) : mT(i);
}
function Di(i) {
  return i != null && typeof i == "object";
}
var bT = "[object Symbol]";
function Rd(i) {
  return typeof i == "symbol" || Di(i) && bs(i) == bT;
}
function l_(i, t) {
  for (var e = -1, n = i == null ? 0 : i.length, r = Array(n); ++e < n; )
    r[e] = t(i[e], e, i);
  return r;
}
var ci = Array.isArray, vT = 1 / 0, Eb = fr ? fr.prototype : void 0, Tb = Eb ? Eb.toString : void 0;
function c_(i) {
  if (typeof i == "string")
    return i;
  if (ci(i))
    return l_(i, c_) + "";
  if (Rd(i))
    return Tb ? Tb.call(i) : "";
  var t = i + "";
  return t == "0" && 1 / i == -vT ? "-0" : t;
}
var wT = /\s/;
function yT(i) {
  for (var t = i.length; t-- && wT.test(i.charAt(t)); )
    ;
  return t;
}
var _T = /^\s+/;
function CT(i) {
  return i && i.slice(0, yT(i) + 1).replace(_T, "");
}
function vt(i) {
  var t = typeof i;
  return i != null && (t == "object" || t == "function");
}
var Sb = NaN, kT = /^[-+]0x[0-9a-f]+$/i, xT = /^0b[01]+$/i, ET = /^0o[0-7]+$/i, TT = parseInt;
function Ab(i) {
  if (typeof i == "number")
    return i;
  if (Rd(i))
    return Sb;
  if (vt(i)) {
    var t = typeof i.valueOf == "function" ? i.valueOf() : i;
    i = vt(t) ? t + "" : t;
  }
  if (typeof i != "string")
    return i === 0 ? i : +i;
  i = CT(i);
  var e = xT.test(i);
  return e || ET.test(i) ? TT(i.slice(2), e ? 2 : 8) : kT.test(i) ? Sb : +i;
}
function es(i) {
  return i;
}
var ST = "[object AsyncFunction]", AT = "[object Function]", PT = "[object GeneratorFunction]", IT = "[object Proxy]";
function Ws(i) {
  if (!vt(i))
    return !1;
  var t = bs(i);
  return t == AT || t == PT || t == ST || t == IT;
}
var Oh = tr["__core-js_shared__"], Pb = function() {
  var i = /[^.]+$/.exec(Oh && Oh.keys && Oh.keys.IE_PROTO || "");
  return i ? "Symbol(src)_1." + i : "";
}();
function RT(i) {
  return !!Pb && Pb in i;
}
var OT = Function.prototype, VT = OT.toString;
function Xs(i) {
  if (i != null) {
    try {
      return VT.call(i);
    } catch {
    }
    try {
      return i + "";
    } catch {
    }
  }
  return "";
}
var MT = /[\\^$.*+?()[\]{}|]/g, DT = /^\[object .+?Constructor\]$/, LT = Function.prototype, NT = Object.prototype, BT = LT.toString, FT = NT.hasOwnProperty, $T = RegExp(
  "^" + BT.call(FT).replace(MT, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
);
function zT(i) {
  if (!vt(i) || RT(i))
    return !1;
  var t = Ws(i) ? $T : DT;
  return t.test(Xs(i));
}
function HT(i, t) {
  return i == null ? void 0 : i[t];
}
function Qs(i, t) {
  var e = HT(i, t);
  return zT(e) ? e : void 0;
}
var Hf = Qs(tr, "WeakMap"), Ib = Object.create, jT = /* @__PURE__ */ function() {
  function i() {
  }
  return function(t) {
    if (!vt(t))
      return {};
    if (Ib)
      return Ib(t);
    i.prototype = t;
    var e = new i();
    return i.prototype = void 0, e;
  };
}();
function WT(i, t, e) {
  switch (e.length) {
    case 0:
      return i.call(t);
    case 1:
      return i.call(t, e[0]);
    case 2:
      return i.call(t, e[0], e[1]);
    case 3:
      return i.call(t, e[0], e[1], e[2]);
  }
  return i.apply(t, e);
}
function u_(i, t) {
  var e = -1, n = i.length;
  for (t || (t = Array(n)); ++e < n; )
    t[e] = i[e];
  return t;
}
var UT = 800, qT = 16, GT = Date.now;
function KT(i) {
  var t = 0, e = 0;
  return function() {
    var n = GT(), r = qT - (n - e);
    if (e = n, r > 0) {
      if (++t >= UT)
        return arguments[0];
    } else
      t = 0;
    return i.apply(void 0, arguments);
  };
}
function ZT(i) {
  return function() {
    return i;
  };
}
var Vu = function() {
  try {
    var i = Qs(Object, "defineProperty");
    return i({}, "", {}), i;
  } catch {
  }
}(), YT = Vu ? function(i, t) {
  return Vu(i, "toString", {
    configurable: !0,
    enumerable: !1,
    value: ZT(t),
    writable: !0
  });
} : es, JT = KT(YT);
function XT(i, t) {
  for (var e = -1, n = i == null ? 0 : i.length; ++e < n && t(i[e], e, i) !== !1; )
    ;
  return i;
}
var QT = 9007199254740991, eS = /^(?:0|[1-9]\d*)$/;
function Od(i, t) {
  var e = typeof i;
  return t = t ?? QT, !!t && (e == "number" || e != "symbol" && eS.test(i)) && i > -1 && i % 1 == 0 && i < t;
}
function Vd(i, t, e) {
  t == "__proto__" && Vu ? Vu(i, t, {
    configurable: !0,
    enumerable: !0,
    value: e,
    writable: !0
  }) : i[t] = e;
}
function hc(i, t) {
  return i === t || i !== i && t !== t;
}
var tS = Object.prototype, nS = tS.hasOwnProperty;
function Rg(i, t, e) {
  var n = i[t];
  (!(nS.call(i, t) && hc(n, e)) || e === void 0 && !(t in i)) && Vd(i, t, e);
}
function da(i, t, e, n) {
  var r = !e;
  e || (e = {});
  for (var s = -1, o = t.length; ++s < o; ) {
    var a = t[s], l = void 0;
    l === void 0 && (l = i[a]), r ? Vd(e, a, l) : Rg(e, a, l);
  }
  return e;
}
var Rb = Math.max;
function iS(i, t, e) {
  return t = Rb(t === void 0 ? i.length - 1 : t, 0), function() {
    for (var n = arguments, r = -1, s = Rb(n.length - t, 0), o = Array(s); ++r < s; )
      o[r] = n[t + r];
    r = -1;
    for (var a = Array(t + 1); ++r < t; )
      a[r] = n[r];
    return a[t] = e(o), WT(i, this, a);
  };
}
function rS(i, t) {
  return JT(iS(i, t, es), i + "");
}
var sS = 9007199254740991;
function Og(i) {
  return typeof i == "number" && i > -1 && i % 1 == 0 && i <= sS;
}
function ha(i) {
  return i != null && Og(i.length) && !Ws(i);
}
function oS(i, t, e) {
  if (!vt(e))
    return !1;
  var n = typeof t;
  return (n == "number" ? ha(e) && Od(t, e.length) : n == "string" && t in e) ? hc(e[t], i) : !1;
}
function Vg(i) {
  return rS(function(t, e) {
    var n = -1, r = e.length, s = r > 1 ? e[r - 1] : void 0, o = r > 2 ? e[2] : void 0;
    for (s = i.length > 3 && typeof s == "function" ? (r--, s) : void 0, o && oS(e[0], e[1], o) && (s = r < 3 ? void 0 : s, r = 1), t = Object(t); ++n < r; ) {
      var a = e[n];
      a && i(t, a, n, s);
    }
    return t;
  });
}
var aS = Object.prototype;
function Mg(i) {
  var t = i && i.constructor, e = typeof t == "function" && t.prototype || aS;
  return i === e;
}
function lS(i, t) {
  for (var e = -1, n = Array(i); ++e < i; )
    n[e] = t(e);
  return n;
}
var cS = "[object Arguments]";
function Ob(i) {
  return Di(i) && bs(i) == cS;
}
var d_ = Object.prototype, uS = d_.hasOwnProperty, dS = d_.propertyIsEnumerable, Mu = Ob(/* @__PURE__ */ function() {
  return arguments;
}()) ? Ob : function(i) {
  return Di(i) && uS.call(i, "callee") && !dS.call(i, "callee");
};
function hS() {
  return !1;
}
var h_ = typeof exports == "object" && exports && !exports.nodeType && exports, Vb = h_ && typeof module == "object" && module && !module.nodeType && module, fS = Vb && Vb.exports === h_, Mb = fS ? tr.Buffer : void 0, mS = Mb ? Mb.isBuffer : void 0, Tl = mS || hS, gS = "[object Arguments]", pS = "[object Array]", bS = "[object Boolean]", vS = "[object Date]", wS = "[object Error]", yS = "[object Function]", _S = "[object Map]", CS = "[object Number]", kS = "[object Object]", xS = "[object RegExp]", ES = "[object Set]", TS = "[object String]", SS = "[object WeakMap]", AS = "[object ArrayBuffer]", PS = "[object DataView]", IS = "[object Float32Array]", RS = "[object Float64Array]", OS = "[object Int8Array]", VS = "[object Int16Array]", MS = "[object Int32Array]", DS = "[object Uint8Array]", LS = "[object Uint8ClampedArray]", NS = "[object Uint16Array]", BS = "[object Uint32Array]", Tt = {};
Tt[IS] = Tt[RS] = Tt[OS] = Tt[VS] = Tt[MS] = Tt[DS] = Tt[LS] = Tt[NS] = Tt[BS] = !0;
Tt[gS] = Tt[pS] = Tt[AS] = Tt[bS] = Tt[PS] = Tt[vS] = Tt[wS] = Tt[yS] = Tt[_S] = Tt[CS] = Tt[kS] = Tt[xS] = Tt[ES] = Tt[TS] = Tt[SS] = !1;
function FS(i) {
  return Di(i) && Og(i.length) && !!Tt[bs(i)];
}
function Dg(i) {
  return function(t) {
    return i(t);
  };
}
var f_ = typeof exports == "object" && exports && !exports.nodeType && exports, rl = f_ && typeof module == "object" && module && !module.nodeType && module, $S = rl && rl.exports === f_, Vh = $S && o_.process, Zo = function() {
  try {
    var i = rl && rl.require && rl.require("util").types;
    return i || Vh && Vh.binding && Vh.binding("util");
  } catch {
  }
}(), Db = Zo && Zo.isTypedArray, Lg = Db ? Dg(Db) : FS, zS = Object.prototype, HS = zS.hasOwnProperty;
function m_(i, t) {
  var e = ci(i), n = !e && Mu(i), r = !e && !n && Tl(i), s = !e && !n && !r && Lg(i), o = e || n || r || s, a = o ? lS(i.length, String) : [], l = a.length;
  for (var c in i)
    (t || HS.call(i, c)) && !(o && // Safari 9 has enumerable `arguments.length` in strict mode.
    (c == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
    r && (c == "offset" || c == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
    s && (c == "buffer" || c == "byteLength" || c == "byteOffset") || // Skip index properties.
    Od(c, l))) && a.push(c);
  return a;
}
function g_(i, t) {
  return function(e) {
    return i(t(e));
  };
}
var jS = g_(Object.keys, Object), WS = Object.prototype, US = WS.hasOwnProperty;
function qS(i) {
  if (!Mg(i))
    return jS(i);
  var t = [];
  for (var e in Object(i))
    US.call(i, e) && e != "constructor" && t.push(e);
  return t;
}
function fc(i) {
  return ha(i) ? m_(i) : qS(i);
}
function GS(i) {
  var t = [];
  if (i != null)
    for (var e in Object(i))
      t.push(e);
  return t;
}
var KS = Object.prototype, ZS = KS.hasOwnProperty;
function YS(i) {
  if (!vt(i))
    return GS(i);
  var t = Mg(i), e = [];
  for (var n in i)
    n == "constructor" && (t || !ZS.call(i, n)) || e.push(n);
  return e;
}
function fa(i) {
  return ha(i) ? m_(i, !0) : YS(i);
}
var p_ = Vg(function(i, t) {
  da(t, fa(t), i);
}), JS = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, XS = /^\w*$/;
function Ng(i, t) {
  if (ci(i))
    return !1;
  var e = typeof i;
  return e == "number" || e == "symbol" || e == "boolean" || i == null || Rd(i) ? !0 : XS.test(i) || !JS.test(i) || t != null && i in Object(t);
}
var Sl = Qs(Object, "create");
function QS() {
  this.__data__ = Sl ? Sl(null) : {}, this.size = 0;
}
function eA(i) {
  var t = this.has(i) && delete this.__data__[i];
  return this.size -= t ? 1 : 0, t;
}
var tA = "__lodash_hash_undefined__", nA = Object.prototype, iA = nA.hasOwnProperty;
function rA(i) {
  var t = this.__data__;
  if (Sl) {
    var e = t[i];
    return e === tA ? void 0 : e;
  }
  return iA.call(t, i) ? t[i] : void 0;
}
var sA = Object.prototype, oA = sA.hasOwnProperty;
function aA(i) {
  var t = this.__data__;
  return Sl ? t[i] !== void 0 : oA.call(t, i);
}
var lA = "__lodash_hash_undefined__";
function cA(i, t) {
  var e = this.__data__;
  return this.size += this.has(i) ? 0 : 1, e[i] = Sl && t === void 0 ? lA : t, this;
}
function Us(i) {
  var t = -1, e = i == null ? 0 : i.length;
  for (this.clear(); ++t < e; ) {
    var n = i[t];
    this.set(n[0], n[1]);
  }
}
Us.prototype.clear = QS;
Us.prototype.delete = eA;
Us.prototype.get = rA;
Us.prototype.has = aA;
Us.prototype.set = cA;
function uA() {
  this.__data__ = [], this.size = 0;
}
function Md(i, t) {
  for (var e = i.length; e--; )
    if (hc(i[e][0], t))
      return e;
  return -1;
}
var dA = Array.prototype, hA = dA.splice;
function fA(i) {
  var t = this.__data__, e = Md(t, i);
  if (e < 0)
    return !1;
  var n = t.length - 1;
  return e == n ? t.pop() : hA.call(t, e, 1), --this.size, !0;
}
function mA(i) {
  var t = this.__data__, e = Md(t, i);
  return e < 0 ? void 0 : t[e][1];
}
function gA(i) {
  return Md(this.__data__, i) > -1;
}
function pA(i, t) {
  var e = this.__data__, n = Md(e, i);
  return n < 0 ? (++this.size, e.push([i, t])) : e[n][1] = t, this;
}
function jr(i) {
  var t = -1, e = i == null ? 0 : i.length;
  for (this.clear(); ++t < e; ) {
    var n = i[t];
    this.set(n[0], n[1]);
  }
}
jr.prototype.clear = uA;
jr.prototype.delete = fA;
jr.prototype.get = mA;
jr.prototype.has = gA;
jr.prototype.set = pA;
var Al = Qs(tr, "Map");
function bA() {
  this.size = 0, this.__data__ = {
    hash: new Us(),
    map: new (Al || jr)(),
    string: new Us()
  };
}
function vA(i) {
  var t = typeof i;
  return t == "string" || t == "number" || t == "symbol" || t == "boolean" ? i !== "__proto__" : i === null;
}
function Dd(i, t) {
  var e = i.__data__;
  return vA(t) ? e[typeof t == "string" ? "string" : "hash"] : e.map;
}
function wA(i) {
  var t = Dd(this, i).delete(i);
  return this.size -= t ? 1 : 0, t;
}
function yA(i) {
  return Dd(this, i).get(i);
}
function _A(i) {
  return Dd(this, i).has(i);
}
function CA(i, t) {
  var e = Dd(this, i), n = e.size;
  return e.set(i, t), this.size += e.size == n ? 0 : 1, this;
}
function Wr(i) {
  var t = -1, e = i == null ? 0 : i.length;
  for (this.clear(); ++t < e; ) {
    var n = i[t];
    this.set(n[0], n[1]);
  }
}
Wr.prototype.clear = bA;
Wr.prototype.delete = wA;
Wr.prototype.get = yA;
Wr.prototype.has = _A;
Wr.prototype.set = CA;
var kA = "Expected a function";
function Bg(i, t) {
  if (typeof i != "function" || t != null && typeof t != "function")
    throw new TypeError(kA);
  var e = function() {
    var n = arguments, r = t ? t.apply(this, n) : n[0], s = e.cache;
    if (s.has(r))
      return s.get(r);
    var o = i.apply(this, n);
    return e.cache = s.set(r, o) || s, o;
  };
  return e.cache = new (Bg.Cache || Wr)(), e;
}
Bg.Cache = Wr;
var xA = 500;
function EA(i) {
  var t = Bg(i, function(n) {
    return e.size === xA && e.clear(), n;
  }), e = t.cache;
  return t;
}
var TA = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g, SA = /\\(\\)?/g, AA = EA(function(i) {
  var t = [];
  return i.charCodeAt(0) === 46 && t.push(""), i.replace(TA, function(e, n, r, s) {
    t.push(r ? s.replace(SA, "$1") : n || e);
  }), t;
});
function mc(i) {
  return i == null ? "" : c_(i);
}
function Ld(i, t) {
  return ci(i) ? i : Ng(i, t) ? [i] : AA(mc(i));
}
var PA = 1 / 0;
function ma(i) {
  if (typeof i == "string" || Rd(i))
    return i;
  var t = i + "";
  return t == "0" && 1 / i == -PA ? "-0" : t;
}
function Fg(i, t) {
  t = Ld(t, i);
  for (var e = 0, n = t.length; i != null && e < n; )
    i = i[ma(t[e++])];
  return e && e == n ? i : void 0;
}
function Pl(i, t, e) {
  var n = i == null ? void 0 : Fg(i, t);
  return n === void 0 ? e : n;
}
function b_(i, t) {
  for (var e = -1, n = t.length, r = i.length; ++e < n; )
    i[r + e] = t[e];
  return i;
}
var $g = g_(Object.getPrototypeOf, Object), IA = "[object Object]", RA = Function.prototype, OA = Object.prototype, v_ = RA.toString, VA = OA.hasOwnProperty, MA = v_.call(Object);
function oi(i) {
  if (!Di(i) || bs(i) != IA)
    return !1;
  var t = $g(i);
  if (t === null)
    return !0;
  var e = VA.call(t, "constructor") && t.constructor;
  return typeof e == "function" && e instanceof e && v_.call(e) == MA;
}
function w_(i, t, e) {
  var n = -1, r = i.length;
  t < 0 && (t = -t > r ? 0 : r + t), e = e > r ? r : e, e < 0 && (e += r), r = t > e ? 0 : e - t >>> 0, t >>>= 0;
  for (var s = Array(r); ++n < r; )
    s[n] = i[n + t];
  return s;
}
function DA(i, t, e) {
  var n = i.length;
  return e = e === void 0 ? n : e, !t && e >= n ? i : w_(i, t, e);
}
var LA = "\\ud800-\\udfff", NA = "\\u0300-\\u036f", BA = "\\ufe20-\\ufe2f", FA = "\\u20d0-\\u20ff", $A = NA + BA + FA, zA = "\\ufe0e\\ufe0f", HA = "\\u200d", jA = RegExp("[" + HA + LA + $A + zA + "]");
function y_(i) {
  return jA.test(i);
}
function WA(i) {
  return i.split("");
}
var __ = "\\ud800-\\udfff", UA = "\\u0300-\\u036f", qA = "\\ufe20-\\ufe2f", GA = "\\u20d0-\\u20ff", KA = UA + qA + GA, ZA = "\\ufe0e\\ufe0f", YA = "[" + __ + "]", jf = "[" + KA + "]", Wf = "\\ud83c[\\udffb-\\udfff]", JA = "(?:" + jf + "|" + Wf + ")", C_ = "[^" + __ + "]", k_ = "(?:\\ud83c[\\udde6-\\uddff]){2}", x_ = "[\\ud800-\\udbff][\\udc00-\\udfff]", XA = "\\u200d", E_ = JA + "?", T_ = "[" + ZA + "]?", QA = "(?:" + XA + "(?:" + [C_, k_, x_].join("|") + ")" + T_ + E_ + ")*", eP = T_ + E_ + QA, tP = "(?:" + [C_ + jf + "?", jf, k_, x_, YA].join("|") + ")", nP = RegExp(Wf + "(?=" + Wf + ")|" + tP + eP, "g");
function iP(i) {
  return i.match(nP) || [];
}
function rP(i) {
  return y_(i) ? iP(i) : WA(i);
}
function sP(i) {
  return function(t) {
    t = mc(t);
    var e = y_(t) ? rP(t) : void 0, n = e ? e[0] : t.charAt(0), r = e ? DA(e, 1).join("") : t.slice(1);
    return n[i]() + r;
  };
}
var S_ = sP("toUpperCase");
function oP(i, t, e, n) {
  for (var r = -1, s = i == null ? 0 : i.length; ++r < s; )
    e = t(e, i[r], r, i);
  return e;
}
function aP(i) {
  return function(t) {
    return i == null ? void 0 : i[t];
  };
}
var lP = {
  // Latin-1 Supplement block.
  À: "A",
  Á: "A",
  Â: "A",
  Ã: "A",
  Ä: "A",
  Å: "A",
  à: "a",
  á: "a",
  â: "a",
  ã: "a",
  ä: "a",
  å: "a",
  Ç: "C",
  ç: "c",
  Ð: "D",
  ð: "d",
  È: "E",
  É: "E",
  Ê: "E",
  Ë: "E",
  è: "e",
  é: "e",
  ê: "e",
  ë: "e",
  Ì: "I",
  Í: "I",
  Î: "I",
  Ï: "I",
  ì: "i",
  í: "i",
  î: "i",
  ï: "i",
  Ñ: "N",
  ñ: "n",
  Ò: "O",
  Ó: "O",
  Ô: "O",
  Õ: "O",
  Ö: "O",
  Ø: "O",
  ò: "o",
  ó: "o",
  ô: "o",
  õ: "o",
  ö: "o",
  ø: "o",
  Ù: "U",
  Ú: "U",
  Û: "U",
  Ü: "U",
  ù: "u",
  ú: "u",
  û: "u",
  ü: "u",
  Ý: "Y",
  ý: "y",
  ÿ: "y",
  Æ: "Ae",
  æ: "ae",
  Þ: "Th",
  þ: "th",
  ß: "ss",
  // Latin Extended-A block.
  Ā: "A",
  Ă: "A",
  Ą: "A",
  ā: "a",
  ă: "a",
  ą: "a",
  Ć: "C",
  Ĉ: "C",
  Ċ: "C",
  Č: "C",
  ć: "c",
  ĉ: "c",
  ċ: "c",
  č: "c",
  Ď: "D",
  Đ: "D",
  ď: "d",
  đ: "d",
  Ē: "E",
  Ĕ: "E",
  Ė: "E",
  Ę: "E",
  Ě: "E",
  ē: "e",
  ĕ: "e",
  ė: "e",
  ę: "e",
  ě: "e",
  Ĝ: "G",
  Ğ: "G",
  Ġ: "G",
  Ģ: "G",
  ĝ: "g",
  ğ: "g",
  ġ: "g",
  ģ: "g",
  Ĥ: "H",
  Ħ: "H",
  ĥ: "h",
  ħ: "h",
  Ĩ: "I",
  Ī: "I",
  Ĭ: "I",
  Į: "I",
  İ: "I",
  ĩ: "i",
  ī: "i",
  ĭ: "i",
  į: "i",
  ı: "i",
  Ĵ: "J",
  ĵ: "j",
  Ķ: "K",
  ķ: "k",
  ĸ: "k",
  Ĺ: "L",
  Ļ: "L",
  Ľ: "L",
  Ŀ: "L",
  Ł: "L",
  ĺ: "l",
  ļ: "l",
  ľ: "l",
  ŀ: "l",
  ł: "l",
  Ń: "N",
  Ņ: "N",
  Ň: "N",
  Ŋ: "N",
  ń: "n",
  ņ: "n",
  ň: "n",
  ŋ: "n",
  Ō: "O",
  Ŏ: "O",
  Ő: "O",
  ō: "o",
  ŏ: "o",
  ő: "o",
  Ŕ: "R",
  Ŗ: "R",
  Ř: "R",
  ŕ: "r",
  ŗ: "r",
  ř: "r",
  Ś: "S",
  Ŝ: "S",
  Ş: "S",
  Š: "S",
  ś: "s",
  ŝ: "s",
  ş: "s",
  š: "s",
  Ţ: "T",
  Ť: "T",
  Ŧ: "T",
  ţ: "t",
  ť: "t",
  ŧ: "t",
  Ũ: "U",
  Ū: "U",
  Ŭ: "U",
  Ů: "U",
  Ű: "U",
  Ų: "U",
  ũ: "u",
  ū: "u",
  ŭ: "u",
  ů: "u",
  ű: "u",
  ų: "u",
  Ŵ: "W",
  ŵ: "w",
  Ŷ: "Y",
  ŷ: "y",
  Ÿ: "Y",
  Ź: "Z",
  Ż: "Z",
  Ž: "Z",
  ź: "z",
  ż: "z",
  ž: "z",
  Ĳ: "IJ",
  ĳ: "ij",
  Œ: "Oe",
  œ: "oe",
  ŉ: "'n",
  ſ: "s"
}, cP = aP(lP), uP = /[\xc0-\xd6\xd8-\xf6\xf8-\xff\u0100-\u017f]/g, dP = "\\u0300-\\u036f", hP = "\\ufe20-\\ufe2f", fP = "\\u20d0-\\u20ff", mP = dP + hP + fP, gP = "[" + mP + "]", pP = RegExp(gP, "g");
function bP(i) {
  return i = mc(i), i && i.replace(uP, cP).replace(pP, "");
}
var vP = /[^\x00-\x2f\x3a-\x40\x5b-\x60\x7b-\x7f]+/g;
function wP(i) {
  return i.match(vP) || [];
}
var yP = /[a-z][A-Z]|[A-Z]{2}[a-z]|[0-9][a-zA-Z]|[a-zA-Z][0-9]|[^a-zA-Z0-9 ]/;
function _P(i) {
  return yP.test(i);
}
var A_ = "\\ud800-\\udfff", CP = "\\u0300-\\u036f", kP = "\\ufe20-\\ufe2f", xP = "\\u20d0-\\u20ff", EP = CP + kP + xP, P_ = "\\u2700-\\u27bf", I_ = "a-z\\xdf-\\xf6\\xf8-\\xff", TP = "\\xac\\xb1\\xd7\\xf7", SP = "\\x00-\\x2f\\x3a-\\x40\\x5b-\\x60\\x7b-\\xbf", AP = "\\u2000-\\u206f", PP = " \\t\\x0b\\f\\xa0\\ufeff\\n\\r\\u2028\\u2029\\u1680\\u180e\\u2000\\u2001\\u2002\\u2003\\u2004\\u2005\\u2006\\u2007\\u2008\\u2009\\u200a\\u202f\\u205f\\u3000", R_ = "A-Z\\xc0-\\xd6\\xd8-\\xde", IP = "\\ufe0e\\ufe0f", O_ = TP + SP + AP + PP, V_ = "['’]", Lb = "[" + O_ + "]", RP = "[" + EP + "]", M_ = "\\d+", OP = "[" + P_ + "]", D_ = "[" + I_ + "]", L_ = "[^" + A_ + O_ + M_ + P_ + I_ + R_ + "]", VP = "\\ud83c[\\udffb-\\udfff]", MP = "(?:" + RP + "|" + VP + ")", DP = "[^" + A_ + "]", N_ = "(?:\\ud83c[\\udde6-\\uddff]){2}", B_ = "[\\ud800-\\udbff][\\udc00-\\udfff]", Ao = "[" + R_ + "]", LP = "\\u200d", Nb = "(?:" + D_ + "|" + L_ + ")", NP = "(?:" + Ao + "|" + L_ + ")", Bb = "(?:" + V_ + "(?:d|ll|m|re|s|t|ve))?", Fb = "(?:" + V_ + "(?:D|LL|M|RE|S|T|VE))?", F_ = MP + "?", $_ = "[" + IP + "]?", BP = "(?:" + LP + "(?:" + [DP, N_, B_].join("|") + ")" + $_ + F_ + ")*", FP = "\\d*(?:1st|2nd|3rd|(?![123])\\dth)(?=\\b|[A-Z_])", $P = "\\d*(?:1ST|2ND|3RD|(?![123])\\dTH)(?=\\b|[a-z_])", zP = $_ + F_ + BP, HP = "(?:" + [OP, N_, B_].join("|") + ")" + zP, jP = RegExp([
  Ao + "?" + D_ + "+" + Bb + "(?=" + [Lb, Ao, "$"].join("|") + ")",
  NP + "+" + Fb + "(?=" + [Lb, Ao + Nb, "$"].join("|") + ")",
  Ao + "?" + Nb + "+" + Bb,
  Ao + "+" + Fb,
  $P,
  FP,
  M_,
  HP
].join("|"), "g");
function WP(i) {
  return i.match(jP) || [];
}
function UP(i, t, e) {
  return i = mc(i), t = t, t === void 0 ? _P(i) ? WP(i) : wP(i) : i.match(t) || [];
}
var qP = "['’]", GP = RegExp(qP, "g");
function KP(i) {
  return function(t) {
    return oP(UP(bP(t).replace(GP, "")), i, "");
  };
}
function ZP() {
  this.__data__ = new jr(), this.size = 0;
}
function YP(i) {
  var t = this.__data__, e = t.delete(i);
  return this.size = t.size, e;
}
function JP(i) {
  return this.__data__.get(i);
}
function XP(i) {
  return this.__data__.has(i);
}
var QP = 200;
function eI(i, t) {
  var e = this.__data__;
  if (e instanceof jr) {
    var n = e.__data__;
    if (!Al || n.length < QP - 1)
      return n.push([i, t]), this.size = ++e.size, this;
    e = this.__data__ = new Wr(n);
  }
  return e.set(i, t), this.size = e.size, this;
}
function Zi(i) {
  var t = this.__data__ = new jr(i);
  this.size = t.size;
}
Zi.prototype.clear = ZP;
Zi.prototype.delete = YP;
Zi.prototype.get = JP;
Zi.prototype.has = XP;
Zi.prototype.set = eI;
function tI(i, t) {
  return i && da(t, fc(t), i);
}
function nI(i, t) {
  return i && da(t, fa(t), i);
}
var z_ = typeof exports == "object" && exports && !exports.nodeType && exports, $b = z_ && typeof module == "object" && module && !module.nodeType && module, iI = $b && $b.exports === z_, zb = iI ? tr.Buffer : void 0, Hb = zb ? zb.allocUnsafe : void 0;
function H_(i, t) {
  if (t)
    return i.slice();
  var e = i.length, n = Hb ? Hb(e) : new i.constructor(e);
  return i.copy(n), n;
}
function rI(i, t) {
  for (var e = -1, n = i == null ? 0 : i.length, r = 0, s = []; ++e < n; ) {
    var o = i[e];
    t(o, e, i) && (s[r++] = o);
  }
  return s;
}
function j_() {
  return [];
}
var sI = Object.prototype, oI = sI.propertyIsEnumerable, jb = Object.getOwnPropertySymbols, zg = jb ? function(i) {
  return i == null ? [] : (i = Object(i), rI(jb(i), function(t) {
    return oI.call(i, t);
  }));
} : j_;
function aI(i, t) {
  return da(i, zg(i), t);
}
var lI = Object.getOwnPropertySymbols, W_ = lI ? function(i) {
  for (var t = []; i; )
    b_(t, zg(i)), i = $g(i);
  return t;
} : j_;
function cI(i, t) {
  return da(i, W_(i), t);
}
function U_(i, t, e) {
  var n = t(i);
  return ci(i) ? n : b_(n, e(i));
}
function Uf(i) {
  return U_(i, fc, zg);
}
function uI(i) {
  return U_(i, fa, W_);
}
var qf = Qs(tr, "DataView"), Gf = Qs(tr, "Promise"), Kf = Qs(tr, "Set"), Wb = "[object Map]", dI = "[object Object]", Ub = "[object Promise]", qb = "[object Set]", Gb = "[object WeakMap]", Kb = "[object DataView]", hI = Xs(qf), fI = Xs(Al), mI = Xs(Gf), gI = Xs(Kf), pI = Xs(Hf), Wi = bs;
(qf && Wi(new qf(new ArrayBuffer(1))) != Kb || Al && Wi(new Al()) != Wb || Gf && Wi(Gf.resolve()) != Ub || Kf && Wi(new Kf()) != qb || Hf && Wi(new Hf()) != Gb) && (Wi = function(i) {
  var t = bs(i), e = t == dI ? i.constructor : void 0, n = e ? Xs(e) : "";
  if (n)
    switch (n) {
      case hI:
        return Kb;
      case fI:
        return Wb;
      case mI:
        return Ub;
      case gI:
        return qb;
      case pI:
        return Gb;
    }
  return t;
});
var bI = Object.prototype, vI = bI.hasOwnProperty;
function wI(i) {
  var t = i.length, e = new i.constructor(t);
  return t && typeof i[0] == "string" && vI.call(i, "index") && (e.index = i.index, e.input = i.input), e;
}
var Du = tr.Uint8Array;
function Hg(i) {
  var t = new i.constructor(i.byteLength);
  return new Du(t).set(new Du(i)), t;
}
function yI(i, t) {
  var e = t ? Hg(i.buffer) : i.buffer;
  return new i.constructor(e, i.byteOffset, i.byteLength);
}
var _I = /\w*$/;
function CI(i) {
  var t = new i.constructor(i.source, _I.exec(i));
  return t.lastIndex = i.lastIndex, t;
}
var Zb = fr ? fr.prototype : void 0, Yb = Zb ? Zb.valueOf : void 0;
function kI(i) {
  return Yb ? Object(Yb.call(i)) : {};
}
function q_(i, t) {
  var e = t ? Hg(i.buffer) : i.buffer;
  return new i.constructor(e, i.byteOffset, i.length);
}
var xI = "[object Boolean]", EI = "[object Date]", TI = "[object Map]", SI = "[object Number]", AI = "[object RegExp]", PI = "[object Set]", II = "[object String]", RI = "[object Symbol]", OI = "[object ArrayBuffer]", VI = "[object DataView]", MI = "[object Float32Array]", DI = "[object Float64Array]", LI = "[object Int8Array]", NI = "[object Int16Array]", BI = "[object Int32Array]", FI = "[object Uint8Array]", $I = "[object Uint8ClampedArray]", zI = "[object Uint16Array]", HI = "[object Uint32Array]";
function jI(i, t, e) {
  var n = i.constructor;
  switch (t) {
    case OI:
      return Hg(i);
    case xI:
    case EI:
      return new n(+i);
    case VI:
      return yI(i, e);
    case MI:
    case DI:
    case LI:
    case NI:
    case BI:
    case FI:
    case $I:
    case zI:
    case HI:
      return q_(i, e);
    case TI:
      return new n();
    case SI:
    case II:
      return new n(i);
    case AI:
      return CI(i);
    case PI:
      return new n();
    case RI:
      return kI(i);
  }
}
function G_(i) {
  return typeof i.constructor == "function" && !Mg(i) ? jT($g(i)) : {};
}
var WI = "[object Map]";
function UI(i) {
  return Di(i) && Wi(i) == WI;
}
var Jb = Zo && Zo.isMap, qI = Jb ? Dg(Jb) : UI, GI = "[object Set]";
function KI(i) {
  return Di(i) && Wi(i) == GI;
}
var Xb = Zo && Zo.isSet, ZI = Xb ? Dg(Xb) : KI, YI = 1, JI = 2, XI = 4, K_ = "[object Arguments]", QI = "[object Array]", e3 = "[object Boolean]", t3 = "[object Date]", n3 = "[object Error]", Z_ = "[object Function]", i3 = "[object GeneratorFunction]", r3 = "[object Map]", s3 = "[object Number]", Y_ = "[object Object]", o3 = "[object RegExp]", a3 = "[object Set]", l3 = "[object String]", c3 = "[object Symbol]", u3 = "[object WeakMap]", d3 = "[object ArrayBuffer]", h3 = "[object DataView]", f3 = "[object Float32Array]", m3 = "[object Float64Array]", g3 = "[object Int8Array]", p3 = "[object Int16Array]", b3 = "[object Int32Array]", v3 = "[object Uint8Array]", w3 = "[object Uint8ClampedArray]", y3 = "[object Uint16Array]", _3 = "[object Uint32Array]", kt = {};
kt[K_] = kt[QI] = kt[d3] = kt[h3] = kt[e3] = kt[t3] = kt[f3] = kt[m3] = kt[g3] = kt[p3] = kt[b3] = kt[r3] = kt[s3] = kt[Y_] = kt[o3] = kt[a3] = kt[l3] = kt[c3] = kt[v3] = kt[w3] = kt[y3] = kt[_3] = !0;
kt[n3] = kt[Z_] = kt[u3] = !1;
function Wo(i, t, e, n, r, s) {
  var o, a = t & YI, l = t & JI, c = t & XI;
  if (e && (o = r ? e(i, n, r, s) : e(i)), o !== void 0)
    return o;
  if (!vt(i))
    return i;
  var u = ci(i);
  if (u) {
    if (o = wI(i), !a)
      return u_(i, o);
  } else {
    var d = Wi(i), f = d == Z_ || d == i3;
    if (Tl(i))
      return H_(i, a);
    if (d == Y_ || d == K_ || f && !r) {
      if (o = l || f ? {} : G_(i), !a)
        return l ? cI(i, nI(o, i)) : aI(i, tI(o, i));
    } else {
      if (!kt[d])
        return r ? i : {};
      o = jI(i, d, a);
    }
  }
  s || (s = new Zi());
  var m = s.get(i);
  if (m)
    return m;
  s.set(i, o), ZI(i) ? i.forEach(function(b) {
    o.add(Wo(b, t, e, b, i, s));
  }) : qI(i) && i.forEach(function(b, v) {
    o.set(v, Wo(b, t, e, v, i, s));
  });
  var g = c ? l ? uI : Uf : l ? fa : fc, p = u ? void 0 : g(i);
  return XT(p || i, function(b, v) {
    p && (v = b, b = i[v]), Rg(o, v, Wo(b, t, e, v, i, s));
  }), o;
}
var C3 = 4;
function J_(i) {
  return Wo(i, C3);
}
var k3 = 1, x3 = 4;
function Fi(i) {
  return Wo(i, k3 | x3);
}
var E3 = 1, T3 = 4;
function jg(i, t) {
  return t = typeof t == "function" ? t : void 0, Wo(i, E3 | T3, t);
}
var S3 = "__lodash_hash_undefined__";
function A3(i) {
  return this.__data__.set(i, S3), this;
}
function P3(i) {
  return this.__data__.has(i);
}
function Lu(i) {
  var t = -1, e = i == null ? 0 : i.length;
  for (this.__data__ = new Wr(); ++t < e; )
    this.add(i[t]);
}
Lu.prototype.add = Lu.prototype.push = A3;
Lu.prototype.has = P3;
function I3(i, t) {
  for (var e = -1, n = i == null ? 0 : i.length; ++e < n; )
    if (t(i[e], e, i))
      return !0;
  return !1;
}
function R3(i, t) {
  return i.has(t);
}
var O3 = 1, V3 = 2;
function X_(i, t, e, n, r, s) {
  var o = e & O3, a = i.length, l = t.length;
  if (a != l && !(o && l > a))
    return !1;
  var c = s.get(i), u = s.get(t);
  if (c && u)
    return c == t && u == i;
  var d = -1, f = !0, m = e & V3 ? new Lu() : void 0;
  for (s.set(i, t), s.set(t, i); ++d < a; ) {
    var g = i[d], p = t[d];
    if (n)
      var b = o ? n(p, g, d, t, i, s) : n(g, p, d, i, t, s);
    if (b !== void 0) {
      if (b)
        continue;
      f = !1;
      break;
    }
    if (m) {
      if (!I3(t, function(v, y) {
        if (!R3(m, y) && (g === v || r(g, v, e, n, s)))
          return m.push(y);
      })) {
        f = !1;
        break;
      }
    } else if (!(g === p || r(g, p, e, n, s))) {
      f = !1;
      break;
    }
  }
  return s.delete(i), s.delete(t), f;
}
function M3(i) {
  var t = -1, e = Array(i.size);
  return i.forEach(function(n, r) {
    e[++t] = [r, n];
  }), e;
}
function D3(i) {
  var t = -1, e = Array(i.size);
  return i.forEach(function(n) {
    e[++t] = n;
  }), e;
}
var L3 = 1, N3 = 2, B3 = "[object Boolean]", F3 = "[object Date]", $3 = "[object Error]", z3 = "[object Map]", H3 = "[object Number]", j3 = "[object RegExp]", W3 = "[object Set]", U3 = "[object String]", q3 = "[object Symbol]", G3 = "[object ArrayBuffer]", K3 = "[object DataView]", Qb = fr ? fr.prototype : void 0, Mh = Qb ? Qb.valueOf : void 0;
function Z3(i, t, e, n, r, s, o) {
  switch (e) {
    case K3:
      if (i.byteLength != t.byteLength || i.byteOffset != t.byteOffset)
        return !1;
      i = i.buffer, t = t.buffer;
    case G3:
      return !(i.byteLength != t.byteLength || !s(new Du(i), new Du(t)));
    case B3:
    case F3:
    case H3:
      return hc(+i, +t);
    case $3:
      return i.name == t.name && i.message == t.message;
    case j3:
    case U3:
      return i == t + "";
    case z3:
      var a = M3;
    case W3:
      var l = n & L3;
      if (a || (a = D3), i.size != t.size && !l)
        return !1;
      var c = o.get(i);
      if (c)
        return c == t;
      n |= N3, o.set(i, t);
      var u = X_(a(i), a(t), n, r, s, o);
      return o.delete(i), u;
    case q3:
      if (Mh)
        return Mh.call(i) == Mh.call(t);
  }
  return !1;
}
var Y3 = 1, J3 = Object.prototype, X3 = J3.hasOwnProperty;
function Q3(i, t, e, n, r, s) {
  var o = e & Y3, a = Uf(i), l = a.length, c = Uf(t), u = c.length;
  if (l != u && !o)
    return !1;
  for (var d = l; d--; ) {
    var f = a[d];
    if (!(o ? f in t : X3.call(t, f)))
      return !1;
  }
  var m = s.get(i), g = s.get(t);
  if (m && g)
    return m == t && g == i;
  var p = !0;
  s.set(i, t), s.set(t, i);
  for (var b = o; ++d < l; ) {
    f = a[d];
    var v = i[f], y = t[f];
    if (n)
      var x = o ? n(y, v, f, t, i, s) : n(v, y, f, i, t, s);
    if (!(x === void 0 ? v === y || r(v, y, e, n, s) : x)) {
      p = !1;
      break;
    }
    b || (b = f == "constructor");
  }
  if (p && !b) {
    var S = i.constructor, k = t.constructor;
    S != k && "constructor" in i && "constructor" in t && !(typeof S == "function" && S instanceof S && typeof k == "function" && k instanceof k) && (p = !1);
  }
  return s.delete(i), s.delete(t), p;
}
var e4 = 1, e1 = "[object Arguments]", t1 = "[object Array]", Rc = "[object Object]", t4 = Object.prototype, n1 = t4.hasOwnProperty;
function n4(i, t, e, n, r, s) {
  var o = ci(i), a = ci(t), l = o ? t1 : Wi(i), c = a ? t1 : Wi(t);
  l = l == e1 ? Rc : l, c = c == e1 ? Rc : c;
  var u = l == Rc, d = c == Rc, f = l == c;
  if (f && Tl(i)) {
    if (!Tl(t))
      return !1;
    o = !0, u = !1;
  }
  if (f && !u)
    return s || (s = new Zi()), o || Lg(i) ? X_(i, t, e, n, r, s) : Z3(i, t, l, e, n, r, s);
  if (!(e & e4)) {
    var m = u && n1.call(i, "__wrapped__"), g = d && n1.call(t, "__wrapped__");
    if (m || g) {
      var p = m ? i.value() : i, b = g ? t.value() : t;
      return s || (s = new Zi()), r(p, b, e, n, s);
    }
  }
  return f ? (s || (s = new Zi()), Q3(i, t, e, n, r, s)) : !1;
}
function gc(i, t, e, n, r) {
  return i === t ? !0 : i == null || t == null || !Di(i) && !Di(t) ? i !== i && t !== t : n4(i, t, e, n, gc, r);
}
var i4 = 1, r4 = 2;
function s4(i, t, e, n) {
  var r = e.length, s = r;
  if (i == null)
    return !s;
  for (i = Object(i); r--; ) {
    var o = e[r];
    if (o[2] ? o[1] !== i[o[0]] : !(o[0] in i))
      return !1;
  }
  for (; ++r < s; ) {
    o = e[r];
    var a = o[0], l = i[a], c = o[1];
    if (o[2]) {
      if (l === void 0 && !(a in i))
        return !1;
    } else {
      var u = new Zi(), d;
      if (!(d === void 0 ? gc(c, l, i4 | r4, n, u) : d))
        return !1;
    }
  }
  return !0;
}
function Q_(i) {
  return i === i && !vt(i);
}
function o4(i) {
  for (var t = fc(i), e = t.length; e--; ) {
    var n = t[e], r = i[n];
    t[e] = [n, r, Q_(r)];
  }
  return t;
}
function e2(i, t) {
  return function(e) {
    return e == null ? !1 : e[i] === t && (t !== void 0 || i in Object(e));
  };
}
function a4(i) {
  var t = o4(i);
  return t.length == 1 && t[0][2] ? e2(t[0][0], t[0][1]) : function(e) {
    return e === i || s4(e, i, t);
  };
}
function l4(i, t) {
  return i != null && t in Object(i);
}
function c4(i, t, e) {
  t = Ld(t, i);
  for (var n = -1, r = t.length, s = !1; ++n < r; ) {
    var o = ma(t[n]);
    if (!(s = i != null && e(i, o)))
      break;
    i = i[o];
  }
  return s || ++n != r ? s : (r = i == null ? 0 : i.length, !!r && Og(r) && Od(o, r) && (ci(i) || Mu(i)));
}
function u4(i, t) {
  return i != null && c4(i, t, l4);
}
var d4 = 1, h4 = 2;
function f4(i, t) {
  return Ng(i) && Q_(t) ? e2(ma(i), t) : function(e) {
    var n = Pl(e, i);
    return n === void 0 && n === t ? u4(e, i) : gc(t, n, d4 | h4);
  };
}
function m4(i) {
  return function(t) {
    return t == null ? void 0 : t[i];
  };
}
function g4(i) {
  return function(t) {
    return Fg(t, i);
  };
}
function p4(i) {
  return Ng(i) ? m4(ma(i)) : g4(i);
}
function t2(i) {
  return typeof i == "function" ? i : i == null ? es : typeof i == "object" ? ci(i) ? f4(i[0], i[1]) : a4(i) : p4(i);
}
function b4(i) {
  return function(t, e, n) {
    for (var r = -1, s = Object(t), o = n(t), a = o.length; a--; ) {
      var l = o[++r];
      if (e(s[l], l, s) === !1)
        break;
    }
    return t;
  };
}
var n2 = b4();
function i2(i, t) {
  return i && n2(i, t, fc);
}
function v4(i, t) {
  return function(e, n) {
    if (e == null)
      return e;
    if (!ha(e))
      return i(e, n);
    for (var r = e.length, s = -1, o = Object(e); ++s < r && n(o[s], s, o) !== !1; )
      ;
    return e;
  };
}
var w4 = v4(i2), Dh = function() {
  return tr.Date.now();
}, y4 = "Expected a function", _4 = Math.max, C4 = Math.min;
function Ci(i, t, e) {
  var n, r, s, o, a, l, c = 0, u = !1, d = !1, f = !0;
  if (typeof i != "function")
    throw new TypeError(y4);
  t = Ab(t) || 0, vt(e) && (u = !!e.leading, d = "maxWait" in e, s = d ? _4(Ab(e.maxWait) || 0, t) : s, f = "trailing" in e ? !!e.trailing : f);
  function m(I) {
    var A = n, L = r;
    return n = r = void 0, c = I, o = i.apply(L, A), o;
  }
  function g(I) {
    return c = I, a = setTimeout(v, t), u ? m(I) : o;
  }
  function p(I) {
    var A = I - l, L = I - c, R = t - A;
    return d ? C4(R, s - L) : R;
  }
  function b(I) {
    var A = I - l, L = I - c;
    return l === void 0 || A >= t || A < 0 || d && L >= s;
  }
  function v() {
    var I = Dh();
    if (b(I))
      return y(I);
    a = setTimeout(v, p(I));
  }
  function y(I) {
    return a = void 0, f && n ? m(I) : (n = r = void 0, o);
  }
  function x() {
    a !== void 0 && clearTimeout(a), c = 0, n = l = r = a = void 0;
  }
  function S() {
    return a === void 0 ? o : y(Dh());
  }
  function k() {
    var I = Dh(), A = b(I);
    if (n = arguments, r = this, l = I, A) {
      if (a === void 0)
        return g(l);
      if (d)
        return clearTimeout(a), a = setTimeout(v, t), m(l);
    }
    return a === void 0 && (a = setTimeout(v, t)), o;
  }
  return k.cancel = x, k.flush = S, k;
}
function Zf(i, t, e) {
  (e !== void 0 && !hc(i[t], e) || e === void 0 && !(t in i)) && Vd(i, t, e);
}
function k4(i) {
  return Di(i) && ha(i);
}
function Yf(i, t) {
  if (!(t === "constructor" && typeof i[t] == "function") && t != "__proto__")
    return i[t];
}
function x4(i) {
  return da(i, fa(i));
}
function E4(i, t, e, n, r, s, o) {
  var a = Yf(i, e), l = Yf(t, e), c = o.get(l);
  if (c) {
    Zf(i, e, c);
    return;
  }
  var u = s ? s(a, l, e + "", i, t, o) : void 0, d = u === void 0;
  if (d) {
    var f = ci(l), m = !f && Tl(l), g = !f && !m && Lg(l);
    u = l, f || m || g ? ci(a) ? u = a : k4(a) ? u = u_(a) : m ? (d = !1, u = H_(l, !0)) : g ? (d = !1, u = q_(l, !0)) : u = [] : oi(l) || Mu(l) ? (u = a, Mu(a) ? u = x4(a) : (!vt(a) || Ws(a)) && (u = G_(l))) : d = !1;
  }
  d && (o.set(l, u), r(u, l, n, s, o), o.delete(l)), Zf(i, e, u);
}
function Wg(i, t, e, n, r) {
  i !== t && n2(t, function(s, o) {
    if (r || (r = new Zi()), vt(s))
      E4(i, t, o, e, Wg, n, r);
    else {
      var a = n ? n(Yf(i, o), s, o + "", i, t, r) : void 0;
      a === void 0 && (a = s), Zf(i, o, a);
    }
  }, fa);
}
var T4 = Vg(function(i, t, e, n) {
  Wg(i, t, e, n);
});
function S4(i) {
  var t = i == null ? 0 : i.length;
  return t ? i[t - 1] : void 0;
}
var r2 = /[\\^$.*+?()[\]{}|]/g, A4 = RegExp(r2.source);
function s2(i) {
  return i = mc(i), i && A4.test(i) ? i.replace(r2, "\\$&") : i;
}
function P4(i, t) {
  var e = -1, n = ha(i) ? Array(i.length) : [];
  return w4(i, function(r, s, o) {
    n[++e] = t(r, s, o);
  }), n;
}
function I4(i, t) {
  var e = ci(i) ? l_ : P4;
  return e(i, t2(t));
}
var R4 = "[object String]";
function i1(i) {
  return typeof i == "string" || !ci(i) && Di(i) && bs(i) == R4;
}
function O4(i, t) {
  return t.length < 2 ? i : Fg(i, w_(t, 0, -1));
}
function us(i) {
  return Di(i) && i.nodeType === 1 && !oi(i);
}
function Yo(i, t) {
  return gc(i, t);
}
function V4(i, t, e) {
  e = typeof e == "function" ? e : void 0;
  var n = e ? e(i, t) : void 0;
  return n === void 0 ? gc(i, t, void 0, e) : !!n;
}
function M4(i, t) {
  var e = {};
  return t = t2(t), i2(i, function(n, r, s) {
    Vd(e, r, t(n, r, s));
  }), e;
}
var Ug = Vg(function(i, t, e) {
  Wg(i, t, e);
});
function D4(i, t) {
  return t = Ld(t, i), i = O4(i, t), i == null || delete i[ma(S4(t))];
}
function L4(i, t, e, n) {
  if (!vt(i))
    return i;
  t = Ld(t, i);
  for (var r = -1, s = t.length, o = s - 1, a = i; a != null && ++r < s; ) {
    var l = ma(t[r]), c = e;
    if (l === "__proto__" || l === "constructor" || l === "prototype")
      return i;
    if (r != o) {
      var u = a[l];
      c = void 0, c === void 0 && (c = vt(u) ? u : Od(t[r + 1]) ? [] : {});
    }
    Rg(a, l, c), a = a[l];
  }
  return i;
}
function N4(i, t, e) {
  return i == null ? i : L4(i, t, e);
}
var B4 = KP(function(i, t, e) {
  return i + (e ? " " : "") + S_(t);
}), F4 = "Expected a function";
function Jo(i, t, e) {
  var n = !0, r = !0;
  if (typeof i != "function")
    throw new TypeError(F4);
  return vt(e) && (n = "leading" in e ? !!e.leading : n, r = "trailing" in e ? !!e.trailing : r), Ci(i, t, {
    leading: n,
    maxWait: t,
    trailing: r
  });
}
function $4(i, t) {
  return i == null ? !0 : D4(i, t);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
let Jf;
try {
  Jf = {
    window,
    document
  };
} catch {
  /* istanbul ignore next -- @preserve */
  Jf = {
    window: {},
    document: {}
  };
}
var ae = Jf;
function z4() {
  try {
    return navigator.userAgent.toLowerCase();
  } catch {
    return "";
  }
}
const Es = /* @__PURE__ */ z4(), Me = {
  isMac: /* @__PURE__ */ o2(Es),
  isWindows: /* @__PURE__ */ H4(Es),
  isGecko: /* @__PURE__ */ j4(Es),
  isSafari: /* @__PURE__ */ W4(Es),
  isiOS: /* @__PURE__ */ U4(Es),
  isAndroid: /* @__PURE__ */ q4(Es),
  isBlink: /* @__PURE__ */ G4(Es),
  get isMediaForcedColors() {
    return Z4();
  },
  get isMotionReduced() {
    return Y4();
  },
  features: {
    isRegExpUnicodePropertySupported: /* @__PURE__ */ K4()
  }
};
function o2(i) {
  return i.indexOf("macintosh") > -1;
}
function H4(i) {
  return i.indexOf("windows") > -1;
}
function j4(i) {
  return !!i.match(/gecko\/\d+/);
}
function W4(i) {
  return i.indexOf(" applewebkit/") > -1 && i.indexOf("chrome") === -1;
}
function U4(i) {
  return !!i.match(/iphone|ipad/i) || o2(i) && navigator.maxTouchPoints > 0;
}
function q4(i) {
  return i.indexOf("android") > -1;
}
function G4(i) {
  return i.indexOf("chrome/") > -1 && i.indexOf("edge/") < 0;
}
function K4() {
  let i = !1;
  try {
    i = "ć".search(new RegExp("[\\p{L}]", "u")) === 0;
  } catch {
  }
  return i;
}
function Z4() {
  return ae.window.matchMedia ? ae.window.matchMedia("(forced-colors: active)").matches : !1;
}
function Y4() {
  return ae.window.matchMedia ? ae.window.matchMedia("(prefers-reduced-motion)").matches : !1;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function a2(i, t, e, n) {
  e = e || function(l, c) {
    return l === c;
  };
  const r = Array.isArray(i) ? i : Array.prototype.slice.call(i), s = Array.isArray(t) ? t : Array.prototype.slice.call(t), o = J4(r, s, e);
  return n ? Q4(o, s.length) : X4(s, o);
}
function J4(i, t, e) {
  const n = r1(i, t, e);
  if (n === -1)
    return {
      firstIndex: -1,
      lastIndexOld: -1,
      lastIndexNew: -1
    };
  const r = s1(i, n), s = s1(t, n), o = r1(r, s, e), a = i.length - o, l = t.length - o;
  return {
    firstIndex: n,
    lastIndexOld: a,
    lastIndexNew: l
  };
}
function r1(i, t, e) {
  for (let n = 0; n < Math.max(i.length, t.length); n++)
    if (i[n] === void 0 || t[n] === void 0 || !e(i[n], t[n]))
      return n;
  return -1;
}
function s1(i, t) {
  return i.slice(t).reverse();
}
function X4(i, t) {
  const e = [], { firstIndex: n, lastIndexOld: r, lastIndexNew: s } = t;
  return s - n > 0 && e.push({
    index: n,
    type: "insert",
    values: i.slice(n, s)
  }), r - n > 0 && e.push({
    index: n + (s - n),
    type: "delete",
    howMany: r - n
  }), e;
}
function Q4(i, t) {
  const { firstIndex: e, lastIndexOld: n, lastIndexNew: r } = i;
  if (e === -1)
    return Array(t).fill("equal");
  let s = [];
  return e > 0 && (s = s.concat(Array(e).fill("equal"))), r - e > 0 && (s = s.concat(Array(r - e).fill("insert"))), n - e > 0 && (s = s.concat(Array(n - e).fill("delete"))), r < t && (s = s.concat(Array(t - r).fill("equal"))), s;
}
function sl(i, t, e) {
  e = e || function(p, b) {
    return p === b;
  };
  const n = i.length, r = t.length;
  if (n > 200 || r > 200 || n + r > 300)
    return sl.fastDiff(i, t, e, !0);
  let s, o;
  if (r < n) {
    const p = i;
    i = t, t = p, s = "delete", o = "insert";
  } else
    s = "insert", o = "delete";
  const a = i.length, l = t.length, c = l - a, u = {}, d = {};
  function f(p) {
    const b = (d[p - 1] !== void 0 ? d[p - 1] : -1) + 1, v = d[p + 1] !== void 0 ? d[p + 1] : -1, y = b > v ? -1 : 1;
    u[p + y] && (u[p] = u[p + y].slice(0)), u[p] || (u[p] = []), u[p].push(b > v ? s : o);
    let x = Math.max(b, v), S = x - p;
    for (; S < a && x < l && e(i[S], t[x]); )
      S++, x++, u[p].push("equal");
    return x;
  }
  let m = 0, g;
  do {
    for (g = -m; g < c; g++)
      d[g] = f(g);
    for (g = c + m; g > c; g--)
      d[g] = f(g);
    d[c] = f(c), m++;
  } while (d[c] !== l);
  return u[c].slice(1);
}
sl.fastDiff = a2;
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function o1() {
  return function i() {
    i.called = !0;
  };
}
class Br {
  /**
  * @param source The emitter.
  * @param name The event name.
  */
  constructor(t, e) {
    /**
    * The object that fired the event.
    */
    h(this, "source");
    /**
    * The event name.
    */
    h(this, "name");
    /**
    * Path this event has followed. See {@link module:utils/emittermixin~Emitter#delegate}.
    */
    h(this, "path");
    /**
    * Stops the event emitter to call further callbacks for this event interaction.
    */
    h(this, "stop");
    /**
    * Removes the current callback from future interactions of this event.
    */
    h(this, "off");
    /**
    * The value which will be returned by {@link module:utils/emittermixin~Emitter#fire}.
    *
    * It's `undefined` by default and can be changed by an event listener:
    *
    * ```ts
    * dataController.fire( 'getSelectedContent', ( evt ) => {
    * 	// This listener will make `dataController.fire( 'getSelectedContent' )`
    * 	// always return an empty DocumentFragment.
    * 	evt.return = new DocumentFragment();
    *
    * 	// Make sure no other listeners are executed.
    * 	evt.stop();
    * } );
    * ```
    */
    h(this, "return");
    this.source = t, this.name = e, this.path = [], this.stop = o1(), this.off = o1();
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const Nn = new Array(256).fill("").map((i, t) => ("0" + t.toString(16)).slice(-2));
function Jn() {
  const [i, t, e, n] = crypto.getRandomValues(new Uint32Array(4));
  return "e" + Nn[i >> 0 & 255] + Nn[i >> 8 & 255] + Nn[i >> 16 & 255] + Nn[i >> 24 & 255] + Nn[t >> 0 & 255] + Nn[t >> 8 & 255] + Nn[t >> 16 & 255] + Nn[t >> 24 & 255] + Nn[e >> 0 & 255] + Nn[e >> 8 & 255] + Nn[e >> 16 & 255] + Nn[e >> 24 & 255] + Nn[n >> 0 & 255] + Nn[n >> 8 & 255] + Nn[n >> 16 & 255] + Nn[n >> 24 & 255];
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const wi = {
  get(i = "normal") {
    return typeof i != "number" ? this[i] || this.normal : i;
  },
  highest: 1e5,
  high: 1e3,
  normal: 0,
  low: -1e3,
  lowest: -1e5
};
function eR(i, t) {
  const e = wi.get(t.priority);
  for (let n = 0; n < i.length; n++)
    if (wi.get(i[n].priority) < e) {
      i.splice(n, 0, t);
      return;
    }
  i.push(t);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const tR = "https://ckeditor.com/docs/ckeditor5/latest/support/error-codes.html";
class T extends Error {
  /**
  * Creates an instance of the CKEditorError class.
  *
  * @param errorName The error id in an `error-name` format. A link to this error documentation page will be added
  * to the thrown error's `message`.
  * @param context A context of the error by which the {@link module:watchdog/watchdog~Watchdog watchdog}
  * is able to determine which editor crashed. It should be an editor instance or a property connected to it. It can be also
  * a `null` value if the editor should not be restarted in case of the error (e.g. during the editor initialization).
  * The error context should be checked using the `areConnectedThroughProperties( editor, context )` utility
  * to check if the object works as the context.
  * @param data Additional data describing the error. A stringified version of this object
  * will be appended to the error message, so the data are quickly visible in the console. The original
  * data object will also be later available under the {@link #data} property.
  */
  constructor(e, n, r) {
    super(nR(e, r));
    /**
    * A context of the error by which the Watchdog is able to determine which editor crashed.
    */
    h(this, "context");
    /**
    * The additional error data passed to the constructor. Undefined if none was passed.
    */
    h(this, "data");
    this.name = "CKEditorError", this.context = n, this.data = r;
  }
  /**
  * Checks if the error is of the `CKEditorError` type.
  */
  is(e) {
    return e === "CKEditorError";
  }
  /**
  * A utility that ensures that the thrown error is a {@link module:utils/ckeditorerror~CKEditorError} one.
  * It is useful when combined with the {@link module:watchdog/watchdog~Watchdog} feature, which can restart the editor in case
  * of a {@link module:utils/ckeditorerror~CKEditorError} error.
  *
  * @param err The error to rethrow.
  * @param context An object connected through properties with the editor instance. This context will be used
  * by the watchdog to verify which editor should be restarted.
  */
  static rethrowUnexpectedError(e, n) {
    if (e.is && e.is("CKEditorError"))
      throw e;
    const r = new T(e.message, n);
    throw r.stack = e.stack, r;
  }
}
function Xe(i, t) {
  console.warn(...iR(i, t));
}
function l2(i) {
  return `
Read more: ${tR}#error-${i}`;
}
function nR(i, t) {
  const e = /* @__PURE__ */ new WeakSet(), r = t ? ` ${JSON.stringify(t, (o, a) => {
    if (typeof a == "object" && a !== null) {
      if (e.has(a))
        return `[object ${a.constructor.name}]`;
      e.add(a);
    }
    return a;
  })}` : "", s = l2(i);
  return i + r + s;
}
function iR(i, t) {
  const e = l2(i);
  return t ? [
    i,
    t,
    e
  ] : [
    i,
    e
  ];
}
const rR = "43.3.0", sR = new Date(2024, 9, 29);
/* istanbul ignore next -- @preserve */
if (globalThis.CKEDITOR_VERSION)
  throw new T("ckeditor-duplicated-modules", null);
globalThis.CKEDITOR_VERSION = rR;
const Po = Symbol("listeningTo"), Xf = Symbol("emitterId"), xr = Symbol("delegations"), c2 = /* @__PURE__ */ ht(Object);
function ht(i) {
  if (!i)
    return c2;
  class t extends i {
    on(n, r, s) {
      this.listenTo(this, n, r, s);
    }
    once(n, r, s) {
      let o = !1;
      const a = (l, ...c) => {
        o || (o = !0, l.off(), r.call(this, l, ...c));
      };
      this.listenTo(this, n, a, s);
    }
    off(n, r) {
      this.stopListening(this, n, r);
    }
    listenTo(n, r, s, o = {}) {
      let a, l;
      this[Po] || (this[Po] = {});
      const c = this[Po];
      Lh(n) || u2(n);
      const u = Lh(n);
      (a = c[u]) || (a = c[u] = {
        emitter: n,
        callbacks: {}
      }), (l = a.callbacks[r]) || (l = a.callbacks[r] = []), l.push(s), cR(this, n, r, s, o);
    }
    stopListening(n, r, s) {
      const o = this[Po];
      let a = n && Lh(n);
      const l = o && a ? o[a] : void 0, c = l && r ? l.callbacks[r] : void 0;
      if (!(!o || n && !l || r && !c))
        if (s)
          Nh(this, n, r, s), c.indexOf(s) !== -1 && (c.length === 1 ? delete l.callbacks[r] : Nh(this, n, r, s));
        else if (c) {
          for (; s = c.pop(); )
            Nh(this, n, r, s);
          delete l.callbacks[r];
        } else if (l) {
          for (r in l.callbacks)
            this.stopListening(n, r);
          delete o[a];
        } else {
          for (a in o)
            this.stopListening(o[a].emitter);
          delete this[Po];
        }
    }
    fire(n, ...r) {
      try {
        const s = n instanceof Br ? n : new Br(this, n), o = s.name;
        let a = h2(this, o);
        if (s.path.push(this), a) {
          const c = [
            s,
            ...r
          ];
          a = Array.from(a);
          for (let u = 0; u < a.length && (a[u].callback.apply(this, c), s.off.called && (delete s.off.called, this._removeEventListener(o, a[u].callback)), !s.stop.called); u++)
            ;
        }
        const l = this[xr];
        if (l) {
          const c = l.get(o), u = l.get("*");
          c && a1(c, s, r), u && a1(u, s, r);
        }
        return s.return;
      } catch (s) {
        /* istanbul ignore next -- @preserve */
        T.rethrowUnexpectedError(s, this);
      }
    }
    delegate(...n) {
      return {
        to: (r, s) => {
          this[xr] || (this[xr] = /* @__PURE__ */ new Map()), n.forEach((o) => {
            const a = this[xr].get(o);
            a ? a.set(r, s) : this[xr].set(o, /* @__PURE__ */ new Map([
              [
                r,
                s
              ]
            ]));
          });
        }
      };
    }
    stopDelegating(n, r) {
      if (this[xr])
        if (!n)
          this[xr].clear();
        else if (!r)
          this[xr].delete(n);
        else {
          const s = this[xr].get(n);
          s && s.delete(r);
        }
    }
    _addEventListener(n, r, s) {
      lR(this, n);
      const o = Qf(this, n), a = wi.get(s.priority), l = {
        callback: r,
        priority: a
      };
      for (const c of o)
        eR(c, l);
    }
    _removeEventListener(n, r) {
      const s = Qf(this, n);
      for (const o of s)
        for (let a = 0; a < o.length; a++)
          o[a].callback == r && (o.splice(a, 1), a--);
    }
  }
  return t;
}
[
  "on",
  "once",
  "off",
  "listenTo",
  "stopListening",
  "fire",
  "delegate",
  "stopDelegating",
  "_addEventListener",
  "_removeEventListener"
].forEach((i) => {
  ht[i] = c2.prototype[i];
});
function oR(i, t) {
  const e = i[Po];
  return e && e[t] ? e[t].emitter : null;
}
function u2(i, t) {
  i[Xf] || (i[Xf] = t || Jn());
}
function Lh(i) {
  return i[Xf];
}
function d2(i) {
  return i._events || Object.defineProperty(i, "_events", {
    value: {}
  }), i._events;
}
function aR() {
  return {
    callbacks: [],
    childEvents: []
  };
}
function lR(i, t) {
  const e = d2(i);
  if (e[t])
    return;
  let n = t, r = null;
  const s = [];
  for (; n !== "" && !e[n]; )
    e[n] = aR(), s.push(e[n]), r && e[n].childEvents.push(r), r = n, n = n.substr(0, n.lastIndexOf(":"));
  if (n !== "") {
    for (const o of s)
      o.callbacks = e[n].callbacks.slice();
    e[n].childEvents.push(r);
  }
}
function Qf(i, t) {
  const e = d2(i)[t];
  if (!e)
    return [];
  let n = [
    e.callbacks
  ];
  for (let r = 0; r < e.childEvents.length; r++) {
    const s = Qf(i, e.childEvents[r]);
    n = n.concat(s);
  }
  return n;
}
function h2(i, t) {
  let e;
  return !i._events || !(e = i._events[t]) || !e.callbacks.length ? t.indexOf(":") > -1 ? h2(i, t.substr(0, t.lastIndexOf(":"))) : null : e.callbacks;
}
function a1(i, t, e) {
  for (let [n, r] of i) {
    r ? typeof r == "function" && (r = r(t.name)) : r = t.name;
    const s = new Br(t.source, r);
    s.path = [
      ...t.path
    ], n.fire(s, ...e);
  }
}
function cR(i, t, e, n, r) {
  t._addEventListener ? t._addEventListener(e, n, r) : i._addEventListener.call(t, e, n, r);
}
function Nh(i, t, e, n) {
  t._removeEventListener ? t._removeEventListener(e, n) : i._removeEventListener.call(t, e, n);
}
const ol = Symbol("observableProperties"), Il = Symbol("boundObservables"), al = Symbol("boundProperties"), Ts = Symbol("decoratedMethods"), l1 = Symbol("decoratedOriginal"), f2 = /* @__PURE__ */ st(/* @__PURE__ */ ht());
function st(i) {
  var e, n, r, s, o;
  if (!i)
    return f2;
  class t extends (o = i, s = ol, r = Ts, n = al, e = Il, o) {
    constructor() {
      super(...arguments);
      h(this, s);
      h(this, r);
      h(this, n);
      h(this, e);
    }
    set(c, u) {
      if (vt(c)) {
        Object.keys(c).forEach((f) => {
          this.set(f, c[f]);
        }, this);
        return;
      }
      Bh(this);
      const d = this[ol];
      if (c in this && !d.has(c))
        throw new T("observable-set-cannot-override", this);
      Object.defineProperty(this, c, {
        enumerable: !0,
        configurable: !0,
        get() {
          return d.get(c);
        },
        set(f) {
          const m = d.get(c);
          let g = this.fire(`set:${c}`, c, f, m);
          g === void 0 && (g = f), (m !== g || !d.has(c)) && (d.set(c, g), this.fire(`change:${c}`, c, g, m));
        }
      }), this[c] = u;
    }
    bind(...c) {
      if (!c.length || !c1(c))
        throw new T("observable-bind-wrong-properties", this);
      if (new Set(c).size !== c.length)
        throw new T("observable-bind-duplicate-properties", this);
      Bh(this);
      const u = this[al];
      c.forEach((f) => {
        if (u.has(f))
          throw new T("observable-bind-rebind", this);
      });
      const d = /* @__PURE__ */ new Map();
      return c.forEach((f) => {
        const m = {
          property: f,
          to: []
        };
        u.set(f, m), d.set(f, m);
      }), {
        to: uR,
        toMany: dR,
        _observable: this,
        _bindProperties: c,
        _to: [],
        _bindings: d
      };
    }
    unbind(...c) {
      if (!this[ol])
        return;
      const u = this[al], d = this[Il];
      if (c.length) {
        if (!c1(c))
          throw new T("observable-unbind-wrong-properties", this);
        c.forEach((f) => {
          const m = u.get(f);
          m && (m.to.forEach(([g, p]) => {
            const b = d.get(g), v = b[p];
            v.delete(m), v.size || delete b[p], Object.keys(b).length || (d.delete(g), this.stopListening(g, "change"));
          }), u.delete(f));
        });
      } else
        d.forEach((f, m) => {
          this.stopListening(m, "change");
        }), d.clear(), u.clear();
    }
    decorate(c) {
      Bh(this);
      const u = this[c];
      if (!u)
        throw new T("observablemixin-cannot-decorate-undefined", this, {
          object: this,
          methodName: c
        });
      this.on(c, (d, f) => {
        d.return = u.apply(this, f);
      }), this[c] = function(...d) {
        return this.fire(c, d);
      }, this[c][l1] = u, this[Ts] || (this[Ts] = []), this[Ts].push(c);
    }
    // Override the EmitterMixin stopListening method to be able to clean (and restore) decorated methods.
    // This is needed in case of:
    //  1. Have x.foo() decorated.
    //  2. Call x.stopListening()
    //  3. Call x.foo(). Problem: nothing happens (the original foo() method is not executed)
    stopListening(c, u, d) {
      if (!c && this[Ts]) {
        for (const f of this[Ts])
          this[f] = this[f][l1];
        delete this[Ts];
      }
      super.stopListening(c, u, d);
    }
  }
  return t;
}
[
  "set",
  "bind",
  "unbind",
  "decorate",
  "on",
  "once",
  "off",
  "listenTo",
  "stopListening",
  "fire",
  "delegate",
  "stopDelegating",
  "_addEventListener",
  "_removeEventListener"
].forEach((i) => {
  st[i] = f2.prototype[i];
});
function Bh(i) {
  i[ol] || (Object.defineProperty(i, ol, {
    value: /* @__PURE__ */ new Map()
  }), Object.defineProperty(i, Il, {
    value: /* @__PURE__ */ new Map()
  }), Object.defineProperty(i, al, {
    value: /* @__PURE__ */ new Map()
  }));
}
function uR(...i) {
  const t = fR(...i), e = Array.from(this._bindings.keys()), n = e.length;
  if (!t.callback && t.to.length > 1)
    throw new T("observable-bind-to-no-callback", this);
  if (n > 1 && t.callback)
    throw new T("observable-bind-to-extra-callback", this);
  t.to.forEach((r) => {
    if (r.properties.length && r.properties.length !== n)
      throw new T("observable-bind-to-properties-length", this);
    r.properties.length || (r.properties = this._bindProperties);
  }), this._to = t.to, t.callback && (this._bindings.get(e[0]).callback = t.callback), pR(this._observable, this._to), gR(this), this._bindProperties.forEach((r) => {
    m2(this._observable, r);
  });
}
function dR(i, t, e) {
  if (this._bindings.size > 1)
    throw new T("observable-bind-to-many-not-one-binding", this);
  this.to(
    ...hR(i, t),
    // ...using given callback to parse attribute values.
    e
  );
}
function hR(i, t) {
  const e = i.map((n) => [
    n,
    t
  ]);
  return Array.prototype.concat.apply([], e);
}
function c1(i) {
  return i.every((t) => typeof t == "string");
}
function fR(...i) {
  if (!i.length)
    throw new T("observable-bind-to-parse-error", null);
  const t = {
    to: []
  };
  let e;
  return typeof i[i.length - 1] == "function" && (t.callback = i.pop()), i.forEach((n) => {
    if (typeof n == "string")
      e.properties.push(n);
    else if (typeof n == "object")
      e = {
        observable: n,
        properties: []
      }, t.to.push(e);
    else
      throw new T("observable-bind-to-parse-error", null);
  }), t;
}
function mR(i, t, e, n) {
  const r = i[Il], s = r.get(e), o = s || {};
  o[n] || (o[n] = /* @__PURE__ */ new Set()), o[n].add(t), s || r.set(e, o);
}
function gR(i) {
  let t;
  i._bindings.forEach((e, n) => {
    i._to.forEach((r) => {
      t = r.properties[e.callback ? 0 : i._bindProperties.indexOf(n)], e.to.push([
        r.observable,
        t
      ]), mR(i._observable, e, r.observable, t);
    });
  });
}
function m2(i, t) {
  const n = i[al].get(t);
  let r;
  n.callback ? r = n.callback.apply(i, n.to.map((s) => s[0][s[1]])) : (r = n.to[0], r = r[0][r[1]]), Object.prototype.hasOwnProperty.call(i, t) ? i[t] = r : i.set(t, r);
}
function pR(i, t) {
  t.forEach((e) => {
    const n = i[Il];
    let r;
    n.get(e.observable) || i.listenTo(e.observable, "change", (s, o) => {
      r = n.get(e.observable)[o], r && r.forEach((a) => {
        m2(i, a.property);
      });
    });
  });
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class g2 {
  constructor() {
    /**
    * The elements replaced by {@link #replace} and their replacements.
    */
    h(this, "_replacedElements");
    this._replacedElements = [];
  }
  /**
  * Hides the `element` and, if specified, inserts the the given element next to it.
  *
  * The effect of this method can be reverted by {@link #restore}.
  *
  * @param element The element to replace.
  * @param newElement The replacement element. If not passed, then the `element` will just be hidden.
  */
  replace(t, e) {
    this._replacedElements.push({
      element: t,
      newElement: e
    }), t.style.display = "none", e && t.parentNode.insertBefore(e, t.nextSibling);
  }
  /**
  * Restores what {@link #replace} did.
  */
  restore() {
    this._replacedElements.forEach(({ element: t, newElement: e }) => {
      t.style.display = "", e && e.remove();
    }), this._replacedElements = [];
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function em(i) {
  let t = 0;
  for (const e of i)
    t++;
  return t;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Fn(i, t) {
  const e = Math.min(i.length, t.length);
  for (let n = 0; n < e; n++)
    if (i[n] != t[n])
      return n;
  return i.length == t.length ? "same" : i.length < t.length ? "prefix" : "extension";
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function ki(i) {
  return !!(i && i[Symbol.iterator]);
}
function xn(i, t, e = {}, n = []) {
  const r = e && e.xmlns, s = r ? i.createElementNS(r, t) : i.createElement(t);
  for (const o in e)
    s.setAttribute(o, e[o]);
  (i1(n) || !ki(n)) && (n = [
    n
  ]);
  for (let o of n)
    i1(o) && (o = i.createTextNode(o)), s.appendChild(o);
  return s;
}
class p2 {
  /**
  * Creates an instance of the {@link ~Config} class.
  *
  * @param configurations The initial configurations to be set. Usually, provided by the user.
  * @param defaultConfigurations The default configurations. Usually, provided by the system.
  */
  constructor(t, e) {
    /**
    * Store for the whole configuration.
    */
    h(this, "_config");
    this._config = /* @__PURE__ */ Object.create(null), e && this.define(u1(e)), t && this._setObjectToTarget(this._config, t);
  }
  set(t, e) {
    this._setToTarget(this._config, t, e);
  }
  define(t, e) {
    this._setToTarget(this._config, t, e, !0);
  }
  /**
  * Gets the value for a configuration entry.
  *
  * ```ts
  * config.get( 'name' );
  * ```
  *
  * Deep configurations can be retrieved by separating each part with a dot.
  *
  * ```ts
  * config.get( 'toolbar.collapsed' );
  * ```
  *
  * @param name The configuration name. Configuration names are case-sensitive.
  * @returns The configuration value or `undefined` if the configuration entry was not found.
  */
  get(t) {
    return this._getFromSource(this._config, t);
  }
  /**
  * Iterates over all top level configuration names.
  */
  *names() {
    for (const t of Object.keys(this._config))
      yield t;
  }
  /**
  * Saves passed configuration to the specified target (nested object).
  *
  * @param target Nested config object.
  * @param name The configuration name or an object from which take properties as
  * configuration entries. Configuration names are case-sensitive.
  * @param value The configuration value. Used if a name is passed.
  * @param isDefine Define if passed configuration should overwrite existing one.
  */
  _setToTarget(t, e, n, r = !1) {
    if (oi(e)) {
      this._setObjectToTarget(t, e, r);
      return;
    }
    const s = e.split(".");
    e = s.pop();
    for (const o of s)
      oi(t[o]) || (t[o] = /* @__PURE__ */ Object.create(null)), t = t[o];
    if (oi(n)) {
      oi(t[e]) || (t[e] = /* @__PURE__ */ Object.create(null)), t = t[e], this._setObjectToTarget(t, n, r);
      return;
    }
    r && typeof t[e] < "u" || (t[e] = n);
  }
  /**
  * Get specified configuration from specified source (nested object).
  *
  * @param source level of nested object.
  * @param name The configuration name. Configuration names are case-sensitive.
  * @returns The configuration value or `undefined` if the configuration entry was not found.
  */
  _getFromSource(t, e) {
    const n = e.split(".");
    e = n.pop();
    for (const r of n) {
      if (!oi(t[r])) {
        t = null;
        break;
      }
      t = t[r];
    }
    return t ? u1(t[e]) : void 0;
  }
  /**
  * Iterates through passed object and calls {@link #_setToTarget} method with object key and value for each property.
  *
  * @param target Nested config object.
  * @param configuration Configuration data set
  * @param isDefine Defines if passed configuration is default configuration or not.
  */
  _setObjectToTarget(t, e, n) {
    Object.keys(e).forEach((r) => {
      this._setToTarget(t, r, e[r], n);
    });
  }
}
function u1(i) {
  return jg(i, bR);
}
function bR(i) {
  return us(i) || typeof i == "function" ? i : void 0;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function ds(i) {
  if (i) {
    if (i.defaultView)
      return i instanceof i.defaultView.Document;
    if (i.ownerDocument && i.ownerDocument.defaultView)
      return i instanceof i.ownerDocument.defaultView.Node;
  }
  return !1;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Nu(i) {
  const t = Object.prototype.toString.apply(i);
  return t == "[object Window]" || t == "[object global]";
}
const b2 = /* @__PURE__ */ Xn(/* @__PURE__ */ ht());
function Xn(i) {
  if (!i)
    return b2;
  class t extends i {
    listenTo(n, r, s, o = {}) {
      if (ds(n) || Nu(n)) {
        const a = {
          capture: !!o.useCapture,
          passive: !!o.usePassive
        }, l = this._getProxyEmitter(n, a) || new vR(n, a);
        this.listenTo(l, r, s, o);
      } else
        super.listenTo(n, r, s, o);
    }
    stopListening(n, r, s) {
      if (ds(n) || Nu(n)) {
        const o = this._getAllProxyEmitters(n);
        for (const a of o)
          this.stopListening(a, r, s);
      } else
        super.stopListening(n, r, s);
    }
    /**
    * Retrieves ProxyEmitter instance for given DOM Node residing in this Host and given options.
    *
    * @param node DOM Node of the ProxyEmitter.
    * @param options Additional options.
    * @param options.useCapture Indicates that events of this type will be dispatched to the registered
    * listener before being dispatched to any EventTarget beneath it in the DOM tree.
    * @param options.usePassive Indicates that the function specified by listener will never call preventDefault()
    * and prevents blocking browser's main thread by this event handler.
    * @returns ProxyEmitter instance bound to the DOM Node.
    */
    _getProxyEmitter(n, r) {
      return oR(this, v2(n, r));
    }
    /**
    * Retrieves all the ProxyEmitter instances for given DOM Node residing in this Host.
    *
    * @param node DOM Node of the ProxyEmitter.
    */
    _getAllProxyEmitters(n) {
      return [
        {
          capture: !1,
          passive: !1
        },
        {
          capture: !1,
          passive: !0
        },
        {
          capture: !0,
          passive: !1
        },
        {
          capture: !0,
          passive: !0
        }
      ].map((r) => this._getProxyEmitter(n, r)).filter((r) => !!r);
    }
  }
  return t;
}
[
  "_getProxyEmitter",
  "_getAllProxyEmitters",
  "on",
  "once",
  "off",
  "listenTo",
  "stopListening",
  "fire",
  "delegate",
  "stopDelegating",
  "_addEventListener",
  "_removeEventListener"
].forEach((i) => {
  Xn[i] = b2.prototype[i];
});
class vR extends (/* @__PURE__ */ ht()) {
  /**
  * @param node DOM Node that fires events.
  * @param options Additional options.
  * @param options.useCapture Indicates that events of this type will be dispatched to the registered
  * listener before being dispatched to any EventTarget beneath it in the DOM tree.
  * @param options.usePassive Indicates that the function specified by listener will never call preventDefault()
  * and prevents blocking browser's main thread by this event handler.
  */
  constructor(e, n) {
    super();
    h(this, "_domNode");
    h(this, "_options");
    /**
    * Collection of native DOM listeners.
    */
    h(this, "_domListeners");
    u2(this, v2(e, n)), this._domNode = e, this._options = n;
  }
  /**
  * Registers a callback function to be executed when an event is fired.
  *
  * It attaches a native DOM listener to the DOM Node. When fired,
  * a corresponding Emitter event will also fire with DOM Event object as an argument.
  *
  * **Note**: This is automatically called by the
  * {@link module:utils/emittermixin~Emitter#listenTo `Emitter#listenTo()`}.
  *
  * @param event The name of the event.
  */
  attach(e) {
    if (this._domListeners && this._domListeners[e])
      return;
    const n = this._createDomListener(e);
    this._domNode.addEventListener(e, n, this._options), this._domListeners || (this._domListeners = {}), this._domListeners[e] = n;
  }
  /**
  * Stops executing the callback on the given event.
  *
  * **Note**: This is automatically called by the
  * {@link module:utils/emittermixin~Emitter#stopListening `Emitter#stopListening()`}.
  *
  * @param event The name of the event.
  */
  detach(e) {
    let n;
    this._domListeners[e] && (!(n = this._events[e]) || !n.callbacks.length) && this._domListeners[e].removeListener();
  }
  /**
  * Adds callback to emitter for given event.
  *
  * @internal
  * @param event The name of the event.
  * @param callback The function to be called on event.
  * @param options Additional options.
  */
  _addEventListener(e, n, r) {
    this.attach(e), ht().prototype._addEventListener.call(this, e, n, r);
  }
  /**
  * Removes callback from emitter for given event.
  *
  * @internal
  * @param event The name of the event.
  * @param callback The function to stop being called.
  */
  _removeEventListener(e, n) {
    ht().prototype._removeEventListener.call(this, e, n), this.detach(e);
  }
  /**
  * Creates a native DOM listener callback. When the native DOM event
  * is fired it will fire corresponding event on this ProxyEmitter.
  * Note: A native DOM Event is passed as an argument.
  *
  * @param event The name of the event.
  * @returns The DOM listener callback.
  */
  _createDomListener(e) {
    const n = (r) => {
      this.fire(e, r);
    };
    return n.removeListener = () => {
      this._domNode.removeEventListener(e, n, this._options), delete this._domListeners[e];
    }, n;
  }
}
function wR(i) {
  return i["data-ck-expando"] || (i["data-ck-expando"] = Jn());
}
function v2(i, t) {
  let e = wR(i);
  for (const n of Object.keys(t).sort())
    t[n] && (e += "-" + n);
  return e;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function w2(i) {
  const t = [];
  let e = i;
  for (; e && e.nodeType != Node.DOCUMENT_NODE; )
    t.unshift(e), e = e.parentNode;
  return t;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function yR(i) {
  return i instanceof HTMLTextAreaElement ? i.value : i.innerHTML;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function y2(i) {
  const t = i.ownerDocument.defaultView.getComputedStyle(i);
  return {
    top: parseInt(t.borderTopWidth, 10),
    right: parseInt(t.borderRightWidth, 10),
    bottom: parseInt(t.borderBottomWidth, 10),
    left: parseInt(t.borderLeftWidth, 10)
  };
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function _2(i) {
  if (!i.target)
    return null;
  const t = i.target.ownerDocument, e = i.clientX, n = i.clientY;
  let r = null;
  return t.caretRangeFromPoint && t.caretRangeFromPoint(e, n) ? r = t.caretRangeFromPoint(e, n) : i.rangeParent && (r = t.createRange(), r.setStart(i.rangeParent, i.rangeOffset), r.collapse(!0)), r;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Bt(i) {
  return Object.prototype.toString.call(i) == "[object Text]";
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Nd(i) {
  return Object.prototype.toString.apply(i) == "[object Range]";
}
function C2(i) {
  return !i || !i.parentNode || i.offsetParent === ae.document.body ? null : i.offsetParent;
}
const k2 = [
  "top",
  "right",
  "bottom",
  "left",
  "width",
  "height"
];
let je = class Ar {
  /**
  * Creates an instance of rect.
  *
  * ```ts
  * // Rect of an HTMLElement.
  * const rectA = new Rect( document.body );
  *
  * // Rect of a DOM Range.
  * const rectB = new Rect( document.getSelection().getRangeAt( 0 ) );
  *
  * // Rect of a window (web browser viewport).
  * const rectC = new Rect( window );
  *
  * // Rect out of an object.
  * const rectD = new Rect( { top: 0, right: 10, bottom: 10, left: 0, width: 10, height: 10 } );
  *
  * // Rect out of another Rect instance.
  * const rectE = new Rect( rectD );
  *
  * // Rect out of a ClientRect.
  * const rectF = new Rect( document.body.getClientRects().item( 0 ) );
  * ```
  *
  * **Note**: By default a rect of an HTML element includes its CSS borders and scrollbars (if any)
  * ant the rect of a `window` includes scrollbars too. Use {@link #excludeScrollbarsAndBorders}
  * to get the inner part of the rect.
  *
  * @param source A source object to create the rect.
  */
  constructor(t) {
    /**
    * The "top" value of the rect.
    *
    * @readonly
    */
    h(this, "top");
    /**
    * The "right" value of the rect.
    *
    * @readonly
    */
    h(this, "right");
    /**
    * The "bottom" value of the rect.
    *
    * @readonly
    */
    h(this, "bottom");
    /**
    * The "left" value of the rect.
    *
    * @readonly
    */
    h(this, "left");
    /**
    * The "width" value of the rect.
    *
    * @readonly
    */
    h(this, "width");
    /**
    * The "height" value of the rect.
    *
    * @readonly
    */
    h(this, "height");
    /**
    * The object this rect is for.
    *
    * @readonly
    */
    h(this, "_source");
    const e = Nd(t);
    if (Object.defineProperty(this, "_source", {
      // If the source is a Rect instance, copy it's #_source.
      value: t._source || t,
      writable: !0,
      enumerable: !1
    }), tm(t) || e)
      if (e) {
        const n = Ar.getDomRangeRects(t);
        Oc(this, Ar.getBoundingRect(n));
      } else
        Oc(this, t.getBoundingClientRect());
    else if (Nu(t)) {
      const { innerWidth: n, innerHeight: r } = t;
      Oc(this, {
        top: 0,
        right: n,
        bottom: r,
        left: 0,
        width: n,
        height: r
      });
    } else
      Oc(this, t);
  }
  /**
  * Returns a clone of the rect.
  *
  * @returns A cloned rect.
  */
  clone() {
    return new Ar(this);
  }
  /**
  * Moves the rect so that its upper–left corner lands in desired `[ x, y ]` location.
  *
  * @param x Desired horizontal location.
  * @param y Desired vertical location.
  * @returns A rect which has been moved.
  */
  moveTo(t, e) {
    return this.top = e, this.right = t + this.width, this.bottom = e + this.height, this.left = t, this;
  }
  /**
  * Moves the rect in–place by a dedicated offset.
  *
  * @param x A horizontal offset.
  * @param y A vertical offset
  * @returns A rect which has been moved.
  */
  moveBy(t, e) {
    return this.top += e, this.right += t, this.left += t, this.bottom += e, this;
  }
  /**
  * Returns a new rect a a result of intersection with another rect.
  */
  getIntersection(t) {
    const e = {
      top: Math.max(this.top, t.top),
      right: Math.min(this.right, t.right),
      bottom: Math.min(this.bottom, t.bottom),
      left: Math.max(this.left, t.left),
      width: 0,
      height: 0
    };
    if (e.width = e.right - e.left, e.height = e.bottom - e.top, e.width < 0 || e.height < 0)
      return null;
    {
      const n = new Ar(e);
      return n._source = this._source, n;
    }
  }
  /**
  * Returns the area of intersection with another rect.
  *
  * @returns Area of intersection.
  */
  getIntersectionArea(t) {
    const e = this.getIntersection(t);
    return e ? e.getArea() : 0;
  }
  /**
  * Returns the area of the rect.
  */
  getArea() {
    return this.width * this.height;
  }
  /**
  * Returns a new rect, a part of the original rect, which is actually visible to the user and is relative to the,`body`,
  * e.g. an original rect cropped by parent element rects which have `overflow` set in CSS
  * other than `"visible"`.
  *
  * If there's no such visible rect, which is when the rect is limited by one or many of
  * the ancestors, `null` is returned.
  *
  * **Note**: This method does not consider the boundaries of the viewport (window).
  * To get a rect cropped by all ancestors and the viewport, use an intersection such as:
  *
  * ```ts
  * const visibleInViewportRect = new Rect( window ).getIntersection( new Rect( source ).getVisible() );
  * ```
  *
  * @returns A visible rect instance or `null`, if there's none.
  */
  getVisible() {
    const t = this._source;
    let e = this.clone();
    if (d1(t))
      return e;
    let n = t, r = t.parentNode || t.commonAncestorContainer, s;
    for (; r && !d1(r); ) {
      const o = _R(r) === "visible";
      n instanceof HTMLElement && h1(n) === "absolute" && (s = n);
      const a = h1(r);
      if (o || s && (a === "relative" && o || a !== "relative")) {
        n = r, r = r.parentNode;
        continue;
      }
      const l = new Ar(r), c = e.getIntersection(l);
      if (c)
        c.getArea() < e.getArea() && (e = c);
      else
        return null;
      n = r, r = r.parentNode;
    }
    return e;
  }
  /**
  * Checks if all property values ({@link #top}, {@link #left}, {@link #right},
  * {@link #bottom}, {@link #width} and {@link #height}) are the equal in both rect
  * instances.
  *
  * @param anotherRect A rect instance to compare with.
  * @returns `true` when Rects are equal. `false` otherwise.
  */
  isEqual(t) {
    for (const e of k2)
      if (this[e] !== t[e])
        return !1;
    return !0;
  }
  /**
  * Checks whether a rect fully contains another rect instance.
  *
  * @param anotherRect
  * @returns `true` if contains, `false` otherwise.
  */
  contains(t) {
    const e = this.getIntersection(t);
    return !!(e && e.isEqual(t));
  }
  /**
  * Recalculates screen coordinates to coordinates relative to the positioned ancestor offset.
  */
  toAbsoluteRect() {
    const { scrollX: t, scrollY: e } = ae.window, n = this.clone().moveBy(t, e);
    if (tm(n._source)) {
      const r = C2(n._source);
      r && CR(n, r);
    }
    return n;
  }
  /**
  * Excludes scrollbars and CSS borders from the rect.
  *
  * * Borders are removed when {@link #_source} is an HTML element.
  * * Scrollbars are excluded from HTML elements and the `window`.
  *
  * @returns A rect which has been updated.
  */
  excludeScrollbarsAndBorders() {
    const t = this._source;
    let e, n, r;
    if (Nu(t))
      e = t.innerWidth - t.document.documentElement.clientWidth, n = t.innerHeight - t.document.documentElement.clientHeight, r = t.getComputedStyle(t.document.documentElement).direction;
    else {
      const s = y2(t);
      e = t.offsetWidth - t.clientWidth - s.left - s.right, n = t.offsetHeight - t.clientHeight - s.top - s.bottom, r = t.ownerDocument.defaultView.getComputedStyle(t).direction, this.left += s.left, this.top += s.top, this.right -= s.right, this.bottom -= s.bottom, this.width = this.right - this.left, this.height = this.bottom - this.top;
    }
    return this.width -= e, r === "ltr" ? this.right -= e : this.left += e, this.height -= n, this.bottom -= n, this;
  }
  /**
  * Returns an array of rects of the given native DOM Range.
  *
  * @param range A native DOM range.
  * @returns DOM Range rects.
  */
  static getDomRangeRects(t) {
    const e = [], n = Array.from(t.getClientRects());
    if (n.length)
      for (const r of n)
        e.push(new Ar(r));
    else {
      let r = t.startContainer;
      Bt(r) && (r = r.parentNode);
      const s = new Ar(r.getBoundingClientRect());
      s.right = s.left, s.width = 0, e.push(s);
    }
    return e;
  }
  /**
  * Returns a bounding rectangle that contains all the given `rects`.
  *
  * @param rects A list of rectangles that should be contained in the result rectangle.
  * @returns Bounding rectangle or `null` if no `rects` were given.
  */
  static getBoundingRect(t) {
    const e = {
      left: Number.POSITIVE_INFINITY,
      top: Number.POSITIVE_INFINITY,
      right: Number.NEGATIVE_INFINITY,
      bottom: Number.NEGATIVE_INFINITY,
      width: 0,
      height: 0
    };
    let n = 0;
    for (const r of t)
      n++, e.left = Math.min(e.left, r.left), e.top = Math.min(e.top, r.top), e.right = Math.max(e.right, r.right), e.bottom = Math.max(e.bottom, r.bottom);
    return n == 0 ? null : (e.width = e.right - e.left, e.height = e.bottom - e.top, new Ar(e));
  }
};
function Oc(i, t) {
  for (const e of k2)
    i[e] = t[e];
}
function d1(i) {
  return tm(i) ? i === i.ownerDocument.body : !1;
}
function tm(i) {
  return i !== null && typeof i == "object" && i.nodeType === 1 && typeof i.getBoundingClientRect == "function";
}
function h1(i) {
  return i instanceof HTMLElement ? i.ownerDocument.defaultView.getComputedStyle(i).position : "static";
}
function _R(i) {
  return i instanceof HTMLElement ? i.ownerDocument.defaultView.getComputedStyle(i).overflow : "visible";
}
function CR(i, t) {
  const e = new je(t), n = y2(t);
  let r = 0, s = 0;
  r -= e.left, s -= e.top, r += t.scrollLeft, s += t.scrollTop, r -= n.left, s -= n.top, i.moveBy(r, s);
}
const It = class It {
  /**
  * Creates an instance of the `ResizeObserver` class.
  *
  * @param element A DOM element that is to be observed for resizing. Note that
  * the element must be visible (i.e. not detached from DOM) for the observer to work.
  * @param callback A function called when the observed element was resized. It passes
  * the [`ResizeObserverEntry`](https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserverEntry)
  * object with information about the resize event.
  */
  constructor(t, e) {
    /**
    * The element observed by this observer.
    */
    h(this, "_element");
    /**
    * The callback executed each time {@link #_element} is resized.
    */
    h(this, "_callback");
    It._observerInstance || It._createObserver(), this._element = t, this._callback = e, It._addElementCallback(t, e), It._observerInstance.observe(t);
  }
  /**
  * The element observed by this observer.
  */
  get element() {
    return this._element;
  }
  /**
  * Destroys the observer which disables the `callback` passed to the {@link #constructor}.
  */
  destroy() {
    It._deleteElementCallback(this._element, this._callback);
  }
  /**
  * Registers a new resize callback for the DOM element.
  */
  static _addElementCallback(t, e) {
    It._elementCallbacks || (It._elementCallbacks = /* @__PURE__ */ new Map());
    let n = It._elementCallbacks.get(t);
    n || (n = /* @__PURE__ */ new Set(), It._elementCallbacks.set(t, n)), n.add(e);
  }
  /**
  * Removes a resize callback from the DOM element. If no callbacks are left
  * for the element, it removes the element from the native observer.
  */
  static _deleteElementCallback(t, e) {
    const n = It._getElementCallbacks(t);
    n && (n.delete(e), n.size || (It._elementCallbacks.delete(t), It._observerInstance.unobserve(t))), It._elementCallbacks && !It._elementCallbacks.size && (It._observerInstance = null, It._elementCallbacks = null);
  }
  /**
  * Returns are registered resize callbacks for the DOM element.
  */
  static _getElementCallbacks(t) {
    return It._elementCallbacks ? It._elementCallbacks.get(t) : null;
  }
  /**
  * Creates the single native observer shared across all `ResizeObserver` instances.
  */
  static _createObserver() {
    It._observerInstance = new ae.window.ResizeObserver((t) => {
      for (const e of t) {
        const n = It._getElementCallbacks(e.target);
        if (n)
          for (const r of n)
            r(e);
      }
    });
  }
};
/**
* The single native observer instance shared across all {@link module:utils/dom/resizeobserver~ResizeObserver} instances.
*/
h(It, "_observerInstance", null), /**
* A mapping of native DOM elements and their callbacks shared across all
* {@link module:utils/dom/resizeobserver~ResizeObserver} instances.
*/
h(It, "_elementCallbacks", null);
let qs = It;
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function f1(i, t) {
  i instanceof HTMLTextAreaElement && (i.value = t), i.innerHTML = t;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function vs(i) {
  return (t) => t + i;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Qa(i) {
  let t = 0;
  for (; i.previousSibling; )
    i = i.previousSibling, t++;
  return t;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function x2(i, t, e) {
  i.insertBefore(e, i.childNodes[t] || null);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function ll(i) {
  return i && i.nodeType === Node.COMMENT_NODE;
}
function E2(i) {
  try {
    ae.document.createAttribute(i);
  } catch {
    return !1;
  }
  return !0;
}
function mr(i) {
  return i ? Bt(i) ? mr(i.parentElement) : i.getClientRects ? !!i.getClientRects().length : !1 : !1;
}
function qg({ element: i, target: t, positions: e, limiter: n, fitInViewport: r, viewportOffsetConfig: s }) {
  Ws(t) && (t = t()), Ws(n) && (n = n());
  const o = C2(i), a = kR(s), l = new je(i), c = m1(t, a);
  let u;
  if (!c || !a.getIntersection(c))
    return null;
  const d = {
    targetRect: c,
    elementRect: l,
    positionedElementAncestor: o,
    viewportRect: a
  };
  if (!n && !r)
    u = new T2(e[0], d);
  else {
    if (n) {
      const f = m1(n, a);
      f && (d.limiterRect = f);
    }
    u = xR(e, d);
  }
  return u;
}
function m1(i, t) {
  const e = new je(i).getVisible();
  return e ? e.getIntersection(t) : null;
}
function kR(i) {
  i = Object.assign({
    top: 0,
    bottom: 0,
    left: 0,
    right: 0
  }, i);
  const t = new je(ae.window);
  return t.top += i.top, t.height -= i.top, t.bottom -= i.bottom, t.height -= i.bottom, t;
}
function xR(i, t) {
  const { elementRect: e } = t, n = e.getArea(), r = i.map((a) => new T2(a, t)).filter((a) => !!a.name);
  let s = 0, o = null;
  for (const a of r) {
    const { limiterIntersectionArea: l, viewportIntersectionArea: c } = a;
    if (l === n)
      return a;
    const u = c ** 2 + l ** 2;
    u > s && (s = u, o = a);
  }
  return o;
}
class T2 {
  /**
  * Creates an instance of the {@link module:utils/dom/position~PositionObject} class.
  *
  * @param positioningFunction function The function that defines the expected
  * coordinates the positioned element should move to.
  * @param options options object.
  * @param options.elementRect The positioned element rect.
  * @param options.targetRect The target element rect.
  * @param options.viewportRect The viewport rect.
  * @param options.limiterRect The limiter rect.
  * @param options.positionedElementAncestor Nearest element ancestor element which CSS position is not "static".
  */
  constructor(t, e) {
    h(this, "name");
    h(this, "config");
    h(this, "_positioningFunctionCoordinates");
    h(this, "_options");
    h(this, "_cachedRect");
    h(this, "_cachedAbsoluteRect");
    const n = t(e.targetRect, e.elementRect, e.viewportRect, e.limiterRect);
    if (!n)
      return;
    const { left: r, top: s, name: o, config: a } = n;
    this.name = o, this.config = a, this._positioningFunctionCoordinates = {
      left: r,
      top: s
    }, this._options = e;
  }
  /**
  * The left value in pixels in the CSS `position: absolute` coordinate system.
  * Set it on the positioned element in DOM to move it to the position.
  */
  get left() {
    return this._absoluteRect.left;
  }
  /**
  * The top value in pixels in the CSS `position: absolute` coordinate system.
  * Set it on the positioned element in DOM to move it to the position.
  */
  get top() {
    return this._absoluteRect.top;
  }
  /**
  * An intersection area between positioned element and limiter within viewport constraints.
  */
  get limiterIntersectionArea() {
    const t = this._options.limiterRect;
    return t ? t.getIntersectionArea(this._rect) : 0;
  }
  /**
  * An intersection area between positioned element and viewport.
  */
  get viewportIntersectionArea() {
    return this._options.viewportRect.getIntersectionArea(this._rect);
  }
  /**
  * An already positioned element rect. A clone of the element rect passed to the constructor
  * but placed in the viewport according to the positioning function.
  */
  get _rect() {
    return this._cachedRect ? this._cachedRect : (this._cachedRect = this._options.elementRect.clone().moveTo(this._positioningFunctionCoordinates.left, this._positioningFunctionCoordinates.top), this._cachedRect);
  }
  /**
  * An already absolutely positioned element rect. See ({@link #_rect}).
  */
  get _absoluteRect() {
    return this._cachedAbsoluteRect ? this._cachedAbsoluteRect : (this._cachedAbsoluteRect = this._rect.toAbsoluteRect(), this._cachedAbsoluteRect);
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function g1(i) {
  const t = i.parentNode;
  t && t.removeChild(i);
}
function S2({ target: i, viewportOffset: t = 0, ancestorOffset: e = 0, alignToTop: n, forceScroll: r }) {
  const s = Gg(i);
  let o = s, a = null;
  for (t = SR(t); o; ) {
    let l;
    o == s ? l = p1(i) : l = p1(a), TR({
      parent: l,
      getRect: () => b1(i, o),
      alignToTop: n,
      ancestorOffset: e,
      forceScroll: r
    });
    const c = b1(i, o);
    if (ER({
      window: o,
      rect: c,
      viewportOffset: t,
      alignToTop: n,
      forceScroll: r
    }), o.parent != o) {
      if (a = o.frameElement, o = o.parent, !a)
        return;
    } else
      o = null;
  }
}
function ER({ window: i, rect: t, alignToTop: e, forceScroll: n, viewportOffset: r }) {
  const s = t.clone().moveBy(0, r.bottom), o = t.clone().moveBy(0, -r.top), a = new je(i).excludeScrollbarsAndBorders(), l = [
    o,
    s
  ], c = e && n, u = l.every((p) => a.contains(p));
  let { scrollX: d, scrollY: f } = i;
  const m = d, g = f;
  c ? f -= a.top - t.top + r.top : u || (P2(o, a) ? f -= a.top - t.top + r.top : A2(s, a) && (e ? f += t.top - a.top - r.top : f += t.bottom - a.bottom + r.bottom)), u || (I2(t, a) ? d -= a.left - t.left + r.left : R2(t, a) && (d += t.right - a.right + r.right)), (d != m || f !== g) && i.scrollTo(d, f);
}
function TR({ parent: i, getRect: t, alignToTop: e, forceScroll: n, ancestorOffset: r = 0, limiterElement: s }) {
  const o = Gg(i), a = e && n;
  let l, c, u;
  const d = s || o.document.body;
  for (; i != d; )
    c = t(), l = new je(i).excludeScrollbarsAndBorders(), u = l.contains(c), a ? i.scrollTop -= l.top - c.top + r : u || (P2(c, l) ? i.scrollTop -= l.top - c.top + r : A2(c, l) && (e ? i.scrollTop += c.top - l.top - r : i.scrollTop += c.bottom - l.bottom + r)), u || (I2(c, l) ? i.scrollLeft -= l.left - c.left + r : R2(c, l) && (i.scrollLeft += c.right - l.right + r)), i = i.parentNode;
}
function A2(i, t) {
  return i.bottom > t.bottom;
}
function P2(i, t) {
  return i.top < t.top;
}
function I2(i, t) {
  return i.left < t.left;
}
function R2(i, t) {
  return i.right > t.right;
}
function Gg(i) {
  return Nd(i) ? i.startContainer.ownerDocument.defaultView : i.ownerDocument.defaultView;
}
function p1(i) {
  if (Nd(i)) {
    let t = i.commonAncestorContainer;
    return Bt(t) && (t = t.parentNode), t;
  } else
    return i.parentNode;
}
function b1(i, t) {
  const e = Gg(i), n = new je(i);
  if (e === t)
    return n;
  {
    let r = e;
    for (; r != t; ) {
      const s = r.frameElement, o = new je(s).excludeScrollbarsAndBorders();
      n.moveBy(o.left, o.top), r = r.parent;
    }
  }
  return n;
}
function SR(i) {
  return typeof i == "number" ? {
    top: i,
    bottom: i,
    left: i,
    right: i
  } : i;
}
const AR = {
  ctrl: "⌃",
  cmd: "⌘",
  alt: "⌥",
  shift: "⇧"
}, PR = {
  ctrl: "Ctrl+",
  alt: "Alt+",
  shift: "Shift+"
}, v1 = {
  37: "←",
  38: "↑",
  39: "→",
  40: "↓",
  9: "⇥",
  33: "Page Up",
  34: "Page Down"
}, Ke = /* @__PURE__ */ MR(), IR = /* @__PURE__ */ Object.fromEntries(/* @__PURE__ */ Object.entries(Ke).map(([i, t]) => {
  let e;
  return t in v1 ? e = v1[t] : e = i.charAt(0).toUpperCase() + i.slice(1), [
    t,
    e
  ];
}));
function Xo(i) {
  let t;
  if (typeof i == "string") {
    if (t = Ke[i.toLowerCase()], !t)
      throw new T("keyboard-unknown-key", null, {
        key: i
      });
  } else
    t = i.keyCode + (i.altKey ? Ke.alt : 0) + (i.ctrlKey ? Ke.ctrl : 0) + (i.shiftKey ? Ke.shift : 0) + (i.metaKey ? Ke.cmd : 0);
  return t;
}
function Kg(i) {
  return typeof i == "string" && (i = DR(i)), i.map((t) => typeof t == "string" ? OR(t) : t).reduce((t, e) => e + t, 0);
}
function Bu(i) {
  let t = Kg(i);
  return Object.entries(Me.isMac || Me.isiOS ? AR : PR).reduce((r, [s, o]) => (t & Ke[s] && (t &= ~Ke[s], r += o), r), "") + (t ? IR[t] : "");
}
function RR(i) {
  return i == Ke.arrowright || i == Ke.arrowleft || i == Ke.arrowup || i == Ke.arrowdown;
}
function Zg(i, t) {
  const e = t === "ltr";
  switch (i) {
    case Ke.arrowleft:
      return e ? "left" : "right";
    case Ke.arrowright:
      return e ? "right" : "left";
    case Ke.arrowup:
      return "up";
    case Ke.arrowdown:
      return "down";
  }
}
function OR(i) {
  if (i.endsWith("!"))
    return Xo(i.slice(0, -1));
  const t = Xo(i);
  return (Me.isMac || Me.isiOS) && t == Ke.ctrl ? Ke.cmd : t;
}
function VR(i, t) {
  const e = Zg(i, t);
  return e === "down" || e === "right";
}
function MR() {
  const i = {
    pageup: 33,
    pagedown: 34,
    arrowleft: 37,
    arrowup: 38,
    arrowright: 39,
    arrowdown: 40,
    backspace: 8,
    delete: 46,
    enter: 13,
    space: 32,
    esc: 27,
    tab: 9,
    // The idea about these numbers is that they do not collide with any real key codes, so we can use them
    // like bit masks.
    ctrl: 1114112,
    shift: 2228224,
    alt: 4456448,
    cmd: 8912896
  };
  for (let t = 65; t <= 90; t++) {
    const e = String.fromCharCode(t);
    i[e.toLowerCase()] = t;
  }
  for (let t = 48; t <= 57; t++)
    i[t - 48] = t;
  for (let t = 112; t <= 123; t++)
    i["f" + (t - 111)] = t;
  return Object.assign(i, {
    "'": 222,
    ",": 108,
    "-": 109,
    ".": 110,
    "/": 111,
    ";": 186,
    "=": 187,
    "[": 219,
    "\\": 220,
    "]": 221,
    "`": 223
  }), i;
}
function DR(i) {
  return i.split("+").map((t) => t.trim());
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const LR = [
  "ar",
  "ara",
  "dv",
  "div",
  "fa",
  "per",
  "fas",
  "he",
  "heb",
  "ku",
  "kur",
  "ug",
  "uig"
  // Uighur, Uyghur
];
function w1(i) {
  return LR.includes(i) ? "rtl" : "ltr";
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function tt(i) {
  return Array.isArray(i) ? i : [
    i
  ];
}
/* istanbul ignore else -- @preserve */
ae.window.CKEDITOR_TRANSLATIONS || (ae.window.CKEDITOR_TRANSLATIONS = {});
function NR(i, t, e = 1, n) {
  if (typeof e != "number")
    throw new T("translation-service-quantity-not-a-number", null, {
      quantity: e
    });
  const r = n || ae.window.CKEDITOR_TRANSLATIONS, s = $R(r);
  s === 1 && (i = Object.keys(r)[0]);
  const o = t.id || t.string;
  if (s === 0 || !FR(i, o, r))
    return e !== 1 ? t.plural : t.string;
  const a = r[i].dictionary, l = r[i].getPluralForm || ((d) => d === 1 ? 0 : 1), c = a[o];
  if (typeof c == "string")
    return c;
  const u = Number(l(e));
  return c[u];
}
function BR(i) {
  return Array.isArray(i) ? i.reduce((t, e) => Ug(t, e)) : i;
}
function FR(i, t, e) {
  return !!e[i] && !!e[i].dictionary[t];
}
function $R(i) {
  return Object.keys(i).length;
}
class zR {
  /**
  * Creates a new instance of the locale class. Learn more about
  * {@glink getting-started/setup/ui-language configuring the language of the editor}.
  *
  * @param options Locale configuration.
  * @param options.uiLanguage The editor UI language code in the
  * [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format. See {@link #uiLanguage}.
  * @param options.contentLanguage The editor content language code in the
  * [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format. If not specified, the same as `options.language`.
  * See {@link #contentLanguage}.
  * @param translations Translations passed as a editor config parameter.
  */
  constructor({ uiLanguage: t = "en", contentLanguage: e, translations: n } = {}) {
    /**
    * The editor UI language code in the [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format.
    *
    * If the {@link #contentLanguage content language} was not specified in the `Locale` constructor,
    * it also defines the language of the content.
    */
    h(this, "uiLanguage");
    /**
    * Text direction of the {@link #uiLanguage editor UI language}. Either `'ltr'` or `'rtl'`.
    */
    h(this, "uiLanguageDirection");
    /**
    * The editor content language code in the [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format.
    *
    * Usually the same as the {@link #uiLanguage editor language}, it can be customized by passing an optional
    * argument to the `Locale` constructor.
    */
    h(this, "contentLanguage");
    /**
    * Text direction of the {@link #contentLanguage editor content language}.
    *
    * If the content language was passed directly to the `Locale` constructor, this property represents the
    * direction of that language.
    *
    * If the {@link #contentLanguage editor content language} was derived from the {@link #uiLanguage editor language},
    * the content language direction is the same as the {@link #uiLanguageDirection UI language direction}.
    *
    * The value is either `'ltr'` or `'rtl'`.
    */
    h(this, "contentLanguageDirection");
    /**
    * Translates the given message to the {@link #uiLanguage}. This method is also available in
    * {@link module:core/editor/editor~Editor#t `Editor`} and {@link module:ui/view~View#t `View`}.
    *
    * This method's context is statically bound to the `Locale` instance and **should always be called as a function**:
    *
    * ```ts
    * const t = locale.t;
    * t( 'Label' );
    * ```
    *
    * The message can be either a string or an object implementing the {@link module:utils/translation-service~Message} interface.
    *
    * The message may contain placeholders (`%<index>`) for value(s) that are passed as a `values` parameter.
    * For an array of values, the `%<index>` will be changed to an element of that array at the given index.
    * For a single value passed as the second argument, only the `%0` placeholders will be changed to the provided value.
    *
    * ```ts
    * t( 'Created file "%0" in %1ms.', [ fileName, timeTaken ] );
    * t( 'Created file "%0", fileName );
    * ```
    *
    * The message supports plural forms. To specify the plural form, use the `plural` property. Singular or plural form
    * will be chosen depending on the first value from the passed `values`. The value of the `plural` property is used
    * as a default plural translation when the translation for the target language is missing.
    *
    * ```ts
    * t( { string: 'Add a space', plural: 'Add %0 spaces' }, 1 ); // 'Add a space' for the English language.
    * t( { string: 'Add a space', plural: 'Add %0 spaces' }, 5 ); // 'Add 5 spaces' for the English language.
    * t( { string: '%1 a space', plural: '%1 %0 spaces' }, [ 2, 'Add' ] ); // 'Add 2 spaces' for the English language.
    *
    * t( { string: 'Add a space', plural: 'Add %0 spaces' }, 1 ); // 'Dodaj spację' for the Polish language.
    * t( { string: 'Add a space', plural: 'Add %0 spaces' }, 5 ); // 'Dodaj 5 spacji' for the Polish language.
    * t( { string: '%1 a space', plural: '%1 %0 spaces' }, [ 2, 'Add' ] ); // 'Dodaj 2 spacje' for the Polish language.
    * ```
    *
    *  * The message should provide an ID using the `id` property when the message strings are not unique and their
    * translations should be different.
    *
    * ```ts
    * translate( 'en', { string: 'image', id: 'ADD_IMAGE' } );
    * translate( 'en', { string: 'image', id: 'AN_IMAGE' } );
    * ```
    */
    h(this, "t");
    /**
    * Object that contains translations.
    */
    h(this, "translations");
    this.uiLanguage = t, this.contentLanguage = e || this.uiLanguage, this.uiLanguageDirection = w1(this.uiLanguage), this.contentLanguageDirection = w1(this.contentLanguage), this.translations = BR(n), this.t = (r, s) => this._t(r, s);
  }
  /**
  * The editor UI language code in the [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format.
  *
  * **Note**: This property was deprecated. Please use {@link #uiLanguage} and {@link #contentLanguage}
  * properties instead.
  *
  * @deprecated
  */
  get language() {
    return console.warn("locale-deprecated-language-property: The Locale#language property has been deprecated and will be removed in the near future. Please use #uiLanguage and #contentLanguage properties instead."), this.uiLanguage;
  }
  /**
  * An unbound version of the {@link #t} method.
  */
  _t(t, e = []) {
    e = tt(e), typeof t == "string" && (t = {
      string: t
    });
    const r = !!t.plural ? e[0] : 1, s = NR(this.uiLanguage, t, r, this.translations);
    return HR(s, e);
  }
}
function HR(i, t) {
  return i.replace(/%(\d+)/g, (e, n) => n < t.length ? t[n] : e);
}
class Pn extends (/* @__PURE__ */ ht()) {
  constructor(e = {}, n = {}) {
    super();
    /**
    * The internal list of items in the collection.
    */
    h(this, "_items");
    /**
    * The internal map of items in the collection.
    */
    h(this, "_itemMap");
    /**
    * The name of the property which is considered to identify an item.
    */
    h(this, "_idProperty");
    /**
    * A collection instance this collection is bound to as a result
    * of calling {@link #bindTo} method.
    */
    h(this, "_bindToCollection");
    /**
    * A helper mapping external items of a bound collection ({@link #bindTo})
    * and actual items of this collection. It provides information
    * necessary to properly remove items bound to another collection.
    *
    * See {@link #_bindToInternalToExternalMap}.
    */
    h(this, "_bindToExternalToInternalMap");
    /**
    * A helper mapping items of this collection to external items of a bound collection
    * ({@link #bindTo}). It provides information necessary to manage the bindings, e.g.
    * to avoid loops in two–way bindings.
    *
    * See {@link #_bindToExternalToInternalMap}.
    */
    h(this, "_bindToInternalToExternalMap");
    /**
    * Stores indexes of skipped items from bound external collection.
    */
    h(this, "_skippedIndexesFromExternal");
    const r = ki(e);
    if (r || (n = e), this._items = [], this._itemMap = /* @__PURE__ */ new Map(), this._idProperty = n.idProperty || "id", this._bindToExternalToInternalMap = /* @__PURE__ */ new WeakMap(), this._bindToInternalToExternalMap = /* @__PURE__ */ new WeakMap(), this._skippedIndexesFromExternal = [], r)
      for (const s of e)
        this._items.push(s), this._itemMap.set(this._getItemIdBeforeAdding(s), s);
  }
  /**
  * The number of items available in the collection.
  */
  get length() {
    return this._items.length;
  }
  /**
  * Returns the first item from the collection or null when collection is empty.
  */
  get first() {
    return this._items[0] || null;
  }
  /**
  * Returns the last item from the collection or null when collection is empty.
  */
  get last() {
    return this._items[this.length - 1] || null;
  }
  /**
  * Adds an item into the collection.
  *
  * If the item does not have an id, then it will be automatically generated and set on the item.
  *
  * @param item
  * @param index The position of the item in the collection. The item
  * is pushed to the collection when `index` not specified.
  * @fires add
  * @fires change
  */
  add(e, n) {
    return this.addMany([
      e
    ], n);
  }
  /**
  * Adds multiple items into the collection.
  *
  * Any item not containing an id will get an automatically generated one.
  *
  * @param items
  * @param index The position of the insertion. Items will be appended if no `index` is specified.
  * @fires add
  * @fires change
  */
  addMany(e, n) {
    if (n === void 0)
      n = this._items.length;
    else if (n > this._items.length || n < 0)
      throw new T("collection-add-item-invalid-index", this);
    let r = 0;
    for (const s of e) {
      const o = this._getItemIdBeforeAdding(s), a = n + r;
      this._items.splice(a, 0, s), this._itemMap.set(o, s), this.fire("add", s, a), r++;
    }
    return this.fire("change", {
      added: e,
      removed: [],
      index: n
    }), this;
  }
  /**
  * Gets an item by its ID or index.
  *
  * @param idOrIndex The item ID or index in the collection.
  * @returns The requested item or `null` if such item does not exist.
  */
  get(e) {
    let n;
    if (typeof e == "string")
      n = this._itemMap.get(e);
    else if (typeof e == "number")
      n = this._items[e];
    else
      throw new T("collection-get-invalid-arg", this);
    return n || null;
  }
  /**
  * Returns a Boolean indicating whether the collection contains an item.
  *
  * @param itemOrId The item or its ID in the collection.
  * @returns `true` if the collection contains the item, `false` otherwise.
  */
  has(e) {
    if (typeof e == "string")
      return this._itemMap.has(e);
    {
      const n = this._idProperty, r = e[n];
      return r && this._itemMap.has(r);
    }
  }
  /**
  * Gets an index of an item in the collection.
  * When an item is not defined in the collection, the index will equal -1.
  *
  * @param itemOrId The item or its ID in the collection.
  * @returns The index of a given item.
  */
  getIndex(e) {
    let n;
    return typeof e == "string" ? n = this._itemMap.get(e) : n = e, n ? this._items.indexOf(n) : -1;
  }
  /**
  * Removes an item from the collection.
  *
  * @param subject The item to remove, its ID or index in the collection.
  * @returns The removed item.
  * @fires remove
  * @fires change
  */
  remove(e) {
    const [n, r] = this._remove(e);
    return this.fire("change", {
      added: [],
      removed: [
        n
      ],
      index: r
    }), n;
  }
  /**
  * Executes the callback for each item in the collection and composes an array or values returned by this callback.
  *
  * @typeParam U The result type of the callback.
  * @param callback
  * @param ctx Context in which the `callback` will be called.
  * @returns The result of mapping.
  */
  map(e, n) {
    return this._items.map(e, n);
  }
  /**
  * Performs the specified action for each item in the collection.
  *
  * @param ctx Context in which the `callback` will be called.
  */
  forEach(e, n) {
    this._items.forEach(e, n);
  }
  /**
  * Finds the first item in the collection for which the `callback` returns a true value.
  *
  * @param callback
  * @param ctx Context in which the `callback` will be called.
  * @returns The item for which `callback` returned a true value.
  */
  find(e, n) {
    return this._items.find(e, n);
  }
  /**
  * Returns an array with items for which the `callback` returned a true value.
  *
  * @param callback
  * @param ctx Context in which the `callback` will be called.
  * @returns The array with matching items.
  */
  filter(e, n) {
    return this._items.filter(e, n);
  }
  /**
  * Removes all items from the collection and destroys the binding created using
  * {@link #bindTo}.
  *
  * @fires remove
  * @fires change
  */
  clear() {
    this._bindToCollection && (this.stopListening(this._bindToCollection), this._bindToCollection = null);
    const e = Array.from(this._items);
    for (; this.length; )
      this._remove(0);
    this.fire("change", {
      added: [],
      removed: e,
      index: 0
    });
  }
  /**
  * Binds and synchronizes the collection with another one.
  *
  * The binding can be a simple factory:
  *
  * ```ts
  * class FactoryClass {
  * 	public label: string;
  *
  * 	constructor( data: { label: string } ) {
  * 		this.label = data.label;
  * 	}
  * }
  *
  * const source = new Collection<{ label: string }>( { idProperty: 'label' } );
  * const target = new Collection<FactoryClass>();
  *
  * target.bindTo( source ).as( FactoryClass );
  *
  * source.add( { label: 'foo' } );
  * source.add( { label: 'bar' } );
  *
  * console.log( target.length ); // 2
  * console.log( target.get( 1 ).label ); // 'bar'
  *
  * source.remove( 0 );
  * console.log( target.length ); // 1
  * console.log( target.get( 0 ).label ); // 'bar'
  * ```
  *
  * or the factory driven by a custom callback:
  *
  * ```ts
  * class FooClass {
  * 	public label: string;
  *
  * 	constructor( data: { label: string } ) {
  * 		this.label = data.label;
  * 	}
  * }
  *
  * class BarClass {
  * 	public label: string;
  *
  * 	constructor( data: { label: string } ) {
  * 		this.label = data.label;
  * 	}
  * }
  *
  * const source = new Collection<{ label: string }>( { idProperty: 'label' } );
  * const target = new Collection<FooClass | BarClass>();
  *
  * target.bindTo( source ).using( ( item ) => {
  * 	if ( item.label == 'foo' ) {
  * 		return new FooClass( item );
  * 	} else {
  * 		return new BarClass( item );
  * 	}
  * } );
  *
  * source.add( { label: 'foo' } );
  * source.add( { label: 'bar' } );
  *
  * console.log( target.length ); // 2
  * console.log( target.get( 0 ) instanceof FooClass ); // true
  * console.log( target.get( 1 ) instanceof BarClass ); // true
  * ```
  *
  * or the factory out of property name:
  *
  * ```ts
  * const source = new Collection<{ nested: { value: string } }>();
  * const target = new Collection<{ value: string }>();
  *
  * target.bindTo( source ).using( 'nested' );
  *
  * source.add( { nested: { value: 'foo' } } );
  * source.add( { nested: { value: 'bar' } } );
  *
  * console.log( target.length ); // 2
  * console.log( target.get( 0 ).value ); // 'foo'
  * console.log( target.get( 1 ).value ); // 'bar'
  * ```
  *
  * It's possible to skip specified items by returning null value:
  *
  * ```ts
  * const source = new Collection<{ hidden: boolean }>();
  * const target = new Collection<{ hidden: boolean }>();
  *
  * target.bindTo( source ).using( item => {
  * 	if ( item.hidden ) {
  * 		return null;
  * 	}
  *
  * 	return item;
  * } );
  *
  * source.add( { hidden: true } );
  * source.add( { hidden: false } );
  *
  * console.log( source.length ); // 2
  * console.log( target.length ); // 1
  * ```
  *
  * **Note**: {@link #clear} can be used to break the binding.
  *
  * @typeParam S The type of `externalCollection` element.
  * @param externalCollection A collection to be bound.
  * @returns The binding chain object.
  */
  bindTo(e) {
    if (this._bindToCollection)
      throw new T("collection-bind-to-rebind", this);
    return this._bindToCollection = e, {
      as: (n) => {
        this._setUpBindToBinding((r) => new n(r));
      },
      using: (n) => {
        typeof n == "function" ? this._setUpBindToBinding(n) : this._setUpBindToBinding((r) => r[n]);
      }
    };
  }
  /**
  * Finalizes and activates a binding initiated by {@link #bindTo}.
  *
  * @param factory A function which produces collection items.
  */
  _setUpBindToBinding(e) {
    const n = this._bindToCollection, r = (s, o, a) => {
      const l = n._bindToCollection == this, c = n._bindToInternalToExternalMap.get(o);
      if (l && c)
        this._bindToExternalToInternalMap.set(o, c), this._bindToInternalToExternalMap.set(c, o);
      else {
        const u = e(o);
        if (!u) {
          this._skippedIndexesFromExternal.push(a);
          return;
        }
        let d = a;
        for (const f of this._skippedIndexesFromExternal)
          a > f && d--;
        for (const f of n._skippedIndexesFromExternal)
          d >= f && d++;
        this._bindToExternalToInternalMap.set(o, u), this._bindToInternalToExternalMap.set(u, o), this.add(u, d);
        for (let f = 0; f < n._skippedIndexesFromExternal.length; f++)
          d <= n._skippedIndexesFromExternal[f] && n._skippedIndexesFromExternal[f]++;
      }
    };
    for (const s of n)
      r(null, s, n.getIndex(s));
    this.listenTo(n, "add", r), this.listenTo(n, "remove", (s, o, a) => {
      const l = this._bindToExternalToInternalMap.get(o);
      l && this.remove(l), this._skippedIndexesFromExternal = this._skippedIndexesFromExternal.reduce((c, u) => (a < u && c.push(u - 1), a > u && c.push(u), c), []);
    });
  }
  /**
  * Returns an unique id property for a given `item`.
  *
  * The method will generate new id and assign it to the `item` if it doesn't have any.
  *
  * @param item Item to be added.
  */
  _getItemIdBeforeAdding(e) {
    const n = this._idProperty;
    let r;
    if (n in e) {
      if (r = e[n], typeof r != "string")
        throw new T("collection-add-invalid-id", this);
      if (this.get(r))
        throw new T("collection-add-item-already-exists", this);
    } else
      e[n] = r = Jn();
    return r;
  }
  /**
  * Core {@link #remove} method implementation shared in other functions.
  *
  * In contrast this method **does not** fire the {@link #event:change} event.
  *
  * @param subject The item to remove, its id or index in the collection.
  * @returns Returns an array with the removed item and its index.
  * @fires remove
  */
  _remove(e) {
    let n, r, s, o = !1;
    const a = this._idProperty;
    if (typeof e == "string" ? (r = e, s = this._itemMap.get(r), o = !s, s && (n = this._items.indexOf(s))) : typeof e == "number" ? (n = e, s = this._items[n], o = !s, s && (r = s[a])) : (s = e, r = s[a], n = this._items.indexOf(s), o = n == -1 || !this._itemMap.get(r)), o)
      throw new T("collection-remove-404", this);
    this._items.splice(n, 1), this._itemMap.delete(r);
    const l = this._bindToInternalToExternalMap.get(s);
    return this._bindToInternalToExternalMap.delete(s), this._bindToExternalToInternalMap.delete(l), this.fire("remove", s, n), [
      s,
      n
    ];
  }
  /**
  * Iterable interface.
  */
  [Symbol.iterator]() {
    return this._items[Symbol.iterator]();
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Ft(i) {
  const t = i.next();
  return t.done ? null : t.value;
}
class wt extends (/* @__PURE__ */ Xn(/* @__PURE__ */ st())) {
  // @if CK_DEBUG_FOCUSTRACKER // public _label?: string;
  constructor() {
    super();
    /**
    * List of registered DOM elements.
    *
    * @internal
    */
    h(this, "_elements", /* @__PURE__ */ new Set());
    /**
    * List of views with external focus trackers that contribute to the state of this focus tracker.
    *
    * @internal
    */
    h(this, "_externalViews", /* @__PURE__ */ new Set());
    /**
    * Asynchronous blur event timeout.
    */
    h(this, "_blurTimeout", null);
    this.set("isFocused", !1), this.set("focusedElement", null);
  }
  /**
  * List of registered DOM elements.
  *
  * **Note**: The list does do not include elements from {@link #externalViews}.
  */
  get elements() {
    return Array.from(this._elements.values());
  }
  /**
  * List of external focusable views that contribute to the state of this focus tracker. See {@link #add} to learn more.
  */
  get externalViews() {
    return Array.from(this._externalViews.values());
  }
  /**
  * Starts tracking a specified DOM element or a {@link module:ui/view~View} instance.
  *
  * * If a DOM element is passed, the focus tracker listens to the `focus` and `blur` events on this element.
  * Tracked elements are listed in {@link #elements}.
  * * If a {@link module:ui/view~View} instance is passed that has a `FocusTracker` instance ({@link ~ViewWithFocusTracker}),
  * the external focus tracker's state ({@link #isFocused}, {@link #focusedElement}) starts contributing to the current tracker instance.
  * This allows for increasing the "reach" of a focus tracker instance, by connecting two or more focus trackers together when DOM
  * elements they track are located in different subtrees in DOM. External focus trackers are listed in {@link #externalViews}.
  * * If a {@link module:ui/view~View} instance is passed that has no `FocusTracker` (**not** a {@link ~ViewWithFocusTracker}),
  * its {@link module:ui/view~View#element} is used to track focus like any other DOM element.
  */
  add(e) {
    if (_1(e))
      this._addElement(e);
    else if (y1(e))
      this._addView(e);
    else {
      if (!e.element)
        throw new T("focustracker-add-view-missing-element", {
          focusTracker: this,
          view: e
        });
      this._addElement(e.element);
    }
  }
  /**
  * Stops tracking focus in the specified DOM element or a {@link module:ui/view~View view instance}. See {@link #add} to learn more.
  */
  remove(e) {
    _1(e) ? this._removeElement(e) : y1(e) ? this._removeView(e) : this._removeElement(e.element);
  }
  /**
  * Adds a DOM element to the focus tracker and starts listening to the `focus` and `blur` events on it.
  */
  _addElement(e) {
    if (this._elements.has(e))
      throw new T("focustracker-add-element-already-exist", this);
    this.listenTo(e, "focus", () => {
      const n = this.externalViews.find((r) => jR(e, r));
      n ? this._focus(n.element) : this._focus(e);
    }, {
      useCapture: !0
    }), this.listenTo(e, "blur", () => {
      this._blur();
    }, {
      useCapture: !0
    }), this._elements.add(e);
  }
  /**
  * Removes a DOM element from the focus tracker.
  */
  _removeElement(e) {
    this._elements.has(e) && (this.stopListening(e), this._elements.delete(e)), e === this.focusedElement && this._blur();
  }
  /**
  * Adds an external {@link module:ui/view~View view instance} to this focus tracker and makes it contribute to this focus tracker's
  * state either by its `View#element` or by its `View#focusTracker` instance.
  */
  _addView(e) {
    e.element && this._addElement(e.element), this.listenTo(e.focusTracker, "change:focusedElement", () => {
      e.focusTracker.focusedElement ? e.element && this._focus(e.element) : this._blur();
    }), this._externalViews.add(e);
  }
  /**
  * Removes an external {@link module:ui/view~View view instance} from this focus tracker.
  */
  _removeView(e) {
    e.element && this._removeElement(e.element), this.stopListening(e.focusTracker), this._externalViews.delete(e);
  }
  /**
  * Destroys the focus tracker by:
  * - Disabling all event listeners attached to tracked elements or external views.
  * - Removing all tracked elements and views that were previously added.
  */
  destroy() {
    this.stopListening(), this._elements.clear(), this._externalViews.clear(), this.isFocused = !1, this.focusedElement = null;
  }
  /**
  * Stores currently focused element as {@link #focusedElement} and sets {@link #isFocused} `true`.
  */
  _focus(e) {
    this._clearBlurTimeout(), this.focusedElement = e, this.isFocused = !0;
  }
  /**
  * Clears currently {@link #focusedElement} and sets {@link #isFocused} `false`.
  *
  * This method uses `setTimeout()` to change order of `blur` and `focus` events calls, ensuring that moving focus between
  * two elements within a single focus tracker's scope, will not cause `[ blurA, focusB ]` sequence but just `[ focusB ]`.
  * The former would cause a momentary change of `#isFocused` to `false` which is not desired because any logic listening to
  * a focus tracker state would experience UI flashes and glitches as the user focus travels across the UI.
  */
  _blur() {
    this.elements.find((r) => r.contains(document.activeElement)) || this.externalViews.find((r) => r.focusTracker.isFocused && !r.focusTracker._blurTimeout) || (this._clearBlurTimeout(), this._blurTimeout = setTimeout(() => {
      this.focusedElement = null, this.isFocused = !1;
    }, 0));
  }
  /**
  * Clears the asynchronous blur event timeout on demand. See {@link #_blur} to learn more.
  */
  _clearBlurTimeout() {
    clearTimeout(this._blurTimeout), this._blurTimeout = null;
  }
}
function y1(i) {
  return "focusTracker" in i && i.focusTracker instanceof wt;
}
function _1(i) {
  return us(i);
}
function jR(i, t) {
  return C1(i, t) ? !0 : !!t.focusTracker.externalViews.find((e) => C1(i, e));
}
function C1(i, t) {
  return !!t.element && t.element.contains(document.activeElement) && i.contains(t.element);
}
class Ot {
  /**
  * Creates an instance of the keystroke handler.
  */
  constructor() {
    /**
    * Listener used to listen to events for easier keystroke handler destruction.
    */
    h(this, "_listener");
    this._listener = new (Xn())();
  }
  /**
  * Starts listening for `keydown` events from a given emitter.
  */
  listenTo(t) {
    this._listener.listenTo(t, "keydown", (e, n) => {
      this._listener.fire("_keydown:" + Xo(n), n);
    });
  }
  /**
  * Registers a handler for the specified keystroke.
  *
  * @param keystroke Keystroke defined in a format accepted by
  * the {@link module:utils/keyboard~parseKeystroke} function.
  * @param callback A function called with the
  * {@link module:engine/view/observer/keyobserver~KeyEventData key event data} object and
  * a helper function to call both `preventDefault()` and `stopPropagation()` on the underlying event.
  * @param options Additional options.
  */
  set(t, e, n = {}) {
    const r = Kg(t), s = n.priority;
    this._listener.listenTo(this._listener, "_keydown:" + r, (o, a) => {
      n.filter && !n.filter(a) || (e(a, () => {
        a.preventDefault(), a.stopPropagation(), o.stop();
      }), o.return = !0);
    }, {
      priority: s
    });
  }
  /**
  * Triggers a keystroke handler for a specified key combination, if such a keystroke was {@link #set defined}.
  *
  * @param keyEvtData Key event data.
  * @returns Whether the keystroke was handled.
  */
  press(t) {
    return !!this._listener.fire("_keydown:" + Xo(t), t);
  }
  /**
  * Stops listening to `keydown` events from the given emitter.
  */
  stopListening(t) {
    this._listener.stopListening(t);
  }
  /**
  * Destroys the keystroke handler.
  */
  destroy() {
    this.stopListening();
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function WR(i) {
  const t = /* @__PURE__ */ new Map();
  for (const e in i)
    t.set(e, i[e]);
  return t;
}
function gr(i) {
  return ki(i) ? new Map(i) : WR(i);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const UR = 1e4;
function k1(i, t, e, n) {
  if (Math.max(t.length, i.length) > UR)
    return i.slice(0, e).concat(t).concat(i.slice(e + n, i.length));
  {
    const r = Array.from(i);
    return r.splice(e, n, ...t), r;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Yg(i, t) {
  let e;
  function n(...r) {
    n.cancel(), e = setTimeout(() => i(...r), t);
  }
  return n.cancel = () => {
    clearTimeout(e);
  }, n;
}
function qR(i) {
  function t(d) {
    return d.length >= 40 && d.length <= 255 ? "VALID" : "INVALID";
  }
  if (!i)
    return "INVALID";
  let e = "";
  try {
    e = atob(i);
  } catch {
    return "INVALID";
  }
  const n = e.split("-"), r = n[0], s = n[1];
  if (!s)
    return t(i);
  try {
    atob(s);
  } catch {
    try {
      if (atob(r), !atob(r).length)
        return t(i);
    } catch {
      return t(i);
    }
  }
  if (r.length < 40 || r.length > 255)
    return "INVALID";
  let o = "";
  try {
    atob(r), o = atob(s);
  } catch {
    return "INVALID";
  }
  if (o.length !== 8)
    return "INVALID";
  const a = Number(o.substring(0, 4)), l = Number(o.substring(4, 6)) - 1, c = Number(o.substring(6, 8)), u = new Date(a, l, c);
  return u < sR || isNaN(Number(u)) ? "INVALID" : "VALID";
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function GR(i) {
  return !!i && i.length == 1 && /[\u0300-\u036f\u1ab0-\u1aff\u1dc0-\u1dff\u20d0-\u20ff\ufe20-\ufe2f]/.test(i);
}
function KR(i) {
  return !!i && i.length == 1 && /[\ud800-\udbff]/.test(i);
}
function ZR(i) {
  return !!i && i.length == 1 && /[\udc00-\udfff]/.test(i);
}
function Jg(i, t) {
  return KR(i.charAt(t - 1)) && ZR(i.charAt(t));
}
function Xg(i, t) {
  return GR(i.charAt(t));
}
const YR = /* @__PURE__ */ JR();
function O2(i, t) {
  const e = String(i).matchAll(YR);
  return Array.from(e).some((n) => n.index < t && t < n.index + n[0].length);
}
function JR() {
  const i = [
    // Emoji Tag Sequence (ETS)
    new RegExp("\\p{Emoji}[\\u{E0020}-\\u{E007E}]+\\u{E007F}", "u"),
    // Emoji Keycap Sequence
    new RegExp("\\p{Emoji}\\u{FE0F}?\\u{20E3}", "u"),
    // Emoji Presentation Sequence
    new RegExp("\\p{Emoji}\\u{FE0F}", "u"),
    // Single-Character Emoji / Emoji Modifier Sequence
    new RegExp("(?=\\p{General_Category=Other_Symbol})\\p{Emoji}\\p{Emoji_Modifier}*", "u")
  ], t = new RegExp("\\p{Regional_Indicator}{2}", "u").source, e = "(?:" + i.map((r) => r.source).join("|") + ")", n = `${t}|${e}(?:‍${e})*`;
  return new RegExp(n, "ug");
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const bu = /* @__PURE__ */ new WeakMap();
let x1 = !1;
function Qg({ view: i, element: t, text: e, isDirectHost: n = !0, keepOnFocus: r = !1 }) {
  const s = i.document;
  bu.has(s) || (bu.set(s, /* @__PURE__ */ new Map()), s.registerPostFixer((a) => Fh(s, a)), s.on("change:isComposing", () => {
    i.change((a) => Fh(s, a));
  }, {
    priority: "high"
  })), t.is("editableElement") && t.on("change:placeholder", (a, l, c) => {
    o(c);
  }), t.placeholder ? o(t.placeholder) : e && o(e), e && n6();
  function o(a) {
    bu.get(s).set(t, {
      text: a,
      isDirectHost: n,
      keepOnFocus: r,
      hostElement: n ? t : null
    }), i.change((l) => Fh(s, l));
  }
}
function XR(i, t) {
  return t.hasClass("ck-placeholder") ? !1 : (i.addClass("ck-placeholder", t), !0);
}
function QR(i, t) {
  return t.hasClass("ck-placeholder") ? (i.removeClass("ck-placeholder", t), !0) : !1;
}
function e6(i, t) {
  if (!i.isAttached() || Array.from(i.getChildren()).some((o) => !o.is("uiElement")))
    return !1;
  const n = i.document, s = n.selection.anchor;
  return n.isComposing && s && s.parent === i ? !1 : t || !n.isFocused ? !0 : !!s && s.parent !== i;
}
function Fh(i, t) {
  const e = bu.get(i), n = [];
  let r = !1;
  for (const [s, o] of e)
    o.isDirectHost && (n.push(s), E1(t, s, o) && (r = !0));
  for (const [s, o] of e) {
    if (o.isDirectHost)
      continue;
    const a = t6(s);
    a && (n.includes(a) || (o.hostElement = a, E1(t, s, o) && (r = !0)));
  }
  return r;
}
function E1(i, t, e) {
  const { text: n, isDirectHost: r, hostElement: s } = e;
  let o = !1;
  return s.getAttribute("data-placeholder") !== n && (i.setAttribute("data-placeholder", n, s), o = !0), (r || t.childCount == 1) && e6(s, e.keepOnFocus) ? XR(i, s) && (o = !0) : QR(i, s) && (o = !0), o;
}
function t6(i) {
  if (i.childCount) {
    const t = i.getChild(0);
    if (t.is("element") && !t.is("uiElement") && !t.is("attributeElement"))
      return t;
  }
  return null;
}
function n6() {
  x1 || Xe("enableplaceholder-deprecated-text-option"), x1 = !0;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
let eo = class {
  /* istanbul ignore next -- @preserve */
  is() {
    throw new Error("is() method is abstract");
  }
}, to = class extends (/* @__PURE__ */ ht(eo)) {
  /**
  * Creates a tree view node.
  *
  * @param document The document instance to which this node belongs.
  */
  constructor(e) {
    super();
    /**
    * The document instance to which this node belongs.
    */
    h(this, "document");
    /**
    * Parent element. Null by default. Set by {@link module:engine/view/element~Element#_insertChild}.
    */
    h(this, "parent");
    this.document = e, this.parent = null;
  }
  /**
  * Index of the node in the parent element or null if the node has no parent.
  *
  * Accessing this property throws an error if this node's parent element does not contain it.
  * This means that view tree got broken.
  */
  get index() {
    let e;
    if (!this.parent)
      return null;
    if ((e = this.parent.getChildIndex(this)) == -1)
      throw new T("view-node-not-found-in-parent", this);
    return e;
  }
  /**
  * Node's next sibling, or `null` if it is the last child.
  */
  get nextSibling() {
    const e = this.index;
    return e !== null && this.parent.getChild(e + 1) || null;
  }
  /**
  * Node's previous sibling, or `null` if it is the first child.
  */
  get previousSibling() {
    const e = this.index;
    return e !== null && this.parent.getChild(e - 1) || null;
  }
  /**
  * Top-most ancestor of the node. If the node has no parent it is the root itself.
  */
  get root() {
    let e = this;
    for (; e.parent; )
      e = e.parent;
    return e;
  }
  /**
  * Returns true if the node is in a tree rooted in the document (is a descendant of one of its roots).
  */
  isAttached() {
    return this.root.is("rootElement");
  }
  /**
  * Gets a path to the node. The path is an array containing indices of consecutive ancestors of this node,
  * beginning from {@link module:engine/view/node~Node#root root}, down to this node's index.
  *
  * ```ts
  * const abc = downcastWriter.createText( 'abc' );
  * const foo = downcastWriter.createText( 'foo' );
  * const h1 = downcastWriter.createElement( 'h1', null, downcastWriter.createText( 'header' ) );
  * const p = downcastWriter.createElement( 'p', null, [ abc, foo ] );
  * const div = downcastWriter.createElement( 'div', null, [ h1, p ] );
  * foo.getPath(); // Returns [ 1, 3 ]. `foo` is in `p` which is in `div`. `p` starts at offset 1, while `foo` at 3.
  * h1.getPath(); // Returns [ 0 ].
  * div.getPath(); // Returns [].
  * ```
  *
  * @returns The path.
  */
  getPath() {
    const e = [];
    let n = this;
    for (; n.parent; )
      e.unshift(n.index), n = n.parent;
    return e;
  }
  /**
  * Returns ancestors array of this node.
  *
  * @param options Options object.
  * @param options.includeSelf When set to `true` this node will be also included in parent's array.
  * @param options.parentFirst When set to `true`, array will be sorted from node's parent to root element,
  * otherwise root element will be the first item in the array.
  * @returns Array with ancestors.
  */
  getAncestors(e = {}) {
    const n = [];
    let r = e.includeSelf ? this : this.parent;
    for (; r; )
      n[e.parentFirst ? "push" : "unshift"](r), r = r.parent;
    return n;
  }
  /**
  * Returns a {@link module:engine/view/element~Element} or {@link module:engine/view/documentfragment~DocumentFragment}
  * which is a common ancestor of both nodes.
  *
  * @param node The second node.
  * @param options Options object.
  * @param options.includeSelf When set to `true` both nodes will be considered "ancestors" too.
  * Which means that if e.g. node A is inside B, then their common ancestor will be B.
  */
  getCommonAncestor(e, n = {}) {
    const r = this.getAncestors(n), s = e.getAncestors(n);
    let o = 0;
    for (; r[o] == s[o] && r[o]; )
      o++;
    return o === 0 ? null : r[o - 1];
  }
  /**
  * Returns whether this node is before given node. `false` is returned if nodes are in different trees (for example,
  * in different {@link module:engine/view/documentfragment~DocumentFragment}s).
  *
  * @param node Node to compare with.
  */
  isBefore(e) {
    if (this == e || this.root !== e.root)
      return !1;
    const n = this.getPath(), r = e.getPath(), s = Fn(n, r);
    switch (s) {
      case "prefix":
        return !0;
      case "extension":
        return !1;
      default:
        return n[s] < r[s];
    }
  }
  /**
  * Returns whether this node is after given node. `false` is returned if nodes are in different trees (for example,
  * in different {@link module:engine/view/documentfragment~DocumentFragment}s).
  *
  * @param node Node to compare with.
  */
  isAfter(e) {
    return this == e || this.root !== e.root ? !1 : !this.isBefore(e);
  }
  /**
  * Removes node from parent.
  *
  * @internal
  */
  _remove() {
    this.parent._removeChildren(this.index);
  }
  /**
  * @internal
  * @param type Type of the change.
  * @param node Changed node.
  * @fires change
  */
  _fireChange(e, n) {
    this.fire(`change:${e}`, n), this.parent && this.parent._fireChange(e, n);
  }
  /**
  * Custom toJSON method to solve child-parent circular dependencies.
  *
  * @returns Clone of this object with the parent property removed.
  */
  toJSON() {
    const e = J_(this);
    return delete e.parent, e;
  }
};
to.prototype.is = function(i) {
  return i === "node" || i === "view:node";
};
let St = class nm extends to {
  /**
  * Creates a tree view text node.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createText
  * @internal
  * @param document The document instance to which this text node belongs.
  * @param data The text's data.
  */
  constructor(e, n) {
    super(e);
    /**
    * The text content.
    *
    * Setting the data fires the {@link module:engine/view/node~Node#event:change:text change event}.
    */
    h(this, "_textData");
    this._textData = n;
  }
  /**
  * The text content.
  */
  get data() {
    return this._textData;
  }
  /**
  * The `_data` property is controlled by a getter and a setter.
  *
  * The getter is required when using the addition assignment operator on protected property:
  *
  * ```ts
  * const foo = downcastWriter.createText( 'foo' );
  * const bar = downcastWriter.createText( 'bar' );
  *
  * foo._data += bar.data;   // executes: `foo._data = foo._data + bar.data`
  * console.log( foo.data ); // prints: 'foobar'
  * ```
  *
  * If the protected getter didn't exist, `foo._data` will return `undefined` and result of the merge will be invalid.
  *
  * The setter sets data and fires the {@link module:engine/view/node~Node#event:change:text change event}.
  *
  * @internal
  */
  get _data() {
    return this.data;
  }
  set _data(e) {
    this._fireChange("text", this), this._textData = e;
  }
  /**
  * Checks if this text node is similar to other text node.
  * Both nodes should have the same data to be considered as similar.
  *
  * @param otherNode Node to check if it is same as this node.
  */
  isSimilar(e) {
    return e instanceof nm ? this === e || this.data === e.data : !1;
  }
  /**
  * Clones this node.
  *
  * @internal
  * @returns Text node that is a clone of this node.
  */
  _clone() {
    return new nm(this.document, this.data);
  }
};
St.prototype.is = function(i) {
  return i === "$text" || i === "view:$text" || // This are legacy values kept for backward compatibility.
  i === "text" || i === "view:text" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "node" || i === "view:node";
};
let cr = class extends eo {
  /**
  * Creates a text proxy.
  *
  * @internal
  * @param textNode Text node which part is represented by this text proxy.
  * @param offsetInText Offset in {@link module:engine/view/textproxy~TextProxy#textNode text node}
  * from which the text proxy starts.
  * @param length Text proxy length, that is how many text node's characters, starting from `offsetInText` it represents.
  * @constructor
  */
  constructor(e, n, r) {
    super();
    /**
    * Reference to the {@link module:engine/view/text~Text} element which TextProxy is a substring.
    */
    h(this, "textNode");
    /**
    * Text data represented by this text proxy.
    */
    h(this, "data");
    /**
    * Offset in the `textNode` where this `TextProxy` instance starts.
    */
    h(this, "offsetInText");
    if (this.textNode = e, n < 0 || n > e.data.length)
      throw new T("view-textproxy-wrong-offsetintext", this);
    if (r < 0 || n + r > e.data.length)
      throw new T("view-textproxy-wrong-length", this);
    this.data = e.data.substring(n, n + r), this.offsetInText = n;
  }
  /**
  * Offset size of this node.
  */
  get offsetSize() {
    return this.data.length;
  }
  /**
  * Flag indicating whether `TextProxy` instance covers only part of the original {@link module:engine/view/text~Text text node}
  * (`true`) or the whole text node (`false`).
  *
  * This is `false` when text proxy starts at the very beginning of {@link module:engine/view/textproxy~TextProxy#textNode textNode}
  * ({@link module:engine/view/textproxy~TextProxy#offsetInText offsetInText} equals `0`) and text proxy sizes is equal to
  * text node size.
  */
  get isPartial() {
    return this.data.length !== this.textNode.data.length;
  }
  /**
  * Parent of this text proxy, which is same as parent of text node represented by this text proxy.
  */
  get parent() {
    return this.textNode.parent;
  }
  /**
  * Root of this text proxy, which is same as root of text node represented by this text proxy.
  */
  get root() {
    return this.textNode.root;
  }
  /**
  * {@link module:engine/view/document~Document View document} that owns this text proxy, or `null` if the text proxy is inside
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment}.
  */
  get document() {
    return this.textNode.document;
  }
  /**
  * Returns ancestors array of this text proxy.
  *
  * @param options Options object.
  * @param options.includeSelf When set to `true`, textNode will be also included in parent's array.
  * @param options.parentFirst When set to `true`, array will be sorted from text proxy parent to
  * root element, otherwise root element will be the first item in the array.
  * @returns Array with ancestors.
  */
  getAncestors(e = {}) {
    const n = [];
    let r = e.includeSelf ? this.textNode : this.parent;
    for (; r !== null; )
      n[e.parentFirst ? "push" : "unshift"](r), r = r.parent;
    return n;
  }
};
cr.prototype.is = function(i) {
  return i === "$textProxy" || i === "view:$textProxy" || // This are legacy values kept for backward compatibility.
  i === "textProxy" || i === "view:textProxy";
};
class xi {
  /**
  * Creates new instance of Matcher.
  *
  * @param pattern Match patterns. See {@link module:engine/view/matcher~Matcher#add add method} for more information.
  */
  constructor(...t) {
    h(this, "_patterns", []);
    this.add(...t);
  }
  /**
  * Adds pattern or patterns to matcher instance.
  *
  * ```ts
  * // String.
  * matcher.add( 'div' );
  *
  * // Regular expression.
  * matcher.add( /^\w/ );
  *
  * // Single class.
  * matcher.add( {
  * 	classes: 'foobar'
  * } );
  * ```
  *
  * See {@link module:engine/view/matcher~MatcherPattern} for more examples.
  *
  * Multiple patterns can be added in one call:
  *
  * ```ts
  * matcher.add( 'div', { classes: 'foobar' } );
  * ```
  *
  * @param pattern Object describing pattern details. If string or regular expression
  * is provided it will be used to match element's name. Pattern can be also provided in a form
  * of a function - then this function will be called with each {@link module:engine/view/element~Element element} as a parameter.
  * Function's return value will be stored under `match` key of the object returned from
  * {@link module:engine/view/matcher~Matcher#match match} or {@link module:engine/view/matcher~Matcher#matchAll matchAll} methods.
  */
  add(...t) {
    for (let e of t)
      (typeof e == "string" || e instanceof RegExp) && (e = {
        name: e
      }), this._patterns.push(e);
  }
  /**
  * Matches elements for currently stored patterns. Returns match information about first found
  * {@link module:engine/view/element~Element element}, otherwise returns `null`.
  *
  * Example of returned object:
  *
  * ```ts
  * {
  * 	element: <instance of found element>,
  * 	pattern: <pattern used to match found element>,
  * 	match: {
  * 		name: true,
  * 		attributes: [ 'title', 'href' ],
  * 		classes: [ 'foo' ],
  * 		styles: [ 'color', 'position' ]
  * 	}
  * }
  * ```
  *
  * @see module:engine/view/matcher~Matcher#add
  * @see module:engine/view/matcher~Matcher#matchAll
  * @param element View element to match against stored patterns.
  */
  match(...t) {
    for (const e of t)
      for (const n of this._patterns) {
        const r = T1(e, n);
        if (r)
          return {
            element: e,
            pattern: n,
            match: r
          };
      }
    return null;
  }
  /**
  * Matches elements for currently stored patterns. Returns array of match information with all found
  * {@link module:engine/view/element~Element elements}. If no element is found - returns `null`.
  *
  * @see module:engine/view/matcher~Matcher#add
  * @see module:engine/view/matcher~Matcher#match
  * @param element View element to match against stored patterns.
  * @returns Array with match information about found elements or `null`. For more information
  * see {@link module:engine/view/matcher~Matcher#match match method} description.
  */
  matchAll(...t) {
    const e = [];
    for (const n of t)
      for (const r of this._patterns) {
        const s = T1(n, r);
        s && e.push({
          element: n,
          pattern: r,
          match: s
        });
      }
    return e.length > 0 ? e : null;
  }
  /**
  * Returns the name of the element to match if there is exactly one pattern added to the matcher instance
  * and it matches element name defined by `string` (not `RegExp`). Otherwise, returns `null`.
  *
  * @returns Element name trying to match.
  */
  getElementName() {
    if (this._patterns.length !== 1)
      return null;
    const t = this._patterns[0], e = t.name;
    return typeof t != "function" && e && !(e instanceof RegExp) ? e : null;
  }
}
function T1(i, t) {
  if (typeof t == "function")
    return t(i);
  const e = {};
  return t.name && (e.name = i6(t.name, i.name), !e.name) || t.attributes && (e.attributes = a6(t.attributes, i), !e.attributes) || t.classes && (e.classes = l6(t.classes, i), !e.classes) || t.styles && (e.styles = c6(t.styles, i), !e.styles) ? null : e;
}
function i6(i, t) {
  return i instanceof RegExp ? !!t.match(i) : i === t;
}
function ep(i, t, e) {
  const n = r6(i), r = Array.from(t), s = [];
  if (n.forEach(([o, a]) => {
    r.forEach((l) => {
      s6(o, l) && o6(a, l, e) && s.push(l);
    });
  }), !(!n.length || s.length < n.length))
    return s;
}
function r6(i) {
  return Array.isArray(i) ? i.map((t) => oi(t) ? ((t.key === void 0 || t.value === void 0) && Xe("matcher-pattern-missing-key-or-value", t), [
    t.key,
    t.value
  ]) : [
    t,
    !0
  ]) : oi(i) ? Object.entries(i) : [
    [
      i,
      !0
    ]
  ];
}
function s6(i, t) {
  return i === !0 || i === t || i instanceof RegExp && t.match(i);
}
function o6(i, t, e) {
  if (i === !0)
    return !0;
  const n = e(t);
  return i === n || i instanceof RegExp && !!String(n).match(i);
}
function a6(i, t) {
  const e = new Set(t.getAttributeKeys());
  return oi(i) ? (i.style !== void 0 && Xe("matcher-pattern-deprecated-attributes-style-key", i), i.class !== void 0 && Xe("matcher-pattern-deprecated-attributes-class-key", i)) : (e.delete("style"), e.delete("class")), ep(i, e, (n) => t.getAttribute(n));
}
function l6(i, t) {
  return ep(
    i,
    t.getClassNames(),
    /* istanbul ignore next -- @preserve */
    () => {
    }
  );
}
function c6(i, t) {
  return ep(i, t.getStyleNames(!0), (e) => t.getStyle(e));
}
class Fu {
  /**
  * Creates Styles instance.
  */
  constructor(t) {
    /**
    * Keeps an internal representation of styles map. Normalized styles are kept as object tree to allow unified modification and
    * value access model using lodash's get, set, unset, etc methods.
    *
    * When no style processor rules are defined it acts as simple key-value storage.
    */
    h(this, "_styles");
    /**
    * An instance of the {@link module:engine/view/stylesmap~StylesProcessor}.
    */
    h(this, "_styleProcessor");
    this._styles = {}, this._styleProcessor = t;
  }
  /**
  * Returns true if style map has no styles set.
  */
  get isEmpty() {
    return !Object.entries(this._styles).length;
  }
  /**
  * Number of styles defined.
  */
  get size() {
    return this.isEmpty ? 0 : this.getStyleNames().length;
  }
  /**
  * Set styles map to a new value.
  *
  * ```ts
  * styles.setTo( 'border:1px solid blue;margin-top:1px;' );
  * ```
  */
  setTo(t) {
    this.clear();
    const e = d6(t);
    for (const [n, r] of e)
      this._styleProcessor.toNormalizedForm(n, r, this._styles);
  }
  /**
  * Checks if a given style is set.
  *
  * ```ts
  * styles.setTo( 'margin-left:1px;' );
  *
  * styles.has( 'margin-left' );    // -> true
  * styles.has( 'padding' );        // -> false
  * ```
  *
  * **Note**: This check supports normalized style names.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * styles.setTo( 'margin:2px;' );
  *
  * styles.has( 'margin' );         // -> true
  * styles.has( 'margin-top' );     // -> true
  * styles.has( 'margin-left' );    // -> true
  *
  * styles.remove( 'margin-top' );
  *
  * styles.has( 'margin' );         // -> false
  * styles.has( 'margin-top' );     // -> false
  * styles.has( 'margin-left' );    // -> true
  * ```
  *
  * @param name Style name.
  */
  has(t) {
    if (this.isEmpty)
      return !1;
    const n = this._styleProcessor.getReducedForm(t, this._styles).find(([r]) => r === t);
    return Array.isArray(n);
  }
  set(t, e) {
    if (vt(t))
      for (const [n, r] of Object.entries(t))
        this._styleProcessor.toNormalizedForm(n, r, this._styles);
    else
      this._styleProcessor.toNormalizedForm(t, e, this._styles);
  }
  /**
  * Removes given style.
  *
  * ```ts
  * styles.setTo( 'background:#f00;margin-right:2px;' );
  *
  * styles.remove( 'background' );
  *
  * styles.toString();   // -> 'margin-right:2px;'
  * ```
  *
  * ***Note**:* This method uses {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules
  * enabled style processor rules} to normalize passed values.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * styles.setTo( 'margin:1px' );
  *
  * styles.remove( 'margin-top' );
  * styles.remove( 'margin-right' );
  *
  * styles.toString(); // -> 'margin-bottom:1px;margin-left:1px;'
  * ```
  *
  * @param name Style name.
  */
  remove(t) {
    const e = im(t);
    $4(this._styles, e), delete this._styles[t], this._cleanEmptyObjectsOnPath(e);
  }
  /**
  * Returns a normalized style object or a single value.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * const styles = new Styles();
  * styles.setTo( 'margin:1px 2px 3em;' );
  *
  * styles.getNormalized( 'margin' );
  * // will log:
  * // {
  * //     top: '1px',
  * //     right: '2px',
  * //     bottom: '3em',
  * //     left: '2px'     // normalized value from margin shorthand
  * // }
  *
  * styles.getNormalized( 'margin-left' ); // -> '2px'
  * ```
  *
  * **Note**: This method will only return normalized styles if a style processor was defined.
  *
  * @param name Style name.
  */
  getNormalized(t) {
    return this._styleProcessor.getNormalized(t, this._styles);
  }
  /**
  * Returns a normalized style string. Styles are sorted by name.
  *
  * ```ts
  * styles.set( 'margin' , '1px' );
  * styles.set( 'background', '#f00' );
  *
  * styles.toString(); // -> 'background:#f00;margin:1px;'
  * ```
  *
  * **Note**: This method supports normalized styles if defined.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * styles.set( 'margin' , '1px' );
  * styles.set( 'background', '#f00' );
  * styles.remove( 'margin-top' );
  * styles.remove( 'margin-right' );
  *
  * styles.toString(); // -> 'background:#f00;margin-bottom:1px;margin-left:1px;'
  * ```
  */
  toString() {
    return this.isEmpty ? "" : this.getStylesEntries().map((t) => t.join(":")).sort().join(";") + ";";
  }
  /**
  * Returns property as a value string or undefined if property is not set.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * const styles = new Styles();
  * styles.setTo( 'margin:1px;' );
  * styles.set( 'margin-bottom', '3em' );
  *
  * styles.getAsString( 'margin' ); // -> 'margin: 1px 1px 3em;'
  * ```
  *
  * Note, however, that all sub-values must be set for the longhand property name to return a value:
  *
  * ```ts
  * const styles = new Styles();
  * styles.setTo( 'margin:1px;' );
  * styles.remove( 'margin-bottom' );
  *
  * styles.getAsString( 'margin' ); // -> undefined
  * ```
  *
  * In the above scenario, it is not possible to return a `margin` value, so `undefined` is returned.
  * Instead, you should use:
  *
  * ```ts
  * const styles = new Styles();
  * styles.setTo( 'margin:1px;' );
  * styles.remove( 'margin-bottom' );
  *
  * for ( const styleName of styles.getStyleNames() ) {
  * 	console.log( styleName, styles.getAsString( styleName ) );
  * }
  * // 'margin-top', '1px'
  * // 'margin-right', '1px'
  * // 'margin-left', '1px'
  * ```
  *
  * In general, it is recommend to iterate over style names like in the example above. This way, you will always get all
  * the currently set style values. So, if all the 4 margin values would be set
  * the for-of loop above would yield only `'margin'`, `'1px'`:
  *
  * ```ts
  * const styles = new Styles();
  * styles.setTo( 'margin:1px;' );
  *
  * for ( const styleName of styles.getStyleNames() ) {
  * 	console.log( styleName, styles.getAsString( styleName ) );
  * }
  * // 'margin', '1px'
  * ```
  *
  * **Note**: To get a normalized version of a longhand property use the {@link #getNormalized `#getNormalized()`} method.
  */
  getAsString(t) {
    if (this.isEmpty)
      return;
    if (this._styles[t] && !vt(this._styles[t]))
      return this._styles[t];
    const n = this._styleProcessor.getReducedForm(t, this._styles).find(([r]) => r === t);
    if (Array.isArray(n))
      return n[1];
  }
  /**
  * Returns all style properties names as they would appear when using {@link #toString `#toString()`}.
  *
  * When `expand` is set to true and there's a shorthand style property set, it will also return all equivalent styles:
  *
  * ```ts
  * stylesMap.setTo( 'margin: 1em' )
  * ```
  *
  * will be expanded to:
  *
  * ```ts
  * [ 'margin', 'margin-top', 'margin-right', 'margin-bottom', 'margin-left' ]
  * ```
  *
  * @param expand Expand shorthand style properties and all return equivalent style representations.
  */
  getStyleNames(t = !1) {
    return this.isEmpty ? [] : t ? this._styleProcessor.getStyleNames(this._styles) : this.getStylesEntries().map(([n]) => n);
  }
  /**
  * Removes all styles.
  */
  clear() {
    this._styles = {};
  }
  /**
  * Returns normalized styles entries for further processing.
  */
  getStylesEntries() {
    const t = [], e = Object.keys(this._styles);
    for (const n of e)
      t.push(...this._styleProcessor.getReducedForm(n, this._styles));
    return t;
  }
  /**
  * Removes empty objects upon removing an entry from internal object.
  */
  _cleanEmptyObjectsOnPath(t) {
    const e = t.split(".");
    if (!(e.length > 1))
      return;
    const r = e.splice(0, e.length - 1).join("."), s = Pl(this._styles, r);
    if (!s)
      return;
    !Object.keys(s).length && this.remove(r);
  }
}
class u6 {
  /**
  * Creates StylesProcessor instance.
  *
  * @internal
  */
  constructor() {
    h(this, "_normalizers");
    h(this, "_extractors");
    h(this, "_reducers");
    h(this, "_consumables");
    this._normalizers = /* @__PURE__ */ new Map(), this._extractors = /* @__PURE__ */ new Map(), this._reducers = /* @__PURE__ */ new Map(), this._consumables = /* @__PURE__ */ new Map();
  }
  /**
  * Parse style string value to a normalized object and appends it to styles object.
  *
  * ```ts
  * const styles = {};
  *
  * stylesProcessor.toNormalizedForm( 'margin', '1px', styles );
  *
  * // styles will consist: { margin: { top: '1px', right: '1px', bottom: '1px', left: '1px; } }
  * ```
  *
  * **Note**: To define normalizer callbacks use {@link #setNormalizer}.
  *
  * @param name Name of style property.
  * @param propertyValue Value of style property.
  * @param styles Object holding normalized styles.
  */
  toNormalizedForm(t, e, n) {
    if (vt(e)) {
      $h(n, im(t), e);
      return;
    }
    if (this._normalizers.has(t)) {
      const r = this._normalizers.get(t), { path: s, value: o } = r(e);
      $h(n, s, o);
    } else
      $h(n, t, e);
  }
  /**
  * Returns a normalized version of a style property.
  *
  * ```ts
  * const styles = {
  * 	margin: { top: '1px', right: '1px', bottom: '1px', left: '1px; },
  * 	background: { color: '#f00' }
  * };
  *
  * stylesProcessor.getNormalized( 'background' );
  * // will return: { color: '#f00' }
  *
  * stylesProcessor.getNormalized( 'margin-top' );
  * // will return: '1px'
  * ```
  *
  * **Note**: In some cases extracting single value requires defining an extractor callback {@link #setExtractor}.
  *
  * @param name Name of style property.
  * @param styles Object holding normalized styles.
  */
  getNormalized(t, e) {
    if (!t)
      return Ug({}, e);
    if (e[t] !== void 0)
      return e[t];
    if (this._extractors.has(t)) {
      const n = this._extractors.get(t);
      if (typeof n == "string")
        return Pl(e, n);
      const r = n(t, e);
      if (r)
        return r;
    }
    return Pl(e, im(t));
  }
  /**
  * Returns a reduced form of style property form normalized object.
  *
  * For default margin reducer, the below code:
  *
  * ```ts
  * stylesProcessor.getReducedForm( 'margin', {
  * 	margin: { top: '1px', right: '1px', bottom: '2px', left: '1px; }
  * } );
  * ```
  *
  * will return:
  *
  * ```ts
  * [
  * 	[ 'margin', '1px 1px 2px' ]
  * ]
  * ```
  *
  * because it might be represented as a shorthand 'margin' value. However if one of margin long hand values is missing it should return:
  *
  * ```ts
  * [
  * 	[ 'margin-top', '1px' ],
  * 	[ 'margin-right', '1px' ],
  * 	[ 'margin-bottom', '2px' ]
  * 	// the 'left' value is missing - cannot use 'margin' shorthand.
  * ]
  * ```
  *
  * **Note**: To define reducer callbacks use {@link #setReducer}.
  *
  * @param name Name of style property.
  */
  getReducedForm(t, e) {
    const n = this.getNormalized(t, e);
    return n === void 0 ? [] : this._reducers.has(t) ? this._reducers.get(t)(n) : [
      [
        t,
        n
      ]
    ];
  }
  /**
  * Return all style properties. Also expand shorthand properties (e.g. `margin`, `background`) if respective extractor is available.
  *
  * @param styles Object holding normalized styles.
  */
  getStyleNames(t) {
    const e = /* @__PURE__ */ new Set();
    for (const n of this._consumables.keys()) {
      const r = this.getNormalized(n, t);
      r && (typeof r != "object" || Object.keys(r).length) && e.add(n);
    }
    for (const n of Object.keys(t))
      e.add(n);
    return Array.from(e);
  }
  /**
  * Returns related style names.
  *
  * ```ts
  * stylesProcessor.getRelatedStyles( 'margin' );
  * // will return: [ 'margin-top', 'margin-right', 'margin-bottom', 'margin-left' ];
  *
  * stylesProcessor.getRelatedStyles( 'margin-top' );
  * // will return: [ 'margin' ];
  * ```
  *
  * **Note**: To define new style relations load an existing style processor or use
  * {@link module:engine/view/stylesmap~StylesProcessor#setStyleRelation `StylesProcessor.setStyleRelation()`}.
  */
  getRelatedStyles(t) {
    return this._consumables.get(t) || [];
  }
  /**
  * Adds a normalizer method for a style property.
  *
  * A normalizer returns describing how the value should be normalized.
  *
  * For instance 'margin' style is a shorthand for four margin values:
  *
  * - 'margin-top'
  * - 'margin-right'
  * - 'margin-bottom'
  * - 'margin-left'
  *
  * and can be written in various ways if some values are equal to others. For instance `'margin: 1px 2em;'` is a shorthand for
  * `'margin-top: 1px;margin-right: 2em;margin-bottom: 1px;margin-left: 2em'`.
  *
  * A normalizer should parse various margin notations as a single object:
  *
  * ```ts
  * const styles = {
  * 	margin: {
  * 		top: '1px',
  * 		right: '2em',
  * 		bottom: '1px',
  * 		left: '2em'
  * 	}
  * };
  * ```
  *
  * Thus a normalizer for 'margin' style should return an object defining style path and value to store:
  *
  * ```ts
  * const returnValue = {
  * 	path: 'margin',
  * 	value: {
  * 		top: '1px',
  * 		right: '2em',
  * 		bottom: '1px',
  * 		left: '2em'
  * 	}
  * };
  * ```
  *
  * Additionally to fully support all margin notations there should be also defined 4 normalizers for longhand margin notations. Below
  * is an example for 'margin-top' style property normalizer:
  *
  * ```ts
  * stylesProcessor.setNormalizer( 'margin-top', valueString => {
  * 	return {
  * 		path: 'margin.top',
  * 		value: valueString
  * 	}
  * } );
  * ```
  */
  setNormalizer(t, e) {
    this._normalizers.set(t, e);
  }
  /**
  * Adds a extractor callback for a style property.
  *
  * Most normalized style values are stored as one level objects. It is assumed that `'margin-top'` style will be stored as:
  *
  * ```ts
  * const styles = {
  * 	margin: {
  * 		top: 'value'
  * 	}
  * }
  * ```
  *
  * However, some styles can have conflicting notations and thus it might be harder to extract a style value from shorthand. For instance
  * the 'border-top-style' can be defined using `'border-top:solid'`, `'border-style:solid none none none'` or by `'border:solid'`
  * shorthands. The default border styles processors stores styles as:
  *
  * ```ts
  * const styles = {
  * 	border: {
  * 		style: {
  * 			top: 'solid'
  * 		}
  * 	}
  * }
  * ```
  *
  * as it is better to modify border style independently from other values. On the other part the output of the border might be
  * desired as `border-top`, `border-left`, etc notation.
  *
  * In the above example an extractor should return a side border value that combines style, color and width:
  *
  * ```ts
  * styleProcessor.setExtractor( 'border-top', styles => {
  * 	return {
  * 		color: styles.border.color.top,
  * 		style: styles.border.style.top,
  * 		width: styles.border.width.top
  * 	}
  * } );
  * ```
  *
  * @param callbackOrPath Callback that return a requested value or path string for single values.
  */
  setExtractor(t, e) {
    this._extractors.set(t, e);
  }
  /**
  * Adds a reducer callback for a style property.
  *
  * Reducer returns a minimal notation for given style name. For longhand properties it is not required to write a reducer as
  * by default the direct value from style path is taken.
  *
  * For shorthand styles a reducer should return minimal style notation either by returning single name-value tuple or multiple tuples
  * if a shorthand cannot be used. For instance for a margin shorthand a reducer might return:
  *
  * ```ts
  * const marginShortHandTuple = [
  * 	[ 'margin', '1px 1px 2px' ]
  * ];
  * ```
  *
  * or a longhand tuples for defined values:
  *
  * ```ts
  * // Considering margin.bottom and margin.left are undefined.
  * const marginLonghandsTuples = [
  * 	[ 'margin-top', '1px' ],
  * 	[ 'margin-right', '1px' ]
  * ];
  * ```
  *
  * A reducer obtains a normalized style value:
  *
  * ```ts
  * // Simplified reducer that always outputs 4 values which are always present:
  * stylesProcessor.setReducer( 'margin', margin => {
  * 	return [
  * 		[ 'margin', `${ margin.top } ${ margin.right } ${ margin.bottom } ${ margin.left }` ]
  * 	]
  * } );
  * ```
  */
  setReducer(t, e) {
    this._reducers.set(t, e);
  }
  /**
  * Defines a style shorthand relation to other style notations.
  *
  * ```ts
  * stylesProcessor.setStyleRelation( 'margin', [
  * 	'margin-top',
  * 	'margin-right',
  * 	'margin-bottom',
  * 	'margin-left'
  * ] );
  * ```
  *
  * This enables expanding of style names for shorthands. For instance, if defined,
  * {@link module:engine/conversion/viewconsumable~ViewConsumable view consumable} items are automatically created
  * for long-hand margin style notation alongside the `'margin'` item.
  *
  * This means that when an element being converted has a style `margin`, a converter for `margin-left` will work just
  * fine since the view consumable will contain a consumable `margin-left` item (thanks to the relation) and
  * `element.getStyle( 'margin-left' )` will work as well assuming that the style processor was correctly configured.
  * However, once `margin-left` is consumed, `margin` will not be consumable anymore.
  */
  setStyleRelation(t, e) {
    this._mapStyleNames(t, e);
    for (const n of e)
      this._mapStyleNames(n, [
        t
      ]);
  }
  /**
  * Set two-way binding of style names.
  */
  _mapStyleNames(t, e) {
    this._consumables.has(t) || this._consumables.set(t, []), this._consumables.get(t).push(...e);
  }
}
function d6(i) {
  let t = null, e = 0, n = 0, r = null;
  const s = /* @__PURE__ */ new Map();
  if (i === "")
    return s;
  i.charAt(i.length - 1) != ";" && (i = i + ";");
  for (let o = 0; o < i.length; o++) {
    const a = i.charAt(o);
    if (t === null)
      switch (a) {
        case ":":
          r || (r = i.substr(e, o - e), n = o + 1);
          break;
        case '"':
        case "'":
          t = a;
          break;
        case ";": {
          const l = i.substr(n, o - n);
          r && s.set(r.trim(), l.trim()), r = null, e = o + 1;
          break;
        }
      }
    else a === t && (t = null);
  }
  return s;
}
function im(i) {
  return i.replace("-", ".");
}
function $h(i, t, e) {
  let n = e;
  vt(e) && (n = Ug({}, Pl(i, t), e)), N4(i, t, n);
}
let Ji = class V2 extends to {
  /**
  * Creates a view element.
  *
  * Attributes can be passed in various formats:
  *
  * ```ts
  * new Element( viewDocument, 'div', { class: 'editor', contentEditable: 'true' } ); // object
  * new Element( viewDocument, 'div', [ [ 'class', 'editor' ], [ 'contentEditable', 'true' ] ] ); // map-like iterator
  * new Element( viewDocument, 'div', mapOfAttributes ); // map
  * ```
  *
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(e, n, r, s) {
    super(e);
    /**
    * Name of the element.
    */
    h(this, "name");
    /**
    * A list of attribute names that should be rendered in the editing pipeline even though filtering mechanisms
    * implemented in the {@link module:engine/view/domconverter~DomConverter} (for instance,
    * {@link module:engine/view/domconverter~DomConverter#shouldRenderAttribute}) would filter them out.
    *
    * These attributes can be specified as an option when the element is created by
    * the {@link module:engine/view/downcastwriter~DowncastWriter}. To check whether an unsafe an attribute should
    * be permitted, use the {@link #shouldRenderUnsafeAttribute} method.
    *
    * @internal
    */
    h(this, "_unsafeAttributesToRender", []);
    /**
    * Map of attributes, where attributes names are keys and attributes values are values.
    */
    h(this, "_attrs");
    /**
    * Array of child nodes.
    */
    h(this, "_children");
    /**
    * Set of classes associated with element instance.
    */
    h(this, "_classes");
    /**
    * Normalized styles.
    */
    h(this, "_styles");
    /**
    * Map of custom properties.
    * Custom properties can be added to element instance, will be cloned but not rendered into DOM.
    */
    h(this, "_customProperties", /* @__PURE__ */ new Map());
    if (this.name = n, this._attrs = h6(r), this._children = [], s && this._insertChild(0, s), this._classes = /* @__PURE__ */ new Set(), this._attrs.has("class")) {
      const o = this._attrs.get("class");
      S1(this._classes, o), this._attrs.delete("class");
    }
    this._styles = new Fu(this.document.stylesProcessor), this._attrs.has("style") && (this._styles.setTo(this._attrs.get("style")), this._attrs.delete("style"));
  }
  /**
  * Number of element's children.
  */
  get childCount() {
    return this._children.length;
  }
  /**
  * Is `true` if there are no nodes inside this element, `false` otherwise.
  */
  get isEmpty() {
    return this._children.length === 0;
  }
  /**
  * Gets child at the given index.
  *
  * @param index Index of child.
  * @returns Child node.
  */
  getChild(e) {
    return this._children[e];
  }
  /**
  * Gets index of the given child node. Returns `-1` if child node is not found.
  *
  * @param node Child node.
  * @returns Index of the child node.
  */
  getChildIndex(e) {
    return this._children.indexOf(e);
  }
  /**
  * Gets child nodes iterator.
  *
  * @returns Child nodes iterator.
  */
  getChildren() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Returns an iterator that contains the keys for attributes. Order of inserting attributes is not preserved.
  *
  * @returns Keys for attributes.
  */
  *getAttributeKeys() {
    this._classes.size > 0 && (yield "class"), this._styles.isEmpty || (yield "style"), yield* this._attrs.keys();
  }
  /**
  * Returns iterator that iterates over this element's attributes.
  *
  * Attributes are returned as arrays containing two items. First one is attribute key and second is attribute value.
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  *getAttributes() {
    yield* this._attrs.entries(), this._classes.size > 0 && (yield [
      "class",
      this.getAttribute("class")
    ]), this._styles.isEmpty || (yield [
      "style",
      this.getAttribute("style")
    ]);
  }
  /**
  * Gets attribute by key. If attribute is not present - returns undefined.
  *
  * @param key Attribute key.
  * @returns Attribute value.
  */
  getAttribute(e) {
    if (e == "class")
      return this._classes.size > 0 ? [
        ...this._classes
      ].join(" ") : void 0;
    if (e == "style") {
      const n = this._styles.toString();
      return n == "" ? void 0 : n;
    }
    return this._attrs.get(e);
  }
  /**
  * Returns a boolean indicating whether an attribute with the specified key exists in the element.
  *
  * @param key Attribute key.
  * @returns `true` if attribute with the specified key exists in the element, `false` otherwise.
  */
  hasAttribute(e) {
    return e == "class" ? this._classes.size > 0 : e == "style" ? !this._styles.isEmpty : this._attrs.has(e);
  }
  /**
  * Checks if this element is similar to other element.
  * Both elements should have the same name and attributes to be considered as similar. Two similar elements
  * can contain different set of children nodes.
  */
  isSimilar(e) {
    if (!(e instanceof V2))
      return !1;
    if (this === e)
      return !0;
    if (this.name != e.name || this._attrs.size !== e._attrs.size || this._classes.size !== e._classes.size || this._styles.size !== e._styles.size)
      return !1;
    for (const [n, r] of this._attrs)
      if (!e._attrs.has(n) || e._attrs.get(n) !== r)
        return !1;
    for (const n of this._classes)
      if (!e._classes.has(n))
        return !1;
    for (const n of this._styles.getStyleNames())
      if (!e._styles.has(n) || e._styles.getAsString(n) !== this._styles.getAsString(n))
        return !1;
    return !0;
  }
  /**
  * Returns true if class is present.
  * If more then one class is provided - returns true only when all classes are present.
  *
  * ```ts
  * element.hasClass( 'foo' ); // Returns true if 'foo' class is present.
  * element.hasClass( 'foo', 'bar' ); // Returns true if 'foo' and 'bar' classes are both present.
  * ```
  */
  hasClass(...e) {
    for (const n of e)
      if (!this._classes.has(n))
        return !1;
    return !0;
  }
  /**
  * Returns iterator that contains all class names.
  */
  getClassNames() {
    return this._classes.keys();
  }
  /**
  * Returns style value for the given property mae.
  * If the style does not exist `undefined` is returned.
  *
  * **Note**: This method can work with normalized style names if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#getAsString `StylesMap#getAsString()`} for details.
  *
  * For an element with style set to `'margin:1px'`:
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * const element = view.change( writer => {
  * 	const element = writer.createElement();
  * 	writer.setStyle( 'margin', '1px' );
  * 	writer.setStyle( 'margin-bottom', '3em' );
  *
  * 	return element;
  * } );
  *
  * element.getStyle( 'margin' ); // -> 'margin: 1px 1px 3em;'
  * ```
  */
  getStyle(e) {
    return this._styles.getAsString(e);
  }
  /**
  * Returns a normalized style object or single style value.
  *
  * For an element with style set to: margin:1px 2px 3em;
  *
  * ```ts
  * element.getNormalizedStyle( 'margin' ) );
  * ```
  *
  * will return:
  *
  * ```ts
  * {
  * 	top: '1px',
  * 	right: '2px',
  * 	bottom: '3em',
  * 	left: '2px'    // a normalized value from margin shorthand
  * }
  * ```
  *
  * and reading for single style value:
  *
  * ```ts
  * styles.getNormalizedStyle( 'margin-left' );
  * ```
  *
  * Will return a `2px` string.
  *
  * **Note**: This method will return normalized values only if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#getNormalized `StylesMap#getNormalized()`} for details.
  *
  * @param property Name of CSS property
  */
  getNormalizedStyle(e) {
    return this._styles.getNormalized(e);
  }
  /**
  * Returns iterator that contains all style names.
  *
  * @param expand Expand shorthand style properties and return all equivalent style representations.
  */
  getStyleNames(e) {
    return this._styles.getStyleNames(e);
  }
  /**
  * Returns true if style keys are present.
  * If more then one style property is provided - returns true only when all properties are present.
  *
  * ```ts
  * element.hasStyle( 'color' ); // Returns true if 'border-top' style is present.
  * element.hasStyle( 'color', 'border-top' ); // Returns true if 'color' and 'border-top' styles are both present.
  * ```
  */
  hasStyle(...e) {
    for (const n of e)
      if (!this._styles.has(n))
        return !1;
    return !0;
  }
  /**
  * Returns ancestor element that match specified pattern.
  * Provided patterns should be compatible with {@link module:engine/view/matcher~Matcher Matcher} as it is used internally.
  *
  * @see module:engine/view/matcher~Matcher
  * @param patterns Patterns used to match correct ancestor. See {@link module:engine/view/matcher~Matcher}.
  * @returns Found element or `null` if no matching ancestor was found.
  */
  findAncestor(...e) {
    const n = new xi(...e);
    let r = this.parent;
    for (; r && !r.is("documentFragment"); ) {
      if (n.match(r))
        return r;
      r = r.parent;
    }
    return null;
  }
  /**
  * Returns the custom property value for the given key.
  */
  getCustomProperty(e) {
    return this._customProperties.get(e);
  }
  /**
  * Returns an iterator which iterates over this element's custom properties.
  * Iterator provides `[ key, value ]` pairs for each stored property.
  */
  *getCustomProperties() {
    yield* this._customProperties.entries();
  }
  /**
  * Returns identity string based on element's name, styles, classes and other attributes.
  * Two elements that {@link #isSimilar are similar} will have same identity string.
  * It has the following format:
  *
  * ```ts
  * 'name class="class1,class2" style="style1:value1;style2:value2" attr1="val1" attr2="val2"'
  * ```
   *
  * For example:
  *
  * ```ts
  * const element = writer.createContainerElement( 'foo', {
  * 	banana: '10',
  * 	apple: '20',
  * 	style: 'color: red; border-color: white;',
  * 	class: 'baz'
  * } );
  *
  * // returns 'foo class="baz" style="border-color:white;color:red" apple="20" banana="10"'
  * element.getIdentity();
  * ```
  *
  * **Note**: Classes, styles and other attributes are sorted alphabetically.
  */
  getIdentity() {
    const e = Array.from(this._classes).sort().join(","), n = this._styles.toString(), r = Array.from(this._attrs).map((s) => `${s[0]}="${s[1]}"`).sort().join(" ");
    return this.name + (e == "" ? "" : ` class="${e}"`) + (n ? ` style="${n}"` : "") + (r == "" ? "" : ` ${r}`);
  }
  /**
  * Decides whether an unsafe attribute is whitelisted and should be rendered in the editing pipeline even though filtering mechanisms
  * like {@link module:engine/view/domconverter~DomConverter#shouldRenderAttribute} say it should not.
  *
  * Unsafe attribute names can be specified when creating an element via {@link module:engine/view/downcastwriter~DowncastWriter}.
  *
  * @param attributeName The name of the attribute to be checked.
  */
  shouldRenderUnsafeAttribute(e) {
    return this._unsafeAttributesToRender.includes(e);
  }
  /**
  * Clones provided element.
  *
  * @internal
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any children.
  * @returns Clone of this element.
  */
  _clone(e = !1) {
    const n = [];
    if (e)
      for (const s of this.getChildren())
        n.push(s._clone(e));
    const r = new this.constructor(this.document, this.name, this._attrs, n);
    return r._classes = new Set(this._classes), r._styles.set(this._styles.getNormalized()), r._customProperties = new Map(this._customProperties), r.getFillerOffset = this.getFillerOffset, r._unsafeAttributesToRender = this._unsafeAttributesToRender, r;
  }
  /**
  * {@link module:engine/view/element~Element#_insertChild Insert} a child node or a list of child nodes at the end of this node
  * and sets the parent of these nodes to this element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#insert
  * @internal
  * @param items Items to be inserted.
  * @fires change
  * @returns Number of appended nodes.
  */
  _appendChild(e) {
    return this._insertChild(this.childCount, e);
  }
  /**
  * Inserts a child node or a list of child nodes on the given index and sets the parent of these nodes to
  * this element.
  *
  * @internal
  * @see module:engine/view/downcastwriter~DowncastWriter#insert
  * @param index Position where nodes should be inserted.
  * @param items Items to be inserted.
  * @fires change
  * @returns Number of inserted nodes.
  */
  _insertChild(e, n) {
    this._fireChange("children", this);
    let r = 0;
    const s = f6(this.document, n);
    for (const o of s)
      o.parent !== null && o._remove(), o.parent = this, o.document = this.document, this._children.splice(e, 0, o), e++, r++;
    return r;
  }
  /**
  * Removes number of child nodes starting at the given index and set the parent of these nodes to `null`.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#remove
  * @internal
  * @param index Number of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @fires change
  * @returns The array of removed nodes.
  */
  _removeChildren(e, n = 1) {
    this._fireChange("children", this);
    for (let r = e; r < e + n; r++)
      this._children[r].parent = null;
    return this._children.splice(e, n);
  }
  /**
  * Adds or overwrite attribute with a specified key and value.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#setAttribute
  * @internal
  * @param key Attribute key.
  * @param value Attribute value.
  * @fires change
  */
  _setAttribute(e, n) {
    const r = String(n);
    this._fireChange("attributes", this), e == "class" ? S1(this._classes, r) : e == "style" ? this._styles.setTo(r) : this._attrs.set(e, r);
  }
  /**
  * Removes attribute from the element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeAttribute
  * @internal
  * @param key Attribute key.
  * @returns Returns true if an attribute existed and has been removed.
  * @fires change
  */
  _removeAttribute(e) {
    return this._fireChange("attributes", this), e == "class" ? this._classes.size > 0 ? (this._classes.clear(), !0) : !1 : e == "style" ? this._styles.isEmpty ? !1 : (this._styles.clear(), !0) : this._attrs.delete(e);
  }
  /**
  * Adds specified class.
  *
  * ```ts
  * element._addClass( 'foo' ); // Adds 'foo' class.
  * element._addClass( [ 'foo', 'bar' ] ); // Adds 'foo' and 'bar' classes.
  * ```
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#addClass
  * @internal
  * @fires change
  */
  _addClass(e) {
    this._fireChange("attributes", this);
    for (const n of tt(e))
      this._classes.add(n);
  }
  /**
  * Removes specified class.
  *
  * ```ts
  * element._removeClass( 'foo' );  // Removes 'foo' class.
  * element._removeClass( [ 'foo', 'bar' ] ); // Removes both 'foo' and 'bar' classes.
  * ```
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeClass
  * @internal
  * @fires change
  */
  _removeClass(e) {
    this._fireChange("attributes", this);
    for (const n of tt(e))
      this._classes.delete(n);
  }
  _setStyle(e, n) {
    this._fireChange("attributes", this), typeof e != "string" ? this._styles.set(e) : this._styles.set(e, n);
  }
  /**
  * Removes specified style.
  *
  * ```ts
  * element._removeStyle( 'color' );  // Removes 'color' style.
  * element._removeStyle( [ 'color', 'border-top' ] ); // Removes both 'color' and 'border-top' styles.
  * ```
  *
  * **Note**: This method can work with normalized style names if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#remove `StylesMap#remove()`} for details.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeStyle
  * @internal
  * @fires change
  */
  _removeStyle(e) {
    this._fireChange("attributes", this);
    for (const n of tt(e))
      this._styles.remove(n);
  }
  /**
  * Sets a custom property. Unlike attributes, custom properties are not rendered to the DOM,
  * so they can be used to add special data to elements.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#setCustomProperty
  * @internal
  */
  _setCustomProperty(e, n) {
    this._customProperties.set(e, n);
  }
  /**
  * Removes the custom property stored under the given key.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeCustomProperty
  * @internal
  * @returns Returns true if property was removed.
  */
  _removeCustomProperty(e) {
    return this._customProperties.delete(e);
  }
};
Ji.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "element" || i === "view:element") : i === "element" || i === "view:element" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "node" || i === "view:node";
};
function h6(i) {
  const t = gr(i);
  for (const [e, n] of t)
    n === null ? t.delete(e) : typeof n != "string" && t.set(e, String(n));
  return t;
}
function S1(i, t) {
  const e = t.split(/\s+/);
  i.clear(), e.forEach((n) => i.add(n));
}
function f6(i, t) {
  return typeof t == "string" ? [
    new St(i, t)
  ] : (ki(t) || (t = [
    t
  ]), Array.from(t).map((e) => typeof e == "string" ? new St(i, e) : e instanceof cr ? new St(i, e.data) : e));
}
class Rl extends Ji {
  /**
  * Creates a container element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createContainerElement
  * @see module:engine/view/element~Element
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, n, r) {
    super(t, e, n, r), this.getFillerOffset = m6;
  }
}
Rl.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "containerElement" || i === "view:containerElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "element" || i === "view:element") : i === "containerElement" || i === "view:containerElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "element" || i === "view:element" || i === "node" || i === "view:node";
};
function m6() {
  const i = [
    ...this.getChildren()
  ], t = i[this.childCount - 1];
  if (t && t.is("element", "br"))
    return this.childCount;
  for (const e of i)
    if (!e.is("uiElement"))
      return null;
  return this.childCount;
}
class Bd extends (/* @__PURE__ */ st(Rl)) {
  /**
  * Creates an editable element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createEditableElement
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, n, r) {
    super(t, e, n, r), this.set("isReadOnly", !1), this.set("isFocused", !1), this.set("placeholder", void 0), this.bind("isReadOnly").to(t), this.bind("isFocused").to(t, "isFocused", (s) => s && t.selection.editableElement == this), this.listenTo(t.selection, "change", () => {
      this.isFocused = t.isFocused && t.selection.editableElement == this;
    });
  }
  destroy() {
    this.stopListening();
  }
}
Bd.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "editableElement" || i === "view:editableElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "containerElement" || i === "view:containerElement" || i === "element" || i === "view:element") : i === "editableElement" || i === "view:editableElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "containerElement" || i === "view:containerElement" || i === "element" || i === "view:element" || i === "node" || i === "view:node";
};
const A1 = Symbol("rootName");
class M2 extends Bd {
  /**
  * Creates root editable element.
  *
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  */
  constructor(t, e) {
    super(t, e), this.rootName = "main";
  }
  /**
  * Name of this root inside {@link module:engine/view/document~Document} that is an owner of this root. If no
  * other name is set, `main` name is used.
  *
  * @readonly
  */
  get rootName() {
    return this.getCustomProperty(A1);
  }
  set rootName(t) {
    this._setCustomProperty(A1, t);
  }
  /**
  * Overrides old element name and sets new one.
  * This is needed because view roots are created before they are attached to the DOM.
  * The name of the root element is temporary at this stage. It has to be changed when the
  * view root element is attached to the DOM element.
  *
  * @internal
  * @param name The new name of element.
  */
  set _name(t) {
    this.name = t;
  }
}
M2.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "rootElement" || i === "view:rootElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "editableElement" || i === "view:editableElement" || i === "containerElement" || i === "view:containerElement" || i === "element" || i === "view:element") : i === "rootElement" || i === "view:rootElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "editableElement" || i === "view:editableElement" || i === "containerElement" || i === "view:containerElement" || i === "element" || i === "view:element" || i === "node" || i === "view:node";
};
let Ds = class {
  /**
  * Creates a range iterator. All parameters are optional, but you have to specify either `boundaries` or `startPosition`.
  *
  * @param options Object with configuration.
  */
  constructor(t = {}) {
    /**
    * Walking direction. Defaults `'forward'`.
    */
    h(this, "direction");
    /**
    * Iterator boundaries.
    *
    * When the iterator is walking `'forward'` on the end of boundary or is walking `'backward'`
    * on the start of boundary, then `{ done: true }` is returned.
    *
    * If boundaries are not defined they are set before first and after last child of the root node.
    */
    h(this, "boundaries");
    /**
    * Flag indicating whether all characters from {@link module:engine/view/text~Text} should be returned as one
    * {@link module:engine/view/text~Text} or one by one as {@link module:engine/view/textproxy~TextProxy}.
    */
    h(this, "singleCharacters");
    /**
    * Flag indicating whether iterator should enter elements or not. If the iterator is shallow child nodes of any
    * iterated node will not be returned along with `elementEnd` tag.
    */
    h(this, "shallow");
    /**
    * Flag indicating whether iterator should ignore `elementEnd` tags. If set to `true`, walker will not
    * return a parent node of the start position. Each {@link module:engine/view/element~Element} will be returned once.
    * When set to `false` each element might be returned twice: for `'elementStart'` and `'elementEnd'`.
    */
    h(this, "ignoreElementEnd");
    /**
    * Iterator position. If start position is not defined then position depends on {@link #direction}. If direction is
    * `'forward'` position starts form the beginning, when direction is `'backward'` position starts from the end.
    */
    h(this, "_position");
    /**
    * Start boundary parent.
    */
    h(this, "_boundaryStartParent");
    /**
    * End boundary parent.
    */
    h(this, "_boundaryEndParent");
    if (!t.boundaries && !t.startPosition)
      throw new T("view-tree-walker-no-start-position", null);
    if (t.direction && t.direction != "forward" && t.direction != "backward")
      throw new T("view-tree-walker-unknown-direction", t.startPosition, {
        direction: t.direction
      });
    this.boundaries = t.boundaries || null, t.startPosition ? this._position = fe._createAt(t.startPosition) : this._position = fe._createAt(t.boundaries[t.direction == "backward" ? "end" : "start"]), this.direction = t.direction || "forward", this.singleCharacters = !!t.singleCharacters, this.shallow = !!t.shallow, this.ignoreElementEnd = !!t.ignoreElementEnd, this._boundaryStartParent = this.boundaries ? this.boundaries.start.parent : null, this._boundaryEndParent = this.boundaries ? this.boundaries.end.parent : null;
  }
  /**
  * Iterable interface.
  */
  [Symbol.iterator]() {
    return this;
  }
  /**
  * Iterator position. If start position is not defined then position depends on {@link #direction}. If direction is
  * `'forward'` position starts form the beginning, when direction is `'backward'` position starts from the end.
  */
  get position() {
    return this._position;
  }
  /**
  * Moves {@link #position} in the {@link #direction} skipping values as long as the callback function returns `true`.
  *
  * For example:
  *
  * ```ts
  * walker.skip( value => value.type == 'text' ); // <p>{}foo</p> -> <p>foo[]</p>
  * walker.skip( value => true ); // Move the position to the end: <p>{}foo</p> -> <p>foo</p>[]
  * walker.skip( value => false ); // Do not move the position.
  * ```
  *
  * @param skip Callback function. Gets {@link module:engine/view/treewalker~TreeWalkerValue} and should
  * return `true` if the value should be skipped or `false` if not.
  */
  skip(t) {
    let e, n;
    do
      n = this.position, e = this.next();
    while (!e.done && t(e.value));
    e.done || (this._position = n);
  }
  /**
  * Gets the next tree walker's value.
  *
  * @returns Object implementing iterator interface, returning
  * information about taken step.
  */
  next() {
    return this.direction == "forward" ? this._next() : this._previous();
  }
  /**
  * Makes a step forward in view. Moves the {@link #position} to the next position and returns the encountered value.
  */
  _next() {
    let t = this.position.clone();
    const e = this.position, n = t.parent;
    if (n.parent === null && t.offset === n.childCount)
      return {
        done: !0,
        value: void 0
      };
    if (n === this._boundaryEndParent && t.offset == this.boundaries.end.offset)
      return {
        done: !0,
        value: void 0
      };
    let r;
    if (n instanceof St) {
      if (t.isAtEnd)
        return this._position = fe._createAfter(n), this._next();
      r = n.data[t.offset];
    } else
      r = n.getChild(t.offset);
    if (r instanceof Ji) {
      if (!this.shallow)
        t = new fe(r, 0);
      else {
        if (this.boundaries && this.boundaries.end.isBefore(t))
          return {
            done: !0,
            value: void 0
          };
        t.offset++;
      }
      return this._position = t, this._formatReturnValue("elementStart", r, e, t, 1);
    }
    if (r instanceof St) {
      if (this.singleCharacters)
        return t = new fe(r, 0), this._position = t, this._next();
      let s = r.data.length, o;
      return r == this._boundaryEndParent ? (s = this.boundaries.end.offset, o = new cr(r, 0, s), t = fe._createAfter(o)) : (o = new cr(r, 0, r.data.length), t.offset++), this._position = t, this._formatReturnValue("text", o, e, t, s);
    }
    if (typeof r == "string") {
      let s;
      this.singleCharacters ? s = 1 : s = (n === this._boundaryEndParent ? this.boundaries.end.offset : n.data.length) - t.offset;
      const o = new cr(n, t.offset, s);
      return t.offset += s, this._position = t, this._formatReturnValue("text", o, e, t, s);
    }
    return t = fe._createAfter(n), this._position = t, this.ignoreElementEnd ? this._next() : this._formatReturnValue("elementEnd", n, e, t);
  }
  /**
  * Makes a step backward in view. Moves the {@link #position} to the previous position and returns the encountered value.
  */
  _previous() {
    let t = this.position.clone();
    const e = this.position, n = t.parent;
    if (n.parent === null && t.offset === 0)
      return {
        done: !0,
        value: void 0
      };
    if (n == this._boundaryStartParent && t.offset == this.boundaries.start.offset)
      return {
        done: !0,
        value: void 0
      };
    let r;
    if (n instanceof St) {
      if (t.isAtStart)
        return this._position = fe._createBefore(n), this._previous();
      r = n.data[t.offset - 1];
    } else
      r = n.getChild(t.offset - 1);
    if (r instanceof Ji)
      return this.shallow ? (t.offset--, this._position = t, this._formatReturnValue("elementStart", r, e, t, 1)) : (t = new fe(r, r.childCount), this._position = t, this.ignoreElementEnd ? this._previous() : this._formatReturnValue("elementEnd", r, e, t));
    if (r instanceof St) {
      if (this.singleCharacters)
        return t = new fe(r, r.data.length), this._position = t, this._previous();
      let s = r.data.length, o;
      if (r == this._boundaryStartParent) {
        const a = this.boundaries.start.offset;
        o = new cr(r, a, r.data.length - a), s = o.data.length, t = fe._createBefore(o);
      } else
        o = new cr(r, 0, r.data.length), t.offset--;
      return this._position = t, this._formatReturnValue("text", o, e, t, s);
    }
    if (typeof r == "string") {
      let s;
      if (this.singleCharacters)
        s = 1;
      else {
        const a = n === this._boundaryStartParent ? this.boundaries.start.offset : 0;
        s = t.offset - a;
      }
      t.offset -= s;
      const o = new cr(n, t.offset, s);
      return this._position = t, this._formatReturnValue("text", o, e, t, s);
    }
    return t = fe._createBefore(n), this._position = t, this._formatReturnValue("elementStart", n, e, t, 1);
  }
  /**
  * Format returned data and adjust `previousPosition` and `nextPosition` if reach the bound of the {@link module:engine/view/text~Text}.
  *
  * @param type Type of step.
  * @param item Item between old and new position.
  * @param previousPosition Previous position of iterator.
  * @param nextPosition Next position of iterator.
  * @param length Length of the item.
  */
  _formatReturnValue(t, e, n, r, s) {
    return e instanceof cr && (e.offsetInText + e.data.length == e.textNode.data.length && (this.direction == "forward" && !(this.boundaries && this.boundaries.end.isEqual(this.position)) ? (r = fe._createAfter(e.textNode), this._position = r) : n = fe._createAfter(e.textNode)), e.offsetInText === 0 && (this.direction == "backward" && !(this.boundaries && this.boundaries.start.isEqual(this.position)) ? (r = fe._createBefore(e.textNode), this._position = r) : n = fe._createBefore(e.textNode))), {
      done: !1,
      value: {
        type: t,
        item: e,
        previousPosition: n,
        nextPosition: r,
        length: s
      }
    };
  }
}, fe = class Pr extends eo {
  /**
  * Creates a position.
  *
  * @param parent Position parent.
  * @param offset Position offset.
  */
  constructor(e, n) {
    super();
    /**
    * Position parent.
    */
    h(this, "parent");
    /**
    * Position offset.
    */
    h(this, "offset");
    this.parent = e, this.offset = n;
  }
  /**
  * Node directly after the position. Equals `null` when there is no node after position or position is located
  * inside text node.
  */
  get nodeAfter() {
    return this.parent.is("$text") ? null : this.parent.getChild(this.offset) || null;
  }
  /**
  * Node directly before the position. Equals `null` when there is no node before position or position is located
  * inside text node.
  */
  get nodeBefore() {
    return this.parent.is("$text") ? null : this.parent.getChild(this.offset - 1) || null;
  }
  /**
  * Is `true` if position is at the beginning of its {@link module:engine/view/position~Position#parent parent}, `false` otherwise.
  */
  get isAtStart() {
    return this.offset === 0;
  }
  /**
  * Is `true` if position is at the end of its {@link module:engine/view/position~Position#parent parent}, `false` otherwise.
  */
  get isAtEnd() {
    const e = this.parent.is("$text") ? this.parent.data.length : this.parent.childCount;
    return this.offset === e;
  }
  /**
  * Position's root, that is the root of the position's parent element.
  */
  get root() {
    return this.parent.root;
  }
  /**
  * {@link module:engine/view/editableelement~EditableElement EditableElement} instance that contains this position, or `null` if
  * position is not inside an editable element.
  */
  get editableElement() {
    let e = this.parent;
    for (; !(e instanceof Bd); )
      if (e.parent)
        e = e.parent;
      else
        return null;
    return e;
  }
  /**
  * Returns a new instance of Position with offset incremented by `shift` value.
  *
  * @param shift How position offset should get changed. Accepts negative values.
  * @returns Shifted position.
  */
  getShiftedBy(e) {
    const n = Pr._createAt(this), r = n.offset + e;
    return n.offset = r < 0 ? 0 : r, n;
  }
  /**
  * Gets the farthest position which matches the callback using
  * {@link module:engine/view/treewalker~TreeWalker TreeWalker}.
  *
  * For example:
  *
  * ```ts
  * getLastMatchingPosition( value => value.type == 'text' ); // <p>{}foo</p> -> <p>foo[]</p>
  * getLastMatchingPosition( value => value.type == 'text', { direction: 'backward' } ); // <p>foo[]</p> -> <p>{}foo</p>
  * getLastMatchingPosition( value => false ); // Do not move the position.
  * ```
  *
  * @param skip Callback function. Gets {@link module:engine/view/treewalker~TreeWalkerValue} and should
  * return `true` if the value should be skipped or `false` if not.
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}.
  * @returns The position after the last item which matches the `skip` callback test.
  */
  getLastMatchingPosition(e, n = {}) {
    n.startPosition = this;
    const r = new Ds(n);
    return r.skip(e), r.position;
  }
  /**
  * Returns ancestors array of this position, that is this position's parent and it's ancestors.
  *
  * @returns Array with ancestors.
  */
  getAncestors() {
    return this.parent.is("documentFragment") ? [
      this.parent
    ] : this.parent.getAncestors({
      includeSelf: !0
    });
  }
  /**
  * Returns a {@link module:engine/view/node~Node} or {@link module:engine/view/documentfragment~DocumentFragment}
  * which is a common ancestor of both positions.
  */
  getCommonAncestor(e) {
    const n = this.getAncestors(), r = e.getAncestors();
    let s = 0;
    for (; n[s] == r[s] && n[s]; )
      s++;
    return s === 0 ? null : n[s - 1];
  }
  /**
  * Checks whether this position equals given position.
  *
  * @param otherPosition Position to compare with.
  * @returns True if positions are same.
  */
  isEqual(e) {
    return this.parent == e.parent && this.offset == e.offset;
  }
  /**
  * Checks whether this position is located before given position. When method returns `false` it does not mean that
  * this position is after give one. Two positions may be located inside separate roots and in that situation this
  * method will still return `false`.
  *
  * @see module:engine/view/position~Position#isAfter
  * @see module:engine/view/position~Position#compareWith
  * @param otherPosition Position to compare with.
  * @returns Returns `true` if this position is before given position.
  */
  isBefore(e) {
    return this.compareWith(e) == "before";
  }
  /**
  * Checks whether this position is located after given position. When method returns `false` it does not mean that
  * this position is before give one. Two positions may be located inside separate roots and in that situation this
  * method will still return `false`.
  *
  * @see module:engine/view/position~Position#isBefore
  * @see module:engine/view/position~Position#compareWith
  * @param otherPosition Position to compare with.
  * @returns Returns `true` if this position is after given position.
  */
  isAfter(e) {
    return this.compareWith(e) == "after";
  }
  /**
  * Checks whether this position is before, after or in same position that other position. Two positions may be also
  * different when they are located in separate roots.
  *
  * @param otherPosition Position to compare with.
  */
  compareWith(e) {
    if (this.root !== e.root)
      return "different";
    if (this.isEqual(e))
      return "same";
    const n = this.parent.is("node") ? this.parent.getPath() : [], r = e.parent.is("node") ? e.parent.getPath() : [];
    n.push(this.offset), r.push(e.offset);
    const s = Fn(n, r);
    switch (s) {
      case "prefix":
        return "before";
      case "extension":
        return "after";
      default:
        return n[s] < r[s] ? "before" : "after";
    }
  }
  /**
  * Creates a {@link module:engine/view/treewalker~TreeWalker TreeWalker} instance with this positions as a start position.
  *
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}
  */
  getWalker(e = {}) {
    return e.startPosition = this, new Ds(e);
  }
  /**
  * Clones this position.
  */
  clone() {
    return new Pr(this.parent, this.offset);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/view/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/view/item~Item view item} and `'before'` or `'after'` (sets position before or after given view item).
  *
  * This method is a shortcut to other constructors such as:
  *
  * * {@link module:engine/view/position~Position._createBefore},
  * * {@link module:engine/view/position~Position._createAfter}.
  *
  * @internal
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  static _createAt(e, n) {
    if (e instanceof Pr)
      return new this(e.parent, e.offset);
    {
      const r = e;
      if (n == "end")
        n = r.is("$text") ? r.data.length : r.childCount;
      else {
        if (n == "before")
          return this._createBefore(r);
        if (n == "after")
          return this._createAfter(r);
        if (n !== 0 && !n)
          throw new T("view-createpositionat-offset-required", r);
      }
      return new Pr(r, n);
    }
  }
  /**
  * Creates a new position after given view item.
  *
  * @internal
  * @param item View item after which the position should be located.
  */
  static _createAfter(e) {
    if (e.is("$textProxy"))
      return new Pr(e.textNode, e.offsetInText + e.data.length);
    if (!e.parent)
      throw new T("view-position-after-root", e, {
        root: e
      });
    return new Pr(e.parent, e.index + 1);
  }
  /**
  * Creates a new position before given view item.
  *
  * @internal
  * @param item View item before which the position should be located.
  */
  static _createBefore(e) {
    if (e.is("$textProxy"))
      return new Pr(e.textNode, e.offsetInText);
    if (!e.parent)
      throw new T("view-position-before-root", e, {
        root: e
      });
    return new Pr(e.parent, e.index);
  }
};
fe.prototype.is = function(i) {
  return i === "position" || i === "view:position";
};
let qe = class Jr extends eo {
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * **Note:** Constructor creates it's own {@link module:engine/view/position~Position} instances basing on passed values.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at the `start` position.
  */
  constructor(e, n = null) {
    super();
    /**
    * Start position.
    */
    h(this, "start");
    /**
    * End position.
    */
    h(this, "end");
    this.start = e.clone(), this.end = n ? n.clone() : e.clone();
  }
  /**
  * Iterable interface.
  *
  * Iterates over all {@link module:engine/view/item~Item view items} that are in this range and returns
  * them together with additional information like length or {@link module:engine/view/position~Position positions},
  * grouped as {@link module:engine/view/treewalker~TreeWalkerValue}.
  *
  * This iterator uses {@link module:engine/view/treewalker~TreeWalker TreeWalker} with `boundaries` set to this range and
  * `ignoreElementEnd` option
  * set to `true`.
  */
  *[Symbol.iterator]() {
    yield* new Ds({
      boundaries: this,
      ignoreElementEnd: !0
    });
  }
  /**
  * Returns whether the range is collapsed, that is it start and end positions are equal.
  */
  get isCollapsed() {
    return this.start.isEqual(this.end);
  }
  /**
  * Returns whether this range is flat, that is if {@link module:engine/view/range~Range#start start} position and
  * {@link module:engine/view/range~Range#end end} position are in the same {@link module:engine/view/position~Position#parent parent}.
  */
  get isFlat() {
    return this.start.parent === this.end.parent;
  }
  /**
  * Range root element.
  */
  get root() {
    return this.start.root;
  }
  /**
  * Creates a maximal range that has the same content as this range but is expanded in both ways (at the beginning
  * and at the end).
  *
  * For example:
  *
  * ```html
  * <p>Foo</p><p><b>{Bar}</b></p> -> <p>Foo</p>[<p><b>Bar</b>]</p>
  * <p><b>foo</b>{bar}<span></span></p> -> <p><b>foo[</b>bar<span></span>]</p>
  * ```
  *
  * Note that in the sample above:
  *
  * - `<p>` have type of {@link module:engine/view/containerelement~ContainerElement},
  * - `<b>` have type of {@link module:engine/view/attributeelement~AttributeElement},
  * - `<span>` have type of {@link module:engine/view/uielement~UIElement}.
  *
  * @returns Enlarged range.
  */
  getEnlarged() {
    let e = this.start.getLastMatchingPosition(Vc, {
      direction: "backward"
    }), n = this.end.getLastMatchingPosition(Vc);
    return e.parent.is("$text") && e.isAtStart && (e = fe._createBefore(e.parent)), n.parent.is("$text") && n.isAtEnd && (n = fe._createAfter(n.parent)), new Jr(e, n);
  }
  /**
  * Creates a minimum range that has the same content as this range but is trimmed in both ways (at the beginning
  * and at the end).
  *
  * For example:
  *
  * ```html
  * <p>Foo</p>[<p><b>Bar</b>]</p> -> <p>Foo</p><p><b>{Bar}</b></p>
  * <p><b>foo[</b>bar<span></span>]</p> -> <p><b>foo</b>{bar}<span></span></p>
  * ```
  *
  * Note that in the sample above:
  *
  * - `<p>` have type of {@link module:engine/view/containerelement~ContainerElement},
  * - `<b>` have type of {@link module:engine/view/attributeelement~AttributeElement},
  * - `<span>` have type of {@link module:engine/view/uielement~UIElement}.
  *
  * @returns Shrunk range.
  */
  getTrimmed() {
    let e = this.start.getLastMatchingPosition(Vc);
    if (e.isAfter(this.end) || e.isEqual(this.end))
      return new Jr(e, e);
    let n = this.end.getLastMatchingPosition(Vc, {
      direction: "backward"
    });
    const r = e.nodeAfter, s = n.nodeBefore;
    return r && r.is("$text") && (e = new fe(r, 0)), s && s.is("$text") && (n = new fe(s, s.data.length)), new Jr(e, n);
  }
  /**
  * Two ranges are equal if their start and end positions are equal.
  *
  * @param otherRange Range to compare with.
  * @returns `true` if ranges are equal, `false` otherwise
  */
  isEqual(e) {
    return this == e || this.start.isEqual(e.start) && this.end.isEqual(e.end);
  }
  /**
  * Checks whether this range contains given {@link module:engine/view/position~Position position}.
  *
  * @param position Position to check.
  * @returns `true` if given {@link module:engine/view/position~Position position} is contained in this range, `false` otherwise.
  */
  containsPosition(e) {
    return e.isAfter(this.start) && e.isBefore(this.end);
  }
  /**
  * Checks whether this range contains given {@link module:engine/view/range~Range range}.
  *
  * @param otherRange Range to check.
  * @param loose Whether the check is loose or strict. If the check is strict (`false`), compared range cannot
  * start or end at the same position as this range boundaries. If the check is loose (`true`), compared range can start, end or
  * even be equal to this range. Note that collapsed ranges are always compared in strict mode.
  * @returns `true` if given {@link module:engine/view/range~Range range} boundaries are contained by this range, `false`
  * otherwise.
  */
  containsRange(e, n = !1) {
    e.isCollapsed && (n = !1);
    const r = this.containsPosition(e.start) || n && this.start.isEqual(e.start), s = this.containsPosition(e.end) || n && this.end.isEqual(e.end);
    return r && s;
  }
  /**
  * Computes which part(s) of this {@link module:engine/view/range~Range range} is not a part of given
  * {@link module:engine/view/range~Range range}.
  * Returned array contains zero, one or two {@link module:engine/view/range~Range ranges}.
  *
  * Examples:
  *
  * ```ts
  * let foo = downcastWriter.createText( 'foo' );
  * let img = downcastWriter.createContainerElement( 'img' );
  * let bar = downcastWriter.createText( 'bar' );
  * let p = downcastWriter.createContainerElement( 'p', null, [ foo, img, bar ] );
  *
  * let range = view.createRange( view.createPositionAt( foo, 2 ), view.createPositionAt( bar, 1 ); // "o", img, "b" are in range.
  * let otherRange = view.createRange( // "oo", img, "ba" are in range.
  * 	view.createPositionAt( foo, 1 ),
  * 	view.createPositionAt( bar, 2 )
  * );
  * let transformed = range.getDifference( otherRange );
  * // transformed array has no ranges because `otherRange` contains `range`
  *
  * otherRange = view.createRange( view.createPositionAt( foo, 1 ), view.createPositionAt( p, 2 ); // "oo", img are in range.
  * transformed = range.getDifference( otherRange );
  * // transformed array has one range: from ( p, 2 ) to ( bar, 1 )
  *
  * otherRange = view.createRange( view.createPositionAt( p, 1 ), view.createPositionAt( p, 2 ) ); // img is in range.
  * transformed = range.getDifference( otherRange );
  * // transformed array has two ranges: from ( foo, 1 ) to ( p, 1 ) and from ( p, 2 ) to ( bar, 1 )
  * ```
  *
  * @param otherRange Range to differentiate against.
  * @returns The difference between ranges.
  */
  getDifference(e) {
    const n = [];
    return this.isIntersecting(e) ? (this.containsPosition(e.start) && n.push(new Jr(this.start, e.start)), this.containsPosition(e.end) && n.push(new Jr(e.end, this.end))) : n.push(this.clone()), n;
  }
  /**
  * Returns an intersection of this {@link module:engine/view/range~Range range} and given {@link module:engine/view/range~Range range}.
  * Intersection is a common part of both of those ranges. If ranges has no common part, returns `null`.
  *
  * Examples:
  *
  * ```ts
  * let foo = downcastWriter.createText( 'foo' );
  * let img = downcastWriter.createContainerElement( 'img' );
  * let bar = downcastWriter.createText( 'bar' );
  * let p = downcastWriter.createContainerElement( 'p', null, [ foo, img, bar ] );
  *
  * let range = view.createRange( view.createPositionAt( foo, 2 ), view.createPositionAt( bar, 1 ); // "o", img, "b" are in range.
  * let otherRange = view.createRange( view.createPositionAt( foo, 1 ), view.createPositionAt( p, 2 ); // "oo", img are in range.
  * let transformed = range.getIntersection( otherRange ); // range from ( foo, 1 ) to ( p, 2 ).
  *
  * otherRange = view.createRange( view.createPositionAt( bar, 1 ), view.createPositionAt( bar, 3 ); "ar" is in range.
  * transformed = range.getIntersection( otherRange ); // null - no common part.
  * ```
  *
  * @param otherRange Range to check for intersection.
  * @returns A common part of given ranges or `null` if ranges have no common part.
  */
  getIntersection(e) {
    if (this.isIntersecting(e)) {
      let n = this.start, r = this.end;
      return this.containsPosition(e.start) && (n = e.start), this.containsPosition(e.end) && (r = e.end), new Jr(n, r);
    }
    return null;
  }
  /**
  * Creates a {@link module:engine/view/treewalker~TreeWalker TreeWalker} instance with this range as a boundary.
  *
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}.
  */
  getWalker(e = {}) {
    return e.boundaries = this, new Ds(e);
  }
  /**
  * Returns a {@link module:engine/view/node~Node} or {@link module:engine/view/documentfragment~DocumentFragment}
  * which is a common ancestor of range's both ends (in which the entire range is contained).
  */
  getCommonAncestor() {
    return this.start.getCommonAncestor(this.end);
  }
  /**
  * Returns an {@link module:engine/view/element~Element Element} contained by the range.
  * The element will be returned when it is the **only** node within the range and **fully–contained**
  * at the same time.
  */
  getContainedElement() {
    if (this.isCollapsed)
      return null;
    let e = this.start.nodeAfter, n = this.end.nodeBefore;
    return this.start.parent.is("$text") && this.start.isAtEnd && this.start.parent.nextSibling && (e = this.start.parent.nextSibling), this.end.parent.is("$text") && this.end.isAtStart && this.end.parent.previousSibling && (n = this.end.parent.previousSibling), e && e.is("element") && e === n ? e : null;
  }
  /**
  * Clones this range.
  */
  clone() {
    return new Jr(this.start, this.end);
  }
  /**
  * Returns an iterator that iterates over all {@link module:engine/view/item~Item view items} that are in this range and returns
  * them.
  *
  * This method uses {@link module:engine/view/treewalker~TreeWalker} with `boundaries` set to this range and `ignoreElementEnd` option
  * set to `true`. However it returns only {@link module:engine/view/item~Item items},
  * not {@link module:engine/view/treewalker~TreeWalkerValue}.
  *
  * You may specify additional options for the tree walker. See {@link module:engine/view/treewalker~TreeWalker} for
  * a full list of available options.
  *
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}.
  */
  *getItems(e = {}) {
    e.boundaries = this, e.ignoreElementEnd = !0;
    const n = new Ds(e);
    for (const r of n)
      yield r.item;
  }
  /**
  * Returns an iterator that iterates over all {@link module:engine/view/position~Position positions} that are boundaries or
  * contained in this range.
  *
  * This method uses {@link module:engine/view/treewalker~TreeWalker} with `boundaries` set to this range. However it returns only
  * {@link module:engine/view/position~Position positions}, not {@link module:engine/view/treewalker~TreeWalkerValue}.
  *
  * You may specify additional options for the tree walker. See {@link module:engine/view/treewalker~TreeWalker} for
  * a full list of available options.
  *
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}.
  */
  *getPositions(e = {}) {
    e.boundaries = this;
    const n = new Ds(e);
    yield n.position;
    for (const r of n)
      yield r.nextPosition;
  }
  /**
  * Checks and returns whether this range intersects with the given range.
  *
  * @param otherRange Range to compare with.
  * @returns True if ranges intersect.
  */
  isIntersecting(e) {
    return this.start.isBefore(e.end) && this.end.isAfter(e.start);
  }
  /**
  * Creates a range from the given parents and offsets.
  *
  * @internal
  * @param startElement Start position parent element.
  * @param startOffset Start position offset.
  * @param endElement End position parent element.
  * @param endOffset End position offset.
  * @returns Created range.
  */
  static _createFromParentsAndOffsets(e, n, r, s) {
    return new this(new fe(e, n), new fe(r, s));
  }
  /**
  * Creates a new range, spreading from specified {@link module:engine/view/position~Position position} to a position moved by
  * given `shift`. If `shift` is a negative value, shifted position is treated as the beginning of the range.
  *
  * @internal
  * @param position Beginning of the range.
  * @param shift How long the range should be.
  */
  static _createFromPositionAndShift(e, n) {
    const r = e, s = e.getShiftedBy(n);
    return n > 0 ? new this(r, s) : new this(s, r);
  }
  /**
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @internal
  * @param element Element which is a parent for the range.
  */
  static _createIn(e) {
    return this._createFromParentsAndOffsets(e, 0, e, e.childCount);
  }
  /**
  * Creates a range that starts before given {@link module:engine/view/item~Item view item} and ends after it.
  *
  * @internal
  */
  static _createOn(e) {
    const n = e.is("$textProxy") ? e.offsetSize : 1;
    return this._createFromPositionAndShift(fe._createBefore(e), n);
  }
};
qe.prototype.is = function(i) {
  return i === "range" || i === "view:range";
};
function Vc(i) {
  return !!(i.item.is("attributeElement") || i.item.is("uiElement"));
}
let Dr = class D2 extends (/* @__PURE__ */ ht(eo)) {
  /**
  * Creates new selection instance.
  *
  * **Note**: The selection constructor is available as a factory method:
  *
  * * {@link module:engine/view/view~View#createSelection `View#createSelection()`},
  * * {@link module:engine/view/upcastwriter~UpcastWriter#createSelection `UpcastWriter#createSelection()`}.
  *
  * ```ts
  * // Creates empty selection without ranges.
  * const selection = writer.createSelection();
  *
  * // Creates selection at the given range.
  * const range = writer.createRange( start, end );
  * const selection = writer.createSelection( range );
  *
  * // Creates selection at the given ranges
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( star2, end2 ) ];
  * const selection = writer.createSelection( ranges );
  *
  * // Creates selection from the other selection.
  * const otherSelection = writer.createSelection();
  * const selection = writer.createSelection( otherSelection );
  *
  * // Creates selection from the document selection.
  * const selection = writer.createSelection( editor.editing.view.document.selection );
  *
  * // Creates selection at the given position.
  * const position = writer.createPositionFromPath( root, path );
  * const selection = writer.createSelection( position );
  *
  * // Creates collapsed selection at the position of given item and offset.
  * const paragraph = writer.createContainerElement( 'paragraph' );
  * const selection = writer.createSelection( paragraph, offset );
  *
  * // Creates a range inside an {@link module:engine/view/element~Element element} which starts before the
  * // first child of that element and ends after the last child of that element.
  * const selection = writer.createSelection( paragraph, 'in' );
  *
  * // Creates a range on an {@link module:engine/view/item~Item item} which starts before the item and ends
  * // just after the item.
  * const selection = writer.createSelection( paragraph, 'on' );
  * ```
  *
  * `Selection`'s constructor allow passing additional options (`backward`, `fake` and `label`) as the last argument.
  *
  * ```ts
  * // Creates backward selection.
  * const selection = writer.createSelection( range, { backward: true } );
  * ```
  *
  * Fake selection does not render as browser native selection over selected elements and is hidden to the user.
  * This way, no native selection UI artifacts are displayed to the user and selection over elements can be
  * represented in other way, for example by applying proper CSS class.
  *
  * Additionally fake's selection label can be provided. It will be used to describe fake selection in DOM
  * (and be  properly handled by screen readers).
  *
  * ```ts
  * // Creates fake selection with label.
  * const selection = writer.createSelection( range, { fake: true, label: 'foo' } );
  * ```
  *
  * @internal
  */
  constructor(...e) {
    super();
    /**
    * Stores all ranges that are selected.
    */
    h(this, "_ranges");
    /**
    * Specifies whether the last added range was added as a backward or forward range.
    */
    h(this, "_lastRangeBackward");
    /**
    * Specifies whether selection instance is fake.
    */
    h(this, "_isFake");
    /**
    * Fake selection's label.
    */
    h(this, "_fakeSelectionLabel");
    this._ranges = [], this._lastRangeBackward = !1, this._isFake = !1, this._fakeSelectionLabel = "", e.length && this.setTo(...e);
  }
  /**
  * Returns true if selection instance is marked as `fake`.
  *
  * @see #setTo
  */
  get isFake() {
    return this._isFake;
  }
  /**
  * Returns fake selection label.
  *
  * @see #setTo
  */
  get fakeSelectionLabel() {
    return this._fakeSelectionLabel;
  }
  /**
  * Selection anchor. Anchor may be described as a position where the selection starts. Together with
  * {@link #focus focus} they define the direction of selection, which is important
  * when expanding/shrinking selection. Anchor is always the start or end of the most recent added range.
  * It may be a bit unintuitive when there are multiple ranges in selection.
  *
  * @see #focus
  */
  get anchor() {
    if (!this._ranges.length)
      return null;
    const e = this._ranges[this._ranges.length - 1];
    return (this._lastRangeBackward ? e.end : e.start).clone();
  }
  /**
  * Selection focus. Focus is a position where the selection ends.
  *
  * @see #anchor
  */
  get focus() {
    if (!this._ranges.length)
      return null;
    const e = this._ranges[this._ranges.length - 1];
    return (this._lastRangeBackward ? e.start : e.end).clone();
  }
  /**
  * Returns whether the selection is collapsed. Selection is collapsed when there is exactly one range which is
  * collapsed.
  */
  get isCollapsed() {
    return this.rangeCount === 1 && this._ranges[0].isCollapsed;
  }
  /**
  * Returns number of ranges in selection.
  */
  get rangeCount() {
    return this._ranges.length;
  }
  /**
  * Specifies whether the {@link #focus} precedes {@link #anchor}.
  */
  get isBackward() {
    return !this.isCollapsed && this._lastRangeBackward;
  }
  /**
  * {@link module:engine/view/editableelement~EditableElement EditableElement} instance that contains this selection, or `null`
  * if the selection is not inside an editable element.
  */
  get editableElement() {
    return this.anchor ? this.anchor.editableElement : null;
  }
  /**
  * Returns an iterable that contains copies of all ranges added to the selection.
  */
  *getRanges() {
    for (const e of this._ranges)
      yield e.clone();
  }
  /**
  * Returns copy of the first range in the selection. First range is the one which
  * {@link module:engine/view/range~Range#start start} position {@link module:engine/view/position~Position#isBefore is before} start
  * position of all other ranges (not to confuse with the first range added to the selection).
  * Returns `null` if no ranges are added to selection.
  */
  getFirstRange() {
    let e = null;
    for (const n of this._ranges)
      (!e || n.start.isBefore(e.start)) && (e = n);
    return e ? e.clone() : null;
  }
  /**
  * Returns copy of the last range in the selection. Last range is the one which {@link module:engine/view/range~Range#end end}
  * position {@link module:engine/view/position~Position#isAfter is after} end position of all other ranges (not to confuse
  * with the last range added to the selection). Returns `null` if no ranges are added to selection.
  */
  getLastRange() {
    let e = null;
    for (const n of this._ranges)
      (!e || n.end.isAfter(e.end)) && (e = n);
    return e ? e.clone() : null;
  }
  /**
  * Returns copy of the first position in the selection. First position is the position that
  * {@link module:engine/view/position~Position#isBefore is before} any other position in the selection ranges.
  * Returns `null` if no ranges are added to selection.
  */
  getFirstPosition() {
    const e = this.getFirstRange();
    return e ? e.start.clone() : null;
  }
  /**
  * Returns copy of the last position in the selection. Last position is the position that
  * {@link module:engine/view/position~Position#isAfter is after} any other position in the selection ranges.
  * Returns `null` if no ranges are added to selection.
  */
  getLastPosition() {
    const e = this.getLastRange();
    return e ? e.end.clone() : null;
  }
  /**
  * Checks whether, this selection is equal to given selection. Selections are equal if they have same directions,
  * same number of ranges and all ranges from one selection equal to a range from other selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are equal, `false` otherwise.
  */
  isEqual(e) {
    if (this.isFake != e.isFake || this.isFake && this.fakeSelectionLabel != e.fakeSelectionLabel || this.rangeCount != e.rangeCount)
      return !1;
    if (this.rangeCount === 0)
      return !0;
    if (!this.anchor.isEqual(e.anchor) || !this.focus.isEqual(e.focus))
      return !1;
    for (const n of this._ranges) {
      let r = !1;
      for (const s of e._ranges)
        if (n.isEqual(s)) {
          r = !0;
          break;
        }
      if (!r)
        return !1;
    }
    return !0;
  }
  /**
  * Checks whether this selection is similar to given selection. Selections are similar if they have same directions, same
  * number of ranges, and all {@link module:engine/view/range~Range#getTrimmed trimmed} ranges from one selection are
  * equal to any trimmed range from other selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are similar, `false` otherwise.
  */
  isSimilar(e) {
    if (this.isBackward != e.isBackward)
      return !1;
    const n = em(this.getRanges()), r = em(e.getRanges());
    if (n != r)
      return !1;
    if (n == 0)
      return !0;
    for (let s of this.getRanges()) {
      s = s.getTrimmed();
      let o = !1;
      for (let a of e.getRanges())
        if (a = a.getTrimmed(), s.start.isEqual(a.start) && s.end.isEqual(a.end)) {
          o = !0;
          break;
        }
      if (!o)
        return !1;
    }
    return !0;
  }
  /**
  * Returns the selected element. {@link module:engine/view/element~Element Element} is considered as selected if there is only
  * one range in the selection, and that range contains exactly one element.
  * Returns `null` if there is no selected element.
  */
  getSelectedElement() {
    return this.rangeCount !== 1 ? null : this.getFirstRange().getContainedElement();
  }
  /**
  * Sets this selection's ranges and direction to the specified location based on the given
  * {@link module:engine/view/selection~Selectable selectable}.
  *
  * ```ts
  * // Sets selection to the given range.
  * const range = writer.createRange( start, end );
  * selection.setTo( range );
  *
  * // Sets selection to given ranges.
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( star2, end2 ) ];
  * selection.setTo( range );
  *
  * // Sets selection to the other selection.
  * const otherSelection = writer.createSelection();
  * selection.setTo( otherSelection );
  *
  * // Sets selection to contents of DocumentSelection.
  * selection.setTo( editor.editing.view.document.selection );
  *
  * // Sets collapsed selection at the given position.
  * const position = writer.createPositionAt( root, path );
  * selection.setTo( position );
  *
  * // Sets collapsed selection at the position of given item and offset.
  * selection.setTo( paragraph, offset );
  * ```
  *
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * ```ts
  * selection.setTo( paragraph, 'in' );
  * ```
  *
  * Creates a range on an {@link module:engine/view/item~Item item} which starts before the item and ends just after the item.
  *
  * ```ts
  * selection.setTo( paragraph, 'on' );
  *
  * // Clears selection. Removes all ranges.
  * selection.setTo( null );
  * ```
  *
  * `Selection#setTo()` method allow passing additional options (`backward`, `fake` and `label`) as the last argument.
  *
  * ```ts
  * // Sets selection as backward.
  * selection.setTo( range, { backward: true } );
  * ```
  *
  * Fake selection does not render as browser native selection over selected elements and is hidden to the user.
  * This way, no native selection UI artifacts are displayed to the user and selection over elements can be
  * represented in other way, for example by applying proper CSS class.
  *
  * Additionally fake's selection label can be provided. It will be used to describe fake selection in DOM
  * (and be  properly handled by screen readers).
  *
  * ```ts
  * // Creates fake selection with label.
  * selection.setTo( range, { fake: true, label: 'foo' } );
  * ```
  *
  * @fires change
  */
  setTo(...e) {
    let [n, r, s] = e;
    if (typeof r == "object" && (s = r, r = void 0), n === null)
      this._setRanges([]), this._setFakeOptions(s);
    else if (n instanceof D2 || n instanceof tp)
      this._setRanges(n.getRanges(), n.isBackward), this._setFakeOptions({
        fake: n.isFake,
        label: n.fakeSelectionLabel
      });
    else if (n instanceof qe)
      this._setRanges([
        n
      ], s && s.backward), this._setFakeOptions(s);
    else if (n instanceof fe)
      this._setRanges([
        new qe(n)
      ]), this._setFakeOptions(s);
    else if (n instanceof to) {
      const o = !!s && !!s.backward;
      let a;
      if (r === void 0)
        throw new T("view-selection-setto-required-second-parameter", this);
      r == "in" ? a = qe._createIn(n) : r == "on" ? a = qe._createOn(n) : a = new qe(fe._createAt(n, r)), this._setRanges([
        a
      ], o), this._setFakeOptions(s);
    } else if (ki(n))
      this._setRanges(n, s && s.backward), this._setFakeOptions(s);
    else
      throw new T("view-selection-setto-not-selectable", this);
    this.fire("change");
  }
  /**
  * Moves {@link #focus} to the specified location.
  *
  * The location can be specified in the same form as {@link module:engine/view/view~View#createPositionAt view.createPositionAt()}
  * parameters.
  *
  * @fires change
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  setFocus(e, n) {
    if (this.anchor === null)
      throw new T("view-selection-setfocus-no-ranges", this);
    const r = fe._createAt(e, n);
    if (r.compareWith(this.focus) == "same")
      return;
    const s = this.anchor;
    this._ranges.pop(), r.compareWith(s) == "before" ? this._addRange(new qe(r, s), !0) : this._addRange(new qe(s, r)), this.fire("change");
  }
  /**
  * Replaces all ranges that were added to the selection with given array of ranges. Last range of the array
  * is treated like the last added range and is used to set {@link #anchor anchor} and {@link #focus focus}.
  * Accepts a flag describing in which way the selection is made.
  *
  * @param newRanges Iterable object of ranges to set.
  * @param isLastBackward Flag describing if last added range was selected forward - from start to end
  * (`false`) or backward - from end to start (`true`). Defaults to `false`.
  */
  _setRanges(e, n = !1) {
    e = Array.from(e), this._ranges = [];
    for (const r of e)
      this._addRange(r);
    this._lastRangeBackward = !!n;
  }
  /**
  * Sets this selection instance to be marked as `fake`. A fake selection does not render as browser native selection
  * over selected elements and is hidden to the user. This way, no native selection UI artifacts are displayed to
  * the user and selection over elements can be represented in other way, for example by applying proper CSS class.
  *
  * Additionally fake's selection label can be provided. It will be used to describe fake selection in DOM (and be
  * properly handled by screen readers).
  */
  _setFakeOptions(e = {}) {
    this._isFake = !!e.fake, this._fakeSelectionLabel = e.fake && e.label || "";
  }
  /**
  * Adds a range to the selection. Added range is copied. This means that passed range is not saved in the
  * selection instance and you can safely operate on it.
  *
  * Accepts a flag describing in which way the selection is made - passed range might be selected from
  * {@link module:engine/view/range~Range#start start} to {@link module:engine/view/range~Range#end end}
  * or from {@link module:engine/view/range~Range#end end} to {@link module:engine/view/range~Range#start start}.
  * The flag is used to set {@link #anchor anchor} and {@link #focus focus} properties.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-selection-range-intersects` if added range intersects
  * with ranges already stored in Selection instance.
  */
  _addRange(e, n = !1) {
    if (!(e instanceof qe))
      throw new T("view-selection-add-range-not-range", this);
    this._pushRange(e), this._lastRangeBackward = !!n;
  }
  /**
  * Adds range to selection - creates copy of given range so it can be safely used and modified.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-selection-range-intersects` if added range intersects
  * with ranges already stored in selection instance.
  */
  _pushRange(e) {
    for (const n of this._ranges)
      if (e.isIntersecting(n))
        throw new T("view-selection-range-intersects", this, {
          addedRange: e,
          intersectingRange: n
        });
    this._ranges.push(new qe(e.start, e.end));
  }
};
Dr.prototype.is = function(i) {
  return i === "selection" || i === "view:selection";
};
let tp = class extends (/* @__PURE__ */ ht(eo)) {
  constructor(...e) {
    super();
    /**
    * Selection is used internally (`DocumentSelection` is a proxy to that selection).
    */
    h(this, "_selection");
    this._selection = new Dr(), this._selection.delegate("change").to(this), e.length && this._selection.setTo(...e);
  }
  /**
  * Returns true if selection instance is marked as `fake`.
  *
  * @see #_setTo
  */
  get isFake() {
    return this._selection.isFake;
  }
  /**
  * Returns fake selection label.
  *
  * @see #_setTo
  */
  get fakeSelectionLabel() {
    return this._selection.fakeSelectionLabel;
  }
  /**
  * Selection anchor. Anchor may be described as a position where the selection starts. Together with
  * {@link #focus focus} they define the direction of selection, which is important
  * when expanding/shrinking selection. Anchor is always the start or end of the most recent added range.
  * It may be a bit unintuitive when there are multiple ranges in selection.
  *
  * @see #focus
  */
  get anchor() {
    return this._selection.anchor;
  }
  /**
  * Selection focus. Focus is a position where the selection ends.
  *
  * @see #anchor
  */
  get focus() {
    return this._selection.focus;
  }
  /**
  * Returns whether the selection is collapsed. Selection is collapsed when there is exactly one range which is
  * collapsed.
  */
  get isCollapsed() {
    return this._selection.isCollapsed;
  }
  /**
  * Returns number of ranges in selection.
  */
  get rangeCount() {
    return this._selection.rangeCount;
  }
  /**
  * Specifies whether the {@link #focus} precedes {@link #anchor}.
  */
  get isBackward() {
    return this._selection.isBackward;
  }
  /**
  * {@link module:engine/view/editableelement~EditableElement EditableElement} instance that contains this selection, or `null`
  * if the selection is not inside an editable element.
  */
  get editableElement() {
    return this._selection.editableElement;
  }
  /**
  * Used for the compatibility with the {@link module:engine/view/selection~Selection#isEqual} method.
  *
  * @internal
  */
  get _ranges() {
    return this._selection._ranges;
  }
  /**
  * Returns an iterable that contains copies of all ranges added to the selection.
  */
  *getRanges() {
    yield* this._selection.getRanges();
  }
  /**
  * Returns copy of the first range in the selection. First range is the one which
  * {@link module:engine/view/range~Range#start start} position {@link module:engine/view/position~Position#isBefore is before} start
  * position of all other ranges (not to confuse with the first range added to the selection).
  * Returns `null` if no ranges are added to selection.
  */
  getFirstRange() {
    return this._selection.getFirstRange();
  }
  /**
  * Returns copy of the last range in the selection. Last range is the one which {@link module:engine/view/range~Range#end end}
  * position {@link module:engine/view/position~Position#isAfter is after} end position of all other ranges (not to confuse
  * with the last range added to the selection). Returns `null` if no ranges are added to selection.
  */
  getLastRange() {
    return this._selection.getLastRange();
  }
  /**
  * Returns copy of the first position in the selection. First position is the position that
  * {@link module:engine/view/position~Position#isBefore is before} any other position in the selection ranges.
  * Returns `null` if no ranges are added to selection.
  */
  getFirstPosition() {
    return this._selection.getFirstPosition();
  }
  /**
  * Returns copy of the last position in the selection. Last position is the position that
  * {@link module:engine/view/position~Position#isAfter is after} any other position in the selection ranges.
  * Returns `null` if no ranges are added to selection.
  */
  getLastPosition() {
    return this._selection.getLastPosition();
  }
  /**
  * Returns the selected element. {@link module:engine/view/element~Element Element} is considered as selected if there is only
  * one range in the selection, and that range contains exactly one element.
  * Returns `null` if there is no selected element.
  */
  getSelectedElement() {
    return this._selection.getSelectedElement();
  }
  /**
  * Checks whether, this selection is equal to given selection. Selections are equal if they have same directions,
  * same number of ranges and all ranges from one selection equal to a range from other selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are equal, `false` otherwise.
  */
  isEqual(e) {
    return this._selection.isEqual(e);
  }
  /**
  * Checks whether this selection is similar to given selection. Selections are similar if they have same directions, same
  * number of ranges, and all {@link module:engine/view/range~Range#getTrimmed trimmed} ranges from one selection are
  * equal to any trimmed range from other selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are similar, `false` otherwise.
  */
  isSimilar(e) {
    return this._selection.isSimilar(e);
  }
  /**
  * Sets this selection's ranges and direction to the specified location based on the given
  * {@link module:engine/view/selection~Selectable selectable}.
  *
  * ```ts
  * // Sets selection to the given range.
  * const range = writer.createRange( start, end );
  * documentSelection._setTo( range );
  *
  * // Sets selection to given ranges.
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( start2, end2 ) ];
  * documentSelection._setTo( range );
  *
  * // Sets selection to the other selection.
  * const otherSelection = writer.createSelection();
  * documentSelection._setTo( otherSelection );
  *
  * // Sets collapsed selection at the given position.
  * const position = writer.createPositionAt( root, offset );
  * documentSelection._setTo( position );
  *
  * // Sets collapsed selection at the position of given item and offset.
  * documentSelection._setTo( paragraph, offset );
  * ```
  *
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * ```ts
  * documentSelection._setTo( paragraph, 'in' );
  * ```
  *
  * Creates a range on an {@link module:engine/view/item~Item item} which starts before the item and ends just after the item.
  *
  * ```ts
  * documentSelection._setTo( paragraph, 'on' );
  *
  * // Clears selection. Removes all ranges.
  * documentSelection._setTo( null );
  * ```
  *
  * `Selection#_setTo()` method allow passing additional options (`backward`, `fake` and `label`) as the last argument.
  *
  * ```ts
  * // Sets selection as backward.
  * documentSelection._setTo( range, { backward: true } );
  * ```
  *
  * Fake selection does not render as browser native selection over selected elements and is hidden to the user.
  * This way, no native selection UI artifacts are displayed to the user and selection over elements can be
  * represented in other way, for example by applying proper CSS class.
  *
  * Additionally fake's selection label can be provided. It will be used to des cribe fake selection in DOM
  * (and be  properly handled by screen readers).
  *
  * ```ts
  * // Creates fake selection with label.
  * documentSelection._setTo( range, { fake: true, label: 'foo' } );
  * ```
  *
  * @internal
  * @fires change
  */
  _setTo(...e) {
    this._selection.setTo(...e);
  }
  /**
  * Moves {@link #focus} to the specified location.
  *
  * The location can be specified in the same form as {@link module:engine/view/view~View#createPositionAt view.createPositionAt()}
  * parameters.
  *
  * @internal
  * @fires change
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  _setFocus(e, n) {
    this._selection.setFocus(e, n);
  }
};
tp.prototype.is = function(i) {
  return i === "selection" || i == "documentSelection" || i == "view:selection" || i == "view:documentSelection";
};
class ga extends Br {
  /**
  * @param source The emitter.
  * @param name The event name.
  * @param startRange The view range that the bubbling should start from.
  */
  constructor(e, n, r) {
    super(e, n);
    /**
    * The view range that the bubbling should start from.
    */
    h(this, "startRange");
    /**
    * The current event phase.
    */
    h(this, "_eventPhase");
    /**
    * The current bubbling target.
    */
    h(this, "_currentTarget");
    this.startRange = r, this._eventPhase = "none", this._currentTarget = null;
  }
  /**
  * The current event phase.
  */
  get eventPhase() {
    return this._eventPhase;
  }
  /**
  * The current bubbling target.
  */
  get currentTarget() {
    return this._currentTarget;
  }
}
const zh = Symbol("bubbling contexts");
function rm(i) {
  class t extends i {
    fire(n, ...r) {
      try {
        const s = n instanceof Br ? n : new Br(this, n), o = Hh(this);
        if (!o.size)
          return;
        if (La(s, "capturing", this), go(o, "$capture", s, ...r))
          return s.return;
        const a = s.startRange || this.selection.getFirstRange(), l = a ? a.getContainedElement() : null, c = l ? !!L2(o, l) : !1;
        let u = l || g6(a);
        if (La(s, "atTarget", u), !c) {
          if (go(o, "$text", s, ...r))
            return s.return;
          La(s, "bubbling", u);
        }
        for (; u; ) {
          if (u.is("rootElement")) {
            if (go(o, "$root", s, ...r))
              return s.return;
          } else if (u.is("element") && go(o, u.name, s, ...r))
            return s.return;
          if (go(o, u, s, ...r))
            return s.return;
          u = u.parent, La(s, "bubbling", u);
        }
        return La(s, "bubbling", this), go(o, "$document", s, ...r), s.return;
      } catch (s) {
        /* istanbul ignore next -- @preserve */
        T.rethrowUnexpectedError(s, this);
      }
    }
    _addEventListener(n, r, s) {
      const o = tt(s.context || "$document"), a = Hh(this);
      for (const l of o) {
        let c = a.get(l);
        c || (c = new (ht())(), a.set(l, c)), this.listenTo(c, n, r, s);
      }
    }
    _removeEventListener(n, r) {
      const s = Hh(this);
      for (const o of s.values())
        this.stopListening(o, n, r);
    }
  }
  return t;
}
{
  const i = rm(Object);
  [
    "fire",
    "_addEventListener",
    "_removeEventListener"
  ].forEach((t) => {
    rm[t] = i.prototype[t];
  });
}
function La(i, t, e) {
  i instanceof ga && (i._eventPhase = t, i._currentTarget = e);
}
function go(i, t, e, ...n) {
  const r = typeof t == "string" ? i.get(t) : L2(i, t);
  return r ? (r.fire(e, ...n), e.stop.called) : !1;
}
function L2(i, t) {
  for (const [e, n] of i)
    if (typeof e == "function" && e(t))
      return n;
  return null;
}
function Hh(i) {
  return i[zh] || (i[zh] = /* @__PURE__ */ new Map()), i[zh];
}
function g6(i) {
  if (!i)
    return null;
  const t = i.start.parent, e = i.end.parent, n = t.getPath(), r = e.getPath();
  return n.length > r.length ? t : e;
}
let Fd = class extends (/* @__PURE__ */ rm(/* @__PURE__ */ st())) {
  /**
  * Creates a Document instance.
  *
  * @param stylesProcessor The styles processor instance.
  */
  constructor(e) {
    super();
    /**
    * Selection done on this document.
    */
    h(this, "selection");
    /**
    * Roots of the view tree. Collection of the {@link module:engine/view/element~Element view elements}.
    *
    * View roots are created as a result of binding between {@link module:engine/view/document~Document#roots} and
    * {@link module:engine/model/document~Document#roots} and this is handled by
    * {@link module:engine/controller/editingcontroller~EditingController}, so to create view root we need to create
    * model root using {@link module:engine/model/document~Document#createRoot}.
    */
    h(this, "roots");
    /**
    * The styles processor instance used by this document when normalizing styles.
    */
    h(this, "stylesProcessor");
    /**
    * Post-fixer callbacks registered to the view document.
    */
    h(this, "_postFixers", /* @__PURE__ */ new Set());
    this.selection = new tp(), this.roots = new Pn({
      idProperty: "rootName"
    }), this.stylesProcessor = e, this.set("isReadOnly", !1), this.set("isFocused", !1), this.set("isSelecting", !1), this.set("isComposing", !1);
  }
  /**
  * Gets a {@link module:engine/view/document~Document#roots view root element} with the specified name. If the name is not
  * specific "main" root is returned.
  *
  * @param name Name of the root.
  * @returns The view root element with the specified name or null when there is no root of given name.
  */
  getRoot(e = "main") {
    return this.roots.get(e);
  }
  /**
  * Allows registering post-fixer callbacks. A post-fixers mechanism allows to update the view tree just before it is rendered
  * to the DOM.
  *
  * Post-fixers are executed right after all changes from the outermost change block were applied but
  * before the {@link module:engine/view/view~View#event:render render event} is fired. If a post-fixer callback made
  * a change, it should return `true`. When this happens, all post-fixers are fired again to check if something else should
  * not be fixed in the new document tree state.
  *
  * View post-fixers are useful when you want to apply some fixes whenever the view structure changes. Keep in mind that
  * changes executed in a view post-fixer should not break model-view mapping.
  *
  * The types of changes which should be safe:
  *
  * * adding or removing attribute from elements,
  * * changes inside of {@link module:engine/view/uielement~UIElement UI elements},
  * * {@link module:engine/controller/editingcontroller~EditingController#reconvertItem marking some of the model elements to be
  * re-converted}.
  *
  * Try to avoid changes which touch view structure:
  *
  * * you should not add or remove nor wrap or unwrap any view elements,
  * * you should not change the editor data model in a view post-fixer.
  *
  * As a parameter, a post-fixer callback receives a {@link module:engine/view/downcastwriter~DowncastWriter downcast writer}.
  *
  * Typically, a post-fixer will look like this:
  *
  * ```ts
  * editor.editing.view.document.registerPostFixer( writer => {
  * 	if ( checkSomeCondition() ) {
  * 		writer.doSomething();
  *
  * 		// Let other post-fixers know that something changed.
  * 		return true;
  * 	}
  *
  * 	return false;
  * } );
  * ```
  *
  * Note that nothing happens right after you register a post-fixer (e.g. execute such a code in the console).
  * That is because adding a post-fixer does not execute it.
  * The post-fixer will be executed as soon as any change in the document needs to cause its rendering.
  * If you want to re-render the editor's view after registering the post-fixer then you should do it manually by calling
  * {@link module:engine/view/view~View#forceRender `view.forceRender()`}.
  *
  * If you need to register a callback which is executed when DOM elements are already updated,
  * use {@link module:engine/view/view~View#event:render render event}.
  */
  registerPostFixer(e) {
    this._postFixers.add(e);
  }
  /**
  * Destroys this instance. Makes sure that all observers are destroyed and listeners removed.
  */
  destroy() {
    this.roots.forEach((e) => e.destroy()), this.stopListening();
  }
  /**
  * Performs post-fixer loops. Executes post-fixer callbacks as long as none of them has done any changes to the model.
  *
  * @internal
  */
  _callPostFixers(e) {
    let n = !1;
    do
      for (const r of this._postFixers)
        if (n = r(e), n)
          break;
    while (n);
  }
};
const P1 = 10;
class Ns extends Ji {
  /**
  * Creates an attribute element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createAttributeElement
  * @see module:engine/view/element~Element
  * @protected
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(e, n, r, s) {
    super(e, n, r, s);
    /**
    * Element priority. Decides in what order elements are wrapped by {@link module:engine/view/downcastwriter~DowncastWriter}.
    *
    * @internal
    * @readonly
    */
    h(this, "_priority", P1);
    /**
    * Element identifier. If set, it is used by {@link module:engine/view/element~Element#isSimilar},
    * and then two elements are considered similar if, and only if they have the same `_id`.
    *
    * @internal
    * @readonly
    */
    h(this, "_id", null);
    /**
    * Keeps all the attribute elements that have the same {@link module:engine/view/attributeelement~AttributeElement#id ids}
    * and still exist in the view tree.
    *
    * This property is managed by {@link module:engine/view/downcastwriter~DowncastWriter}.
    */
    h(this, "_clonesGroup", null);
    this.getFillerOffset = p6;
  }
  /**
  * Element priority. Decides in what order elements are wrapped by {@link module:engine/view/downcastwriter~DowncastWriter}.
  */
  get priority() {
    return this._priority;
  }
  /**
  * Element identifier. If set, it is used by {@link module:engine/view/element~Element#isSimilar},
  * and then two elements are considered similar if, and only if they have the same `id`.
  */
  get id() {
    return this._id;
  }
  /**
  * Returns all {@link module:engine/view/attributeelement~AttributeElement attribute elements} that has the
  * same {@link module:engine/view/attributeelement~AttributeElement#id id} and are in the view tree (were not removed).
  *
  * Note: If this element has been removed from the tree, returned set will not include it.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError attribute-element-get-elements-with-same-id-no-id}
  * if this element has no `id`.
  *
  * @returns Set containing all the attribute elements
  * with the same `id` that were added and not removed from the view tree.
  */
  getElementsWithSameId() {
    if (this.id === null)
      throw new T("attribute-element-get-elements-with-same-id-no-id", this);
    return new Set(this._clonesGroup);
  }
  /**
  * Checks if this element is similar to other element.
  *
  * If none of elements has set {@link module:engine/view/attributeelement~AttributeElement#id}, then both elements
  * should have the same name, attributes and priority to be considered as similar. Two similar elements can contain
  * different set of children nodes.
  *
  * If at least one element has {@link module:engine/view/attributeelement~AttributeElement#id} set, then both
  * elements have to have the same {@link module:engine/view/attributeelement~AttributeElement#id} value to be
  * considered similar.
  *
  * Similarity is important for {@link module:engine/view/downcastwriter~DowncastWriter}. For example:
  *
  * * two following similar elements can be merged together into one, longer element,
  * * {@link module:engine/view/downcastwriter~DowncastWriter#unwrap} checks similarity of passed element and processed element to
  * decide whether processed element should be unwrapped,
  * * etc.
  */
  isSimilar(e) {
    return this.id !== null || e.id !== null ? this.id === e.id : super.isSimilar(e) && this.priority == e.priority;
  }
  /**
  * Clones provided element with priority.
  *
  * @internal
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any children.
  * @returns Clone of this element.
  */
  _clone(e = !1) {
    const n = super._clone(e);
    return n._priority = this._priority, n._id = this._id, n;
  }
}
h(Ns, "DEFAULT_PRIORITY", P1);
Ns.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "attributeElement" || i === "view:attributeElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "element" || i === "view:element") : i === "attributeElement" || i === "view:attributeElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "element" || i === "view:element" || i === "node" || i === "view:node";
};
function p6() {
  if (jh(this))
    return null;
  let i = this.parent;
  for (; i && i.is("attributeElement"); ) {
    if (jh(i) > 1)
      return null;
    i = i.parent;
  }
  return !i || jh(i) > 1 ? null : this.childCount;
}
function jh(i) {
  return Array.from(i.getChildren()).filter((t) => !t.is("uiElement")).length;
}
class np extends Ji {
  /**
  * Creates new instance of EmptyElement.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-emptyelement-cannot-add` when third parameter is passed,
  * to inform that usage of EmptyElement is incorrect (adding child nodes to EmptyElement is forbidden).
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createEmptyElement
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, n, r) {
    super(t, e, n, r), this.getFillerOffset = b6;
  }
  /**
  * Overrides {@link module:engine/view/element~Element#_insertChild} method.
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-emptyelement-cannot-add` to prevent
  * adding any child nodes to EmptyElement.
  *
  * @internal
  */
  _insertChild(t, e) {
    if (e && (e instanceof to || Array.from(e).length > 0))
      throw new T("view-emptyelement-cannot-add", [
        this,
        e
      ]);
    return 0;
  }
}
np.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "emptyElement" || i === "view:emptyElement" || i === "element" || i === "view:element") : i === "emptyElement" || i === "view:emptyElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "element" || i === "view:element" || i === "node" || i === "view:node";
};
function b6() {
  return null;
}
class $d extends Ji {
  /**
  * Creates new instance of UIElement.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-uielement-cannot-add` when third parameter is passed,
  * to inform that usage of UIElement is incorrect (adding child nodes to UIElement is forbidden).
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createUIElement
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, n, r) {
    super(t, e, n, r), this.getFillerOffset = w6;
  }
  /**
  * Overrides {@link module:engine/view/element~Element#_insertChild} method.
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-uielement-cannot-add` to prevent adding any child nodes
  * to UIElement.
  *
  * @internal
  */
  _insertChild(t, e) {
    if (e && (e instanceof to || Array.from(e).length > 0))
      throw new T("view-uielement-cannot-add", [
        this,
        e
      ]);
    return 0;
  }
  /**
  * Renders this {@link module:engine/view/uielement~UIElement} to DOM. This method is called by
  * {@link module:engine/view/domconverter~DomConverter}.
  * Do not use inheritance to create custom rendering method, replace `render()` method instead:
  *
  * ```ts
  * const myUIElement = downcastWriter.createUIElement( 'span' );
  * myUIElement.render = function( domDocument, domConverter ) {
  * 	const domElement = this.toDomElement( domDocument );
  *
  * 	domConverter.setContentOf( domElement, '<b>this is ui element</b>' );
  *
  * 	return domElement;
  * };
  * ```
  *
  * If changes in your UI element should trigger some editor UI update you should call
  * the {@link module:ui/editorui/editorui~EditorUI#update `editor.ui.update()`} method
  * after rendering your UI element.
  *
  * @param domConverter Instance of the DomConverter used to optimize the output.
  */
  render(t, e) {
    return this.toDomElement(t);
  }
  /**
  * Creates DOM element based on this view UIElement.
  * Note that each time this method is called new DOM element is created.
  */
  toDomElement(t) {
    const e = t.createElement(this.name);
    for (const n of this.getAttributeKeys())
      e.setAttribute(n, this.getAttribute(n));
    return e;
  }
}
$d.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "uiElement" || i === "view:uiElement" || i === "element" || i === "view:element") : i === "uiElement" || i === "view:uiElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "element" || i === "view:element" || i === "node" || i === "view:node";
};
function v6(i) {
  i.document.on("arrowKey", (t, e) => y6(t, e, i.domConverter), {
    priority: "low"
  });
}
function w6() {
  return null;
}
function y6(i, t, e) {
  if (t.keyCode == Ke.arrowright) {
    const n = t.domTarget.ownerDocument.defaultView.getSelection(), r = n.rangeCount == 1 && n.getRangeAt(0).collapsed;
    if (r || t.shiftKey) {
      const s = n.focusNode, o = n.focusOffset, a = e.domPositionToView(s, o);
      if (a === null)
        return;
      let l = !1;
      const c = a.getLastMatchingPosition((u) => (u.item.is("uiElement") && (l = !0), !!(u.item.is("uiElement") || u.item.is("attributeElement"))));
      if (l) {
        const u = e.viewPositionToDom(c);
        r ? n.collapse(u.parent, u.offset) : n.extend(u.parent, u.offset);
      }
    }
  }
}
class ip extends Ji {
  /**
  * Creates a new instance of a raw element.
  *
  * Throws the `view-rawelement-cannot-add` {@link module:utils/ckeditorerror~CKEditorError CKEditorError} when the `children`
  * parameter is passed to inform that the usage of `RawElement` is incorrect (adding child nodes to `RawElement` is forbidden).
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createRawElement
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, n, r) {
    super(t, e, n, r), this.getFillerOffset = _6;
  }
  /**
  * Overrides the {@link module:engine/view/element~Element#_insertChild} method.
  * Throws the `view-rawelement-cannot-add` {@link module:utils/ckeditorerror~CKEditorError CKEditorError} to prevent
  * adding any child nodes to a raw element.
  *
  * @internal
  */
  _insertChild(t, e) {
    if (e && (e instanceof to || Array.from(e).length > 0))
      throw new T("view-rawelement-cannot-add", [
        this,
        e
      ]);
    return 0;
  }
  /**
  * This allows rendering the children of a {@link module:engine/view/rawelement~RawElement} on the DOM level.
  * This method is called by the {@link module:engine/view/domconverter~DomConverter} with the raw DOM element
  * passed as an argument, leaving the number and shape of the children up to the integrator.
  *
  * This method **must be defined** for the raw element to work:
  *
  * ```ts
  * const myRawElement = downcastWriter.createRawElement( 'div' );
  *
  * myRawElement.render = function( domElement, domConverter ) {
  * 	domConverter.setContentOf( domElement, '<b>This is the raw content of myRawElement.</b>' );
  * };
  * ```
  *
  * @param domElement The native DOM element representing the raw view element.
  * @param domConverter Instance of the DomConverter used to optimize the output.
  */
  render(t, e) {
  }
}
ip.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "rawElement" || i === "view:rawElement" || i === "element" || i === "view:element") : i === "rawElement" || i === "view:rawElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === this.name || i === "view:" + this.name || i === "element" || i === "view:element" || i === "node" || i === "view:node";
};
function _6() {
  return null;
}
let Bs = class extends (/* @__PURE__ */ ht(eo)) {
  /**
  * Creates new DocumentFragment instance.
  *
  * @internal
  * @param document The document to which this document fragment belongs.
  * @param children A list of nodes to be inserted into the created document fragment.
  */
  constructor(e, n) {
    super();
    /**
    * The document to which this document fragment belongs.
    */
    h(this, "document");
    /**
    * Array of child nodes.
    */
    h(this, "_children", []);
    /**
    * Map of custom properties.
    * Custom properties can be added to document fragment instance.
    */
    h(this, "_customProperties", /* @__PURE__ */ new Map());
    this.document = e, n && this._insertChild(0, n);
  }
  /**
  * Iterable interface.
  *
  * Iterates over nodes added to this document fragment.
  */
  [Symbol.iterator]() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Number of child nodes in this document fragment.
  */
  get childCount() {
    return this._children.length;
  }
  /**
  * Is `true` if there are no nodes inside this document fragment, `false` otherwise.
  */
  get isEmpty() {
    return this.childCount === 0;
  }
  /**
  * Artificial root of `DocumentFragment`. Returns itself. Added for compatibility reasons.
  */
  get root() {
    return this;
  }
  /**
  * Artificial parent of `DocumentFragment`. Returns `null`. Added for compatibility reasons.
  */
  get parent() {
    return null;
  }
  /**
  * Artificial element name. Returns `undefined`. Added for compatibility reasons.
  */
  get name() {
  }
  /**
  * Artificial element getFillerOffset. Returns `undefined`. Added for compatibility reasons.
  */
  get getFillerOffset() {
  }
  /**
  * Returns the custom property value for the given key.
  */
  getCustomProperty(e) {
    return this._customProperties.get(e);
  }
  /**
  * Returns an iterator which iterates over this document fragment's custom properties.
  * Iterator provides `[ key, value ]` pairs for each stored property.
  */
  *getCustomProperties() {
    yield* this._customProperties.entries();
  }
  /**
  * {@link module:engine/view/documentfragment~DocumentFragment#_insertChild Insert} a child node or a list of child nodes at the end
  * and sets the parent of these nodes to this fragment.
  *
  * @internal
  * @param items Items to be inserted.
  * @returns Number of appended nodes.
  */
  _appendChild(e) {
    return this._insertChild(this.childCount, e);
  }
  /**
  * Gets child at the given index.
  *
  * @param index Index of child.
  * @returns Child node.
  */
  getChild(e) {
    return this._children[e];
  }
  /**
  * Gets index of the given child node. Returns `-1` if child node is not found.
  *
  * @param node Child node.
  * @returns Index of the child node.
  */
  getChildIndex(e) {
    return this._children.indexOf(e);
  }
  /**
  * Gets child nodes iterator.
  *
  * @returns Child nodes iterator.
  */
  getChildren() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Inserts a child node or a list of child nodes on the given index and sets the parent of these nodes to
  * this fragment.
  *
  * @internal
  * @param index Position where nodes should be inserted.
  * @param items Items to be inserted.
  * @returns Number of inserted nodes.
  */
  _insertChild(e, n) {
    this._fireChange("children", this);
    let r = 0;
    const s = C6(this.document, n);
    for (const o of s)
      o.parent !== null && o._remove(), o.parent = this, this._children.splice(e, 0, o), e++, r++;
    return r;
  }
  /**
  * Removes number of child nodes starting at the given index and set the parent of these nodes to `null`.
  *
  * @internal
  * @param index Number of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @returns The array of removed nodes.
  */
  _removeChildren(e, n = 1) {
    this._fireChange("children", this);
    for (let r = e; r < e + n; r++)
      this._children[r].parent = null;
    return this._children.splice(e, n);
  }
  /**
  * Fires `change` event with given type of the change.
  *
  * @internal
  * @param type Type of the change.
  * @param node Changed node.
  */
  _fireChange(e, n) {
    this.fire("change:" + e, n);
  }
  /**
  * Sets a custom property. They can be used to add special data to elements.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#setCustomProperty
  * @internal
  */
  _setCustomProperty(e, n) {
    this._customProperties.set(e, n);
  }
  /**
  * Removes the custom property stored under the given key.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeCustomProperty
  * @internal
  * @returns Returns true if property was removed.
  */
  _removeCustomProperty(e) {
    return this._customProperties.delete(e);
  }
};
Bs.prototype.is = function(i) {
  return i === "documentFragment" || i === "view:documentFragment";
};
function C6(i, t) {
  return typeof t == "string" ? [
    new St(i, t)
  ] : (ki(t) || (t = [
    t
  ]), Array.from(t).map((e) => typeof e == "string" ? new St(i, e) : e instanceof cr ? new St(i, e.data) : e));
}
class N2 {
  /**
  * @param document The view document instance.
  */
  constructor(t) {
    /**
    * The view document instance in which this writer operates.
    */
    h(this, "document");
    /**
    * Holds references to the attribute groups that share the same {@link module:engine/view/attributeelement~AttributeElement#id id}.
    * The keys are `id`s, the values are `Set`s holding {@link module:engine/view/attributeelement~AttributeElement}s.
    */
    h(this, "_cloneGroups", /* @__PURE__ */ new Map());
    /**
    * The slot factory used by the `elementToStructure` downcast helper.
    */
    h(this, "_slotFactory", null);
    this.document = t;
  }
  setSelection(...t) {
    this.document.selection._setTo(...t);
  }
  /**
  * Moves {@link module:engine/view/documentselection~DocumentSelection#focus selection's focus} to the specified location.
  *
  * The location can be specified in the same form as {@link module:engine/view/view~View#createPositionAt view.createPositionAt()}
  * parameters.
  *
  * @param Offset or one of the flags. Used only when the first parameter is a {@link module:engine/view/item~Item view item}.
  */
  setSelectionFocus(t, e) {
    this.document.selection._setFocus(t, e);
  }
  /**
  * Creates a new {@link module:engine/view/documentfragment~DocumentFragment} instance.
  *
  * @param children A list of nodes to be inserted into the created document fragment.
  * @returns The created document fragment.
  */
  createDocumentFragment(t) {
    return new Bs(this.document, t);
  }
  /**
  * Creates a new {@link module:engine/view/text~Text text node}.
  *
  * ```ts
  * writer.createText( 'foo' );
  * ```
  *
  * @param data The text's data.
  * @returns The created text node.
  */
  createText(t) {
    return new St(this.document, t);
  }
  /**
  * Creates a new {@link module:engine/view/attributeelement~AttributeElement}.
  *
  * ```ts
  * writer.createAttributeElement( 'strong' );
  * writer.createAttributeElement( 'a', { href: 'foo.bar' } );
  *
  * // Make `<a>` element contain other attributes element so the `<a>` element is not broken.
  * writer.createAttributeElement( 'a', { href: 'foo.bar' }, { priority: 5 } );
  *
  * // Set `id` of a marker element so it is not joined or merged with "normal" elements.
  * writer.createAttributeElement( 'span', { class: 'my-marker' }, { id: 'marker:my' } );
  * ```
  *
  * @param name Name of the element.
  * @param attributes Element's attributes.
  * @param options Element's options.
  * @param options.priority Element's {@link module:engine/view/attributeelement~AttributeElement#priority priority}.
  * @param options.id Element's {@link module:engine/view/attributeelement~AttributeElement#id id}.
  * @param options.renderUnsafeAttributes A list of attribute names that should be rendered in the editing
  * pipeline even though they would normally be filtered out by unsafe attribute detection mechanisms.
  * @returns Created element.
  */
  createAttributeElement(t, e, n = {}) {
    const r = new Ns(this.document, t, e);
    return typeof n.priority == "number" && (r._priority = n.priority), n.id && (r._id = n.id), n.renderUnsafeAttributes && r._unsafeAttributesToRender.push(...n.renderUnsafeAttributes), r;
  }
  createContainerElement(t, e, n = {}, r = {}) {
    let s = null;
    oi(n) ? r = n : s = n;
    const o = new Rl(this.document, t, e, s);
    return r.renderUnsafeAttributes && o._unsafeAttributesToRender.push(...r.renderUnsafeAttributes), o;
  }
  /**
  * Creates a new {@link module:engine/view/editableelement~EditableElement}.
  *
  * ```ts
  * writer.createEditableElement( 'div' );
  * writer.createEditableElement( 'div', { id: 'foo-1234' } );
  * ```
  *
  * Note: The editable element is to be used in the editing pipeline. Usually, together with
  * {@link module:widget/utils~toWidgetEditable `toWidgetEditable()`}.
  *
  * @param name Name of the element.
  * @param attributes Elements attributes.
  * @param options Element's options.
  * @param options.renderUnsafeAttributes A list of attribute names that should be rendered in the editing
  * pipeline even though they would normally be filtered out by unsafe attribute detection mechanisms.
  * @returns Created element.
  */
  createEditableElement(t, e, n = {}) {
    const r = new Bd(this.document, t, e);
    return n.renderUnsafeAttributes && r._unsafeAttributesToRender.push(...n.renderUnsafeAttributes), r;
  }
  /**
  * Creates a new {@link module:engine/view/emptyelement~EmptyElement}.
  *
  * ```ts
  * writer.createEmptyElement( 'img' );
  * writer.createEmptyElement( 'img', { id: 'foo-1234' } );
  * ```
  *
  * @param name Name of the element.
  * @param attributes Elements attributes.
  * @param options Element's options.
  * @param options.renderUnsafeAttributes A list of attribute names that should be rendered in the editing
  * pipeline even though they would normally be filtered out by unsafe attribute detection mechanisms.
  * @returns Created element.
  */
  createEmptyElement(t, e, n = {}) {
    const r = new np(this.document, t, e);
    return n.renderUnsafeAttributes && r._unsafeAttributesToRender.push(...n.renderUnsafeAttributes), r;
  }
  /**
  * Creates a new {@link module:engine/view/uielement~UIElement}.
  *
  * ```ts
  * writer.createUIElement( 'span' );
  * writer.createUIElement( 'span', { id: 'foo-1234' } );
  * ```
  *
  * A custom render function can be provided as the third parameter:
  *
  * ```ts
  * writer.createUIElement( 'span', null, function( domDocument ) {
  * 	const domElement = this.toDomElement( domDocument );
  * 	domElement.innerHTML = '<b>this is ui element</b>';
  *
  * 	return domElement;
  * } );
  * ```
  *
  * Unlike {@link #createRawElement raw elements}, UI elements are by no means editor content, for instance,
  * they are ignored by the editor selection system.
  *
  * You should not use UI elements as data containers. Check out {@link #createRawElement} instead.
  *
  * @param name The name of the element.
  * @param attributes Element attributes.
  * @param renderFunction A custom render function.
  * @returns The created element.
  */
  createUIElement(t, e, n) {
    const r = new $d(this.document, t, e);
    return n && (r.render = n), r;
  }
  /**
  * Creates a new {@link module:engine/view/rawelement~RawElement}.
  *
  * ```ts
  * writer.createRawElement( 'span', { id: 'foo-1234' }, function( domElement ) {
  * 	domElement.innerHTML = '<b>This is the raw content of the raw element.</b>';
  * } );
  * ```
  *
  * Raw elements work as data containers ("wrappers", "sandboxes") but their children are not managed or
  * even recognized by the editor. This encapsulation allows integrations to maintain custom DOM structures
  * in the editor content without, for instance, worrying about compatibility with other editor features.
  * Raw elements are a perfect tool for integration with external frameworks and data sources.
  *
  * Unlike {@link #createUIElement UI elements}, raw elements act like "real" editor content (similar to
  * {@link module:engine/view/containerelement~ContainerElement} or {@link module:engine/view/emptyelement~EmptyElement}),
  * and they are considered by the editor selection.
  *
  * You should not use raw elements to render the UI in the editor content. Check out {@link #createUIElement `#createUIElement()`}
  * instead.
  *
  * @param name The name of the element.
  * @param attributes Element attributes.
  * @param renderFunction A custom render function.
  * @param options Element's options.
  * @param options.renderUnsafeAttributes A list of attribute names that should be rendered in the editing
  * pipeline even though they would normally be filtered out by unsafe attribute detection mechanisms.
  * @returns The created element.
  */
  createRawElement(t, e, n, r = {}) {
    const s = new ip(this.document, t, e);
    return n && (s.render = n), r.renderUnsafeAttributes && s._unsafeAttributesToRender.push(...r.renderUnsafeAttributes), s;
  }
  /**
  * Adds or overwrites the element's attribute with a specified key and value.
  *
  * ```ts
  * writer.setAttribute( 'href', 'http://ckeditor.com', linkElement );
  * ```
  *
  * @param key The attribute key.
  * @param value The attribute value.
  */
  setAttribute(t, e, n) {
    n._setAttribute(t, e);
  }
  /**
  * Removes attribute from the element.
  *
  * ```ts
  * writer.removeAttribute( 'href', linkElement );
  * ```
  *
  * @param key Attribute key.
  */
  removeAttribute(t, e) {
    e._removeAttribute(t);
  }
  /**
  * Adds specified class to the element.
  *
  * ```ts
  * writer.addClass( 'foo', linkElement );
  * writer.addClass( [ 'foo', 'bar' ], linkElement );
  * ```
  */
  addClass(t, e) {
    e._addClass(t);
  }
  /**
  * Removes specified class from the element.
  *
  * ```ts
  * writer.removeClass( 'foo', linkElement );
  * writer.removeClass( [ 'foo', 'bar' ], linkElement );
  * ```
  */
  removeClass(t, e) {
    e._removeClass(t);
  }
  setStyle(t, e, n) {
    oi(t) && n === void 0 ? e._setStyle(t) : n._setStyle(t, e);
  }
  /**
  * Removes specified style from the element.
  *
  * ```ts
  * writer.removeStyle( 'color', element ); // Removes 'color' style.
  * writer.removeStyle( [ 'color', 'border-top' ], element ); // Removes both 'color' and 'border-top' styles.
  * ```
  *
  * **Note**: This method can work with normalized style names if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#remove `StylesMap#remove()`} for details.
  */
  removeStyle(t, e) {
    e._removeStyle(t);
  }
  /**
  * Sets a custom property on element. Unlike attributes, custom properties are not rendered to the DOM,
  * so they can be used to add special data to elements.
  */
  setCustomProperty(t, e, n) {
    n._setCustomProperty(t, e);
  }
  /**
  * Removes a custom property stored under the given key.
  *
  * @returns Returns true if property was removed.
  */
  removeCustomProperty(t, e) {
    return e._removeCustomProperty(t);
  }
  /**
  * Breaks attribute elements at the provided position or at the boundaries of a provided range. It breaks attribute elements
  * up to their first ancestor that is a container element.
  *
  * In following examples `<p>` is a container, `<b>` and `<u>` are attribute elements:
  *
  * ```html
  * <p>foo<b><u>bar{}</u></b></p> -> <p>foo<b><u>bar</u></b>[]</p>
  * <p>foo<b><u>{}bar</u></b></p> -> <p>foo{}<b><u>bar</u></b></p>
  * <p>foo<b><u>b{}ar</u></b></p> -> <p>foo<b><u>b</u></b>[]<b><u>ar</u></b></p>
  * <p><b>fo{o</b><u>ba}r</u></p> -> <p><b>fo</b><b>o</b><u>ba</u><u>r</u></b></p>
  * ```
  *
  * **Note:** {@link module:engine/view/documentfragment~DocumentFragment DocumentFragment} is treated like a container.
  *
  * **Note:** The difference between {@link module:engine/view/downcastwriter~DowncastWriter#breakAttributes breakAttributes()} and
  * {@link module:engine/view/downcastwriter~DowncastWriter#breakContainer breakContainer()} is that `breakAttributes()` breaks all
  * {@link module:engine/view/attributeelement~AttributeElement attribute elements} that are ancestors of a given `position`,
  * up to the first encountered {@link module:engine/view/containerelement~ContainerElement container element}.
  * `breakContainer()` assumes that a given `position` is directly in the container element and breaks that container element.
  *
  * Throws the `view-writer-invalid-range-container` {@link module:utils/ckeditorerror~CKEditorError CKEditorError}
  * when the {@link module:engine/view/range~Range#start start}
  * and {@link module:engine/view/range~Range#end end} positions of a passed range are not placed inside same parent container.
  *
  * Throws the `view-writer-cannot-break-empty-element` {@link module:utils/ckeditorerror~CKEditorError CKEditorError}
  * when trying to break attributes inside an {@link module:engine/view/emptyelement~EmptyElement EmptyElement}.
  *
  * Throws the `view-writer-cannot-break-ui-element` {@link module:utils/ckeditorerror~CKEditorError CKEditorError}
  * when trying to break attributes inside a {@link module:engine/view/uielement~UIElement UIElement}.
  *
  * @see module:engine/view/attributeelement~AttributeElement
  * @see module:engine/view/containerelement~ContainerElement
  * @see module:engine/view/downcastwriter~DowncastWriter#breakContainer
  * @param positionOrRange The position where to break attribute elements.
  * @returns The new position or range, after breaking the attribute elements.
  */
  breakAttributes(t) {
    return t instanceof fe ? this._breakAttributes(t) : this._breakAttributesRange(t);
  }
  /**
  * Breaks a {@link module:engine/view/containerelement~ContainerElement container view element} into two, at the given position.
  * The position has to be directly inside the container element and cannot be in the root. It does not break the conrainer view element
  * if the position is at the beginning or at the end of its parent element.
  *
  * ```html
  * <p>foo^bar</p> -> <p>foo</p><p>bar</p>
  * <div><p>foo</p>^<p>bar</p></div> -> <div><p>foo</p></div><div><p>bar</p></div>
  * <p>^foobar</p> -> ^<p>foobar</p>
  * <p>foobar^</p> -> <p>foobar</p>^
  * ```
  *
  * **Note:** The difference between {@link module:engine/view/downcastwriter~DowncastWriter#breakAttributes breakAttributes()} and
  * {@link module:engine/view/downcastwriter~DowncastWriter#breakContainer breakContainer()} is that `breakAttributes()` breaks all
  * {@link module:engine/view/attributeelement~AttributeElement attribute elements} that are ancestors of a given `position`,
  * up to the first encountered {@link module:engine/view/containerelement~ContainerElement container element}.
  * `breakContainer()` assumes that the given `position` is directly in the container element and breaks that container element.
  *
  * @see module:engine/view/attributeelement~AttributeElement
  * @see module:engine/view/containerelement~ContainerElement
  * @see module:engine/view/downcastwriter~DowncastWriter#breakAttributes
  * @param position The position where to break the element.
  * @returns The position between broken elements. If an element has not been broken,
  * the returned position is placed either before or after it.
  */
  breakContainer(t) {
    const e = t.parent;
    if (!e.is("containerElement"))
      throw new T("view-writer-break-non-container-element", this.document);
    if (!e.parent)
      throw new T("view-writer-break-root", this.document);
    if (t.isAtStart)
      return fe._createBefore(e);
    if (!t.isAtEnd) {
      const n = e._clone(!1);
      this.insert(fe._createAfter(e), n);
      const r = new qe(t, fe._createAt(e, "end")), s = new fe(n, 0);
      this.move(r, s);
    }
    return fe._createAfter(e);
  }
  /**
  * Merges {@link module:engine/view/attributeelement~AttributeElement attribute elements}. It also merges text nodes if needed.
  * Only {@link module:engine/view/attributeelement~AttributeElement#isSimilar similar} attribute elements can be merged.
  *
  * In following examples `<p>` is a container and `<b>` is an attribute element:
  *
  * ```html
  * <p>foo[]bar</p> -> <p>foo{}bar</p>
  * <p><b>foo</b>[]<b>bar</b></p> -> <p><b>foo{}bar</b></p>
  * <p><b foo="bar">a</b>[]<b foo="baz">b</b></p> -> <p><b foo="bar">a</b>[]<b foo="baz">b</b></p>
  * ```
  *
  * It will also take care about empty attributes when merging:
  *
  * ```html
  * <p><b>[]</b></p> -> <p>[]</p>
  * <p><b>foo</b><i>[]</i><b>bar</b></p> -> <p><b>foo{}bar</b></p>
  * ```
  *
  * **Note:** Difference between {@link module:engine/view/downcastwriter~DowncastWriter#mergeAttributes mergeAttributes} and
  * {@link module:engine/view/downcastwriter~DowncastWriter#mergeContainers mergeContainers} is that `mergeAttributes` merges two
  * {@link module:engine/view/attributeelement~AttributeElement attribute elements} or {@link module:engine/view/text~Text text nodes}
  * while `mergeContainer` merges two {@link module:engine/view/containerelement~ContainerElement container elements}.
  *
  * @see module:engine/view/attributeelement~AttributeElement
  * @see module:engine/view/containerelement~ContainerElement
  * @see module:engine/view/downcastwriter~DowncastWriter#mergeContainers
  * @param position Merge position.
  * @returns Position after merge.
  */
  mergeAttributes(t) {
    const e = t.offset, n = t.parent;
    if (n.is("$text"))
      return t;
    if (n.is("attributeElement") && n.childCount === 0) {
      const o = n.parent, a = n.index;
      return n._remove(), this._removeFromClonedElementsGroup(n), this.mergeAttributes(new fe(o, a));
    }
    const r = n.getChild(e - 1), s = n.getChild(e);
    if (!r || !s)
      return t;
    if (r.is("$text") && s.is("$text"))
      return R1(r, s);
    if (r.is("attributeElement") && s.is("attributeElement") && r.isSimilar(s)) {
      const o = r.childCount;
      return r._appendChild(s.getChildren()), s._remove(), this._removeFromClonedElementsGroup(s), this.mergeAttributes(new fe(r, o));
    }
    return t;
  }
  /**
  * Merges two {@link module:engine/view/containerelement~ContainerElement container elements} that are before and after given position.
  * Precisely, the element after the position is removed and it's contents are moved to element before the position.
  *
  * ```html
  * <p>foo</p>^<p>bar</p> -> <p>foo^bar</p>
  * <div>foo</div>^<p>bar</p> -> <div>foo^bar</div>
  * ```
  *
  * **Note:** Difference between {@link module:engine/view/downcastwriter~DowncastWriter#mergeAttributes mergeAttributes} and
  * {@link module:engine/view/downcastwriter~DowncastWriter#mergeContainers mergeContainers} is that `mergeAttributes` merges two
  * {@link module:engine/view/attributeelement~AttributeElement attribute elements} or {@link module:engine/view/text~Text text nodes}
  * while `mergeContainer` merges two {@link module:engine/view/containerelement~ContainerElement container elements}.
  *
  * @see module:engine/view/attributeelement~AttributeElement
  * @see module:engine/view/containerelement~ContainerElement
  * @see module:engine/view/downcastwriter~DowncastWriter#mergeAttributes
  * @param position Merge position.
  * @returns Position after merge.
  */
  mergeContainers(t) {
    const e = t.nodeBefore, n = t.nodeAfter;
    if (!e || !n || !e.is("containerElement") || !n.is("containerElement"))
      throw new T("view-writer-merge-containers-invalid-position", this.document);
    const r = e.getChild(e.childCount - 1), s = r instanceof St ? fe._createAt(r, "end") : fe._createAt(e, "end");
    return this.move(qe._createIn(n), fe._createAt(e, "end")), this.remove(qe._createOn(n)), s;
  }
  /**
  * Inserts a node or nodes at specified position. Takes care about breaking attributes before insertion
  * and merging them afterwards.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-insert-invalid-node` when nodes to insert
  * contains instances that are not {@link module:engine/view/text~Text Texts},
  * {@link module:engine/view/attributeelement~AttributeElement AttributeElements},
  * {@link module:engine/view/containerelement~ContainerElement ContainerElements},
  * {@link module:engine/view/emptyelement~EmptyElement EmptyElements},
  * {@link module:engine/view/rawelement~RawElement RawElements} or
  * {@link module:engine/view/uielement~UIElement UIElements}.
  *
  * @param position Insertion position.
  * @param nodes Node or nodes to insert.
  * @returns Range around inserted nodes.
  */
  insert(t, e) {
    e = ki(e) ? [
      ...e
    ] : [
      e
    ], B2(e, this.document);
    const n = e.reduce((o, a) => {
      const l = o[o.length - 1], c = !a.is("uiElement");
      return !l || l.breakAttributes != c ? o.push({
        breakAttributes: c,
        nodes: [
          a
        ]
      }) : l.nodes.push(a), o;
    }, []);
    let r = null, s = t;
    for (const { nodes: o, breakAttributes: a } of n) {
      const l = this._insertNodes(s, o, a);
      r || (r = l.start), s = l.end;
    }
    return r ? new qe(r, s) : new qe(t);
  }
  /**
  * Removes provided range from the container.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-invalid-range-container` when
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions are not placed inside
  * same parent container.
  *
  * @param rangeOrItem Range to remove from container
  * or an {@link module:engine/view/item~Item item} to remove. If range is provided, after removing, it will be updated
  * to a collapsed range showing the new position.
  * @returns Document fragment containing removed nodes.
  */
  remove(t) {
    const e = t instanceof qe ? t : qe._createOn(t);
    if (Na(e, this.document), e.isCollapsed)
      return new Bs(this.document);
    const { start: n, end: r } = this._breakAttributesRange(e, !0), s = n.parent, o = r.offset - n.offset, a = s._removeChildren(n.offset, o);
    for (const c of a)
      this._removeFromClonedElementsGroup(c);
    const l = this.mergeAttributes(n);
    return e.start = l, e.end = l.clone(), new Bs(this.document, a);
  }
  /**
  * Removes matching elements from given range.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-invalid-range-container` when
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions are not placed inside
  * same parent container.
  *
  * @param range Range to clear.
  * @param element Element to remove.
  */
  clear(t, e) {
    Na(t, this.document);
    const n = t.getWalker({
      direction: "backward",
      ignoreElementEnd: !0
    });
    for (const r of n) {
      const s = r.item;
      let o;
      if (s.is("element") && e.isSimilar(s))
        o = qe._createOn(s);
      else if (!r.nextPosition.isAfter(t.start) && s.is("$textProxy")) {
        const a = s.getAncestors().find((l) => l.is("element") && e.isSimilar(l));
        a && (o = qe._createIn(a));
      }
      o && (o.end.isAfter(t.end) && (o.end = t.end), o.start.isBefore(t.start) && (o.start = t.start), this.remove(o));
    }
  }
  /**
  * Moves nodes from provided range to target position.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-invalid-range-container` when
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions are not placed inside
  * same parent container.
  *
  * @param sourceRange Range containing nodes to move.
  * @param targetPosition Position to insert.
  * @returns Range in target container. Inserted nodes are placed between
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions.
  */
  move(t, e) {
    let n;
    if (e.isAfter(t.end)) {
      e = this._breakAttributes(e, !0);
      const r = e.parent, s = r.childCount;
      t = this._breakAttributesRange(t, !0), n = this.remove(t), e.offset += r.childCount - s;
    } else
      n = this.remove(t);
    return this.insert(e, n);
  }
  /**
  * Wraps elements within range with provided {@link module:engine/view/attributeelement~AttributeElement AttributeElement}.
  * If a collapsed range is provided, it will be wrapped only if it is equal to view selection.
  *
  * If a collapsed range was passed and is same as selection, the selection
  * will be moved to the inside of the wrapped attribute element.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-invalid-range-container`
  * when {@link module:engine/view/range~Range#start}
  * and {@link module:engine/view/range~Range#end} positions are not placed inside same parent container.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-wrap-invalid-attribute` when passed attribute element is not
  * an instance of {@link module:engine/view/attributeelement~AttributeElement AttributeElement}.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-wrap-nonselection-collapsed-range` when passed range
  * is collapsed and different than view selection.
  *
  * @param range Range to wrap.
  * @param attribute Attribute element to use as wrapper.
  * @returns range Range after wrapping, spanning over wrapping attribute element.
  */
  wrap(t, e) {
    if (!(e instanceof Ns))
      throw new T("view-writer-wrap-invalid-attribute", this.document);
    if (Na(t, this.document), t.isCollapsed) {
      let n = t.start;
      n.parent.is("element") && !k6(n.parent) && (n = n.getLastMatchingPosition((s) => s.item.is("uiElement"))), n = this._wrapPosition(n, e);
      const r = this.document.selection;
      return r.isCollapsed && r.getFirstPosition().isEqual(t.start) && this.setSelection(n), new qe(n);
    } else
      return this._wrapRange(t, e);
  }
  /**
  * Unwraps nodes within provided range from attribute element.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-invalid-range-container` when
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions are not placed inside
  * same parent container.
  */
  unwrap(t, e) {
    if (!(e instanceof Ns))
      throw new T("view-writer-unwrap-invalid-attribute", this.document);
    if (Na(t, this.document), t.isCollapsed)
      return t;
    const { start: n, end: r } = this._breakAttributesRange(t, !0), s = n.parent, o = this._unwrapChildren(s, n.offset, r.offset, e), a = this.mergeAttributes(o.start);
    a.isEqual(o.start) || o.end.offset--;
    const l = this.mergeAttributes(o.end);
    return new qe(a, l);
  }
  /**
  * Renames element by creating a copy of renamed element but with changed name and then moving contents of the
  * old element to the new one. Keep in mind that this will invalidate all {@link module:engine/view/position~Position positions} which
  * has renamed element as {@link module:engine/view/position~Position#parent a parent}.
  *
  * New element has to be created because `Element#tagName` property in DOM is readonly.
  *
  * Since this function creates a new element and removes the given one, the new element is returned to keep reference.
  *
  * @param newName New name for element.
  * @param viewElement Element to be renamed.
  * @returns Element created due to rename.
  */
  rename(t, e) {
    const n = new Rl(this.document, t, e.getAttributes());
    return this.insert(fe._createAfter(e), n), this.move(qe._createIn(e), fe._createAt(n, 0)), this.remove(qe._createOn(e)), n;
  }
  /**
  * Cleans up memory by removing obsolete cloned elements group from the writer.
  *
  * Should be used whenever all {@link module:engine/view/attributeelement~AttributeElement attribute elements}
  * with the same {@link module:engine/view/attributeelement~AttributeElement#id id} are going to be removed from the view and
  * the group will no longer be needed.
  *
  * Cloned elements group are not removed automatically in case if the group is still needed after all its elements
  * were removed from the view.
  *
  * Keep in mind that group names are equal to the `id` property of the attribute element.
  *
  * @param groupName Name of the group to clear.
  */
  clearClonedElementsGroup(t) {
    this._cloneGroups.delete(t);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/view/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/view/item~Item view item} and `'before'` or `'after'` (sets position before or after given view item).
  *
  * This method is a shortcut to other constructors such as:
  *
  * * {@link #createPositionBefore},
  * * {@link #createPositionAfter},
  *
  * @param offset Offset or one of the flags. Used only when the first parameter is a {@link module:engine/view/item~Item view item}.
  */
  createPositionAt(t, e) {
    return fe._createAt(t, e);
  }
  /**
  * Creates a new position after given view item.
  *
  * @param item View item after which the position should be located.
  */
  createPositionAfter(t) {
    return fe._createAfter(t);
  }
  /**
  * Creates a new position before given view item.
  *
  * @param item View item before which the position should be located.
  */
  createPositionBefore(t) {
    return fe._createBefore(t);
  }
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * **Note:** This factory method creates its own {@link module:engine/view/position~Position} instances basing on passed values.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at `start` position.
  */
  createRange(t, e) {
    return new qe(t, e);
  }
  /**
  * Creates a range that starts before given {@link module:engine/view/item~Item view item} and ends after it.
  */
  createRangeOn(t) {
    return qe._createOn(t);
  }
  /**
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(t) {
    return qe._createIn(t);
  }
  createSelection(...t) {
    return new Dr(...t);
  }
  /**
  * Creates placeholders for child elements of the {@link module:engine/conversion/downcasthelpers~DowncastHelpers#elementToStructure
  * `elementToStructure()`} conversion helper.
  *
  * ```ts
  * const viewSlot = conversionApi.writer.createSlot();
  * const viewPosition = conversionApi.writer.createPositionAt( viewElement, 0 );
  *
  * conversionApi.writer.insert( viewPosition, viewSlot );
  * ```
  *
  * It could be filtered down to a specific subset of children (only `<foo>` model elements in this case):
  *
  * ```ts
  * const viewSlot = conversionApi.writer.createSlot( node => node.is( 'element', 'foo' ) );
  * const viewPosition = conversionApi.writer.createPositionAt( viewElement, 0 );
  *
  * conversionApi.writer.insert( viewPosition, viewSlot );
  * ```
  *
  * While providing a filtered slot, make sure to provide slots for all child nodes. A single node can not be downcasted into
  * multiple slots.
  *
  * **Note**: You should not change the order of nodes. View elements should be in the same order as model nodes.
  *
  * @param modeOrFilter The filter for child nodes.
  * @returns The slot element to be placed in to the view structure while processing
  * {@link module:engine/conversion/downcasthelpers~DowncastHelpers#elementToStructure `elementToStructure()`}.
  */
  createSlot(t = "children") {
    if (!this._slotFactory)
      throw new T("view-writer-invalid-create-slot-context", this.document);
    return this._slotFactory(this, t);
  }
  /**
  * Registers a slot factory.
  *
  * @internal
  * @param slotFactory The slot factory.
  */
  _registerSlotFactory(t) {
    this._slotFactory = t;
  }
  /**
  * Clears the registered slot factory.
  *
  * @internal
  */
  _clearSlotFactory() {
    this._slotFactory = null;
  }
  /**
  * Inserts a node or nodes at the specified position. Takes care of breaking attributes before insertion
  * and merging them afterwards if requested by the breakAttributes param.
  *
  * @param position Insertion position.
  * @param nodes Node or nodes to insert.
  * @param breakAttributes Whether attributes should be broken.
  * @returns Range around inserted nodes.
  */
  _insertNodes(t, e, n) {
    let r;
    if (n ? r = sm(t) : r = t.parent.is("$text") ? t.parent.parent : t.parent, !r)
      throw new T("view-writer-invalid-position-container", this.document);
    let s;
    n ? s = this._breakAttributes(t, !0) : s = t.parent.is("$text") ? Wh(t) : t;
    const o = r._insertChild(s.offset, e);
    for (const u of e)
      this._addToClonedElementsGroup(u);
    const a = s.getShiftedBy(o), l = this.mergeAttributes(s);
    l.isEqual(s) || a.offset--;
    const c = this.mergeAttributes(a);
    return new qe(l, c);
  }
  /**
  * Wraps children with provided `wrapElement`. Only children contained in `parent` element between
  * `startOffset` and `endOffset` will be wrapped.
  */
  _wrapChildren(t, e, n, r) {
    let s = e;
    const o = [];
    for (; s < n; ) {
      const l = t.getChild(s), c = l.is("$text"), u = l.is("attributeElement");
      if (u && this._wrapAttributeElement(r, l))
        o.push(new fe(t, s));
      else if (c || !u || x6(r, l)) {
        const d = r._clone();
        l._remove(), d._appendChild(l), t._insertChild(s, d), this._addToClonedElementsGroup(d), o.push(new fe(t, s));
      } else
        this._wrapChildren(l, 0, l.childCount, r);
      s++;
    }
    let a = 0;
    for (const l of o) {
      if (l.offset -= a, l.offset == e)
        continue;
      this.mergeAttributes(l).isEqual(l) || (a++, n--);
    }
    return qe._createFromParentsAndOffsets(t, e, t, n);
  }
  /**
  * Unwraps children from provided `unwrapElement`. Only children contained in `parent` element between
  * `startOffset` and `endOffset` will be unwrapped.
  */
  _unwrapChildren(t, e, n, r) {
    let s = e;
    const o = [];
    for (; s < n; ) {
      const l = t.getChild(s);
      if (!l.is("attributeElement")) {
        s++;
        continue;
      }
      if (l.isSimilar(r)) {
        const c = l.getChildren(), u = l.childCount;
        l._remove(), t._insertChild(s, c), this._removeFromClonedElementsGroup(l), o.push(new fe(t, s), new fe(t, s + u)), s += u, n += u - 1;
        continue;
      }
      if (this._unwrapAttributeElement(r, l)) {
        o.push(new fe(t, s), new fe(t, s + 1)), s++;
        continue;
      }
      this._unwrapChildren(l, 0, l.childCount, r), s++;
    }
    let a = 0;
    for (const l of o) {
      if (l.offset -= a, l.offset == e || l.offset == n)
        continue;
      this.mergeAttributes(l).isEqual(l) || (a++, n--);
    }
    return qe._createFromParentsAndOffsets(t, e, t, n);
  }
  /**
  * Helper function for `view.writer.wrap`. Wraps range with provided attribute element.
  * This method will also merge newly added attribute element with its siblings whenever possible.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-wrap-invalid-attribute` when passed attribute element is not
  * an instance of {@link module:engine/view/attributeelement~AttributeElement AttributeElement}.
  *
  * @returns New range after wrapping, spanning over wrapping attribute element.
  */
  _wrapRange(t, e) {
    const { start: n, end: r } = this._breakAttributesRange(t, !0), s = n.parent, o = this._wrapChildren(s, n.offset, r.offset, e), a = this.mergeAttributes(o.start);
    a.isEqual(o.start) || o.end.offset--;
    const l = this.mergeAttributes(o.end);
    return new qe(a, l);
  }
  /**
  * Helper function for {@link #wrap}. Wraps position with provided attribute element.
  * This method will also merge newly added attribute element with its siblings whenever possible.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-wrap-invalid-attribute` when passed attribute element is not
  * an instance of {@link module:engine/view/attributeelement~AttributeElement AttributeElement}.
  *
  * @returns New position after wrapping.
  */
  _wrapPosition(t, e) {
    if (e.isSimilar(t.parent))
      return I1(t.clone());
    t.parent.is("$text") && (t = Wh(t));
    const n = this.createAttributeElement("_wrapPosition-fake-element");
    n._priority = Number.POSITIVE_INFINITY, n.isSimilar = () => !1, t.parent._insertChild(t.offset, n);
    const r = new qe(t, t.getShiftedBy(1));
    this.wrap(r, e);
    const s = new fe(n.parent, n.index);
    n._remove();
    const o = s.nodeBefore, a = s.nodeAfter;
    return o instanceof St && a instanceof St ? R1(o, a) : I1(s);
  }
  /**
  * Wraps one {@link module:engine/view/attributeelement~AttributeElement AttributeElement} into another by
  * merging them if possible. When merging is possible - all attributes, styles and classes are moved from wrapper
  * element to element being wrapped.
  *
  * @param wrapper Wrapper AttributeElement.
  * @param toWrap AttributeElement to wrap using wrapper element.
  * @returns Returns `true` if elements are merged.
  */
  _wrapAttributeElement(t, e) {
    if (!O1(t, e) || t.name !== e.name || t.priority !== e.priority)
      return !1;
    for (const n of t.getAttributeKeys())
      if (!(n === "class" || n === "style") && e.hasAttribute(n) && e.getAttribute(n) !== t.getAttribute(n))
        return !1;
    for (const n of t.getStyleNames())
      if (e.hasStyle(n) && e.getStyle(n) !== t.getStyle(n))
        return !1;
    for (const n of t.getAttributeKeys())
      n === "class" || n === "style" || e.hasAttribute(n) || this.setAttribute(n, t.getAttribute(n), e);
    for (const n of t.getStyleNames())
      e.hasStyle(n) || this.setStyle(n, t.getStyle(n), e);
    for (const n of t.getClassNames())
      e.hasClass(n) || this.addClass(n, e);
    return !0;
  }
  /**
  * Unwraps {@link module:engine/view/attributeelement~AttributeElement AttributeElement} from another by removing
  * corresponding attributes, classes and styles. All attributes, classes and styles from wrapper should be present
  * inside element being unwrapped.
  *
  * @param wrapper Wrapper AttributeElement.
  * @param toUnwrap AttributeElement to unwrap using wrapper element.
  * @returns Returns `true` if elements are unwrapped.
  **/
  _unwrapAttributeElement(t, e) {
    if (!O1(t, e) || t.name !== e.name || t.priority !== e.priority)
      return !1;
    for (const n of t.getAttributeKeys())
      if (!(n === "class" || n === "style") && (!e.hasAttribute(n) || e.getAttribute(n) !== t.getAttribute(n)))
        return !1;
    if (!e.hasClass(...t.getClassNames()))
      return !1;
    for (const n of t.getStyleNames())
      if (!e.hasStyle(n) || e.getStyle(n) !== t.getStyle(n))
        return !1;
    for (const n of t.getAttributeKeys())
      n === "class" || n === "style" || this.removeAttribute(n, e);
    return this.removeClass(Array.from(t.getClassNames()), e), this.removeStyle(Array.from(t.getStyleNames()), e), !0;
  }
  /**
  * Helper function used by other `DowncastWriter` methods. Breaks attribute elements at the boundaries of given range.
  *
  * @param range Range which `start` and `end` positions will be used to break attributes.
  * @param forceSplitText If set to `true`, will break text nodes even if they are directly in container element.
  * This behavior will result in incorrect view state, but is needed by other view writing methods which then fixes view state.
  * @returns New range with located at break positions.
  */
  _breakAttributesRange(t, e = !1) {
    const n = t.start, r = t.end;
    if (Na(t, this.document), t.isCollapsed) {
      const l = this._breakAttributes(t.start, e);
      return new qe(l, l);
    }
    const s = this._breakAttributes(r, e), o = s.parent.childCount, a = this._breakAttributes(n, e);
    return s.offset += s.parent.childCount - o, new qe(a, s);
  }
  /**
  * Helper function used by other `DowncastWriter` methods. Breaks attribute elements at given position.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-cannot-break-empty-element` when break position
  * is placed inside {@link module:engine/view/emptyelement~EmptyElement EmptyElement}.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-cannot-break-ui-element` when break position
  * is placed inside {@link module:engine/view/uielement~UIElement UIElement}.
  *
  * @param position Position where to break attributes.
  * @param forceSplitText If set to `true`, will break text nodes even if they are directly in container element.
  * This behavior will result in incorrect view state, but is needed by other view writing methods which then fixes view state.
  * @returns New position after breaking the attributes.
  */
  _breakAttributes(t, e = !1) {
    const n = t.offset, r = t.parent;
    if (t.parent.is("emptyElement"))
      throw new T("view-writer-cannot-break-empty-element", this.document);
    if (t.parent.is("uiElement"))
      throw new T("view-writer-cannot-break-ui-element", this.document);
    if (t.parent.is("rawElement"))
      throw new T("view-writer-cannot-break-raw-element", this.document);
    if (!e && r.is("$text") && om(r.parent) || om(r))
      return t.clone();
    if (r.is("$text"))
      return this._breakAttributes(Wh(t), e);
    const s = r.childCount;
    if (n == s) {
      const o = new fe(r.parent, r.index + 1);
      return this._breakAttributes(o, e);
    } else if (n === 0) {
      const o = new fe(r.parent, r.index);
      return this._breakAttributes(o, e);
    } else {
      const o = r.index + 1, a = r._clone();
      r.parent._insertChild(o, a), this._addToClonedElementsGroup(a);
      const l = r.childCount - n, c = r._removeChildren(n, l);
      a._appendChild(c);
      const u = new fe(r.parent, o);
      return this._breakAttributes(u, e);
    }
  }
  /**
  * Stores the information that an {@link module:engine/view/attributeelement~AttributeElement attribute element} was
  * added to the tree. Saves the reference to the group in the given element and updates the group, so other elements
  * from the group now keep a reference to the given attribute element.
  *
  * The clones group can be obtained using {@link module:engine/view/attributeelement~AttributeElement#getElementsWithSameId}.
  *
  * Does nothing if added element has no {@link module:engine/view/attributeelement~AttributeElement#id id}.
  *
  * @param element Attribute element to save.
  */
  _addToClonedElementsGroup(t) {
    if (!t.root.is("rootElement"))
      return;
    if (t.is("element"))
      for (const r of t.getChildren())
        this._addToClonedElementsGroup(r);
    const e = t.id;
    if (!e)
      return;
    let n = this._cloneGroups.get(e);
    n || (n = /* @__PURE__ */ new Set(), this._cloneGroups.set(e, n)), n.add(t), t._clonesGroup = n;
  }
  /**
  * Removes all the information about the given {@link module:engine/view/attributeelement~AttributeElement attribute element}
  * from its clones group.
  *
  * Keep in mind, that the element will still keep a reference to the group (but the group will not keep a reference to it).
  * This allows to reference the whole group even if the element was already removed from the tree.
  *
  * Does nothing if the element has no {@link module:engine/view/attributeelement~AttributeElement#id id}.
  *
  * @param element Attribute element to remove.
  */
  _removeFromClonedElementsGroup(t) {
    if (t.is("element"))
      for (const r of t.getChildren())
        this._removeFromClonedElementsGroup(r);
    const e = t.id;
    if (!e)
      return;
    const n = this._cloneGroups.get(e);
    n && n.delete(t);
  }
}
function k6(i) {
  return Array.from(i.getChildren()).some((t) => !t.is("uiElement"));
}
function sm(i) {
  let t = i.parent;
  for (; !om(t); ) {
    if (!t)
      return;
    t = t.parent;
  }
  return t;
}
function x6(i, t) {
  return i.priority < t.priority ? !0 : i.priority > t.priority ? !1 : i.getIdentity() < t.getIdentity();
}
function I1(i) {
  const t = i.nodeBefore;
  if (t && t.is("$text"))
    return new fe(t, t.data.length);
  const e = i.nodeAfter;
  return e && e.is("$text") ? new fe(e, 0) : i;
}
function Wh(i) {
  if (i.offset == i.parent.data.length)
    return new fe(i.parent.parent, i.parent.index + 1);
  if (i.offset === 0)
    return new fe(i.parent.parent, i.parent.index);
  const t = i.parent.data.slice(i.offset);
  return i.parent._data = i.parent.data.slice(0, i.offset), i.parent.parent._insertChild(i.parent.index + 1, new St(i.root.document, t)), new fe(i.parent.parent, i.parent.index + 1);
}
function R1(i, t) {
  const e = i.data.length;
  return i._data += t.data, t._remove(), new fe(i, e);
}
const E6 = [
  St,
  Ns,
  Rl,
  np,
  ip,
  $d
];
function B2(i, t) {
  for (const e of i) {
    if (!E6.some((n) => e instanceof n))
      throw new T("view-writer-insert-invalid-node-type", t);
    e.is("$text") || B2(e.getChildren(), t);
  }
}
function om(i) {
  return i && (i.is("containerElement") || i.is("documentFragment"));
}
function Na(i, t) {
  const e = sm(i.start), n = sm(i.end);
  if (!e || !n || e !== n)
    throw new T("view-writer-invalid-range-container", t);
}
function O1(i, t) {
  return i.id === null && t.id === null;
}
const F2 = (i) => i.createTextNode(" "), $2 = (i) => {
  const t = i.createElement("span");
  return t.dataset.ckeFiller = "true", t.innerText = " ", t;
}, z2 = (i) => {
  const t = i.createElement("br");
  return t.dataset.ckeFiller = "true", t;
}, qi = 7, Ol = "⁠".repeat(qi);
function bi(i) {
  return typeof i == "string" ? i.substr(0, qi) === Ol : Bt(i) && i.data.substr(0, qi) === Ol;
}
function Fo(i) {
  return i.data.length == qi && bi(i);
}
function V1(i) {
  const t = typeof i == "string" ? i : i.data;
  return bi(i) ? t.slice(qi) : t;
}
function T6(i) {
  i.document.on("arrowKey", S6, {
    priority: "low"
  });
}
function S6(i, t) {
  if (t.keyCode == Ke.arrowleft) {
    const e = t.domTarget.ownerDocument.defaultView.getSelection();
    if (e.rangeCount == 1 && e.getRangeAt(0).collapsed) {
      const n = e.getRangeAt(0).startContainer, r = e.getRangeAt(0).startOffset;
      bi(n) && r <= qi && e.collapse(n, 0);
    }
  }
}
let A6 = class extends (/* @__PURE__ */ st()) {
  /**
  * Creates a renderer instance.
  *
  * @param domConverter Converter instance.
  * @param selection View selection.
  */
  constructor(e, n) {
    super();
    /**
    * Set of DOM Documents instances.
    */
    h(this, "domDocuments", /* @__PURE__ */ new Set());
    /**
    * Converter instance.
    */
    h(this, "domConverter");
    /**
    * Set of nodes which attributes changed and may need to be rendered.
    */
    h(this, "markedAttributes", /* @__PURE__ */ new Set());
    /**
    * Set of elements which child lists changed and may need to be rendered.
    */
    h(this, "markedChildren", /* @__PURE__ */ new Set());
    /**
    * Set of text nodes which text data changed and may need to be rendered.
    */
    h(this, "markedTexts", /* @__PURE__ */ new Set());
    /**
    * View selection. Renderer updates DOM selection based on the view selection.
    */
    h(this, "selection");
    /**
    * The text node in which the inline filler was rendered.
    */
    h(this, "_inlineFiller", null);
    /**
    * DOM element containing fake selection.
    */
    h(this, "_fakeSelectionContainer", null);
    this.domConverter = e, this.selection = n, this.set("isFocused", !1), this.set("isSelecting", !1), this.set("isComposing", !1), Me.isBlink && !Me.isAndroid && this.on("change:isSelecting", () => {
      this.isSelecting || this.render();
    });
  }
  /**
  * Marks a view node to be updated in the DOM by {@link #render `render()`}.
  *
  * Note that only view nodes whose parents have corresponding DOM elements need to be marked to be synchronized.
  *
  * @see #markedAttributes
  * @see #markedChildren
  * @see #markedTexts
  *
  * @param type Type of the change.
  * @param node ViewNode to be marked.
  */
  markToSync(e, n) {
    if (e === "text")
      this.domConverter.mapViewToDom(n.parent) && this.markedTexts.add(n);
    else {
      if (!this.domConverter.mapViewToDom(n))
        return;
      if (e === "attributes")
        this.markedAttributes.add(n);
      else if (e === "children")
        this.markedChildren.add(n);
      else
        throw new T("view-renderer-unknown-type", this);
    }
  }
  /**
  * Renders all buffered changes ({@link #markedAttributes}, {@link #markedChildren} and {@link #markedTexts}) and
  * the current view selection (if needed) to the DOM by applying a minimal set of changes to it.
  *
  * Renderer tries not to break the text composition (e.g. IME) and x-index of the selection,
  * so it does as little as it is needed to update the DOM.
  *
  * Renderer also handles {@link module:engine/view/filler fillers}. Especially, it checks if the inline filler is needed
  * at the selection position and adds or removes it. To prevent breaking text composition inline filler will not be
  * removed as long as the selection is in the text node which needed it at first.
  */
  render() {
    if (this.isComposing && !Me.isAndroid)
      return;
    let e = null;
    const n = Me.isBlink && !Me.isAndroid ? !this.isSelecting : !0;
    for (const r of this.markedChildren)
      this._updateChildrenMappings(r);
    n ? (this._inlineFiller && !this._isSelectionInInlineFiller() && this._removeInlineFiller(), this._inlineFiller ? e = this._getInlineFillerPosition() : this._needsInlineFillerAtSelection() && (e = this.selection.getFirstPosition(), this.markedChildren.add(e.parent))) : this._inlineFiller && this._inlineFiller.parentNode && (e = this.domConverter.domPositionToView(this._inlineFiller), e && e.parent.is("$text") && (e = fe._createBefore(e.parent)));
    for (const r of this.markedAttributes)
      this._updateAttrs(r);
    for (const r of this.markedChildren)
      this._updateChildren(r, {
        inlineFillerPosition: e
      });
    for (const r of this.markedTexts)
      !this.markedChildren.has(r.parent) && this.domConverter.mapViewToDom(r.parent) && this._updateText(r, {
        inlineFillerPosition: e
      });
    if (n)
      if (e) {
        const r = this.domConverter.viewPositionToDom(e), s = r.parent.ownerDocument;
        bi(r.parent) ? this._inlineFiller = r.parent : this._inlineFiller = M1(s, r.parent, r.offset);
      } else
        this._inlineFiller = null;
    this._updateFocus(), this._updateSelection(), this.domConverter._clearTemporaryCustomProperties(), this.markedTexts.clear(), this.markedAttributes.clear(), this.markedChildren.clear();
  }
  /**
  * Updates mappings of view element's children.
  *
  * Children that were replaced in the view structure by similar elements (same tag name) are treated as 'replaced'.
  * This means that their mappings can be updated so the new view elements are mapped to the existing DOM elements.
  * Thanks to that these elements do not need to be re-rendered completely.
  *
  * @param viewElement The view element whose children mappings will be updated.
  */
  _updateChildrenMappings(e) {
    const n = this.domConverter.mapViewToDom(e);
    if (!n)
      return;
    const r = Array.from(n.childNodes), s = Array.from(this.domConverter.viewChildrenToDom(e, {
      withChildren: !1
    })), o = this._diffNodeLists(r, s), a = this._findUpdateActions(o, r, s, I6);
    if (a.indexOf("update") !== -1) {
      const l = {
        equal: 0,
        insert: 0,
        delete: 0
      };
      for (const c of a)
        if (c === "update") {
          const u = l.equal + l.insert, d = l.equal + l.delete, f = e.getChild(u);
          f && !f.is("uiElement") && !f.is("rawElement") && this._updateElementMappings(f, r[d]), g1(s[u]), l.equal++;
        } else
          l[c]++;
    }
  }
  /**
  * Updates mappings of a given view element.
  *
  * @param viewElement The view element whose mappings will be updated.
  * @param domElement The DOM element representing the given view element.
  */
  _updateElementMappings(e, n) {
    this.domConverter.unbindDomElement(n), this.domConverter.bindElements(n, e), this.markedChildren.add(e), this.markedAttributes.add(e);
  }
  /**
  * Gets the position of the inline filler based on the current selection.
  * Here, we assume that we know that the filler is needed and
  * {@link #_isSelectionInInlineFiller is at the selection position}, and, since it is needed,
  * it is somewhere at the selection position.
  *
  * Note: The filler position cannot be restored based on the filler's DOM text node, because
  * when this method is called (before rendering), the bindings will often be broken. View-to-DOM
  * bindings are only dependable after rendering.
  */
  _getInlineFillerPosition() {
    const e = this.selection.getFirstPosition();
    return e.parent.is("$text") ? fe._createBefore(e.parent) : e;
  }
  /**
  * Returns `true` if the selection has not left the inline filler's text node.
  * If it is `true`, it means that the filler had been added for a reason and the selection did not
  * leave the filler's text node. For example, the user can be in the middle of a composition so it should not be touched.
  *
  * @returns `true` if the inline filler and selection are in the same place.
  */
  _isSelectionInInlineFiller() {
    if (this.selection.rangeCount != 1 || !this.selection.isCollapsed)
      return !1;
    const e = this.selection.getFirstPosition(), n = this.domConverter.viewPositionToDom(e);
    return !!(n && Bt(n.parent) && bi(n.parent));
  }
  /**
  * Removes the inline filler.
  */
  _removeInlineFiller() {
    const e = this._inlineFiller;
    if (!bi(e))
      throw new T("view-renderer-filler-was-lost", this);
    Fo(e) ? e.remove() : e.data = e.data.substr(qi), this._inlineFiller = null;
  }
  /**
  * Checks if the inline {@link module:engine/view/filler filler} should be added.
  *
  * @returns `true` if the inline filler should be added.
  */
  _needsInlineFillerAtSelection() {
    if (this.selection.rangeCount != 1 || !this.selection.isCollapsed)
      return !1;
    const e = this.selection.getFirstPosition(), n = e.parent, r = e.offset;
    if (!this.domConverter.mapViewToDom(n.root) || !n.is("element") || !P6(n))
      return !1;
    const s = e.nodeBefore, o = e.nodeAfter;
    return !(s instanceof St || o instanceof St || r === n.getFillerOffset() && (!s || !s.is("element", "br")) || Me.isAndroid && (s || o));
  }
  /**
  * Checks if text needs to be updated and possibly updates it.
  *
  * @param viewText View text to update.
  * @param options.inlineFillerPosition The position where the inline filler should be rendered.
  */
  _updateText(e, n) {
    const r = this.domConverter.findCorrespondingDomText(e);
    let o = this.domConverter.viewToDom(e).data;
    const a = n.inlineFillerPosition;
    a && a.parent == e.parent && a.offset == e.index && (o = Ol + o), this._updateTextNode(r, o);
  }
  /**
  * Checks if attribute list needs to be updated and possibly updates it.
  *
  * @param viewElement The view element to update.
  */
  _updateAttrs(e) {
    const n = this.domConverter.mapViewToDom(e);
    if (n) {
      for (const r of n.attributes) {
        const s = r.name;
        e.hasAttribute(s) || this.domConverter.removeDomElementAttribute(n, s);
      }
      for (const r of e.getAttributeKeys())
        this.domConverter.setDomElementAttribute(n, r, e.getAttribute(r), e);
    }
  }
  /**
  * Checks if elements child list needs to be updated and possibly updates it.
  *
  * Note that on Android, to reduce the risk of composition breaks, it tries to update data of an existing
  * child text nodes instead of replacing them completely.
  *
  * @param viewElement View element to update.
  * @param options.inlineFillerPosition The position where the inline filler should be rendered.
  */
  _updateChildren(e, n) {
    const r = this.domConverter.mapViewToDom(e);
    if (!r)
      return;
    if (Me.isAndroid) {
      let f = null;
      for (const m of Array.from(r.childNodes)) {
        if (f && Bt(f) && Bt(m)) {
          r.normalize();
          break;
        }
        f = m;
      }
    }
    const s = n.inlineFillerPosition, o = r.childNodes, a = Array.from(this.domConverter.viewChildrenToDom(e, {
      bind: !0
    }));
    s && s.parent === e && M1(r.ownerDocument, a, s.offset);
    const l = this._diffNodeLists(o, a), c = this._findUpdateActions(l, o, a, R6);
    let u = 0;
    const d = /* @__PURE__ */ new Set();
    for (const f of c)
      f === "delete" ? (d.add(o[u]), g1(o[u])) : (f === "equal" || f === "update") && u++;
    u = 0;
    for (const f of c)
      f === "insert" ? (x2(r, u, a[u]), u++) : f === "update" ? (this._updateTextNode(o[u], a[u].data), u++) : f === "equal" && (this._markDescendantTextToSync(this.domConverter.domToView(a[u])), u++);
    for (const f of d)
      f.parentNode || this.domConverter.unbindDomElement(f);
  }
  /**
  * Shorthand for diffing two arrays or node lists of DOM nodes.
  *
  * @param actualDomChildren Actual DOM children
  * @param expectedDomChildren Expected DOM children.
  * @returns The list of actions based on the {@link module:utils/diff~diff} function.
  */
  _diffNodeLists(e, n) {
    return e = M6(e, this._fakeSelectionContainer), sl(e, n, O6.bind(null, this.domConverter));
  }
  /**
  * Finds DOM nodes that were replaced with the similar nodes (same tag name) in the view. All nodes are compared
  * within one `insert`/`delete` action group, for example:
  *
  * ```
  * Actual DOM:		<p><b>Foo</b>Bar<i>Baz</i><b>Bax</b></p>
  * Expected DOM:	<p>Bar<b>123</b><i>Baz</i><b>456</b></p>
  * Input actions:	[ insert, insert, delete, delete, equal, insert, delete ]
  * Output actions:	[ insert, replace, delete, equal, replace ]
  * ```
  *
  * @param actions Actions array which is a result of the {@link module:utils/diff~diff} function.
  * @param actualDom Actual DOM children
  * @param expectedDom Expected DOM children.
  * @param comparator A comparator function that should return `true` if the given node should be reused
  * (either by the update of a text node data or an element children list for similar elements).
  * @returns Actions array modified with the `update` actions.
  */
  _findUpdateActions(e, n, r, s) {
    if (e.indexOf("insert") === -1 || e.indexOf("delete") === -1)
      return e;
    let o = [], a = [], l = [];
    const c = {
      equal: 0,
      insert: 0,
      delete: 0
    };
    for (const u of e)
      u === "insert" ? l.push(r[c.equal + c.insert]) : u === "delete" ? a.push(n[c.equal + c.delete]) : (o = o.concat(sl(a, l, s).map((d) => d === "equal" ? "update" : d)), o.push("equal"), a = [], l = []), c[u]++;
    return o.concat(sl(a, l, s).map((u) => u === "equal" ? "update" : u));
  }
  /**
  * Checks if text needs to be updated and possibly updates it by removing and inserting only parts
  * of the data from the existing text node to reduce impact on the IME composition.
  *
  * @param domText DOM text node to update.
  * @param expectedText The expected data of a text node.
  */
  _updateTextNode(e, n) {
    const r = e.data;
    r != n && (Me.isAndroid && this.isComposing && r.replace(/\u00A0/g, " ") == n.replace(/\u00A0/g, " ") || this._updateTextNodeInternal(e, n));
  }
  /**
  * Part of the `_updateTextNode` method extracted for easier testing.
  */
  _updateTextNodeInternal(e, n) {
    const r = a2(e.data, n);
    for (const s of r)
      s.type === "insert" ? e.insertData(s.index, s.values.join("")) : e.deleteData(s.index, s.howMany);
  }
  /**
  * Marks text nodes to be synchronized.
  *
  * If a text node is passed, it will be marked. If an element is passed, all descendant text nodes inside it will be marked.
  *
  * @param viewNode View node to sync.
  */
  _markDescendantTextToSync(e) {
    if (e) {
      if (e.is("$text"))
        this.markedTexts.add(e);
      else if (e.is("element"))
        for (const n of e.getChildren())
          this._markDescendantTextToSync(n);
    }
  }
  /**
  * Checks if the selection needs to be updated and possibly updates it.
  */
  _updateSelection() {
    if (Me.isBlink && !Me.isAndroid && this.isSelecting && !this.markedChildren.size)
      return;
    if (this.selection.rangeCount === 0) {
      this._removeDomSelection(), this._removeFakeSelection();
      return;
    }
    const e = this.domConverter.mapViewToDom(this.selection.editableElement);
    !this.isFocused || !e || (this.selection.isFake ? this._updateFakeSelection(e) : this._fakeSelectionContainer && this._fakeSelectionContainer.isConnected ? (this._removeFakeSelection(), this._updateDomSelection(e)) : this.isComposing && Me.isAndroid || this._updateDomSelection(e));
  }
  /**
  * Updates the fake selection.
  *
  * @param domRoot A valid DOM root where the fake selection container should be added.
  */
  _updateFakeSelection(e) {
    const n = e.ownerDocument;
    this._fakeSelectionContainer || (this._fakeSelectionContainer = D6(n));
    const r = this._fakeSelectionContainer;
    if (this.domConverter.bindFakeSelection(r, this.selection), !this._fakeSelectionNeedsUpdate(e))
      return;
    (!r.parentElement || r.parentElement != e) && e.appendChild(r), r.textContent = this.selection.fakeSelectionLabel || " ";
    const s = n.getSelection(), o = n.createRange();
    s.removeAllRanges(), o.selectNodeContents(r), s.addRange(o);
  }
  /**
  * Updates the DOM selection.
  *
  * @param domRoot A valid DOM root where the DOM selection should be rendered.
  */
  _updateDomSelection(e) {
    const n = e.ownerDocument.defaultView.getSelection();
    if (!this._domSelectionNeedsUpdate(n))
      return;
    const r = this.domConverter.viewPositionToDom(this.selection.anchor), s = this.domConverter.viewPositionToDom(this.selection.focus);
    n.setBaseAndExtent(r.parent, r.offset, s.parent, s.offset), Me.isGecko && V6(s, n);
  }
  /**
  * Checks whether a given DOM selection needs to be updated.
  *
  * @param domSelection The DOM selection to check.
  */
  _domSelectionNeedsUpdate(e) {
    if (!this.domConverter.isDomSelectionCorrect(e))
      return !0;
    const n = e && this.domConverter.domSelectionToView(e);
    return !(n && this.selection.isEqual(n) || !this.selection.isCollapsed && this.selection.isSimilar(n));
  }
  /**
  * Checks whether the fake selection needs to be updated.
  *
  * @param domRoot A valid DOM root where a new fake selection container should be added.
  */
  _fakeSelectionNeedsUpdate(e) {
    const n = this._fakeSelectionContainer, r = e.ownerDocument.getSelection();
    return !n || n.parentElement !== e || r.anchorNode !== n && !n.contains(r.anchorNode) ? !0 : n.textContent !== this.selection.fakeSelectionLabel;
  }
  /**
  * Removes the DOM selection.
  */
  _removeDomSelection() {
    for (const e of this.domDocuments) {
      const n = e.getSelection();
      if (n.rangeCount) {
        const r = e.activeElement, s = this.domConverter.mapDomToView(r);
        r && s && n.removeAllRanges();
      }
    }
  }
  /**
  * Removes the fake selection.
  */
  _removeFakeSelection() {
    const e = this._fakeSelectionContainer;
    e && e.remove();
  }
  /**
  * Checks if focus needs to be updated and possibly updates it.
  */
  _updateFocus() {
    if (this.isFocused) {
      const e = this.selection.editableElement;
      e && this.domConverter.focus(e);
    }
  }
};
function P6(i) {
  if (i.getAttribute("contenteditable") == "false")
    return !1;
  const t = i.findAncestor((e) => e.hasAttribute("contenteditable"));
  return !t || t.getAttribute("contenteditable") == "true";
}
function M1(i, t, e) {
  const n = t instanceof Array ? t : t.childNodes, r = n[e];
  if (Bt(r))
    return r.data = Ol + r.data, r;
  {
    const s = i.createTextNode(Ol);
    return Array.isArray(t) ? n.splice(e, 0, s) : x2(t, e, s), s;
  }
}
function I6(i, t) {
  return ds(i) && ds(t) && !Bt(i) && !Bt(t) && !ll(i) && !ll(t) && i.tagName.toLowerCase() === t.tagName.toLowerCase();
}
function R6(i, t) {
  return ds(i) && ds(t) && Bt(i) && Bt(t);
}
function O6(i, t, e) {
  return t === e ? !0 : Bt(t) && Bt(e) ? t.data === e.data : !!(i.isBlockFiller(t) && i.isBlockFiller(e));
}
function V6(i, t) {
  let e = i.parent, n = i.offset;
  if (Bt(e) && Fo(e) && (n = Qa(e) + 1, e = e.parentNode), e.nodeType != Node.ELEMENT_NODE || n != e.childNodes.length - 1)
    return;
  const r = e.childNodes[n];
  r && r.tagName == "BR" && t.addRange(t.getRangeAt(0));
}
function M6(i, t) {
  const e = Array.from(i);
  return e.length == 0 || !t || e[e.length - 1] == t && e.pop(), e;
}
function D6(i) {
  const t = i.createElement("div");
  return t.className = "ck-fake-selection-container", Object.assign(t.style, {
    position: "fixed",
    top: 0,
    left: "-9999px",
    // See https://github.com/ckeditor/ckeditor5/issues/752.
    width: "42px"
  }), t.textContent = " ", t;
}
const L6 = z2(ae.document), N6 = F2(ae.document), B6 = $2(ae.document), Mc = "data-ck-unsafe-attribute-", D1 = "data-ck-unsafe-element";
class zd {
  /**
  * Creates a DOM converter.
  *
  * @param document The view document instance.
  * @param options An object with configuration options.
  * @param options.blockFillerMode The type of the block filler to use.
  * Default value depends on the options.renderingMode:
  *  'nbsp' when options.renderingMode == 'data',
  *  'br' when options.renderingMode == 'editing'.
  * @param options.renderingMode Whether to leave the View-to-DOM conversion result unchanged
  * or improve editing experience by filtering out interactive data.
  */
  constructor(t, { blockFillerMode: e, renderingMode: n = "editing" } = {}) {
    h(this, "document");
    /**
    * Whether to leave the View-to-DOM conversion result unchanged or improve editing experience by filtering out interactive data.
    */
    h(this, "renderingMode");
    /**
    * The mode of a block filler used by the DOM converter.
    */
    h(this, "blockFillerMode");
    /**
    * Elements which are considered pre-formatted elements.
    */
    h(this, "preElements");
    /**
    * Elements which are considered block elements (and hence should be filled with a
    * {@link #isBlockFiller block filler}).
    *
    * Whether an element is considered a block element also affects handling of trailing whitespaces.
    *
    * You can extend this array if you introduce support for block elements which are not yet recognized here.
    */
    h(this, "blockElements");
    /**
    * A list of elements that exist inline (in text) but their inner structure cannot be edited because
    * of the way they are rendered by the browser. They are mostly HTML form elements but there are other
    * elements such as `<img>` or `<iframe>` that also have non-editable children or no children whatsoever.
    *
    * Whether an element is considered an inline object has an impact on white space rendering (trimming)
    * around (and inside of it). In short, white spaces in text nodes next to inline objects are not trimmed.
    *
    * You can extend this array if you introduce support for inline object elements which are not yet recognized here.
    */
    h(this, "inlineObjectElements");
    /**
    * A list of elements which may affect the editing experience. To avoid this, those elements are replaced with
    * `<span data-ck-unsafe-element="[element name]"></span>` while rendering in the editing mode.
    */
    h(this, "unsafeElements");
    /**
    * The DOM Document used to create DOM nodes.
    */
    h(this, "_domDocument");
    /**
    * The DOM-to-view mapping.
    */
    h(this, "_domToViewMapping", /* @__PURE__ */ new WeakMap());
    /**
    * The view-to-DOM mapping.
    */
    h(this, "_viewToDomMapping", /* @__PURE__ */ new WeakMap());
    /**
    * Holds the mapping between fake selection containers and corresponding view selections.
    */
    h(this, "_fakeSelectionMapping", /* @__PURE__ */ new WeakMap());
    /**
    * Matcher for view elements whose content should be treated as raw data
    * and not processed during the conversion from DOM nodes to view elements.
    */
    h(this, "_rawContentElementMatcher", new xi());
    /**
    * Matcher for inline object view elements. This is an extension of a simple {@link #inlineObjectElements} array of element names.
    */
    h(this, "_inlineObjectElementMatcher", new xi());
    /**
    * Set of elements with temporary custom properties that require clearing after render.
    */
    h(this, "_elementsWithTemporaryCustomProperties", /* @__PURE__ */ new Set());
    this.document = t, this.renderingMode = n, this.blockFillerMode = e || (n === "editing" ? "br" : "nbsp"), this.preElements = [
      "pre",
      "textarea"
    ], this.blockElements = [
      "address",
      "article",
      "aside",
      "blockquote",
      "caption",
      "center",
      "dd",
      "details",
      "dir",
      "div",
      "dl",
      "dt",
      "fieldset",
      "figcaption",
      "figure",
      "footer",
      "form",
      "h1",
      "h2",
      "h3",
      "h4",
      "h5",
      "h6",
      "header",
      "hgroup",
      "legend",
      "li",
      "main",
      "menu",
      "nav",
      "ol",
      "p",
      "pre",
      "section",
      "summary",
      "table",
      "tbody",
      "td",
      "tfoot",
      "th",
      "thead",
      "tr",
      "ul"
    ], this.inlineObjectElements = [
      "object",
      "iframe",
      "input",
      "button",
      "textarea",
      "select",
      "option",
      "video",
      "embed",
      "audio",
      "img",
      "canvas"
    ], this.unsafeElements = [
      "script",
      "style"
    ], this._domDocument = this.renderingMode === "editing" ? ae.document : ae.document.implementation.createHTMLDocument("");
  }
  /**
  * Binds a given DOM element that represents fake selection to a **position** of a
  * {@link module:engine/view/documentselection~DocumentSelection document selection}.
  * Document selection copy is stored and can be retrieved by the
  * {@link module:engine/view/domconverter~DomConverter#fakeSelectionToView} method.
  */
  bindFakeSelection(t, e) {
    this._fakeSelectionMapping.set(t, new Dr(e));
  }
  /**
  * Returns a {@link module:engine/view/selection~Selection view selection} instance corresponding to a given
  * DOM element that represents fake selection. Returns `undefined` if binding to the given DOM element does not exist.
  */
  fakeSelectionToView(t) {
    return this._fakeSelectionMapping.get(t);
  }
  /**
  * Binds DOM and view elements, so it will be possible to get corresponding elements using
  * {@link module:engine/view/domconverter~DomConverter#mapDomToView} and
  * {@link module:engine/view/domconverter~DomConverter#mapViewToDom}.
  *
  * @param domElement The DOM element to bind.
  * @param viewElement The view element to bind.
  */
  bindElements(t, e) {
    this._domToViewMapping.set(t, e), this._viewToDomMapping.set(e, t);
  }
  /**
  * Unbinds a given DOM element from the view element it was bound to. Unbinding is deep, meaning that all children of
  * the DOM element will be unbound too.
  *
  * @param domElement The DOM element to unbind.
  */
  unbindDomElement(t) {
    const e = this._domToViewMapping.get(t);
    if (e) {
      this._domToViewMapping.delete(t), this._viewToDomMapping.delete(e);
      for (const n of t.children)
        this.unbindDomElement(n);
    }
  }
  /**
  * Binds DOM and view document fragments, so it will be possible to get corresponding document fragments using
  * {@link module:engine/view/domconverter~DomConverter#mapDomToView} and
  * {@link module:engine/view/domconverter~DomConverter#mapViewToDom}.
  *
  * @param domFragment The DOM document fragment to bind.
  * @param viewFragment The view document fragment to bind.
  */
  bindDocumentFragments(t, e) {
    this._domToViewMapping.set(t, e), this._viewToDomMapping.set(e, t);
  }
  /**
  * Decides whether a given pair of attribute key and value should be passed further down the pipeline.
  *
  * @param elementName Element name in lower case.
  */
  shouldRenderAttribute(t, e, n) {
    return this.renderingMode === "data" ? !0 : (t = t.toLowerCase(), t.startsWith("on") || t === "srcdoc" && e.match(/\bon\S+\s*=|javascript:|<\s*\/*script/i) ? !1 : n === "img" && (t === "src" || t === "srcset") || n === "source" && t === "srcset" ? !0 : !e.match(/^\s*(javascript:|data:(image\/svg|text\/x?html))/i));
  }
  /**
  * Set `domElement`'s content using provided `html` argument. Apply necessary filtering for the editing pipeline.
  *
  * @param domElement DOM element that should have `html` set as its content.
  * @param html Textual representation of the HTML that will be set on `domElement`.
  */
  setContentOf(t, e) {
    if (this.renderingMode === "data") {
      t.innerHTML = e;
      return;
    }
    const n = new DOMParser().parseFromString(e, "text/html"), r = n.createDocumentFragment(), s = n.body.childNodes;
    for (; s.length > 0; )
      r.appendChild(s[0]);
    const o = n.createTreeWalker(r, NodeFilter.SHOW_ELEMENT), a = [];
    let l;
    for (; l = o.nextNode(); )
      a.push(l);
    for (const c of a) {
      for (const d of c.getAttributeNames())
        this.setDomElementAttribute(c, d, c.getAttribute(d));
      const u = c.tagName.toLowerCase();
      this._shouldRenameElement(u) && (N1(u), c.replaceWith(this._createReplacementDomElement(u, c)));
    }
    for (; t.firstChild; )
      t.firstChild.remove();
    t.append(r);
  }
  /**
  * Converts the view to the DOM. For all text nodes, not bound elements and document fragments new items will
  * be created. For bound elements and document fragments the method will return corresponding items.
  *
  * @param viewNode View node or document fragment to transform.
  * @param options Conversion options.
  * @param options.bind Determines whether new elements will be bound.
  * @param options.withChildren If `false`, node's and document fragment's children will not be converted.
  * @returns Converted node or DocumentFragment.
  */
  viewToDom(t, e = {}) {
    if (t.is("$text")) {
      const n = this._processDataFromViewText(t);
      return this._domDocument.createTextNode(n);
    } else {
      const n = t;
      if (this.mapViewToDom(n))
        if (n.getCustomProperty("editingPipeline:doNotReuseOnce"))
          this._elementsWithTemporaryCustomProperties.add(n);
        else
          return this.mapViewToDom(n);
      let r;
      if (n.is("documentFragment"))
        r = this._domDocument.createDocumentFragment(), e.bind && this.bindDocumentFragments(r, n);
      else {
        if (n.is("uiElement"))
          return n.name === "$comment" ? r = this._domDocument.createComment(n.getCustomProperty("$rawContent")) : r = n.render(this._domDocument, this), e.bind && this.bindElements(r, n), r;
        this._shouldRenameElement(n.name) ? (N1(n.name), r = this._createReplacementDomElement(n.name)) : n.hasAttribute("xmlns") ? r = this._domDocument.createElementNS(n.getAttribute("xmlns"), n.name) : r = this._domDocument.createElement(n.name), n.is("rawElement") && n.render(r, this), e.bind && this.bindElements(r, n);
        for (const s of n.getAttributeKeys())
          this.setDomElementAttribute(r, s, n.getAttribute(s), n);
      }
      if (e.withChildren !== !1)
        for (const s of this.viewChildrenToDom(n, e))
          r instanceof HTMLTemplateElement ? r.content.appendChild(s) : r.appendChild(s);
      return r;
    }
  }
  /**
  * Sets the attribute on a DOM element.
  *
  * **Note**: To remove the attribute, use {@link #removeDomElementAttribute}.
  *
  * @param domElement The DOM element the attribute should be set on.
  * @param key The name of the attribute.
  * @param value The value of the attribute.
  * @param relatedViewElement The view element related to the `domElement` (if there is any).
  * It helps decide whether the attribute set is unsafe. For instance, view elements created via the
  * {@link module:engine/view/downcastwriter~DowncastWriter} methods can allow certain attributes that would normally be filtered out.
  */
  setDomElementAttribute(t, e, n, r) {
    const s = this.shouldRenderAttribute(e, n, t.tagName.toLowerCase()) || r && r.shouldRenderUnsafeAttribute(e);
    if (s || Xe("domconverter-unsafe-attribute-detected", {
      domElement: t,
      key: e,
      value: n
    }), !E2(e)) {
      Xe("domconverter-invalid-attribute-detected", {
        domElement: t,
        key: e,
        value: n
      });
      return;
    }
    t.hasAttribute(e) && !s ? t.removeAttribute(e) : t.hasAttribute(Mc + e) && s && t.removeAttribute(Mc + e), t.setAttribute(s ? e : Mc + e, n);
  }
  /**
  * Removes an attribute from a DOM element.
  *
  * **Note**: To set the attribute, use {@link #setDomElementAttribute}.
  *
  * @param domElement The DOM element the attribute should be removed from.
  * @param key The name of the attribute.
  */
  removeDomElementAttribute(t, e) {
    e != D1 && (t.removeAttribute(e), t.removeAttribute(Mc + e));
  }
  /**
  * Converts children of the view element to DOM using the
  * {@link module:engine/view/domconverter~DomConverter#viewToDom} method.
  * Additionally, this method adds block {@link module:engine/view/filler filler} to the list of children, if needed.
  *
  * @param viewElement Parent view element.
  * @param options See {@link module:engine/view/domconverter~DomConverter#viewToDom} options parameter.
  * @returns DOM nodes.
  */
  *viewChildrenToDom(t, e = {}) {
    const n = t.getFillerOffset && t.getFillerOffset();
    let r = 0;
    for (const s of t.getChildren()) {
      n === r && (yield this._getBlockFiller());
      const o = s.is("element") && !!s.getCustomProperty("dataPipeline:transparentRendering") && !Ft(s.getAttributes());
      if (o && this.renderingMode == "data")
        if (s.is("rawElement")) {
          const a = this._domDocument.createElement(s.name);
          s.render(a, this), yield* [
            ...a.childNodes
          ];
        } else
          yield* this.viewChildrenToDom(s, e);
      else
        o && Xe("domconverter-transparent-rendering-unsupported-in-editing-pipeline", {
          viewElement: s
        }), yield this.viewToDom(s, e);
      r++;
    }
    n === r && (yield this._getBlockFiller());
  }
  /**
  * Converts view {@link module:engine/view/range~Range} to DOM range.
  * Inline and block {@link module:engine/view/filler fillers} are handled during the conversion.
  *
  * @param viewRange View range.
  * @returns DOM range.
  */
  viewRangeToDom(t) {
    const e = this.viewPositionToDom(t.start), n = this.viewPositionToDom(t.end), r = this._domDocument.createRange();
    return r.setStart(e.parent, e.offset), r.setEnd(n.parent, n.offset), r;
  }
  /**
  * Converts view {@link module:engine/view/position~Position} to DOM parent and offset.
  *
  * Inline and block {@link module:engine/view/filler fillers} are handled during the conversion.
  * If the converted position is directly before inline filler it is moved inside the filler.
  *
  * @param viewPosition View position.
  * @returns DOM position or `null` if view position could not be converted to DOM.
  * DOM position has two properties:
  * * `parent` - DOM position parent.
  * * `offset` - DOM position offset.
  */
  viewPositionToDom(t) {
    const e = t.parent;
    if (e.is("$text")) {
      const n = this.findCorrespondingDomText(e);
      if (!n)
        return null;
      let r = t.offset;
      return bi(n) && (r += qi), {
        parent: n,
        offset: r
      };
    } else {
      let n, r, s;
      if (t.offset === 0) {
        if (n = this.mapViewToDom(e), !n)
          return null;
        s = n.childNodes[0];
      } else {
        const a = t.nodeBefore;
        if (r = a.is("$text") ? this.findCorrespondingDomText(a) : this.mapViewToDom(a), !r)
          return null;
        n = r.parentNode, s = r.nextSibling;
      }
      if (Bt(s) && bi(s))
        return {
          parent: s,
          offset: qi
        };
      const o = r ? Qa(r) + 1 : 0;
      return {
        parent: n,
        offset: o
      };
    }
  }
  /**
  * Converts DOM to view. For all text nodes, not bound elements and document fragments new items will
  * be created. For bound elements and document fragments function will return corresponding items. For
  * {@link module:engine/view/filler fillers} `null` will be returned.
  * For all DOM elements rendered by {@link module:engine/view/uielement~UIElement} that UIElement will be returned.
  *
  * @param domNode DOM node or document fragment to transform.
  * @param options Conversion options.
  * @param options.bind Determines whether new elements will be bound. False by default.
  * @param options.withChildren If `true`, node's and document fragment's children will be converted too. True by default.
  * @param options.keepOriginalCase If `false`, node's tag name will be converted to lower case. False by default.
  * @param options.skipComments If `false`, comment nodes will be converted to `$comment`
  * {@link module:engine/view/uielement~UIElement view UI elements}. False by default.
  * @returns Converted node or document fragment or `null` if DOM node is a {@link module:engine/view/filler filler}
  * or the given node is an empty text node.
  */
  domToView(t, e = {}) {
    const n = [], r = this._domToView(t, e, n), s = r.next().value;
    return !s || (r.next(), this._processDomInlineNodes(null, n, e), s.is("$text") && s.data.length == 0) ? null : s;
  }
  /**
  * Converts children of the DOM element to view nodes using
  * the {@link module:engine/view/domconverter~DomConverter#domToView} method.
  * Additionally this method omits block {@link module:engine/view/filler filler}, if it exists in the DOM parent.
  *
  * @param domElement Parent DOM element.
  * @param options See {@link module:engine/view/domconverter~DomConverter#domToView} options parameter.
  * @param inlineNodes An array that will be populated with inline nodes. It's used internally for whitespace processing.
  * @returns View nodes.
  */
  *domChildrenToView(t, e = {}, n = []) {
    let r = [];
    t instanceof HTMLTemplateElement ? r = [
      ...t.content.childNodes
    ] : r = [
      ...t.childNodes
    ];
    for (let s = 0; s < r.length; s++) {
      const o = r[s], a = this._domToView(o, e, n), l = a.next().value;
      l !== null && (this._isBlockViewElement(l) && this._processDomInlineNodes(t, n, e), yield l, a.next());
    }
    this._processDomInlineNodes(t, n, e);
  }
  /**
  * Converts DOM selection to view {@link module:engine/view/selection~Selection}.
  * Ranges which cannot be converted will be omitted.
  *
  * @param domSelection DOM selection.
  * @returns View selection.
  */
  domSelectionToView(t) {
    if (z6(t))
      return new Dr([]);
    if (t.rangeCount === 1) {
      let r = t.getRangeAt(0).startContainer;
      Bt(r) && (r = r.parentNode);
      const s = this.fakeSelectionToView(r);
      if (s)
        return s;
    }
    const e = this.isDomSelectionBackward(t), n = [];
    for (let r = 0; r < t.rangeCount; r++) {
      const s = t.getRangeAt(r), o = this.domRangeToView(s);
      o && n.push(o);
    }
    return new Dr(n, {
      backward: e
    });
  }
  /**
  * Converts DOM Range to view {@link module:engine/view/range~Range}.
  * If the start or end position can not be converted `null` is returned.
  *
  * @param domRange DOM range.
  * @returns View range.
  */
  domRangeToView(t) {
    const e = this.domPositionToView(t.startContainer, t.startOffset), n = this.domPositionToView(t.endContainer, t.endOffset);
    return e && n ? new qe(e, n) : null;
  }
  /**
  * Converts DOM parent and offset to view {@link module:engine/view/position~Position}.
  *
  * If the position is inside a {@link module:engine/view/filler filler} which has no corresponding view node,
  * position of the filler will be converted and returned.
  *
  * If the position is inside DOM element rendered by {@link module:engine/view/uielement~UIElement}
  * that position will be converted to view position before that UIElement.
  *
  * If structures are too different and it is not possible to find corresponding position then `null` will be returned.
  *
  * @param domParent DOM position parent.
  * @param domOffset DOM position offset. You can skip it when converting the inline filler node.
  * @returns View position.
  */
  domPositionToView(t, e = 0) {
    if (this.isBlockFiller(t))
      return this.domPositionToView(t.parentNode, Qa(t));
    const n = this.mapDomToView(t);
    if (n && (n.is("uiElement") || n.is("rawElement")))
      return fe._createBefore(n);
    if (Bt(t)) {
      if (Fo(t))
        return this.domPositionToView(t.parentNode, Qa(t));
      const r = this.findCorrespondingViewText(t);
      let s = e;
      return r ? (bi(t) && (s -= qi, s = s < 0 ? 0 : s), new fe(r, s)) : null;
    } else {
      if (e === 0) {
        const r = this.mapDomToView(t);
        if (r)
          return new fe(r, 0);
      } else {
        const r = t.childNodes[e - 1];
        if (Bt(r) && Fo(r) || r && this.isBlockFiller(r))
          return this.domPositionToView(r.parentNode, Qa(r));
        const s = Bt(r) ? this.findCorrespondingViewText(r) : this.mapDomToView(r);
        if (s && s.parent)
          return new fe(s.parent, s.index + 1);
      }
      return null;
    }
  }
  /**
  * Returns corresponding view {@link module:engine/view/element~Element Element} or
  * {@link module:engine/view/documentfragment~DocumentFragment} for provided DOM element or
  * document fragment. If there is no view item {@link module:engine/view/domconverter~DomConverter#bindElements bound}
  * to the given DOM - `undefined` is returned.
  *
  * For all DOM elements rendered by a {@link module:engine/view/uielement~UIElement} or
  * a {@link module:engine/view/rawelement~RawElement}, the parent `UIElement` or `RawElement` will be returned.
  *
  * @param domElementOrDocumentFragment DOM element or document fragment.
  * @returns Corresponding view element, document fragment or `undefined` if no element was bound.
  */
  mapDomToView(t) {
    return this.getHostViewElement(t) || this._domToViewMapping.get(t);
  }
  /**
  * Finds corresponding text node. Text nodes are not {@link module:engine/view/domconverter~DomConverter#bindElements bound},
  * corresponding text node is returned based on the sibling or parent.
  *
  * If the directly previous sibling is a {@link module:engine/view/domconverter~DomConverter#bindElements bound} element, it is used
  * to find the corresponding text node.
  *
  * If this is a first child in the parent and the parent is a {@link module:engine/view/domconverter~DomConverter#bindElements bound}
  * element, it is used to find the corresponding text node.
  *
  * For all text nodes rendered by a {@link module:engine/view/uielement~UIElement} or
  * a {@link module:engine/view/rawelement~RawElement}, the parent `UIElement` or `RawElement` will be returned.
  *
  * Otherwise `null` is returned.
  *
  * Note that for the block or inline {@link module:engine/view/filler filler} this method returns `null`.
  *
  * @param domText DOM text node.
  * @returns Corresponding view text node or `null`, if it was not possible to find a corresponding node.
  */
  findCorrespondingViewText(t) {
    if (Fo(t))
      return null;
    const e = this.getHostViewElement(t);
    if (e)
      return e;
    const n = t.previousSibling;
    if (n) {
      if (!this.isElement(n))
        return null;
      const r = this.mapDomToView(n);
      if (r) {
        const s = r.nextSibling;
        return s instanceof St ? s : null;
      }
    } else {
      const r = this.mapDomToView(t.parentNode);
      if (r) {
        const s = r.getChild(0);
        return s instanceof St ? s : null;
      }
    }
    return null;
  }
  mapViewToDom(t) {
    return this._viewToDomMapping.get(t);
  }
  /**
  * Finds corresponding text node. Text nodes are not {@link module:engine/view/domconverter~DomConverter#bindElements bound},
  * corresponding text node is returned based on the sibling or parent.
  *
  * If the directly previous sibling is a {@link module:engine/view/domconverter~DomConverter#bindElements bound} element, it is used
  * to find the corresponding text node.
  *
  * If this is a first child in the parent and the parent is a {@link module:engine/view/domconverter~DomConverter#bindElements bound}
  * element, it is used to find the corresponding text node.
  *
  * Otherwise `null` is returned.
  *
  * @param viewText View text node.
  * @returns Corresponding DOM text node or `null`, if it was not possible to find a corresponding node.
  */
  findCorrespondingDomText(t) {
    const e = t.previousSibling;
    return e && this.mapViewToDom(e) ? this.mapViewToDom(e).nextSibling : !e && t.parent && this.mapViewToDom(t.parent) ? this.mapViewToDom(t.parent).childNodes[0] : null;
  }
  /**
  * Focuses DOM editable that is corresponding to provided {@link module:engine/view/editableelement~EditableElement}.
  */
  focus(t) {
    const e = this.mapViewToDom(t);
    if (e && e.ownerDocument.activeElement !== e) {
      const { scrollX: n, scrollY: r } = ae.window, s = [];
      L1(e, (o) => {
        const { scrollLeft: a, scrollTop: l } = o;
        s.push([
          a,
          l
        ]);
      }), e.focus(), L1(e, (o) => {
        const [a, l] = s.shift();
        o.scrollLeft = a, o.scrollTop = l;
      }), ae.window.scrollTo(n, r);
    }
  }
  /**
  * Remove DOM selection from blurred editable, so it won't interfere with clicking on dropdowns (especially on iOS).
  *
  * @internal
  */
  _clearDomSelection() {
    const t = this.mapViewToDom(this.document.selection.editableElement);
    if (!t)
      return;
    const e = t.ownerDocument.defaultView.getSelection(), n = this.domSelectionToView(e);
    n && n.rangeCount > 0 && e.removeAllRanges();
  }
  /**
  * Returns `true` when `node.nodeType` equals `Node.ELEMENT_NODE`.
  *
  * @param node Node to check.
  */
  isElement(t) {
    return t && t.nodeType == Node.ELEMENT_NODE;
  }
  /**
  * Returns `true` when `node.nodeType` equals `Node.DOCUMENT_FRAGMENT_NODE`.
  *
  * @param node Node to check.
  */
  isDocumentFragment(t) {
    return t && t.nodeType == Node.DOCUMENT_FRAGMENT_NODE;
  }
  /**
  * Checks if the node is an instance of the block filler for this DOM converter.
  *
  * ```ts
  * const converter = new DomConverter( viewDocument, { blockFillerMode: 'br' } );
  *
  * converter.isBlockFiller( BR_FILLER( document ) ); // true
  * converter.isBlockFiller( NBSP_FILLER( document ) ); // false
  * ```
  *
  * **Note:**: For the `'nbsp'` mode the method also checks context of a node so it cannot be a detached node.
  *
  * **Note:** A special case in the `'nbsp'` mode exists where the `<br>` in `<p><br></p>` is treated as a block filler.
  *
  * @param domNode DOM node to check.
  * @returns True if a node is considered a block filler for given mode.
  */
  isBlockFiller(t) {
    return this.blockFillerMode == "br" ? t.isEqualNode(L6) : t.tagName === "BR" && H2(t, this.blockElements) && t.parentNode.childNodes.length === 1 ? !0 : t.isEqualNode(B6) || $6(t, this.blockElements);
  }
  /**
  * Returns `true` if given selection is a backward selection, that is, if it's `focus` is before `anchor`.
  *
  * @param DOM Selection instance to check.
  */
  isDomSelectionBackward(t) {
    if (t.isCollapsed)
      return !1;
    const e = this._domDocument.createRange();
    try {
      e.setStart(t.anchorNode, t.anchorOffset), e.setEnd(t.focusNode, t.focusOffset);
    } catch {
      return !1;
    }
    const n = e.collapsed;
    return e.detach(), n;
  }
  /**
  * Returns a parent {@link module:engine/view/uielement~UIElement} or {@link module:engine/view/rawelement~RawElement}
  * that hosts the provided DOM node. Returns `null` if there is no such parent.
  */
  getHostViewElement(t) {
    const e = w2(t);
    for (e.pop(); e.length; ) {
      const n = e.pop(), r = this._domToViewMapping.get(n);
      if (r && (r.is("uiElement") || r.is("rawElement")))
        return r;
    }
    return null;
  }
  /**
  * Checks if the given selection's boundaries are at correct places.
  *
  * The following places are considered as incorrect for selection boundaries:
  *
  * * before or in the middle of an inline filler sequence,
  * * inside a DOM element which represents {@link module:engine/view/uielement~UIElement a view UI element},
  * * inside a DOM element which represents {@link module:engine/view/rawelement~RawElement a view raw element}.
  *
  * @param domSelection The DOM selection object to be checked.
  * @returns `true` if the given selection is at a correct place, `false` otherwise.
  */
  isDomSelectionCorrect(t) {
    return this._isDomSelectionPositionCorrect(t.anchorNode, t.anchorOffset) && this._isDomSelectionPositionCorrect(t.focusNode, t.focusOffset);
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} for view elements whose content should be treated as raw data
  * and not processed during the conversion from DOM nodes to view elements.
  *
  * This is affecting how {@link module:engine/view/domconverter~DomConverter#domToView} and
  * {@link module:engine/view/domconverter~DomConverter#domChildrenToView} process DOM nodes.
  *
  * The raw data can be later accessed by a
  * {@link module:engine/view/element~Element#getCustomProperty custom property of a view element} called `"$rawContent"`.
  *
  * @param pattern Pattern matching a view element whose content should
  * be treated as raw data.
  */
  registerRawContentMatcher(t) {
    this._rawContentElementMatcher.add(t);
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} for inline object view elements.
  *
  * This is affecting how {@link module:engine/view/domconverter~DomConverter#domToView} and
  * {@link module:engine/view/domconverter~DomConverter#domChildrenToView} process DOM nodes.
  *
  * This is an extension of a simple {@link #inlineObjectElements} array of element names.
  *
  * @param pattern Pattern matching a view element which should be treated as an inline object.
  */
  registerInlineObjectMatcher(t) {
    this._inlineObjectElementMatcher.add(t);
  }
  /**
  * Clear temporary custom properties.
  *
  * @internal
  */
  _clearTemporaryCustomProperties() {
    for (const t of this._elementsWithTemporaryCustomProperties)
      t._removeCustomProperty("editingPipeline:doNotReuseOnce");
    this._elementsWithTemporaryCustomProperties.clear();
  }
  /**
  * Returns the block {@link module:engine/view/filler filler} node based on the current {@link #blockFillerMode} setting.
  */
  _getBlockFiller() {
    switch (this.blockFillerMode) {
      case "nbsp":
        return F2(this._domDocument);
      case "markedNbsp":
        return $2(this._domDocument);
      case "br":
        return z2(this._domDocument);
    }
  }
  /**
  * Checks if the given DOM position is a correct place for selection boundary. See {@link #isDomSelectionCorrect}.
  *
  * @param domParent Position parent.
  * @param offset Position offset.
  * @returns `true` if given position is at a correct place for selection boundary, `false` otherwise.
  */
  _isDomSelectionPositionCorrect(t, e) {
    if (Bt(t) && bi(t) && e < qi || this.isElement(t) && bi(t.childNodes[e]))
      return !1;
    const n = this.mapDomToView(t);
    return !(n && (n.is("uiElement") || n.is("rawElement")));
  }
  /**
  * Internal generator for {@link #domToView}. Also used by {@link #domChildrenToView}.
  * Separates DOM nodes conversion from whitespaces processing.
  *
  * @param domNode DOM node or document fragment to transform.
  * @param inlineNodes An array of recently encountered inline nodes truncated to the block element boundaries.
  * Used later to process whitespaces.
  */
  *_domToView(t, e, n) {
    if (this.isBlockFiller(t))
      return null;
    const r = this.getHostViewElement(t);
    if (r)
      return r;
    if (ll(t) && e.skipComments)
      return null;
    if (Bt(t)) {
      if (Fo(t))
        return null;
      {
        const s = t.data;
        if (s === "")
          return null;
        const o = new St(this.document, s);
        return n.push(o), o;
      }
    } else {
      let s = this.mapDomToView(t);
      if (s)
        return this._isInlineObjectElement(s) && n.push(s), s;
      if (this.isDocumentFragment(t))
        s = new Bs(this.document), e.bind && this.bindDocumentFragments(t, s);
      else {
        s = this._createViewElement(t, e), e.bind && this.bindElements(t, s);
        const a = t.attributes;
        if (a)
          for (let l = a.length, c = 0; c < l; c++)
            s._setAttribute(a[c].name, a[c].value);
        if (this._isViewElementWithRawContent(s, e))
          return s._setCustomProperty("$rawContent", t.innerHTML), this._isBlockViewElement(s) || n.push(s), s;
        if (ll(t))
          return s._setCustomProperty("$rawContent", t.data), s;
      }
      yield s;
      const o = [];
      if (e.withChildren !== !1)
        for (const a of this.domChildrenToView(t, e, o))
          s._appendChild(a);
      if (this._isInlineObjectElement(s))
        n.push(s), this._processDomInlineNodes(null, o, e);
      else
        for (const a of o)
          n.push(a);
    }
  }
  /**
  * Internal helper that walks the list of inline view nodes already generated from DOM nodes
  * and handles whitespaces and NBSPs.
  *
  * @param domParent The DOM parent of the given inline nodes. This should be a document fragment or
  * a block element to whitespace processing start cleaning.
  * @param inlineNodes An array of recently encountered inline nodes truncated to the block element boundaries.
  */
  _processDomInlineNodes(t, e, n) {
    if (!e.length || t && !this.isDocumentFragment(t) && !this._isBlockDomElement(t))
      return;
    let r = !1;
    for (let s = 0; s < e.length; s++) {
      const o = e[s];
      if (!o.is("$text")) {
        r = !1;
        continue;
      }
      let a, l = !1;
      if (this._isPreFormatted(o))
        a = V1(o.data);
      else {
        a = o.data.replace(/[ \n\t\r]{1,}/g, " "), l = /[^\S\u00A0]/.test(a.charAt(a.length - 1));
        const c = s > 0 ? e[s - 1] : null, u = s + 1 < e.length ? e[s + 1] : null, d = !c || c.is("element") && c.name == "br" || r, f = u ? !1 : !bi(o.data);
        n.withChildren !== !1 && (d && (a = a.replace(/^ /, "")), f && (a = a.replace(/ $/, ""))), a = V1(a), a = a.replace(/ \u00A0/g, "  ");
        const m = u && u.is("element") && u.name != "br", g = u && u.is("$text") && u.data.charAt(0) == " ";
        (/[ \u00A0]\u00A0$/.test(a) || !u || m || g) && (a = a.replace(/\u00A0$/, " ")), (d || c && c.is("element") && c.name != "br") && (a = a.replace(/^\u00A0/, " "));
      }
      a.length == 0 && o.parent ? (o._remove(), e.splice(s, 1), s--) : (o._data = a, r = l);
    }
    e.length = 0;
  }
  /**
  * Takes text data from a given {@link module:engine/view/text~Text#data} and processes it so
  * it is correctly displayed in the DOM.
  *
  * Following changes are done:
  *
  * * a space at the beginning is changed to `&nbsp;` if this is the first text node in its container
  * element or if a previous text node ends with a space character,
  * * space at the end of the text node is changed to `&nbsp;` if there are two spaces at the end of a node or if next node
  * starts with a space or if it is the last text node in its container,
  * * remaining spaces are replaced to a chain of spaces and `&nbsp;` (e.g. `'x   x'` becomes `'x &nbsp; x'`).
  *
  * Content of {@link #preElements} is not processed.
  *
  * @param node View text node to process.
  * @returns Processed text data.
  */
  _processDataFromViewText(t) {
    let e = t.data;
    if (this._isPreFormatted(t))
      return e;
    if (e.charAt(0) == " ") {
      const n = this._getTouchingInlineViewNode(t, !1);
      (n && n.is("$textProxy") && this._nodeEndsWithSpace(n) || !n) && (e = " " + e.substr(1));
    }
    if (e.charAt(e.length - 1) == " ") {
      const n = this._getTouchingInlineViewNode(t, !0), r = n && n.is("$textProxy") && n.data.charAt(0) == " ";
      (e.charAt(e.length - 2) == " " || !n || r) && (e = e.substr(0, e.length - 1) + " ");
    }
    return e.replace(/ {2}/g, "  ");
  }
  /**
  * Checks whether given node ends with a space character after changing appropriate space characters to `&nbsp;`s.
  *
  * @param  node Node to check.
  * @returns `true` if given `node` ends with space, `false` otherwise.
  */
  _nodeEndsWithSpace(t) {
    if (this._isPreFormatted(t))
      return !1;
    const e = this._processDataFromViewText(t);
    return e.charAt(e.length - 1) == " ";
  }
  /**
  * Checks whether given text contains preformatted white space. This is the case if
  * * any of node ancestors has a name which is in `preElements` array, or
  * * the closest ancestor that has the `white-space` CSS property sets it to a value that preserves spaces
  *
  * @param node Node to check
  * @returns `true` if given node contains preformatted white space, `false` otherwise.
  */
  _isPreFormatted(t) {
    if (F6(t, this.preElements))
      return !0;
    for (const e of t.getAncestors({
      parentFirst: !0
    }))
      if (!(!e.is("element") || !e.hasStyle("white-space") || e.getStyle("white-space") === "inherit"))
        return [
          "pre",
          "pre-wrap",
          "break-spaces"
        ].includes(e.getStyle("white-space"));
    return !1;
  }
  /**
  * Helper function. For given {@link module:engine/view/text~Text view text node}, it finds previous or next sibling
  * that is contained in the same container element. If there is no such sibling, `null` is returned.
  *
  * @param node Reference node.
  * @returns Touching text node, an inline object
  * or `null` if there is no next or previous touching text node.
  */
  _getTouchingInlineViewNode(t, e) {
    const n = new Ds({
      startPosition: e ? fe._createAfter(t) : fe._createBefore(t),
      direction: e ? "forward" : "backward"
    });
    for (const { item: r } of n) {
      if (r.is("$textProxy"))
        return r;
      if (r.is("element") && r.getCustomProperty("dataPipeline:transparentRendering"))
        continue;
      if (r.is("element", "br"))
        return null;
      if (this._isInlineObjectElement(r))
        return r;
      if (r.is("containerElement"))
        return null;
    }
    return null;
  }
  /**
  * Returns `true` if a DOM node belongs to {@link #blockElements}. `false` otherwise.
  */
  _isBlockDomElement(t) {
    return this.isElement(t) && this.blockElements.includes(t.tagName.toLowerCase());
  }
  /**
  * Returns `true` if a view node belongs to {@link #blockElements}. `false` otherwise.
  */
  _isBlockViewElement(t) {
    return t.is("element") && this.blockElements.includes(t.name);
  }
  /**
  * Returns `true` if a DOM node belongs to {@link #inlineObjectElements}. `false` otherwise.
  */
  _isInlineObjectElement(t) {
    return t.is("element") ? t.name == "br" || this.inlineObjectElements.includes(t.name) || !!this._inlineObjectElementMatcher.match(t) : !1;
  }
  /**
  * Creates view element basing on the node type.
  *
  * @param node DOM node to check.
  * @param options Conversion options. See {@link module:engine/view/domconverter~DomConverter#domToView} options parameter.
  */
  _createViewElement(t, e) {
    if (ll(t))
      return new $d(this.document, "$comment");
    const n = e.keepOriginalCase ? t.tagName : t.tagName.toLowerCase();
    return new Ji(this.document, n);
  }
  /**
  * Checks if view element's content should be treated as a raw data.
  *
  * @param viewElement View element to check.
  * @param options Conversion options. See {@link module:engine/view/domconverter~DomConverter#domToView} options parameter.
  */
  _isViewElementWithRawContent(t, e) {
    return e.withChildren !== !1 && t.is("element") && !!this._rawContentElementMatcher.match(t);
  }
  /**
  * Checks whether a given element name should be renamed in a current rendering mode.
  *
  * @param elementName The name of view element.
  */
  _shouldRenameElement(t) {
    const e = t.toLowerCase();
    return this.renderingMode === "editing" && this.unsafeElements.includes(e);
  }
  /**
  * Return a <span> element with a special attribute holding the name of the original element.
  * Optionally, copy all the attributes of the original element if that element is provided.
  *
  * @param elementName The name of view element.
  * @param originalDomElement The original DOM element to copy attributes and content from.
  */
  _createReplacementDomElement(t, e) {
    const n = this._domDocument.createElement("span");
    if (n.setAttribute(D1, t), e) {
      for (; e.firstChild; )
        n.appendChild(e.firstChild);
      for (const r of e.getAttributeNames())
        n.setAttribute(r, e.getAttribute(r));
    }
    return n;
  }
}
function F6(i, t) {
  return i.getAncestors().some((e) => e.is("element") && t.includes(e.name));
}
function L1(i, t) {
  let e = i;
  for (; e; )
    t(e), e = e.parentElement;
}
function $6(i, t) {
  return i.isEqualNode(N6) && H2(i, t) && i.parentNode.childNodes.length === 1;
}
function H2(i, t) {
  const e = i.parentNode;
  return !!e && !!e.tagName && t.includes(e.tagName.toLowerCase());
}
function N1(i) {
  i === "script" && Xe("domconverter-unsafe-script-element-detected"), i === "style" && Xe("domconverter-unsafe-style-element-detected");
}
function z6(i) {
  if (!Me.isGecko || !i.rangeCount)
    return !1;
  const t = i.getRangeAt(0).startContainer;
  try {
    Object.prototype.toString.call(t);
  } catch {
    return !0;
  }
  return !1;
}
class nr extends (/* @__PURE__ */ Xn()) {
  /**
  * Creates an instance of the observer.
  */
  constructor(e) {
    super();
    /**
    * An instance of the view controller.
    */
    h(this, "view");
    /**
    * A reference to the {@link module:engine/view/document~Document} object.
    */
    h(this, "document");
    /**
    * The state of the observer. If it is disabled, no events will be fired.
    */
    h(this, "_isEnabled", !1);
    this.view = e, this.document = e.document;
  }
  /**
  * The state of the observer. If it is disabled, no events will be fired.
  */
  get isEnabled() {
    return this._isEnabled;
  }
  /**
  * Enables the observer. This method is called when the observer is registered to the
  * {@link module:engine/view/view~View} and after {@link module:engine/view/view~View#forceRender rendering}
  * (all observers are {@link #disable disabled} before rendering).
  *
  * A typical use case for disabling observers is that mutation observers need to be disabled for the rendering.
  * However, a child class may not need to be disabled, so it can implement an empty method.
  *
  * @see module:engine/view/observer/observer~Observer#disable
  */
  enable() {
    this._isEnabled = !0;
  }
  /**
  * Disables the observer. This method is called before
  * {@link module:engine/view/view~View#forceRender rendering} to prevent firing events during rendering.
  *
  * @see module:engine/view/observer/observer~Observer#enable
  */
  disable() {
    this._isEnabled = !1;
  }
  /**
  * Disables and destroys the observer, among others removes event listeners created by the observer.
  */
  destroy() {
    this.disable(), this.stopListening();
  }
  /**
  * Checks whether a given DOM event should be ignored (should not be turned into a synthetic view document event).
  *
  * Currently, an event will be ignored only if its target or any of its ancestors has the `data-cke-ignore-events` attribute.
  * This attribute can be used inside the structures generated by
  * {@link module:engine/view/downcastwriter~DowncastWriter#createUIElement `DowncastWriter#createUIElement()`} to ignore events
  * fired within a UI that should be excluded from CKEditor 5's realms.
  *
  * @param domTarget The DOM event target to check (usually an element, sometimes a text node and
  * potentially sometimes a document, too).
  * @returns Whether this event should be ignored by the observer.
  */
  checkShouldIgnoreEventFromTarget(e) {
    return e && e.nodeType === 3 && (e = e.parentNode), !e || e.nodeType !== 1 ? !1 : e.matches("[data-cke-ignore-events], [data-cke-ignore-events] *");
  }
}
class Qo {
  /**
  * @param view The instance of the view controller.
  * @param domEvent The DOM event.
  * @param additionalData Additional properties that the instance should contain.
  */
  constructor(t, e, n) {
    /**
    * Instance of the view controller.
    */
    h(this, "view");
    /**
    * The instance of the document.
    */
    h(this, "document");
    /**
    * The DOM event.
    */
    h(this, "domEvent");
    /**
    * The DOM target.
    */
    h(this, "domTarget");
    this.view = t, this.document = t.document, this.domEvent = e, this.domTarget = e.target, p_(this, n);
  }
  /**
  * The tree view element representing the target.
  */
  get target() {
    return this.view.domConverter.mapDomToView(this.domTarget);
  }
  /**
  * Prevents the native's event default action.
  */
  preventDefault() {
    this.domEvent.preventDefault();
  }
  /**
  * Stops native event propagation.
  */
  stopPropagation() {
    this.domEvent.stopPropagation();
  }
}
class ws extends nr {
  constructor() {
    super(...arguments);
    /**
    * If set to `true` DOM events will be listened on the capturing phase.
    * Default value is `false`.
    */
    h(this, "useCapture", !1);
    /**
    * If set to `true`, indicates that the function specified by listener will never call `preventDefault()`.
    * Default value is `false`.
    */
    h(this, "usePassive", !1);
  }
  /**
  * @inheritDoc
  */
  observe(e) {
    (typeof this.domEventType == "string" ? [
      this.domEventType
    ] : this.domEventType).forEach((r) => {
      this.listenTo(e, r, (s, o) => {
        this.isEnabled && !this.checkShouldIgnoreEventFromTarget(o.target) && this.onDomEvent(o);
      }, {
        useCapture: this.useCapture,
        usePassive: this.usePassive
      });
    });
  }
  /**
  * @inheritDoc
  */
  stopObserving(e) {
    this.stopListening(e);
  }
  /**
  * Calls `Document#fire()` if observer {@link #isEnabled is enabled}.
  *
  * @see module:utils/emittermixin~Emitter#fire
  * @param eventType The event type (name).
  * @param domEvent The DOM event.
  * @param additionalData The additional data which should extend the
  * {@link module:engine/view/observer/domeventdata~DomEventData event data} object.
  */
  fire(e, n, r) {
    this.isEnabled && this.document.fire(e, new Qo(this.view, n, r));
  }
}
class H6 extends ws {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    h(this, "domEventType", [
      "keydown",
      "keyup"
    ]);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    const n = {
      keyCode: e.keyCode,
      altKey: e.altKey,
      ctrlKey: e.ctrlKey,
      shiftKey: e.shiftKey,
      metaKey: e.metaKey,
      get keystroke() {
        return Xo(this);
      }
    };
    this.fire(e.type, e, n);
  }
}
class j6 extends nr {
  /**
  * Creates new FakeSelectionObserver instance.
  */
  constructor(e) {
    super(e);
    /**
    * Fires debounced event `selectionChangeDone`. It uses `lodash#debounce` method to delay function call.
    */
    h(this, "_fireSelectionChangeDoneDebounced");
    this._fireSelectionChangeDoneDebounced = Ci((n) => {
      this.document.fire("selectionChangeDone", n);
    }, 200);
  }
  /**
  * @inheritDoc
  */
  observe() {
    const e = this.document;
    e.on("arrowKey", (n, r) => {
      e.selection.isFake && this.isEnabled && r.preventDefault();
    }, {
      context: "$capture"
    }), e.on("arrowKey", (n, r) => {
      e.selection.isFake && this.isEnabled && this._handleSelectionMove(r.keyCode);
    }, {
      priority: "lowest"
    });
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._fireSelectionChangeDoneDebounced.cancel();
  }
  /**
  * Handles collapsing view selection according to given key code. If left or up key is provided - new selection will be
  * collapsed to left. If right or down key is pressed - new selection will be collapsed to right.
  *
  * This method fires {@link module:engine/view/document~Document#event:selectionChange} and
  * {@link module:engine/view/document~Document#event:selectionChangeDone} events imitating behaviour of
  * {@link module:engine/view/observer/selectionobserver~SelectionObserver}.
  */
  _handleSelectionMove(e) {
    const n = this.document.selection, r = new Dr(n.getRanges(), {
      backward: n.isBackward,
      fake: !1
    });
    (e == Ke.arrowleft || e == Ke.arrowup) && r.setTo(r.getFirstPosition()), (e == Ke.arrowright || e == Ke.arrowdown) && r.setTo(r.getLastPosition());
    const s = {
      oldSelection: n,
      newSelection: r,
      domSelection: null
    };
    this.document.fire("selectionChange", s), this._fireSelectionChangeDoneDebounced(s);
  }
}
let j2 = class extends nr {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Reference to the {@link module:engine/view/view~View#domConverter}.
    */
    h(this, "domConverter");
    /**
    * Native mutation observer config.
    */
    h(this, "_config");
    /**
    * Observed DOM elements.
    */
    h(this, "_domElements");
    /**
    * Native mutation observer.
    */
    h(this, "_mutationObserver");
    this._config = {
      childList: !0,
      characterData: !0,
      subtree: !0
    }, this.domConverter = e.domConverter, this._domElements = /* @__PURE__ */ new Set(), this._mutationObserver = new window.MutationObserver(this._onMutations.bind(this));
  }
  /**
  * Synchronously handles mutations and empties the queue.
  */
  flush() {
    this._onMutations(this._mutationObserver.takeRecords());
  }
  /**
  * @inheritDoc
  */
  observe(e) {
    this._domElements.add(e), this.isEnabled && this._mutationObserver.observe(e, this._config);
  }
  /**
  * @inheritDoc
  */
  stopObserving(e) {
    if (this._domElements.delete(e), this.isEnabled) {
      this._mutationObserver.disconnect();
      for (const n of this._domElements)
        this._mutationObserver.observe(n, this._config);
    }
  }
  /**
  * @inheritDoc
  */
  enable() {
    super.enable();
    for (const e of this._domElements)
      this._mutationObserver.observe(e, this._config);
  }
  /**
  * @inheritDoc
  */
  disable() {
    super.disable(), this._mutationObserver.disconnect();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._mutationObserver.disconnect();
  }
  /**
  * Handles mutations. Mark view elements to sync and call render.
  *
  * @param domMutations Array of native mutations.
  */
  _onMutations(e) {
    if (e.length === 0)
      return;
    const n = this.domConverter, r = /* @__PURE__ */ new Set(), s = /* @__PURE__ */ new Set();
    for (const a of e) {
      const l = n.mapDomToView(a.target);
      l && (l.is("uiElement") || l.is("rawElement") || a.type === "childList" && !this._isBogusBrMutation(a) && s.add(l));
    }
    for (const a of e) {
      const l = n.mapDomToView(a.target);
      if (!(l && (l.is("uiElement") || l.is("rawElement"))) && a.type === "characterData") {
        const c = n.findCorrespondingViewText(a.target);
        c && !s.has(c.parent) ? r.add(c) : !c && bi(a.target) && s.add(n.mapDomToView(a.target.parentNode));
      }
    }
    const o = [];
    for (const a of r)
      o.push({
        type: "text",
        node: a
      });
    for (const a of s) {
      const l = n.mapViewToDom(a), c = Array.from(a.getChildren()), u = Array.from(n.domChildrenToView(l, {
        withChildren: !1
      }));
      V4(c, u, W6) || o.push({
        type: "children",
        node: a
      });
    }
    o.length && this.document.fire("mutations", {
      mutations: o
    });
  }
  /**
  * Checks if mutation was generated by the browser inserting bogus br on the end of the block element.
  * Such mutations are generated while pressing space or performing native spellchecker correction
  * on the end of the block element in Firefox browser.
  *
  * @param mutation Native mutation object.
  */
  _isBogusBrMutation(e) {
    let n = null;
    return e.nextSibling === null && e.removedNodes.length === 0 && e.addedNodes.length == 1 && (n = this.domConverter.domToView(e.addedNodes[0], {
      withChildren: !1
    })), n && n.is("element", "br");
  }
};
function W6(i, t) {
  if (!Array.isArray(i))
    return i === t ? !0 : i.is("$text") && t.is("$text") ? i.data === t.data : !1;
}
class $u extends ws {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Identifier of the timeout currently used by focus listener to delay rendering execution.
    */
    h(this, "_renderTimeoutId", null);
    /**
    * Set to `true` if the document is in the process of setting the focus.
    *
    * The flag is used to indicate that setting the focus is in progress.
    */
    h(this, "_isFocusChanging", !1);
    /**
    * @inheritDoc
    */
    h(this, "domEventType", [
      "focus",
      "blur"
    ]);
    this.useCapture = !0;
    const n = this.document;
    n.on("focus", () => this._handleFocus()), n.on("blur", (r, s) => this._handleBlur(s)), n.on("beforeinput", () => {
      n.isFocused || this._handleFocus();
    }, {
      priority: "highest"
    });
  }
  /**
  * Finishes setting the document focus state.
  */
  flush() {
    this._isFocusChanging && (this._isFocusChanging = !1, this.document.isFocused = !0);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._clearTimeout(), super.destroy();
  }
  /**
  * The `focus` event handler.
  */
  _handleFocus() {
    this._clearTimeout(), this._isFocusChanging = !0, this._renderTimeoutId = setTimeout(() => {
      this._renderTimeoutId = null, this.flush(), this.view.change(() => {
      });
    }, 50);
  }
  /**
  * The `blur` event handler.
  */
  _handleBlur(e) {
    const n = this.document.selection.editableElement;
    (n === null || n === e.target) && (this.document.isFocused = !1, this._isFocusChanging = !1, this.view.change(() => {
    }));
  }
  /**
  * Clears timeout.
  */
  _clearTimeout() {
    this._renderTimeoutId && (clearTimeout(this._renderTimeoutId), this._renderTimeoutId = null);
  }
}
class U6 extends nr {
  constructor(e) {
    super(e);
    /**
    * Instance of the mutation observer. Selection observer calls
    * {@link module:engine/view/observer/mutationobserver~MutationObserver#flush} to ensure that the mutations will be handled
    * before the {@link module:engine/view/document~Document#event:selectionChange} event is fired.
    */
    h(this, "mutationObserver");
    /**
    * Instance of the focus observer. Focus observer calls
    * {@link module:engine/view/observer/focusobserver~FocusObserver#flush} to mark the latest focus change as complete.
    */
    h(this, "focusObserver");
    /**
    * Reference to the view {@link module:engine/view/documentselection~DocumentSelection} object used to compare
    * new selection with it.
    */
    h(this, "selection");
    /**
    * Reference to the {@link module:engine/view/view~View#domConverter}.
    */
    h(this, "domConverter");
    /**
    * A set of documents which have added `selectionchange` listener to avoid adding a listener twice to the same
    * document.
    */
    h(this, "_documents");
    /**
    * Fires debounced event `selectionChangeDone`. It uses `lodash#debounce` method to delay function call.
    */
    h(this, "_fireSelectionChangeDoneDebounced");
    /**
    * When called, starts clearing the {@link #_loopbackCounter} counter in time intervals. When the number of selection
    * changes exceeds a certain limit within the interval of time, the observer will not fire `selectionChange` but warn about
    * possible infinite selection loop.
    */
    h(this, "_clearInfiniteLoopInterval");
    /**
    * Unlocks the `isSelecting` state of the view document in case the selection observer did not record this fact
    * correctly (for whatever reason). It is a safeguard (paranoid check), that returns document to the normal state
    * after a certain period of time (debounced, postponed by each selectionchange event).
    */
    h(this, "_documentIsSelectingInactivityTimeoutDebounced");
    /**
    * Private property to check if the code does not enter infinite loop.
    */
    h(this, "_loopbackCounter");
    this.mutationObserver = e.getObserver(j2), this.focusObserver = e.getObserver($u), this.selection = this.document.selection, this.domConverter = e.domConverter, this._documents = /* @__PURE__ */ new WeakSet(), this._fireSelectionChangeDoneDebounced = Ci((n) => {
      this.document.fire("selectionChangeDone", n);
    }, 200), this._clearInfiniteLoopInterval = setInterval(() => this._clearInfiniteLoop(), 1e3), this._documentIsSelectingInactivityTimeoutDebounced = Ci(() => this.document.isSelecting = !1, 5e3), this._loopbackCounter = 0;
  }
  /**
  * @inheritDoc
  */
  observe(e) {
    const n = e.ownerDocument, r = () => {
      this.document.isSelecting = !0, this._documentIsSelectingInactivityTimeoutDebounced();
    }, s = () => {
      this.document.isSelecting && (this._handleSelectionChange(n), this.document.isSelecting = !1, this._documentIsSelectingInactivityTimeoutDebounced.cancel());
    };
    this.listenTo(e, "selectstart", r, {
      priority: "highest"
    }), this.listenTo(e, "keydown", s, {
      priority: "highest",
      useCapture: !0
    }), this.listenTo(e, "keyup", s, {
      priority: "highest",
      useCapture: !0
    }), !this._documents.has(n) && (this.listenTo(n, "mouseup", s, {
      priority: "highest",
      useCapture: !0
    }), this.listenTo(n, "selectionchange", () => {
      this.document.isComposing && !Me.isAndroid || (this._handleSelectionChange(n), this._documentIsSelectingInactivityTimeoutDebounced());
    }), this.listenTo(this.view.document, "compositionstart", () => {
      this._handleSelectionChange(n);
    }, {
      priority: "lowest"
    }), this._documents.add(n));
  }
  /**
  * @inheritDoc
  */
  stopObserving(e) {
    this.stopListening(e);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), clearInterval(this._clearInfiniteLoopInterval), this._fireSelectionChangeDoneDebounced.cancel(), this._documentIsSelectingInactivityTimeoutDebounced.cancel();
  }
  /* istanbul ignore next -- @preserve */
  _reportInfiniteLoop() {
  }
  /**
  * Selection change listener. {@link module:engine/view/observer/mutationobserver~MutationObserver#flush Flush} mutations, check if
  * a selection changes and fires {@link module:engine/view/document~Document#event:selectionChange} event on every change
  * and {@link module:engine/view/document~Document#event:selectionChangeDone} when a selection stop changing.
  *
  * @param domDocument DOM document.
  */
  _handleSelectionChange(e) {
    if (!this.isEnabled)
      return;
    const n = e.defaultView.getSelection();
    if (this.checkShouldIgnoreEventFromTarget(n.anchorNode))
      return;
    this.mutationObserver.flush();
    const r = this.domConverter.domSelectionToView(n);
    if (r.rangeCount == 0) {
      this.view.hasDomSelection = !1;
      return;
    }
    if (this.view.hasDomSelection = !0, this.focusObserver.flush(), !(this.selection.isEqual(r) && this.domConverter.isDomSelectionCorrect(n))) {
      if (++this._loopbackCounter > 60) {
        this._reportInfiniteLoop();
        return;
      }
      if (this.selection.isSimilar(r))
        this.view.forceRender();
      else {
        const s = {
          oldSelection: this.selection,
          newSelection: r,
          domSelection: n
        };
        this.document.fire("selectionChange", s), this._fireSelectionChangeDoneDebounced(s);
      }
    }
  }
  /**
  * Clears `SelectionObserver` internal properties connected with preventing infinite loop.
  */
  _clearInfiniteLoop() {
    this._loopbackCounter = 0;
  }
}
class q6 extends ws {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * @inheritDoc
    */
    h(this, "domEventType", [
      "compositionstart",
      "compositionupdate",
      "compositionend"
    ]);
    const n = this.document;
    n.on("compositionstart", () => {
      n.isComposing = !0;
    }, {
      priority: "low"
    }), n.on("compositionend", () => {
      n.isComposing = !1;
    }, {
      priority: "low"
    });
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e, {
      data: e.data
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class W2 {
  /**
  * @param nativeDataTransfer The native [`DataTransfer`](https://developer.mozilla.org/en-US/docs/Web/API/DataTransfer) object.
  * @param options.cacheFiles Whether `files` list should be initialized in the constructor.
  */
  constructor(t, e = {}) {
    /**
    * The array of files created from the native `DataTransfer#files` or `DataTransfer#items`.
    */
    h(this, "_files");
    /**
    * The native DataTransfer object.
    */
    h(this, "_native");
    this._files = e.cacheFiles ? B1(t) : null, this._native = t;
  }
  /**
  * The array of files created from the native `DataTransfer#files` or `DataTransfer#items`.
  */
  get files() {
    return this._files || (this._files = B1(this._native)), this._files;
  }
  /**
  * Returns an array of available native content types.
  */
  get types() {
    return this._native.types;
  }
  /**
  * Gets the data from the data transfer by its MIME type.
  *
  * ```ts
  * dataTransfer.getData( 'text/plain' );
  * ```
  *
  * @param type The MIME type. E.g. `text/html` or `text/plain`.
  */
  getData(t) {
    return this._native.getData(t);
  }
  /**
  * Sets the data in the data transfer.
  *
  * @param type The MIME type. E.g. `text/html` or `text/plain`.
  */
  setData(t, e) {
    this._native.setData(t, e);
  }
  /**
  * The effect that is allowed for a drag operation.
  */
  set effectAllowed(t) {
    this._native.effectAllowed = t;
  }
  get effectAllowed() {
    return this._native.effectAllowed;
  }
  /**
  * The actual drop effect.
  */
  set dropEffect(t) {
    this._native.dropEffect = t;
  }
  get dropEffect() {
    return this._native.dropEffect;
  }
  /**
  * Set a preview image of the dragged content.
  */
  setDragImage(t, e, n) {
    this._native.setDragImage(t, e, n);
  }
  /**
  * Whether the dragging operation was canceled.
  */
  get isCanceled() {
    return this._native.dropEffect == "none" || !!this._native.mozUserCancelled;
  }
}
function B1(i) {
  const t = Array.from(i.files || []), e = Array.from(i.items || []);
  return t.length ? t : e.filter((n) => n.kind === "file").map((n) => n.getAsFile());
}
class G6 extends ws {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    h(this, "domEventType", "beforeinput");
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    const n = e.getTargetRanges(), r = this.view, s = r.document;
    let o = null, a = null, l = [];
    if (e.dataTransfer && (o = new W2(e.dataTransfer)), e.data !== null ? a = e.data : o && (a = o.getData("text/plain")), s.selection.isFake)
      l = Array.from(s.selection.getRanges());
    else if (n.length)
      l = n.map((c) => {
        const u = r.domConverter.domPositionToView(c.startContainer, c.startOffset), d = r.domConverter.domPositionToView(c.endContainer, c.endOffset);
        if (u)
          return r.createRange(u, d);
        if (d)
          return r.createRange(d);
      }).filter((c) => !!c);
    else if (Me.isAndroid) {
      const c = e.target.ownerDocument.defaultView.getSelection();
      l = Array.from(r.domConverter.domSelectionToView(c).getRanges());
    }
    if (Me.isAndroid && e.inputType == "insertCompositionText" && a && a.endsWith(`
`)) {
      this.fire(e.type, e, {
        inputType: "insertParagraph",
        targetRanges: [
          r.createRange(l[0].end)
        ]
      });
      return;
    }
    if (e.inputType == "insertText" && a && a.includes(`
`)) {
      const c = a.split(/\n{1,2}/g);
      let u = l;
      for (let d = 0; d < c.length; d++) {
        const f = c[d];
        f != "" && (this.fire(e.type, e, {
          data: f,
          dataTransfer: o,
          targetRanges: u,
          inputType: e.inputType,
          isComposing: e.isComposing
        }), u = [
          s.selection.getFirstRange()
        ]), d + 1 < c.length && (this.fire(e.type, e, {
          inputType: "insertParagraph",
          targetRanges: u
        }), u = [
          s.selection.getFirstRange()
        ]);
      }
      return;
    }
    this.fire(e.type, e, {
      data: a,
      dataTransfer: o,
      targetRanges: l,
      inputType: e.inputType,
      isComposing: e.isComposing
    });
  }
}
class K6 extends nr {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.document.on("keydown", (e, n) => {
      if (this.isEnabled && RR(n.keyCode)) {
        const r = new ga(this.document, "arrowKey", this.document.selection.getFirstRange());
        this.document.fire(r, n), r.stop.called && e.stop();
      }
    });
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
class Z6 extends nr {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = this.document;
    e.on("keydown", (n, r) => {
      if (!this.isEnabled || r.keyCode != Ke.tab || r.ctrlKey)
        return;
      const s = new ga(e, "tab", e.selection.getFirstRange());
      e.fire(s, r), s.stop.called && n.stop();
    });
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
let Y6 = class extends (/* @__PURE__ */ st()) {
  /**
  * @param stylesProcessor The styles processor instance.
  */
  constructor(e) {
    super();
    /**
    * Instance of the {@link module:engine/view/document~Document} associated with this view controller.
    */
    h(this, "document");
    /**
    * Instance of the {@link module:engine/view/domconverter~DomConverter domConverter} used by
    * {@link module:engine/view/view~View#_renderer renderer}
    * and {@link module:engine/view/observer/observer~Observer observers}.
    */
    h(this, "domConverter");
    /**
    * Roots of the DOM tree. Map on the `HTMLElement`s with roots names as keys.
    */
    h(this, "domRoots", /* @__PURE__ */ new Map());
    /**
    * Instance of the {@link module:engine/view/renderer~Renderer renderer}.
    */
    h(this, "_renderer");
    /**
    * A DOM root attributes cache. It saves the initial values of DOM root attributes before the DOM element
    * is {@link module:engine/view/view~View#attachDomRoot attached} to the view so later on, when
    * the view is destroyed ({@link module:engine/view/view~View#detachDomRoot}), they can be easily restored.
    * This way, the DOM element can go back to the (clean) state as if the editing view never used it.
    */
    h(this, "_initialDomRootAttributes", /* @__PURE__ */ new WeakMap());
    /**
    * Map of registered {@link module:engine/view/observer/observer~Observer observers}.
    */
    h(this, "_observers", /* @__PURE__ */ new Map());
    /**
    * DowncastWriter instance used in {@link #change change method} callbacks.
    */
    h(this, "_writer");
    /**
    * Is set to `true` when {@link #change view changes} are currently in progress.
    */
    h(this, "_ongoingChange", !1);
    /**
    * Used to prevent calling {@link #forceRender} and {@link #change} during rendering view to the DOM.
    */
    h(this, "_postFixersInProgress", !1);
    /**
    * Internal flag to temporary disable rendering. See the usage in the {@link #_disableRendering}.
    */
    h(this, "_renderingDisabled", !1);
    /**
    * Internal flag that disables rendering when there are no changes since the last rendering.
    * It stores information about changed selection and changed elements from attached document roots.
    */
    h(this, "_hasChangedSinceTheLastRendering", !1);
    this.document = new Fd(e), this.domConverter = new zd(this.document), this.set("isRenderingInProgress", !1), this.set("hasDomSelection", !1), this._renderer = new A6(this.domConverter, this.document.selection), this._renderer.bind("isFocused", "isSelecting", "isComposing").to(this.document, "isFocused", "isSelecting", "isComposing"), this._writer = new N2(this.document), this.addObserver(j2), this.addObserver($u), this.addObserver(U6), this.addObserver(H6), this.addObserver(j6), this.addObserver(q6), this.addObserver(K6), this.addObserver(G6), this.addObserver(Z6), T6(this), v6(this), this.on("render", () => {
      this._render(), this.document.fire("layoutChanged"), this._hasChangedSinceTheLastRendering = !1;
    }), this.listenTo(this.document.selection, "change", () => {
      this._hasChangedSinceTheLastRendering = !0;
    }), this.listenTo(this.document, "change:isFocused", () => {
      this._hasChangedSinceTheLastRendering = !0;
    }), Me.isiOS && this.listenTo(this.document, "blur", (n, r) => {
      this.domConverter.mapDomToView(r.domEvent.relatedTarget) || this.domConverter._clearDomSelection();
    }), this.listenTo(this.document, "mutations", (n, { mutations: r }) => {
      r.forEach((s) => this._renderer.markToSync(s.type, s.node));
    }, {
      priority: "low"
    }), this.listenTo(this.document, "mutations", () => {
      this.forceRender();
    }, {
      priority: "lowest"
    });
  }
  /**
  * Attaches a DOM root element to the view element and enable all observers on that element.
  * Also {@link module:engine/view/renderer~Renderer#markToSync mark element} to be synchronized
  * with the view what means that all child nodes will be removed and replaced with content of the view root.
  *
  * This method also will change view element name as the same as tag name of given dom root.
  * Name is always transformed to lower case.
  *
  * **Note:** Use {@link #detachDomRoot `detachDomRoot()`} to revert this action.
  *
  * @param domRoot DOM root element.
  * @param name Name of the root.
  */
  attachDomRoot(e, n = "main") {
    const r = this.document.getRoot(n);
    r._name = e.tagName.toLowerCase();
    const s = {};
    for (const { name: a, value: l } of Array.from(e.attributes))
      s[a] = l, a === "class" ? this._writer.addClass(l.split(" "), r) : r.hasAttribute(a) || this._writer.setAttribute(a, l, r);
    this._initialDomRootAttributes.set(e, s);
    const o = () => {
      this._writer.setAttribute("contenteditable", (!r.isReadOnly).toString(), r), r.isReadOnly ? this._writer.addClass("ck-read-only", r) : this._writer.removeClass("ck-read-only", r);
    };
    o(), this.domRoots.set(n, e), this.domConverter.bindElements(e, r), this._renderer.markToSync("children", r), this._renderer.markToSync("attributes", r), this._renderer.domDocuments.add(e.ownerDocument), r.on("change:children", (a, l) => this._renderer.markToSync("children", l)), r.on("change:attributes", (a, l) => this._renderer.markToSync("attributes", l)), r.on("change:text", (a, l) => this._renderer.markToSync("text", l)), r.on("change:isReadOnly", () => this.change(o)), r.on("change", () => {
      this._hasChangedSinceTheLastRendering = !0;
    });
    for (const a of this._observers.values())
      a.observe(e, n);
  }
  /**
  * Detaches a DOM root element from the view element and restores its attributes to the state before
  * {@link #attachDomRoot `attachDomRoot()`}.
  *
  * @param name Name of the root to detach.
  */
  detachDomRoot(e) {
    const n = this.domRoots.get(e);
    Array.from(n.attributes).forEach(({ name: s }) => n.removeAttribute(s));
    const r = this._initialDomRootAttributes.get(n);
    for (const s in r)
      n.setAttribute(s, r[s]);
    this.domRoots.delete(e), this.domConverter.unbindDomElement(n);
    for (const s of this._observers.values())
      s.stopObserving(n);
  }
  /**
  * Gets DOM root element.
  *
  * @param name  Name of the root.
  * @returns DOM root element instance.
  */
  getDomRoot(e = "main") {
    return this.domRoots.get(e);
  }
  /**
  * Creates observer of the given type if not yet created, {@link module:engine/view/observer/observer~Observer#enable enables} it
  * and {@link module:engine/view/observer/observer~Observer#observe attaches} to all existing and future
  * {@link #domRoots DOM roots}.
  *
  * Note: Observers are recognized by their constructor (classes). A single observer will be instantiated and used only
  * when registered for the first time. This means that features and other components can register a single observer
  * multiple times without caring whether it has been already added or not.
  *
  * @param ObserverConstructor The constructor of an observer to add.
  * Should create an instance inheriting from {@link module:engine/view/observer/observer~Observer}.
  * @returns Added observer instance.
  */
  addObserver(e) {
    let n = this._observers.get(e);
    if (n)
      return n;
    n = new e(this), this._observers.set(e, n);
    for (const [r, s] of this.domRoots)
      n.observe(s, r);
    return n.enable(), n;
  }
  /**
  * Returns observer of the given type or `undefined` if such observer has not been added yet.
  *
  * @param ObserverConstructor The constructor of an observer to get.
  * @returns Observer instance or undefined.
  */
  getObserver(e) {
    return this._observers.get(e);
  }
  /**
  * Disables all added observers.
  */
  disableObservers() {
    for (const e of this._observers.values())
      e.disable();
  }
  /**
  * Enables all added observers.
  */
  enableObservers() {
    for (const e of this._observers.values())
      e.enable();
  }
  /**
  * Scrolls the page viewport and {@link #domRoots} with their ancestors to reveal the
  * caret, **if not already visible to the user**.
  *
  * **Note**: Calling this method fires the {@link module:engine/view/view~ViewScrollToTheSelectionEvent} event that
  * allows custom behaviors.
  *
  * @param options Additional configuration of the scrolling behavior.
  * @param options.viewportOffset A distance between the DOM selection and the viewport boundary to be maintained
  * while scrolling to the selection (default is 20px). Setting this value to `0` will reveal the selection precisely at
  * the viewport boundary.
  * @param options.ancestorOffset A distance between the DOM selection and scrollable DOM root ancestor(s) to be maintained
  * while scrolling to the selection (default is 20px). Setting this value to `0` will reveal the selection precisely at
  * the scrollable ancestor(s) boundary.
  * @param options.alignToTop When set `true`, the DOM selection will be aligned to the top of the viewport if not already visible
  * (see `forceScroll` to learn more).
  * @param options.forceScroll When set `true`, the DOM selection will be aligned to the top of the viewport and scrollable ancestors
  * whether it is already visible or not. This option will only work when `alignToTop` is `true`.
  */
  scrollToTheSelection({ alignToTop: e, forceScroll: n, viewportOffset: r = 20, ancestorOffset: s = 20 } = {}) {
    const o = this.document.selection.getFirstRange();
    if (!o)
      return;
    const a = Fi({
      alignToTop: e,
      forceScroll: n,
      viewportOffset: r,
      ancestorOffset: s
    });
    typeof r == "number" && (r = {
      top: r,
      bottom: r,
      left: r,
      right: r
    });
    const l = {
      target: this.domConverter.viewRangeToDom(o),
      viewportOffset: r,
      ancestorOffset: s,
      alignToTop: e,
      forceScroll: n
    };
    this.fire("scrollToTheSelection", l, a), S2(l);
  }
  /**
  * It will focus DOM element representing {@link module:engine/view/editableelement~EditableElement EditableElement}
  * that is currently having selection inside.
  */
  focus() {
    if (!this.document.isFocused) {
      const e = this.document.selection.editableElement;
      e && (this.domConverter.focus(e), this.forceRender());
    }
  }
  /**
  * The `change()` method is the primary way of changing the view. You should use it to modify any node in the view tree.
  * It makes sure that after all changes are made the view is rendered to the DOM (assuming that the view will be changed
  * inside the callback). It prevents situations when the DOM is updated when the view state is not yet correct. It allows
  * to nest calls one inside another and still performs a single rendering after all those changes are made.
  * It also returns the return value of its callback.
  *
  * ```ts
  * const text = view.change( writer => {
  * 	const newText = writer.createText( 'foo' );
  * 	writer.insert( position1, newText );
  *
  * 	view.change( writer => {
  * 		writer.insert( position2, writer.createText( 'bar' ) );
  * 	} );
  *
  * 	writer.remove( range );
  *
  * 	return newText;
  * } );
  * ```
  *
  * When the outermost change block is done and rendering to the DOM is over the
  * {@link module:engine/view/view~View#event:render `View#render`} event is fired.
  *
  * This method throws a `applying-view-changes-on-rendering` error when
  * the change block is used after rendering to the DOM has started.
  *
  * @param callback Callback function which may modify the view.
  * @returns Value returned by the callback.
  */
  change(e) {
    if (this.isRenderingInProgress || this._postFixersInProgress)
      throw new T("cannot-change-view-tree", this);
    try {
      if (this._ongoingChange)
        return e(this._writer);
      this._ongoingChange = !0;
      const n = e(this._writer);
      return this._ongoingChange = !1, !this._renderingDisabled && this._hasChangedSinceTheLastRendering && (this._postFixersInProgress = !0, this.document._callPostFixers(this._writer), this._postFixersInProgress = !1, this.fire("render")), n;
    } catch (n) {
      /* istanbul ignore next -- @preserve */
      T.rethrowUnexpectedError(n, this);
    }
  }
  /**
  * Forces rendering {@link module:engine/view/document~Document view document} to DOM. If any view changes are
  * currently in progress, rendering will start after all {@link #change change blocks} are processed.
  *
  * Note that this method is dedicated for special cases. All view changes should be wrapped in the {@link #change}
  * block and the view will automatically check whether it needs to render DOM or not.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `applying-view-changes-on-rendering` when
  * trying to re-render when rendering to DOM has already started.
  */
  forceRender() {
    this._hasChangedSinceTheLastRendering = !0, this.getObserver($u).flush(), this.change(() => {
    });
  }
  /**
  * Destroys this instance. Makes sure that all observers are destroyed and listeners removed.
  */
  destroy() {
    for (const e of this._observers.values())
      e.destroy();
    this.document.destroy(), this.stopListening();
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/view/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/view/item~Item view item} and `'before'` or `'after'` (sets position before or after given view item).
  *
  * This method is a shortcut to other constructors such as:
  *
  * * {@link #createPositionBefore},
  * * {@link #createPositionAfter},
  *
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  createPositionAt(e, n) {
    return fe._createAt(e, n);
  }
  /**
  * Creates a new position after given view item.
  *
  * @param item View item after which the position should be located.
  */
  createPositionAfter(e) {
    return fe._createAfter(e);
  }
  /**
  * Creates a new position before given view item.
  *
  * @param item View item before which the position should be located.
  */
  createPositionBefore(e) {
    return fe._createBefore(e);
  }
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * **Note:** This factory method creates it's own {@link module:engine/view/position~Position} instances basing on passed values.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at `start` position.
  */
  createRange(e, n) {
    return new qe(e, n);
  }
  /**
  * Creates a range that starts before given {@link module:engine/view/item~Item view item} and ends after it.
  */
  createRangeOn(e) {
    return qe._createOn(e);
  }
  /**
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(e) {
    return qe._createIn(e);
  }
  createSelection(...e) {
    return new Dr(...e);
  }
  /**
  * Disables or enables rendering. If the flag is set to `true` then the rendering will be disabled.
  * If the flag is set to `false` and if there was some change in the meantime, then the rendering action will be performed.
  *
  * @internal
  * @param flag A flag indicates whether the rendering should be disabled.
  */
  _disableRendering(e) {
    this._renderingDisabled = e, e == !1 && this.change(() => {
    });
  }
  /**
  * Renders all changes. In order to avoid triggering the observers (e.g. selection) all observers are disabled
  * before rendering and re-enabled after that.
  */
  _render() {
    this.isRenderingInProgress = !0, this.disableObservers(), this._renderer.render(), this.enableObservers(), this.isRenderingInProgress = !1;
  }
};
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class ys {
  /* istanbul ignore next -- @preserve */
  is() {
    throw new Error("is() method is abstract");
  }
}
let no = class extends ys {
  /**
  * Creates a model node.
  *
  * This is an abstract class, so this constructor should not be used directly.
  *
  * @param attrs Node's attributes. See {@link module:utils/tomap~toMap} for a list of accepted values.
  */
  constructor(e) {
    super();
    /**
    * Parent of this node. It could be {@link module:engine/model/element~Element}
    * or {@link module:engine/model/documentfragment~DocumentFragment}.
    * Equals to `null` if the node has no parent.
    */
    h(this, "parent", null);
    /**
    * Attributes set on this node.
    */
    h(this, "_attrs");
    /**
    * Index of this node in its parent or `null` if the node has no parent.
    *
    * @internal
    */
    h(this, "_index", null);
    /**
    * Offset at which this node starts in its parent or `null` if the node has no parent.
    *
    * @internal
    */
    h(this, "_startOffset", null);
    this._attrs = gr(e);
  }
  /**
  * {@link module:engine/model/document~Document Document} that owns this root element.
  */
  get document() {
    return null;
  }
  /**
  * Index of this node in its parent or `null` if the node has no parent.
  */
  get index() {
    return this._index;
  }
  /**
  * Offset at which this node starts in its parent. It is equal to the sum of {@link #offsetSize offsetSize}
  * of all its previous siblings. Equals to `null` if node has no parent.
  */
  get startOffset() {
    return this._startOffset;
  }
  /**
  * Offset size of this node.
  *
  * Represents how much "offset space" is occupied by the node in its parent. It is important for
  * {@link module:engine/model/position~Position position}. When node has `offsetSize` greater than `1`, position can be placed between
  * that node start and end. `offsetSize` greater than `1` is for nodes that represents more than one entity, i.e.
  * a {@link module:engine/model/text~Text text node}.
  */
  get offsetSize() {
    return 1;
  }
  /**
  * Offset at which this node ends in its parent. It is equal to the sum of this node's
  * {@link module:engine/model/node~Node#startOffset start offset} and {@link #offsetSize offset size}.
  * Equals to `null` if the node has no parent.
  */
  get endOffset() {
    return this.startOffset === null ? null : this.startOffset + this.offsetSize;
  }
  /**
  * Node's next sibling or `null` if the node is a last child of it's parent or if the node has no parent.
  */
  get nextSibling() {
    const e = this.index;
    return e !== null && this.parent.getChild(e + 1) || null;
  }
  /**
  * Node's previous sibling or `null` if the node is a first child of it's parent or if the node has no parent.
  */
  get previousSibling() {
    const e = this.index;
    return e !== null && this.parent.getChild(e - 1) || null;
  }
  /**
  * The top-most ancestor of the node. If node has no parent it is the root itself. If the node is a part
  * of {@link module:engine/model/documentfragment~DocumentFragment}, it's `root` is equal to that `DocumentFragment`.
  */
  get root() {
    let e = this;
    for (; e.parent; )
      e = e.parent;
    return e;
  }
  /**
  * Returns `true` if the node is inside a document root that is attached to the document.
  */
  isAttached() {
    return this.parent === null ? !1 : this.root.isAttached();
  }
  /**
  * Gets path to the node. The path is an array containing starting offsets of consecutive ancestors of this node,
  * beginning from {@link module:engine/model/node~Node#root root}, down to this node's starting offset. The path can be used to
  * create {@link module:engine/model/position~Position Position} instance.
  *
  * ```ts
  * const abc = new Text( 'abc' );
  * const foo = new Text( 'foo' );
  * const h1 = new Element( 'h1', null, new Text( 'header' ) );
  * const p = new Element( 'p', null, [ abc, foo ] );
  * const div = new Element( 'div', null, [ h1, p ] );
  * foo.getPath(); // Returns [ 1, 3 ]. `foo` is in `p` which is in `div`. `p` starts at offset 1, while `foo` at 3.
  * h1.getPath(); // Returns [ 0 ].
  * div.getPath(); // Returns [].
  * ```
  */
  getPath() {
    const e = [];
    let n = this;
    for (; n.parent; )
      e.unshift(n.startOffset), n = n.parent;
    return e;
  }
  /**
  * Returns ancestors array of this node.
  *
  * @param options Options object.
  * @param options.includeSelf When set to `true` this node will be also included in parent's array.
  * @param options.parentFirst When set to `true`, array will be sorted from node's parent to root element,
  * otherwise root element will be the first item in the array.
  * @returns Array with ancestors.
  */
  getAncestors(e = {}) {
    const n = [];
    let r = e.includeSelf ? this : this.parent;
    for (; r; )
      n[e.parentFirst ? "push" : "unshift"](r), r = r.parent;
    return n;
  }
  /**
  * Returns a {@link module:engine/model/element~Element} or {@link module:engine/model/documentfragment~DocumentFragment}
  * which is a common ancestor of both nodes.
  *
  * @param node The second node.
  * @param options Options object.
  * @param options.includeSelf When set to `true` both nodes will be considered "ancestors" too.
  * Which means that if e.g. node A is inside B, then their common ancestor will be B.
  */
  getCommonAncestor(e, n = {}) {
    const r = this.getAncestors(n), s = e.getAncestors(n);
    let o = 0;
    for (; r[o] == s[o] && r[o]; )
      o++;
    return o === 0 ? null : r[o - 1];
  }
  /**
  * Returns whether this node is before given node. `false` is returned if nodes are in different trees (for example,
  * in different {@link module:engine/model/documentfragment~DocumentFragment}s).
  *
  * @param node Node to compare with.
  */
  isBefore(e) {
    if (this == e || this.root !== e.root)
      return !1;
    const n = this.getPath(), r = e.getPath(), s = Fn(n, r);
    switch (s) {
      case "prefix":
        return !0;
      case "extension":
        return !1;
      default:
        return n[s] < r[s];
    }
  }
  /**
  * Returns whether this node is after given node. `false` is returned if nodes are in different trees (for example,
  * in different {@link module:engine/model/documentfragment~DocumentFragment}s).
  *
  * @param node Node to compare with.
  */
  isAfter(e) {
    return this == e || this.root !== e.root ? !1 : !this.isBefore(e);
  }
  /**
  * Checks if the node has an attribute with given key.
  *
  * @param key Key of attribute to check.
  * @returns `true` if attribute with given key is set on node, `false` otherwise.
  */
  hasAttribute(e) {
    return this._attrs.has(e);
  }
  /**
  * Gets an attribute value for given key or `undefined` if that attribute is not set on node.
  *
  * @param key Key of attribute to look for.
  * @returns Attribute value or `undefined`.
  */
  getAttribute(e) {
    return this._attrs.get(e);
  }
  /**
  * Returns iterator that iterates over this node's attributes.
  *
  * Attributes are returned as arrays containing two items. First one is attribute key and second is attribute value.
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  getAttributes() {
    return this._attrs.entries();
  }
  /**
  * Returns iterator that iterates over this node's attribute keys.
  */
  getAttributeKeys() {
    return this._attrs.keys();
  }
  /**
  * Converts `Node` to plain object and returns it.
  *
  * @returns `Node` converted to plain object.
  */
  toJSON() {
    const e = {};
    return this._attrs.size && (e.attributes = Array.from(this._attrs).reduce((n, r) => (n[r[0]] = r[1], n), {})), e;
  }
  /**
  * Creates a copy of this node, that is a node with exactly same attributes, and returns it.
  *
  * @internal
  * @returns Node with same attributes as this node.
  */
  _clone(e) {
    return new this.constructor(this._attrs);
  }
  /**
  * Removes this node from its parent.
  *
  * @internal
  * @see module:engine/model/writer~Writer#remove
  */
  _remove() {
    this.parent._removeChildren(this.index);
  }
  /**
  * Sets attribute on the node. If attribute with the same key already is set, it's value is overwritten.
  *
  * @see module:engine/model/writer~Writer#setAttribute
  * @internal
  * @param key Key of attribute to set.
  * @param value Attribute value.
  */
  _setAttribute(e, n) {
    this._attrs.set(e, n);
  }
  /**
  * Removes all attributes from the node and sets given attributes.
  *
  * @see module:engine/model/writer~Writer#setAttributes
  * @internal
  * @param attrs Attributes to set. See {@link module:utils/tomap~toMap} for a list of accepted values.
  */
  _setAttributesTo(e) {
    this._attrs = gr(e);
  }
  /**
  * Removes an attribute with given key from the node.
  *
  * @see module:engine/model/writer~Writer#removeAttribute
  * @internal
  * @param key Key of attribute to remove.
  * @returns `true` if the attribute was set on the element, `false` otherwise.
  */
  _removeAttribute(e) {
    return this._attrs.delete(e);
  }
  /**
  * Removes all attributes from the node.
  *
  * @see module:engine/model/writer~Writer#clearAttributes
  * @internal
  */
  _clearAttributes() {
    this._attrs.clear();
  }
};
no.prototype.is = function(i) {
  return i === "node" || i === "model:node";
};
class cl {
  /**
  * Creates a node list.
  *
  * @internal
  * @param nodes Nodes contained in this node list.
  */
  constructor(t) {
    /**
    * Nodes contained in this node list.
    */
    h(this, "_nodes", []);
    /**
    * This array maps numbers (offsets) to node that is placed at that offset.
    *
    * This array is similar to `_nodes` with the difference that one node may occupy multiple consecutive items in the array.
    *
    * This array is needed to quickly retrieve a node that is placed at given offset.
    */
    h(this, "_offsetToNode", []);
    t && this._insertNodes(0, t);
  }
  /**
  * Iterable interface.
  *
  * Iterates over all nodes contained inside this node list.
  */
  [Symbol.iterator]() {
    return this._nodes[Symbol.iterator]();
  }
  /**
  * Number of nodes contained inside this node list.
  */
  get length() {
    return this._nodes.length;
  }
  /**
  * Sum of {@link module:engine/model/node~Node#offsetSize offset sizes} of all nodes contained inside this node list.
  */
  get maxOffset() {
    return this._offsetToNode.length;
  }
  /**
  * Gets the node at the given index. Returns `null` if incorrect index was passed.
  */
  getNode(t) {
    return this._nodes[t] || null;
  }
  /**
  * Gets the node at the given offset. Returns `null` if incorrect offset was passed.
  */
  getNodeAtOffset(t) {
    return this._offsetToNode[t] || null;
  }
  /**
  * Returns an index of the given node or `null` if given node does not have a parent.
  *
  * This is an alias to {@link module:engine/model/node~Node#index}.
  */
  getNodeIndex(t) {
    return t.index;
  }
  /**
  * Returns the offset at which given node is placed in its parent or `null` if given node does not have a parent.
  *
  * This is an alias to {@link module:engine/model/node~Node#startOffset}.
  */
  getNodeStartOffset(t) {
    return t.startOffset;
  }
  /**
  * Converts index to offset in node list.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `model-nodelist-index-out-of-bounds` if given index is less
  * than `0` or more than {@link #length}.
  */
  indexToOffset(t) {
    if (t == this._nodes.length)
      return this.maxOffset;
    const e = this._nodes[t];
    if (!e)
      throw new T("model-nodelist-index-out-of-bounds", this);
    return this.getNodeStartOffset(e);
  }
  /**
  * Converts offset in node list to index.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `model-nodelist-offset-out-of-bounds` if given offset is less
  * than `0` or more than {@link #maxOffset}.
  */
  offsetToIndex(t) {
    if (t == this._offsetToNode.length)
      return this._nodes.length;
    const e = this._offsetToNode[t];
    if (!e)
      throw new T("model-nodelist-offset-out-of-bounds", this, {
        offset: t,
        nodeList: this
      });
    return this.getNodeIndex(e);
  }
  /**
  * Inserts given nodes at given index.
  *
  * @internal
  * @param index Index at which nodes should be inserted.
  * @param nodes Nodes to be inserted.
  */
  _insertNodes(t, e) {
    for (const o of e)
      if (!(o instanceof no))
        throw new T("model-nodelist-insertnodes-not-node", this);
    const n = Array.from(e), r = J6(n);
    let s = this.indexToOffset(t);
    this._nodes = k1(this._nodes, n, t, 0), this._offsetToNode = k1(this._offsetToNode, r, s, 0);
    for (let o = t; o < this._nodes.length; o++)
      this._nodes[o]._index = o, this._nodes[o]._startOffset = s, s += this._nodes[o].offsetSize;
  }
  /**
  * Removes one or more nodes starting at the given index.
  *
  * @internal
  * @param indexStart Index of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @returns Array containing removed nodes.
  */
  _removeNodes(t, e = 1) {
    if (e == 0)
      return [];
    let n = this.indexToOffset(t);
    const r = this._nodes.splice(t, e), s = r[r.length - 1], o = s.startOffset + s.offsetSize - n;
    this._offsetToNode.splice(n, o);
    for (const a of r)
      a._index = null, a._startOffset = null;
    for (let a = t; a < this._nodes.length; a++)
      this._nodes[a]._index = a, this._nodes[a]._startOffset = n, n += this._nodes[a].offsetSize;
    return r;
  }
  /**
  * Converts `NodeList` instance to an array containing nodes that were inserted in the node list. Nodes
  * are also converted to their plain object representation.
  *
  * @returns `NodeList` instance converted to `Array`.
  */
  toJSON() {
    return this._nodes.map((t) => t.toJSON());
  }
}
function J6(i) {
  const t = [];
  for (const e of i) {
    const n = t.length;
    t.length += e.offsetSize, t.fill(e, n);
  }
  return t;
}
class mt extends no {
  /**
  * Creates a text node.
  *
  * **Note:** Constructor of this class shouldn't be used directly in the code.
  * Use the {@link module:engine/model/writer~Writer#createText} method instead.
  *
  * @internal
  * @param data Node's text.
  * @param attrs Node's attributes. See {@link module:utils/tomap~toMap} for a list of accepted values.
  */
  constructor(e, n) {
    super(n);
    /**
    * Text data contained in this text node.
    *
    * @internal
    */
    h(this, "_data");
    this._data = e || "";
  }
  /**
  * @inheritDoc
  */
  get offsetSize() {
    return this.data.length;
  }
  /**
  * Returns a text data contained in the node.
  */
  get data() {
    return this._data;
  }
  /**
  * Converts `Text` instance to plain object and returns it.
  *
  * @returns`Text` instance converted to plain object.
  */
  toJSON() {
    const e = super.toJSON();
    return e.data = this.data, e;
  }
  /**
  * Creates a copy of this text node and returns it. Created text node has same text data and attributes as original text node.
  *
  * @internal
  * @returns `Text` instance created using given plain object.
  */
  _clone() {
    return new mt(this.data, this.getAttributes());
  }
  /**
  * Creates a `Text` instance from given plain object (i.e. parsed JSON string).
  *
  * @param json Plain object to be converted to `Text`.
  * @returns `Text` instance created using given plain object.
  */
  static fromJSON(e) {
    return new mt(e.data, e.attributes);
  }
}
mt.prototype.is = function(i) {
  return i === "$text" || i === "model:$text" || // This are legacy values kept for backward compatibility.
  i === "text" || i === "model:text" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "node" || i === "model:node";
};
class Gi extends ys {
  /**
  * Creates a text proxy.
  *
  * @internal
  * @param textNode Text node which part is represented by this text proxy.
  * @param offsetInText Offset in {@link module:engine/model/textproxy~TextProxy#textNode text node} from which the text proxy
  * starts.
  * @param length Text proxy length, that is how many text node's characters, starting from `offsetInText` it represents.
  */
  constructor(e, n, r) {
    super();
    /**
    * Text node which part is represented by this text proxy.
    */
    h(this, "textNode");
    /**
    * Text data represented by this text proxy.
    */
    h(this, "data");
    /**
    * Offset in {@link module:engine/model/textproxy~TextProxy#textNode text node} from which the text proxy starts.
    */
    h(this, "offsetInText");
    if (this.textNode = e, n < 0 || n > e.offsetSize)
      throw new T("model-textproxy-wrong-offsetintext", this);
    if (r < 0 || n + r > e.offsetSize)
      throw new T("model-textproxy-wrong-length", this);
    this.data = e.data.substring(n, n + r), this.offsetInText = n;
  }
  /**
  * Offset at which this text proxy starts in it's parent.
  *
  * @see module:engine/model/node~Node#startOffset
  */
  get startOffset() {
    return this.textNode.startOffset !== null ? this.textNode.startOffset + this.offsetInText : null;
  }
  /**
  * Offset size of this text proxy. Equal to the number of characters represented by the text proxy.
  *
  * @see module:engine/model/node~Node#offsetSize
  */
  get offsetSize() {
    return this.data.length;
  }
  /**
  * Offset at which this text proxy ends in it's parent.
  *
  * @see module:engine/model/node~Node#endOffset
  */
  get endOffset() {
    return this.startOffset !== null ? this.startOffset + this.offsetSize : null;
  }
  /**
  * Flag indicating whether `TextProxy` instance covers only part of the original {@link module:engine/model/text~Text text node}
  * (`true`) or the whole text node (`false`).
  *
  * This is `false` when text proxy starts at the very beginning of {@link module:engine/model/textproxy~TextProxy#textNode textNode}
  * ({@link module:engine/model/textproxy~TextProxy#offsetInText offsetInText} equals `0`) and text proxy sizes is equal to
  * text node size.
  */
  get isPartial() {
    return this.offsetSize !== this.textNode.offsetSize;
  }
  /**
  * Parent of this text proxy, which is same as parent of text node represented by this text proxy.
  */
  get parent() {
    return this.textNode.parent;
  }
  /**
  * Root of this text proxy, which is same as root of text node represented by this text proxy.
  */
  get root() {
    return this.textNode.root;
  }
  /**
  * Gets path to this text proxy.
  *
  * @see module:engine/model/node~Node#getPath
  */
  getPath() {
    const e = this.textNode.getPath();
    return e.length > 0 && (e[e.length - 1] += this.offsetInText), e;
  }
  /**
  * Returns ancestors array of this text proxy.
  *
  * @param options Options object.
  * @param options.includeSelf When set to `true` this text proxy will be also included in parent's array.
  * @param options.parentFirst When set to `true`, array will be sorted from text proxy parent to root element,
  * otherwise root element will be the first item in the array.
  * @returns Array with ancestors.
  */
  getAncestors(e = {}) {
    const n = [];
    let r = e.includeSelf ? this : this.parent;
    for (; r; )
      n[e.parentFirst ? "push" : "unshift"](r), r = r.parent;
    return n;
  }
  /**
  * Checks if this text proxy has an attribute for given key.
  *
  * @param key Key of attribute to check.
  * @returns `true` if attribute with given key is set on text proxy, `false` otherwise.
  */
  hasAttribute(e) {
    return this.textNode.hasAttribute(e);
  }
  /**
  * Gets an attribute value for given key or `undefined` if that attribute is not set on text proxy.
  *
  * @param key Key of attribute to look for.
  * @returns Attribute value or `undefined`.
  */
  getAttribute(e) {
    return this.textNode.getAttribute(e);
  }
  /**
  * Returns iterator that iterates over this node's attributes. Attributes are returned as arrays containing two
  * items. First one is attribute key and second is attribute value.
  *
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  getAttributes() {
    return this.textNode.getAttributes();
  }
  /**
  * Returns iterator that iterates over this node's attribute keys.
  */
  getAttributeKeys() {
    return this.textNode.getAttributeKeys();
  }
}
Gi.prototype.is = function(i) {
  return i === "$textProxy" || i === "model:$textProxy" || // This are legacy values kept for backward compatibility.
  i === "textProxy" || i === "model:textProxy";
};
let xt = class vu extends no {
  /**
  * Creates a model element.
  *
  * **Note:** Constructor of this class shouldn't be used directly in the code.
  * Use the {@link module:engine/model/writer~Writer#createElement} method instead.
  *
  * @internal
  * @param name Element's name.
  * @param attrs Element's attributes. See {@link module:utils/tomap~toMap} for a list of accepted values.
  * @param children One or more nodes to be inserted as children of created element.
  */
  constructor(e, n, r) {
    super(n);
    /**
    * Element name.
    */
    h(this, "name");
    /**
    * List of children nodes.
    */
    h(this, "_children", new cl());
    this.name = e, r && this._insertChild(0, r);
  }
  /**
  * Number of this element's children.
  */
  get childCount() {
    return this._children.length;
  }
  /**
  * Sum of {@link module:engine/model/node~Node#offsetSize offset sizes} of all of this element's children.
  */
  get maxOffset() {
    return this._children.maxOffset;
  }
  /**
  * Is `true` if there are no nodes inside this element, `false` otherwise.
  */
  get isEmpty() {
    return this.childCount === 0;
  }
  /**
  * Gets the child at the given index. Returns `null` if incorrect index was passed.
  *
  * @param index Index in this element.
  * @returns Child node.
  */
  getChild(e) {
    return this._children.getNode(e);
  }
  /**
  * Gets the child at the given offset. Returns `null` if incorrect index was passed.
  *
  * @param offset Offset in this element.
  * @returns Child node.
  */
  getChildAtOffset(e) {
    return this._children.getNodeAtOffset(e);
  }
  /**
  * Returns an iterator that iterates over all of this element's children.
  */
  getChildren() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Returns an index of the given child node. Returns `null` if given node is not a child of this element.
  *
  * @param node Child node to look for.
  * @returns Child node's index in this element.
  */
  getChildIndex(e) {
    return this._children.getNodeIndex(e);
  }
  /**
  * Returns the starting offset of given child. Starting offset is equal to the sum of
  * {@link module:engine/model/node~Node#offsetSize offset sizes} of all node's siblings that are before it. Returns `null` if
  * given node is not a child of this element.
  *
  * @param node Child node to look for.
  * @returns Child node's starting offset.
  */
  getChildStartOffset(e) {
    return this._children.getNodeStartOffset(e);
  }
  /**
  * Returns index of a node that occupies given offset. If given offset is too low, returns `0`. If given offset is
  * too high, returns {@link module:engine/model/element~Element#getChildIndex index after last child}.
  *
  * ```ts
  * const textNode = new Text( 'foo' );
  * const pElement = new Element( 'p' );
  * const divElement = new Element( [ textNode, pElement ] );
  * divElement.offsetToIndex( -1 ); // Returns 0, because offset is too low.
  * divElement.offsetToIndex( 0 ); // Returns 0, because offset 0 is taken by `textNode` which is at index 0.
  * divElement.offsetToIndex( 1 ); // Returns 0, because `textNode` has `offsetSize` equal to 3, so it occupies offset 1 too.
  * divElement.offsetToIndex( 2 ); // Returns 0.
  * divElement.offsetToIndex( 3 ); // Returns 1.
  * divElement.offsetToIndex( 4 ); // Returns 2. There are no nodes at offset 4, so last available index is returned.
  * ```
  */
  offsetToIndex(e) {
    return this._children.offsetToIndex(e);
  }
  /**
  * Returns a descendant node by its path relative to this element.
  *
  * ```ts
  * // <this>a<b>c</b></this>
  * this.getNodeByPath( [ 0 ] );     // -> "a"
  * this.getNodeByPath( [ 1 ] );     // -> <b>
  * this.getNodeByPath( [ 1, 0 ] );  // -> "c"
  * ```
  *
  * @param relativePath Path of the node to find, relative to this element.
  */
  getNodeByPath(e) {
    let n = this;
    for (const r of e)
      n = n.getChildAtOffset(r);
    return n;
  }
  /**
  * Returns the parent element of the given name. Returns null if the element is not inside the desired parent.
  *
  * @param parentName The name of the parent element to find.
  * @param options Options object.
  * @param options.includeSelf When set to `true` this node will be also included while searching.
  */
  findAncestor(e, n = {}) {
    let r = n.includeSelf ? this : this.parent;
    for (; r; ) {
      if (r.name === e)
        return r;
      r = r.parent;
    }
    return null;
  }
  /**
  * Converts `Element` instance to plain object and returns it. Takes care of converting all of this element's children.
  *
  * @returns `Element` instance converted to plain object.
  */
  toJSON() {
    const e = super.toJSON();
    if (e.name = this.name, this._children.length > 0) {
      e.children = [];
      for (const n of this._children)
        e.children.push(n.toJSON());
    }
    return e;
  }
  /**
  * Creates a copy of this element and returns it. Created element has the same name and attributes as the original element.
  * If clone is deep, the original element's children are also cloned. If not, then empty element is returned.
  *
  * @internal
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any child.
  */
  _clone(e = !1) {
    const n = e ? Array.from(this._children).map((r) => r._clone(!0)) : void 0;
    return new vu(this.name, this.getAttributes(), n);
  }
  /**
  * {@link module:engine/model/element~Element#_insertChild Inserts} one or more nodes at the end of this element.
  *
  * @see module:engine/model/writer~Writer#append
  * @internal
  * @param nodes Nodes to be inserted.
  */
  _appendChild(e) {
    this._insertChild(this.childCount, e);
  }
  /**
  * Inserts one or more nodes at the given index and sets {@link module:engine/model/node~Node#parent parent} of these nodes
  * to this element.
  *
  * @see module:engine/model/writer~Writer#insert
  * @internal
  * @param index Index at which nodes should be inserted.
  * @param items Items to be inserted.
  */
  _insertChild(e, n) {
    const r = X6(n);
    for (const s of r)
      s.parent !== null && s._remove(), s.parent = this;
    this._children._insertNodes(e, r);
  }
  /**
  * Removes one or more nodes starting at the given index and sets
  * {@link module:engine/model/node~Node#parent parent} of these nodes to `null`.
  *
  * @see module:engine/model/writer~Writer#remove
  * @internal
  * @param index Index of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @returns Array containing removed nodes.
  */
  _removeChildren(e, n = 1) {
    const r = this._children._removeNodes(e, n);
    for (const s of r)
      s.parent = null;
    return r;
  }
  /**
  * Creates an `Element` instance from given plain object (i.e. parsed JSON string).
  * Converts `Element` children to proper nodes.
  *
  * @param json Plain object to be converted to `Element`.
  * @returns `Element` instance created using given plain object.
  */
  static fromJSON(e) {
    let n;
    if (e.children) {
      n = [];
      for (const r of e.children)
        r.name ? n.push(vu.fromJSON(r)) : n.push(mt.fromJSON(r));
    }
    return new vu(e.name, e.attributes, n);
  }
};
xt.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "element" || i === "model:element") : i === "element" || i === "model:element" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "node" || i === "model:node";
};
function X6(i) {
  return typeof i == "string" ? [
    new mt(i)
  ] : (ki(i) || (i = [
    i
  ]), Array.from(i).map((t) => typeof t == "string" ? new mt(t) : t instanceof Gi ? new mt(t.data, t.getAttributes()) : t));
}
class Vr {
  /**
  * Creates a range iterator. All parameters are optional, but you have to specify either `boundaries` or `startPosition`.
  *
  * @param options Object with configuration.
  */
  constructor(t) {
    /**
    * Walking direction. Defaults `'forward'`.
    */
    h(this, "direction");
    /**
    * Iterator boundaries.
    *
    * When the iterator is walking `'forward'` on the end of boundary or is walking `'backward'`
    * on the start of boundary, then `{ done: true }` is returned.
    *
    * If boundaries are not defined they are set before first and after last child of the root node.
    */
    h(this, "boundaries");
    /**
    * Flag indicating whether all consecutive characters with the same attributes should be
    * returned as one {@link module:engine/model/textproxy~TextProxy} (`true`) or one by one (`false`).
    */
    h(this, "singleCharacters");
    /**
    * Flag indicating whether iterator should enter elements or not. If the iterator is shallow child nodes of any
    * iterated node will not be returned along with `elementEnd` tag.
    */
    h(this, "shallow");
    /**
    * Flag indicating whether iterator should ignore `elementEnd` tags. If the option is true walker will not
    * return a parent node of the start position. If this option is `true` each {@link module:engine/model/element~Element} will
    * be returned once, while if the option is `false` they might be returned twice:
    * for `'elementStart'` and `'elementEnd'`.
    */
    h(this, "ignoreElementEnd");
    /**
    * Iterator position. This is always static position, even if the initial position was a
    * {@link module:engine/model/liveposition~LivePosition live position}. If start position is not defined then position depends
    * on {@link #direction}. If direction is `'forward'` position starts form the beginning, when direction
    * is `'backward'` position starts from the end.
    */
    h(this, "_position");
    /**
    * Start boundary cached for optimization purposes.
    */
    h(this, "_boundaryStartParent");
    /**
    * End boundary cached for optimization purposes.
    */
    h(this, "_boundaryEndParent");
    /**
    * Parent of the most recently visited node. Cached for optimization purposes.
    */
    h(this, "_visitedParent");
    if (!t || !t.boundaries && !t.startPosition)
      throw new T("model-tree-walker-no-start-position", null);
    const e = t.direction || "forward";
    if (e != "forward" && e != "backward")
      throw new T("model-tree-walker-unknown-direction", t, {
        direction: e
      });
    this.direction = e, this.boundaries = t.boundaries || null, t.startPosition ? this._position = t.startPosition.clone() : this._position = G._createAt(this.boundaries[this.direction == "backward" ? "end" : "start"]), this.position.stickiness = "toNone", this.singleCharacters = !!t.singleCharacters, this.shallow = !!t.shallow, this.ignoreElementEnd = !!t.ignoreElementEnd, this._boundaryStartParent = this.boundaries ? this.boundaries.start.parent : null, this._boundaryEndParent = this.boundaries ? this.boundaries.end.parent : null, this._visitedParent = this.position.parent;
  }
  /**
  * Iterable interface.
  *
  * @returns {Iterable.<module:engine/model/treewalker~TreeWalkerValue>}
  */
  [Symbol.iterator]() {
    return this;
  }
  /**
  * Iterator position. This is always static position, even if the initial position was a
  * {@link module:engine/model/liveposition~LivePosition live position}. If start position is not defined then position depends
  * on {@link #direction}. If direction is `'forward'` position starts form the beginning, when direction
  * is `'backward'` position starts from the end.
  */
  get position() {
    return this._position;
  }
  /**
  * Moves {@link #position} in the {@link #direction} skipping values as long as the callback function returns `true`.
  *
  * For example:
  *
  * ```ts
  * walker.skip( value => value.type == 'text' ); // <paragraph>[]foo</paragraph> -> <paragraph>foo[]</paragraph>
  * walker.skip( () => true ); // Move the position to the end: <paragraph>[]foo</paragraph> -> <paragraph>foo</paragraph>[]
  * walker.skip( () => false ); // Do not move the position.
  * ```
  *
  * @param skip Callback function. Gets {@link module:engine/model/treewalker~TreeWalkerValue} and should
  * return `true` if the value should be skipped or `false` if not.
  */
  skip(t) {
    let e, n, r, s;
    do
      r = this.position, s = this._visitedParent, { done: e, value: n } = this.next();
    while (!e && t(n));
    e || (this._position = r, this._visitedParent = s);
  }
  /**
  * Gets the next tree walker's value.
  */
  next() {
    return this.direction == "forward" ? this._next() : this._previous();
  }
  /**
  * Makes a step forward in model. Moves the {@link #position} to the next position and returns the encountered value.
  */
  _next() {
    const t = this.position, e = this.position.clone(), n = this._visitedParent;
    if (n.parent === null && e.offset === n.maxOffset)
      return {
        done: !0,
        value: void 0
      };
    if (n === this._boundaryEndParent && e.offset == this.boundaries.end.offset)
      return {
        done: !0,
        value: void 0
      };
    const r = ul(e, n), s = r || U2(e, n, r);
    if (s instanceof xt) {
      if (!this.shallow)
        e.path.push(0), this._visitedParent = s;
      else {
        if (this.boundaries && this.boundaries.end.isBefore(e))
          return {
            done: !0,
            value: void 0
          };
        e.offset++;
      }
      return this._position = e, Ss("elementStart", s, t, e, 1);
    }
    if (s instanceof mt) {
      let o;
      if (this.singleCharacters)
        o = 1;
      else {
        let c = s.endOffset;
        this._boundaryEndParent == n && this.boundaries.end.offset < c && (c = this.boundaries.end.offset), o = c - e.offset;
      }
      const a = e.offset - s.startOffset, l = new Gi(s, a, o);
      return e.offset += o, this._position = e, Ss("text", l, t, e, o);
    }
    return e.path.pop(), e.offset++, this._position = e, this._visitedParent = n.parent, this.ignoreElementEnd ? this._next() : Ss("elementEnd", n, t, e);
  }
  /**
  * Makes a step backward in model. Moves the {@link #position} to the previous position and returns the encountered value.
  */
  _previous() {
    const t = this.position, e = this.position.clone(), n = this._visitedParent;
    if (n.parent === null && e.offset === 0)
      return {
        done: !0,
        value: void 0
      };
    if (n == this._boundaryStartParent && e.offset == this.boundaries.start.offset)
      return {
        done: !0,
        value: void 0
      };
    const r = e.parent, s = ul(e, r), o = s || q2(e, r, s);
    if (o instanceof xt)
      return e.offset--, this.shallow ? (this._position = e, Ss("elementStart", o, t, e, 1)) : (e.path.push(o.maxOffset), this._position = e, this._visitedParent = o, this.ignoreElementEnd ? this._previous() : Ss("elementEnd", o, t, e));
    if (o instanceof mt) {
      let a;
      if (this.singleCharacters)
        a = 1;
      else {
        let u = o.startOffset;
        this._boundaryStartParent == n && this.boundaries.start.offset > u && (u = this.boundaries.start.offset), a = e.offset - u;
      }
      const l = e.offset - o.startOffset, c = new Gi(o, l - a, a);
      return e.offset -= a, this._position = e, Ss("text", c, t, e, a);
    }
    return e.path.pop(), this._position = e, this._visitedParent = n.parent, Ss("elementStart", n, t, e, 1);
  }
}
function Ss(i, t, e, n, r) {
  return {
    done: !1,
    value: {
      type: i,
      item: t,
      previousPosition: e,
      nextPosition: n,
      length: r
    }
  };
}
class G extends ys {
  /**
  * Creates a position.
  *
  * @param root Root of the position.
  * @param path Position path. See {@link module:engine/model/position~Position#path}.
  * @param stickiness Position stickiness. See {@link module:engine/model/position~PositionStickiness}.
  */
  constructor(e, n, r = "toNone") {
    super();
    /**
    * Root of the position path.
    */
    h(this, "root");
    /**
    * Position of the node in the tree. **Path contains offsets, not indexes.**
    *
    * Position can be placed before, after or in a {@link module:engine/model/node~Node node} if that node has
    * {@link module:engine/model/node~Node#offsetSize} greater than `1`. Items in position path are
    * {@link module:engine/model/node~Node#startOffset starting offsets} of position ancestors, starting from direct root children,
    * down to the position offset in it's parent.
    *
    * ```
    * ROOT
    *  |- P            before: [ 0 ]         after: [ 1 ]
    *  |- UL           before: [ 1 ]         after: [ 2 ]
    *     |- LI        before: [ 1, 0 ]      after: [ 1, 1 ]
    *     |  |- foo    before: [ 1, 0, 0 ]   after: [ 1, 0, 3 ]
    *     |- LI        before: [ 1, 1 ]      after: [ 1, 2 ]
    *        |- bar    before: [ 1, 1, 0 ]   after: [ 1, 1, 3 ]
    * ```
    *
    * `foo` and `bar` are representing {@link module:engine/model/text~Text text nodes}. Since text nodes has offset size
    * greater than `1` you can place position offset between their start and end:
    *
    * ```
    * ROOT
    *  |- P
    *  |- UL
    *     |- LI
    *     |  |- f^o|o  ^ has path: [ 1, 0, 1 ]   | has path: [ 1, 0, 2 ]
    *     |- LI
    *        |- b^a|r  ^ has path: [ 1, 1, 1 ]   | has path: [ 1, 1, 2 ]
    * ```
    */
    h(this, "path");
    /**
    * Position stickiness. See {@link module:engine/model/position~PositionStickiness}.
    */
    h(this, "stickiness");
    if (!e.is("element") && !e.is("documentFragment"))
      throw new T("model-position-root-invalid", e);
    if (!(n instanceof Array) || n.length === 0)
      throw new T("model-position-path-incorrect-format", e, {
        path: n
      });
    e.is("rootElement") ? n = n.slice() : (n = [
      ...e.getPath(),
      ...n
    ], e = e.root), this.root = e, this.path = n, this.stickiness = r;
  }
  /**
  * Offset at which this position is located in its {@link module:engine/model/position~Position#parent parent}. It is equal
  * to the last item in position {@link module:engine/model/position~Position#path path}.
  *
  * @type {Number}
  */
  get offset() {
    return this.path[this.path.length - 1];
  }
  set offset(e) {
    this.path[this.path.length - 1] = e;
  }
  /**
  * Parent element of this position.
  *
  * Keep in mind that `parent` value is calculated when the property is accessed.
  * If {@link module:engine/model/position~Position#path position path}
  * leads to a non-existing element, `parent` property will throw error.
  *
  * Also it is a good idea to cache `parent` property if it is used frequently in an algorithm (i.e. in a long loop).
  */
  get parent() {
    let e = this.root;
    for (let n = 0; n < this.path.length - 1; n++)
      if (e = e.getChildAtOffset(this.path[n]), !e)
        throw new T("model-position-path-incorrect", this, {
          position: this
        });
    if (e.is("$text"))
      throw new T("model-position-path-incorrect", this, {
        position: this
      });
    return e;
  }
  /**
  * Position {@link module:engine/model/position~Position#offset offset} converted to an index in position's parent node. It is
  * equal to the {@link module:engine/model/node~Node#index index} of a node after this position. If position is placed
  * in text node, position index is equal to the index of that text node.
  */
  get index() {
    return this.parent.offsetToIndex(this.offset);
  }
  /**
  * Returns {@link module:engine/model/text~Text text node} instance in which this position is placed or `null` if this
  * position is not in a text node.
  */
  get textNode() {
    return ul(this, this.parent);
  }
  /**
  * Node directly after this position. Returns `null` if this position is at the end of its parent, or if it is in a text node.
  */
  get nodeAfter() {
    const e = this.parent;
    return U2(this, e, ul(this, e));
  }
  /**
  * Node directly before this position. Returns `null` if this position is at the start of its parent, or if it is in a text node.
  */
  get nodeBefore() {
    const e = this.parent;
    return q2(this, e, ul(this, e));
  }
  /**
  * Is `true` if position is at the beginning of its {@link module:engine/model/position~Position#parent parent}, `false` otherwise.
  */
  get isAtStart() {
    return this.offset === 0;
  }
  /**
  * Is `true` if position is at the end of its {@link module:engine/model/position~Position#parent parent}, `false` otherwise.
  */
  get isAtEnd() {
    return this.offset == this.parent.maxOffset;
  }
  /**
  * Checks whether the position is valid in current model tree, that is whether it points to an existing place in the model.
  */
  isValid() {
    if (this.offset < 0)
      return !1;
    let e = this.root;
    for (let n = 0; n < this.path.length - 1; n++)
      if (e = e.getChildAtOffset(this.path[n]), !e)
        return !1;
    return this.offset <= e.maxOffset;
  }
  /**
  * Checks whether this position is before or after given position.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  */
  compareWith(e) {
    if (this.root != e.root)
      return "different";
    const n = Fn(this.path, e.path);
    switch (n) {
      case "same":
        return "same";
      case "prefix":
        return "before";
      case "extension":
        return "after";
      default:
        return this.path[n] < e.path[n] ? "before" : "after";
    }
  }
  /**
  * Gets the farthest position which matches the callback using
  * {@link module:engine/model/treewalker~TreeWalker TreeWalker}.
  *
  * For example:
  *
  * ```ts
  * getLastMatchingPosition( value => value.type == 'text' );
  * // <paragraph>[]foo</paragraph> -> <paragraph>foo[]</paragraph>
  *
  * getLastMatchingPosition( value => value.type == 'text', { direction: 'backward' } );
  * // <paragraph>foo[]</paragraph> -> <paragraph>[]foo</paragraph>
  *
  * getLastMatchingPosition( value => false );
  * // Do not move the position.
  * ```
  *
  * @param skip Callback function. Gets {@link module:engine/model/treewalker~TreeWalkerValue} and should
  * return `true` if the value should be skipped or `false` if not.
  * @param options Object with configuration options. See {@link module:engine/model/treewalker~TreeWalker}.
  *
  * @returns The position after the last item which matches the `skip` callback test.
  */
  getLastMatchingPosition(e, n = {}) {
    n.startPosition = this;
    const r = new Vr(n);
    return r.skip(e), r.position;
  }
  /**
  * Returns a path to this position's parent. Parent path is equal to position {@link module:engine/model/position~Position#path path}
  * but without the last item.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @returns Path to the parent.
  */
  getParentPath() {
    return this.path.slice(0, -1);
  }
  /**
  * Returns ancestors array of this position, that is this position's parent and its ancestors.
  *
  * @returns Array with ancestors.
  */
  getAncestors() {
    const e = this.parent;
    return e.is("documentFragment") ? [
      e
    ] : e.getAncestors({
      includeSelf: !0
    });
  }
  /**
  * Returns the parent element of the given name. Returns null if the position is not inside the desired parent.
  *
  * @param parentName The name of the parent element to find.
  */
  findAncestor(e) {
    const n = this.parent;
    return n.is("element") ? n.findAncestor(e, {
      includeSelf: !0
    }) : null;
  }
  /**
  * Returns the slice of two position {@link #path paths} which is identical. The {@link #root roots}
  * of these two paths must be identical.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param position The second position.
  * @returns The common path.
  */
  getCommonPath(e) {
    if (this.root != e.root)
      return [];
    const n = Fn(this.path, e.path), r = typeof n == "string" ? Math.min(this.path.length, e.path.length) : n;
    return this.path.slice(0, r);
  }
  /**
  * Returns an {@link module:engine/model/element~Element} or {@link module:engine/model/documentfragment~DocumentFragment}
  * which is a common ancestor of both positions. The {@link #root roots} of these two positions must be identical.
  *
  * @param position The second position.
  */
  getCommonAncestor(e) {
    const n = this.getAncestors(), r = e.getAncestors();
    let s = 0;
    for (; n[s] == r[s] && n[s]; )
      s++;
    return s === 0 ? null : n[s - 1];
  }
  /**
  * Returns a new instance of `Position`, that has same {@link #parent parent} but it's offset
  * is shifted by `shift` value (can be a negative value).
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param shift Offset shift. Can be a negative value.
  * @returns Shifted position.
  */
  getShiftedBy(e) {
    const n = this.clone(), r = n.offset + e;
    return n.offset = r < 0 ? 0 : r, n;
  }
  /**
  * Checks whether this position is after given position.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @see module:engine/model/position~Position#isBefore
  * @param  otherPosition Position to compare with.
  * @returns True if this position is after given position.
  */
  isAfter(e) {
    return this.compareWith(e) == "after";
  }
  /**
  * Checks whether this position is before given position.
  *
  * **Note:** watch out when using negation of the value returned by this method, because the negation will also
  * be `true` if positions are in different roots and you might not expect this. You should probably use
  * `a.isAfter( b ) || a.isEqual( b )` or `!a.isBefore( p ) && a.root == b.root` in most scenarios. If your
  * condition uses multiple `isAfter` and `isBefore` checks, build them so they do not use negated values, i.e.:
  *
  * ```ts
  * if ( a.isBefore( b ) && c.isAfter( d ) ) {
  * 	// do A.
  * } else {
  * 	// do B.
  * }
  * ```
  *
  * or, if you have only one if-branch:
  *
  * ```ts
  * if ( !( a.isBefore( b ) && c.isAfter( d ) ) {
  * 	// do B.
  * }
  * ```
  *
  * rather than:
  *
  * ```ts
  * if ( !a.isBefore( b ) || && !c.isAfter( d ) ) {
  * 	// do B.
  * } else {
  * 	// do A.
  * }
  * ```
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param otherPosition Position to compare with.
  * @returns True if this position is before given position.
  */
  isBefore(e) {
    return this.compareWith(e) == "before";
  }
  /**
  * Checks whether this position is equal to given position.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param otherPosition Position to compare with.
  * @returns True if positions are same.
  */
  isEqual(e) {
    return this.compareWith(e) == "same";
  }
  /**
  * Checks whether this position is touching given position. Positions touch when there are no text nodes
  * or empty nodes in a range between them. Technically, those positions are not equal but in many cases
  * they are very similar or even indistinguishable.
  *
  * @param otherPosition Position to compare with.
  * @returns True if positions touch.
  */
  isTouching(e) {
    if (this.root !== e.root)
      return !1;
    const n = Math.min(this.path.length, e.path.length);
    for (let r = 0; r < n; r++) {
      const s = this.path[r] - e.path[r];
      if (s < -1 || s > 1)
        return !1;
      if (s === 1)
        return F1(e, this, r);
      if (s === -1)
        return F1(this, e, r);
    }
    return this.path.length === e.path.length ? !0 : this.path.length > e.path.length ? am(this.path, n) : am(e.path, n);
  }
  /**
  * Checks if two positions are in the same parent.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param position Position to compare with.
  * @returns `true` if positions have the same parent, `false` otherwise.
  */
  hasSameParentAs(e) {
    if (this.root !== e.root)
      return !1;
    const n = this.getParentPath(), r = e.getParentPath();
    return Fn(n, r) == "same";
  }
  /**
  * Returns a copy of this position that is transformed by given `operation`.
  *
  * The new position's parameters are updated accordingly to the effect of the `operation`.
  *
  * For example, if `n` nodes are inserted before the position, the returned position {@link ~Position#offset} will be
  * increased by `n`. If the position was in a merged element, it will be accordingly moved to the new element, etc.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param operation Operation to transform by.
  * @returns Transformed position.
  */
  getTransformedByOperation(e) {
    let n;
    switch (e.type) {
      case "insert":
        n = this._getTransformedByInsertOperation(e);
        break;
      case "move":
      case "remove":
      case "reinsert":
        n = this._getTransformedByMoveOperation(e);
        break;
      case "split":
        n = this._getTransformedBySplitOperation(e);
        break;
      case "merge":
        n = this._getTransformedByMergeOperation(e);
        break;
      default:
        n = G._createAt(this);
        break;
    }
    return n;
  }
  /**
  * Returns a copy of this position transformed by an insert operation.
  *
  * @internal
  */
  _getTransformedByInsertOperation(e) {
    return this._getTransformedByInsertion(e.position, e.howMany);
  }
  /**
  * Returns a copy of this position transformed by a move operation.
  *
  * @internal
  */
  _getTransformedByMoveOperation(e) {
    return this._getTransformedByMove(e.sourcePosition, e.targetPosition, e.howMany);
  }
  /**
  * Returns a copy of this position transformed by a split operation.
  *
  * @internal
  */
  _getTransformedBySplitOperation(e) {
    const n = e.movedRange;
    return n.containsPosition(this) || n.start.isEqual(this) && this.stickiness == "toNext" ? this._getCombined(e.splitPosition, e.moveTargetPosition) : e.graveyardPosition ? this._getTransformedByMove(e.graveyardPosition, e.insertionPosition, 1) : this._getTransformedByInsertion(e.insertionPosition, 1);
  }
  /**
  * Returns a copy of this position transformed by merge operation.
  *
  * @internal
  */
  _getTransformedByMergeOperation(e) {
    const n = e.movedRange, r = n.containsPosition(this) || n.start.isEqual(this);
    let s;
    return r ? (s = this._getCombined(e.sourcePosition, e.targetPosition), e.sourcePosition.isBefore(e.targetPosition) && (s = s._getTransformedByDeletion(e.deletionPosition, 1))) : this.isEqual(e.deletionPosition) ? s = G._createAt(e.deletionPosition) : s = this._getTransformedByMove(e.deletionPosition, e.graveyardPosition, 1), s;
  }
  /**
  * Returns a copy of this position that is updated by removing `howMany` nodes starting from `deletePosition`.
  * It may happen that this position is in a removed node. If that is the case, `null` is returned instead.
  *
  * @internal
  * @param deletePosition Position before the first removed node.
  * @param howMany How many nodes are removed.
  * @returns Transformed position or `null`.
  */
  _getTransformedByDeletion(e, n) {
    const r = G._createAt(this);
    if (this.root != e.root)
      return r;
    if (Fn(e.getParentPath(), this.getParentPath()) == "same") {
      if (e.offset < this.offset) {
        if (e.offset + n > this.offset)
          return null;
        r.offset -= n;
      }
    } else if (Fn(e.getParentPath(), this.getParentPath()) == "prefix") {
      const s = e.path.length - 1;
      if (e.offset <= this.path[s]) {
        if (e.offset + n > this.path[s])
          return null;
        r.path[s] -= n;
      }
    }
    return r;
  }
  /**
  * Returns a copy of this position that is updated by inserting `howMany` nodes at `insertPosition`.
  *
  * @internal
  * @param insertPosition Position where nodes are inserted.
  * @param howMany How many nodes are inserted.
  * @returns Transformed position.
  */
  _getTransformedByInsertion(e, n) {
    const r = G._createAt(this);
    if (this.root != e.root)
      return r;
    if (Fn(e.getParentPath(), this.getParentPath()) == "same")
      (e.offset < this.offset || e.offset == this.offset && this.stickiness != "toPrevious") && (r.offset += n);
    else if (Fn(e.getParentPath(), this.getParentPath()) == "prefix") {
      const s = e.path.length - 1;
      e.offset <= this.path[s] && (r.path[s] += n);
    }
    return r;
  }
  /**
  * Returns a copy of this position that is updated by moving `howMany` nodes from `sourcePosition` to `targetPosition`.
  *
  * @internal
  * @param sourcePosition Position before the first element to move.
  * @param targetPosition Position where moved elements will be inserted.
  * @param howMany How many consecutive nodes to move, starting from `sourcePosition`.
  * @returns Transformed position.
  */
  _getTransformedByMove(e, n, r) {
    if (n = n._getTransformedByDeletion(e, r), e.isEqual(n))
      return G._createAt(this);
    const s = this._getTransformedByDeletion(e, r);
    return s === null || e.isEqual(this) && this.stickiness == "toNext" || e.getShiftedBy(r).isEqual(this) && this.stickiness == "toPrevious" ? this._getCombined(e, n) : s._getTransformedByInsertion(n, r);
  }
  /**
  * Returns a new position that is a combination of this position and given positions.
  *
  * The combined position is a copy of this position transformed by moving a range starting at `source` position
  * to the `target` position. It is expected that this position is inside the moved range.
  *
  * Example:
  *
  * ```ts
  * let original = model.createPositionFromPath( root, [ 2, 3, 1 ] );
  * let source = model.createPositionFromPath( root, [ 2, 2 ] );
  * let target = model.createPositionFromPath( otherRoot, [ 1, 1, 3 ] );
  * original._getCombined( source, target ); // path is [ 1, 1, 4, 1 ], root is `otherRoot`
  * ```
  *
  * Explanation:
  *
  * We have a position `[ 2, 3, 1 ]` and move some nodes from `[ 2, 2 ]` to `[ 1, 1, 3 ]`. The original position
  * was inside moved nodes and now should point to the new place. The moved nodes will be after
  * positions `[ 1, 1, 3 ]`, `[ 1, 1, 4 ]`, `[ 1, 1, 5 ]`. Since our position was in the second moved node,
  * the transformed position will be in a sub-tree of a node at `[ 1, 1, 4 ]`. Looking at original path, we
  * took care of `[ 2, 3 ]` part of it. Now we have to add the rest of the original path to the transformed path.
  * Finally, the transformed position will point to `[ 1, 1, 4, 1 ]`.
  *
  * @internal
  * @param source Beginning of the moved range.
  * @param target Position where the range is moved.
  * @returns Combined position.
  */
  _getCombined(e, n) {
    const r = e.path.length - 1, s = G._createAt(n);
    return s.stickiness = this.stickiness, s.offset = s.offset + this.path[r] - e.offset, s.path = [
      ...s.path,
      ...this.path.slice(r + 1)
    ], s;
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    return {
      root: this.root.toJSON(),
      path: Array.from(this.path),
      stickiness: this.stickiness
    };
  }
  /**
  * Returns a new position that is equal to current position.
  */
  clone() {
    return new this.constructor(this.root, this.path, this.stickiness);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/model/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/model/item~Item model item} and `'before'` or `'after'` (sets position before or after given model item).
  *
  * This method is a shortcut to other factory methods such as:
  *
  * * {@link module:engine/model/position~Position._createBefore},
  * * {@link module:engine/model/position~Position._createAfter}.
  *
  * @internal
  * @param offset Offset or one of the flags. Used only when the first parameter is a {@link module:engine/model/item~Item model item}.
  * @param stickiness Position stickiness. Used only when the first parameter is a {@link module:engine/model/item~Item model item}.
  */
  static _createAt(e, n, r = "toNone") {
    if (e instanceof G)
      return new G(e.root, e.path, e.stickiness);
    {
      const s = e;
      if (n == "end")
        n = s.maxOffset;
      else {
        if (n == "before")
          return this._createBefore(s, r);
        if (n == "after")
          return this._createAfter(s, r);
        if (n !== 0 && !n)
          throw new T("model-createpositionat-offset-required", [
            this,
            e
          ]);
      }
      if (!s.is("element") && !s.is("documentFragment"))
        throw new T("model-position-parent-incorrect", [
          this,
          e
        ]);
      const o = s.getPath();
      return o.push(n), new this(s.root, o, r);
    }
  }
  /**
  * Creates a new position, after given {@link module:engine/model/item~Item model item}.
  *
  * @internal
  * @param item Item after which the position should be placed.
  * @param stickiness Position stickiness.
  */
  static _createAfter(e, n) {
    if (!e.parent)
      throw new T("model-position-after-root", [
        this,
        e
      ], {
        root: e
      });
    return this._createAt(e.parent, e.endOffset, n);
  }
  /**
  * Creates a new position, before the given {@link module:engine/model/item~Item model item}.
  *
  * @internal
  * @param item Item before which the position should be placed.
  * @param stickiness Position stickiness.
  */
  static _createBefore(e, n) {
    if (!e.parent)
      throw new T("model-position-before-root", e, {
        root: e
      });
    return this._createAt(e.parent, e.startOffset, n);
  }
  /**
  * Creates a `Position` instance from given plain object (i.e. parsed JSON string).
  *
  * @param json Plain object to be converted to `Position`.
  * @param doc Document object that will be position owner.
  * @returns `Position` instance created using given plain object.
  */
  static fromJSON(e, n) {
    if (e.root === "$graveyard") {
      const r = new G(n.graveyard, e.path);
      return r.stickiness = e.stickiness, r;
    }
    if (!n.getRoot(e.root))
      throw new T("model-position-fromjson-no-root", n, {
        rootName: e.root
      });
    return new G(n.getRoot(e.root), e.path, e.stickiness);
  }
}
G.prototype.is = function(i) {
  return i === "position" || i === "model:position";
};
function ul(i, t) {
  const e = t.getChildAtOffset(i.offset);
  return e && e.is("$text") && e.startOffset < i.offset ? e : null;
}
function U2(i, t, e) {
  return e !== null ? null : t.getChildAtOffset(i.offset);
}
function q2(i, t, e) {
  return e !== null ? null : t.getChild(t.offsetToIndex(i.offset) - 1);
}
function F1(i, t, e) {
  return !(e + 1 === i.path.length || !am(t.path, e + 1) || !Q6(i, e + 1));
}
function am(i, t) {
  for (; t < i.length; ) {
    if (i[t] !== 0)
      return !1;
    t++;
  }
  return !0;
}
function Q6(i, t) {
  let e = i.parent, n = i.path.length - 1, r = 0;
  for (; n >= t; ) {
    if (i.path[n] + r !== e.maxOffset)
      return !1;
    r = 1, n--, e = e.parent;
  }
  return !0;
}
class D extends ys {
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * @param start The start position.
  * @param end The end position. If not set, the range will be collapsed at the `start` position.
  */
  constructor(e, n) {
    super();
    /**
    * Start position.
    */
    h(this, "start");
    /**
    * End position.
    */
    h(this, "end");
    this.start = G._createAt(e), this.end = n ? G._createAt(n) : G._createAt(e), this.start.stickiness = this.isCollapsed ? "toNone" : "toNext", this.end.stickiness = this.isCollapsed ? "toNone" : "toPrevious";
  }
  /**
  * Iterable interface.
  *
  * Iterates over all {@link module:engine/model/item~Item items} that are in this range and returns
  * them together with additional information like length or {@link module:engine/model/position~Position positions},
  * grouped as {@link module:engine/model/treewalker~TreeWalkerValue}.
  * It iterates over all {@link module:engine/model/textproxy~TextProxy text contents} that are inside the range
  * and all the {@link module:engine/model/element~Element}s that are entered into when iterating over this range.
  *
  * This iterator uses {@link module:engine/model/treewalker~TreeWalker} with `boundaries` set to this range
  * and `ignoreElementEnd` option set to `true`.
  */
  *[Symbol.iterator]() {
    yield* new Vr({
      boundaries: this,
      ignoreElementEnd: !0
    });
  }
  /**
  * Describes whether the range is collapsed, that is if {@link #start} and
  * {@link #end} positions are equal.
  */
  get isCollapsed() {
    return this.start.isEqual(this.end);
  }
  /**
  * Describes whether this range is flat, that is if {@link #start} position and
  * {@link #end} position are in the same {@link module:engine/model/position~Position#parent}.
  */
  get isFlat() {
    const e = this.start.getParentPath(), n = this.end.getParentPath();
    return Fn(e, n) == "same";
  }
  /**
  * Range root element.
  */
  get root() {
    return this.start.root;
  }
  /**
  * Checks whether this range contains given {@link module:engine/model/position~Position position}.
  *
  * @param position Position to check.
  * @returns `true` if given {@link module:engine/model/position~Position position} is contained
  * in this range,`false` otherwise.
  */
  containsPosition(e) {
    return e.isAfter(this.start) && e.isBefore(this.end);
  }
  /**
  * Checks whether this range contains given {@link ~Range range}.
  *
  * @param otherRange Range to check.
  * @param loose Whether the check is loose or strict. If the check is strict (`false`), compared range cannot
  * start or end at the same position as this range boundaries. If the check is loose (`true`), compared range can start, end or
  * even be equal to this range. Note that collapsed ranges are always compared in strict mode.
  * @returns {Boolean} `true` if given {@link ~Range range} boundaries are contained by this range, `false` otherwise.
  */
  containsRange(e, n = !1) {
    e.isCollapsed && (n = !1);
    const r = this.containsPosition(e.start) || n && this.start.isEqual(e.start), s = this.containsPosition(e.end) || n && this.end.isEqual(e.end);
    return r && s;
  }
  /**
  * Checks whether given {@link module:engine/model/item~Item} is inside this range.
  */
  containsItem(e) {
    const n = G._createBefore(e);
    return this.containsPosition(n) || this.start.isEqual(n);
  }
  /**
  * Two ranges are equal if their {@link #start} and {@link #end} positions are equal.
  *
  * @param otherRange Range to compare with.
  * @returns `true` if ranges are equal, `false` otherwise.
  */
  isEqual(e) {
    return this.start.isEqual(e.start) && this.end.isEqual(e.end);
  }
  /**
  * Checks and returns whether this range intersects with given range.
  *
  * @param otherRange Range to compare with.
  * @returns `true` if ranges intersect, `false` otherwise.
  */
  isIntersecting(e) {
    return this.start.isBefore(e.end) && this.end.isAfter(e.start);
  }
  /**
  * Computes which part(s) of this {@link ~Range range} is not a part of given {@link ~Range range}.
  * Returned array contains zero, one or two {@link ~Range ranges}.
  *
  * Examples:
  *
  * ```ts
  * let range = model.createRange(
  * 	model.createPositionFromPath( root, [ 2, 7 ] ),
  * 	model.createPositionFromPath( root, [ 4, 0, 1 ] )
  * );
  * let otherRange = model.createRange( model.createPositionFromPath( root, [ 1 ] ), model.createPositionFromPath( root, [ 5 ] ) );
  * let transformed = range.getDifference( otherRange );
  * // transformed array has no ranges because `otherRange` contains `range`
  *
  * otherRange = model.createRange( model.createPositionFromPath( root, [ 1 ] ), model.createPositionFromPath( root, [ 3 ] ) );
  * transformed = range.getDifference( otherRange );
  * // transformed array has one range: from [ 3 ] to [ 4, 0, 1 ]
  *
  * otherRange = model.createRange( model.createPositionFromPath( root, [ 3 ] ), model.createPositionFromPath( root, [ 4 ] ) );
  * transformed = range.getDifference( otherRange );
  * // transformed array has two ranges: from [ 2, 7 ] to [ 3 ] and from [ 4 ] to [ 4, 0, 1 ]
  * ```
  *
  * @param otherRange Range to differentiate against.
  * @returns The difference between ranges.
  */
  getDifference(e) {
    const n = [];
    return this.isIntersecting(e) ? (this.containsPosition(e.start) && n.push(new D(this.start, e.start)), this.containsPosition(e.end) && n.push(new D(e.end, this.end))) : n.push(new D(this.start, this.end)), n;
  }
  /**
  * Returns an intersection of this {@link ~Range range} and given {@link ~Range range}.
  * Intersection is a common part of both of those ranges. If ranges has no common part, returns `null`.
  *
  * Examples:
  *
  * ```ts
  * let range = model.createRange(
  * 	model.createPositionFromPath( root, [ 2, 7 ] ),
  * 	model.createPositionFromPath( root, [ 4, 0, 1 ] )
  * );
  * let otherRange = model.createRange( model.createPositionFromPath( root, [ 1 ] ), model.createPositionFromPath( root, [ 2 ] ) );
  * let transformed = range.getIntersection( otherRange ); // null - ranges have no common part
  *
  * otherRange = model.createRange( model.createPositionFromPath( root, [ 3 ] ), model.createPositionFromPath( root, [ 5 ] ) );
  * transformed = range.getIntersection( otherRange ); // range from [ 3 ] to [ 4, 0, 1 ]
  * ```
  *
  * @param otherRange Range to check for intersection.
  * @returns A common part of given ranges or `null` if ranges have no common part.
  */
  getIntersection(e) {
    if (this.isIntersecting(e)) {
      let n = this.start, r = this.end;
      return this.containsPosition(e.start) && (n = e.start), this.containsPosition(e.end) && (r = e.end), new D(n, r);
    }
    return null;
  }
  /**
  * Returns a range created by joining this {@link ~Range range} with the given {@link ~Range range}.
  * If ranges have no common part, returns `null`.
  *
  * Examples:
  *
  * ```ts
  * let range = model.createRange(
  * 	model.createPositionFromPath( root, [ 2, 7 ] ),
  * 	model.createPositionFromPath( root, [ 4, 0, 1 ] )
  * );
  * let otherRange = model.createRange(
  * 	model.createPositionFromPath( root, [ 1 ] ),
  * 	model.createPositionFromPath( root, [ 2 ] )
   * );
  * let transformed = range.getJoined( otherRange ); // null - ranges have no common part
  *
  * otherRange = model.createRange(
  * 	model.createPositionFromPath( root, [ 3 ] ),
  * 	model.createPositionFromPath( root, [ 5 ] )
  * );
  * transformed = range.getJoined( otherRange ); // range from [ 2, 7 ] to [ 5 ]
  * ```
  *
  * @param otherRange Range to be joined.
  * @param loose Whether the intersection check is loose or strict. If the check is strict (`false`),
  * ranges are tested for intersection or whether start/end positions are equal. If the check is loose (`true`),
  * compared range is also checked if it's {@link module:engine/model/position~Position#isTouching touching} current range.
  * @returns A sum of given ranges or `null` if ranges have no common part.
  */
  getJoined(e, n = !1) {
    let r = this.isIntersecting(e);
    if (r || (this.start.isBefore(e.start) ? r = n ? this.end.isTouching(e.start) : this.end.isEqual(e.start) : r = n ? e.end.isTouching(this.start) : e.end.isEqual(this.start)), !r)
      return null;
    let s = this.start, o = this.end;
    return e.start.isBefore(s) && (s = e.start), e.end.isAfter(o) && (o = e.end), new D(s, o);
  }
  /**
  * Computes and returns the smallest set of {@link #isFlat flat} ranges, that covers this range in whole.
  *
  * See an example of a model structure (`[` and `]` are range boundaries):
  *
  * ```
  * root                                                            root
  *  |- element DIV                         DIV             P2              P3             DIV
  *  |   |- element H                   H        P1        f o o           b a r       H         P4
  *  |   |   |- "fir[st"             fir[st     lorem                               se]cond     ipsum
  *  |   |- element P1
  *  |   |   |- "lorem"                                              ||
  *  |- element P2                                                   ||
  *  |   |- "foo"                                                    VV
  *  |- element P3
  *  |   |- "bar"                                                   root
  *  |- element DIV                         DIV             [P2             P3]             DIV
  *  |   |- element H                   H       [P1]       f o o           b a r        H         P4
  *  |   |   |- "se]cond"            fir[st]    lorem                               [se]cond     ipsum
  *  |   |- element P4
  *  |   |   |- "ipsum"
  * ```
  *
  * As it can be seen, letters contained in the range are: `stloremfoobarse`, spread across different parents.
  * We are looking for minimal set of flat ranges that contains the same nodes.
  *
  * Minimal flat ranges for above range `( [ 0, 0, 3 ], [ 3, 0, 2 ] )` will be:
  *
  * ```
  * ( [ 0, 0, 3 ], [ 0, 0, 5 ] ) = "st"
  * ( [ 0, 1 ], [ 0, 2 ] ) = element P1 ("lorem")
  * ( [ 1 ], [ 3 ] ) = element P2, element P3 ("foobar")
  * ( [ 3, 0, 0 ], [ 3, 0, 2 ] ) = "se"
  * ```
  *
  * **Note:** if an {@link module:engine/model/element~Element element} is not wholly contained in this range, it won't be returned
  * in any of the returned flat ranges. See in the example how `H` elements at the beginning and at the end of the range
  * were omitted. Only their parts that were wholly in the range were returned.
  *
  * **Note:** this method is not returning flat ranges that contain no nodes.
  *
  * @returns Array of flat ranges covering this range.
  */
  getMinimalFlatRanges() {
    const e = [], n = this.start.getCommonPath(this.end).length, r = G._createAt(this.start);
    let s = r.parent;
    for (; r.path.length > n + 1; ) {
      const o = s.maxOffset - r.offset;
      o !== 0 && e.push(new D(r, r.getShiftedBy(o))), r.path = r.path.slice(0, -1), r.offset++, s = s.parent;
    }
    for (; r.path.length <= this.end.path.length; ) {
      const o = this.end.path[r.path.length - 1], a = o - r.offset;
      a !== 0 && e.push(new D(r, r.getShiftedBy(a))), r.offset = o, r.path.push(0);
    }
    return e;
  }
  /**
  * Creates a {@link module:engine/model/treewalker~TreeWalker TreeWalker} instance with this range as a boundary.
  *
  * For example, to iterate over all items in the entire document root:
  *
  * ```ts
  * // Create a range spanning over the entire root content:
  * const range = editor.model.createRangeIn( editor.model.document.getRoot() );
  *
  * // Iterate over all items in this range:
  * for ( const value of range.getWalker() ) {
  * 	console.log( value.item );
  * }
  * ```
  *
  * @param options Object with configuration options. See {@link module:engine/model/treewalker~TreeWalker}.
  */
  getWalker(e = {}) {
    return e.boundaries = this, new Vr(e);
  }
  /**
  * Returns an iterator that iterates over all {@link module:engine/model/item~Item items} that are in this range and returns
  * them.
  *
  * This method uses {@link module:engine/model/treewalker~TreeWalker} with `boundaries` set to this range and `ignoreElementEnd` option
  * set to `true`. However it returns only {@link module:engine/model/item~Item model items},
  * not {@link module:engine/model/treewalker~TreeWalkerValue}.
  *
  * You may specify additional options for the tree walker. See {@link module:engine/model/treewalker~TreeWalker} for
  * a full list of available options.
  *
  * @param options Object with configuration options. See {@link module:engine/model/treewalker~TreeWalker}.
  */
  *getItems(e = {}) {
    e.boundaries = this, e.ignoreElementEnd = !0;
    const n = new Vr(e);
    for (const r of n)
      yield r.item;
  }
  /**
  * Returns an iterator that iterates over all {@link module:engine/model/position~Position positions} that are boundaries or
  * contained in this range.
  *
  * This method uses {@link module:engine/model/treewalker~TreeWalker} with `boundaries` set to this range. However it returns only
  * {@link module:engine/model/position~Position positions}, not {@link module:engine/model/treewalker~TreeWalkerValue}.
  *
  * You may specify additional options for the tree walker. See {@link module:engine/model/treewalker~TreeWalker} for
  * a full list of available options.
  *
  * @param options Object with configuration options. See {@link module:engine/model/treewalker~TreeWalker}.
  */
  *getPositions(e = {}) {
    e.boundaries = this;
    const n = new Vr(e);
    yield n.position;
    for (const r of n)
      yield r.nextPosition;
  }
  /**
  * Returns a range that is a result of transforming this range by given `operation`.
  *
  * **Note:** transformation may break one range into multiple ranges (for example, when a part of the range is
  * moved to a different part of document tree). For this reason, an array is returned by this method and it
  * may contain one or more `Range` instances.
  *
  * @param operation Operation to transform range by.
  * @returns Range which is the result of transformation.
  */
  getTransformedByOperation(e) {
    switch (e.type) {
      case "insert":
        return this._getTransformedByInsertOperation(e);
      case "move":
      case "remove":
      case "reinsert":
        return this._getTransformedByMoveOperation(e);
      case "split":
        return [
          this._getTransformedBySplitOperation(e)
        ];
      case "merge":
        return [
          this._getTransformedByMergeOperation(e)
        ];
    }
    return [
      new D(this.start, this.end)
    ];
  }
  /**
  * Returns a range that is a result of transforming this range by multiple `operations`.
  *
  * @see ~Range#getTransformedByOperation
  * @param operations Operations to transform the range by.
  * @returns Range which is the result of transformation.
  */
  getTransformedByOperations(e) {
    const n = [
      new D(this.start, this.end)
    ];
    for (const r of e)
      for (let s = 0; s < n.length; s++) {
        const o = n[s].getTransformedByOperation(r);
        n.splice(s, 1, ...o), s += o.length - 1;
      }
    for (let r = 0; r < n.length; r++) {
      const s = n[r];
      for (let o = r + 1; o < n.length; o++) {
        const a = n[o];
        (s.containsRange(a) || a.containsRange(s) || s.isEqual(a)) && n.splice(o, 1);
      }
    }
    return n;
  }
  /**
  * Returns an {@link module:engine/model/element~Element} or {@link module:engine/model/documentfragment~DocumentFragment}
  * which is a common ancestor of the range's both ends (in which the entire range is contained).
  */
  getCommonAncestor() {
    return this.start.getCommonAncestor(this.end);
  }
  /**
  * Returns an {@link module:engine/model/element~Element Element} contained by the range.
  * The element will be returned when it is the **only** node within the range and **fully–contained**
  * at the same time.
  */
  getContainedElement() {
    if (this.isCollapsed)
      return null;
    const e = this.start.nodeAfter, n = this.end.nodeBefore;
    return e && e.is("element") && e === n ? e : null;
  }
  /**
  * Converts `Range` to plain object and returns it.
  *
  * @returns `Node` converted to plain object.
  */
  toJSON() {
    return {
      start: this.start.toJSON(),
      end: this.end.toJSON()
    };
  }
  /**
  * Returns a new range that is equal to current range.
  */
  clone() {
    return new this.constructor(this.start, this.end);
  }
  /**
  * Returns a result of transforming a copy of this range by insert operation.
  *
  * One or more ranges may be returned as a result of this transformation.
  *
  * @internal
  */
  _getTransformedByInsertOperation(e, n = !1) {
    return this._getTransformedByInsertion(e.position, e.howMany, n);
  }
  /**
  * Returns a result of transforming a copy of this range by move operation.
  *
  * One or more ranges may be returned as a result of this transformation.
  *
  * @internal
  */
  _getTransformedByMoveOperation(e, n = !1) {
    const r = e.sourcePosition, s = e.howMany, o = e.targetPosition;
    return this._getTransformedByMove(r, o, s, n);
  }
  /**
  * Returns a result of transforming a copy of this range by split operation.
  *
  * Always one range is returned. The transformation is done in a way to not break the range.
  *
  * @internal
  */
  _getTransformedBySplitOperation(e) {
    const n = this.start._getTransformedBySplitOperation(e);
    let r = this.end._getTransformedBySplitOperation(e);
    return this.end.isEqual(e.insertionPosition) && (r = this.end.getShiftedBy(1)), n.root != r.root && (r = this.end.getShiftedBy(-1)), new D(n, r);
  }
  /**
  * Returns a result of transforming a copy of this range by merge operation.
  *
  * Always one range is returned. The transformation is done in a way to not break the range.
  *
  * @internal
  */
  _getTransformedByMergeOperation(e) {
    if (this.start.isEqual(e.targetPosition) && this.end.isEqual(e.deletionPosition))
      return new D(this.start);
    let n = this.start._getTransformedByMergeOperation(e), r = this.end._getTransformedByMergeOperation(e);
    return n.root != r.root && (r = this.end.getShiftedBy(-1)), n.isAfter(r) ? (e.sourcePosition.isBefore(e.targetPosition) ? (n = G._createAt(r), n.offset = 0) : (e.deletionPosition.isEqual(n) || (r = e.deletionPosition), n = e.targetPosition), new D(n, r)) : new D(n, r);
  }
  /**
  * Returns an array containing one or two {@link ~Range ranges} that are a result of transforming this
  * {@link ~Range range} by inserting `howMany` nodes at `insertPosition`. Two {@link ~Range ranges} are
  * returned if the insertion was inside this {@link ~Range range} and `spread` is set to `true`.
  *
  * Examples:
  *
  * ```ts
  * let range = model.createRange(
  * 	model.createPositionFromPath( root, [ 2, 7 ] ),
  * 	model.createPositionFromPath( root, [ 4, 0, 1 ] )
  * );
  * let transformed = range._getTransformedByInsertion( model.createPositionFromPath( root, [ 1 ] ), 2 );
  * // transformed array has one range from [ 4, 7 ] to [ 6, 0, 1 ]
  *
  * transformed = range._getTransformedByInsertion( model.createPositionFromPath( root, [ 4, 0, 0 ] ), 4 );
  * // transformed array has one range from [ 2, 7 ] to [ 4, 0, 5 ]
  *
  * transformed = range._getTransformedByInsertion( model.createPositionFromPath( root, [ 3, 2 ] ), 4 );
  * // transformed array has one range, which is equal to original range
  *
  * transformed = range._getTransformedByInsertion( model.createPositionFromPath( root, [ 3, 2 ] ), 4, true );
  * // transformed array has two ranges: from [ 2, 7 ] to [ 3, 2 ] and from [ 3, 6 ] to [ 4, 0, 1 ]
  * ```
  *
  * @internal
  * @param insertPosition Position where nodes are inserted.
  * @param howMany How many nodes are inserted.
  * @param spread Flag indicating whether this range should be spread if insertion
  * was inside the range. Defaults to `false`.
  * @returns Result of the transformation.
  */
  _getTransformedByInsertion(e, n, r = !1) {
    if (r && this.containsPosition(e))
      return [
        new D(this.start, e),
        new D(e.getShiftedBy(n), this.end._getTransformedByInsertion(e, n))
      ];
    {
      const s = new D(this.start, this.end);
      return s.start = s.start._getTransformedByInsertion(e, n), s.end = s.end._getTransformedByInsertion(e, n), [
        s
      ];
    }
  }
  /**
  * Returns an array containing {@link ~Range ranges} that are a result of transforming this
  * {@link ~Range range} by moving `howMany` nodes from `sourcePosition` to `targetPosition`.
  *
  * @internal
  * @param sourcePosition Position from which nodes are moved.
  * @param targetPosition Position to where nodes are moved.
  * @param howMany How many nodes are moved.
  * @param spread Whether the range should be spread if the move points inside the range.
  * @returns  Result of the transformation.
  */
  _getTransformedByMove(e, n, r, s = !1) {
    if (this.isCollapsed) {
      const f = this.start._getTransformedByMove(e, n, r);
      return [
        new D(f)
      ];
    }
    const o = D._createFromPositionAndShift(e, r), a = n._getTransformedByDeletion(e, r);
    if (this.containsPosition(n) && !s && (o.containsPosition(this.start) || o.containsPosition(this.end))) {
      const f = this.start._getTransformedByMove(e, n, r), m = this.end._getTransformedByMove(e, n, r);
      return [
        new D(f, m)
      ];
    }
    let l;
    const c = this.getDifference(o);
    let u = null;
    const d = this.getIntersection(o);
    if (c.length == 1 ? u = new D(c[0].start._getTransformedByDeletion(e, r), c[0].end._getTransformedByDeletion(e, r)) : c.length == 2 && (u = new D(this.start, this.end._getTransformedByDeletion(e, r))), u ? l = u._getTransformedByInsertion(a, r, d !== null || s) : l = [], d) {
      const f = new D(d.start._getCombined(o.start, a), d.end._getCombined(o.start, a));
      l.length == 2 ? l.splice(1, 0, f) : l.push(f);
    }
    return l;
  }
  /**
  * Returns a copy of this range that is transformed by deletion of `howMany` nodes from `deletePosition`.
  *
  * If the deleted range is intersecting with the transformed range, the transformed range will be shrank.
  *
  * If the deleted range contains transformed range, `null` will be returned.
  *
  * @internal
  * @param deletionPosition Position from which nodes are removed.
  * @param howMany How many nodes are removed.
  * @returns Result of the transformation.
  */
  _getTransformedByDeletion(e, n) {
    let r = this.start._getTransformedByDeletion(e, n), s = this.end._getTransformedByDeletion(e, n);
    return r == null && s == null ? null : (r == null && (r = e), s == null && (s = e), new D(r, s));
  }
  /**
  * Creates a new range, spreading from specified {@link module:engine/model/position~Position position} to a position moved by
  * given `shift`. If `shift` is a negative value, shifted position is treated as the beginning of the range.
  *
  * @internal
  * @param position Beginning of the range.
  * @param shift How long the range should be.
  */
  static _createFromPositionAndShift(e, n) {
    const r = e, s = e.getShiftedBy(n);
    return n > 0 ? new this(r, s) : new this(s, r);
  }
  /**
  * Creates a range inside an {@link module:engine/model/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @internal
  * @param element Element which is a parent for the range.
  */
  static _createIn(e) {
    return new this(G._createAt(e, 0), G._createAt(e, e.maxOffset));
  }
  /**
  * Creates a range that starts before given {@link module:engine/model/item~Item model item} and ends after it.
  *
  * @internal
  */
  static _createOn(e) {
    return this._createFromPositionAndShift(G._createBefore(e), e.offsetSize);
  }
  /**
  * Combines all ranges from the passed array into a one range. At least one range has to be passed.
  * Passed ranges must not have common parts.
  *
  * The first range from the array is a reference range. If other ranges start or end on the exactly same position where
  * the reference range, they get combined into one range.
  *
  * ```
  * [  ][]  [    ][ ][             ][ ][]  [  ]  // Passed ranges, shown sorted
  * [    ]                                       // The result of the function if the first range was a reference range.
  *         [                           ]        // The result of the function if the third-to-seventh range was a reference range.
  *                                        [  ]  // The result of the function if the last range was a reference range.
  * ```
  *
  * @internal
  * @param ranges Ranges to combine.
  * @returns Combined range.
  */
  static _createFromRanges(e) {
    if (e.length === 0)
      throw new T("range-create-from-ranges-empty-array", null);
    if (e.length == 1)
      return e[0].clone();
    const n = e[0];
    e.sort((o, a) => o.start.isAfter(a.start) ? 1 : -1);
    const r = e.indexOf(n), s = new this(n.start, n.end);
    for (let o = r - 1; o >= 0 && e[o].end.isEqual(s.start); o--)
      s.start = G._createAt(e[o].start);
    for (let o = r + 1; o < e.length && e[o].start.isEqual(s.end); o++)
      s.end = G._createAt(e[o].end);
    return s;
  }
  /**
  * Creates a `Range` instance from given plain object (i.e. parsed JSON string).
  *
  * @param json Plain object to be converted to `Range`.
  * @param doc Document object that will be range owner.
  * @returns `Range` instance created using given plain object.
  */
  static fromJSON(e, n) {
    return new this(G.fromJSON(e.start, n), G.fromJSON(e.end, n));
  }
}
D.prototype.is = function(i) {
  return i === "range" || i === "model:range";
};
class G2 extends (/* @__PURE__ */ ht()) {
  /**
  * Creates an instance of the mapper.
  */
  constructor() {
    super();
    /**
    * Model element to view element mapping.
    */
    h(this, "_modelToViewMapping", /* @__PURE__ */ new WeakMap());
    /**
    * View element to model element mapping.
    */
    h(this, "_viewToModelMapping", /* @__PURE__ */ new WeakMap());
    /**
    * A map containing callbacks between view element names and functions evaluating length of view elements
    * in model.
    */
    h(this, "_viewToModelLengthCallbacks", /* @__PURE__ */ new Map());
    /**
    * Model marker name to view elements mapping.
    *
    * Keys are `String`s while values are `Set`s with {@link module:engine/view/element~Element view elements}.
    * One marker (name) can be mapped to multiple elements.
    */
    h(this, "_markerNameToElements", /* @__PURE__ */ new Map());
    /**
    * View element to model marker names mapping.
    *
    * This is reverse to {@link ~Mapper#_markerNameToElements} map.
    */
    h(this, "_elementToMarkerNames", /* @__PURE__ */ new Map());
    /**
    * The map of removed view elements with their current root (used for deferred unbinding).
    */
    h(this, "_deferredBindingRemovals", /* @__PURE__ */ new Map());
    /**
    * Stores marker names of markers which have changed due to unbinding a view element (so it is assumed that the view element
    * has been removed, moved or renamed).
    */
    h(this, "_unboundMarkerNames", /* @__PURE__ */ new Set());
    this.on("modelToViewPosition", (e, n) => {
      if (n.viewPosition)
        return;
      const r = this._modelToViewMapping.get(n.modelPosition.parent);
      if (!r)
        throw new T("mapping-model-position-view-parent-not-found", this, {
          modelPosition: n.modelPosition
        });
      n.viewPosition = this.findPositionIn(r, n.modelPosition.offset);
    }, {
      priority: "low"
    }), this.on("viewToModelPosition", (e, n) => {
      if (n.modelPosition)
        return;
      const r = this.findMappedViewAncestor(n.viewPosition), s = this._viewToModelMapping.get(r), o = this._toModelOffset(n.viewPosition.parent, n.viewPosition.offset, r);
      n.modelPosition = G._createAt(s, o);
    }, {
      priority: "low"
    });
  }
  /**
  * Marks model and view elements as corresponding. Corresponding elements can be retrieved by using
  * the {@link module:engine/conversion/mapper~Mapper#toModelElement toModelElement} and
  * {@link module:engine/conversion/mapper~Mapper#toViewElement toViewElement} methods.
  * The information that elements are bound is also used to translate positions.
  *
  * @param modelElement Model element.
  * @param viewElement View element.
  */
  bindElements(e, n) {
    this._modelToViewMapping.set(e, n), this._viewToModelMapping.set(n, e);
  }
  /**
  * Unbinds the given {@link module:engine/view/element~Element view element} from the map.
  *
  * **Note:** view-to-model binding will be removed, if it existed. However, corresponding model-to-view binding
  * will be removed only if model element is still bound to the passed `viewElement`.
  *
  * This behavior allows for re-binding model element to another view element without fear of losing the new binding
  * when the previously bound view element is unbound.
  *
  * @param viewElement View element to unbind.
  * @param options The options object.
  * @param options.defer Controls whether the binding should be removed immediately or deferred until a
  * {@link #flushDeferredBindings `flushDeferredBindings()`} call.
  */
  unbindViewElement(e, n = {}) {
    const r = this.toModelElement(e);
    if (this._elementToMarkerNames.has(e))
      for (const s of this._elementToMarkerNames.get(e))
        this._unboundMarkerNames.add(s);
    n.defer ? this._deferredBindingRemovals.set(e, e.root) : (this._viewToModelMapping.delete(e), this._modelToViewMapping.get(r) == e && this._modelToViewMapping.delete(r));
  }
  /**
  * Unbinds the given {@link module:engine/model/element~Element model element} from the map.
  *
  * **Note:** the model-to-view binding will be removed, if it existed. However, the corresponding view-to-model binding
  * will be removed only if the view element is still bound to the passed `modelElement`.
  *
  * This behavior lets for re-binding view element to another model element without fear of losing the new binding
  * when the previously bound model element is unbound.
  *
  * @param modelElement Model element to unbind.
  */
  unbindModelElement(e) {
    const n = this.toViewElement(e);
    this._modelToViewMapping.delete(e), this._viewToModelMapping.get(n) == e && this._viewToModelMapping.delete(n);
  }
  /**
  * Binds the given marker name with the given {@link module:engine/view/element~Element view element}. The element
  * will be added to the current set of elements bound with the given marker name.
  *
  * @param element Element to bind.
  * @param name Marker name.
  */
  bindElementToMarker(e, n) {
    const r = this._markerNameToElements.get(n) || /* @__PURE__ */ new Set();
    r.add(e);
    const s = this._elementToMarkerNames.get(e) || /* @__PURE__ */ new Set();
    s.add(n), this._markerNameToElements.set(n, r), this._elementToMarkerNames.set(e, s);
  }
  /**
  * Unbinds an element from given marker name.
  *
  * @param element Element to unbind.
  * @param name Marker name.
  */
  unbindElementFromMarkerName(e, n) {
    const r = this._markerNameToElements.get(n);
    r && (r.delete(e), r.size == 0 && this._markerNameToElements.delete(n));
    const s = this._elementToMarkerNames.get(e);
    s && (s.delete(n), s.size == 0 && this._elementToMarkerNames.delete(e));
  }
  /**
  * Returns all marker names of markers which have changed due to unbinding a view element (so it is assumed that the view element
  * has been removed, moved or renamed) since the last flush. After returning, the marker names list is cleared.
  */
  flushUnboundMarkerNames() {
    const e = Array.from(this._unboundMarkerNames);
    return this._unboundMarkerNames.clear(), e;
  }
  /**
  * Unbinds all deferred binding removals of view elements that in the meantime were not re-attached to some root or document fragment.
  *
  * See: {@link #unbindViewElement `unbindViewElement()`}.
  */
  flushDeferredBindings() {
    for (const [e, n] of this._deferredBindingRemovals)
      e.root == n && this.unbindViewElement(e);
    this._deferredBindingRemovals = /* @__PURE__ */ new Map();
  }
  /**
  * Removes all model to view and view to model bindings.
  */
  clearBindings() {
    this._modelToViewMapping = /* @__PURE__ */ new WeakMap(), this._viewToModelMapping = /* @__PURE__ */ new WeakMap(), this._markerNameToElements = /* @__PURE__ */ new Map(), this._elementToMarkerNames = /* @__PURE__ */ new Map(), this._unboundMarkerNames = /* @__PURE__ */ new Set(), this._deferredBindingRemovals = /* @__PURE__ */ new Map();
  }
  toModelElement(e) {
    return this._viewToModelMapping.get(e);
  }
  toViewElement(e) {
    return this._modelToViewMapping.get(e);
  }
  /**
  * Gets the corresponding model range.
  *
  * @param viewRange View range.
  * @returns Corresponding model range.
  */
  toModelRange(e) {
    return new D(this.toModelPosition(e.start), this.toModelPosition(e.end));
  }
  /**
  * Gets the corresponding view range.
  *
  * @param modelRange Model range.
  * @returns Corresponding view range.
  */
  toViewRange(e) {
    return new qe(this.toViewPosition(e.start), this.toViewPosition(e.end));
  }
  /**
  * Gets the corresponding model position.
  *
  * @fires viewToModelPosition
  * @param viewPosition View position.
  * @returns Corresponding model position.
  */
  toModelPosition(e) {
    const n = {
      viewPosition: e,
      mapper: this
    };
    return this.fire("viewToModelPosition", n), n.modelPosition;
  }
  /**
  * Gets the corresponding view position.
  *
  * @fires modelToViewPosition
  * @param modelPosition Model position.
  * @param options Additional options for position mapping process.
  * @param options.isPhantom Should be set to `true` if the model position to map is pointing to a place
  * in model tree which no longer exists. For example, it could be an end of a removed model range.
  * @returns Corresponding view position.
  */
  toViewPosition(e, n = {}) {
    const r = {
      modelPosition: e,
      mapper: this,
      isPhantom: n.isPhantom
    };
    return this.fire("modelToViewPosition", r), r.viewPosition;
  }
  /**
  * Gets all view elements bound to the given marker name.
  *
  * @param name Marker name.
  * @returns View elements bound with the given marker name or `null`
  * if no elements are bound to the given marker name.
  */
  markerNameToElements(e) {
    const n = this._markerNameToElements.get(e);
    if (!n)
      return null;
    const r = /* @__PURE__ */ new Set();
    for (const s of n)
      if (s.is("attributeElement"))
        for (const o of s.getElementsWithSameId())
          r.add(o);
      else
        r.add(s);
    return r;
  }
  /**
  * Registers a callback that evaluates the length in the model of a view element with the given name.
  *
  * The callback is fired with one argument, which is a view element instance. The callback is expected to return
  * a number representing the length of the view element in the model.
  *
  * ```ts
  * // List item in view may contain nested list, which have other list items. In model though,
  * // the lists are represented by flat structure. Because of those differences, length of list view element
  * // may be greater than one. In the callback it's checked how many nested list items are in evaluated list item.
  *
  * function getViewListItemLength( element ) {
  * 	let length = 1;
  *
  * 	for ( let child of element.getChildren() ) {
  * 		if ( child.name == 'ul' || child.name == 'ol' ) {
  * 			for ( let item of child.getChildren() ) {
  * 				length += getViewListItemLength( item );
  * 			}
  * 		}
  * 	}
  *
  * 	return length;
  * }
  *
  * mapper.registerViewToModelLength( 'li', getViewListItemLength );
  * ```
  *
  * @param viewElementName Name of view element for which callback is registered.
  * @param lengthCallback Function return a length of view element instance in model.
  */
  registerViewToModelLength(e, n) {
    this._viewToModelLengthCallbacks.set(e, n);
  }
  /**
  * For the given `viewPosition`, finds and returns the closest ancestor of this position that has a mapping to
  * the model.
  *
  * @param viewPosition Position for which a mapped ancestor should be found.
  */
  findMappedViewAncestor(e) {
    let n = e.parent;
    for (; !this._viewToModelMapping.has(n); )
      n = n.parent;
    return n;
  }
  /**
  * Calculates model offset based on the view position and the block element.
  *
  * Example:
  *
  * ```html
  * <p>foo<b>ba|r</b></p> // _toModelOffset( b, 2, p ) -> 5
  * ```
  *
  * Is a sum of:
  *
  * ```html
  * <p>foo|<b>bar</b></p> // _toModelOffset( p, 3, p ) -> 3
  * <p>foo<b>ba|r</b></p> // _toModelOffset( b, 2, b ) -> 2
  * ```
  *
  * @param viewParent Position parent.
  * @param viewOffset Position offset.
  * @param viewBlock Block used as a base to calculate offset.
  * @returns Offset in the model.
  */
  _toModelOffset(e, n, r) {
    if (r != e) {
      const o = this._toModelOffset(e.parent, e.index, r), a = this._toModelOffset(e, n, e);
      return o + a;
    }
    if (e.is("$text"))
      return n;
    let s = 0;
    for (let o = 0; o < n; o++)
      s += this.getModelLength(e.getChild(o));
    return s;
  }
  /**
  * Gets the length of the view element in the model.
  *
  * The length is calculated as follows:
  * * if a {@link #registerViewToModelLength length mapping callback} is provided for the given `viewNode`, it is used to
  * evaluate the model length (`viewNode` is used as first and only parameter passed to the callback),
  * * length of a {@link module:engine/view/text~Text text node} is equal to the length of its
  * {@link module:engine/view/text~Text#data data},
  * * length of a {@link module:engine/view/uielement~UIElement ui element} is equal to 0,
  * * length of a mapped {@link module:engine/view/element~Element element} is equal to 1,
  * * length of a non-mapped {@link module:engine/view/element~Element element} is equal to the length of its children.
  *
  * Examples:
  *
  * ```
  * foo                          -> 3 // Text length is equal to its data length.
  * <p>foo</p>                   -> 1 // Length of an element which is mapped is by default equal to 1.
  * <b>foo</b>                   -> 3 // Length of an element which is not mapped is a length of its children.
  * <div><p>x</p><p>y</p></div>  -> 2 // Assuming that <div> is not mapped and <p> are mapped.
  * ```
  *
  * @param viewNode View node.
  * @returns Length of the node in the tree model.
  */
  getModelLength(e) {
    if (this._viewToModelLengthCallbacks.get(e.name))
      return this._viewToModelLengthCallbacks.get(e.name)(e);
    if (this._viewToModelMapping.has(e))
      return 1;
    if (e.is("$text"))
      return e.data.length;
    if (e.is("uiElement"))
      return 0;
    {
      let n = 0;
      for (const r of e.getChildren())
        n += this.getModelLength(r);
      return n;
    }
  }
  /**
  * Finds the position in the view node (or in its children) with the expected model offset.
  *
  * Example:
  *
  * ```
  * <p>fo<b>bar</b>bom</p> -> expected offset: 4
  *
  * findPositionIn( p, 4 ):
  * <p>|fo<b>bar</b>bom</p> -> expected offset: 4, actual offset: 0
  * <p>fo|<b>bar</b>bom</p> -> expected offset: 4, actual offset: 2
  * <p>fo<b>bar</b>|bom</p> -> expected offset: 4, actual offset: 5 -> we are too far
  *
  * findPositionIn( b, 4 - ( 5 - 3 ) ):
  * <p>fo<b>|bar</b>bom</p> -> expected offset: 2, actual offset: 0
  * <p>fo<b>bar|</b>bom</p> -> expected offset: 2, actual offset: 3 -> we are too far
  *
  * findPositionIn( bar, 2 - ( 3 - 3 ) ):
  * We are in the text node so we can simple find the offset.
  * <p>fo<b>ba|r</b>bom</p> -> expected offset: 2, actual offset: 2 -> position found
  * ```
  *
  * @param viewParent Tree view element in which we are looking for the position.
  * @param expectedOffset Expected offset.
  * @returns Found position.
  */
  findPositionIn(e, n) {
    let r, s = 0, o = 0, a = 0;
    if (e.is("$text"))
      return new fe(e, n);
    for (; o < n; )
      r = e.getChild(a), s = this.getModelLength(r), o += s, a++;
    return o == n ? this._moveViewPositionToTextNode(new fe(e, a)) : this.findPositionIn(r, n - (o - s));
  }
  /**
  * Because we prefer positions in the text nodes over positions next to text nodes, if the view position was next to a text node,
  * it moves it into the text node instead.
  *
  * ```
  * <p>[]<b>foo</b></p> -> <p>[]<b>foo</b></p> // do not touch if position is not directly next to text
  * <p>foo[]<b>foo</b></p> -> <p>foo{}<b>foo</b></p> // move to text node
  * <p><b>[]foo</b></p> -> <p><b>{}foo</b></p> // move to text node
  * ```
  *
  * @param viewPosition Position potentially next to the text node.
  * @returns Position in the text node if possible.
  */
  _moveViewPositionToTextNode(e) {
    const n = e.nodeBefore, r = e.nodeAfter;
    return n instanceof St ? new fe(n, n.data.length) : r instanceof St ? new fe(r, 0) : e;
  }
}
class eO {
  constructor() {
    /**
    * Contains list of consumable values.
    */
    h(this, "_consumable", /* @__PURE__ */ new Map());
    /**
    * For each {@link module:engine/model/textproxy~TextProxy} added to `ModelConsumable`, this registry holds a parent
    * of that `TextProxy` and the start and end indices of that `TextProxy`. This allows identification of the `TextProxy`
    * instances that point to the same part of the model but are different instances. Each distinct `TextProxy`
    * is given a unique `Symbol` which is then registered as consumable. This process is transparent for the `ModelConsumable`
    * API user because whenever `TextProxy` is added, tested, consumed or reverted, the internal mechanisms of
    * `ModelConsumable` translate `TextProxy` to that unique `Symbol`.
    */
    h(this, "_textProxyRegistry", /* @__PURE__ */ new Map());
  }
  /**
  * Adds a consumable value to the consumables list and links it with a given model item.
  *
  * ```ts
  * modelConsumable.add( modelElement, 'insert' ); // Add `modelElement` insertion change to consumable values.
  * modelConsumable.add( modelElement, 'addAttribute:bold' ); // Add `bold` attribute insertion on `modelElement` change.
  * modelConsumable.add( modelElement, 'removeAttribute:bold' ); // Add `bold` attribute removal on `modelElement` change.
  * modelConsumable.add( modelSelection, 'selection' ); // Add `modelSelection` to consumable values.
  * modelConsumable.add( modelRange, 'range' ); // Add `modelRange` to consumable values.
  * ```
  *
  * @param item Model item, range or selection that has the consumable.
  * @param type Consumable type. Will be normalized to a proper form, that is either `<word>` or `<part>:<part>`.
  * Second colon and everything after will be cut. Passing event name is a safe and good practice.
  */
  add(t, e) {
    e = Dc(e), t instanceof Gi && (t = this._getSymbolForTextProxy(t)), this._consumable.has(t) || this._consumable.set(t, /* @__PURE__ */ new Map()), this._consumable.get(t).set(e, !0);
  }
  /**
  * Removes a given consumable value from a given model item.
  *
  * ```ts
  * modelConsumable.consume( modelElement, 'insert' ); // Remove `modelElement` insertion change from consumable values.
  * modelConsumable.consume( modelElement, 'addAttribute:bold' ); // Remove `bold` attribute insertion on `modelElement` change.
  * modelConsumable.consume( modelElement, 'removeAttribute:bold' ); // Remove `bold` attribute removal on `modelElement` change.
  * modelConsumable.consume( modelSelection, 'selection' ); // Remove `modelSelection` from consumable values.
  * modelConsumable.consume( modelRange, 'range' ); // Remove 'modelRange' from consumable values.
  * ```
  *
  * @param item Model item, range or selection from which consumable will be consumed.
  * @param type Consumable type. Will be normalized to a proper form, that is either `<word>` or `<part>:<part>`.
  * Second colon and everything after will be cut. Passing event name is a safe and good practice.
  * @returns `true` if consumable value was available and was consumed, `false` otherwise.
  */
  consume(t, e) {
    return e = Dc(e), t instanceof Gi && (t = this._getSymbolForTextProxy(t)), this.test(t, e) ? (this._consumable.get(t).set(e, !1), !0) : !1;
  }
  /**
  * Tests whether there is a consumable value of a given type connected with a given model item.
  *
  * ```ts
  * modelConsumable.test( modelElement, 'insert' ); // Check for `modelElement` insertion change.
  * modelConsumable.test( modelElement, 'addAttribute:bold' ); // Check for `bold` attribute insertion on `modelElement` change.
  * modelConsumable.test( modelElement, 'removeAttribute:bold' ); // Check for `bold` attribute removal on `modelElement` change.
  * modelConsumable.test( modelSelection, 'selection' ); // Check if `modelSelection` is consumable.
  * modelConsumable.test( modelRange, 'range' ); // Check if `modelRange` is consumable.
  * ```
  *
  * @param item Model item, range or selection to be tested.
  * @param type Consumable type. Will be normalized to a proper form, that is either `<word>` or `<part>:<part>`.
  * Second colon and everything after will be cut. Passing event name is a safe and good practice.
  * @returns `null` if such consumable was never added, `false` if the consumable values was
  * already consumed or `true` if it was added and not consumed yet.
  */
  test(t, e) {
    e = Dc(e), t instanceof Gi && (t = this._getSymbolForTextProxy(t));
    const n = this._consumable.get(t);
    if (n === void 0)
      return null;
    const r = n.get(e);
    return r === void 0 ? null : r;
  }
  /**
  * Reverts consuming of a consumable value.
  *
  * ```ts
  * modelConsumable.revert( modelElement, 'insert' ); // Revert consuming `modelElement` insertion change.
  * modelConsumable.revert( modelElement, 'addAttribute:bold' ); // Revert consuming `bold` attribute insert from `modelElement`.
  * modelConsumable.revert( modelElement, 'removeAttribute:bold' ); // Revert consuming `bold` attribute remove from `modelElement`.
  * modelConsumable.revert( modelSelection, 'selection' ); // Revert consuming `modelSelection`.
  * modelConsumable.revert( modelRange, 'range' ); // Revert consuming `modelRange`.
  * ```
  *
  * @param item Model item, range or selection to be reverted.
  * @param type Consumable type.
  * @returns `true` if consumable has been reversed, `false` otherwise. `null` if the consumable has
  * never been added.
  */
  revert(t, e) {
    e = Dc(e), t instanceof Gi && (t = this._getSymbolForTextProxy(t));
    const n = this.test(t, e);
    return n === !1 ? (this._consumable.get(t).set(e, !0), !0) : n === !0 ? !1 : null;
  }
  /**
  * Verifies if all events from the specified group were consumed.
  *
  * @param eventGroup The events group to verify.
  */
  verifyAllConsumed(t) {
    const e = [];
    for (const [n, r] of this._consumable)
      for (const [s, o] of r) {
        const a = s.split(":")[0];
        o && t == a && e.push({
          event: s,
          item: n.name || n.description
        });
      }
    if (e.length)
      throw new T("conversion-model-consumable-not-consumed", null, {
        items: e
      });
  }
  /**
  * Gets a unique symbol for the passed {@link module:engine/model/textproxy~TextProxy} instance. All `TextProxy` instances that
  * have same parent, same start index and same end index will get the same symbol.
  *
  * Used internally to correctly consume `TextProxy` instances.
  *
  * @internal
  * @param textProxy `TextProxy` instance to get a symbol for.
  * @returns Symbol representing all equal instances of `TextProxy`.
  */
  _getSymbolForTextProxy(t) {
    let e = null;
    const n = this._textProxyRegistry.get(t.startOffset);
    if (n) {
      const r = n.get(t.endOffset);
      r && (e = r.get(t.parent));
    }
    return e || (e = this._addSymbolForTextProxy(t)), e;
  }
  /**
  * Adds a symbol for the given {@link module:engine/model/textproxy~TextProxy} instance.
  *
  * Used internally to correctly consume `TextProxy` instances.
  *
  * @param textProxy Text proxy instance.
  * @returns Symbol generated for given `TextProxy`.
  */
  _addSymbolForTextProxy(t) {
    const e = t.startOffset, n = t.endOffset, r = t.parent, s = Symbol("$textProxy:" + t.data);
    let o, a;
    return o = this._textProxyRegistry.get(e), o || (o = /* @__PURE__ */ new Map(), this._textProxyRegistry.set(e, o)), a = o.get(n), a || (a = /* @__PURE__ */ new Map(), o.set(n, a)), a.set(r, s), s;
  }
}
function Dc(i) {
  const t = i.split(":");
  return t[0] == "insert" ? t[0] : t[0] == "addMarker" || t[0] == "removeMarker" ? i : t.length > 1 ? t[0] + ":" + t[1] : t[0];
}
class K2 extends (/* @__PURE__ */ ht()) {
  /**
  * Creates a downcast dispatcher instance.
  *
  * @see module:engine/conversion/downcastdispatcher~DowncastConversionApi
  *
  * @param conversionApi Additional properties for an interface that will be passed to events fired
  * by the downcast dispatcher.
  */
  constructor(e) {
    super();
    /**
    * A template for an interface passed by the dispatcher to the event callbacks.
    *
    * @internal
    */
    h(this, "_conversionApi");
    /**
    * A map of already fired events for a given `ModelConsumable`.
    */
    h(this, "_firedEventsMap");
    this._conversionApi = {
      dispatcher: this,
      ...e
    }, this._firedEventsMap = /* @__PURE__ */ new WeakMap();
  }
  /**
  * Converts changes buffered in the given {@link module:engine/model/differ~Differ model differ}
  * and fires conversion events based on it.
  *
  * @fires insert
  * @fires remove
  * @fires attribute
  * @fires addMarker
  * @fires removeMarker
  * @fires reduceChanges
  * @param differ The differ object with buffered changes.
  * @param markers Markers related to the model fragment to convert.
  * @param writer The view writer that should be used to modify the view document.
  */
  convertChanges(e, n, r) {
    const s = this._createConversionApi(r, e.getRefreshedItems());
    for (const a of e.getMarkersToRemove())
      this._convertMarkerRemove(a.name, a.range, s);
    const o = this._reduceChanges(e.getChanges());
    for (const a of o)
      a.type === "insert" ? this._convertInsert(D._createFromPositionAndShift(a.position, a.length), s) : a.type === "reinsert" ? this._convertReinsert(D._createFromPositionAndShift(a.position, a.length), s) : a.type === "remove" ? this._convertRemove(a.position, a.length, a.name, s) : this._convertAttribute(a.range, a.attributeKey, a.attributeOldValue, a.attributeNewValue, s);
    s.mapper.flushDeferredBindings();
    for (const a of s.mapper.flushUnboundMarkerNames()) {
      const l = n.get(a).getRange();
      this._convertMarkerRemove(a, l, s), this._convertMarkerAdd(a, l, s);
    }
    for (const a of e.getMarkersToAdd())
      this._convertMarkerAdd(a.name, a.range, s);
    s.consumable.verifyAllConsumed("insert");
  }
  /**
  * Starts a conversion of a model range and the provided markers.
  *
  * @fires insert
  * @fires attribute
  * @fires addMarker
  * @param range The inserted range.
  * @param markers The map of markers that should be down-casted.
  * @param writer The view writer that should be used to modify the view document.
  * @param options Optional options object passed to `convertionApi.options`.
  */
  convert(e, n, r, s = {}) {
    const o = this._createConversionApi(r, void 0, s);
    this._convertInsert(e, o);
    for (const [a, l] of n)
      this._convertMarkerAdd(a, l, o);
    o.consumable.verifyAllConsumed("insert");
  }
  /**
  * Starts the model selection conversion.
  *
  * Fires events for a given {@link module:engine/model/selection~Selection selection} to start the selection conversion.
  *
  * @fires selection
  * @fires addMarker
  * @fires attribute
  * @param selection The selection to convert.
  * @param markers Markers connected with the converted model.
  * @param writer View writer that should be used to modify the view document.
  */
  convertSelection(e, n, r) {
    const s = this._createConversionApi(r);
    this.fire("cleanSelection", {
      selection: e
    }, s);
    const o = e.getFirstPosition().root;
    if (!s.mapper.toViewElement(o))
      return;
    const a = Array.from(n.getMarkersAtPosition(e.getFirstPosition()));
    if (this._addConsumablesForSelection(s.consumable, e, a), this.fire("selection", {
      selection: e
    }, s), !!e.isCollapsed) {
      for (const l of a)
        if (s.consumable.test(e, "addMarker:" + l.name)) {
          const c = l.getRange();
          if (!tO(e.getFirstPosition(), l, s.mapper))
            continue;
          const u = {
            item: e,
            markerName: l.name,
            markerRange: c
          };
          this.fire(`addMarker:${l.name}`, u, s);
        }
      for (const l of e.getAttributeKeys())
        if (s.consumable.test(e, "attribute:" + l)) {
          const c = {
            item: e,
            range: e.getFirstRange(),
            attributeKey: l,
            attributeOldValue: null,
            attributeNewValue: e.getAttribute(l)
          };
          this.fire(`attribute:${l}:$text`, c, s);
        }
    }
  }
  /**
  * Fires insertion conversion of a range of nodes.
  *
  * For each node in the range, {@link #event:insert `insert` event is fired}. For each attribute on each node,
  * {@link #event:attribute `attribute` event is fired}.
  *
  * @fires insert
  * @fires attribute
  * @param range The inserted range.
  * @param conversionApi The conversion API object.
  * @param options.doNotAddConsumables Whether the ModelConsumable should not get populated
  * for items in the provided range.
  */
  _convertInsert(e, n, r = {}) {
    r.doNotAddConsumables || this._addConsumablesForInsert(n.consumable, e);
    for (const s of Array.from(e.getWalker({
      shallow: !0
    })).map($1))
      this._testAndFire("insert", s, n);
  }
  /**
  * Fires conversion of a single node removal. Fires {@link #event:remove remove event} with provided data.
  *
  * @param position Position from which node was removed.
  * @param length Offset size of removed node.
  * @param name Name of removed node.
  * @param conversionApi The conversion API object.
  */
  _convertRemove(e, n, r, s) {
    this.fire(`remove:${r}`, {
      position: e,
      length: n
    }, s);
  }
  /**
  * Starts a conversion of an attribute change on a given `range`.
  *
  * For each node in the given `range`, {@link #event:attribute attribute event} is fired with the passed data.
  *
  * @fires attribute
  * @param range Changed range.
  * @param key Key of the attribute that has changed.
  * @param oldValue Attribute value before the change or `null` if the attribute has not been set before.
  * @param newValue New attribute value or `null` if the attribute has been removed.
  * @param conversionApi The conversion API object.
  */
  _convertAttribute(e, n, r, s, o) {
    this._addConsumablesForRange(o.consumable, e, `attribute:${n}`);
    for (const a of e) {
      const l = {
        item: a.item,
        range: D._createFromPositionAndShift(a.previousPosition, a.length),
        attributeKey: n,
        attributeOldValue: r,
        attributeNewValue: s
      };
      this._testAndFire(`attribute:${n}`, l, o);
    }
  }
  /**
  * Fires re-insertion conversion (with a `reconversion` flag passed to `insert` events)
  * of a range of elements (only elements on the range depth, without children).
  *
  * For each node in the range on its depth (without children), {@link #event:insert `insert` event} is fired.
  * For each attribute on each node, {@link #event:attribute `attribute` event} is fired.
  *
  * @fires insert
  * @fires attribute
  * @param range The range to reinsert.
  * @param conversionApi The conversion API object.
  */
  _convertReinsert(e, n) {
    const r = Array.from(e.getWalker({
      shallow: !0
    }));
    this._addConsumablesForInsert(n.consumable, r);
    for (const s of r.map($1))
      this._testAndFire("insert", {
        ...s,
        reconversion: !0
      }, n);
  }
  /**
  * Converts the added marker. Fires the {@link #event:addMarker `addMarker`} event for each item
  * in the marker's range. If the range is collapsed, a single event is dispatched. See the event description for more details.
  *
  * @fires addMarker
  * @param markerName Marker name.
  * @param markerRange The marker range.
  * @param conversionApi The conversion API object.
  */
  _convertMarkerAdd(e, n, r) {
    if (n.root.rootName == "$graveyard")
      return;
    const s = `addMarker:${e}`;
    if (r.consumable.add(n, s), this.fire(s, {
      markerName: e,
      markerRange: n
    }, r), !!r.consumable.consume(n, s)) {
      this._addConsumablesForRange(r.consumable, n, s);
      for (const o of n.getItems()) {
        if (!r.consumable.test(o, s))
          continue;
        const a = {
          item: o,
          range: D._createOn(o),
          markerName: e,
          markerRange: n
        };
        this.fire(s, a, r);
      }
    }
  }
  /**
  * Fires the conversion of the marker removal. Fires the {@link #event:removeMarker `removeMarker`} event with the provided data.
  *
  * @fires removeMarker
  * @param markerName Marker name.
  * @param markerRange The marker range.
  * @param conversionApi The conversion API object.
  */
  _convertMarkerRemove(e, n, r) {
    n.root.rootName != "$graveyard" && this.fire(`removeMarker:${e}`, {
      markerName: e,
      markerRange: n
    }, r);
  }
  /**
  * Fires the reduction of changes buffered in the {@link module:engine/model/differ~Differ `Differ`}.
  *
  * Features can replace selected {@link module:engine/model/differ~DiffItem `DiffItem`}s with `reinsert` entries to trigger
  * reconversion. The {@link module:engine/conversion/downcasthelpers~DowncastHelpers#elementToStructure
  * `DowncastHelpers.elementToStructure()`} is using this event to trigger reconversion.
  *
  * @fires reduceChanges
  */
  _reduceChanges(e) {
    const n = {
      changes: e
    };
    return this.fire("reduceChanges", n), n.changes;
  }
  /**
  * Populates provided {@link module:engine/conversion/modelconsumable~ModelConsumable} with values to consume from a given range,
  * assuming that the range has just been inserted to the model.
  *
  * @param consumable The consumable.
  * @param walkerValues The walker values for the inserted range.
  * @returns The values to consume.
  */
  _addConsumablesForInsert(e, n) {
    for (const r of n) {
      const s = r.item;
      if (e.test(s, "insert") === null) {
        e.add(s, "insert");
        for (const o of s.getAttributeKeys())
          e.add(s, "attribute:" + o);
      }
    }
    return e;
  }
  /**
  * Populates provided {@link module:engine/conversion/modelconsumable~ModelConsumable} with values to consume for a given range.
  *
  * @param consumable The consumable.
  * @param range The affected range.
  * @param type Consumable type.
  * @returns The values to consume.
  */
  _addConsumablesForRange(e, n, r) {
    for (const s of n.getItems())
      e.add(s, r);
    return e;
  }
  /**
  * Populates provided {@link module:engine/conversion/modelconsumable~ModelConsumable} with selection consumable values.
  *
  * @param consumable The consumable.
  * @param selection The selection to create the consumable from.
  * @param markers Markers that contain the selection.
  * @returns The values to consume.
  */
  _addConsumablesForSelection(e, n, r) {
    e.add(n, "selection");
    for (const s of r)
      e.add(n, "addMarker:" + s.name);
    for (const s of n.getAttributeKeys())
      e.add(n, "attribute:" + s);
    return e;
  }
  /**
  * Tests whether given event wasn't already fired and if so, fires it.
  *
  * @fires insert
  * @fires attribute
  * @param type Event type.
  * @param data Event data.
  * @param conversionApi The conversion API object.
  */
  _testAndFire(e, n, r) {
    const s = nO(e, n), o = n.item.is("$textProxy") ? r.consumable._getSymbolForTextProxy(n.item) : n.item, a = this._firedEventsMap.get(r), l = a.get(o);
    if (!l)
      a.set(o, /* @__PURE__ */ new Set([
        s
      ]));
    else if (!l.has(s))
      l.add(s);
    else
      return;
    this.fire(s, n, r);
  }
  /**
  * Fires not already fired events for setting attributes on just inserted item.
  *
  * @param item The model item to convert attributes for.
  * @param conversionApi The conversion API object.
  */
  _testAndFireAddAttributes(e, n) {
    const r = {
      item: e,
      range: D._createOn(e)
    };
    for (const s of r.item.getAttributeKeys())
      r.attributeKey = s, r.attributeOldValue = null, r.attributeNewValue = r.item.getAttribute(s), this._testAndFire(`attribute:${s}`, r, n);
  }
  /**
  * Builds an instance of the {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi} from a template and a given
  * {@link module:engine/view/downcastwriter~DowncastWriter `DowncastWriter`} and options object.
  *
  * @param writer View writer that should be used to modify the view document.
  * @param refreshedItems A set of model elements that should not reuse their
  * previous view representations.
  * @param options Optional options passed to `convertionApi.options`.
  * @return The conversion API object.
  */
  _createConversionApi(e, n = /* @__PURE__ */ new Set(), r = {}) {
    const s = {
      ...this._conversionApi,
      consumable: new eO(),
      writer: e,
      options: r,
      convertItem: (o) => this._convertInsert(D._createOn(o), s),
      convertChildren: (o) => this._convertInsert(D._createIn(o), s, {
        doNotAddConsumables: !0
      }),
      convertAttributes: (o) => this._testAndFireAddAttributes(o, s),
      canReuseView: (o) => !n.has(s.mapper.toModelElement(o))
    };
    return this._firedEventsMap.set(s, /* @__PURE__ */ new Map()), s;
  }
}
function tO(i, t, e) {
  const n = t.getRange(), r = Array.from(i.getAncestors());
  return r.shift(), r.reverse(), !r.some((o) => {
    if (n.containsItem(o))
      return !!e.toViewElement(o).getCustomProperty("addHighlight");
  });
}
function nO(i, t) {
  const e = t.item.is("element") ? t.item.name : "$text";
  return `${i}:${e}`;
}
function $1(i) {
  const t = i.item, e = D._createFromPositionAndShift(i.previousPosition, i.length);
  return {
    item: t,
    range: e
  };
}
class vi extends (/* @__PURE__ */ ht(ys)) {
  /**
  * Creates a new selection instance based on the given {@link module:engine/model/selection~Selectable selectable}
  * or creates an empty selection if no arguments were passed.
  *
  * ```ts
  * // Creates empty selection without ranges.
  * const selection = writer.createSelection();
  *
  * // Creates selection at the given range.
  * const range = writer.createRange( start, end );
  * const selection = writer.createSelection( range );
  *
  * // Creates selection at the given ranges
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( star2, end2 ) ];
  * const selection = writer.createSelection( ranges );
  *
  * // Creates selection from the other selection.
  * // Note: It doesn't copy selection attributes.
  * const otherSelection = writer.createSelection();
  * const selection = writer.createSelection( otherSelection );
  *
  * // Creates selection from the given document selection.
  * // Note: It doesn't copy selection attributes.
  * const documentSelection = model.document.selection;
  * const selection = writer.createSelection( documentSelection );
  *
  * // Creates selection at the given position.
  * const position = writer.createPositionFromPath( root, path );
  * const selection = writer.createSelection( position );
  *
  * // Creates selection at the given offset in the given element.
  * const paragraph = writer.createElement( 'paragraph' );
  * const selection = writer.createSelection( paragraph, offset );
  *
  * // Creates a range inside an {@link module:engine/model/element~Element element} which starts before the
  * // first child of that element and ends after the last child of that element.
  * const selection = writer.createSelection( paragraph, 'in' );
  *
  * // Creates a range on an {@link module:engine/model/item~Item item} which starts before the item and ends
  * // just after the item.
  * const selection = writer.createSelection( paragraph, 'on' );
  * ```
  *
  * Selection's constructor allow passing additional options (`'backward'`) as the last argument.
  *
  * ```ts
  * // Creates backward selection.
  * const selection = writer.createSelection( range, { backward: true } );
  * ```
  *
  * @internal
  */
  constructor(...e) {
    super();
    /**
    * Specifies whether the last added range was added as a backward or forward range.
    */
    h(this, "_lastRangeBackward", !1);
    /**
    * List of attributes set on current selection.
    */
    h(this, "_attrs", /* @__PURE__ */ new Map());
    /** @internal */
    h(this, "_ranges", []);
    e.length && this.setTo(...e);
  }
  /**
  * Selection anchor. Anchor is the position from which the selection was started. If a user is making a selection
  * by dragging the mouse, the anchor is where the user pressed the mouse button (the beginning of the selection).
  *
  * Anchor and {@link #focus} define the direction of the selection, which is important
  * when expanding/shrinking selection. The focus moves, while the anchor should remain in the same place.
  *
  * Anchor is always set to the {@link module:engine/model/range~Range#start start} or
  * {@link module:engine/model/range~Range#end end} position of the last of selection's ranges. Whether it is
  * the `start` or `end` depends on the specified `options.backward`. See the {@link #setTo `setTo()`} method.
  *
  * May be set to `null` if there are no ranges in the selection.
  *
  * @see #focus
  */
  get anchor() {
    if (this._ranges.length > 0) {
      const e = this._ranges[this._ranges.length - 1];
      return this._lastRangeBackward ? e.end : e.start;
    }
    return null;
  }
  /**
  * Selection focus. Focus is the position where the selection ends. If a user is making a selection
  * by dragging the mouse, the focus is where the mouse cursor is.
  *
  * May be set to `null` if there are no ranges in the selection.
  *
  * @see #anchor
  */
  get focus() {
    if (this._ranges.length > 0) {
      const e = this._ranges[this._ranges.length - 1];
      return this._lastRangeBackward ? e.start : e.end;
    }
    return null;
  }
  /**
  * Whether the selection is collapsed. Selection is collapsed when there is exactly one range in it
  * and it is collapsed.
  */
  get isCollapsed() {
    return this._ranges.length === 1 ? this._ranges[0].isCollapsed : !1;
  }
  /**
  * Returns the number of ranges in the selection.
  */
  get rangeCount() {
    return this._ranges.length;
  }
  /**
  * Specifies whether the selection's {@link #focus} precedes the selection's {@link #anchor}.
  */
  get isBackward() {
    return !this.isCollapsed && this._lastRangeBackward;
  }
  /**
  * Checks whether this selection is equal to the given selection. Selections are equal if they have the same directions,
  * the same number of ranges and all ranges from one selection equal to ranges from the another selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are equal, `false` otherwise.
  */
  isEqual(e) {
    if (this.rangeCount != e.rangeCount)
      return !1;
    if (this.rangeCount === 0)
      return !0;
    if (!this.anchor.isEqual(e.anchor) || !this.focus.isEqual(e.focus))
      return !1;
    for (const n of this._ranges) {
      let r = !1;
      for (const s of e._ranges)
        if (n.isEqual(s)) {
          r = !0;
          break;
        }
      if (!r)
        return !1;
    }
    return !0;
  }
  /**
  * Returns an iterable object that iterates over copies of selection ranges.
  */
  *getRanges() {
    for (const e of this._ranges)
      yield new D(e.start, e.end);
  }
  /**
  * Returns a copy of the first range in the selection.
  * First range is the one which {@link module:engine/model/range~Range#start start} position
  * {@link module:engine/model/position~Position#isBefore is before} start position of all other ranges
  * (not to confuse with the first range added to the selection).
  *
  * Returns `null` if there are no ranges in selection.
  */
  getFirstRange() {
    let e = null;
    for (const n of this._ranges)
      (!e || n.start.isBefore(e.start)) && (e = n);
    return e ? new D(e.start, e.end) : null;
  }
  /**
  * Returns a copy of the last range in the selection.
  * Last range is the one which {@link module:engine/model/range~Range#end end} position
  * {@link module:engine/model/position~Position#isAfter is after} end position of all other ranges (not to confuse with the range most
  * recently added to the selection).
  *
  * Returns `null` if there are no ranges in selection.
  */
  getLastRange() {
    let e = null;
    for (const n of this._ranges)
      (!e || n.end.isAfter(e.end)) && (e = n);
    return e ? new D(e.start, e.end) : null;
  }
  /**
  * Returns the first position in the selection.
  * First position is the position that {@link module:engine/model/position~Position#isBefore is before}
  * any other position in the selection.
  *
  * Returns `null` if there are no ranges in selection.
  */
  getFirstPosition() {
    const e = this.getFirstRange();
    return e ? e.start.clone() : null;
  }
  /**
  * Returns the last position in the selection.
  * Last position is the position that {@link module:engine/model/position~Position#isAfter is after}
  * any other position in the selection.
  *
  * Returns `null` if there are no ranges in selection.
  */
  getLastPosition() {
    const e = this.getLastRange();
    return e ? e.end.clone() : null;
  }
  /**
  * Sets this selection's ranges and direction to the specified location based on the given
  * {@link module:engine/model/selection~Selectable selectable}.
  *
  * ```ts
  * // Removes all selection's ranges.
  * selection.setTo( null );
  *
  * // Sets selection to the given range.
  * const range = writer.createRange( start, end );
  * selection.setTo( range );
  *
  * // Sets selection to given ranges.
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( star2, end2 ) ];
  * selection.setTo( ranges );
  *
  * // Sets selection to other selection.
  * // Note: It doesn't copy selection attributes.
  * const otherSelection = writer.createSelection();
  * selection.setTo( otherSelection );
  *
  * // Sets selection to the given document selection.
  * // Note: It doesn't copy selection attributes.
  * const documentSelection = new DocumentSelection( doc );
  * selection.setTo( documentSelection );
  *
  * // Sets collapsed selection at the given position.
  * const position = writer.createPositionFromPath( root, path );
  * selection.setTo( position );
  *
  * // Sets collapsed selection at the position of the given node and an offset.
  * selection.setTo( paragraph, offset );
  * ```
  *
  * Creates a range inside an {@link module:engine/model/element~Element element} which starts before the first child of
   * that element and ends after the last child of that element.
  *
  * ```ts
  * selection.setTo( paragraph, 'in' );
  * ```
  *
  * Creates a range on an {@link module:engine/model/item~Item item} which starts before the item and ends just after the item.
  *
  * ```ts
  * selection.setTo( paragraph, 'on' );
  * ```
  *
  * `Selection#setTo()`' method allow passing additional options (`backward`) as the last argument.
  *
  * ```ts
  * // Sets backward selection.
  * const selection = writer.createSelection( range, { backward: true } );
  * ```
  */
  setTo(...e) {
    let [n, r, s] = e;
    if (typeof r == "object" && (s = r, r = void 0), n === null)
      this._setRanges([]);
    else if (n instanceof vi)
      this._setRanges(n.getRanges(), n.isBackward);
    else if (n && typeof n.getRanges == "function")
      this._setRanges(n.getRanges(), n.isBackward);
    else if (n instanceof D)
      this._setRanges([
        n
      ], !!s && !!s.backward);
    else if (n instanceof G)
      this._setRanges([
        new D(n)
      ]);
    else if (n instanceof no) {
      const o = !!s && !!s.backward;
      let a;
      if (r == "in")
        a = D._createIn(n);
      else if (r == "on")
        a = D._createOn(n);
      else if (r !== void 0)
        a = new D(G._createAt(n, r));
      else
        throw new T("model-selection-setto-required-second-parameter", [
          this,
          n
        ]);
      this._setRanges([
        a
      ], o);
    } else if (ki(n))
      this._setRanges(n, s && !!s.backward);
    else
      throw new T("model-selection-setto-not-selectable", [
        this,
        n
      ]);
  }
  /**
  * Replaces all ranges that were added to the selection with given array of ranges. Last range of the array
  * is treated like the last added range and is used to set {@link module:engine/model/selection~Selection#anchor} and
  * {@link module:engine/model/selection~Selection#focus}. Accepts a flag describing in which direction the selection is made.
  *
  * @fires change:range
  * @param newRanges Ranges to set.
  * @param isLastBackward Flag describing if last added range was selected forward - from start to end (`false`)
  * or backward - from end to start (`true`).
  */
  _setRanges(e, n = !1) {
    const r = Array.from(e), s = r.some((o) => {
      if (!(o instanceof D))
        throw new T("model-selection-set-ranges-not-range", [
          this,
          e
        ]);
      return this._ranges.every((a) => !a.isEqual(o));
    });
    r.length === this._ranges.length && !s || (this._replaceAllRanges(r), this._lastRangeBackward = !!n, this.fire("change:range", {
      directChange: !0
    }));
  }
  /**
  * Moves {@link module:engine/model/selection~Selection#focus} to the specified location.
  *
  * The location can be specified in the same form as
  * {@link module:engine/model/writer~Writer#createPositionAt writer.createPositionAt()} parameters.
  *
  * @fires change:range
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/model/item~Item model item}.
  */
  setFocus(e, n) {
    if (this.anchor === null)
      throw new T("model-selection-setfocus-no-ranges", [
        this,
        e
      ]);
    const r = G._createAt(e, n);
    if (r.compareWith(this.focus) == "same")
      return;
    const s = this.anchor;
    this._ranges.length && this._popRange(), r.compareWith(s) == "before" ? (this._pushRange(new D(r, s)), this._lastRangeBackward = !0) : (this._pushRange(new D(s, r)), this._lastRangeBackward = !1), this.fire("change:range", {
      directChange: !0
    });
  }
  /**
  * Gets an attribute value for given key or `undefined` if that attribute is not set on the selection.
  *
  * @param key Key of attribute to look for.
  * @returns Attribute value or `undefined`.
  */
  getAttribute(e) {
    return this._attrs.get(e);
  }
  /**
  * Returns iterable that iterates over this selection's attributes.
  *
  * Attributes are returned as arrays containing two items. First one is attribute key and second is attribute value.
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  getAttributes() {
    return this._attrs.entries();
  }
  /**
  * Returns iterable that iterates over this selection's attribute keys.
  */
  getAttributeKeys() {
    return this._attrs.keys();
  }
  /**
  * Checks if the selection has an attribute for given key.
  *
  * @param key Key of attribute to check.
  * @returns `true` if attribute with given key is set on selection, `false` otherwise.
  */
  hasAttribute(e) {
    return this._attrs.has(e);
  }
  /**
  * Removes an attribute with given key from the selection.
  *
  * If given attribute was set on the selection, fires the {@link #event:change:range} event with
  * removed attribute key.
  *
  * @fires change:attribute
  * @param key Key of attribute to remove.
  */
  removeAttribute(e) {
    this.hasAttribute(e) && (this._attrs.delete(e), this.fire("change:attribute", {
      attributeKeys: [
        e
      ],
      directChange: !0
    }));
  }
  /**
  * Sets attribute on the selection. If attribute with the same key already is set, it's value is overwritten.
  *
  * If the attribute value has changed, fires the {@link #event:change:range} event with
  * the attribute key.
  *
  * @fires change:attribute
  * @param key Key of attribute to set.
  * @param value Attribute value.
  */
  setAttribute(e, n) {
    this.getAttribute(e) !== n && (this._attrs.set(e, n), this.fire("change:attribute", {
      attributeKeys: [
        e
      ],
      directChange: !0
    }));
  }
  /**
  * Returns the selected element. {@link module:engine/model/element~Element Element} is considered as selected if there is only
  * one range in the selection, and that range contains exactly one element.
  * Returns `null` if there is no selected element.
  */
  getSelectedElement() {
    return this.rangeCount !== 1 ? null : this.getFirstRange().getContainedElement();
  }
  /**
  * Gets elements of type {@link module:engine/model/schema~Schema#isBlock "block"} touched by the selection.
  *
  * This method's result can be used for example to apply block styling to all blocks covered by this selection.
  *
  * **Note:** `getSelectedBlocks()` returns blocks that are nested in other non-block elements
  * but will not return blocks nested in other blocks.
  *
  * In this case the function will return exactly all 3 paragraphs (note: `<blockQuote>` is not a block itself):
  *
  * ```xml
  * <paragraph>[a</paragraph>
  * <blockQuote>
  * 	<paragraph>b</paragraph>
  * </blockQuote>
  * <paragraph>c]d</paragraph>
  * ```
  *
  * In this case the paragraph will also be returned, despite the collapsed selection:
  *
  * ```xml
  * <paragraph>[]a</paragraph>
  * ```
  *
  * In such a scenario, however, only blocks A, B & E will be returned as blocks C & D are nested in block B:
  *
  * ```xml
  * [<blockA></blockA>
  * <blockB>
  * 	<blockC></blockC>
  * 	<blockD></blockD>
  * </blockB>
  * <blockE></blockE>]
  * ```
  *
  * If the selection is inside a block all the inner blocks (A & B) are returned:
  *
  * ```xml
  * <block>
  * 	<blockA>[a</blockA>
  * 	<blockB>b]</blockB>
  * </block>
  * ```
  *
  * **Special case**: Selection ignores first and/or last blocks if nothing (from user perspective) is selected in them.
  *
  * ```xml
  * // Selection ends and the beginning of the last block.
  * <paragraph>[a</paragraph>
  * <paragraph>b</paragraph>
  * <paragraph>]c</paragraph> // This block will not be returned
  *
  * // Selection begins at the end of the first block.
  * <paragraph>a[</paragraph> // This block will not be returned
  * <paragraph>b</paragraph>
  * <paragraph>c]</paragraph>
  *
  * // Selection begings at the end of the first block and ends at the beginning of the last block.
  * <paragraph>a[</paragraph> // This block will not be returned
  * <paragraph>b</paragraph>
  * <paragraph>]c</paragraph> // This block will not be returned
  * ```
  */
  *getSelectedBlocks() {
    const e = /* @__PURE__ */ new WeakSet();
    for (const n of this.getRanges()) {
      const r = z1(n.start, e);
      rO(r, n) && (yield r);
      for (const o of n.getWalker()) {
        const a = o.item;
        o.type == "elementEnd" && iO(a, e, n) && (yield a);
      }
      const s = z1(n.end, e);
      sO(s, n) && (yield s);
    }
  }
  /**
  * Checks whether the selection contains the entire content of the given element. This means that selection must start
  * at a position {@link module:engine/model/position~Position#isTouching touching} the element's start and ends at position
  * touching the element's end.
  *
  * By default, this method will check whether the entire content of the selection's current root is selected.
  * Useful to check if e.g. the user has just pressed <kbd>Ctrl</kbd> + <kbd>A</kbd>.
  */
  containsEntireContent(e = this.anchor.root) {
    const n = G._createAt(e, 0), r = G._createAt(e, "end");
    return n.isTouching(this.getFirstPosition()) && r.isTouching(this.getLastPosition());
  }
  /**
  * Adds given range to internal {@link #_ranges ranges array}. Throws an error
  * if given range is intersecting with any range that is already stored in this selection.
  */
  _pushRange(e) {
    this._checkRange(e), this._ranges.push(new D(e.start, e.end));
  }
  /**
  * Checks if given range intersects with ranges that are already in the selection. Throws an error if it does.
  */
  _checkRange(e) {
    for (let n = 0; n < this._ranges.length; n++)
      if (e.isIntersecting(this._ranges[n]))
        throw new T("model-selection-range-intersects", [
          this,
          e
        ], {
          addedRange: e,
          intersectingRange: this._ranges[n]
        });
  }
  /**
  * Replaces all the ranges by the given ones.
  * Uses {@link #_popRange _popRange} and {@link #_pushRange _pushRange} to ensure proper ranges removal and addition.
  */
  _replaceAllRanges(e) {
    this._removeAllRanges();
    for (const n of e)
      this._pushRange(n);
  }
  /**
  * Deletes ranges from internal range array. Uses {@link #_popRange _popRange} to
  * ensure proper ranges removal.
  */
  _removeAllRanges() {
    for (; this._ranges.length > 0; )
      this._popRange();
  }
  /**
  * Removes most recently added range from the selection.
  */
  _popRange() {
    this._ranges.pop();
  }
}
vi.prototype.is = function(i) {
  return i === "selection" || i === "model:selection";
};
function Z2(i, t) {
  return t.has(i) ? !1 : (t.add(i), i.root.document.model.schema.isBlock(i) && !!i.parent);
}
function iO(i, t, e) {
  return Z2(i, t) && rp(i, e);
}
function z1(i, t) {
  const n = i.parent.root.document.model.schema, r = i.parent.getAncestors({
    parentFirst: !0,
    includeSelf: !0
  });
  let s = !1;
  const o = r.find((a) => s ? !1 : (s = n.isLimit(a), !s && Z2(a, t)));
  return r.forEach((a) => t.add(a)), o;
}
function rp(i, t) {
  const e = oO(i);
  return e ? !t.containsRange(D._createOn(e), !0) : !0;
}
function rO(i, t) {
  return i ? t.isCollapsed || i.isEmpty ? !0 : t.start.isTouching(G._createAt(i, i.maxOffset)) ? !1 : rp(i, t) : !1;
}
function sO(i, t) {
  return i ? t.isCollapsed || i.isEmpty ? !0 : t.end.isTouching(G._createAt(i, 0)) ? !1 : rp(i, t) : !1;
}
function oO(i) {
  const t = i.root.document.model.schema;
  let e = i.parent;
  for (; e; ) {
    if (t.isBlock(e))
      return e;
    e = e.parent;
  }
}
class Kn extends (/* @__PURE__ */ ht(D)) {
  /**
  * Creates a live range.
  *
  * @see module:engine/model/range~Range
  */
  constructor(t, e) {
    super(t, e), aO.call(this);
  }
  /**
  * Unbinds all events previously bound by `LiveRange`. Use it whenever you don't need `LiveRange` instance
  * anymore (i.e. when leaving scope in which it was declared or before re-assigning variable that was
  * referring to it).
  */
  detach() {
    this.stopListening();
  }
  /**
  * Creates a {@link module:engine/model/range~Range range instance} that is equal to this live range.
  */
  toRange() {
    return new D(this.start, this.end);
  }
  /**
  * Creates a `LiveRange` instance that is equal to the given range.
  */
  static fromRange(t) {
    return new Kn(t.start, t.end);
  }
}
Kn.prototype.is = function(i) {
  return i === "liveRange" || i === "model:liveRange" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i == "range" || i === "model:range";
};
function aO() {
  this.listenTo(this.root.document.model, "applyOperation", (i, t) => {
    const e = t[0];
    e.isDocumentOperation && lO.call(this, e);
  }, {
    priority: "low"
  });
}
function lO(i) {
  const t = this.getTransformedByOperation(i), e = D._createFromRanges(t), n = !e.isEqual(this), r = cO(this, i);
  let s = null;
  if (n) {
    e.root.rootName == "$graveyard" && (i.type == "remove" ? s = i.sourcePosition : s = i.deletionPosition);
    const o = this.toRange();
    this.start = e.start, this.end = e.end, this.fire("change:range", o, {
      deletionPosition: s
    });
  } else r && this.fire("change:content", this.toRange(), {
    deletionPosition: s
  });
}
function cO(i, t) {
  switch (t.type) {
    case "insert":
      return i.containsPosition(t.position);
    case "move":
    case "remove":
    case "reinsert":
    case "merge":
      return i.containsPosition(t.sourcePosition) || i.start.isEqual(t.sourcePosition) || i.containsPosition(t.targetPosition);
    case "split":
      return i.containsPosition(t.splitPosition) || i.containsPosition(t.insertionPosition);
  }
  return !1;
}
const Vl = "selection:";
class Xi extends (/* @__PURE__ */ ht(ys)) {
  /**
  * Creates an empty live selection for given {@link module:engine/model/document~Document}.
  *
  * @param doc Document which owns this selection.
  */
  constructor(e) {
    super();
    /**
    * Selection used internally by that class (`DocumentSelection` is a proxy to that selection).
    */
    h(this, "_selection");
    this._selection = new uO(e), this._selection.delegate("change:range").to(this), this._selection.delegate("change:attribute").to(this), this._selection.delegate("change:marker").to(this);
  }
  /**
  * Describes whether the selection is collapsed. Selection is collapsed when there is exactly one range which is
  * collapsed.
  */
  get isCollapsed() {
    return this._selection.isCollapsed;
  }
  /**
  * Selection anchor. Anchor may be described as a position where the most recent part of the selection starts.
  * Together with {@link #focus} they define the direction of selection, which is important
  * when expanding/shrinking selection. Anchor is always {@link module:engine/model/range~Range#start start} or
  * {@link module:engine/model/range~Range#end end} position of the most recently added range.
  *
  * Is set to `null` if there are no ranges in selection.
  *
  * @see #focus
  */
  get anchor() {
    return this._selection.anchor;
  }
  /**
  * Selection focus. Focus is a position where the selection ends.
  *
  * Is set to `null` if there are no ranges in selection.
  *
  * @see #anchor
  */
  get focus() {
    return this._selection.focus;
  }
  /**
  * Number of ranges in selection.
  */
  get rangeCount() {
    return this._selection.rangeCount;
  }
  /**
  * Describes whether `Documentselection` has own range(s) set, or if it is defaulted to
  * {@link module:engine/model/document~Document#_getDefaultRange document's default range}.
  */
  get hasOwnRange() {
    return this._selection.hasOwnRange;
  }
  /**
  * Specifies whether the {@link #focus}
  * precedes {@link #anchor}.
  *
  * @readonly
  * @type {Boolean}
  */
  get isBackward() {
    return this._selection.isBackward;
  }
  /**
  * Describes whether the gravity is overridden (using {@link module:engine/model/writer~Writer#overrideSelectionGravity}) or not.
  *
  * Note that the gravity remains overridden as long as will not be restored the same number of times as it was overridden.
  */
  get isGravityOverridden() {
    return this._selection.isGravityOverridden;
  }
  /**
  * A collection of selection {@link module:engine/model/markercollection~Marker markers}.
  * Marker is a selection marker when selection range is inside the marker range.
  *
  * **Note**: Only markers from {@link ~DocumentSelection#observeMarkers observed markers groups} are collected.
  */
  get markers() {
    return this._selection.markers;
  }
  /**
  * Used for the compatibility with the {@link module:engine/model/selection~Selection#isEqual} method.
  *
  * @internal
  */
  get _ranges() {
    return this._selection._ranges;
  }
  /**
  * Returns an iterable that iterates over copies of selection ranges.
  */
  getRanges() {
    return this._selection.getRanges();
  }
  /**
  * Returns the first position in the selection.
  * First position is the position that {@link module:engine/model/position~Position#isBefore is before}
  * any other position in the selection.
  *
  * Returns `null` if there are no ranges in selection.
  */
  getFirstPosition() {
    return this._selection.getFirstPosition();
  }
  /**
  * Returns the last position in the selection.
  * Last position is the position that {@link module:engine/model/position~Position#isAfter is after}
  * any other position in the selection.
  *
  * Returns `null` if there are no ranges in selection.
  */
  getLastPosition() {
    return this._selection.getLastPosition();
  }
  /**
  * Returns a copy of the first range in the selection.
  * First range is the one which {@link module:engine/model/range~Range#start start} position
  * {@link module:engine/model/position~Position#isBefore is before} start position of all other ranges
  * (not to confuse with the first range added to the selection).
  *
  * Returns `null` if there are no ranges in selection.
  */
  getFirstRange() {
    return this._selection.getFirstRange();
  }
  /**
  * Returns a copy of the last range in the selection.
  * Last range is the one which {@link module:engine/model/range~Range#end end} position
  * {@link module:engine/model/position~Position#isAfter is after} end position of all other ranges (not to confuse with the range most
  * recently added to the selection).
  *
  * Returns `null` if there are no ranges in selection.
  */
  getLastRange() {
    return this._selection.getLastRange();
  }
  /**
  * Gets elements of type {@link module:engine/model/schema~Schema#isBlock "block"} touched by the selection.
  *
  * This method's result can be used for example to apply block styling to all blocks covered by this selection.
  *
  * **Note:** `getSelectedBlocks()` returns blocks that are nested in other non-block elements
  * but will not return blocks nested in other blocks.
  *
  * In this case the function will return exactly all 3 paragraphs (note: `<blockQuote>` is not a block itself):
  *
  * ```
  * <paragraph>[a</paragraph>
  * <blockQuote>
  * 	<paragraph>b</paragraph>
  * </blockQuote>
  * <paragraph>c]d</paragraph>
  * ```
  *
  * In this case the paragraph will also be returned, despite the collapsed selection:
  *
  * ```
  * <paragraph>[]a</paragraph>
  * ```
  *
  * In such a scenario, however, only blocks A, B & E will be returned as blocks C & D are nested in block B:
  *
  * ```
  * [<blockA></blockA>
  * <blockB>
  * 	<blockC></blockC>
  * 	<blockD></blockD>
  * </blockB>
  * <blockE></blockE>]
  * ```
  *
  * If the selection is inside a block all the inner blocks (A & B) are returned:
  *
  * ```
  * <block>
  * 	<blockA>[a</blockA>
  * 	<blockB>b]</blockB>
  * </block>
  * ```
  *
  * **Special case**: If a selection ends at the beginning of a block, that block is not returned as from user perspective
  * this block wasn't selected. See [#984](https://github.com/ckeditor/ckeditor5-engine/issues/984) for more details.
  *
  * ```
  * <paragraph>[a</paragraph>
  * <paragraph>b</paragraph>
  * <paragraph>]c</paragraph> // this block will not be returned
  * ```
  */
  getSelectedBlocks() {
    return this._selection.getSelectedBlocks();
  }
  /**
  * Returns the selected element. {@link module:engine/model/element~Element Element} is considered as selected if there is only
  * one range in the selection, and that range contains exactly one element.
  * Returns `null` if there is no selected element.
  */
  getSelectedElement() {
    return this._selection.getSelectedElement();
  }
  /**
  * Checks whether the selection contains the entire content of the given element. This means that selection must start
  * at a position {@link module:engine/model/position~Position#isTouching touching} the element's start and ends at position
  * touching the element's end.
  *
  * By default, this method will check whether the entire content of the selection's current root is selected.
  * Useful to check if e.g. the user has just pressed <kbd>Ctrl</kbd> + <kbd>A</kbd>.
  */
  containsEntireContent(e) {
    return this._selection.containsEntireContent(e);
  }
  /**
  * Unbinds all events previously bound by document selection.
  */
  destroy() {
    this._selection.destroy();
  }
  /**
  * Returns iterable that iterates over this selection's attribute keys.
  */
  getAttributeKeys() {
    return this._selection.getAttributeKeys();
  }
  /**
  * Returns iterable that iterates over this selection's attributes.
  *
  * Attributes are returned as arrays containing two items. First one is attribute key and second is attribute value.
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  getAttributes() {
    return this._selection.getAttributes();
  }
  /**
  * Gets an attribute value for given key or `undefined` if that attribute is not set on the selection.
  *
  * @param key Key of attribute to look for.
  * @returns Attribute value or `undefined`.
  */
  getAttribute(e) {
    return this._selection.getAttribute(e);
  }
  /**
  * Checks if the selection has an attribute for given key.
  *
  * @param key Key of attribute to check.
  * @returns `true` if attribute with given key is set on selection, `false` otherwise.
  */
  hasAttribute(e) {
    return this._selection.hasAttribute(e);
  }
  /**
  * Refreshes selection attributes and markers according to the current position in the model.
  */
  refresh() {
    this._selection.updateMarkers(), this._selection._updateAttributes(!1);
  }
  /**
  * Registers a marker group prefix or a marker name to be collected in the
  * {@link ~DocumentSelection#markers selection markers collection}.
  *
  * See also {@link module:engine/model/markercollection~MarkerCollection#getMarkersGroup `MarkerCollection#getMarkersGroup()`}.
  *
  * @param prefixOrName The marker group prefix or marker name.
  */
  observeMarkers(e) {
    this._selection.observeMarkers(e);
  }
  /**
  * Moves {@link module:engine/model/documentselection~DocumentSelection#focus} to the specified location.
  * Should be used only within the {@link module:engine/model/writer~Writer#setSelectionFocus} method.
  *
  * The location can be specified in the same form as
  * {@link module:engine/model/writer~Writer#createPositionAt writer.createPositionAt()} parameters.
  *
  * @see module:engine/model/writer~Writer#setSelectionFocus
  * @internal
  * @param offset Offset or one of the flags. Used only when
  * first parameter is a {@link module:engine/model/item~Item model item}.
  */
  _setFocus(e, n) {
    this._selection.setFocus(e, n);
  }
  /**
  * Sets this selection's ranges and direction to the specified location based on the given
  * {@link module:engine/model/selection~Selectable selectable}.
  * Should be used only within the {@link module:engine/model/writer~Writer#setSelection} method.
  *
  * @see module:engine/model/writer~Writer#setSelection
  * @internal
  */
  _setTo(...e) {
    this._selection.setTo(...e);
  }
  /**
  * Sets attribute on the selection. If attribute with the same key already is set, it's value is overwritten.
  * Should be used only within the {@link module:engine/model/writer~Writer#setSelectionAttribute} method.
  *
  * @see module:engine/model/writer~Writer#setSelectionAttribute
  * @internal
  * @param key Key of the attribute to set.
  * @param value Attribute value.
  */
  _setAttribute(e, n) {
    this._selection.setAttribute(e, n);
  }
  /**
  * Removes an attribute with given key from the selection.
  * If the given attribute was set on the selection, fires the {@link module:engine/model/selection~Selection#event:change:range}
  * event with removed attribute key.
  * Should be used only within the {@link module:engine/model/writer~Writer#removeSelectionAttribute} method.
  *
  * @see module:engine/model/writer~Writer#removeSelectionAttribute
  * @internal
  * @param key Key of the attribute to remove.
  */
  _removeAttribute(e) {
    this._selection.removeAttribute(e);
  }
  /**
  * Returns an iterable that iterates through all selection attributes stored in current selection's parent.
  *
  * @internal
  */
  _getStoredAttributes() {
    return this._selection.getStoredAttributes();
  }
  /**
  * Temporarily changes the gravity of the selection from the left to the right.
  *
  * The gravity defines from which direction the selection inherits its attributes. If it's the default left
  * gravity, the selection (after being moved by the the user) inherits attributes from its left hand side.
  * This method allows to temporarily override this behavior by forcing the gravity to the right.
  *
  * It returns an unique identifier which is required to restore the gravity. It guarantees the symmetry
  * of the process.
  *
  * @see module:engine/model/writer~Writer#overrideSelectionGravity
  * @internal
  * @returns The unique id which allows restoring the gravity.
  */
  _overrideGravity() {
    return this._selection.overrideGravity();
  }
  /**
  * Restores the {@link ~DocumentSelection#_overrideGravity overridden gravity}.
  *
  * Restoring the gravity is only possible using the unique identifier returned by
  * {@link ~DocumentSelection#_overrideGravity}. Note that the gravity remains overridden as long as won't be restored
  * the same number of times it was overridden.
  *
  * @see module:engine/model/writer~Writer#restoreSelectionGravity
  * @internal
  * @param uid The unique id returned by {@link #_overrideGravity}.
  */
  _restoreGravity(e) {
    this._selection.restoreGravity(e);
  }
  /**
  * Generates and returns an attribute key for selection attributes store, basing on original attribute key.
  *
  * @internal
  * @param key Attribute key to convert.
  * @returns Converted attribute key, applicable for selection store.
  */
  static _getStoreAttributeKey(e) {
    return Vl + e;
  }
  /**
  * Checks whether the given attribute key is an attribute stored on an element.
  *
  * @internal
  */
  static _isStoreAttributeKey(e) {
    return e.startsWith(Vl);
  }
}
Xi.prototype.is = function(i) {
  return i === "selection" || i == "model:selection" || i == "documentSelection" || i == "model:documentSelection";
};
class uO extends vi {
  /**
  * Creates an empty live selection for given {@link module:engine/model/document~Document}.
  *
  * @param doc Document which owns this selection.
  */
  constructor(e) {
    super();
    /**
    * List of selection markers.
    * Marker is a selection marker when selection range is inside the marker range.
    */
    h(this, "markers", new Pn({
      idProperty: "name"
    }));
    /**
    * Document which owns this selection.
    */
    h(this, "_model");
    /**
    * Document which owns this selection.
    */
    h(this, "_document");
    /**
    * Keeps mapping of attribute name to priority with which the attribute got modified (added/changed/removed)
    * last time. Possible values of priority are: `'low'` and `'normal'`.
    *
    * Priorities are used by internal `LiveSelection` mechanisms. All attributes set using `LiveSelection`
    * attributes API are set with `'normal'` priority.
    */
    h(this, "_attributePriority", /* @__PURE__ */ new Map());
    /**
    * Position to which the selection should be set if the last selection range was moved to the graveyard.
    */
    h(this, "_selectionRestorePosition", null);
    /**
    * Flag that informs whether the selection ranges have changed. It is changed on true when `LiveRange#change:range` event is fired.
    */
    h(this, "_hasChangedRange", !1);
    /**
    * Each overriding gravity adds an UID to the set and each removal removes it.
    * Gravity is overridden when there's at least one UID in the set.
    * Gravity is restored when the set is empty.
    * This is to prevent conflicts when gravity is overridden by more than one feature at the same time.
    */
    h(this, "_overriddenGravityRegister", /* @__PURE__ */ new Set());
    /**
    * Prefixes of marker names that should affect `LiveSelection#markers` collection.
    */
    h(this, "_observedMarkers", /* @__PURE__ */ new Set());
    this._model = e.model, this._document = e, this.listenTo(this._model, "applyOperation", (n, r) => {
      const s = r[0];
      !s.isDocumentOperation || s.type == "marker" || s.type == "rename" || s.type == "noop" || (this._ranges.length == 0 && this._selectionRestorePosition && this._fixGraveyardSelection(this._selectionRestorePosition), this._selectionRestorePosition = null, this._hasChangedRange && (this._hasChangedRange = !1, this.fire("change:range", {
        directChange: !1
      })));
    }, {
      priority: "lowest"
    }), this.on("change:range", () => {
      this._validateSelectionRanges(this.getRanges());
    }), this.listenTo(this._model.markers, "update", (n, r, s, o) => {
      this._updateMarker(r, o);
    }), this.listenTo(this._document, "change", (n, r) => {
      dO(this._model, r);
    });
  }
  get isCollapsed() {
    return this._ranges.length === 0 ? this._document._getDefaultRange().isCollapsed : super.isCollapsed;
  }
  get anchor() {
    return super.anchor || this._document._getDefaultRange().start;
  }
  get focus() {
    return super.focus || this._document._getDefaultRange().end;
  }
  get rangeCount() {
    return this._ranges.length ? this._ranges.length : 1;
  }
  /**
  * Describes whether `LiveSelection` has own range(s) set, or if it is defaulted to
  * {@link module:engine/model/document~Document#_getDefaultRange document's default range}.
  */
  get hasOwnRange() {
    return this._ranges.length > 0;
  }
  /**
  * When set to `true` then selection attributes on node before the caret won't be taken
  * into consideration while updating selection attributes.
  */
  get isGravityOverridden() {
    return !!this._overriddenGravityRegister.size;
  }
  /**
  * Unbinds all events previously bound by live selection.
  */
  destroy() {
    for (let e = 0; e < this._ranges.length; e++)
      this._ranges[e].detach();
    this.stopListening();
  }
  *getRanges() {
    this._ranges.length ? yield* super.getRanges() : yield this._document._getDefaultRange();
  }
  getFirstRange() {
    return super.getFirstRange() || this._document._getDefaultRange();
  }
  getLastRange() {
    return super.getLastRange() || this._document._getDefaultRange();
  }
  setTo(...e) {
    super.setTo(...e), this._updateAttributes(!0), this.updateMarkers();
  }
  setFocus(e, n) {
    super.setFocus(e, n), this._updateAttributes(!0), this.updateMarkers();
  }
  setAttribute(e, n) {
    if (this._setAttribute(e, n)) {
      const r = [
        e
      ];
      this.fire("change:attribute", {
        attributeKeys: r,
        directChange: !0
      });
    }
  }
  removeAttribute(e) {
    if (this._removeAttribute(e)) {
      const n = [
        e
      ];
      this.fire("change:attribute", {
        attributeKeys: n,
        directChange: !0
      });
    }
  }
  overrideGravity() {
    const e = Jn();
    return this._overriddenGravityRegister.add(e), this._overriddenGravityRegister.size === 1 && this._updateAttributes(!0), e;
  }
  restoreGravity(e) {
    if (!this._overriddenGravityRegister.has(e))
      throw new T("document-selection-gravity-wrong-restore", this, {
        uid: e
      });
    this._overriddenGravityRegister.delete(e), this.isGravityOverridden || this._updateAttributes(!0);
  }
  observeMarkers(e) {
    this._observedMarkers.add(e), this.updateMarkers();
  }
  _replaceAllRanges(e) {
    this._validateSelectionRanges(e), super._replaceAllRanges(e);
  }
  _popRange() {
    this._ranges.pop().detach();
  }
  _pushRange(e) {
    const n = this._prepareRange(e);
    n && this._ranges.push(n);
  }
  _validateSelectionRanges(e) {
    for (const n of e)
      if (!this._document._validateSelectionRange(n))
        throw new T("document-selection-wrong-position", this, {
          range: n
        });
  }
  /**
  * Prepares given range to be added to selection. Checks if it is correct,
  * converts it to {@link module:engine/model/liverange~LiveRange LiveRange}
  * and sets listeners listening to the range's change event.
  */
  _prepareRange(e) {
    if (this._checkRange(e), e.root == this._document.graveyard)
      return;
    const n = Kn.fromRange(e);
    return n.on("change:range", (r, s, o) => {
      if (this._hasChangedRange = !0, n.root == this._document.graveyard) {
        this._selectionRestorePosition = o.deletionPosition;
        const a = this._ranges.indexOf(n);
        this._ranges.splice(a, 1), n.detach();
      }
    }), n;
  }
  updateMarkers() {
    if (!this._observedMarkers.size)
      return;
    const e = [];
    let n = !1;
    for (const s of this._model.markers) {
      const o = s.name.split(":", 1)[0];
      if (!this._observedMarkers.has(o))
        continue;
      const a = s.getRange();
      for (const l of this.getRanges())
        a.containsRange(l, !l.isCollapsed) && e.push(s);
    }
    const r = Array.from(this.markers);
    for (const s of e)
      this.markers.has(s) || (this.markers.add(s), n = !0);
    for (const s of Array.from(this.markers))
      e.includes(s) || (this.markers.remove(s), n = !0);
    n && this.fire("change:marker", {
      oldMarkers: r,
      directChange: !1
    });
  }
  _updateMarker(e, n) {
    const r = e.name.split(":", 1)[0];
    if (!this._observedMarkers.has(r))
      return;
    let s = !1;
    const o = Array.from(this.markers), a = this.markers.has(e);
    if (!n)
      a && (this.markers.remove(e), s = !0);
    else {
      let l = !1;
      for (const c of this.getRanges())
        if (n.containsRange(c, !c.isCollapsed)) {
          l = !0;
          break;
        }
      l && !a ? (this.markers.add(e), s = !0) : !l && a && (this.markers.remove(e), s = !0);
    }
    s && this.fire("change:marker", {
      oldMarkers: o,
      directChange: !1
    });
  }
  /**
  * Updates this selection attributes according to its ranges and the {@link module:engine/model/document~Document model document}.
  */
  _updateAttributes(e) {
    const n = gr(this._getSurroundingAttributes()), r = gr(this.getAttributes());
    if (e)
      this._attributePriority = /* @__PURE__ */ new Map(), this._attrs = /* @__PURE__ */ new Map();
    else
      for (const [o, a] of this._attributePriority)
        a == "low" && (this._attrs.delete(o), this._attributePriority.delete(o));
    this._setAttributesTo(n);
    const s = [];
    for (const [o, a] of this.getAttributes())
      (!r.has(o) || r.get(o) !== a) && s.push(o);
    for (const [o] of r)
      this.hasAttribute(o) || s.push(o);
    s.length > 0 && this.fire("change:attribute", {
      attributeKeys: s,
      directChange: !1
    });
  }
  /**
  * Internal method for setting `LiveSelection` attribute. Supports attribute priorities (through `directChange`
  * parameter).
  */
  _setAttribute(e, n, r = !0) {
    const s = r ? "normal" : "low";
    return s == "low" && this._attributePriority.get(e) == "normal" || super.getAttribute(e) === n ? !1 : (this._attrs.set(e, n), this._attributePriority.set(e, s), !0);
  }
  /**
  * Internal method for removing `LiveSelection` attribute. Supports attribute priorities (through `directChange`
  * parameter).
  *
  * NOTE: Even if attribute is not present in the selection but is provided to this method, it's priority will
  * be changed according to `directChange` parameter.
  */
  _removeAttribute(e, n = !0) {
    const r = n ? "normal" : "low";
    return r == "low" && this._attributePriority.get(e) == "normal" || (this._attributePriority.set(e, r), !super.hasAttribute(e)) ? !1 : (this._attrs.delete(e), !0);
  }
  /**
  * Internal method for setting multiple `LiveSelection` attributes. Supports attribute priorities (through
  * `directChange` parameter).
  */
  _setAttributesTo(e) {
    const n = /* @__PURE__ */ new Set();
    for (const [r, s] of this.getAttributes())
      e.get(r) !== s && this._removeAttribute(r, !1);
    for (const [r, s] of e)
      this._setAttribute(r, s, !1) && n.add(r);
    return n;
  }
  /**
  * Returns an iterable that iterates through all selection attributes stored in current selection's parent.
  */
  *getStoredAttributes() {
    const e = this.getFirstPosition().parent;
    if (this.isCollapsed && e.isEmpty)
      for (const n of e.getAttributeKeys())
        n.startsWith(Vl) && (yield [
          n.substr(Vl.length),
          e.getAttribute(n)
        ]);
  }
  /**
  * Checks model text nodes that are closest to the selection's first position and returns attributes of first
  * found element. If there are no text nodes in selection's first position parent, it returns selection
  * attributes stored in that parent.
  */
  _getSurroundingAttributes() {
    const e = this.getFirstPosition(), n = this._model.schema;
    if (e.root.rootName == "$graveyard")
      return null;
    let r = null;
    if (this.isCollapsed) {
      const s = e.textNode ? e.textNode : e.nodeBefore, o = e.textNode ? e.textNode : e.nodeAfter;
      if (this.isGravityOverridden || (r = Ba(s, n)), r || (r = Ba(o, n)), !this.isGravityOverridden && !r) {
        let a = s;
        for (; a && !r; )
          a = a.previousSibling, r = Ba(a, n);
      }
      if (!r) {
        let a = o;
        for (; a && !r; )
          a = a.nextSibling, r = Ba(a, n);
      }
      r || (r = this.getStoredAttributes());
    } else {
      const s = this.getFirstRange();
      for (const o of s) {
        if (o.item.is("element") && n.isObject(o.item)) {
          r = Ba(o.item, n);
          break;
        }
        if (o.type == "text") {
          r = o.item.getAttributes();
          break;
        }
      }
    }
    return r;
  }
  /**
  * Fixes the selection after all its ranges got removed.
  * @param deletionPosition Position where the deletion happened.
  */
  _fixGraveyardSelection(e) {
    const n = this._model.schema.getNearestSelectionRange(e);
    n && this._pushRange(n);
  }
}
function Ba(i, t) {
  if (!i)
    return null;
  if (i instanceof Gi || i instanceof mt)
    return i.getAttributes();
  if (!t.isInline(i))
    return null;
  if (!t.isObject(i))
    return [];
  const e = [];
  for (const [n, r] of i.getAttributes())
    t.checkAttribute("$text", n) && t.getAttributeProperties(n).copyFromObject !== !1 && e.push([
      n,
      r
    ]);
  return e;
}
function dO(i, t) {
  const e = i.document.differ;
  for (const n of e.getChanges()) {
    if (n.type != "insert")
      continue;
    const r = n.position.parent;
    n.length === r.maxOffset && i.enqueueChange(t, (o) => {
      const a = Array.from(r.getAttributeKeys()).filter((l) => l.startsWith(Vl));
      for (const l of a)
        o.removeAttribute(l, r);
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class Y2 {
  /**
  * Creates a conversion helpers instance.
  */
  constructor(t) {
    h(this, "_dispatchers");
    this._dispatchers = t;
  }
  /**
  * Registers a conversion helper.
  *
  * **Note**: See full usage example in the `{@link module:engine/conversion/conversion~Conversion#for conversion.for()}`
  * method description.
  *
  * @param conversionHelper The function to be called on event.
  */
  add(t) {
    for (const e of this._dispatchers)
      t(e);
    return this;
  }
}
class hO extends Y2 {
  /**
  * Model element to view element conversion helper.
  *
  * This conversion results in creating a view element. For example, model `<paragraph>Foo</paragraph>` becomes `<p>Foo</p>` in the view.
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: 'paragraph',
  * 	view: 'p'
  * } );
  *
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: 'paragraph',
  * 	view: 'div',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: 'fancyParagraph',
  * 	view: {
  * 		name: 'p',
  * 		classes: 'fancy'
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: 'heading',
  * 	view: ( modelElement, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createContainerElement( 'h' + modelElement.getAttribute( 'level' ) );
  * 	}
  * } );
  * ```
  *
  * The element-to-element conversion supports the reconversion mechanism. It can be enabled by using either the `attributes` or
  * the `children` props on a model description. You will find a couple examples below.
  *
  * In order to reconvert an element if any of its direct children have been added or removed, use the `children` property on a `model`
  * description. For example, this model:
  *
  * ```xml
  * <box>
  * 	<paragraph>Some text.</paragraph>
  * </box>
  * ```
  *
  * will be converted into this structure in the view:
  *
  * ```html
  * <div class="box" data-type="single">
  * 	<p>Some text.</p>
  * </div>
  * ```
  *
  * But if more items were inserted in the model:
  *
  * ```xml
  * <box>
  * 	<paragraph>Some text.</paragraph>
  * 	<paragraph>Other item.</paragraph>
  * </box>
  * ```
  *
  * it will be converted into this structure in the view (note the element `data-type` change):
  *
  * ```html
  * <div class="box" data-type="multiple">
  * 	<p>Some text.</p>
  * 	<p>Other item.</p>
  * </div>
  * ```
  *
  * Such a converter would look like this (note that the `paragraph` elements are converted separately):
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: {
  * 		name: 'box',
  * 		children: true
  * 	},
  * 	view: ( modelElement, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createContainerElement( 'div', {
  * 			class: 'box',
  * 			'data-type': modelElement.childCount == 1 ? 'single' : 'multiple'
  * 		} );
  * 	}
  * } );
  * ```
  *
  * In order to reconvert element if any of its attributes have been updated, use the `attributes` property on a `model`
  * description. For example, this model:
  *
  * ```xml
  * <heading level="2">Some text.</heading>
  * ```
  *
  * will be converted into this structure in the view:
  *
  * ```html
  * <h2>Some text.</h2>
  * ```
  *
  * But if the `heading` element's `level` attribute has been updated to `3` for example, then
  * it will be converted into this structure in the view:
  *
  * ```html
  * <h3>Some text.</h3>
  * ```
  *
  * Such a converter would look as follows:
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: {
  * 		name: 'heading',
  * 		attributes: 'level'
  * 	},
  * 	view: ( modelElement, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createContainerElement( 'h' + modelElement.getAttribute( 'level' ) );
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * You can read more about the element-to-element conversion in the
  * {@glink framework/deep-dive/conversion/downcast downcast conversion} guide.
  *
  * @param config Conversion configuration.
  * @param config.model The description or a name of the model element to convert.
  * @param config.model.attributes The list of attribute names that should be consumed while creating
  * the view element. Note that the view will be reconverted if any of the listed attributes changes.
   * @param config.model.children Specifies whether the view element requires reconversion if the list
  * of the model child nodes changed.
  * @param config.view A view element definition or a function that takes the model element and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API}
  * as parameters and returns a view container element.
  */
  elementToElement(t) {
    return this.add(IO(t));
  }
  /**
  * The model element to view structure (several elements) conversion helper.
  *
  * This conversion results in creating a view structure with one or more slots defined for the child nodes.
  * For example, a model `<table>` may become this structure in the view:
  *
  * ```html
  * <figure class="table">
  * 	<table>
  * 		<tbody>${ slot for table rows }</tbody>
  * 	</table>
  * </figure>
  * ```
  *
  * The children of the model's `<table>` element will be inserted into the `<tbody>` element.
  * If the `elementToElement()` helper was used, the children would be inserted into the `<figure>`.
  *
  * Imagine a table feature where for this model structure:
  *
  * ```xml
  * <table headingRows="1">
  * 	<tableRow> ... table cells 1 ... </tableRow>
  * 	<tableRow> ... table cells 2 ... </tableRow>
  * 	<tableRow> ... table cells 3 ... </tableRow>
  * 	<caption>Caption text</caption>
  * </table>
  * ```
  *
  * we want to generate this view structure:
  *
  * ```html
  * <figure class="table">
  * 	<table>
  * 		<thead>
  * 			<tr> ... table cells 1 ... </tr>
  * 		</thead>
  * 		<tbody>
  * 			<tr> ... table cells 2 ... </tr>
  * 			<tr> ... table cells 3 ... </tr>
  * 		</tbody>
  * 	</table>
  * 	<figcaption>Caption text</figcaption>
  * </figure>
  * ```
  *
  * The converter has to take the `headingRows` attribute into consideration when allocating the `<tableRow>` elements
  * into the `<tbody>` and `<thead>` elements. Hence, we need two slots and need to define proper filter callbacks for them.
  *
  * Additionally, all elements other than `<tableRow>` should be placed outside the `<table>` tag.
  * In the example above, this will handle the table caption.
  *
  * Such a converter would look like this:
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).elementToStructure( {
  * 	model: {
  * 		name: 'table',
  * 		attributes: [ 'headingRows' ]
  * 	},
  * 	view: ( modelElement, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		const figureElement = writer.createContainerElement( 'figure', { class: 'table' } );
  * 		const tableElement = writer.createContainerElement( 'table' );
  *
  * 		writer.insert( writer.createPositionAt( figureElement, 0 ), tableElement );
  *
  * 		const headingRows = modelElement.getAttribute( 'headingRows' ) || 0;
  *
  * 		if ( headingRows > 0 ) {
  * 			const tableHead = writer.createContainerElement( 'thead' );
  *
  * 			const headSlot = writer.createSlot( node => node.is( 'element', 'tableRow' ) && node.index < headingRows );
  *
  * 			writer.insert( writer.createPositionAt( tableElement, 'end' ), tableHead );
  * 			writer.insert( writer.createPositionAt( tableHead, 0 ), headSlot );
  * 		}
  *
  * 		if ( headingRows < tableUtils.getRows( table ) ) {
  * 			const tableBody = writer.createContainerElement( 'tbody' );
  *
  * 			const bodySlot = writer.createSlot( node => node.is( 'element', 'tableRow' ) && node.index >= headingRows );
  *
  * 			writer.insert( writer.createPositionAt( tableElement, 'end' ), tableBody );
  * 			writer.insert( writer.createPositionAt( tableBody, 0 ), bodySlot );
  * 		}
  *
  * 		const restSlot = writer.createSlot( node => !node.is( 'element', 'tableRow' ) );
  *
  * 		writer.insert( writer.createPositionAt( figureElement, 'end' ), restSlot );
  *
  * 		return figureElement;
  * 	}
  * } );
  * ```
  *
  * Note: The children of a model element that's being converted must be allocated in the same order in the view
  * in which they are placed in the model.
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
   * @param config.model The description or a name of the model element to convert.
  * @param config.model.name The name of the model element to convert.
   * @param config.model.attributes The list of attribute names that should be consumed while creating
  * the view structure. Note that the view will be reconverted if any of the listed attributes will change.
  * @param config.view A function that takes the model element and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as parameters
  * and returns a view container element with slots for model child nodes to be converted into.
  */
  elementToStructure(t) {
    return this.add(RO(t));
  }
  /**
  * Model attribute to view element conversion helper.
  *
  * This conversion results in wrapping view nodes with a view attribute element. For example, a model text node with
  * `"Foo"` as data and the `bold` attribute becomes `<strong>Foo</strong>` in the view.
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: 'bold',
  * 	view: 'strong'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: 'bold',
  * 	view: 'b',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: 'invert',
  * 	view: {
  * 		name: 'span',
  * 		classes: [ 'font-light', 'bg-dark' ]
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: {
  * 		key: 'fontSize',
  * 		values: [ 'big', 'small' ]
  * 	},
  * 	view: {
  * 		big: {
  * 			name: 'span',
  * 			styles: {
  * 				'font-size': '1.2em'
  * 			}
  * 		},
  * 		small: {
  * 			name: 'span',
  * 			styles: {
  * 				'font-size': '0.8em'
  * 			}
  * 		}
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: 'bold',
  * 	view: ( modelAttributeValue, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createAttributeElement( 'span', {
  * 			style: 'font-weight:' + modelAttributeValue
  * 		} );
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: {
  * 		key: 'color',
  * 		name: '$text'
  * 	},
  * 	view: ( modelAttributeValue, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createAttributeElement( 'span', {
  * 			style: 'color:' + modelAttributeValue
  * 		} );
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The key of the attribute to convert from or a `{ key, values }` object. `values` is an array
  * of `String`s with possible values if the model attribute is an enumerable.
  * @param config.view A view element definition or a function
  * that takes the model attribute value and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as parameters and returns a view
  * attribute element. If `config.model.values` is given, `config.view` should be an object assigning values from `config.model.values`
  * to view element definitions or functions.
  * @param config.converterPriority Converter priority.
  */
  attributeToElement(t) {
    return this.add(OO(t));
  }
  /**
  * Model attribute to view attribute conversion helper.
  *
  * This conversion results in adding an attribute to a view node, basing on an attribute from a model node. For example,
  * `<imageInline src='foo.jpg'></imageInline>` is converted to `<img src='foo.jpg'></img>`.
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: 'source',
  * 	view: 'src'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: 'source',
  * 	view: 'href',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: {
  * 		name: 'imageInline',
  * 		key: 'source'
  * 	},
  * 	view: 'src'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: {
  * 		name: 'styled',
  * 		values: [ 'dark', 'light' ]
  * 	},
  * 	view: {
  * 		dark: {
  * 			key: 'class',
  * 			value: [ 'styled', 'styled-dark' ]
  * 		},
  * 		light: {
  * 			key: 'class',
  * 			value: [ 'styled', 'styled-light' ]
  * 		}
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: 'styled',
  * 	view: modelAttributeValue => ( {
  * 		key: 'class',
  * 		value: 'styled-' + modelAttributeValue
  * 	} )
  * } );
  * ```
  *
  * **Note**: Downcasting to a style property requires providing `value` as an object:
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: 'lineHeight',
  * 	view: modelAttributeValue => ( {
  * 		key: 'style',
  * 		value: {
  * 			'line-height': modelAttributeValue,
  * 			'border-bottom': '1px dotted #ba2'
  * 		}
  * 	} )
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The key of the attribute to convert from or a `{ key, values, [ name ] }` object describing
  * the attribute key, possible values and, optionally, an element name to convert from.
  * @param config.view A view attribute key, or a `{ key, value }` object or a function that takes the model attribute value and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API}
  * as parameters and returns a `{ key, value }` object. If the `key` is `'class'`, the `value` can be a `String` or an
  * array of `String`s. If the `key` is `'style'`, the `value` is an object with key-value pairs. In other cases, `value` is a `String`.
  * If `config.model.values` is set, `config.view` should be an object assigning values from `config.model.values` to
  * `{ key, value }` objects or a functions.
  * @param config.converterPriority Converter priority.
  */
  attributeToAttribute(t) {
    return this.add(VO(t));
  }
  /**
  * Model marker to view element conversion helper.
  *
  * **Note**: This method should be used mainly for editing the downcast and it is recommended
  * to use the {@link #markerToData `#markerToData()`} helper instead.
  *
  * This helper may produce invalid HTML code (e.g. a span between table cells).
  * It should only be used when you are sure that the produced HTML will be semantically correct.
  *
  * This conversion results in creating a view element on the boundaries of the converted marker. If the converted marker
  * is collapsed, only one element is created. For example, a model marker set like this: `<paragraph>F[oo b]ar</paragraph>`
  * becomes `<p>F<span data-marker="search"></span>oo b<span data-marker="search"></span>ar</p>` in the view.
  *
  * ```ts
  * editor.conversion.for( 'editingDowncast' ).markerToElement( {
  * 	model: 'search',
  * 	view: 'marker-search'
  * } );
  *
  * editor.conversion.for( 'editingDowncast' ).markerToElement( {
  * 	model: 'search',
  * 	view: 'search-result',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'editingDowncast' ).markerToElement( {
  * 	model: 'search',
  * 	view: {
  * 		name: 'span',
  * 		attributes: {
  * 			'data-marker': 'search'
  * 		}
  * 	}
  * } );
  *
  * editor.conversion.for( 'editingDowncast' ).markerToElement( {
  * 	model: 'search',
  * 	view: ( markerData, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createUIElement( 'span', {
  * 			'data-marker': 'search',
  * 			'data-start': markerData.isOpening
  * 		} );
  * 	}
  * } );
  * ```
  *
  * If a function is passed as the `config.view` parameter, it will be used to generate both boundary elements. The function
  * receives the `data` object and {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API}
  * as a parameters and should return an instance of the
  * {@link module:engine/view/uielement~UIElement view UI element}. The `data` object and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi `conversionApi`} are passed from
  * {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher#event:addMarker}. Additionally,
  * the `data.isOpening` parameter is passed, which is set to `true` for the marker start boundary element, and `false` for
  * the marker end boundary element.
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The name of the model marker (or model marker group) to convert.
  * @param config.view A view element definition or a function that takes the model marker data and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as a parameters
  * and returns a view UI element.
  * @param config.converterPriority Converter priority.
  */
  markerToElement(t) {
    return this.add(MO(t));
  }
  /**
  * Model marker to highlight conversion helper.
  *
  * This conversion results in creating a highlight on view nodes. For this kind of conversion,
  * the {@link module:engine/conversion/downcasthelpers~HighlightDescriptor} should be provided.
  *
  * For text nodes, a `<span>` {@link module:engine/view/attributeelement~AttributeElement} is created and it wraps all text nodes
  * in the converted marker range. For example, a model marker set like this: `<paragraph>F[oo b]ar</paragraph>` becomes
  * `<p>F<span class="comment">oo b</span>ar</p>` in the view.
  *
  * {@link module:engine/view/containerelement~ContainerElement} may provide a custom way of handling highlight. Most often,
  * the element itself is given classes and attributes described in the highlight descriptor (instead of being wrapped in `<span>`).
  * For example, a model marker set like this:
  * `[<imageInline src="foo.jpg"></imageInline>]` becomes `<img src="foo.jpg" class="comment"></img>` in the view.
  *
  * For container elements, the conversion is two-step. While the converter processes the highlight descriptor and passes it
  * to a container element, it is the container element instance itself that applies values from the highlight descriptor.
  * So, in a sense, the converter takes care of stating what should be applied on what, while the element decides how to apply that.
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).markerToHighlight( { model: 'comment', view: { classes: 'comment' } } );
  *
  * editor.conversion.for( 'downcast' ).markerToHighlight( {
  * 	model: 'comment',
  * 	view: { classes: 'comment' },
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'downcast' ).markerToHighlight( {
  * 	model: 'comment',
  * 	view: ( data, conversionApi ) => {
  * 		// Assuming that the marker name is in a form of comment:commentType:commentId.
  * 		const [ , commentType, commentId ] = data.markerName.split( ':' );
  *
  * 		return {
  * 			classes: [ 'comment', 'comment-' + commentType ],
  * 			attributes: { 'data-comment-id': commentId }
  * 		};
  * 	}
  * } );
  * ```
  *
  * If a function is passed as the `config.view` parameter, it will be used to generate the highlight descriptor. The function
  * receives the `data` object and {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API}
  * as the parameters and should return a
  * {@link module:engine/conversion/downcasthelpers~HighlightDescriptor highlight descriptor}.
  * The `data` object properties are passed from {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher#event:addMarker}.
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The name of the model marker (or model marker group) to convert.
  * @param config.view A highlight descriptor that will be used for highlighting or a function that takes the model marker data and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as a parameters
  * and returns a highlight descriptor.
  * @param config.converterPriority Converter priority.
  */
  markerToHighlight(t) {
    return this.add(LO(t));
  }
  /**
  * Model marker converter for data downcast.
  *
  * This conversion creates a representation for model marker boundaries in the view:
  *
  * * If the marker boundary is before or after a model element, a view attribute is set on a corresponding view element.
  * * In other cases, a view element with the specified tag name is inserted at the corresponding view position.
  *
  * Typically, the marker names use the `group:uniqueId:otherData` convention. For example: `comment:e34zfk9k2n459df53sjl34:zx32c`.
  * The default configuration for this conversion is that the first part is the `group` part and the rest of
  * the marker name becomes the `name` part.
  *
  * Tag and attribute names and values are generated from the marker name:
  *
  * * The templates for attributes are `data-[group]-start-before="[name]"`, `data-[group]-start-after="[name]"`,
  * `data-[group]-end-before="[name]"` and `data-[group]-end-after="[name]"`.
  * * The templates for view elements are `<[group]-start name="[name]">` and `<[group]-end name="[name]">`.
  *
  * Attributes mark whether the given marker's start or end boundary is before or after the given element.
  * The `data-[group]-start-before` and `data-[group]-end-after` attributes are favored.
  * The other two are used when the former two cannot be used.
  *
  * The conversion configuration can take a function that will generate different group and name parts.
  * If such a function is set as the `config.view` parameter, it is passed a marker name and it is expected to return an object with two
  * properties: `group` and `name`. If the function returns a falsy value, the conversion will not take place.
  *
  * Basic usage:
  *
  * ```ts
  * // Using the default conversion.
  * // In this case, all markers with names starting with 'comment:' will be converted.
  * // The `group` parameter will be set to `comment`.
  * // The `name` parameter will be the rest of the marker name (without the `:`).
  * editor.conversion.for( 'dataDowncast' ).markerToData( {
  * 	model: 'comment'
  * } );
  * ```
  *
  * An example of a view that may be generated by this conversion (assuming a marker with the name `comment:commentId:uid` marked
  * by `[]`):
  *
  * ```
  * // Model:
  * <paragraph>Foo[bar</paragraph>
  * <imageBlock src="abc.jpg"></imageBlock>]
  *
  * // View:
  * <p>Foo<comment-start name="commentId:uid"></comment-start>bar</p>
  * <figure data-comment-end-after="commentId:uid" class="image"><img src="abc.jpg" /></figure>
  * ```
  *
  * In the example above, the comment starts before "bar" and ends after the image.
  *
  * If the `name` part is empty, the following view may be generated:
  *
  * ```html
  * <p>Foo <myMarker-start></myMarker-start>bar</p>
  * <figure data-myMarker-end-after="" class="image"><img src="abc.jpg" /></figure>
  * ```
  *
  * **Note:** A situation where some markers have the `name` part and some do not, is incorrect and should be avoided.
  *
  * Examples where `data-group-start-after` and `data-group-end-before` are used:
  *
  * ```
  * // Model:
  * <blockQuote>[]<paragraph>Foo</paragraph></blockQuote>
  *
  * // View:
  * <blockquote><p data-group-end-before="name" data-group-start-before="name">Foo</p></blockquote>
  * ```
  *
  * Similarly, when a marker is collapsed after the last element:
  *
  * ```
  * // Model:
  * <blockQuote><paragraph>Foo</paragraph>[]</blockQuote>
  *
  * // View:
  * <blockquote><p data-group-end-after="name" data-group-start-after="name">Foo</p></blockquote>
  * ```
  *
  * When there are multiple markers from the same group stored in the same attribute of the same element, their
  * name parts are put together in the attribute value, for example: `data-group-start-before="name1,name2,name3"`.
  *
  * Other examples of usage:
  *
  * ```ts
  * // Using a custom function which is the same as the default conversion:
  * editor.conversion.for( 'dataDowncast' ).markerToData( {
  * 	model: 'comment',
  * 	view: markerName => ( {
  * 		group: 'comment',
  * 		name: markerName.substr( 8 ) // Removes 'comment:' part.
  * 	} )
  * } );
  *
  * // Using the converter priority:
  * editor.conversion.for( 'dataDowncast' ).markerToData( {
  * 	model: 'comment',
  * 	view: markerName => ( {
  * 		group: 'comment',
  * 		name: markerName.substr( 8 ) // Removes 'comment:' part.
  * 	} ),
  * 	converterPriority: 'high'
  * } );
  * ```
  *
  * This kind of conversion is useful for saving data into the database, so it should be used in the data conversion pipeline.
  *
  * See the {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} API guide to learn how to
  * add a converter to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The name of the model marker (or the model marker group) to convert.
  * @param config.view A function that takes the model marker name and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as the parameters
  * and returns an object with the `group` and `name` properties.
  * @param config.converterPriority Converter priority.
  */
  markerToData(t) {
    return this.add(DO(t));
  }
}
function J2() {
  return (i, t, e) => {
    if (!e.consumable.consume(t.item, i.name))
      return;
    const n = e.writer, r = e.mapper.toViewPosition(t.range.start), s = n.createText(t.item.data);
    n.insert(r, s);
  };
}
function X2() {
  return (i, t, e) => {
    e.convertAttributes(t.item), !t.reconversion && t.item.is("element") && !t.item.isEmpty && e.convertChildren(t.item);
  };
}
function fO() {
  return (i, t, e) => {
    const n = e.mapper.toViewPosition(t.position), r = t.position.getShiftedBy(t.length), s = e.mapper.toViewPosition(r, {
      isPhantom: !0
    }), o = e.writer.createRange(n, s), a = e.writer.remove(o.getTrimmed());
    for (const l of e.writer.createRangeIn(a).getItems())
      e.mapper.unbindViewElement(l, {
        defer: !0
      });
  };
}
function Q2(i, t) {
  const e = i.createAttributeElement("span", t.attributes);
  return t.classes && e._addClass(t.classes), typeof t.priority == "number" && (e._priority = t.priority), e._id = t.id, e;
}
function mO() {
  return (i, t, e) => {
    const n = t.selection;
    if (n.isCollapsed || !e.consumable.consume(n, "selection"))
      return;
    const r = [];
    for (const s of n.getRanges())
      r.push(e.mapper.toViewRange(s));
    e.writer.setSelection(r, {
      backward: n.isBackward
    });
  };
}
function gO() {
  return (i, t, e) => {
    const n = t.selection;
    if (!n.isCollapsed || !e.consumable.consume(n, "selection"))
      return;
    const r = e.writer, s = n.getFirstPosition(), o = e.mapper.toViewPosition(s), a = r.breakAttributes(o);
    r.setSelection(a);
  };
}
function pO() {
  return (i, t, e) => {
    const n = e.writer, r = n.document.selection;
    for (const s of r.getRanges())
      s.isCollapsed && s.end.parent.isAttached() && e.writer.mergeAttributes(s.start);
    n.setSelection(null);
  };
}
function bO(i) {
  return (t, e, n) => {
    if (!n.consumable.test(e.item, t.name))
      return;
    const r = i(e.attributeOldValue, n, e), s = i(e.attributeNewValue, n, e);
    if (!r && !s)
      return;
    n.consumable.consume(e.item, t.name);
    const o = n.writer, a = o.document.selection;
    if (e.item instanceof vi || e.item instanceof Xi)
      o.wrap(a.getFirstRange(), s);
    else {
      let l = n.mapper.toViewRange(e.range);
      e.attributeOldValue !== null && r && (l = o.unwrap(l, r)), e.attributeNewValue !== null && s && o.wrap(l, s);
    }
  };
}
function vO(i, t = jO) {
  return (e, n, r) => {
    if (!t(n.item, r.consumable, {
      preflight: !0
    }))
      return;
    const s = i(n.item, r, n);
    if (!s)
      return;
    t(n.item, r.consumable);
    const o = r.mapper.toViewPosition(n.range.start);
    r.mapper.bindElements(n.item, s), r.writer.insert(o, s), r.convertAttributes(n.item), rC(s, n.item.getChildren(), r, {
      reconversion: n.reconversion
    });
  };
}
function wO(i, t) {
  return (e, n, r) => {
    if (!t(n.item, r.consumable, {
      preflight: !0
    }))
      return;
    const s = /* @__PURE__ */ new Map();
    r.writer._registerSlotFactory(FO(n.item, s, r));
    const o = i(n.item, r, n);
    if (r.writer._clearSlotFactory(), !o)
      return;
    $O(n.item, s, r), t(n.item, r.consumable);
    const a = r.mapper.toViewPosition(n.range.start);
    r.mapper.bindElements(n.item, o), r.writer.insert(a, o), r.convertAttributes(n.item), zO(o, s, r, {
      reconversion: n.reconversion
    });
  };
}
function yO(i) {
  return (t, e, n) => {
    e.isOpening = !0;
    const r = i(e, n);
    e.isOpening = !1;
    const s = i(e, n);
    if (!r || !s)
      return;
    const o = e.markerRange;
    if (o.isCollapsed && !n.consumable.consume(o, t.name))
      return;
    for (const c of o)
      if (!n.consumable.consume(c.item, t.name))
        return;
    const a = n.mapper, l = n.writer;
    l.insert(a.toViewPosition(o.start), r), n.mapper.bindElementToMarker(r, e.markerName), o.isCollapsed || (l.insert(a.toViewPosition(o.end), s), n.mapper.bindElementToMarker(s, e.markerName)), t.stop();
  };
}
function _O() {
  return (i, t, e) => {
    const n = e.mapper.markerNameToElements(t.markerName);
    if (n) {
      for (const r of n)
        e.mapper.unbindElementFromMarkerName(r, t.markerName), e.writer.clear(e.writer.createRangeOn(r), r);
      e.writer.clearClonedElementsGroup(t.markerName), i.stop();
    }
  };
}
function CO(i) {
  return (t, e, n) => {
    const r = i(e.markerName, n);
    if (!r)
      return;
    const s = e.markerRange;
    n.consumable.consume(s, t.name) && (H1(s, !1, n, e, r), H1(s, !0, n, e, r), t.stop());
  };
}
function H1(i, t, e, n, r) {
  const s = t ? i.start : i.end, o = s.nodeAfter && s.nodeAfter.is("element") ? s.nodeAfter : null, a = s.nodeBefore && s.nodeBefore.is("element") ? s.nodeBefore : null;
  if (o || a) {
    let c, u;
    t && o || !t && !a ? (c = o, u = !0) : (c = a, u = !1);
    const d = e.mapper.toViewElement(c);
    if (d) {
      kO(d, t, u, e, n, r);
      return;
    }
  }
  const l = e.mapper.toViewPosition(s);
  xO(l, t, e, n, r);
}
function kO(i, t, e, n, r, s) {
  const o = `data-${s.group}-${t ? "start" : "end"}-${e ? "before" : "after"}`, a = i.hasAttribute(o) ? i.getAttribute(o).split(",") : [];
  a.unshift(s.name), n.writer.setAttribute(o, a.join(","), i), n.mapper.bindElementToMarker(i, r.markerName);
}
function xO(i, t, e, n, r) {
  const s = `${r.group}-${t ? "start" : "end"}`, o = r.name ? {
    name: r.name
  } : null, a = e.writer.createUIElement(s, o);
  e.writer.insert(i, a), e.mapper.bindElementToMarker(a, n.markerName);
}
function EO(i) {
  return (t, e, n) => {
    const r = i(e.markerName, n);
    if (!r)
      return;
    const s = n.mapper.markerNameToElements(e.markerName);
    if (!s)
      return;
    for (const a of s)
      n.mapper.unbindElementFromMarkerName(a, e.markerName), a.is("containerElement") ? (o(`data-${r.group}-start-before`, a), o(`data-${r.group}-start-after`, a), o(`data-${r.group}-end-before`, a), o(`data-${r.group}-end-after`, a)) : n.writer.clear(n.writer.createRangeOn(a), a);
    n.writer.clearClonedElementsGroup(e.markerName), t.stop();
    function o(a, l) {
      if (l.hasAttribute(a)) {
        const c = new Set(l.getAttribute(a).split(","));
        c.delete(r.name), c.size == 0 ? n.writer.removeAttribute(a, l) : n.writer.setAttribute(a, Array.from(c).join(","), l);
      }
    }
  };
}
function TO(i) {
  return (t, e, n) => {
    if (!n.consumable.test(e.item, t.name))
      return;
    const r = i(e.attributeOldValue, n, e), s = i(e.attributeNewValue, n, e);
    if (!r && !s)
      return;
    n.consumable.consume(e.item, t.name);
    const o = n.mapper.toViewElement(e.item), a = n.writer;
    if (!o)
      throw new T("conversion-attribute-to-attribute-on-text", n.dispatcher, e);
    if (e.attributeOldValue !== null && r)
      if (r.key == "class") {
        const l = typeof r.value == "string" ? r.value.split(/\s+/) : r.value;
        for (const c of l)
          a.removeClass(c, o);
      } else if (r.key == "style")
        if (typeof r.value == "string") {
          const l = new Fu(a.document.stylesProcessor);
          l.setTo(r.value);
          for (const [c] of l.getStylesEntries())
            a.removeStyle(c, o);
        } else {
          const l = Object.keys(r.value);
          for (const c of l)
            a.removeStyle(c, o);
        }
      else
        a.removeAttribute(r.key, o);
    if (e.attributeNewValue !== null && s)
      if (s.key == "class") {
        const l = typeof s.value == "string" ? s.value.split(/\s+/) : s.value;
        for (const c of l)
          a.addClass(c, o);
      } else if (s.key == "style")
        if (typeof s.value == "string") {
          const l = new Fu(a.document.stylesProcessor);
          l.setTo(s.value);
          for (const [c, u] of l.getStylesEntries())
            a.setStyle(c, u, o);
        } else {
          const l = Object.keys(s.value);
          for (const c of l)
            a.setStyle(c, s.value[c], o);
        }
      else
        a.setAttribute(s.key, s.value, o);
  };
}
function SO(i) {
  return (t, e, n) => {
    if (!e.item || !(e.item instanceof vi || e.item instanceof Xi) && !e.item.is("$textProxy"))
      return;
    const r = sp(i, e, n);
    if (!r || !n.consumable.consume(e.item, t.name))
      return;
    const s = n.writer, o = Q2(s, r), a = s.document.selection;
    if (e.item instanceof vi || e.item instanceof Xi)
      s.wrap(a.getFirstRange(), o);
    else {
      const l = n.mapper.toViewRange(e.range), c = s.wrap(l, o);
      for (const u of c.getItems())
        if (u.is("attributeElement") && u.isSimilar(o)) {
          n.mapper.bindElementToMarker(u, e.markerName);
          break;
        }
    }
  };
}
function AO(i) {
  return (t, e, n) => {
    if (!e.item || !(e.item instanceof xt))
      return;
    const r = sp(i, e, n);
    if (!r || !n.consumable.test(e.item, t.name))
      return;
    const s = n.mapper.toViewElement(e.item);
    if (s && s.getCustomProperty("addHighlight")) {
      n.consumable.consume(e.item, t.name);
      for (const a of D._createIn(e.item))
        n.consumable.consume(a.item, t.name);
      s.getCustomProperty("addHighlight")(s, r, n.writer), n.mapper.bindElementToMarker(s, e.markerName);
    }
  };
}
function PO(i) {
  return (t, e, n) => {
    if (e.markerRange.isCollapsed)
      return;
    const r = sp(i, e, n);
    if (!r)
      return;
    const s = Q2(n.writer, r), o = n.mapper.markerNameToElements(e.markerName);
    if (o) {
      for (const a of o)
        n.mapper.unbindElementFromMarkerName(a, e.markerName), a.is("attributeElement") ? n.writer.unwrap(n.writer.createRangeOn(a), s) : a.getCustomProperty("removeHighlight")(a, r.id, n.writer);
      n.writer.clearClonedElementsGroup(e.markerName), t.stop();
    }
  };
}
function IO(i) {
  const t = eC(i.model), e = Ml(i.view, "container");
  return t.attributes.length && (t.children = !0), (n) => {
    n.on(`insert:${t.name}`, vO(e, iC(t)), {
      priority: i.converterPriority || "normal"
    }), (t.children || t.attributes.length) && n.on("reduceChanges", nC(t), {
      priority: "low"
    });
  };
}
function RO(i) {
  const t = eC(i.model), e = Ml(i.view, "container");
  return t.children = !0, (n) => {
    if (n._conversionApi.schema.checkChild(t.name, "$text"))
      throw new T("conversion-element-to-structure-disallowed-text", n, {
        elementName: t.name
      });
    n.on(`insert:${t.name}`, wO(e, iC(t)), {
      priority: i.converterPriority || "normal"
    }), n.on("reduceChanges", nC(t), {
      priority: "low"
    });
  };
}
function OO(i) {
  i = Fi(i);
  let t = i.model;
  typeof t == "string" && (t = {
    key: t
  });
  let e = `attribute:${t.key}`;
  if (t.name && (e += ":" + t.name), t.values)
    for (const r of t.values)
      i.view[r] = Ml(i.view[r], "attribute");
  else
    i.view = Ml(i.view, "attribute");
  const n = tC(i);
  return (r) => {
    r.on(e, bO(n), {
      priority: i.converterPriority || "normal"
    });
  };
}
function VO(i) {
  i = Fi(i);
  let t = i.model;
  typeof t == "string" && (t = {
    key: t
  });
  let e = `attribute:${t.key}`;
  if (t.name && (e += ":" + t.name), t.values)
    for (const r of t.values)
      i.view[r] = j1(i.view[r]);
  else
    i.view = j1(i.view);
  const n = tC(i);
  return (r) => {
    r.on(e, TO(n), {
      priority: i.converterPriority || "normal"
    });
  };
}
function MO(i) {
  const t = Ml(i.view, "ui");
  return (e) => {
    e.on(`addMarker:${i.model}`, yO(t), {
      priority: i.converterPriority || "normal"
    }), e.on(`removeMarker:${i.model}`, _O(), {
      priority: i.converterPriority || "normal"
    });
  };
}
function DO(i) {
  i = Fi(i);
  const t = i.model;
  let e = i.view;
  return e || (e = (n) => ({
    group: t,
    name: n.substr(i.model.length + 1)
  })), (n) => {
    n.on(`addMarker:${t}`, CO(e), {
      priority: i.converterPriority || "normal"
    }), n.on(`removeMarker:${t}`, EO(e), {
      priority: i.converterPriority || "normal"
    });
  };
}
function LO(i) {
  return (t) => {
    t.on(`addMarker:${i.model}`, SO(i.view), {
      priority: i.converterPriority || "normal"
    }), t.on(`addMarker:${i.model}`, AO(i.view), {
      priority: i.converterPriority || "normal"
    }), t.on(`removeMarker:${i.model}`, PO(i.view), {
      priority: i.converterPriority || "normal"
    });
  };
}
function eC(i) {
  return typeof i == "string" && (i = {
    name: i
  }), {
    name: i.name,
    attributes: i.attributes ? tt(i.attributes) : [],
    children: !!i.children
  };
}
function Ml(i, t) {
  return typeof i == "function" ? i : (e, n) => NO(i, n, t);
}
function NO(i, t, e) {
  typeof i == "string" && (i = {
    name: i
  });
  let n;
  const r = t.writer, s = Object.assign({}, i.attributes);
  if (e == "container")
    n = r.createContainerElement(i.name, s);
  else if (e == "attribute") {
    const o = {
      priority: i.priority || Ns.DEFAULT_PRIORITY
    };
    n = r.createAttributeElement(i.name, s, o);
  } else
    n = r.createUIElement(i.name, s);
  if (i.styles) {
    const o = Object.keys(i.styles);
    for (const a of o)
      r.setStyle(a, i.styles[a], n);
  }
  if (i.classes) {
    const o = i.classes;
    if (typeof o == "string")
      r.addClass(o, n);
    else
      for (const a of o)
        r.addClass(a, n);
  }
  return n;
}
function tC(i) {
  return i.model.values ? (t, e, n) => {
    const r = i.view[t];
    return r ? r(t, e, n) : null;
  } : i.view;
}
function j1(i) {
  return typeof i == "string" ? (t) => ({
    key: i,
    value: t
  }) : typeof i == "object" ? i.value ? () => i : (t) => ({
    key: i.key,
    value: t
  }) : i;
}
function sp(i, t, e) {
  const n = typeof i == "function" ? i(t, e) : i;
  return n ? (n.priority || (n.priority = 10), n.id || (n.id = t.markerName), n) : null;
}
function BO(i) {
  return (t, e) => {
    if (!t.is("element", i.name))
      return !1;
    if (e.type == "attribute") {
      if (i.attributes.includes(e.attributeKey))
        return !0;
    } else {
      /* istanbul ignore else: This is always true because otherwise it would not register a reducer callback. -- @preserve */
      if (i.children)
        return !0;
    }
    return !1;
  };
}
function nC(i) {
  const t = BO(i);
  return (e, n) => {
    const r = [];
    n.reconvertedElements || (n.reconvertedElements = /* @__PURE__ */ new Set());
    for (const s of n.changes) {
      const o = s.type == "attribute" ? s.range.start.nodeAfter : s.position.parent;
      if (!o || !t(o, s)) {
        r.push(s);
        continue;
      }
      if (!n.reconvertedElements.has(o)) {
        n.reconvertedElements.add(o);
        const a = G._createBefore(o);
        let l = r.length;
        for (let c = r.length - 1; c >= 0; c--) {
          const u = r[c], f = (u.type == "attribute" ? u.range.start : u.position).compareWith(a);
          if (f == "before" || u.type == "remove" && f == "same")
            break;
          l = c;
        }
        r.splice(l, 0, {
          type: "remove",
          name: o.name,
          position: a,
          length: 1
        }, {
          type: "reinsert",
          name: o.name,
          position: a,
          length: 1
        });
      }
    }
    n.changes = r;
  };
}
function iC(i) {
  return (t, e, n = {}) => {
    const r = [
      "insert"
    ];
    for (const s of i.attributes)
      t.hasAttribute(s) && r.push(`attribute:${s}`);
    return r.every((s) => e.test(t, s)) ? (n.preflight || r.forEach((s) => e.consume(t, s)), !0) : !1;
  };
}
function FO(i, t, e) {
  return (n, r) => {
    const s = n.createContainerElement("$slot");
    let o = null;
    if (r === "children")
      o = Array.from(i.getChildren());
    else if (typeof r == "function")
      o = Array.from(i.getChildren()).filter((a) => r(a));
    else
      throw new T("conversion-slot-mode-unknown", e.dispatcher, {
        modeOrFilter: r
      });
    return t.set(s, o), s;
  };
}
function $O(i, t, e) {
  const n = Array.from(t.values()).flat(), r = new Set(n);
  if (r.size != n.length)
    throw new T("conversion-slot-filter-overlap", e.dispatcher, {
      element: i
    });
  if (r.size != i.childCount)
    throw new T("conversion-slot-filter-incomplete", e.dispatcher, {
      element: i
    });
}
function zO(i, t, e, n) {
  e.mapper.on("modelToViewPosition", o, {
    priority: "highest"
  });
  let r = null, s = null;
  for ([r, s] of t)
    rC(i, s, e, n), e.writer.move(e.writer.createRangeIn(r), e.writer.createPositionBefore(r)), e.writer.remove(r);
  e.mapper.off("modelToViewPosition", o);
  function o(a, l) {
    const c = l.modelPosition.nodeAfter, u = s.indexOf(c);
    u < 0 || (l.viewPosition = l.mapper.findPositionIn(r, u));
  }
}
function rC(i, t, e, n) {
  for (const r of t)
    HO(i.root, r, e, n) || e.convertItem(r);
}
function HO(i, t, e, n) {
  const { writer: r, mapper: s } = e;
  if (!n.reconversion)
    return !1;
  const o = s.toViewElement(t);
  return !o || o.root == i || !e.canReuseView(o) ? !1 : (r.move(r.createRangeOn(o), s.toViewPosition(G._createBefore(t))), !0);
}
function jO(i, t, { preflight: e } = {}) {
  return e ? t.test(i, "insert") : t.consume(i, "insert");
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function sC(i) {
  const { schema: t, document: e } = i.model;
  for (const n of e.getRoots())
    if (n.isEmpty && !t.checkChild(n, "$text") && t.checkChild(n, "paragraph"))
      return i.insertElement("paragraph", n), !0;
  return !1;
}
function oC(i, t, e) {
  const n = e.createContext(i);
  return !(!e.checkChild(n, "paragraph") || !e.checkChild(n.push("paragraph"), t));
}
function aC(i, t) {
  const e = t.createElement("paragraph");
  return t.insert(e, i), t.createPositionAt(e, 0);
}
class WO extends Y2 {
  /**
  * View element to model element conversion helper.
  *
  * This conversion results in creating a model element. For example,
  * view `<p>Foo</p>` becomes `<paragraph>Foo</paragraph>` in the model.
  *
  * Keep in mind that the element will be inserted only if it is allowed
  * by {@link module:engine/model/schema~Schema schema} configuration.
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).elementToElement( {
  * 	view: 'p',
  * 	model: 'paragraph'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToElement( {
  * 	view: 'p',
  * 	model: 'paragraph',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToElement( {
  * 	view: {
  * 		name: 'p',
  * 		classes: 'fancy'
  * 	},
  * 	model: 'fancyParagraph'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToElement( {
  * 	view: {
  * 		name: 'p',
  * 		classes: 'heading'
  * 	},
  * 	model: ( viewElement, conversionApi ) => {
  * 		const modelWriter = conversionApi.writer;
  *
  * 		return modelWriter.createElement( 'heading', { level: viewElement.getAttribute( 'data-level' ) } );
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view Pattern matching all view elements which should be converted. If not set, the converter
  * will fire for every view element.
  * @param config.model Name of the model element, a model element instance or a function that takes a view element
  * and {@link module:engine/conversion/upcastdispatcher~UpcastConversionApi upcast conversion API}
  * and returns a model element. The model element will be inserted in the model.
  * @param config.converterPriority Converter priority.
  */
  elementToElement(t) {
    return this.add(lC(t));
  }
  /**
  * View element to model attribute conversion helper.
  *
  * This conversion results in setting an attribute on a model node. For example, view `<strong>Foo</strong>` becomes
  * `Foo` {@link module:engine/model/text~Text model text node} with `bold` attribute set to `true`.
  *
  * This helper is meant to set a model attribute on all the elements that are inside the converted element:
  *
  * ```
  * <strong>Foo</strong>   -->   <strong><p>Foo</p></strong>   -->   <paragraph><$text bold="true">Foo</$text></paragraph>
  * ```
  *
  * Above is a sample of HTML code, that goes through autoparagraphing (first step) and then is converted (second step).
  * Even though `<strong>` is over `<p>` element, `bold="true"` was added to the text. See
  * {@link module:engine/conversion/upcasthelpers~UpcastHelpers#attributeToAttribute} for comparison.
  *
  * Keep in mind that the attribute will be set only if it is allowed by {@link module:engine/model/schema~Schema schema} configuration.
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: 'strong',
  * 	model: 'bold'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: 'strong',
  * 	model: 'bold',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: {
  * 		name: 'span',
  * 		classes: 'bold'
  * 	},
  * 	model: 'bold'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: {
  * 		name: 'span',
  * 		classes: [ 'styled', 'styled-dark' ]
  * 	},
  * 	model: {
  * 		key: 'styled',
  * 		value: 'dark'
  * 	}
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: {
  * 		name: 'span',
  * 		styles: {
  * 			'font-size': /[\s\S]+/
  * 		}
  * 	},
  * 	model: {
  * 		key: 'fontSize',
  * 		value: ( viewElement, conversionApi ) => {
  * 			const fontSize = viewElement.getStyle( 'font-size' );
  * 			const value = fontSize.substr( 0, fontSize.length - 2 );
  *
  * 			if ( value <= 10 ) {
  * 				return 'small';
  * 			} else if ( value > 12 ) {
  * 				return 'big';
  * 			}
  *
  * 			return null;
  * 		}
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view Pattern matching all view elements which should be converted.
  * @param config.model Model attribute key or an object with `key` and `value` properties, describing
  * the model attribute. `value` property may be set as a function that takes a view element and
  * {@link module:engine/conversion/upcastdispatcher~UpcastConversionApi upcast conversion API} and returns the value.
  * If `String` is given, the model attribute value will be set to `true`.
  * @param config.converterPriority Converter priority. Defaults to `low`.
  */
  elementToAttribute(t) {
    return this.add(GO(t));
  }
  /**
  * View attribute to model attribute conversion helper.
  *
  * This conversion results in setting an attribute on a model node. For example, view `<img src="foo.jpg"></img>` becomes
  * `<imageBlock source="foo.jpg"></imageBlock>` in the model.
  *
  * This helper is meant to convert view attributes from view elements which got converted to the model, so the view attribute
  * is set only on the corresponding model node:
  *
  * ```
  * <div class="dark"><div>foo</div></div>    -->    <div dark="true"><div>foo</div></div>
  * ```
  *
  * Above, `class="dark"` attribute is added only to the `<div>` elements that has it. This is in contrast to
  * {@link module:engine/conversion/upcasthelpers~UpcastHelpers#elementToAttribute} which sets attributes for
  * all the children in the model:
  *
  * ```
  * <strong>Foo</strong>   -->   <strong><p>Foo</p></strong>   -->   <paragraph><$text bold="true">Foo</$text></paragraph>
  * ```
  *
  * Above is a sample of HTML code, that goes through autoparagraphing (first step) and then is converted (second step).
  * Even though `<strong>` is over `<p>` element, `bold="true"` was added to the text.
  *
  * Keep in mind that the attribute will be set only if it is allowed by {@link module:engine/model/schema~Schema schema} configuration.
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: 'src',
  * 	model: 'source'
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: { key: 'src' },
  * 	model: 'source'
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: { key: 'src' },
  * 	model: 'source',
  * 	converterPriority: 'normal'
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		key: 'data-style',
  * 		value: /[\s\S]+/
  * 	},
  * 	model: 'styled'
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		name: 'img',
  * 		key: 'class',
  * 		value: 'styled-dark'
  * 	},
  * 	model: {
  * 		key: 'styled',
  * 		value: 'dark'
  * 	}
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		key: 'class',
  * 		value: /styled-[\S]+/
  * 	},
  * 	model: {
  * 		key: 'styled'
  * 		value: ( viewElement, conversionApi ) => {
  * 			const regexp = /styled-([\S]+)/;
  * 			const match = viewElement.getAttribute( 'class' ).match( regexp );
  *
  * 			return match[ 1 ];
  * 		}
  * 	}
  * } );
  * ```
  *
  * Converting styles works a bit differently as it requires `view.styles` to be an object and by default
  * a model attribute will be set to `true` by such a converter. You can set the model attribute to any value by providing the `value`
  * callback that returns the desired value.
  *
  * ```ts
  * // Default conversion of font-weight style will result in setting bold attribute to true.
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		styles: {
  * 			'font-weight': 'bold'
  * 		}
  * 	},
  * 	model: 'bold'
  * } );
  *
  * // This converter will pass any style value to the `lineHeight` model attribute.
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		styles: {
  * 			'line-height': /[\s\S]+/
  * 		}
  * 	},
  * 	model: {
  * 		key: 'lineHeight',
  * 		value: ( viewElement, conversionApi ) => viewElement.getStyle( 'line-height' )
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view Specifies which view attribute will be converted. If a `String` is passed,
  * attributes with given key will be converted. If an `Object` is passed, it must have a required `key` property,
  * specifying view attribute key, and may have an optional `value` property, specifying view attribute value and optional `name`
  * property specifying a view element name from/on which the attribute should be converted. `value` can be given as a `String`,
  * a `RegExp` or a function callback, that takes view attribute value as the only parameter and returns `Boolean`.
  * @param config.model Model attribute key or an object with `key` and `value` properties, describing
  * the model attribute. `value` property may be set as a function that takes a view element and
  * {@link module:engine/conversion/upcastdispatcher~UpcastConversionApi upcast conversion API} and returns the value.
  * If `String` is given, the model attribute value will be same as view attribute value.
  * @param config.converterPriority Converter priority. Defaults to `low`.
  */
  attributeToAttribute(t) {
    return this.add(KO(t));
  }
  /**
  * View element to model marker conversion helper.
  *
  * This conversion results in creating a model marker. For example, if the marker was stored in a view as an element:
  * `<p>Fo<span data-marker="comment" data-comment-id="7"></span>o</p><p>B<span data-marker="comment" data-comment-id="7"></span>ar</p>`,
  * after the conversion is done, the marker will be available in
  * {@link module:engine/model/model~Model#markers model document markers}.
  *
  * **Note**: When this helper is used in the data upcast in combination with
  * {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToData `#markerToData()`} in the data downcast,
  * then invalid HTML code (e.g. a span between table cells) may be produced by the latter converter.
  *
  * In most of the cases, the {@link #dataToMarker} should be used instead.
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).elementToMarker( {
  * 	view: 'marker-search',
  * 	model: 'search'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToMarker( {
  * 	view: 'marker-search',
  * 	model: 'search',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToMarker( {
  * 	view: 'marker-search',
  * 	model: ( viewElement, conversionApi ) => 'comment:' + viewElement.getAttribute( 'data-comment-id' )
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToMarker( {
  * 	view: {
  * 		name: 'span',
  * 		attributes: {
  * 			'data-marker': 'search'
  * 		}
  * 	},
  * 	model: 'search'
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view Pattern matching all view elements which should be converted.
  * @param config.model Name of the model marker, or a function that takes a view element and returns
  * a model marker name.
  * @param config.converterPriority Converter priority.
  */
  elementToMarker(t) {
    return this.add(ZO(t));
  }
  /**
  * View-to-model marker conversion helper.
  *
  * Converts view data created by {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToData `#markerToData()`}
  * back to a model marker.
  *
  * This converter looks for specific view elements and view attributes that mark marker boundaries. See
  * {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToData `#markerToData()`} to learn what view data
  * is expected by this converter.
  *
  * The `config.view` property is equal to the marker group name to convert.
  *
  * By default, this converter creates markers with the `group:name` name convention (to match the default `markerToData` conversion).
  *
  * The conversion configuration can take a function that will generate a marker name.
  * If such function is set as the `config.model` parameter, it is passed the `name` part from the view element or attribute and it is
  * expected to return a string with the marker name.
  *
  * Basic usage:
  *
  * ```ts
  * // Using the default conversion.
  * // In this case, all markers from the `comment` group will be converted.
  * // The conversion will look for `<comment-start>` and `<comment-end>` tags and
  * // `data-comment-start-before`, `data-comment-start-after`,
  * // `data-comment-end-before` and `data-comment-end-after` attributes.
  * editor.conversion.for( 'upcast' ).dataToMarker( {
  * 	view: 'comment'
  * } );
  * ```
  *
  * An example of a model that may be generated by this conversion:
  *
  * ```
  * // View:
  * <p>Foo<comment-start name="commentId:uid"></comment-start>bar</p>
  * <figure data-comment-end-after="commentId:uid" class="image"><img src="abc.jpg" /></figure>
  *
  * // Model:
  * <paragraph>Foo[bar</paragraph>
  * <imageBlock src="abc.jpg"></imageBlock>]
  * ```
  *
  * Where `[]` are boundaries of a marker that will receive the `comment:commentId:uid` name.
  *
  * Other examples of usage:
  *
  * ```ts
  * // Using a custom function which is the same as the default conversion:
  * editor.conversion.for( 'upcast' ).dataToMarker( {
  * 	view: 'comment',
  * 	model: ( name, conversionApi ) => 'comment:' + name,
  * } );
  *
  * // Using the converter priority:
  * editor.conversion.for( 'upcast' ).dataToMarker( {
  * 	view: 'comment',
  * 	model: ( name, conversionApi ) => 'comment:' + name,
  * 	converterPriority: 'high'
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view The marker group name to convert.
  * @param config.model A function that takes the `name` part from the view element or attribute and
  * {@link module:engine/conversion/upcastdispatcher~UpcastConversionApi upcast conversion API} and returns the marker name.
  * @param config.converterPriority Converter priority.
  */
  dataToMarker(t) {
    return this.add(YO(t));
  }
}
function W1() {
  return (i, t, e) => {
    if (!t.modelRange && e.consumable.consume(t.viewItem, {
      name: !0
    })) {
      const { modelRange: n, modelCursor: r } = e.convertChildren(t.viewItem, t.modelCursor);
      t.modelRange = n, t.modelCursor = r;
    }
  };
}
function UO() {
  return (i, t, { schema: e, consumable: n, writer: r }) => {
    let s = t.modelCursor;
    if (!n.test(t.viewItem))
      return;
    if (!e.checkChild(s, "$text")) {
      if (!oC(s, "$text", e) || t.viewItem.data.trim().length == 0)
        return;
      s = aC(s, r);
    }
    n.consume(t.viewItem);
    const o = r.createText(t.viewItem.data);
    r.insert(o, s), t.modelRange = r.createRange(s, s.getShiftedBy(o.offsetSize)), t.modelCursor = t.modelRange.end;
  };
}
function qO(i, t) {
  return (e, n) => {
    const r = n.newSelection, s = [];
    for (const a of r.getRanges())
      s.push(t.toModelRange(a));
    const o = i.createSelection(s, {
      backward: r.isBackward
    });
    o.isEqual(i.document.selection) || i.change((a) => {
      a.setSelection(o);
    });
  };
}
function lC(i) {
  i = Fi(i);
  const t = lm(i), e = op(i.view), n = e ? `element:${e}` : "element";
  return (r) => {
    r.on(n, t, {
      priority: i.converterPriority || "normal"
    });
  };
}
function GO(i) {
  i = Fi(i), cC(i);
  const t = uC(i, !1), e = op(i.view), n = e ? `element:${e}` : "element";
  return (r) => {
    r.on(n, t, {
      priority: i.converterPriority || "low"
    });
  };
}
function KO(i) {
  i = Fi(i);
  let t = null;
  (typeof i.view == "string" || i.view.key) && (t = QO(i)), cC(i, t);
  const e = uC(i, !0);
  return (n) => {
    n.on("element", e, {
      priority: i.converterPriority || "low"
    });
  };
}
function ZO(i) {
  const t = n7(i.model);
  return lC({
    ...i,
    model: t
  });
}
function YO(i) {
  i = Fi(i), i.model || (i.model = (r) => r ? i.view + ":" + r : i.view);
  const t = {
    view: i.view,
    model: i.model
  }, e = lm(U1(t, "start")), n = lm(U1(t, "end"));
  return (r) => {
    r.on(`element:${i.view}-start`, e, {
      priority: i.converterPriority || "normal"
    }), r.on(`element:${i.view}-end`, n, {
      priority: i.converterPriority || "normal"
    });
    const s = wi.low, o = wi.highest, a = wi.get(i.converterPriority) / o;
    r.on("element", JO(t), {
      priority: s + a
    });
  };
}
function JO(i) {
  return (t, e, n) => {
    const r = `data-${i.view}`;
    if (!n.consumable.test(e.viewItem, {
      attributes: r + "-end-after"
    }) && !n.consumable.test(e.viewItem, {
      attributes: r + "-start-after"
    }) && !n.consumable.test(e.viewItem, {
      attributes: r + "-end-before"
    }) && !n.consumable.test(e.viewItem, {
      attributes: r + "-start-before"
    }))
      return;
    e.modelRange || Object.assign(e, n.convertChildren(e.viewItem, e.modelCursor)), n.consumable.consume(e.viewItem, {
      attributes: r + "-end-after"
    }) && s(e.modelRange.end, e.viewItem.getAttribute(r + "-end-after").split(",")), n.consumable.consume(e.viewItem, {
      attributes: r + "-start-after"
    }) && s(e.modelRange.end, e.viewItem.getAttribute(r + "-start-after").split(",")), n.consumable.consume(e.viewItem, {
      attributes: r + "-end-before"
    }) && s(e.modelRange.start, e.viewItem.getAttribute(r + "-end-before").split(",")), n.consumable.consume(e.viewItem, {
      attributes: r + "-start-before"
    }) && s(e.modelRange.start, e.viewItem.getAttribute(r + "-start-before").split(","));
    function s(o, a) {
      for (const l of a) {
        const c = i.model(l, n), u = n.writer.createElement("$marker", {
          "data-name": c
        });
        n.writer.insert(u, o), e.modelCursor.isEqual(o) ? e.modelCursor = e.modelCursor.getShiftedBy(1) : e.modelCursor = e.modelCursor._getTransformedByInsertion(o, 1), e.modelRange = e.modelRange._getTransformedByInsertion(o, 1)[0];
      }
    }
  };
}
function op(i) {
  return typeof i == "string" ? i : typeof i == "object" && typeof i.name == "string" ? i.name : null;
}
function lm(i) {
  const t = new xi(i.view);
  return (e, n, r) => {
    const s = t.match(n.viewItem);
    if (!s)
      return;
    const o = s.match;
    if (o.name = !0, !r.consumable.test(n.viewItem, o))
      return;
    const a = XO(i.model, n.viewItem, r);
    a && r.safeInsert(a, n.modelCursor) && (r.consumable.consume(n.viewItem, o), r.convertChildren(n.viewItem, a), r.updateConversionResult(a, n));
  };
}
function XO(i, t, e) {
  return i instanceof Function ? i(t, e) : e.writer.createElement(i);
}
function QO(i) {
  typeof i.view == "string" && (i.view = {
    key: i.view
  });
  const t = i.view.key, e = typeof i.view.value > "u" ? /[\s\S]*/ : i.view.value;
  let n;
  return t == "class" || t == "style" ? n = {
    [t == "class" ? "classes" : "styles"]: e
  } : n = {
    attributes: {
      [t]: e
    }
  }, i.view.name && (n.name = i.view.name), i.view = n, t;
}
function cC(i, t = null) {
  const e = t === null ? !0 : (s) => s.getAttribute(t), n = typeof i.model != "object" ? i.model : i.model.key, r = typeof i.model != "object" || typeof i.model.value > "u" ? e : i.model.value;
  i.model = {
    key: n,
    value: r
  };
}
function uC(i, t) {
  const e = new xi(i.view);
  return (n, r, s) => {
    if (!r.modelRange && t)
      return;
    const o = e.match(r.viewItem);
    if (!o || (e7(i.view, r.viewItem) ? o.match.name = !0 : delete o.match.name, !s.consumable.test(r.viewItem, o.match)))
      return;
    const a = i.model.key, l = typeof i.model.value == "function" ? i.model.value(r.viewItem, s) : i.model.value;
    if (l === null)
      return;
    r.modelRange || Object.assign(r, s.convertChildren(r.viewItem, r.modelCursor)), t7(r.modelRange, {
      key: a,
      value: l
    }, t, s) && (s.consumable.test(r.viewItem, {
      name: !0
    }) && (o.match.name = !0), s.consumable.consume(r.viewItem, o.match));
  };
}
function e7(i, t) {
  const e = typeof i == "function" ? i(t) : i;
  return typeof e == "object" && !op(e) ? !1 : !e.classes && !e.attributes && !e.styles;
}
function t7(i, t, e, n) {
  let r = !1;
  for (const s of Array.from(i.getItems({
    shallow: e
  })))
    n.schema.checkAttribute(s, t.key) && (r = !0, !s.hasAttribute(t.key) && n.writer.setAttribute(t.key, t.value, s));
  return r;
}
function n7(i) {
  return (t, e) => {
    const n = typeof i == "string" ? i : i(t, e);
    return e.writer.createElement("$marker", {
      "data-name": n
    });
  };
}
function U1(i, t) {
  const e = (n, r) => {
    const s = n.getAttribute("name"), o = i.model(s, r);
    return r.writer.createElement("$marker", {
      "data-name": o
    });
  };
  return {
    // Upcast <markerGroup-start> and <markerGroup-end> elements.
    view: `${i.view}-${t}`,
    model: e
  };
}
function i7(i) {
  i.document.registerPostFixer((t) => r7(t, i));
}
function r7(i, t) {
  const e = t.document.selection, n = t.schema, r = [];
  let s = !1;
  for (const o of e.getRanges()) {
    const a = dC(o, n);
    a && !a.isEqual(o) ? (r.push(a), s = !0) : r.push(o);
  }
  return s && i.setSelection(l7(r), {
    backward: e.isBackward
  }), !1;
}
function dC(i, t) {
  return i.isCollapsed ? s7(i, t) : o7(i, t);
}
function s7(i, t) {
  const e = i.start, n = t.getNearestSelectionRange(e);
  if (!n) {
    const s = e.getAncestors().reverse().find((o) => t.isObject(o));
    return s ? D._createOn(s) : null;
  }
  if (!n.isCollapsed)
    return n;
  const r = n.start;
  return e.isEqual(r) ? null : new D(r);
}
function o7(i, t) {
  const { start: e, end: n } = i, r = t.checkChild(e, "$text"), s = t.checkChild(n, "$text"), o = t.getLimitElement(e), a = t.getLimitElement(n);
  if (o === a) {
    if (r && s)
      return null;
    if (a7(e, n, t)) {
      const d = e.nodeAfter && t.isSelectable(e.nodeAfter) ? null : t.getNearestSelectionRange(e, "forward"), m = n.nodeBefore && t.isSelectable(n.nodeBefore) ? null : t.getNearestSelectionRange(n, "backward"), g = d ? d.start : e, p = m ? m.end : n;
      return new D(g, p);
    }
  }
  const l = o && !o.is("rootElement"), c = a && !a.is("rootElement");
  if (l || c) {
    const u = e.nodeAfter && n.nodeBefore && e.nodeAfter.parent === n.nodeBefore.parent, d = l && (!u || !G1(e.nodeAfter, t)), f = c && (!u || !G1(n.nodeBefore, t));
    let m = e, g = n;
    return d && (m = G._createBefore(q1(o, t))), f && (g = G._createAfter(q1(a, t))), new D(m, g);
  }
  return null;
}
function q1(i, t) {
  let e = i, n = e;
  for (; t.isLimit(n) && n.parent; )
    e = n, n = n.parent;
  return e;
}
function a7(i, t, e) {
  const n = i.nodeAfter && !e.isLimit(i.nodeAfter) || e.checkChild(i, "$text"), r = t.nodeBefore && !e.isLimit(t.nodeBefore) || e.checkChild(t, "$text");
  return n || r;
}
function l7(i) {
  const t = [
    ...i
  ], e = /* @__PURE__ */ new Set();
  let n = 1;
  for (; n < t.length; ) {
    const s = t[n], o = t.slice(0, n);
    for (const [a, l] of o.entries())
      if (!e.has(a)) {
        if (s.isEqual(l))
          e.add(a);
        else if (s.isIntersecting(l)) {
          e.add(a), e.add(n);
          const c = s.getJoined(l);
          t.push(c);
        }
      }
    n++;
  }
  return t.filter((s, o) => !e.has(o));
}
function G1(i, t) {
  return i && t.isSelectable(i);
}
class c7 extends (/* @__PURE__ */ st()) {
  /**
  * Creates an editing controller instance.
  *
  * @param model Editing model.
  * @param stylesProcessor The styles processor instance.
  */
  constructor(e, n) {
    super();
    /**
    * Editor model.
    */
    h(this, "model");
    /**
    * Editing view controller.
    */
    h(this, "view");
    /**
    * A mapper that describes the model-view binding.
    */
    h(this, "mapper");
    /**
    * Downcast dispatcher that converts changes from the model to the {@link #view editing view}.
    */
    h(this, "downcastDispatcher");
    this.model = e, this.view = new Y6(n), this.mapper = new G2(), this.downcastDispatcher = new K2({
      mapper: this.mapper,
      schema: e.schema
    });
    const r = this.model.document, s = r.selection, o = this.model.markers;
    this.listenTo(this.model, "_beforeChanges", () => {
      this.view._disableRendering(!0);
    }, {
      priority: "highest"
    }), this.listenTo(this.model, "_afterChanges", () => {
      this.view._disableRendering(!1);
    }, {
      priority: "lowest"
    }), this.listenTo(r, "change", () => {
      this.view.change((a) => {
        this.downcastDispatcher.convertChanges(r.differ, o, a), this.downcastDispatcher.convertSelection(s, o, a);
      });
    }, {
      priority: "low"
    }), this.listenTo(this.view.document, "selectionChange", qO(this.model, this.mapper)), this.listenTo(this.view.document, "beforeinput", u7(this.mapper, this.model.schema, this.view), {
      priority: "high"
    }), this.downcastDispatcher.on("insert:$text", J2(), {
      priority: "lowest"
    }), this.downcastDispatcher.on("insert", X2(), {
      priority: "lowest"
    }), this.downcastDispatcher.on("remove", fO(), {
      priority: "low"
    }), this.downcastDispatcher.on("cleanSelection", pO()), this.downcastDispatcher.on("selection", mO(), {
      priority: "low"
    }), this.downcastDispatcher.on("selection", gO(), {
      priority: "low"
    }), this.view.document.roots.bindTo(this.model.document.roots).using((a) => {
      if (a.rootName == "$graveyard")
        return null;
      const l = new M2(this.view.document, a.name);
      return l.rootName = a.rootName, this.mapper.bindElements(a, l), l;
    });
  }
  /**
  * Removes all event listeners attached to the `EditingController`. Destroys all objects created
  * by `EditingController` that need to be destroyed.
  */
  destroy() {
    this.view.destroy(), this.stopListening();
  }
  /**
  * Calling this method will refresh the marker by triggering the downcast conversion for it.
  *
  * Reconverting the marker is useful when you want to change its {@link module:engine/view/element~Element view element}
  * without changing any marker data. For instance:
  *
  * ```ts
  * let isCommentActive = false;
  *
  * model.conversion.markerToHighlight( {
  * 	model: 'comment',
  * 	view: data => {
  * 		const classes = [ 'comment-marker' ];
  *
  * 		if ( isCommentActive ) {
  * 			classes.push( 'comment-marker--active' );
  * 		}
  *
  * 		return { classes };
  * 	}
  * } );
  *
  * // ...
  *
  * // Change the property that indicates if marker is displayed as active or not.
  * isCommentActive = true;
  *
  * // Reconverting will downcast and synchronize the marker with the new isCommentActive state value.
  * editor.editing.reconvertMarker( 'comment' );
  * ```
  *
  * **Note**: If you want to reconvert a model item, use {@link #reconvertItem} instead.
  *
  * @param markerOrName Name of a marker to update, or a marker instance.
  */
  reconvertMarker(e) {
    const n = typeof e == "string" ? e : e.name, r = this.model.markers.get(n);
    if (!r)
      throw new T("editingcontroller-reconvertmarker-marker-not-exist", this, {
        markerName: n
      });
    this.model.change(() => {
      this.model.markers._refresh(r);
    });
  }
  /**
  * Calling this method will downcast a model item on demand (by requesting a refresh in the {@link module:engine/model/differ~Differ}).
  *
  * You can use it if you want the view representation of a specific item updated as a response to external modifications. For instance,
  * when the view structure depends not only on the associated model data but also on some external state.
  *
  * **Note**: If you want to reconvert a model marker, use {@link #reconvertMarker} instead.
  *
  * @param item Item to refresh.
  */
  reconvertItem(e) {
    this.model.change(() => {
      this.model.document.differ._refreshItem(e);
    });
  }
}
function u7(i, t, e) {
  return (n, r) => {
    if (!(e.document.isComposing && !Me.isAndroid))
      for (let s = 0; s < r.targetRanges.length; s++) {
        const o = r.targetRanges[s], a = i.toModelRange(o), l = dC(a, t);
        !l || l.isEqual(a) || (r.targetRanges[s] = i.toViewRange(l));
      }
  };
}
class dl {
  constructor() {
    /**
    * Map of consumable elements. If {@link module:engine/view/element~Element element} is used as a key,
    * {@link module:engine/conversion/viewconsumable~ViewElementConsumables ViewElementConsumables} instance is stored as value.
    * For {@link module:engine/view/text~Text text nodes} and
    * {@link module:engine/view/documentfragment~DocumentFragment document fragments} boolean value is stored as value.
    */
    h(this, "_consumables", /* @__PURE__ */ new Map());
  }
  add(t, e) {
    let n;
    if (t.is("$text") || t.is("documentFragment")) {
      this._consumables.set(t, !0);
      return;
    }
    this._consumables.has(t) ? n = this._consumables.get(t) : (n = new d7(t), this._consumables.set(t, n)), n.add(e);
  }
  /**
  * Tests if {@link module:engine/view/element~Element view element}, {@link module:engine/view/text~Text text node} or
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment} can be consumed.
  * It returns `true` when all items included in method's call can be consumed. Returns `false` when
  * first already consumed item is found and `null` when first non-consumable item is found.
  *
  * ```ts
  * viewConsumable.test( p, { name: true } ); // Tests element's name.
  * viewConsumable.test( p, { attributes: 'name' } ); // Tests attribute.
  * viewConsumable.test( p, { classes: 'foobar' } ); // Tests class.
  * viewConsumable.test( p, { styles: 'color' } ); // Tests style.
  * viewConsumable.test( p, { attributes: 'name', styles: 'color' } ); // Tests attribute and style.
  * viewConsumable.test( p, { classes: [ 'baz', 'bar' ] } ); // Multiple consumables can be tested.
  * viewConsumable.test( textNode ); // Tests text node.
  * viewConsumable.test( docFragment ); // Tests document fragment.
  * ```
  *
  * Testing classes and styles as attribute will test if all added classes/styles can be consumed.
  *
  * ```ts
  * viewConsumable.test( p, { attributes: 'class' } ); // Tests if all added classes can be consumed.
  * viewConsumable.test( p, { attributes: 'style' } ); // Tests if all added styles can be consumed.
  * ```
  *
  * @param consumables Used only if first parameter is {@link module:engine/view/element~Element view element} instance.
  * @param consumables.name If set to true element's name will be included.
  * @param consumables.attributes Attribute name or array of attribute names.
  * @param consumables.classes Class name or array of class names.
  * @param consumables.styles Style name or array of style names.
  * @returns Returns `true` when all items included in method's call can be consumed. Returns `false`
  * when first already consumed item is found and `null` when first non-consumable item is found.
  */
  test(t, e) {
    const n = this._consumables.get(t);
    return n === void 0 ? null : t.is("$text") || t.is("documentFragment") ? n : n.test(e);
  }
  /**
  * Consumes {@link module:engine/view/element~Element view element}, {@link module:engine/view/text~Text text node} or
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment}.
  * It returns `true` when all items included in method's call can be consumed, otherwise returns `false`.
  *
  * ```ts
  * viewConsumable.consume( p, { name: true } ); // Consumes element's name.
  * viewConsumable.consume( p, { attributes: 'name' } ); // Consumes element's attribute.
  * viewConsumable.consume( p, { classes: 'foobar' } ); // Consumes element's class.
  * viewConsumable.consume( p, { styles: 'color' } ); // Consumes element's style.
  * viewConsumable.consume( p, { attributes: 'name', styles: 'color' } ); // Consumes attribute and style.
  * viewConsumable.consume( p, { classes: [ 'baz', 'bar' ] } ); // Multiple consumables can be consumed.
  * viewConsumable.consume( textNode ); // Consumes text node.
  * viewConsumable.consume( docFragment ); // Consumes document fragment.
  * ```
  *
  * Consuming classes and styles as attribute will test if all added classes/styles can be consumed.
  *
  * ```ts
  * viewConsumable.consume( p, { attributes: 'class' } ); // Consume only if all added classes can be consumed.
  * viewConsumable.consume( p, { attributes: 'style' } ); // Consume only if all added styles can be consumed.
  * ```
  *
  * @param consumables Used only if first parameter is {@link module:engine/view/element~Element view element} instance.
  * @param consumables.name If set to true element's name will be included.
  * @param consumables.attributes Attribute name or array of attribute names.
  * @param consumables.classes Class name or array of class names.
  * @param consumables.styles Style name or array of style names.
  * @returns Returns `true` when all items included in method's call can be consumed,
  * otherwise returns `false`.
  */
  consume(t, e) {
    return this.test(t, e) ? (t.is("$text") || t.is("documentFragment") ? this._consumables.set(t, !1) : this._consumables.get(t).consume(e), !0) : !1;
  }
  /**
  * Reverts {@link module:engine/view/element~Element view element}, {@link module:engine/view/text~Text text node} or
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment} so they can be consumed once again.
  * Method does not revert items that were never previously added for consumption, even if they are included in
  * method's call.
  *
  * ```ts
  * viewConsumable.revert( p, { name: true } ); // Reverts element's name.
  * viewConsumable.revert( p, { attributes: 'name' } ); // Reverts element's attribute.
  * viewConsumable.revert( p, { classes: 'foobar' } ); // Reverts element's class.
  * viewConsumable.revert( p, { styles: 'color' } ); // Reverts element's style.
  * viewConsumable.revert( p, { attributes: 'name', styles: 'color' } ); // Reverts attribute and style.
  * viewConsumable.revert( p, { classes: [ 'baz', 'bar' ] } ); // Multiple names can be reverted.
  * viewConsumable.revert( textNode ); // Reverts text node.
  * viewConsumable.revert( docFragment ); // Reverts document fragment.
  * ```
  *
  * Reverting classes and styles as attribute will revert all classes/styles that were previously added for
  * consumption.
  *
  * ```ts
  * viewConsumable.revert( p, { attributes: 'class' } ); // Reverts all classes added for consumption.
  * viewConsumable.revert( p, { attributes: 'style' } ); // Reverts all styles added for consumption.
  * ```
  *
  * @param consumables Used only if first parameter is {@link module:engine/view/element~Element view element} instance.
  * @param consumables.name If set to true element's name will be included.
  * @param consumables.attributes Attribute name or array of attribute names.
  * @param consumables.classes Class name or array of class names.
  * @param consumables.styles Style name or array of style names.
  */
  revert(t, e) {
    const n = this._consumables.get(t);
    n !== void 0 && (t.is("$text") || t.is("documentFragment") ? this._consumables.set(t, !0) : n.revert(e));
  }
  /**
  * Creates consumable object from {@link module:engine/view/element~Element view element}. Consumable object will include
  * element's name and all its attributes, classes and styles.
  */
  static consumablesFromElement(t) {
    const e = {
      element: t,
      name: !0,
      attributes: [],
      classes: [],
      styles: []
    }, n = t.getAttributeKeys();
    for (const o of n)
      o == "style" || o == "class" || e.attributes.push(o);
    const r = t.getClassNames();
    for (const o of r)
      e.classes.push(o);
    const s = t.getStyleNames();
    for (const o of s)
      e.styles.push(o);
    return e;
  }
  /**
  * Creates {@link module:engine/conversion/viewconsumable~ViewConsumable ViewConsumable} instance from
  * {@link module:engine/view/node~Node node} or {@link module:engine/view/documentfragment~DocumentFragment document fragment}.
  * Instance will contain all elements, child nodes, attributes, styles and classes added for consumption.
  *
  * @param from View node or document fragment from which `ViewConsumable` will be created.
  * @param instance If provided, given `ViewConsumable` instance will be used
  * to add all consumables. It will be returned instead of a new instance.
  */
  static createFrom(t, e) {
    if (e || (e = new dl()), t.is("$text"))
      return e.add(t), e;
    t.is("element") && e.add(t, dl.consumablesFromElement(t)), t.is("documentFragment") && e.add(t);
    for (const n of t.getChildren())
      e = dl.createFrom(n, e);
    return e;
  }
}
const Lc = [
  "attributes",
  "classes",
  "styles"
];
class d7 {
  /**
  * Creates ViewElementConsumables instance.
  *
  * @param from View node or document fragment from which `ViewElementConsumables` is being created.
  */
  constructor(t) {
    h(this, "element");
    /**
    * Flag indicating if name of the element can be consumed.
    */
    h(this, "_canConsumeName");
    /**
    * Contains maps of element's consumables: attributes, classes and styles.
    */
    h(this, "_consumables");
    this.element = t, this._canConsumeName = null, this._consumables = {
      attributes: /* @__PURE__ */ new Map(),
      styles: /* @__PURE__ */ new Map(),
      classes: /* @__PURE__ */ new Map()
    };
  }
  /**
  * Adds consumable parts of the {@link module:engine/view/element~Element view element}.
  * Element's name itself can be marked to be consumed (when element's name is consumed its attributes, classes and
  * styles still could be consumed):
  *
  * ```ts
  * consumables.add( { name: true } );
  * ```
  *
  * Attributes classes and styles:
  *
  * ```ts
  * consumables.add( { attributes: 'title', classes: 'foo', styles: 'color' } );
  * consumables.add( { attributes: [ 'title', 'name' ], classes: [ 'foo', 'bar' ] );
  * ```
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `viewconsumable-invalid-attribute` when `class` or `style`
  * attribute is provided - it should be handled separately by providing `style` and `class` in consumables object.
  *
  * @param consumables Object describing which parts of the element can be consumed.
  * @param consumables.name If set to `true` element's name will be added as consumable.
  * @param consumables.attributes Attribute name or array of attribute names to add as consumable.
  * @param consumables.classes Class name or array of class names to add as consumable.
  * @param consumables.styles Style name or array of style names to add as consumable.
  */
  add(t) {
    t.name && (this._canConsumeName = !0);
    for (const e of Lc)
      e in t && this._add(e, t[e]);
  }
  /**
  * Tests if parts of the {@link module:engine/view/node~Node view node} can be consumed.
  *
  * Element's name can be tested:
  *
  * ```ts
  * consumables.test( { name: true } );
  * ```
  *
  * Attributes classes and styles:
  *
  * ```ts
  * consumables.test( { attributes: 'title', classes: 'foo', styles: 'color' } );
  * consumables.test( { attributes: [ 'title', 'name' ], classes: [ 'foo', 'bar' ] );
  * ```
  *
  * @param consumables Object describing which parts of the element should be tested.
  * @param consumables.name If set to `true` element's name will be tested.
  * @param consumables.attributes Attribute name or array of attribute names to test.
  * @param consumables.classes Class name or array of class names to test.
  * @param consumables.styles Style name or array of style names to test.
  * @returns `true` when all tested items can be consumed, `null` when even one of the items
  * was never marked for consumption and `false` when even one of the items was already consumed.
  */
  test(t) {
    if (t.name && !this._canConsumeName)
      return this._canConsumeName;
    for (const e of Lc)
      if (e in t) {
        const n = this._test(e, t[e]);
        if (n !== !0)
          return n;
      }
    return !0;
  }
  /**
  * Consumes parts of {@link module:engine/view/element~Element view element}. This function does not check if consumable item
  * is already consumed - it consumes all consumable items provided.
  * Element's name can be consumed:
  *
  * ```ts
  * consumables.consume( { name: true } );
  * ```
  *
  * Attributes classes and styles:
  *
  * ```ts
  * consumables.consume( { attributes: 'title', classes: 'foo', styles: 'color' } );
  * consumables.consume( { attributes: [ 'title', 'name' ], classes: [ 'foo', 'bar' ] );
  * ```
  *
  * @param consumables Object describing which parts of the element should be consumed.
  * @param consumables.name If set to `true` element's name will be consumed.
  * @param consumables.attributes Attribute name or array of attribute names to consume.
  * @param consumables.classes Class name or array of class names to consume.
  * @param consumables.styles Style name or array of style names to consume.
  */
  consume(t) {
    t.name && (this._canConsumeName = !1);
    for (const e of Lc)
      e in t && this._consume(e, t[e]);
  }
  /**
  * Revert already consumed parts of {@link module:engine/view/element~Element view Element}, so they can be consumed once again.
  * Element's name can be reverted:
  *
  * ```ts
  * consumables.revert( { name: true } );
  * ```
  *
  * Attributes classes and styles:
  *
  * ```ts
  * consumables.revert( { attributes: 'title', classes: 'foo', styles: 'color' } );
  * consumables.revert( { attributes: [ 'title', 'name' ], classes: [ 'foo', 'bar' ] );
  * ```
  *
  * @param consumables Object describing which parts of the element should be reverted.
  * @param consumables.name If set to `true` element's name will be reverted.
  * @param consumables.attributes Attribute name or array of attribute names to revert.
  * @param consumables.classes Class name or array of class names to revert.
  * @param consumables.styles Style name or array of style names to revert.
  */
  revert(t) {
    t.name && (this._canConsumeName = !0);
    for (const e of Lc)
      e in t && this._revert(e, t[e]);
  }
  /**
  * Helper method that adds consumables of a given type: attribute, class or style.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `viewconsumable-invalid-attribute` when `class` or `style`
  * type is provided - it should be handled separately by providing actual style/class type.
  *
  * @param type Type of the consumable item: `attributes`, `classes` or `styles`.
  * @param item Consumable item or array of items.
  */
  _add(t, e) {
    const n = tt(e), r = this._consumables[t];
    for (const s of n) {
      if (t === "attributes" && (s === "class" || s === "style"))
        throw new T("viewconsumable-invalid-attribute", this);
      if (r.set(s, !0), t === "styles")
        for (const o of this.element.document.stylesProcessor.getRelatedStyles(s))
          r.set(o, !0);
    }
  }
  /**
  * Helper method that tests consumables of a given type: attribute, class or style.
  *
  * @param type Type of the consumable item: `attributes`, `classes` or `styles`.
  * @param item Consumable item or array of items.
  * @returns Returns `true` if all items can be consumed, `null` when one of the items cannot be
  * consumed and `false` when one of the items is already consumed.
  */
  _test(t, e) {
    const n = tt(e), r = this._consumables[t];
    for (const s of n)
      if (t === "attributes" && (s === "class" || s === "style")) {
        const o = s == "class" ? "classes" : "styles", a = this._test(o, [
          ...this._consumables[o].keys()
        ]);
        if (a !== !0)
          return a;
      } else {
        const o = r.get(s);
        if (o === void 0)
          return null;
        if (!o)
          return !1;
      }
    return !0;
  }
  /**
  * Helper method that consumes items of a given type: attribute, class or style.
  *
  * @param type Type of the consumable item: `attributes`, `classes` or `styles`.
  * @param item Consumable item or array of items.
  */
  _consume(t, e) {
    const n = tt(e), r = this._consumables[t];
    for (const s of n)
      if (t === "attributes" && (s === "class" || s === "style")) {
        const o = s == "class" ? "classes" : "styles";
        this._consume(o, [
          ...this._consumables[o].keys()
        ]);
      } else if (r.set(s, !1), t == "styles")
        for (const o of this.element.document.stylesProcessor.getRelatedStyles(s))
          r.set(o, !1);
  }
  /**
  * Helper method that reverts items of a given type: attribute, class or style.
  *
  * @param type Type of the consumable item: `attributes`, `classes` or , `styles`.
  * @param item Consumable item or array of items.
  */
  _revert(t, e) {
    const n = tt(e), r = this._consumables[t];
    for (const s of n)
      if (t === "attributes" && (s === "class" || s === "style")) {
        const o = s == "class" ? "classes" : "styles";
        this._revert(o, [
          ...this._consumables[o].keys()
        ]);
      } else
        r.get(s) === !1 && r.set(s, !0);
  }
}
class h7 extends (/* @__PURE__ */ st()) {
  /**
  * Creates a schema instance.
  */
  constructor() {
    super();
    h(this, "_sourceDefinitions", {});
    /**
    * A dictionary containing attribute properties.
    */
    h(this, "_attributeProperties", /* @__PURE__ */ Object.create(null));
    /**
    * Stores additional callbacks registered for schema items, which are evaluated when {@link ~Schema#checkChild} is called.
    *
    * Keys are schema item names for which the callbacks are registered. Values are arrays with the callbacks.
    *
    * Some checks are added under {@link ~Schema#_genericCheckSymbol} key, these are evaluated for every {@link ~Schema#checkChild} call.
    */
    h(this, "_customChildChecks", /* @__PURE__ */ new Map());
    /**
    * Stores additional callbacks registered for attribute names, which are evaluated when {@link ~Schema#checkAttribute} is called.
    *
    * Keys are schema attribute names for which the callbacks are registered. Values are arrays with the callbacks.
    *
    * Some checks are added under {@link ~Schema#_genericCheckSymbol} key, these are evaluated for every
    * {@link ~Schema#checkAttribute} call.
    */
    h(this, "_customAttributeChecks", /* @__PURE__ */ new Map());
    h(this, "_genericCheckSymbol", Symbol("$generic"));
    h(this, "_compiledDefinitions");
    this.decorate("checkChild"), this.decorate("checkAttribute"), this.on("checkAttribute", (e, n) => {
      n[0] = new ss(n[0]);
    }, {
      priority: "highest"
    }), this.on("checkChild", (e, n) => {
      n[0] = new ss(n[0]), n[1] = this.getDefinition(n[1]);
    }, {
      priority: "highest"
    });
  }
  /**
  * Registers a schema item. Can only be called once for every item name.
  *
  * ```ts
  * schema.register( 'paragraph', {
  * 	inheritAllFrom: '$block'
  * } );
  * ```
  */
  register(e, n) {
    if (this._sourceDefinitions[e])
      throw new T("schema-cannot-register-item-twice", this, {
        itemName: e
      });
    this._sourceDefinitions[e] = [
      Object.assign({}, n)
    ], this._clearCache();
  }
  /**
  * Extends a {@link #register registered} item's definition.
  *
  * Extending properties such as `allowIn` will add more items to the existing properties,
  * while redefining properties such as `isBlock` will override the previously defined ones.
  *
  * ```ts
  * schema.register( 'foo', {
  * 	allowIn: '$root',
  * 	isBlock: true;
  * } );
  * schema.extend( 'foo', {
  * 	allowIn: 'blockQuote',
  * 	isBlock: false
  * } );
  *
  * schema.getDefinition( 'foo' );
  * //	{
  * //		allowIn: [ '$root', 'blockQuote' ],
  * // 		isBlock: false
  * //	}
  * ```
  */
  extend(e, n) {
    if (!this._sourceDefinitions[e])
      throw new T("schema-cannot-extend-missing-item", this, {
        itemName: e
      });
    this._sourceDefinitions[e].push(Object.assign({}, n)), this._clearCache();
  }
  /**
  * Returns data of all registered items.
  *
  * This method should normally be used for reflection purposes (e.g. defining a clone of a certain element,
  * checking a list of all block elements, etc).
  * Use specific methods (such as {@link #checkChild `checkChild()`} or {@link #isLimit `isLimit()`})
  * in other cases.
  */
  getDefinitions() {
    return this._compiledDefinitions || this._compile(), this._compiledDefinitions;
  }
  /**
  * Returns a definition of the given item or `undefined` if an item is not registered.
  *
  * This method should normally be used for reflection purposes (e.g. defining a clone of a certain element,
  * checking a list of all block elements, etc).
  * Use specific methods (such as {@link #checkChild `checkChild()`} or {@link #isLimit `isLimit()`})
  * in other cases.
  */
  getDefinition(e) {
    let n;
    return typeof e == "string" ? n = e : "is" in e && (e.is("$text") || e.is("$textProxy")) ? n = "$text" : n = e.name, this.getDefinitions()[n];
  }
  /**
  * Returns `true` if the given item is registered in the schema.
  *
  * ```ts
  * schema.isRegistered( 'paragraph' ); // -> true
  * schema.isRegistered( editor.model.document.getRoot() ); // -> true
  * schema.isRegistered( 'foo' ); // -> false
  * ```
  */
  isRegistered(e) {
    return !!this.getDefinition(e);
  }
  /**
  * Returns `true` if the given item is defined to be
  * a block by the {@link module:engine/model/schema~SchemaItemDefinition}'s `isBlock` property.
  *
  * ```ts
  * schema.isBlock( 'paragraph' ); // -> true
  * schema.isBlock( '$root' ); // -> false
  *
  * const paragraphElement = writer.createElement( 'paragraph' );
  * schema.isBlock( paragraphElement ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#block-elements Block elements} section of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isBlock(e) {
    const n = this.getDefinition(e);
    return !!(n && n.isBlock);
  }
  /**
  * Returns `true` if the given item should be treated as a limit element.
  *
  * It considers an item to be a limit element if its
  * {@link module:engine/model/schema~SchemaItemDefinition}'s
  * {@link module:engine/model/schema~SchemaItemDefinition#isLimit `isLimit`} or
  * {@link module:engine/model/schema~SchemaItemDefinition#isObject `isObject`} property
  * was set to `true`.
  *
  * ```ts
  * schema.isLimit( 'paragraph' ); // -> false
  * schema.isLimit( '$root' ); // -> true
  * schema.isLimit( editor.model.document.getRoot() ); // -> true
  * schema.isLimit( 'imageBlock' ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#limit-elements Limit elements} section of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isLimit(e) {
    const n = this.getDefinition(e);
    return n ? !!(n.isLimit || n.isObject) : !1;
  }
  /**
  * Returns `true` if the given item should be treated as an object element.
  *
  * It considers an item to be an object element if its
  * {@link module:engine/model/schema~SchemaItemDefinition}'s
  * {@link module:engine/model/schema~SchemaItemDefinition#isObject `isObject`} property
  * was set to `true`.
  *
  * ```ts
  * schema.isObject( 'paragraph' ); // -> false
  * schema.isObject( 'imageBlock' ); // -> true
  *
  * const imageElement = writer.createElement( 'imageBlock' );
  * schema.isObject( imageElement ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#object-elements Object elements} section of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isObject(e) {
    const n = this.getDefinition(e);
    return n ? !!(n.isObject || n.isLimit && n.isSelectable && n.isContent) : !1;
  }
  /**
  * Returns `true` if the given item is defined to be
  * an inline element by the {@link module:engine/model/schema~SchemaItemDefinition}'s `isInline` property.
  *
  * ```ts
  * schema.isInline( 'paragraph' ); // -> false
  * schema.isInline( 'softBreak' ); // -> true
  *
  * const text = writer.createText( 'foo' );
  * schema.isInline( text ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#inline-elements Inline elements} section of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isInline(e) {
    const n = this.getDefinition(e);
    return !!(n && n.isInline);
  }
  /**
  * Returns `true` if the given item is defined to be
  * a selectable element by the {@link module:engine/model/schema~SchemaItemDefinition}'s `isSelectable` property.
  *
  * ```ts
  * schema.isSelectable( 'paragraph' ); // -> false
  * schema.isSelectable( 'heading1' ); // -> false
  * schema.isSelectable( 'imageBlock' ); // -> true
  * schema.isSelectable( 'tableCell' ); // -> true
  *
  * const text = writer.createText( 'foo' );
  * schema.isSelectable( text ); // -> false
  * ```
  *
  * See the {@glink framework/deep-dive/schema#selectable-elements Selectable elements section} of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isSelectable(e) {
    const n = this.getDefinition(e);
    return n ? !!(n.isSelectable || n.isObject) : !1;
  }
  /**
  * Returns `true` if the given item is defined to be
  * a content by the {@link module:engine/model/schema~SchemaItemDefinition}'s `isContent` property.
  *
  * ```ts
  * schema.isContent( 'paragraph' ); // -> false
  * schema.isContent( 'heading1' ); // -> false
  * schema.isContent( 'imageBlock' ); // -> true
  * schema.isContent( 'horizontalLine' ); // -> true
  *
  * const text = writer.createText( 'foo' );
  * schema.isContent( text ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#content-elements Content elements section} of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isContent(e) {
    const n = this.getDefinition(e);
    return n ? !!(n.isContent || n.isObject) : !1;
  }
  /**
  * Checks whether the given node can be a child of the given context.
  *
  * ```ts
  * schema.checkChild( model.document.getRoot(), paragraph ); // -> false
  *
  * schema.register( 'paragraph', {
  * 	allowIn: '$root'
  * } );
  *
  * schema.checkChild( model.document.getRoot(), paragraph ); // -> true
  * ```
  *
  * Both {@link module:engine/model/schema~Schema#addChildCheck callback checks} and declarative rules (added when
  * {@link module:engine/model/schema~Schema#register registering} and {@link module:engine/model/schema~Schema#extend extending} items)
  * are evaluated when this method is called.
  *
  * Note that callback checks have bigger priority than declarative rules checks and may overwrite them.
  *
  * Note that when verifying whether the given node can be a child of the given context, the schema also verifies the entire
  * context &ndash; from its root to its last element. Therefore, it is possible for `checkChild()` to return `false` even though
  * the `context` last element can contain the checked child. It happens if one of the `context` elements does not allow its child.
  * When `context` is verified, {@link module:engine/model/schema~Schema#addChildCheck custom checks} are considered as well.
  *
  * @fires checkChild
  * @param context The context in which the child will be checked.
  * @param def The child to check.
  */
  checkChild(e, n) {
    return n ? this._checkContextMatch(e, n) : !1;
  }
  /**
  * Checks whether the given attribute can be applied in the given context (on the last item of the context).
  *
  * ```ts
  * schema.checkAttribute( textNode, 'bold' ); // -> false
  *
  * schema.extend( '$text', {
  * 	allowAttributes: 'bold'
  * } );
  *
  * schema.checkAttribute( textNode, 'bold' ); // -> true
  * ```
  *
  * Both {@link module:engine/model/schema~Schema#addAttributeCheck callback checks} and declarative rules (added when
  * {@link module:engine/model/schema~Schema#register registering} and {@link module:engine/model/schema~Schema#extend extending} items)
  * are evaluated when this method is called.
  *
  * Note that callback checks have bigger priority than declarative rules checks and may overwrite them.
  *
  * @fires checkAttribute
  * @param context The context in which the attribute will be checked.
  * @param attributeName Name of attribute to check in the given context.
  */
  checkAttribute(e, n) {
    const r = this.getDefinition(e.last);
    if (!r)
      return !1;
    const s = this._evaluateAttributeChecks(e, n);
    return s !== void 0 ? s : r.allowAttributes.includes(n);
  }
  /**
  * Checks whether the given element (`elementToMerge`) can be merged with the specified base element (`positionOrBaseElement`).
  *
  * In other words &ndash; both elements are not a limit elements and whether `elementToMerge`'s children
  * {@link #checkChild are allowed} in the `positionOrBaseElement`.
  *
  * This check ensures that elements merged with {@link module:engine/model/writer~Writer#merge `Writer#merge()`}
  * will be valid.
  *
  * Instead of elements, you can pass the instance of the {@link module:engine/model/position~Position} class as the
  * `positionOrBaseElement`. It means that the elements before and after the position will be checked whether they can be merged.
  *
  * @param positionOrBaseElement The position or base element to which the `elementToMerge` will be merged.
  * @param elementToMerge The element to merge. Required if `positionOrBaseElement` is an element.
  */
  checkMerge(e, n) {
    if (e instanceof G) {
      const r = e.nodeBefore, s = e.nodeAfter;
      if (!(r instanceof xt))
        throw new T("schema-check-merge-no-element-before", this);
      if (!(s instanceof xt))
        throw new T("schema-check-merge-no-element-after", this);
      return this.checkMerge(r, s);
    }
    if (this.isLimit(e) || this.isLimit(n))
      return !1;
    for (const r of n.getChildren())
      if (!this.checkChild(e, r))
        return !1;
    return !0;
  }
  /**
  * Allows registering a callback to the {@link #checkChild} method calls.
  *
  * Callbacks allow you to implement rules which are not otherwise possible to achieve
  * by using the declarative API of {@link module:engine/model/schema~SchemaItemDefinition}.
  *
  * Note that callback checks have bigger priority than declarative rules checks and may overwrite them.
  *
  * For example, by using this method you can disallow elements in specific contexts:
  *
  * ```ts
  * // Disallow `heading1` inside a `blockQuote` that is inside a table.
  * schema.addChildCheck( ( context, childDefinition ) => {
  * 	if ( context.endsWith( 'tableCell blockQuote' ) ) {
  * 		return false;
  * 	}
  * }, 'heading1' );
  * ```
  *
  * You can skip the optional `itemName` parameter to evaluate the callback for every `checkChild()` call.
  *
  * ```ts
  * // Inside specific custom element, allow only children, which allows for a specific attribute.
  * schema.addChildCheck( ( context, childDefinition ) => {
  * 	if ( context.endsWith( 'myElement' ) ) {
  * 		return childDefinition.allowAttributes.includes( 'myAttribute' );
  * 	}
  * } );
  * ```
  *
  * Please note that the generic callbacks may affect the editor performance and should be avoided if possible.
  *
  * When one of the callbacks makes a decision (returns `true` or `false`) the processing is finished and other callbacks are not fired.
  * Callbacks are fired in the order they were added, however generic callbacks are fired before callbacks added for a specified item.
  *
  * You can also use `checkChild` event, if you need even better control. The result from the example above could also be
  * achieved with following event callback:
  *
  * ```ts
  * schema.on( 'checkChild', ( evt, args ) => {
  * 	const context = args[ 0 ];
  * 	const childDefinition = args[ 1 ];
  *
  * 	if ( context.endsWith( 'myElement' ) ) {
  * 		// Prevent next listeners from being called.
  * 		evt.stop();
  * 		// Set the `checkChild()` return value.
  * 		evt.return = childDefinition.allowAttributes.includes( 'myAttribute' );
  * 	}
  * }, { priority: 'high' } );
  * ```
  *
  * Note that the callback checks and declarative rules checks are processed on `normal` priority.
  *
  * Adding callbacks this way can also negatively impact editor performance.
  *
  * @param callback The callback to be called. It is called with two parameters:
  * {@link module:engine/model/schema~SchemaContext} (context) instance and
  * {@link module:engine/model/schema~SchemaCompiledItemDefinition} (definition). The callback may return `true/false` to override
  * `checkChild()`'s return value. If it does not return a boolean value, the default algorithm (or other callbacks) will define
  * `checkChild()`'s return value.
  * @param itemName Name of the schema item for which the callback is registered. If specified, the callback will be run only for
  * `checkChild()` calls which `def` parameter matches the `itemName`. Otherwise, the callback will run for every `checkChild` call.
  */
  addChildCheck(e, n) {
    const r = n !== void 0 ? n : this._genericCheckSymbol, s = this._customChildChecks.get(r) || [];
    s.push(e), this._customChildChecks.set(r, s);
  }
  /**
  * Allows registering a callback to the {@link #checkAttribute} method calls.
  *
  * Callbacks allow you to implement rules which are not otherwise possible to achieve
  * by using the declarative API of {@link module:engine/model/schema~SchemaItemDefinition}.
  *
  * Note that callback checks have bigger priority than declarative rules checks and may overwrite them.
  *
  * For example, by using this method you can disallow setting attributes on nodes in specific contexts:
  *
  * ```ts
  * // Disallow setting `bold` on text inside `heading1` element:
  * schema.addAttributeCheck( context => {
  * 	if ( context.endsWith( 'heading1 $text' ) ) {
  * 		return false;
  * 	}
  * }, 'bold' );
  * ```
  *
  * You can skip the optional `attributeName` parameter to evaluate the callback for every `checkAttribute()` call.
  *
  * ```ts
  * // Disallow formatting attributes on text inside custom `myTitle` element:
  * schema.addAttributeCheck( ( context, attributeName ) => {
  * 	if ( context.endsWith( 'myTitle $text' ) && schema.getAttributeProperties( attributeName ).isFormatting ) {
  * 		return false;
  * 	}
  * } );
  * ```
  *
  * Please note that the generic callbacks may affect the editor performance and should be avoided if possible.
  *
  * When one of the callbacks makes a decision (returns `true` or `false`) the processing is finished and other callbacks are not fired.
  * Callbacks are fired in the order they were added, however generic callbacks are fired before callbacks added for a specified item.
  *
  * You can also use {@link #event:checkAttribute} event, if you need even better control. The result from the example above could also
  * be achieved with following event callback:
  *
  * ```ts
  * schema.on( 'checkAttribute', ( evt, args ) => {
  * 	const context = args[ 0 ];
  * 	const attributeName = args[ 1 ];
  *
  * 	if ( context.endsWith( 'myTitle $text' ) && schema.getAttributeProperties( attributeName ).isFormatting ) {
  * 		// Prevent next listeners from being called.
  * 		evt.stop();
  * 		// Set the `checkAttribute()` return value.
  * 		evt.return = false;
  * 	}
  * }, { priority: 'high' } );
  * ```
  *
  * Note that the callback checks and declarative rules checks are processed on `normal` priority.
  *
  * Adding callbacks this way can also negatively impact editor performance.
  *
  * @param callback The callback to be called. It is called with two parameters:
  * {@link module:engine/model/schema~SchemaContext `context`} and attribute name. The callback may return `true` or `false`, to
  * override `checkAttribute()`'s return value. If it does not return a boolean value, the default algorithm (or other callbacks)
  * will define `checkAttribute()`'s return value.
  * @param attributeName Name of the attribute for which the callback is registered. If specified, the callback will be run only for
  * `checkAttribute()` calls with matching `attributeName`. Otherwise, the callback will run for every `checkAttribute()` call.
  */
  addAttributeCheck(e, n) {
    const r = n !== void 0 ? n : this._genericCheckSymbol, s = this._customAttributeChecks.get(r) || [];
    s.push(e), this._customAttributeChecks.set(r, s);
  }
  /**
  * This method allows assigning additional metadata to the model attributes. For example,
  * {@link module:engine/model/schema~AttributeProperties `AttributeProperties#isFormatting` property} is
  * used to mark formatting attributes (like `bold` or `italic`).
  *
  * ```ts
  * // Mark bold as a formatting attribute.
  * schema.setAttributeProperties( 'bold', {
  * 	isFormatting: true
  * } );
  *
  * // Override code not to be considered a formatting markup.
  * schema.setAttributeProperties( 'code', {
  * 	isFormatting: false
  * } );
  * ```
  *
  * Properties are not limited to members defined in the
  * {@link module:engine/model/schema~AttributeProperties `AttributeProperties` type} and you can also use custom properties:
  *
  * ```ts
  * schema.setAttributeProperties( 'blockQuote', {
  * 	customProperty: 'value'
  * } );
  * ```
  *
  * Subsequent calls with the same attribute will extend its custom properties:
  *
  * ```ts
  * schema.setAttributeProperties( 'blockQuote', {
  * 	one: 1
  * } );
  *
  * schema.setAttributeProperties( 'blockQuote', {
  * 	two: 2
  * } );
  *
  * console.log( schema.getAttributeProperties( 'blockQuote' ) );
  * // Logs: { one: 1, two: 2 }
  * ```
  *
  * @param attributeName A name of the attribute to receive the properties.
  * @param properties A dictionary of properties.
  */
  setAttributeProperties(e, n) {
    this._attributeProperties[e] = Object.assign(this.getAttributeProperties(e), n);
  }
  /**
  * Returns properties associated with a given model attribute. See {@link #setAttributeProperties `setAttributeProperties()`}.
  *
  * @param attributeName A name of the attribute.
  */
  getAttributeProperties(e) {
    return this._attributeProperties[e] || /* @__PURE__ */ Object.create(null);
  }
  /**
  * Returns the lowest {@link module:engine/model/schema~Schema#isLimit limit element} containing the entire
  * selection/range/position or the root otherwise.
  *
  * @param selectionOrRangeOrPosition The selection/range/position to check.
  * @returns The lowest limit element containing the entire `selectionOrRangeOrPosition`.
  */
  getLimitElement(e) {
    let n;
    for (e instanceof G ? n = e.parent : n = (e instanceof D ? [
      e
    ] : Array.from(e.getRanges())).reduce((s, o) => {
      const a = o.getCommonAncestor();
      return s ? s.getCommonAncestor(a, {
        includeSelf: !0
      }) : a;
    }, null); !this.isLimit(n) && n.parent; )
      n = n.parent;
    return n;
  }
  /**
  * Checks whether the attribute is allowed in selection:
  *
  * * if the selection is not collapsed, then checks if the attribute is allowed on any of nodes in that range,
  * * if the selection is collapsed, then checks if on the selection position there's a text with the
  * specified attribute allowed.
  *
  * @param selection Selection which will be checked.
  * @param attribute The name of the attribute to check.
  */
  checkAttributeInSelection(e, n) {
    if (e.isCollapsed) {
      const s = [
        ...e.getFirstPosition().getAncestors(),
        new mt("", e.getAttributes())
      ];
      return this.checkAttribute(s, n);
    } else {
      const r = e.getRanges();
      for (const s of r)
        for (const o of s)
          if (this.checkAttribute(o.item, n))
            return !0;
    }
    return !1;
  }
  /**
  * Transforms the given set of ranges into a set of ranges where the given attribute is allowed (and can be applied).
  *
  * @param ranges Ranges to be validated.
  * @param attribute The name of the attribute to check.
  * @returns Ranges in which the attribute is allowed.
  */
  *getValidRanges(e, n) {
    e = A7(e);
    for (const r of e)
      yield* this._getValidRangesForRange(r, n);
  }
  /**
  * Basing on given `position`, finds and returns a {@link module:engine/model/range~Range range} which is
  * nearest to that `position` and is a correct range for selection.
  *
  * The correct selection range might be collapsed when it is located in a position where the text node can be placed.
  * Non-collapsed range is returned when selection can be placed around element marked as an "object" in
  * the {@link module:engine/model/schema~Schema schema}.
  *
  * Direction of searching for the nearest correct selection range can be specified as:
  *
  * * `both` - searching will be performed in both ways,
  * * `forward` - searching will be performed only forward,
  * * `backward` - searching will be performed only backward.
  *
  * When valid selection range cannot be found, `null` is returned.
  *
  * @param position Reference position where new selection range should be looked for.
  * @param direction Search direction.
  * @returns Nearest selection range or `null` if one cannot be found.
  */
  getNearestSelectionRange(e, n = "both") {
    if (e.root.rootName == "$graveyard")
      return null;
    if (this.checkChild(e, "$text"))
      return new D(e);
    let r, s;
    const o = e.getAncestors().reverse().find((a) => this.isLimit(a)) || e.root;
    (n == "both" || n == "backward") && (r = new Vr({
      boundaries: D._createIn(o),
      startPosition: e,
      direction: "backward"
    })), (n == "both" || n == "forward") && (s = new Vr({
      boundaries: D._createIn(o),
      startPosition: e
    }));
    for (const a of S7(r, s)) {
      const l = a.walker == r ? "elementEnd" : "elementStart", c = a.value;
      if (c.type == l && this.isObject(c.item))
        return D._createOn(c.item);
      if (this.checkChild(c.nextPosition, "$text"))
        return new D(c.nextPosition);
    }
    return null;
  }
  /**
  * Tries to find position ancestors that allow to insert a given node.
  * It starts searching from the given position and goes node by node to the top of the model tree
  * as long as a {@link module:engine/model/schema~Schema#isLimit limit element}, an
  * {@link module:engine/model/schema~Schema#isObject object element} or a topmost ancestor is not reached.
  *
  * @param position The position that the search will start from.
  * @param node The node for which an allowed parent should be found or its name.
  * @returns Allowed parent or null if nothing was found.
  */
  findAllowedParent(e, n) {
    let r = e.parent;
    for (; r; ) {
      if (this.checkChild(r, n))
        return r;
      if (this.isLimit(r))
        return null;
      r = r.parent;
    }
    return null;
  }
  /**
  * Sets attributes allowed by the schema on a given node.
  *
  * @param node A node to set attributes on.
  * @param attributes Attributes keys and values.
  * @param writer An instance of the model writer.
  */
  setAllowedAttributes(e, n, r) {
    const s = r.model;
    for (const [o, a] of Object.entries(n))
      s.schema.checkAttribute(e, o) && r.setAttribute(o, a, e);
  }
  /**
  * Removes attributes disallowed by the schema.
  *
  * @param nodes Nodes that will be filtered.
  */
  removeDisallowedAttributes(e, n) {
    for (const r of e)
      if (r.is("$text"))
        K1(this, r, n);
      else {
        const o = D._createIn(r).getPositions();
        for (const a of o) {
          const l = a.nodeBefore || a.parent;
          K1(this, l, n);
        }
      }
  }
  /**
  * Gets attributes of a node that have a given property.
  *
  * @param node Node to get attributes from.
  * @param propertyName Name of the property that attribute must have to return it.
  * @param propertyValue Desired value of the property that we want to check.
  * When `undefined` attributes will be returned if they have set a given property no matter what the value is. If specified it will
  * return attributes which given property's value is equal to this parameter.
  * @returns Object with attributes' names as key and attributes' values as value.
  */
  getAttributesWithProperty(e, n, r) {
    const s = {};
    for (const [o, a] of e.getAttributes()) {
      const l = this.getAttributeProperties(o);
      l[n] !== void 0 && (r === void 0 || r === l[n]) && (s[o] = a);
    }
    return s;
  }
  /**
  * Creates an instance of the schema context.
  */
  createContext(e) {
    return new ss(e);
  }
  _clearCache() {
    this._compiledDefinitions = null;
  }
  _compile() {
    const e = {}, n = this._sourceDefinitions, r = Object.keys(n);
    for (const o of r)
      e[o] = f7(n[o], o);
    const s = Object.values(e);
    for (const o of s)
      m7(e, o), g7(e, o), p7(e, o), b7(e, o);
    for (const o of s)
      v7(e, o);
    for (const o of s)
      w7(e, o);
    for (const o of s)
      y7(e, o);
    for (const o of s)
      C7(e, o);
    for (const o of s)
      k7(e, o);
    this._compiledDefinitions = _7(e);
  }
  _checkContextMatch(e, n) {
    const r = e.last;
    let s = this._evaluateChildChecks(e, n);
    if (s = s !== void 0 ? s : n.allowIn.includes(r.name), !s)
      return !1;
    const o = this.getDefinition(r), a = e.trimLast();
    return o ? a.length == 0 ? !0 : this._checkContextMatch(a, o) : !1;
  }
  /**
  * Calls child check callbacks to decide whether `def` is allowed in `context`. It uses both generic and specific (defined for `def`
  * item) callbacks. If neither callback makes a decision, `undefined` is returned.
  *
  * Note that the first callback that makes a decision "wins", i.e., if any callback returns `true` or `false`, then the processing
  * is over and that result is returned.
  */
  _evaluateChildChecks(e, n) {
    const r = this._customChildChecks.get(this._genericCheckSymbol) || [], s = this._customChildChecks.get(n.name) || [];
    for (const o of [
      ...r,
      ...s
    ]) {
      const a = o(e, n);
      if (a !== void 0)
        return a;
    }
  }
  /**
  * Calls attribute check callbacks to decide whether `attributeName` can be set on the last element of `context`. It uses both
  * generic and specific (defined for `attributeName`) callbacks. If neither callback makes a decision, `undefined` is returned.
  *
  * Note that the first callback that makes a decision "wins", i.e., if any callback returns `true` or `false`, then the processing
  * is over and that result is returned.
  */
  _evaluateAttributeChecks(e, n) {
    const r = this._customAttributeChecks.get(this._genericCheckSymbol) || [], s = this._customAttributeChecks.get(n) || [];
    for (const o of [
      ...r,
      ...s
    ]) {
      const a = o(e, n);
      if (a !== void 0)
        return a;
    }
  }
  /**
  * Takes a flat range and an attribute name. Traverses the range recursively and deeply to find and return all ranges
  * inside the given range on which the attribute can be applied.
  *
  * This is a helper function for {@link ~Schema#getValidRanges}.
  *
  * @param range The range to process.
  * @param attribute The name of the attribute to check.
  * @returns Ranges in which the attribute is allowed.
  */
  *_getValidRangesForRange(e, n) {
    let r = e.start, s = e.start;
    for (const o of e.getItems({
      shallow: !0
    }))
      o.is("element") && (yield* this._getValidRangesForRange(D._createIn(o), n)), this.checkAttribute(o, n) || (r.isEqual(s) || (yield new D(r, s)), r = G._createAfter(o)), s = G._createAfter(o);
    r.isEqual(s) || (yield new D(r, s));
  }
  /**
  * Returns a model range which is optimal (in terms of UX) for inserting a widget block.
  *
  * For instance, if a selection is in the middle of a paragraph, the collapsed range before this paragraph
  * will be returned so that it is not split. If the selection is at the end of a paragraph,
  * the collapsed range after this paragraph will be returned.
  *
  * Note: If the selection is placed in an empty block, the range in that block will be returned. If that range
  * is then passed to {@link module:engine/model/model~Model#insertContent}, the block will be fully replaced
  * by the inserted widget block.
  *
  * @internal
  * @param selection The selection based on which the insertion position should be calculated.
  * @param place The place where to look for optimal insertion range.
  * The `auto` value will determine itself the best position for insertion.
  * The `before` value will try to find a position before selection.
  * The `after` value will try to find a position after selection.
  * @returns The optimal range.
  */
  findOptimalInsertionRange(e, n) {
    const r = e.getSelectedElement();
    if (r && this.isObject(r) && !this.isInline(r))
      return n == "before" || n == "after" ? new D(G._createAt(r, n)) : D._createOn(r);
    const s = Ft(e.getSelectedBlocks());
    if (!s)
      return new D(e.focus);
    if (s.isEmpty)
      return new D(G._createAt(s, 0));
    const o = G._createAfter(s);
    return e.focus.isTouching(o) ? new D(o) : new D(G._createBefore(s));
  }
}
class ss {
  /**
  * Creates an instance of the context.
  */
  constructor(t) {
    h(this, "_items");
    if (t instanceof ss)
      return t;
    let e;
    typeof t == "string" ? e = [
      t
    ] : Array.isArray(t) ? e = t : e = t.getAncestors({
      includeSelf: !0
    }), this._items = e.map(T7);
  }
  /**
  * The number of items.
  */
  get length() {
    return this._items.length;
  }
  /**
  * The last item (the lowest node).
  */
  get last() {
    return this._items[this._items.length - 1];
  }
  /**
  * Iterable interface.
  *
  * Iterates over all context items.
  */
  [Symbol.iterator]() {
    return this._items[Symbol.iterator]();
  }
  /**
  * Returns a new schema context instance with an additional item.
  *
  * Item can be added as:
  *
  * ```ts
  * const context = new SchemaContext( [ '$root' ] );
  *
  * // An element.
  * const fooElement = writer.createElement( 'fooElement' );
  * const newContext = context.push( fooElement ); // [ '$root', 'fooElement' ]
  *
  * // A text node.
  * const text = writer.createText( 'foobar' );
  * const newContext = context.push( text ); // [ '$root', '$text' ]
  *
  * // A string (element name).
  * const newContext = context.push( 'barElement' ); // [ '$root', 'barElement' ]
  * ```
  *
  * **Note** {@link module:engine/model/node~Node} that is already in the model tree will be added as the only item
  * (without ancestors).
  *
  * @param item An item that will be added to the current context.
  * @returns A new schema context instance with an additional item.
  */
  push(t) {
    const e = new ss([
      t
    ]);
    return e._items = [
      ...this._items,
      ...e._items
    ], e;
  }
  /**
  * Returns a new schema context that is based on this context but has the last item removed.
  *
  * ```ts
  * const ctxParagraph = new SchemaContext( [ '$root', 'blockQuote', 'paragraph' ] );
  * const ctxBlockQuote = ctxParagraph.trimLast(); // Items in `ctxBlockQuote` are: `$root` an `blockQuote`.
  * const ctxRoot = ctxBlockQuote.trimLast(); // Items in `ctxRoot` are: `$root`.
  * ```
  *
  * @returns A new reduced schema context instance.
  */
  trimLast() {
    const t = new ss([]);
    return t._items = this._items.slice(0, -1), t;
  }
  /**
  * Gets an item on the given index.
  */
  getItem(t) {
    return this._items[t];
  }
  /**
  * Returns the names of items.
  */
  *getNames() {
    yield* this._items.map((t) => t.name);
  }
  /**
  * Checks whether the context ends with the given nodes.
  *
  * ```ts
  * const ctx = new SchemaContext( [ rootElement, paragraphElement, textNode ] );
  *
  * ctx.endsWith( '$text' ); // -> true
  * ctx.endsWith( 'paragraph $text' ); // -> true
  * ctx.endsWith( '$root' ); // -> false
  * ctx.endsWith( 'paragraph' ); // -> false
  * ```
  */
  endsWith(t) {
    return Array.from(this.getNames()).join(" ").endsWith(t);
  }
  /**
  * Checks whether the context starts with the given nodes.
  *
  * ```ts
  * const ctx = new SchemaContext( [ rootElement, paragraphElement, textNode ] );
  *
  * ctx.endsWith( '$root' ); // -> true
  * ctx.endsWith( '$root paragraph' ); // -> true
  * ctx.endsWith( '$text' ); // -> false
  * ctx.endsWith( 'paragraph' ); // -> false
  * ```
  */
  startsWith(t) {
    return Array.from(this.getNames()).join(" ").startsWith(t);
  }
}
function f7(i, t) {
  const e = {
    name: t,
    allowIn: /* @__PURE__ */ new Set(),
    allowChildren: /* @__PURE__ */ new Set(),
    disallowIn: /* @__PURE__ */ new Set(),
    disallowChildren: /* @__PURE__ */ new Set(),
    allowContentOf: /* @__PURE__ */ new Set(),
    allowWhere: /* @__PURE__ */ new Set(),
    allowAttributes: /* @__PURE__ */ new Set(),
    disallowAttributes: /* @__PURE__ */ new Set(),
    allowAttributesOf: /* @__PURE__ */ new Set(),
    inheritTypesFrom: /* @__PURE__ */ new Set()
  };
  return x7(i, e), ar(i, e, "allowIn"), ar(i, e, "allowChildren"), ar(i, e, "disallowIn"), ar(i, e, "disallowChildren"), ar(i, e, "allowContentOf"), ar(i, e, "allowWhere"), ar(i, e, "allowAttributes"), ar(i, e, "disallowAttributes"), ar(i, e, "allowAttributesOf"), ar(i, e, "inheritTypesFrom"), E7(i, e), e;
}
function m7(i, t) {
  for (const e of t.allowIn) {
    const n = i[e];
    n ? n.allowChildren.add(t.name) : t.allowIn.delete(e);
  }
}
function g7(i, t) {
  for (const e of t.allowChildren) {
    const n = i[e];
    n ? n.allowIn.add(t.name) : t.allowChildren.delete(e);
  }
}
function p7(i, t) {
  for (const e of t.disallowIn) {
    const n = i[e];
    n ? n.disallowChildren.add(t.name) : t.disallowIn.delete(e);
  }
}
function b7(i, t) {
  for (const e of t.disallowChildren) {
    const n = i[e];
    n ? n.disallowIn.add(t.name) : t.disallowChildren.delete(e);
  }
}
function v7(i, t) {
  for (const e of t.disallowChildren)
    t.allowChildren.delete(e);
  for (const e of t.disallowIn)
    t.allowIn.delete(e);
  for (const e of t.disallowAttributes)
    t.allowAttributes.delete(e);
}
function w7(i, t) {
  for (const e of t.allowContentOf) {
    const n = i[e];
    n && (n.disallowChildren.forEach((r) => {
      t.allowChildren.has(r) || (t.disallowChildren.add(r), i[r].disallowIn.add(t.name));
    }), n.allowChildren.forEach((r) => {
      t.disallowChildren.has(r) || (t.allowChildren.add(r), i[r].allowIn.add(t.name));
    }));
  }
}
function y7(i, t) {
  for (const e of t.allowWhere) {
    const n = i[e];
    n && (n.disallowIn.forEach((r) => {
      t.allowIn.has(r) || (t.disallowIn.add(r), i[r].disallowChildren.add(t.name));
    }), n.allowIn.forEach((r) => {
      t.disallowIn.has(r) || (t.allowIn.add(r), i[r].allowChildren.add(t.name));
    }));
  }
}
function _7(i) {
  const t = {};
  for (const e of Object.values(i))
    t[e.name] = {
      name: e.name,
      // `is*` properties may not be set - convert `undefined` to `false`.
      isBlock: !!e.isBlock,
      isContent: !!e.isContent,
      isInline: !!e.isInline,
      isLimit: !!e.isLimit,
      isObject: !!e.isObject,
      isSelectable: !!e.isSelectable,
      // Filter out non-existing items.
      allowIn: Array.from(e.allowIn).filter((n) => !!i[n]),
      allowChildren: Array.from(e.allowChildren).filter((n) => !!i[n]),
      allowAttributes: Array.from(e.allowAttributes)
    };
  return t;
}
function C7(i, t) {
  for (const e of t.allowAttributesOf) {
    const n = i[e];
    if (!n)
      return;
    n.allowAttributes.forEach((r) => {
      t.disallowAttributes.has(r) || t.allowAttributes.add(r);
    });
  }
}
function k7(i, t) {
  for (const e of t.inheritTypesFrom) {
    const n = i[e];
    if (n) {
      const r = Object.keys(n).filter((s) => s.startsWith("is"));
      for (const s of r)
        s in t || (t[s] = n[s]);
    }
  }
}
function x7(i, t) {
  for (const e of i) {
    const n = Object.keys(e).filter((r) => r.startsWith("is"));
    for (const r of n)
      t[r] = !!e[r];
  }
}
function ar(i, t, e) {
  for (const n of i) {
    let r = n[e];
    typeof r == "string" && (r = [
      r
    ]), Array.isArray(r) && r.forEach((s) => t[e].add(s));
  }
}
function E7(i, t) {
  for (const e of i) {
    const n = e.inheritAllFrom;
    n && (t.allowContentOf.add(n), t.allowWhere.add(n), t.allowAttributesOf.add(n), t.inheritTypesFrom.add(n));
  }
}
function T7(i) {
  return typeof i == "string" || i.is("documentFragment") ? {
    name: typeof i == "string" ? i : "$documentFragment",
    *getAttributeKeys() {
    },
    getAttribute() {
    }
  } : {
    // '$text' means text nodes and text proxies.
    name: i.is("element") ? i.name : "$text",
    *getAttributeKeys() {
      yield* i.getAttributeKeys();
    },
    getAttribute(t) {
      return i.getAttribute(t);
    }
  };
}
function* S7(i, t) {
  let e = !1;
  for (; !e; ) {
    if (e = !0, i) {
      const n = i.next();
      n.done || (e = !1, yield {
        walker: i,
        value: n.value
      });
    }
    if (t) {
      const n = t.next();
      n.done || (e = !1, yield {
        walker: t,
        value: n.value
      });
    }
  }
}
function* A7(i) {
  for (const t of i)
    yield* t.getMinimalFlatRanges();
}
function K1(i, t, e) {
  for (const n of t.getAttributeKeys())
    i.checkAttribute(t, n) || e.removeAttribute(n, t);
}
class P7 extends (/* @__PURE__ */ ht()) {
  /**
  * Creates an upcast dispatcher that operates using the passed API.
  *
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi
  * @param conversionApi Additional properties for an interface that will be passed to events fired
  * by the upcast dispatcher.
  */
  constructor(e) {
    super();
    /**
    * An interface passed by the dispatcher to the event callbacks.
    */
    h(this, "conversionApi");
    /**
    * The list of elements that were created during splitting.
    *
    * After the conversion process, the list is cleared.
    */
    h(this, "_splitParts", /* @__PURE__ */ new Map());
    /**
    * The list of cursor parent elements that were created during splitting.
    *
    * After the conversion process the list is cleared.
    */
    h(this, "_cursorParents", /* @__PURE__ */ new Map());
    /**
    * The position in the temporary structure where the converted content is inserted. The structure reflects the context of
    * the target position where the content will be inserted. This property is built based on the context parameter of the
    * convert method.
    */
    h(this, "_modelCursor", null);
    /**
    * The list of elements that were created during the splitting but should not get removed on conversion end even if they are empty.
    *
    * The list is cleared after the conversion process.
    */
    h(this, "_emptyElementsToKeep", /* @__PURE__ */ new Set());
    this.conversionApi = {
      ...e,
      consumable: null,
      writer: null,
      store: null,
      convertItem: (n, r) => this._convertItem(n, r),
      convertChildren: (n, r) => this._convertChildren(n, r),
      safeInsert: (n, r) => this._safeInsert(n, r),
      updateConversionResult: (n, r) => this._updateConversionResult(n, r),
      // Advanced API - use only if custom position handling is needed.
      splitToAllowedParent: (n, r) => this._splitToAllowedParent(n, r),
      getSplitParts: (n) => this._getSplitParts(n),
      keepEmptyElement: (n) => this._keepEmptyElement(n)
    };
  }
  /**
  * Starts the conversion process. The entry point for the conversion.
  *
  * @fires element
  * @fires text
  * @fires documentFragment
  * @param viewElement The part of the view to be converted.
  * @param writer An instance of the model writer.
  * @param context Elements will be converted according to this context.
  * @returns Model data that is the result of the conversion process
  * wrapped in `DocumentFragment`. Converted marker elements will be set as the document fragment's
  * {@link module:engine/model/documentfragment~DocumentFragment#markers static markers map}.
  */
  convert(e, n, r = [
    "$root"
  ]) {
    this.fire("viewCleanup", e), this._modelCursor = R7(r, n), this.conversionApi.writer = n, this.conversionApi.consumable = dl.createFrom(e), this.conversionApi.store = {};
    const { modelRange: s } = this._convertItem(e, this._modelCursor), o = n.createDocumentFragment();
    if (s) {
      this._removeEmptyElements();
      for (const a of Array.from(this._modelCursor.parent.getChildren()))
        n.append(a, o);
      o.markers = I7(o, n);
    }
    return this._modelCursor = null, this._splitParts.clear(), this._cursorParents.clear(), this._emptyElementsToKeep.clear(), this.conversionApi.writer = null, this.conversionApi.store = null, o;
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#convertItem
  */
  _convertItem(e, n) {
    const r = {
      viewItem: e,
      modelCursor: n,
      modelRange: null
    };
    if (e.is("element") ? this.fire(`element:${e.name}`, r, this.conversionApi) : e.is("$text") ? this.fire("text", r, this.conversionApi) : this.fire("documentFragment", r, this.conversionApi), r.modelRange && !(r.modelRange instanceof D))
      throw new T("view-conversion-dispatcher-incorrect-result", this);
    return {
      modelRange: r.modelRange,
      modelCursor: r.modelCursor
    };
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#convertChildren
  */
  _convertChildren(e, n) {
    let r = n.is("position") ? n : G._createAt(n, 0);
    const s = new D(r);
    for (const o of Array.from(e.getChildren())) {
      const a = this._convertItem(o, r);
      a.modelRange instanceof D && (s.end = a.modelRange.end, r = a.modelCursor);
    }
    return {
      modelRange: s,
      modelCursor: r
    };
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#safeInsert
  */
  _safeInsert(e, n) {
    const r = this._splitToAllowedParent(e, n);
    return r ? (this.conversionApi.writer.insert(e, r.position), !0) : !1;
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#updateConversionResult
  */
  _updateConversionResult(e, n) {
    const r = this._getSplitParts(e), s = this.conversionApi.writer;
    n.modelRange || (n.modelRange = s.createRange(s.createPositionBefore(e), s.createPositionAfter(r[r.length - 1])));
    const o = this._cursorParents.get(e);
    o ? n.modelCursor = s.createPositionAt(o, 0) : n.modelCursor = n.modelRange.end;
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#splitToAllowedParent
  */
  _splitToAllowedParent(e, n) {
    const { schema: r, writer: s } = this.conversionApi;
    let o = r.findAllowedParent(n, e);
    if (o) {
      if (o === n.parent)
        return {
          position: n
        };
      this._modelCursor.parent.getAncestors().includes(o) && (o = null);
    }
    if (!o)
      return oC(n, e, r) ? {
        position: aC(n, s)
      } : null;
    const a = this.conversionApi.writer.split(n, o), l = [];
    for (const u of a.range.getWalker())
      if (u.type == "elementEnd")
        l.push(u.item);
      else {
        const d = l.pop(), f = u.item;
        this._registerSplitPair(d, f);
      }
    const c = a.range.end.parent;
    return this._cursorParents.set(e, c), {
      position: a.position,
      cursorParent: c
    };
  }
  /**
  * Registers that a `splitPart` element is a split part of the `originalPart` element.
  *
  * The data set by this method is used by {@link #_getSplitParts} and {@link #_removeEmptyElements}.
  */
  _registerSplitPair(e, n) {
    this._splitParts.has(e) || this._splitParts.set(e, [
      e
    ]);
    const r = this._splitParts.get(e);
    this._splitParts.set(n, r), r.push(n);
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#getSplitParts
  */
  _getSplitParts(e) {
    let n;
    return this._splitParts.has(e) ? n = this._splitParts.get(e) : n = [
      e
    ], n;
  }
  /**
  * Mark an element that were created during the splitting to not get removed on conversion end even if it is empty.
  */
  _keepEmptyElement(e) {
    this._emptyElementsToKeep.add(e);
  }
  /**
  * Checks if there are any empty elements created while splitting and removes them.
  *
  * This method works recursively to re-check empty elements again after at least one element was removed in the initial call,
  * as some elements might have become empty after other empty elements were removed from them.
  */
  _removeEmptyElements() {
    let e = !1;
    for (const n of this._splitParts.keys())
      n.isEmpty && !this._emptyElementsToKeep.has(n) && (this.conversionApi.writer.remove(n), this._splitParts.delete(n), e = !0);
    e && this._removeEmptyElements();
  }
}
function I7(i, t) {
  const e = /* @__PURE__ */ new Set(), n = /* @__PURE__ */ new Map(), r = D._createIn(i).getItems();
  for (const s of r)
    s.is("element", "$marker") && e.add(s);
  for (const s of e) {
    const o = s.getAttribute("data-name"), a = t.createPositionBefore(s);
    n.has(o) ? n.get(o).end = a.clone() : n.set(o, new D(a.clone())), t.remove(s);
  }
  return n;
}
function R7(i, t) {
  let e;
  for (const n of new ss(i)) {
    const r = {};
    for (const o of n.getAttributeKeys())
      r[o] = n.getAttribute(o);
    const s = t.createElement(n.name, r);
    e && t.insert(s, e), e = G._createAt(s, 0);
  }
  return e;
}
class O7 {
  /**
  * Returns an HTML string created from the document fragment.
  */
  getHtml(t) {
    const n = ae.document.implementation.createHTMLDocument("").createElement("div");
    return n.appendChild(t), n.innerHTML;
  }
}
class hC {
  /**
  * Creates a new instance of the HTML data processor class.
  *
  * @param document The view document instance.
  */
  constructor(t) {
    /**
    * A DOM parser instance used to parse an HTML string to an HTML document.
    */
    h(this, "domParser");
    /**
    * A DOM converter used to convert DOM elements to view elements.
    */
    h(this, "domConverter");
    /**
    * A basic HTML writer instance used to convert DOM elements to an HTML string.
    */
    h(this, "htmlWriter");
    h(this, "skipComments", !0);
    this.domParser = new DOMParser(), this.domConverter = new zd(t, {
      renderingMode: "data"
    }), this.htmlWriter = new O7();
  }
  /**
  * Converts a provided {@link module:engine/view/documentfragment~DocumentFragment document fragment}
  * to data format &ndash; in this case to an HTML string.
  *
  * @returns HTML string.
  */
  toData(t) {
    const e = this.domConverter.viewToDom(t);
    return this.htmlWriter.getHtml(e);
  }
  /**
  * Converts the provided HTML string to a view tree.
  *
  * @param data An HTML string.
  * @returns A converted view element.
  */
  toView(t) {
    const e = this._toDom(t);
    return this.domConverter.domToView(e, {
      skipComments: this.skipComments
    });
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} for view elements whose content should be treated as raw data
  * and not processed during the conversion from the DOM to the view elements.
  *
  * The raw data can be later accessed by a
  * {@link module:engine/view/element~Element#getCustomProperty custom property of a view element} called `"$rawContent"`.
  *
  * @param pattern Pattern matching all view elements whose content should be treated as raw data.
  */
  registerRawContentMatcher(t) {
    this.domConverter.registerRawContentMatcher(t);
  }
  /**
  * If the processor is set to use marked fillers, it will insert `&nbsp;` fillers wrapped in `<span>` elements
  * (`<span data-cke-filler="true">&nbsp;</span>`) instead of regular `&nbsp;` characters.
  *
  * This mode allows for a more precise handling of the block fillers (so they do not leak into the editor content) but
  * bloats the editor data with additional markup.
  *
  * This mode may be required by some features and will be turned on by them automatically.
  *
  * @param type Whether to use the default or the marked `&nbsp;` block fillers.
  */
  useFillerType(t) {
    this.domConverter.blockFillerMode = t == "marked" ? "markedNbsp" : "nbsp";
  }
  /**
  * Converts an HTML string to its DOM representation. Returns a document fragment containing nodes parsed from
  * the provided data.
  */
  _toDom(t) {
    /<(?:html|body|head|meta)(?:\s[^>]*)?>/i.test(t.trim().slice(0, 1e4)) || (t = `<body>${t}</body>`);
    const e = this.domParser.parseFromString(t, "text/html"), n = e.createDocumentFragment(), r = e.body.childNodes;
    for (; r.length > 0; )
      n.appendChild(r[0]);
    return n;
  }
}
class V7 extends (/* @__PURE__ */ ht()) {
  /**
  * Creates a data controller instance.
  *
  * @param model Data model.
  * @param stylesProcessor The styles processor instance.
  */
  constructor(e, n) {
    super();
    /**
    * Data model.
    */
    h(this, "model");
    /**
    * Mapper used for the conversion. It has no permanent bindings, because these are created while getting data and
    * are cleared directly after the data are converted. However, the mapper is defined as a class property, because
    * it needs to be passed to the `DowncastDispatcher` as a conversion API.
    */
    h(this, "mapper");
    /**
    * Downcast dispatcher used by the {@link #get get method}. Downcast converters should be attached to it.
    */
    h(this, "downcastDispatcher");
    /**
    * Upcast dispatcher used by the {@link #set set method}. Upcast converters should be attached to it.
    */
    h(this, "upcastDispatcher");
    /**
    * The view document used by the data controller.
    */
    h(this, "viewDocument");
    /**
    * Styles processor used during the conversion.
    */
    h(this, "stylesProcessor");
    /**
    * Data processor used specifically for HTML conversion.
    */
    h(this, "htmlProcessor");
    /**
    * Data processor used during the conversion.
    * Same instance as {@link #htmlProcessor} by default. Can be replaced at run time to handle different format, e.g. XML or Markdown.
    */
    h(this, "processor");
    /**
    * The view downcast writer just for data conversion purposes, i.e. to modify
    * the {@link #viewDocument}.
    */
    h(this, "_viewWriter");
    this.model = e, this.mapper = new G2(), this.downcastDispatcher = new K2({
      mapper: this.mapper,
      schema: e.schema
    }), this.downcastDispatcher.on("insert:$text", J2(), {
      priority: "lowest"
    }), this.downcastDispatcher.on("insert", X2(), {
      priority: "lowest"
    }), this.upcastDispatcher = new P7({
      schema: e.schema
    }), this.viewDocument = new Fd(n), this.stylesProcessor = n, this.htmlProcessor = new hC(this.viewDocument), this.processor = this.htmlProcessor, this._viewWriter = new N2(this.viewDocument), this.upcastDispatcher.on("text", UO(), {
      priority: "lowest"
    }), this.upcastDispatcher.on("element", W1(), {
      priority: "lowest"
    }), this.upcastDispatcher.on("documentFragment", W1(), {
      priority: "lowest"
    }), st().prototype.decorate.call(this, "init"), st().prototype.decorate.call(this, "set"), st().prototype.decorate.call(this, "get"), st().prototype.decorate.call(this, "toView"), st().prototype.decorate.call(this, "toModel"), this.on("init", () => {
      this.fire("ready");
    }, {
      priority: "lowest"
    }), this.on("ready", () => {
      this.model.enqueueChange({
        isUndoable: !1
      }, sC);
    }, {
      priority: "lowest"
    });
  }
  /**
  * Returns the model's data converted by downcast dispatchers attached to {@link #downcastDispatcher} and
  * formatted by the {@link #processor data processor}.
  *
  * A warning is logged when you try to retrieve data for a detached root, as most probably this is a mistake. A detached root should
  * be treated like it is removed, and you should not save its data. Note, that the detached root data is always an empty string.
  *
  * @fires get
  * @param options Additional configuration for the retrieved data. `DataController` provides two optional
  * properties: `rootName` and `trim`. Other properties of this object are specified by various editor features.
  * @param options.rootName Root name. Default 'main'.
  * @param options.trim Whether returned data should be trimmed. This option is set to `empty` by default,
  * which means whenever editor content is considered empty, an empty string will be returned. To turn off trimming completely
  * use `'none'`. In such cases the exact content will be returned (for example a `<p>&nbsp;</p>` for an empty editor).
  * @returns Output data.
  */
  get(e = {}) {
    const { rootName: n = "main", trim: r = "empty" } = e;
    if (!this._checkIfRootsExists([
      n
    ]))
      throw new T("datacontroller-get-non-existent-root", this);
    const s = this.model.document.getRoot(n);
    return s.isAttached() || Xe("datacontroller-get-detached-root", this), r === "empty" && !this.model.hasContent(s, {
      ignoreWhitespaces: !0
    }) ? "" : this.stringify(s, e);
  }
  /**
  * Returns the content of the given {@link module:engine/model/element~Element model's element} or
  * {@link module:engine/model/documentfragment~DocumentFragment model document fragment} converted by the downcast converters
  * attached to the {@link #downcastDispatcher} and formatted by the {@link #processor data processor}.
  *
  * @param modelElementOrFragment The element whose content will be stringified.
  * @param options Additional configuration passed to the conversion process.
  * @returns Output data.
  */
  stringify(e, n = {}) {
    const r = this.toView(e, n);
    return this.processor.toData(r);
  }
  /**
  * Returns the content of the given {@link module:engine/model/element~Element model element} or
  * {@link module:engine/model/documentfragment~DocumentFragment model document fragment} converted by the downcast
  * converters attached to {@link #downcastDispatcher} into a
  * {@link module:engine/view/documentfragment~DocumentFragment view document fragment}.
  *
  * @fires toView
  * @param modelElementOrFragment Element or document fragment whose content will be converted.
  * @param options Additional configuration that will be available through the
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi#options} during the conversion process.
  * @returns Output view DocumentFragment.
  */
  toView(e, n = {}) {
    const r = this.viewDocument, s = this._viewWriter;
    this.mapper.clearBindings();
    const o = D._createIn(e), a = new Bs(r);
    this.mapper.bindElements(e, a);
    const l = e.is("documentFragment") ? e.markers : M7(e);
    return this.downcastDispatcher.convert(o, l, s, n), a;
  }
  /**
  * Sets the initial input data parsed by the {@link #processor data processor} and
  * converted by the {@link #upcastDispatcher view-to-model converters}.
  * Initial data can be only set to a document whose {@link module:engine/model/document~Document#version} is equal 0.
  *
  * **Note** This method is {@link module:utils/observablemixin~Observable#decorate decorated} which is
  * used by e.g. collaborative editing plugin that syncs remote data on init.
  *
  * When data is passed as a string, it is initialized on the default `main` root:
  *
  * ```ts
  * dataController.init( '<p>Foo</p>' ); // Initializes data on the `main` root only, as no other is specified.
  * ```
  *
  * To initialize data on a different root or multiple roots at once, an object containing `rootName` - `data` pairs should be passed:
  *
  * ```ts
  * dataController.init( { main: '<p>Foo</p>', title: '<h1>Bar</h1>' } ); // Initializes data on both the `main` and `title` roots.
  * ```
  *
  * @fires init
  * @param data Input data as a string or an object containing the `rootName` - `data`
  * pairs to initialize data on multiple roots at once.
  * @returns Promise that is resolved after the data is set on the editor.
  */
  init(e) {
    if (this.model.document.version)
      throw new T("datacontroller-init-document-not-empty", this);
    let n = {};
    if (typeof e == "string" ? n.main = e : n = e, !this._checkIfRootsExists(Object.keys(n)))
      throw new T("datacontroller-init-non-existent-root", this);
    return this.model.enqueueChange({
      isUndoable: !1
    }, (r) => {
      for (const s of Object.keys(n)) {
        const o = this.model.document.getRoot(s);
        r.insert(this.parse(n[s], o), o, 0);
      }
    }), Promise.resolve();
  }
  /**
  * Sets the input data parsed by the {@link #processor data processor} and
  * converted by the {@link #upcastDispatcher view-to-model converters}.
  * This method can be used any time to replace existing editor data with the new one without clearing the
  * {@link module:engine/model/document~Document#history document history}.
  *
  * This method also creates a batch with all the changes applied. If all you need is to parse data, use
  * the {@link #parse} method.
  *
  * When data is passed as a string it is set on the default `main` root:
  *
  * ```ts
  * dataController.set( '<p>Foo</p>' ); // Sets data on the `main` root, as no other is specified.
  * ```
  *
  * To set data on a different root or multiple roots at once, an object containing `rootName` - `data` pairs should be passed:
  *
  * ```ts
  * dataController.set( { main: '<p>Foo</p>', title: '<h1>Bar</h1>' } ); // Sets data on the `main` and `title` roots as specified.
  * ```
  *
  * To set the data with a preserved undo stack and add the change to the undo stack, set `{ isUndoable: true }` as a `batchType` option.
  *
  * ```ts
  * dataController.set( '<p>Foo</p>', { batchType: { isUndoable: true } } );
  * ```
  *
  * @fires set
  * @param data Input data as a string or an object containing the `rootName` - `data`
  * pairs to set data on multiple roots at once.
  * @param options Options for setting data.
  * @param options.batchType The batch type that will be used to create a batch for the changes applied by this method.
  * By default, the batch will be set as {@link module:engine/model/batch~Batch#isUndoable not undoable} and the undo stack will be
  * cleared after the new data is applied (all undo steps will be removed). If the batch type `isUndoable` flag is be set to `true`,
  * the undo stack will be preserved instead and not cleared when new data is applied.
  */
  set(e, n = {}) {
    let r = {};
    if (typeof e == "string" ? r.main = e : r = e, !this._checkIfRootsExists(Object.keys(r)))
      throw new T("datacontroller-set-non-existent-root", this);
    this.model.enqueueChange(n.batchType || {}, (s) => {
      s.setSelection(null), s.removeSelectionAttribute(this.model.document.selection.getAttributeKeys());
      for (const o of Object.keys(r)) {
        const a = this.model.document.getRoot(o);
        s.remove(s.createRangeIn(a)), s.insert(this.parse(r[o], a), a, 0);
      }
    });
  }
  /**
  * Returns the data parsed by the {@link #processor data processor} and then converted by upcast converters
  * attached to the {@link #upcastDispatcher}.
  *
  * @see #set
  * @param data Data to parse.
  * @param context Base context in which the view will be converted to the model.
  * See: {@link module:engine/conversion/upcastdispatcher~UpcastDispatcher#convert}.
  * @returns Parsed data.
  */
  parse(e, n = "$root") {
    const r = this.processor.toView(e);
    return this.toModel(r, n);
  }
  /**
  * Returns the result of the given {@link module:engine/view/element~Element view element} or
  * {@link module:engine/view/documentfragment~DocumentFragment view document fragment} converted by the
  * {@link #upcastDispatcher view-to-model converters}, wrapped by {@link module:engine/model/documentfragment~DocumentFragment}.
  *
  * When marker elements were converted during the conversion process, it will be set as a document fragment's
  * {@link module:engine/model/documentfragment~DocumentFragment#markers static markers map}.
  *
  * @fires toModel
  * @param viewElementOrFragment The element or document fragment whose content will be converted.
  * @param context Base context in which the view will be converted to the model.
  * See: {@link module:engine/conversion/upcastdispatcher~UpcastDispatcher#convert}.
  * @returns Output document fragment.
  */
  toModel(e, n = "$root") {
    return this.model.change((r) => this.upcastDispatcher.convert(e, r, n));
  }
  /**
  * Adds the style processor normalization rules.
  *
  * You can implement your own rules as well as use one of the available processor rules:
  *
  * * background: {@link module:engine/view/styles/background~addBackgroundRules}
  * * border: {@link module:engine/view/styles/border~addBorderRules}
  * * margin: {@link module:engine/view/styles/margin~addMarginRules}
  * * padding: {@link module:engine/view/styles/padding~addPaddingRules}
  */
  addStyleProcessorRules(e) {
    e(this.stylesProcessor);
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} on an {@link #htmlProcessor htmlProcessor}
  * and a {@link #processor processor} for view elements whose content should be treated as raw data
  * and not processed during the conversion from DOM to view elements.
  *
  * The raw data can be later accessed by the {@link module:engine/view/element~Element#getCustomProperty view element custom property}
  * `"$rawContent"`.
  *
  * @param pattern Pattern matching all view elements whose content should be treated as a raw data.
  */
  registerRawContentMatcher(e) {
    this.processor && this.processor !== this.htmlProcessor && this.processor.registerRawContentMatcher(e), this.htmlProcessor.registerRawContentMatcher(e);
  }
  /**
  * Removes all event listeners set by the DataController.
  */
  destroy() {
    this.stopListening();
  }
  /**
  * Checks whether all provided root names are actually existing editor roots.
  *
  * @param rootNames Root names to check.
  * @returns Whether all provided root names are existing editor roots.
  */
  _checkIfRootsExists(e) {
    for (const n of e)
      if (!this.model.document.getRoot(n))
        return !1;
    return !0;
  }
}
function M7(i) {
  const t = [], e = i.root.document;
  if (!e)
    return /* @__PURE__ */ new Map();
  const n = D._createIn(i);
  for (const r of e.model.markers) {
    const s = r.getRange(), o = s.isCollapsed, a = s.start.isEqual(n.start) || s.end.isEqual(n.end);
    if (o && a)
      t.push([
        r.name,
        s
      ]);
    else {
      const l = n.getIntersection(s);
      l && t.push([
        r.name,
        l
      ]);
    }
  }
  return t.sort(([r, s], [o, a]) => {
    if (s.end.compareWith(a.start) !== "after")
      return 1;
    if (s.start.compareWith(a.end) !== "before")
      return -1;
    switch (s.start.compareWith(a.start)) {
      case "before":
        return 1;
      case "after":
        return -1;
      default:
        switch (s.end.compareWith(a.end)) {
          case "before":
            return 1;
          case "after":
            return -1;
          default:
            return o.localeCompare(r);
        }
    }
  }), new Map(t);
}
class D7 {
  /**
  * Creates a new conversion instance.
  */
  constructor(t, e) {
    /**
    * Maps dispatchers group name to ConversionHelpers instances.
    */
    h(this, "_helpers", /* @__PURE__ */ new Map());
    h(this, "_downcast");
    h(this, "_upcast");
    this._downcast = tt(t), this._createConversionHelpers({
      name: "downcast",
      dispatchers: this._downcast,
      isDowncast: !0
    }), this._upcast = tt(e), this._createConversionHelpers({
      name: "upcast",
      dispatchers: this._upcast,
      isDowncast: !1
    });
  }
  /**
  * Define an alias for registered dispatcher.
  *
  * ```ts
  * const conversion = new Conversion(
  * 	[ dataDowncastDispatcher, editingDowncastDispatcher ],
  * 	upcastDispatcher
  * );
  *
  * conversion.addAlias( 'dataDowncast', dataDowncastDispatcher );
  * ```
  *
  * @param alias An alias of a dispatcher.
  * @param dispatcher Dispatcher which should have an alias.
  */
  addAlias(t, e) {
    const n = this._downcast.includes(e);
    if (!this._upcast.includes(e) && !n)
      throw new T("conversion-add-alias-dispatcher-not-registered", this);
    this._createConversionHelpers({
      name: t,
      dispatchers: [
        e
      ],
      isDowncast: n
    });
  }
  /**
  * Provides a chainable API to assign converters to a conversion dispatchers group.
  *
  * If the given group name has not been registered, the
  * {@link module:utils/ckeditorerror~CKEditorError `conversion-for-unknown-group` error} is thrown.
  *
  * You can use conversion helpers available directly in the `for()` chain or your custom ones via
  * the {@link module:engine/conversion/conversionhelpers~ConversionHelpers#add `add()`} method.
  *
  * # Using built-in conversion helpers
  *
  * The `for()` chain comes with a set of conversion helpers which you can use like this:
  *
  * ```ts
  * editor.conversion.for( 'downcast' )
  * 	.elementToElement( config1 )        // Adds an element-to-element downcast converter.
  * 	.attributeToElement( config2 );     // Adds an attribute-to-element downcast converter.
  *
  * editor.conversion.for( 'upcast' )
  * 	.elementToAttribute( config3 );     // Adds an element-to-attribute upcast converter.
  * ```
  *
  * Refer to the documentation of built-in conversion helpers to learn about their configuration options.
  *
  * * downcast (model-to-view) conversion helpers:
  *
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#elementToElement `elementToElement()`},
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#attributeToElement `attributeToElement()`},
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#attributeToAttribute `attributeToAttribute()`}.
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToElement `markerToElement()`}.
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToHighlight `markerToHighlight()`}.
  *
  * * upcast (view-to-model) conversion helpers:
  *
  *	* {@link module:engine/conversion/upcasthelpers~UpcastHelpers#elementToElement `elementToElement()`},
  *	* {@link module:engine/conversion/upcasthelpers~UpcastHelpers#elementToAttribute `elementToAttribute()`},
  *	* {@link module:engine/conversion/upcasthelpers~UpcastHelpers#attributeToAttribute `attributeToAttribute()`}.
  *	* {@link module:engine/conversion/upcasthelpers~UpcastHelpers#elementToMarker `elementToMarker()`}.
  *
  * # Using custom conversion helpers
  *
  * If you need to implement an atypical converter, you can do so by calling:
  *
  * ```ts
  * editor.conversion.for( direction ).add( customHelper );
  * ```
  *
  * The `.add()` method takes exactly one parameter, which is a function. This function should accept one parameter that
  * is a dispatcher instance. The function should add an actual converter to the passed dispatcher instance.
  *
  * Example:
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).add( dispatcher => {
  * 	dispatcher.on( 'element:a',  ( evt, data, conversionApi ) => {
  * 		// Do something with a view <a> element.
  * 	} );
  * } );
  * ```
  *
  * Refer to the documentation of {@link module:engine/conversion/upcastdispatcher~UpcastDispatcher}
  * and {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher} to learn how to write
  * custom converters.
  *
  * @param groupName The name of dispatchers group to add the converters to.
  */
  for(t) {
    if (!this._helpers.has(t))
      throw new T("conversion-for-unknown-group", this);
    return this._helpers.get(t);
  }
  /**
  * Sets up converters between the model and the view that convert a model element to a view element (and vice versa).
  * For example, the model `<paragraph>Foo</paragraph>` is turned into `<p>Foo</p>` in the view.
  *
  * ```ts
  * // A simple conversion from the `paragraph` model element to the `<p>` view element (and vice versa).
  * editor.conversion.elementToElement( { model: 'paragraph', view: 'p' } );
  *
  * // Override other converters by specifying a converter definition with a higher priority.
  * editor.conversion.elementToElement( { model: 'paragraph', view: 'div', converterPriority: 'high' } );
  *
  * // View specified as an object instead of a string.
  * editor.conversion.elementToElement( {
  * 	model: 'fancyParagraph',
  * 	view: {
  * 		name: 'p',
  * 		classes: 'fancy'
  * 	}
  * } );
  *
  * // Use `upcastAlso` to define other view elements that should also be converted to a `paragraph` element.
  * editor.conversion.elementToElement( {
  * 	model: 'paragraph',
  * 	view: 'p',
  * 	upcastAlso: [
  * 		'div',
  * 		{
  * 			// Any element with the `display: block` style.
  * 			styles: {
  * 				display: 'block'
  * 			}
  * 		}
  * 	]
  * } );
  *
  * // `upcastAlso` set as callback enables a conversion of a wide range of different view elements.
  * editor.conversion.elementToElement( {
  * 	model: 'heading',
  * 	view: 'h2',
  * 	// Convert "heading-like" paragraphs to headings.
  * 	upcastAlso: viewElement => {
  * 		const fontSize = viewElement.getStyle( 'font-size' );
  *
  * 		if ( !fontSize ) {
  * 			return null;
  * 		}
  *
  * 		const match = fontSize.match( /(\d+)\s*px/ );
  *
  * 		if ( !match ) {
  * 			return null;
  * 		}
  *
  * 		const size = Number( match[ 1 ] );
  *
  * 		if ( size > 26 ) {
  * 			// Returned value can be an object with the matched properties.
  * 			// These properties will be "consumed" during the conversion.
  * 			// See `engine.view.Matcher~MatcherPattern` and `engine.view.Matcher#match` for more details.
  *
  * 			return { name: true, styles: [ 'font-size' ] };
  * 		}
  *
  * 		return null;
  * 	}
  * } );
  * ```
  *
  * `definition.model` is a `String` with a model element name to convert from or to.
  *
  * @param definition The converter definition.
  */
  elementToElement(t) {
    this.for("downcast").elementToElement(t);
    for (const { model: e, view: n } of Uh(t))
      this.for("upcast").elementToElement({
        model: e,
        view: n,
        converterPriority: t.converterPriority
      });
  }
  /**
  * Sets up converters between the model and the view that convert a model attribute to a view element (and vice versa).
  * For example, a model text node with `"Foo"` as data and the `bold` attribute will be turned to `<strong>Foo</strong>` in the view.
  *
  * ```ts
  * // A simple conversion from the `bold=true` attribute to the `<strong>` view element (and vice versa).
  * editor.conversion.attributeToElement( { model: 'bold', view: 'strong' } );
  *
  * // Override other converters by specifying a converter definition with a higher priority.
  * editor.conversion.attributeToElement( { model: 'bold', view: 'b', converterPriority: 'high' } );
  *
  * // View specified as an object instead of a string.
  * editor.conversion.attributeToElement( {
  * 	model: 'bold',
  * 	view: {
  * 		name: 'span',
  * 		classes: 'bold'
  * 	}
  * } );
  *
  * // Use `config.model.name` to define the conversion only from a given node type, `$text` in this case.
  * // The same attribute on different elements may then be handled by a different converter.
  * editor.conversion.attributeToElement( {
  * 	model: {
  * 		key: 'textDecoration',
  * 		values: [ 'underline', 'lineThrough' ],
  * 		name: '$text'
  * 	},
  * 	view: {
  * 		underline: {
  * 			name: 'span',
  * 			styles: {
  * 				'text-decoration': 'underline'
  * 			}
  * 		},
  * 		lineThrough: {
  * 			name: 'span',
  * 			styles: {
  * 				'text-decoration': 'line-through'
  * 			}
  * 		}
  * 	}
  * } );
  *
  * // Use `upcastAlso` to define other view elements that should also be converted to the `bold` attribute.
  * editor.conversion.attributeToElement( {
  * 	model: 'bold',
  * 	view: 'strong',
  * 	upcastAlso: [
  * 		'b',
  * 		{
  * 			name: 'span',
  * 			classes: 'bold'
  * 		},
  * 		{
  * 			name: 'span',
  * 			styles: {
  * 				'font-weight': 'bold'
  * 			}
  * 		},
  * 		viewElement => {
  * 			const fontWeight = viewElement.getStyle( 'font-weight' );
  *
  * 			if ( viewElement.is( 'element', 'span' ) && fontWeight && /\d+/.test() && Number( fontWeight ) > 500 ) {
  * 				// Returned value can be an object with the matched properties.
  * 				// These properties will be "consumed" during the conversion.
  * 				// See `engine.view.Matcher~MatcherPattern` and `engine.view.Matcher#match` for more details.
  *
  * 				return {
  * 					name: true,
  * 					styles: [ 'font-weight' ]
  * 				};
  * 			}
  * 		}
  * 	]
  * } );
  *
  * // Conversion from and to a model attribute key whose value is an enum (`fontSize=big|small`).
  * // `upcastAlso` set as callback enables a conversion of a wide range of different view elements.
  * editor.conversion.attributeToElement( {
  * 	model: {
  * 		key: 'fontSize',
  * 		values: [ 'big', 'small' ]
  * 	},
  * 	view: {
  * 		big: {
  * 			name: 'span',
  * 			styles: {
  * 				'font-size': '1.2em'
  * 			}
  * 		},
  * 		small: {
  * 			name: 'span',
  * 			styles: {
  * 				'font-size': '0.8em'
  * 			}
  * 		}
  * 	},
  * 	upcastAlso: {
  * 		big: viewElement => {
  * 			const fontSize = viewElement.getStyle( 'font-size' );
  *
  * 			if ( !fontSize ) {
  * 				return null;
  * 			}
  *
  * 			const match = fontSize.match( /(\d+)\s*px/ );
  *
  * 			if ( !match ) {
  * 				return null;
  * 			}
  *
  * 			const size = Number( match[ 1 ] );
  *
  * 			if ( viewElement.is( 'element', 'span' ) && size > 10 ) {
  * 				// Returned value can be an object with the matched properties.
  * 				// These properties will be "consumed" during the conversion.
  * 				// See `engine.view.Matcher~MatcherPattern` and `engine.view.Matcher#match` for more details.
  *
  * 				return { name: true, styles: [ 'font-size' ] };
  * 			}
  *
  * 			return null;
  * 		},
  * 		small: viewElement => {
  * 			const fontSize = viewElement.getStyle( 'font-size' );
  *
  * 			if ( !fontSize ) {
  * 				return null;
  * 			}
  *
  * 			const match = fontSize.match( /(\d+)\s*px/ );
  *
  * 			if ( !match ) {
  * 				return null;
  * 			}
  *
  * 			const size = Number( match[ 1 ] );
  *
  * 			if ( viewElement.is( 'element', 'span' ) && size < 10 ) {
  * 				// Returned value can be an object with the matched properties.
  * 				// These properties will be "consumed" during the conversion.
  * 				// See `engine.view.Matcher~MatcherPattern` and `engine.view.Matcher#match` for more details.
  *
  * 				return { name: true, styles: [ 'font-size' ] };
  * 			}
  *
  * 			return null;
  * 		}
  * 	}
  * } );
  * ```
  *
  * The `definition.model` parameter specifies which model attribute should be converted from or to. It can be a `{ key, value }` object
  * describing the attribute key and value to convert or a `String` specifying just the attribute key (in such a case
  * `value` is set to `true`).
  *
  * @param definition The converter definition.
  */
  attributeToElement(t) {
    this.for("downcast").attributeToElement(t);
    for (const { model: e, view: n } of Uh(t))
      this.for("upcast").elementToAttribute({
        view: n,
        model: e,
        converterPriority: t.converterPriority
      });
  }
  /**
  * Sets up converters between the model and the view that convert a model attribute to a view attribute (and vice versa). For example,
  * `<imageBlock src='foo.jpg'></imageBlock>` is converted to `<img src='foo.jpg'></img>` (the same attribute key and value).
  * This type of converters is intended to be used with {@link module:engine/model/element~Element model element} nodes.
  * To convert the text attributes,
  * the {@link module:engine/conversion/conversion~Conversion#attributeToElement `attributeToElement converter`}should be set up.
  *
  * ```ts
  * // A simple conversion from the `source` model attribute to the `src` view attribute (and vice versa).
  * editor.conversion.attributeToAttribute( { model: 'source', view: 'src' } );
  *
  * // Attribute values are strictly specified.
  * editor.conversion.attributeToAttribute( {
  * 	model: {
  * 		name: 'imageInline',
  * 		key: 'aside',
  * 		values: [ 'aside' ]
  * 	},
  * 	view: {
  * 		aside: {
  * 			name: 'img',
  * 			key: 'class',
  * 			value: [ 'aside', 'half-size' ]
  * 		}
  * 	}
  * } );
  *
  * // Set the style attribute.
  * editor.conversion.attributeToAttribute( {
  * 	model: {
  * 		name: 'imageInline',
  * 		key: 'aside',
  * 		values: [ 'aside' ]
  * 	},
  * 	view: {
  * 		aside: {
  * 			name: 'img',
  * 			key: 'style',
  * 			value: {
  * 				float: 'right',
  * 				width: '50%',
  * 				margin: '5px'
  * 			}
  * 		}
  * 	}
  * } );
  *
  * // Conversion from and to a model attribute key whose value is an enum (`align=right|center`).
  * // Use `upcastAlso` to define other view elements that should also be converted to the `align=right` attribute.
  * editor.conversion.attributeToAttribute( {
  * 	model: {
  * 		key: 'align',
  * 		values: [ 'right', 'center' ]
  * 	},
  * 	view: {
  * 		right: {
  * 			key: 'class',
  * 			value: 'align-right'
  * 		},
  * 		center: {
  * 			key: 'class',
  * 			value: 'align-center'
  * 		}
  * 	},
  * 	upcastAlso: {
  * 		right: {
  * 			styles: {
  * 				'text-align': 'right'
  * 			}
  * 		},
  * 		center: {
  * 			styles: {
  * 				'text-align': 'center'
  * 			}
  * 		}
  * 	}
  * } );
  * ```
  *
  * The `definition.model` parameter specifies which model attribute should be converted from and to.
  * It can be a `{ key, [ values ], [ name ] }` object or a `String`, which will be treated like `{ key: definition.model }`.
  * The `key` property is the model attribute key to convert from and to.
  * The `values` are the possible model attribute values. If the `values` parameter is not set, the model attribute value
  * will be the same as the view attribute value.
  * If `name` is set, the conversion will be set up only for model elements with the given name.
  *
  * The `definition.view` parameter specifies which view attribute should be converted from and to.
  * It can be a `{ key, value, [ name ] }` object or a `String`, which will be treated like `{ key: definition.view }`.
  * The `key` property is the view attribute key to convert from and to.
  * The `value` is the view attribute value to convert from and to. If `definition.value` is not set, the view attribute value will be
  * the same as the model attribute value.
  * If `key` is `'class'`, `value` can be a `String` or an array of `String`s.
  * If `key` is `'style'`, `value` is an object with key-value pairs.
  * In other cases, `value` is a `String`.
  * If `name` is set, the conversion will be set up only for model elements with the given name.
  * If `definition.model.values` is set, `definition.view` is an object that assigns values from `definition.model.values`
  * to `{ key, value, [ name ] }` objects.
  *
  * `definition.upcastAlso` specifies which other matching view elements should also be upcast to the given model configuration.
  * If `definition.model.values` is set, `definition.upcastAlso` should be an object assigning values from `definition.model.values`
  * to {@link module:engine/view/matcher~MatcherPattern}s or arrays of {@link module:engine/view/matcher~MatcherPattern}s.
  *
  * **Note:** `definition.model` and `definition.view` form should be mirrored, so the same types of parameters should
  * be given in both parameters.
  *
  * @param definition The converter definition.
  * @param definition.model The model attribute to convert from and to.
  * @param definition.view The view attribute to convert from and to.
  * @param definition.upcastAlso Any view element matching `definition.upcastAlso` will also be converted to the given model attribute.
  * `definition.upcastAlso` is used only if `config.model.values` is specified.
  */
  attributeToAttribute(t) {
    this.for("downcast").attributeToAttribute(t);
    for (const { model: e, view: n } of Uh(t))
      this.for("upcast").attributeToAttribute({
        view: n,
        model: e
      });
  }
  /**
  * Creates and caches conversion helpers for given dispatchers group.
  *
  * @param options.name Group name.
  */
  _createConversionHelpers({ name: t, dispatchers: e, isDowncast: n }) {
    if (this._helpers.has(t))
      throw new T("conversion-group-exists", this);
    const r = n ? new hO(e) : new WO(e);
    this._helpers.set(t, r);
  }
}
function* Uh(i) {
  if (i.model.values)
    for (const t of i.model.values) {
      const e = {
        key: i.model.key,
        value: t
      }, n = i.view[t], r = i.upcastAlso ? i.upcastAlso[t] : void 0;
      yield* Z1(e, n, r);
    }
  else
    yield* Z1(i.model, i.view, i.upcastAlso);
}
function* Z1(i, t, e) {
  if (yield {
    model: i,
    view: t
  }, e)
    for (const n of tt(e))
      yield {
        model: i,
        view: n
      };
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class Ei {
  /**
  * Base operation constructor.
  *
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(t) {
    /**
    * {@link module:engine/model/document~Document#version} on which operation can be applied. If you try to
    * {@link module:engine/model/model~Model#applyOperation apply} operation with different base version than the
    * {@link module:engine/model/document~Document#version document version} the
    * {@link module:utils/ckeditorerror~CKEditorError model-document-applyOperation-wrong-version} error is thrown.
    */
    h(this, "baseVersion");
    /**
    * Defines whether operation is executed on attached or detached {@link module:engine/model/item~Item items}.
    */
    h(this, "isDocumentOperation");
    /**
    * {@link module:engine/model/batch~Batch Batch} to which the operation is added or `null` if the operation is not
    * added to any batch yet.
    */
    h(this, "batch");
    this.baseVersion = t, this.isDocumentOperation = this.baseVersion !== null, this.batch = null;
  }
  /**
  * Checks whether the operation's parameters are correct and the operation can be correctly executed. Throws
  * an error if operation is not valid.
  *
  * @internal
  */
  _validate() {
  }
  /**
  * Custom toJSON method to solve child-parent circular dependencies.
  *
  * @returns Clone of this object with the operation property replaced with string.
  */
  toJSON() {
    const t = Object.assign({}, this);
    return t.__className = this.constructor.className, delete t.batch, delete t.isDocumentOperation, t;
  }
  /**
  * Name of the operation class used for serialization.
  */
  static get className() {
    return "Operation";
  }
  /**
  * Creates `Operation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param doc Document on which this operation will be applied.
  */
  static fromJSON(t, e) {
    return new this(t.baseVersion);
  }
}
function ap(i, t) {
  const e = mC(t), n = e.reduce((o, a) => o + a.offsetSize, 0), r = i.parent;
  Nl(i);
  const s = i.index;
  return r._insertChild(s, e), Ll(r, s + e.length), Ll(r, s), new D(i, i.getShiftedBy(n));
}
function fC(i) {
  if (!i.isFlat)
    throw new T("operation-utils-remove-range-not-flat", this);
  const t = i.start.parent;
  Nl(i.start), Nl(i.end);
  const e = t._removeChildren(i.start.index, i.end.index - i.start.index);
  return Ll(t, i.start.index), e;
}
function Dl(i, t) {
  if (!i.isFlat)
    throw new T("operation-utils-move-range-not-flat", this);
  const e = fC(i);
  return t = t._getTransformedByDeletion(i.start, i.end.offset - i.start.offset), ap(t, e);
}
function L7(i, t, e) {
  Nl(i.start), Nl(i.end);
  for (const n of i.getItems({
    shallow: !0
  })) {
    const r = n.is("$textProxy") ? n.textNode : n;
    e !== null ? r._setAttribute(t, e) : r._removeAttribute(t), Ll(r.parent, r.index);
  }
  Ll(i.end.parent, i.end.index);
}
function mC(i) {
  const t = [];
  function e(n) {
    if (typeof n == "string")
      t.push(new mt(n));
    else if (n instanceof Gi)
      t.push(new mt(n.data, n.getAttributes()));
    else if (n instanceof no)
      t.push(n);
    else if (ki(n))
      for (const r of n)
        e(r);
  }
  e(i);
  for (let n = 1; n < t.length; n++) {
    const r = t[n], s = t[n - 1];
    r instanceof mt && s instanceof mt && gC(r, s) && (t.splice(n - 1, 2, new mt(s.data + r.data, s.getAttributes())), n--);
  }
  return t;
}
function Ll(i, t) {
  const e = i.getChild(t - 1), n = i.getChild(t);
  if (e && n && e.is("$text") && n.is("$text") && gC(e, n)) {
    const r = new mt(e.data + n.data, e.getAttributes());
    i._removeChildren(t - 1, 2), i._insertChild(t - 1, r);
  }
}
function Nl(i) {
  const t = i.textNode, e = i.parent;
  if (t) {
    const n = i.offset - t.startOffset, r = t.index;
    e._removeChildren(r, 1);
    const s = new mt(t.data.substr(0, n), t.getAttributes()), o = new mt(t.data.substr(n), t.getAttributes());
    e._insertChild(r, [
      s,
      o
    ]);
  }
}
function gC(i, t) {
  const e = i.getAttributes(), n = t.getAttributes();
  for (const r of e) {
    if (r[1] !== t.getAttribute(r[0]))
      return !1;
    n.next();
  }
  return n.next().done;
}
class ot extends Ei {
  /**
  * Creates a move operation.
  *
  * @param sourcePosition Position before the first {@link module:engine/model/item~Item model item} to move.
  * @param howMany Offset size of moved range. Moved range will start from `sourcePosition` and end at
  * `sourcePosition` with offset shifted by `howMany`.
  * @param targetPosition Position at which moved nodes will be inserted.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, n, r, s) {
    super(s);
    /**
    * Position before the first {@link module:engine/model/item~Item model item} to move.
    */
    h(this, "sourcePosition");
    /**
    * Offset size of moved range.
    */
    h(this, "howMany");
    /**
    * Position at which moved nodes will be inserted.
    */
    h(this, "targetPosition");
    this.sourcePosition = e.clone(), this.sourcePosition.stickiness = "toNext", this.howMany = n, this.targetPosition = r.clone(), this.targetPosition.stickiness = "toNone";
  }
  /**
  * @inheritDoc
  */
  get type() {
    return this.targetPosition.root.rootName == "$graveyard" ? "remove" : this.sourcePosition.root.rootName == "$graveyard" ? "reinsert" : "move";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return [
      D._createFromPositionAndShift(this.sourcePosition, this.howMany),
      D._createFromPositionAndShift(this.targetPosition, 0)
    ];
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new ot(this.sourcePosition, this.howMany, this.targetPosition, this.baseVersion);
  }
  /**
  * Returns the start position of the moved range after it got moved. This may be different than
  * {@link module:engine/model/operation/moveoperation~MoveOperation#targetPosition} in some cases, i.e. when a range is moved
  * inside the same parent but {@link module:engine/model/operation/moveoperation~MoveOperation#targetPosition targetPosition}
  * is after {@link module:engine/model/operation/moveoperation~MoveOperation#sourcePosition sourcePosition}.
  *
  * ```
  *  vv              vv
  * abcdefg ===> adefbcg
  *      ^          ^
  *      targetPos  movedRangeStart
  *      offset 6   offset 4
  *```
  */
  getMovedRangeStart() {
    return this.targetPosition._getTransformedByDeletion(this.sourcePosition, this.howMany);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    const e = this.sourcePosition._getTransformedByInsertion(this.targetPosition, this.howMany);
    return new ot(this.getMovedRangeStart(), this.howMany, e, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.sourcePosition.parent, n = this.targetPosition.parent, r = this.sourcePosition.offset, s = this.targetPosition.offset;
    if (r + this.howMany > e.maxOffset)
      throw new T("move-operation-nodes-do-not-exist", this);
    if (e === n && r < s && s < r + this.howMany)
      throw new T("move-operation-range-into-itself", this);
    if (this.sourcePosition.root == this.targetPosition.root && Fn(this.sourcePosition.getParentPath(), this.targetPosition.getParentPath()) == "prefix") {
      const o = this.sourcePosition.path.length - 1;
      if (this.targetPosition.path[o] >= r && this.targetPosition.path[o] < r + this.howMany)
        throw new T("move-operation-node-into-itself", this);
    }
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    Dl(D._createFromPositionAndShift(this.sourcePosition, this.howMany), this.targetPosition);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.sourcePosition = this.sourcePosition.toJSON(), e.targetPosition = this.targetPosition.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "MoveOperation";
  }
  /**
  * Creates `MoveOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    const r = G.fromJSON(e.sourcePosition, n), s = G.fromJSON(e.targetPosition, n);
    return new this(r, e.howMany, s, e.baseVersion);
  }
}
class An extends Ei {
  /**
  * Creates an insert operation.
  *
  * @param position Position of insertion.
  * @param nodes The list of nodes to be inserted.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, n, r) {
    super(r);
    /**
    * Position of insertion.
    *
    * @readonly
    */
    h(this, "position");
    /**
    * List of nodes to insert.
    *
    * @readonly
    */
    h(this, "nodes");
    /**
    * Flag deciding how the operation should be transformed. If set to `true`, nodes might get additional attributes
    * during operational transformation. This happens when the operation insertion position is inside of a range
    * where attributes have changed.
    */
    h(this, "shouldReceiveAttributes");
    this.position = e.clone(), this.position.stickiness = "toNone", this.nodes = new cl(mC(n)), this.shouldReceiveAttributes = !1;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "insert";
  }
  /**
  * Total offset size of inserted nodes.
  */
  get howMany() {
    return this.nodes.maxOffset;
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this.position.clone();
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    const e = new cl([
      ...this.nodes
    ].map((r) => r._clone(!0))), n = new An(this.position, e, this.baseVersion);
    return n.shouldReceiveAttributes = this.shouldReceiveAttributes, n;
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    const e = this.position.root.document.graveyard, n = new G(e, [
      0
    ]);
    return new ot(this.position, this.nodes.maxOffset, n, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.position.parent;
    if (!e || e.maxOffset < this.position.offset)
      throw new T("insert-operation-position-invalid", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    const e = this.nodes;
    this.nodes = new cl([
      ...e
    ].map((n) => n._clone(!0))), ap(this.position, e);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.position = this.position.toJSON(), e.nodes = this.nodes.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "InsertOperation";
  }
  /**
  * Creates `InsertOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    const r = [];
    for (const o of e.nodes)
      o.name ? r.push(xt.fromJSON(o)) : r.push(mt.fromJSON(o));
    const s = new An(G.fromJSON(e.position, n), r, e.baseVersion);
    return s.shouldReceiveAttributes = e.shouldReceiveAttributes, s;
  }
}
class gt extends Ei {
  /**
  * Creates a split operation.
  *
  * @param splitPosition Position at which an element should be split.
  * @param howMany Total offset size of elements that are in the split element after `position`.
  * @param insertionPosition Position at which the clone of split element (or element from graveyard) will be inserted.
  * @param graveyardPosition Position in the graveyard root before the element which
  * should be used as a parent of the nodes after `position`. If it is not set, a copy of the the `position` parent will be used.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, n, r, s, o) {
    super(o);
    /**
    * Position at which an element should be split.
    */
    h(this, "splitPosition");
    /**
    * Total offset size of elements that are in the split element after `position`.
    */
    h(this, "howMany");
    /**
    * Position at which the clone of split element (or element from graveyard) will be inserted.
    */
    h(this, "insertionPosition");
    /**
    * Position in the graveyard root before the element which should be used as a parent of the nodes after `position`.
    * If it is not set, a copy of the the `position` parent will be used.
    *
    * The default behavior is to clone the split element. Element from graveyard is used during undo.
    */
    h(this, "graveyardPosition");
    this.splitPosition = e.clone(), this.splitPosition.stickiness = "toNext", this.howMany = n, this.insertionPosition = r, this.graveyardPosition = s ? s.clone() : null, this.graveyardPosition && (this.graveyardPosition.stickiness = "toNext");
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "split";
  }
  /**
  * Position inside the new clone of a split element.
  *
  * This is a position where nodes that are after the split position will be moved to.
  */
  get moveTargetPosition() {
    const e = this.insertionPosition.path.slice();
    return e.push(0), new G(this.insertionPosition.root, e);
  }
  /**
  * Artificial range that contains all the nodes from the split element that will be moved to the new element.
  * The range starts at {@link #splitPosition} and ends in the same parent, at `POSITIVE_INFINITY` offset.
  */
  get movedRange() {
    const e = this.splitPosition.getShiftedBy(Number.POSITIVE_INFINITY);
    return new D(this.splitPosition, e);
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    const e = [
      D._createFromPositionAndShift(this.splitPosition, 0),
      D._createFromPositionAndShift(this.insertionPosition, 0)
    ];
    return this.graveyardPosition && e.push(D._createFromPositionAndShift(this.graveyardPosition, 0)), e;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  *
  * @returns Clone of this operation.
  */
  clone() {
    return new gt(this.splitPosition, this.howMany, this.insertionPosition, this.graveyardPosition, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    const e = this.splitPosition.root.document.graveyard, n = new G(e, [
      0
    ]);
    return new Kt(this.moveTargetPosition, this.howMany, this.splitPosition, n, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.splitPosition.parent, n = this.splitPosition.offset;
    if (!e || e.maxOffset < n)
      throw new T("split-operation-position-invalid", this);
    if (e.parent) {
      if (this.howMany != e.maxOffset - this.splitPosition.offset)
        throw new T("split-operation-how-many-invalid", this);
      if (this.graveyardPosition && !this.graveyardPosition.nodeAfter)
        throw new T("split-operation-graveyard-position-invalid", this);
    } else throw new T("split-operation-split-in-root", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    const e = this.splitPosition.parent;
    if (this.graveyardPosition)
      Dl(D._createFromPositionAndShift(this.graveyardPosition, 1), this.insertionPosition);
    else {
      const r = e._clone();
      ap(this.insertionPosition, r);
    }
    const n = new D(G._createAt(e, this.splitPosition.offset), G._createAt(e, e.maxOffset));
    Dl(n, this.moveTargetPosition);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.splitPosition = this.splitPosition.toJSON(), e.insertionPosition = this.insertionPosition.toJSON(), this.graveyardPosition && (e.graveyardPosition = this.graveyardPosition.toJSON()), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "SplitOperation";
  }
  /**
  * Helper function that returns a default insertion position basing on given `splitPosition`. The default insertion
  * position is after the split element.
  */
  static getInsertionPosition(e) {
    const n = e.path.slice(0, -1);
    return n[n.length - 1]++, new G(e.root, n, "toPrevious");
  }
  /**
  * Creates `SplitOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    const r = G.fromJSON(e.splitPosition, n), s = G.fromJSON(e.insertionPosition, n), o = e.graveyardPosition ? G.fromJSON(e.graveyardPosition, n) : null;
    return new this(r, e.howMany, s, o, e.baseVersion);
  }
}
class Kt extends Ei {
  /**
  * Creates a merge operation.
  *
  * @param sourcePosition Position inside the merged element. All nodes from that
  * element after that position will be moved to {@link #targetPosition}.
  * @param howMany Summary offset size of nodes which will be moved from the merged element to the new parent.
  * @param targetPosition Position which the nodes from the merged elements will be moved to.
  * @param graveyardPosition Position in graveyard to which the merged element will be moved.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, n, r, s, o) {
    super(o);
    /**
    * Position inside the merged element. All nodes from that element after that position will be moved to {@link #targetPosition}.
    */
    h(this, "sourcePosition");
    /**
    * Summary offset size of nodes which will be moved from the merged element to the new parent.
    */
    h(this, "howMany");
    /**
    * Position which the nodes from the merged elements will be moved to.
    */
    h(this, "targetPosition");
    /**
    * Position in graveyard to which the merged element will be moved.
    */
    h(this, "graveyardPosition");
    this.sourcePosition = e.clone(), this.sourcePosition.stickiness = "toPrevious", this.howMany = n, this.targetPosition = r.clone(), this.targetPosition.stickiness = "toNext", this.graveyardPosition = s.clone();
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "merge";
  }
  /**
  * Position before the merged element (which will be deleted).
  */
  get deletionPosition() {
    return new G(this.sourcePosition.root, this.sourcePosition.path.slice(0, -1));
  }
  /**
  * Artificial range that contains all the nodes from the merged element that will be moved to {@link ~MergeOperation#sourcePosition}.
  * The range starts at {@link ~MergeOperation#sourcePosition} and ends in the same parent, at `POSITIVE_INFINITY` offset.
  */
  get movedRange() {
    const e = this.sourcePosition.getShiftedBy(Number.POSITIVE_INFINITY);
    return new D(this.sourcePosition, e);
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    const e = this.sourcePosition.parent;
    return [
      D._createOn(e),
      // These could be positions but `Selectable` type only supports `Iterable<Range>`.
      D._createFromPositionAndShift(this.targetPosition, 0),
      D._createFromPositionAndShift(this.graveyardPosition, 0)
    ];
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new Kt(this.sourcePosition, this.howMany, this.targetPosition, this.graveyardPosition, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    const e = this.targetPosition._getTransformedByMergeOperation(this), n = this.sourcePosition.path.slice(0, -1), r = new G(this.sourcePosition.root, n)._getTransformedByMergeOperation(this);
    return new gt(e, this.howMany, r, this.graveyardPosition, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.sourcePosition.parent, n = this.targetPosition.parent;
    if (e.parent)
      if (n.parent) {
        if (this.howMany != e.maxOffset)
          throw new T("merge-operation-how-many-invalid", this);
      } else throw new T("merge-operation-target-position-invalid", this);
    else throw new T("merge-operation-source-position-invalid", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    const e = this.sourcePosition.parent, n = D._createIn(e);
    Dl(n, this.targetPosition), Dl(D._createOn(e), this.graveyardPosition);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.sourcePosition = e.sourcePosition.toJSON(), e.targetPosition = e.targetPosition.toJSON(), e.graveyardPosition = e.graveyardPosition.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "MergeOperation";
  }
  /**
  * Creates `MergeOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    const r = G.fromJSON(e.sourcePosition, n), s = G.fromJSON(e.targetPosition, n), o = G.fromJSON(e.graveyardPosition, n);
    return new this(r, e.howMany, s, o, e.baseVersion);
  }
}
class Zn extends Ei {
  /**
  * @param name Marker name.
  * @param oldRange Marker range before the change.
  * @param newRange Marker range after the change.
  * @param markers Marker collection on which change should be executed.
  * @param affectsData Specifies whether the marker operation affects the data produced by the data pipeline
  * (is persisted in the editor's data).
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, n, r, s, o, a) {
    super(a);
    /**
    * Marker name.
    *
    * @readonly
    */
    h(this, "name");
    /**
    * Marker range before the change.
    *
    * @readonly
    */
    h(this, "oldRange");
    /**
    * Marker range after the change.
    *
    * @readonly
    */
    h(this, "newRange");
    /**
    * Specifies whether the marker operation affects the data produced by the data pipeline
    * (is persisted in the editor's data).
    *
    * @readonly
    */
    h(this, "affectsData");
    /**
    * Marker collection on which change should be executed.
    */
    h(this, "_markers");
    this.name = e, this.oldRange = n ? n.clone() : null, this.newRange = r ? r.clone() : null, this.affectsData = o, this._markers = s;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "marker";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    const e = [];
    return this.oldRange && e.push(this.oldRange.clone()), this.newRange && (this.oldRange ? e.push(...this.newRange.getDifference(this.oldRange)) : e.push(this.newRange.clone())), e;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new Zn(this.name, this.oldRange, this.newRange, this._markers, this.affectsData, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new Zn(this.name, this.newRange, this.oldRange, this._markers, this.affectsData, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    this.newRange ? this._markers._set(this.name, this.newRange, !0, this.affectsData) : this._markers._remove(this.name);
  }
  /**
  * @inheritDoc
  * @internal
  */
  toJSON() {
    const e = super.toJSON();
    return this.oldRange && (e.oldRange = this.oldRange.toJSON()), this.newRange && (e.newRange = this.newRange.toJSON()), delete e._markers, e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "MarkerOperation";
  }
  /**
  * Creates `MarkerOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    return new Zn(e.name, e.oldRange ? D.fromJSON(e.oldRange, n) : null, e.newRange ? D.fromJSON(e.newRange, n) : null, n.model.markers, e.affectsData, e.baseVersion);
  }
}
class en extends Ei {
  /**
  * Creates an operation that changes, removes or adds attributes.
  *
  * If only `newValue` is set, attribute will be added on a node. Note that all nodes in operation's range must not
  * have an attribute with the same key as the added attribute.
  *
  * If only `oldValue` is set, then attribute with given key will be removed. Note that all nodes in operation's range
  * must have an attribute with that key added.
  *
  * If both `newValue` and `oldValue` are set, then the operation will change the attribute value. Note that all nodes in
  * operation's ranges must already have an attribute with given key and `oldValue` as value
  *
  * @param range Range on which the operation should be applied. Must be a flat range.
  * @param key Key of an attribute to change or remove.
  * @param oldValue Old value of the attribute with given key or `null`, if attribute was not set before.
  * @param newValue New value of the attribute with given key or `null`, if operation should remove attribute.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, n, r, s, o) {
    super(o);
    /**
    * Range on which operation should be applied.
    *
    * @readonly
    */
    h(this, "range");
    /**
    * Key of an attribute to change or remove.
    *
    * @readonly
    */
    h(this, "key");
    /**
    * Old value of the attribute with given key or `null`, if attribute was not set before.
    *
    * @readonly
    */
    h(this, "oldValue");
    /**
    * New value of the attribute with given key or `null`, if operation should remove attribute.
    *
    * @readonly
    */
    h(this, "newValue");
    this.range = e.clone(), this.key = n, this.oldValue = r === void 0 ? null : r, this.newValue = s === void 0 ? null : s;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return this.oldValue === null ? "addAttribute" : this.newValue === null ? "removeAttribute" : "changeAttribute";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this.range.clone();
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new en(this.range, this.key, this.oldValue, this.newValue, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new en(this.range, this.key, this.newValue, this.oldValue, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.range = this.range.toJSON(), e;
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    if (!this.range.isFlat)
      throw new T("attribute-operation-range-not-flat", this);
    for (const e of this.range.getItems({
      shallow: !0
    })) {
      if (this.oldValue !== null && !Yo(e.getAttribute(this.key), this.oldValue))
        throw new T("attribute-operation-wrong-old-value", this, {
          item: e,
          key: this.key,
          value: this.oldValue
        });
      if (this.oldValue === null && this.newValue !== null && e.hasAttribute(this.key))
        throw new T("attribute-operation-attribute-exists", this, {
          node: e,
          key: this.key
        });
    }
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    Yo(this.oldValue, this.newValue) || L7(this.range, this.key, this.newValue);
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "AttributeOperation";
  }
  /**
  * Creates `AttributeOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    return new en(D.fromJSON(e.range, n), e.key, e.oldValue, e.newValue, e.baseVersion);
  }
}
class gn extends Ei {
  get type() {
    return "noop";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return null;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new gn(this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new gn(this.baseVersion + 1);
  }
  /** @internal */
  _execute() {
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "NoOperation";
  }
}
class ai extends Ei {
  /**
  * Creates an operation that changes element's name.
  *
  * @param position Position before an element to change.
  * @param oldName Current name of the element.
  * @param newName New name for the element.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, n, r, s) {
    super(s);
    /**
    * Position before an element to change.
    */
    h(this, "position");
    /**
    * Current name of the element.
    */
    h(this, "oldName");
    /**
    * New name for the element.
    */
    h(this, "newName");
    this.position = e, this.position.stickiness = "toNext", this.oldName = n, this.newName = r;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "rename";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this.position.nodeAfter;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  *
  * @returns Clone of this operation.
  */
  clone() {
    return new ai(this.position.clone(), this.oldName, this.newName, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new ai(this.position.clone(), this.newName, this.oldName, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.position.nodeAfter;
    if (e instanceof xt) {
      if (e.name !== this.oldName)
        throw new T("rename-operation-wrong-name", this);
    } else throw new T("rename-operation-wrong-position", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    const e = this.position.nodeAfter;
    e.name = this.newName;
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.position = this.position.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "RenameOperation";
  }
  /**
  * Creates `RenameOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    return new ai(G.fromJSON(e.position, n), e.oldName, e.newName, e.baseVersion);
  }
}
class Lr extends Ei {
  /**
  * Creates an operation that changes, removes or adds attributes on root element.
  *
  * @see module:engine/model/operation/attributeoperation~AttributeOperation
  * @param root Root element to change.
  * @param key Key of an attribute to change or remove.
  * @param oldValue Old value of the attribute with given key or `null`, if attribute was not set before.
  * @param newValue New value of the attribute with given key or `null`, if operation should remove attribute.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, n, r, s, o) {
    super(o);
    /**
    * Root element to change.
    */
    h(this, "root");
    /**
    * Key of an attribute to change or remove.
    */
    h(this, "key");
    /**
    * Old value of the attribute with given key or `null`, if attribute was not set before.
    *
    * @readonly
    */
    h(this, "oldValue");
    /**
    * New value of the attribute with given key or `null`, if operation should remove attribute.
    *
    * @readonly
    */
    h(this, "newValue");
    this.root = e, this.key = n, this.oldValue = r === void 0 ? null : r, this.newValue = s === void 0 ? null : s;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return this.oldValue === null ? "addRootAttribute" : this.newValue === null ? "removeRootAttribute" : "changeRootAttribute";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this.root;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  *
  * @returns Clone of this operation.
  */
  clone() {
    return new Lr(this.root, this.key, this.oldValue, this.newValue, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new Lr(this.root, this.key, this.newValue, this.oldValue, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    if (this.root != this.root.root || this.root.is("documentFragment"))
      throw new T("rootattribute-operation-not-a-root", this, {
        root: this.root,
        key: this.key
      });
    if (this.oldValue !== null && this.root.getAttribute(this.key) !== this.oldValue)
      throw new T("rootattribute-operation-wrong-old-value", this, {
        root: this.root,
        key: this.key
      });
    if (this.oldValue === null && this.newValue !== null && this.root.hasAttribute(this.key))
      throw new T("rootattribute-operation-attribute-exists", this, {
        root: this.root,
        key: this.key
      });
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    this.newValue !== null ? this.root._setAttribute(this.key, this.newValue) : this.root._removeAttribute(this.key);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.root = this.root.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "RootAttributeOperation";
  }
  /**
  * Creates `RootAttributeOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    if (!n.getRoot(e.root))
      throw new T("rootattribute-operation-fromjson-no-root", this, {
        rootName: e.root
      });
    return new Lr(n.getRoot(e.root), e.key, e.oldValue, e.newValue, e.baseVersion);
  }
}
class ur extends Ei {
  /**
  * Creates an operation that creates or removes a root element.
  *
  * @param rootName Root name to create or detach.
  * @param elementName Root element name.
  * @param isAdd Specifies whether the operation adds (`true`) or detaches the root (`false`).
  * @param document Document which owns the root.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation can be applied.
  */
  constructor(e, n, r, s, o) {
    super(o);
    /**
    * Root name to create or detach.
    */
    h(this, "rootName");
    /**
    * Root element name.
    */
    h(this, "elementName");
    /**
    * Specifies whether the operation adds (`true`) or detaches the root (`false`).
    */
    h(this, "isAdd");
    /**
    * Document which owns the root.
    */
    h(this, "_document");
    if (this.rootName = e, this.elementName = n, this.isAdd = r, this._document = s, !this._document.getRoot(this.rootName)) {
      const a = this._document.createRoot(this.elementName, this.rootName);
      a._isAttached = !1;
    }
  }
  /**
  * @inheritDoc
  */
  get type() {
    return this.isAdd ? "addRoot" : "detachRoot";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this._document.getRoot(this.rootName);
  }
  /**
  * @inheritDoc
  */
  clone() {
    return new ur(this.rootName, this.elementName, this.isAdd, this._document, this.baseVersion);
  }
  /**
  * @inheritDoc
  */
  getReversed() {
    return new ur(this.rootName, this.elementName, !this.isAdd, this._document, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  */
  _execute() {
    this._document.getRoot(this.rootName)._isAttached = this.isAdd;
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return delete e._document, e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "RootOperation";
  }
  /**
  * Creates `RootOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, n) {
    return new ur(e.rootName, e.elementName, e.isAdd, n, e.baseVersion);
  }
}
const $i = {};
$i[en.className] = en;
$i[An.className] = An;
$i[Zn.className] = Zn;
$i[ot.className] = ot;
$i[gn.className] = gn;
$i[Ei.className] = Ei;
$i[ai.className] = ai;
$i[Lr.className] = Lr;
$i[ur.className] = ur;
$i[gt.className] = gt;
$i[Kt.className] = Kt;
class N7 {
  /**
  * Creates an operation instance from a JSON object (parsed JSON string).
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(t, e) {
    return $i[t.__className].fromJSON(t, e);
  }
}
const cm = /* @__PURE__ */ new Map();
function nt(i, t, e) {
  let n = cm.get(i);
  n || (n = /* @__PURE__ */ new Map(), cm.set(i, n)), n.set(t, e);
}
function B7(i, t) {
  const e = cm.get(i);
  return e && e.has(t) ? e.get(t) : F7;
}
function F7(i) {
  return [
    i
  ];
}
function Y1(i, t, e = {}) {
  const n = B7(i.constructor, t.constructor);
  try {
    return i = i.clone(), n(i, t, e);
  } catch (r) {
    throw r;
  }
}
function $7(i, t, e) {
  i = i.slice(), t = t.slice();
  const n = new z7(e.document, e.useRelations, e.forceWeakRemove);
  n.setOriginalOperations(i), n.setOriginalOperations(t);
  const r = n.originalOperations;
  if (i.length == 0 || t.length == 0)
    return {
      operationsA: i,
      operationsB: t,
      originalOperations: r
    };
  const s = /* @__PURE__ */ new WeakMap();
  for (const l of i)
    s.set(l, 0);
  const o = {
    nextBaseVersionA: i[i.length - 1].baseVersion + 1,
    nextBaseVersionB: t[t.length - 1].baseVersion + 1,
    originalOperationsACount: i.length,
    originalOperationsBCount: t.length
  };
  let a = 0;
  for (; a < i.length; ) {
    const l = i[a], c = s.get(l);
    if (c == t.length) {
      a++;
      continue;
    }
    const u = t[c], d = Y1(l, u, n.getContext(l, u, !0)), f = Y1(u, l, n.getContext(u, l, !1));
    n.updateRelation(l, u), n.setOriginalOperations(d, l), n.setOriginalOperations(f, u);
    for (const m of d)
      s.set(m, c + f.length);
    i.splice(a, 1, ...d), t.splice(c, 1, ...f);
  }
  return X1(i), X1(t), J1(i, o.nextBaseVersionB), J1(t, o.nextBaseVersionA), {
    operationsA: i,
    operationsB: t,
    originalOperations: r
  };
}
class z7 {
  /**
  * Creates `ContextFactory` instance.
  *
  * @param document Document which the operations change.
  * @param useRelations Whether during transformation relations should be used (used during undo for
  * better conflict resolution).
  * @param forceWeakRemove If set to `false`, remove operation will be always stronger than move operation,
  * so the removed nodes won't end up back in the document root. When set to `true`, context data will be used.
  */
  constructor(t, e, n = !1) {
    h(this, "originalOperations");
    h(this, "_history");
    h(this, "_useRelations");
    h(this, "_forceWeakRemove");
    h(this, "_relations");
    this.originalOperations = /* @__PURE__ */ new Map(), this._history = t.history, this._useRelations = e, this._forceWeakRemove = !!n, this._relations = /* @__PURE__ */ new Map();
  }
  /**
  * Sets "original operation" for given operations.
  *
  * During transformation process, operations are cloned, then changed, then processed again, sometimes broken into two
  * or multiple operations. When gathering additional data it is important that all operations can be somehow linked
  * so a cloned and transformed "version" still kept track of the data assigned earlier to it.
  *
  * The original operation object will be used as such an universal linking id. Throughout the transformation process
  * all cloned operations will refer to "the original operation" when storing and reading additional data.
  *
  * If `takeFrom` is not set, each operation from `operations` array will be assigned itself as "the original operation".
  * This should be used as an initialization step.
  *
  * If `takeFrom` is set, each operation from `operations` will be assigned the same original operation as assigned
  * for `takeFrom` operation. This should be used to update original operations. It should be used in a way that
  * `operations` are the result of `takeFrom` transformation to ensure proper "original operation propagation".
  */
  setOriginalOperations(t, e = null) {
    const n = e ? this.originalOperations.get(e) : null;
    for (const r of t)
      this.originalOperations.set(r, n || r);
  }
  /**
  * Saves a relation between operations `opA` and `opB`.
  *
  * Relations are then later used to help solve conflicts when operations are transformed.
  */
  updateRelation(t, e) {
    if (t instanceof ot)
      e instanceof Kt ? t.targetPosition.isEqual(e.sourcePosition) || e.movedRange.containsPosition(t.targetPosition) ? this._setRelation(t, e, "insertAtSource") : t.targetPosition.isEqual(e.deletionPosition) ? this._setRelation(t, e, "insertBetween") : t.targetPosition.isAfter(e.sourcePosition) && this._setRelation(t, e, "moveTargetAfter") : e instanceof ot && (t.targetPosition.isEqual(e.sourcePosition) || t.targetPosition.isBefore(e.sourcePosition) ? this._setRelation(t, e, "insertBefore") : this._setRelation(t, e, "insertAfter"));
    else if (t instanceof gt) {
      if (e instanceof Kt)
        t.splitPosition.isBefore(e.sourcePosition) && this._setRelation(t, e, "splitBefore");
      else if (e instanceof ot)
        if (t.splitPosition.isEqual(e.sourcePosition) || t.splitPosition.isBefore(e.sourcePosition))
          this._setRelation(t, e, "splitBefore");
        else {
          const n = D._createFromPositionAndShift(e.sourcePosition, e.howMany);
          if (t.splitPosition.hasSameParentAs(e.sourcePosition) && n.containsPosition(t.splitPosition)) {
            const r = n.end.offset - t.splitPosition.offset, s = t.splitPosition.offset - n.start.offset;
            this._setRelation(t, e, {
              howMany: r,
              offset: s
            });
          }
        }
    } else if (t instanceof Kt)
      e instanceof Kt ? (t.targetPosition.isEqual(e.sourcePosition) || this._setRelation(t, e, "mergeTargetNotMoved"), t.sourcePosition.isEqual(e.targetPosition) && this._setRelation(t, e, "mergeSourceNotMoved"), t.sourcePosition.isEqual(e.sourcePosition) && this._setRelation(t, e, "mergeSameElement")) : e instanceof gt ? t.sourcePosition.isEqual(e.splitPosition) && this._setRelation(t, e, "splitAtSource") : e instanceof ot && e.howMany > 0 && (t.sourcePosition.isEqual(e.sourcePosition.getShiftedBy(e.howMany)) && this._setRelation(t, e, "mergeSourceAffected"), t.targetPosition.isEqual(e.sourcePosition) && this._setRelation(t, e, "mergeTargetWasBefore"));
    else if (t instanceof Zn) {
      const n = t.newRange;
      if (!n)
        return;
      if (e instanceof Kt) {
        const r = n.start.isEqual(e.targetPosition), s = n.start.isEqual(e.deletionPosition), o = n.end.isEqual(e.deletionPosition), a = n.end.isEqual(e.sourcePosition);
        (r || s || o || a) && this._setRelation(t, e, {
          wasInLeftElement: r,
          wasStartBeforeMergedElement: s,
          wasEndBeforeMergedElement: o,
          wasInRightElement: a
        });
      }
    }
  }
  /**
  * Evaluates and returns contextual information about two given operations `opA` and `opB` which are about to be transformed.
  */
  getContext(t, e, n) {
    return {
      aIsStrong: n,
      aWasUndone: this._wasUndone(t),
      bWasUndone: this._wasUndone(e),
      abRelation: this._useRelations ? this._getRelation(t, e) : null,
      baRelation: this._useRelations ? this._getRelation(e, t) : null,
      forceWeakRemove: this._forceWeakRemove
    };
  }
  /**
  * Returns whether given operation `op` has already been undone.
  *
  * Information whether an operation was undone gives more context when making a decision when two operations are in conflict.
  */
  _wasUndone(t) {
    const e = this.originalOperations.get(t);
    return e.wasUndone || this._history.isUndoneOperation(e);
  }
  /**
  * Returns a relation between `opA` and an operation which is undone by `opB`. This can be `String` value if a relation
  * was set earlier or `null` if there was no relation between those operations.
  *
  * This is a little tricky to understand, so let's compare it to `ContextFactory#_wasUndone`.
  *
  * When `wasUndone( opB )` is used, we check if the `opB` has already been undone. It is obvious, that the
  * undoing operation must happen after the undone operation. So, essentially, we have `opB`, we take document history,
  * we look forward in the future and ask if in that future `opB` was undone.
  *
  * Relations is a backward process to `wasUndone()`.
  *
  * Long story short - using relations is asking what happened in the past. Looking back. This time we have an undoing
  * operation `opB` which has undone some other operation. When there is a transformation `opA` x `opB` and there is
  * a conflict to solve and `opB` is an undoing operation, we can look back in the history and see what was a relation
  * between `opA` and the operation which `opB` undone. Basing on that relation from the past, we can now make
  * a better decision when resolving a conflict between two operations, because we know more about the context of
  * those two operations.
  *
  * This is why this function does not return a relation directly between `opA` and `opB` because we need to look
  * back to search for a meaningful contextual information.
  */
  _getRelation(t, e) {
    const n = this.originalOperations.get(e), r = this._history.getUndoneOperation(n);
    if (!r)
      return null;
    const s = this.originalOperations.get(t), o = this._relations.get(s);
    return o && o.get(r) || null;
  }
  /**
  * Helper function for `ContextFactory#updateRelations`.
  */
  _setRelation(t, e, n) {
    const r = this.originalOperations.get(t), s = this.originalOperations.get(e);
    let o = this._relations.get(r);
    o || (o = /* @__PURE__ */ new Map(), this._relations.set(r, o)), o.set(s, n);
  }
}
function J1(i, t) {
  for (const e of i)
    e.baseVersion = t++;
}
function X1(i) {
  const t = /* @__PURE__ */ new Map();
  for (let e = 0; e < i.length; e++) {
    const n = i[e];
    n instanceof Zn && (n.baseVersion !== -1 ? t.set(n.name, {
      op: n,
      ranges: n.newRange ? [
        n.newRange
      ] : []
    }) : (n.newRange && t.get(n.name).ranges.push(n.newRange), i.splice(e, 1), e--));
  }
  for (const { op: e, ranges: n } of t.values())
    n.length ? e.newRange = D._createFromRanges(n) : e.newRange = null;
}
nt(en, en, (i, t, e) => {
  if (i.key === t.key && i.range.start.hasSameParentAs(t.range.start)) {
    const n = i.range.getDifference(t.range).map((s) => new en(s, i.key, i.oldValue, i.newValue, 0)), r = i.range.getIntersection(t.range);
    return r && e.aIsStrong && n.push(new en(r, t.key, t.newValue, i.newValue, 0)), n.length == 0 ? [
      new gn(0)
    ] : n;
  } else
    return [
      i
    ];
});
nt(en, An, (i, t) => {
  if (i.range.start.hasSameParentAs(t.position) && i.range.containsPosition(t.position)) {
    const n = i.range._getTransformedByInsertion(t.position, t.howMany, !t.shouldReceiveAttributes).map((r) => new en(r, i.key, i.oldValue, i.newValue, i.baseVersion));
    if (t.shouldReceiveAttributes) {
      const r = pC(t, i.key, i.oldValue);
      r && n.unshift(r);
    }
    return n;
  }
  return i.range = i.range._getTransformedByInsertion(t.position, t.howMany, !1)[0], [
    i
  ];
});
function pC(i, t, e) {
  const r = i.nodes.getNode(0).getAttribute(t);
  if (r == e)
    return null;
  const s = new D(i.position, i.position.getShiftedBy(i.howMany));
  return new en(s, t, r, e, 0);
}
nt(en, Kt, (i, t) => {
  const e = [];
  i.range.start.hasSameParentAs(t.deletionPosition) && (i.range.containsPosition(t.deletionPosition) || i.range.start.isEqual(t.deletionPosition)) && e.push(D._createFromPositionAndShift(t.graveyardPosition, 1));
  const n = i.range._getTransformedByMergeOperation(t);
  return n.isCollapsed || e.push(n), e.map((r) => new en(r, i.key, i.oldValue, i.newValue, i.baseVersion));
});
nt(en, ot, (i, t) => H7(i.range, t).map((n) => new en(n, i.key, i.oldValue, i.newValue, i.baseVersion)));
function H7(i, t) {
  const e = D._createFromPositionAndShift(t.sourcePosition, t.howMany);
  let n = null, r = [];
  e.containsRange(i, !0) ? n = i : i.start.hasSameParentAs(e.start) ? (r = i.getDifference(e), n = i.getIntersection(e)) : r = [
    i
  ];
  const s = [];
  for (let o of r) {
    o = o._getTransformedByDeletion(t.sourcePosition, t.howMany);
    const a = t.getMovedRangeStart(), l = o.start.hasSameParentAs(a), c = o._getTransformedByInsertion(a, t.howMany, l);
    s.push(...c);
  }
  return n && s.push(n._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany, !1)[0]), s;
}
nt(en, gt, (i, t) => {
  if (i.range.end.isEqual(t.insertionPosition))
    return t.graveyardPosition || i.range.end.offset++, [
      i
    ];
  if (i.range.start.hasSameParentAs(t.splitPosition) && i.range.containsPosition(t.splitPosition)) {
    const e = i.clone();
    return e.range = new D(t.moveTargetPosition.clone(), i.range.end._getCombined(t.splitPosition, t.moveTargetPosition)), i.range.end = t.splitPosition.clone(), i.range.end.stickiness = "toPrevious", [
      i,
      e
    ];
  }
  return i.range = i.range._getTransformedBySplitOperation(t), [
    i
  ];
});
nt(An, en, (i, t) => {
  const e = [
    i
  ];
  if (i.shouldReceiveAttributes && i.position.hasSameParentAs(t.range.start) && t.range.containsPosition(i.position)) {
    const n = pC(i, t.key, t.newValue);
    n && e.push(n);
  }
  return e;
});
nt(An, An, (i, t, e) => i.position.isEqual(t.position) && e.aIsStrong ? [
  i
] : (i.position = i.position._getTransformedByInsertOperation(t), [
  i
]));
nt(An, ot, (i, t) => (i.position = i.position._getTransformedByMoveOperation(t), [
  i
]));
nt(An, gt, (i, t) => (i.position = i.position._getTransformedBySplitOperation(t), [
  i
]));
nt(An, Kt, (i, t) => (i.position = i.position._getTransformedByMergeOperation(t), [
  i
]));
nt(Zn, An, (i, t) => (i.oldRange && (i.oldRange = i.oldRange._getTransformedByInsertOperation(t)[0]), i.newRange && (i.newRange = i.newRange._getTransformedByInsertOperation(t)[0]), [
  i
]));
nt(Zn, Zn, (i, t, e) => {
  if (i.name == t.name)
    if (e.aIsStrong)
      i.oldRange = t.newRange ? t.newRange.clone() : null;
    else
      return [
        new gn(0)
      ];
  return [
    i
  ];
});
nt(Zn, Kt, (i, t) => (i.oldRange && (i.oldRange = i.oldRange._getTransformedByMergeOperation(t)), i.newRange && (i.newRange = i.newRange._getTransformedByMergeOperation(t)), [
  i
]));
nt(Zn, ot, (i, t) => {
  const e = [
    i
  ];
  if (i.oldRange && (i.oldRange = D._createFromRanges(i.oldRange._getTransformedByMoveOperation(t))), i.newRange) {
    const n = i.newRange._getTransformedByMoveOperation(t);
    i.newRange = n[0];
    for (let r = 1; r < n.length; r++) {
      const s = i.clone();
      s.oldRange = null, s.newRange = n[r], s.baseVersion = -1, e.push(s);
    }
  }
  return e;
});
nt(Zn, gt, (i, t, e) => {
  if (i.oldRange && (i.oldRange = i.oldRange._getTransformedBySplitOperation(t)), i.newRange) {
    if (e.abRelation) {
      const n = i.newRange._getTransformedBySplitOperation(t);
      return i.newRange.start.isEqual(t.splitPosition) && e.abRelation.wasStartBeforeMergedElement ? i.newRange.start = G._createAt(t.insertionPosition) : i.newRange.start.isEqual(t.splitPosition) && !e.abRelation.wasInLeftElement ? i.newRange.start = G._createAt(t.moveTargetPosition) : i.newRange.start = n.start, i.newRange.end.isEqual(t.splitPosition) && e.abRelation.wasInRightElement ? i.newRange.end = G._createAt(t.moveTargetPosition) : i.newRange.end.isEqual(t.splitPosition) && e.abRelation.wasEndBeforeMergedElement ? i.newRange.end = G._createAt(t.insertionPosition) : i.newRange.end = n.end, [
        i
      ];
    }
    i.newRange = i.newRange._getTransformedBySplitOperation(t);
  }
  return [
    i
  ];
});
nt(Kt, An, (i, t) => (i.sourcePosition.hasSameParentAs(t.position) && (i.howMany += t.howMany), i.sourcePosition = i.sourcePosition._getTransformedByInsertOperation(t), i.targetPosition = i.targetPosition._getTransformedByInsertOperation(t), [
  i
]));
nt(Kt, Kt, (i, t, e) => {
  if (i.sourcePosition.isEqual(t.sourcePosition) && i.targetPosition.isEqual(t.targetPosition))
    if (e.bWasUndone) {
      const n = t.graveyardPosition.path.slice();
      return n.push(0), i.sourcePosition = new G(t.graveyardPosition.root, n), i.howMany = 0, [
        i
      ];
    } else
      return [
        new gn(0)
      ];
  if (i.sourcePosition.isEqual(t.sourcePosition) && !i.targetPosition.isEqual(t.targetPosition) && !e.bWasUndone && e.abRelation != "splitAtSource") {
    const n = i.targetPosition.root.rootName == "$graveyard", r = t.targetPosition.root.rootName == "$graveyard";
    if (r && !n || !(n && !r) && e.aIsStrong) {
      const l = t.targetPosition._getTransformedByMergeOperation(t), c = i.targetPosition._getTransformedByMergeOperation(t);
      return [
        new ot(l, i.howMany, c, 0)
      ];
    } else
      return [
        new gn(0)
      ];
  }
  return i.sourcePosition.hasSameParentAs(t.targetPosition) && (i.howMany += t.howMany), i.sourcePosition = i.sourcePosition._getTransformedByMergeOperation(t), i.targetPosition = i.targetPosition._getTransformedByMergeOperation(t), (!i.graveyardPosition.isEqual(t.graveyardPosition) || !e.aIsStrong) && (i.graveyardPosition = i.graveyardPosition._getTransformedByMergeOperation(t)), [
    i
  ];
});
nt(Kt, ot, (i, t, e) => {
  const n = D._createFromPositionAndShift(t.sourcePosition, t.howMany);
  return t.type == "remove" && !e.bWasUndone && i.deletionPosition.hasSameParentAs(t.sourcePosition) && n.containsPosition(i.sourcePosition) ? [
    new gn(0)
  ] : (t.sourcePosition.getShiftedBy(t.howMany).isEqual(i.sourcePosition) ? i.sourcePosition.stickiness = "toNone" : t.targetPosition.isEqual(i.sourcePosition) && e.abRelation == "mergeSourceAffected" ? i.sourcePosition.stickiness = "toNext" : t.sourcePosition.isEqual(i.targetPosition) ? (i.targetPosition.stickiness = "toNone", i.howMany -= t.howMany) : t.targetPosition.isEqual(i.targetPosition) && e.abRelation == "mergeTargetWasBefore" ? (i.targetPosition.stickiness = "toPrevious", i.howMany += t.howMany) : (i.sourcePosition.hasSameParentAs(t.targetPosition) && (i.howMany += t.howMany), i.sourcePosition.hasSameParentAs(t.sourcePosition) && (i.howMany -= t.howMany)), i.sourcePosition = i.sourcePosition._getTransformedByMoveOperation(t), i.targetPosition = i.targetPosition._getTransformedByMoveOperation(t), i.sourcePosition.stickiness = "toPrevious", i.targetPosition.stickiness = "toNext", i.graveyardPosition.isEqual(t.targetPosition) || (i.graveyardPosition = i.graveyardPosition._getTransformedByMoveOperation(t)), [
    i
  ]);
});
nt(Kt, gt, (i, t, e) => {
  if (t.graveyardPosition && (i.graveyardPosition = i.graveyardPosition._getTransformedByDeletion(t.graveyardPosition, 1), i.deletionPosition.isEqual(t.graveyardPosition) && (i.howMany = t.howMany)), i.targetPosition.isEqual(t.splitPosition) && (t.graveyardPosition && i.deletionPosition.isEqual(t.graveyardPosition) || e.abRelation == "mergeTargetNotMoved"))
    return i.sourcePosition = i.sourcePosition._getTransformedBySplitOperation(t), [
      i
    ];
  if (i.sourcePosition.isEqual(t.splitPosition)) {
    if (e.abRelation == "mergeSourceNotMoved")
      return i.howMany = 0, i.targetPosition = i.targetPosition._getTransformedBySplitOperation(t), [
        i
      ];
    if (e.abRelation == "mergeSameElement" || i.sourcePosition.offset > 0)
      return i.sourcePosition = t.moveTargetPosition.clone(), i.targetPosition = i.targetPosition._getTransformedBySplitOperation(t), [
        i
      ];
  }
  return i.sourcePosition.hasSameParentAs(t.splitPosition) && (i.howMany = t.splitPosition.offset), i.sourcePosition = i.sourcePosition._getTransformedBySplitOperation(t), i.targetPosition = i.targetPosition._getTransformedBySplitOperation(t), [
    i
  ];
});
nt(ot, An, (i, t) => {
  const n = D._createFromPositionAndShift(i.sourcePosition, i.howMany)._getTransformedByInsertOperation(t, !1)[0];
  return i.sourcePosition = n.start, i.howMany = n.end.offset - n.start.offset, i.targetPosition.isEqual(t.position) || (i.targetPosition = i.targetPosition._getTransformedByInsertOperation(t)), [
    i
  ];
});
nt(ot, ot, (i, t, e) => {
  const n = D._createFromPositionAndShift(i.sourcePosition, i.howMany), r = D._createFromPositionAndShift(t.sourcePosition, t.howMany);
  let s = e.aIsStrong, o = !e.aIsStrong;
  e.abRelation == "insertBefore" || e.baRelation == "insertAfter" ? o = !0 : (e.abRelation == "insertAfter" || e.baRelation == "insertBefore") && (o = !1);
  let a;
  if (i.targetPosition.isEqual(t.targetPosition) && o ? a = i.targetPosition._getTransformedByDeletion(t.sourcePosition, t.howMany) : a = i.targetPosition._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), Q1(i, t) && Q1(t, i))
    return [
      t.getReversed()
    ];
  if (n.containsPosition(t.targetPosition) && n.containsRange(r, !0))
    return n.start = n.start._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), n.end = n.end._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), $o([
      n
    ], a);
  if (r.containsPosition(i.targetPosition) && r.containsRange(n, !0))
    return n.start = n.start._getCombined(t.sourcePosition, t.getMovedRangeStart()), n.end = n.end._getCombined(t.sourcePosition, t.getMovedRangeStart()), $o([
      n
    ], a);
  const u = Fn(i.sourcePosition.getParentPath(), t.sourcePosition.getParentPath());
  if (u == "prefix" || u == "extension")
    return n.start = n.start._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), n.end = n.end._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), $o([
      n
    ], a);
  i.type == "remove" && t.type != "remove" && !e.aWasUndone && !e.forceWeakRemove ? s = !0 : i.type != "remove" && t.type == "remove" && !e.bWasUndone && !e.forceWeakRemove && (s = !1);
  const d = [], f = n.getDifference(r);
  for (const g of f) {
    g.start = g.start._getTransformedByDeletion(t.sourcePosition, t.howMany), g.end = g.end._getTransformedByDeletion(t.sourcePosition, t.howMany);
    const p = Fn(g.start.getParentPath(), t.getMovedRangeStart().getParentPath()) == "same", b = g._getTransformedByInsertion(t.getMovedRangeStart(), t.howMany, p);
    d.push(...b);
  }
  const m = n.getIntersection(r);
  return m !== null && s && (m.start = m.start._getCombined(t.sourcePosition, t.getMovedRangeStart()), m.end = m.end._getCombined(t.sourcePosition, t.getMovedRangeStart()), d.length === 0 ? d.push(m) : d.length == 1 ? r.start.isBefore(n.start) || r.start.isEqual(n.start) ? d.unshift(m) : d.push(m) : d.splice(1, 0, m)), d.length === 0 ? [
    new gn(i.baseVersion)
  ] : $o(d, a);
});
nt(ot, gt, (i, t, e) => {
  let n = i.targetPosition.clone();
  (!i.targetPosition.isEqual(t.insertionPosition) || !t.graveyardPosition || e.abRelation == "moveTargetAfter") && (n = i.targetPosition._getTransformedBySplitOperation(t));
  const r = D._createFromPositionAndShift(i.sourcePosition, i.howMany);
  if (r.end.isEqual(t.insertionPosition))
    return t.graveyardPosition || i.howMany++, i.targetPosition = n, [
      i
    ];
  if (r.start.hasSameParentAs(t.splitPosition) && r.containsPosition(t.splitPosition)) {
    let a = new D(t.splitPosition, r.end);
    a = a._getTransformedBySplitOperation(t);
    const l = [
      new D(r.start, t.splitPosition),
      a
    ];
    return $o(l, n);
  }
  i.targetPosition.isEqual(t.splitPosition) && e.abRelation == "insertAtSource" && (n = t.moveTargetPosition), i.targetPosition.isEqual(t.insertionPosition) && e.abRelation == "insertBetween" && (n = i.targetPosition);
  const o = [
    r._getTransformedBySplitOperation(t)
  ];
  if (t.graveyardPosition) {
    const a = r.start.isEqual(t.graveyardPosition) || r.containsPosition(t.graveyardPosition);
    i.howMany > 1 && a && !e.aWasUndone && o.push(D._createFromPositionAndShift(t.insertionPosition, 1));
  }
  return $o(o, n);
});
nt(ot, Kt, (i, t, e) => {
  const n = D._createFromPositionAndShift(i.sourcePosition, i.howMany);
  if (t.deletionPosition.hasSameParentAs(i.sourcePosition) && n.containsPosition(t.sourcePosition)) {
    if (i.type == "remove" && !e.forceWeakRemove) {
      if (!e.aWasUndone) {
        const o = [];
        let a = t.graveyardPosition.clone(), l = t.targetPosition._getTransformedByMergeOperation(t);
        const c = i.targetPosition.getTransformedByOperation(t);
        i.howMany > 1 && (o.push(new ot(i.sourcePosition, i.howMany - 1, c, 0)), a = a._getTransformedByMove(i.sourcePosition, c, i.howMany - 1), l = l._getTransformedByMove(i.sourcePosition, c, i.howMany - 1));
        const u = t.deletionPosition._getCombined(i.sourcePosition, c), d = new ot(a, 1, u, 0), f = d.getMovedRangeStart().path.slice();
        f.push(0);
        const m = new G(d.targetPosition.root, f);
        l = l._getTransformedByMove(a, u, 1);
        const g = new ot(l, t.howMany, m, 0);
        return o.push(d), o.push(g), o;
      }
    } else if (i.howMany == 1)
      return e.bWasUndone ? (i.sourcePosition = t.graveyardPosition.clone(), i.targetPosition = i.targetPosition._getTransformedByMergeOperation(t), [
        i
      ]) : [
        new gn(0)
      ];
  }
  const s = D._createFromPositionAndShift(i.sourcePosition, i.howMany)._getTransformedByMergeOperation(t);
  return i.sourcePosition = s.start, i.howMany = s.end.offset - s.start.offset, i.targetPosition = i.targetPosition._getTransformedByMergeOperation(t), [
    i
  ];
});
nt(ai, An, (i, t) => (i.position = i.position._getTransformedByInsertOperation(t), [
  i
]));
nt(ai, Kt, (i, t) => i.position.isEqual(t.deletionPosition) ? (i.position = t.graveyardPosition.clone(), i.position.stickiness = "toNext", [
  i
]) : (i.position = i.position._getTransformedByMergeOperation(t), [
  i
]));
nt(ai, ot, (i, t) => (i.position = i.position._getTransformedByMoveOperation(t), [
  i
]));
nt(ai, ai, (i, t, e) => {
  if (i.position.isEqual(t.position))
    if (e.aIsStrong)
      i.oldName = t.newName;
    else
      return [
        new gn(0)
      ];
  return [
    i
  ];
});
nt(ai, gt, (i, t) => {
  const e = i.position.path, n = t.splitPosition.getParentPath();
  if (Fn(e, n) == "same" && !t.graveyardPosition) {
    const r = new ai(i.position.getShiftedBy(1), i.oldName, i.newName, 0);
    return [
      i,
      r
    ];
  }
  return i.position = i.position._getTransformedBySplitOperation(t), [
    i
  ];
});
nt(Lr, Lr, (i, t, e) => {
  if (i.root === t.root && i.key === t.key) {
    if (!e.aIsStrong || i.newValue === t.newValue)
      return [
        new gn(0)
      ];
    i.oldValue = t.newValue;
  }
  return [
    i
  ];
});
nt(ur, ur, (i, t) => i.rootName === t.rootName && i.isAdd === t.isAdd ? [
  new gn(0)
] : [
  i
]);
nt(gt, An, (i, t) => (i.splitPosition.hasSameParentAs(t.position) && i.splitPosition.offset < t.position.offset && (i.howMany += t.howMany), i.splitPosition = i.splitPosition._getTransformedByInsertOperation(t), i.insertionPosition = i.insertionPosition._getTransformedByInsertOperation(t), [
  i
]));
nt(gt, Kt, (i, t, e) => {
  if (!i.graveyardPosition && !e.bWasUndone && i.splitPosition.hasSameParentAs(t.sourcePosition)) {
    const n = t.graveyardPosition.path.slice();
    n.push(0);
    const r = new G(t.graveyardPosition.root, n), s = gt.getInsertionPosition(new G(t.graveyardPosition.root, n)), o = new gt(r, 0, s, null, 0);
    return i.splitPosition = i.splitPosition._getTransformedByMergeOperation(t), i.insertionPosition = gt.getInsertionPosition(i.splitPosition), i.graveyardPosition = o.insertionPosition.clone(), i.graveyardPosition.stickiness = "toNext", [
      o,
      i
    ];
  }
  return i.splitPosition.hasSameParentAs(t.deletionPosition) && !i.splitPosition.isAfter(t.deletionPosition) && i.howMany--, i.splitPosition.hasSameParentAs(t.targetPosition) && (i.howMany += t.howMany), i.splitPosition = i.splitPosition._getTransformedByMergeOperation(t), i.insertionPosition = gt.getInsertionPosition(i.splitPosition), i.graveyardPosition && (i.graveyardPosition = i.graveyardPosition._getTransformedByMergeOperation(t)), [
    i
  ];
});
nt(gt, ot, (i, t, e) => {
  const n = D._createFromPositionAndShift(t.sourcePosition, t.howMany);
  if (i.graveyardPosition) {
    const s = n.start.isEqual(i.graveyardPosition) || n.containsPosition(i.graveyardPosition);
    if (!e.bWasUndone && s) {
      const o = i.splitPosition._getTransformedByMoveOperation(t), a = i.graveyardPosition._getTransformedByMoveOperation(t), l = a.path.slice();
      l.push(0);
      const c = new G(a.root, l);
      return [
        new ot(o, i.howMany, c, 0)
      ];
    }
    i.graveyardPosition = i.graveyardPosition._getTransformedByMoveOperation(t);
  }
  const r = i.splitPosition.isEqual(t.targetPosition);
  if (r && (e.baRelation == "insertAtSource" || e.abRelation == "splitBefore"))
    return i.howMany += t.howMany, i.splitPosition = i.splitPosition._getTransformedByDeletion(t.sourcePosition, t.howMany), i.insertionPosition = gt.getInsertionPosition(i.splitPosition), [
      i
    ];
  if (r && e.abRelation && e.abRelation.howMany) {
    const { howMany: s, offset: o } = e.abRelation;
    return i.howMany += s, i.splitPosition = i.splitPosition.getShiftedBy(o), [
      i
    ];
  }
  if (i.splitPosition.hasSameParentAs(t.sourcePosition) && n.containsPosition(i.splitPosition)) {
    const s = t.howMany - (i.splitPosition.offset - t.sourcePosition.offset);
    return i.howMany -= s, i.splitPosition.hasSameParentAs(t.targetPosition) && i.splitPosition.offset < t.targetPosition.offset && (i.howMany += t.howMany), i.splitPosition = t.sourcePosition.clone(), i.insertionPosition = gt.getInsertionPosition(i.splitPosition), [
      i
    ];
  }
  return t.sourcePosition.isEqual(t.targetPosition) || (i.splitPosition.hasSameParentAs(t.sourcePosition) && i.splitPosition.offset <= t.sourcePosition.offset && (i.howMany -= t.howMany), i.splitPosition.hasSameParentAs(t.targetPosition) && i.splitPosition.offset < t.targetPosition.offset && (i.howMany += t.howMany)), i.splitPosition.stickiness = "toNone", i.splitPosition = i.splitPosition._getTransformedByMoveOperation(t), i.splitPosition.stickiness = "toNext", i.graveyardPosition ? i.insertionPosition = i.insertionPosition._getTransformedByMoveOperation(t) : i.insertionPosition = gt.getInsertionPosition(i.splitPosition), [
    i
  ];
});
nt(gt, gt, (i, t, e) => {
  if (i.splitPosition.isEqual(t.splitPosition)) {
    if (!i.graveyardPosition && !t.graveyardPosition)
      return [
        new gn(0)
      ];
    if (i.graveyardPosition && t.graveyardPosition && i.graveyardPosition.isEqual(t.graveyardPosition))
      return [
        new gn(0)
      ];
    if (e.abRelation == "splitBefore")
      return i.howMany = 0, i.graveyardPosition = i.graveyardPosition._getTransformedBySplitOperation(t), [
        i
      ];
  }
  if (i.graveyardPosition && t.graveyardPosition && i.graveyardPosition.isEqual(t.graveyardPosition)) {
    const n = i.splitPosition.root.rootName == "$graveyard", r = t.splitPosition.root.rootName == "$graveyard";
    if (r && !n || !(n && !r) && e.aIsStrong) {
      const l = [];
      return t.howMany && l.push(new ot(t.moveTargetPosition, t.howMany, t.splitPosition, 0)), i.howMany && l.push(new ot(i.splitPosition, i.howMany, i.moveTargetPosition, 0)), l;
    } else
      return [
        new gn(0)
      ];
  }
  if (i.graveyardPosition && (i.graveyardPosition = i.graveyardPosition._getTransformedBySplitOperation(t)), i.splitPosition.isEqual(t.insertionPosition) && e.abRelation == "splitBefore")
    return i.howMany++, [
      i
    ];
  if (t.splitPosition.isEqual(i.insertionPosition) && e.baRelation == "splitBefore") {
    const n = t.insertionPosition.path.slice();
    n.push(0);
    const r = new G(t.insertionPosition.root, n), s = new ot(i.insertionPosition, 1, r, 0);
    return [
      i,
      s
    ];
  }
  return i.splitPosition.hasSameParentAs(t.splitPosition) && i.splitPosition.offset < t.splitPosition.offset && (i.howMany -= t.howMany), i.splitPosition = i.splitPosition._getTransformedBySplitOperation(t), i.insertionPosition = gt.getInsertionPosition(i.splitPosition), [
    i
  ];
});
function Q1(i, t) {
  return i.targetPosition._getTransformedByDeletion(t.sourcePosition, t.howMany) === null;
}
function $o(i, t) {
  const e = [];
  for (let n = 0; n < i.length; n++) {
    const r = i[n], s = new ot(r.start, r.end.offset - r.start.offset, t, 0);
    e.push(s);
    for (let o = n + 1; o < i.length; o++)
      i[o] = i[o]._getTransformedByMove(s.sourcePosition, s.targetPosition, s.howMany)[0];
    t = t._getTransformedByMove(s.sourcePosition, s.targetPosition, s.howMany);
  }
  return e;
}
class ln extends (/* @__PURE__ */ ht(G)) {
  /**
  * Creates a live position.
  *
  * @see module:engine/model/position~Position
  */
  constructor(t, e, n = "toNone") {
    if (super(t, e, n), !this.root.is("rootElement"))
      throw new T("model-liveposition-root-not-rootelement", t);
    j7.call(this);
  }
  /**
  * Unbinds all events previously bound by `LivePosition`. Use it whenever you don't need `LivePosition` instance
  * anymore (i.e. when leaving scope in which it was declared or before re-assigning variable that was
  * referring to it).
  */
  detach() {
    this.stopListening();
  }
  /**
  * Creates a {@link module:engine/model/position~Position position instance}, which is equal to this live position.
  */
  toPosition() {
    return new G(this.root, this.path.slice(), this.stickiness);
  }
  /**
  * Creates a `LivePosition` instance that is equal to position.
  */
  static fromPosition(t, e) {
    return new this(t.root, t.path.slice(), e || t.stickiness);
  }
}
ln.prototype.is = function(i) {
  return i === "livePosition" || i === "model:livePosition" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i == "position" || i === "model:position";
};
function j7() {
  this.listenTo(this.root.document.model, "applyOperation", (i, t) => {
    const e = t[0];
    e.isDocumentOperation && W7.call(this, e);
  }, {
    priority: "low"
  });
}
function W7(i) {
  const t = this.getTransformedByOperation(i);
  if (!this.isEqual(t)) {
    const e = this.toPosition();
    this.path = t.path, this.root = t.root, this.fire("change", e);
  }
}
class po {
  /**
  * Creates a batch instance.
  *
  * @see module:engine/model/model~Model#enqueueChange
  * @see module:engine/model/model~Model#change
  * @param type A set of flags that specify the type of the batch. Batch type can alter how some of the features work
  * when encountering a given `Batch` instance (for example, when a feature listens to applied operations).
  */
  constructor(t = {}) {
    /**
    * An array of operations that compose this batch.
    */
    h(this, "operations");
    /**
    * Whether the batch can be undone through the undo feature.
    */
    h(this, "isUndoable");
    /**
    * Whether the batch includes operations created locally (`true`) or operations created on other, remote editors (`false`).
    */
    h(this, "isLocal");
    /**
    * Whether the batch was created by the undo feature and undoes other operations.
    */
    h(this, "isUndo");
    /**
    * Whether the batch includes operations connected with typing.
    */
    h(this, "isTyping");
    typeof t == "string" && (t = t === "transparent" ? {
      isUndoable: !1
    } : {}, Xe("batch-constructor-deprecated-string-type"));
    const { isUndoable: e = !0, isLocal: n = !0, isUndo: r = !1, isTyping: s = !1 } = t;
    this.operations = [], this.isUndoable = e, this.isLocal = n, this.isUndo = r, this.isTyping = s;
  }
  /**
  * The type of the batch.
  *
  * **This property has been deprecated and is always set to the `'default'` value.**
  *
  * It can be one of the following values:
  * * `'default'` &ndash; All "normal" batches. This is the most commonly used type.
  * * `'transparent'` &ndash; A batch that should be ignored by other features, i.e. an initial batch or collaborative editing
  * changes.
  *
  * @deprecated
  */
  get type() {
    return Xe("batch-type-deprecated"), "default";
  }
  /**
  * Returns the base version of this batch, which is equal to the base version of the first operation in the batch.
  * If there are no operations in the batch or neither operation has the base version set, it returns `null`.
  */
  get baseVersion() {
    for (const t of this.operations)
      if (t.baseVersion !== null)
        return t.baseVersion;
    return null;
  }
  /**
  * Adds an operation to the batch instance.
  *
  * @param operation An operation to add.
  * @returns The added operation.
  */
  addOperation(t) {
    return t.batch = this, this.operations.push(t), t;
  }
}
const El = class El {
  /**
  * Creates a `Differ` instance.
  *
  * @param markerCollection Model's marker collection.
  */
  constructor(t) {
    /**
    * Reference to the model's marker collection.
    */
    h(this, "_markerCollection");
    /**
    * A map that stores changes that happened in a given element.
    *
    * The keys of the map are references to the model elements.
    * The values of the map are arrays with changes that were done on this element.
    */
    h(this, "_changesInElement", /* @__PURE__ */ new Map());
    /**
    * Stores a snapshot for these model nodes that might have changed.
    *
    * This complements {@link ~Differ#_elementChildrenSnapshots `_elementChildrenSnapshots`}.
    *
    * See also {@link ~DifferSnapshot}.
    */
    h(this, "_elementsSnapshots", /* @__PURE__ */ new Map());
    /**
    * For each element or document fragment inside which there was a change, it stores a snapshot of the child nodes list (an array
    * of children snapshots that represent the state in the element / fragment before any change has happened).
    *
    * This complements {@link ~Differ#_elementsSnapshots `_elementsSnapshots`}.
    *
    * See also {@link ~DifferSnapshot}.
    */
    h(this, "_elementChildrenSnapshots", /* @__PURE__ */ new Map());
    /**
    * Keeps the state for a given element, describing how the element was changed so far. It is used to evaluate the `action` property
    * of diff items returned by {@link ~Differ#getChanges}.
    *
    * Possible values, in the order from the lowest priority to the highest priority:
    *
    * * `'refresh'` - element was refreshed,
    * * `'rename'` - element was renamed,
    * * `'move'` - element was moved (or, usually, removed, that is moved to the graveyard).
    *
    * Element that was refreshed, may change its state to `'rename'` if it was later renamed, or to `'move'` if it was removed.
    * But the element cannot change its state from `'move'` to `'rename'`, or from `'rename'` to `'refresh'`.
    *
    * Only already existing elements are registered in `_elementState`. If a new element was inserted as a result of a buffered operation,
    * it is not be registered in `_elementState`.
    */
    h(this, "_elementState", /* @__PURE__ */ new Map());
    /**
    * A map that stores all changed markers.
    *
    * The keys of the map are marker names.
    *
    * The values of the map are objects with the following properties:
    *
    * * `oldMarkerData`,
    * * `newMarkerData`.
    */
    h(this, "_changedMarkers", /* @__PURE__ */ new Map());
    /**
    * A map that stores all roots that have been changed.
    *
    * The keys are the names of the roots while value represents the changes.
    */
    h(this, "_changedRoots", /* @__PURE__ */ new Map());
    /**
    * Stores the number of changes that were processed. Used to order the changes chronologically. It is important
    * when changes are sorted.
    */
    h(this, "_changeCount", 0);
    /**
    * For efficiency purposes, `Differ` stores the change set returned by the differ after {@link #getChanges} call.
    * Cache is reset each time a new operation is buffered. If the cache has not been reset, {@link #getChanges} will
    * return the cached value instead of calculating it again.
    *
    * This property stores those changes that did not take place in graveyard root.
    */
    h(this, "_cachedChanges", null);
    /**
    * For efficiency purposes, `Differ` stores the change set returned by the differ after the {@link #getChanges} call.
    * The cache is reset each time a new operation is buffered. If the cache has not been reset, {@link #getChanges} will
    * return the cached value instead of calculating it again.
    *
    * This property stores all changes evaluated by `Differ`, including those that took place in the graveyard.
    */
    h(this, "_cachedChangesWithGraveyard", null);
    /**
    * Set of model items that were marked to get refreshed in {@link #_refreshItem}.
    */
    h(this, "_refreshedItems", /* @__PURE__ */ new Set());
    this._markerCollection = t;
  }
  /**
  * Informs whether there are any changes buffered in `Differ`.
  */
  get isEmpty() {
    return this._changesInElement.size == 0 && this._changedMarkers.size == 0 && this._changedRoots.size == 0;
  }
  /**
  * Buffers the given operation. **An operation has to be buffered before it is executed.**
  *
  * @param operationToBuffer An operation to buffer.
  */
  bufferOperation(t) {
    const e = t;
    switch (e.type) {
      case "insert": {
        if (this._isInInsertedElement(e.position.parent))
          return;
        this._markInsert(e.position.parent, e.position.offset, e.nodes.maxOffset);
        break;
      }
      case "addAttribute":
      case "removeAttribute":
      case "changeAttribute": {
        for (const n of e.range.getItems({
          shallow: !0
        }))
          this._isInInsertedElement(n.parent) || this._markAttribute(n);
        break;
      }
      case "remove":
      case "move":
      case "reinsert": {
        if (e.sourcePosition.isEqual(e.targetPosition) || e.sourcePosition.getShiftedBy(e.howMany).isEqual(e.targetPosition))
          return;
        const n = this._isInInsertedElement(e.sourcePosition.parent), r = this._isInInsertedElement(e.targetPosition.parent);
        n || this._markRemove(e.sourcePosition.parent, e.sourcePosition.offset, e.howMany), r || this._markInsert(e.targetPosition.parent, e.getMovedRangeStart().offset, e.howMany);
        const s = D._createFromPositionAndShift(e.sourcePosition, e.howMany);
        for (const o of s.getItems({
          shallow: !0
        }))
          this._setElementState(o, "move");
        break;
      }
      case "rename": {
        if (this._isInInsertedElement(e.position.parent))
          return;
        this._markRemove(e.position.parent, e.position.offset, 1), this._markInsert(e.position.parent, e.position.offset, 1);
        const n = D._createFromPositionAndShift(e.position, 1);
        for (const r of this._markerCollection.getMarkersIntersectingRange(n)) {
          const s = r.getData();
          this.bufferMarkerChange(r.name, s, s);
        }
        this._setElementState(e.position.nodeAfter, "rename");
        break;
      }
      case "split": {
        const n = e.splitPosition.parent;
        if (!this._isInInsertedElement(n)) {
          this._markRemove(n, e.splitPosition.offset, e.howMany);
          const r = D._createFromPositionAndShift(e.splitPosition, e.howMany);
          for (const s of r.getItems({
            shallow: !0
          }))
            this._setElementState(s, "move");
        }
        this._isInInsertedElement(e.insertionPosition.parent) || this._markInsert(e.insertionPosition.parent, e.insertionPosition.offset, 1), e.graveyardPosition && (this._markRemove(e.graveyardPosition.parent, e.graveyardPosition.offset, 1), this._setElementState(e.graveyardPosition.nodeAfter, "move"));
        break;
      }
      case "merge": {
        const n = e.sourcePosition.parent;
        this._isInInsertedElement(n.parent) || this._markRemove(n.parent, n.startOffset, 1);
        const r = e.graveyardPosition.parent;
        this._markInsert(r, e.graveyardPosition.offset, 1), this._setElementState(n, "move");
        const s = e.targetPosition.parent;
        if (!this._isInInsertedElement(s)) {
          this._markInsert(s, e.targetPosition.offset, n.maxOffset);
          const o = D._createFromPositionAndShift(e.sourcePosition, e.howMany);
          for (const a of o.getItems({
            shallow: !0
          }))
            this._setElementState(a, "move");
        }
        break;
      }
      case "detachRoot":
      case "addRoot": {
        const n = e.affectedSelectable;
        if (!n._isLoaded || n.isAttached() == e.isAdd)
          return;
        this._bufferRootStateChange(e.rootName, e.isAdd);
        break;
      }
      case "addRootAttribute":
      case "removeRootAttribute":
      case "changeRootAttribute": {
        if (!e.root._isLoaded)
          return;
        const n = e.root.rootName;
        this._bufferRootAttributeChange(n, e.key, e.oldValue, e.newValue);
        break;
      }
    }
    this._cachedChanges = null;
  }
  /**
  * Buffers a marker change.
  *
  * @param markerName The name of the marker that changed.
  * @param oldMarkerData Marker data before the change.
  * @param newMarkerData Marker data after the change.
  */
  bufferMarkerChange(t, e, n) {
    e.range && e.range.root.is("rootElement") && !e.range.root._isLoaded && (e.range = null), n.range && n.range.root.is("rootElement") && !n.range.root._isLoaded && (n.range = null);
    let r = this._changedMarkers.get(t);
    r ? r.newMarkerData = n : (r = {
      newMarkerData: n,
      oldMarkerData: e
    }, this._changedMarkers.set(t, r)), r.oldMarkerData.range == null && n.range == null && this._changedMarkers.delete(t);
  }
  /**
  * Returns all markers that should be removed as a result of buffered changes.
  *
  * @returns Markers to remove. Each array item is an object containing the `name` and `range` properties.
  */
  getMarkersToRemove() {
    const t = [];
    for (const [e, n] of this._changedMarkers)
      n.oldMarkerData.range != null && t.push({
        name: e,
        range: n.oldMarkerData.range
      });
    return t;
  }
  /**
  * Returns all markers which should be added as a result of buffered changes.
  *
  * @returns Markers to add. Each array item is an object containing the `name` and `range` properties.
  */
  getMarkersToAdd() {
    const t = [];
    for (const [e, n] of this._changedMarkers)
      n.newMarkerData.range != null && t.push({
        name: e,
        range: n.newMarkerData.range
      });
    return t;
  }
  /**
  * Returns all markers which changed.
  */
  getChangedMarkers() {
    return Array.from(this._changedMarkers).map(([t, e]) => ({
      name: t,
      data: {
        oldRange: e.oldMarkerData.range,
        newRange: e.newMarkerData.range
      }
    }));
  }
  /**
  * Checks whether some of the buffered changes affect the editor data.
  *
  * Types of changes which affect the editor data:
  *
  * * model structure changes,
  * * attribute changes,
  * * a root is added or detached,
  * * changes of markers which were defined as `affectsData`,
  * * changes of markers' `affectsData` property.
  */
  hasDataChanges() {
    if (this.getChanges().length || this._changedRoots.size > 0)
      return !0;
    for (const { newMarkerData: t, oldMarkerData: e } of this._changedMarkers.values()) {
      if (t.affectsData !== e.affectsData)
        return !0;
      if (t.affectsData) {
        const n = t.range && !e.range, r = !t.range && e.range, s = t.range && e.range && !t.range.isEqual(e.range);
        if (n || r || s)
          return !0;
      }
    }
    return !1;
  }
  /**
  * Calculates the diff between the old model tree state (the state before the first buffered operations since the last {@link #reset}
  * call) and the new model tree state (actual one). It should be called after all buffered operations are executed.
  *
  * The diff set is returned as an array of {@link module:engine/model/differ~DiffItem diff items}, each describing a change done
  * on the model. The items are sorted by the position on which the change happened. If a position
  * {@link module:engine/model/position~Position#isBefore is before} another one, it will be on an earlier index in the diff set.
  *
  * **Note**: Elements inside inserted element will not have a separate diff item, only the top most element change will be reported.
  *
  * Because calculating the diff is a costly operation, the result is cached. If no new operation was buffered since the
  * previous {@link #getChanges} call, the next call will return the cached value.
  *
  * @param options Additional options.
  * @param options.includeChangesInGraveyard If set to `true`, also changes that happened
  * in the graveyard root will be returned. By default, changes in the graveyard root are not returned.
  * @returns Diff between the old and the new model tree state.
  */
  getChanges(t = {}) {
    if (this._cachedChanges)
      return t.includeChangesInGraveyard ? this._cachedChangesWithGraveyard.slice() : this._cachedChanges.slice();
    let e = [];
    for (const n of this._changesInElement.keys()) {
      const r = this._changesInElement.get(n).sort((u, d) => u.offset === d.offset ? u.type != d.type ? u.type == "remove" ? -1 : 1 : 0 : u.offset < d.offset ? -1 : 1), s = this._elementChildrenSnapshots.get(n), o = tv(n.getChildren()), a = U7(s.length, r);
      let l = 0, c = 0;
      for (const u of a)
        if (u === "i") {
          const d = this._getDiffActionForNode(o[l].node, "insert"), f = this._elementsSnapshots.get(o[l].node), m = this._getInsertDiff(n, l, d, o[l], f);
          e.push(m), l++;
        } else if (u === "r") {
          const d = this._getDiffActionForNode(s[c].node, "remove"), f = this._getRemoveDiff(n, l, d, s[c]);
          e.push(f), c++;
        } else if (u === "a") {
          const d = s[c].attributes, f = o[l].attributes;
          let m;
          if (o[l].name == "$text")
            m = new D(G._createAt(n, l), G._createAt(n, l + 1));
          else {
            const p = n.offsetToIndex(l);
            m = new D(G._createAt(n, l), G._createAt(n.getChild(p), 0));
          }
          const g = this._getAttributesDiff(m, d, f);
          e.push(...g), l++, c++;
        } else
          l++, c++;
    }
    e.sort((n, r) => n.position.root != r.position.root ? n.position.root.rootName < r.position.root.rootName ? -1 : 1 : n.position.isEqual(r.position) ? n.changeCount - r.changeCount : n.position.isBefore(r.position) ? -1 : 1);
    for (let n = 1, r = 0; n < e.length; n++) {
      const s = e[r], o = e[n], a = s.type == "remove" && o.type == "remove" && s.name == "$text" && o.name == "$text" && s.position.isEqual(o.position), l = s.type == "insert" && o.type == "insert" && s.name == "$text" && o.name == "$text" && s.position.parent == o.position.parent && s.position.offset + s.length == o.position.offset, c = s.type == "attribute" && o.type == "attribute" && s.position.parent == o.position.parent && s.range.isFlat && o.range.isFlat && s.position.offset + s.length == o.position.offset && s.attributeKey == o.attributeKey && s.attributeOldValue == o.attributeOldValue && s.attributeNewValue == o.attributeNewValue;
      a || l || c ? (s.length++, c && (s.range.end = s.range.end.getShiftedBy(1)), e[n] = null) : r = n;
    }
    e = e.filter((n) => n);
    for (const n of e)
      delete n.changeCount, n.type == "attribute" && (delete n.position, delete n.length);
    return this._changeCount = 0, this._cachedChangesWithGraveyard = e, this._cachedChanges = e.filter(q7), t.includeChangesInGraveyard ? this._cachedChangesWithGraveyard.slice() : this._cachedChanges.slice();
  }
  /**
  * Returns all roots that have changed (either were attached, or detached, or their attributes changed).
  *
  * @returns Diff between the old and the new roots state.
  */
  getChangedRoots() {
    return Array.from(this._changedRoots.values()).map((t) => {
      const e = {
        ...t
      };
      return e.state !== void 0 && delete e.attributes, e;
    });
  }
  /**
  * Returns a set of model items that were marked to get refreshed.
  */
  getRefreshedItems() {
    return new Set(this._refreshedItems);
  }
  /**
  * Resets `Differ`. Removes all buffered changes.
  */
  reset() {
    this._changesInElement.clear(), this._elementChildrenSnapshots.clear(), this._elementsSnapshots.clear(), this._elementState.clear(), this._changedMarkers.clear(), this._changedRoots.clear(), this._refreshedItems.clear(), this._cachedChanges = null;
  }
  /**
  * Marks the given `item` in differ to be "refreshed". It means that the item will be marked as removed and inserted
  * in the differ changes set, so it will be effectively re-converted when the differ changes are handled by a dispatcher.
  *
  * @internal
  * @param item Item to refresh.
  */
  _refreshItem(t) {
    if (this._isInInsertedElement(t.parent))
      return;
    this._markRemove(t.parent, t.startOffset, t.offsetSize), this._markInsert(t.parent, t.startOffset, t.offsetSize), this._refreshedItems.add(t), this._setElementState(t, "refresh");
    const e = D._createOn(t);
    for (const n of this._markerCollection.getMarkersIntersectingRange(e)) {
      const r = n.getData();
      this.bufferMarkerChange(n.name, r, r);
    }
    this._cachedChanges = null;
  }
  /**
  * Buffers all the data related to given root like it was all just added to the editor.
  *
  * Following changes are buffered:
  *
  * * root is attached,
  * * all root content is inserted,
  * * all root attributes are added,
  * * all markers inside the root are added.
  *
  * @internal
  */
  _bufferRootLoad(t) {
    if (t.isAttached()) {
      this._bufferRootStateChange(t.rootName, !0), this._markInsert(t, 0, t.maxOffset);
      for (const e of t.getAttributeKeys())
        this._bufferRootAttributeChange(t.rootName, e, null, t.getAttribute(e));
      for (const e of this._markerCollection)
        if (e.getRange().root == t) {
          const n = e.getData();
          this.bufferMarkerChange(e.name, {
            ...n,
            range: null
          }, n);
        }
    }
  }
  /**
  * Buffers the root state change after the root was attached or detached
  */
  _bufferRootStateChange(t, e) {
    if (!this._changedRoots.has(t)) {
      this._changedRoots.set(t, {
        name: t,
        state: e ? "attached" : "detached"
      });
      return;
    }
    const n = this._changedRoots.get(t);
    n.state !== void 0 ? (delete n.state, n.attributes === void 0 && this._changedRoots.delete(t)) : n.state = e ? "attached" : "detached";
  }
  /**
  * Buffers a root attribute change.
  */
  _bufferRootAttributeChange(t, e, n, r) {
    const s = this._changedRoots.get(t) || {
      name: t
    }, o = s.attributes || {};
    if (o[e]) {
      const a = o[e];
      r === a.oldValue ? delete o[e] : a.newValue = r;
    } else
      o[e] = {
        oldValue: n,
        newValue: r
      };
    Object.entries(o).length === 0 ? (delete s.attributes, s.state === void 0 && this._changedRoots.delete(t)) : (s.attributes = o, this._changedRoots.set(t, s));
  }
  /**
  * Saves and handles an insert change.
  */
  _markInsert(t, e, n) {
    if (t.root.is("rootElement") && !t.root._isLoaded)
      return;
    const r = {
      type: "insert",
      offset: e,
      howMany: n,
      count: this._changeCount++
    };
    this._markChange(t, r);
  }
  /**
  * Saves and handles a remove change.
  */
  _markRemove(t, e, n) {
    if (t.root.is("rootElement") && !t.root._isLoaded)
      return;
    const r = {
      type: "remove",
      offset: e,
      howMany: n,
      count: this._changeCount++
    };
    this._markChange(t, r), this._removeAllNestedChanges(t, e, n);
  }
  /**
  * Saves and handles an attribute change.
  */
  _markAttribute(t) {
    if (t.root.is("rootElement") && !t.root._isLoaded)
      return;
    const e = {
      type: "attribute",
      offset: t.startOffset,
      howMany: t.offsetSize,
      count: this._changeCount++
    };
    this._markChange(t.parent, e);
  }
  /**
  * Saves and handles a model change.
  */
  _markChange(t, e) {
    this._makeSnapshots(t);
    const n = this._getChangesForElement(t);
    this._handleChange(e, n), n.push(e);
    for (let r = 0; r < n.length; r++)
      n[r].howMany < 1 && (n.splice(r, 1), r--);
  }
  /**
  * Tries to set given state for given item.
  *
  * This method does simple validation (it sets the state only for model elements, not for text proxy nodes). It also follows state
  * setting rules, that is, `'refresh'` cannot overwrite `'rename'`, and `'rename'` cannot overwrite `'move'`.
  */
  _setElementState(t, e) {
    if (!t.is("element"))
      return;
    const n = El._statesPriority.indexOf(this._elementState.get(t));
    El._statesPriority.indexOf(e) > n && this._elementState.set(t, e);
  }
  /**
  * Returns a value for {@link ~DifferItemAction `action`} property for diff items returned by {@link ~Differ#getChanges}.
  * This method aims to return `'rename'` or `'refresh'` when it should, and `diffItemType` ("default action") in all other cases.
  *
  * It bases on a few factors:
  *
  * * for text nodes, the method always returns `diffItemType`,
  * * for newly inserted element, the method returns `diffItemType`,
  * * if {@link ~Differ#_elementState element state} was not recorded, the method returns `diffItemType`,
  * * if state was recorded, and it was `'move'` (default action), the method returns `diffItemType`,
  * * finally, if state was `'refresh'` or `'rename'`, the method returns the state value.
  */
  _getDiffActionForNode(t, e) {
    if (!t.is("element") || !this._elementsSnapshots.has(t))
      return e;
    const n = this._elementState.get(t);
    return !n || n == "move" ? e : n;
  }
  /**
  * Gets an array of changes that have already been saved for a given element.
  */
  _getChangesForElement(t) {
    let e;
    return this._changesInElement.has(t) ? e = this._changesInElement.get(t) : (e = [], this._changesInElement.set(t, e)), e;
  }
  /**
  * Creates and saves a snapshot for all children of the given element.
  */
  _makeSnapshots(t) {
    if (this._elementChildrenSnapshots.has(t))
      return;
    const e = tv(t.getChildren());
    this._elementChildrenSnapshots.set(t, e);
    for (const n of e)
      this._elementsSnapshots.set(n.node, n);
  }
  /**
  * For a given newly saved change, compares it with a change already done on the element and modifies the incoming
  * change and/or the old change.
  *
  * @param inc Incoming (new) change.
  * @param changes An array containing all the changes done on that element.
  */
  _handleChange(t, e) {
    t.nodesToHandle = t.howMany;
    for (const n of e) {
      const r = t.offset + t.howMany, s = n.offset + n.howMany;
      if (t.type == "insert" && (n.type == "insert" && (t.offset <= n.offset ? n.offset += t.howMany : t.offset < s && (n.howMany += t.nodesToHandle, t.nodesToHandle = 0)), n.type == "remove" && t.offset < n.offset && (n.offset += t.howMany), n.type == "attribute")) {
        if (t.offset <= n.offset)
          n.offset += t.howMany;
        else if (t.offset < s) {
          const o = n.howMany;
          n.howMany = t.offset - n.offset, e.unshift({
            type: "attribute",
            offset: r,
            howMany: o - n.howMany,
            count: this._changeCount++
          });
        }
      }
      if (t.type == "remove") {
        if (n.type == "insert") {
          if (r <= n.offset)
            n.offset -= t.howMany;
          else if (r <= s)
            if (t.offset < n.offset) {
              const o = r - n.offset;
              n.offset = t.offset, n.howMany -= o, t.nodesToHandle -= o;
            } else
              n.howMany -= t.nodesToHandle, t.nodesToHandle = 0;
          else if (t.offset <= n.offset)
            t.nodesToHandle -= n.howMany, n.howMany = 0;
          else if (t.offset < s) {
            const o = s - t.offset;
            n.howMany -= o, t.nodesToHandle -= o;
          }
        }
        if (n.type == "remove" && (r <= n.offset ? n.offset -= t.howMany : t.offset < n.offset && (t.nodesToHandle += n.howMany, n.howMany = 0)), n.type == "attribute") {
          if (r <= n.offset)
            n.offset -= t.howMany;
          else if (t.offset < n.offset) {
            const o = r - n.offset;
            n.offset = t.offset, n.howMany -= o;
          } else if (t.offset < s)
            if (r <= s) {
              const o = n.howMany;
              n.howMany = t.offset - n.offset;
              const a = o - n.howMany - t.nodesToHandle;
              e.unshift({
                type: "attribute",
                offset: t.offset,
                howMany: a,
                count: this._changeCount++
              });
            } else
              n.howMany -= s - t.offset;
        }
      }
      if (t.type == "attribute") {
        if (n.type == "insert")
          if (t.offset < n.offset && r > n.offset) {
            if (r > s) {
              const o = {
                type: "attribute",
                offset: s,
                howMany: r - s,
                count: this._changeCount++
              };
              this._handleChange(o, e), e.push(o);
            }
            t.nodesToHandle = n.offset - t.offset, t.howMany = t.nodesToHandle;
          } else t.offset >= n.offset && t.offset < s && (r > s ? (t.nodesToHandle = r - s, t.offset = s) : t.nodesToHandle = 0);
        if (n.type == "remove" && t.offset < n.offset && r > n.offset) {
          const o = {
            type: "attribute",
            offset: n.offset,
            howMany: r - n.offset,
            count: this._changeCount++
          };
          this._handleChange(o, e), e.push(o), t.nodesToHandle = n.offset - t.offset, t.howMany = t.nodesToHandle;
        }
        n.type == "attribute" && (t.offset >= n.offset && r <= s ? (t.nodesToHandle = 0, t.howMany = 0, t.offset = 0) : t.offset <= n.offset && r >= s && (n.howMany = 0));
      }
    }
    t.howMany = t.nodesToHandle, delete t.nodesToHandle;
  }
  /**
  * Returns an object with a single insert change description.
  *
  * @param parent The element in which the change happened.
  * @param offset The offset at which change happened.
  * @param action Further specifies what kind of action led to generating this change.
  * @param elementSnapshot Snapshot of the inserted node after changes.
  * @param elementSnapshotBefore Snapshot of the inserted node before changes.
  * @returns The diff item.
  */
  _getInsertDiff(t, e, n, r, s) {
    const o = {
      type: "insert",
      position: G._createAt(t, e),
      name: r.name,
      attributes: new Map(r.attributes),
      length: 1,
      changeCount: this._changeCount++,
      action: n
    };
    return n != "insert" && s && (o.before = {
      name: s.name,
      attributes: new Map(s.attributes)
    }), o;
  }
  /**
  * Returns an object with a single remove change description.
  *
  * @param parent The element in which change happened.
  * @param offset The offset at which change happened.
  * @param action Further specifies what kind of action led to generating this change.
  * @param elementSnapshot The snapshot of the removed node before changes.
  * @returns The diff item.
  */
  _getRemoveDiff(t, e, n, r) {
    return {
      type: "remove",
      action: n,
      position: G._createAt(t, e),
      name: r.name,
      attributes: new Map(r.attributes),
      length: 1,
      changeCount: this._changeCount++
    };
  }
  /**
  * Returns an array of objects where each one is a single attribute change description.
  *
  * @param range The range where the change happened.
  * @param oldAttributes A map, map iterator or compatible object that contains attributes before the change.
  * @param newAttributes A map, map iterator or compatible object that contains attributes after the change.
  * @returns An array containing one or more diff items.
  */
  _getAttributesDiff(t, e, n) {
    const r = [];
    n = new Map(n);
    for (const [s, o] of e) {
      const a = n.has(s) ? n.get(s) : null;
      a !== o && r.push({
        type: "attribute",
        position: t.start,
        range: t.clone(),
        length: 1,
        attributeKey: s,
        attributeOldValue: o,
        attributeNewValue: a,
        changeCount: this._changeCount++
      }), n.delete(s);
    }
    for (const [s, o] of n)
      r.push({
        type: "attribute",
        position: t.start,
        range: t.clone(),
        length: 1,
        attributeKey: s,
        attributeOldValue: null,
        attributeNewValue: o,
        changeCount: this._changeCount++
      });
    return r;
  }
  /**
  * Checks whether given element or any of its parents is an element that is buffered as an inserted element.
  */
  _isInInsertedElement(t) {
    const e = t.parent;
    if (!e)
      return !1;
    const n = this._changesInElement.get(e), r = t.startOffset;
    if (n) {
      for (const s of n)
        if (s.type == "insert" && r >= s.offset && r < s.offset + s.howMany)
          return !0;
    }
    return this._isInInsertedElement(e);
  }
  /**
  * Removes deeply all buffered changes that are registered in elements from range specified by `parent`, `offset`
  * and `howMany`.
  */
  _removeAllNestedChanges(t, e, n) {
    const r = new D(G._createAt(t, e), G._createAt(t, e + n));
    for (const s of r.getItems({
      shallow: !0
    }))
      s.is("element") && (this._changesInElement.delete(s), this._removeAllNestedChanges(s, 0, s.maxOffset));
  }
};
/**
* Priority of the {@link ~Differ#_elementState element states}. States on higher indexes of the array can overwrite states on the lower
* indexes.
*/
h(El, "_statesPriority", [
  void 0,
  "refresh",
  "rename",
  "move"
]);
let um = El;
function ev(i) {
  return {
    node: i,
    name: i.is("$text") ? "$text" : i.name,
    attributes: new Map(i.getAttributes())
  };
}
function tv(i) {
  const t = [];
  for (const e of i)
    if (e.is("$text"))
      for (let n = 0; n < e.data.length; ++n)
        t.push(ev(e));
    else
      t.push(ev(e));
  return t;
}
function U7(i, t) {
  const e = [];
  let n = 0, r = 0;
  for (const s of t) {
    if (s.offset > n) {
      for (let o = 0; o < s.offset - n; o++)
        e.push("e");
      r += s.offset - n;
    }
    if (s.type == "insert") {
      for (let o = 0; o < s.howMany; o++)
        e.push("i");
      n = s.offset + s.howMany;
    } else if (s.type == "remove") {
      for (let o = 0; o < s.howMany; o++)
        e.push("r");
      n = s.offset, r += s.howMany;
    } else {
      if (s.howMany > 1500)
        for (let o = 0; o < s.howMany; o++)
          e.push("a");
      else
        e.push(..."a".repeat(s.howMany).split(""));
      n = s.offset + s.howMany, r += s.howMany;
    }
  }
  if (r < i)
    for (let s = 0; s < i - r - n; s++)
      e.push("e");
  return e;
}
function q7(i) {
  const t = "position" in i && i.position.root.rootName == "$graveyard", e = "range" in i && i.range.root.rootName == "$graveyard";
  return !t && !e;
}
class G7 {
  constructor() {
    /**
    * Operations added to the history.
    */
    h(this, "_operations", []);
    /**
    * Holds an information which {@link module:engine/model/operation/operation~Operation operation} undoes which
    * {@link module:engine/model/operation/operation~Operation operation}.
    *
    * Keys of the map are "undoing operations", that is operations that undone some other operations. For each key, the
    * value is an operation that has been undone by the "undoing operation".
    */
    h(this, "_undoPairs", /* @__PURE__ */ new Map());
    /**
    * Holds all undone operations.
    */
    h(this, "_undoneOperations", /* @__PURE__ */ new Set());
    /**
    * A map that allows retrieving the operations fast based on the given base version.
    */
    h(this, "_baseVersionToOperationIndex", /* @__PURE__ */ new Map());
    /**
    * The history version.
    */
    h(this, "_version", 0);
    /**
    * The gap pairs kept in the <from,to> format.
    *
    * Anytime the `history.version` is set to a version larger than `history.version + 1`,
    * a new <lastHistoryVersion, newHistoryVersion> entry is added to the map.
    */
    h(this, "_gaps", /* @__PURE__ */ new Map());
  }
  /**
  * The version of the last operation in the history.
  *
  * The history version is incremented automatically when a new operation is added to the history.
  * Setting the version manually should be done only in rare circumstances when a gap is planned
  * between history versions. When doing so, a gap will be created and the history will accept adding
  * an operation with base version equal to the new history version.
  */
  get version() {
    return this._version;
  }
  set version(t) {
    this._operations.length && t > this._version + 1 && this._gaps.set(this._version, t), this._version = t;
  }
  /**
  * The last history operation.
  */
  get lastOperation() {
    return this._operations[this._operations.length - 1];
  }
  /**
  * Adds an operation to the history and increments the history version.
  *
  * The operation's base version should be equal to the history version. Otherwise an error is thrown.
  */
  addOperation(t) {
    if (t.baseVersion !== this.version)
      throw new T("model-document-history-addoperation-incorrect-version", this, {
        operation: t,
        historyVersion: this.version
      });
    this._operations.push(t), this._version++, this._baseVersionToOperationIndex.set(t.baseVersion, this._operations.length - 1);
  }
  /**
  * Returns operations from the given range of operation base versions that were added to the history.
  *
  * Note that there may be gaps in operations base versions.
  *
  * @param fromBaseVersion Base version from which operations should be returned (inclusive).
  * @param toBaseVersion Base version up to which operations should be returned (exclusive).
     * @returns History operations for the given range, in chronological order.
  */
  getOperations(t, e = this.version) {
    if (!this._operations.length)
      return [];
    const n = this._operations[0];
    t === void 0 && (t = n.baseVersion);
    let r = e - 1;
    for (const [a, l] of this._gaps)
      t > a && t < l && (t = l), r > a && r < l && (r = a - 1);
    if (r < n.baseVersion || t > this.lastOperation.baseVersion)
      return [];
    let s = this._baseVersionToOperationIndex.get(t);
    s === void 0 && (s = 0);
    let o = this._baseVersionToOperationIndex.get(r);
    return o === void 0 && (o = this._operations.length - 1), this._operations.slice(
      s,
      // The `toIndex` should be included in the returned operations, so add `1`.
      o + 1
    );
  }
  /**
  * Returns operation from the history that bases on given `baseVersion`.
  *
  * @param baseVersion Base version of the operation to get.
  * @returns Operation with given base version or `undefined` if there is no such operation in history.
  */
  getOperation(t) {
    const e = this._baseVersionToOperationIndex.get(t);
    if (e !== void 0)
      return this._operations[e];
  }
  /**
  * Marks in history that one operation is an operation that is undoing the other operation. By marking operation this way,
  * history is keeping more context information about operations, which helps in operational transformation.
  *
  * @param undoneOperation Operation which is undone by `undoingOperation`.
  * @param undoingOperation Operation which undoes `undoneOperation`.
  */
  setOperationAsUndone(t, e) {
    this._undoPairs.set(e, t), this._undoneOperations.add(t);
  }
  /**
  * Checks whether given `operation` is undoing any other operation.
  *
  * @param operation Operation to check.
  * @returns `true` if given `operation` is undoing any other operation, `false` otherwise.
  */
  isUndoingOperation(t) {
    return this._undoPairs.has(t);
  }
  /**
  * Checks whether given `operation` has been undone by any other operation.
  *
  * @param operation Operation to check.
  * @returns `true` if given `operation` has been undone any other operation, `false` otherwise.
  */
  isUndoneOperation(t) {
    return this._undoneOperations.has(t);
  }
  /**
  * For given `undoingOperation`, returns the operation which has been undone by it.
  *
  * @returns Operation that has been undone by given `undoingOperation` or `undefined`
  * if given `undoingOperation` is not undoing any other operation.
  */
  getUndoneOperation(t) {
    return this._undoPairs.get(t);
  }
  /**
  * Resets the history of operations.
  */
  reset() {
    this._version = 0, this._undoPairs = /* @__PURE__ */ new Map(), this._operations = [], this._undoneOperations = /* @__PURE__ */ new Set(), this._gaps = /* @__PURE__ */ new Map(), this._baseVersionToOperationIndex = /* @__PURE__ */ new Map();
  }
}
class zu extends xt {
  /**
  * Creates root element.
  *
  * @param document Document that is an owner of this root.
  * @param name Node name.
  * @param rootName Unique root name used to identify this root element by {@link module:engine/model/document~Document}.
  */
  constructor(e, n, r = "main") {
    super(n);
    /**
    * Unique root name used to identify this root element by {@link module:engine/model/document~Document}.
    */
    h(this, "rootName");
    /**
    * Document that is an owner of this root.
    */
    h(this, "_document");
    /**
    * @internal
    */
    h(this, "_isAttached", !0);
    /**
    * Informs if the root element is loaded (default).
    *
    * @internal
    */
    h(this, "_isLoaded", !0);
    this._document = e, this.rootName = r;
  }
  /**
  * {@link module:engine/model/document~Document Document} that owns this root element.
  */
  get document() {
    return this._document;
  }
  /**
  * Informs if the root element is currently attached to the document, or not.
  *
  * A detached root is equivalent to being removed and cannot contain any children or markers.
  *
  * By default, a newly added root is attached. It can be detached using
  * {@link module:engine/model/writer~Writer#detachRoot `Writer#detachRoot`}. A detached root can be re-attached again using
  * {@link module:engine/model/writer~Writer#addRoot `Writer#addRoot`}.
  */
  isAttached() {
    return this._isAttached;
  }
  /**
  * Converts `RootElement` instance to `string` containing its name.
  *
  * @returns `RootElement` instance converted to `string`.
  */
  toJSON() {
    return this.rootName;
  }
}
zu.prototype.is = function(i, t) {
  return t ? t === this.name && (i === "rootElement" || i === "model:rootElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "element" || i === "model:element") : i === "rootElement" || i === "model:rootElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  i === "element" || i === "model:element" || i === "node" || i === "model:node";
};
const nv = "$graveyard";
class K7 extends (/* @__PURE__ */ ht()) {
  /**
  * Creates an empty document instance with no {@link #roots} (other than
  * the {@link #graveyard graveyard root}).
  */
  constructor(e) {
    super();
    /**
    * The {@link module:engine/model/model~Model model} that the document is a part of.
    */
    h(this, "model");
    /**
    * The document's history.
    */
    h(this, "history");
    /**
    * The selection in this document.
    */
    h(this, "selection");
    /**
    * A list of roots that are owned and managed by this document. Use {@link #createRoot}, {@link #getRoot} and
    * {@link #getRootNames} to manipulate it.
    */
    h(this, "roots");
    /**
    * The model differ object. Its role is to buffer changes done on the model document and then calculate a diff of those changes.
    */
    h(this, "differ");
    /**
    * Defines whether the document is in a read-only mode.
    *
    * The user should not be able to change the data of a document that is read-only.
    *
    * @readonly
    */
    h(this, "isReadOnly");
    /**
    * Post-fixer callbacks registered to the model document.
    */
    h(this, "_postFixers");
    /**
    * A flag that indicates whether the selection has changed since last change block.
    */
    h(this, "_hasSelectionChangedFromTheLastChangeBlock");
    this.model = e, this.history = new G7(), this.selection = new Xi(this), this.roots = new Pn({
      idProperty: "rootName"
    }), this.differ = new um(e.markers), this.isReadOnly = !1, this._postFixers = /* @__PURE__ */ new Set(), this._hasSelectionChangedFromTheLastChangeBlock = !1, this.createRoot("$root", nv), this.listenTo(e, "applyOperation", (n, r) => {
      const s = r[0];
      s.isDocumentOperation && this.differ.bufferOperation(s);
    }, {
      priority: "high"
    }), this.listenTo(e, "applyOperation", (n, r) => {
      const s = r[0];
      s.isDocumentOperation && this.history.addOperation(s);
    }, {
      priority: "low"
    }), this.listenTo(this.selection, "change", () => {
      this._hasSelectionChangedFromTheLastChangeBlock = !0;
    }), this.listenTo(e.markers, "update", (n, r, s, o, a) => {
      const l = {
        ...r.getData(),
        range: o
      };
      this.differ.bufferMarkerChange(r.name, a, l), s === null && r.on("change", (c, u) => {
        const d = r.getData();
        this.differ.bufferMarkerChange(r.name, {
          ...d,
          range: u
        }, d);
      });
    }), this.registerPostFixer((n) => {
      let r = !1;
      for (const s of this.roots)
        !s.isAttached() && !s.isEmpty && (n.remove(n.createRangeIn(s)), r = !0);
      for (const s of this.model.markers)
        s.getRange().root.isAttached() || (n.removeMarker(s), r = !0);
      return r;
    });
  }
  /**
  * The document version. Every applied operation increases the version number. It is used to
  * ensure that operations are applied on a proper document version.
  *
  * This property is equal to {@link module:engine/model/history~History#version `model.Document#history#version`}.
  *
  * If the {@link module:engine/model/operation/operation~Operation#baseVersion base version} does not match the document version,
  * a {@link module:utils/ckeditorerror~CKEditorError model-document-applyoperation-wrong-version} error is thrown.
  */
  get version() {
    return this.history.version;
  }
  set version(e) {
    this.history.version = e;
  }
  /**
  * The graveyard tree root. A document always has a graveyard root that stores removed nodes.
  */
  get graveyard() {
    return this.getRoot(nv);
  }
  /**
  * Creates a new root.
  *
  * **Note:** do not use this method after the editor has been initialized! If you want to dynamically add a root, use
  * {@link module:engine/model/writer~Writer#addRoot `model.Writer#addRoot`} instead.
  *
  * @param elementName The element name. Defaults to `'$root'` which also has some basic schema defined
  * (e.g. `$block` elements are allowed inside the `$root`). Make sure to define a proper schema if you use a different name.
  * @param rootName A unique root name.
  * @returns The created root.
  */
  createRoot(e = "$root", n = "main") {
    if (this.roots.get(n))
      throw new T("model-document-createroot-name-exists", this, {
        name: n
      });
    const r = new zu(this, e, n);
    return this.roots.add(r), r;
  }
  /**
  * Removes all event listeners set by the document instance.
  */
  destroy() {
    this.selection.destroy(), this.stopListening();
  }
  /**
  * Returns a root by its name.
  *
  * Detached roots are returned by this method. This is to be able to operate on the detached root (for example, to be able to create
  * a position inside such a root for undo feature purposes).
  *
  * @param name The root name of the root to return.
  * @returns The root registered under a given name or `null` when there is no root with the given name.
  */
  getRoot(e = "main") {
    return this.roots.get(e);
  }
  /**
  * Returns an array with names of all roots added to the document (except the {@link #graveyard graveyard root}).
  *
  * Detached roots **are not** returned by this method by default. This is to make sure that all features or algorithms that operate
  * on the document data know which roots are still a part of the document and should be processed.
  *
  * @param includeDetached Specified whether detached roots should be returned as well.
  */
  getRootNames(e = !1) {
    return this.getRoots(e).map((n) => n.rootName);
  }
  /**
  * Returns an array with all roots added to the document (except the {@link #graveyard graveyard root}).
  *
  * Detached roots **are not** returned by this method by default. This is to make sure that all features or algorithms that operate
  * on the document data know which roots are still a part of the document and should be processed.
  *
  * @param includeDetached Specified whether detached roots should be returned as well.
  */
  getRoots(e = !1) {
    return this.roots.filter((n) => n != this.graveyard && (e || n.isAttached()) && n._isLoaded);
  }
  /**
  * Used to register a post-fixer callback. A post-fixer mechanism guarantees that the features
  * will operate on a correct model state.
  *
  * An execution of a feature may lead to an incorrect document tree state. The callbacks are used to fix the document tree after
  * it has changed. Post-fixers are fired just after all changes from the outermost change block were applied but
  * before the {@link module:engine/model/document~Document#event:change change event} is fired. If a post-fixer callback made
  * a change, it should return `true`. When this happens, all post-fixers are fired again to check if something else should
  * not be fixed in the new document tree state.
  *
  * As a parameter, a post-fixer callback receives a {@link module:engine/model/writer~Writer writer} instance connected with the
  * executed changes block. Thanks to that, all changes done by the callback will be added to the same
  * {@link module:engine/model/batch~Batch batch} (and undo step) as the original changes. This makes post-fixer changes transparent
  * for the user.
  *
  * An example of a post-fixer is a callback that checks if all the data were removed from the editor. If so, the
  * callback should add an empty paragraph so that the editor is never empty:
  *
  * ```ts
  * document.registerPostFixer( writer => {
  * 	const changes = document.differ.getChanges();
  *
  * 	// Check if the changes lead to an empty root in the editor.
  * 	for ( const entry of changes ) {
  * 		if ( entry.type == 'remove' && entry.position.root.isEmpty ) {
  * 			writer.insertElement( 'paragraph', entry.position.root, 0 );
  *
  * 			// It is fine to return early, even if multiple roots would need to be fixed.
  * 			// All post-fixers will be fired again, so if there are more empty roots, those will be fixed, too.
  * 			return true;
  * 		}
  * 	}
  *
  * 	return false;
  * } );
  * ```
  */
  registerPostFixer(e) {
    this._postFixers.add(e);
  }
  /**
  * A custom `toJSON()` method to solve child-parent circular dependencies.
  *
  * @returns A clone of this object with the document property changed to a string.
  */
  toJSON() {
    const e = J_(this);
    return e.selection = "[engine.model.DocumentSelection]", e.model = "[engine.model.Model]", e;
  }
  /**
  * Check if there were any changes done on document, and if so, call post-fixers,
  * fire `change` event for features and conversion and then reset the differ.
  * Fire `change:data` event when at least one operation or buffered marker changes the data.
  *
  * @internal
  * @fires change
  * @fires change:data
  * @param writer The writer on which post-fixers will be called.
  */
  _handleChangeBlock(e) {
    this._hasDocumentChangedFromTheLastChangeBlock() && (this._callPostFixers(e), this.selection.refresh(), this.differ.hasDataChanges() ? this.fire("change:data", e.batch) : this.fire("change", e.batch), this.selection.refresh(), this.differ.reset()), this._hasSelectionChangedFromTheLastChangeBlock = !1;
  }
  /**
  * Returns whether there is a buffered change or if the selection has changed from the last
  * {@link module:engine/model/model~Model#enqueueChange `enqueueChange()` block}
  * or {@link module:engine/model/model~Model#change `change()` block}.
  *
  * @returns Returns `true` if document has changed from the last `change()` or `enqueueChange()` block.
  */
  _hasDocumentChangedFromTheLastChangeBlock() {
    return !this.differ.isEmpty || this._hasSelectionChangedFromTheLastChangeBlock;
  }
  /**
  * Returns the default root for this document which is either the first root that was added to the document using
  * {@link #createRoot} or the {@link #graveyard graveyard root} if no other roots were created.
  *
  * @returns The default root for this document.
  */
  _getDefaultRoot() {
    const e = this.getRoots();
    return e.length ? e[0] : this.graveyard;
  }
  /**
  * Returns the default range for this selection. The default range is a collapsed range that starts and ends
  * at the beginning of this selection's document {@link #_getDefaultRoot default root}.
  *
  * @internal
  */
  _getDefaultRange() {
    const e = this._getDefaultRoot(), n = this.model, r = n.schema, s = n.createPositionFromPath(e, [
      0
    ]);
    return r.getNearestSelectionRange(s) || n.createRange(s);
  }
  /**
  * Checks whether a given {@link module:engine/model/range~Range range} is a valid range for
  * the {@link #selection document's selection}.
  *
  * @internal
  * @param range A range to check.
  * @returns `true` if `range` is valid, `false` otherwise.
  */
  _validateSelectionRange(e) {
    return e.start.isValid() && e.end.isValid() && iv(e.start) && iv(e.end);
  }
  /**
  * Performs post-fixer loops. Executes post-fixer callbacks as long as none of them has done any changes to the model.
  *
  * @param writer The writer on which post-fixer callbacks will be called.
  */
  _callPostFixers(e) {
    let n = !1;
    do
      for (const r of this._postFixers)
        if (this.selection.refresh(), n = r(e), n)
          break;
    while (n);
  }
}
function iv(i) {
  const t = i.textNode;
  if (t) {
    const e = t.data, n = i.offset - t.startOffset;
    return !Jg(e, n) && !Xg(e, n);
  }
  return !0;
}
class Z7 extends (/* @__PURE__ */ ht()) {
  constructor() {
    super(...arguments);
    /**
    * Stores {@link ~Marker markers} added to the collection.
    */
    h(this, "_markers", /* @__PURE__ */ new Map());
  }
  /**
  * Iterable interface.
  *
  * Iterates over all {@link ~Marker markers} added to the collection.
  */
  [Symbol.iterator]() {
    return this._markers.values();
  }
  /**
  * Checks if given {@link ~Marker marker} or marker name is in the collection.
  *
  * @param markerOrName Name of marker or marker instance to check.
  * @returns `true` if marker is in the collection, `false` otherwise.
  */
  has(e) {
    const n = e instanceof Io ? e.name : e;
    return this._markers.has(n);
  }
  /**
  * Returns {@link ~Marker marker} with given `markerName`.
  *
  * @param markerName Name of marker to get.
  * @returns Marker with given name or `null` if such marker was
  * not added to the collection.
  */
  get(e) {
    return this._markers.get(e) || null;
  }
  /**
  * Creates and adds a {@link ~Marker marker} to the `MarkerCollection` with given name on given
  * {@link module:engine/model/range~Range range}.
  *
  * If `MarkerCollection` already had a marker with given name (or {@link ~Marker marker} was passed), the marker in
  * collection is updated and {@link module:engine/model/markercollection~MarkerCollection#event:update} event is fired
  * but only if there was a change (marker range or {@link module:engine/model/markercollection~Marker#managedUsingOperations}
  * flag has changed.
  *
  * @internal
  * @fires update
  * @param markerOrName Name of marker to set or marker instance to update.
  * @param range Marker range.
  * @param managedUsingOperations Specifies whether the marker is managed using operations.
  * @param affectsData Specifies whether the marker affects the data produced by the data pipeline
  * (is persisted in the editor's data).
  * @returns `Marker` instance which was added or updated.
  */
  _set(e, n, r = !1, s = !1) {
    const o = e instanceof Io ? e.name : e;
    if (o.includes(","))
      throw new T("markercollection-incorrect-marker-name", this);
    const a = this._markers.get(o);
    if (a) {
      const u = a.getData(), d = a.getRange();
      let f = !1;
      return d.isEqual(n) || (a._attachLiveRange(Kn.fromRange(n)), f = !0), r != a.managedUsingOperations && (a._managedUsingOperations = r, f = !0), typeof s == "boolean" && s != a.affectsData && (a._affectsData = s, f = !0), f && this.fire(`update:${o}`, a, d, n, u), a;
    }
    const l = Kn.fromRange(n), c = new Io(o, l, r, s);
    return this._markers.set(o, c), this.fire(`update:${o}`, c, null, n, {
      ...c.getData(),
      range: null
    }), c;
  }
  /**
  * Removes given {@link ~Marker marker} or a marker with given name from the `MarkerCollection`.
  *
  * @internal
  * @fires update
  * @param markerOrName Marker or name of a marker to remove.
  * @returns `true` if marker was found and removed, `false` otherwise.
  */
  _remove(e) {
    const n = e instanceof Io ? e.name : e, r = this._markers.get(n);
    return r ? (this._markers.delete(n), this.fire(`update:${n}`, r, r.getRange(), null, r.getData()), this._destroyMarker(r), !0) : !1;
  }
  /**
  * Fires an {@link module:engine/model/markercollection~MarkerCollection#event:update} event for the given {@link ~Marker marker}
  * but does not change the marker. Useful to force {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher downcast
  * conversion} for the marker.
  *
  * @internal
  * @fires update
  * @param markerOrName Marker or name of a marker to refresh.
  */
  _refresh(e) {
    const n = e instanceof Io ? e.name : e, r = this._markers.get(n);
    if (!r)
      throw new T("markercollection-refresh-marker-not-exists", this);
    const s = r.getRange();
    this.fire(`update:${n}`, r, s, s, r.getData());
  }
  /**
  * Returns iterator that iterates over all markers, which ranges contain given {@link module:engine/model/position~Position position}.
  */
  *getMarkersAtPosition(e) {
    for (const n of this)
      n.getRange().containsPosition(e) && (yield n);
  }
  /**
  * Returns iterator that iterates over all markers, which intersects with given {@link module:engine/model/range~Range range}.
  */
  *getMarkersIntersectingRange(e) {
    for (const n of this)
      n.getRange().getIntersection(e) !== null && (yield n);
  }
  /**
  * Destroys marker collection and all markers inside it.
  */
  destroy() {
    for (const e of this._markers.values())
      this._destroyMarker(e);
    this._markers = null, this.stopListening();
  }
  /**
  * Iterates over all markers that starts with given `prefix`.
  *
  * ```ts
  * const markerFooA = markersCollection._set( 'foo:a', rangeFooA );
  * const markerFooB = markersCollection._set( 'foo:b', rangeFooB );
  * const markerBarA = markersCollection._set( 'bar:a', rangeBarA );
  * const markerFooBarA = markersCollection._set( 'foobar:a', rangeFooBarA );
  * Array.from( markersCollection.getMarkersGroup( 'foo' ) ); // [ markerFooA, markerFooB ]
  * Array.from( markersCollection.getMarkersGroup( 'a' ) ); // []
  * ```
  */
  *getMarkersGroup(e) {
    for (const n of this._markers.values())
      n.name.startsWith(e + ":") && (yield n);
  }
  /**
  * Destroys the marker.
  */
  _destroyMarker(e) {
    e.stopListening(), e._detachLiveRange();
  }
}
class Io extends (/* @__PURE__ */ ht(ys)) {
  /**
  * Creates a marker instance.
  *
  * @param name Marker name.
  * @param liveRange Range marked by the marker.
  * @param managedUsingOperations Specifies whether the marker is managed using operations.
  * @param affectsData Specifies whether the marker affects the data produced by the data pipeline (is persisted in the editor's data).
  */
  constructor(e, n, r, s) {
    super();
    /**
    * Marker's name.
    */
    h(this, "name");
    /**
    * Flag indicates if the marker is managed using operations or not.
    *
    * @internal
    */
    h(this, "_managedUsingOperations");
    /**
    * Specifies whether the marker affects the data produced by the data pipeline
    * (is persisted in the editor's data).
    *
    * @internal
    */
    h(this, "_affectsData");
    /**
    * Range marked by the marker.
    */
    h(this, "_liveRange");
    this.name = e, this._liveRange = this._attachLiveRange(n), this._managedUsingOperations = r, this._affectsData = s;
  }
  /**
  * A value indicating if the marker is managed using operations.
  * See {@link ~Marker marker class description} to learn more about marker types.
  * See {@link module:engine/model/writer~Writer#addMarker}.
  */
  get managedUsingOperations() {
    if (!this._liveRange)
      throw new T("marker-destroyed", this);
    return this._managedUsingOperations;
  }
  /**
  * A value indicating if the marker changes the data.
  */
  get affectsData() {
    if (!this._liveRange)
      throw new T("marker-destroyed", this);
    return this._affectsData;
  }
  /**
  * Returns the marker data (properties defining the marker).
  */
  getData() {
    return {
      range: this.getRange(),
      affectsData: this.affectsData,
      managedUsingOperations: this.managedUsingOperations
    };
  }
  /**
  * Returns current marker start position.
  */
  getStart() {
    if (!this._liveRange)
      throw new T("marker-destroyed", this);
    return this._liveRange.start.clone();
  }
  /**
  * Returns current marker end position.
  */
  getEnd() {
    if (!this._liveRange)
      throw new T("marker-destroyed", this);
    return this._liveRange.end.clone();
  }
  /**
  * Returns a range that represents the current state of the marker.
  *
  * Keep in mind that returned value is a {@link module:engine/model/range~Range Range}, not a
  * {@link module:engine/model/liverange~LiveRange LiveRange}. This means that it is up-to-date and relevant only
  * until next model document change. Do not store values returned by this method. Instead, store {@link ~Marker#name}
  * and get `Marker` instance from {@link module:engine/model/markercollection~MarkerCollection MarkerCollection} every
  * time there is a need to read marker properties. This will guarantee that the marker has not been removed and
  * that it's data is up-to-date.
  */
  getRange() {
    if (!this._liveRange)
      throw new T("marker-destroyed", this);
    return this._liveRange.toRange();
  }
  /**
  * Binds new live range to the marker and detach the old one if is attached.
  *
  * @internal
  * @param liveRange Live range to attach
  * @returns Attached live range.
  */
  _attachLiveRange(e) {
    return this._liveRange && this._detachLiveRange(), e.delegate("change:range").to(this), e.delegate("change:content").to(this), this._liveRange = e, e;
  }
  /**
  * Unbinds and destroys currently attached live range.
  *
  * @internal
  */
  _detachLiveRange() {
    this._liveRange.stopDelegating("change:range", this), this._liveRange.stopDelegating("change:content", this), this._liveRange.detach(), this._liveRange = null;
  }
}
Io.prototype.is = function(i) {
  return i === "marker" || i === "model:marker";
};
class Y7 extends Ei {
  /**
  * Creates an insert operation.
  *
  * @param sourcePosition Position before the first {@link module:engine/model/item~Item model item} to move.
  * @param howMany Offset size of moved range. Moved range will start from `sourcePosition` and end at
  * `sourcePosition` with offset shifted by `howMany`.
  */
  constructor(e, n) {
    super(null);
    /**
    * Position before the first {@link module:engine/model/item~Item model item} to detach.
    */
    h(this, "sourcePosition");
    /**
    * Offset size of moved range.
    */
    h(this, "howMany");
    this.sourcePosition = e.clone(), this.howMany = n;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "detach";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return null;
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.sourcePosition = this.sourcePosition.toJSON(), e;
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    if (this.sourcePosition.root.document)
      throw new T("detach-operation-on-document-node", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    fC(D._createFromPositionAndShift(this.sourcePosition, this.howMany));
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "DetachOperation";
  }
}
class Rr extends ys {
  /**
  * Creates an empty `DocumentFragment`.
  *
  * **Note:** Constructor of this class shouldn't be used directly in the code.
  * Use the {@link module:engine/model/writer~Writer#createDocumentFragment} method instead.
  *
  * @internal
  * @param children Nodes to be contained inside the `DocumentFragment`.
  */
  constructor(e) {
    super();
    /**
    * DocumentFragment static markers map. This is a list of names and {@link module:engine/model/range~Range ranges}
    * which will be set as Markers to {@link module:engine/model/model~Model#markers model markers collection}
    * when DocumentFragment will be inserted to the document.
    */
    h(this, "markers", /* @__PURE__ */ new Map());
    /**
    * List of nodes contained inside the document fragment.
    */
    h(this, "_children", new cl());
    e && this._insertChild(0, e);
  }
  /**
  * Returns an iterator that iterates over all nodes contained inside this document fragment.
  */
  [Symbol.iterator]() {
    return this.getChildren();
  }
  /**
  * Number of this document fragment's children.
  */
  get childCount() {
    return this._children.length;
  }
  /**
  * Sum of {@link module:engine/model/node~Node#offsetSize offset sizes} of all of this document fragment's children.
  */
  get maxOffset() {
    return this._children.maxOffset;
  }
  /**
  * Is `true` if there are no nodes inside this document fragment, `false` otherwise.
  */
  get isEmpty() {
    return this.childCount === 0;
  }
  /**
  * Artificial next sibling. Returns `null`. Added for compatibility reasons.
  */
  get nextSibling() {
    return null;
  }
  /**
  * Artificial previous sibling. Returns `null`. Added for compatibility reasons.
  */
  get previousSibling() {
    return null;
  }
  /**
  * Artificial root of `DocumentFragment`. Returns itself. Added for compatibility reasons.
  */
  get root() {
    return this;
  }
  /**
  * Artificial parent of `DocumentFragment`. Returns `null`. Added for compatibility reasons.
  */
  get parent() {
    return null;
  }
  /**
  * Artificial owner of `DocumentFragment`. Returns `null`. Added for compatibility reasons.
  */
  get document() {
    return null;
  }
  /**
  * Returns `false` as `DocumentFragment` by definition is not attached to a document. Added for compatibility reasons.
  */
  isAttached() {
    return !1;
  }
  /**
  * Returns empty array. Added for compatibility reasons.
  */
  getAncestors() {
    return [];
  }
  /**
  * Gets the child at the given index. Returns `null` if incorrect index was passed.
  *
  * @param index Index in this document fragment.
  * @returns Child node.
  */
  getChild(e) {
    return this._children.getNode(e);
  }
  /**
  * Gets the child at the given offset. Returns `null` if incorrect index was passed.
  *
  * @param offset Offset in this document fragment.
  * @returns Child node.
  */
  getChildAtOffset(e) {
    return this._children.getNodeAtOffset(e);
  }
  /**
  * Returns an iterator that iterates over all of this document fragment's children.
  */
  getChildren() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Returns an index of the given child node. Returns `null` if given node is not a child of this document fragment.
  *
  * @param node Child node to look for.
  * @returns Child node's index.
  */
  getChildIndex(e) {
    return this._children.getNodeIndex(e);
  }
  /**
  * Returns the starting offset of given child. Starting offset is equal to the sum of
  * {@link module:engine/model/node~Node#offsetSize offset sizes} of all node's siblings that are before it. Returns `null` if
  * given node is not a child of this document fragment.
  *
  * @param node Child node to look for.
  * @returns Child node's starting offset.
  */
  getChildStartOffset(e) {
    return this._children.getNodeStartOffset(e);
  }
  /**
  * Returns path to a `DocumentFragment`, which is an empty array. Added for compatibility reasons.
  */
  getPath() {
    return [];
  }
  /**
  * Returns a descendant node by its path relative to this element.
  *
  * ```ts
  * // <this>a<b>c</b></this>
  * this.getNodeByPath( [ 0 ] );     // -> "a"
  * this.getNodeByPath( [ 1 ] );     // -> <b>
  * this.getNodeByPath( [ 1, 0 ] );  // -> "c"
  * ```
  *
  * @param relativePath Path of the node to find, relative to this element.
  */
  getNodeByPath(e) {
    let n = this;
    for (const r of e)
      n = n.getChildAtOffset(r);
    return n;
  }
  /**
  * Converts offset "position" to index "position".
  *
  * Returns index of a node that occupies given offset. If given offset is too low, returns `0`. If given offset is
  * too high, returns index after last child.
  *
  * ```ts
  * const textNode = new Text( 'foo' );
  * const pElement = new Element( 'p' );
  * const docFrag = new DocumentFragment( [ textNode, pElement ] );
  * docFrag.offsetToIndex( -1 ); // Returns 0, because offset is too low.
  * docFrag.offsetToIndex( 0 ); // Returns 0, because offset 0 is taken by `textNode` which is at index 0.
  * docFrag.offsetToIndex( 1 ); // Returns 0, because `textNode` has `offsetSize` equal to 3, so it occupies offset 1 too.
  * docFrag.offsetToIndex( 2 ); // Returns 0.
  * docFrag.offsetToIndex( 3 ); // Returns 1.
  * docFrag.offsetToIndex( 4 ); // Returns 2. There are no nodes at offset 4, so last available index is returned.
  * ```
  *
  * @param offset Offset to look for.
  * @returns Index of a node that occupies given offset.
  */
  offsetToIndex(e) {
    return this._children.offsetToIndex(e);
  }
  /**
  * Converts `DocumentFragment` instance to plain object and returns it.
  * Takes care of converting all of this document fragment's children.
  *
  * @returns `DocumentFragment` instance converted to plain object.
  */
  toJSON() {
    const e = [];
    for (const n of this._children)
      e.push(n.toJSON());
    return e;
  }
  /**
  * Creates a `DocumentFragment` instance from given plain object (i.e. parsed JSON string).
  * Converts `DocumentFragment` children to proper nodes.
  *
  * @param json Plain object to be converted to `DocumentFragment`.
  * @returns `DocumentFragment` instance created using given plain object.
  */
  static fromJSON(e) {
    const n = [];
    for (const r of e)
      r.name ? n.push(xt.fromJSON(r)) : n.push(mt.fromJSON(r));
    return new Rr(n);
  }
  /**
  * {@link #_insertChild Inserts} one or more nodes at the end of this document fragment.
  *
  * @internal
  * @param items Items to be inserted.
  */
  _appendChild(e) {
    this._insertChild(this.childCount, e);
  }
  /**
  * Inserts one or more nodes at the given index and sets {@link module:engine/model/node~Node#parent parent} of these nodes
  * to this document fragment.
  *
  * @internal
  * @param index Index at which nodes should be inserted.
  * @param items Items to be inserted.
  */
  _insertChild(e, n) {
    const r = J7(n);
    for (const s of r)
      s.parent !== null && s._remove(), s.parent = this;
    this._children._insertNodes(e, r);
  }
  /**
  * Removes one or more nodes starting at the given index
  * and sets {@link module:engine/model/node~Node#parent parent} of these nodes to `null`.
  *
  * @internal
  * @param index Index of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @returns Array containing removed nodes.
  */
  _removeChildren(e, n = 1) {
    const r = this._children._removeNodes(e, n);
    for (const s of r)
      s.parent = null;
    return r;
  }
}
Rr.prototype.is = function(i) {
  return i === "documentFragment" || i === "model:documentFragment";
};
function J7(i) {
  return typeof i == "string" ? [
    new mt(i)
  ] : (ki(i) || (i = [
    i
  ]), Array.from(i).map((t) => typeof t == "string" ? new mt(t) : t instanceof Gi ? new mt(t.data, t.getAttributes()) : t));
}
class X7 {
  /**
  * Creates a writer instance.
  *
  * **Note:** It is not recommended to use it directly. Use {@link module:engine/model/model~Model#change `Model#change()`} or
  * {@link module:engine/model/model~Model#enqueueChange `Model#enqueueChange()`} instead.
  *
  * @internal
  */
  constructor(t, e) {
    /**
    * Instance of the model on which this writer operates.
    */
    h(this, "model");
    /**
    * The batch to which this writer will add changes.
    */
    h(this, "batch");
    this.model = t, this.batch = e;
  }
  /**
  * Creates a new {@link module:engine/model/text~Text text node}.
  *
  * ```ts
  * writer.createText( 'foo' );
  * writer.createText( 'foo', { bold: true } );
  * ```
  *
  * @param data Text data.
  * @param attributes Text attributes.
  * @returns {module:engine/model/text~Text} Created text node.
  */
  createText(t, e) {
    return new mt(t, e);
  }
  /**
  * Creates a new {@link module:engine/model/element~Element element}.
  *
  * ```ts
  * writer.createElement( 'paragraph' );
  * writer.createElement( 'paragraph', { alignment: 'center' } );
  * ```
  *
  * @param name Name of the element.
  * @param attributes Elements attributes.
  * @returns Created element.
  */
  createElement(t, e) {
    return new xt(t, e);
  }
  /**
  * Creates a new {@link module:engine/model/documentfragment~DocumentFragment document fragment}.
  *
  * @returns Created document fragment.
  */
  createDocumentFragment() {
    return new Rr();
  }
  /**
  * Creates a copy of the element and returns it. Created element has the same name and attributes as the original element.
  * If clone is deep, the original element's children are also cloned. If not, then empty element is returned.
  *
  * @param element The element to clone.
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any child.
  */
  cloneElement(t, e = !0) {
    return t._clone(e);
  }
  /**
  * Inserts item on given position.
  *
  * ```ts
  * const paragraph = writer.createElement( 'paragraph' );
  * writer.insert( paragraph, position );
  * ```
  *
  * Instead of using position you can use parent and offset:
  *
  * ```ts
  * const text = writer.createText( 'foo' );
  * writer.insert( text, paragraph, 5 );
  * ```
  *
  * You can also use `end` instead of the offset to insert at the end:
  *
  * ```ts
  * const text = writer.createText( 'foo' );
  * writer.insert( text, paragraph, 'end' );
  * ```
  *
  * Or insert before or after another element:
  *
  * ```ts
  * const paragraph = writer.createElement( 'paragraph' );
  * writer.insert( paragraph, anotherParagraph, 'after' );
  * ```
  *
  * These parameters works the same way as {@link #createPositionAt `writer.createPositionAt()`}.
  *
  * Note that if the item already has parent it will be removed from the previous parent.
  *
  * Note that you cannot re-insert a node from a document to a different document or a document fragment. In this case,
  * `model-writer-insert-forbidden-move` is thrown.
  *
  * If you want to move {@link module:engine/model/range~Range range} instead of an
  * {@link module:engine/model/item~Item item} use {@link module:engine/model/writer~Writer#move `Writer#move()`}.
  *
  * **Note:** For a paste-like content insertion mechanism see
  * {@link module:engine/model/model~Model#insertContent `model.insertContent()`}.
  *
  * @param item Item or document fragment to insert.
  * @param offset Offset or one of the flags. Used only when second parameter is a {@link module:engine/model/item~Item model item}.
  */
  insert(t, e, n = 0) {
    if (this._assertWriterUsedCorrectly(), t instanceof mt && t.data == "")
      return;
    const r = G._createAt(e, n);
    if (t.parent)
      if (ov(t.root, r.root)) {
        this.move(D._createOn(t), r);
        return;
      } else {
        if (t.root.document)
          throw new T("model-writer-insert-forbidden-move", this);
        this.remove(t);
      }
    const s = r.root.document ? r.root.document.version : null, o = new An(r, t, s);
    if (t instanceof mt && (o.shouldReceiveAttributes = !0), this.batch.addOperation(o), this.model.applyOperation(o), t instanceof Rr)
      for (const [a, l] of t.markers) {
        const c = G._createAt(l.root, 0), d = {
          range: new D(l.start._getCombined(c, r), l.end._getCombined(c, r)),
          usingOperation: !0,
          affectsData: !0
        };
        this.model.markers.has(a) ? this.updateMarker(a, d) : this.addMarker(a, d);
      }
  }
  insertText(t, e, n, r) {
    e instanceof Rr || e instanceof xt || e instanceof G ? this.insert(this.createText(t), e, n) : this.insert(this.createText(t, e), n, r);
  }
  insertElement(t, e, n, r) {
    e instanceof Rr || e instanceof xt || e instanceof G ? this.insert(this.createElement(t), e, n) : this.insert(this.createElement(t, e), n, r);
  }
  /**
  * Inserts item at the end of the given parent.
  *
  * ```ts
  * const paragraph = writer.createElement( 'paragraph' );
  * writer.append( paragraph, root );
  * ```
  *
  * Note that if the item already has parent it will be removed from the previous parent.
  *
  * If you want to move {@link module:engine/model/range~Range range} instead of an
  * {@link module:engine/model/item~Item item} use {@link module:engine/model/writer~Writer#move `Writer#move()`}.
  *
  * @param item Item or document fragment to insert.
  */
  append(t, e) {
    this.insert(t, e, "end");
  }
  appendText(t, e, n) {
    e instanceof Rr || e instanceof xt ? this.insert(this.createText(t), e, "end") : this.insert(this.createText(t, e), n, "end");
  }
  appendElement(t, e, n) {
    e instanceof Rr || e instanceof xt ? this.insert(this.createElement(t), e, "end") : this.insert(this.createElement(t, e), n, "end");
  }
  /**
  * Sets value of the attribute with given key on a {@link module:engine/model/item~Item model item}
  * or on a {@link module:engine/model/range~Range range}.
  *
  * @param key Attribute key.
  * @param value Attribute new value.
  * @param itemOrRange Model item or range on which the attribute will be set.
  */
  setAttribute(t, e, n) {
    if (this._assertWriterUsedCorrectly(), n instanceof D) {
      const r = n.getMinimalFlatRanges();
      for (const s of r)
        rv(this, t, e, s);
    } else
      sv(this, t, e, n);
  }
  /**
  * Sets values of attributes on a {@link module:engine/model/item~Item model item}
  * or on a {@link module:engine/model/range~Range range}.
  *
  * ```ts
  * writer.setAttributes( {
  * 	bold: true,
  * 	italic: true
  * }, range );
  * ```
  *
  * @param attributes Attributes keys and values.
  * @param itemOrRange Model item or range on which the attributes will be set.
  */
  setAttributes(t, e) {
    for (const [n, r] of gr(t))
      this.setAttribute(n, r, e);
  }
  /**
  * Removes an attribute with given key from a {@link module:engine/model/item~Item model item}
  * or from a {@link module:engine/model/range~Range range}.
  *
  * @param key Attribute key.
  * @param itemOrRange Model item or range from which the attribute will be removed.
  */
  removeAttribute(t, e) {
    if (this._assertWriterUsedCorrectly(), e instanceof D) {
      const n = e.getMinimalFlatRanges();
      for (const r of n)
        rv(this, t, null, r);
    } else
      sv(this, t, null, e);
  }
  /**
  * Removes all attributes from all elements in the range or from the given item.
  *
  * @param itemOrRange Model item or range from which all attributes will be removed.
  */
  clearAttributes(t) {
    this._assertWriterUsedCorrectly();
    const e = (n) => {
      for (const r of n.getAttributeKeys())
        this.removeAttribute(r, n);
    };
    if (!(t instanceof D))
      e(t);
    else
      for (const n of t.getItems())
        e(n);
  }
  /**
  * Moves all items in the source range to the target position.
  *
  * ```ts
  * writer.move( sourceRange, targetPosition );
  * ```
  *
  * Instead of the target position you can use parent and offset or define that range should be moved to the end
  * or before or after chosen item:
  *
  * ```ts
  * // Moves all items in the range to the paragraph at offset 5:
  * writer.move( sourceRange, paragraph, 5 );
  * // Moves all items in the range to the end of a blockquote:
  * writer.move( sourceRange, blockquote, 'end' );
  * // Moves all items in the range to a position after an image:
  * writer.move( sourceRange, image, 'after' );
  * ```
  *
  * These parameters work the same way as {@link #createPositionAt `writer.createPositionAt()`}.
  *
  * Note that items can be moved only within the same tree. It means that you can move items within the same root
  * (element or document fragment) or between {@link module:engine/model/document~Document#roots documents roots},
  * but you can not move items from document fragment to the document or from one detached element to another. Use
  * {@link module:engine/model/writer~Writer#insert} in such cases.
  *
  * @param range Source range.
  * @param offset Offset or one of the flags. Used only when second parameter is a {@link module:engine/model/item~Item model item}.
  */
  move(t, e, n) {
    if (this._assertWriterUsedCorrectly(), !(t instanceof D))
      throw new T("writer-move-invalid-range", this);
    if (!t.isFlat)
      throw new T("writer-move-range-not-flat", this);
    const r = G._createAt(e, n);
    if (r.isEqual(t.start))
      return;
    if (this._addOperationForAffectedMarkers("move", t), !ov(t.root, r.root))
      throw new T("writer-move-different-document", this);
    const s = t.root.document ? t.root.document.version : null, o = new ot(t.start, t.end.offset - t.start.offset, r, s);
    this.batch.addOperation(o), this.model.applyOperation(o);
  }
  /**
  * Removes given model {@link module:engine/model/item~Item item} or {@link module:engine/model/range~Range range}.
  *
  * @param itemOrRange Model item or range to remove.
  */
  remove(t) {
    this._assertWriterUsedCorrectly();
    const n = (t instanceof D ? t : D._createOn(t)).getMinimalFlatRanges().reverse();
    for (const r of n)
      this._addOperationForAffectedMarkers("move", r), Q7(r.start, r.end.offset - r.start.offset, this.batch, this.model);
  }
  /**
  * Merges two siblings at the given position.
  *
  * Node before and after the position have to be an element. Otherwise `writer-merge-no-element-before` or
  * `writer-merge-no-element-after` error will be thrown.
  *
  * @param position Position between merged elements.
  */
  merge(t) {
    this._assertWriterUsedCorrectly();
    const e = t.nodeBefore, n = t.nodeAfter;
    if (this._addOperationForAffectedMarkers("merge", t), !(e instanceof xt))
      throw new T("writer-merge-no-element-before", this);
    if (!(n instanceof xt))
      throw new T("writer-merge-no-element-after", this);
    t.root.document ? this._merge(t) : this._mergeDetached(t);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createPositionFromPath `Model#createPositionFromPath()`}.
  *
  * @param root Root of the position.
  * @param path Position path. See {@link module:engine/model/position~Position#path}.
  * @param stickiness Position stickiness. See {@link module:engine/model/position~PositionStickiness}.
  */
  createPositionFromPath(t, e, n) {
    return this.model.createPositionFromPath(t, e, n);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createPositionAt `Model#createPositionAt()`}.
  *
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/model/item~Item model item}.
  */
  createPositionAt(t, e) {
    return this.model.createPositionAt(t, e);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createPositionAfter `Model#createPositionAfter()`}.
  *
  * @param item Item after which the position should be placed.
  */
  createPositionAfter(t) {
    return this.model.createPositionAfter(t);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createPositionBefore `Model#createPositionBefore()`}.
  *
  * @param item Item after which the position should be placed.
  */
  createPositionBefore(t) {
    return this.model.createPositionBefore(t);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createRange `Model#createRange()`}.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at `start` position.
  */
  createRange(t, e) {
    return this.model.createRange(t, e);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createRangeIn `Model#createRangeIn()`}.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(t) {
    return this.model.createRangeIn(t);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createRangeOn `Model#createRangeOn()`}.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeOn(t) {
    return this.model.createRangeOn(t);
  }
  createSelection(...t) {
    return this.model.createSelection(...t);
  }
  /**
  * Performs merge action in a detached tree.
  *
  * @param position Position between merged elements.
  */
  _mergeDetached(t) {
    const e = t.nodeBefore, n = t.nodeAfter;
    this.move(D._createIn(n), G._createAt(e, "end")), this.remove(n);
  }
  /**
  * Performs merge action in a non-detached tree.
  *
  * @param position Position between merged elements.
  */
  _merge(t) {
    const e = G._createAt(t.nodeBefore, "end"), n = G._createAt(t.nodeAfter, 0), r = t.root.document.graveyard, s = new G(r, [
      0
    ]), o = t.root.document.version, a = new Kt(n, t.nodeAfter.maxOffset, e, s, o);
    this.batch.addOperation(a), this.model.applyOperation(a);
  }
  /**
  * Renames the given element.
  *
  * @param element The element to rename.
  * @param newName New element name.
  */
  rename(t, e) {
    if (this._assertWriterUsedCorrectly(), !(t instanceof xt))
      throw new T("writer-rename-not-element-instance", this);
    const n = t.root.document ? t.root.document.version : null, r = new ai(G._createBefore(t), t.name, e, n);
    this.batch.addOperation(r), this.model.applyOperation(r);
  }
  /**
  * Splits elements starting from the given position and going to the top of the model tree as long as given
  * `limitElement` is reached. When `limitElement` is not defined then only the parent of the given position will be split.
  *
  * The element needs to have a parent. It cannot be a root element nor a document fragment.
  * The `writer-split-element-no-parent` error will be thrown if you try to split an element with no parent.
  *
  * @param position Position of split.
  * @param limitElement Stop splitting when this element will be reached.
  * @returns Split result with properties:
  * * `position` - Position between split elements.
  * * `range` - Range that stars from the end of the first split element and ends at the beginning of the first copy element.
  */
  split(t, e) {
    this._assertWriterUsedCorrectly();
    let n = t.parent;
    if (!n.parent)
      throw new T("writer-split-element-no-parent", this);
    if (e || (e = n.parent), !t.parent.getAncestors({
      includeSelf: !0
    }).includes(e))
      throw new T("writer-split-invalid-limit-element", this);
    let r, s;
    do {
      const o = n.root.document ? n.root.document.version : null, a = n.maxOffset - t.offset, l = gt.getInsertionPosition(t), c = new gt(t, a, l, null, o);
      this.batch.addOperation(c), this.model.applyOperation(c), !r && !s && (r = n, s = t.parent.nextSibling), t = this.createPositionAfter(t.parent), n = t.parent;
    } while (n !== e);
    return {
      position: t,
      range: new D(G._createAt(r, "end"), G._createAt(s, 0))
    };
  }
  /**
  * Wraps the given range with the given element or with a new element (if a string was passed).
  *
  * **Note:** range to wrap should be a "flat range" (see {@link module:engine/model/range~Range#isFlat `Range#isFlat`}).
  * If not, an error will be thrown.
  *
  * @param range Range to wrap.
  * @param elementOrString Element or name of element to wrap the range with.
  */
  wrap(t, e) {
    if (this._assertWriterUsedCorrectly(), !t.isFlat)
      throw new T("writer-wrap-range-not-flat", this);
    const n = e instanceof xt ? e : new xt(e);
    if (n.childCount > 0)
      throw new T("writer-wrap-element-not-empty", this);
    if (n.parent !== null)
      throw new T("writer-wrap-element-attached", this);
    this.insert(n, t.start);
    const r = new D(t.start.getShiftedBy(1), t.end.getShiftedBy(1));
    this.move(r, G._createAt(n, 0));
  }
  /**
  * Unwraps children of the given element – all its children are moved before it and then the element is removed.
  * Throws error if you try to unwrap an element which does not have a parent.
  *
  * @param element Element to unwrap.
  */
  unwrap(t) {
    if (this._assertWriterUsedCorrectly(), t.parent === null)
      throw new T("writer-unwrap-element-no-parent", this);
    this.move(D._createIn(t), this.createPositionAfter(t)), this.remove(t);
  }
  /**
  * Adds a {@link module:engine/model/markercollection~Marker marker}. Marker is a named range, which tracks
  * changes in the document and updates its range automatically, when model tree changes.
  *
  * As the first parameter you can set marker name.
  *
  * The required `options.usingOperation` parameter lets you decide if the marker should be managed by operations or not. See
  * {@link module:engine/model/markercollection~Marker marker class description} to learn about the difference between
  * markers managed by operations and not-managed by operations.
  *
  * The `options.affectsData` parameter, which defaults to `false`, allows you to define if a marker affects the data. It should be
  * `true` when the marker change changes the data returned by the
  * {@link module:core/editor/editor~Editor#getData `editor.getData()`} method.
  * When set to `true` it fires the {@link module:engine/model/document~Document#event:change:data `change:data`} event.
  * When set to `false` it fires the {@link module:engine/model/document~Document#event:change `change`} event.
  *
  * Create marker directly base on marker's name:
  *
  * ```ts
  * addMarker( markerName, { range, usingOperation: false } );
  * ```
  *
  * Create marker using operation:
  *
  * ```ts
  * addMarker( markerName, { range, usingOperation: true } );
  * ```
  *
  * Create marker that affects the editor data:
  *
  * ```ts
  * addMarker( markerName, { range, usingOperation: false, affectsData: true } );
  * ```
  *
  * Note: For efficiency reasons, it's best to create and keep as little markers as possible.
  *
  * @see module:engine/model/markercollection~Marker
  * @param name Name of a marker to create - must be unique.
  * @param options.usingOperation Flag indicating that the marker should be added by MarkerOperation.
  * See {@link module:engine/model/markercollection~Marker#managedUsingOperations}.
  * @param options.range Marker range.
  * @param options.affectsData Flag indicating that the marker changes the editor data.
  * @returns Marker that was set.
  */
  addMarker(t, e) {
    if (this._assertWriterUsedCorrectly(), !e || typeof e.usingOperation != "boolean")
      throw new T("writer-addmarker-no-usingoperation", this);
    const n = e.usingOperation, r = e.range, s = e.affectsData === void 0 ? !1 : e.affectsData;
    if (this.model.markers.has(t))
      throw new T("writer-addmarker-marker-exists", this);
    if (!r)
      throw new T("writer-addmarker-no-range", this);
    return n ? (Fa(this, t, null, r, s), this.model.markers.get(t)) : this.model.markers._set(t, r, n, s);
  }
  /**
  * Adds, updates or refreshes a {@link module:engine/model/markercollection~Marker marker}. Marker is a named range, which tracks
  * changes in the document and updates its range automatically, when model tree changes. Still, it is possible to change the
  * marker's range directly using this method.
  *
  * As the first parameter you can set marker name or instance. If none of them is provided, new marker, with a unique
  * name is created and returned.
  *
  * **Note**: If you want to change the {@link module:engine/view/element~Element view element} of the marker while its data in the model
  * remains the same, use the dedicated {@link module:engine/controller/editingcontroller~EditingController#reconvertMarker} method.
  *
  * The `options.usingOperation` parameter lets you change if the marker should be managed by operations or not. See
  * {@link module:engine/model/markercollection~Marker marker class description} to learn about the difference between
  * markers managed by operations and not-managed by operations. It is possible to change this option for an existing marker.
  *
  * The `options.affectsData` parameter, which defaults to `false`, allows you to define if a marker affects the data. It should be
  * `true` when the marker change changes the data returned by
  * the {@link module:core/editor/editor~Editor#getData `editor.getData()`} method.
  * When set to `true` it fires the {@link module:engine/model/document~Document#event:change:data `change:data`} event.
  * When set to `false` it fires the {@link module:engine/model/document~Document#event:change `change`} event.
  *
  * Update marker directly base on marker's name:
  *
  * ```ts
  * updateMarker( markerName, { range } );
  * ```
  *
  * Update marker using operation:
  *
  * ```ts
  * updateMarker( marker, { range, usingOperation: true } );
  * updateMarker( markerName, { range, usingOperation: true } );
  * ```
  *
  * Change marker's option (start using operations to manage it):
  *
  * ```ts
  * updateMarker( marker, { usingOperation: true } );
  * ```
  *
  * Change marker's option (inform the engine, that the marker does not affect the data anymore):
  *
  * ```ts
  * updateMarker( markerName, { affectsData: false } );
  * ```
  *
  * @see module:engine/model/markercollection~Marker
  * @param markerOrName Name of a marker to update, or a marker instance.
  * @param options If options object is not defined then marker will be refreshed by triggering
  * downcast conversion for this marker with the same data.
  * @param options.range Marker range to update.
  * @param options.usingOperation Flag indicated whether the marker should be added by MarkerOperation.
  * See {@link module:engine/model/markercollection~Marker#managedUsingOperations}.
  * @param options.affectsData Flag indicating that the marker changes the editor data.
  */
  updateMarker(t, e) {
    this._assertWriterUsedCorrectly();
    const n = typeof t == "string" ? t : t.name, r = this.model.markers.get(n);
    if (!r)
      throw new T("writer-updatemarker-marker-not-exists", this);
    if (!e) {
      Xe("writer-updatemarker-reconvert-using-editingcontroller", {
        markerName: n
      }), this.model.markers._refresh(r);
      return;
    }
    const s = typeof e.usingOperation == "boolean", o = typeof e.affectsData == "boolean", a = o ? e.affectsData : r.affectsData;
    if (!s && !e.range && !o)
      throw new T("writer-updatemarker-wrong-options", this);
    const l = r.getRange(), c = e.range ? e.range : l;
    if (s && e.usingOperation !== r.managedUsingOperations) {
      e.usingOperation ? Fa(this, n, null, c, a) : (Fa(this, n, l, null, a), this.model.markers._set(n, c, void 0, a));
      return;
    }
    r.managedUsingOperations ? Fa(this, n, l, c, a) : this.model.markers._set(n, c, void 0, a);
  }
  /**
  * Removes given {@link module:engine/model/markercollection~Marker marker} or marker with given name.
  * The marker is removed accordingly to how it has been created, so if the marker was created using operation,
  * it will be destroyed using operation.
  *
  * @param markerOrName Marker or marker name to remove.
  */
  removeMarker(t) {
    this._assertWriterUsedCorrectly();
    const e = typeof t == "string" ? t : t.name;
    if (!this.model.markers.has(e))
      throw new T("writer-removemarker-no-marker", this);
    const n = this.model.markers.get(e);
    if (!n.managedUsingOperations) {
      this.model.markers._remove(e);
      return;
    }
    const r = n.getRange();
    Fa(this, e, r, null, n.affectsData);
  }
  /**
  * Adds a new root to the document (or re-attaches a {@link #detachRoot detached root}).
  *
  * Throws an error, if trying to add a root that is already added and attached.
  *
  * @param rootName Name of the added root.
  * @param elementName The element name. Defaults to `'$root'` which also has some basic schema defined
  * (e.g. `$block` elements are allowed inside the `$root`). Make sure to define a proper schema if you use a different name.
  * @returns The added root element.
  */
  addRoot(t, e = "$root") {
    this._assertWriterUsedCorrectly();
    const n = this.model.document.getRoot(t);
    if (n && n.isAttached())
      throw new T("writer-addroot-root-exists", this);
    const r = this.model.document, s = new ur(t, e, !0, r, r.version);
    return this.batch.addOperation(s), this.model.applyOperation(s), this.model.document.getRoot(t);
  }
  /**
  * Detaches the root from the document.
  *
  * All content and markers are removed from the root upon detaching. New content and new markers cannot be added to the root, as long
  * as it is detached.
  *
  * A root cannot be fully removed from the document, it can be only detached. A root is permanently removed only after you
  * re-initialize the editor and do not specify the root in the initial data.
  *
  * A detached root can be re-attached using {@link #addRoot}.
  *
  * Throws an error if the root does not exist or the root is already detached.
  *
  * @param rootOrName Name of the detached root.
  */
  detachRoot(t) {
    this._assertWriterUsedCorrectly();
    const e = typeof t == "string" ? this.model.document.getRoot(t) : t;
    if (!e || !e.isAttached())
      throw new T("writer-detachroot-no-root", this);
    for (const s of this.model.markers)
      s.getRange().root === e && this.removeMarker(s);
    for (const s of e.getAttributeKeys())
      this.removeAttribute(s, e);
    this.remove(this.createRangeIn(e));
    const n = this.model.document, r = new ur(e.rootName, e.name, !1, n, n.version);
    this.batch.addOperation(r), this.model.applyOperation(r);
  }
  setSelection(...t) {
    this._assertWriterUsedCorrectly(), this.model.document.selection._setTo(...t);
  }
  /**
  * Moves {@link module:engine/model/documentselection~DocumentSelection#focus} to the specified location.
  *
  * The location can be specified in the same form as
  * {@link #createPositionAt `writer.createPositionAt()`} parameters.
  *
  * @param itemOrPosition
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/model/item~Item model item}.
  */
  setSelectionFocus(t, e) {
    this._assertWriterUsedCorrectly(), this.model.document.selection._setFocus(t, e);
  }
  setSelectionAttribute(t, e) {
    if (this._assertWriterUsedCorrectly(), typeof t == "string")
      this._setSelectionAttribute(t, e);
    else
      for (const [n, r] of gr(t))
        this._setSelectionAttribute(n, r);
  }
  /**
  * Removes attribute(s) with given key(s) from the selection.
  *
  * Remove one attribute:
  *
  * ```ts
  * writer.removeSelectionAttribute( 'italic' );
  * ```
  *
  * Remove multiple attributes:
  *
  * ```ts
  * writer.removeSelectionAttribute( [ 'italic', 'bold' ] );
  * ```
  *
  * @param keyOrIterableOfKeys Key of the attribute to remove or an iterable of attribute keys to remove.
  */
  removeSelectionAttribute(t) {
    if (this._assertWriterUsedCorrectly(), typeof t == "string")
      this._removeSelectionAttribute(t);
    else
      for (const e of t)
        this._removeSelectionAttribute(e);
  }
  /**
  * Temporarily changes the {@link module:engine/model/documentselection~DocumentSelection#isGravityOverridden gravity}
  * of the selection from left to right.
  *
  * The gravity defines from which direction the selection inherits its attributes. If it's the default left gravity,
  * then the selection (after being moved by the user) inherits attributes from its left-hand side.
  * This method allows to temporarily override this behavior by forcing the gravity to the right.
  *
  * For the following model fragment:
  *
  * ```xml
  * <$text bold="true" linkHref="url">bar[]</$text><$text bold="true">biz</$text>
  * ```
  *
  * * Default gravity: selection will have the `bold` and `linkHref` attributes.
  * * Overridden gravity: selection will have `bold` attribute.
  *
  * **Note**: It returns an unique identifier which is required to restore the gravity. It guarantees the symmetry
  * of the process.
  *
  * @returns The unique id which allows restoring the gravity.
  */
  overrideSelectionGravity() {
    return this.model.document.selection._overrideGravity();
  }
  /**
  * Restores {@link ~Writer#overrideSelectionGravity} gravity to default.
  *
  * Restoring the gravity is only possible using the unique identifier returned by
  * {@link ~Writer#overrideSelectionGravity}. Note that the gravity remains overridden as long as won't be restored
  * the same number of times it was overridden.
  *
  * @param uid The unique id returned by {@link ~Writer#overrideSelectionGravity}.
  */
  restoreSelectionGravity(t) {
    this.model.document.selection._restoreGravity(t);
  }
  /**
  * @param key Key of the attribute to remove.
  * @param value Attribute value.
  */
  _setSelectionAttribute(t, e) {
    const n = this.model.document.selection;
    if (n.isCollapsed && n.anchor.parent.isEmpty) {
      const r = Xi._getStoreAttributeKey(t);
      this.setAttribute(r, e, n.anchor.parent);
    }
    n._setAttribute(t, e);
  }
  /**
  * @param key Key of the attribute to remove.
  */
  _removeSelectionAttribute(t) {
    const e = this.model.document.selection;
    if (e.isCollapsed && e.anchor.parent.isEmpty) {
      const n = Xi._getStoreAttributeKey(t);
      this.removeAttribute(n, e.anchor.parent);
    }
    e._removeAttribute(t);
  }
  /**
  * Throws `writer-detached-writer-tries-to-modify-model` error when the writer is used outside of the `change()` block.
  */
  _assertWriterUsedCorrectly() {
    if (this.model._currentWriter !== this)
      throw new T("writer-incorrect-use", this);
  }
  /**
  * For given action `type` and `positionOrRange` where the action happens, this function finds all affected markers
  * and applies a marker operation with the new marker range equal to the current range. Thanks to this, the marker range
  * can be later correctly processed during undo.
  *
  * @param type Writer action type.
  * @param positionOrRange Position or range where the writer action happens.
  */
  _addOperationForAffectedMarkers(t, e) {
    for (const n of this.model.markers) {
      if (!n.managedUsingOperations)
        continue;
      const r = n.getRange();
      let s = !1;
      if (t === "move") {
        const o = e;
        s = o.containsPosition(r.start) || o.start.isEqual(r.start) || o.containsPosition(r.end) || o.end.isEqual(r.end);
      } else {
        const o = e, a = o.nodeBefore, l = o.nodeAfter, c = r.start.parent == a && r.start.isAtEnd, u = r.end.parent == l && r.end.offset == 0, d = r.end.nodeAfter == l, f = r.start.nodeAfter == l;
        s = c || u || d || f;
      }
      s && this.updateMarker(n.name, {
        range: r
      });
    }
  }
}
function rv(i, t, e, n) {
  const r = i.model, s = r.document;
  let o = n.start, a, l, c;
  for (const d of n.getWalker({
    shallow: !0
  }))
    c = d.item.getAttribute(t), a && l != c && (l != e && u(), o = a), a = d.nextPosition, l = c;
  a instanceof G && a != o && l != e && u();
  function u() {
    const d = new D(o, a), f = d.root.document ? s.version : null, m = new en(d, t, l, e, f);
    i.batch.addOperation(m), r.applyOperation(m);
  }
}
function sv(i, t, e, n) {
  const r = i.model, s = r.document, o = n.getAttribute(t);
  let a, l;
  if (o != e) {
    if (n.root === n) {
      const u = n.document ? s.version : null;
      l = new Lr(n, t, o, e, u);
    } else {
      a = new D(G._createBefore(n), i.createPositionAfter(n));
      const u = a.root.document ? s.version : null;
      l = new en(a, t, o, e, u);
    }
    i.batch.addOperation(l), r.applyOperation(l);
  }
}
function Fa(i, t, e, n, r) {
  const s = i.model, o = s.document, a = new Zn(t, e, n, s.markers, !!r, o.version);
  i.batch.addOperation(a), s.applyOperation(a);
}
function Q7(i, t, e, n) {
  let r;
  if (i.root.document) {
    const s = n.document, o = new G(s.graveyard, [
      0
    ]);
    r = new ot(i, t, o, s.version);
  } else
    r = new Y7(i, t);
  e.addOperation(r), n.applyOperation(r);
}
function ov(i, t) {
  return i === t || i instanceof zu && t instanceof zu;
}
function e8(i, t, e = {}) {
  if (t.isCollapsed)
    return;
  const n = t.getFirstRange();
  if (n.root.rootName == "$graveyard")
    return;
  const r = i.schema;
  i.change((s) => {
    if (!e.doNotResetEntireContent && c8(r, t)) {
      l8(s, t);
      return;
    }
    const o = {};
    if (!e.doNotAutoparagraph) {
      const c = t.getSelectedElement();
      c && Object.assign(o, r.getAttributesWithProperty(c, "copyOnReplace", !0));
    }
    const [a, l] = t8(n);
    a.isTouching(l) || s.remove(s.createRange(a, l)), e.leaveUnmerged || (i8(s, a, l), r.removeDisallowedAttributes(a.parent.getChildren(), s)), yC(s, t, a), !e.doNotAutoparagraph && o8(r, a) && wC(s, a, t, o), a.detach(), l.detach();
  });
}
function t8(i) {
  const t = i.root.document.model, e = i.start;
  let n = i.end;
  if (t.hasContent(i, {
    ignoreMarkers: !0
  })) {
    const r = n8(n);
    if (r && n.isTouching(t.createPositionAt(r, 0))) {
      const s = t.createSelection(i);
      t.modifySelection(s, {
        direction: "backward"
      });
      const o = s.getLastPosition(), a = t.createRange(o, n);
      t.hasContent(a, {
        ignoreMarkers: !0
      }) || (n = o);
    }
  }
  return [
    ln.fromPosition(e, "toPrevious"),
    ln.fromPosition(n, "toNext")
  ];
}
function n8(i) {
  const t = i.parent, e = t.root.document.model.schema, n = t.getAncestors({
    parentFirst: !0,
    includeSelf: !0
  });
  for (const r of n) {
    if (e.isLimit(r))
      return null;
    if (e.isBlock(r))
      return r;
  }
}
function i8(i, t, e) {
  const n = i.model;
  if (!lp(i.model.schema, t, e))
    return;
  const [r, s] = s8(t, e);
  !r || !s || (!n.hasContent(r, {
    ignoreMarkers: !0
  }) && n.hasContent(s, {
    ignoreMarkers: !0
  }) ? vC(i, t, e, r.parent) : bC(i, t, e, r.parent));
}
function bC(i, t, e, n) {
  const r = t.parent, s = e.parent;
  if (!(r == n || s == n)) {
    for (t = i.createPositionAfter(r), e = i.createPositionBefore(s), e.isEqual(t) || i.insert(s, t), i.merge(t); e.parent.isEmpty; ) {
      const o = e.parent;
      e = i.createPositionBefore(o), i.remove(o);
    }
    lp(i.model.schema, t, e) && bC(i, t, e, n);
  }
}
function vC(i, t, e, n) {
  const r = t.parent, s = e.parent;
  if (!(r == n || s == n)) {
    for (t = i.createPositionAfter(r), e = i.createPositionBefore(s), e.isEqual(t) || i.insert(r, e); t.parent.isEmpty; ) {
      const o = t.parent;
      t = i.createPositionBefore(o), i.remove(o);
    }
    e = i.createPositionBefore(s), r8(i, e), lp(i.model.schema, t, e) && vC(i, t, e, n);
  }
}
function r8(i, t) {
  const e = t.nodeBefore, n = t.nodeAfter;
  e.name != n.name && i.rename(e, n.name), i.clearAttributes(e), i.setAttributes(Object.fromEntries(n.getAttributes()), e), i.merge(t);
}
function lp(i, t, e) {
  const n = t.parent, r = e.parent;
  return n == r || i.isLimit(n) || i.isLimit(r) ? !1 : a8(t, e, i);
}
function s8(i, t) {
  const e = i.getAncestors(), n = t.getAncestors();
  let r = 0;
  for (; e[r] && e[r] == n[r]; )
    r++;
  return [
    e[r],
    n[r]
  ];
}
function o8(i, t) {
  const e = i.checkChild(t, "$text"), n = i.checkChild(t, "paragraph");
  return !e && n;
}
function a8(i, t, e) {
  const n = new D(i, t);
  for (const r of n.getWalker())
    if (e.isLimit(r.item))
      return !1;
  return !0;
}
function wC(i, t, e, n = {}) {
  const r = i.createElement("paragraph");
  i.model.schema.setAllowedAttributes(r, n, i), i.insert(r, t), yC(i, e, i.createPositionAt(r, 0));
}
function l8(i, t) {
  const e = i.model.schema.getLimitElement(t);
  i.remove(i.createRangeIn(e)), wC(i, i.createPositionAt(e, 0), t);
}
function c8(i, t) {
  const e = i.getLimitElement(t);
  if (!t.containsEntireContent(e))
    return !1;
  const n = t.getFirstRange();
  return n.start.parent == n.end.parent ? !1 : i.checkChild(e, "paragraph");
}
function yC(i, t, e) {
  t instanceof Xi ? i.setSelection(e) : t.setTo(e);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function u8(i, t) {
  return i.change((e) => {
    const n = e.createDocumentFragment(), r = t.getFirstRange();
    if (!r || r.isCollapsed)
      return n;
    const s = r.start.root, o = r.start.getCommonPath(r.end), a = s.getNodeByPath(o);
    let l;
    r.start.parent == r.end.parent ? l = r : l = e.createRange(e.createPositionAt(a, r.start.path[o.length]), e.createPositionAt(a, r.end.path[o.length] + 1));
    const c = l.end.offset - l.start.offset;
    for (const u of l.getItems({
      shallow: !0
    }))
      u.is("$textProxy") ? e.appendText(u.data, u.getAttributes(), n) : e.append(e.cloneElement(u, !0), n);
    if (l != r) {
      const u = r._getTransformedByMove(l.start, e.createPositionAt(n, 0), c)[0], d = e.createRange(e.createPositionAt(n, 0), u.start), f = e.createRange(u.end, e.createPositionAt(n, "end"));
      av(f, e), av(d, e);
    }
    return n;
  });
}
function av(i, t) {
  const e = [];
  Array.from(i.getItems({
    direction: "backward"
  })).map((n) => t.createRangeOn(n)).filter((n) => (n.start.isAfter(i.start) || n.start.isEqual(i.start)) && (n.end.isBefore(i.end) || n.end.isEqual(i.end))).forEach((n) => {
    e.push(n.start.parent), t.remove(n);
  }), e.forEach((n) => {
    let r = n;
    for (; r.parent && r.isEmpty; ) {
      const s = t.createRangeOn(r);
      r = r.parent, t.remove(s);
    }
  });
}
function d8(i, t, e) {
  return i.change((n) => {
    const r = e || i.document.selection;
    r.isCollapsed || i.deleteContent(r, {
      doNotAutoparagraph: !0
    });
    const s = new h8(i, n, r.anchor), o = [];
    let a;
    if (t.is("documentFragment")) {
      if (t.markers.size) {
        const u = [];
        for (const [d, f] of t.markers) {
          const { start: m, end: g } = f, p = m.isEqual(g);
          u.push({
            position: m,
            name: d,
            isCollapsed: p
          }, {
            position: g,
            name: d,
            isCollapsed: p
          });
        }
        u.sort(({ position: d }, { position: f }) => d.isBefore(f) ? 1 : -1);
        for (const { position: d, name: f, isCollapsed: m } of u) {
          let g = null, p = null;
          const b = d.parent === t && d.isAtStart, v = d.parent === t && d.isAtEnd;
          !b && !v ? (g = n.createElement("$marker"), n.insert(g, d)) : m && (p = b ? "start" : "end"), o.push({
            name: f,
            element: g,
            collapsed: p
          });
        }
      }
      a = t.getChildren();
    } else
      a = [
        t
      ];
    s.handleNodes(a);
    let l = s.getSelectionRange();
    if (t.is("documentFragment") && o.length) {
      const u = l ? Kn.fromRange(l) : null, d = {};
      for (let f = o.length - 1; f >= 0; f--) {
        const { name: m, element: g, collapsed: p } = o[f], b = !d[m];
        if (b && (d[m] = []), g) {
          const v = n.createPositionAt(g, "before");
          d[m].push(v), n.remove(g);
        } else {
          const v = s.getAffectedRange();
          if (!v) {
            p && d[m].push(s.position);
            continue;
          }
          p ? d[m].push(v[p]) : d[m].push(b ? v.start : v.end);
        }
      }
      for (const [f, [m, g]] of Object.entries(d))
        m && g && m.root === g.root && m.root.document && !n.model.markers.has(f) && n.addMarker(f, {
          usingOperation: !0,
          affectsData: !0,
          range: new D(m, g)
        });
      u && (l = u.toRange(), u.detach());
    }
    /* istanbul ignore else -- @preserve */
    l && (r instanceof Xi ? n.setSelection(l) : r.setTo(l));
    const c = s.getAffectedRange() || i.createRange(r.anchor);
    return s.destroy(), c;
  });
}
class h8 {
  constructor(t, e, n) {
    /**
    * The model in context of which the insertion should be performed.
    */
    h(this, "model");
    /**
    * Batch to which operations will be added.
    */
    h(this, "writer");
    /**
    * The position at which (or near which) the next node will be inserted.
    */
    h(this, "position");
    /**
    * Elements with which the inserted elements can be merged.
    *
    * ```html
    * <p>x^</p><p>y</p> + <p>z</p> (can merge to <p>x</p>)
    * <p>x</p><p>^y</p> + <p>z</p> (can merge to <p>y</p>)
    * <p>x^y</p> + <p>z</p> (can merge to <p>xy</p> which will be split during the action,
    * 						so both its pieces will be added to this set)
    * ```
    */
    h(this, "canMergeWith");
    /**
    * Schema of the model.
    */
    h(this, "schema");
    /**
    * The temporary DocumentFragment used for grouping multiple nodes for single insert operation.
    */
    h(this, "_documentFragment");
    /**
    * The current position in the temporary DocumentFragment.
    */
    h(this, "_documentFragmentPosition");
    /**
    * The reference to the first inserted node.
    */
    h(this, "_firstNode", null);
    /**
    * The reference to the last inserted node.
    */
    h(this, "_lastNode", null);
    /**
    * The reference to the last auto paragraph node.
    */
    h(this, "_lastAutoParagraph", null);
    /**
    * The array of nodes that should be cleaned of not allowed attributes.
    */
    h(this, "_filterAttributesOf", []);
    /**
    * Beginning of the affected range. See {@link module:engine/model/utils/insertcontent~Insertion#getAffectedRange}.
    */
    h(this, "_affectedStart", null);
    /**
    * End of the affected range. See {@link module:engine/model/utils/insertcontent~Insertion#getAffectedRange}.
    */
    h(this, "_affectedEnd", null);
    h(this, "_nodeToSelect", null);
    this.model = t, this.writer = e, this.position = n, this.canMergeWith = /* @__PURE__ */ new Set([
      this.position.parent
    ]), this.schema = t.schema, this._documentFragment = e.createDocumentFragment(), this._documentFragmentPosition = e.createPositionAt(this._documentFragment, 0);
  }
  /**
  * Handles insertion of a set of nodes.
  *
  * @param nodes Nodes to insert.
  */
  handleNodes(t) {
    for (const e of Array.from(t))
      this._handleNode(e);
    this._insertPartialFragment(), this._lastAutoParagraph && this._updateLastNodeFromAutoParagraph(this._lastAutoParagraph), this._mergeOnRight(), this.schema.removeDisallowedAttributes(this._filterAttributesOf, this.writer), this._filterAttributesOf = [];
  }
  /**
  * Updates the last node after the auto paragraphing.
  *
  * @param node The last auto paragraphing node.
  */
  _updateLastNodeFromAutoParagraph(t) {
    const e = this.writer.createPositionAfter(this._lastNode), n = this.writer.createPositionAfter(t);
    if (n.isAfter(e)) {
      this._lastNode = t;
      /* istanbul ignore if -- @preserve */
      if (this.position.parent != t || !this.position.isAtEnd)
        throw new T("insertcontent-invalid-insertion-position", this);
      this.position = n, this._setAffectedBoundaries(this.position);
    }
  }
  /**
  * Returns range to be selected after insertion.
  * Returns `null` if there is no valid range to select after insertion.
  */
  getSelectionRange() {
    return this._nodeToSelect ? D._createOn(this._nodeToSelect) : this.model.schema.getNearestSelectionRange(this.position);
  }
  /**
  * Returns a range which contains all the performed changes. This is a range that, if removed, would return the model to the state
  * before the insertion. Returns `null` if no changes were done.
  */
  getAffectedRange() {
    return this._affectedStart ? new D(this._affectedStart, this._affectedEnd) : null;
  }
  /**
  * Destroys `Insertion` instance.
  */
  destroy() {
    this._affectedStart && this._affectedStart.detach(), this._affectedEnd && this._affectedEnd.detach();
  }
  /**
  * Handles insertion of a single node.
  */
  _handleNode(t) {
    if (!this._checkAndSplitToAllowedPosition(t)) {
      this.schema.isObject(t) || this._handleDisallowedNode(t);
      return;
    }
    this._appendToFragment(t), this._firstNode || (this._firstNode = t), this._lastNode = t;
  }
  /**
  * Inserts the temporary DocumentFragment into the model.
  */
  _insertPartialFragment() {
    if (this._documentFragment.isEmpty)
      return;
    const t = ln.fromPosition(this.position, "toNext");
    this._setAffectedBoundaries(this.position), this._documentFragment.getChild(0) == this._firstNode && (this.writer.insert(this._firstNode, this.position), this._mergeOnLeft(), this.position = t.toPosition()), this._documentFragment.isEmpty || this.writer.insert(this._documentFragment, this.position), this._documentFragmentPosition = this.writer.createPositionAt(this._documentFragment, 0), this.position = t.toPosition(), t.detach();
  }
  /**
  * @param node The disallowed node which needs to be handled.
  */
  _handleDisallowedNode(t) {
    t.is("element") && this.handleNodes(t.getChildren());
  }
  /**
  * Append a node to the temporary DocumentFragment.
  *
  * @param node The node to insert.
  */
  _appendToFragment(t) {
    /* istanbul ignore if -- @preserve */
    if (!this.schema.checkChild(this.position, t))
      throw new T("insertcontent-wrong-position", this, {
        node: t,
        position: this.position
      });
    this.writer.insert(t, this._documentFragmentPosition), this._documentFragmentPosition = this._documentFragmentPosition.getShiftedBy(t.offsetSize), this.schema.isObject(t) && !this.schema.checkChild(this.position, "$text") ? this._nodeToSelect = t : this._nodeToSelect = null, this._filterAttributesOf.push(t);
  }
  /**
  * Sets `_affectedStart` and `_affectedEnd` to the given `position`. Should be used before a change is done during insertion process to
  * mark the affected range.
  *
  * This method is used before inserting a node or splitting a parent node. `_affectedStart` and `_affectedEnd` are also changed
  * during merging, but the logic there is more complicated so it is left out of this function.
  */
  _setAffectedBoundaries(t) {
    this._affectedStart || (this._affectedStart = ln.fromPosition(t, "toPrevious")), (!this._affectedEnd || this._affectedEnd.isBefore(t)) && (this._affectedEnd && this._affectedEnd.detach(), this._affectedEnd = ln.fromPosition(t, "toNext"));
  }
  /**
  * Merges the previous sibling of the first node if it should be merged.
  *
  * After the content was inserted we may try to merge it with its siblings.
  * This should happen only if the selection was in those elements initially.
  */
  _mergeOnLeft() {
    const t = this._firstNode;
    if (!(t instanceof xt) || !this._canMergeLeft(t))
      return;
    const e = ln._createBefore(t);
    e.stickiness = "toNext";
    const n = ln.fromPosition(this.position, "toNext");
    this._affectedStart.isEqual(e) && (this._affectedStart.detach(), this._affectedStart = ln._createAt(e.nodeBefore, "end", "toPrevious")), this._firstNode === this._lastNode && (this._firstNode = e.nodeBefore, this._lastNode = e.nodeBefore), this.writer.merge(e), e.isEqual(this._affectedEnd) && this._firstNode === this._lastNode && (this._affectedEnd.detach(), this._affectedEnd = ln._createAt(e.nodeBefore, "end", "toNext")), this.position = n.toPosition(), n.detach(), this._filterAttributesOf.push(this.position.parent), e.detach();
  }
  /**
  * Merges the next sibling of the last node if it should be merged.
  *
  * After the content was inserted we may try to merge it with its siblings.
  * This should happen only if the selection was in those elements initially.
  */
  _mergeOnRight() {
    const t = this._lastNode;
    if (!(t instanceof xt) || !this._canMergeRight(t))
      return;
    const e = ln._createAfter(t);
    e.stickiness = "toNext";
    /* istanbul ignore if -- @preserve */
    if (!this.position.isEqual(e))
      throw new T("insertcontent-invalid-insertion-position", this);
    this.position = G._createAt(e.nodeBefore, "end");
    const n = ln.fromPosition(this.position, "toPrevious");
    this._affectedEnd.isEqual(e) && (this._affectedEnd.detach(), this._affectedEnd = ln._createAt(e.nodeBefore, "end", "toNext")), this._firstNode === this._lastNode && (this._firstNode = e.nodeBefore, this._lastNode = e.nodeBefore), this.writer.merge(e), e.getShiftedBy(-1).isEqual(this._affectedStart) && this._firstNode === this._lastNode && (this._affectedStart.detach(), this._affectedStart = ln._createAt(e.nodeBefore, 0, "toPrevious")), this.position = n.toPosition(), n.detach(), this._filterAttributesOf.push(this.position.parent), e.detach();
  }
  /**
  * Checks whether specified node can be merged with previous sibling element.
  *
  * @param node The node which could potentially be merged.
  */
  _canMergeLeft(t) {
    const e = t.previousSibling;
    return e instanceof xt && this.canMergeWith.has(e) && this.model.schema.checkMerge(e, t);
  }
  /**
  * Checks whether specified node can be merged with next sibling element.
  *
  * @param node The node which could potentially be merged.
  */
  _canMergeRight(t) {
    const e = t.nextSibling;
    return e instanceof xt && this.canMergeWith.has(e) && this.model.schema.checkMerge(t, e);
  }
  /**
  * Inserts a paragraph and moves the insertion position into it.
  */
  _insertAutoParagraph() {
    this._insertPartialFragment();
    const t = this.writer.createElement("paragraph");
    this.writer.insert(t, this.position), this._setAffectedBoundaries(this.position), this._lastAutoParagraph = t, this.position = this.writer.createPositionAt(t, 0);
  }
  /**
  * @returns Whether an allowed position was found.
  * `false` is returned if the node isn't allowed at any position up in the tree, `true` if was.
  */
  _checkAndSplitToAllowedPosition(t) {
    const e = this._getAllowedIn(this.position.parent, t);
    if (!e)
      return !1;
    for (e != this.position.parent && this._insertPartialFragment(); e != this.position.parent; )
      if (this.position.isAtStart) {
        const n = this.position.parent;
        this.position = this.writer.createPositionBefore(n), n.isEmpty && n.parent === e && this.writer.remove(n);
      } else if (this.position.isAtEnd)
        this.position = this.writer.createPositionAfter(this.position.parent);
      else {
        const n = this.writer.createPositionAfter(this.position.parent);
        this._setAffectedBoundaries(this.position), this.writer.split(this.position), this.position = n, this.canMergeWith.add(this.position.nodeAfter);
      }
    return this.schema.checkChild(this.position.parent, t) || this._insertAutoParagraph(), !0;
  }
  /**
  * Gets the element in which the given node is allowed. It checks the passed element and all its ancestors.
  *
  * It also verifies if auto-paragraphing could help.
  *
  * @param contextElement The element in which context the node should be checked.
  * @param childNode The node to check.
  */
  _getAllowedIn(t, e) {
    return this.schema.checkChild(t, e) || this.schema.checkChild(t, "paragraph") && this.schema.checkChild("paragraph", e) ? t : this.schema.isLimit(t) ? null : this._getAllowedIn(t.parent, e);
  }
}
function f8(i, t, e, n = {}) {
  if (!i.schema.isObject(t))
    throw new T("insertobject-element-not-an-object", i, {
      object: t
    });
  const r = e || i.document.selection;
  let s = r;
  n.findOptimalPosition && i.schema.isBlock(t) && (s = i.createSelection(i.schema.findOptimalInsertionRange(r, n.findOptimalPosition)));
  const o = Ft(r.getSelectedBlocks()), a = {};
  return o && Object.assign(a, i.schema.getAttributesWithProperty(o, "copyOnReplace", !0)), i.change((l) => {
    s.isCollapsed || i.deleteContent(s, {
      doNotAutoparagraph: !0
    });
    let c = t;
    const u = s.anchor.parent;
    !i.schema.checkChild(u, t) && i.schema.checkChild(u, "paragraph") && i.schema.checkChild("paragraph", t) && (c = l.createElement("paragraph"), l.insert(t, c)), i.schema.setAllowedAttributes(c, a, l);
    const d = i.insertContent(c, s);
    return d.isCollapsed || n.setSelection && m8(l, t, n.setSelection, a), d;
  });
}
function m8(i, t, e, n) {
  const r = i.model;
  if (e == "on") {
    i.setSelection(t, "on");
    return;
  }
  if (e != "after")
    throw new T("insertobject-invalid-place-parameter-value", r);
  let s = t.nextSibling;
  if (r.schema.isInline(t)) {
    i.setSelection(t, "after");
    return;
  }
  !(s && r.schema.checkChild(s, "$text")) && r.schema.checkChild(t.parent, "paragraph") && (s = i.createElement("paragraph"), r.schema.setAllowedAttributes(s, n, i), r.insertContent(s, i.createPositionAfter(t))), s && i.setSelection(s, 0);
}
const g8 = ' ,.?!:;"-()';
function p8(i, t, e = {}) {
  const n = i.schema, r = e.direction != "backward", s = e.unit ? e.unit : "character", o = !!e.treatEmojiAsSingleUnit, a = t.focus, l = new Vr({
    boundaries: y8(a, r),
    singleCharacters: !0,
    direction: r ? "forward" : "backward"
  }), c = {
    walker: l,
    schema: n,
    isForward: r,
    unit: s,
    treatEmojiAsSingleUnit: o
  };
  let u;
  for (; u = l.next(); ) {
    if (u.done)
      return;
    const d = b8(c, u.value);
    if (d) {
      t instanceof Xi ? i.change((f) => {
        f.setSelectionFocus(d);
      }) : t.setFocus(d);
      return;
    }
  }
}
function b8(i, t) {
  const { isForward: e, walker: n, unit: r, schema: s, treatEmojiAsSingleUnit: o } = i, { type: a, item: l, nextPosition: c } = t;
  if (a == "text")
    return i.unit === "word" ? w8(n, e) : v8(n, r, o);
  if (a == (e ? "elementStart" : "elementEnd")) {
    if (s.isSelectable(l))
      return G._createAt(l, e ? "after" : "before");
    if (s.checkChild(c, "$text"))
      return c;
  } else {
    if (s.isLimit(l)) {
      n.skip(() => !0);
      return;
    }
    if (s.checkChild(c, "$text"))
      return c;
  }
}
function v8(i, t, e) {
  const n = i.position.textNode;
  if (n) {
    const r = n.data;
    let s = i.position.offset - n.startOffset;
    for (; Jg(r, s) || t == "character" && Xg(r, s) || e && O2(r, s); )
      i.next(), s = i.position.offset - n.startOffset;
  }
  return i.position;
}
function w8(i, t) {
  let e = i.position.textNode;
  for (e || (e = t ? i.position.nodeAfter : i.position.nodeBefore); e && e.is("$text"); ) {
    const n = i.position.offset - e.startOffset;
    if (C8(e, n, t))
      e = t ? i.position.nodeAfter : i.position.nodeBefore;
    else {
      if (_8(e.data, n, t))
        break;
      i.next();
    }
  }
  return i.position;
}
function y8(i, t) {
  const e = i.root, n = G._createAt(e, t ? "end" : 0);
  return t ? new D(i, n) : new D(n, i);
}
function _8(i, t, e) {
  const n = t + (e ? 0 : -1);
  return g8.includes(i.charAt(n));
}
function C8(i, t, e) {
  return t === (e ? i.offsetSize : 0);
}
let k8 = class extends (/* @__PURE__ */ st()) {
  // @if CK_DEBUG_ENGINE // private _operationLogs: Array<string>;
  // @if CK_DEBUG_ENGINE // private _appliedOperations: Array<Operation>;
  constructor() {
    super();
    /**
    * Model's marker collection.
    */
    h(this, "markers");
    /**
    * Model's document.
    */
    h(this, "document");
    /**
    * Model's schema.
    */
    h(this, "schema");
    /**
    * All callbacks added by {@link module:engine/model/model~Model#change} or
    * {@link module:engine/model/model~Model#enqueueChange} methods waiting to be executed.
    */
    h(this, "_pendingChanges");
    /**
    * The last created and currently used writer instance.
    */
    h(this, "_currentWriter");
    this.markers = new Z7(), this.document = new K7(this), this.schema = new h7(), this._pendingChanges = [], this._currentWriter = null, [
      "deleteContent",
      "modifySelection",
      "getSelectedContent",
      "applyOperation"
    ].forEach((e) => this.decorate(e)), this.on("applyOperation", (e, n) => {
      n[0]._validate();
    }, {
      priority: "highest"
    }), this.schema.register("$root", {
      isLimit: !0
    }), this.schema.register("$container", {
      allowIn: [
        "$root",
        "$container"
      ]
    }), this.schema.register("$block", {
      allowIn: [
        "$root",
        "$container"
      ],
      isBlock: !0
    }), this.schema.register("$blockObject", {
      allowWhere: "$block",
      isBlock: !0,
      isObject: !0
    }), this.schema.register("$inlineObject", {
      allowWhere: "$text",
      allowAttributesOf: "$text",
      isInline: !0,
      isObject: !0
    }), this.schema.register("$text", {
      allowIn: "$block",
      isInline: !0,
      isContent: !0
    }), this.schema.register("$clipboardHolder", {
      allowContentOf: "$root",
      allowChildren: "$text",
      isLimit: !0
    }), this.schema.register("$documentFragment", {
      allowContentOf: "$root",
      allowChildren: "$text",
      isLimit: !0
    }), this.schema.register("$marker"), this.schema.addChildCheck(() => !0, "$marker"), i7(this), this.document.registerPostFixer(sC), this.on("insertContent", (e, [n, r]) => {
      e.return = d8(this, n, r);
    }), this.on("insertObject", (e, [n, r, s]) => {
      e.return = f8(this, n, r, s);
    }), this.on("canEditAt", (e) => {
      const n = !this.document.isReadOnly;
      e.return = n, n || e.stop();
    });
  }
  /**
  * The `change()` method is the primary way of changing the model. You should use it to modify all document nodes
  * (including detached nodes – i.e. nodes not added to the {@link module:engine/model/model~Model#document model document}),
  * the {@link module:engine/model/document~Document#selection document's selection}, and
  * {@link module:engine/model/model~Model#markers model markers}.
  *
  * ```ts
  * model.change( writer => {
  * 	writer.insertText( 'foo', paragraph, 'end' );
  * } );
  * ```
  *
  * All changes inside the change block use the same {@link module:engine/model/batch~Batch} so they are combined
  * into a single undo step.
  *
  * ```ts
  * model.change( writer => {
  * 	writer.insertText( 'foo', paragraph, 'end' ); // foo.
  *
  * 	model.change( writer => {
  * 		writer.insertText( 'bar', paragraph, 'end' ); // foobar.
  * 	} );
  *
  * 	writer.insertText( 'bom', paragraph, 'end' ); // foobarbom.
  * } );
  * ```
  *
  * The callback of the `change()` block is executed synchronously.
  *
  * You can also return a value from the change block.
  *
  * ```ts
  * const img = model.change( writer => {
  * 	return writer.createElement( 'img' );
  * } );
  * ```
  *
  * @see #enqueueChange
  * @typeParam TReturn The return type of the provided callback.
  * @param callback Callback function which may modify the model.
  */
  change(e) {
    try {
      return this._pendingChanges.length === 0 ? (this._pendingChanges.push({
        batch: new po(),
        callback: e
      }), this._runPendingChanges()[0]) : e(this._currentWriter);
    } catch (n) {
      /* istanbul ignore next -- @preserve */
      T.rethrowUnexpectedError(n, this);
    }
  }
  enqueueChange(e, n) {
    try {
      e ? typeof e == "function" ? (n = e, e = new po()) : e instanceof po || (e = new po(e)) : e = new po(), this._pendingChanges.push({
        batch: e,
        callback: n
      }), this._pendingChanges.length == 1 && this._runPendingChanges();
    } catch (r) {
      /* istanbul ignore next -- @preserve */
      T.rethrowUnexpectedError(r, this);
    }
  }
  /**
  * {@link module:utils/observablemixin~Observable#decorate Decorated} function for applying
  * {@link module:engine/model/operation/operation~Operation operations} to the model.
  *
  * This is a low-level way of changing the model. It is exposed for very specific use cases (like the undo feature).
  * Normally, to modify the model, you will want to use {@link module:engine/model/writer~Writer `Writer`}.
  * See also {@glink framework/architecture/editing-engine#changing-the-model Changing the model} section
  * of the {@glink framework/architecture/editing-engine Editing architecture} guide.
  *
  * @param operation The operation to apply.
  */
  applyOperation(e) {
    e._execute();
  }
  // @if CK_DEBUG_ENGINE // public getAppliedOperation(): string {
  // @if CK_DEBUG_ENGINE // 	if ( !this._appliedOperations ) {
  // @if CK_DEBUG_ENGINE // 		return '';
  // @if CK_DEBUG_ENGINE // 	}
  // @if CK_DEBUG_ENGINE // 	return this._appliedOperations.map( operation => JSON.stringify( operation ) ).join( '-------' );
  // @if CK_DEBUG_ENGINE // }
  // @if CK_DEBUG_ENGINE // public createReplayer( stringifiedOperations: string ): typeof OperationReplayer {
  // @if CK_DEBUG_ENGINE // 	return new OperationReplayer( this, '-------', stringifiedOperations );
  // @if CK_DEBUG_ENGINE // }
  /**
  * Inserts content at the position in the editor specified by the selection, as one would expect the paste
  * functionality to work.
  *
  * **Note**: If you want to insert an {@glink framework/deep-dive/schema#object-elements object element}
  * (e.g. a {@link module:widget/utils~toWidget widget}), see {@link #insertObject} instead.
  *
  * This is a high-level method. It takes the {@link #schema schema} into consideration when inserting
  * the content, clears the given selection's content before inserting nodes and moves the selection
  * to its target position at the end of the process.
  * It can split elements, merge them, wrap bare text nodes with paragraphs, etc. &ndash; just like the
  * pasting feature should do.
  *
  * For lower-level methods see {@link module:engine/model/writer~Writer `Writer`}.
  *
  * This method, unlike {@link module:engine/model/writer~Writer `Writer`}'s methods, does not have to be used
  * inside a {@link #change `change()` block}.
  *
  * # Conversion and schema
  *
  * Inserting elements and text nodes into the model is not enough to make CKEditor 5 render that content
  * to the user. CKEditor 5 implements a model-view-controller architecture and what `model.insertContent()` does
  * is only adding nodes to the model. Additionally, you need to define
  * {@glink framework/architecture/editing-engine#conversion converters} between the model and view
  * and define those nodes in the {@glink framework/architecture/editing-engine#schema schema}.
  *
  * So, while this method may seem similar to CKEditor 4 `editor.insertHtml()` (in fact, both methods
  * are used for paste-like content insertion), the CKEditor 5 method cannot be use to insert arbitrary HTML
  * unless converters are defined for all elements and attributes in that HTML.
  *
  * # Examples
  *
  * Using `insertContent()` with a manually created model structure:
  *
  * ```ts
  * // Let's create a document fragment containing such content as:
  * //
  * // <paragraph>foo</paragraph>
  * // <blockQuote>
  * //    <paragraph>bar</paragraph>
  * // </blockQuote>
  * const docFrag = editor.model.change( writer => {
  * 	const p1 = writer.createElement( 'paragraph' );
  * 	const p2 = writer.createElement( 'paragraph' );
  * 	const blockQuote = writer.createElement( 'blockQuote' );
  * 	const docFrag = writer.createDocumentFragment();
  *
  * 	writer.append( p1, docFrag );
  * 	writer.append( blockQuote, docFrag );
  * 	writer.append( p2, blockQuote );
  * 	writer.insertText( 'foo', p1 );
  * 	writer.insertText( 'bar', p2 );
  *
  * 	return docFrag;
  * } );
  *
  * // insertContent() does not have to be used in a change() block. It can, though,
  * // so this code could be moved to the callback defined above.
  * editor.model.insertContent( docFrag );
  * ```
  *
  * Using `insertContent()` with an HTML string converted to a model document fragment (similar to the pasting mechanism):
  *
  * ```ts
  * // You can create your own HtmlDataProcessor instance or use editor.data.processor
  * // if you have not overridden the default one (which is the HtmlDataProcessor instance).
  * const htmlDP = new HtmlDataProcessor( viewDocument );
  *
  * // Convert an HTML string to a view document fragment:
  * const viewFragment = htmlDP.toView( htmlString );
  *
  * // Convert the view document fragment to a model document fragment
  * // in the context of $root. This conversion takes the schema into
  * // account so if, for example, the view document fragment contained a bare text node,
  * // this text node cannot be a child of $root, so it will be automatically
  * // wrapped with a <paragraph>. You can define the context yourself (in the second parameter),
  * // and e.g. convert the content like it would happen in a <paragraph>.
  * // Note: The clipboard feature uses a custom context called $clipboardHolder
  * // which has a loosened schema.
  * const modelFragment = editor.data.toModel( viewFragment );
  *
  * editor.model.insertContent( modelFragment );
  * ```
  *
  * By default this method will use the document selection but it can also be used with a position, range or selection instance.
  *
  * ```ts
  * // Insert text at the current document selection position.
  * editor.model.change( writer => {
  * 	editor.model.insertContent( writer.createText( 'x' ) );
  * } );
  *
  * // Insert text at a given position - the document selection will not be modified.
  * editor.model.change( writer => {
  * 	editor.model.insertContent( writer.createText( 'x' ), doc.getRoot(), 2 );
  *
  * 	// Which is a shorthand for:
  * 	editor.model.insertContent( writer.createText( 'x' ), writer.createPositionAt( doc.getRoot(), 2 ) );
  * } );
  * ```
  *
  * If you want the document selection to be moved to the inserted content, use the
  * {@link module:engine/model/writer~Writer#setSelection `setSelection()`} method of the writer after inserting
  * the content:
  *
  * ```ts
  * editor.model.change( writer => {
  * 	const paragraph = writer.createElement( 'paragraph' );
  *
  * 	// Insert an empty paragraph at the beginning of the root.
  * 	editor.model.insertContent( paragraph, writer.createPositionAt( editor.model.document.getRoot(), 0 ) );
  *
  * 	// Move the document selection to the inserted paragraph.
  * 	writer.setSelection( paragraph, 'in' );
  * } );
  * ```
  *
  * If an instance of the {@link module:engine/model/selection~Selection model selection} is passed as `selectable`,
  * the new content will be inserted at the passed selection (instead of document selection):
  *
  * ```ts
  * editor.model.change( writer => {
  * 	// Create a selection in a paragraph that will be used as a place of insertion.
  * 	const selection = writer.createSelection( paragraph, 'in' );
  *
  * 	// Insert the new text at the created selection.
  * 	editor.model.insertContent( writer.createText( 'x' ), selection );
  *
  * 	// insertContent() modifies the passed selection instance so it can be used to set the document selection.
  * 	// Note: This is not necessary when you passed the document selection to insertContent().
  * 	writer.setSelection( selection );
  * } );
  * ```
  *
  * @fires insertContent
  * @param content The content to insert.
  * @param selectable The selection into which the content should be inserted.
  * If not provided the current model document selection will be used.
  * @param placeOrOffset To be used when a model item was passed as `selectable`.
  * This param defines a position in relation to that item.
  * at the insertion position.
  */
  insertContent(e, n, r, ...s) {
    const o = qh(n, r);
    return this.fire("insertContent", [
      e,
      o,
      r,
      ...s
    ]);
  }
  /**
  * Inserts an {@glink framework/deep-dive/schema#object-elements object element} at a specific position in the editor content.
  *
  * This is a high-level API:
  * * It takes the {@link #schema schema} into consideration,
  * * It clears the content of passed `selectable` before inserting,
  * * It can move the selection at the end of the process,
  * * It will copy the selected block's attributes to preserve them upon insertion,
  * * It can split elements or wrap inline objects with paragraphs if they are not allowed in target position,
  * * etc.
  *
  * # Notes
  *
  * * If you want to insert a non-object content, see {@link #insertContent} instead.
  * * For lower-level API, see {@link module:engine/model/writer~Writer `Writer`}.
  * * Unlike {@link module:engine/model/writer~Writer `Writer`}, this method does not have to be used inside
  * a {@link #change `change()` block}.
  * * Inserting object into the model is not enough to make CKEditor 5 render that content to the user.
  * CKEditor 5 implements a model-view-controller architecture and what `model.insertObject()` does
  * is only adding nodes to the model. Additionally, you need to define
  * {@glink framework/architecture/editing-engine#conversion converters} between the model and view
  * and define those nodes in the {@glink framework/architecture/editing-engine#schema schema}.
  *
  * # Examples
  *
  * Use the following code to insert an object at the current selection and keep the selection on the inserted element:
  *
  * ```ts
  * const rawHtmlEmbedElement = writer.createElement( 'rawHtml' );
  *
  * model.insertObject( rawHtmlEmbedElement, null, null, {
  * 	setSelection: 'on'
  * } );
  * ```
  *
  * Use the following code to insert an object at the current selection and nudge the selection after the inserted object:
  *
  * ```ts
  * const pageBreakElement = writer.createElement( 'pageBreak' );
   *
  * model.insertObject( pageBreakElement, null, null, {
  * 	setSelection: 'after'
  * } );
  * ```
  *
  * Use the following code to insert an object at the current selection and avoid splitting the content (non-destructive insertion):
  *
  * ```ts
  * const tableElement = writer.createElement( 'table' );
   *
  * model.insertObject( tableElement, null, null, {
  * 	findOptimalPosition: 'auto'
  * } );
  * ```
  *
  * Use the following code to insert an object at the specific range (also: replace the content of the range):
  *
  * ```ts
  * const tableElement = writer.createElement( 'table' );
  * const range = model.createRangeOn( model.document.getRoot().getChild( 1 ) );
   *
  * model.insertObject( tableElement, range );
  * ```
  *
  * @param element An object to be inserted into the model document.
  * @param selectable A selectable where the content should be inserted. If not specified, the current
  * {@link module:engine/model/document~Document#selection document selection} will be used instead.
  * @param placeOrOffset Specifies the exact place or offset for the insertion to take place, relative to `selectable`.
  * @param options Additional options.
  * @param options.findOptimalPosition An option that, when set, adjusts the insertion position (relative to
  * `selectable` and `placeOrOffset`) so that the content of `selectable` is not split upon insertion (a.k.a. non-destructive insertion).
  * * When `'auto'`, the algorithm will decide whether to insert the object before or after `selectable` to avoid content splitting.
  * * When `'before'`, the closest position before `selectable` will be used that will not result in content splitting.
  * * When `'after'`, the closest position after `selectable` will be used that will not result in content splitting.
  *
  * Note that this option only works for block objects. Inline objects are inserted into text and do not split blocks.
  * @param options.setSelection An option that, when set, moves the
  * {@link module:engine/model/document~Document#selection document selection} after inserting the object.
  * * When `'on'`, the document selection will be set on the inserted object.
  * * When `'after'`, the document selection will move to the closest text node after the inserted object. If there is no
  * such text node, a paragraph will be created and the document selection will be moved inside it.
  * at the insertion position.
  */
  insertObject(e, n, r, s, ...o) {
    const a = qh(n, r);
    return this.fire("insertObject", [
      e,
      a,
      s,
      s,
      ...o
    ]);
  }
  /**
  * Deletes content of the selection and merge siblings. The resulting selection is always collapsed.
  *
  * **Note:** For the sake of predictability, the resulting selection should always be collapsed.
  * In cases where a feature wants to modify deleting behavior so selection isn't collapsed
  * (e.g. a table feature may want to keep row selection after pressing <kbd>Backspace</kbd>),
  * then that behavior should be implemented in the view's listener. At the same time, the table feature
  * will need to modify this method's behavior too, e.g. to "delete contents and then collapse
  * the selection inside the last selected cell" or "delete the row and collapse selection somewhere near".
  * That needs to be done in order to ensure that other features which use `deleteContent()` will work well with tables.
  *
  * @fires deleteContent
  * @param selection Selection of which the content should be deleted.
  * @param options.leaveUnmerged Whether to merge elements after removing the content of the selection.
  *
  * For example `<heading1>x[x</heading1><paragraph>y]y</paragraph>` will become:
  *
  * * `<heading1>x^y</heading1>` with the option disabled (`leaveUnmerged == false`)
  * * `<heading1>x^</heading1><paragraph>y</paragraph>` with enabled (`leaveUnmerged == true`).
  *
  * Note: {@link module:engine/model/schema~Schema#isObject object} and {@link module:engine/model/schema~Schema#isLimit limit}
  * elements will not be merged.
  *
  * @param options.doNotResetEntireContent Whether to skip replacing the entire content with a
  * paragraph when the entire content was selected.
  *
  * For example `<heading1>[x</heading1><paragraph>y]</paragraph>` will become:
  *
  * * `<paragraph>^</paragraph>` with the option disabled (`doNotResetEntireContent == false`)
  * * `<heading1>^</heading1>` with enabled (`doNotResetEntireContent == true`)
  *
  * @param options.doNotAutoparagraph Whether to create a paragraph if after content deletion selection is moved
  * to a place where text cannot be inserted.
  *
  * For example `<paragraph>x</paragraph>[<imageBlock src="foo.jpg"></imageBlock>]` will become:
  *
  * * `<paragraph>x</paragraph><paragraph>[]</paragraph>` with the option disabled (`doNotAutoparagraph == false`)
  * * `<paragraph>x[]</paragraph>` with the option enabled (`doNotAutoparagraph == true`).
  *
  * **Note:** if there is no valid position for the selection, the paragraph will always be created:
  *
  * `[<imageBlock src="foo.jpg"></imageBlock>]` -> `<paragraph>[]</paragraph>`.
  *
  * @param options.direction The direction in which the content is being consumed.
  * Deleting backward corresponds to using the <kbd>Backspace</kbd> key, while deleting content forward corresponds to
  * the <kbd>Shift</kbd>+<kbd>Backspace</kbd> keystroke.
  */
  deleteContent(e, n) {
    e8(this, e, n);
  }
  /**
  * Modifies the selection. Currently, the supported modifications are:
  *
  * * Extending. The selection focus is moved in the specified `options.direction` with a step specified in `options.unit`.
  * Possible values for `unit` are:
  *  * `'character'` (default) - moves selection by one user-perceived character. In most cases this means moving by one
  *  character in `String` sense. However, unicode also defines "combing marks". These are special symbols, that combines
  *  with a symbol before it ("base character") to create one user-perceived character. For example, `q̣̇` is a normal
  *  letter `q` with two "combining marks": upper dot (`Ux0307`) and lower dot (`Ux0323`). For most actions, i.e. extending
  *  selection by one position, it is correct to include both "base character" and all of it's "combining marks". That is
  *  why `'character'` value is most natural and common method of modifying selection.
  *  * `'codePoint'` - moves selection by one unicode code point. In contrary to, `'character'` unit, this will insert
  *  selection between "base character" and "combining mark", because "combining marks" have their own unicode code points.
  *  However, for technical reasons, unicode code points with values above `UxFFFF` are represented in native `String` by
  *  two characters, called "surrogate pairs". Halves of "surrogate pairs" have a meaning only when placed next to each other.
  *  For example `𨭎` is represented in `String` by `\uD862\uDF4E`. Both `\uD862` and `\uDF4E` do not have any meaning
  *  outside the pair (are rendered as ? when alone). Position between them would be incorrect. In this case, selection
  *  extension will include whole "surrogate pair".
  *  * `'word'` - moves selection by a whole word.
  *
  * **Note:** if you extend a forward selection in a backward direction you will in fact shrink it.
  *
  * @fires modifySelection
  * @param selection The selection to modify.
  * @param options.direction The direction in which the selection should be modified.
  * @param options.unit The unit by which selection should be modified.
  * @param options.treatEmojiAsSingleUnit Whether multi-characer emoji sequences should be handled as single unit.
  */
  modifySelection(e, n) {
    p8(this, e, n);
  }
  /**
  * Gets a clone of the selected content.
  *
  * For example, for the following selection:
  *
  * ```html
  * <paragraph>x</paragraph>
  * <blockQuote>
  * 	<paragraph>y</paragraph>
  * 	<heading1>fir[st</heading1>
  * </blockQuote>
  * <paragraph>se]cond</paragraph>
  * <paragraph>z</paragraph>
  * ```
  *
  * It will return a document fragment with such a content:
  *
  * ```html
  * <blockQuote>
  * 	<heading1>st</heading1>
  * </blockQuote>
  * <paragraph>se</paragraph>
  * ```
  *
  * @fires getSelectedContent
  * @param selection The selection of which content will be returned.
  */
  getSelectedContent(e) {
    return u8(this, e);
  }
  /**
  * Checks whether the given {@link module:engine/model/range~Range range} or
  * {@link module:engine/model/element~Element element} has any meaningful content.
  *
  * Meaningful content is:
  *
  * * any text node (`options.ignoreWhitespaces` allows controlling whether this text node must also contain
  * any non-whitespace characters),
  * * or any {@link module:engine/model/schema~Schema#isContent content element},
  * * or any {@link module:engine/model/markercollection~Marker marker} which
  * {@link module:engine/model/markercollection~Marker#_affectsData affects data}.
  *
  * This means that a range containing an empty `<paragraph></paragraph>` is not considered to have a meaningful content.
  * However, a range containing an `<imageBlock></imageBlock>` (which would normally be marked in the schema as an object element)
  * is considered non-empty.
  *
  * @param rangeOrElement Range or element to check.
  * @param options.ignoreWhitespaces Whether text node with whitespaces only should be considered empty.
  * @param options.ignoreMarkers Whether markers should be ignored.
  */
  hasContent(e, n = {}) {
    const r = e instanceof D ? e : D._createIn(e);
    if (r.isCollapsed)
      return !1;
    const { ignoreWhitespaces: s = !1, ignoreMarkers: o = !1 } = n;
    if (!o) {
      for (const a of this.markers.getMarkersIntersectingRange(r))
        if (a.affectsData)
          return !0;
    }
    for (const a of r.getItems())
      if (this.schema.isContent(a))
        if (a.is("$textProxy"))
          if (s) {
            if (a.data.search(/\S/) !== -1)
              return !0;
          } else return !0;
        else
          return !0;
    return !1;
  }
  /**
  * Check whether given selectable is at a place in the model where it can be edited (returns `true`) or not (returns `false`).
  *
  * Should be used instead of {@link module:core/editor/editor~Editor#isReadOnly} to check whether a user action can happen at
  * given selectable. It may be decorated and used differently in different environment (e.g. multi-root editor can disable
  * a particular root).
  *
  * This method is decorated. Although this method accepts any parameter of `Selectable` type, the
  * {@link ~Model#event:canEditAt `canEditAt` event} is fired with `selectable` normalized to an instance of
  * {@link module:engine/model/selection~Selection} or {@link module:engine/model/documentselection~DocumentSelection}
  *
  * @fires canEditAt
  */
  canEditAt(e) {
    const n = qh(e);
    return this.fire("canEditAt", [
      n
    ]);
  }
  /**
  * Creates a position from the given root and path in that root.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createPositionFromPath `Writer#createPositionFromPath()`}.
  *
  * @param root Root of the position.
  * @param path Position path. See {@link module:engine/model/position~Position#path}.
  * @param stickiness Position stickiness. See {@link module:engine/model/position~PositionStickiness}.
  */
  createPositionFromPath(e, n, r) {
    return new G(e, n, r);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/model/position~Position position},
  * * a parent element and offset in that element,
  * * a parent element and `'end'` (the position will be set at the end of that element),
  * * a {@link module:engine/model/item~Item model item} and `'before'` or `'after'`
  * (the position will be set before or after the given model item).
  *
  * This method is a shortcut to other factory methods such as:
  *
  * * {@link module:engine/model/model~Model#createPositionBefore `createPositionBefore()`},
  * * {@link module:engine/model/model~Model#createPositionAfter `createPositionAfter()`}.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createPositionAt `Writer#createPositionAt()`},
  *
  * @param itemOrPosition
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/model/item~Item model item}.
  */
  createPositionAt(e, n) {
    return G._createAt(e, n);
  }
  /**
  * Creates a new position after the given {@link module:engine/model/item~Item model item}.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createPositionAfter `Writer#createPositionAfter()`}.
  *
  * @param item Item after which the position should be placed.
  */
  createPositionAfter(e) {
    return G._createAfter(e);
  }
  /**
  * Creates a new position before the given {@link module:engine/model/item~Item model item}.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createPositionBefore `Writer#createPositionBefore()`}.
  *
  * @param item Item before which the position should be placed.
  */
  createPositionBefore(e) {
    return G._createBefore(e);
  }
  /**
  * Creates a range spanning from the `start` position to the `end` position.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createRange `Writer#createRange()`}:
  *
  * ```ts
  * model.change( writer => {
  * 	const range = writer.createRange( start, end );
  * } );
  * ```
  *
  * @param start Start position.
  * @param end End position. If not set, the range will be collapsed to the `start` position.
  */
  createRange(e, n) {
    return new D(e, n);
  }
  /**
  * Creates a range inside the given element which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createRangeIn `Writer#createRangeIn()`}:
  *
  * ```ts
  * model.change( writer => {
  * 	const range = writer.createRangeIn( paragraph );
  * } );
  * ```
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(e) {
    return D._createIn(e);
  }
  /**
  * Creates a range that starts before the given {@link module:engine/model/item~Item model item} and ends after it.
  *
  * Note: This method is also available on `writer` instance as
  * {@link module:engine/model/writer~Writer#createRangeOn `Writer.createRangeOn()`}:
  *
  * ```ts
  * model.change( writer => {
  * 	const range = writer.createRangeOn( paragraph );
  * } );
  * ```
  *
  * @param item
  */
  createRangeOn(e) {
    return D._createOn(e);
  }
  createSelection(...e) {
    return new vi(...e);
  }
  /**
  * Creates a {@link module:engine/model/batch~Batch} instance.
  *
  * **Note:** In most cases creating a batch instance is not necessary as they are created when using:
  *
  * * {@link #change `change()`},
  * * {@link #enqueueChange `enqueueChange()`}.
  *
  * @param type {@link module:engine/model/batch~Batch#constructor The type} of the batch.
  */
  createBatch(e) {
    return new po(e);
  }
  /**
  * Creates an operation instance from a JSON object (parsed JSON string).
  *
  * This is an alias for {@link module:engine/model/operation/operationfactory~OperationFactory.fromJSON `OperationFactory.fromJSON()`}.
  *
  * @param json Deserialized JSON object.
  */
  createOperationFromJSON(e) {
    return N7.fromJSON(e, this.document);
  }
  /**
  * Removes all events listeners set by model instance and destroys {@link module:engine/model/document~Document}.
  */
  destroy() {
    this.document.destroy(), this.stopListening();
  }
  /**
  * Common part of {@link module:engine/model/model~Model#change} and {@link module:engine/model/model~Model#enqueueChange}
  * which calls callbacks and returns array of values returned by these callbacks.
  */
  _runPendingChanges() {
    const e = [];
    this.fire("_beforeChanges");
    try {
      for (; this._pendingChanges.length; ) {
        const n = this._pendingChanges[0].batch;
        this._currentWriter = new X7(this, n);
        const r = this._pendingChanges[0].callback(this._currentWriter);
        e.push(r), this.document._handleChangeBlock(this._currentWriter), this._pendingChanges.shift(), this._currentWriter = null;
      }
    } finally {
      this._pendingChanges.length = 0, this._currentWriter = null, this.fire("_afterChanges");
    }
    return e;
  }
};
function qh(i, t) {
  if (i)
    return i instanceof vi || i instanceof Xi ? i : i instanceof no ? t || t === 0 ? new vi(i, t) : i.is("rootElement") ? new vi(i, "in") : new vi(i, "on") : new vi(i);
}
class x8 extends ws {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    h(this, "domEventType", "click");
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
}
class Hd extends ws {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    h(this, "domEventType", [
      "mousedown",
      "mouseup",
      "mouseover",
      "mouseout"
    ]);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
}
class vr {
  /**
  * @param document The view document instance in which this upcast writer operates.
  */
  constructor(t) {
    /**
    * The view document instance in which this upcast writer operates.
    */
    h(this, "document");
    this.document = t;
  }
  /**
  * Creates a new {@link module:engine/view/documentfragment~DocumentFragment} instance.
  *
  * @param children A list of nodes to be inserted into the created document fragment.
  * @returns The created document fragment.
  */
  createDocumentFragment(t) {
    return new Bs(this.document, t);
  }
  /**
  * Creates a new {@link module:engine/view/element~Element} instance.
  *
  * Attributes can be passed in various formats:
  *
  * ```ts
  * upcastWriter.createElement( 'div', { class: 'editor', contentEditable: 'true' } ); // object
  * upcastWriter.createElement( 'div', [ [ 'class', 'editor' ], [ 'contentEditable', 'true' ] ] ); // map-like iterator
  * upcastWriter.createElement( 'div', mapOfAttributes ); // map
  * ```
  *
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  * @returns Created element.
  */
  createElement(t, e, n) {
    return new Ji(this.document, t, e, n);
  }
  /**
  * Creates a new {@link module:engine/view/text~Text} instance.
  *
  * @param data The text's data.
  * @returns The created text node.
  */
  createText(t) {
    return new St(this.document, t);
  }
  /**
  * Clones the provided element.
  *
  * @see module:engine/view/element~Element#_clone
  * @param element Element to be cloned.
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any children.
  * @returns Clone of this element.
  */
  clone(t, e = !1) {
    return t._clone(e);
  }
  /**
  * Appends a child node or a list of child nodes at the end of this node
  * and sets the parent of these nodes to this element.
  *
  * @see module:engine/view/element~Element#_appendChild
  * @param items Items to be inserted.
  * @param element Element to which items will be appended.
  * @returns Number of appended nodes.
  */
  appendChild(t, e) {
    return e._appendChild(t);
  }
  /**
  * Inserts a child node or a list of child nodes on the given index and sets the parent of these nodes to
  * this element.
  *
  * @see module:engine/view/element~Element#_insertChild
  * @param index Offset at which nodes should be inserted.
  * @param items Items to be inserted.
  * @param element Element to which items will be inserted.
  * @returns Number of inserted nodes.
  */
  insertChild(t, e, n) {
    return n._insertChild(t, e);
  }
  /**
  * Removes the given number of child nodes starting at the given index and set the parent of these nodes to `null`.
  *
  * @see module:engine/view/element~Element#_removeChildren
  * @param index Offset from which nodes will be removed.
  * @param howMany Number of nodes to remove.
  * @param element Element which children will be removed.
  * @returns The array containing removed nodes.
  */
  removeChildren(t, e, n) {
    return n._removeChildren(t, e);
  }
  /**
  * Removes given element from the view structure. Will not have effect on detached elements.
  *
  * @param element Element which will be removed.
  * @returns The array containing removed nodes.
  */
  remove(t) {
    const e = t.parent;
    return e ? this.removeChildren(e.getChildIndex(t), 1, e) : [];
  }
  /**
  * Replaces given element with the new one in the view structure. Will not have effect on detached elements.
  *
  * @param oldElement Element which will be replaced.
  * @param newElement Element which will be inserted in the place of the old element.
  * @returns Whether old element was successfully replaced.
  */
  replace(t, e) {
    const n = t.parent;
    if (n) {
      const r = n.getChildIndex(t);
      return this.removeChildren(r, 1, n), this.insertChild(r, e, n), !0;
    }
    return !1;
  }
  /**
  * Removes given element from view structure and places its children in its position.
  * It does nothing if element has no parent.
  *
  * @param element Element to unwrap.
  */
  unwrapElement(t) {
    const e = t.parent;
    if (e) {
      const n = e.getChildIndex(t);
      this.remove(t), this.insertChild(n, t.getChildren(), e);
    }
  }
  /**
  * Renames element by creating a copy of a given element but with its name changed and then moving contents of the
  * old element to the new one.
  *
  * Since this function creates a new element and removes the given one, the new element is returned to keep reference.
  *
  * @param newName New element name.
  * @param  element Element to be renamed.
  * @returns New element or null if the old element was not replaced (happens for detached elements).
  */
  rename(t, e) {
    const n = new Ji(this.document, t, e.getAttributes(), e.getChildren());
    return this.replace(e, n) ? n : null;
  }
  /**
  * Adds or overwrites element's attribute with a specified key and value.
  *
  * ```ts
  * writer.setAttribute( 'href', 'http://ckeditor.com', linkElement );
  * ```
  *
  * @see module:engine/view/element~Element#_setAttribute
  * @param key Attribute key.
  * @param value Attribute value.
  * @param element Element for which attribute will be set.
  */
  setAttribute(t, e, n) {
    n._setAttribute(t, e);
  }
  /**
  * Removes attribute from the element.
  *
  * ```ts
  * writer.removeAttribute( 'href', linkElement );
  * ```
  *
  * @see module:engine/view/element~Element#_removeAttribute
  * @param key Attribute key.
  * @param element Element from which attribute will be removed.
  */
  removeAttribute(t, e) {
    e._removeAttribute(t);
  }
  /**
  * Adds specified class to the element.
  *
  * ```ts
  * writer.addClass( 'foo', linkElement );
  * writer.addClass( [ 'foo', 'bar' ], linkElement );
  * ```
  *
  * @see module:engine/view/element~Element#_addClass
  * @param className Single class name or array of class names which will be added.
  * @param element Element for which class will be added.
  */
  addClass(t, e) {
    e._addClass(t);
  }
  /**
  * Removes specified class from the element.
  *
  * ```ts
  * writer.removeClass( 'foo', linkElement );
  * writer.removeClass( [ 'foo', 'bar' ], linkElement );
  * ```
  *
  * @see module:engine/view/element~Element#_removeClass
  * @param className Single class name or array of class names which will be removed.
  * @param element Element from which class will be removed.
  */
  removeClass(t, e) {
    e._removeClass(t);
  }
  setStyle(t, e, n) {
    oi(t) && n === void 0 ? e._setStyle(t) : n._setStyle(t, e);
  }
  /**
  * Removes specified style from the element.
  *
  * ```ts
  * writer.removeStyle( 'color', element );  // Removes 'color' style.
  * writer.removeStyle( [ 'color', 'border-top' ], element ); // Removes both 'color' and 'border-top' styles.
  * ```
  *
  * **Note**: This method can work with normalized style names if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#remove `StylesMap#remove()`} for details.
  *
  * @see module:engine/view/element~Element#_removeStyle
  * @param property Style property name or names to be removed.
  * @param element Element from which style will be removed.
  */
  removeStyle(t, e) {
    e._removeStyle(t);
  }
  /**
  * Sets a custom property on element. Unlike attributes, custom properties are not rendered to the DOM,
  * so they can be used to add special data to elements.
  *
  * @see module:engine/view/element~Element#_setCustomProperty
  * @param key Custom property name/key.
  * @param value Custom property value to be stored.
  * @param element Element for which custom property will be set.
  */
  setCustomProperty(t, e, n) {
    n._setCustomProperty(t, e);
  }
  /**
  * Removes a custom property stored under the given key.
  *
  * @see module:engine/view/element~Element#_removeCustomProperty
  * @param key Name/key of the custom property to be removed.
  * @param element Element from which the custom property will be removed.
  * @returns Returns true if property was removed.
  */
  removeCustomProperty(t, e) {
    return e._removeCustomProperty(t);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/view/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/view/item~Item view item} and `'before'` or `'after'` (sets position before or after given view item).
  *
  * This method is a shortcut to other constructors such as:
  *
  * * {@link #createPositionBefore},
  * * {@link #createPositionAfter},
  *
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  createPositionAt(t, e) {
    return fe._createAt(t, e);
  }
  /**
  * Creates a new position after given view item.
  *
  * @param item View item after which the position should be located.
  */
  createPositionAfter(t) {
    return fe._createAfter(t);
  }
  /**
  * Creates a new position before given view item.
  *
  * @param item View item before which the position should be located.
  */
  createPositionBefore(t) {
    return fe._createBefore(t);
  }
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * **Note:** This factory method creates it's own {@link module:engine/view/position~Position} instances basing on passed values.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at `start` position.
  */
  createRange(t, e) {
    return new qe(t, e);
  }
  /**
  * Creates a range that starts before given {@link module:engine/view/item~Item view item} and ends after it.
  */
  createRangeOn(t) {
    return qe._createOn(t);
  }
  /**
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(t) {
    return qe._createIn(t);
  }
  createSelection(...t) {
    return new Dr(...t);
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const E8 = /^#([0-9a-f]{3,4}|[0-9a-f]{6}|[0-9a-f]{8})$/i, T8 = /^rgb\([ ]?([0-9]{1,3}[ %]?,[ ]?){2,3}[0-9]{1,3}[ %]?\)$/i, S8 = /^rgba\([ ]?([0-9]{1,3}[ %]?,[ ]?){3}(1|[0-9]+%|[0]?\.?[0-9]+)\)$/i, A8 = /^hsl\([ ]?([0-9]{1,3}[ %]?[,]?[ ]*){3}(1|[0-9]+%|[0]?\.?[0-9]+)?\)$/i, P8 = /^hsla\([ ]?([0-9]{1,3}[ %]?,[ ]?){2,3}(1|[0-9]+%|[0]?\.?[0-9]+)\)$/i, I8 = /\w+\((?:[^()]|\([^()]*\))*\)|\S+/gi, R8 = /* @__PURE__ */ new Set([
  // CSS Level 1
  "black",
  "silver",
  "gray",
  "white",
  "maroon",
  "red",
  "purple",
  "fuchsia",
  "green",
  "lime",
  "olive",
  "yellow",
  "navy",
  "blue",
  "teal",
  "aqua",
  // CSS Level 2 (Revision 1)
  "orange",
  // CSS Color Module Level 3
  "aliceblue",
  "antiquewhite",
  "aquamarine",
  "azure",
  "beige",
  "bisque",
  "blanchedalmond",
  "blueviolet",
  "brown",
  "burlywood",
  "cadetblue",
  "chartreuse",
  "chocolate",
  "coral",
  "cornflowerblue",
  "cornsilk",
  "crimson",
  "cyan",
  "darkblue",
  "darkcyan",
  "darkgoldenrod",
  "darkgray",
  "darkgreen",
  "darkgrey",
  "darkkhaki",
  "darkmagenta",
  "darkolivegreen",
  "darkorange",
  "darkorchid",
  "darkred",
  "darksalmon",
  "darkseagreen",
  "darkslateblue",
  "darkslategray",
  "darkslategrey",
  "darkturquoise",
  "darkviolet",
  "deeppink",
  "deepskyblue",
  "dimgray",
  "dimgrey",
  "dodgerblue",
  "firebrick",
  "floralwhite",
  "forestgreen",
  "gainsboro",
  "ghostwhite",
  "gold",
  "goldenrod",
  "greenyellow",
  "grey",
  "honeydew",
  "hotpink",
  "indianred",
  "indigo",
  "ivory",
  "khaki",
  "lavender",
  "lavenderblush",
  "lawngreen",
  "lemonchiffon",
  "lightblue",
  "lightcoral",
  "lightcyan",
  "lightgoldenrodyellow",
  "lightgray",
  "lightgreen",
  "lightgrey",
  "lightpink",
  "lightsalmon",
  "lightseagreen",
  "lightskyblue",
  "lightslategray",
  "lightslategrey",
  "lightsteelblue",
  "lightyellow",
  "limegreen",
  "linen",
  "magenta",
  "mediumaquamarine",
  "mediumblue",
  "mediumorchid",
  "mediumpurple",
  "mediumseagreen",
  "mediumslateblue",
  "mediumspringgreen",
  "mediumturquoise",
  "mediumvioletred",
  "midnightblue",
  "mintcream",
  "mistyrose",
  "moccasin",
  "navajowhite",
  "oldlace",
  "olivedrab",
  "orangered",
  "orchid",
  "palegoldenrod",
  "palegreen",
  "paleturquoise",
  "palevioletred",
  "papayawhip",
  "peachpuff",
  "peru",
  "pink",
  "plum",
  "powderblue",
  "rosybrown",
  "royalblue",
  "saddlebrown",
  "salmon",
  "sandybrown",
  "seagreen",
  "seashell",
  "sienna",
  "skyblue",
  "slateblue",
  "slategray",
  "slategrey",
  "snow",
  "springgreen",
  "steelblue",
  "tan",
  "thistle",
  "tomato",
  "turquoise",
  "violet",
  "wheat",
  "whitesmoke",
  "yellowgreen",
  // CSS Color Module Level 3 (System Colors)
  "activeborder",
  "activecaption",
  "appworkspace",
  "background",
  "buttonface",
  "buttonhighlight",
  "buttonshadow",
  "buttontext",
  "captiontext",
  "graytext",
  "highlight",
  "highlighttext",
  "inactiveborder",
  "inactivecaption",
  "inactivecaptiontext",
  "infobackground",
  "infotext",
  "menu",
  "menutext",
  "scrollbar",
  "threeddarkshadow",
  "threedface",
  "threedhighlight",
  "threedlightshadow",
  "threedshadow",
  "window",
  "windowframe",
  "windowtext",
  // CSS Color Module Level 4
  "rebeccapurple",
  // Keywords
  "currentcolor",
  "transparent"
]);
function _C(i) {
  return i.startsWith("#") ? E8.test(i) : i.startsWith("rgb") ? T8.test(i) || S8.test(i) : i.startsWith("hsl") ? A8.test(i) || P8.test(i) : R8.has(i.toLowerCase());
}
const O8 = [
  "none",
  "hidden",
  "dotted",
  "dashed",
  "solid",
  "double",
  "groove",
  "ridge",
  "inset",
  "outset"
];
function V8(i) {
  return O8.includes(i);
}
const M8 = /^([+-]?[0-9]*([.][0-9]+)?(px|cm|mm|in|pc|pt|ch|em|ex|rem|vh|vw|vmin|vmax)|0)$/;
function cp(i) {
  return M8.test(i);
}
const D8 = /^[+-]?[0-9]*([.][0-9]+)?%$/;
function L8(i) {
  return D8.test(i);
}
const N8 = [
  "repeat-x",
  "repeat-y",
  "repeat",
  "space",
  "round",
  "no-repeat"
];
function B8(i) {
  return N8.includes(i);
}
const F8 = [
  "center",
  "top",
  "bottom",
  "left",
  "right"
];
function $8(i) {
  return F8.includes(i);
}
const z8 = [
  "fixed",
  "scroll",
  "local"
];
function H8(i) {
  return z8.includes(i);
}
const j8 = /^url\(/;
function W8(i) {
  return j8.test(i);
}
function hl(i = "") {
  if (i === "")
    return {
      top: void 0,
      right: void 0,
      bottom: void 0,
      left: void 0
    };
  const t = up(i), e = t[0], n = t[2] || e, r = t[1] || e, s = t[3] || r;
  return {
    top: e,
    bottom: n,
    right: r,
    left: s
  };
}
function wu(i) {
  return (t) => {
    const { top: e, right: n, bottom: r, left: s } = t, o = [];
    return [
      e,
      n,
      s,
      r
    ].every((a) => !!a) ? o.push([
      i,
      U8(t)
    ]) : (e && o.push([
      i + "-top",
      e
    ]), n && o.push([
      i + "-right",
      n
    ]), r && o.push([
      i + "-bottom",
      r
    ]), s && o.push([
      i + "-left",
      s
    ])), o;
  };
}
function U8({ top: i, right: t, bottom: e, left: n }) {
  const r = [];
  return n !== t ? r.push(i, t, e, n) : e !== i ? r.push(i, t, e) : t !== i ? r.push(i, t) : r.push(i), r.join(" ");
}
function q8(i) {
  return (t) => ({
    path: i,
    value: hl(t)
  });
}
function up(i) {
  const t = i.trim().slice(0, 1500).matchAll(I8);
  return Array.from(t).map((e) => e[0]);
}
function CC(i) {
  i.setNormalizer("background", G8()), i.setNormalizer("background-color", K8()), i.setReducer("background", Z8()), i.setStyleRelation("background", [
    "background-color"
  ]);
}
function G8() {
  return (i) => {
    const t = {}, e = up(i);
    for (const n of e)
      B8(n) ? (t.repeat = t.repeat || [], t.repeat.push(n)) : $8(n) ? (t.position = t.position || [], t.position.push(n)) : H8(n) ? t.attachment = n : _C(n) ? t.color = n : W8(n) && (t.image = n);
    return {
      path: "background",
      value: t
    };
  };
}
function K8() {
  return (i) => ({
    path: "background.color",
    value: i
  });
}
function Z8() {
  return (i) => {
    const t = [];
    return t.push([
      "background-color",
      i.color
    ]), t;
  };
}
function kC(i) {
  i.setNormalizer("border", Y8()), i.setNormalizer("border-top", Nc("top")), i.setNormalizer("border-right", Nc("right")), i.setNormalizer("border-bottom", Nc("bottom")), i.setNormalizer("border-left", Nc("left")), i.setNormalizer("border-color", Gh("color")), i.setNormalizer("border-width", Gh("width")), i.setNormalizer("border-style", Gh("style")), i.setNormalizer("border-top-color", Ai("color", "top")), i.setNormalizer("border-top-style", Ai("style", "top")), i.setNormalizer("border-top-width", Ai("width", "top")), i.setNormalizer("border-right-color", Ai("color", "right")), i.setNormalizer("border-right-style", Ai("style", "right")), i.setNormalizer("border-right-width", Ai("width", "right")), i.setNormalizer("border-bottom-color", Ai("color", "bottom")), i.setNormalizer("border-bottom-style", Ai("style", "bottom")), i.setNormalizer("border-bottom-width", Ai("width", "bottom")), i.setNormalizer("border-left-color", Ai("color", "left")), i.setNormalizer("border-left-style", Ai("style", "left")), i.setNormalizer("border-left-width", Ai("width", "left")), i.setExtractor("border-top", Bc("top")), i.setExtractor("border-right", Bc("right")), i.setExtractor("border-bottom", Bc("bottom")), i.setExtractor("border-left", Bc("left")), i.setExtractor("border-top-color", "border.color.top"), i.setExtractor("border-right-color", "border.color.right"), i.setExtractor("border-bottom-color", "border.color.bottom"), i.setExtractor("border-left-color", "border.color.left"), i.setExtractor("border-top-width", "border.width.top"), i.setExtractor("border-right-width", "border.width.right"), i.setExtractor("border-bottom-width", "border.width.bottom"), i.setExtractor("border-left-width", "border.width.left"), i.setExtractor("border-top-style", "border.style.top"), i.setExtractor("border-right-style", "border.style.right"), i.setExtractor("border-bottom-style", "border.style.bottom"), i.setExtractor("border-left-style", "border.style.left"), i.setReducer("border-color", wu("border-color")), i.setReducer("border-style", wu("border-style")), i.setReducer("border-width", wu("border-width")), i.setReducer("border-top", Fc("top")), i.setReducer("border-right", Fc("right")), i.setReducer("border-bottom", Fc("bottom")), i.setReducer("border-left", Fc("left")), i.setReducer("border", X8()), i.setStyleRelation("border", [
    "border-color",
    "border-style",
    "border-width",
    "border-top",
    "border-right",
    "border-bottom",
    "border-left",
    "border-top-color",
    "border-right-color",
    "border-bottom-color",
    "border-left-color",
    "border-top-style",
    "border-right-style",
    "border-bottom-style",
    "border-left-style",
    "border-top-width",
    "border-right-width",
    "border-bottom-width",
    "border-left-width"
  ]), i.setStyleRelation("border-color", [
    "border-top-color",
    "border-right-color",
    "border-bottom-color",
    "border-left-color"
  ]), i.setStyleRelation("border-style", [
    "border-top-style",
    "border-right-style",
    "border-bottom-style",
    "border-left-style"
  ]), i.setStyleRelation("border-width", [
    "border-top-width",
    "border-right-width",
    "border-bottom-width",
    "border-left-width"
  ]), i.setStyleRelation("border-top", [
    "border-top-color",
    "border-top-style",
    "border-top-width"
  ]), i.setStyleRelation("border-right", [
    "border-right-color",
    "border-right-style",
    "border-right-width"
  ]), i.setStyleRelation("border-bottom", [
    "border-bottom-color",
    "border-bottom-style",
    "border-bottom-width"
  ]), i.setStyleRelation("border-left", [
    "border-left-color",
    "border-left-style",
    "border-left-width"
  ]);
}
function Y8() {
  return (i) => {
    const { color: t, style: e, width: n } = xC(i);
    return {
      path: "border",
      value: {
        color: hl(t),
        style: hl(e),
        width: hl(n)
      }
    };
  };
}
function Nc(i) {
  return (t) => {
    const { color: e, style: n, width: r } = xC(t), s = {};
    return e !== void 0 && (s.color = {
      [i]: e
    }), n !== void 0 && (s.style = {
      [i]: n
    }), r !== void 0 && (s.width = {
      [i]: r
    }), {
      path: "border",
      value: s
    };
  };
}
function Gh(i) {
  return (t) => ({
    path: "border",
    value: J8(t, i)
  });
}
function J8(i, t) {
  return {
    [t]: hl(i)
  };
}
function Ai(i, t) {
  return (e) => ({
    path: "border",
    value: {
      [i]: {
        [t]: e
      }
    }
  });
}
function Bc(i) {
  return (t, e) => {
    if (e.border)
      return el(e.border, i);
  };
}
function el(i, t) {
  const e = {};
  return i.width && i.width[t] && (e.width = i.width[t]), i.style && i.style[t] && (e.style = i.style[t]), i.color && i.color[t] && (e.color = i.color[t]), e;
}
function xC(i) {
  const t = {}, e = up(i);
  for (const n of e)
    cp(n) || /thin|medium|thick/.test(n) ? t.width = n : V8(n) ? t.style = n : t.color = n;
  return t;
}
function X8() {
  return (t) => {
    const e = el(t, "top"), n = el(t, "right"), r = el(t, "bottom"), s = el(t, "left"), o = [
      e,
      n,
      r,
      s
    ], a = {
      width: i(o, "width"),
      style: i(o, "style"),
      color: i(o, "color")
    }, l = Ro(a, "all");
    return l.length ? l : [
      ...Object.entries(a).reduce((u, [d, f]) => (f && (u.push([
        `border-${d}`,
        f
      ]), o.forEach((m) => delete m[d])), u), []),
      ...Ro(e, "top"),
      ...Ro(n, "right"),
      ...Ro(r, "bottom"),
      ...Ro(s, "left")
    ];
  };
  function i(t, e) {
    return t.map((n) => n[e]).reduce((n, r) => n == r ? n : null);
  }
}
function Fc(i) {
  return (t) => Ro(t, i);
}
function Ro(i, t) {
  const e = [];
  if (i && i.width && e.push("width"), i && i.style && e.push("style"), i && i.color && e.push("color"), e.length == 3) {
    const n = e.map((r) => i[r]).join(" ");
    return [
      t == "all" ? [
        "border",
        n
      ] : [
        `border-${t}`,
        n
      ]
    ];
  }
  return t == "all" ? [] : e.map((n) => [
    `border-${t}-${n}`,
    i[n]
  ]);
}
function Q8(i) {
  i.setNormalizer("padding", q8("padding")), i.setNormalizer("padding-top", (t) => ({
    path: "padding.top",
    value: t
  })), i.setNormalizer("padding-right", (t) => ({
    path: "padding.right",
    value: t
  })), i.setNormalizer("padding-bottom", (t) => ({
    path: "padding.bottom",
    value: t
  })), i.setNormalizer("padding-left", (t) => ({
    path: "padding.left",
    value: t
  })), i.setReducer("padding", wu("padding")), i.setStyleRelation("padding", [
    "padding-top",
    "padding-right",
    "padding-bottom",
    "padding-left"
  ]);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class EC {
  /**
  * @param {module:watchdog/watchdog~WatchdogConfig} config The watchdog plugin configuration.
  */
  constructor(t) {
    /**
    * An array of crashes saved as an object with the following properties:
    *
    * * `message`: `String`,
    * * `stack`: `String`,
    * * `date`: `Number`,
    * * `filename`: `String | undefined`,
    * * `lineno`: `Number | undefined`,
    * * `colno`: `Number | undefined`,
    */
    h(this, "crashes", []);
    /**
    * Specifies the state of the item watched by the watchdog. The state can be one of the following values:
    *
    * * `initializing` &ndash; Before the first initialization, and after crashes, before the item is ready.
    * * `ready` &ndash; A state when the user can interact with the item.
    * * `crashed` &ndash; A state when an error occurs. It quickly changes to `initializing` or `crashedPermanently`
    * depending on how many and how frequent errors have been caught recently.
    * * `crashedPermanently` &ndash; A state when the watchdog stops reacting to errors and keeps the item it is watching crashed,
    * * `destroyed` &ndash; A state when the item is manually destroyed by the user after calling `watchdog.destroy()`.
    */
    h(this, "state", "initializing");
    /**
    * @see module:watchdog/watchdog~WatchdogConfig
    */
    h(this, "_crashNumberLimit");
    /**
    * Returns the result of the `Date.now()` call. It can be overridden in tests to mock time as some popular
    * approaches like `sinon.useFakeTimers()` do not work well with error handling.
    */
    h(this, "_now", Date.now);
    /**
    * @see module:watchdog/watchdog~WatchdogConfig
    */
    h(this, "_minimumNonErrorTimePeriod");
    /**
    * Checks if the event error comes from the underlying item and restarts the item.
    */
    h(this, "_boundErrorHandler");
    /**
    * A dictionary of event emitter listeners.
    */
    h(this, "_listeners");
    if (this.crashes = [], this._crashNumberLimit = typeof t.crashNumberLimit == "number" ? t.crashNumberLimit : 3, this._minimumNonErrorTimePeriod = typeof t.minimumNonErrorTimePeriod == "number" ? t.minimumNonErrorTimePeriod : 5e3, this._boundErrorHandler = (e) => {
      const n = "error" in e ? e.error : e.reason;
      n instanceof Error && this._handleError(n, e);
    }, this._listeners = {}, !this._restart)
      throw new Error("The Watchdog class was split into the abstract `Watchdog` class and the `EditorWatchdog` class. Please, use `EditorWatchdog` if you have used the `Watchdog` class previously.");
  }
  /**
  * Destroys the watchdog and releases the resources.
  */
  destroy() {
    this._stopErrorHandling(), this._listeners = {};
  }
  /**
  * Starts listening to a specific event name by registering a callback that will be executed
  * whenever an event with a given name fires.
  *
  * Note that this method differs from the CKEditor 5's default `EventEmitterMixin` implementation.
  *
  * @param eventName The event name.
  * @param callback A callback which will be added to event listeners.
  */
  on(t, e) {
    this._listeners[t] || (this._listeners[t] = []), this._listeners[t].push(e);
  }
  /**
  * Stops listening to the specified event name by removing the callback from event listeners.
  *
  * Note that this method differs from the CKEditor 5's default `EventEmitterMixin` implementation.
  *
  * @param eventName The event name.
  * @param callback A callback which will be removed from event listeners.
  */
  off(t, e) {
    this._listeners[t] = this._listeners[t].filter((n) => n !== e);
  }
  /**
  * Fires an event with a given event name and arguments.
  *
  * Note that this method differs from the CKEditor 5's default `EventEmitterMixin` implementation.
  */
  _fire(t, ...e) {
    const n = this._listeners[t] || [];
    for (const r of n)
      r.apply(this, [
        null,
        ...e
      ]);
  }
  /**
  * Starts error handling by attaching global error handlers.
  */
  _startErrorHandling() {
    window.addEventListener("error", this._boundErrorHandler), window.addEventListener("unhandledrejection", this._boundErrorHandler);
  }
  /**
  * Stops error handling by detaching global error handlers.
  */
  _stopErrorHandling() {
    window.removeEventListener("error", this._boundErrorHandler), window.removeEventListener("unhandledrejection", this._boundErrorHandler);
  }
  /**
  * Checks if an error comes from the watched item and restarts it.
  * It reacts to {@link module:utils/ckeditorerror~CKEditorError `CKEditorError` errors} only.
  *
  * @fires error
  * @param error Error.
  * @param evt An error event.
  */
  _handleError(t, e) {
    if (this._shouldReactToError(t)) {
      this.crashes.push({
        message: t.message,
        stack: t.stack,
        // `evt.filename`, `evt.lineno` and `evt.colno` are available only in ErrorEvent events
        filename: e instanceof ErrorEvent ? e.filename : void 0,
        lineno: e instanceof ErrorEvent ? e.lineno : void 0,
        colno: e instanceof ErrorEvent ? e.colno : void 0,
        date: this._now()
      });
      const n = this._shouldRestart();
      this.state = "crashed", this._fire("stateChange"), this._fire("error", {
        error: t,
        causesRestart: n
      }), n ? this._restart() : (this.state = "crashedPermanently", this._fire("stateChange"));
    }
  }
  /**
  * Checks whether an error should be handled by the watchdog.
  *
  * @param error An error that was caught by the error handling process.
  */
  _shouldReactToError(t) {
    return t.is && t.is("CKEditorError") && t.context !== void 0 && // In some cases the watched item should not be restarted - e.g. during the item initialization.
    // That's why the `null` was introduced as a correct error context which does cause restarting.
    t.context !== null && // Do not react to errors if the watchdog is in states other than `ready`.
    this.state === "ready" && this._isErrorComingFromThisItem(t);
  }
  /**
  * Checks if the watchdog should restart the underlying item.
  */
  _shouldRestart() {
    if (this.crashes.length <= this._crashNumberLimit)
      return !0;
    const t = this.crashes[this.crashes.length - 1].date, e = this.crashes[this.crashes.length - 1 - this._crashNumberLimit].date;
    return (t - e) / this._crashNumberLimit > this._minimumNonErrorTimePeriod;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function dm(i, t = /* @__PURE__ */ new Set()) {
  const e = [
    i
  ], n = /* @__PURE__ */ new Set();
  let r = 0;
  for (; e.length > r; ) {
    const s = e[r++];
    if (!(n.has(s) || !e9(s) || t.has(s)))
      if (n.add(s), Symbol.iterator in s)
        try {
          for (const o of s)
            e.push(o);
        } catch {
        }
      else
        for (const o in s)
          o !== "defaultValue" && e.push(s[o]);
  }
  return n;
}
function e9(i) {
  const t = Object.prototype.toString.call(i), e = typeof i;
  return !(e === "number" || e === "boolean" || e === "string" || e === "symbol" || e === "function" || t === "[object Date]" || t === "[object RegExp]" || t === "[object Module]" || i === void 0 || i === null || // This flag is meant to exclude singletons shared across editor instances. So when an error is thrown in one editor,
  // the other editors connected through the reference to the same singleton are not restarted. This is a temporary workaround
  // until a better solution is found.
  // More in https://github.com/ckeditor/ckeditor5/issues/12292.
  i._watchdogExcluded || // Skip native DOM objects, e.g. Window, nodes, events, etc.
  i instanceof EventTarget || i instanceof Event);
}
function TC(i, t, e = /* @__PURE__ */ new Set()) {
  if (i === t && t9(i))
    return !0;
  const n = dm(i, e), r = dm(t, e);
  for (const s of n)
    if (r.has(s))
      return !0;
  return !1;
}
function t9(i) {
  return typeof i == "object" && i !== null;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class SC extends EC {
  /**
  * @param Editor The editor class.
  * @param watchdogConfig The watchdog plugin configuration.
  */
  constructor(e, n = {}) {
    super(n);
    /**
    * The current editor instance.
    */
    h(this, "_editor", null);
    /**
    * A promise associated with the life cycle of the editor (creation or destruction processes).
    *
    * It is used to prevent the initialization of the editor if the previous instance has not been destroyed yet,
    * and conversely, to prevent the destruction of the editor if it has not been initialized.
    */
    h(this, "_lifecyclePromise", null);
    /**
    * Throttled save method. The `save()` method is called the specified `saveInterval` after `throttledSave()` is called,
    * unless a new action happens in the meantime.
    */
    h(this, "_throttledSave");
    /**
    * The latest saved editor data represented as a root name -> root data object.
    */
    h(this, "_data");
    /**
    * The last document version.
    */
    h(this, "_lastDocumentVersion");
    /**
    * The editor source element or data.
    */
    h(this, "_elementOrData");
    /**
    * Specifies whether the editor was initialized using document data (`true`) or HTML elements (`false`).
    */
    h(this, "_initUsingData", !0);
    /**
    * The latest record of the editor editable elements. Used to restart the editor.
    */
    h(this, "_editables", {});
    /**
    * The editor configuration.
    */
    h(this, "_config");
    h(this, "_excludedProps");
    this._throttledSave = Jo(this._save.bind(this), typeof n.saveInterval == "number" ? n.saveInterval : 5e3), e && (this._creator = (r, s) => e.create(r, s)), this._destructor = (r) => r.destroy();
  }
  /**
  * The current editor instance.
  */
  get editor() {
    return this._editor;
  }
  /**
  * @internal
  */
  get _item() {
    return this._editor;
  }
  /**
  * Sets the function that is responsible for the editor creation.
  * It expects a function that should return a promise.
  *
  * ```ts
  * watchdog.setCreator( ( element, config ) => ClassicEditor.create( element, config ) );
  * ```
  */
  setCreator(e) {
    this._creator = e;
  }
  /**
  * Sets the function that is responsible for the editor destruction.
  * Overrides the default destruction function, which destroys only the editor instance.
  * It expects a function that should return a promise or `undefined`.
  *
  * ```ts
  * watchdog.setDestructor( editor => {
  * 	// Do something before the editor is destroyed.
  *
  * 	return editor
  * 		.destroy()
  * 		.then( () => {
  * 			// Do something after the editor is destroyed.
  * 		} );
  * } );
  * ```
  */
  setDestructor(e) {
    this._destructor = e;
  }
  /**
  * Restarts the editor instance. This method is called whenever an editor error occurs. It fires the `restart` event and changes
  * the state to `initializing`.
  *
  * @fires restart
  */
  _restart() {
    return Promise.resolve().then(() => (this.state = "initializing", this._fire("stateChange"), this._destroy())).catch((e) => {
      console.error("An error happened during the editor destroying.", e);
    }).then(() => {
      const e = {}, n = [], r = this._config.rootsAttributes || {}, s = {};
      for (const [a, l] of Object.entries(this._data.roots))
        l.isLoaded ? (e[a] = "", s[a] = r[a] || {}) : n.push(a);
      const o = {
        ...this._config,
        extraPlugins: this._config.extraPlugins || [],
        lazyRoots: n,
        rootsAttributes: s,
        _watchdogInitialData: this._data
      };
      return delete o.initialData, o.extraPlugins.push(n9), this._initUsingData ? this.create(e, o, o.context) : us(this._elementOrData) ? this.create(this._elementOrData, o, o.context) : this.create(this._editables, o, o.context);
    }).then(() => {
      this._fire("restart");
    });
  }
  /**
  * Creates the editor instance and keeps it running, using the defined creator and destructor.
  *
  * @param elementOrData The editor source element or the editor data.
  * @param config The editor configuration.
  * @param context A context for the editor.
  */
  create(e = this._elementOrData, n = this._config, r) {
    return this._lifecyclePromise = Promise.resolve(this._lifecyclePromise).then(() => (super._startErrorHandling(), this._elementOrData = e, this._initUsingData = typeof e == "string" || Object.keys(e).length > 0 && typeof Object.values(e)[0] == "string", this._config = this._cloneEditorConfiguration(n) || {}, this._config.context = r, this._creator(e, this._config))).then((s) => {
      this._editor = s, s.model.document.on("change:data", this._throttledSave), this._lastDocumentVersion = s.model.document.version, this._data = this._getData(), this._initUsingData || (this._editables = this._getEditables()), this.state = "ready", this._fire("stateChange");
    }).finally(() => {
      this._lifecyclePromise = null;
    }), this._lifecyclePromise;
  }
  /**
  * Destroys the watchdog and the current editor instance. It fires the callback
  * registered in {@link #setDestructor `setDestructor()`} and uses it to destroy the editor instance.
  * It also sets the state to `destroyed`.
  */
  destroy() {
    return this._lifecyclePromise = Promise.resolve(this._lifecyclePromise).then(() => (this.state = "destroyed", this._fire("stateChange"), super.destroy(), this._destroy())).finally(() => {
      this._lifecyclePromise = null;
    }), this._lifecyclePromise;
  }
  _destroy() {
    return Promise.resolve().then(() => {
      this._stopErrorHandling(), this._throttledSave.cancel();
      const e = this._editor;
      return this._editor = null, e.model.document.off("change:data", this._throttledSave), this._destructor(e);
    });
  }
  /**
  * Saves the editor data, so it can be restored after the crash even if the data cannot be fetched at
  * the moment of the crash.
  */
  _save() {
    const e = this._editor.model.document.version;
    try {
      this._data = this._getData(), this._initUsingData || (this._editables = this._getEditables()), this._lastDocumentVersion = e;
    } catch (n) {
      console.error(n, "An error happened during restoring editor data. Editor will be restored from the previously saved data.");
    }
  }
  /**
  * @internal
  */
  _setExcludedProperties(e) {
    this._excludedProps = e;
  }
  /**
  * Gets all data that is required to reinitialize editor instance.
  */
  _getData() {
    const e = this._editor, n = e.model.document.roots.filter((l) => l.isAttached() && l.rootName != "$graveyard"), { plugins: r } = e, s = r.has("CommentsRepository") && r.get("CommentsRepository"), o = r.has("TrackChanges") && r.get("TrackChanges"), a = {
      roots: {},
      markers: {},
      commentThreads: JSON.stringify([]),
      suggestions: JSON.stringify([])
    };
    n.forEach((l) => {
      a.roots[l.rootName] = {
        content: JSON.stringify(Array.from(l.getChildren())),
        attributes: JSON.stringify(Array.from(l.getAttributes())),
        isLoaded: l._isLoaded
      };
    });
    for (const l of e.model.markers)
      l._affectsData && (a.markers[l.name] = {
        rangeJSON: l.getRange().toJSON(),
        usingOperation: l._managedUsingOperations,
        affectsData: l._affectsData
      });
    return s && (a.commentThreads = JSON.stringify(s.getCommentThreads({
      toJSON: !0,
      skipNotAttached: !0
    }))), o && (a.suggestions = JSON.stringify(o.getSuggestions({
      toJSON: !0,
      skipNotAttached: !0
    }))), a;
  }
  /**
  * For each attached model root, returns its HTML editable element (if available).
  */
  _getEditables() {
    const e = {};
    for (const n of this.editor.model.document.getRootNames()) {
      const r = this.editor.ui.getEditableElement(n);
      r && (e[n] = r);
    }
    return e;
  }
  /**
  * Traverses the error context and the current editor to find out whether these structures are connected
  * to each other via properties.
  *
  * @internal
  */
  _isErrorComingFromThisItem(e) {
    return TC(this._editor, e.context, this._excludedProps);
  }
  /**
  * Clones the editor configuration.
  */
  _cloneEditorConfiguration(e) {
    return jg(e, (n, r) => {
      if (us(n) || r === "context")
        return n;
    });
  }
}
class n9 {
  constructor(t) {
    h(this, "editor");
    h(this, "_data");
    this.editor = t, this._data = t.config.get("_watchdogInitialData");
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.data.on("init", (t) => {
      t.stop(), this.editor.model.enqueueChange({
        isUndoable: !1
      }, (e) => {
        this._restoreCollaborationData(), this._restoreEditorData(e);
      }), this.editor.data.fire("ready");
    }, {
      priority: 999
    });
  }
  /**
  * Creates a model node (element or text) based on provided JSON.
  */
  _createNode(t, e) {
    if ("name" in e) {
      const n = t.createElement(e.name, e.attributes);
      if (e.children)
        for (const r of e.children)
          n._appendChild(this._createNode(t, r));
      return n;
    } else
      return t.createText(e.data, e.attributes);
  }
  /**
  * Restores the editor by setting the document data, roots attributes and markers.
  */
  _restoreEditorData(t) {
    const e = this.editor;
    Object.entries(this._data.roots).forEach(([n, { content: r, attributes: s }]) => {
      const o = JSON.parse(r), a = JSON.parse(s), l = e.model.document.getRoot(n);
      for (const [c, u] of a)
        t.setAttribute(c, u, l);
      for (const c of o) {
        const u = this._createNode(t, c);
        t.insert(u, l, "end");
      }
    }), Object.entries(this._data.markers).forEach(([n, r]) => {
      const { document: s } = e.model, { rangeJSON: { start: o, end: a }, ...l } = r, c = s.getRoot(o.root), u = t.createPositionFromPath(c, o.path, o.stickiness), d = t.createPositionFromPath(c, a.path, a.stickiness), f = t.createRange(u, d);
      t.addMarker(n, {
        range: f,
        ...l
      });
    });
  }
  /**
  * Restores the editor collaboration data - comment threads and suggestions.
  */
  _restoreCollaborationData() {
    const t = JSON.parse(this._data.commentThreads), e = JSON.parse(this._data.suggestions);
    t.forEach((n) => {
      const r = this.editor.config.get("collaboration.channelId"), s = this.editor.plugins.get("CommentsRepository");
      s.hasCommentThread(n.threadId) && s.getCommentThread(n.threadId).remove(), s.addCommentThread({
        channelId: r,
        ...n
      });
    }), e.forEach((n) => {
      const r = this.editor.plugins.get("TrackChangesEditing");
      if (r.hasSuggestion(n.id)) {
        const s = r.getSuggestion(n.id);
        s.attributes = n.attributes;
      } else
        r.addSuggestionData(n);
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const fl = Symbol("MainQueueId");
class i9 extends EC {
  /**
  * The context watchdog class constructor.
  *
  * ```ts
  * const watchdog = new ContextWatchdog( Context );
  *
  * await watchdog.create( contextConfiguration );
  *
  * await watchdog.add( item );
  * ```
  *
  * See the {@glink features/watchdog Watchdog feature guide} to learn more how to use this feature.
  *
  * @param Context The {@link module:core/context~Context} class.
  * @param watchdogConfig The watchdog configuration.
  */
  constructor(e, n = {}) {
    super(n);
    /**
    * A map of internal watchdogs for added items.
    */
    h(this, "_watchdogs", /* @__PURE__ */ new Map());
    /**
    * The watchdog configuration.
    */
    h(this, "_watchdogConfig");
    /**
    * The current context instance.
    */
    h(this, "_context", null);
    /**
    * Context properties (nodes/references) that are gathered during the initial context creation
    * and are used to distinguish the origin of an error.
    */
    h(this, "_contextProps", /* @__PURE__ */ new Set());
    /**
    * An action queue, which is used to handle async functions queuing.
    */
    h(this, "_actionQueues", new r9());
    /**
    * The configuration for the {@link module:core/context~Context}.
    */
    h(this, "_contextConfig");
    /**
    * The watched item.
    */
    h(this, "_item");
    this._watchdogConfig = n, this._creator = (r) => e.create(r), this._destructor = (r) => r.destroy(), this._actionQueues.onEmpty(() => {
      this.state === "initializing" && (this.state = "ready", this._fire("stateChange"));
    });
  }
  /**
  * Sets the function that is responsible for the context creation.
  * It expects a function that should return a promise (or `undefined`).
  *
  * ```ts
  * watchdog.setCreator( config => Context.create( config ) );
  * ```
  */
  setCreator(e) {
    this._creator = e;
  }
  /**
  * Sets the function that is responsible for the context destruction.
  * Overrides the default destruction function, which destroys only the context instance.
  * It expects a function that should return a promise (or `undefined`).
  *
  * ```ts
  * watchdog.setDestructor( context => {
  * 	// Do something before the context is destroyed.
  *
  * 	return context
  * 		.destroy()
  * 		.then( () => {
  * 			// Do something after the context is destroyed.
  * 		} );
  * } );
  * ```
  */
  setDestructor(e) {
    this._destructor = e;
  }
  /**
  * The context instance. Keep in mind that this property might be changed when the context watchdog restarts,
  * so do not keep this instance internally. Always operate on the `ContextWatchdog#context` property.
  */
  get context() {
    return this._context;
  }
  /**
  * Initializes the context watchdog. Once it is created, the watchdog takes care about
  * recreating the context and the provided items, and starts the error handling mechanism.
  *
  * ```ts
  * await watchdog.create( {
  * 	plugins: []
  * } );
  * ```
  *
  * @param contextConfig The context configuration. See {@link module:core/context~Context}.
  */
  create(e = {}) {
    return this._actionQueues.enqueue(fl, () => (this._contextConfig = e, this._create()));
  }
  /**
  * Returns an item instance with the given `itemId`.
  *
  * ```ts
  * const editor1 = watchdog.getItem( 'editor1' );
  * ```
  *
  * @param itemId The item ID.
  * @returns The item instance or `undefined` if an item with a given ID has not been found.
  */
  getItem(e) {
    return this._getWatchdog(e)._item;
  }
  /**
  * Gets the state of the given item. See {@link #state} for a list of available states.
  *
  * ```ts
  * const editor1State = watchdog.getItemState( 'editor1' );
  * ```
  *
  * @param itemId Item ID.
  * @returns The state of the item.
  */
  getItemState(e) {
    return this._getWatchdog(e).state;
  }
  /**
  * Adds items to the watchdog. Once created, instances of these items will be available using the {@link #getItem} method.
  *
  * Items can be passed together as an array of objects:
  *
  * ```ts
  * await watchdog.add( [ {
  * 	id: 'editor1',
  * 	type: 'editor',
  * 	sourceElementOrData: document.querySelector( '#editor' ),
  * 	config: {
  * 		plugins: [ Essentials, Paragraph, Bold, Italic ],
  * 		toolbar: [ 'bold', 'italic', 'alignment' ]
  * 	},
  * 	creator: ( element, config ) => ClassicEditor.create( element, config )
  * } ] );
  * ```
  *
  * Or one by one as objects:
  *
  * ```ts
  * await watchdog.add( {
  * 	id: 'editor1',
  * 	type: 'editor',
  * 	sourceElementOrData: document.querySelector( '#editor' ),
  * 	config: {
  * 		plugins: [ Essentials, Paragraph, Bold, Italic ],
  * 		toolbar: [ 'bold', 'italic', 'alignment' ]
  * 	},
  * 	creator: ( element, config ) => ClassicEditor.create( element, config )
  * ] );
  * ```
  *
  * Then an instance can be retrieved using the {@link #getItem} method:
  *
  * ```ts
  * const editor1 = watchdog.getItem( 'editor1' );
  * ```
  *
  * Note that this method can be called multiple times, but for performance reasons it is better
  * to pass all items together.
  *
  * @param itemConfigurationOrItemConfigurations An item configuration object or an array of item configurations.
  */
  add(e) {
    const n = lv(e);
    return Promise.all(n.map((r) => this._actionQueues.enqueue(r.id, () => {
      if (this.state === "destroyed")
        throw new Error("Cannot add items to destroyed watchdog.");
      if (!this._context)
        throw new Error("Context was not created yet. You should call the `ContextWatchdog#create()` method first.");
      let s;
      if (this._watchdogs.has(r.id))
        throw new Error(`Item with the given id is already added: '${r.id}'.`);
      if (r.type === "editor")
        return s = new SC(null, this._watchdogConfig), s.setCreator(r.creator), s._setExcludedProperties(this._contextProps), r.destructor && s.setDestructor(r.destructor), this._watchdogs.set(r.id, s), s.on("error", (o, { error: a, causesRestart: l }) => {
          this._fire("itemError", {
            itemId: r.id,
            error: a
          }), l && this._actionQueues.enqueue(r.id, () => new Promise((c) => {
            const u = () => {
              s.off("restart", u), this._fire("itemRestart", {
                itemId: r.id
              }), c();
            };
            s.on("restart", u);
          }));
        }), s.create(r.sourceElementOrData, r.config, this._context);
      throw new Error(`Not supported item type: '${r.type}'.`);
    })));
  }
  /**
  * Removes and destroys item(s) with given ID(s).
  *
  * ```ts
  * await watchdog.remove( 'editor1' );
  * ```
  *
  * Or
  *
  * ```ts
  * await watchdog.remove( [ 'editor1', 'editor2' ] );
  * ```
  *
  * @param itemIdOrItemIds Item ID or an array of item IDs.
  */
  remove(e) {
    const n = lv(e);
    return Promise.all(n.map((r) => this._actionQueues.enqueue(r, () => {
      const s = this._getWatchdog(r);
      return this._watchdogs.delete(r), s.destroy();
    })));
  }
  /**
  * Destroys the context watchdog and all added items.
  * Once the context watchdog is destroyed, new items cannot be added.
  *
  * ```ts
  * await watchdog.destroy();
  * ```
  */
  destroy() {
    return this._actionQueues.enqueue(fl, () => (this.state = "destroyed", this._fire("stateChange"), super.destroy(), this._destroy()));
  }
  /**
  * Restarts the context watchdog.
  */
  _restart() {
    return this._actionQueues.enqueue(fl, () => (this.state = "initializing", this._fire("stateChange"), this._destroy().catch((e) => {
      console.error("An error happened during destroying the context or items.", e);
    }).then(() => this._create()).then(() => this._fire("restart"))));
  }
  /**
  * Initializes the context watchdog.
  */
  _create() {
    return Promise.resolve().then(() => (this._startErrorHandling(), this._creator(this._contextConfig))).then((e) => (this._context = e, this._contextProps = dm(this._context), Promise.all(Array.from(this._watchdogs.values()).map((n) => (n._setExcludedProperties(this._contextProps), n.create(void 0, void 0, this._context))))));
  }
  /**
  * Destroys the context instance and all added items.
  */
  _destroy() {
    return Promise.resolve().then(() => {
      this._stopErrorHandling();
      const e = this._context;
      return this._context = null, this._contextProps = /* @__PURE__ */ new Set(), Promise.all(Array.from(this._watchdogs.values()).map((n) => n.destroy())).then(() => this._destructor(e));
    });
  }
  /**
  * Returns the watchdog for a given item ID.
  *
  * @param itemId Item ID.
  */
  _getWatchdog(e) {
    const n = this._watchdogs.get(e);
    if (!n)
      throw new Error(`Item with the given id was not registered: ${e}.`);
    return n;
  }
  /**
  * Checks whether an error comes from the context instance and not from the item instances.
  *
  * @internal
  */
  _isErrorComingFromThisItem(e) {
    for (const n of this._watchdogs.values())
      if (n._isErrorComingFromThisItem(e))
        return !1;
    return TC(this._context, e.context);
  }
}
class r9 {
  constructor() {
    h(this, "_onEmptyCallbacks", []);
    h(this, "_queues", /* @__PURE__ */ new Map());
    h(this, "_activeActions", 0);
  }
  /**
  * Used to register callbacks that will be run when the queue becomes empty.
  *
  * @param onEmptyCallback A callback that will be run whenever the queue becomes empty.
  */
  onEmpty(t) {
    this._onEmptyCallbacks.push(t);
  }
  /**
  * It adds asynchronous actions (functions) to the proper queue and runs them one by one.
  *
  * @param queueId The action queue ID.
  * @param action A function that should be enqueued.
  */
  enqueue(t, e) {
    const n = t === fl;
    this._activeActions++, this._queues.get(t) || this._queues.set(t, Promise.resolve());
    const s = (n ? Promise.all(this._queues.values()) : Promise.all([
      this._queues.get(fl),
      this._queues.get(t)
    ])).then(e), o = s.catch(() => {
    });
    return this._queues.set(t, o), s.finally(() => {
      this._activeActions--, this._queues.get(t) === o && this._activeActions === 0 && this._onEmptyCallbacks.forEach((a) => a());
    });
  }
}
function lv(i) {
  return Array.isArray(i) ? i : [
    i
  ];
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class V extends (/* @__PURE__ */ st()) {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super();
    /**
    * The editor instance.
    *
    * Note that most editors implement the {@link module:core/editor/editor~Editor#ui} property.
    * However, editors with an external UI (i.e. Bootstrap-based) or a headless editor may not have this property or
    * throw an error when accessing it.
    *
    * Because of above, to make plugins more universal, it is recommended to split features into:
    *  - The "editing" part that uses the {@link module:core/editor/editor~Editor} class without `ui` property.
    *  - The "UI" part that uses the {@link module:core/editor/editor~Editor} class and accesses `ui` property.
    */
    h(this, "editor");
    /**
    * Holds identifiers for {@link #forceDisabled} mechanism.
    */
    h(this, "_disableStack", /* @__PURE__ */ new Set());
    this.editor = e, this.set("isEnabled", !0);
  }
  /**
  * Disables the plugin.
  *
  * Plugin may be disabled by multiple features or algorithms (at once). When disabling a plugin, unique id should be passed
  * (e.g. feature name). The same identifier should be used when {@link #clearForceDisabled enabling back} the plugin.
  * The plugin becomes enabled only after all features {@link #clearForceDisabled enabled it back}.
  *
  * Disabling and enabling a plugin:
  *
  * ```ts
  * plugin.isEnabled; // -> true
  * plugin.forceDisabled( 'MyFeature' );
  * plugin.isEnabled; // -> false
  * plugin.clearForceDisabled( 'MyFeature' );
  * plugin.isEnabled; // -> true
  * ```
  *
  * Plugin disabled by multiple features:
  *
  * ```ts
  * plugin.forceDisabled( 'MyFeature' );
  * plugin.forceDisabled( 'OtherFeature' );
  * plugin.clearForceDisabled( 'MyFeature' );
  * plugin.isEnabled; // -> false
  * plugin.clearForceDisabled( 'OtherFeature' );
  * plugin.isEnabled; // -> true
  * ```
  *
  * Multiple disabling with the same identifier is redundant:
  *
  * ```ts
  * plugin.forceDisabled( 'MyFeature' );
  * plugin.forceDisabled( 'MyFeature' );
  * plugin.clearForceDisabled( 'MyFeature' );
  * plugin.isEnabled; // -> true
  * ```
  *
  * **Note:** some plugins or algorithms may have more complex logic when it comes to enabling or disabling certain plugins,
  * so the plugin might be still disabled after {@link #clearForceDisabled} was used.
  *
  * @param id Unique identifier for disabling. Use the same id when {@link #clearForceDisabled enabling back} the plugin.
  */
  forceDisabled(e) {
    this._disableStack.add(e), this._disableStack.size == 1 && (this.on("set:isEnabled", cv, {
      priority: "highest"
    }), this.isEnabled = !1);
  }
  /**
  * Clears forced disable previously set through {@link #forceDisabled}. See {@link #forceDisabled}.
  *
  * @param id Unique identifier, equal to the one passed in {@link #forceDisabled} call.
  */
  clearForceDisabled(e) {
    this._disableStack.delete(e), this._disableStack.size == 0 && (this.off("set:isEnabled", cv), this.isEnabled = !0);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this.stopListening();
  }
  /**
  * @inheritDoc
  */
  static get isContextPlugin() {
    return !1;
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !1;
  }
  /**
  * @inheritDoc
  */
  static get isPremiumPlugin() {
    return !1;
  }
}
function cv(i) {
  i.return = !1, i.stop();
}
class Ve extends (/* @__PURE__ */ st()) {
  /**
  * Creates a new `Command` instance.
  *
  * @param editor The editor on which this command will be used.
  */
  constructor(e) {
    super();
    /**
    * The editor on which this command will be used.
    */
    h(this, "editor");
    /**
    * A flag indicating whether a command's `isEnabled` state should be changed depending on where the document
    * selection is placed.
    *
    * By default, it is set to `true`. If the document selection is placed in a
    * {@link module:engine/model/model~Model#canEditAt non-editable} place (such as non-editable root), the command becomes disabled.
    *
    * The flag should be changed to `false` in a concrete command's constructor if the command should not change its `isEnabled`
    * accordingly to the document selection.
    */
    h(this, "_isEnabledBasedOnSelection");
    /**
    * A flag indicating whether a command execution changes the editor data or not.
    *
    * @see #affectsData
    */
    h(this, "_affectsData");
    /**
    * Holds identifiers for {@link #forceDisabled} mechanism.
    */
    h(this, "_disableStack");
    this.editor = e, this.set("value", void 0), this.set("isEnabled", !1), this._affectsData = !0, this._isEnabledBasedOnSelection = !0, this._disableStack = /* @__PURE__ */ new Set(), this.decorate("execute"), this.listenTo(this.editor.model.document, "change", () => {
      this.refresh();
    }), this.listenTo(e, "change:isReadOnly", () => {
      this.refresh();
    }), this.on("set:isEnabled", (n) => {
      if (!this.affectsData)
        return;
      const r = e.model.document.selection, o = !(r.getFirstPosition().root.rootName == "$graveyard") && e.model.canEditAt(r);
      (e.isReadOnly || this._isEnabledBasedOnSelection && !o) && (n.return = !1, n.stop());
    }, {
      priority: "highest"
    }), this.on("execute", (n) => {
      this.isEnabled || n.stop();
    }, {
      priority: "high"
    });
  }
  /**
  * A flag indicating whether a command execution changes the editor data or not.
  *
  * Commands with `affectsData` set to `false` will not be automatically disabled in
  * the {@link module:core/editor/editor~Editor#isReadOnly read-only mode} and
  * {@glink features/read-only#related-features other editor modes} with restricted user write permissions.
  *
  * **Note:** You do not have to set it for your every command. It is `true` by default.
  *
  * @default true
  */
  get affectsData() {
    return this._affectsData;
  }
  set affectsData(e) {
    this._affectsData = e;
  }
  /**
  * Refreshes the command. The command should update its {@link #isEnabled} and {@link #value} properties
  * in this method.
  *
  * This method is automatically called when
  * {@link module:engine/model/document~Document#event:change any changes are applied to the document}.
  */
  refresh() {
    this.isEnabled = !0;
  }
  /**
  * Disables the command.
  *
  * Command may be disabled by multiple features or algorithms (at once). When disabling a command, unique id should be passed
  * (e.g. the feature name). The same identifier should be used when {@link #clearForceDisabled enabling back} the command.
  * The command becomes enabled only after all features {@link #clearForceDisabled enabled it back}.
  *
  * Disabling and enabling a command:
  *
  * ```ts
  * command.isEnabled; // -> true
  * command.forceDisabled( 'MyFeature' );
  * command.isEnabled; // -> false
  * command.clearForceDisabled( 'MyFeature' );
  * command.isEnabled; // -> true
  * ```
  *
  * Command disabled by multiple features:
  *
  * ```ts
  * command.forceDisabled( 'MyFeature' );
  * command.forceDisabled( 'OtherFeature' );
  * command.clearForceDisabled( 'MyFeature' );
  * command.isEnabled; // -> false
  * command.clearForceDisabled( 'OtherFeature' );
  * command.isEnabled; // -> true
  * ```
  *
  * Multiple disabling with the same identifier is redundant:
  *
  * ```ts
  * command.forceDisabled( 'MyFeature' );
  * command.forceDisabled( 'MyFeature' );
  * command.clearForceDisabled( 'MyFeature' );
  * command.isEnabled; // -> true
  * ```
  *
  * **Note:** some commands or algorithms may have more complex logic when it comes to enabling or disabling certain commands,
  * so the command might be still disabled after {@link #clearForceDisabled} was used.
  *
  * @param id Unique identifier for disabling. Use the same id when {@link #clearForceDisabled enabling back} the command.
  */
  forceDisabled(e) {
    this._disableStack.add(e), this._disableStack.size == 1 && (this.on("set:isEnabled", uv, {
      priority: "highest"
    }), this.isEnabled = !1);
  }
  /**
  * Clears forced disable previously set through {@link #forceDisabled}. See {@link #forceDisabled}.
  *
  * @param id Unique identifier, equal to the one passed in {@link #forceDisabled} call.
  */
  clearForceDisabled(e) {
    this._disableStack.delete(e), this._disableStack.size == 0 && (this.off("set:isEnabled", uv), this.refresh());
  }
  /**
  * Executes the command.
  *
  * A command may accept parameters. They will be passed from {@link module:core/editor/editor~Editor#execute `editor.execute()`}
  * to the command.
  *
  * The `execute()` method will automatically abort when the command is disabled ({@link #isEnabled} is `false`).
  * This behavior is implemented by a high priority listener to the {@link #event:execute} event.
  *
  * In order to see how to disable a command from "outside" see the {@link #isEnabled} documentation.
  *
  * This method may return a value, which would be forwarded all the way down to the
  * {@link module:core/editor/editor~Editor#execute `editor.execute()`}.
  *
  * @fires execute
  */
  execute(...e) {
  }
  /**
  * Destroys the command.
  */
  destroy() {
    this.stopListening();
  }
}
function uv(i) {
  i.return = !1, i.stop();
}
class AC extends (/* @__PURE__ */ ht()) {
  /**
  * Creates an instance of the plugin collection class.
  * Allows loading and initializing plugins and their dependencies.
  * Allows providing a list of already loaded plugins. These plugins will not be destroyed along with this collection.
  *
  * @param availablePlugins Plugins (constructors) which the collection will be able to use
  * when {@link module:core/plugincollection~PluginCollection#init} is used with the plugin names (strings, instead of constructors).
  * Usually, the editor will pass its built-in plugins to the collection so they can later be
  * used in `config.plugins` or `config.removePlugins` by names.
  * @param contextPlugins A list of already initialized plugins represented by a `[ PluginConstructor, pluginInstance ]` pair.
  */
  constructor(e, n = [], r = []) {
    super();
    h(this, "_context");
    h(this, "_plugins", /* @__PURE__ */ new Map());
    /**
    * A map of plugin constructors that can be retrieved by their names.
    */
    h(this, "_availablePlugins");
    /**
    * Map of {@link module:core/contextplugin~ContextPlugin context plugins} which can be retrieved by their constructors or instances.
    */
    h(this, "_contextPlugins");
    this._context = e, this._availablePlugins = /* @__PURE__ */ new Map();
    for (const s of n)
      s.pluginName && this._availablePlugins.set(s.pluginName, s);
    this._contextPlugins = /* @__PURE__ */ new Map();
    for (const [s, o] of r)
      this._contextPlugins.set(s, o), this._contextPlugins.set(o, s), s.pluginName && this._availablePlugins.set(s.pluginName, s);
  }
  /**
  * Iterable interface.
  *
  * Returns `[ PluginConstructor, pluginInstance ]` pairs.
  */
  *[Symbol.iterator]() {
    for (const e of this._plugins)
      typeof e[0] == "function" && (yield e);
  }
  /**
  * Gets the plugin instance by its constructor or name.
  *
  * ```ts
  * // Check if 'Clipboard' plugin was loaded.
  * if ( editor.plugins.has( 'ClipboardPipeline' ) ) {
  * 	// Get clipboard plugin instance
  * 	const clipboard = editor.plugins.get( 'ClipboardPipeline' );
  *
  * 	this.listenTo( clipboard, 'inputTransformation', ( evt, data ) => {
  * 		// Do something on clipboard input.
  * 	} );
  * }
  * ```
  *
  * **Note**: This method will throw an error if a plugin is not loaded. Use `{@link #has editor.plugins.has()}`
  * to check if a plugin is available.
  *
  * @param key The plugin constructor or {@link module:core/plugin~PluginStaticMembers#pluginName name}.
  */
  get(e) {
    const n = this._plugins.get(e);
    if (!n) {
      let r = e;
      throw typeof e == "function" && (r = e.pluginName || e.name), new T("plugincollection-plugin-not-loaded", this._context, {
        plugin: r
      });
    }
    return n;
  }
  /**
  * Checks if a plugin is loaded.
  *
  * ```ts
  * // Check if the 'Clipboard' plugin was loaded.
  * if ( editor.plugins.has( 'ClipboardPipeline' ) ) {
  * 	// Now use the clipboard plugin instance:
  * 	const clipboard = editor.plugins.get( 'ClipboardPipeline' );
  *
  * 	// ...
  * }
  * ```
  *
  * @param key The plugin constructor or {@link module:core/plugin~PluginStaticMembers#pluginName name}.
  */
  has(e) {
    return this._plugins.has(e);
  }
  /**
  * Initializes a set of plugins and adds them to the collection.
  *
  * @param plugins An array of {@link module:core/plugin~PluginInterface plugin constructors}
  * or {@link module:core/plugin~PluginStaticMembers#pluginName plugin names}.
  * @param pluginsToRemove Names of the plugins or plugin constructors
  * that should not be loaded (despite being specified in the `plugins` array).
  * @param pluginsSubstitutions An array of {@link module:core/plugin~PluginInterface plugin constructors}
  * that will be used to replace plugins of the same names that were passed in `plugins` or that are in their dependency tree.
  * A useful option for replacing built-in plugins while creating tests (for mocking their APIs). Plugins that will be replaced
  * must follow these rules:
  *   * The new plugin must be a class.
  *   * The new plugin must be named.
  *   * Both plugins must not depend on other plugins.
  * @returns A promise which gets resolved once all plugins are loaded and available in the collection.
  */
  init(e, n = [], r = []) {
    const s = this, o = this._context;
    g(e), b(e);
    const a = e.filter((A) => !f(A, n)), l = [
      ...p(a)
    ];
    I(l, r);
    const c = S(l);
    return k(c, "init").then(() => k(c, "afterInit")).then(() => c);
    function u(A) {
      return typeof A == "function";
    }
    function d(A) {
      return u(A) && !!A.isContextPlugin;
    }
    function f(A, L) {
      return L.some((R) => R === A || m(A) === R || m(R) === A);
    }
    function m(A) {
      return u(A) ? A.pluginName || A.name : A;
    }
    function g(A, L = /* @__PURE__ */ new Set()) {
      A.forEach((R) => {
        u(R) && (L.has(R) || (L.add(R), R.pluginName && !s._availablePlugins.has(R.pluginName) && s._availablePlugins.set(R.pluginName, R), R.requires && g(R.requires, L)));
      });
    }
    function p(A, L = /* @__PURE__ */ new Set()) {
      return A.map((R) => u(R) ? R : s._availablePlugins.get(R)).reduce((R, O) => L.has(O) ? R : (L.add(O), O.requires && (b(O.requires, O), p(O.requires, L).forEach((N) => R.add(N))), R.add(O)), /* @__PURE__ */ new Set());
    }
    function b(A, L = null) {
      A.map((R) => u(R) ? R : s._availablePlugins.get(R) || R).forEach((R) => {
        v(R, L), y(R, L), x(R, L);
      });
    }
    function v(A, L) {
      if (!u(A))
        throw L ? new T("plugincollection-soft-required", o, {
          missingPlugin: A,
          requiredBy: m(L)
        }) : new T("plugincollection-plugin-not-found", o, {
          plugin: A
        });
    }
    function y(A, L) {
      if (d(L) && !d(A))
        throw new T("plugincollection-context-required", o, {
          plugin: m(A),
          requiredBy: m(L)
        });
    }
    function x(A, L) {
      if (L && f(A, n))
        throw new T("plugincollection-required", o, {
          plugin: m(A),
          requiredBy: m(L)
        });
    }
    function S(A) {
      return A.map((L) => {
        let R = s._contextPlugins.get(L);
        return R = R || new L(o), s._add(L, R), R;
      });
    }
    function k(A, L) {
      return A.reduce((R, O) => !O[L] || s._contextPlugins.has(O) ? R : R.then(O[L].bind(O)), Promise.resolve());
    }
    function I(A, L) {
      for (const R of L) {
        if (typeof R != "function")
          throw new T("plugincollection-replace-plugin-invalid-type", null, {
            pluginItem: R
          });
        const O = R.pluginName;
        if (!O)
          throw new T("plugincollection-replace-plugin-missing-name", null, {
            pluginItem: R
          });
        if (R.requires && R.requires.length)
          throw new T("plugincollection-plugin-for-replacing-cannot-have-dependencies", null, {
            pluginName: O
          });
        const N = s._availablePlugins.get(O);
        if (!N)
          throw new T("plugincollection-plugin-for-replacing-not-exist", null, {
            pluginName: O
          });
        const le = A.indexOf(N);
        if (le === -1) {
          if (s._contextPlugins.has(N))
            return;
          throw new T("plugincollection-plugin-for-replacing-not-loaded", null, {
            pluginName: O
          });
        }
        if (N.requires && N.requires.length)
          throw new T("plugincollection-replaced-plugin-cannot-have-dependencies", null, {
            pluginName: O
          });
        A.splice(le, 1, R), s._availablePlugins.set(O, R);
      }
    }
  }
  /**
  * Destroys all loaded plugins.
  */
  destroy() {
    const e = [];
    for (const [, n] of this)
      typeof n.destroy == "function" && !this._contextPlugins.has(n) && e.push(n.destroy());
    return Promise.all(e);
  }
  /**
  * Adds the plugin to the collection. Exposed mainly for testing purposes.
  *
  * @param PluginConstructor The plugin constructor.
  * @param plugin The instance of the plugin.
  */
  _add(e, n) {
    this._plugins.set(e, n);
    const r = e.pluginName;
    if (r) {
      if (this._plugins.has(r))
        throw new T("plugincollection-plugin-name-conflict", null, {
          pluginName: r,
          plugin1: this._plugins.get(r).constructor,
          plugin2: e
        });
      this._plugins.set(r, n);
    }
  }
}
var pu;
let dv = (pu = class {
  /**
  * Creates a context instance with a given configuration.
  *
  * Usually not to be used directly. See the static {@link module:core/context~Context.create `create()`} method.
  *
  * @param config The context configuration.
  */
  constructor(t) {
    /**
    * Stores all the configurations specific to this context instance.
    */
    h(this, "config");
    /**
    * The plugins loaded and in use by this context instance.
    */
    h(this, "plugins");
    h(this, "locale");
    /**
    * Shorthand for {@link module:utils/locale~Locale#t}.
    */
    h(this, "t");
    /**
    * A list of editors that this context instance is injected to.
    */
    h(this, "editors");
    /**
    * Reference to the editor which created the context.
    * Null when the context was created outside of the editor.
    *
    * It is used to destroy the context when removing the editor that has created the context.
    */
    h(this, "_contextOwner", null);
    const { translations: e, ...n } = t || {};
    this.config = new p2(n, this.constructor.defaultConfig);
    const r = this.constructor.builtinPlugins;
    this.config.define("plugins", r), this.plugins = new AC(this, r);
    const s = this.config.get("language") || {};
    this.locale = new zR({
      uiLanguage: typeof s == "string" ? s : s.ui,
      contentLanguage: this.config.get("language.content"),
      translations: e
    }), this.t = this.locale.t, this.editors = new Pn();
  }
  /**
  * Loads and initializes plugins specified in the configuration.
  *
  * @returns A promise which resolves once the initialization is completed, providing an array of loaded plugins.
  */
  initPlugins() {
    const t = this.config.get("plugins") || [], e = this.config.get("substitutePlugins") || [];
    for (const n of t.concat(e)) {
      if (typeof n != "function")
        throw new T("context-initplugins-constructor-only", null, {
          Plugin: n
        });
      if (n.isContextPlugin !== !0)
        throw new T("context-initplugins-invalid-plugin", null, {
          Plugin: n
        });
    }
    return this.plugins.init(t, [], e);
  }
  /**
  * Destroys the context instance and all editors used with the context,
  * releasing all resources used by the context.
  *
  * @returns A promise that resolves once the context instance is fully destroyed.
  */
  destroy() {
    return Promise.all(Array.from(this.editors, (t) => t.destroy())).then(() => this.plugins.destroy());
  }
  /**
  * Adds a reference to the editor which is used with this context.
  *
  * When the given editor has created the context, the reference to this editor will be stored
  * as a {@link ~Context#_contextOwner}.
  *
  * This method should only be used by the editor.
  *
  * @internal
  * @param isContextOwner Stores the given editor as a context owner.
  */
  _addEditor(t, e) {
    if (this._contextOwner)
      throw new T("context-addeditor-private-context");
    this.editors.add(t), e && (this._contextOwner = t);
  }
  /**
  * Removes a reference to the editor which was used with this context.
  * When the context was created by the given editor, the context will be destroyed.
  *
  * This method should only be used by the editor.
  *
  * @internal
  * @return A promise that resolves once the editor is removed from the context or when the context was destroyed.
  */
  _removeEditor(t) {
    return this.editors.has(t) && this.editors.remove(t), this._contextOwner === t ? this.destroy() : Promise.resolve();
  }
  /**
  * Returns the context configuration which will be copied to the editors created using this context.
  *
  * The configuration returned by this method has the plugins configuration removed &ndash; plugins are shared with all editors
  * through another mechanism.
  *
  * This method should only be used by the editor.
  *
  * @internal
  * @returns Configuration as a plain object.
  */
  _getEditorConfig() {
    const t = {};
    for (const e of this.config.names())
      [
        "plugins",
        "removePlugins",
        "extraPlugins"
      ].includes(e) || (t[e] = this.config.get(e));
    return t;
  }
  /**
  * Creates and initializes a new context instance.
  *
  * ```ts
  * const commonConfig = { ... }; // Configuration for all the plugins and editors.
  * const editorPlugins = [ ... ]; // Regular plugins here.
  *
  * Context
  * 	.create( {
  * 		// Only context plugins here.
  * 		plugins: [ ... ],
  *
  * 		// Configure the language for all the editors (it cannot be overwritten).
  * 		language: { ... },
  *
  * 		// Configuration for context plugins.
  * 		comments: { ... },
  * 		...
  *
  * 		// Default configuration for editor plugins.
  * 		toolbar: { ... },
  * 		image: { ... },
  * 		...
  * 	} )
  * 	.then( context => {
  * 		const promises = [];
  *
  * 		promises.push( ClassicEditor.create(
  * 			document.getElementById( 'editor1' ),
  * 			{
  * 				editorPlugins,
  * 				context
  * 			}
  * 		) );
  *
  * 		promises.push( ClassicEditor.create(
  * 			document.getElementById( 'editor2' ),
  * 			{
  * 				editorPlugins,
  * 				context,
  * 				toolbar: { ... } // You can overwrite the configuration of the context.
  * 			}
  * 		) );
  *
  * 		return Promise.all( promises );
  * 	} );
  * ```
  *
  * @param config The context configuration.
  * @returns A promise resolved once the context is ready. The promise resolves with the created context instance.
  */
  static create(t) {
    return new Promise((e) => {
      const n = new this(t);
      e(n.initPlugins().then(() => n));
    });
  }
}, /**
* The default configuration which is built into the `Context` class.
*
* It is used in CKEditor 5 builds featuring `Context` to provide the default configuration options which are later used during the
* context initialization.
*
* ```ts
* Context.defaultConfig = {
* 	foo: 1,
* 	bar: 2
* };
*
* Context
* 	.create()
* 	.then( context => {
* 		context.config.get( 'foo' ); // -> 1
* 		context.config.get( 'bar' ); // -> 2
* 	} );
*
* // The default options can be overridden by the configuration passed to create().
* Context
* 	.create( { bar: 3 } )
* 	.then( context => {
* 		context.config.get( 'foo' ); // -> 1
* 		context.config.get( 'bar' ); // -> 3
* 	} );
* ```
*
* See also {@link module:core/context~Context.builtinPlugins `Context.builtinPlugins`}
* and {@link module:core/editor/editor~Editor.defaultConfig `Editor.defaultConfig`}.
*/
h(pu, "defaultConfig"), /**
* An array of plugins built into the `Context` class.
*
* It is used in CKEditor 5 builds featuring `Context` to provide a list of context plugins which are later automatically initialized
* during the context initialization.
*
* They will be automatically initialized by `Context` unless `config.plugins` is passed.
*
* ```ts
* // Build some context plugins into the Context class first.
* Context.builtinPlugins = [ FooPlugin, BarPlugin ];
*
* // Normally, you need to define config.plugins, but since Context.builtinPlugins was
* // defined, now you can call create() without any configuration.
* Context
* 	.create()
* 	.then( context => {
* 		context.plugins.get( FooPlugin ); // -> An instance of the Foo plugin.
* 		context.plugins.get( BarPlugin ); // -> An instance of the Bar plugin.
* 	} );
* ```
*
* See also {@link module:core/context~Context.defaultConfig `Context.defaultConfig`}
* and {@link module:core/editor/editor~Editor.builtinPlugins `Editor.builtinPlugins`}.
*/
h(pu, "builtinPlugins"), pu);
class jd extends (/* @__PURE__ */ st()) {
  /**
  * Creates a new plugin instance.
  */
  constructor(e) {
    super();
    /**
    * The context or editor instance.
    */
    h(this, "context");
    this.context = e;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this.stopListening();
  }
  /**
  * @inheritDoc
  */
  static get isContextPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !1;
  }
  /**
  * @inheritDoc
  */
  static get isPremiumPlugin() {
    return !1;
  }
}
class s9 {
  /**
  * Creates collection instance.
  */
  constructor() {
    /**
    * Command map.
    */
    h(this, "_commands");
    this._commands = /* @__PURE__ */ new Map();
  }
  /**
  * Registers a new command.
  *
  * @param commandName The name of the command.
  */
  add(t, e) {
    this._commands.set(t, e);
  }
  /**
  * Retrieves a command from the collection.
  *
  * @param commandName The name of the command.
  */
  get(t) {
    return this._commands.get(t);
  }
  /**
  * Executes a command.
  *
  * @param commandName The name of the command.
  * @param commandParams Command parameters.
  * @returns The value returned by the {@link module:core/command~Command#execute `command.execute()`}.
  */
  execute(t, ...e) {
    const n = this.get(t);
    if (!n)
      throw new T("commandcollection-command-not-found", this, {
        commandName: t
      });
    return n.execute(...e);
  }
  /**
  * Returns iterator of command names.
  */
  *names() {
    yield* this._commands.keys();
  }
  /**
  * Returns iterator of command instances.
  */
  *commands() {
    yield* this._commands.values();
  }
  /**
  * Iterable interface.
  *
  * Returns `[ commandName, commandInstance ]` pairs.
  */
  [Symbol.iterator]() {
    return this._commands[Symbol.iterator]();
  }
  /**
  * Destroys all collection commands.
  */
  destroy() {
    for (const t of this.commands())
      t.destroy();
  }
}
class o9 extends Ot {
  /**
  * Creates an instance of the keystroke handler.
  */
  constructor(e) {
    super();
    /**
    * The editor instance.
    */
    h(this, "editor");
    this.editor = e;
  }
  /**
  * Registers a handler for the specified keystroke.
  *
  * The handler can be specified as a command name or a callback.
  *
  * @param keystroke Keystroke defined in a format accepted by
  * the {@link module:utils/keyboard~parseKeystroke} function.
  * @param callback If a string is passed, then the keystroke will
  * {@link module:core/editor/editor~Editor#execute execute a command}.
  * If a function, then it will be called with the
  * {@link module:engine/view/observer/keyobserver~KeyEventData key event data} object and
  * a `cancel()` helper to both `preventDefault()` and `stopPropagation()` of the event.
  * @param options Additional options.
  * @param options.priority The priority of the keystroke callback. The higher the priority value
  * the sooner the callback will be executed. Keystrokes having the same priority
  * are called in the order they were added.
  */
  set(e, n, r = {}) {
    if (typeof n == "string") {
      const s = n;
      n = (o, a) => {
        this.editor.execute(s), a();
      };
    }
    super.set(e, n, r);
  }
}
const Kh = "contentEditing", hv = "common";
let a9 = class {
  /**
  * @inheritDoc
  */
  constructor(t) {
    /**
    * Stores information about keystrokes brought by editor features for the users to interact with the editor, mainly
    * keystroke combinations and their accessible labels.
    *
    * This information is particularly useful for screen reader and other assistive technology users. It gets displayed
    * by the {@link module:ui/editorui/accessibilityhelp/accessibilityhelp~AccessibilityHelp Accessibility help} dialog.
    *
    * Keystrokes are organized in categories and groups. They can be added using ({@link #addKeystrokeInfoCategory},
    * {@link #addKeystrokeInfoGroup}, and {@link #addKeystrokeInfos}) methods.
    *
    * Please note that:
    * * two categories are always available:
    *   * `'contentEditing'` for keystrokes related to content creation,
    *   * `'navigation'` for keystrokes related to navigation in the UI and the content.
    * * unless specified otherwise, new keystrokes are added into the `'contentEditing'` category and the `'common'`
    * keystroke group within that category while using the {@link #addKeystrokeInfos} method.
    */
    h(this, "keystrokeInfos", /* @__PURE__ */ new Map());
    /**
    * The editor instance.
    */
    h(this, "_editor");
    this._editor = t;
    const e = t.config.get("menuBar.isVisible"), n = t.locale.t;
    this.addKeystrokeInfoCategory({
      id: Kh,
      label: n("Content editing keystrokes"),
      description: n("These keyboard shortcuts allow for quick access to content editing features.")
    });
    const r = [
      {
        label: n("Close contextual balloons, dropdowns, and dialogs"),
        keystroke: "Esc"
      },
      {
        label: n("Open the accessibility help dialog"),
        keystroke: "Alt+0"
      },
      {
        label: n("Move focus between form fields (inputs, buttons, etc.)"),
        keystroke: [
          [
            "Tab"
          ],
          [
            "Shift+Tab"
          ]
        ]
      },
      {
        label: n("Move focus to the toolbar, navigate between toolbars"),
        keystroke: "Alt+F10",
        mayRequireFn: !0
      },
      {
        label: n("Navigate through the toolbar or menu bar"),
        keystroke: [
          [
            "arrowup"
          ],
          [
            "arrowright"
          ],
          [
            "arrowdown"
          ],
          [
            "arrowleft"
          ]
        ]
      },
      {
        // eslint-disable-next-line max-len
        label: n("Execute the currently focused button. Executing buttons that interact with the editor content moves the focus back to the content."),
        keystroke: [
          [
            "Enter"
          ],
          [
            "Space"
          ]
        ]
      }
    ];
    e && r.push({
      label: n("Move focus to the menu bar, navigate between menu bars"),
      keystroke: "Alt+F9",
      mayRequireFn: !0
    }), this.addKeystrokeInfoCategory({
      id: "navigation",
      label: n("User interface and content navigation keystrokes"),
      description: n("Use the following keystrokes for more efficient navigation in the CKEditor 5 user interface."),
      groups: [
        {
          id: "common",
          keystrokes: r
        }
      ]
    });
  }
  /**
  * Adds a top-level category in the {@link #keystrokeInfos keystroke information database} with a label and optional description.
  *
  * Categories organize keystrokes and help users to find the right keystroke. Each category can have multiple groups
  * of keystrokes that narrow down the context in which the keystrokes are available. Every keystroke category comes
  * with a `'common'` group by default.
  *
  * By default, two categories are available:
  * * `'contentEditing'` for keystrokes related to content creation,
  * * `'navigation'` for keystrokes related to navigation in the UI and the content.
  *
  * To create a new keystroke category with new groups, use the following code:
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfoCategory( {
  * 			id: 'myCategory',
  * 			label: t( 'My category' ),
  * 			description: t( 'My category description.' ),
  * 			groups: [
  * 				{
  * 					id: 'myGroup',
  * 					label: t( 'My keystroke group' ),
  * 					keystrokes: [
  * 						{
  * 							label: t( 'Keystroke label 1' ),
  * 							keystroke: 'Ctrl+Shift+N'
  * 						},
  * 						{
  * 							label: t( 'Keystroke label 2' ),
  * 							keystroke: 'Ctrl+Shift+M'
  * 						}
  * 					]
  * 				}
  * 			]
  * 		};
  * 	}
  * }
  * ```
  *
  * See {@link #keystrokeInfos}, {@link #addKeystrokeInfoGroup}, and {@link #addKeystrokeInfos}.
  */
  addKeystrokeInfoCategory({ id: t, label: e, description: n, groups: r }) {
    this.keystrokeInfos.set(t, {
      id: t,
      label: e,
      description: n,
      groups: /* @__PURE__ */ new Map()
    }), this.addKeystrokeInfoGroup({
      categoryId: t,
      id: hv
    }), r && r.forEach((s) => {
      this.addKeystrokeInfoGroup({
        categoryId: t,
        ...s
      });
    });
  }
  /**
  * Adds a group of keystrokes in a specific category to the {@link #keystrokeInfos keystroke information database}.
  *
  * Groups narrow down the context in which the keystrokes are available. When `categoryId` is not specified,
  * the group goes to the `'contentEditing'` category (default).
  *
  * To create a new group within an existing category, use the following code:
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfoGroup( {
  * 			id: 'myGroup',
  * 			categoryId: 'navigation',
  * 			label: t( 'My keystroke group' ),
  * 			keystrokes: [
  * 				{
  * 					label: t( 'Keystroke label 1' ),
  * 					keystroke: 'Ctrl+Shift+N'
  * 				},
  * 				{
  * 					label: t( 'Keystroke label 2' ),
  * 					keystroke: 'Ctrl+Shift+M'
  * 				}
  * 			]
  * 		} );
  * 	}
  * }
  * ```
  *
  * See {@link #keystrokeInfos}, {@link #addKeystrokeInfoCategory}, and {@link #addKeystrokeInfos}.
  */
  addKeystrokeInfoGroup({ categoryId: t = Kh, id: e, label: n, keystrokes: r }) {
    const s = this.keystrokeInfos.get(t);
    if (!s)
      throw new T("accessibility-unknown-keystroke-info-category", this._editor, {
        groupId: e,
        categoryId: t
      });
    s.groups.set(e, {
      id: e,
      label: n,
      keystrokes: r || []
    });
  }
  /**
  * Adds information about keystrokes to the {@link #keystrokeInfos keystroke information database}.
  *
  * Keystrokes without specified `groupId` or `categoryId` go to the `'common'` group in the `'contentEditing'` category (default).
  *
  * To add a keystroke brought by your plugin (using default group and category), use the following code:
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfos( {
  * 			keystrokes: [
  * 				{
  * 					label: t( 'Keystroke label' ),
  * 					keystroke: 'CTRL+B'
  * 				}
  * 			]
  * 		} );
  * 	}
  * }
  * ```
  * To add a keystroke in a specific existing `'widget'` group in the default `'contentEditing'` category:
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfos( {
  * 			// Add a keystroke to the existing "widget" group.
  * 			groupId: 'widget',
  * 			keystrokes: [
  * 				{
  * 					label: t( 'A an action on a selected widget' ),
  * 					keystroke: 'Ctrl+D',
  * 				}
  * 			]
  * 		} );
  * 	}
  * }
  * ```
  *
  * To add a keystroke to another existing category (using default group):
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfos( {
  * 			// Add keystrokes to the "navigation" category (one of defaults).
  * 			categoryId: 'navigation',
  * 			keystrokes: [
  * 				{
  * 					label: t( 'Keystroke label' ),
  * 					keystroke: 'CTRL+B'
  * 				}
  * 			]
  * 		} );
  * 	}
  * }
  * ```
  *
  * See {@link #keystrokeInfos}, {@link #addKeystrokeInfoGroup}, and {@link #addKeystrokeInfoCategory}.
  */
  addKeystrokeInfos({ categoryId: t = Kh, groupId: e = hv, keystrokes: n }) {
    if (!this.keystrokeInfos.has(t))
      throw new T("accessibility-unknown-keystroke-info-category", this._editor, {
        categoryId: t,
        keystrokes: n
      });
    const r = this.keystrokeInfos.get(t);
    if (!r.groups.has(e))
      throw new T("accessibility-unknown-keystroke-info-group", this._editor, {
        groupId: e,
        categoryId: t,
        keystrokes: n
      });
    r.groups.get(e).keystrokes.push(...n);
  }
};
class Oo extends (/* @__PURE__ */ st()) {
  /**
  * Creates a new instance of the editor class.
  *
  * Usually, not to be used directly. See the static {@link module:core/editor/editor~Editor.create `create()`} method.
  *
  * @param config The editor configuration.
  */
  constructor(e = {}) {
    super();
    /**
    * A namespace for the accessibility features of the editor.
    */
    h(this, "accessibility");
    /**
    * Commands registered to the editor.
    *
    * Use the shorthand {@link #execute `editor.execute()`} method to execute commands:
    *
    * ```ts
    * // Execute the bold command:
    * editor.execute( 'bold' );
    *
    * // Check the state of the bold command:
    * editor.commands.get( 'bold' ).value;
    * ```
    */
    h(this, "commands");
    /**
    * Stores all configurations specific to this editor instance.
    *
    * ```ts
    * editor.config.get( 'image.toolbar' );
    * // -> [ 'imageStyle:block', 'imageStyle:side', '|', 'toggleImageCaption', 'imageTextAlternative' ]
    * ```
    */
    h(this, "config");
    /**
    * Conversion manager through which you can register model-to-view and view-to-model converters.
    *
    * See the {@link module:engine/conversion/conversion~Conversion} documentation to learn how to add converters.
    */
    h(this, "conversion");
    /**
    * The {@link module:engine/controller/datacontroller~DataController data controller}.
    * Used e.g. for setting and retrieving the editor data.
    */
    h(this, "data");
    /**
    * The {@link module:engine/controller/editingcontroller~EditingController editing controller}.
    * Controls user input and rendering the content for editing.
    */
    h(this, "editing");
    /**
    * The locale instance.
    */
    h(this, "locale");
    /**
    * The editor's model.
    *
    * The central point of the editor's abstract data model.
    */
    h(this, "model");
    /**
    * The plugins loaded and in use by this editor instance.
    *
    * ```ts
    * editor.plugins.get( 'ClipboardPipeline' ); // -> An instance of the clipboard pipeline plugin.
    * ```
    */
    h(this, "plugins");
    /**
    * An instance of the {@link module:core/editingkeystrokehandler~EditingKeystrokeHandler}.
    *
    * It allows setting simple keystrokes:
    *
    * ```ts
    * // Execute the bold command on Ctrl+E:
    * editor.keystrokes.set( 'Ctrl+E', 'bold' );
    *
    * // Execute your own callback:
    * editor.keystrokes.set( 'Ctrl+E', ( data, cancel ) => {
    * 	console.log( data.keyCode );
    *
    * 	// Prevent the default (native) action and stop the underlying keydown event
    * 	// so no other editor feature will interfere.
    * 	cancel();
    * } );
    * ```
    *
    * Note: Certain typing-oriented keystrokes (like <kbd>Backspace</kbd> or <kbd>Enter</kbd>) are handled
    * by a low-level mechanism and trying to listen to them via the keystroke handler will not work reliably.
    * To handle these specific keystrokes, see the events fired by the
    * {@link module:engine/view/document~Document editing view document} (`editor.editing.view.document`).
    */
    h(this, "keystrokes");
    /**
    * Shorthand for {@link module:utils/locale~Locale#t}.
    *
    * @see module:utils/locale~Locale#t
    */
    h(this, "t");
    /**
    * The editor context.
    * When it is not provided through the configuration, the editor creates it.
    */
    h(this, "_context");
    /**
    * A set of lock IDs for the {@link #isReadOnly} getter.
    */
    h(this, "_readOnlyLocks");
    if ("sanitizeHtml" in e)
      throw new T("editor-config-sanitizehtml-not-supported");
    const n = this.constructor, { translations: r, ...s } = n.defaultConfig || {}, { translations: o = r, ...a } = e, l = e.language || s.language;
    this._context = e.context || new dv({
      language: l,
      translations: o
    }), this._context._addEditor(this, !e.context);
    const c = Array.from(n.builtinPlugins || []);
    this.config = new p2(a, s), this.config.define("plugins", c), this.config.define(this._context._getEditorConfig()), this.plugins = new AC(this, c, this._context.plugins), this.locale = this._context.locale, this.t = this.locale.t, this._readOnlyLocks = /* @__PURE__ */ new Set(), this.commands = new s9(), this.set("state", "initializing"), this.once("ready", () => this.state = "ready", {
      priority: "high"
    }), this.once("destroy", () => this.state = "destroyed", {
      priority: "high"
    }), this.model = new k8(), this.on("change:isReadOnly", () => {
      this.model.document.isReadOnly = this.isReadOnly;
    });
    const u = new u6();
    this.data = new V7(this.model, u), this.editing = new c7(this.model, u), this.editing.view.document.bind("isReadOnly").to(this), this.conversion = new D7([
      this.editing.downcastDispatcher,
      this.data.downcastDispatcher
    ], this.data.upcastDispatcher), this.conversion.addAlias("dataDowncast", this.data.downcastDispatcher), this.conversion.addAlias("editingDowncast", this.editing.downcastDispatcher), this.keystrokes = new o9(this), this.keystrokes.listenTo(this.editing.view.document), this.accessibility = new a9(this);
  }
  /**
  * Defines whether the editor is in the read-only mode.
  *
  * In read-only mode the editor {@link #commands commands} are disabled so it is not possible
  * to modify the document by using them. Also, the editable element(s) become non-editable.
  *
  * In order to make the editor read-only, you need to call the {@link #enableReadOnlyMode} method:
  *
  * ```ts
  * editor.enableReadOnlyMode( 'feature-id' );
  * ```
  *
     * Later, to turn off the read-only mode, call {@link #disableReadOnlyMode}:
  *
  * ```ts
  * editor.disableReadOnlyMode( 'feature-id' );
  * ```
  *
  * @readonly
  * @observable
  */
  get isReadOnly() {
    return this._readOnlyLocks.size > 0;
  }
  set isReadOnly(e) {
    throw new T("editor-isreadonly-has-no-setter");
  }
  /**
  * Turns on the read-only mode in the editor.
  *
  * Editor can be switched to or out of the read-only mode by many features, under various circumstances. The editor supports locking
  * mechanism for the read-only mode. It enables easy control over the read-only mode when many features wants to turn it on or off at
  * the same time, without conflicting with each other. It guarantees that you will not make the editor editable accidentally (which
  * could lead to errors).
  *
  * Each read-only mode request is identified by a unique id (also called "lock"). If multiple plugins requested to turn on the
  * read-only mode, then, the editor will become editable only after all these plugins turn the read-only mode off (using the same ids).
  *
  * Note, that you cannot force the editor to disable the read-only mode if other plugins set it.
  *
  * After the first `enableReadOnlyMode()` call, the {@link #isReadOnly `isReadOnly` property} will be set to `true`:
  *
  * ```ts
  * editor.isReadOnly; // `false`.
  * editor.enableReadOnlyMode( 'my-feature-id' );
  * editor.isReadOnly; // `true`.
  * ```
  *
  * You can turn off the read-only mode ("clear the lock") using the {@link #disableReadOnlyMode `disableReadOnlyMode()`} method:
  *
  * ```ts
  * editor.enableReadOnlyMode( 'my-feature-id' );
  * // ...
  * editor.disableReadOnlyMode( 'my-feature-id' );
  * editor.isReadOnly; // `false`.
  * ```
  *
  * All "locks" need to be removed to enable editing:
  *
  * ```ts
  * editor.enableReadOnlyMode( 'my-feature-id' );
  * editor.enableReadOnlyMode( 'my-other-feature-id' );
  * // ...
  * editor.disableReadOnlyMode( 'my-feature-id' );
  * editor.isReadOnly; // `true`.
  * editor.disableReadOnlyMode( 'my-other-feature-id' );
  * editor.isReadOnly; // `false`.
  * ```
  *
  * @param lockId A unique ID for setting the editor to the read-only state.
  */
  enableReadOnlyMode(e) {
    if (typeof e != "string" && typeof e != "symbol")
      throw new T("editor-read-only-lock-id-invalid", null, {
        lockId: e
      });
    this._readOnlyLocks.has(e) || (this._readOnlyLocks.add(e), this._readOnlyLocks.size === 1 && this.fire("change:isReadOnly", "isReadOnly", !0, !1));
  }
  /**
  * Removes the read-only lock from the editor with given lock ID.
  *
  * When no lock is present on the editor anymore, then the {@link #isReadOnly `isReadOnly` property} will be set to `false`.
  *
  * @param lockId The lock ID for setting the editor to the read-only state.
  */
  disableReadOnlyMode(e) {
    if (typeof e != "string" && typeof e != "symbol")
      throw new T("editor-read-only-lock-id-invalid", null, {
        lockId: e
      });
    this._readOnlyLocks.has(e) && (this._readOnlyLocks.delete(e), this._readOnlyLocks.size === 0 && this.fire("change:isReadOnly", "isReadOnly", !1, !0));
  }
  /**
  * Sets the data in the editor.
  *
  * ```ts
  * editor.setData( '<p>This is editor!</p>' );
  * ```
  *
  * If your editor implementation uses multiple roots, you should pass an object with keys corresponding
  * to the editor root names and values equal to the data that should be set in each root:
  *
  * ```ts
  * editor.setData( {
  *     header: '<p>Content for header part.</p>',
  *     content: '<p>Content for main part.</p>',
  *     footer: '<p>Content for footer part.</p>'
  * } );
  * ```
  *
  * By default the editor accepts HTML. This can be controlled by injecting a different data processor.
  * See the {@glink features/markdown Markdown output} guide for more details.
  *
  * @param data Input data.
  */
  setData(e) {
    this.data.set(e);
  }
  /**
  * Gets the data from the editor.
  *
  * ```ts
  * editor.getData(); // -> '<p>This is editor!</p>'
  * ```
  *
  * If your editor implementation uses multiple roots, you should pass root name as one of the options:
  *
  * ```ts
  * editor.getData( { rootName: 'header' } ); // -> '<p>Content for header part.</p>'
  * ```
  *
  * By default, the editor outputs HTML. This can be controlled by injecting a different data processor.
  * See the {@glink features/markdown Markdown output} guide for more details.
  *
  * A warning is logged when you try to retrieve data for a detached root, as most probably this is a mistake. A detached root should
  * be treated like it is removed, and you should not save its data. Note, that the detached root data is always an empty string.
  *
  * @param options Additional configuration for the retrieved data.
  * Editor features may introduce more configuration options that can be set through this parameter.
  * @param options.rootName Root name. Defaults to `'main'`.
  * @param options.trim Whether returned data should be trimmed. This option is set to `'empty'` by default,
  * which means that whenever editor content is considered empty, an empty string is returned. To turn off trimming
  * use `'none'`. In such cases exact content will be returned (for example `'<p>&nbsp;</p>'` for an empty editor).
  * @returns Output data.
  */
  getData(e) {
    return this.data.get(e);
  }
  /**
  * Loads and initializes plugins specified in the configuration.
  *
  * @returns A promise which resolves once the initialization is completed, providing an array of loaded plugins.
  */
  initPlugins() {
    const e = this.config, n = e.get("plugins"), r = e.get("removePlugins") || [], s = e.get("extraPlugins") || [], o = e.get("substitutePlugins") || [];
    return this.plugins.init(n.concat(s), r, o);
  }
  /**
  * Destroys the editor instance, releasing all resources used by it.
  *
  * **Note** The editor cannot be destroyed during the initialization phase so if it is called
  * while the editor {@link #state is being initialized}, it will wait for the editor initialization before destroying it.
  *
  * @fires destroy
  * @returns A promise that resolves once the editor instance is fully destroyed.
  */
  destroy() {
    let e = Promise.resolve();
    return this.state == "initializing" && (e = new Promise((n) => this.once("ready", n))), e.then(() => {
      this.fire("destroy"), this.stopListening(), this.commands.destroy();
    }).then(() => this.plugins.destroy()).then(() => {
      this.model.destroy(), this.data.destroy(), this.editing.destroy(), this.keystrokes.destroy();
    }).then(() => this._context._removeEditor(this));
  }
  /**
  * Executes the specified command with given parameters.
  *
  * Shorthand for:
  *
  * ```ts
  * editor.commands.get( commandName ).execute( ... );
  * ```
  *
  * @param commandName The name of the command to execute.
  * @param commandParams Command parameters.
  * @returns The value returned by the {@link module:core/commandcollection~CommandCollection#execute `commands.execute()`}.
  */
  execute(e, ...n) {
    try {
      return this.commands.execute(e, ...n);
    } catch (r) {
      /* istanbul ignore next -- @preserve */
      T.rethrowUnexpectedError(r, this);
    }
  }
  /**
  * Focuses the editor.
  *
  * **Note** To explicitly focus the editing area of the editor, use the
  * {@link module:engine/view/view~View#focus `editor.editing.view.focus()`} method of the editing view.
  *
  * Check out the {@glink framework/deep-dive/ui/focus-tracking#focus-in-the-editor-ui Focus in the editor UI} section
  * of the {@glink framework/deep-dive/ui/focus-tracking Deep dive into focus tracking} guide to learn more.
  */
  focus() {
    this.editing.view.focus();
  }
  /* istanbul ignore next -- @preserve */
  /**
  * Creates and initializes a new editor instance.
  *
  * This is an abstract method. Every editor type needs to implement its own initialization logic.
  *
  * See the `create()` methods of the existing editor types to learn how to use them:
  *
  * * {@link module:editor-classic/classiceditor~ClassicEditor.create `ClassicEditor.create()`}
  * * {@link module:editor-balloon/ballooneditor~BalloonEditor.create `BalloonEditor.create()`}
  * * {@link module:editor-decoupled/decouplededitor~DecoupledEditor.create `DecoupledEditor.create()`}
  * * {@link module:editor-inline/inlineeditor~InlineEditor.create `InlineEditor.create()`}
  */
  static create(...e) {
    throw new Error("This is an abstract method.");
  }
}
/**
* The default configuration which is built into the editor class.
*
* It is used in CKEditor 5 builds to provide the default configuration options which are later used during the editor initialization.
*
* ```ts
* ClassicEditor.defaultConfig = {
* 	foo: 1,
* 	bar: 2
* };
*
* ClassicEditor
* 	.create( sourceElement )
* 	.then( editor => {
* 		editor.config.get( 'foo' ); // -> 1
* 		editor.config.get( 'bar' ); // -> 2
* 	} );
*
* // The default options can be overridden by the configuration passed to create().
* ClassicEditor
* 	.create( sourceElement, { bar: 3 } )
* 	.then( editor => {
* 		editor.config.get( 'foo' ); // -> 1
* 		editor.config.get( 'bar' ); // -> 3
* 	} );
* ```
*
* See also {@link module:core/editor/editor~Editor.builtinPlugins}.
*/
h(Oo, "defaultConfig"), /**
* An array of plugins built into this editor class.
*
* It is used in CKEditor 5 builds to provide a list of plugins which are later automatically initialized
* during the editor initialization.
*
* They will be automatically initialized by the editor, unless listed in `config.removePlugins` and
* unless `config.plugins` is passed.
*
* ```ts
* // Build some plugins into the editor class first.
* ClassicEditor.builtinPlugins = [ FooPlugin, BarPlugin ];
*
* // Normally, you need to define config.plugins, but since ClassicEditor.builtinPlugins was
* // defined, now you can call create() without any configuration.
* ClassicEditor
* 	.create( sourceElement )
* 	.then( editor => {
* 		editor.plugins.get( FooPlugin ); // -> An instance of the Foo plugin.
* 		editor.plugins.get( BarPlugin ); // -> An instance of the Bar plugin.
* 	} );
*
* ClassicEditor
* 	.create( sourceElement, {
* 		// Do not initialize these plugins (note: it is defined by a string):
* 		removePlugins: [ 'Foo' ]
* 	} )
* 	.then( editor => {
* 		editor.plugins.get( FooPlugin ); // -> Undefined.
* 		editor.config.get( BarPlugin ); // -> An instance of the Bar plugin.
* 	} );
*
* ClassicEditor
* 	.create( sourceElement, {
* 		// Load only this plugin. It can also be defined by a string if
* 		// this plugin was built into the editor class.
* 		plugins: [ FooPlugin ]
* 	} )
* 	.then( editor => {
* 		editor.plugins.get( FooPlugin ); // -> An instance of the Foo plugin.
* 		editor.config.get( BarPlugin ); // -> Undefined.
* 	} );
* ```
*
* See also {@link module:core/editor/editor~Editor.defaultConfig}.
*/
h(Oo, "builtinPlugins"), /**
* The {@link module:core/context~Context} class.
*
* Exposed as static editor field for easier access in editor builds.
*/
h(Oo, "Context", dv), /**
* The {@link module:watchdog/editorwatchdog~EditorWatchdog} class.
*
* Exposed as static editor field for easier access in editor builds.
*/
h(Oo, "EditorWatchdog", SC), /**
* The {@link module:watchdog/contextwatchdog~ContextWatchdog} class.
*
* Exposed as static editor field for easier access in editor builds.
*/
h(Oo, "ContextWatchdog", i9);
function l9(i) {
  if (!Ws(i.updateSourceElement))
    throw new T("attachtoform-missing-elementapi-interface", i);
  const t = i.sourceElement;
  if (c9(t) && t.form) {
    let e;
    const n = t.form, r = () => i.updateSourceElement();
    Ws(n.submit) && (e = n.submit, n.submit = () => {
      r(), e.apply(n);
    }), n.addEventListener("submit", r), i.on("destroy", () => {
      n.removeEventListener("submit", r), e && (n.submit = e);
    });
  }
}
function c9(i) {
  return !!i && i.tagName.toLowerCase() === "textarea";
}
function hm(i) {
  class t extends i {
    constructor() {
      super(...arguments);
      h(this, "sourceElement");
    }
    updateSourceElement(r) {
      if (!this.sourceElement)
        throw new T("editor-missing-sourceelement", this);
      const s = this.config.get("updateSourceElementOnDestroy"), o = this.sourceElement instanceof HTMLTextAreaElement;
      if (!s && !o) {
        f1(this.sourceElement, "");
        return;
      }
      const a = typeof r == "string" ? r : this.data.get();
      f1(this.sourceElement, a);
    }
  }
  return t;
}
hm.updateSourceElement = hm(Object).prototype.updateSourceElement;
class ea extends jd {
  constructor() {
    super(...arguments);
    /**
    * A list of pending actions.
    */
    h(this, "_actions");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PendingActions";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.set("hasAny", !1), this._actions = new Pn({
      idProperty: "_id"
    }), this._actions.delegate("add", "remove").to(this);
  }
  /**
  * Adds an action to the list of pending actions.
  *
  * This method returns an action object with an observable message property.
  * The action object can be later used in the {@link #remove} method. It also allows you to change the message.
  *
  * @param message The action message.
  * @returns An observable object that represents a pending action.
  */
  add(e) {
    if (typeof e != "string")
      throw new T("pendingactions-add-invalid-message", this);
    const n = new (st())();
    return n.set("message", e), this._actions.add(n), this.hasAny = !0, n;
  }
  /**
  * Removes an action from the list of pending actions.
  *
  * @param action An action object.
  */
  remove(e) {
    this._actions.remove(e), this.hasAny = !!this._actions.length;
  }
  /**
  * Returns the first action from the list or null if the list is empty
  *
  * @returns The pending action object.
  */
  get first() {
    return this._actions.get(0);
  }
  /**
  * Iterable interface.
  */
  [Symbol.iterator]() {
    return this._actions[Symbol.iterator]();
  }
}
var u9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.591 10.177 4.243 4.242a1 1 0 0 1-1.415 1.415l-4.242-4.243-4.243 4.243a1 1 0 0 1-1.414-1.415l4.243-4.242L4.52 5.934A1 1 0 0 1 5.934 4.52l4.243 4.243 4.242-4.243a1 1 0 1 1 1.415 1.414l-4.243 4.243z"/></svg>', d9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 16h9a1 1 0 0 1 0 2H2a1 1 0 0 1 0-2z"/><path d="M17 1a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V3a2 2 0 0 1 2-2h14zm0 1.5H3a.5.5 0 0 0-.492.41L2.5 3v9a.5.5 0 0 0 .41.492L3 12.5h14a.5.5 0 0 0 .492-.41L17.5 12V3a.5.5 0 0 0-.41-.492L17 2.5z" fill-opacity=".6"/></svg>', h9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M6.972 16.615a.997.997 0 0 1-.744-.292l-4.596-4.596a1 1 0 1 1 1.414-1.414l3.926 3.926 9.937-9.937a1 1 0 0 1 1.414 1.415L7.717 16.323a.997.997 0 0 1-.745.292z"/></svg>', f9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.333 2 .19 2.263a5.899 5.899 0 0 1 1.458.604L14.714 3.4 16.6 5.286l-1.467 1.733c.263.452.468.942.605 1.46L18 8.666v2.666l-2.263.19a5.899 5.899 0 0 1-.604 1.458l1.467 1.733-1.886 1.886-1.733-1.467a5.899 5.899 0 0 1-1.46.605L11.334 18H8.667l-.19-2.263a5.899 5.899 0 0 1-1.458-.604L5.286 16.6 3.4 14.714l1.467-1.733a5.899 5.899 0 0 1-.604-1.458L2 11.333V8.667l2.262-.189a5.899 5.899 0 0 1 .605-1.459L3.4 5.286 5.286 3.4l1.733 1.467a5.899 5.899 0 0 1 1.46-.605L8.666 2h2.666zM10 6.267a3.733 3.733 0 1 0 0 7.466 3.733 3.733 0 0 0 0-7.466z"/></svg>', m9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10.209 18.717A8.5 8.5 0 1 1 18.686 9.6h-.008l.002.12a3 3 0 0 1-2.866 2.997h-.268l-.046-.002v.002h-4.791a2 2 0 1 0 0 4 1 1 0 1 1-.128 1.992 8.665 8.665 0 0 1-.372.008Zm-3.918-7.01a1.25 1.25 0 1 0-2.415-.648 1.25 1.25 0 0 0 2.415.647ZM5.723 8.18a1.25 1.25 0 1 0 .647-2.414 1.25 1.25 0 0 0-.647 2.414ZM9.76 6.155a1.25 1.25 0 1 0 .647-2.415 1.25 1.25 0 0 0-.647 2.415Zm4.028 1.759a1.25 1.25 0 1 0 .647-2.415 1.25 1.25 0 0 0-.647 2.415Z"/></svg>', g9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m8.636 9.531-2.758 3.94a.5.5 0 0 0 .122.696l3.224 2.284h1.314l2.636-3.736L8.636 9.53zm.288 8.451L5.14 15.396a2 2 0 0 1-.491-2.786l6.673-9.53a2 2 0 0 1 2.785-.49l3.742 2.62a2 2 0 0 1 .491 2.785l-7.269 10.053-2.147-.066z"/><path d="M4 18h5.523v-1H4zm-2 0h1v-1H2z"/></svg>', p9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M11 1a9 9 0 1 1-8.027 13.075l1.128-1.129A7.502 7.502 0 0 0 18.5 10a7.5 7.5 0 1 0-14.962.759l-.745-.746-.76.76A9 9 0 0 1 11 1z"/><path d="M.475 8.17a.75.75 0 0 1 .978.047l.075.082 1.284 1.643 1.681-1.284a.75.75 0 0 1 .978.057l.073.083a.75.75 0 0 1-.057.978l-.083.073-2.27 1.737a.75.75 0 0 1-.973-.052l-.074-.082-1.741-2.23a.75.75 0 0 1 .13-1.052z"/><path d="M11.5 5v4.999l3.196 3.196-1.06 1.06L10.1 10.72l-.1-.113V5z"/></svg>', b9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M5.085 6.22 2.943 4.078a.75.75 0 1 1 1.06-1.06l2.592 2.59A11.094 11.094 0 0 1 10 5.068c4.738 0 8.578 3.101 8.578 5.083 0 1.197-1.401 2.803-3.555 3.887l1.714 1.713a.75.75 0 0 1-.09 1.138.488.488 0 0 1-.15.084.75.75 0 0 1-.821-.16L6.17 7.304c-.258.11-.51.233-.757.365l6.239 6.24-.006.005.78.78c-.388.094-.78.166-1.174.215l-1.11-1.11h.011L4.55 8.197a7.2 7.2 0 0 0-.665.514l-.112.098 4.897 4.897-.005.006 1.276 1.276a10.164 10.164 0 0 1-1.477-.117l-.479-.479-.009.009-4.863-4.863-.022.031a2.563 2.563 0 0 0-.124.2c-.043.077-.08.158-.108.241a.534.534 0 0 0-.028.133.29.29 0 0 0 .008.072.927.927 0 0 0 .082.226c.067.133.145.26.234.379l3.242 3.365.025.01.59.623c-3.265-.918-5.59-3.155-5.59-4.668 0-1.194 1.448-2.838 3.663-3.93zm7.07.531a4.632 4.632 0 0 1 1.108 5.992l.345.344.046-.018a9.313 9.313 0 0 0 2-1.112c.256-.187.5-.392.727-.613.137-.134.27-.277.392-.431.072-.091.141-.185.203-.286.057-.093.107-.19.148-.292a.72.72 0 0 0 .036-.12.29.29 0 0 0 .008-.072.492.492 0 0 0-.028-.133.999.999 0 0 0-.036-.096 2.165 2.165 0 0 0-.071-.145 2.917 2.917 0 0 0-.125-.2 3.592 3.592 0 0 0-.263-.335 5.444 5.444 0 0 0-.53-.523 7.955 7.955 0 0 0-1.054-.768 9.766 9.766 0 0 0-1.879-.891c-.337-.118-.68-.219-1.027-.301zm-2.85.21-.069.002a.508.508 0 0 0-.254.097.496.496 0 0 0-.104.679.498.498 0 0 0 .326.199l.045.005c.091.003.181.003.272.012a2.45 2.45 0 0 1 2.017 1.513c.024.061.043.125.069.185a.494.494 0 0 0 .45.287h.008a.496.496 0 0 0 .35-.158.482.482 0 0 0 .13-.335.638.638 0 0 0-.048-.219 3.379 3.379 0 0 0-.36-.723 3.438 3.438 0 0 0-2.791-1.543l-.028-.001h-.013z"/></svg>', v9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.035 1C2.446 1 2 1.54 2 2.098V10.5h1.5v-8h13v8H18V2.098C18 1.539 17.48 1 16.9 1H3.035Zm10.453 2.61a1.885 1.885 0 0 0-1.442.736 1.89 1.89 0 0 0 1.011 2.976 1.903 1.903 0 0 0 2.253-1.114 1.887 1.887 0 0 0-1.822-2.598ZM7.463 8.163a.611.611 0 0 0-.432.154L5.071 10.5h5.119L7.88 8.348a.628.628 0 0 0-.417-.185Zm6.236 1.059a.62.62 0 0 0-.42.164L12.07 10.5h2.969l-.92-1.113a.618.618 0 0 0-.42-.165ZM.91 11.5a.91.91 0 0 0-.91.912v6.877c0 .505.405.91.91.91h18.178a.91.91 0 0 0 .912-.91v-6.877a.908.908 0 0 0-.912-.912H.91ZM3.668 13h1.947l2.135 5.7H5.898l-.28-.946H3.601l-.278.945H1.516L3.668 13Zm4.947 0h1.801v4.3h2.7v1.4h-4.5V13h-.001Zm4.5 0h5.4v1.4h-1.798v4.3h-1.701v-4.3h-1.9V13h-.001Zm-8.517 1.457-.614 2.059h1.262l-.648-2.059Z"/></svg>', w9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M12.68 13.74h-.001l4.209 4.208a1 1 0 1 0 1.414-1.414l-4.267-4.268a6 6 0 1 0-1.355 1.474ZM13 9a4 4 0 1 1-8 0 4 4 0 0 1 8 0Z"/></svg>', y9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M11.463 5.187a.888.888 0 1 1 1.254 1.255L9.16 10l3.557 3.557a.888.888 0 1 1-1.254 1.255L7.26 10.61a.888.888 0 0 1 .16-1.382l4.043-4.042z"/></svg>', _9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M8.537 14.813a.888.888 0 1 1-1.254-1.255L10.84 10 7.283 6.442a.888.888 0 1 1 1.254-1.255L12.74 9.39a.888.888 0 0 1-.16 1.382l-4.043 4.042z"/></svg>', C9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M6.66 9.118a.693.693 0 0 1 .956.032l3.65 3.411 2.422-2.238a.695.695 0 0 1 .945 0L17.5 13.6V2.5h-15v11.1l4.16-4.482ZM17.8 1c.652 0 1.2.47 1.2 1.1v14.362c0 .64-.532 1.038-1.184 1.038H2.184C1.532 17.5 1 17.103 1 16.462V2.1C1 1.47 1.537 1 2.2 1h15.6Zm-5.655 6a2.128 2.128 0 0 1 .157-2.364A2.133 2.133 0 1 1 12.145 7Z"/></svg>', k9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M1.201 1C.538 1 0 1.47 0 2.1v14.363c0 .64.534 1.037 1.186 1.037h9.494a2.97 2.97 0 0 1-.414-.287 2.998 2.998 0 0 1-1.055-2.03 3.003 3.003 0 0 1 .693-2.185l.383-.455-.02.018-3.65-3.41a.695.695 0 0 0-.957-.034L1.5 13.6V2.5h15v5.535a2.97 2.97 0 0 1 1.412.932l.088.105V2.1c0-.63-.547-1.1-1.2-1.1H1.202Zm11.713 2.803a2.146 2.146 0 0 0-2.049 1.992 2.14 2.14 0 0 0 1.28 2.096 2.13 2.13 0 0 0 2.644-3.11 2.134 2.134 0 0 0-1.875-.978Z"/><path d="M15.522 19.1a.79.79 0 0 0 .79-.79v-5.373l2.059 2.455a.79.79 0 1 0 1.211-1.015l-3.352-3.995a.79.79 0 0 0-.995-.179.784.784 0 0 0-.299.221l-3.35 3.99a.79.79 0 1 0 1.21 1.017l1.936-2.306v5.185c0 .436.353.79.79.79Z"/><path d="M15.522 19.1a.79.79 0 0 0 .79-.79v-5.373l2.059 2.455a.79.79 0 1 0 1.211-1.015l-3.352-3.995a.79.79 0 0 0-.995-.179.784.784 0 0 0-.299.221l-3.35 3.99a.79.79 0 1 0 1.21 1.017l1.936-2.306v5.185c0 .436.353.79.79.79Z"/></svg>', x9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M1.201 1c-.662 0-1.2.47-1.2 1.1v14.248c0 .64.533 1.152 1.185 1.152h6.623v-7.236L6.617 9.15a.694.694 0 0 0-.957-.033L1.602 13.55V2.553l14.798.003V9.7H18V2.1c0-.63-.547-1.1-1.2-1.1H1.202Zm11.723 2.805a2.094 2.094 0 0 0-1.621.832 2.127 2.127 0 0 0 1.136 3.357 2.13 2.13 0 0 0 2.611-1.506 2.133 2.133 0 0 0-.76-2.244 2.13 2.13 0 0 0-1.366-.44Z"/><path clip-rule="evenodd" d="M19.898 12.369v6.187a.844.844 0 0 1-.844.844h-8.719a.844.844 0 0 1-.843-.844v-7.312a.844.844 0 0 1 .843-.844h2.531a.843.843 0 0 1 .597.248l.838.852h4.75c.223 0 .441.114.6.272a.844.844 0 0 1 .247.597Zm-1.52.654-4.377.02-1.1-1.143H11v6h7.4l-.023-4.877Z"/></svg>', E9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M1.201 1C.538 1 0 1.47 0 2.1v14.363c0 .64.534 1.037 1.186 1.037h7.029a5.401 5.401 0 0 1 .615-4.338l.762-1.232-2.975-2.78a.696.696 0 0 0-.957-.033L1.5 13.6V2.5h15v6.023c.449.131.887.32 1.307.573l.058.033c.046.028.09.057.135.086V2.1c0-.63-.547-1.1-1.2-1.1H1.202Zm11.713 2.803a2.15 2.15 0 0 0-1.611.834 2.118 2.118 0 0 0-.438 1.158 2.14 2.14 0 0 0 1.277 2.096 2.132 2.132 0 0 0 2.645-3.11 2.13 2.13 0 0 0-1.873-.978Z"/><path d="M16.63 10.294a3.003 3.003 0 0 0-4.142.887l-.117.177a.647.647 0 0 0-.096.492.664.664 0 0 0 .278.418.7.7 0 0 0 .944-.234 1.741 1.741 0 0 1 2.478-.463 1.869 1.869 0 0 1 .476 2.55.637.637 0 0 0-.071.5.646.646 0 0 0 .309.396.627.627 0 0 0 .869-.19l.027-.041a3.226 3.226 0 0 0-.956-4.492Zm-6.061 3.78-.044.066a3.228 3.228 0 0 0 .82 4.403 3.005 3.005 0 0 0 4.275-.798l.13-.197a.626.626 0 0 0 .092-.475.638.638 0 0 0-.268-.402.713.713 0 0 0-.99.26l-.018.029a1.741 1.741 0 0 1-2.477.461 1.87 1.87 0 0 1-.475-2.55l.029-.047a.647.647 0 0 0 .086-.485.66.66 0 0 0-.275-.408l-.04-.027a.609.609 0 0 0-.845.17Z"/><path d="M15.312 13.925c.24-.36.154-.838-.19-1.067-.346-.23-.82-.124-1.059.236l-1.268 1.907c-.239.36-.153.838.192 1.067.345.23.818.123 1.057-.236l1.268-1.907Z"/></svg>', T9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m9.239 13.938-2.88-1.663a.75.75 0 0 1 .75-1.3L9 12.067V4.75a.75.75 0 1 1 1.5 0v7.318l1.89-1.093a.75.75 0 0 1 .75 1.3l-2.879 1.663a.752.752 0 0 1-.511.187.752.752 0 0 1-.511-.187zM4.25 17a.75.75 0 1 1 0-1.5h10.5a.75.75 0 0 1 0 1.5H4.25z"/></svg>', S9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M9.75 11.875a.752.752 0 0 1 .508.184l2.883 1.666a.75.75 0 0 1-.659 1.344l-.091-.044-1.892-1.093.001 4.318a.75.75 0 1 1-1.5 0v-4.317l-1.89 1.092a.75.75 0 0 1-.75-1.3l2.879-1.663a.752.752 0 0 1 .51-.187zM15.25 9a.75.75 0 1 1 0 1.5H4.75a.75.75 0 1 1 0-1.5h10.5zM9.75.375a.75.75 0 0 1 .75.75v4.318l1.89-1.093.092-.045a.75.75 0 0 1 .659 1.344l-2.883 1.667a.752.752 0 0 1-.508.184.752.752 0 0 1-.511-.187L6.359 5.65a.75.75 0 0 1 .75-1.299L9 5.442V1.125a.75.75 0 0 1 .75-.75z"/></svg>', A9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m10.261 7.062 2.88 1.663a.75.75 0 0 1-.75 1.3L10.5 8.933v7.317a.75.75 0 1 1-1.5 0V8.932l-1.89 1.093a.75.75 0 0 1-.75-1.3l2.879-1.663a.752.752 0 0 1 .511-.187.752.752 0 0 1 .511.187zM15.25 4a.75.75 0 1 1 0 1.5H4.75a.75.75 0 0 1 0-1.5h10.5z"/></svg>', P9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 8c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 4c0 .414.336.75.75.75h9.929a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0-8c0 .414.336.75.75.75h9.929a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75z"/></svg>', I9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 8c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm2.286 4c0 .414.336.75.75.75h9.928a.75.75 0 1 0 0-1.5H5.036a.75.75 0 0 0-.75.75zm0-8c0 .414.336.75.75.75h9.928a.75.75 0 1 0 0-1.5H5.036a.75.75 0 0 0-.75.75z"/></svg>', R9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M18 3.75a.75.75 0 0 1-.75.75H2.75a.75.75 0 1 1 0-1.5h14.5a.75.75 0 0 1 .75.75zm0 8a.75.75 0 0 1-.75.75H2.75a.75.75 0 1 1 0-1.5h14.5a.75.75 0 0 1 .75.75zm0 4a.75.75 0 0 1-.75.75H7.321a.75.75 0 1 1 0-1.5h9.929a.75.75 0 0 1 .75.75zm0-8a.75.75 0 0 1-.75.75H7.321a.75.75 0 1 1 0-1.5h9.929a.75.75 0 0 1 .75.75z"/></svg>', O9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 8c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 4c0 .414.336.75.75.75h9.929a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0-8c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75z"/></svg>', V9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2z"/><path d="M12.003 7v5.5a1 1 0 0 1-1 1H2.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H3.5V12h6.997V7.5z"/></svg>', M9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2z"/><path d="M15.003 7v5.5a1 1 0 0 1-1 1H5.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H6.5V12h6.997V7.5z"/></svg>', D9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2z"/><path d="M18.003 7v5.5a1 1 0 0 1-1 1H8.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H9.5V12h6.997V7.5z"/></svg>', L9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2z"/><path d="M18 7v5.5a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h14a1 1 0 0 1 1 1zm-1.505.5H3.504V12h12.991V7.5z"/></svg>', N9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm11.5 9H18v1.5h-4.5zM2 15h16v1.5H2z"/><path d="M12.003 7v5.5a1 1 0 0 1-1 1H2.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H3.5V12h6.997V7.5z"/></svg>', B9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm11.5 9H18v1.5h-4.5zm0-3H18v1.5h-4.5zm0-3H18v1.5h-4.5zM2 15h16v1.5H2z"/><path d="M12.003 7v5.5a1 1 0 0 1-1 1H2.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H3.5V12h6.997V7.5z"/></svg>', F9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2zm0-9h5v1.5H2zm0 3h5v1.5H2zm0 3h5v1.5H2z"/><path d="M18.003 7v5.5a1 1 0 0 1-1 1H8.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H9.5V12h6.997V7.5z"/></svg>', $9 = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20"><path d="M2.5 17v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zM1 15.5v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm0-2v1h-1v-1h1zm-19 0v1H0v-1h1zM14.5 2v1h-1V2h1zm2 0v1h-1V2h1zm2 0v1h-1V2h1zm-8 0v1h-1V2h1zm-2 0v1h-1V2h1zm-2 0v1h-1V2h1zm-2 0v1h-1V2h1zm8 0v1h-1V2h1zm-10 0v1h-1V2h1z"/><path d="M18.095 2H1.905C.853 2 0 2.895 0 4v12c0 1.105.853 2 1.905 2h16.19C19.147 18 20 17.105 20 16V4c0-1.105-.853-2-1.905-2zm0 1.5c.263 0 .476.224.476.5v12c0 .276-.213.5-.476.5H1.905a.489.489 0 0 1-.476-.5V4c0-.276.213-.5.476-.5h16.19z"/></svg>', z9 = '<svg xmlns="http://www.w3.org/2000/svg" xmlns:v="https://vecta.io/nano" viewBox="0 0 20 20"><path d="M.95 1.43a.95.95 0 0 0-.95.95v3.1a.95.95 0 0 0 .95.95h.75v6.3H.95a.95.95 0 0 0-.95.95v3.1a.95.95 0 0 0 .95.95h3.1a.95.95 0 0 0 .95-.95v-.65h1.932l1.539-1.5H5v-.95a.95.95 0 0 0-.95-.95H3.2v-6.3h.85A.95.95 0 0 0 5 5.48v-.55h10v.55a.95.95 0 0 0 .95.95h3.1a.95.95 0 0 0 .95-.95v-3.1a.95.95 0 0 0-.95-.95h-3.1a.95.95 0 0 0-.95.95v1.05H5V2.38a.95.95 0 0 0-.95-.95H.95zm.55 3.5v-2h2v2h-2zm0 9.3v2h2v-2h-2zm15-11.3v2h2v-2h-2z"/><path d="M8.139 20.004v-2.388l7.045-7.048 2.391 2.391-7.046 7.046h-2.39zm11.421-9.101a.64.64 0 0 1-.138.206l-1.165 1.168-2.391-2.391 1.167-1.163a.63.63 0 0 1 .206-.138.635.635 0 0 1 .243-.049.63.63 0 0 1 .449.187l1.491 1.488c.059.059.108.129.138.206s.049.16.049.243a.6.6 0 0 1-.049.243z"/></svg>', H9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 16.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1ZM1 15v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 13v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 11v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 9v1H0V9h1Zm19 0v1h-1V9h1ZM1 7v1H0V7h1Zm19 0v1h-1V7h1ZM1 5v1H0V5h1Zm19 0v1h-1V5h1Zm0-2v1h-1V3h1ZM1 3v1H0V3h1Zm13.5-1.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm-8 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm8 0v1h-1v-1h1Zm-10 0v1h-1v-1h1Z"/><path d="M13 5.5H2a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h11a2 2 0 0 0 2-2v-8a2 2 0 0 0-2-2ZM13 7a.5.5 0 0 1 .5.5v8a.5.5 0 0 1-.5.5H2a.5.5 0 0 1-.5-.5v-8A.5.5 0 0 1 2 7h11Z"/></svg>', j9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 16.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1ZM1 15v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 13v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 11v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 9v1H0V9h1Zm19 0v1h-1V9h1ZM1 7v1H0V7h1Zm19 0v1h-1V7h1ZM1 5v1H0V5h1Zm19 0v1h-1V5h1Zm0-2v1h-1V3h1ZM1 3v1H0V3h1Zm13.5-1.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm-8 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm8 0v1h-1v-1h1Zm-10 0v1h-1v-1h1Z"/><path d="M7 9.5H2a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h5a2 2 0 0 0 2-2v-4a2 2 0 0 0-2-2ZM7 11a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5H2a.5.5 0 0 1-.5-.5v-4A.5.5 0 0 1 2 11h5Z"/></svg>', W9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 16.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1ZM1 15v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 13v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 11v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 9v1H0V9h1Zm19 0v1h-1V9h1ZM1 7v1H0V7h1Zm19 0v1h-1V7h1ZM1 5v1H0V5h1Zm19 0v1h-1V5h1Zm0-2v1h-1V3h1ZM1 3v1H0V3h1Zm13.5-1.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm-8 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm8 0v1h-1v-1h1Zm-10 0v1h-1v-1h1Z"/><path d="M10 7.5H2a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2v-6a2 2 0 0 0-2-2ZM10 9a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-.5.5H2a.5.5 0 0 1-.5-.5v-6A.5.5 0 0 1 2 9h8Z"/></svg>', U9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m7.3 17.37-.061.088a1.518 1.518 0 0 1-.934.535l-4.178.663-.806-4.153a1.495 1.495 0 0 1 .187-1.058l.056-.086L8.77 2.639c.958-1.351 2.803-1.076 4.296-.03 1.497 1.047 2.387 2.693 1.433 4.055L7.3 17.37zM9.14 4.728l-5.545 8.346 3.277 2.294 5.544-8.346L9.14 4.728zM6.07 16.512l-3.276-2.295.53 2.73 2.746-.435zM9.994 3.506 13.271 5.8c.316-.452-.16-1.333-1.065-1.966-.905-.634-1.895-.78-2.212-.328zM8 18.5 9.375 17H19v1.5H8z"/></svg>', q9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M6.999 2H15a1 1 0 0 1 0 2h-1.004v13a1 1 0 1 1-2 0V4H8.999v13a1 1 0 1 1-2 0v-7A4 4 0 0 1 3 6a4 4 0 0 1 3.999-4z"/></svg>', G9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 10.423a6.5 6.5 0 0 1 6.056-6.408l.038.67C6.448 5.423 5.354 7.663 5.22 10H9c.552 0 .5.432.5.986v4.511c0 .554-.448.503-1 .503h-5c-.552 0-.5-.449-.5-1.003v-4.574zm8 0a6.5 6.5 0 0 1 6.056-6.408l.038.67c-2.646.739-3.74 2.979-3.873 5.315H17c.552 0 .5.432.5.986v4.511c0 .554-.448.503-1 .503h-5c-.552 0-.5-.449-.5-1.003v-4.574z"/></svg>', K9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><circle cx="9.5" cy="4.5" r="1.5"/><circle cx="9.5" cy="10.5" r="1.5"/><circle cx="9.5" cy="16.5" r="1.5"/></svg>', Z9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M5 3.25a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M12 3.25a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M5 10a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M12 10a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M5 16.75a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M12 16.75a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/></svg>', Y9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10.187 17H5.773c-.637 0-1.092-.138-1.364-.415-.273-.277-.409-.718-.409-1.323V4.738c0-.617.14-1.062.419-1.332.279-.27.73-.406 1.354-.406h4.68c.69 0 1.288.041 1.793.124.506.083.96.242 1.36.478.341.197.644.447.906.75a3.262 3.262 0 0 1 .808 2.162c0 1.401-.722 2.426-2.167 3.075C15.05 10.175 16 11.315 16 13.01a3.756 3.756 0 0 1-2.296 3.504 6.1 6.1 0 0 1-1.517.377c-.571.073-1.238.11-2 .11zm-.217-6.217H7v4.087h3.069c1.977 0 2.965-.69 2.965-2.072 0-.707-.256-1.22-.768-1.537-.512-.319-1.277-.478-2.296-.478zM7 5.13v3.619h2.606c.729 0 1.292-.067 1.69-.2a1.6 1.6 0 0 0 .91-.765c.165-.267.247-.566.247-.897 0-.707-.26-1.176-.778-1.409-.519-.232-1.31-.348-2.375-.348H7z"/></svg>', J9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10.5 5.5H7v5h3.5a2.5 2.5 0 1 0 0-5zM5 3h6.5v.025a5 5 0 0 1 0 9.95V13H7v4a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1z"/></svg>', X9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10 2a1 1 0 0 0-1 1v6H3a1 1 0 1 0 0 2h6v6a1 1 0 1 0 2 0v-6h6a1 1 0 1 0 0-2h-6V3a1 1 0 0 0-1-1Z"/></svg>', Q9 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M9.816 11.5 7.038 4.785 4.261 11.5h5.555Zm.62 1.5H3.641l-1.666 4.028H.312l5.789-14h1.875l5.789 14h-1.663L10.436 13Z"/><path d="m12.09 17-.534-1.292.848-1.971.545 1.319L12.113 17h-.023Zm1.142-5.187.545 1.319L15.5 9.13l1.858 4.316h-3.45l.398.965h3.467L18.887 17H20l-3.873-9h-1.254l-1.641 3.813Z"/></svg>', eV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M19 4.5 14 0H3v12.673l.868-1.041c.185-.222.4-.402.632-.54V1.5h8v5h5v7.626a2.24 2.24 0 0 1 1.5.822V4.5ZM14 5V2l3.3 3H14Zm-3.692 12.5c.062.105.133.206.213.303L11.52 19H8v-.876a2.243 2.243 0 0 0 1.82-.624h.488Zm7.518-.657a.75.75 0 0 0-1.152-.96L15.5 17.29V12H14v5.29l-1.174-1.408a.75.75 0 0 0-1.152.96l2.346 2.816a.95.95 0 0 0 1.46 0l2.346-2.815Zm-15.056-.38a.75.75 0 0 1-.096-1.056l2.346-2.815a.95.95 0 0 1 1.46 0l2.346 2.815a.75.75 0 1 1-1.152.96L6.5 14.96V20H5v-5.04l-1.174 1.408a.75.75 0 0 1-1.056.096Z"/></svg>', tV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m14.958 9.367-2.189 1.837a.75.75 0 0 0 .965 1.149l3.788-3.18a.747.747 0 0 0 .21-.284.75.75 0 0 0-.17-.945L13.77 4.762a.75.75 0 1 0-.964 1.15l2.331 1.955H6.22A.75.75 0 0 0 6 7.9a4 4 0 1 0 1.477 7.718l-.344-1.489A2.5 2.5 0 1 1 6.039 9.4l-.008-.032h8.927z"/></svg>', nV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m5.042 9.367 2.189 1.837a.75.75 0 0 1-.965 1.149l-3.788-3.18a.747.747 0 0 1-.21-.284.75.75 0 0 1 .17-.945L6.23 4.762a.75.75 0 1 1 .964 1.15L4.863 7.866h8.917A.75.75 0 0 1 14 7.9a4 4 0 1 1-1.477 7.718l.344-1.489a2.5 2.5 0 1 0 1.094-4.73l.008-.032H5.042z"/></svg>', iV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M7 5.75c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zm-6 0C1 4.784 1.777 4 2.75 4c.966 0 1.75.777 1.75 1.75 0 .966-.777 1.75-1.75 1.75C1.784 7.5 1 6.723 1 5.75zm6 9c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zm-6 0c0-.966.777-1.75 1.75-1.75.966 0 1.75.777 1.75 1.75 0 .966-.777 1.75-1.75 1.75-.966 0-1.75-.777-1.75-1.75z"/></svg>', rV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M7 5.75c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zM3.5 3v5H2V3.7H1v-1h2.5V3zM.343 17.857l2.59-3.257H2.92a.6.6 0 1 0-1.04 0H.302a2 2 0 1 1 3.995 0h-.001c-.048.405-.16.734-.333.988-.175.254-.59.692-1.244 1.312H4.3v1h-4l.043-.043zM7 14.75a.75.75 0 0 1 .75-.75h9.5a.75.75 0 1 1 0 1.5h-9.5a.75.75 0 0 1-.75-.75z"/></svg>', sV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m2.315 14.705 2.224-2.24a.689.689 0 0 1 .963 0 .664.664 0 0 1 0 .949L2.865 16.07a.682.682 0 0 1-.112.089.647.647 0 0 1-.852-.051L.688 14.886a.635.635 0 0 1 0-.903.647.647 0 0 1 .91 0l.717.722zm5.185.045a.75.75 0 0 1 .75-.75h9.5a.75.75 0 1 1 0 1.5h-9.5a.75.75 0 0 1-.75-.75zM2.329 5.745l2.21-2.226a.689.689 0 0 1 .963 0 .664.664 0 0 1 0 .95L2.865 7.125a.685.685 0 0 1-.496.196.644.644 0 0 1-.468-.187L.688 5.912a.635.635 0 0 1 0-.903.647.647 0 0 1 .91 0l.73.736zM7.5 5.75A.75.75 0 0 1 8.25 5h9.5a.75.75 0 1 1 0 1.5h-9.5a.75.75 0 0 1-.75-.75z"/></svg>', oV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M12.87 12.61a.75.75 0 0 1-.089.976l-.085.07-3.154 2.254 3.412 2.414a.75.75 0 0 1 .237.95l-.057.095a.75.75 0 0 1-.95.237l-.096-.058-4.272-3.022-.003-1.223 4.01-2.867a.75.75 0 0 1 1.047.174zm2.795-.231.095.057 4.011 2.867-.003 1.223-4.272 3.022-.095.058a.75.75 0 0 1-.88-.151l-.07-.086-.058-.095a.75.75 0 0 1 .15-.88l.087-.07 3.412-2.414-3.154-2.253-.085-.071a.75.75 0 0 1 .862-1.207zM16 0a2 2 0 0 1 2 2v9.354l-.663-.492-.837-.001V2a.5.5 0 0 0-.5-.5H2a.5.5 0 0 0-.5.5v15a.5.5 0 0 0 .5.5h3.118L7.156 19H2a2 2 0 0 1-2-2V2a2 2 0 0 1 2-2h14zM5.009 15l.003 1H3v-1h2.009zm2.188-2-1.471 1H5v-1h2.197zM10 11v.095L8.668 12H7v-1h3zm4-2v1H7V9h7zm0-2v1H7V7h7zm-4-2v1H5V5h5zM6 3v1H3V3h3z"/></svg>', aV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M11.627 16.5zm5.873-.196zm0-7.001V8h-13v8.5h4.341c.191.54.457 1.044.785 1.5H2a1.5 1.5 0 0 1-1.5-1.5v-13A1.5 1.5 0 0 1 2 2h4.5a1.5 1.5 0 0 1 1.06.44L9.122 4H16a1.5 1.5 0 0 1 1.5 1.5v1A1.5 1.5 0 0 1 19 8v2.531a6.027 6.027 0 0 0-1.5-1.228zM16 6.5v-1H8.5l-2-2H2v13h1V8a1.5 1.5 0 0 1 1.5-1.5H16z"/><path d="M14.5 19.5a5 5 0 1 1 0-10 5 5 0 0 1 0 10zM15 14v-2h-1v2h-2v1h2v2h1v-2h2v-1h-2z"/></svg>', lV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M19 9v10h-2v-8h-2V9h4zM4 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1H10a1 1 0 0 1-1-1V11H4v4.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1H3a1 1 0 0 1 1 1v4.5z"/></svg>', cV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1H9a1 1 0 0 1-1-1V11H3v4.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1H2a1 1 0 0 1 1 1v4.5zm16.076 8.343V18.5h-6.252c.067-.626.27-1.22.61-1.78.338-.561 1.006-1.305 2.005-2.232.804-.749 1.297-1.257 1.479-1.523.245-.368.368-.732.368-1.092 0-.398-.107-.703-.32-.917-.214-.214-.51-.32-.886-.32-.372 0-.669.111-.889.336-.22.224-.347.596-.38 1.117l-1.778-.178c.106-.982.438-1.686.997-2.114.558-.427 1.257-.64 2.095-.64.918 0 1.64.247 2.164.742.525.495.787 1.11.787 1.847 0 .419-.075.818-.225 1.197-.15.378-.388.775-.714 1.19-.216.275-.605.67-1.168 1.187-.563.516-.92.859-1.07 1.028a3.11 3.11 0 0 0-.365.495h3.542z"/></svg>', uV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1H9a1 1 0 0 1-1-1V11H3v4.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1H2a1 1 0 0 1 1 1v4.5zm9.989 7.53 1.726-.209c.055.44.203.777.445 1.01.24.232.533.349.876.349.368 0 .678-.14.93-.42.251-.279.377-.655.377-1.13 0-.448-.12-.803-.362-1.066a1.153 1.153 0 0 0-.882-.393c-.228 0-.501.044-.819.133l.197-1.453c.482.012.85-.092 1.105-.315.253-.222.38-.517.38-.885 0-.313-.093-.563-.279-.75-.186-.185-.434-.278-.743-.278a1.07 1.07 0 0 0-.78.317c-.216.212-.347.52-.394.927l-1.644-.28c.114-.562.287-1.012.517-1.348.231-.337.553-.601.965-.794a3.24 3.24 0 0 1 1.387-.289c.876 0 1.579.28 2.108.838.436.457.653.973.653 1.549 0 .817-.446 1.468-1.339 1.955.533.114.96.37 1.28.768.319.398.478.878.478 1.441 0 .817-.298 1.513-.895 2.088-.596.576-1.339.864-2.228.864-.842 0-1.54-.243-2.094-.727-.555-.485-.876-1.118-.965-1.901z"/></svg>', dV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.5 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V11h-5v4.5a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v4.5zm13.55 10v-1.873h-3.81v-1.561l4.037-5.91h1.498v5.904h1.156v1.567h-1.156V18.5H17.05zm0-3.44v-3.18l-2.14 3.18h2.14z"/></svg>', hV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.5 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V11h-5v4.5a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v4.5zm9.578 7.607 1.777-.184c.05.402.201.72.45.955a1.223 1.223 0 0 0 1.81-.101c.258-.303.387-.759.387-1.368 0-.572-.128-1-.384-1.286-.256-.285-.59-.428-1-.428-.512 0-.971.226-1.377.679l-1.448-.21.915-4.843h4.716v1.67H15.56l-.28 1.58a2.697 2.697 0 0 1 1.219-.298 2.68 2.68 0 0 1 2.012.863c.55.576.825 1.323.825 2.241a3.36 3.36 0 0 1-.666 2.05c-.605.821-1.445 1.232-2.52 1.232-.86 0-1.56-.23-2.101-.692-.542-.461-.866-1.081-.971-1.86z"/></svg>', fV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.5 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V11h-5v4.5a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v4.5zm15.595 2.973-1.726.19c-.043-.355-.153-.617-.33-.787-.178-.169-.409-.253-.692-.253-.377 0-.695.169-.956.507-.26.339-.424 1.043-.492 2.114.445-.525.997-.787 1.657-.787.745 0 1.383.284 1.914.85.531.568.797 1.3.797 2.197 0 .952-.28 1.716-.838 2.291-.559.576-1.276.864-2.152.864-.94 0-1.712-.365-2.317-1.095-.605-.73-.908-1.927-.908-3.59 0-1.705.316-2.935.946-3.688.63-.753 1.45-1.13 2.457-1.13.706 0 1.291.198 1.755.594.463.395.758.97.885 1.723zm-4.043 3.891c0 .58.133 1.028.4 1.343.266.315.57.473.914.473.33 0 .605-.13.825-.388.22-.258.33-.68.33-1.27 0-.604-.118-1.047-.355-1.329a1.115 1.115 0 0 0-.89-.422c-.342 0-.632.134-.869.403s-.355.666-.355 1.19z"/></svg>', mV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 9h16v2H2z"/></svg>', gV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M17 0a2 2 0 0 1 2 2v7a1 1 0 0 1 1 1v5a1 1 0 0 1-.883.993l-.118.006L19 17a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2l-.001-1.001-.116-.006A1 1 0 0 1 0 15v-5a1 1 0 0 1 .999-1L1 2a2 2 0 0 1 2-2h14zm.499 15.999h-15L2.5 17a.5.5 0 0 0 .5.5h14a.5.5 0 0 0 .5-.5l-.001-1.001zm-3.478-6.013-.014.014H14v.007l-1.525 1.525-1.46-1.46-.015.013V10h-1v5h1v-3.53l1.428 1.43.048.043.131-.129L14 11.421V15h1v-5h-.965l-.014-.014zM2 10H1v5h1v-2h2v2h1v-5H4v2H2v-2zm7 0H6v1h1v4h1v-4h1v-1zm8 0h-1v5h3v-1h-2v-4zm0-8.5H3a.5.5 0 0 0-.5.5l-.001 6.999h15L17.5 2a.5.5 0 0 0-.5-.5zM10 7v1H4V7h6zm3-2v1H4V5h9zm-3-2v1H4V3h6z"/></svg>', pV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm5 6c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zM2.75 16.5h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 1 0 0 1.5zM1.632 6.95 5.02 9.358a.4.4 0 0 1-.013.661l-3.39 2.207A.4.4 0 0 1 1 11.892V7.275a.4.4 0 0 1 .632-.326z"/></svg>', bV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm5 6c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zM2.75 16.5h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 1 0 0 1.5zm1.618-9.55L.98 9.358a.4.4 0 0 0 .013.661l3.39 2.207A.4.4 0 0 0 5 11.892V7.275a.4.4 0 0 0-.632-.326z"/></svg>', vV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 5.5v3h4v-3H3Zm0 4v3h4v-3H3Zm0 4v3h4v-3H3Zm5 3h4v-3H8v3Zm5 0h4v-3h-4v3Zm4-4v-3h-4v3h4Zm0-4v-3h-4v3h4Zm1.5 8A1.5 1.5 0 0 1 17 18H3a1.5 1.5 0 0 1-1.5-1.5V3c.222-.863 1.068-1.5 2-1.5h13c.932 0 1.778.637 2 1.5v13.5Zm-6.5-4v-3H8v3h4Zm0-4v-3H8v3h4Z"/></svg>';
const ee = {
  bold: Y9,
  cancel: u9,
  caption: d9,
  check: h9,
  cog: f9,
  colorPalette: m9,
  eraser: g9,
  history: p9,
  image: C9,
  imageUpload: k9,
  imageAssetManager: x9,
  imageUrl: E9,
  lowVision: b9,
  textAlternative: v9,
  loupe: w9,
  previousArrow: y9,
  nextArrow: _9,
  importExport: eV,
  paragraph: J9,
  plus: X9,
  text: Q9,
  alignBottom: T9,
  alignMiddle: S9,
  alignTop: A9,
  alignLeft: P9,
  alignCenter: I9,
  alignRight: R9,
  alignJustify: O9,
  objectLeft: B9,
  objectCenter: M9,
  objectRight: F9,
  objectFullWidth: L9,
  objectInline: N9,
  objectBlockLeft: V9,
  objectBlockRight: D9,
  objectSizeCustom: z9,
  objectSizeFull: $9,
  objectSizeLarge: H9,
  objectSizeSmall: j9,
  objectSizeMedium: W9,
  pencil: U9,
  pilcrow: q9,
  quote: G9,
  threeVerticalDots: K9,
  dragIndicator: Z9,
  redo: tV,
  undo: nV,
  bulletedList: iV,
  numberedList: rV,
  todoList: sV,
  codeBlock: oV,
  browseFiles: aV,
  heading1: lV,
  heading2: cV,
  heading3: uV,
  heading4: dV,
  heading5: hV,
  heading6: fV,
  horizontalLine: mV,
  html: gV,
  indent: pV,
  outdent: bV,
  table: vV
};
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class wV extends (/* @__PURE__ */ st()) {
  /**
  * Creates an instance of the FileReader.
  */
  constructor() {
    super();
    h(this, "total");
    /**
    * Instance of native FileReader.
    */
    h(this, "_reader");
    /**
    * Holds the data of an already loaded file. The file must be first loaded
    * by using {@link module:upload/filereader~FileReader#read `read()`}.
    */
    h(this, "_data");
    const e = new window.FileReader();
    this._reader = e, this._data = void 0, this.set("loaded", 0), e.onprogress = (n) => {
      this.loaded = n.loaded;
    };
  }
  /**
  * Returns error that occurred during file reading.
  */
  get error() {
    return this._reader.error;
  }
  /**
  * Holds the data of an already loaded file. The file must be first loaded
  * by using {@link module:upload/filereader~FileReader#read `read()`}.
  */
  get data() {
    return this._data;
  }
  /**
  * Reads the provided file.
  *
  * @param file Native File object.
  * @returns Returns a promise that will be resolved with file's content.
  * The promise will be rejected in case of an error or when the reading process is aborted.
  */
  read(e) {
    const n = this._reader;
    return this.total = e.size, new Promise((r, s) => {
      n.onload = () => {
        const o = n.result;
        this._data = o, r(o);
      }, n.onerror = () => {
        s("error");
      }, n.onabort = () => {
        s("aborted");
      }, this._reader.readAsDataURL(e);
    });
  }
  /**
  * Aborts file reader.
  */
  abort() {
    this._reader.abort();
  }
}
class ml extends V {
  constructor() {
    super(...arguments);
    /**
    * Collection of loaders associated with this repository.
    */
    h(this, "loaders", new Pn());
    /**
    * Loaders mappings used to retrieve loaders references.
    */
    h(this, "_loadersMap", /* @__PURE__ */ new Map());
    /**
    * Reference to a pending action registered in a {@link module:core/pendingactions~PendingActions} plugin
    * while upload is in progress. When there is no upload then value is `null`.
    */
    h(this, "_pendingAction", null);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FileRepository";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ea
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    this.loaders.on("change", () => this._updatePendingAction()), this.set("uploaded", 0), this.set("uploadTotal", null), this.bind("uploadedPercent").to(this, "uploaded", this, "uploadTotal", (e, n) => n ? e / n * 100 : 0);
  }
  /**
  * Returns the loader associated with specified file or promise.
  *
  * To get loader by id use `fileRepository.loaders.get( id )`.
  *
  * @param fileOrPromise Native file or promise handle.
  */
  getLoader(e) {
    return this._loadersMap.get(e) || null;
  }
  /**
  * Creates a loader instance for the given file.
  *
  * Requires {@link #createUploadAdapter} factory to be defined.
  *
  * @param fileOrPromise Native File object or native Promise object which resolves to a File.
  */
  createLoader(e) {
    if (!this.createUploadAdapter)
      return Xe("filerepository-no-upload-adapter"), null;
    const n = new fv(Promise.resolve(e), this.createUploadAdapter);
    return this.loaders.add(n), this._loadersMap.set(e, n), e instanceof Promise && n.file.then((r) => {
      this._loadersMap.set(r, n);
    }).catch(() => {
    }), n.on("change:uploaded", () => {
      let r = 0;
      for (const s of this.loaders)
        r += s.uploaded;
      this.uploaded = r;
    }), n.on("change:uploadTotal", () => {
      let r = 0;
      for (const s of this.loaders)
        s.uploadTotal && (r += s.uploadTotal);
      this.uploadTotal = r;
    }), n;
  }
  /**
  * Destroys the given loader.
  *
  * @param fileOrPromiseOrLoader File or Promise associated with that loader or loader itself.
  */
  destroyLoader(e) {
    const n = e instanceof fv ? e : this.getLoader(e);
    n._destroy(), this.loaders.remove(n), this._loadersMap.forEach((r, s) => {
      r === n && this._loadersMap.delete(s);
    });
  }
  /**
  * Registers or deregisters pending action bound with upload progress.
  */
  _updatePendingAction() {
    const e = this.editor.plugins.get(ea);
    if (this.loaders.length) {
      if (!this._pendingAction) {
        const n = this.editor.t, r = (s) => `${n("Upload in progress")} ${parseInt(s)}%.`;
        this._pendingAction = e.add(r(this.uploadedPercent)), this._pendingAction.bind("message").to(this, "uploadedPercent", r);
      }
    } else
      e.remove(this._pendingAction), this._pendingAction = null;
  }
}
class fv extends (/* @__PURE__ */ st()) {
  /**
  * Creates a new instance of `FileLoader`.
  *
  * @param filePromise A promise which resolves to a file instance.
  * @param uploadAdapterCreator The function which returns {@link module:upload/filerepository~UploadAdapter} instance.
  */
  constructor(e, n) {
    super();
    /**
    * Unique id of FileLoader instance.
    *
    * @readonly
    */
    h(this, "id");
    /**
    * Additional wrapper over the initial file promise passed to this loader.
    */
    h(this, "_filePromiseWrapper");
    /**
    * Adapter instance associated with this file loader.
    */
    h(this, "_adapter");
    /**
    * FileReader used by FileLoader.
    */
    h(this, "_reader");
    this.id = Jn(), this._filePromiseWrapper = this._createFilePromiseWrapper(e), this._adapter = n(this), this._reader = new wV(), this.set("status", "idle"), this.set("uploaded", 0), this.set("uploadTotal", null), this.bind("uploadedPercent").to(this, "uploaded", this, "uploadTotal", (r, s) => s ? r / s * 100 : 0), this.set("uploadResponse", null);
  }
  /**
  * A `Promise` which resolves to a `File` instance associated with this file loader.
  */
  get file() {
    return this._filePromiseWrapper ? this._filePromiseWrapper.promise.then((e) => this._filePromiseWrapper ? e : null) : Promise.resolve(null);
  }
  /**
  * Returns the file data. To read its data, you need for first load the file
  * by using the {@link module:upload/filerepository~FileLoader#read `read()`} method.
  */
  get data() {
    return this._reader.data;
  }
  /**
  * Reads file using {@link module:upload/filereader~FileReader}.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `filerepository-read-wrong-status` when status
  * is different than `idle`.
  *
  * Example usage:
  *
  * ```ts
  * fileLoader.read()
  * 	.then( data => { ... } )
  * 	.catch( err => {
  * 		if ( err === 'aborted' ) {
  * 			console.log( 'Reading aborted.' );
  * 		} else {
  * 			console.log( 'Reading error.', err );
  * 		}
  * 	} );
  * ```
  *
  * @returns Returns promise that will be resolved with read data. Promise will be rejected if error
  * occurs or if read process is aborted.
  */
  read() {
    if (this.status != "idle")
      throw new T("filerepository-read-wrong-status", this);
    return this.status = "reading", this.file.then((e) => this._reader.read(e)).then((e) => {
      if (this.status !== "reading")
        throw this.status;
      return this.status = "idle", e;
    }).catch((e) => {
      throw e === "aborted" ? (this.status = "aborted", "aborted") : (this.status = "error", this._reader.error ? this._reader.error : e);
    });
  }
  /**
  * Reads file using the provided {@link module:upload/filerepository~UploadAdapter}.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `filerepository-upload-wrong-status` when status
  * is different than `idle`.
  * Example usage:
  *
  * ```ts
  * fileLoader.upload()
  * 	.then( data => { ... } )
  * 	.catch( e => {
  * 		if ( e === 'aborted' ) {
  * 			console.log( 'Uploading aborted.' );
  * 		} else {
  * 			console.log( 'Uploading error.', e );
  * 		}
  * 	} );
  * ```
  *
  * @returns Returns promise that will be resolved with response data. Promise will be rejected if error
  * occurs or if read process is aborted.
  */
  upload() {
    if (this.status != "idle")
      throw new T("filerepository-upload-wrong-status", this);
    return this.status = "uploading", this.file.then(() => this._adapter.upload()).then((e) => (this.uploadResponse = e, this.status = "idle", e)).catch((e) => {
      throw this.status === "aborted" ? "aborted" : (this.status = "error", e);
    });
  }
  /**
  * Aborts loading process.
  */
  abort() {
    const e = this.status;
    this.status = "aborted", this._filePromiseWrapper.isFulfilled ? e == "reading" ? this._reader.abort() : e == "uploading" && this._adapter.abort && this._adapter.abort() : (this._filePromiseWrapper.promise.catch(() => {
    }), this._filePromiseWrapper.rejecter("aborted")), this._destroy();
  }
  /**
  * Performs cleanup.
  *
  * @internal
  */
  _destroy() {
    this._filePromiseWrapper = void 0, this._reader = void 0, this._adapter = void 0, this.uploadResponse = void 0;
  }
  /**
  * Wraps a given file promise into another promise giving additional
  * control (resolving, rejecting, checking if fulfilled) over it.
  *
  * @param filePromise The initial file promise to be wrapped.
  */
  _createFilePromiseWrapper(e) {
    const n = {};
    return n.promise = new Promise((r, s) => {
      n.rejecter = s, n.isFulfilled = !1, e.then((o) => {
        n.isFulfilled = !0, r(o);
      }).catch((o) => {
        n.isFulfilled = !0, s(o);
      });
    }), n;
  }
}
var yV = {
  aliceblue: [240, 248, 255],
  antiquewhite: [250, 235, 215],
  aqua: [0, 255, 255],
  aquamarine: [127, 255, 212],
  azure: [240, 255, 255],
  beige: [245, 245, 220],
  bisque: [255, 228, 196],
  black: [0, 0, 0],
  blanchedalmond: [255, 235, 205],
  blue: [0, 0, 255],
  blueviolet: [138, 43, 226],
  brown: [165, 42, 42],
  burlywood: [222, 184, 135],
  cadetblue: [95, 158, 160],
  chartreuse: [127, 255, 0],
  chocolate: [210, 105, 30],
  coral: [255, 127, 80],
  cornflowerblue: [100, 149, 237],
  cornsilk: [255, 248, 220],
  crimson: [220, 20, 60],
  cyan: [0, 255, 255],
  darkblue: [0, 0, 139],
  darkcyan: [0, 139, 139],
  darkgoldenrod: [184, 134, 11],
  darkgray: [169, 169, 169],
  darkgreen: [0, 100, 0],
  darkgrey: [169, 169, 169],
  darkkhaki: [189, 183, 107],
  darkmagenta: [139, 0, 139],
  darkolivegreen: [85, 107, 47],
  darkorange: [255, 140, 0],
  darkorchid: [153, 50, 204],
  darkred: [139, 0, 0],
  darksalmon: [233, 150, 122],
  darkseagreen: [143, 188, 143],
  darkslateblue: [72, 61, 139],
  darkslategray: [47, 79, 79],
  darkslategrey: [47, 79, 79],
  darkturquoise: [0, 206, 209],
  darkviolet: [148, 0, 211],
  deeppink: [255, 20, 147],
  deepskyblue: [0, 191, 255],
  dimgray: [105, 105, 105],
  dimgrey: [105, 105, 105],
  dodgerblue: [30, 144, 255],
  firebrick: [178, 34, 34],
  floralwhite: [255, 250, 240],
  forestgreen: [34, 139, 34],
  fuchsia: [255, 0, 255],
  gainsboro: [220, 220, 220],
  ghostwhite: [248, 248, 255],
  gold: [255, 215, 0],
  goldenrod: [218, 165, 32],
  gray: [128, 128, 128],
  green: [0, 128, 0],
  greenyellow: [173, 255, 47],
  grey: [128, 128, 128],
  honeydew: [240, 255, 240],
  hotpink: [255, 105, 180],
  indianred: [205, 92, 92],
  indigo: [75, 0, 130],
  ivory: [255, 255, 240],
  khaki: [240, 230, 140],
  lavender: [230, 230, 250],
  lavenderblush: [255, 240, 245],
  lawngreen: [124, 252, 0],
  lemonchiffon: [255, 250, 205],
  lightblue: [173, 216, 230],
  lightcoral: [240, 128, 128],
  lightcyan: [224, 255, 255],
  lightgoldenrodyellow: [250, 250, 210],
  lightgray: [211, 211, 211],
  lightgreen: [144, 238, 144],
  lightgrey: [211, 211, 211],
  lightpink: [255, 182, 193],
  lightsalmon: [255, 160, 122],
  lightseagreen: [32, 178, 170],
  lightskyblue: [135, 206, 250],
  lightslategray: [119, 136, 153],
  lightslategrey: [119, 136, 153],
  lightsteelblue: [176, 196, 222],
  lightyellow: [255, 255, 224],
  lime: [0, 255, 0],
  limegreen: [50, 205, 50],
  linen: [250, 240, 230],
  magenta: [255, 0, 255],
  maroon: [128, 0, 0],
  mediumaquamarine: [102, 205, 170],
  mediumblue: [0, 0, 205],
  mediumorchid: [186, 85, 211],
  mediumpurple: [147, 112, 219],
  mediumseagreen: [60, 179, 113],
  mediumslateblue: [123, 104, 238],
  mediumspringgreen: [0, 250, 154],
  mediumturquoise: [72, 209, 204],
  mediumvioletred: [199, 21, 133],
  midnightblue: [25, 25, 112],
  mintcream: [245, 255, 250],
  mistyrose: [255, 228, 225],
  moccasin: [255, 228, 181],
  navajowhite: [255, 222, 173],
  navy: [0, 0, 128],
  oldlace: [253, 245, 230],
  olive: [128, 128, 0],
  olivedrab: [107, 142, 35],
  orange: [255, 165, 0],
  orangered: [255, 69, 0],
  orchid: [218, 112, 214],
  palegoldenrod: [238, 232, 170],
  palegreen: [152, 251, 152],
  paleturquoise: [175, 238, 238],
  palevioletred: [219, 112, 147],
  papayawhip: [255, 239, 213],
  peachpuff: [255, 218, 185],
  peru: [205, 133, 63],
  pink: [255, 192, 203],
  plum: [221, 160, 221],
  powderblue: [176, 224, 230],
  purple: [128, 0, 128],
  rebeccapurple: [102, 51, 153],
  red: [255, 0, 0],
  rosybrown: [188, 143, 143],
  royalblue: [65, 105, 225],
  saddlebrown: [139, 69, 19],
  salmon: [250, 128, 114],
  sandybrown: [244, 164, 96],
  seagreen: [46, 139, 87],
  seashell: [255, 245, 238],
  sienna: [160, 82, 45],
  silver: [192, 192, 192],
  skyblue: [135, 206, 235],
  slateblue: [106, 90, 205],
  slategray: [112, 128, 144],
  slategrey: [112, 128, 144],
  snow: [255, 250, 250],
  springgreen: [0, 255, 127],
  steelblue: [70, 130, 180],
  tan: [210, 180, 140],
  teal: [0, 128, 128],
  thistle: [216, 191, 216],
  tomato: [255, 99, 71],
  turquoise: [64, 224, 208],
  violet: [238, 130, 238],
  wheat: [245, 222, 179],
  white: [255, 255, 255],
  whitesmoke: [245, 245, 245],
  yellow: [255, 255, 0],
  yellowgreen: [154, 205, 50]
};
const mv = /* @__PURE__ */ Ag(yV);
var gv = {
  red: 0,
  orange: 60,
  yellow: 120,
  green: 180,
  blue: 240,
  purple: 300
};
function pv(i) {
  var t, e = [], n = 1, r;
  if (typeof i == "string")
    if (mv[i])
      e = mv[i].slice(), r = "rgb";
    else if (i === "transparent")
      n = 0, r = "rgb", e = [0, 0, 0];
    else if (/^#[A-Fa-f0-9]+$/.test(i)) {
      var s = i.slice(1), o = s.length, a = o <= 4;
      n = 1, a ? (e = [
        parseInt(s[0] + s[0], 16),
        parseInt(s[1] + s[1], 16),
        parseInt(s[2] + s[2], 16)
      ], o === 4 && (n = parseInt(s[3] + s[3], 16) / 255)) : (e = [
        parseInt(s[0] + s[1], 16),
        parseInt(s[2] + s[3], 16),
        parseInt(s[4] + s[5], 16)
      ], o === 8 && (n = parseInt(s[6] + s[7], 16) / 255)), e[0] || (e[0] = 0), e[1] || (e[1] = 0), e[2] || (e[2] = 0), r = "rgb";
    } else if (t = /^((?:rgb|hs[lvb]|hwb|cmyk?|xy[zy]|gray|lab|lchu?v?|[ly]uv|lms)a?)\s*\(([^\)]*)\)/.exec(i)) {
      var l = t[1], c = l === "rgb", s = l.replace(/a$/, "");
      r = s;
      var o = s === "cmyk" ? 4 : s === "gray" ? 1 : 3;
      e = t[2].trim().split(/\s*[,\/]\s*|\s+/).map(function(f, m) {
        if (/%$/.test(f))
          return m === o ? parseFloat(f) / 100 : s === "rgb" ? parseFloat(f) * 255 / 100 : parseFloat(f);
        if (s[m] === "h") {
          if (/deg$/.test(f))
            return parseFloat(f);
          if (gv[f] !== void 0)
            return gv[f];
        }
        return parseFloat(f);
      }), l === s && e.push(1), n = c || e[o] === void 0 ? 1 : e[o], e = e.slice(0, o);
    } else i.length > 10 && /[0-9](?:\s|\/)/.test(i) && (e = i.match(/([0-9]+)/g).map(function(u) {
      return parseFloat(u);
    }), r = i.match(/([a-z])/ig).join("").toLowerCase());
  else isNaN(i) ? Array.isArray(i) || i.length ? (e = [i[0], i[1], i[2]], r = "rgb", n = i.length === 4 ? i[3] : 1) : i instanceof Object && (i.r != null || i.red != null || i.R != null ? (r = "rgb", e = [
    i.r || i.red || i.R || 0,
    i.g || i.green || i.G || 0,
    i.b || i.blue || i.B || 0
  ]) : (r = "hsl", e = [
    i.h || i.hue || i.H || 0,
    i.s || i.saturation || i.S || 0,
    i.l || i.lightness || i.L || i.b || i.brightness
  ]), n = i.a || i.alpha || i.opacity || 1, i.opacity != null && (n /= 100)) : (r = "rgb", e = [i >>> 16, (i & 65280) >>> 8, i & 255]);
  return {
    space: r,
    values: e,
    alpha: n
  };
}
var _V = {
  aliceblue: [240, 248, 255],
  antiquewhite: [250, 235, 215],
  aqua: [0, 255, 255],
  aquamarine: [127, 255, 212],
  azure: [240, 255, 255],
  beige: [245, 245, 220],
  bisque: [255, 228, 196],
  black: [0, 0, 0],
  blanchedalmond: [255, 235, 205],
  blue: [0, 0, 255],
  blueviolet: [138, 43, 226],
  brown: [165, 42, 42],
  burlywood: [222, 184, 135],
  cadetblue: [95, 158, 160],
  chartreuse: [127, 255, 0],
  chocolate: [210, 105, 30],
  coral: [255, 127, 80],
  cornflowerblue: [100, 149, 237],
  cornsilk: [255, 248, 220],
  crimson: [220, 20, 60],
  cyan: [0, 255, 255],
  darkblue: [0, 0, 139],
  darkcyan: [0, 139, 139],
  darkgoldenrod: [184, 134, 11],
  darkgray: [169, 169, 169],
  darkgreen: [0, 100, 0],
  darkgrey: [169, 169, 169],
  darkkhaki: [189, 183, 107],
  darkmagenta: [139, 0, 139],
  darkolivegreen: [85, 107, 47],
  darkorange: [255, 140, 0],
  darkorchid: [153, 50, 204],
  darkred: [139, 0, 0],
  darksalmon: [233, 150, 122],
  darkseagreen: [143, 188, 143],
  darkslateblue: [72, 61, 139],
  darkslategray: [47, 79, 79],
  darkslategrey: [47, 79, 79],
  darkturquoise: [0, 206, 209],
  darkviolet: [148, 0, 211],
  deeppink: [255, 20, 147],
  deepskyblue: [0, 191, 255],
  dimgray: [105, 105, 105],
  dimgrey: [105, 105, 105],
  dodgerblue: [30, 144, 255],
  firebrick: [178, 34, 34],
  floralwhite: [255, 250, 240],
  forestgreen: [34, 139, 34],
  fuchsia: [255, 0, 255],
  gainsboro: [220, 220, 220],
  ghostwhite: [248, 248, 255],
  gold: [255, 215, 0],
  goldenrod: [218, 165, 32],
  gray: [128, 128, 128],
  green: [0, 128, 0],
  greenyellow: [173, 255, 47],
  grey: [128, 128, 128],
  honeydew: [240, 255, 240],
  hotpink: [255, 105, 180],
  indianred: [205, 92, 92],
  indigo: [75, 0, 130],
  ivory: [255, 255, 240],
  khaki: [240, 230, 140],
  lavender: [230, 230, 250],
  lavenderblush: [255, 240, 245],
  lawngreen: [124, 252, 0],
  lemonchiffon: [255, 250, 205],
  lightblue: [173, 216, 230],
  lightcoral: [240, 128, 128],
  lightcyan: [224, 255, 255],
  lightgoldenrodyellow: [250, 250, 210],
  lightgray: [211, 211, 211],
  lightgreen: [144, 238, 144],
  lightgrey: [211, 211, 211],
  lightpink: [255, 182, 193],
  lightsalmon: [255, 160, 122],
  lightseagreen: [32, 178, 170],
  lightskyblue: [135, 206, 250],
  lightslategray: [119, 136, 153],
  lightslategrey: [119, 136, 153],
  lightsteelblue: [176, 196, 222],
  lightyellow: [255, 255, 224],
  lime: [0, 255, 0],
  limegreen: [50, 205, 50],
  linen: [250, 240, 230],
  magenta: [255, 0, 255],
  maroon: [128, 0, 0],
  mediumaquamarine: [102, 205, 170],
  mediumblue: [0, 0, 205],
  mediumorchid: [186, 85, 211],
  mediumpurple: [147, 112, 219],
  mediumseagreen: [60, 179, 113],
  mediumslateblue: [123, 104, 238],
  mediumspringgreen: [0, 250, 154],
  mediumturquoise: [72, 209, 204],
  mediumvioletred: [199, 21, 133],
  midnightblue: [25, 25, 112],
  mintcream: [245, 255, 250],
  mistyrose: [255, 228, 225],
  moccasin: [255, 228, 181],
  navajowhite: [255, 222, 173],
  navy: [0, 0, 128],
  oldlace: [253, 245, 230],
  olive: [128, 128, 0],
  olivedrab: [107, 142, 35],
  orange: [255, 165, 0],
  orangered: [255, 69, 0],
  orchid: [218, 112, 214],
  palegoldenrod: [238, 232, 170],
  palegreen: [152, 251, 152],
  paleturquoise: [175, 238, 238],
  palevioletred: [219, 112, 147],
  papayawhip: [255, 239, 213],
  peachpuff: [255, 218, 185],
  peru: [205, 133, 63],
  pink: [255, 192, 203],
  plum: [221, 160, 221],
  powderblue: [176, 224, 230],
  purple: [128, 0, 128],
  rebeccapurple: [102, 51, 153],
  red: [255, 0, 0],
  rosybrown: [188, 143, 143],
  royalblue: [65, 105, 225],
  saddlebrown: [139, 69, 19],
  salmon: [250, 128, 114],
  sandybrown: [244, 164, 96],
  seagreen: [46, 139, 87],
  seashell: [255, 245, 238],
  sienna: [160, 82, 45],
  silver: [192, 192, 192],
  skyblue: [135, 206, 235],
  slateblue: [106, 90, 205],
  slategray: [112, 128, 144],
  slategrey: [112, 128, 144],
  snow: [255, 250, 250],
  springgreen: [0, 255, 127],
  steelblue: [70, 130, 180],
  tan: [210, 180, 140],
  teal: [0, 128, 128],
  thistle: [216, 191, 216],
  tomato: [255, 99, 71],
  turquoise: [64, 224, 208],
  violet: [238, 130, 238],
  wheat: [245, 222, 179],
  white: [255, 255, 255],
  whitesmoke: [245, 245, 245],
  yellow: [255, 255, 0],
  yellowgreen: [154, 205, 50]
};
const Bl = _V, PC = {};
for (const i of Object.keys(Bl))
  PC[Bl[i]] = i;
const xe = {
  rgb: { channels: 3, labels: "rgb" },
  hsl: { channels: 3, labels: "hsl" },
  hsv: { channels: 3, labels: "hsv" },
  hwb: { channels: 3, labels: "hwb" },
  cmyk: { channels: 4, labels: "cmyk" },
  xyz: { channels: 3, labels: "xyz" },
  lab: { channels: 3, labels: "lab" },
  lch: { channels: 3, labels: "lch" },
  hex: { channels: 1, labels: ["hex"] },
  keyword: { channels: 1, labels: ["keyword"] },
  ansi16: { channels: 1, labels: ["ansi16"] },
  ansi256: { channels: 1, labels: ["ansi256"] },
  hcg: { channels: 3, labels: ["h", "c", "g"] },
  apple: { channels: 3, labels: ["r16", "g16", "b16"] },
  gray: { channels: 1, labels: ["gray"] }
};
var IC = xe;
for (const i of Object.keys(xe)) {
  if (!("channels" in xe[i]))
    throw new Error("missing channels property: " + i);
  if (!("labels" in xe[i]))
    throw new Error("missing channel labels property: " + i);
  if (xe[i].labels.length !== xe[i].channels)
    throw new Error("channel and label counts mismatch: " + i);
  const { channels: t, labels: e } = xe[i];
  delete xe[i].channels, delete xe[i].labels, Object.defineProperty(xe[i], "channels", { value: t }), Object.defineProperty(xe[i], "labels", { value: e });
}
xe.rgb.hsl = function(i) {
  const t = i[0] / 255, e = i[1] / 255, n = i[2] / 255, r = Math.min(t, e, n), s = Math.max(t, e, n), o = s - r;
  let a, l;
  s === r ? a = 0 : t === s ? a = (e - n) / o : e === s ? a = 2 + (n - t) / o : n === s && (a = 4 + (t - e) / o), a = Math.min(a * 60, 360), a < 0 && (a += 360);
  const c = (r + s) / 2;
  return s === r ? l = 0 : c <= 0.5 ? l = o / (s + r) : l = o / (2 - s - r), [a, l * 100, c * 100];
};
xe.rgb.hsv = function(i) {
  let t, e, n, r, s;
  const o = i[0] / 255, a = i[1] / 255, l = i[2] / 255, c = Math.max(o, a, l), u = c - Math.min(o, a, l), d = function(f) {
    return (c - f) / 6 / u + 1 / 2;
  };
  return u === 0 ? (r = 0, s = 0) : (s = u / c, t = d(o), e = d(a), n = d(l), o === c ? r = n - e : a === c ? r = 1 / 3 + t - n : l === c && (r = 2 / 3 + e - t), r < 0 ? r += 1 : r > 1 && (r -= 1)), [
    r * 360,
    s * 100,
    c * 100
  ];
};
xe.rgb.hwb = function(i) {
  const t = i[0], e = i[1];
  let n = i[2];
  const r = xe.rgb.hsl(i)[0], s = 1 / 255 * Math.min(t, Math.min(e, n));
  return n = 1 - 1 / 255 * Math.max(t, Math.max(e, n)), [r, s * 100, n * 100];
};
xe.rgb.cmyk = function(i) {
  const t = i[0] / 255, e = i[1] / 255, n = i[2] / 255, r = Math.min(1 - t, 1 - e, 1 - n), s = (1 - t - r) / (1 - r) || 0, o = (1 - e - r) / (1 - r) || 0, a = (1 - n - r) / (1 - r) || 0;
  return [s * 100, o * 100, a * 100, r * 100];
};
function CV(i, t) {
  return (i[0] - t[0]) ** 2 + (i[1] - t[1]) ** 2 + (i[2] - t[2]) ** 2;
}
xe.rgb.keyword = function(i) {
  const t = PC[i];
  if (t)
    return t;
  let e = 1 / 0, n;
  for (const r of Object.keys(Bl)) {
    const s = Bl[r], o = CV(i, s);
    o < e && (e = o, n = r);
  }
  return n;
};
xe.keyword.rgb = function(i) {
  return Bl[i];
};
xe.rgb.xyz = function(i) {
  let t = i[0] / 255, e = i[1] / 255, n = i[2] / 255;
  t = t > 0.04045 ? ((t + 0.055) / 1.055) ** 2.4 : t / 12.92, e = e > 0.04045 ? ((e + 0.055) / 1.055) ** 2.4 : e / 12.92, n = n > 0.04045 ? ((n + 0.055) / 1.055) ** 2.4 : n / 12.92;
  const r = t * 0.4124 + e * 0.3576 + n * 0.1805, s = t * 0.2126 + e * 0.7152 + n * 0.0722, o = t * 0.0193 + e * 0.1192 + n * 0.9505;
  return [r * 100, s * 100, o * 100];
};
xe.rgb.lab = function(i) {
  const t = xe.rgb.xyz(i);
  let e = t[0], n = t[1], r = t[2];
  e /= 95.047, n /= 100, r /= 108.883, e = e > 8856e-6 ? e ** (1 / 3) : 7.787 * e + 16 / 116, n = n > 8856e-6 ? n ** (1 / 3) : 7.787 * n + 16 / 116, r = r > 8856e-6 ? r ** (1 / 3) : 7.787 * r + 16 / 116;
  const s = 116 * n - 16, o = 500 * (e - n), a = 200 * (n - r);
  return [s, o, a];
};
xe.hsl.rgb = function(i) {
  const t = i[0] / 360, e = i[1] / 100, n = i[2] / 100;
  let r, s, o;
  if (e === 0)
    return o = n * 255, [o, o, o];
  n < 0.5 ? r = n * (1 + e) : r = n + e - n * e;
  const a = 2 * n - r, l = [0, 0, 0];
  for (let c = 0; c < 3; c++)
    s = t + 1 / 3 * -(c - 1), s < 0 && s++, s > 1 && s--, 6 * s < 1 ? o = a + (r - a) * 6 * s : 2 * s < 1 ? o = r : 3 * s < 2 ? o = a + (r - a) * (2 / 3 - s) * 6 : o = a, l[c] = o * 255;
  return l;
};
xe.hsl.hsv = function(i) {
  const t = i[0];
  let e = i[1] / 100, n = i[2] / 100, r = e;
  const s = Math.max(n, 0.01);
  n *= 2, e *= n <= 1 ? n : 2 - n, r *= s <= 1 ? s : 2 - s;
  const o = (n + e) / 2, a = n === 0 ? 2 * r / (s + r) : 2 * e / (n + e);
  return [t, a * 100, o * 100];
};
xe.hsv.rgb = function(i) {
  const t = i[0] / 60, e = i[1] / 100;
  let n = i[2] / 100;
  const r = Math.floor(t) % 6, s = t - Math.floor(t), o = 255 * n * (1 - e), a = 255 * n * (1 - e * s), l = 255 * n * (1 - e * (1 - s));
  switch (n *= 255, r) {
    case 0:
      return [n, l, o];
    case 1:
      return [a, n, o];
    case 2:
      return [o, n, l];
    case 3:
      return [o, a, n];
    case 4:
      return [l, o, n];
    case 5:
      return [n, o, a];
  }
};
xe.hsv.hsl = function(i) {
  const t = i[0], e = i[1] / 100, n = i[2] / 100, r = Math.max(n, 0.01);
  let s, o;
  o = (2 - e) * n;
  const a = (2 - e) * r;
  return s = e * r, s /= a <= 1 ? a : 2 - a, s = s || 0, o /= 2, [t, s * 100, o * 100];
};
xe.hwb.rgb = function(i) {
  const t = i[0] / 360;
  let e = i[1] / 100, n = i[2] / 100;
  const r = e + n;
  let s;
  r > 1 && (e /= r, n /= r);
  const o = Math.floor(6 * t), a = 1 - n;
  s = 6 * t - o, o & 1 && (s = 1 - s);
  const l = e + s * (a - e);
  let c, u, d;
  switch (o) {
    default:
    case 6:
    case 0:
      c = a, u = l, d = e;
      break;
    case 1:
      c = l, u = a, d = e;
      break;
    case 2:
      c = e, u = a, d = l;
      break;
    case 3:
      c = e, u = l, d = a;
      break;
    case 4:
      c = l, u = e, d = a;
      break;
    case 5:
      c = a, u = e, d = l;
      break;
  }
  return [c * 255, u * 255, d * 255];
};
xe.cmyk.rgb = function(i) {
  const t = i[0] / 100, e = i[1] / 100, n = i[2] / 100, r = i[3] / 100, s = 1 - Math.min(1, t * (1 - r) + r), o = 1 - Math.min(1, e * (1 - r) + r), a = 1 - Math.min(1, n * (1 - r) + r);
  return [s * 255, o * 255, a * 255];
};
xe.xyz.rgb = function(i) {
  const t = i[0] / 100, e = i[1] / 100, n = i[2] / 100;
  let r, s, o;
  return r = t * 3.2406 + e * -1.5372 + n * -0.4986, s = t * -0.9689 + e * 1.8758 + n * 0.0415, o = t * 0.0557 + e * -0.204 + n * 1.057, r = r > 31308e-7 ? 1.055 * r ** (1 / 2.4) - 0.055 : r * 12.92, s = s > 31308e-7 ? 1.055 * s ** (1 / 2.4) - 0.055 : s * 12.92, o = o > 31308e-7 ? 1.055 * o ** (1 / 2.4) - 0.055 : o * 12.92, r = Math.min(Math.max(0, r), 1), s = Math.min(Math.max(0, s), 1), o = Math.min(Math.max(0, o), 1), [r * 255, s * 255, o * 255];
};
xe.xyz.lab = function(i) {
  let t = i[0], e = i[1], n = i[2];
  t /= 95.047, e /= 100, n /= 108.883, t = t > 8856e-6 ? t ** (1 / 3) : 7.787 * t + 16 / 116, e = e > 8856e-6 ? e ** (1 / 3) : 7.787 * e + 16 / 116, n = n > 8856e-6 ? n ** (1 / 3) : 7.787 * n + 16 / 116;
  const r = 116 * e - 16, s = 500 * (t - e), o = 200 * (e - n);
  return [r, s, o];
};
xe.lab.xyz = function(i) {
  const t = i[0], e = i[1], n = i[2];
  let r, s, o;
  s = (t + 16) / 116, r = e / 500 + s, o = s - n / 200;
  const a = s ** 3, l = r ** 3, c = o ** 3;
  return s = a > 8856e-6 ? a : (s - 16 / 116) / 7.787, r = l > 8856e-6 ? l : (r - 16 / 116) / 7.787, o = c > 8856e-6 ? c : (o - 16 / 116) / 7.787, r *= 95.047, s *= 100, o *= 108.883, [r, s, o];
};
xe.lab.lch = function(i) {
  const t = i[0], e = i[1], n = i[2];
  let r;
  r = Math.atan2(n, e) * 360 / 2 / Math.PI, r < 0 && (r += 360);
  const o = Math.sqrt(e * e + n * n);
  return [t, o, r];
};
xe.lch.lab = function(i) {
  const t = i[0], e = i[1], r = i[2] / 360 * 2 * Math.PI, s = e * Math.cos(r), o = e * Math.sin(r);
  return [t, s, o];
};
xe.rgb.ansi16 = function(i, t = null) {
  const [e, n, r] = i;
  let s = t === null ? xe.rgb.hsv(i)[2] : t;
  if (s = Math.round(s / 50), s === 0)
    return 30;
  let o = 30 + (Math.round(r / 255) << 2 | Math.round(n / 255) << 1 | Math.round(e / 255));
  return s === 2 && (o += 60), o;
};
xe.hsv.ansi16 = function(i) {
  return xe.rgb.ansi16(xe.hsv.rgb(i), i[2]);
};
xe.rgb.ansi256 = function(i) {
  const t = i[0], e = i[1], n = i[2];
  return t === e && e === n ? t < 8 ? 16 : t > 248 ? 231 : Math.round((t - 8) / 247 * 24) + 232 : 16 + 36 * Math.round(t / 255 * 5) + 6 * Math.round(e / 255 * 5) + Math.round(n / 255 * 5);
};
xe.ansi16.rgb = function(i) {
  let t = i % 10;
  if (t === 0 || t === 7)
    return i > 50 && (t += 3.5), t = t / 10.5 * 255, [t, t, t];
  const e = (~~(i > 50) + 1) * 0.5, n = (t & 1) * e * 255, r = (t >> 1 & 1) * e * 255, s = (t >> 2 & 1) * e * 255;
  return [n, r, s];
};
xe.ansi256.rgb = function(i) {
  if (i >= 232) {
    const s = (i - 232) * 10 + 8;
    return [s, s, s];
  }
  i -= 16;
  let t;
  const e = Math.floor(i / 36) / 5 * 255, n = Math.floor((t = i % 36) / 6) / 5 * 255, r = t % 6 / 5 * 255;
  return [e, n, r];
};
xe.rgb.hex = function(i) {
  const e = (((Math.round(i[0]) & 255) << 16) + ((Math.round(i[1]) & 255) << 8) + (Math.round(i[2]) & 255)).toString(16).toUpperCase();
  return "000000".substring(e.length) + e;
};
xe.hex.rgb = function(i) {
  const t = i.toString(16).match(/[a-f0-9]{6}|[a-f0-9]{3}/i);
  if (!t)
    return [0, 0, 0];
  let e = t[0];
  t[0].length === 3 && (e = e.split("").map((a) => a + a).join(""));
  const n = parseInt(e, 16), r = n >> 16 & 255, s = n >> 8 & 255, o = n & 255;
  return [r, s, o];
};
xe.rgb.hcg = function(i) {
  const t = i[0] / 255, e = i[1] / 255, n = i[2] / 255, r = Math.max(Math.max(t, e), n), s = Math.min(Math.min(t, e), n), o = r - s;
  let a, l;
  return o < 1 ? a = s / (1 - o) : a = 0, o <= 0 ? l = 0 : r === t ? l = (e - n) / o % 6 : r === e ? l = 2 + (n - t) / o : l = 4 + (t - e) / o, l /= 6, l %= 1, [l * 360, o * 100, a * 100];
};
xe.hsl.hcg = function(i) {
  const t = i[1] / 100, e = i[2] / 100, n = e < 0.5 ? 2 * t * e : 2 * t * (1 - e);
  let r = 0;
  return n < 1 && (r = (e - 0.5 * n) / (1 - n)), [i[0], n * 100, r * 100];
};
xe.hsv.hcg = function(i) {
  const t = i[1] / 100, e = i[2] / 100, n = t * e;
  let r = 0;
  return n < 1 && (r = (e - n) / (1 - n)), [i[0], n * 100, r * 100];
};
xe.hcg.rgb = function(i) {
  const t = i[0] / 360, e = i[1] / 100, n = i[2] / 100;
  if (e === 0)
    return [n * 255, n * 255, n * 255];
  const r = [0, 0, 0], s = t % 1 * 6, o = s % 1, a = 1 - o;
  let l = 0;
  switch (Math.floor(s)) {
    case 0:
      r[0] = 1, r[1] = o, r[2] = 0;
      break;
    case 1:
      r[0] = a, r[1] = 1, r[2] = 0;
      break;
    case 2:
      r[0] = 0, r[1] = 1, r[2] = o;
      break;
    case 3:
      r[0] = 0, r[1] = a, r[2] = 1;
      break;
    case 4:
      r[0] = o, r[1] = 0, r[2] = 1;
      break;
    default:
      r[0] = 1, r[1] = 0, r[2] = a;
  }
  return l = (1 - e) * n, [
    (e * r[0] + l) * 255,
    (e * r[1] + l) * 255,
    (e * r[2] + l) * 255
  ];
};
xe.hcg.hsv = function(i) {
  const t = i[1] / 100, e = i[2] / 100, n = t + e * (1 - t);
  let r = 0;
  return n > 0 && (r = t / n), [i[0], r * 100, n * 100];
};
xe.hcg.hsl = function(i) {
  const t = i[1] / 100, n = i[2] / 100 * (1 - t) + 0.5 * t;
  let r = 0;
  return n > 0 && n < 0.5 ? r = t / (2 * n) : n >= 0.5 && n < 1 && (r = t / (2 * (1 - n))), [i[0], r * 100, n * 100];
};
xe.hcg.hwb = function(i) {
  const t = i[1] / 100, e = i[2] / 100, n = t + e * (1 - t);
  return [i[0], (n - t) * 100, (1 - n) * 100];
};
xe.hwb.hcg = function(i) {
  const t = i[1] / 100, n = 1 - i[2] / 100, r = n - t;
  let s = 0;
  return r < 1 && (s = (n - r) / (1 - r)), [i[0], r * 100, s * 100];
};
xe.apple.rgb = function(i) {
  return [i[0] / 65535 * 255, i[1] / 65535 * 255, i[2] / 65535 * 255];
};
xe.rgb.apple = function(i) {
  return [i[0] / 255 * 65535, i[1] / 255 * 65535, i[2] / 255 * 65535];
};
xe.gray.rgb = function(i) {
  return [i[0] / 100 * 255, i[0] / 100 * 255, i[0] / 100 * 255];
};
xe.gray.hsl = function(i) {
  return [0, 0, i[0]];
};
xe.gray.hsv = xe.gray.hsl;
xe.gray.hwb = function(i) {
  return [0, 100, i[0]];
};
xe.gray.cmyk = function(i) {
  return [0, 0, 0, i[0]];
};
xe.gray.lab = function(i) {
  return [i[0], 0, 0];
};
xe.gray.hex = function(i) {
  const t = Math.round(i[0] / 100 * 255) & 255, n = ((t << 16) + (t << 8) + t).toString(16).toUpperCase();
  return "000000".substring(n.length) + n;
};
xe.rgb.gray = function(i) {
  return [(i[0] + i[1] + i[2]) / 3 / 255 * 100];
};
const Hu = IC;
function kV() {
  const i = {}, t = Object.keys(Hu);
  for (let e = t.length, n = 0; n < e; n++)
    i[t[n]] = {
      // http://jsperf.com/1-vs-infinity
      // micro-opt, but this is simple.
      distance: -1,
      parent: null
    };
  return i;
}
function xV(i) {
  const t = kV(), e = [i];
  for (t[i].distance = 0; e.length; ) {
    const n = e.pop(), r = Object.keys(Hu[n]);
    for (let s = r.length, o = 0; o < s; o++) {
      const a = r[o], l = t[a];
      l.distance === -1 && (l.distance = t[n].distance + 1, l.parent = n, e.unshift(a));
    }
  }
  return t;
}
function EV(i, t) {
  return function(e) {
    return t(i(e));
  };
}
function TV(i, t) {
  const e = [t[i].parent, i];
  let n = Hu[t[i].parent][i], r = t[i].parent;
  for (; t[r].parent; )
    e.unshift(t[r].parent), n = EV(Hu[t[r].parent][r], n), r = t[r].parent;
  return n.conversion = e, n;
}
var SV = function(i) {
  const t = xV(i), e = {}, n = Object.keys(t);
  for (let r = n.length, s = 0; s < r; s++) {
    const o = n[s];
    t[o].parent !== null && (e[o] = TV(o, t));
  }
  return e;
};
const fm = IC, AV = SV, Vo = {}, PV = Object.keys(fm);
function IV(i) {
  const t = function(...e) {
    const n = e[0];
    return n == null ? n : (n.length > 1 && (e = n), i(e));
  };
  return "conversion" in i && (t.conversion = i.conversion), t;
}
function RV(i) {
  const t = function(...e) {
    const n = e[0];
    if (n == null)
      return n;
    n.length > 1 && (e = n);
    const r = i(e);
    if (typeof r == "object")
      for (let s = r.length, o = 0; o < s; o++)
        r[o] = Math.round(r[o]);
    return r;
  };
  return "conversion" in i && (t.conversion = i.conversion), t;
}
PV.forEach((i) => {
  Vo[i] = {}, Object.defineProperty(Vo[i], "channels", { value: fm[i].channels }), Object.defineProperty(Vo[i], "labels", { value: fm[i].labels });
  const t = AV(i);
  Object.keys(t).forEach((n) => {
    const r = t[n];
    Vo[i][n] = RV(r), Vo[i][n].raw = IV(r);
  });
});
var RC = Vo;
const OV = /* @__PURE__ */ Ag(RC), OC = /* @__PURE__ */ P5({
  __proto__: null,
  default: OV
}, [RC]), Fl = (i, t = 0, e = 1) => i > e ? e : i < t ? t : i, Vn = (i, t = 0, e = Math.pow(10, t)) => Math.round(e * i) / e, VV = (i) => BV(mm(i)), mm = (i) => (i[0] === "#" && (i = i.substring(1)), i.length < 6 ? {
  r: parseInt(i[0] + i[0], 16),
  g: parseInt(i[1] + i[1], 16),
  b: parseInt(i[2] + i[2], 16),
  a: i.length === 4 ? Vn(parseInt(i[3] + i[3], 16) / 255, 2) : 1
} : {
  r: parseInt(i.substring(0, 2), 16),
  g: parseInt(i.substring(2, 4), 16),
  b: parseInt(i.substring(4, 6), 16),
  a: i.length === 8 ? Vn(parseInt(i.substring(6, 8), 16) / 255, 2) : 1
}), MV = (i) => NV(LV(i)), DV = ({ h: i, s: t, v: e, a: n }) => {
  const r = (200 - t) * e / 100;
  return {
    h: Vn(i),
    s: Vn(r > 0 && r < 200 ? t * e / 100 / (r <= 100 ? r : 200 - r) * 100 : 0),
    l: Vn(r / 2),
    a: Vn(n, 2)
  };
}, gm = (i) => {
  const { h: t, s: e, l: n } = DV(i);
  return `hsl(${t}, ${e}%, ${n}%)`;
}, LV = ({ h: i, s: t, v: e, a: n }) => {
  i = i / 360 * 6, t = t / 100, e = e / 100;
  const r = Math.floor(i), s = e * (1 - t), o = e * (1 - (i - r) * t), a = e * (1 - (1 - i + r) * t), l = r % 6;
  return {
    r: Vn([e, o, s, s, a, e][l] * 255),
    g: Vn([a, e, e, o, s, s][l] * 255),
    b: Vn([s, s, a, e, e, o][l] * 255),
    a: Vn(n, 2)
  };
}, $c = (i) => {
  const t = i.toString(16);
  return t.length < 2 ? "0" + t : t;
}, NV = ({ r: i, g: t, b: e, a: n }) => {
  const r = n < 1 ? $c(Vn(n * 255)) : "";
  return "#" + $c(i) + $c(t) + $c(e) + r;
}, BV = ({ r: i, g: t, b: e, a: n }) => {
  const r = Math.max(i, t, e), s = r - Math.min(i, t, e), o = s ? r === i ? (t - e) / s : r === t ? 2 + (e - i) / s : 4 + (i - t) / s : 0;
  return {
    h: Vn(60 * (o < 0 ? o + 6 : o)),
    s: Vn(r ? s / r * 100 : 0),
    v: Vn(r / 255 * 100),
    a: n
  };
}, VC = (i, t) => {
  if (i === t)
    return !0;
  for (const e in i)
    if (i[e] !== t[e])
      return !1;
  return !0;
}, FV = (i, t) => i.toLowerCase() === t.toLowerCase() ? !0 : VC(mm(i), mm(t)), bv = {}, MC = (i) => {
  let t = bv[i];
  return t || (t = document.createElement("template"), t.innerHTML = i, bv[i] = t), t;
}, dp = (i, t, e) => {
  i.dispatchEvent(new CustomEvent(t, {
    bubbles: !0,
    detail: e
  }));
};
let Uo = !1;
const pm = (i) => "touches" in i, $V = (i) => Uo && !pm(i) ? !1 : (Uo || (Uo = pm(i)), !0), vv = (i, t) => {
  const e = pm(t) ? t.touches[0] : t, n = i.el.getBoundingClientRect();
  dp(i.el, "move", i.getMove({
    x: Fl((e.pageX - (n.left + window.pageXOffset)) / n.width),
    y: Fl((e.pageY - (n.top + window.pageYOffset)) / n.height)
  }));
}, zV = (i, t) => {
  const e = t.keyCode;
  e > 40 || i.xy && e < 37 || e < 33 || (t.preventDefault(), dp(i.el, "move", i.getMove({
    x: e === 39 ? 0.01 : e === 37 ? -0.01 : e === 34 ? 0.05 : e === 33 ? -0.05 : e === 35 ? 1 : e === 36 ? -1 : 0,
    y: e === 40 ? 0.01 : e === 38 ? -0.01 : 0
  }, !0)));
};
class DC {
  constructor(t, e, n, r) {
    const s = MC(`<div role="slider" tabindex="0" part="${e}" ${n}><div part="${e}-pointer"></div></div>`);
    t.appendChild(s.content.cloneNode(!0));
    const o = t.querySelector(`[part=${e}]`);
    o.addEventListener("mousedown", this), o.addEventListener("touchstart", this), o.addEventListener("keydown", this), this.el = o, this.xy = r, this.nodes = [o.firstChild, o];
  }
  set dragging(t) {
    const e = t ? document.addEventListener : document.removeEventListener;
    e(Uo ? "touchmove" : "mousemove", this), e(Uo ? "touchend" : "mouseup", this);
  }
  handleEvent(t) {
    switch (t.type) {
      case "mousedown":
      case "touchstart":
        if (t.preventDefault(), !$V(t) || !Uo && t.button != 0)
          return;
        this.el.focus(), vv(this, t), this.dragging = !0;
        break;
      case "mousemove":
      case "touchmove":
        t.preventDefault(), vv(this, t);
        break;
      case "mouseup":
      case "touchend":
        this.dragging = !1;
        break;
      case "keydown":
        zV(this, t);
        break;
    }
  }
  style(t) {
    t.forEach((e, n) => {
      for (const r in e)
        this.nodes[n].style.setProperty(r, e[r]);
    });
  }
}
class HV extends DC {
  constructor(t) {
    super(t, "hue", 'aria-label="Hue" aria-valuemin="0" aria-valuemax="360"', !1);
  }
  update({ h: t }) {
    this.h = t, this.style([
      {
        left: `${t / 360 * 100}%`,
        color: gm({ h: t, s: 100, v: 100, a: 1 })
      }
    ]), this.el.setAttribute("aria-valuenow", `${Vn(t)}`);
  }
  getMove(t, e) {
    return { h: e ? Fl(this.h + t.x * 360, 0, 360) : 360 * t.x };
  }
}
class jV extends DC {
  constructor(t) {
    super(t, "saturation", 'aria-label="Color"', !0);
  }
  update(t) {
    this.hsva = t, this.style([
      {
        top: `${100 - t.v}%`,
        left: `${t.s}%`,
        color: gm(t)
      },
      {
        "background-color": gm({ h: t.h, s: 100, v: 100, a: 1 })
      }
    ]), this.el.setAttribute("aria-valuetext", `Saturation ${Vn(t.s)}%, Brightness ${Vn(t.v)}%`);
  }
  getMove(t, e) {
    return {
      s: e ? Fl(this.hsva.s + t.x * 100, 0, 100) : t.x * 100,
      v: e ? Fl(this.hsva.v - t.y * 100, 0, 100) : Math.round(100 - t.y * 100)
    };
  }
}
const WV = ':host{display:flex;flex-direction:column;position:relative;width:200px;height:200px;user-select:none;-webkit-user-select:none;cursor:default}:host([hidden]){display:none!important}[role=slider]{position:relative;touch-action:none;user-select:none;-webkit-user-select:none;outline:0}[role=slider]:last-child{border-radius:0 0 8px 8px}[part$=pointer]{position:absolute;z-index:1;box-sizing:border-box;width:28px;height:28px;display:flex;place-content:center center;transform:translate(-50%,-50%);background-color:#fff;border:2px solid #fff;border-radius:50%;box-shadow:0 2px 4px rgba(0,0,0,.2)}[part$=pointer]::after{content:"";width:100%;height:100%;border-radius:inherit;background-color:currentColor}[role=slider]:focus [part$=pointer]{transform:translate(-50%,-50%) scale(1.1)}', UV = "[part=hue]{flex:0 0 24px;background:linear-gradient(to right,red 0,#ff0 17%,#0f0 33%,#0ff 50%,#00f 67%,#f0f 83%,red 100%)}[part=hue-pointer]{top:50%;z-index:2}", qV = "[part=saturation]{flex-grow:1;border-color:transparent;border-bottom:12px solid #000;border-radius:8px 8px 0 0;background-image:linear-gradient(to top,#000,transparent),linear-gradient(to right,#fff,rgba(255,255,255,0));box-shadow:inset 0 0 0 1px rgba(0,0,0,.05)}[part=saturation-pointer]{z-index:3}", zc = Symbol("same"), Zh = Symbol("color"), wv = Symbol("hsva"), Yh = Symbol("update"), yv = Symbol("parts"), _v = Symbol("css"), Cv = Symbol("sliders");
class GV extends HTMLElement {
  static get observedAttributes() {
    return ["color"];
  }
  get [_v]() {
    return [WV, UV, qV];
  }
  get [Cv]() {
    return [jV, HV];
  }
  get color() {
    return this[Zh];
  }
  set color(t) {
    if (!this[zc](t)) {
      const e = this.colorModel.toHsva(t);
      this[Yh](e), this[Zh] = t;
    }
  }
  constructor() {
    super();
    const t = MC(`<style>${this[_v].join("")}</style>`), e = this.attachShadow({ mode: "open" });
    e.appendChild(t.content.cloneNode(!0)), e.addEventListener("move", this), this[yv] = this[Cv].map((n) => new n(e));
  }
  connectedCallback() {
    if (this.hasOwnProperty("color")) {
      const t = this.color;
      delete this.color, this.color = t;
    } else this.color || (this.color = this.colorModel.defaultColor);
  }
  attributeChangedCallback(t, e, n) {
    const r = this.colorModel.fromAttr(n);
    this[zc](r) || (this.color = r);
  }
  handleEvent(t) {
    const e = this[wv], n = { ...e, ...t.detail };
    this[Yh](n);
    let r;
    !VC(n, e) && !this[zc](r = this.colorModel.fromHsva(n)) && (this[Zh] = r, dp(this, "color-changed", { value: r }));
  }
  [zc](t) {
    return this.color && this.colorModel.equal(t, this.color);
  }
  [Yh](t) {
    this[wv] = t, this[yv].forEach((e) => e.update(t));
  }
}
const KV = {
  defaultColor: "#000",
  toHsva: VV,
  fromHsva: ({ h: i, s: t, v: e }) => MV({ h: i, s: t, v: e, a: 1 }),
  equal: FV,
  fromAttr: (i) => i
};
class ZV extends GV {
  get colorModel() {
    return KV;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class In extends Pn {
  /**
  * Creates a new instance of the {@link module:ui/viewcollection~ViewCollection}.
  *
  * @param initialItems The initial items of the collection.
  */
  constructor(e = []) {
    super(e, {
      // An #id Number attribute should be legal and not break the `ViewCollection` instance.
      // https://github.com/ckeditor/ckeditor5-ui/issues/93
      idProperty: "viewUid"
    });
    /**
    * A parent element within which child views are rendered and managed in DOM.
    */
    h(this, "_parentElement");
    this.on("add", (n, r, s) => {
      this._renderViewIntoCollectionParent(r, s);
    }), this.on("remove", (n, r) => {
      r.element && this._parentElement && r.element.remove();
    }), this._parentElement = null;
  }
  /**
  * Destroys the view collection along with child views.
  * See the view {@link module:ui/view~View#destroy} method.
  */
  destroy() {
    this.map((e) => e.destroy());
  }
  /**
  * Sets the parent HTML element of this collection. When parent is set, {@link #add adding} and
  * {@link #remove removing} views in the collection synchronizes their
  * {@link module:ui/view~View#element elements} in the parent element.
  *
  * @param element A new parent element.
  */
  setParent(e) {
    this._parentElement = e;
    for (const n of this)
      this._renderViewIntoCollectionParent(n);
  }
  /**
  * Delegates selected events coming from within views in the collection to any
  * {@link module:utils/emittermixin~Emitter}.
  *
  * For the following views and collection:
  *
  * ```ts
  * const viewA = new View();
  * const viewB = new View();
  * const viewC = new View();
  *
  * const views = parentView.createCollection();
  *
  * views.delegate( 'eventX' ).to( viewB );
  * views.delegate( 'eventX', 'eventY' ).to( viewC );
  *
  * views.add( viewA );
  * ```
  *
  * the `eventX` is delegated (fired by) `viewB` and `viewC` along with `customData`:
  *
  * ```ts
  * viewA.fire( 'eventX', customData );
  * ```
  *
  * and `eventY` is delegated (fired by) `viewC` along with `customData`:
  *
  * ```ts
  * viewA.fire( 'eventY', customData );
  * ```
  *
  * See {@link module:utils/emittermixin~Emitter#delegate}.
  *
  * @param events {@link module:ui/view~View} event names to be delegated to another
  * {@link module:utils/emittermixin~Emitter}.
  * @returns Object with `to` property, a function which accepts the destination
  * of {@link module:utils/emittermixin~Emitter#delegate delegated} events.
  */
  delegate(...e) {
    if (!e.length || !YV(e))
      throw new T("ui-viewcollection-delegate-wrong-events", this);
    return {
      to: (n) => {
        for (const r of this)
          for (const s of e)
            r.delegate(s).to(n);
        this.on("add", (r, s) => {
          for (const o of e)
            s.delegate(o).to(n);
        }), this.on("remove", (r, s) => {
          for (const o of e)
            s.stopDelegating(o, n);
        });
      }
    };
  }
  /**
  * This method {@link module:ui/view~View#render renders} a new view added to the collection.
  *
  * If the {@link #_parentElement parent element} of the collection is set, this method also adds
  * the view's {@link module:ui/view~View#element} as a child of the parent in DOM at a specified index.
  *
  * **Note**: If index is not specified, the view's element is pushed as the last child
  * of the parent element.
  *
  * @param view A new view added to the collection.
  * @param index An index the view holds in the collection. When not specified,
  * the view is added at the end.
  */
  _renderViewIntoCollectionParent(e, n) {
    e.isRendered || e.render(), e.element && this._parentElement && this._parentElement.insertBefore(e.element, this._parentElement.children[n]);
  }
  /**
  * Removes a child view from the collection. If the {@link #setParent parent element} of the
  * collection has been set, the {@link module:ui/view~View#element element} of the view is also removed
  * in DOM, reflecting the order of the collection.
  *
  * See the {@link #add} method.
  *
  * @param subject The view to remove, its id or index in the collection.
  * @returns The removed view.
  */
  remove(e) {
    return super.remove(e);
  }
}
function YV(i) {
  return i.every((t) => typeof t == "string");
}
const JV = "http://www.w3.org/1999/xhtml";
class li extends (/* @__PURE__ */ ht()) {
  /**
  * Creates an instance of the {@link ~Template} class.
  *
  * @param def The definition of the template.
  */
  constructor(e) {
    super();
    h(this, "ns");
    /**
    * The tag (`tagName`) of this template, e.g. `div`. It also indicates that the template
    * renders to an HTML element.
    */
    h(this, "tag");
    /**
    * The text of the template. It also indicates that the template renders to a DOM text node.
    */
    h(this, "text");
    /**
    * The attributes of the template, e.g. `{ id: [ 'ck-id' ] }`, corresponding with
    * the attributes of an HTML element.
    *
    * **Note**: This property only makes sense when {@link #tag} is defined.
    */
    h(this, "attributes");
    /**
    * The children of the template. They can be either:
    * * independent instances of {@link ~Template} (sub–templates),
    * * native DOM Nodes.
    *
    * **Note**: This property only makes sense when {@link #tag} is defined.
    */
    h(this, "children");
    /**
    * The DOM event listeners of the template.
    */
    h(this, "eventListeners");
    /**
    * Indicates whether this particular Template instance has been
    * {@link #render rendered}.
    */
    h(this, "_isRendered");
    /**
    * The data used by the {@link #revert} method to restore a node to its original state.
    *
    * See: {@link #apply}.
    */
    h(this, "_revertData");
    Object.assign(this, xv(kv(e))), this._isRendered = !1, this._revertData = null;
  }
  /**
  * Renders a DOM Node (an HTML element or text) out of the template.
  *
  * ```ts
  * const domNode = new Template( { ... } ).render();
  * ```
  *
  * See: {@link #apply}.
  */
  render() {
    const e = this._renderNode({
      intoFragment: !0
    });
    return this._isRendered = !0, e;
  }
  /**
  * Applies the template to an existing DOM Node, either HTML element or text.
  *
  * **Note:** No new DOM nodes will be created. Applying extends:
  *
  * {@link module:ui/template~TemplateDefinition attributes},
  * {@link module:ui/template~TemplateDefinition event listeners}, and
  * `textContent` of {@link module:ui/template~TemplateDefinition children} only.
  *
  * **Note:** Existing `class` and `style` attributes are extended when a template
  * is applied to an HTML element, while other attributes and `textContent` are overridden.
  *
  * **Note:** The process of applying a template can be easily reverted using the
  * {@link module:ui/template~Template#revert} method.
  *
  * ```ts
  * const element = document.createElement( 'div' );
  * const observable = new Model( { divClass: 'my-div' } );
  * const emitter = Object.create( EmitterMixin );
  * const bind = Template.bind( observable, emitter );
  *
  * new Template( {
  * 	attributes: {
  * 		id: 'first-div',
  * 		class: bind.to( 'divClass' )
  * 	},
  * 	on: {
  * 		click: bind( 'elementClicked' ) // Will be fired by the observable.
  * 	},
  * 	children: [
  * 		'Div text.'
  * 	]
  * } ).apply( element );
  *
  * console.log( element.outerHTML ); // -> '<div id="first-div" class="my-div"></div>'
  * ```
  *
  * @see module:ui/template~Template#render
  * @see module:ui/template~Template#revert
  * @param node Root node for the template to apply.
  */
  apply(e) {
    return this._revertData = Sv(), this._renderNode({
      node: e,
      intoFragment: !1,
      isApplying: !0,
      revertData: this._revertData
    }), e;
  }
  /**
  * Reverts a template {@link module:ui/template~Template#apply applied} to a DOM node.
  *
  * @param node The root node for the template to revert. In most of the cases, it is the
  * same node used by {@link module:ui/template~Template#apply}.
  */
  revert(e) {
    if (!this._revertData)
      throw new T("ui-template-revert-not-applied", [
        this,
        e
      ]);
    this._revertTemplateFromNode(e, this._revertData);
  }
  /**
  * Returns an iterator which traverses the template in search of {@link module:ui/view~View}
  * instances and returns them one by one.
  *
  * ```ts
  * const viewFoo = new View();
  * const viewBar = new View();
  * const viewBaz = new View();
  * const template = new Template( {
  * 	tag: 'div',
  * 	children: [
  * 		viewFoo,
  * 		{
  * 			tag: 'div',
  * 			children: [
  * 				viewBar
  * 			]
  * 		},
  * 		viewBaz
  * 	]
  * } );
  *
  * // Logs: viewFoo, viewBar, viewBaz
  * for ( const view of template.getViews() ) {
  * 	console.log( view );
  * }
  * ```
  */
  *getViews() {
    function* e(n) {
      if (n.children)
        for (const r of n.children)
          ju(r) ? yield r : hp(r) && (yield* e(r));
    }
    yield* e(this);
  }
  /**
  * An entry point to the interface which binds DOM nodes to
  * {@link module:utils/observablemixin~Observable observables}.
  * There are two types of bindings:
  *
  * * HTML element attributes or text `textContent` synchronized with attributes of an
  * {@link module:utils/observablemixin~Observable}. Learn more about {@link module:ui/template~BindChain#to}
  * and {@link module:ui/template~BindChain#if}.
  *
  * ```ts
  * const bind = Template.bind( observable, emitter );
  *
  * new Template( {
  * 	attributes: {
  * 		// Binds the element "class" attribute to observable#classAttribute.
  * 		class: bind.to( 'classAttribute' )
  * 	}
  * } ).render();
  * ```
  *
  * * DOM events fired on HTML element propagated through
  * {@link module:utils/observablemixin~Observable}. Learn more about {@link module:ui/template~BindChain#to}.
  *
  * ```ts
  * const bind = Template.bind( observable, emitter );
  *
  * new Template( {
  * 	on: {
  * 		// Will be fired by the observable.
  * 		click: bind( 'elementClicked' )
  * 	}
  * } ).render();
  * ```
  *
  * Also see {@link module:ui/view~View#bindTemplate}.
  *
  * @param observable An observable which provides boundable attributes.
  * @param emitter An emitter that listens to observable attribute
  * changes or DOM Events (depending on the kind of the binding). Usually, a {@link module:ui/view~View} instance.
  */
  static bind(e, n) {
    return {
      to(r, s) {
        return new XV({
          eventNameOrFunction: r,
          attribute: r,
          observable: e,
          emitter: n,
          callback: s
        });
      },
      if(r, s, o) {
        return new LC({
          observable: e,
          emitter: n,
          attribute: r,
          valueIfTrue: s,
          callback: o
        });
      }
    };
  }
  /**
  * Extends an existing {@link module:ui/template~Template} instance with some additional content
  * from another {@link module:ui/template~TemplateDefinition}.
  *
  * ```ts
  * const bind = Template.bind( observable, emitter );
  *
  * const template = new Template( {
  * 	tag: 'p',
  * 	attributes: {
  * 		class: 'a',
  * 		data-x: bind.to( 'foo' )
  * 	},
  * 	children: [
  * 		{
  * 			tag: 'span',
  * 			attributes: {
  * 				class: 'b'
  * 			},
  * 			children: [
  * 				'Span'
  * 			]
  * 		}
  * 	]
  *  } );
  *
  * // Instance-level extension.
  * Template.extend( template, {
  * 	attributes: {
  * 		class: 'b',
  * 		data-x: bind.to( 'bar' )
  * 	},
  * 	children: [
  * 		{
  * 			attributes: {
  * 				class: 'c'
  * 			}
  * 		}
  * 	]
  * } );
  *
  * // Child extension.
  * Template.extend( template.children[ 0 ], {
  * 	attributes: {
  * 		class: 'd'
  * 	}
  * } );
  * ```
  *
  * the `outerHTML` of `template.render()` is:
  *
  * ```html
  * <p class="a b" data-x="{ observable.foo } { observable.bar }">
  * 	<span class="b c d">Span</span>
  * </p>
  * ```
  *
  * @param template An existing template instance to be extended.
  * @param def Additional definition to be applied to a template.
  */
  static extend(e, n) {
    if (e._isRendered)
      throw new T("template-extend-render", [
        this,
        e
      ]);
    $C(e, xv(kv(n)));
  }
  /**
  * Renders a DOM Node (either an HTML element or text) out of the template.
  *
  * @param data Rendering data.
  */
  _renderNode(e) {
    let n;
    if (e.node ? n = this.tag && this.text : n = this.tag ? this.text : !this.text, n)
      throw new T("ui-template-wrong-syntax", this);
    return this.text ? this._renderText(e) : this._renderElement(e);
  }
  /**
  * Renders an HTML element out of the template.
  *
  * @param data Rendering data.
  */
  _renderElement(e) {
    let n = e.node;
    return n || (n = e.node = document.createElementNS(this.ns || JV, this.tag)), this._renderAttributes(e), this._renderElementChildren(e), this._setUpListeners(e), n;
  }
  /**
  * Renders a text node out of {@link module:ui/template~Template#text}.
  *
  * @param data Rendering data.
  */
  _renderText(e) {
    let n = e.node;
    return n ? e.revertData.text = n.textContent : n = e.node = document.createTextNode(""), yu(this.text) ? this._bindToObservable({
      schema: this.text,
      updater: eM(n),
      data: e
    }) : n.textContent = this.text.join(""), n;
  }
  /**
  * Renders HTML element attributes out of {@link module:ui/template~Template#attributes}.
  *
  * @param data Rendering data.
  */
  _renderAttributes(e) {
    if (!this.attributes)
      return;
    const n = e.node, r = e.revertData;
    for (const s in this.attributes) {
      const o = n.getAttribute(s), a = this.attributes[s];
      r && (r.attributes[s] = o);
      const l = Tv(a) ? a[0].ns : null;
      if (yu(a)) {
        const c = Tv(a) ? a[0].value : a;
        r && Av(s) && c.unshift(o), this._bindToObservable({
          schema: c,
          updater: tM(n, s, l),
          data: e
        });
      } else if (s == "style" && typeof a[0] != "string")
        this._renderStyleAttribute(a[0], e);
      else {
        r && o && Av(s) && a.unshift(o);
        const c = a.map((u) => u && (u.value || u)).reduce((u, d) => u.concat(d), []).reduce(FC, "");
        ta(c) || n.setAttributeNS(l, s, c);
      }
    }
  }
  /**
  * Renders the `style` attribute of an HTML element based on
  * {@link module:ui/template~Template#attributes}.
  *
  * A style attribute is an object with static values:
  *
  * ```ts
  * attributes: {
  * 	style: {
  * 		color: 'red'
  * 	}
  * }
  * ```
  *
  * or values bound to {@link module:ui/model~Model} properties:
  *
  * ```ts
  * attributes: {
  * 	style: {
  * 		color: bind.to( ... )
  * 	}
  * }
  * ```
  *
  * Note: The `style` attribute is rendered without setting the namespace. It does not seem to be
  * needed.
  *
  * @param styles Styles located in `attributes.style` of {@link module:ui/template~TemplateDefinition}.
  * @param data Rendering data.
  */
  _renderStyleAttribute(e, n) {
    const r = n.node;
    for (const s in e) {
      const o = e[s];
      yu(o) ? this._bindToObservable({
        schema: [
          o
        ],
        updater: nM(r, s),
        data: n
      }) : r.style[s] = o;
    }
  }
  /**
  * Recursively renders HTML element's children from {@link module:ui/template~Template#children}.
  *
  * @param data Rendering data.
  */
  _renderElementChildren(e) {
    const n = e.node, r = e.intoFragment ? document.createDocumentFragment() : n, s = e.isApplying;
    let o = 0;
    for (const a of this.children)
      if (fp(a)) {
        if (!s) {
          a.setParent(n);
          for (const l of a)
            r.appendChild(l.element);
        }
      } else if (ju(a))
        s || (a.isRendered || a.render(), r.appendChild(a.element));
      else if (ds(a))
        r.appendChild(a);
      else if (s) {
        const l = e.revertData, c = Sv();
        l.children.push(c), a._renderNode({
          intoFragment: !1,
          node: r.childNodes[o++],
          isApplying: !0,
          revertData: c
        });
      } else
        r.appendChild(a.render());
    e.intoFragment && n.appendChild(r);
  }
  /**
  * Activates `on` event listeners from the {@link module:ui/template~TemplateDefinition}
  * on an HTML element.
  *
  * @param data Rendering data.
  */
  _setUpListeners(e) {
    if (this.eventListeners)
      for (const n in this.eventListeners) {
        const r = this.eventListeners[n].map((s) => {
          const [o, a] = n.split("@");
          return s.activateDomEventListener(o, a, e);
        });
        e.revertData && e.revertData.bindings.push(r);
      }
  }
  /**
  * For a given {@link module:ui/template~TemplateValueSchema} containing {@link module:ui/template~TemplateBinding}
  * activates the binding and sets its initial value.
  *
  * Note: {@link module:ui/template~TemplateValueSchema} can be for HTML element attributes or
  * text node `textContent`.
  *
  * @param options Binding options.
  * @param options.updater A function which updates the DOM (like attribute or text).
  * @param options.data Rendering data.
  */
  _bindToObservable({ schema: e, updater: n, data: r }) {
    const s = r.revertData;
    NC(e, n, r);
    const o = e.filter((a) => !ta(a)).filter((a) => a.observable).map((a) => a.activateAttributeListener(e, n, r));
    s && s.bindings.push(o);
  }
  /**
  * Reverts {@link module:ui/template~RenderData#revertData template data} from a node to
  * return it to the original state.
  *
  * @param node A node to be reverted.
  * @param revertData An object that stores information about what changes have been made by
  * {@link #apply} to the node. See {@link module:ui/template~RenderData#revertData} for more information.
  */
  _revertTemplateFromNode(e, n) {
    for (const s of n.bindings)
      for (const o of s)
        o();
    if (n.text) {
      e.textContent = n.text;
      return;
    }
    const r = e;
    for (const s in n.attributes) {
      const o = n.attributes[s];
      o === null ? r.removeAttribute(s) : r.setAttribute(s, o);
    }
    for (let s = 0; s < n.children.length; ++s)
      this._revertTemplateFromNode(r.childNodes[s], n.children[s]);
  }
}
class pc {
  /**
  * Creates an instance of the {@link module:ui/template~TemplateBinding} class.
  *
  * @param def The definition of the binding.
  */
  constructor(t) {
    /**
    * The name of the {@link module:ui/template~TemplateBinding#observable observed attribute}.
    */
    h(this, "attribute");
    /**
    * An observable instance of the binding. It either:
    *
    * * provides the attribute with the value,
    * * or passes the event when a corresponding DOM event is fired.
    */
    h(this, "observable");
    /**
    * An {@link module:utils/emittermixin~Emitter} used by the binding to:
    *
    * * listen to the attribute change in the {@link module:ui/template~TemplateBinding#observable},
    * * or listen to the event in the DOM.
    */
    h(this, "emitter");
    /**
    * A custom function to process the value of the {@link module:ui/template~TemplateBinding#attribute}.
    */
    h(this, "callback");
    this.attribute = t.attribute, this.observable = t.observable, this.emitter = t.emitter, this.callback = t.callback;
  }
  /**
  * Returns the value of the binding. It is the value of the {@link module:ui/template~TemplateBinding#attribute} in
  * {@link module:ui/template~TemplateBinding#observable}. The value may be processed by the
  * {@link module:ui/template~TemplateBinding#callback}, if such has been passed to the binding.
  *
  * @param node A native DOM node, passed to the custom {@link module:ui/template~TemplateBinding#callback}.
  * @returns The value of {@link module:ui/template~TemplateBinding#attribute} in
  * {@link module:ui/template~TemplateBinding#observable}.
  */
  getValue(t) {
    const e = this.observable[this.attribute];
    return this.callback ? this.callback(e, t) : e;
  }
  /**
  * Activates the listener which waits for changes of the {@link module:ui/template~TemplateBinding#attribute} in
  * {@link module:ui/template~TemplateBinding#observable}, then updates the DOM with the aggregated
  * value of {@link module:ui/template~TemplateValueSchema}.
  *
  * @param schema A full schema to generate an attribute or text in the DOM.
  * @param updater A DOM updater function used to update the native DOM attribute or text.
  * @param data Rendering data.
  * @returns A function to sever the listener binding.
  */
  activateAttributeListener(t, e, n) {
    const r = () => NC(t, e, n);
    return this.emitter.listenTo(this.observable, `change:${this.attribute}`, r), () => {
      this.emitter.stopListening(this.observable, `change:${this.attribute}`, r);
    };
  }
}
class XV extends pc {
  constructor(e) {
    super(e);
    h(this, "eventNameOrFunction");
    this.eventNameOrFunction = e.eventNameOrFunction;
  }
  /**
  * Activates the listener for the native DOM event, which when fired, is propagated by
  * the {@link module:ui/template~TemplateBinding#emitter}.
  *
  * @param domEvtName The name of the native DOM event.
  * @param domSelector The selector in the DOM to filter delegated events.
  * @param data Rendering data.
  * @returns A function to sever the listener binding.
  */
  activateDomEventListener(e, n, r) {
    const s = (o, a) => {
      (!n || a.target.matches(n)) && (typeof this.eventNameOrFunction == "function" ? this.eventNameOrFunction(a) : this.observable.fire(this.eventNameOrFunction, a));
    };
    return this.emitter.listenTo(r.node, e, s), () => {
      this.emitter.stopListening(r.node, e, s);
    };
  }
}
class LC extends pc {
  constructor(e) {
    super(e);
    /**
    * The value of the DOM attribute or text to be set if the {@link module:ui/template~TemplateBinding#attribute} in
    * {@link module:ui/template~TemplateBinding#observable} is `true`.
    */
    h(this, "valueIfTrue");
    this.valueIfTrue = e.valueIfTrue;
  }
  /**
  * @inheritDoc
  */
  getValue(e) {
    const n = super.getValue(e);
    return ta(n) ? !1 : this.valueIfTrue || !0;
  }
}
function yu(i) {
  return i ? (i.value && (i = i.value), Array.isArray(i) ? i.some(yu) : i instanceof pc) : !1;
}
function QV(i, t) {
  return i.map((e) => e instanceof pc ? e.getValue(t) : e);
}
function NC(i, t, { node: e }) {
  const n = QV(i, e);
  let r;
  i.length == 1 && i[0] instanceof LC ? r = n[0] : r = n.reduce(FC, ""), ta(r) ? t.remove() : t.set(r);
}
function eM(i) {
  return {
    set(t) {
      i.textContent = t;
    },
    remove() {
      i.textContent = "";
    }
  };
}
function tM(i, t, e) {
  return {
    set(n) {
      i.setAttributeNS(e, t, n);
    },
    remove() {
      i.removeAttributeNS(e, t);
    }
  };
}
function nM(i, t) {
  return {
    set(e) {
      i.style[t] = e;
    },
    remove() {
      i.style[t] = null;
    }
  };
}
function kv(i) {
  return jg(i, (e) => {
    if (e && (e instanceof pc || hp(e) || ju(e) || fp(e)))
      return e;
  });
}
function xv(i) {
  if (typeof i == "string" ? i = sM(i) : i.text && oM(i), i.on && (i.eventListeners = rM(i.on), delete i.on), !i.text) {
    i.attributes && iM(i.attributes);
    const t = [];
    if (i.children)
      if (fp(i.children))
        t.push(i.children);
      else
        for (const e of i.children)
          hp(e) || ju(e) || ds(e) ? t.push(e) : t.push(new li(e));
    i.children = t;
  }
  return i;
}
function iM(i) {
  for (const t in i)
    i[t].value && (i[t].value = tt(i[t].value)), BC(i, t);
}
function rM(i) {
  for (const t in i)
    BC(i, t);
  return i;
}
function sM(i) {
  return {
    text: [
      i
    ]
  };
}
function oM(i) {
  i.text = tt(i.text);
}
function BC(i, t) {
  i[t] = tt(i[t]);
}
function FC(i, t) {
  return ta(t) ? i : ta(i) ? t : `${i} ${t}`;
}
function Ev(i, t) {
  for (const e in t)
    i[e] ? i[e].push(...t[e]) : i[e] = t[e];
}
function $C(i, t) {
  if (t.attributes && (i.attributes || (i.attributes = {}), Ev(i.attributes, t.attributes)), t.eventListeners && (i.eventListeners || (i.eventListeners = {}), Ev(i.eventListeners, t.eventListeners)), t.text && i.text.push(...t.text), t.children && t.children.length) {
    if (i.children.length != t.children.length)
      throw new T("ui-template-extend-children-mismatch", i);
    let e = 0;
    for (const n of t.children)
      $C(i.children[e++], n);
  }
}
function ta(i) {
  return !i && i !== 0;
}
function ju(i) {
  return i instanceof de;
}
function hp(i) {
  return i instanceof li;
}
function fp(i) {
  return i instanceof In;
}
function Tv(i) {
  return vt(i[0]) && i[0].ns;
}
function Sv() {
  return {
    children: [],
    bindings: [],
    attributes: {}
  };
}
function Av(i) {
  return i == "class" || i == "style";
}
class de extends (/* @__PURE__ */ Xn(/* @__PURE__ */ st())) {
  /**
  * Creates an instance of the {@link module:ui/view~View} class.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super();
    /**
    * An HTML element of the view. `null` until {@link #render rendered}
    * from the {@link #template}.
    *
    * ```ts
    * class SampleView extends View {
    * 	constructor() {
    * 		super();
    *
    * 		// A template instance the #element will be created from.
    * 		this.setTemplate( {
    * 			tag: 'p'
    *
    * 			// ...
    * 		} );
    * 	}
    * }
    *
    * const view = new SampleView();
    *
    * // Renders the #template.
    * view.render();
    *
    * // Append the HTML element of the view to <body>.
    * document.body.appendChild( view.element );
    * ```
    *
    * **Note**: The element of the view can also be assigned directly:
    *
    * ```ts
    * view.element = document.querySelector( '#my-container' );
    * ```
    */
    h(this, "element");
    /**
    * Set `true` when the view has already been {@link module:ui/view~View#render rendered}.
    *
    * @readonly
    */
    h(this, "isRendered");
    /**
    * A set of tools to localize the user interface.
    *
    * Also see {@link module:core/editor/editor~Editor#locale}.
    *
    * @readonly
    */
    h(this, "locale");
    /**
    * Shorthand for {@link module:utils/locale~Locale#t}.
    *
    * Note: If {@link #locale} instance hasn't been passed to the view this method may not
    * be available.
    *
    * @see module:utils/locale~Locale#t
    */
    h(this, "t");
    /**
    * Template of this view. It provides the {@link #element} representing
    * the view in DOM, which is {@link #render rendered}.
    */
    h(this, "template");
    /**
    * Collections registered with {@link #createCollection}.
    */
    h(this, "_viewCollections");
    /**
    * A collection of view instances, which have been added directly
    * into the {@link module:ui/template~Template#children}.
    */
    h(this, "_unboundChildren");
    /**
    * Cached {@link module:ui/template~BindChain bind chain} object created by the
    * {@link #template}. See {@link #bindTemplate}.
    */
    h(this, "_bindTemplate");
    this.element = null, this.isRendered = !1, this.locale = e, this.t = e && e.t, this._viewCollections = new Pn(), this._unboundChildren = this.createCollection(), this._viewCollections.on("add", (n, r) => {
      r.locale = e, r.t = e && e.t;
    }), this.decorate("render");
  }
  /**
  * Shorthand for {@link module:ui/template~Template.bind}, a binding
  * {@link module:ui/template~BindChain interface} pre–configured for the view instance.
  *
  * It provides {@link module:ui/template~BindChain#to `to()`} and
  * {@link module:ui/template~BindChain#if `if()`} methods that initialize bindings with
  * observable attributes and attach DOM listeners.
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor( locale ) {
  * 		super( locale );
  *
  * 		const bind = this.bindTemplate;
  *
  * 		// These {@link module:utils/observablemixin~Observable observable} attributes will control
  * 		// the state of the view in DOM.
  * 		this.set( {
  * 			elementClass: 'foo',
  * 		 	isEnabled: true
  * 		 } );
  *
  * 		this.setTemplate( {
  * 			tag: 'p',
  *
  * 			attributes: {
  * 				// The class HTML attribute will follow elementClass
  * 				// and isEnabled view attributes.
  * 				class: [
  * 					bind.to( 'elementClass' )
  * 					bind.if( 'isEnabled', 'present-when-enabled' )
  * 				]
  * 			},
  *
  * 			on: {
  * 				// The view will fire the "clicked" event upon clicking <p> in DOM.
  * 				click: bind.to( 'clicked' )
  * 			}
  * 		} );
  * 	}
  * }
  * ```
  */
  get bindTemplate() {
    return this._bindTemplate ? this._bindTemplate : this._bindTemplate = li.bind(this, this);
  }
  /**
  * Creates a new collection of views, which can be used as
  * {@link module:ui/template~Template#children} of this view.
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor( locale ) {
  * 		super( locale );
  *
  * 		const child = new ChildView( locale );
  * 		this.items = this.createCollection( [ child ] );
   *
  * 		this.setTemplate( {
  * 			tag: 'p',
  *
  * 			// `items` collection will render here.
  * 			children: this.items
  * 		} );
  * 	}
  * }
  *
  * const view = new SampleView( locale );
  * view.render();
  *
  * // It will append <p><child#element></p> to the <body>.
  * document.body.appendChild( view.element );
  * ```
  *
  * @param views Initial views of the collection.
  * @returns A new collection of view instances.
  */
  createCollection(e) {
    const n = new In(e);
    return this._viewCollections.add(n), n;
  }
  /**
  * Registers a new child view under the view instance. Once registered, a child
  * view is managed by its parent, including {@link #render rendering}
  * and {@link #destroy destruction}.
  *
  * To revert this, use {@link #deregisterChild}.
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor( locale ) {
  * 		super( locale );
  *
  * 		this.childA = new SomeChildView( locale );
  * 		this.childB = new SomeChildView( locale );
  *
  * 		this.setTemplate( { tag: 'p' } );
  *
  * 		// Register the children.
  * 		this.registerChild( [ this.childA, this.childB ] );
  * 	}
  *
  * 	render() {
  * 		super.render();
  *
  * 		this.element.appendChild( this.childA.element );
  * 		this.element.appendChild( this.childB.element );
  * 	}
  * }
  *
  * const view = new SampleView( locale );
  *
  * view.render();
  *
  * // Will append <p><childA#element><b></b><childB#element></p>.
  * document.body.appendChild( view.element );
  * ```
  *
  * **Note**: There's no need to add child views if they're already referenced in the
  * {@link #template}:
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor( locale ) {
  * 		super( locale );
  *
  * 		this.childA = new SomeChildView( locale );
  * 		this.childB = new SomeChildView( locale );
  *
  * 		this.setTemplate( {
  * 			tag: 'p',
  *
   * 			// These children will be added automatically. There's no
   * 			// need to call {@link #registerChild} for any of them.
  * 			children: [ this.childA, this.childB ]
  * 		} );
  * 	}
  *
  * 	// ...
  * }
  * ```
  *
  * @param children Children views to be registered.
  */
  registerChild(e) {
    ki(e) || (e = [
      e
    ]);
    for (const n of e)
      this._unboundChildren.add(n);
  }
  /**
  * The opposite of {@link #registerChild}. Removes a child view from this view instance.
  * Once removed, the child is no longer managed by its parent, e.g. it can safely
  * become a child of another parent view.
  *
  * @see #registerChild
  * @param children Child views to be removed.
  */
  deregisterChild(e) {
    ki(e) || (e = [
      e
    ]);
    for (const n of e)
      this._unboundChildren.remove(n);
  }
  /**
  * Sets the {@link #template} of the view with with given definition.
  *
  * A shorthand for:
  *
  * ```ts
  * view.setTemplate( definition );
  * ```
  *
  * @param definition Definition of view's template.
  */
  setTemplate(e) {
    this.template = new li(e);
  }
  /**
  * {@link module:ui/template~Template.extend Extends} the {@link #template} of the view with
  * with given definition.
  *
  * A shorthand for:
  *
  * ```ts
  * Template.extend( view.template, definition );
  * ```
  *
  * **Note**: Is requires the {@link #template} to be already set. See {@link #setTemplate}.
  *
  * @param definition Definition which extends the {@link #template}.
  */
  extendTemplate(e) {
    li.extend(this.template, e);
  }
  /**
  * Recursively renders the view.
  *
  * Once the view is rendered:
  * * the {@link #element} becomes an HTML element out of {@link #template},
  * * the {@link #isRendered} flag is set `true`.
  *
  * **Note**: The children of the view:
  * * defined directly in the {@link #template}
  * * residing in collections created by the {@link #createCollection} method,
  * * and added by {@link #registerChild}
  * are also rendered in the process.
  *
  * In general, `render()` method is the right place to keep the code which refers to the
  * {@link #element} and should be executed at the very beginning of the view's life cycle.
  *
  * It is possible to {@link module:ui/template~Template.extend} the {@link #template} before
  * the view is rendered. To allow an early customization of the view (e.g. by its parent),
  * such references should be done in `render()`.
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor() {
  * 		this.setTemplate( {
  * 			// ...
  * 		} );
  * 	},
  *
  * 	render() {
  * 		// View#element becomes available.
  * 		super.render();
  *
  * 		// The "scroll" listener depends on #element.
  * 		this.listenTo( window, 'scroll', () => {
  * 			// A reference to #element would render the #template and make it non-extendable.
  * 			if ( window.scrollY > 0 ) {
  * 				this.element.scrollLeft = 100;
  * 			} else {
  * 				this.element.scrollLeft = 0;
  * 			}
  * 		} );
  * 	}
  * }
  *
  * const view = new SampleView();
  *
  * // Let's customize the view before it gets rendered.
  * view.extendTemplate( {
  * 	attributes: {
  * 		class: [
  * 			'additional-class'
  * 		]
  * 	}
  * } );
  *
  * // Late rendering allows customization of the view.
  * view.render();
  * ```
  */
  render() {
    if (this.isRendered)
      throw new T("ui-view-render-already-rendered", this);
    this.template && (this.element = this.template.render(), this.registerChild(this.template.getViews())), this.isRendered = !0;
  }
  /**
  * Recursively destroys the view instance and child views added by {@link #registerChild} and
  * residing in collections created by the {@link #createCollection}.
  *
  * Destruction disables all event listeners:
  * * created on the view, e.g. `view.on( 'event', () => {} )`,
  * * defined in the {@link #template} for DOM events.
  */
  destroy() {
    this.stopListening(), this._viewCollections.map((e) => e.destroy()), this.template && this.template._revertData && this.template.revert(this.element);
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function _s({ emitter: i, activator: t, callback: e, contextElements: n, listenerOptions: r }) {
  i.listenTo(document, "mousedown", (s, o) => {
    if (!t())
      return;
    const a = typeof o.composedPath == "function" ? o.composedPath() : [], l = typeof n == "function" ? n() : n;
    for (const c of l)
      if (c.contains(o.target) || a.includes(c))
        return;
    e();
  }, r);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function bc(i) {
  class t extends i {
    disableCssTransitions() {
      this._isCssTransitionsDisabled = !0;
    }
    enableCssTransitions() {
      this._isCssTransitionsDisabled = !1;
    }
    constructor(...n) {
      super(...n), this.set("_isCssTransitionsDisabled", !1), this.initializeCssTransitionDisablerMixin();
    }
    initializeCssTransitionDisablerMixin() {
      this.extendTemplate({
        attributes: {
          class: [
            this.bindTemplate.if("_isCssTransitionsDisabled", "ck-transitions-disabled")
          ]
        }
      });
    }
  }
  return t;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Cs({ view: i }) {
  i.listenTo(i.element, "submit", (t, e) => {
    e.preventDefault(), i.fire("submit");
  }, {
    useCapture: !0
  });
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function mp({ keystrokeHandler: i, focusTracker: t, gridItems: e, numberOfColumns: n, uiLanguageDirection: r }) {
  const s = typeof n == "number" ? () => n : n;
  i.set("arrowright", o((c, u) => r === "rtl" ? l(c, u.length) : a(c, u.length))), i.set("arrowleft", o((c, u) => r === "rtl" ? a(c, u.length) : l(c, u.length))), i.set("arrowup", o((c, u) => {
    let d = c - s();
    return d < 0 && (d = c + s() * Math.floor(u.length / s()), d > u.length - 1 && (d -= s())), d;
  })), i.set("arrowdown", o((c, u) => {
    let d = c + s();
    return d > u.length - 1 && (d = c % s()), d;
  }));
  function o(c) {
    return (u) => {
      const d = e.find((g) => g.element === t.focusedElement), f = e.getIndex(d), m = c(f, e);
      e.get(m).focus(), u.stopPropagation(), u.preventDefault();
    };
  }
  function a(c, u) {
    return c === u - 1 ? 0 : c + 1;
  }
  function l(c, u) {
    return c === 0 ? u - 1 : c - 1;
  }
}
const Ed = class Ed extends de {
  /**
  * @inheritDoc
  */
  constructor() {
    super();
    const t = this.bindTemplate;
    this.set("content", ""), this.set("viewBox", "0 0 20 20"), this.set("fillColor", ""), this.set("isColorInherited", !0), this.set("isVisible", !0), this.setTemplate({
      tag: "svg",
      ns: "http://www.w3.org/2000/svg",
      attributes: {
        class: [
          "ck",
          "ck-icon",
          t.if("isVisible", "ck-hidden", (e) => !e),
          // Exclude icon internals from the CSS reset to allow rich (non-monochromatic) icons
          // (https://github.com/ckeditor/ckeditor5/issues/12599).
          "ck-reset_all-excluded",
          // The class to remove the dynamic color inheritance is toggleable
          // (https://github.com/ckeditor/ckeditor5/issues/12599).
          t.if("isColorInherited", "ck-icon_inherit-color")
        ],
        viewBox: t.to("viewBox")
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this._updateXMLContent(), this._colorFillPaths(), this.on("change:content", () => {
      this._updateXMLContent(), this._colorFillPaths();
    }), this.on("change:fillColor", () => {
      this._colorFillPaths();
    });
  }
  /**
  * Updates the {@link #element} with the value of {@link #content}.
  */
  _updateXMLContent() {
    if (this.content) {
      const e = new DOMParser().parseFromString(this.content.trim(), "image/svg+xml").querySelector("svg"), n = e.getAttribute("viewBox");
      n && (this.viewBox = n);
      for (const { name: r, value: s } of Array.from(e.attributes))
        Ed.presentationalAttributeNames.includes(r) && this.element.setAttribute(r, s);
      for (; this.element.firstChild; )
        this.element.removeChild(this.element.firstChild);
      for (; e.childNodes.length > 0; )
        this.element.appendChild(e.childNodes[0]);
    }
  }
  /**
  * Fills all child `path.ck-icon__fill` with the `#fillColor`.
  */
  _colorFillPaths() {
    this.fillColor && this.element.querySelectorAll(".ck-icon__fill").forEach((t) => {
      t.style.fill = this.fillColor;
    });
  }
};
/**
* A list of presentational attributes that can be set on the `<svg>` element and should be preserved
* when the icon {@link module:ui/icon/iconview~IconView#content content} is loaded.
*
* See the [specification](https://www.w3.org/TR/SVG/styling.html#TermPresentationAttribute) to learn more.
*/
h(Ed, "presentationalAttributeNames", [
  "alignment-baseline",
  "baseline-shift",
  "clip-path",
  "clip-rule",
  "color",
  "color-interpolation",
  "color-interpolation-filters",
  "color-rendering",
  "cursor",
  "direction",
  "display",
  "dominant-baseline",
  "fill",
  "fill-opacity",
  "fill-rule",
  "filter",
  "flood-color",
  "flood-opacity",
  "font-family",
  "font-size",
  "font-size-adjust",
  "font-stretch",
  "font-style",
  "font-variant",
  "font-weight",
  "image-rendering",
  "letter-spacing",
  "lighting-color",
  "marker-end",
  "marker-mid",
  "marker-start",
  "mask",
  "opacity",
  "overflow",
  "paint-order",
  "pointer-events",
  "shape-rendering",
  "stop-color",
  "stop-opacity",
  "stroke",
  "stroke-dasharray",
  "stroke-dashoffset",
  "stroke-linecap",
  "stroke-linejoin",
  "stroke-miterlimit",
  "stroke-opacity",
  "stroke-width",
  "text-anchor",
  "text-decoration",
  "text-overflow",
  "text-rendering",
  "transform",
  "unicode-bidi",
  "vector-effect",
  "visibility",
  "white-space",
  "word-spacing",
  "writing-mode"
]);
let pr = Ed;
class zC extends de {
  /**
  * @inheritDoc
  */
  constructor() {
    super(), this.set({
      style: void 0,
      text: void 0,
      id: void 0
    });
    const t = this.bindTemplate;
    this.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-button__label"
        ],
        style: t.to("style"),
        id: t.to("id")
      },
      children: [
        {
          text: t.to("text")
        }
      ]
    });
  }
}
class Te extends de {
  /**
  * Creates an instance of the button view class.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param labelView The instance of the button's label. If not provided, an instance of
  * {@link module:ui/button/buttonlabelview~ButtonLabelView} is used.
  */
  constructor(e, n = new zC()) {
    super(e);
    /**
    * Collection of the child views inside of the button {@link #element}.
    */
    h(this, "children");
    /**
    * Label of the button view. Its text is configurable using the {@link #label label attribute}.
    *
    * If not configured otherwise in the `constructor()`, by default the label is an instance
    * of {@link module:ui/button/buttonlabelview~ButtonLabelView}.
    */
    h(this, "labelView");
    /**
    * The icon view of the button. Will be added to {@link #children} when the
    * {@link #icon icon attribute} is defined.
    */
    h(this, "iconView");
    /**
    * A view displaying the keystroke of the button next to the {@link #labelView label}.
    * Added to {@link #children} when the {@link #withKeystroke `withKeystroke` attribute}
    * is defined.
    */
    h(this, "keystrokeView");
    /**
    * Delayed focus function for focus handling in Safari.
    */
    h(this, "_focusDelayed", null);
    const r = this.bindTemplate, s = Jn();
    this.set("_ariaPressed", !1), this.set("_ariaChecked", !1), this.set("ariaLabel", void 0), this.set("ariaLabelledBy", `ck-editor__aria-label_${s}`), this.set("class", void 0), this.set("labelStyle", void 0), this.set("icon", void 0), this.set("isEnabled", !0), this.set("isOn", !1), this.set("isVisible", !0), this.set("isToggleable", !1), this.set("keystroke", void 0), this.set("label", void 0), this.set("role", void 0), this.set("tabindex", -1), this.set("tooltip", !1), this.set("tooltipPosition", "s"), this.set("type", "button"), this.set("withText", !1), this.set("withKeystroke", !1), this.children = this.createCollection(), this.labelView = this._setupLabelView(n), this.iconView = new pr(), this.iconView.extendTemplate({
      attributes: {
        class: "ck-button__icon"
      }
    }), this.keystrokeView = this._createKeystrokeView(), this.bind("_tooltipString").to(this, "tooltip", this, "label", this, "keystroke", this._getTooltipString.bind(this));
    const o = {
      tag: "button",
      attributes: {
        class: [
          "ck",
          "ck-button",
          r.to("class"),
          r.if("isEnabled", "ck-disabled", (a) => !a),
          r.if("isVisible", "ck-hidden", (a) => !a),
          r.to("isOn", (a) => a ? "ck-on" : "ck-off"),
          r.if("withText", "ck-button_with-text"),
          r.if("withKeystroke", "ck-button_with-keystroke")
        ],
        role: r.to("role"),
        type: r.to("type", (a) => a || "button"),
        tabindex: r.to("tabindex"),
        "aria-checked": r.to("_ariaChecked"),
        "aria-pressed": r.to("_ariaPressed"),
        "aria-label": r.to("ariaLabel"),
        "aria-labelledby": r.to("ariaLabelledBy"),
        "aria-disabled": r.if("isEnabled", !0, (a) => !a),
        "data-cke-tooltip-text": r.to("_tooltipString"),
        "data-cke-tooltip-position": r.to("tooltipPosition")
      },
      children: this.children,
      on: {
        click: r.to((a) => {
          this.isEnabled ? this.fire("execute") : a.preventDefault();
        })
      }
    };
    this.bind("_ariaPressed").to(this, "isOn", this, "isToggleable", this, "role", (a, l, c) => !l || Pv(c) ? !1 : String(!!a)), this.bind("_ariaChecked").to(this, "isOn", this, "isToggleable", this, "role", (a, l, c) => !l || !Pv(c) ? !1 : String(!!a)), Me.isSafari && (this._focusDelayed || (this._focusDelayed = Yg(() => this.focus(), 0)), o.on.mousedown = r.to(() => {
      this._focusDelayed();
    }), o.on.mouseup = r.to(() => {
      this._focusDelayed.cancel();
    })), this.setTemplate(o);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.icon && (this.iconView.bind("content").to(this, "icon"), this.children.add(this.iconView)), this.children.add(this.labelView), this.withKeystroke && this.keystroke && this.children.add(this.keystrokeView);
  }
  /**
  * Focuses the {@link #element} of the button.
  */
  focus() {
    this.element.focus();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._focusDelayed && this._focusDelayed.cancel(), super.destroy();
  }
  /**
  * Binds the label view instance it with button attributes.
  */
  _setupLabelView(e) {
    return e.bind("text", "style", "id").to(this, "label", "labelStyle", "ariaLabelledBy"), e;
  }
  /**
  * Creates a view that displays a keystroke next to a {@link #labelView label }
  * and binds it with button attributes.
  */
  _createKeystrokeView() {
    const e = new de();
    return e.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-button__keystroke"
        ]
      },
      children: [
        {
          text: this.bindTemplate.to("keystroke", (n) => Bu(n))
        }
      ]
    }), e;
  }
  /**
  * Gets the text for the tooltip from the combination of
  * {@link #tooltip}, {@link #label} and {@link #keystroke} attributes.
  *
  * @see #tooltip
  * @see #_tooltipString
  * @param tooltip Button tooltip.
  * @param label Button label.
  * @param keystroke Button keystroke.
  */
  _getTooltipString(e, n, r) {
    return e ? typeof e == "string" ? e : (r && (r = Bu(r)), e instanceof Function ? e(n, r) : `${n}${r ? ` (${r})` : ""}`) : "";
  }
}
function Pv(i) {
  switch (i) {
    case "radio":
    case "checkbox":
    case "option":
    case "switch":
    case "menuitemcheckbox":
    case "menuitemradio":
      return !0;
    default:
      return !1;
  }
}
class Wd extends de {
  /**
  * Creates an instance of the form header class.
  *
  * @param locale The locale instance.
  * @param options.label A label.
  * @param options.class An additional class.
  */
  constructor(e, n = {}) {
    super(e);
    /**
    * A collection of header items.
    */
    h(this, "children");
    /**
    * The icon view instance. Defined only if icon was passed in the constructor's options.
    */
    h(this, "iconView");
    const r = this.bindTemplate;
    this.set("label", n.label || ""), this.set("class", n.class || null), this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-form__header",
          r.to("class")
        ]
      },
      children: this.children
    }), n.icon && (this.iconView = new pr(), this.iconView.content = n.icon, this.children.add(this.iconView));
    const s = new de(e);
    s.setTemplate({
      tag: "h2",
      attributes: {
        class: [
          "ck",
          "ck-form__header__label"
        ],
        role: "presentation"
      },
      children: [
        {
          text: r.to("label")
        }
      ]
    }), this.children.add(s);
  }
}
class jn extends (/* @__PURE__ */ ht()) {
  /**
  * Creates an instance of the focus cycler utility.
  *
  * @param options Configuration options.
  */
  constructor(e) {
    super();
    /**
    * A {@link module:ui/focuscycler~FocusableView focusable views} collection that the cycler operates on.
    */
    h(this, "focusables");
    /**
    * A focus tracker instance that the cycler uses to determine the current focus
    * state in {@link #focusables}.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}
    * which can respond to certain keystrokes and cycle the focus.
    */
    h(this, "keystrokeHandler");
    /**
    * Actions that the cycler can take when a keystroke is pressed. Requires
    * `options.keystrokeHandler` to be passed and working. When an action is
    * performed, `preventDefault` and `stopPropagation` will be called on the event
    * the keystroke fired in the DOM.
    *
    * ```ts
    * actions: {
    * 	// Will call #focusPrevious() when arrowleft or arrowup is pressed.
    * 	focusPrevious: [ 'arrowleft', 'arrowup' ],
    *
    * 	// Will call #focusNext() when arrowdown is pressed.
    * 	focusNext: 'arrowdown'
    * }
    * ```
    */
    h(this, "actions");
    if (this.focusables = e.focusables, this.focusTracker = e.focusTracker, this.keystrokeHandler = e.keystrokeHandler, this.actions = e.actions, e.actions && e.keystrokeHandler)
      for (const n in e.actions) {
        let r = e.actions[n];
        typeof r == "string" && (r = [
          r
        ]);
        for (const s of r)
          e.keystrokeHandler.set(s, (o, a) => {
            this[n](), a();
          }, e.keystrokeHandlerOptions);
      }
    this.on("forwardCycle", () => this.focusFirst(), {
      priority: "low"
    }), this.on("backwardCycle", () => this.focusLast(), {
      priority: "low"
    });
  }
  /**
  * Returns the first focusable view in {@link #focusables}.
  * Returns `null` if there is none.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  get first() {
    return this.focusables.find(Jh) || null;
  }
  /**
  * Returns the last focusable view in {@link #focusables}.
  * Returns `null` if there is none.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  get last() {
    return this.focusables.filter(Jh).slice(-1)[0] || null;
  }
  /**
  * Returns the next focusable view in {@link #focusables} based on {@link #current}.
  * Returns `null` if there is none.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  get next() {
    return this._getDomFocusableItem(1);
  }
  /**
  * Returns the previous focusable view in {@link #focusables} based on {@link #current}.
  * Returns `null` if there is none.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  get previous() {
    return this._getDomFocusableItem(-1);
  }
  /**
  * An index of the view in the {@link #focusables} which is focused according
  * to {@link #focusTracker}. Returns `null` when there is no such view.
  */
  get current() {
    let e = null;
    return this.focusTracker.focusedElement === null ? null : (this.focusables.find((n, r) => {
      const s = n.element === this.focusTracker.focusedElement;
      return s && (e = r), s;
    }), e);
  }
  /**
  * Focuses the {@link #first} item in {@link #focusables}.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  focusFirst() {
    this._focus(this.first, 1);
  }
  /**
  * Focuses the {@link #last} item in {@link #focusables}.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  focusLast() {
    this._focus(this.last, -1);
  }
  /**
  * Focuses the {@link #next} item in {@link #focusables}.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  focusNext() {
    const e = this.next;
    if (e && this.focusables.getIndex(e) === this.current) {
      this.fire("forwardCycle");
      return;
    }
    e === this.first ? this.fire("forwardCycle") : this._focus(e, 1);
  }
  /**
  * Focuses the {@link #previous} item in {@link #focusables}.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  focusPrevious() {
    const e = this.previous;
    if (e && this.focusables.getIndex(e) === this.current) {
      this.fire("backwardCycle");
      return;
    }
    e === this.last ? this.fire("backwardCycle") : this._focus(e, -1);
  }
  /**
  * Allows for creating continuous focus cycling across multiple focus cyclers and their collections of {@link #focusables}.
  *
  * It starts listening to the {@link module:ui/focuscycler~FocusCyclerForwardCycleEvent} and
  * {@link module:ui/focuscycler~FocusCyclerBackwardCycleEvent} events of the chained focus cycler and engages,
  * whenever the user reaches the last (forwards navigation) or first (backwards navigation) focusable view
  * and would normally start over. Instead, the navigation continues on the higher level (flattens).
  *
  * For instance, for the following nested focus navigation structure, the focus would get stuck the moment
  * the AB gets focused and its focus cycler starts managing it:
  *
  *	   ┌────────────┐   ┌──────────────────────────────────┐   ┌────────────┐
  *	   │ AA         │   │ AB                               │   │ AC         │
  *	   │            │   │                                  │   │            │
  *	   │            │   │    ┌─────┐  ┌─────┐  ┌─────┐     │   │            │
  *	   │            │   │ ┌──► ABA ├──► ABB ├──► ABC ├───┐ │   │            │
  *	   │            ├───► │  └─────┘  └─────┘  └─────┘   │ │   │            │
  *	   │            │   │ │                              │ │   │            │
  *	   │            │   │ │                              │ │   │            │
  *	   │            │   │ └──────────────────────────────┘ │   │            │
  *	   │            │   │                                  │   │            │
  *	   └────────────┘   └──────────────────────────────────┘   └────────────┘
  *
  * Chaining a focus tracker that manages AA, AB, and AC with the focus tracker that manages ABA, ABB, and ABC
  * creates a seamless navigation experience instead:
  *
  *	   ┌────────────┐   ┌──────────────────────────────────┐   ┌────────────┐
  *	   │ AA         │   │ AB                               │   │ AC         │
  *	   │            │   │                                  │   │            │
  *	   │            │   │    ┌─────┐  ┌─────┐  ┌─────┐     │   │            │
  *	   │            │   │ ┌──► ABA ├──► ABB ├──► ABC ├──┐  │   │            │
  *	┌──►            ├───┼─┘  └─────┘  └─────┘  └─────┘  └──┼───►            ├──┐
  *	│  │            │   │                                  │   │            │  │
  *	│  │            │   │                                  │   │            │  │
  *	│  │            │   │                                  │   │            │  │
  *	│  │            │   │                                  │   │            │  │
  *	│  └────────────┘   └──────────────────────────────────┘   └────────────┘  │
  *	│                                                                          │
  *	│                                                                          │
  *	└──────────────────────────────────────────────────────────────────────────┘
  *
  * See {@link #unchain} to reverse the chaining.
  */
  chain(e) {
    const n = () => this.current === null ? null : this.focusables.get(this.current);
    this.listenTo(e, "forwardCycle", (r) => {
      const s = n();
      this.focusNext(), s !== n() && r.stop();
    }, {
      priority: "low"
    }), this.listenTo(e, "backwardCycle", (r) => {
      const s = n();
      this.focusPrevious(), s !== n() && r.stop();
    }, {
      priority: "low"
    });
  }
  /**
  * Reverses a chaining made by {@link #chain}.
  */
  unchain(e) {
    this.stopListening(e);
  }
  /**
  * Focuses the given view if it exists.
  *
  * @param view The view to be focused
  * @param direction The direction of the focus if the view has focusable children.
  * @returns
  */
  _focus(e, n) {
    e && this.focusTracker.focusedElement !== e.element && e.focus(n);
  }
  /**
  * Returns the next or previous focusable view in {@link #focusables} with respect
  * to {@link #current}.
  *
  * @param step Either `1` for checking forward from {@link #current} or `-1` for checking backwards.
  */
  _getDomFocusableItem(e) {
    const n = this.focusables.length;
    if (!n)
      return null;
    const r = this.current;
    if (r === null)
      return this[e === 1 ? "first" : "last"];
    let s = this.focusables.get(r), o = (r + n + e) % n;
    do {
      const a = this.focusables.get(o);
      if (Jh(a)) {
        s = a;
        break;
      }
      o = (o + n + e) % n;
    } while (o !== r);
    return s;
  }
}
function Jh(i) {
  return vc(i) && mr(i.element);
}
function vc(i) {
  return "focus" in i && typeof i.focus == "function";
}
function aM(i) {
  return vc(i) && "focusCycler" in i && i.focusCycler instanceof jn;
}
function lM(i) {
  class t extends i {
    /**
    * @inheritDoc
    */
    constructor(...r) {
      super(...r);
      /**
      * A bound version of {@link #_onDrag}.
      */
      h(this, "_onDragBound", this._onDrag.bind(this));
      /**
      * A bound version of {@link #_onDragEnd}.
      */
      h(this, "_onDragEndBound", this._onDragEnd.bind(this));
      /**
      * The last coordinates of the view. It is updated on every mouse move.
      */
      h(this, "_lastDraggingCoordinates", {
        x: 0,
        y: 0
      });
      this.on("render", () => {
        this._attachListeners();
      }), this.set("isDragging", !1);
    }
    /**
    * Attaches the listeners for the drag start.
    */
    _attachListeners() {
      this.listenTo(this.element, "mousedown", this._onDragStart.bind(this)), this.listenTo(this.element, "touchstart", this._onDragStart.bind(this));
    }
    /**
    * Attaches the listeners for the dragging and drag end.
    */
    _attachDragListeners() {
      this.listenTo(ae.document, "mouseup", this._onDragEndBound), this.listenTo(ae.document, "touchend", this._onDragEndBound), this.listenTo(ae.document, "mousemove", this._onDragBound), this.listenTo(ae.document, "touchmove", this._onDragBound);
    }
    /**
    * Detaches the listeners after the drag end.
    */
    _detachDragListeners() {
      this.stopListening(ae.document, "mouseup", this._onDragEndBound), this.stopListening(ae.document, "touchend", this._onDragEndBound), this.stopListening(ae.document, "mousemove", this._onDragBound), this.stopListening(ae.document, "touchmove", this._onDragBound);
    }
    /**
    * Starts the dragging listeners and sets the initial view coordinates.
    */
    _onDragStart(r, s) {
      if (!this._isHandleElementPressed(s))
        return;
      this._attachDragListeners();
      let o = 0, a = 0;
      s instanceof MouseEvent ? (o = s.clientX, a = s.clientY) : (o = s.touches[0].clientX, a = s.touches[0].clientY), this._lastDraggingCoordinates = {
        x: o,
        y: a
      }, this.isDragging = !0;
    }
    /**
    * Updates the view coordinates and fires the `drag` event.
    */
    _onDrag(r, s) {
      if (!this.isDragging) {
        this._detachDragListeners();
        return;
      }
      let o = 0, a = 0;
      s instanceof MouseEvent ? (o = s.clientX, a = s.clientY) : (o = s.touches[0].clientX, a = s.touches[0].clientY), s.preventDefault(), this.fire("drag", {
        deltaX: Math.round(o - this._lastDraggingCoordinates.x),
        deltaY: Math.round(a - this._lastDraggingCoordinates.y)
      }), this._lastDraggingCoordinates = {
        x: o,
        y: a
      };
    }
    /**
    * Stops the dragging and detaches the listeners.
    */
    _onDragEnd() {
      this._detachDragListeners(), this.isDragging = !1;
    }
    /**
    * Checks if the drag handle element was pressed.
    */
    _isHandleElementPressed(r) {
      return this.dragHandleElement ? this.dragHandleElement === r.target || r.target instanceof HTMLElement && this.dragHandleElement.contains(r.target) : !1;
    }
  }
  return t;
}
class cM extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of button views.
    */
    h(this, "children");
    /**
    * A keystroke handler instance.
    */
    h(this, "keystrokes");
    /**
    * A focus cycler instance.
    */
    h(this, "focusCycler");
    /**
    * A focus tracker instance.
    */
    h(this, "_focusTracker");
    /**
    * A collection of focusable views.
    */
    h(this, "_focusables");
    this.children = this.createCollection(), this.keystrokes = new Ot(), this._focusTracker = new wt(), this._focusables = new In(), this.focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this._focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-dialog__actions"
        ]
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element);
  }
  /**
  * Creates the button views based on the given definitions.
  * Then adds them to the {@link #children} collection and to the focus cycler.
  */
  setButtons(e) {
    for (const n of e) {
      const r = new Te(this.locale);
      let s;
      r.on("execute", () => n.onExecute()), n.onCreate && n.onCreate(r);
      for (s in n)
        s != "onExecute" && s != "onCreate" && r.set(s, n[s]);
      this.children.add(r);
    }
    this._updateFocusCyclableItems();
  }
  /**
  * @inheritDoc
  */
  focus(e) {
    e === -1 ? this.focusCycler.focusLast() : this.focusCycler.focusFirst();
  }
  /**
  * Adds all elements from the {@link #children} collection to the {@link #_focusables} collection
  * and to the {@link #_focusTracker} instance.
  */
  _updateFocusCyclableItems() {
    Array.from(this.children).forEach((e) => {
      this._focusables.add(e), this._focusTracker.add(e.element);
    });
  }
}
class uM extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of content items.
    */
    h(this, "children");
    this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-dialog__content"
        ]
      },
      children: this.children
    });
  }
  /**
  * Removes all the child views.
  */
  reset() {
    for (; this.children.length; )
      this.children.remove(0);
  }
}
const Ii = {
  SCREEN_CENTER: "screen-center",
  EDITOR_CENTER: "editor-center",
  EDITOR_TOP_SIDE: "editor-top-side",
  EDITOR_TOP_CENTER: "editor-top-center",
  EDITOR_BOTTOM_CENTER: "editor-bottom-center",
  EDITOR_ABOVE_CENTER: "editor-above-center",
  EDITOR_BELOW_CENTER: "editor-below-center"
}, Iv = /* @__PURE__ */ vs("px"), Td = class Td extends (/* @__PURE__ */ lM(de)) {
  /**
  * @inheritDoc
  */
  constructor(e, { getCurrentDomRoot: n, getViewportOffset: r }) {
    super(e);
    /**
    * A collection of the child views inside of the dialog.
    * A dialog can have 3 optional parts: header, content, and actions.
    */
    h(this, "parts");
    /**
    * A header view of the dialog. It is also a drag handle of the dialog.
    */
    h(this, "headerView");
    /**
    * A close button view. It is automatically added to the header view if present.
    */
    h(this, "closeButtonView");
    /**
    * A view with the action buttons available to the user.
    */
    h(this, "actionsView");
    /**
    * A view with the dialog content.
    */
    h(this, "contentView");
    /**
    * A keystroke handler instance.
    */
    h(this, "keystrokes");
    /**
    * A focus tracker instance.
    */
    h(this, "focusTracker");
    /**
    * A flag indicating if the dialog was moved manually. If so, its position
    * will not be updated automatically upon window resize or document scroll.
    */
    h(this, "wasMoved", !1);
    /**
    * A callback returning the DOM root that requested the dialog.
    */
    h(this, "_getCurrentDomRoot");
    /**
    * A callback returning the configured editor viewport offset.
    */
    h(this, "_getViewportOffset");
    /**
    * The list of the focusable elements inside the dialog view.
    */
    h(this, "_focusables");
    /**
    * The focus cycler instance.
    */
    h(this, "_focusCycler");
    const s = this.bindTemplate, o = e.t;
    this.set("className", ""), this.set("ariaLabel", o("Editor dialog")), this.set("isModal", !1), this.set("position", Ii.SCREEN_CENTER), this.set("_isVisible", !1), this.set("_isTransparent", !1), this.set("_top", 0), this.set("_left", 0), this._getCurrentDomRoot = n, this._getViewportOffset = r, this.decorate("moveTo"), this.parts = this.createCollection(), this.keystrokes = new Ot(), this.focusTracker = new wt(), this._focusables = new In(), this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-dialog-overlay",
          s.if("isModal", "ck-dialog-overlay__transparent", (a) => !a),
          s.if("_isVisible", "ck-hidden", (a) => !a)
        ],
        // Prevent from editor losing focus when clicking on the modal overlay.
        tabindex: "-1"
      },
      children: [
        {
          tag: "div",
          attributes: {
            tabindex: "-1",
            class: [
              "ck",
              "ck-dialog",
              s.if("isModal", "ck-dialog_modal"),
              s.to("className")
            ],
            role: "dialog",
            "aria-label": s.to("ariaLabel"),
            style: {
              top: s.to("_top", (a) => Iv(a)),
              left: s.to("_left", (a) => Iv(a)),
              visibility: s.if("_isTransparent", "hidden")
            }
          },
          children: this.parts
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.set("Esc", (e, n) => {
      this.fire("close", {
        source: "escKeyPress"
      }), n();
    }), this.on("drag", (e, { deltaX: n, deltaY: r }) => {
      this.wasMoved = !0, this.moveBy(n, r);
    }), this.listenTo(ae.window, "resize", () => {
      this._isVisible && !this.wasMoved && this.updatePosition();
    }), this.listenTo(ae.document, "scroll", () => {
      this._isVisible && !this.wasMoved && this.updatePosition();
    }), this.on("change:_isVisible", (e, n, r) => {
      r && (this._isTransparent = !0, setTimeout(() => {
        this.updatePosition(), this._isTransparent = !1, this.focus();
      }, 10));
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * Returns the element that should be used as a drag handle.
  */
  get dragHandleElement() {
    return this.headerView && !this.isModal ? this.headerView.element : null;
  }
  /**
  * Creates the dialog parts. Which of them are created depends on the arguments passed to the method.
  * There are no rules regarding the dialog construction, that is, no part is mandatory.
  * Each part can only be created once.
  *
  * @internal
  */
  setupParts({ icon: e, title: n, hasCloseButton: r = !0, content: s, actionButtons: o }) {
    n && (this.headerView = new Wd(this.locale, {
      icon: e
    }), r && (this.closeButtonView = this._createCloseButton(), this.headerView.children.add(this.closeButtonView)), this.headerView.label = n, this.ariaLabel = n, this.parts.add(this.headerView, 0)), s && (s instanceof de && (s = [
      s
    ]), this.contentView = new uM(this.locale), this.contentView.children.addMany(s), this.parts.add(this.contentView)), o && (this.actionsView = new cM(this.locale), this.actionsView.setButtons(o), this.parts.add(this.actionsView)), this._updateFocusCyclableItems();
  }
  /**
  * Focuses the first focusable element inside the dialog.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Normalizes the passed coordinates to make sure the dialog view
  * is displayed within the visible viewport and moves it there.
  *
  * @internal
  */
  moveTo(e, n) {
    const r = this._getViewportRect(), s = this._getDialogRect();
    e + s.width > r.right && (e = r.right - s.width), e < r.left && (e = r.left), n < r.top && (n = r.top), this._moveTo(e, n);
  }
  /**
  * Moves the dialog to the specified coordinates.
  */
  _moveTo(e, n) {
    this._left = e, this._top = n;
  }
  /**
  * Moves the dialog by the specified offset.
  *
  * @internal
  */
  moveBy(e, n) {
    this.moveTo(this._left + e, this._top + n);
  }
  /**
  * Moves the dialog view to the off-screen position.
  * Used when there is no space to display the dialog.
  */
  _moveOffScreen() {
    this._moveTo(-9999, -9999);
  }
  /**
  * Recalculates the dialog according to the set position and viewport,
  * and moves it to the new position.
  */
  updatePosition() {
    if (!this.element || !this.element.parentNode)
      return;
    const e = this._getViewportRect();
    let n = this.position, r;
    this._getCurrentDomRoot() ? r = this._getVisibleDomRootRect(e) : n = Ii.SCREEN_CENTER;
    const s = Td.defaultOffset, o = this._getDialogRect();
    switch (n) {
      case Ii.EDITOR_TOP_SIDE: {
        if (r) {
          const a = this.locale.contentLanguageDirection === "ltr" ? r.right - o.width - s : r.left + s;
          this.moveTo(a, r.top + s);
        } else
          this._moveOffScreen();
        break;
      }
      case Ii.EDITOR_CENTER: {
        r ? this.moveTo(Math.round(r.left + r.width / 2 - o.width / 2), Math.round(r.top + r.height / 2 - o.height / 2)) : this._moveOffScreen();
        break;
      }
      case Ii.SCREEN_CENTER: {
        this.moveTo(Math.round((e.width - o.width) / 2), Math.round((e.height - o.height) / 2));
        break;
      }
      case Ii.EDITOR_TOP_CENTER: {
        r ? this.moveTo(Math.round(r.left + r.width / 2 - o.width / 2), r.top + s) : this._moveOffScreen();
        break;
      }
      case Ii.EDITOR_BOTTOM_CENTER: {
        r ? this.moveTo(Math.round(r.left + r.width / 2 - o.width / 2), r.bottom - o.height - s) : this._moveOffScreen();
        break;
      }
      case Ii.EDITOR_ABOVE_CENTER: {
        r ? this.moveTo(Math.round(r.left + r.width / 2 - o.width / 2), r.top - o.height - s) : this._moveOffScreen();
        break;
      }
      case Ii.EDITOR_BELOW_CENTER: {
        r ? this.moveTo(Math.round(r.left + r.width / 2 - o.width / 2), r.bottom + s) : this._moveOffScreen();
        break;
      }
    }
  }
  /**
  * Calculates the visible DOM root part.
  */
  _getVisibleDomRootRect(e) {
    let n = new je(this._getCurrentDomRoot()).getVisible();
    if (n) {
      if (n = e.getIntersection(n), !n)
        return null;
    } else
      return null;
    return n;
  }
  /**
  * Calculates the dialog element rect.
  */
  _getDialogRect() {
    return new je(this.element.firstElementChild);
  }
  /**
  * Returns a viewport `Rect` shrunk by the viewport offset config from all sides.
  *
  * TODO: This is a duplicate from position.ts module. It should either be exported there or land somewhere in utils.
  */
  _getViewportRect() {
    const e = new je(ae.window);
    if (this.isModal)
      return e;
    const n = {
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      ...this._getViewportOffset()
    };
    return e.top += n.top, e.height -= n.top, e.bottom -= n.bottom, e.height -= n.bottom, e.left += n.left, e.right -= n.right, e.width -= n.left + n.right, e;
  }
  /**
  * Collects all focusable elements inside the dialog parts
  * and adds them to the focus tracker and focus cycler.
  */
  _updateFocusCyclableItems() {
    const e = [];
    if (this.contentView)
      for (const n of this.contentView.children)
        vc(n) && e.push(n);
    this.actionsView && e.push(this.actionsView), this.closeButtonView && e.push(this.closeButtonView), e.forEach((n) => {
      this._focusables.add(n), this.focusTracker.add(n.element), aM(n) && this._focusCycler.chain(n.focusCycler);
    });
  }
  /**
  * Creates the close button view that is displayed in the header view corner.
  */
  _createCloseButton() {
    const e = new Te(this.locale), n = this.locale.t;
    return e.set({
      label: n("Close"),
      tooltip: !0,
      icon: ee.cancel
    }), e.on("execute", () => this.fire("close", {
      source: "closeButton"
    })), e;
  }
};
/**
* A default dialog element offset from the reference element (e.g. editor editable area).
*/
h(Td, "defaultOffset", 15);
let $l = Td;
var pi;
let Ud = (pi = class extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The currently visible dialog view instance.
    */
    h(this, "view");
    /**
    * A configurable callback called when the dialog is hidden.
    */
    h(this, "_onHide");
    const n = e.t;
    this._initShowHideListeners(), this._initFocusToggler(), this._initMultiRootIntegration(), this.set({
      id: null,
      isOpen: !1
    }), e.accessibility.addKeystrokeInfos({
      categoryId: "navigation",
      keystrokes: [
        {
          label: n("Move focus in and out of an active dialog window"),
          keystroke: "Ctrl+F6",
          mayRequireFn: !0
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Dialog";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._unlockBodyScroll();
  }
  /**
  * Initiates listeners for the `show` and `hide` events emitted by this plugin.
  *
  * We could not simply decorate the {@link #show} and {@link #hide} methods to fire events,
  * because they would be fired in the wrong order &ndash; first would be `show` and then `hide`
  * (because showing the dialog actually starts with hiding the previously visible one).
  * Hence, we added private methods {@link #_show} and {@link #_hide} which are called on events
  * in the desired sequence.
  */
  _initShowHideListeners() {
    this.on("show", (e, n) => {
      this._show(n);
    }), this.on("show", (e, n) => {
      n.onShow && n.onShow(this);
    }, {
      priority: "low"
    }), this.on("hide", () => {
      pi._visibleDialogPlugin && pi._visibleDialogPlugin._hide();
    }), this.on("hide", () => {
      this._onHide && (this._onHide(this), this._onHide = void 0);
    }, {
      priority: "low"
    });
  }
  /**
  * Initiates keystroke handler for toggling the focus between the editor and the dialog view.
  */
  _initFocusToggler() {
    const e = this.editor;
    e.keystrokes.set("Ctrl+F6", (n, r) => {
      !this.isOpen || this.view.isModal || (this.view.focusTracker.isFocused ? e.editing.view.focus() : this.view.focus(), r());
    });
  }
  /**
  * Provides an integration between the root attaching and detaching and positioning of the view.
  */
  _initMultiRootIntegration() {
    const e = this.editor.model;
    e.document.on("change:data", () => {
      if (!this.view)
        return;
      const n = e.document.differ.getChangedRoots();
      for (const r of n)
        r.state && this.view.updatePosition();
    });
  }
  /**
  * Displays a dialog window.
  *
  * This method requires a {@link ~DialogDefinition} that defines the dialog's content, title, icon, action buttons, etc.
  *
  * For example, the following definition will create a dialog with:
  * * A header consisting of an icon, a title, and a "Close" button (it is added by default).
  * * A content consisting of a view with a single paragraph.
  * * A footer consisting of two buttons: "Yes" and "No".
  *
  * ```js
  * // Create the view that will be used as the dialog's content.
  * const textView = new View( locale );
  *
  * textView.setTemplate( {
  * 	tag: 'div',
  * 	attributes: {
  * 		style: {
  * 			padding: 'var(--ck-spacing-large)',
  * 			whiteSpace: 'initial',
  * 			width: '100%',
  * 			maxWidth: '500px'
  * 		},
  * 		tabindex: -1
  * 	},
  * 	children: [
  * 		'Lorem ipsum dolor sit amet...'
  * 	]
  * } );
  *
  * // Show the dialog.
  * editor.plugins.get( 'Dialog' ).show( {
  *	id: 'myDialog',
  * 	icon: 'myIcon', // This should be an SVG string.
  * 	title: 'My dialog',
  * 	content: textView,
  * 	actionButtons: [
  *		{
  *			label: t( 'Yes' ),
  *			class: 'ck-button-action',
  *			withText: true,
  *			onExecute: () => dialog.hide()
  *		},
  *		{
  *			label: t( 'No' ),
  *			withText: true,
  *			onExecute: () => dialog.hide()
  *		}
  *	]
  * } );
  * ```
  *
  * By specifying the {@link ~DialogDefinition#onShow} and {@link ~DialogDefinition#onHide} callbacks
  * it is also possible to add callbacks that will be called when the dialog is shown or hidden.
  *
  * For example, the callbacks in the following definition:
  * * Disable the default behavior of the <kbd>Esc</kbd> key.
  * * Fire a custom event when the dialog gets hidden.
  *
  * ```js
  * editor.plugins.get( 'Dialog' ).show( {
  * 	// ...
  * 	onShow: dialog => {
  * 		dialog.view.on( 'close', ( evt, data ) => {
  * 			// Only prevent the event from the "Esc" key - do not affect the other ways of closing the dialog.
  * 			if ( data.source === 'escKeyPress' ) {
  * 				evt.stop();
  * 			}
  * 		} );
  * 	},
  * 	onHide: dialog => {
  * 		dialog.fire( 'dialogDestroyed' );
  * 	}
  * } );
  * ```
  *
  * Internally, calling this method:
  * 1. Hides the currently visible dialog (if any) calling the {@link #hide} method
  * (fires the {@link ~DialogHideEvent hide event}).
  * 2. Fires the {@link ~DialogShowEvent show event} which allows for adding callbacks that customize the
  * behavior of the dialog.
  * 3. Shows the dialog.
  */
  show(e) {
    this.hide(), this.fire(`show:${e.id}`, e);
  }
  /**
  * Handles creating the {@link module:ui/dialog/dialogview~DialogView} instance and making it visible.
  */
  _show({ id: e, icon: n, title: r, hasCloseButton: s = !0, content: o, actionButtons: a, className: l, isModal: c, position: u, onHide: d }) {
    const f = this.editor;
    this.view = new $l(f.locale, {
      getCurrentDomRoot: () => f.editing.view.getDomRoot(f.model.document.selection.anchor.root.rootName),
      getViewportOffset: () => f.ui.viewportOffset
    });
    const m = this.view;
    m.on("close", () => {
      this.hide();
    }), f.ui.view.body.add(m), f.keystrokes.listenTo(m.element), u || (u = c ? Ii.SCREEN_CENTER : Ii.EDITOR_CENTER), c && this._lockBodyScroll(), m.set({
      position: u,
      _isVisible: !0,
      className: l,
      isModal: c
    }), m.setupParts({
      icon: n,
      title: r,
      hasCloseButton: s,
      content: o,
      actionButtons: a
    }), this.id = e, d && (this._onHide = d), this.isOpen = !0, pi._visibleDialogPlugin = this;
  }
  /**
  * Hides the dialog. This method is decorated to enable interacting on the {@link ~DialogHideEvent hide event}.
  *
  * See {@link #show}.
  */
  hide() {
    pi._visibleDialogPlugin && pi._visibleDialogPlugin.fire(`hide:${pi._visibleDialogPlugin.id}`);
  }
  /**
  * Destroys the {@link module:ui/dialog/dialogview~DialogView} and cleans up the stored dialog state.
  */
  _hide() {
    if (!this.view)
      return;
    const e = this.editor, n = this.view;
    n.isModal && this._unlockBodyScroll(), n.contentView && n.contentView.reset(), e.ui.view.body.remove(n), e.ui.focusTracker.remove(n.element), e.keystrokes.stopListening(n.element), n.destroy(), e.editing.view.focus(), this.id = null, this.isOpen = !1, pi._visibleDialogPlugin = null;
  }
  /**
  * Makes the <body> unscrollable (e.g. when the modal shows up).
  */
  _lockBodyScroll() {
    document.documentElement.classList.add("ck-dialog-scroll-locked");
  }
  /**
  * Makes the <body> scrollable again (e.g. once the modal hides).
  */
  _unlockBodyScroll() {
    document.documentElement.classList.remove("ck-dialog-scroll-locked");
  }
}, /**
* The `Dialog` plugin instance which most recently showed the dialog.
*
* Only one dialog can be visible at once, even if there are many editor instances on the page.
* If an editor wants to show a dialog, it should first hide the dialog that is already opened.
* But only the `Dialog` instance that showed the dialog is able able to properly hide it.
* This is why we need to store it in a globally available space (static property).
* This way every `Dialog` plugin in every editor is able to correctly close any open dialog window.
*/
h(pi, "_visibleDialogPlugin"), pi);
class na extends Te {
  /**
  * @inheritDoc
  */
  constructor(e, n = new zC()) {
    super(e, n);
    /**
    * Holds the view for the check icon of a button list item.
    */
    h(this, "_checkIconHolderView", new dM());
    this.set({
      hasCheckSpace: !1,
      _hasCheck: this.isToggleable
    });
    const r = this.bindTemplate;
    this.extendTemplate({
      attributes: {
        class: [
          "ck-list-item-button",
          r.if("isToggleable", "ck-list-item-button_toggleable")
        ]
      }
    }), this.bind("_hasCheck").to(this, "hasCheckSpace", this, "isToggleable", (s, o) => s || o);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this._hasCheck && this.children.add(this._checkIconHolderView, 0), this._watchCheckIconHolderMount();
  }
  /**
  * Renders the check icon if the button is toggleable.
  */
  _watchCheckIconHolderMount() {
    this._checkIconHolderView.bind("isOn").to(this, "isOn", (e) => this.isToggleable && e), this.on("change:_hasCheck", (e, n, r) => {
      const { children: s, _checkIconHolderView: o } = this;
      r ? s.add(o, 0) : s.remove(o);
    });
  }
}
class dM extends de {
  /**
  * @inheritDoc
  */
  constructor() {
    super();
    /**
    * Collection of child views.
    */
    h(this, "children");
    /**
    * The view for the check icon of the button list item.
    */
    h(this, "_checkIconView", this._createCheckIconView());
    const e = this.bindTemplate;
    this.children = this.createCollection(), this.set("isOn", !1), this.setTemplate({
      tag: "span",
      children: this.children,
      attributes: {
        class: [
          "ck",
          "ck-list-item-button__check-holder",
          e.to("isOn", (n) => n ? "ck-on" : "ck-off")
        ]
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.isOn && this.children.add(this._checkIconView, 0), this._watchCheckIconMount();
  }
  /**
  * Renders the check icon if the button is toggleable.
  */
  _watchCheckIconMount() {
    this.on("change:isOn", (e, n, r) => {
      const { children: s, _checkIconView: o } = this;
      r && !s.has(o) ? s.add(o) : !r && s.has(o) && s.remove(o);
    });
  }
  /**
  * Creates a check icon view.
  */
  _createCheckIconView() {
    const e = new pr();
    return e.content = ee.check, e.extendTemplate({
      attributes: {
        class: "ck-list-item-button__check-icon"
      }
    }), e;
  }
}
class At extends na {
  /**
  * Creates an instance of the menu bar list button view.
  *
  * @param locale The localization services instance.
  */
  constructor(t) {
    super(t), this.set({
      withText: !0,
      withKeystroke: !0,
      tooltip: !1,
      role: "menuitem"
    }), this.extendTemplate({
      attributes: {
        class: [
          "ck-menu-bar__menu__item__button"
        ]
      }
    });
  }
}
class Oi extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * An unique id of the label. It can be used by other UI components to reference
    * the label, for instance, using the `aria-describedby` DOM attribute.
    */
    h(this, "id");
    this.set("text", void 0), this.set("for", void 0), this.id = `ck-editor__label_${Jn()}`;
    const n = this.bindTemplate;
    this.setTemplate({
      tag: "label",
      attributes: {
        class: [
          "ck",
          "ck-label"
        ],
        id: this.id,
        for: n.to("for")
      },
      children: [
        {
          text: n.to("text")
        }
      ]
    });
  }
}
class hM extends de {
  /**
  * @inheritDoc
  */
  constructor(t, e) {
    super(t);
    const n = t.t, r = new Oi();
    r.text = n("Help Contents. To close this dialog press ESC."), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-accessibility-help-dialog__content"
        ],
        "aria-labelledby": r.id,
        role: "document",
        tabindex: -1
      },
      children: [
        xn(document, "p", {}, n("Below, you can find a list of keyboard shortcuts that can be used in the editor.")),
        ...this._createCategories(Array.from(e.values())),
        r
      ]
    });
  }
  /**
  * @inheritDoc
  */
  focus() {
    this.element.focus();
  }
  /**
  * Creates `<section><h3>Category label</h3>...</section>` elements for each category of keystrokes.
  */
  _createCategories(t) {
    return t.map((e) => {
      const n = [
        // Category header.
        xn(document, "h3", {}, e.label),
        // Category definitions (<dl>) and their optional headers (<h4>).
        ...Array.from(e.groups.values()).map((r) => this._createGroup(r)).flat()
      ];
      return e.description && n.splice(1, 0, xn(document, "p", {}, e.description)), xn(document, "section", {}, n);
    });
  }
  /**
  * Creates `[<h4>Optional label</h4>]<dl>...</dl>` elements for each group of keystrokes in a category.
  */
  _createGroup(t) {
    const e = t.keystrokes.sort((r, s) => r.label.localeCompare(s.label)).map((r) => this._createGroupRow(r)).flat(), n = [
      xn(document, "dl", {}, e)
    ];
    return t.label && n.unshift(xn(document, "h4", {}, t.label)), n;
  }
  /**
  * Creates `<dt>Keystroke label</dt><dd>Keystroke definition</dd>` elements for each keystroke in a group.
  */
  _createGroupRow(t) {
    const e = this.locale.t, n = xn(document, "dt"), r = xn(document, "dd"), s = mM(t.keystroke), o = [];
    for (const a of s)
      o.push(a.map(fM).join(""));
    return n.innerHTML = t.label, r.innerHTML = o.join(", ") + (t.mayRequireFn && Me.isMac ? ` ${e("(may require <kbd>Fn</kbd>)")}` : ""), [
      n,
      r
    ];
  }
}
function fM(i) {
  return Bu(i).split("+").map((t) => `<kbd>${t}</kbd>`).join("+");
}
function mM(i) {
  return typeof i == "string" ? [
    [
      i
    ]
  ] : typeof i[0] == "string" ? [
    i
  ] : i;
}
var Rv = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10 6.628a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3Z"/><path d="M8.5 9.125a.3.3 0 0 0-.253-.296L5.11 8.327a.75.75 0 1 1 .388-1.449l4.04.716c.267.072.624.08.893.009l4.066-.724a.75.75 0 1 1 .388 1.45l-3.132.5a.3.3 0 0 0-.253.296v1.357a.3.3 0 0 0 .018.102l1.615 4.438a.75.75 0 0 1-1.41.513l-1.35-3.71a.3.3 0 0 0-.281-.197h-.209a.3.3 0 0 0-.282.198l-1.35 3.711a.75.75 0 0 1-1.41-.513l1.64-4.509a.3.3 0 0 0 .019-.103V9.125Z"/><path clip-rule="evenodd" d="M10 18.5a8.5 8.5 0 1 1 0-17 8.5 8.5 0 0 1 0 17Zm0 1.5c5.523 0 10-4.477 10-10S15.523 0 10 0 0 4.477 0 10s4.477 10 10 10Z"/></svg>';
class HC extends V {
  constructor() {
    super(...arguments);
    /**
    * The view that displays the dialog content (list of keystrokes).
    * Created when the dialog is opened for the first time.
    */
    h(this, "contentView", null);
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Ud
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AccessibilityHelp";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.locale.t;
    e.ui.componentFactory.add("accessibilityHelp", () => {
      const r = this._createButton(Te);
      return r.set({
        tooltip: !0,
        withText: !1,
        label: n("Accessibility help")
      }), r;
    }), e.ui.componentFactory.add("menuBar:accessibilityHelp", () => {
      const r = this._createButton(At);
      return r.label = n("Accessibility"), r;
    }), e.keystrokes.set("Alt+0", (r, s) => {
      this._toggleDialog(), s();
    }), this._setupRootLabels();
  }
  /**
  * Creates a button to show accessibility help dialog, for use either in toolbar or in menu bar.
  */
  _createButton(e) {
    const n = this.editor, r = n.plugins.get("Dialog"), s = n.locale, o = new e(s);
    return o.set({
      keystroke: "Alt+0",
      icon: Rv,
      isToggleable: !0
    }), o.on("execute", () => this._toggleDialog()), o.bind("isOn").to(r, "id", (a) => a === "accessibilityHelp"), o;
  }
  /**
  * Injects a help text into each editing root's `aria-label` attribute allowing assistive technology users
  * to discover the availability of the Accessibility help dialog.
  */
  _setupRootLabels() {
    const e = this.editor, n = e.editing.view, r = e.t;
    e.ui.on("ready", () => {
      n.change((o) => {
        for (const a of n.document.roots)
          s(o, a);
      }), e.on("addRoot", (o, a) => {
        const l = e.editing.view.document.getRoot(a.rootName);
        n.change((c) => s(c, l));
      }, {
        priority: "low"
      });
    });
    function s(o, a) {
      const c = [
        a.getAttribute("aria-label"),
        r("Press %0 for help.", [
          Bu("Alt+0")
        ])
      ].filter((u) => u).join(". ");
      o.setAttribute("aria-label", c, a);
    }
  }
  /**
  * Shows the accessibility help dialog. Also, creates {@link #contentView} on demand.
  */
  _toggleDialog() {
    const e = this.editor, n = e.plugins.get("Dialog"), r = e.locale.t;
    this.contentView || (this.contentView = new hM(e.locale, e.accessibility.keystrokeInfos)), n.id === "accessibilityHelp" ? n.hide() : n.show({
      id: "accessibilityHelp",
      className: "ck-accessibility-help-dialog",
      title: r("Accessibility help"),
      icon: Rv,
      hasCloseButton: !0,
      content: this.contentView
    });
  }
}
class gM extends In {
  /**
  * Creates a new instance of the {@link module:ui/editorui/bodycollection~BodyCollection}.
  *
  * @param locale The {@link module:core/editor/editor~Editor editor's locale} instance.
  * @param initialItems The initial items of the collection.
  */
  constructor(e, n = []) {
    super(n);
    /**
    * The {@link module:core/editor/editor~Editor#locale editor's locale} instance.
    * See the view {@link module:ui/view~View#locale locale} property.
    */
    h(this, "locale");
    /**
    * The element holding elements of the body region.
    */
    h(this, "_bodyCollectionContainer");
    this.locale = e;
  }
  /**
  * The element holding elements of the body region.
  */
  get bodyCollectionContainer() {
    return this._bodyCollectionContainer;
  }
  /**
  * Attaches the body collection to the DOM body element. You need to execute this method to render the content of
  * the body collection.
  */
  attachToDom() {
    this._bodyCollectionContainer = new li({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset_all",
          "ck-body",
          "ck-rounded-corners"
        ],
        dir: this.locale.uiLanguageDirection,
        role: "application"
      },
      children: this
    }).render();
    let e = document.querySelector(".ck-body-wrapper");
    e || (e = xn(document, "div", {
      class: "ck-body-wrapper"
    }), document.body.appendChild(e)), e.appendChild(this._bodyCollectionContainer);
  }
  /**
  * Detaches the collection from the DOM structure. Use this method when you do not need to use the body collection
  * anymore to clean-up the DOM structure.
  */
  detachFromDom() {
    super.destroy(), this._bodyCollectionContainer && this._bodyCollectionContainer.remove();
    const e = document.querySelector(".ck-body-wrapper");
    e && e.childElementCount == 0 && e.remove();
  }
}
class Gs extends Te {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The toggle switch of the button.
    */
    h(this, "toggleSwitchView");
    this.isToggleable = !0, this.toggleSwitchView = this._createToggleView(), this.extendTemplate({
      attributes: {
        class: "ck-switchbutton"
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.children.add(this.toggleSwitchView);
  }
  /**
  * Creates a toggle child view.
  */
  _createToggleView() {
    const e = new de();
    return e.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-button__toggle"
        ]
      },
      children: [
        {
          tag: "span",
          attributes: {
            class: [
              "ck",
              "ck-button__toggle__inner"
            ]
          }
        }
      ]
    }), e;
  }
}
class Ov extends (/* @__PURE__ */ jC(Te)) {
}
class pM extends (/* @__PURE__ */ jC(na)) {
}
function jC(i) {
  class t extends i {
    /**
    * @inheritDoc
    */
    constructor(...r) {
      super(...r);
      /**
      * The button view of the component.
      *
      * @deprecated
      */
      h(this, "buttonView");
      /**
      * A hidden `<input>` view used to execute file dialog.
      */
      h(this, "_fileInputView");
      this.buttonView = this, this._fileInputView = new bM(this.locale), this._fileInputView.bind("acceptedType").to(this), this._fileInputView.bind("allowMultipleFiles").to(this), this._fileInputView.delegate("done").to(this), this.on("execute", () => {
        this._fileInputView.open();
      }), this.extendTemplate({
        attributes: {
          class: "ck-file-dialog-button"
        }
      });
    }
    /**
    * @inheritDoc
    */
    render() {
      super.render(), this.children.add(this._fileInputView);
    }
  }
  return t;
}
class bM extends de {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.set("acceptedType", void 0), this.set("allowMultipleFiles", !1);
    const e = this.bindTemplate;
    this.setTemplate({
      tag: "input",
      attributes: {
        class: [
          "ck-hidden"
        ],
        type: "file",
        tabindex: "-1",
        accept: e.to("acceptedType"),
        multiple: e.to("allowMultipleFiles")
      },
      on: {
        // Removing from code coverage since we cannot programmatically set input element files.
        change: e.to(
          /* istanbul ignore next -- @preserve */
          () => {
            this.element && this.element.files && this.element.files.length && this.fire("done", this.element.files), this.element.value = "";
          }
        )
      }
    });
  }
  /**
  * Opens file dialog.
  */
  open() {
    this.element.click();
  }
}
var qd = '<svg viewBox="0 0 10 10" xmlns="http://www.w3.org/2000/svg"><path d="M.941 4.523a.75.75 0 1 1 1.06-1.06l3.006 3.005 3.005-3.005a.75.75 0 1 1 1.06 1.06l-3.549 3.55a.75.75 0 0 1-1.168-.136L.941 4.523z"/></svg>';
class gp extends de {
  /**
  * Creates an instance of the collapsible view.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param childViews An optional array of initial child views to be inserted into the collapsible.
  */
  constructor(e, n) {
    super(e);
    /**
    * The main button that, when clicked, collapses or expands the container with {@link #children}.
    */
    h(this, "buttonView");
    /**
    * A collection of the child views that can be collapsed by clicking the {@link #buttonView}.
    */
    h(this, "children");
    const r = this.bindTemplate;
    this.set("isCollapsed", !1), this.set("label", ""), this.buttonView = this._createButtonView(), this.children = this.createCollection(), this.set("_collapsibleAriaLabelUid", void 0), n && this.children.addMany(n), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-collapsible",
          r.if("isCollapsed", "ck-collapsible_collapsed")
        ]
      },
      children: [
        this.buttonView,
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-collapsible__children"
            ],
            role: "region",
            hidden: r.if("isCollapsed", "hidden"),
            "aria-labelledby": r.to("_collapsibleAriaLabelUid")
          },
          children: this.children
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this._collapsibleAriaLabelUid = this.buttonView.labelView.element.id;
  }
  /**
  * Focuses the first focusable.
  */
  focus() {
    this.buttonView.focus();
  }
  /**
  * Creates the main {@link #buttonView} of the collapsible.
  */
  _createButtonView() {
    const e = new Te(this.locale), n = e.bindTemplate;
    return e.set({
      withText: !0,
      icon: qd
    }), e.extendTemplate({
      attributes: {
        "aria-expanded": n.to("isOn", (r) => String(r))
      }
    }), e.bind("label").to(this), e.bind("isOn").to(this, "isCollapsed", (r) => !r), e.on("execute", () => {
      this.isCollapsed = !this.isCollapsed;
    }), e;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Wu(i, t) {
  const e = i.t, n = {
    Black: e("Black"),
    "Dim grey": e("Dim grey"),
    Grey: e("Grey"),
    "Light grey": e("Light grey"),
    White: e("White"),
    Red: e("Red"),
    Orange: e("Orange"),
    Yellow: e("Yellow"),
    "Light green": e("Light green"),
    Green: e("Green"),
    Aquamarine: e("Aquamarine"),
    Turquoise: e("Turquoise"),
    "Light blue": e("Light blue"),
    Blue: e("Blue"),
    Purple: e("Purple")
  };
  return t.map((r) => {
    const s = n[r.label];
    return s && s != r.label && (r.label = s), r;
  });
}
function Uu(i) {
  return i.map(vM).filter((t) => !!t);
}
function vM(i) {
  return typeof i == "string" ? {
    model: i,
    label: i,
    hasBorder: !1,
    view: {
      name: "span",
      styles: {
        color: i
      }
    }
  } : {
    model: i.color,
    label: i.label || i.color,
    hasBorder: i.hasBorder === void 0 ? !1 : i.hasBorder,
    view: {
      name: "span",
      styles: {
        color: `${i.color}`
      }
    }
  };
}
var wM = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path class="ck-icon__fill" d="M16.935 5.328a2 2 0 0 1 0 2.829l-7.778 7.778a2 2 0 0 1-2.829 0L3.5 13.107a1.999 1.999 0 1 1 2.828-2.829l.707.707a1 1 0 0 0 1.414 0l5.658-5.657a2 2 0 0 1 2.828 0z"/><path d="M14.814 6.035 8.448 12.4a1 1 0 0 1-1.414 0l-1.413-1.415A1 1 0 1 0 4.207 12.4l2.829 2.829a1 1 0 0 0 1.414 0l7.778-7.778a1 1 0 1 0-1.414-1.415z"/></svg>';
class WC extends Te {
  constructor(t) {
    super(t);
    const e = this.bindTemplate;
    this.set("color", void 0), this.set("hasBorder", !1), this.icon = wM, this.extendTemplate({
      attributes: {
        style: {
          // https://github.com/ckeditor/ckeditor5/issues/14907
          backgroundColor: e.to("color", (n) => Me.isMediaForcedColors ? null : n)
        },
        class: [
          "ck",
          "ck-color-grid__tile",
          e.if("hasBorder", "ck-color-selector__color-tile_bordered")
        ]
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.iconView.fillColor = "hsl(0, 0%, 100%)";
  }
}
class Vv extends de {
  /**
  * Creates an instance of a color grid containing {@link module:ui/colorgrid/colortileview~ColorTileView tiles}.
  *
  * @fires execute
  * @param locale The localization services instance.
  * @param options Component configuration
  * @param options.colorDefinitions Array with definitions
  * required to create the {@link module:ui/colorgrid/colortileview~ColorTileView tiles}.
  * @param options.columns A number of columns to display the tiles.
  */
  constructor(e, n) {
    super(e);
    /**
    * A number of columns for the tiles grid.
    */
    h(this, "columns");
    /**
    * Collection of the child tile views.
    */
    h(this, "items");
    /**
    * Tracks information about DOM focus in the grid.
    */
    h(this, "focusTracker");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    const r = n && n.colorDefinitions ? n.colorDefinitions : [];
    this.columns = n && n.columns ? n.columns : 5;
    const s = {
      gridTemplateColumns: `repeat( ${this.columns}, 1fr)`
    };
    this.set("selectedColor", void 0), this.items = this.createCollection(), this.focusTracker = new wt(), this.keystrokes = new Ot(), this.items.on("add", (o, a) => {
      a.isOn = a.color === this.selectedColor;
    }), r.forEach((o) => {
      const a = new WC();
      a.set({
        color: o.color,
        label: o.label,
        tooltip: !0,
        hasBorder: o.options.hasBorder
      }), a.on("execute", () => {
        this.fire("execute", {
          value: o.color,
          hasBorder: o.options.hasBorder,
          label: o.label
        });
      }), this.items.add(a);
    }), this.setTemplate({
      tag: "div",
      children: this.items,
      attributes: {
        class: [
          "ck",
          "ck-color-grid"
        ],
        style: s
      }
    }), this.on("change:selectedColor", (o, a, l) => {
      for (const c of this.items)
        c.isOn = c.color === l;
    });
  }
  /**
  * Focuses the first focusable in {@link #items}.
  */
  focus() {
    this.items.length && this.items.first.focus();
  }
  /**
  * Focuses the last focusable in {@link #items}.
  */
  focusLast() {
    this.items.length && this.items.last.focus();
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    for (const e of this.items)
      this.focusTracker.add(e.element);
    this.items.on("add", (e, n) => {
      this.focusTracker.add(n.element);
    }), this.items.on("remove", (e, n) => {
      this.focusTracker.remove(n.element);
    }), this.keystrokes.listenTo(this.element), mp({
      keystrokeHandler: this.keystrokes,
      focusTracker: this.focusTracker,
      gridItems: this.items,
      numberOfColumns: this.columns,
      uiLanguageDirection: this.locale && this.locale.uiLanguageDirection
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function UC(i, t) {
  if (!i)
    return "";
  const e = qC(i);
  if (!e)
    return "";
  if (e.space === t)
    return i;
  if (!kM(e))
    return "";
  const r = OC[e.space][t];
  if (!r)
    return "";
  const s = r(e.space === "hex" ? e.hexValue : e.values);
  return CM(s, t);
}
function yM(i) {
  if (!i)
    return "";
  const t = qC(i);
  return t ? t.space === "hex" ? t.hexValue : UC(i, "hex") : "#000";
}
function _M(i, t) {
  customElements.get(i) === void 0 && customElements.define(i, t);
}
function CM(i, t) {
  switch (t) {
    case "hex":
      return `#${i}`;
    case "rgb":
      return `rgb( ${i[0]}, ${i[1]}, ${i[2]} )`;
    case "hsl":
      return `hsl( ${i[0]}, ${i[1]}%, ${i[2]}% )`;
    case "hwb":
      return `hwb( ${i[0]}, ${i[1]}, ${i[2]} )`;
    case "lab":
      return `lab( ${i[0]}% ${i[1]} ${i[2]} )`;
    case "lch":
      return `lch( ${i[0]}% ${i[1]} ${i[2]} )`;
    default:
      return "";
  }
}
function qC(i) {
  if (i.startsWith("#")) {
    const e = pv(i);
    return {
      space: "hex",
      values: e.values,
      hexValue: i,
      alpha: e.alpha
    };
  }
  const t = pv(i);
  return t.space ? t : null;
}
function kM(i) {
  return Object.keys(OC).includes(i.space);
}
class Qt extends de {
  /**
  * Creates an instance of the labeled field view class using a provided creator function
  * that provides the view to be labeled.
  *
  * @param locale The locale instance.
  * @param viewCreator A function that returns a {@link module:ui/view~View}
  * that will be labeled. The following arguments are passed to the creator function:
  *
  * * an instance of the `LabeledFieldView` to allow binding observable properties,
  * * an UID string that connects the {@link #labelView label} and the labeled field view in DOM,
  * * an UID string that connects the {@link #statusView status} and the labeled field view in DOM.
  */
  constructor(e, n) {
    super(e);
    /**
    * The field view that gets labeled.
    */
    h(this, "fieldView");
    /**
    * The label view instance that describes the entire view.
    */
    h(this, "labelView");
    /**
    * The status view for the {@link #fieldView}. It displays {@link #errorText} and
    * {@link #infoText}.
    */
    h(this, "statusView");
    /**
    * A collection of children of the internal wrapper element. Allows inserting additional DOM elements (views) next to
    * the {@link #fieldView} for easy styling (e.g. positioning).
    *
    * By default, the collection contains {@link #fieldView} and {@link #labelView}.
    */
    h(this, "fieldWrapperChildren");
    const r = `ck-labeled-field-view-${Jn()}`, s = `ck-labeled-field-view-status-${Jn()}`;
    this.fieldView = n(this, r, s), this.set("label", void 0), this.set("isEnabled", !0), this.set("isEmpty", !0), this.set("isFocused", !1), this.set("errorText", null), this.set("infoText", null), this.set("class", void 0), this.set("placeholder", void 0), this.labelView = this._createLabelView(r), this.statusView = this._createStatusView(s), this.fieldWrapperChildren = this.createCollection([
      this.fieldView,
      this.labelView
    ]), this.bind("_statusText").to(this, "errorText", this, "infoText", (a, l) => a || l);
    const o = this.bindTemplate;
    this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-labeled-field-view",
          o.to("class"),
          o.if("isEnabled", "ck-disabled", (a) => !a),
          o.if("isEmpty", "ck-labeled-field-view_empty"),
          o.if("isFocused", "ck-labeled-field-view_focused"),
          o.if("placeholder", "ck-labeled-field-view_placeholder"),
          o.if("errorText", "ck-error")
        ]
      },
      children: [
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-labeled-field-view__input-wrapper"
            ]
          },
          children: this.fieldWrapperChildren
        },
        this.statusView
      ]
    });
  }
  /**
  * Creates label view class instance and bind with view.
  *
  * @param id Unique id to set as labelView#for attribute.
  */
  _createLabelView(e) {
    const n = new Oi(this.locale);
    return n.for = e, n.bind("text").to(this, "label"), n;
  }
  /**
  * Creates the status view instance. It displays {@link #errorText} and {@link #infoText}
  * next to the {@link #fieldView}. See {@link #_statusText}.
  *
  * @param statusUid Unique id of the status, shared with the {@link #fieldView view's}
  * `aria-describedby` attribute.
  */
  _createStatusView(e) {
    const n = new de(this.locale), r = this.bindTemplate;
    return n.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-labeled-field-view__status",
          r.if("errorText", "ck-labeled-field-view__status_error"),
          r.if("_statusText", "ck-hidden", (s) => !s)
        ],
        id: e,
        role: r.if("errorText", "alert")
      },
      children: [
        {
          text: r.to("_statusText")
        }
      ]
    }), n;
  }
  /**
  * Focuses the {@link #fieldView}.
  */
  focus(e) {
    this.fieldView.focus(e);
  }
}
class xM extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Stores information about the editor UI focus and propagates it so various plugins and components
    * are unified as a focus group.
    */
    h(this, "focusTracker");
    this.set("value", void 0), this.set("id", void 0), this.set("placeholder", void 0), this.set("tabIndex", void 0), this.set("isReadOnly", !1), this.set("hasError", !1), this.set("ariaDescribedById", void 0), this.set("ariaLabel", void 0), this.focusTracker = new wt(), this.bind("isFocused").to(this.focusTracker), this.set("isEmpty", !0);
    const n = this.bindTemplate;
    this.setTemplate({
      tag: "input",
      attributes: {
        class: [
          "ck",
          "ck-input",
          n.if("isFocused", "ck-input_focused"),
          n.if("isEmpty", "ck-input-text_empty"),
          n.if("hasError", "ck-error")
        ],
        id: n.to("id"),
        placeholder: n.to("placeholder"),
        tabindex: n.to("tabIndex"),
        readonly: n.to("isReadOnly"),
        "aria-invalid": n.if("hasError", !0),
        "aria-describedby": n.to("ariaDescribedById"),
        "aria-label": n.to("ariaLabel")
      },
      on: {
        input: n.to((...r) => {
          this.fire("input", ...r), this._updateIsEmpty();
        }),
        change: n.to(this._updateIsEmpty.bind(this))
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.element), this._setDomElementValue(this.value), this._updateIsEmpty(), this.on("change:value", (e, n, r) => {
      this._setDomElementValue(r), this._updateIsEmpty();
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy();
  }
  /**
  * Moves the focus to the input and selects the value.
  */
  select() {
    this.element.select();
  }
  /**
  * Focuses the input.
  */
  focus() {
    this.element.focus();
  }
  /**
  * Resets the value of the input
  */
  reset() {
    this.value = this.element.value = "", this._updateIsEmpty();
  }
  /**
  * Updates the {@link #isEmpty} property value on demand.
  */
  _updateIsEmpty() {
    this.isEmpty = EM(this.element);
  }
  /**
  * Sets the `value` property of the {@link #element DOM element} on demand.
  */
  _setDomElementValue(e) {
    this.element.value = !e && e !== 0 ? "" : e;
  }
}
function EM(i) {
  return !i.value;
}
class GC extends xM {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.set("inputMode", "text");
    const e = this.bindTemplate;
    this.extendTemplate({
      attributes: {
        inputmode: e.to("inputMode")
      }
    });
  }
}
class KC extends GC {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.extendTemplate({
      attributes: {
        type: "text",
        class: [
          "ck-input-text"
        ]
      }
    });
  }
}
class TM extends GC {
  /**
  * Creates an instance of the input number view.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param options The options of the input.
  * @param options.min The value of the `min` DOM attribute (the lowest accepted value).
  * @param options.max The value of the `max` DOM attribute (the highest accepted value).
  * @param options.step The value of the `step` DOM attribute.
  */
  constructor(t, { min: e, max: n, step: r } = {}) {
    super(t);
    const s = this.bindTemplate;
    this.set("min", e), this.set("max", n), this.set("step", r), this.extendTemplate({
      attributes: {
        type: "number",
        class: [
          "ck-input-number"
        ],
        min: s.to("min"),
        max: s.to("max"),
        step: s.to("step")
      }
    });
  }
}
class SM extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views in this panel.
    *
    * A common child type is the {@link module:ui/list/listview~ListView} and {@link module:ui/toolbar/toolbarview~ToolbarView}.
    * See {@link module:ui/dropdown/utils~addListToDropdown} and
    * {@link module:ui/dropdown/utils~addToolbarToDropdown} to learn more about child views of dropdowns.
    */
    h(this, "children");
    const n = this.bindTemplate;
    this.set("isVisible", !1), this.set("position", "se"), this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset",
          "ck-dropdown__panel",
          n.to("position", (r) => `ck-dropdown__panel_${r}`),
          n.if("isVisible", "ck-dropdown__panel-visible")
        ],
        tabindex: "-1"
      },
      children: this.children,
      on: {
        // Drag and drop in the panel should not break the selection in the editor.
        // https://github.com/ckeditor/ckeditor5-ui/issues/228
        selectstart: n.to((r) => {
          r.target.tagName.toLocaleLowerCase() !== "input" && r.preventDefault();
        })
      }
    });
  }
  /**
  * Focuses the first view in the {@link #children} collection.
  *
  * See also {@link module:ui/dropdown/dropdownpanelfocusable~DropdownPanelFocusable}.
  */
  focus() {
    if (this.children.length) {
      const e = this.children.first;
      typeof e.focus == "function" ? e.focus() : Xe("ui-dropdown-panel-focus-child-missing-focus", {
        childView: this.children.first,
        dropdownPanel: this
      });
    }
  }
  /**
  * Focuses the view element or last item in view collection on opening dropdown's panel.
  *
  * See also {@link module:ui/dropdown/dropdownpanelfocusable~DropdownPanelFocusable}.
  */
  focusLast() {
    if (this.children.length) {
      const e = this.children.last;
      typeof e.focusLast == "function" ? e.focusLast() : e.focus();
    }
  }
}
const jo = class jo extends de {
  /**
  * Creates an instance of the dropdown.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  */
  constructor(e, n, r) {
    super(e);
    /**
    * Button of the dropdown view. Clicking the button opens the {@link #panelView}.
    */
    h(this, "buttonView");
    /**
    * Panel of the dropdown. It opens when the {@link #buttonView} is
    * {@link module:ui/button/button~Button#event:execute executed} (i.e. clicked).
    *
    * Child views can be added to the panel's `children` collection:
    *
    * ```ts
    * dropdown.panelView.children.add( childView );
    * ```
    *
    * See {@link module:ui/dropdown/dropdownpanelview~DropdownPanelView#children} and
    * {@link module:ui/viewcollection~ViewCollection#add}.
    */
    h(this, "panelView");
    /**
    * Tracks information about the DOM focus in the dropdown.
    */
    h(this, "focusTracker");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}. It manages
    * keystrokes of the dropdown:
    *
    * * <kbd>▼</kbd> opens the dropdown,
    * * <kbd>◀</kbd> and <kbd>Esc</kbd> closes the dropdown.
    */
    h(this, "keystrokes");
    /**
    * A child {@link module:ui/list/listview~ListView list view} of the dropdown located
    * in its {@link module:ui/dropdown/dropdownview~DropdownView#panelView panel}.
    *
    * **Note**: Only supported when dropdown has list view added using {@link module:ui/dropdown/utils~addListToDropdown}.
    */
    h(this, "listView");
    /**
    * A child toolbar of the dropdown located in the
    * {@link module:ui/dropdown/dropdownview~DropdownView#panelView panel}.
    *
    * **Note**: Only supported when dropdown has a toolbar added using {@link module:ui/dropdown/utils~addToolbarToDropdown}.
    */
    h(this, "toolbarView");
    /**
    * A child menu component of the dropdown located
    * in its {@link module:ui/dropdown/dropdownview~DropdownView#panelView panel}.
    *
    * **Note**: Only supported when dropdown has a menu added using {@link module:ui/dropdown/utils~addMenuToDropdown}.
    */
    h(this, "menuView");
    const s = this.bindTemplate;
    this.buttonView = n, this.panelView = r, this.set("isOpen", !1), this.set("isEnabled", !0), this.set("class", void 0), this.set("id", void 0), this.set("panelPosition", "auto"), this.panelView.bind("isVisible").to(this, "isOpen"), this.keystrokes = new Ot(), this.focusTracker = new wt(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-dropdown",
          s.to("class"),
          s.if("isEnabled", "ck-disabled", (o) => !o)
        ],
        id: s.to("id"),
        "aria-describedby": s.to("ariaDescribedById")
      },
      children: [
        n,
        r
      ]
    }), n.extendTemplate({
      attributes: {
        class: [
          "ck-dropdown__button"
        ],
        "data-cke-tooltip-disabled": s.to("isOpen")
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.buttonView.element), this.focusTracker.add(this.panelView.element), this.listenTo(this.buttonView, "open", () => {
      this.isOpen = !this.isOpen;
    }), this.on("change:isOpen", (n, r, s) => {
      if (s)
        if (this.panelPosition === "auto") {
          const o = jo._getOptimalPosition({
            element: this.panelView.element,
            target: this.buttonView.element,
            fitInViewport: !0,
            positions: this._panelPositions
          });
          this.panelView.position = o ? o.name : this._panelPositions[0].name;
        } else
          this.panelView.position = this.panelPosition;
    }), this.keystrokes.listenTo(this.element);
    const e = (n, r) => {
      this.isOpen && (this.isOpen = !1, r());
    };
    this.keystrokes.set("arrowdown", (n, r) => {
      this.buttonView.isEnabled && !this.isOpen && (this.isOpen = !0, r());
    }), this.keystrokes.set("arrowright", (n, r) => {
      this.isOpen && r();
    }), this.keystrokes.set("arrowleft", e), this.keystrokes.set("esc", e);
  }
  /**
  * Focuses the {@link #buttonView}.
  */
  focus() {
    this.buttonView.focus();
  }
  /**
  * Returns {@link #panelView panel} positions to be used by the
  * {@link module:utils/dom/position~getOptimalPosition `getOptimalPosition()`}
  * utility considering the direction of the language the UI of the editor is displayed in.
  */
  get _panelPositions() {
    const { south: e, north: n, southEast: r, southWest: s, northEast: o, northWest: a, southMiddleEast: l, southMiddleWest: c, northMiddleEast: u, northMiddleWest: d } = jo.defaultPanelPositions;
    return this.locale.uiLanguageDirection !== "rtl" ? [
      r,
      s,
      l,
      c,
      e,
      o,
      a,
      u,
      d,
      n
    ] : [
      s,
      r,
      c,
      l,
      e,
      a,
      o,
      d,
      u,
      n
    ];
  }
};
/**
* A set of positioning functions used by the dropdown view to determine
* the optimal position (i.e. fitting into the browser viewport) of its
* {@link module:ui/dropdown/dropdownview~DropdownView#panelView panel} when
* {@link module:ui/dropdown/dropdownview~DropdownView#panelPosition} is set to 'auto'`.
*
* The available positioning functions are as follow:
*
* **South**
*
* * `south`
*
* ```
*			[ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* * `southEast`
*
* ```
*		[ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* * `southWest`
*
* ```
*		         [ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* * `southMiddleEast`
*
* ```
*		  [ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* * `southMiddleWest`
*
* ```
*		       [ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* **North**
*
* * `north`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		    [ Button ]
* ```
*
* * `northEast`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		[ Button ]
* ```
*
* * `northWest`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		         [ Button ]
* ```
*
* * `northMiddleEast`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		  [ Button ]
* ```
*
* * `northMiddleWest`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		       [ Button ]
* ```
*
* Positioning functions are compatible with {@link module:utils/dom/position~DomPoint}.
*
* The name that position function returns will be reflected in dropdown panel's class that
* controls its placement. See {@link module:ui/dropdown/dropdownview~DropdownView#panelPosition}
* to learn more.
*/
h(jo, "defaultPanelPositions", {
  south: (e, n) => ({
    top: e.bottom,
    left: e.left - (n.width - e.width) / 2,
    name: "s"
  }),
  southEast: (e) => ({
    top: e.bottom,
    left: e.left,
    name: "se"
  }),
  southWest: (e, n) => ({
    top: e.bottom,
    left: e.left - n.width + e.width,
    name: "sw"
  }),
  southMiddleEast: (e, n) => ({
    top: e.bottom,
    left: e.left - (n.width - e.width) / 4,
    name: "sme"
  }),
  southMiddleWest: (e, n) => ({
    top: e.bottom,
    left: e.left - (n.width - e.width) * 3 / 4,
    name: "smw"
  }),
  north: (e, n) => ({
    top: e.top - n.height,
    left: e.left - (n.width - e.width) / 2,
    name: "n"
  }),
  northEast: (e, n) => ({
    top: e.top - n.height,
    left: e.left,
    name: "ne"
  }),
  northWest: (e, n) => ({
    top: e.top - n.height,
    left: e.left - n.width + e.width,
    name: "nw"
  }),
  northMiddleEast: (e, n) => ({
    top: e.top - n.height,
    left: e.left - (n.width - e.width) / 4,
    name: "nme"
  }),
  northMiddleWest: (e, n) => ({
    top: e.top - n.height,
    left: e.left - (n.width - e.width) * 3 / 4,
    name: "nmw"
  })
}), /**
* A function used to calculate the optimal position for the dropdown panel.
*/
h(jo, "_getOptimalPosition", qg);
let qu = jo;
class ZC extends Te {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * An icon that displays arrow to indicate a dropdown button.
    */
    h(this, "arrowView");
    this.arrowView = this._createArrowView(), this.extendTemplate({
      attributes: {
        "aria-haspopup": !0,
        "aria-expanded": this.bindTemplate.to("isOn", (n) => String(n))
      }
    }), this.delegate("execute").to(this, "open");
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.children.add(this.arrowView);
  }
  /**
  * Creates a {@link module:ui/icon/iconview~IconView} instance as {@link #arrowView}.
  */
  _createArrowView() {
    const e = new pr();
    return e.content = qd, e.extendTemplate({
      attributes: {
        class: "ck-dropdown__arrow"
      }
    }), e;
  }
}
class hs extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views inside of the list item {@link #element}.
    */
    h(this, "children");
    const n = this.bindTemplate;
    this.set("isVisible", !0), this.children = this.createCollection(), this.setTemplate({
      tag: "li",
      attributes: {
        class: [
          "ck",
          "ck-list__item",
          n.if("isVisible", "ck-hidden", (r) => !r)
        ],
        role: "presentation"
      },
      children: this.children
    });
  }
  /**
  * Focuses the list item.
  */
  focus() {
    this.children.first && this.children.first.focus();
  }
}
class Gd extends de {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.setTemplate({
      tag: "li",
      attributes: {
        class: [
          "ck",
          "ck-list__separator"
        ]
      }
    });
  }
}
class _u extends de {
  /**
  * Creates an instance of the list item group view class.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param labelView The instance of the group's label. If not provided, an instance of
  * {@link module:ui/label/labelview~LabelView} is used.
  */
  constructor(e, n = new Oi()) {
    super(e);
    /**
    * Label of the group view. Its text is configurable using the {@link #label label attribute}.
    *
    * If a custom label view is not passed in `ListItemGroupView` constructor, the label is an instance
    * of {@link module:ui/label/labelview~LabelView}.
    */
    h(this, "labelView");
    /**
    * Collection of the child list items inside this group.
    */
    h(this, "items");
    /**
    * Collection of the child elements of the group.
    */
    h(this, "children");
    const r = this.bindTemplate, s = new pp(e);
    this.set({
      label: "",
      isVisible: !0
    }), this.labelView = n, this.labelView.bind("text").to(this, "label"), this.children = this.createCollection(), this.children.addMany([
      this.labelView,
      s
    ]), s.set({
      role: "group",
      ariaLabelledBy: n.id
    }), s.focusTracker.destroy(), s.keystrokes.destroy(), this.items = s.items, this.setTemplate({
      tag: "li",
      attributes: {
        role: "presentation",
        class: [
          "ck",
          "ck-list__group",
          r.if("isVisible", "ck-hidden", (o) => !o)
        ]
      },
      children: this.children
    });
  }
  /**
  * Focuses the list item (which is not a separator).
  */
  focus() {
    if (this.items) {
      const e = this.items.find((n) => !(n instanceof Gd));
      e && e.focus();
    }
  }
}
class pp extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The collection of focusable views in the list. It is used to determine accessible navigation
    * between the {@link module:ui/list/listitemview~ListItemView list items} and
    * {@link module:ui/list/listitemgroupview~ListItemGroupView list groups}.
    */
    h(this, "focusables");
    /**
    * Collection of the child list views.
    */
    h(this, "items");
    /**
    * Tracks information about DOM focus in the list.
    */
    h(this, "focusTracker");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * Helps cycling over focusable {@link #items} in the list.
    */
    h(this, "_focusCycler");
    /**
    * A cached map of {@link module:ui/list/listitemgroupview~ListItemGroupView} to `change` event listeners for their `items`.
    * Used for accessibility and keyboard navigation purposes.
    */
    h(this, "_listItemGroupToChangeListeners", /* @__PURE__ */ new WeakMap());
    const n = this.bindTemplate;
    this.focusables = new In(), this.items = this.createCollection(), this.focusTracker = new wt(), this.keystrokes = new Ot(), this._focusCycler = new jn({
      focusables: this.focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate list items backwards using the arrowup key.
        focusPrevious: "arrowup",
        // Navigate toolbar items forwards using the arrowdown key.
        focusNext: "arrowdown"
      }
    }), this.set("ariaLabel", void 0), this.set("ariaLabelledBy", void 0), this.set("role", void 0), this.setTemplate({
      tag: "ul",
      attributes: {
        class: [
          "ck",
          "ck-reset",
          "ck-list"
        ],
        role: n.to("role"),
        "aria-label": n.to("ariaLabel"),
        "aria-labelledby": n.to("ariaLabelledBy")
      },
      children: this.items
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    for (const e of this.items)
      e instanceof _u ? this._registerFocusableItemsGroup(e) : e instanceof hs && this._registerFocusableListItem(e);
    this.items.on("change", (e, n) => {
      for (const r of n.removed)
        r instanceof _u ? this._deregisterFocusableItemsGroup(r) : r instanceof hs && this._deregisterFocusableListItem(r);
      for (const r of Array.from(n.added).reverse())
        r instanceof _u ? this._registerFocusableItemsGroup(r, n.index) : this._registerFocusableListItem(r, n.index);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the first focusable in {@link #items}.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the first focusable in {@link #items}.
  */
  focusFirst() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the last focusable in {@link #items}.
  */
  focusLast() {
    this._focusCycler.focusLast();
  }
  /**
  * Registers a list item view in the focus tracker.
  *
  * @param item The list item view to be registered.
  * @param index Index of the list item view in the {@link #items} collection. If not specified, the item will be added at the end.
  */
  _registerFocusableListItem(e, n) {
    this.focusTracker.add(e.element), this.focusables.add(e, n);
  }
  /**
  * Removes a list item view from the focus tracker.
  *
  * @param item The list item view to be removed.
  */
  _deregisterFocusableListItem(e) {
    this.focusTracker.remove(e.element), this.focusables.remove(e);
  }
  /**
  * Gets a callback that will be called when the `items` collection of a {@link module:ui/list/listitemgroupview~ListItemGroupView}
  * change.
  *
  * @param groupView The group view for which the callback will be created.
  * @returns The callback function to be used for the items `change` event listener in a group.
  */
  _getOnGroupItemsChangeCallback(e) {
    return (n, r) => {
      for (const s of r.removed)
        this._deregisterFocusableListItem(s);
      for (const s of Array.from(r.added).reverse())
        this._registerFocusableListItem(s, this.items.getIndex(e) + r.index);
    };
  }
  /**
  * Registers a list item group view (and its children) in the focus tracker.
  *
  * @param groupView A group view to be registered.
  * @param groupIndex Index of the group view in the {@link #items} collection. If not specified, the group will be added at the end.
  */
  _registerFocusableItemsGroup(e, n) {
    Array.from(e.items).forEach((s, o) => {
      const a = typeof n < "u" ? n + o : void 0;
      this._registerFocusableListItem(s, a);
    });
    const r = this._getOnGroupItemsChangeCallback(e);
    this._listItemGroupToChangeListeners.set(e, r), e.items.on("change", r);
  }
  /**
  * Removes a list item group view (and its children) from the focus tracker.
  *
  * @param groupView The group view to be removed.
  */
  _deregisterFocusableItemsGroup(e) {
    for (const n of e.items)
      this._deregisterFocusableListItem(n);
    e.items.off("change", this._listItemGroupToChangeListeners.get(e)), this._listItemGroupToChangeListeners.delete(e);
  }
}
const Mv = /* @__PURE__ */ vs("px"), AM = {
  top: -99999,
  left: -99999,
  name: "arrowless",
  config: {
    withArrow: !1
  }
}, mi = class mi extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of the child views that creates the balloon panel contents.
    */
    h(this, "content");
    /**
    * A callback that starts pinning the panel when {@link #isVisible} gets
    * `true`. Used by {@link #pin}.
    *
    * @private
    */
    h(this, "_pinWhenIsVisibleCallback");
    /**
    * An instance of resize observer used to detect if target element is still visible.
    */
    h(this, "_resizeObserver");
    const n = this.bindTemplate;
    this.set("top", 0), this.set("left", 0), this.set("position", "arrow_nw"), this.set("isVisible", !1), this.set("withArrow", !0), this.set("class", void 0), this._pinWhenIsVisibleCallback = null, this._resizeObserver = null, this.content = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-balloon-panel",
          n.to("position", (r) => `ck-balloon-panel_${r}`),
          n.if("isVisible", "ck-balloon-panel_visible"),
          n.if("withArrow", "ck-balloon-panel_with-arrow"),
          n.to("class")
        ],
        style: {
          top: n.to("top", Mv),
          left: n.to("left", Mv)
        }
      },
      children: this.content
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this.hide(), super.destroy();
  }
  /**
  * Shows the panel.
  *
  * See {@link #isVisible}.
  */
  show() {
    this.isVisible = !0;
  }
  /**
  * Hides the panel.
  *
  * See {@link #isVisible}.
  */
  hide() {
    this.isVisible = !1;
  }
  /**
  * Attaches the panel to a specified {@link module:utils/dom/position~Options#target} with a
  * smart positioning heuristics that chooses from available positions to make sure the panel
  * is visible to the user i.e. within the limits of the viewport.
  *
  * This method accepts configuration {@link module:utils/dom/position~Options options}
  * to set the `target`, optional `limiter` and `positions` the balloon should choose from.
  *
  * ```ts
  * const panel = new BalloonPanelView( locale );
  * const positions = BalloonPanelView.defaultPositions;
  *
  * panel.render();
  *
  * // Attach the panel to an element with the "target" id DOM.
  * panel.attachTo( {
  * 	target: document.querySelector( '#target' ),
  * 	positions: [
  * 		positions.northArrowSouth,
  * 		positions.southArrowNorth
  * 	]
  * } );
  * ```
  *
  * **Note**: Attaching the panel will also automatically {@link #show} it.
  *
  * **Note**: An attached panel will not follow its target when the window is scrolled or resized.
  * See the {@link #pin} method for a more permanent positioning strategy.
  *
  * @param options Positioning options compatible with {@link module:utils/dom/position~getOptimalPosition}.
  * Default `positions` array is {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.defaultPositions}.
  * @returns Whether the balloon was shown and successfully attached or not. Attaching can fail if the target
  * provided in the options is invisible (e.g. element detached from DOM).
  */
  attachTo(e) {
    const n = Cu(e.target);
    if (n && !mr(n))
      return !1;
    this.show();
    const r = mi.defaultPositions, s = Object.assign({}, {
      element: this.element,
      positions: [
        r.southArrowNorth,
        r.southArrowNorthMiddleWest,
        r.southArrowNorthMiddleEast,
        r.southArrowNorthWest,
        r.southArrowNorthEast,
        r.northArrowSouth,
        r.northArrowSouthMiddleWest,
        r.northArrowSouthMiddleEast,
        r.northArrowSouthWest,
        r.northArrowSouthEast,
        r.viewportStickyNorth
      ],
      limiter: ae.document.body,
      fitInViewport: !0
    }, e), o = mi._getOptimalPosition(s) || AM, a = parseInt(o.left), l = parseInt(o.top), c = o.name, u = o.config || {}, { withArrow: d = !0 } = u;
    return this.top = l, this.left = a, this.position = c, this.withArrow = d, !0;
  }
  /**
  * Works the same way as the {@link #attachTo} method except that the position of the panel is
  * continuously updated when:
  *
  * * any ancestor of the {@link module:utils/dom/position~Options#target}
  * or {@link module:utils/dom/position~Options#limiter} is scrolled,
  * * the browser window gets resized or scrolled.
  *
  * Thanks to that, the panel always sticks to the {@link module:utils/dom/position~Options#target}
  * and is immune to the changing environment.
  *
  * ```ts
  * const panel = new BalloonPanelView( locale );
  * const positions = BalloonPanelView.defaultPositions;
  *
  * panel.render();
  *
  * // Pin the panel to an element with the "target" id DOM.
  * panel.pin( {
  * 	target: document.querySelector( '#target' ),
  * 	positions: [
  * 		positions.northArrowSouth,
  * 		positions.southArrowNorth
  * 	]
  * } );
  * ```
  *
  * To leave the pinned state, use the {@link #unpin} method.
  *
  * **Note**: Pinning the panel will also automatically {@link #show} it.
  *
  * @param options Positioning options compatible with {@link module:utils/dom/position~getOptimalPosition}.
  * Default `positions` array is {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.defaultPositions}.
  */
  pin(e) {
    this.unpin(), this._startPinning(e) && (this._pinWhenIsVisibleCallback = () => {
      this.isVisible ? this._startPinning(e) : this._stopPinning();
    }, this.listenTo(this, "change:isVisible", this._pinWhenIsVisibleCallback));
  }
  /**
  * Stops pinning the panel, as set up by {@link #pin}.
  */
  unpin() {
    this._pinWhenIsVisibleCallback && (this._stopPinning(), this.stopListening(this, "change:isVisible", this._pinWhenIsVisibleCallback), this._pinWhenIsVisibleCallback = null, this.hide());
  }
  /**
  * Starts managing the pinned state of the panel. See {@link #pin}.
  *
  * @param options Positioning options compatible with {@link module:utils/dom/position~getOptimalPosition}.
  * @returns Whether the balloon was shown and successfully attached or not. Attaching can fail if the target
  * provided in the options is invisible (e.g. element detached from DOM).
  */
  _startPinning(e) {
    if (!this.attachTo(e))
      return !1;
    let n = Cu(e.target);
    const r = e.limiter ? Cu(e.limiter) : ae.document.body;
    if (this.listenTo(ae.document, "scroll", (s, o) => {
      const a = o.target, l = n && a.contains(n), c = r && a.contains(r);
      (l || c || !n || !r) && this.attachTo(e);
    }, {
      useCapture: !0
    }), this.listenTo(ae.window, "resize", () => {
      this.attachTo(e);
    }), !this._resizeObserver && (n && Bt(n) && (n = n.parentElement), n)) {
      const s = () => {
        mr(n) || this.unpin();
      };
      this._resizeObserver = new qs(n, s);
    }
    return !0;
  }
  /**
  * Stops managing the pinned state of the panel. See {@link #pin}.
  */
  _stopPinning() {
    this.stopListening(ae.document, "scroll"), this.stopListening(ae.window, "resize"), this._resizeObserver && (this._resizeObserver.destroy(), this._resizeObserver = null);
  }
  /**
  * Returns available {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView}
  * {@link module:utils/dom/position~PositioningFunction positioning functions} adjusted by the specific offsets.
  *
  * @internal
  * @param options Options to generate positions. If not specified, this helper will simply return
  * {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.defaultPositions}.
  * @param options.sideOffset A custom side offset (in pixels) of each position. If
  * not specified, {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.arrowSideOffset the default value}
  * will be used.
  * @param options.heightOffset A custom height offset (in pixels) of each position. If
  * not specified, {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.arrowHeightOffset the default value}
  * will be used.
  * @param options.stickyVerticalOffset A custom offset (in pixels) of the `viewportStickyNorth` positioning function.
  * If not specified, {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.stickyVerticalOffset the default value}
  * will be used.
  * @param options.config Additional configuration of the balloon balloon panel view.
  * Currently only {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView#withArrow} is supported. Learn more
  * about {@link module:utils/dom/position~PositioningFunction positioning functions}.
  */
  static generatePositions(e = {}) {
    const { sideOffset: n = mi.arrowSideOffset, heightOffset: r = mi.arrowHeightOffset, stickyVerticalOffset: s = mi.stickyVerticalOffset, config: o } = e;
    return {
      // ------- North west
      northWestArrowSouthWest: (c, u) => ({
        top: a(c, u),
        left: c.left - n,
        name: "arrow_sw",
        ...o && {
          config: o
        }
      }),
      northWestArrowSouthMiddleWest: (c, u) => ({
        top: a(c, u),
        left: c.left - u.width * 0.25 - n,
        name: "arrow_smw",
        ...o && {
          config: o
        }
      }),
      northWestArrowSouth: (c, u) => ({
        top: a(c, u),
        left: c.left - u.width / 2,
        name: "arrow_s",
        ...o && {
          config: o
        }
      }),
      northWestArrowSouthMiddleEast: (c, u) => ({
        top: a(c, u),
        left: c.left - u.width * 0.75 + n,
        name: "arrow_sme",
        ...o && {
          config: o
        }
      }),
      northWestArrowSouthEast: (c, u) => ({
        top: a(c, u),
        left: c.left - u.width + n,
        name: "arrow_se",
        ...o && {
          config: o
        }
      }),
      // ------- North
      northArrowSouthWest: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - n,
        name: "arrow_sw",
        ...o && {
          config: o
        }
      }),
      northArrowSouthMiddleWest: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - u.width * 0.25 - n,
        name: "arrow_smw",
        ...o && {
          config: o
        }
      }),
      northArrowSouth: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - u.width / 2,
        name: "arrow_s",
        ...o && {
          config: o
        }
      }),
      northArrowSouthMiddleEast: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - u.width * 0.75 + n,
        name: "arrow_sme",
        ...o && {
          config: o
        }
      }),
      northArrowSouthEast: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - u.width + n,
        name: "arrow_se",
        ...o && {
          config: o
        }
      }),
      // ------- North east
      northEastArrowSouthWest: (c, u) => ({
        top: a(c, u),
        left: c.right - n,
        name: "arrow_sw",
        ...o && {
          config: o
        }
      }),
      northEastArrowSouthMiddleWest: (c, u) => ({
        top: a(c, u),
        left: c.right - u.width * 0.25 - n,
        name: "arrow_smw",
        ...o && {
          config: o
        }
      }),
      northEastArrowSouth: (c, u) => ({
        top: a(c, u),
        left: c.right - u.width / 2,
        name: "arrow_s",
        ...o && {
          config: o
        }
      }),
      northEastArrowSouthMiddleEast: (c, u) => ({
        top: a(c, u),
        left: c.right - u.width * 0.75 + n,
        name: "arrow_sme",
        ...o && {
          config: o
        }
      }),
      northEastArrowSouthEast: (c, u) => ({
        top: a(c, u),
        left: c.right - u.width + n,
        name: "arrow_se",
        ...o && {
          config: o
        }
      }),
      // ------- South west
      southWestArrowNorthWest: (c) => ({
        top: l(c),
        left: c.left - n,
        name: "arrow_nw",
        ...o && {
          config: o
        }
      }),
      southWestArrowNorthMiddleWest: (c, u) => ({
        top: l(c),
        left: c.left - u.width * 0.25 - n,
        name: "arrow_nmw",
        ...o && {
          config: o
        }
      }),
      southWestArrowNorth: (c, u) => ({
        top: l(c),
        left: c.left - u.width / 2,
        name: "arrow_n",
        ...o && {
          config: o
        }
      }),
      southWestArrowNorthMiddleEast: (c, u) => ({
        top: l(c),
        left: c.left - u.width * 0.75 + n,
        name: "arrow_nme",
        ...o && {
          config: o
        }
      }),
      southWestArrowNorthEast: (c, u) => ({
        top: l(c),
        left: c.left - u.width + n,
        name: "arrow_ne",
        ...o && {
          config: o
        }
      }),
      // ------- South
      southArrowNorthWest: (c) => ({
        top: l(c),
        left: c.left + c.width / 2 - n,
        name: "arrow_nw",
        ...o && {
          config: o
        }
      }),
      southArrowNorthMiddleWest: (c, u) => ({
        top: l(c),
        left: c.left + c.width / 2 - u.width * 0.25 - n,
        name: "arrow_nmw",
        ...o && {
          config: o
        }
      }),
      southArrowNorth: (c, u) => ({
        top: l(c),
        left: c.left + c.width / 2 - u.width / 2,
        name: "arrow_n",
        ...o && {
          config: o
        }
      }),
      southArrowNorthMiddleEast: (c, u) => ({
        top: l(c),
        left: c.left + c.width / 2 - u.width * 0.75 + n,
        name: "arrow_nme",
        ...o && {
          config: o
        }
      }),
      southArrowNorthEast: (c, u) => ({
        top: l(c),
        left: c.left + c.width / 2 - u.width + n,
        name: "arrow_ne",
        ...o && {
          config: o
        }
      }),
      // ------- South east
      southEastArrowNorthWest: (c) => ({
        top: l(c),
        left: c.right - n,
        name: "arrow_nw",
        ...o && {
          config: o
        }
      }),
      southEastArrowNorthMiddleWest: (c, u) => ({
        top: l(c),
        left: c.right - u.width * 0.25 - n,
        name: "arrow_nmw",
        ...o && {
          config: o
        }
      }),
      southEastArrowNorth: (c, u) => ({
        top: l(c),
        left: c.right - u.width / 2,
        name: "arrow_n",
        ...o && {
          config: o
        }
      }),
      southEastArrowNorthMiddleEast: (c, u) => ({
        top: l(c),
        left: c.right - u.width * 0.75 + n,
        name: "arrow_nme",
        ...o && {
          config: o
        }
      }),
      southEastArrowNorthEast: (c, u) => ({
        top: l(c),
        left: c.right - u.width + n,
        name: "arrow_ne",
        ...o && {
          config: o
        }
      }),
      // ------- West
      westArrowEast: (c, u) => ({
        top: c.top + c.height / 2 - u.height / 2,
        left: c.left - u.width - r,
        name: "arrow_e",
        ...o && {
          config: o
        }
      }),
      // ------- East
      eastArrowWest: (c, u) => ({
        top: c.top + c.height / 2 - u.height / 2,
        left: c.right + r,
        name: "arrow_w",
        ...o && {
          config: o
        }
      }),
      // ------- Sticky
      viewportStickyNorth: (c, u, d, f) => {
        const m = f || d;
        return !c.getIntersection(m) || m.height - c.height > s ? null : {
          top: m.top + s,
          left: c.left + c.width / 2 - u.width / 2,
          name: "arrowless",
          config: {
            withArrow: !1,
            ...o
          }
        };
      }
    };
    function a(c, u) {
      return c.top - u.height - r;
    }
    function l(c) {
      return c.bottom + r;
    }
  }
};
/**
* A side offset of the arrow tip from the edge of the balloon. Controlled by CSS.
*
* ```
*		 ┌───────────────────────┐
*		 │                       │
*		 │         Balloon       │
*		 │         Content       │
*		 │                       │
*		 └──+    +───────────────┘
*		 |   \  /
*		 |    \/
*		>┼─────┼< ─────────────────────── side offset
*
* ```
*
* @default 25
*/
h(mi, "arrowSideOffset", 25), /**
 * A height offset of the arrow from the edge of the balloon. Controlled by CSS.
 *
 * ```
 *		 ┌───────────────────────┐
 *		 │                       │
 *		 │         Balloon       │
 *		 │         Content       │      ╱-- arrow height offset
 *		 │                       │      V
 *		 └──+    +───────────────┘ --- ─┼───────
 *		     \  /                       │
 *		      \/                        │
 *		────────────────────────────────┼───────
 *		                                ^
 *
 *
 *		>┼────┼<  arrow height offset
 *		 │    │
 *		 │    ┌────────────────────────┐
 *		 │    │                        │
 *		 │   ╱                         │
 *		 │ ╱            Balloon        │
 *		 │ ╲            Content        │
 *		 │   ╲                         │
 *		 │    │                        │
 *		 │    └────────────────────────┘
 * ```
 *
 * @default 10
*/
h(mi, "arrowHeightOffset", 10), /**
* A vertical offset of the balloon panel from the edge of the viewport if sticky.
* It helps in accessing toolbar buttons underneath the balloon panel.
*
* ```
*		  ┌───────────────────────────────────────────────────┐
*		  │                      Target                       │
*		  │                                                   │
*		  │                            /── vertical offset    │
*		┌─────────────────────────────V─────────────────────────┐
*		│ Toolbar            ┌─────────────┐                    │
*		├────────────────────│   Balloon   │────────────────────┤
*		│ │                  └─────────────┘                  │ │
*		│ │                                                   │ │
*		│ │                                                   │ │
*		│ │                                                   │ │
*		│ └───────────────────────────────────────────────────┘ │
*		│                        Viewport                       │
*		└───────────────────────────────────────────────────────┘
* ```
*
* @default 20
*/
h(mi, "stickyVerticalOffset", 20), /**
* Function used to calculate the optimal position for the balloon.
*/
h(mi, "_getOptimalPosition", qg), /**
* A default set of positioning functions used by the balloon panel view
* when attaching using the {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView#attachTo} method.
*
* The available positioning functions are as follows:
*
* **North west**
*
* * `northWestArrowSouthWest`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		 V
*		 [ Target ]
* ```
*
* * `northWestArrowSouthMiddleWest`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		    V
*		    [ Target ]
* ```
*
* * `northWestArrowSouth`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		         V
*		         [ Target ]
* ```
*
* * `northWestArrowSouthMiddleEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		             V
*		             [ Target ]
* ```
*
* * `northWestArrowSouthEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		                 V
*		                 [ Target ]
* ```
*
* **North**
*
* * `northArrowSouthWest`
*
* ```
*		    +-----------------+
*		    |     Balloon     |
*		    +-----------------+
*		     V
*		[ Target ]
* ```
*
* * `northArrowSouthMiddleWest`
*
* ```
*		 +-----------------+
*		 |     Balloon     |
*		 +-----------------+
*		     V
*		[ Target ]
* ```
* * `northArrowSouth`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		         V
*		    [ Target ]
* ```
*
* * `northArrowSouthMiddleEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		             V
*		        [ Target ]
* ```
*
* * `northArrowSouthEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		                V
*		           [ Target ]
* ```
*
* **North east**
*
* * `northEastArrowSouthWest`
*
* ```
*		        +-----------------+
*		        |     Balloon     |
*		        +-----------------+
*		         V
*		[ Target ]
* ```
*
* * `northEastArrowSouthMiddleWest`
*
* ```
*		     +-----------------+
*		     |     Balloon     |
*		     +-----------------+
*		         V
*		[ Target ]
* ```
*
* * `northEastArrowSouth`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		         V
*		[ Target ]
* ```
*
* * `northEastArrowSouthMiddleEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		             V
*		    [ Target ]
* ```
*
* * `northEastArrowSouthEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		                 V
*		        [ Target ]
* ```
*
* **South**
*
* * `southArrowNorthWest`
*
* ```
*		[ Target ]
*		     ^
*		    +-----------------+
*		    |     Balloon     |
*		    +-----------------+
* ```
*
* * `southArrowNorthMiddleWest`
*
* ```
*		   [ Target ]
*		        ^
*		    +-----------------+
*		    |     Balloon     |
*		    +-----------------+
* ```
*
* * `southArrowNorth`
*
* ```
*		    [ Target ]
*		         ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southArrowNorthMiddleEast`
*
* ```
*		            [ Target ]
*		                 ^
*		   +-----------------+
*		   |     Balloon     |
*		   +-----------------+
* ```
*
* * `southArrowNorthEast`
*
* ```
*		            [ Target ]
*		                 ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* **South west**
*
* * `southWestArrowNorthWest`
*
*
* ```
*		 [ Target ]
*		 ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southWestArrowNorthMiddleWest`
*
* ```
*		     [ Target ]
*		     ^
*		 +-----------------+
*		 |     Balloon     |
*		 +-----------------+
* ```
*
* * `southWestArrowNorth`
*
* ```
*		         [ Target ]
*		         ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southWestArrowNorthMiddleEast`
*
* ```
*		              [ Target ]
*		              ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southWestArrowNorthEast`
*
* ```
*		                 [ Target ]
*		                 ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* **South east**
*
* * `southEastArrowNorthWest`
*
* ```
*		[ Target ]
*		         ^
*		        +-----------------+
*		        |     Balloon     |
*		        +-----------------+
* ```
*
* * `southEastArrowNorthMiddleWest`
*
* ```
*		   [ Target ]
*		            ^
*		        +-----------------+
*		        |     Balloon     |
*		        +-----------------+
* ```
*
* * `southEastArrowNorth`
*
* ```
*		[ Target ]
*		         ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southEastArrowNorthMiddleEast`
*
* ```
*		     [ Target ]
*		              ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southEastArrowNorthEast`
*
* ```
*		        [ Target ]
*		                 ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* **West**
*
* * `westArrowEast`
*
* ```
*		+-----------------+
*		|     Balloon     |>[ Target ]
*		+-----------------+
* ```
*
* **East**
*
* * `eastArrowWest`
*
* ```
*		           +-----------------+
*		[ Target ]<|     Balloon     |
*		           +-----------------+
* ```
*
* **Sticky**
*
* * `viewportStickyNorth`
*
* ```
*		    +---------------------------+
*		    |        [ Target ]         |
*		    |                           |
*		+-----------------------------------+
*		|   |    +-----------------+    |   |
*		|   |    |     Balloon     |    |   |
*		|   |    +-----------------+    |   |
*		|   |                           |   |
*		|   |                           |   |
*		|   |                           |   |
*		|   |                           |   |
*		|   +---------------------------+   |
*		|             Viewport              |
*		+-----------------------------------+
* ```
*
* See {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView#attachTo}.
*
* Positioning functions must be compatible with {@link module:utils/dom/position~DomPoint}.
*
* Default positioning functions with customized offsets can be generated using
* {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.generatePositions}.
*
* The name that the position function returns will be reflected in the balloon panel's class that
* controls the placement of the "arrow". See {@link #position} to learn more.
*/
h(mi, "defaultPositions", /* @__PURE__ */ mi.generatePositions());
let fn = mi;
function Cu(i) {
  return us(i) ? i : Nd(i) ? i.commonAncestorContainer : typeof i == "function" ? Cu(i()) : null;
}
class bp extends de {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-toolbar__separator"
        ]
      }
    });
  }
}
class PM extends de {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-toolbar__line-break"
        ]
      }
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function IM(i) {
  return i.bindTemplate.to((t) => {
    t.target === i.element && t.preventDefault();
  });
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Kd(i) {
  if (Array.isArray(i))
    return {
      items: i,
      removeItems: []
    };
  const t = {
    items: [],
    removeItems: []
  };
  return i ? {
    ...t,
    ...i
  } : t;
}
const bm = {
  alignLeft: ee.alignLeft,
  bold: ee.bold,
  importExport: ee.importExport,
  paragraph: ee.paragraph,
  plus: ee.plus,
  text: ee.text,
  threeVerticalDots: ee.threeVerticalDots,
  pilcrow: ee.pilcrow,
  dragIndicator: ee.dragIndicator
};
class fs extends de {
  /**
  * Creates an instance of the {@link module:ui/toolbar/toolbarview~ToolbarView} class.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  * @param options Configuration options of the toolbar.
  */
  constructor(e, n) {
    super(e);
    /**
    * A reference to the options object passed to the constructor.
    */
    h(this, "options");
    /**
    * A collection of toolbar items (buttons, dropdowns, etc.).
    */
    h(this, "items");
    /**
    * Tracks information about the DOM focus in the toolbar.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}
    * to handle keyboard navigation in the toolbar.
    */
    h(this, "keystrokes");
    /**
    * A (child) view containing {@link #items toolbar items}.
    */
    h(this, "itemsView");
    /**
    * A top–level collection aggregating building blocks of the toolbar.
    *
    *	┌───────────────── ToolbarView ─────────────────┐
    *	| ┌──────────────── #children ────────────────┐ |
    *	| |   ┌──────────── #itemsView ───────────┐   | |
    *	| |   | [ item1 ] [ item2 ] ... [ itemN ] |   | |
    *	| |   └──────────────────────────────────-┘   | |
    *	| └───────────────────────────────────────────┘ |
    *	└───────────────────────────────────────────────┘
    *
    * By default, it contains the {@link #itemsView} but it can be extended with additional
    * UI elements when necessary.
    */
    h(this, "children");
    /**
    * A collection of {@link #items} that take part in the focus cycling
    * (i.e. navigation using the keyboard). Usually, it contains a subset of {@link #items} with
    * some optional UI elements that also belong to the toolbar and should be focusable
    * by the user.
    */
    h(this, "focusables");
    /**
    * Helps cycling over {@link #focusables focusable items} in the toolbar.
    */
    h(this, "_focusCycler");
    /**
    * An instance of the active toolbar behavior that shapes its look and functionality.
    *
    * See {@link module:ui/toolbar/toolbarview~ToolbarBehavior} to learn more.
    */
    h(this, "_behavior");
    const r = this.bindTemplate, s = this.t;
    this.options = n || {}, this.set("ariaLabel", s("Editor toolbar")), this.set("maxWidth", "auto"), this.items = this.createCollection(), this.focusTracker = new wt(), this.keystrokes = new Ot(), this.set("class", void 0), this.set("isCompact", !1), this.itemsView = new RM(e), this.children = this.createCollection(), this.children.add(this.itemsView), this.focusables = this.createCollection();
    const o = e.uiLanguageDirection === "rtl";
    this._focusCycler = new jn({
      focusables: this.focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate toolbar items backwards using the arrow[left,up] keys.
        focusPrevious: [
          o ? "arrowright" : "arrowleft",
          "arrowup"
        ],
        // Navigate toolbar items forwards using the arrow[right,down] keys.
        focusNext: [
          o ? "arrowleft" : "arrowright",
          "arrowdown"
        ]
      }
    });
    const a = [
      "ck",
      "ck-toolbar",
      r.to("class"),
      r.if("isCompact", "ck-toolbar_compact")
    ];
    this.options.shouldGroupWhenFull && this.options.isFloating && a.push("ck-toolbar_floating"), this.setTemplate({
      tag: "div",
      attributes: {
        class: a,
        role: "toolbar",
        "aria-label": r.to("ariaLabel"),
        style: {
          maxWidth: r.to("maxWidth")
        },
        tabindex: -1
      },
      children: this.children,
      on: {
        // https://github.com/ckeditor/ckeditor5-ui/issues/206
        mousedown: IM(this)
      }
    }), this._behavior = this.options.shouldGroupWhenFull ? new VM(this) : new OM(this);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.element);
    for (const e of this.items)
      this.focusTracker.add(e);
    this.items.on("add", (e, n) => {
      this.focusTracker.add(n);
    }), this.items.on("remove", (e, n) => {
      this.focusTracker.remove(n);
    }), this.keystrokes.listenTo(this.element), this._behavior.render(this);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this._behavior.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy(), super.destroy();
  }
  /**
  * Focuses the first focusable in {@link #focusables}.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the last focusable in {@link #focusables}.
  */
  focusLast() {
    this._focusCycler.focusLast();
  }
  /**
  * A utility that expands the plain toolbar configuration into
  * {@link module:ui/toolbar/toolbarview~ToolbarView#items} using a given component factory.
  *
  * @param itemsOrConfig The toolbar items or the entire toolbar configuration object.
  * @param factory A factory producing toolbar items.
  * @param removeItems An array of items names to be removed from the configuration. When present, applies
  * to this toolbar and all nested ones as well.
  */
  fillFromConfig(e, n, r) {
    this.items.addMany(this._buildItemsFromConfig(e, n, r));
  }
  /**
  * A utility that expands the plain toolbar configuration into a list of view items using a given component factory.
  *
  * @param itemsOrConfig The toolbar items or the entire toolbar configuration object.
  * @param factory A factory producing toolbar items.
  * @param removeItems An array of items names to be removed from the configuration. When present, applies
  * to this toolbar and all nested ones as well.
  */
  _buildItemsFromConfig(e, n, r) {
    const s = Kd(e), o = r || s.removeItems;
    return this._cleanItemsConfiguration(s.items, n, o).map((l) => vt(l) ? this._createNestedToolbarDropdown(l, n, o) : l === "|" ? new bp() : l === "-" ? new PM() : n.create(l)).filter((l) => !!l);
  }
  /**
  * Cleans up the {@link module:ui/toolbar/toolbarview~ToolbarView#items} of the toolbar by removing unwanted items and
  * duplicated (obsolete) separators or line breaks.
  *
  * @param items The toolbar items configuration.
  * @param factory A factory producing toolbar items.
  * @param removeItems An array of items names to be removed from the configuration.
  * @returns Items after the clean-up.
  */
  _cleanItemsConfiguration(e, n, r) {
    const s = e.filter((o, a, l) => o === "|" ? !0 : r.indexOf(o) !== -1 ? !1 : o === "-" ? this.options.shouldGroupWhenFull ? (Xe("toolbarview-line-break-ignored-when-grouping-items", l), !1) : !0 : !vt(o) && !n.has(o) ? (Xe("toolbarview-item-unavailable", {
      item: o
    }), !1) : !0);
    return this._cleanSeparatorsAndLineBreaks(s);
  }
  /**
  * Remove leading, trailing, and duplicated separators (`-` and `|`).
  *
  * @returns Toolbar items after the separator and line break clean-up.
  */
  _cleanSeparatorsAndLineBreaks(e) {
    const n = (a) => a !== "-" && a !== "|", r = e.length, s = e.findIndex(n);
    if (s === -1)
      return [];
    const o = r - e.slice().reverse().findIndex(n);
    return e.slice(s, o).filter((a, l, c) => n(a) ? !0 : !(l > 0 && c[l - 1] === a));
  }
  /**
  * Creates a user-defined dropdown containing a toolbar with items.
  *
  * @param definition A definition of the nested toolbar dropdown.
  * @param definition.label A label of the dropdown.
  * @param definition.icon An icon of the drop-down. One of 'bold', 'plus', 'text', 'importExport', 'alignLeft',
  * 'paragraph' or an SVG string. When `false` is passed, no icon will be used.
  * @param definition.withText When set `true`, the label of the dropdown will be visible. See
  * {@link module:ui/button/buttonview~ButtonView#withText} to learn more.
  * @param definition.tooltip A tooltip of the dropdown button. See
  * {@link module:ui/button/buttonview~ButtonView#tooltip} to learn more. Defaults to `true`.
  * @param componentFactory Component factory used to create items
  * of the nested toolbar.
  */
  _createNestedToolbarDropdown(e, n, r) {
    let { label: s, icon: o, items: a, tooltip: l = !0, withText: c = !1 } = e;
    if (a = this._cleanItemsConfiguration(a, n, r), !a.length)
      return null;
    const u = this.locale, d = Yn(u);
    return s || Xe("toolbarview-nested-toolbar-dropdown-missing-label", e), d.class = "ck-toolbar__nested-toolbar-dropdown", d.buttonView.set({
      label: s,
      tooltip: l,
      withText: !!c
    }), o !== !1 ? d.buttonView.icon = bm[o] || o || ee.threeVerticalDots : d.buttonView.withText = !0, Zd(d, () => d.toolbarView._buildItemsFromConfig(a, n, r)), d;
  }
}
class RM extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of items (buttons, dropdowns, etc.).
    */
    h(this, "children");
    this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-toolbar__items"
        ]
      },
      children: this.children
    });
  }
}
class OM {
  /**
  * Creates an instance of the {@link module:ui/toolbar/toolbarview~StaticLayout} toolbar
  * behavior.
  *
  * @param view An instance of the toolbar that this behavior is added to.
  */
  constructor(t) {
    const e = t.bindTemplate;
    t.set("isVertical", !1), t.itemsView.children.bindTo(t.items).using((n) => n), t.focusables.bindTo(t.items).using((n) => vc(n) ? n : null), t.extendTemplate({
      attributes: {
        class: [
          // When vertical, the toolbar has an additional CSS class.
          e.if("isVertical", "ck-toolbar_vertical")
        ]
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
  }
  /**
  * @inheritDoc
  */
  destroy() {
  }
}
class VM {
  /**
  * Creates an instance of the {@link module:ui/toolbar/toolbarview~DynamicGrouping} toolbar
  * behavior.
  *
  * @param view An instance of the toolbar that this behavior is added to.
  */
  constructor(t) {
    /**
    * A toolbar view this behavior belongs to.
    */
    h(this, "view");
    /**
    * A collection of toolbar children.
    */
    h(this, "viewChildren");
    /**
    * A collection of focusable toolbar elements.
    */
    h(this, "viewFocusables");
    /**
    * A view containing toolbar items.
    */
    h(this, "viewItemsView");
    /**
    * Toolbar focus tracker.
    */
    h(this, "viewFocusTracker");
    /**
    * Toolbar locale.
    */
    h(this, "viewLocale");
    /**
    * A subset of toolbar {@link module:ui/toolbar/toolbarview~ToolbarView#items}.
    * Aggregates items that fit into a single row of the toolbar and were not {@link #groupedItems grouped}
    * into a {@link #groupedItemsDropdown dropdown}. Items of this collection are displayed in the
    * {@link module:ui/toolbar/toolbarview~ToolbarView#itemsView}.
    *
    * When none of the {@link module:ui/toolbar/toolbarview~ToolbarView#items} were grouped, it
    * matches the {@link module:ui/toolbar/toolbarview~ToolbarView#items} collection in size and order.
    */
    h(this, "ungroupedItems");
    /**
    * A subset of toolbar {@link module:ui/toolbar/toolbarview~ToolbarView#items}.
    * A collection of the toolbar items that do not fit into a single row of the toolbar.
    * Grouped items are displayed in a dedicated {@link #groupedItemsDropdown dropdown}.
    *
    * When none of the {@link module:ui/toolbar/toolbarview~ToolbarView#items} were grouped,
    * this collection is empty.
    */
    h(this, "groupedItems");
    /**
    * The dropdown that aggregates {@link #groupedItems grouped items} that do not fit into a single
    * row of the toolbar. It is displayed on demand as the last of
    * {@link module:ui/toolbar/toolbarview~ToolbarView#children toolbar children} and offers another
    * (nested) toolbar which displays items that would normally overflow.
    */
    h(this, "groupedItemsDropdown");
    /**
    * An instance of the resize observer that helps dynamically determine the geometry of the toolbar
    * and manage items that do not fit into a single row.
    *
    * **Note:** Created in {@link #_enableGroupingOnResize}.
    *
    * @readonly
    */
    h(this, "resizeObserver", null);
    /**
    * A cached value of the horizontal padding style used by {@link #_updateGrouping}
    * to manage the {@link module:ui/toolbar/toolbarview~ToolbarView#items} that do not fit into
    * a single toolbar line. This value can be reused between updates because it is unlikely that
    * the padding will change and re–using `Window.getComputedStyle()` is expensive.
    *
    * @readonly
    */
    h(this, "cachedPadding", null);
    /**
    * A flag indicating that an items grouping update has been queued (e.g. due to the toolbar being visible)
    * and should be executed immediately the next time the toolbar shows up.
    *
    * @readonly
    */
    h(this, "shouldUpdateGroupingOnNextResize", !1);
    /**
    * Toolbar element.
    *
    * @readonly
    */
    h(this, "viewElement");
    this.view = t, this.viewChildren = t.children, this.viewFocusables = t.focusables, this.viewItemsView = t.itemsView, this.viewFocusTracker = t.focusTracker, this.viewLocale = t.locale, this.ungroupedItems = t.createCollection(), this.groupedItems = t.createCollection(), this.groupedItemsDropdown = this._createGroupedItemsDropdown(), t.itemsView.children.bindTo(this.ungroupedItems).using((e) => e), this.ungroupedItems.on("change", this._updateFocusCyclableItems.bind(this)), t.children.on("change", this._updateFocusCyclableItems.bind(this)), t.items.on("change", (e, n) => {
      const r = n.index, s = Array.from(n.added);
      for (const o of n.removed)
        r >= this.ungroupedItems.length ? this.groupedItems.remove(o) : this.ungroupedItems.remove(o);
      for (let o = r; o < r + s.length; o++) {
        const a = s[o - r];
        o > this.ungroupedItems.length ? this.groupedItems.add(a, o - this.ungroupedItems.length) : this.ungroupedItems.add(a, o);
      }
      this._updateGrouping();
    }), t.extendTemplate({
      attributes: {
        class: [
          // To group items dynamically, the toolbar needs a dedicated CSS class.
          "ck-toolbar_grouping"
        ]
      }
    });
  }
  /**
  * Enables dynamic items grouping based on the dimensions of the toolbar.
  *
  * @param view An instance of the toolbar that this behavior is added to.
  */
  render(t) {
    this.viewElement = t.element, this._enableGroupingOnResize(), this._enableGroupingOnMaxWidthChange(t);
  }
  /**
  * Cleans up the internals used by this behavior.
  */
  destroy() {
    this.groupedItemsDropdown.destroy(), this.resizeObserver.destroy();
  }
  /**
  * When called, it will check if any of the {@link #ungroupedItems} do not fit into a single row of the toolbar,
  * and it will move them to the {@link #groupedItems} when it happens.
  *
  * At the same time, it will also check if there is enough space in the toolbar for the first of the
  * {@link #groupedItems} to be returned back to {@link #ungroupedItems} and still fit into a single row
  * without the toolbar wrapping.
  */
  _updateGrouping() {
    if (!this.viewElement.ownerDocument.body.contains(this.viewElement))
      return;
    if (!mr(this.viewElement)) {
      this.shouldUpdateGroupingOnNextResize = !0;
      return;
    }
    const t = this.groupedItems.length;
    let e;
    for (; this._areItemsOverflowing; )
      this._groupLastItem(), e = !0;
    if (!e && this.groupedItems.length) {
      for (; this.groupedItems.length && !this._areItemsOverflowing; )
        this._ungroupFirstItem();
      this._areItemsOverflowing && this._groupLastItem();
    }
    this.groupedItems.length !== t && this.view.fire("groupedItemsUpdate");
  }
  /**
  * Returns `true` when {@link module:ui/toolbar/toolbarview~ToolbarView#element} children visually overflow,
  * for instance if the toolbar is narrower than its members. Returns `false` otherwise.
  */
  get _areItemsOverflowing() {
    if (!this.ungroupedItems.length)
      return !1;
    const t = this.viewElement, e = this.viewLocale.uiLanguageDirection, n = new je(t.lastChild), r = new je(t);
    if (!this.cachedPadding) {
      const s = ae.window.getComputedStyle(t), o = e === "ltr" ? "paddingRight" : "paddingLeft";
      this.cachedPadding = Number.parseInt(s[o]);
    }
    return e === "ltr" ? n.right > r.right - this.cachedPadding : n.left < r.left + this.cachedPadding;
  }
  /**
  * Enables the functionality that prevents {@link #ungroupedItems} from overflowing (wrapping to the next row)
  * upon resize when there is little space available. Instead, the toolbar items are moved to the
  * {@link #groupedItems} collection and displayed in a dropdown at the end of the row (which has its own nested toolbar).
  *
  * When called, the toolbar will automatically analyze the location of its {@link #ungroupedItems} and "group"
  * them in the dropdown if necessary. It will also observe the browser window for size changes in
  * the future and respond to them by grouping more items or reverting already grouped back, depending
  * on the visual space available.
  */
  _enableGroupingOnResize() {
    let t;
    this.resizeObserver = new qs(this.viewElement, (e) => {
      (!t || t !== e.contentRect.width || this.shouldUpdateGroupingOnNextResize) && (this.shouldUpdateGroupingOnNextResize = !1, this._updateGrouping(), t = e.contentRect.width);
    }), this._updateGrouping();
  }
  /**
  * Enables the grouping functionality, just like {@link #_enableGroupingOnResize} but the difference is that
  * it listens to the changes of {@link module:ui/toolbar/toolbarview~ToolbarView#maxWidth} instead.
  */
  _enableGroupingOnMaxWidthChange(t) {
    t.on("change:maxWidth", () => {
      this._updateGrouping();
    });
  }
  /**
  * When called, it will remove the last item from {@link #ungroupedItems} and move it back
  * to the {@link #groupedItems} collection.
  *
  * The opposite of {@link #_ungroupFirstItem}.
  */
  _groupLastItem() {
    this.groupedItems.length || (this.viewChildren.add(new bp()), this.viewChildren.add(this.groupedItemsDropdown), this.viewFocusTracker.add(this.groupedItemsDropdown.element)), this.groupedItems.add(this.ungroupedItems.remove(this.ungroupedItems.last), 0);
  }
  /**
  * Moves the very first item belonging to {@link #groupedItems} back
  * to the {@link #ungroupedItems} collection.
  *
  * The opposite of {@link #_groupLastItem}.
  */
  _ungroupFirstItem() {
    this.ungroupedItems.add(this.groupedItems.remove(this.groupedItems.first)), this.groupedItems.length || (this.viewChildren.remove(this.groupedItemsDropdown), this.viewChildren.remove(this.viewChildren.last), this.viewFocusTracker.remove(this.groupedItemsDropdown.element));
  }
  /**
  * Creates the {@link #groupedItemsDropdown} that hosts the members of the {@link #groupedItems}
  * collection when there is not enough space in the toolbar to display all items in a single row.
  */
  _createGroupedItemsDropdown() {
    const t = this.viewLocale, e = t.t, n = Yn(t);
    return n.class = "ck-toolbar__grouped-dropdown", n.panelPosition = t.uiLanguageDirection === "ltr" ? "sw" : "se", Zd(n, this.groupedItems), n.buttonView.set({
      label: e("Show more items"),
      tooltip: !0,
      tooltipPosition: t.uiLanguageDirection === "rtl" ? "se" : "sw",
      icon: ee.threeVerticalDots
    }), n;
  }
  /**
  * Updates the {@link module:ui/toolbar/toolbarview~ToolbarView#focusables focus–cyclable items}
  * collection so it represents the up–to–date state of the UI from the perspective of the user.
  *
  * For instance, the {@link #groupedItemsDropdown} can show up and hide but when it is visible,
  * it must be subject to focus cycling in the toolbar.
  *
  * See the {@link module:ui/toolbar/toolbarview~ToolbarView#focusables collection} documentation
  * to learn more about the purpose of this method.
  */
  _updateFocusCyclableItems() {
    this.viewFocusables.clear(), this.ungroupedItems.map((t) => {
      vc(t) && this.viewFocusables.add(t);
    }), this.groupedItems.length && this.viewFocusables.add(this.groupedItemsDropdown);
  }
}
class io extends de {
  /**
  * @inheritDoc
  */
  constructor(e, n) {
    super(e);
    /**
    * Collection of the child views inside of the split button {@link #element}.
    */
    h(this, "children");
    /**
    * A main button of split button.
    */
    h(this, "actionView");
    /**
    * A secondary button of split button that opens dropdown.
    */
    h(this, "arrowView");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}. It manages
    * keystrokes of the split button:
    *
    * * <kbd>▶</kbd> moves focus to arrow view when action view is focused,
    * * <kbd>◀</kbd> moves focus to action view when arrow view is focused.
    */
    h(this, "keystrokes");
    /**
    * Tracks information about DOM focus in the dropdown.
    */
    h(this, "focusTracker");
    const r = this.bindTemplate;
    this.set("class", void 0), this.set("labelStyle", void 0), this.set("icon", void 0), this.set("isEnabled", !0), this.set("isOn", !1), this.set("isToggleable", !1), this.set("isVisible", !0), this.set("keystroke", void 0), this.set("withKeystroke", !1), this.set("label", void 0), this.set("tabindex", -1), this.set("tooltip", !1), this.set("tooltipPosition", "s"), this.set("type", "button"), this.set("withText", !1), this.children = this.createCollection(), this.actionView = this._createActionView(n), this.arrowView = this._createArrowView(), this.keystrokes = new Ot(), this.focusTracker = new wt(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-splitbutton",
          r.to("class"),
          r.if("isVisible", "ck-hidden", (s) => !s),
          this.arrowView.bindTemplate.if("isOn", "ck-splitbutton_open")
        ]
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.children.add(this.actionView), this.children.add(this.arrowView), this.focusTracker.add(this.actionView.element), this.focusTracker.add(this.arrowView.element), this.keystrokes.listenTo(this.element), this.keystrokes.set("arrowright", (e, n) => {
      this.focusTracker.focusedElement === this.actionView.element && (this.arrowView.focus(), n());
    }), this.keystrokes.set("arrowleft", (e, n) => {
      this.focusTracker.focusedElement === this.arrowView.element && (this.actionView.focus(), n());
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the {@link module:ui/button/buttonview~ButtonView#element} of the action part of split button.
  */
  focus() {
    this.actionView.focus();
  }
  /**
  * Creates a {@link module:ui/button/buttonview~ButtonView} instance as {@link #actionView} and binds it with main split button
  * attributes.
  */
  _createActionView(e) {
    const n = e || new Te();
    return e || n.bind("icon", "isEnabled", "isOn", "isToggleable", "keystroke", "label", "tabindex", "tooltip", "tooltipPosition", "type", "withText").to(this), n.extendTemplate({
      attributes: {
        class: "ck-splitbutton__action"
      }
    }), n.delegate("execute").to(this), n;
  }
  /**
  * Creates a {@link module:ui/button/buttonview~ButtonView} instance as {@link #arrowView} and binds it with main split button
  * attributes.
  */
  _createArrowView() {
    const e = new Te(), n = e.bindTemplate;
    return e.icon = qd, e.extendTemplate({
      attributes: {
        class: [
          "ck-splitbutton__arrow"
        ],
        "data-cke-tooltip-disabled": n.to("isOn"),
        "aria-haspopup": !0,
        "aria-expanded": n.to("isOn", (r) => String(r))
      }
    }), e.bind("isEnabled").to(this), e.bind("label").to(this), e.bind("tooltip").to(this), e.delegate("execute").to(this, "open"), e;
  }
}
function Yn(i, t = ZC) {
  const e = typeof t == "function" ? new t(i) : t, n = new SM(i), r = new qu(i, e, n);
  return e.bind("isEnabled").to(r), e instanceof io ? e.arrowView.bind("isOn").to(r, "isOpen") : e.bind("isOn").to(r, "isOpen"), MM(r), r;
}
function Zd(i, t, e = {}) {
  i.extendTemplate({
    attributes: {
      class: [
        "ck-toolbar-dropdown"
      ]
    }
  }), i.isOpen ? Dv(i, t, e) : i.once("change:isOpen", () => Dv(i, t, e), {
    priority: "highest"
  }), e.enableActiveItemFocusOnDropdownOpen && vp(i, () => i.toolbarView.items.find((n) => n.isOn));
}
function Dv(i, t, e) {
  const n = i.locale, r = n.t, s = i.toolbarView = new fs(n), o = typeof t == "function" ? t() : t;
  s.ariaLabel = e.ariaLabel || r("Dropdown toolbar"), e.maxWidth && (s.maxWidth = e.maxWidth), e.class && (s.class = e.class), e.isCompact && (s.isCompact = e.isCompact), e.isVertical && (s.isVertical = !0), o instanceof In ? s.items.bindTo(o).using((a) => a) : s.items.addMany(o), i.panelView.children.add(s), i.focusTracker.add(s), s.items.delegate("execute").to(i);
}
function pa(i, t, e = {}) {
  i.isOpen ? Lv(i, t, e) : i.once("change:isOpen", () => Lv(i, t, e), {
    priority: "highest"
  }), vp(i, () => i.listView.items.find((n) => n instanceof hs ? n.children.first.isOn : !1));
}
function Lv(i, t, e) {
  const n = i.locale, r = i.listView = new pp(n), s = typeof t == "function" ? t() : t;
  r.ariaLabel = e.ariaLabel, r.role = e.role, JC(i, r.items, s, n), i.panelView.children.add(r), r.items.delegate("execute").to(i);
}
function vp(i, t) {
  i.on("change:isOpen", () => {
    if (!i.isOpen)
      return;
    const e = t();
    e && (typeof e.focus == "function" ? e.focus() : Xe("ui-dropdown-focus-child-on-open-child-missing-focus", {
      view: e
    }));
  }, {
    priority: wi.low - 10
  });
}
function MM(i) {
  DM(i), LM(i), NM(i), BM(i), FM(i), $M(i);
}
function DM(i) {
  _s({
    emitter: i,
    activator: () => i.isRendered && i.isOpen,
    callback: () => {
      i.isOpen = !1;
    },
    contextElements: () => [
      i.element,
      // Include all elements connected to the dropdown's focus tracker, but exclude those that are direct children
      // of DropdownView#element. They would be identified as descendants of #element anyway upon clicking and would
      // not contribute to the logic.
      ...YC(i.focusTracker).filter((t) => !i.element.contains(t))
    ]
  });
}
function YC(i) {
  return [
    ...i.elements,
    ...i.externalViews.flatMap((t) => YC(t.focusTracker))
  ];
}
function LM(i) {
  i.on("execute", (t) => {
    t.source instanceof Gs || (i.isOpen = !1);
  });
}
function NM(i) {
  i.focusTracker.on("change:isFocused", (t, e, n) => {
    n || !i.isOpen || (i.isOpen = !1);
  });
}
function BM(i) {
  i.keystrokes.set("arrowdown", (t, e) => {
    i.isOpen && (i.panelView.focus(), e());
  }), i.keystrokes.set("arrowup", (t, e) => {
    i.isOpen && (i.panelView.focusLast(), e());
  });
}
function FM(i) {
  i.on("change:isOpen", (t, e, n) => {
    if (n)
      return;
    i.focusTracker.elements.some((s) => s.contains(ae.document.activeElement)) && i.buttonView.focus();
  });
}
function $M(i) {
  i.on("change:isOpen", (t, e, n) => {
    n && i.panelView.focus();
  }, {
    priority: "low"
  });
}
function JC(i, t, e, n) {
  t.on("change", () => {
    const r = [
      ...t
    ].reduce((o, a) => (a instanceof hs && a.children.first instanceof na && o.push(a.children.first), o), []), s = r.some((o) => o.isToggleable);
    r.forEach((o) => {
      o.hasCheckSpace = s;
    });
  }), t.bindTo(e).using((r) => {
    if (r.type === "separator")
      return new Gd(n);
    if (r.type === "group") {
      const s = new _u(n);
      return s.set({
        label: r.label
      }), JC(i, s.items, r.items, n), s.items.delegate("execute").to(i), s;
    } else if (r.type === "button" || r.type === "switchbutton") {
      const s = r.model.role === "menuitemcheckbox" || r.model.role === "menuitemradio", o = new hs(n);
      let a;
      return r.type === "button" ? (a = new na(n), a.set({
        isToggleable: s
      })) : a = new Gs(n), a.bind(...Object.keys(r.model)).to(r.model), a.delegate("execute").to(o), o.children.add(a), o;
    }
    return null;
  });
}
const yi = (i, t, e) => {
  const n = new KC(i.locale);
  return n.set({
    id: t,
    ariaDescribedById: e
  }), n.bind("isReadOnly").to(i, "isEnabled", (r) => !r), n.bind("hasError").to(i, "errorText", (r) => !!r), n.on("input", () => {
    i.errorText = null;
  }), i.bind("isEmpty", "isFocused", "placeholder").to(n), n;
}, XC = (i, t, e) => {
  const n = new TM(i.locale);
  return n.set({
    id: t,
    ariaDescribedById: e,
    inputMode: "numeric"
  }), n.bind("isReadOnly").to(i, "isEnabled", (r) => !r), n.bind("hasError").to(i, "errorText", (r) => !!r), n.on("input", () => {
    i.errorText = null;
  }), i.bind("isEmpty", "isFocused", "placeholder").to(n), n;
}, QC = (i, t, e) => {
  const n = Yn(i.locale);
  return n.set({
    id: t,
    ariaDescribedById: e
  }), n.bind("isEnabled").to(i), n;
}, zM = 150;
class HM extends de {
  /**
  * Creates a view of color picker.
  *
  * @param locale
  * @param config
  */
  constructor(e, n = {}) {
    super(e);
    /**
    * Container for a `#` sign prefix and an input for displaying and defining custom colors
    * in HEX format.
    */
    h(this, "hexInputRow");
    /**
    * Debounced function updating the `color` property in the component
    * and firing the `ColorPickerColorSelectedEvent`. Executed whenever color in component
    * is changed by the user interaction (through the palette or input).
    *
    * @private
    */
    h(this, "_debounceColorPickerEvent");
    /**
    * A reference to the configuration of the color picker specified in the constructor.
    *
    * @private
    */
    h(this, "_config");
    this.set({
      color: "",
      _hexColor: ""
    }), this.hexInputRow = this._createInputRow();
    const r = this.createCollection();
    n.hideInput || r.add(this.hexInputRow), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-picker"
        ],
        tabindex: -1
      },
      children: r
    }), this._config = n, this._debounceColorPickerEvent = Ci((s) => {
      this.set("color", s), this.fire("colorSelected", {
        color: this.color
      });
    }, zM, {
      leading: !0
    }), this.on("set:color", (s, o, a) => {
      s.return = UC(a, this._config.format || "hsl");
    }), this.on("change:color", () => {
      this._hexColor = Xh(this.color);
    }), this.on("change:_hexColor", () => {
      document.activeElement !== this.picker && this.picker.setAttribute("color", this._hexColor), Xh(this.color) != Xh(this._hexColor) && (this.color = this._hexColor);
    });
  }
  /**
  * Renders color picker in the view.
  */
  render() {
    if (super.render(), _M("hex-color-picker", ZV), this.picker = ae.document.createElement("hex-color-picker"), this.picker.setAttribute("class", "hex-color-picker"), this.picker.setAttribute("tabindex", "-1"), this._createSlidersView(), this.element) {
      this.hexInputRow.element ? this.element.insertBefore(this.picker, this.hexInputRow.element) : this.element.appendChild(this.picker);
      const e = document.createElement("style");
      e.textContent = '[role="slider"]:focus [part$="pointer"] {border: 1px solid #fff;outline: 1px solid var(--ck-color-focus-border);box-shadow: 0 0 0 2px #fff;}', this.picker.shadowRoot.appendChild(e);
    }
    this.picker.addEventListener("color-changed", (e) => {
      const n = e.detail.value;
      this._debounceColorPickerEvent(n);
    });
  }
  /**
  * Focuses the first pointer in color picker.
  *
  */
  focus() {
    /* istanbul ignore next -- @preserve */
    !this._config.hideInput && (Me.isGecko || Me.isiOS || Me.isSafari) && this.hexInputRow.children.get(1).focus(), this.slidersView.first.focus();
  }
  /**
  * Creates collection of sliders in color picker.
  *
  * @private
  */
  _createSlidersView() {
    const r = [
      ...this.picker.shadowRoot.children
    ].filter((s) => s.getAttribute("role") === "slider").map((s) => new jM(s));
    this.slidersView = this.createCollection(), r.forEach((s) => {
      this.slidersView.add(s);
    });
  }
  /**
  * Creates input row for defining custom colors in color picker.
  *
  * @private
  */
  _createInputRow() {
    const e = this._createColorInput();
    return new UM(this.locale, e);
  }
  /**
  * Creates the input where user can type or paste the color in hex format.
  *
  * @private
  */
  _createColorInput() {
    const e = new Qt(this.locale, yi), { t: n } = this.locale;
    return e.set({
      label: n("HEX"),
      class: "color-picker-hex-input"
    }), e.fieldView.bind("value").to(this, "_hexColor", (r) => e.isFocused ? e.fieldView.value : r.startsWith("#") ? r.substring(1) : r), e.fieldView.on("input", () => {
      const r = e.fieldView.element.value;
      if (r) {
        const s = ek(r);
        s && this._debounceColorPickerEvent(s);
      }
    }), e;
  }
  /**
  * Validates the view and returns `false` when some fields are invalid.
  */
  isValid() {
    const { t: e } = this.locale;
    return this._config.hideInput ? !0 : (this.resetValidationStatus(), this.hexInputRow.getParsedColor() ? !0 : (this.hexInputRow.inputView.errorText = e('Please enter a valid color (e.g. "ff0000").'), !1));
  }
  /**
  * Cleans up the supplementary error and information text of input inside the {@link #hexInputRow}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetValidationStatus() {
    this.hexInputRow.inputView.errorText = null;
  }
}
function Xh(i) {
  let t = yM(i);
  return t || (t = "#000"), t.length === 4 && (t = "#" + [
    t[1],
    t[1],
    t[2],
    t[2],
    t[3],
    t[3]
  ].join("")), t.toLowerCase();
}
class jM extends de {
  /**
  * @param element HTML element of slider in color picker.
  */
  constructor(t) {
    super(), this.element = t;
  }
  /**
  * Focuses element.
  */
  focus() {
    this.element.focus();
  }
}
class WM extends de {
  constructor(t) {
    super(t), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-picker__hash-view"
        ]
      },
      children: "#"
    });
  }
}
class UM extends de {
  /**
  * Creates an instance of the form row class.
  *
  * @param locale The locale instance.
  * @param inputView Hex color input element.
  */
  constructor(e, n) {
    super(e);
    /**
    * A collection of row items (buttons, dropdowns, etc.).
    */
    h(this, "children");
    /**
    * Hex input view element.
    */
    h(this, "inputView");
    this.inputView = n, this.children = this.createCollection([
      new WM(),
      this.inputView
    ]), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-picker__row"
        ]
      },
      children: this.children
    });
  }
  /**
  * Returns false if color input value is not in hex format.
  */
  getParsedColor() {
    return ek(this.inputView.fieldView.element.value);
  }
}
function ek(i) {
  if (!i)
    return null;
  const t = i.trim().replace(/^#/, "");
  return ![
    3,
    4,
    6,
    8
  ].includes(t.length) || !/^(([0-9a-fA-F]{2}){3,4}|([0-9a-fA-F]){3,4})$/.test(t) ? null : `#${t}`;
}
class qM extends (/* @__PURE__ */ st(Pn)) {
  constructor(t) {
    super(t), this.set("isEmpty", !0), this.on("change", () => {
      this.set("isEmpty", this.length === 0);
    });
  }
  /**
  * Adds a color to the document color collection.
  *
  * This method ensures that no color duplicates are inserted (compared using
  * the color value of the {@link module:ui/colorgrid/colorgridview~ColorDefinition}).
  *
  * If the item does not have an ID, it will be automatically generated and set on the item.
  *
  * @param index The position of the item in the collection. The item is pushed to the collection when `index` is not specified.
  * @fires add
  * @fires change
  */
  add(t, e) {
    return this.find((n) => n.color === t.color) ? this : super.add(t, e);
  }
  /**
  * Checks if an object with given colors is present in the document color collection.
  */
  hasColor(t) {
    return !!this.find((e) => e.color === t);
  }
}
class GM extends de {
  /**
  * Creates an instance of the view.
  *
  * @param locale The localization services instance.
  * @param colors An array with definitions of colors to be displayed in the table.
  * @param columns The number of columns in the color grid.
  * @param removeButtonLabel The label of the button responsible for removing the color.
  * @param colorPickerLabel The label of the button responsible for color picker appearing.
  * @param documentColorsLabel The label for the section with the document colors.
  * @param documentColorsCount The number of colors in the document colors section inside the color dropdown.
  * @param focusTracker Tracks information about the DOM focus in the list.
  * @param focusables A collection of views that can be focused in the view.
  */
  constructor(e, { colors: n, columns: r, removeButtonLabel: s, documentColorsLabel: o, documentColorsCount: a, colorPickerLabel: l, focusTracker: c, focusables: u }) {
    super(e);
    /**
    * A collection of the children of the table.
    */
    h(this, "items");
    /**
    * An array with objects representing colors to be displayed in the grid.
    */
    h(this, "colorDefinitions");
    /**
    * Tracks information about the DOM focus in the list.
    */
    h(this, "focusTracker");
    /**
    * The number of columns in the color grid.
    */
    h(this, "columns");
    /**
    * Preserves the reference to {@link module:ui/colorselector/documentcolorcollection~DocumentColorCollection} used to collect
    * definitions that store the document colors.
    *
    * @readonly
    */
    h(this, "documentColors");
    /**
    * The maximum number of colors in the document colors section.
    * If it equals 0, the document colors section is not added.
    *
    * @readonly
    */
    h(this, "documentColorsCount");
    /**
    * Preserves the reference to {@link module:ui/colorgrid/colorgridview~ColorGridView} used to create
    * the default (static) color set.
    *
    * The property is loaded once the the parent dropdown is opened the first time.
    *
    * @readonly
    */
    h(this, "staticColorsGrid");
    /**
    * Preserves the reference to {@link module:ui/colorgrid/colorgridview~ColorGridView} used to create
    * the document colors. It remains undefined if the document colors feature is disabled.
    *
    * The property is loaded once the the parent dropdown is opened the first time.
    *
    * @readonly
    */
    h(this, "documentColorsGrid");
    /**
    * The "Color picker" button view.
    */
    h(this, "colorPickerButtonView");
    /**
    * The "Remove color" button view.
    */
    h(this, "removeColorButtonView");
    /**
    * A collection of views that can be focused in the view.
    *
    * @readonly
    */
    h(this, "_focusables");
    /**
    * Document color section's label.
    *
    * @readonly
    */
    h(this, "_documentColorsLabel");
    /**
    * The label of the button responsible for removing color attributes.
    */
    h(this, "_removeButtonLabel");
    /**
    * The label of the button responsible for switching to the color picker component.
    */
    h(this, "_colorPickerLabel");
    const d = this.bindTemplate;
    this.set("isVisible", !0), this.focusTracker = c, this.items = this.createCollection(), this.colorDefinitions = n, this.columns = r, this.documentColors = new qM(), this.documentColorsCount = a, this._focusables = u, this._removeButtonLabel = s, this._colorPickerLabel = l, this._documentColorsLabel = o, this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck-color-grids-fragment",
          d.if("isVisible", "ck-hidden", (f) => !f)
        ]
      },
      children: this.items
    }), this.removeColorButtonView = this._createRemoveColorButton(), this.items.add(this.removeColorButtonView);
  }
  /**
  * Scans through the editor model and searches for text node attributes with the given attribute name.
  * Found entries are set as document colors.
  *
  * All the previously stored document colors will be lost in the process.
  *
  * @param model The model used as a source to obtain the document colors.
  * @param attributeName Determines the name of the related model's attribute for a given dropdown.
  */
  updateDocumentColors(e, n) {
    const r = e.document, s = this.documentColorsCount;
    this.documentColors.clear();
    for (const o of r.getRoots()) {
      const a = e.createRangeIn(o);
      for (const l of a.getItems())
        if (l.is("$textProxy") && l.hasAttribute(n) && (this._addColorToDocumentColors(l.getAttribute(n)), this.documentColors.length >= s))
          return;
    }
  }
  /**
  * Refreshes the state of the selected color in one or both {@link module:ui/colorgrid/colorgridview~ColorGridView}s
  * available in the {@link module:ui/colorselector/colorselectorview~ColorSelectorView}. It guarantees that the selection will
  * occur only in one of them.
  */
  updateSelectedColors() {
    const e = this.documentColorsGrid, n = this.staticColorsGrid, r = this.selectedColor;
    n.selectedColor = r, e && (e.selectedColor = r);
  }
  /**
  * @inheritDoc
  */
  render() {
    if (super.render(), this.staticColorsGrid = this._createStaticColorsGrid(), this.items.add(this.staticColorsGrid), this.documentColorsCount) {
      const e = li.bind(this.documentColors, this.documentColors), n = new de(this.locale);
      n.setTemplate({
        tag: "span",
        attributes: {
          class: [
            "ck",
            "ck-color-grid__label",
            e.if("isEmpty", "ck-hidden")
          ]
        },
        children: [
          {
            text: this._documentColorsLabel
          }
        ]
      }), this.items.add(n), this.documentColorsGrid = this._createDocumentColorsGrid(), this.items.add(this.documentColorsGrid);
    }
    this._createColorPickerButton(), this._addColorSelectorElementsToFocusTracker();
  }
  /**
  * Focuses the component.
  */
  focus() {
    this.removeColorButtonView.focus();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy();
  }
  /**
  * Handles displaying the color picker button (if it was previously created) and making it focusable.
  */
  addColorPickerButton() {
    this.colorPickerButtonView && (this.items.add(this.colorPickerButtonView), this.focusTracker.add(this.colorPickerButtonView.element), this._focusables.add(this.colorPickerButtonView));
  }
  /**
  * Adds color selector elements to focus tracker.
  */
  _addColorSelectorElementsToFocusTracker() {
    this.focusTracker.add(this.removeColorButtonView.element), this._focusables.add(this.removeColorButtonView), this.staticColorsGrid && (this.focusTracker.add(this.staticColorsGrid.element), this._focusables.add(this.staticColorsGrid)), this.documentColorsGrid && (this.focusTracker.add(this.documentColorsGrid.element), this._focusables.add(this.documentColorsGrid));
  }
  /**
  * Creates the button responsible for displaying the color picker component.
  */
  _createColorPickerButton() {
    this.colorPickerButtonView = new Te(), this.colorPickerButtonView.set({
      label: this._colorPickerLabel,
      withText: !0,
      icon: ee.colorPalette,
      class: "ck-color-selector__color-picker"
    }), this.colorPickerButtonView.on("execute", () => {
      this.fire("colorPicker:show");
    });
  }
  /**
  * Adds the remove color button as a child of the current view.
  */
  _createRemoveColorButton() {
    const e = new Te();
    return e.set({
      withText: !0,
      icon: ee.eraser,
      label: this._removeButtonLabel
    }), e.class = "ck-color-selector__remove-color", e.on("execute", () => {
      this.fire("execute", {
        value: null,
        source: "removeColorButton"
      });
    }), e.render(), e;
  }
  /**
  * Creates a static color grid based on the editor configuration.
  */
  _createStaticColorsGrid() {
    const e = new Vv(this.locale, {
      colorDefinitions: this.colorDefinitions,
      columns: this.columns
    });
    return e.on("execute", (n, r) => {
      this.fire("execute", {
        value: r.value,
        source: "staticColorsGrid"
      });
    }), e;
  }
  /**
  * Creates the document colors section view and binds it to {@link #documentColors}.
  */
  _createDocumentColorsGrid() {
    const e = li.bind(this.documentColors, this.documentColors), n = new Vv(this.locale, {
      columns: this.columns
    });
    return n.extendTemplate({
      attributes: {
        class: e.if("isEmpty", "ck-hidden")
      }
    }), n.items.bindTo(this.documentColors).using((r) => {
      const s = new WC();
      return s.set({
        color: r.color,
        hasBorder: r.options && r.options.hasBorder
      }), r.label && s.set({
        label: r.label,
        tooltip: !0
      }), s.on("execute", () => {
        this.fire("execute", {
          value: r.color,
          source: "documentColorsGrid"
        });
      }), s;
    }), this.documentColors.on("change:isEmpty", (r, s, o) => {
      o && (n.selectedColor = null);
    }), n;
  }
  /**
  * Adds a given color to the document colors list. If possible, the method will attempt to use
  * data from the {@link #colorDefinitions} (label, color options).
  *
  * @param color A string that stores the value of the recently applied color.
  */
  _addColorToDocumentColors(e) {
    const n = this.colorDefinitions.find((r) => r.color === e);
    n ? this.documentColors.add(Object.assign({}, n)) : this.documentColors.add({
      color: e,
      label: e,
      options: {
        hasBorder: !1
      }
    });
  }
}
class KM extends de {
  /**
  * Creates an instance of the view.
  *
  * @param locale The localization services instance.
  * @param focusTracker Tracks information about the DOM focus in the list.
  * @param focusables A collection of views that can be focused in the view..
  * @param keystrokes An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
  * @param colorPickerViewConfig The configuration of color picker feature. If set to `false`, the color picker
  * will not be rendered.
  */
  constructor(e, { focusTracker: n, focusables: r, keystrokes: s, colorPickerViewConfig: o }) {
    super(e);
    /**
    * A collection of component's children.
    */
    h(this, "items");
    /**
    * A view with saturation and hue sliders and color input.
    */
    h(this, "colorPickerView");
    /**
    * The "Save" button view.
    */
    h(this, "saveButtonView");
    /**
    * The "Cancel" button view.
    */
    h(this, "cancelButtonView");
    /**
    * The action bar where are "Save" button and "Cancel" button.
    */
    h(this, "actionBarView");
    /**
    * Tracks information about the DOM focus in the list.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * A collection of views that can be focused in the view.
    *
    * @readonly
    */
    h(this, "_focusables");
    /**
    * A reference to the configuration of {@link #colorPickerView}. `false` when the view was
    * configured without a color picker.
    *
    * @readonly
    */
    h(this, "_colorPickerViewConfig");
    this.items = this.createCollection(), this.focusTracker = n, this.keystrokes = s, this.set("isVisible", !1), this.set("selectedColor", void 0), this._focusables = r, this._colorPickerViewConfig = o;
    const a = this.bindTemplate, { saveButtonView: l, cancelButtonView: c } = this._createActionButtons();
    this.saveButtonView = l, this.cancelButtonView = c, this.actionBarView = this._createActionBarView({
      saveButtonView: l,
      cancelButtonView: c
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck-color-picker-fragment",
          a.if("isVisible", "ck-hidden", (u) => !u)
        ]
      },
      children: this.items
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    const e = new HM(this.locale, {
      ...this._colorPickerViewConfig
    });
    this.colorPickerView = e, this.colorPickerView.render(), this.selectedColor && (e.color = this.selectedColor), this.listenTo(this, "change:selectedColor", (n, r, s) => {
      e.color = s;
    }), this.items.add(this.colorPickerView), this.items.add(this.actionBarView), this._addColorPickersElementsToFocusTracker(), this._stopPropagationOnArrowsKeys(), this._executeOnEnterPress(), this._executeUponColorChange();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy();
  }
  /**
  * Focuses the color picker.
  */
  focus() {
    this.colorPickerView.focus();
  }
  /**
  * Reset validation messages.
  */
  resetValidationStatus() {
    this.colorPickerView.resetValidationStatus();
  }
  /**
  * When color picker is focused and "enter" is pressed it executes command.
  */
  _executeOnEnterPress() {
    this.keystrokes.set("enter", (e) => {
      this.isVisible && this.focusTracker.focusedElement !== this.cancelButtonView.element && this.colorPickerView.isValid() && (this.fire("execute", {
        value: this.selectedColor
      }), e.stopPropagation(), e.preventDefault());
    });
  }
  /**
  * Removes default behavior of arrow keys in dropdown.
  */
  _stopPropagationOnArrowsKeys() {
    const e = (n) => n.stopPropagation();
    this.keystrokes.set("arrowright", e), this.keystrokes.set("arrowleft", e), this.keystrokes.set("arrowup", e), this.keystrokes.set("arrowdown", e);
  }
  /**
  * Adds color picker elements to focus tracker.
  */
  _addColorPickersElementsToFocusTracker() {
    for (const n of this.colorPickerView.slidersView)
      this.focusTracker.add(n.element), this._focusables.add(n);
    const e = this.colorPickerView.hexInputRow.children.get(1);
    e.element && (this.focusTracker.add(e.element), this._focusables.add(e)), this.focusTracker.add(this.saveButtonView.element), this._focusables.add(this.saveButtonView), this.focusTracker.add(this.cancelButtonView.element), this._focusables.add(this.cancelButtonView);
  }
  /**
  * Creates bar containing "Save" and "Cancel" buttons.
  */
  _createActionBarView({ saveButtonView: e, cancelButtonView: n }) {
    const r = new de(), s = this.createCollection();
    return s.add(e), s.add(n), r.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-selector_action-bar"
        ]
      },
      children: s
    }), r;
  }
  /**
  * Creates "Save" and "Cancel" buttons.
  */
  _createActionButtons() {
    const e = this.locale, n = e.t, r = new Te(e), s = new Te(e);
    return r.set({
      icon: ee.check,
      class: "ck-button-save",
      type: "button",
      withText: !1,
      label: n("Accept")
    }), s.set({
      icon: ee.cancel,
      class: "ck-button-cancel",
      type: "button",
      withText: !1,
      label: n("Cancel")
    }), r.on("execute", () => {
      this.colorPickerView.isValid() && this.fire("execute", {
        source: "colorPickerSaveButton",
        value: this.selectedColor
      });
    }), s.on("execute", () => {
      this.fire("colorPicker:cancel");
    }), {
      saveButtonView: r,
      cancelButtonView: s
    };
  }
  /**
  * Fires the `execute` event if color in color picker has been changed
  * by the user.
  */
  _executeUponColorChange() {
    this.colorPickerView.on("colorSelected", (e, n) => {
      this.fire("execute", {
        value: n.color,
        source: "colorPicker"
      }), this.set("selectedColor", n.color);
    });
  }
}
class ZM extends de {
  /**
  * Creates a view to be inserted as a child of {@link module:ui/dropdown/dropdownview~DropdownView}.
  *
  * @param locale The localization services instance.
  * @param colors An array with definitions of colors to be displayed in the table.
  * @param columns The number of columns in the color grid.
  * @param removeButtonLabel The label of the button responsible for removing the color.
  * @param colorPickerLabel The label of the button responsible for color picker appearing.
  * @param documentColorsLabel The label for the section with the document colors.
  * @param documentColorsCount The number of colors in the document colors section inside the color dropdown.
  * @param colorPickerViewConfig The configuration of color picker feature. If set to `false`, the color picker will be hidden.
  */
  constructor(e, { colors: n, columns: r, removeButtonLabel: s, documentColorsLabel: o, documentColorsCount: a, colorPickerLabel: l, colorPickerViewConfig: c }) {
    super(e);
    /**
    * Tracks information about the DOM focus in the list.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * A collection of components.
    */
    h(this, "items");
    /**
    * A fragment that allows users to select colors from the a predefined set and from existing document colors.
    */
    h(this, "colorGridsFragmentView");
    /**
    * A fragment that allows users to select a color from a color picker.
    */
    h(this, "colorPickerFragmentView");
    /**
    * Helps cycling over focusable {@link #items} in the list.
    *
    * @readonly
    */
    h(this, "_focusCycler");
    /**
    * A collection of views that can be focused in the view.
    *
    * @readonly
    */
    h(this, "_focusables");
    /**
    * The configuration of color picker sub-component.
    */
    h(this, "_colorPickerViewConfig");
    this.items = this.createCollection(), this.focusTracker = new wt(), this.keystrokes = new Ot(), this._focusables = new In(), this._colorPickerViewConfig = c, this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate list items backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: "shift + tab",
        // Navigate list items forwards using the <kbd>Tab</kbd> key.
        focusNext: "tab"
      }
    }), this.colorGridsFragmentView = new GM(e, {
      colors: n,
      columns: r,
      removeButtonLabel: s,
      documentColorsLabel: o,
      documentColorsCount: a,
      colorPickerLabel: l,
      focusTracker: this.focusTracker,
      focusables: this._focusables
    }), this.colorPickerFragmentView = new KM(e, {
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokes: this.keystrokes,
      colorPickerViewConfig: c
    }), this.set("_isColorGridsFragmentVisible", !0), this.set("_isColorPickerFragmentVisible", !1), this.set("selectedColor", void 0), this.colorGridsFragmentView.bind("isVisible").to(this, "_isColorGridsFragmentVisible"), this.colorPickerFragmentView.bind("isVisible").to(this, "_isColorPickerFragmentVisible"), this.on("change:selectedColor", (u, d, f) => {
      this.colorGridsFragmentView.set("selectedColor", f), this.colorPickerFragmentView.set("selectedColor", f);
    }), this.colorGridsFragmentView.on("change:selectedColor", (u, d, f) => {
      this.set("selectedColor", f);
    }), this.colorPickerFragmentView.on("change:selectedColor", (u, d, f) => {
      this.set("selectedColor", f);
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-selector"
        ]
      },
      children: this.items
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Renders the internals of the component on demand:
  * * {@link #colorPickerFragmentView},
  * * {@link #colorGridsFragmentView}.
  *
  * It allows for deferring component initialization to improve the performance.
  *
  * See {@link #showColorPickerFragment}, {@link #showColorGridsFragment}.
  */
  appendUI() {
    this._appendColorGridsFragment(), this._colorPickerViewConfig && this._appendColorPickerFragment();
  }
  /**
  * Shows the {@link #colorPickerFragmentView} and hides the {@link #colorGridsFragmentView}.
  *
  * **Note**: It requires {@link #appendUI} to be called first.
  *
  * See {@link #showColorGridsFragment}, {@link ~ColorSelectorView#event:colorPicker:show}.
  */
  showColorPickerFragment() {
    !this.colorPickerFragmentView.colorPickerView || this._isColorPickerFragmentVisible || (this._isColorPickerFragmentVisible = !0, this.colorPickerFragmentView.focus(), this.colorPickerFragmentView.resetValidationStatus(), this._isColorGridsFragmentVisible = !1);
  }
  /**
  * Shows the {@link #colorGridsFragmentView} and hides the {@link #colorPickerFragmentView}.
  *
  * See {@link #showColorPickerFragment}.
  *
  * **Note**: It requires {@link #appendUI} to be called first.
  */
  showColorGridsFragment() {
    this._isColorGridsFragmentVisible || (this._isColorGridsFragmentVisible = !0, this.colorGridsFragmentView.focus(), this._isColorPickerFragmentVisible = !1);
  }
  /**
  * Focuses the first focusable element in {@link #items}.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the last focusable element in {@link #items}.
  */
  focusLast() {
    this._focusCycler.focusLast();
  }
  /**
  * Scans through the editor model and searches for text node attributes with the given `attributeName`.
  * Found entries are set as document colors in {@link #colorGridsFragmentView}.
  *
  * All the previously stored document colors will be lost in the process.
  *
  * @param model The model used as a source to obtain the document colors.
  * @param attributeName Determines the name of the related model's attribute for a given dropdown.
  */
  updateDocumentColors(e, n) {
    this.colorGridsFragmentView.updateDocumentColors(e, n);
  }
  /**
  * Refreshes the state of the selected color in one or both grids located in {@link #colorGridsFragmentView}.
  *
  * It guarantees that the selection will occur only in one of them.
  */
  updateSelectedColors() {
    this.colorGridsFragmentView.updateSelectedColors();
  }
  /**
  * Appends the view containing static and document color grid views.
  */
  _appendColorGridsFragment() {
    this.items.length || (this.items.add(this.colorGridsFragmentView), this.colorGridsFragmentView.delegate("execute").to(this), this.colorGridsFragmentView.delegate("colorPicker:show").to(this));
  }
  /**
  * Appends the view with the color picker.
  */
  _appendColorPickerFragment() {
    this.items.length !== 2 && (this.items.add(this.colorPickerFragmentView), this.colorGridsFragmentView.colorPickerButtonView && this.colorGridsFragmentView.colorPickerButtonView.on("execute", () => {
      this.showColorPickerFragment();
    }), this.colorGridsFragmentView.addColorPickerButton(), this.colorPickerFragmentView.delegate("execute").to(this), this.colorPickerFragmentView.delegate("colorPicker:cancel").to(this));
  }
}
class YM {
  /**
  * Creates an instance of the factory.
  *
  * @param editor The editor instance.
  */
  constructor(t) {
    /**
    * The editor instance that the factory belongs to.
    */
    h(this, "editor");
    /**
    * Registered component factories.
    */
    h(this, "_components", /* @__PURE__ */ new Map());
    this.editor = t;
  }
  /**
  * Returns an iterator of registered component names. Names are returned in lower case.
  */
  *names() {
    for (const t of this._components.values())
      yield t.originalName;
  }
  /**
  * Registers a component factory function that will be used by the
  * {@link #create create} method and called with the
  * {@link module:core/editor/editor~Editor#locale editor locale} as an argument,
  * allowing localization of the {@link module:ui/view~View view}.
  *
  * @param name The name of the component.
  * @param callback The callback that returns the component.
  */
  add(t, e) {
    this._components.set(Qh(t), {
      callback: e,
      originalName: t
    });
  }
  /**
  * Creates an instance of a component registered in the factory under a specific name.
  *
  * When called, the {@link module:core/editor/editor~Editor#locale editor locale} is passed to
  * the previously {@link #add added} factory function, allowing localization of the
  * {@link module:ui/view~View view}.
  *
  * @param name The name of the component.
  * @returns The instantiated component view.
  */
  create(t) {
    if (!this.has(t))
      throw new T("componentfactory-item-missing", this, {
        name: t
      });
    return this._components.get(Qh(t)).callback(this.editor.locale);
  }
  /**
  * Checks if a component of a given name is registered in the factory.
  *
  * @param name The name of the component.
  */
  has(t) {
    return this._components.has(Qh(t));
  }
}
function Qh(i) {
  return String(i).toLowerCase();
}
const Nv = "ck-tooltip", Cn = class Cn extends (/* @__PURE__ */ Xn()) {
  /**
  * Creates an instance of the tooltip manager.
  */
  constructor(e) {
    super();
    /**
    * The view rendering text of the tooltip.
    */
    h(this, "tooltipTextView");
    /**
    * The instance of the balloon panel that renders and positions the tooltip.
    */
    h(this, "balloonPanelView");
    /**
    * Stores the reference to the DOM element the tooltip is attached to. `null` when there's no tooltip
    * in the UI.
    */
    h(this, "_currentElementWithTooltip", null);
    /**
    * Stores the current tooltip position. `null` when there's no tooltip in the UI.
    */
    h(this, "_currentTooltipPosition", null);
    /**
    * An instance of the mutation observer that keeps track on target element attributes changes.
    */
    h(this, "_mutationObserver", null);
    /**
    * A debounced version of {@link #_pinTooltip}. Tooltips show with a delay to avoid flashing and
    * to improve the UX.
    */
    h(this, "_pinTooltipDebounced");
    /**
    * A debounced version of {@link #_unpinTooltip}. Tooltips hide with a delay to allow hovering of their titles.
    */
    h(this, "_unpinTooltipDebounced");
    h(this, "_watchdogExcluded");
    if (Cn._editors.add(e), Cn._instance)
      return Cn._instance;
    Cn._instance = this, this.tooltipTextView = new de(e.locale), this.tooltipTextView.set("text", ""), this.tooltipTextView.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-tooltip__text"
        ]
      },
      children: [
        {
          text: this.tooltipTextView.bindTemplate.to("text")
        }
      ]
    }), this.balloonPanelView = new fn(e.locale), this.balloonPanelView.class = Nv, this.balloonPanelView.content.add(this.tooltipTextView), this._mutationObserver = JM(() => {
      this._updateTooltipPosition();
    }), this._pinTooltipDebounced = Ci(this._pinTooltip, 600), this._unpinTooltipDebounced = Ci(this._unpinTooltip, 400), this.listenTo(ae.document, "keydown", this._onKeyDown.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "mouseenter", this._onEnterOrFocus.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "mouseleave", this._onLeaveOrBlur.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "focus", this._onEnterOrFocus.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "blur", this._onLeaveOrBlur.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "scroll", this._onScroll.bind(this), {
      useCapture: !0
    }), this._watchdogExcluded = !0;
  }
  /**
  * Destroys the tooltip manager.
  *
  * **Note**: The manager singleton cannot be destroyed until all editors that use it are destroyed.
  *
  * @param editor The editor the manager was created for.
  */
  destroy(e) {
    const n = e.ui.view && e.ui.view.body;
    Cn._editors.delete(e), this.stopListening(e.ui), n && n.has(this.balloonPanelView) && n.remove(this.balloonPanelView), Cn._editors.size || (this._unpinTooltip(), this.balloonPanelView.destroy(), this.stopListening(), Cn._instance = null);
  }
  /**
  * Returns {@link #balloonPanelView} {@link module:utils/dom/position~PositioningFunction positioning functions} for a given position
  * name.
  *
  * @param position Name of the position (`s`, `se`, `sw`, `n`, `e`, or `w`).
  * @returns Positioning functions to be used by the {@link #balloonPanelView}.
  */
  static getPositioningFunctions(e) {
    const n = Cn.defaultBalloonPositions;
    return {
      // South is most popular. We can use positioning heuristics to avoid clipping by the viewport with the sane fallback.
      s: [
        n.southArrowNorth,
        n.southArrowNorthEast,
        n.southArrowNorthWest
      ],
      n: [
        n.northArrowSouth
      ],
      e: [
        n.eastArrowWest
      ],
      w: [
        n.westArrowEast
      ],
      sw: [
        n.southArrowNorthEast
      ],
      se: [
        n.southArrowNorthWest
      ]
    }[e];
  }
  /**
  * Handles hiding tooltips on `keydown` in DOM.
  *
  * @param evt An object containing information about the fired event.
  * @param domEvent The DOM event.
  */
  _onKeyDown(e, n) {
    n.key === "Escape" && this._currentElementWithTooltip && (this._unpinTooltip(), n.stopPropagation());
  }
  /**
  * Handles displaying tooltips on `mouseenter` and `focus` in DOM.
  *
  * @param evt An object containing information about the fired event.
  * @param domEvent The DOM event.
  */
  _onEnterOrFocus(e, { target: n }) {
    const r = ef(n);
    if (!r) {
      e.name === "focus" && this._unpinTooltip();
      return;
    }
    r !== this._currentElementWithTooltip && (this._unpinTooltip(), e.name === "focus" && !r.matches(":hover") ? this._pinTooltip(r, tf(r)) : this._pinTooltipDebounced(r, tf(r)));
  }
  /**
  * Handles hiding tooltips on `mouseleave` and `blur` in DOM.
  *
  * @param evt An object containing information about the fired event.
  * @param domEvent The DOM event.
  */
  _onLeaveOrBlur(e, { target: n, relatedTarget: r }) {
    if (e.name === "mouseleave") {
      if (!us(n))
        return;
      const s = this.balloonPanelView.element, o = s && (s === r || s.contains(r)), a = !o && n === s;
      if (o) {
        this._unpinTooltipDebounced.cancel();
        return;
      }
      if (!a && this._currentElementWithTooltip && n !== this._currentElementWithTooltip)
        return;
      const l = ef(n), c = ef(r);
      (a || l && l !== c) && this._unpinTooltipDebounced();
    } else {
      if (this._currentElementWithTooltip && n !== this._currentElementWithTooltip)
        return;
      this._unpinTooltipDebounced();
    }
  }
  /**
  * Handles hiding tooltips on `scroll` in DOM.
  *
  * @param evt An object containing information about the fired event.
  * @param domEvent The DOM event.
  */
  _onScroll(e, { target: n }) {
    this._currentElementWithTooltip && (n.contains(this.balloonPanelView.element) && n.contains(this._currentElementWithTooltip) || this._unpinTooltip());
  }
  /**
  * Pins the tooltip to a specific DOM element.
  *
  * @param options.text Text of the tooltip to display.
  * @param options.position The position of the tooltip.
  * @param options.cssClass Additional CSS class of the balloon with the tooltip.
  */
  _pinTooltip(e, { text: n, position: r, cssClass: s }) {
    this._unpinTooltip();
    const o = Ft(Cn._editors.values()).ui.view.body;
    o.has(this.balloonPanelView) || o.add(this.balloonPanelView), this.tooltipTextView.text = n, this.balloonPanelView.class = [
      Nv,
      s
    ].filter((a) => a).join(" "), this.balloonPanelView.pin({
      target: e,
      positions: Cn.getPositioningFunctions(r)
    }), this._mutationObserver.attach(e);
    for (const a of Cn._editors)
      this.listenTo(a.ui, "update", this._updateTooltipPosition.bind(this), {
        priority: "low"
      });
    this._currentElementWithTooltip = e, this._currentTooltipPosition = r;
  }
  /**
  * Unpins the tooltip and cancels all queued pinning.
  */
  _unpinTooltip() {
    this._unpinTooltipDebounced.cancel(), this._pinTooltipDebounced.cancel(), this.balloonPanelView.unpin();
    for (const e of Cn._editors)
      this.stopListening(e.ui, "update");
    this._currentElementWithTooltip = null, this._currentTooltipPosition = null, this.tooltipTextView.text = "", this._mutationObserver.detach();
  }
  /**
  * Updates the position of the tooltip so it stays in sync with the element it is pinned to.
  *
  * Hides the tooltip when the element is no longer visible in DOM or the tooltip text was removed.
  */
  _updateTooltipPosition() {
    if (!this._currentElementWithTooltip)
      return;
    const e = tf(this._currentElementWithTooltip);
    if (!mr(this._currentElementWithTooltip) || !e.text) {
      this._unpinTooltip();
      return;
    }
    this.balloonPanelView.pin({
      target: this._currentElementWithTooltip,
      positions: Cn.getPositioningFunctions(e.position)
    });
  }
};
/**
* A set of default {@link module:utils/dom/position~PositioningFunction positioning functions} used by the `TooltipManager`
* to pin tooltips in different positions.
*/
h(Cn, "defaultBalloonPositions", /* @__PURE__ */ fn.generatePositions({
  heightOffset: 5,
  sideOffset: 13
})), /**
* A set of editors the single tooltip manager instance must listen to.
* This is mostly to handle `EditorUI#update` listeners from individual editors.
*/
h(Cn, "_editors", /* @__PURE__ */ new Set()), /**
* A reference to the `TooltipManager` instance. The class is a singleton and as such,
* successive attempts at creating instances should return this instance.
*/
h(Cn, "_instance", null);
let vm = Cn;
function ef(i) {
  return us(i) ? i.closest("[data-cke-tooltip-text]:not([data-cke-tooltip-disabled])") : null;
}
function tf(i) {
  return {
    text: i.dataset.ckeTooltipText,
    position: i.dataset.ckeTooltipPosition || "s",
    cssClass: i.dataset.ckeTooltipClass || ""
  };
}
function JM(i) {
  const t = new MutationObserver(() => {
    i();
  });
  return {
    attach(e) {
      t.observe(e, {
        attributes: !0,
        attributeFilter: [
          "data-cke-tooltip-text",
          "data-cke-tooltip-position"
        ]
      });
    },
    detach() {
      t.disconnect();
    }
  };
}
var XM = `<svg xmlns="http://www.w3.org/2000/svg" width="53" height="10" viewBox="0 0 53 10"><path fill="#1C2331" d="M31.724 1.492a15.139 15.139 0 0 0 .045 1.16 2.434 2.434 0 0 0-.687-.34 3.68 3.68 0 0 0-1.103-.166 2.332 2.332 0 0 0-1.14.255 1.549 1.549 0 0 0-.686.87c-.15.41-.225.98-.225 1.712 0 .939.148 1.659.444 2.161.297.503.792.754 1.487.754.452.015.9-.094 1.294-.316.296-.174.557-.4.771-.669l.14.852h1.282V.007h-1.623v1.485ZM31 6.496a1.77 1.77 0 0 1-.494.061.964.964 0 0 1-.521-.127.758.758 0 0 1-.296-.466 3.984 3.984 0 0 1-.093-.992 4.208 4.208 0 0 1 .098-1.052.753.753 0 0 1 .307-.477 1.08 1.08 0 0 1 .55-.122c.233-.004.466.026.69.089l.483.144v2.553c-.11.076-.213.143-.307.2a1.73 1.73 0 0 1-.417.189ZM35.68 0l-.702.004c-.322.002-.482.168-.48.497l.004.581c.002.33.164.493.486.49l.702-.004c.322-.002.481-.167.48-.496L36.165.49c-.002-.33-.164-.493-.486-.491ZM36.145 2.313l-1.612.01.034 5.482 1.613-.01-.035-5.482ZM39.623.79 37.989.8 38 2.306l-.946.056.006 1.009.949-.006.024 2.983c.003.476.143.844.419 1.106.275.26.658.39 1.148.387.132 0 .293-.01.483-.03.19-.02.38-.046.57-.08.163-.028.324-.068.482-.119l-.183-1.095-.702.004a.664.664 0 0 1-.456-.123.553.553 0 0 1-.14-.422l-.016-2.621 1.513-.01-.006-1.064-1.514.01-.01-1.503ZM46.226 2.388c-.41-.184-.956-.274-1.636-.27-.673.004-1.215.101-1.627.29-.402.179-.72.505-.888.91-.18.419-.268.979-.264 1.68.004.688.1 1.24.285 1.655.172.404.495.724.9.894.414.18.957.268 1.63.264.68-.004 1.224-.099 1.632-.284.4-.176.714-.501.878-.905.176-.418.263-.971.258-1.658-.004-.702-.097-1.261-.28-1.677a1.696 1.696 0 0 0-.888-.9Zm-.613 3.607a.77.77 0 0 1-.337.501 1.649 1.649 0 0 1-1.317.009.776.776 0 0 1-.343-.497 4.066 4.066 0 0 1-.105-1.02 4.136 4.136 0 0 1 .092-1.03.786.786 0 0 1 .337-.507 1.59 1.59 0 0 1 1.316-.008.79.79 0 0 1 .344.502c.078.337.113.683.105 1.03.012.343-.019.685-.092 1.02ZM52.114 2.07a2.67 2.67 0 0 0-1.128.278c-.39.191-.752.437-1.072.73l-.157-.846-1.273.008.036 5.572 1.623-.01-.024-3.78c.35-.124.646-.22.887-.286.26-.075.53-.114.8-.118l.45-.003.144-1.546-.286.001ZM22.083 7.426l-1.576-2.532a2.137 2.137 0 0 0-.172-.253 1.95 1.95 0 0 0-.304-.29.138.138 0 0 1 .042-.04 1.7 1.7 0 0 0 .328-.374l1.75-2.71c.01-.015.025-.028.024-.048-.01-.01-.021-.007-.031-.007L20.49 1.17a.078.078 0 0 0-.075.045l-.868 1.384c-.23.366-.46.732-.688 1.099a.108.108 0 0 1-.112.06c-.098-.005-.196-.001-.294-.002-.018 0-.038.006-.055-.007.002-.02.002-.039.005-.058a4.6 4.6 0 0 0 .046-.701V1.203c0-.02-.009-.032-.03-.03h-.033L16.93 1.17c-.084 0-.073-.01-.073.076v6.491c-.001.018.006.028.025.027h1.494c.083 0 .072.007.072-.071v-2.19c0-.055-.003-.11-.004-.166a3.366 3.366 0 0 0-.05-.417h.06c.104 0 .209.002.313-.002a.082.082 0 0 1 .084.05c.535.913 1.07 1.824 1.607 2.736a.104.104 0 0 0 .103.062c.554-.003 1.107-.002 1.66-.002l.069-.003-.019-.032-.188-.304ZM27.112 6.555c-.005-.08-.004-.08-.082-.08h-2.414c-.053 0-.106-.003-.159-.011a.279.279 0 0 1-.246-.209.558.558 0 0 1-.022-.15c0-.382 0-.762-.002-1.143 0-.032.007-.049.042-.044h2.504c.029.003.037-.012.034-.038V3.814c0-.089.013-.078-.076-.078h-2.44c-.07 0-.062.003-.062-.06v-.837c0-.047.004-.093.013-.14a.283.283 0 0 1 .241-.246.717.717 0 0 1 .146-.011h2.484c.024.002.035-.009.036-.033l.003-.038.03-.496c.01-.183.024-.365.034-.548.005-.085.003-.087-.082-.094-.218-.018-.437-.038-.655-.05a17.845 17.845 0 0 0-.657-.026 72.994 72.994 0 0 0-1.756-.016 1.7 1.7 0 0 0-.471.064 1.286 1.286 0 0 0-.817.655c-.099.196-.149.413-.145.633v3.875c0 .072.003.144.011.216a1.27 1.27 0 0 0 .711 1.029c.228.113.48.167.734.158.757-.005 1.515.002 2.272-.042.274-.016.548-.034.82-.053.03-.002.043-.008.04-.041-.008-.104-.012-.208-.019-.312a69.964 69.964 0 0 1-.05-.768ZM16.14 7.415l-.127-1.075c-.004-.03-.014-.04-.044-.037a13.125 13.125 0 0 1-.998.073c-.336.01-.672.02-1.008.016-.116-.001-.233-.014-.347-.039a.746.746 0 0 1-.45-.262c-.075-.1-.132-.211-.167-.33a3.324 3.324 0 0 1-.126-.773 9.113 9.113 0 0 1-.015-.749c0-.285.022-.57.065-.852.023-.158.066-.312.127-.46a.728.728 0 0 1 .518-.443 1.64 1.64 0 0 1 .397-.048c.628-.001 1.255.003 1.882.05.022.001.033-.006.036-.026l.003-.031.06-.55c.019-.177.036-.355.057-.532.004-.034-.005-.046-.04-.056a5.595 5.595 0 0 0-1.213-.21 10.783 10.783 0 0 0-.708-.02c-.24-.003-.48.01-.719.041a3.477 3.477 0 0 0-.625.14 1.912 1.912 0 0 0-.807.497c-.185.2-.33.433-.424.688a4.311 4.311 0 0 0-.24 1.096c-.031.286-.045.572-.042.86-.006.43.024.86.091 1.286.04.25.104.497.193.734.098.279.26.53.473.734.214.205.473.358.756.446.344.11.702.17 1.063.177a8.505 8.505 0 0 0 1.578-.083 6.11 6.11 0 0 0 .766-.18c.03-.008.047-.023.037-.057a.157.157 0 0 1-.003-.025Z"/><path fill="#AFE229" d="M6.016 6.69a1.592 1.592 0 0 0-.614.21c-.23.132-.422.32-.56.546-.044.072-.287.539-.287.539l-.836 1.528.009.006c.038.025.08.046.123.063.127.046.26.07.395.073.505.023 1.011-.007 1.517-.003.29.009.58.002.869-.022a.886.886 0 0 0 .395-.116.962.962 0 0 0 .312-.286c.056-.083.114-.163.164-.249.24-.408.48-.816.718-1.226.075-.128.148-.257.222-.386l.112-.192a1.07 1.07 0 0 0 .153-.518l-1.304.023s-1.258-.005-1.388.01Z"/><path fill="#771BFF" d="m2.848 9.044.76-1.39.184-.352c-.124-.067-.245-.14-.367-.21-.346-.204-.706-.384-1.045-.6a.984.984 0 0 1-.244-.207c-.108-.134-.136-.294-.144-.46-.021-.409-.002-.818-.009-1.227-.003-.195 0-.39.003-.585.004-.322.153-.553.427-.713l.833-.488c.22-.13.44-.257.662-.385.05-.029.105-.052.158-.077.272-.128.519-.047.76.085l.044.028c.123.06.242.125.358.196.318.178.635.357.952.537.095.056.187.117.275.184.194.144.254.35.266.578.016.284.007.569.006.853-.001.28.004.558 0 .838.592-.003 1.259 0 1.259 0l.723-.013c-.003-.292-.007-.584-.007-.876 0-.524.015-1.048-.016-1.571-.024-.42-.135-.8-.492-1.067a5.02 5.02 0 0 0-.506-.339A400.52 400.52 0 0 0 5.94.787C5.722.664 5.513.524 5.282.423 5.255.406 5.228.388 5.2.373 4.758.126 4.305-.026 3.807.21c-.097.046-.197.087-.29.14A699.896 699.896 0 0 0 .783 1.948c-.501.294-.773.717-.778 1.31-.004.36-.009.718-.001 1.077.016.754-.017 1.508.024 2.261.016.304.07.6.269.848.127.15.279.28.448.382.622.4 1.283.734 1.92 1.11l.183.109Z"/></svg>
`;
const QM = 53, eD = 10, tD = 50, nD = 350, iD = "Powered by";
class rD extends (/* @__PURE__ */ Xn()) {
  /**
  * Creates a "powered by" helper for a given editor. The feature is initialized on Editor#ready
  * event.
  *
  * @param editor
  */
  constructor(e) {
    super();
    /**
    * Editor instance the helper was created for.
    */
    h(this, "editor");
    /**
    * A reference to the balloon panel hosting and positioning the "powered by" link and logo.
    */
    h(this, "_balloonView");
    /**
    * A throttled version of the {@link #_showBalloon} method meant for frequent use to avoid performance loss.
    */
    h(this, "_showBalloonThrottled");
    /**
    * A reference to the last editable element (root, source editing area, etc.) focused by the user.
    * Since the focus can move to other focusable elements in the UI, this reference allows positioning the balloon over the
    * right element whether the user is typing or using the UI.
    */
    h(this, "_lastFocusedEditableElement");
    this.editor = e, this._balloonView = null, this._lastFocusedEditableElement = null, this._showBalloonThrottled = Jo(this._showBalloon.bind(this), 50, {
      leading: !0
    }), e.on("ready", this._handleEditorReady.bind(this));
  }
  /**
  * Destroys the "powered by" helper along with its view.
  */
  destroy() {
    const e = this._balloonView;
    e && (e.unpin(), this._balloonView = null), this._showBalloonThrottled.cancel(), this.stopListening();
  }
  /**
  * Enables "powered by" label once the editor (ui) is ready.
  */
  _handleEditorReady() {
    const e = this.editor, n = !!e.config.get("ui.poweredBy.forceVisible");
    /* istanbul ignore next -- @preserve */
    !n && qR(e.config.get("licenseKey")) === "VALID" || e.ui.view && (e.ui.focusTracker.on("change:isFocused", (r, s, o) => {
      this._updateLastFocusedEditableElement(), o ? this._showBalloon() : this._hideBalloon();
    }), e.ui.focusTracker.on("change:focusedElement", (r, s, o) => {
      this._updateLastFocusedEditableElement(), o && this._showBalloon();
    }), e.ui.on("update", () => {
      this._showBalloonThrottled();
    }));
  }
  /**
  * Creates an instance of the {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView balloon panel}
  * with the "powered by" view inside ready for positioning.
  */
  _createBalloonView() {
    const e = this.editor, n = this._balloonView = new fn(), r = nk(e), s = new sD(e.locale, r.label);
    n.content.add(s), n.set({
      class: "ck-powered-by-balloon"
    }), e.ui.view.body.add(n), this._balloonView = n;
  }
  /**
  * Attempts to display the balloon with the "powered by" view.
  */
  _showBalloon() {
    if (!this._lastFocusedEditableElement)
      return;
    const e = oD(this.editor, this._lastFocusedEditableElement);
    e && (this._balloonView || this._createBalloonView(), this._balloonView.pin(e));
  }
  /**
  * Hides the "powered by" balloon if already visible.
  */
  _hideBalloon() {
    this._balloonView && this._balloonView.unpin();
  }
  /**
  * Updates the {@link #_lastFocusedEditableElement} based on the state of the global focus tracker.
  */
  _updateLastFocusedEditableElement() {
    const e = this.editor, n = e.ui.focusTracker.isFocused, r = e.ui.focusTracker.focusedElement;
    if (!n || !r) {
      this._lastFocusedEditableElement = null;
      return;
    }
    const s = Array.from(e.ui.getEditableElementsNames()).map((o) => e.ui.getEditableElement(o));
    s.includes(r) ? this._lastFocusedEditableElement = r : this._lastFocusedEditableElement = s[0];
  }
}
class sD extends de {
  /**
  * Created an instance of the "powered by" view.
  *
  * @param locale The localization services instance.
  * @param label The label text.
  */
  constructor(t, e) {
    super(t);
    const n = new pr(), r = this.bindTemplate;
    n.set({
      content: XM,
      isColorInherited: !1
    }), n.extendTemplate({
      attributes: {
        style: {
          width: QM + "px",
          height: eD + "px"
        }
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-powered-by"
        ],
        "aria-hidden": !0
      },
      children: [
        {
          tag: "a",
          attributes: {
            href: "https://ckeditor.com/powered-by-ckeditor/?utm_source=ckeditor&utm_medium=referral&utm_campaign=701Dn000000hVgmIAE_powered_by_ckeditor_logo",
            target: "_blank",
            tabindex: "-1"
          },
          children: [
            ...e ? [
              {
                tag: "span",
                attributes: {
                  class: [
                    "ck",
                    "ck-powered-by__label"
                  ]
                },
                children: [
                  e
                ]
              }
            ] : [],
            n
          ],
          on: {
            dragstart: r.to((s) => s.preventDefault())
          }
        }
      ]
    });
  }
}
function oD(i, t) {
  const e = nk(i), n = e.side === "right" ? aD(t, e) : lD(t, e);
  return {
    target: t,
    positions: [
      n
    ]
  };
}
function aD(i, t) {
  return tk(i, t, (e, n) => e.left + e.width - n.width - t.horizontalOffset);
}
function lD(i, t) {
  return tk(i, t, (e) => e.left + t.horizontalOffset);
}
function tk(i, t, e) {
  return (n, r) => {
    const s = new je(i);
    if (s.width < nD || s.height < tD)
      return null;
    let o;
    t.position === "inside" ? o = s.bottom - r.height : o = s.bottom - r.height / 2, o -= t.verticalOffset;
    const a = e(s, r), c = n.clone().moveTo(a, o).getIntersection(r.clone().moveTo(a, o)).getVisible();
    return !c || c.getArea() < r.getArea() ? null : {
      top: o,
      left: a,
      name: `position_${t.position}-side_${t.side}`,
      config: {
        withArrow: !1
      }
    };
  };
}
function nk(i) {
  const t = i.config.get("ui.poweredBy"), e = t && t.position || "border";
  return {
    position: e,
    label: iD,
    verticalOffset: e === "inside" ? 5 : 0,
    horizontalOffset: 5,
    side: i.locale.contentLanguageDirection === "ltr" ? "right" : "left",
    ...t
  };
}
const Bv = {
  POLITE: "polite",
  ASSERTIVE: "assertive"
};
class cD {
  /**
  * @inheritDoc
  */
  constructor(t) {
    /**
    * The editor instance.
    */
    h(this, "editor");
    /**
    * The view that aggregates all `aria-live` regions.
    */
    h(this, "view");
    this.editor = t, t.once("ready", () => {
      for (const e of Object.values(Bv))
        this.announce("", e);
    });
  }
  /**
  * Sets an announcement text to an aria region that is then announced by a screen reader to the user.
  *
  * If the aria region of a specified politeness does not exist, it will be created and can be re-used later.
  *
  * The default announcement politeness level is `'polite'`.
  *
  * ```ts
  * // Most screen readers will queue announcements from multiple aria-live regions and read them out in the order they were emitted.
   * editor.ui.ariaLiveAnnouncer.announce( 'Image uploaded.' );
   * editor.ui.ariaLiveAnnouncer.announce( 'Connection lost. Reconnecting.' );
   * ```
  */
  announce(t, e = Bv.POLITE) {
    const n = this.editor;
    if (!n.ui.view)
      return;
    this.view || (this.view = new uD(n.locale), n.ui.view.body.add(this.view));
    const { politeness: r, isUnsafeHTML: s } = typeof e == "string" ? {
      politeness: e
    } : e;
    let o = this.view.regionViews.find((a) => a.politeness === r);
    o || (o = new dD(n, r), this.view.regionViews.add(o)), o.announce({
      announcement: t,
      isUnsafeHTML: s
    });
  }
}
class uD extends de {
  constructor(e) {
    super(e);
    /**
    * A collection of all views that represent individual `aria-live` regions.
    */
    h(this, "regionViews");
    this.regionViews = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-aria-live-announcer"
        ]
      },
      children: this.regionViews
    });
  }
}
class dD extends de {
  constructor(e, n) {
    super(e.locale);
    /**
    * Current politeness level of the region.
    */
    h(this, "politeness");
    /**
    * DOM converter used to sanitize unsafe HTML passed to {@link #announce} method.
    */
    h(this, "_domConverter");
    /**
    * Interval used to remove additions. It prevents accumulation of added nodes in region.
    */
    h(this, "_pruneAnnouncementsInterval");
    this.setTemplate({
      tag: "div",
      attributes: {
        "aria-live": n,
        "aria-relevant": "additions"
      },
      children: [
        {
          tag: "ul",
          attributes: {
            class: [
              "ck",
              "ck-aria-live-region-list"
            ]
          }
        }
      ]
    }), e.on("destroy", () => {
      this._pruneAnnouncementsInterval !== null && (clearInterval(this._pruneAnnouncementsInterval), this._pruneAnnouncementsInterval = null);
    }), this.politeness = n, this._domConverter = e.data.htmlProcessor.domConverter, this._pruneAnnouncementsInterval = setInterval(() => {
      this.element && this._listElement.firstChild && this._listElement.firstChild.remove();
    }, 5e3);
  }
  /**
  * Appends new announcement to region.
  */
  announce({ announcement: e, isUnsafeHTML: n }) {
    if (!e.trim().length)
      return;
    const r = document.createElement("li");
    n ? this._domConverter.setContentOf(r, e) : r.innerText = e, this._listElement.appendChild(r);
  }
  /**
  * Return current announcements list HTML element.
  */
  get _listElement() {
    return this.element.querySelector("ul");
  }
}
class Ks extends hs {
  /**
  * Creates an instance of the list item view.
  *
  * @param locale The localization services instance.
  */
  constructor(t, e) {
    super(t);
    const n = this.bindTemplate;
    this.extendTemplate({
      attributes: {
        class: [
          "ck-menu-bar__menu__item"
        ]
      },
      on: {
        mouseenter: n.to("mouseenter")
      }
    }), this.delegate("mouseenter").to(e);
  }
}
const Hc = 5, bo = {
  /**
  * When the bar is already open:
  * * Opens the menu when the user hovers over its button.
  * * Closes open menu when another menu's button gets hovered.
  */
  toggleMenusAndFocusItemsOnHover(i) {
    i.on("menu:mouseenter", (t) => {
      if (!(!i.isFocusBorderEnabled && !i.isOpen)) {
        if (i.isOpen)
          for (const e of i.menus) {
            const n = t.path[0], r = n instanceof Ks && n.children.first === e;
            e.isOpen = (t.path.includes(e) || r) && e.isEnabled;
          }
        t.source.focus();
      }
    });
  },
  /**
  * Moves between top-level menus using the arrow left and right keys.
  *
  * If the menubar has already been open, the arrow keys move focus between top-level menu buttons and open them.
  * If the menubar is closed, the arrow keys only move focus between top-level menu buttons.
  */
  focusCycleMenusOnArrows(i) {
    const t = i.locale.uiLanguageDirection === "rtl";
    i.on("menu:arrowright", (n) => {
      e(n.source, t ? -1 : 1);
    }), i.on("menu:arrowleft", (n) => {
      e(n.source, t ? 1 : -1);
    });
    function e(n, r) {
      const s = i.children.getIndex(n), o = n.isOpen, a = i.children.length, l = i.children.get((s + a + r) % a);
      n.isOpen = !1, o && (l.isOpen = !0), l.buttonView.focus();
    }
  },
  /**
  * Closes the entire sub-menu structure when the bar is closed. This prevents sub-menus from being open if the user
  * closes the entire bar, and then re-opens some top-level menu.
  */
  closeMenusWhenTheBarCloses(i) {
    i.on("change:isOpen", () => {
      i.isOpen || i.menus.forEach((t) => {
        t.isOpen = !1;
      });
    });
  },
  /**
  * Handles the following case:
  * 1. Hover to open a sub-menu (A). The button has focus.
  * 2. Press arrow up/down to move focus to another sub-menu (B) button.
  * 3. Press arrow right to open the sub-menu (B).
  * 4. The sub-menu (A) should close as it would with `toggleMenusAndFocusItemsOnHover()`.
  */
  closeMenuWhenAnotherOnTheSameLevelOpens(i) {
    i.on("menu:change:isOpen", (t, e, n) => {
      n && i.menus.filter((r) => t.source.parentMenuView === r.parentMenuView && t.source !== r && r.isOpen).forEach((r) => {
        r.isOpen = !1;
      });
    });
  },
  /**
  * Closes the bar when the user clicked outside of it (page body, editor root, etc.).
  */
  closeOnClickOutside(i) {
    _s({
      emitter: i,
      activator: () => i.isOpen,
      callback: () => i.close(),
      contextElements: () => i.children.map((t) => t.element)
    });
  },
  /**
  * Tracks the keyboard focus interaction on the menu bar view. It is used to determine if the nested items
  * of the menu bar should render focus rings after first interaction with the keyboard.
  */
  enableFocusHighlightOnInteraction(i) {
    let t = !1;
    i.on("change:isOpen", (e, n, r) => {
      r || (t || (i.isFocusBorderEnabled = !1), t = !1);
    }), i.listenTo(i.element, "keydown", () => {
      t = !0;
    }, {
      useCapture: !0
    }), i.listenTo(i.element, "keyup", () => {
      t = !1;
    }, {
      useCapture: !0
    }), i.listenTo(i.element, "focus", () => {
      t && (i.isFocusBorderEnabled = !0);
    }, {
      useCapture: !0
    });
  }
}, Kr = {
  /**
  * If the button of the menu is focused, pressing the arrow down key should open the panel and focus it.
  * This is analogous to the {@link module:ui/dropdown/dropdownview~DropdownView}.
  */
  openAndFocusPanelOnArrowDownKey(i) {
    i.keystrokes.set("arrowdown", (t, e) => {
      i.focusTracker.focusedElement === i.buttonView.element && (i.isOpen || (i.isOpen = !0), i.panelView.focus(), e());
    });
  },
  /**
  * Open the menu on the right arrow key press. This allows for navigating to sub-menus using the keyboard.
  */
  openOnArrowRightKey(i) {
    const t = i.locale.uiLanguageDirection === "rtl" ? "arrowleft" : "arrowright";
    i.keystrokes.set(t, (e, n) => {
      i.focusTracker.focusedElement !== i.buttonView.element || !i.isEnabled || (i.isOpen || (i.isOpen = !0), i.panelView.focus(), n());
    });
  },
  /**
  * Opens the menu on its button click. Note that this behavior only opens but never closes the menu (unlike
  * {@link module:ui/dropdown/dropdownview~DropdownView}).
  */
  openOnButtonClick(i) {
    i.buttonView.on("execute", () => {
      i.isOpen = !0;
    });
  },
  /**
  * Toggles the menu on its button click. This behavior is analogous to {@link module:ui/dropdown/dropdownview~DropdownView}.
  */
  toggleOnButtonClick(i) {
    i.buttonView.on("execute", () => {
      i.isOpen = !i.isOpen;
    });
  },
  /**
  * Opens the menu and focuses the panel content upon pressing the Enter key.
  */
  openAndFocusOnEnterKeyPress(i) {
    i.keystrokes.set("enter", (t, e) => {
      i.focusTracker.focusedElement === i.buttonView.element && (i.isOpen = !0, i.panelView.focus(), e());
    });
  },
  /**
  * Closes the menu on the right left key press. This allows for navigating to sub-menus using the keyboard.
  */
  closeOnArrowLeftKey(i) {
    const t = i.locale.uiLanguageDirection === "rtl" ? "arrowright" : "arrowleft";
    i.keystrokes.set(t, (e, n) => {
      i.isOpen && (i.isOpen = !1, i.focus(), n());
    });
  },
  /**
  * Closes the menu on the esc key press. This allows for navigating to sub-menus using the keyboard.
  */
  closeOnEscKey(i) {
    i.keystrokes.set("esc", (t, e) => {
      i.isOpen && (i.isOpen = !1, i.focus(), e());
    });
  },
  /**
  * Closes the menu when its parent menu also closed. This prevents from orphaned open menus when the parent menu re-opens.
  */
  closeOnParentClose(i) {
    i.parentMenuView.on("change:isOpen", (t, e, n) => {
      !n && t.source === i.parentMenuView && (i.isOpen = !1);
    });
  }
}, hD = {
  southEast: (i) => ({
    top: i.bottom,
    left: i.left,
    name: "se"
  }),
  southWest: (i, t) => ({
    top: i.bottom,
    left: i.left - t.width + i.width,
    name: "sw"
  }),
  northEast: (i, t) => ({
    top: i.top - t.height,
    left: i.left,
    name: "ne"
  }),
  northWest: (i, t) => ({
    top: i.top - t.height,
    left: i.left - t.width + i.width,
    name: "nw"
  }),
  eastSouth: (i) => ({
    top: i.top,
    left: i.right - Hc,
    name: "es"
  }),
  eastNorth: (i, t) => ({
    top: i.top - t.height,
    left: i.right - Hc,
    name: "en"
  }),
  westSouth: (i, t) => ({
    top: i.top,
    left: i.left - t.width + Hc,
    name: "ws"
  }),
  westNorth: (i, t) => ({
    top: i.top - t.height,
    left: i.left - t.width + Hc,
    name: "wn"
  })
}, fD = [
  {
    menuId: "file",
    label: "File",
    groups: [
      {
        groupId: "export",
        items: [
          "menuBar:exportPdf",
          "menuBar:exportWord"
        ]
      },
      {
        groupId: "import",
        items: [
          "menuBar:importWord"
        ]
      },
      {
        groupId: "revisionHistory",
        items: [
          "menuBar:revisionHistory"
        ]
      }
    ]
  },
  {
    menuId: "edit",
    label: "Edit",
    groups: [
      {
        groupId: "undo",
        items: [
          "menuBar:undo",
          "menuBar:redo"
        ]
      },
      {
        groupId: "selectAll",
        items: [
          "menuBar:selectAll"
        ]
      },
      {
        groupId: "findAndReplace",
        items: [
          "menuBar:findAndReplace"
        ]
      }
    ]
  },
  {
    menuId: "view",
    label: "View",
    groups: [
      {
        groupId: "sourceEditing",
        items: [
          "menuBar:sourceEditing"
        ]
      },
      {
        groupId: "showBlocks",
        items: [
          "menuBar:showBlocks"
        ]
      },
      {
        groupId: "previewMergeFields",
        items: [
          "menuBar:previewMergeFields"
        ]
      },
      {
        groupId: "restrictedEditing",
        items: [
          "menuBar:restrictedEditing"
        ]
      }
    ]
  },
  {
    menuId: "insert",
    label: "Insert",
    groups: [
      {
        groupId: "insertMainWidgets",
        items: [
          "menuBar:insertImage",
          "menuBar:ckbox",
          "menuBar:ckfinder",
          "menuBar:insertTable"
        ]
      },
      {
        groupId: "insertInline",
        items: [
          "menuBar:link",
          "menuBar:comment",
          "menuBar:insertMergeField"
        ]
      },
      {
        groupId: "insertMinorWidgets",
        items: [
          "menuBar:mediaEmbed",
          "menuBar:insertTemplate",
          "menuBar:specialCharacters",
          "menuBar:blockQuote",
          "menuBar:codeBlock",
          "menuBar:htmlEmbed"
        ]
      },
      {
        groupId: "insertStructureWidgets",
        items: [
          "menuBar:horizontalLine",
          "menuBar:pageBreak",
          "menuBar:tableOfContents"
        ]
      },
      {
        groupId: "restrictedEditingException",
        items: [
          "menuBar:restrictedEditingException"
        ]
      }
    ]
  },
  {
    menuId: "format",
    label: "Format",
    groups: [
      {
        groupId: "textAndFont",
        items: [
          {
            menuId: "text",
            label: "Text",
            groups: [
              {
                groupId: "basicStyles",
                items: [
                  "menuBar:bold",
                  "menuBar:italic",
                  "menuBar:underline",
                  "menuBar:strikethrough",
                  "menuBar:superscript",
                  "menuBar:subscript",
                  "menuBar:code"
                ]
              },
              {
                groupId: "textPartLanguage",
                items: [
                  "menuBar:textPartLanguage"
                ]
              }
            ]
          },
          {
            menuId: "font",
            label: "Font",
            groups: [
              {
                groupId: "fontProperties",
                items: [
                  "menuBar:fontSize",
                  "menuBar:fontFamily"
                ]
              },
              {
                groupId: "fontColors",
                items: [
                  "menuBar:fontColor",
                  "menuBar:fontBackgroundColor"
                ]
              },
              {
                groupId: "highlight",
                items: [
                  "menuBar:highlight"
                ]
              }
            ]
          },
          "menuBar:heading"
        ]
      },
      {
        groupId: "list",
        items: [
          "menuBar:bulletedList",
          "menuBar:numberedList",
          "menuBar:multiLevelList",
          "menuBar:todoList"
        ]
      },
      {
        groupId: "indent",
        items: [
          "menuBar:alignment",
          "menuBar:indent",
          "menuBar:outdent"
        ]
      },
      {
        groupId: "caseChange",
        items: [
          "menuBar:caseChange"
        ]
      },
      {
        groupId: "removeFormat",
        items: [
          "menuBar:removeFormat"
        ]
      }
    ]
  },
  {
    menuId: "tools",
    label: "Tools",
    groups: [
      {
        groupId: "aiTools",
        items: [
          "menuBar:aiAssistant",
          "menuBar:aiCommands"
        ]
      },
      {
        groupId: "tools",
        items: [
          "menuBar:trackChanges",
          "menuBar:commentsArchive"
        ]
      }
    ]
  },
  {
    menuId: "help",
    label: "Help",
    groups: [
      {
        groupId: "help",
        items: [
          "menuBar:accessibilityHelp"
        ]
      }
    ]
  }
];
function mD(i) {
  let t;
  return !("items" in i) || !i.items ? t = {
    items: Fi(fD),
    addItems: [],
    removeItems: [],
    isVisible: !0,
    isUsingDefaultConfig: !0,
    ...i
  } : t = {
    items: i.items,
    removeItems: [],
    addItems: [],
    isVisible: !0,
    isUsingDefaultConfig: !1,
    ...i
  }, t;
}
function gD({ normalizedConfig: i, locale: t, componentFactory: e, extraItems: n }) {
  const r = Fi(i);
  return Fv(i, r, n), pD(i, r), Fv(i, r, r.addItems), bD(i, r, e), ik(i, r), vD(r, t), r;
}
function pD(i, t) {
  const e = t.removeItems, n = [];
  t.items = t.items.filter(({ menuId: r }) => e.includes(r) ? (n.push(r), !1) : !0), ba(t.items, (r) => {
    r.groups = r.groups.filter(({ groupId: s }) => e.includes(s) ? (n.push(s), !1) : !0);
    for (const s of r.groups)
      s.items = s.items.filter((o) => {
        const a = rk(o);
        return e.includes(a) ? (n.push(a), !1) : !0;
      });
  });
  for (const r of e)
    n.includes(r) || Xe("menu-bar-item-could-not-be-removed", {
      menuBarConfig: i,
      itemName: r
    });
}
function Fv(i, t, e) {
  const n = [];
  if (e.length != 0) {
    for (const r of e) {
      const s = _D(r.position), o = CD(r.position);
      if (wD(r))
        if (!o)
          s === "start" ? (t.items.unshift(r.menu), n.push(r)) : s === "end" && (t.items.push(r.menu), n.push(r));
        else {
          const a = t.items.findIndex((l) => l.menuId === o);
          a != -1 ? s === "before" ? (t.items.splice(a, 0, r.menu), n.push(r)) : s === "after" && (t.items.splice(a + 1, 0, r.menu), n.push(r)) : $v(t, r.menu, o, s) && n.push(r);
        }
      else yD(r) ? ba(t.items, (a) => {
        if (a.menuId === o)
          s === "start" ? (a.groups.unshift(r.group), n.push(r)) : s === "end" && (a.groups.push(r.group), n.push(r));
        else {
          const l = a.groups.findIndex((c) => c.groupId === o);
          l !== -1 && (s === "before" ? (a.groups.splice(l, 0, r.group), n.push(r)) : s === "after" && (a.groups.splice(l + 1, 0, r.group), n.push(r)));
        }
      }) : $v(t, r.item, o, s) && n.push(r);
    }
    for (const r of e)
      n.includes(r) || Xe("menu-bar-item-could-not-be-added", {
        menuBarConfig: i,
        addedItemConfig: r
      });
  }
}
function $v(i, t, e, n) {
  let r = !1;
  return ba(i.items, (s) => {
    for (const { groupId: o, items: a } of s.groups) {
      if (r)
        return;
      if (o === e)
        n === "start" ? (a.unshift(t), r = !0) : n === "end" && (a.push(t), r = !0);
      else {
        const l = a.findIndex((c) => rk(c) === e);
        l !== -1 && (n === "before" ? (a.splice(l, 0, t), r = !0) : n === "after" && (a.splice(l + 1, 0, t), r = !0));
      }
    }
  }), r;
}
function bD(i, t, e) {
  ba(t.items, (n) => {
    for (const r of n.groups)
      r.items = r.items.filter((s) => {
        const o = typeof s == "string" && !e.has(s);
        return o && !t.isUsingDefaultConfig && Xe("menu-bar-item-unavailable", {
          menuBarConfig: i,
          parentMenuConfig: Fi(n),
          componentName: s
        }), !o;
      });
  });
}
function ik(i, t) {
  const e = t.isUsingDefaultConfig;
  let n = !1;
  if (t.items = t.items.filter((r) => r.groups.length ? !0 : (nf(i, r, e), !1)), !t.items.length) {
    nf(i, i, e);
    return;
  }
  ba(t.items, (r) => {
    r.groups = r.groups.filter((s) => s.items.length ? !0 : (n = !0, !1));
    for (const s of r.groups)
      s.items = s.items.filter((o) => sk(o) && !o.groups.length ? (nf(i, o, e), n = !0, !1) : !0);
  }), n && ik(i, t);
}
function nf(i, t, e) {
  e || Xe("menu-bar-menu-empty", {
    menuBarConfig: i,
    emptyMenuConfig: t
  });
}
function vD(i, t) {
  const e = t.t, n = {
    // Top-level categories.
    File: e({
      string: "File",
      id: "MENU_BAR_MENU_FILE"
    }),
    Edit: e({
      string: "Edit",
      id: "MENU_BAR_MENU_EDIT"
    }),
    View: e({
      string: "View",
      id: "MENU_BAR_MENU_VIEW"
    }),
    Insert: e({
      string: "Insert",
      id: "MENU_BAR_MENU_INSERT"
    }),
    Format: e({
      string: "Format",
      id: "MENU_BAR_MENU_FORMAT"
    }),
    Tools: e({
      string: "Tools",
      id: "MENU_BAR_MENU_TOOLS"
    }),
    Help: e({
      string: "Help",
      id: "MENU_BAR_MENU_HELP"
    }),
    // Sub-menus.
    Text: e({
      string: "Text",
      id: "MENU_BAR_MENU_TEXT"
    }),
    Font: e({
      string: "Font",
      id: "MENU_BAR_MENU_FONT"
    })
  };
  ba(i.items, (r) => {
    r.label in n && (r.label = n[r.label]);
  });
}
function ba(i, t) {
  if (Array.isArray(i))
    for (const n of i)
      e(n);
  function e(n) {
    t(n);
    for (const r of n.groups)
      for (const s of r.items)
        sk(s) && e(s);
  }
}
function wD(i) {
  return typeof i == "object" && "menu" in i;
}
function yD(i) {
  return typeof i == "object" && "group" in i;
}
function _D(i) {
  return i.startsWith("start") ? "start" : i.startsWith("end") ? "end" : i.startsWith("after") ? "after" : "before";
}
function CD(i) {
  const t = i.match(/^[^:]+:(.+)/);
  return t ? t[1] : null;
}
function rk(i) {
  return typeof i == "string" ? i : i.menuId;
}
function sk(i) {
  return typeof i == "object" && "menuId" in i;
}
class kD extends (/* @__PURE__ */ st()) {
  /**
  * Creates an instance of the editor UI class.
  *
  * @param editor The editor instance.
  */
  constructor(e) {
    super();
    /**
    * The editor that the UI belongs to.
    */
    h(this, "editor");
    /**
    * An instance of the {@link module:ui/componentfactory~ComponentFactory}, a registry used by plugins
    * to register factories of specific UI components.
    */
    h(this, "componentFactory");
    /**
    * Stores the information about the editor UI focus and propagates it so various plugins and components
    * are unified as a focus group.
    */
    h(this, "focusTracker");
    /**
    * Manages the tooltips displayed on mouseover and focus across the UI.
    */
    h(this, "tooltipManager");
    /**
    * A helper that enables the "powered by" feature in the editor and renders a link to the project's webpage.
    */
    h(this, "poweredBy");
    /**
    * A helper that manages the content of an `aria-live` regions used by editor features to announce status changes
    * to screen readers.
    */
    h(this, "ariaLiveAnnouncer");
    /**
    * Indicates the UI is ready. Set `true` after {@link #event:ready} event is fired.
    *
    * @readonly
    * @default false
    */
    h(this, "isReady", !1);
    /**
    * Stores all editable elements used by the editor instance.
    */
    h(this, "_editableElementsMap", /* @__PURE__ */ new Map());
    /**
    * All available & focusable toolbars.
    */
    h(this, "_focusableToolbarDefinitions", []);
    /**
    * All additional menu bar items, groups or menus that have their default location defined.
    */
    h(this, "_extraMenuBarElements", []);
    /**
    * The last focused element to which focus should return on `Esc` press.
    */
    h(this, "_lastFocusedForeignElement", null);
    const n = e.editing.view;
    this.editor = e, this.componentFactory = new YM(e), this.focusTracker = new wt(), this.tooltipManager = new vm(e), this.poweredBy = new rD(e), this.ariaLiveAnnouncer = new cD(e), this.set("viewportOffset", this._readViewportOffsetFromConfig()), this.once("ready", () => {
      this._bindBodyCollectionWithFocusTracker(), this.isReady = !0;
    }), this.listenTo(n.document, "layoutChanged", this.update.bind(this)), this.listenTo(n, "scrollToTheSelection", this._handleScrollToTheSelection.bind(this)), this._initFocusTracking();
  }
  /**
  * The main (outermost) DOM element of the editor UI.
  *
  * For example, in {@link module:editor-classic/classiceditor~ClassicEditor} it is a `<div>` which
  * wraps the editable element and the toolbar. In {@link module:editor-inline/inlineeditor~InlineEditor}
  * it is the editable element itself (as there is no other wrapper). However, in
  * {@link module:editor-decoupled/decouplededitor~DecoupledEditor} it is set to `null` because this editor does not
  * come with a single "main" HTML element (its editable element and toolbar are separate).
  *
  * This property can be understood as a shorthand for retrieving the element that a specific editor integration
  * considers to be its main DOM element.
  */
  get element() {
    return null;
  }
  /**
  * Fires the {@link module:ui/editorui/editorui~EditorUI#event:update `update`} event.
  *
  * This method should be called when the editor UI (e.g. positions of its balloons) needs to be updated due to
  * some environmental change which CKEditor 5 is not aware of (e.g. resize of a container in which it is used).
  */
  update() {
    this.fire("update");
  }
  /**
  * Destroys the UI.
  */
  destroy() {
    this.stopListening(), this.focusTracker.destroy(), this.tooltipManager.destroy(this.editor), this.poweredBy.destroy();
    for (const e of this._editableElementsMap.values())
      e.ckeditorInstance = null, this.editor.keystrokes.stopListening(e);
    this._editableElementsMap = /* @__PURE__ */ new Map(), this._focusableToolbarDefinitions = [];
  }
  /**
  * Stores the native DOM editable element used by the editor under a unique name.
  *
  * Also, registers the element in the editor to maintain the accessibility of the UI. When the user is editing text in a focusable
  * editable area, they can use the <kbd>Alt</kbd> + <kbd>F10</kbd> keystroke to navigate over editor toolbars. See {@link #addToolbar}.
  *
  * @param rootName The unique name of the editable element.
  * @param domElement The native DOM editable element.
  */
  setEditableElement(e, n) {
    this._editableElementsMap.set(e, n), n.ckeditorInstance || (n.ckeditorInstance = this.editor), this.focusTracker.add(n);
    const r = () => {
      this.editor.editing.view.getDomRoot(e) || this.editor.keystrokes.listenTo(n);
    };
    this.isReady ? r() : this.once("ready", r);
  }
  /**
  * Removes the editable from the editor UI. Removes all handlers added by {@link #setEditableElement}.
  *
  * @param rootName The name of the editable element to remove.
  */
  removeEditableElement(e) {
    const n = this._editableElementsMap.get(e);
    n && (this._editableElementsMap.delete(e), this.editor.keystrokes.stopListening(n), this.focusTracker.remove(n), n.ckeditorInstance = null);
  }
  /**
  * Returns the editable editor element with the given name or null if editable does not exist.
  *
  * @param rootName The editable name.
  */
  getEditableElement(e = "main") {
    return this._editableElementsMap.get(e);
  }
  /**
  * Returns array of names of all editor editable elements.
  */
  getEditableElementsNames() {
    return this._editableElementsMap.keys();
  }
  /**
  * Adds a toolbar to the editor UI. Used primarily to maintain the accessibility of the UI.
  *
  * Focusable toolbars can be accessed (focused) by users by pressing the <kbd>Alt</kbd> + <kbd>F10</kbd> keystroke.
  * Successive keystroke presses navigate over available toolbars.
  *
  * @param toolbarView A instance of the toolbar to be registered.
  */
  addToolbar(e, n = {}) {
    e.isRendered ? (this.focusTracker.add(e), this.editor.keystrokes.listenTo(e.element)) : e.once("render", () => {
      this.focusTracker.add(e), this.editor.keystrokes.listenTo(e.element);
    }), this._focusableToolbarDefinitions.push({
      toolbarView: e,
      options: n
    });
  }
  /**
  * Registers an extra menu bar element, which could be a single item, a group of items, or a menu containing groups.
  *
  * ```ts
  * // Register a new menu bar item.
  * editor.ui.extendMenuBar( {
  *   item: 'menuBar:customFunctionButton',
  *   position: 'after:menuBar:bold'
  * } );
  *
  * // Register a new menu bar group.
  * editor.ui.extendMenuBar( {
  *   group: {
  *     groupId: 'customGroup',
  *     items: [
  *       'menuBar:customFunctionButton'
  *     ]
  *   },
  *   position: 'start:help'
  * } );
  *
  * // Register a new menu bar menu.
  * editor.ui.extendMenuBar( {
  *   menu: {
  *     menuId: 'customMenu',
  *     label: 'customMenu',
  *     groups: [
  *       {
  *         groupId: 'customGroup',
  *         items: [
  *           'menuBar:customFunctionButton'
  *         ]
  *       }
  *     ]
  *   },
  *   position: 'after:help'
  * } );
  * ```
  */
  extendMenuBar(e) {
    this._extraMenuBarElements.push(e);
  }
  /**
  * Stores all editable elements used by the editor instance.
  *
  * @deprecated
  */
  get _editableElements() {
    return console.warn("editor-ui-deprecated-editable-elements: The EditorUI#_editableElements property has been deprecated and will be removed in the near future.", {
      editorUI: this
    }), this._editableElementsMap;
  }
  /**
  * Initializes menu bar.
  */
  _initMenuBar(e) {
    const n = e.element;
    this.focusTracker.add(n), this.editor.keystrokes.listenTo(n);
    const r = mD(this.editor.config.get("menuBar") || {});
    e.fillFromConfig(r, this.componentFactory, this._extraMenuBarElements), this.editor.keystrokes.set("Esc", (s, o) => {
      n.contains(this.editor.ui.focusTracker.focusedElement) && (this._lastFocusedForeignElement ? (this._lastFocusedForeignElement.focus(), this._lastFocusedForeignElement = null) : this.editor.editing.view.focus(), o());
    }), this.editor.keystrokes.set("Alt+F9", (s, o) => {
      n.contains(this.editor.ui.focusTracker.focusedElement) || (this._saveLastFocusedForeignElement(), e.isFocusBorderEnabled = !0, e.focus(), o());
    });
  }
  /**
  * Returns viewport offsets object:
  *
  * ```js
  * {
  * 	top: Number,
  * 	right: Number,
  * 	bottom: Number,
  * 	left: Number
  * }
  * ```
  *
  * Only top property is currently supported.
  */
  _readViewportOffsetFromConfig() {
    const e = this.editor, n = e.config.get("ui.viewportOffset");
    if (n)
      return n;
    const r = e.config.get("toolbar.viewportTopOffset");
    return r ? (console.warn("editor-ui-deprecated-viewport-offset-config: The `toolbar.vieportTopOffset` configuration option is deprecated. It will be removed from future CKEditor versions. Use `ui.viewportOffset.top` instead."), {
      top: r
    }) : {
      top: 0
    };
  }
  /**
  * Starts listening for <kbd>Alt</kbd> + <kbd>F10</kbd> and <kbd>Esc</kbd> keystrokes in the context of focusable
  * {@link #setEditableElement editable elements} and {@link #addToolbar toolbars}
  * to allow users navigate across the UI.
  */
  _initFocusTracking() {
    const e = this.editor;
    let n;
    e.keystrokes.set("Alt+F10", (r, s) => {
      this._saveLastFocusedForeignElement();
      const o = this._getCurrentFocusedToolbarDefinition();
      (!o || !n) && (n = this._getFocusableCandidateToolbarDefinitions());
      for (let a = 0; a < n.length; a++) {
        const l = n.shift();
        if (n.push(l), l !== o && this._focusFocusableCandidateToolbar(l)) {
          o && o.options.afterBlur && o.options.afterBlur();
          break;
        }
      }
      s();
    }), e.keystrokes.set("Esc", (r, s) => {
      const o = this._getCurrentFocusedToolbarDefinition();
      o && (this._lastFocusedForeignElement ? (this._lastFocusedForeignElement.focus(), this._lastFocusedForeignElement = null) : e.editing.view.focus(), o.options.afterBlur && o.options.afterBlur(), s());
    });
  }
  /**
  * Saves last focused element that doen not belong to editing view to restore focus on `Esc`.
  */
  _saveLastFocusedForeignElement() {
    const e = this.focusTracker.focusedElement;
    Array.from(this._editableElementsMap.values()).includes(e) && !Array.from(this.editor.editing.view.domRoots.values()).includes(e) && (this._lastFocusedForeignElement = e);
  }
  /**
  * Returns definitions of toolbars that could potentially be focused, sorted by their importance for the user.
  *
  * Focusable toolbars candidates are either:
  * * already visible,
  * * have `beforeFocus()` set in their {@link module:ui/editorui/editorui~FocusableToolbarDefinition definition} that suggests that
  * they might show up when called. Keep in mind that determining whether a toolbar will show up (and become focusable) is impossible
  * at this stage because it depends on its implementation, that in turn depends on the editing context (selection).
  *
  * **Note**: Contextual toolbars take precedence over regular toolbars.
  */
  _getFocusableCandidateToolbarDefinitions() {
    const e = [];
    for (const n of this._focusableToolbarDefinitions) {
      const { toolbarView: r, options: s } = n;
      (mr(r.element) || s.beforeFocus) && e.push(n);
    }
    return e.sort((n, r) => zv(n) - zv(r)), e;
  }
  /**
  * Returns a definition of the toolbar that is currently visible and focused (one of its children has focus).
  *
  * `null` is returned when no toolbar is currently focused.
  */
  _getCurrentFocusedToolbarDefinition() {
    for (const e of this._focusableToolbarDefinitions)
      if (e.toolbarView.element && e.toolbarView.element.contains(this.focusTracker.focusedElement))
        return e;
    return null;
  }
  /**
  * Focuses a focusable toolbar candidate using its definition.
  *
  * @param candidateToolbarDefinition A definition of the toolbar to focus.
  * @returns `true` when the toolbar candidate was focused. `false` otherwise.
  */
  _focusFocusableCandidateToolbar(e) {
    const { toolbarView: n, options: { beforeFocus: r } } = e;
    return r && r(), mr(n.element) ? (n.focus(), !0) : !1;
  }
  /**
  * Provides an integration between {@link #viewportOffset} and {@link module:utils/dom/scroll~scrollViewportToShowTarget}.
  * It allows the UI-agnostic engine method to consider user-configured viewport offsets specific for the integration.
  *
  * @param evt The `scrollToTheSelection` event info.
  * @param data The payload carried by the `scrollToTheSelection` event.
  */
  _handleScrollToTheSelection(e, n) {
    const r = {
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      ...this.viewportOffset
    };
    n.viewportOffset.top += r.top, n.viewportOffset.bottom += r.bottom, n.viewportOffset.left += r.left, n.viewportOffset.right += r.right;
  }
  /**
  * Ensures that the focus tracker is aware of all views' DOM elements in the body collection.
  */
  _bindBodyCollectionWithFocusTracker() {
    const e = this.view.body;
    for (const n of e)
      this.focusTracker.add(n.element);
    e.on("add", (n, r) => {
      this.focusTracker.add(r.element);
    }), e.on("remove", (n, r) => {
      this.focusTracker.remove(r.element);
    });
  }
}
function zv(i) {
  const { toolbarView: t, options: e } = i;
  let n = 10;
  return mr(t.element) && n--, e.isContextual && n--, n;
}
class xD extends de {
  /**
  * Creates an instance of the editor UI view class.
  *
  * @param locale The locale instance.
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views, detached from the DOM
    * structure of the editor, like panels, icons etc.
    */
    h(this, "body");
    /**
    * Menu bar view instance. May not be initialized in some editor types.
    */
    h(this, "menuBarView");
    this.body = new gM(e);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.body.attachToDom();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this.body.detachFromDom(), super.destroy();
  }
}
class ED extends xD {
  /**
  * Creates an instance of the boxed editor UI view class.
  *
  * @param locale The locale instance..
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views located in the top (`.ck-editor__top`)
    * area of the UI.
    */
    h(this, "top");
    /**
    * Collection of the child views located in the main (`.ck-editor__main`)
    * area of the UI.
    */
    h(this, "main");
    /**
    * Voice label of the UI.
    */
    h(this, "_voiceLabelView");
    this.top = this.createCollection(), this.main = this.createCollection(), this._voiceLabelView = this._createVoiceLabel(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset",
          "ck-editor",
          "ck-rounded-corners"
        ],
        role: "application",
        dir: e.uiLanguageDirection,
        lang: e.uiLanguage,
        "aria-labelledby": this._voiceLabelView.id
      },
      children: [
        this._voiceLabelView,
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-editor__top",
              "ck-reset_all"
            ],
            role: "presentation"
          },
          children: this.top
        },
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-editor__main"
            ],
            role: "presentation"
          },
          children: this.main
        }
      ]
    });
  }
  /**
  * Creates a voice label view instance.
  */
  _createVoiceLabel() {
    const e = this.t, n = new Oi();
    return n.text = e("Rich Text Editor"), n.extendTemplate({
      attributes: {
        class: "ck-voice-label"
      }
    }), n;
  }
}
class TD extends de {
  /**
  * Creates an instance of EditableUIView class.
  *
  * @param locale The locale instance.
  * @param editingView The editing view instance the editable is related to.
  * @param editableElement The editable element. If not specified, this view
  * should create it. Otherwise, the existing element should be used.
  */
  constructor(e, n, r) {
    super(e);
    /**
    * The name of the editable UI view.
    */
    h(this, "name", null);
    /**
    * The editing view instance the editable is related to. Editable uses the editing
    * view to dynamically modify its certain DOM attributes after {@link #render rendering}.
    *
    * **Note**: The DOM attributes are performed by the editing view and not UI
    * {@link module:ui/view~View#bindTemplate template bindings} because once rendered,
    * the editable DOM element must remain under the full control of the engine to work properly.
    */
    h(this, "_editingView");
    /**
    * The element which is the main editable element (usually the one with `contentEditable="true"`).
    */
    h(this, "_editableElement");
    /**
    * Whether an external {@link #_editableElement} was passed into the constructor, which also means
    * the view will not render its {@link #template}.
    */
    h(this, "_hasExternalElement");
    this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-content",
          "ck-editor__editable",
          "ck-rounded-corners"
        ],
        lang: e.contentLanguage,
        dir: e.contentLanguageDirection
      }
    }), this.set("isFocused", !1), this._editableElement = r, this._hasExternalElement = !!this._editableElement, this._editingView = n;
  }
  /**
  * Renders the view by either applying the {@link #template} to the existing
  * {@link module:ui/editableui/editableuiview~EditableUIView#_editableElement} or assigning {@link #element}
  * as {@link module:ui/editableui/editableuiview~EditableUIView#_editableElement}.
  */
  render() {
    super.render(), this._hasExternalElement ? this.template.apply(this.element = this._editableElement) : this._editableElement = this.element, this.on("change:isFocused", () => this._updateIsFocusedClasses()), this._updateIsFocusedClasses();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._hasExternalElement && this.template.revert(this._editableElement), super.destroy();
  }
  /**
  * Whether an external {@link #_editableElement} was passed into the constructor, which also means
  * the view will not render its {@link #template}.
  */
  get hasExternalElement() {
    return this._hasExternalElement;
  }
  /**
  * Updates the `ck-focused` and `ck-blurred` CSS classes on the {@link #element} according to
  * the {@link #isFocused} property value using the {@link #_editingView editing view} API.
  */
  _updateIsFocusedClasses() {
    const e = this._editingView;
    e.isRenderingInProgress ? r(this) : n(this);
    function n(s) {
      e.change((o) => {
        const a = e.document.getRoot(s.name);
        o.addClass(s.isFocused ? "ck-focused" : "ck-blurred", a), o.removeClass(s.isFocused ? "ck-blurred" : "ck-focused", a);
      });
    }
    function r(s) {
      e.once("change:isRenderingInProgress", (o, a, l) => {
        l ? r(s) : n(s);
      });
    }
  }
}
class SD extends TD {
  /**
  * Creates an instance of the InlineEditableUIView class.
  *
  * @param locale The locale instance.
  * @param editingView The editing view instance the editable is related to.
  * @param editableElement The editable element. If not specified, the
  * {@link module:ui/editableui/editableuiview~EditableUIView}
  * will create it. Otherwise, the existing element will be used.
  * @param options Additional configuration of the view.
  * @param options.label The label of the editable for assistive technologies. If not provided, a default label is used or,
  * the existing `aria-label` attribute value from the specified `editableElement` is preserved.
  */
  constructor(e, n, r, s = {}) {
    super(e, n, r);
    /**
    * The cached options object passed to the constructor.
    */
    h(this, "_options");
    this._options = s, this.extendTemplate({
      attributes: {
        role: "textbox",
        class: "ck-editor__editable_inline"
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    const e = this._editingView;
    e.change((n) => {
      const r = e.document.getRoot(this.name);
      n.setAttribute("aria-label", this.getEditableAriaLabel(), r);
    });
  }
  /**
  * Returns a normalized label for the editable view based on the environment.
  */
  getEditableAriaLabel() {
    const e = this.locale.t, n = this._options.label, r = this._editableElement, s = this.name;
    if (typeof n == "string")
      return n;
    if (typeof n == "object")
      return n[s];
    if (typeof n == "function")
      return n(this);
    if (r) {
      const o = r.getAttribute("aria-label");
      if (o)
        return o;
    }
    return e("Rich Text Editor. Editing area: %0", s);
  }
}
class Hv extends jd {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Notification";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.on("show:warning", (t, e) => {
      window.alert(e.message);
    }, {
      priority: "lowest"
    });
  }
  /**
  * Shows a success notification.
  *
  * By default, it fires the {@link #event:show:success `show:success` event} with the given `data`. The event namespace can be extended
  * using the `data.namespace` option. For example:
  *
  * ```ts
  * showSuccess( 'Image is uploaded.', {
  * 	namespace: 'upload:image'
  * } );
  * ```
  *
  * will fire the `show:success:upload:image` event.
  *
  * You can provide the title of the notification:
  *
  * ```ts
  * showSuccess( 'Image is uploaded.', {
  * 	title: 'Image upload success'
  * } );
  * ```
  *
  * @param message The content of the notification.
  * @param data Additional data.
  * @param data.namespace Additional event namespace.
  * @param data.title The title of the notification.
  */
  showSuccess(t, e = {}) {
    this._showNotification({
      message: t,
      type: "success",
      namespace: e.namespace,
      title: e.title
    });
  }
  /**
  * Shows an information notification.
  *
  * By default, it fires the {@link #event:show:info `show:info` event} with the given `data`. The event namespace can be extended
  * using the `data.namespace` option. For example:
  *
  * ```ts
  * showInfo( 'Editor is offline.', {
  * 	namespace: 'editor:status'
  * } );
  * ```
  *
  * will fire the `show:info:editor:status` event.
  *
  * You can provide the title of the notification:
  *
  * ```ts
  * showInfo( 'Editor is offline.', {
  * 	title: 'Network information'
  * } );
  * ```
  *
  * @param message The content of the notification.
  * @param data Additional data.
  * @param data.namespace Additional event namespace.
  * @param data.title The title of the notification.
  */
  showInfo(t, e = {}) {
    this._showNotification({
      message: t,
      type: "info",
      namespace: e.namespace,
      title: e.title
    });
  }
  /**
  * Shows a warning notification.
  *
  * By default, it fires the {@link #event:show:warning `show:warning` event}
  * with the given `data`. The event namespace can be extended using the `data.namespace` option. For example:
  *
  * ```ts
  * showWarning( 'Image upload error.', {
  * 	namespace: 'upload:image'
  * } );
  * ```
  *
  * will fire the `show:warning:upload:image` event.
  *
  * You can provide the title of the notification:
  *
  * ```ts
  * showWarning( 'Image upload error.', {
  * 	title: 'Upload failed'
  * } );
  * ```
  *
  * Note that each unhandled and not stopped `warning` notification will be displayed as a system alert.
  * The plugin responsible for displaying warnings should `stop()` the event to prevent displaying it as an alert:
  *
  * ```ts
  * notifications.on( 'show:warning', ( evt, data ) => {
  * 	// Do something with the data.
  *
  * 	// Stop this event to prevent displaying it as an alert.
  * 	evt.stop();
  * } );
  * ```
  *
  * You can attach many listeners to the same event and `stop()` this event in a listener with a low priority:
  *
  * ```ts
  * notifications.on( 'show:warning', ( evt, data ) => {
  * 	// Show the warning in the UI, but do not stop it.
  * } );
  *
  * notifications.on( 'show:warning', ( evt, data ) => {
  * 	// Log the warning to some error tracker.
  *
  * 	// Stop this event to prevent displaying it as an alert.
  * 	evt.stop();
  * }, { priority: 'low' } );
  * ```
  *
  * @param message The content of the notification.
  * @param data Additional data.
  * @param data.namespace Additional event namespace.
  * @param data.title The title of the notification.
  */
  showWarning(t, e = {}) {
    this._showNotification({
      message: t,
      type: "warning",
      namespace: e.namespace,
      title: e.title
    });
  }
  /**
  * Fires the `show` event with the specified type, namespace and message.
  *
  * @param data The message data.
  * @param data.message The content of the notification.
  * @param data.type The type of the message.
  * @param data.namespace Additional event namespace.
  * @param data.title The title of the notification.
  */
  _showNotification(t) {
    const e = t.namespace ? `show:${t.type}:${t.namespace}` : `show:${t.type}`;
    this.fire(e, {
      message: t.message,
      type: t.type,
      title: t.title || ""
    });
  }
}
class ia extends (/* @__PURE__ */ st()) {
  /**
  * Creates a new Model instance.
  *
  * @param attributes The model state attributes to be defined during the instance creation.
  * @param properties The (out of state) properties to be appended to the instance during creation.
  */
  constructor(t, e) {
    super(), e && p_(this, e), t && this.set(t);
  }
}
const jc = /* @__PURE__ */ vs("px");
class Qi extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The {@link module:utils/dom/position~Options#limiter position limiter}
    * for the {@link #view balloon}, used when no `limiter` has been passed into {@link #add}
    * or {@link #updatePosition}.
    *
    * By default, a function that obtains the farthest DOM
    * {@link module:engine/view/rooteditableelement~RootEditableElement}
    * of the {@link module:engine/view/document~Document#selection}.
    */
    h(this, "positionLimiter");
    h(this, "visibleStack");
    /**
    * The map of views and their stacks.
    */
    h(this, "_viewToStack", /* @__PURE__ */ new Map());
    /**
    * The map of IDs and stacks.
    */
    h(this, "_idToStack", /* @__PURE__ */ new Map());
    /**
    * The common balloon panel view.
    */
    h(this, "_view", null);
    /**
    * Rotator view embedded in the contextual balloon.
    * Displays the currently visible view in the balloon and provides navigation for switching stacks.
    */
    h(this, "_rotatorView", null);
    /**
    * Displays fake panels under the balloon panel view when multiple stacks are added to the balloon.
    */
    h(this, "_fakePanelsView", null);
    this.positionLimiter = () => {
      const n = this.editor.editing.view, s = n.document.selection.editableElement;
      return s ? n.domConverter.mapViewToDom(s.root) : null;
    }, this.decorate("getPositionOptions"), this.set("visibleView", null), this.set("_numberOfStacks", 0), this.set("_singleViewMode", !1);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ContextualBalloon";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._view && this._view.destroy(), this._rotatorView && this._rotatorView.destroy(), this._fakePanelsView && this._fakePanelsView.destroy();
  }
  /**
  * The common balloon panel view.
  */
  get view() {
    return this._view || this._createPanelView(), this._view;
  }
  /**
  * Returns `true` when the given view is in one of the stacks. Otherwise returns `false`.
  */
  hasView(e) {
    return Array.from(this._viewToStack.keys()).includes(e);
  }
  /**
  * Adds a new view to the stack and makes it visible if the current stack is visible
  * or it is the first view in the balloon.
  *
  * @param data The configuration of the view.
  * @param data.stackId The ID of the stack that the view is added to. Defaults to `'main'`.
  * @param data.view The content of the balloon.
  * @param data.position Positioning options.
  * @param data.balloonClassName An additional CSS class added to the {@link #view balloon} when visible.
  * @param data.withArrow Whether the {@link #view balloon} should be rendered with an arrow. Defaults to `true`.
  * @param data.singleViewMode Whether the view should be the only visible view even if other stacks were added. Defaults to `false`.
  */
  add(e) {
    if (this._view || this._createPanelView(), this.hasView(e.view))
      throw new T("contextualballoon-add-view-exist", [
        this,
        e
      ]);
    const n = e.stackId || "main";
    if (!this._idToStack.has(n)) {
      this._idToStack.set(n, /* @__PURE__ */ new Map([
        [
          e.view,
          e
        ]
      ])), this._viewToStack.set(e.view, this._idToStack.get(n)), this._numberOfStacks = this._idToStack.size, (!this._visibleStack || e.singleViewMode) && this.showStack(n);
      return;
    }
    const r = this._idToStack.get(n);
    e.singleViewMode && this.showStack(n), r.set(e.view, e), this._viewToStack.set(e.view, r), r === this._visibleStack && this._showView(e);
  }
  /**
  * Removes the given view from the stack. If the removed view was visible,
  * the view preceding it in the stack will become visible instead.
  * When there is no view in the stack, the next stack will be displayed.
  * When there are no more stacks, the balloon will hide.
  *
  * @param view A view to be removed from the balloon.
  */
  remove(e) {
    if (!this.hasView(e))
      throw new T("contextualballoon-remove-view-not-exist", [
        this,
        e
      ]);
    const n = this._viewToStack.get(e);
    this._singleViewMode && this.visibleView === e && (this._singleViewMode = !1), this.visibleView === e && (n.size === 1 ? this._idToStack.size > 1 ? this._showNextStack() : (this.view.hide(), this.visibleView = null, this._rotatorView.hideView()) : this._showView(Array.from(n.values())[n.size - 2])), n.size === 1 ? (this._idToStack.delete(this._getStackId(n)), this._numberOfStacks = this._idToStack.size) : n.delete(e), this._viewToStack.delete(e);
  }
  /**
  * Updates the position of the balloon using the position data of the first visible view in the stack.
  * When new position data is given, the position data of the currently visible view will be updated.
  *
  * @param position Position options.
  */
  updatePosition(e) {
    e && (this._visibleStack.get(this.visibleView).position = e), this.view.pin(this.getPositionOptions()), this._fakePanelsView.updatePosition();
  }
  /**
  * Returns position options of the last view in the stack.
  * This keeps the balloon in the same position when the view is changed.
  */
  getPositionOptions() {
    let e = Array.from(this._visibleStack.values()).pop().position;
    return e && (e.limiter || (e = Object.assign({}, e, {
      limiter: this.positionLimiter
    })), e = Object.assign({}, e, {
      viewportOffsetConfig: this.editor.ui.viewportOffset
    })), e;
  }
  /**
  * Shows the last view from the stack of a given ID.
  */
  showStack(e) {
    this.visibleStack = e;
    const n = this._idToStack.get(e);
    if (!n)
      throw new T("contextualballoon-showstack-stack-not-exist", this);
    this._visibleStack !== n && this._showView(Array.from(n.values()).pop());
  }
  /**
  * Initializes view instances.
  */
  _createPanelView() {
    this._view = new fn(this.editor.locale), this.editor.ui.view.body.add(this._view), this._rotatorView = this._createRotatorView(), this._fakePanelsView = this._createFakePanelsView();
  }
  /**
  * Returns the stack of the currently visible view.
  */
  get _visibleStack() {
    return this._viewToStack.get(this.visibleView);
  }
  /**
  * Returns the ID of the given stack.
  */
  _getStackId(e) {
    return Array.from(this._idToStack.entries()).find((r) => r[1] === e)[0];
  }
  /**
  * Shows the last view from the next stack.
  */
  _showNextStack() {
    const e = Array.from(this._idToStack.values());
    let n = e.indexOf(this._visibleStack) + 1;
    e[n] || (n = 0), this.showStack(this._getStackId(e[n]));
  }
  /**
  * Shows the last view from the previous stack.
  */
  _showPrevStack() {
    const e = Array.from(this._idToStack.values());
    let n = e.indexOf(this._visibleStack) - 1;
    e[n] || (n = e.length - 1), this.showStack(this._getStackId(e[n]));
  }
  /**
  * Creates a rotator view.
  */
  _createRotatorView() {
    const e = new AD(this.editor.locale), n = this.editor.locale.t;
    return this.view.content.add(e), e.bind("isNavigationVisible").to(this, "_numberOfStacks", this, "_singleViewMode", (r, s) => !s && r > 1), e.on("change:isNavigationVisible", () => this.updatePosition(), {
      priority: "low"
    }), e.bind("counter").to(this, "visibleView", this, "_numberOfStacks", (r, s) => {
      if (s < 2)
        return "";
      const o = Array.from(this._idToStack.values()).indexOf(this._visibleStack) + 1;
      return n("%0 of %1", [
        o,
        s
      ]);
    }), e.buttonNextView.on("execute", () => {
      e.focusTracker.isFocused && this.editor.editing.view.focus(), this._showNextStack();
    }), e.buttonPrevView.on("execute", () => {
      e.focusTracker.isFocused && this.editor.editing.view.focus(), this._showPrevStack();
    }), e;
  }
  /**
  * Creates a fake panels view.
  */
  _createFakePanelsView() {
    const e = new PD(this.editor.locale, this.view);
    return e.bind("numberOfPanels").to(this, "_numberOfStacks", this, "_singleViewMode", (n, r) => !r && n >= 2 ? Math.min(n - 1, 2) : 0), e.listenTo(this.view, "change:top", () => e.updatePosition()), e.listenTo(this.view, "change:left", () => e.updatePosition()), this.editor.ui.view.body.add(e), e;
  }
  /**
  * Sets the view as the content of the balloon and attaches the balloon using position
  * options of the first view.
  *
  * @param data Configuration.
  * @param data.view The view to show in the balloon.
  * @param data.balloonClassName Additional class name which will be added to the {@link #view balloon}.
  * @param data.withArrow Whether the {@link #view balloon} should be rendered with an arrow.
  */
  _showView({ view: e, balloonClassName: n = "", withArrow: r = !0, singleViewMode: s = !1 }) {
    this.view.class = n, this.view.withArrow = r, this._rotatorView.showView(e), this.visibleView = e, this.view.pin(this.getPositionOptions()), this._fakePanelsView.updatePosition(), s && (this._singleViewMode = !0);
  }
}
class AD extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Used for checking if a view is focused or not.
    */
    h(this, "focusTracker");
    /**
    * Navigation button for switching the stack to the previous one.
    */
    h(this, "buttonPrevView");
    /**
    * Navigation button for switching the stack to the next one.
    */
    h(this, "buttonNextView");
    /**
    * A collection of the child views that creates the rotator content.
    */
    h(this, "content");
    const n = e.t, r = this.bindTemplate;
    this.set("isNavigationVisible", !0), this.focusTracker = new wt(), this.buttonPrevView = this._createButtonView(n("Previous"), ee.previousArrow), this.buttonNextView = this._createButtonView(n("Next"), ee.nextArrow), this.content = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-balloon-rotator"
        ],
        "z-index": "-1"
      },
      children: [
        {
          tag: "div",
          attributes: {
            class: [
              "ck-balloon-rotator__navigation",
              r.to("isNavigationVisible", (s) => s ? "" : "ck-hidden")
            ]
          },
          children: [
            this.buttonPrevView,
            {
              tag: "span",
              attributes: {
                class: [
                  "ck-balloon-rotator__counter"
                ]
              },
              children: [
                {
                  text: r.to("counter")
                }
              ]
            },
            this.buttonNextView
          ]
        },
        {
          tag: "div",
          attributes: {
            class: "ck-balloon-rotator__content"
          },
          children: this.content
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy();
  }
  /**
  * Shows a given view.
  *
  * @param view The view to show.
  */
  showView(e) {
    this.hideView(), this.content.add(e);
  }
  /**
  * Hides the currently displayed view.
  */
  hideView() {
    this.content.clear();
  }
  /**
  * Creates a navigation button view.
  *
  * @param label The button label.
  * @param icon The button icon.
  */
  _createButtonView(e, n) {
    const r = new Te(this.locale);
    return r.set({
      label: e,
      icon: n,
      tooltip: !0
    }), r;
  }
}
class PD extends de {
  /**
  * @inheritDoc
  */
  constructor(e, n) {
    super(e);
    /**
    * Collection of the child views which creates fake panel content.
    */
    h(this, "content");
    /**
    * Context.
    */
    h(this, "_balloonPanelView");
    const r = this.bindTemplate;
    this.set("top", 0), this.set("left", 0), this.set("height", 0), this.set("width", 0), this.set("numberOfPanels", 0), this.content = this.createCollection(), this._balloonPanelView = n, this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck-fake-panel",
          r.to("numberOfPanels", (s) => s ? "" : "ck-hidden")
        ],
        style: {
          top: r.to("top", jc),
          left: r.to("left", jc),
          width: r.to("width", jc),
          height: r.to("height", jc)
        }
      },
      children: this.content
    }), this.on("change:numberOfPanels", (s, o, a, l) => {
      a > l ? this._addPanels(a - l) : this._removePanels(l - a), this.updatePosition();
    });
  }
  _addPanels(e) {
    for (; e--; ) {
      const n = new de();
      n.setTemplate({
        tag: "div"
      }), this.content.add(n), this.registerChild(n);
    }
  }
  _removePanels(e) {
    for (; e--; ) {
      const n = this.content.last;
      this.content.remove(n), this.deregisterChild(n), n.destroy();
    }
  }
  /**
  * Updates coordinates of fake panels.
  */
  updatePosition() {
    if (this.numberOfPanels) {
      const { top: e, left: n } = this._balloonPanelView, { width: r, height: s } = new je(this._balloonPanelView.element);
      Object.assign(this, {
        top: e,
        left: n,
        width: r,
        height: s
      });
    }
  }
}
const vo = /* @__PURE__ */ vs("px");
class ID extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views which creates balloon panel contents.
    */
    h(this, "content");
    /**
    * The panel which accepts children into {@link #content} collection.
    * Also an element which is positioned when {@link #isSticky}.
    */
    h(this, "contentPanelElement");
    /**
    * A dummy element which visually fills the space as long as the
    * actual panel is sticky. It prevents flickering of the UI.
    */
    h(this, "_contentPanelPlaceholder");
    const n = this.bindTemplate;
    this.set("isActive", !1), this.set("isSticky", !1), this.set("limiterElement", null), this.set("limiterBottomOffset", 50), this.set("viewportTopOffset", 0), this.set("_marginLeft", null), this.set("_isStickyToTheBottomOfLimiter", !1), this.set("_stickyTopOffset", null), this.set("_stickyBottomOffset", null), this.content = this.createCollection(), this._contentPanelPlaceholder = new li({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-sticky-panel__placeholder"
        ],
        style: {
          display: n.to("isSticky", (r) => r ? "block" : "none"),
          height: n.to("isSticky", (r) => r ? vo(this._contentPanelRect.height) : null)
        }
      }
    }).render(), this.contentPanelElement = new li({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-sticky-panel__content",
          // Toggle class of the panel when "sticky" state changes in the view.
          n.if("isSticky", "ck-sticky-panel__content_sticky"),
          n.if("_isStickyToTheBottomOfLimiter", "ck-sticky-panel__content_sticky_bottom-limit")
        ],
        style: {
          width: n.to("isSticky", (r) => r ? vo(this._contentPanelPlaceholder.getBoundingClientRect().width) : null),
          top: n.to("_stickyTopOffset", (r) => r && vo(r)),
          bottom: n.to("_stickyBottomOffset", (r) => r && vo(r)),
          marginLeft: n.to("_marginLeft")
        }
      },
      children: this.content
    }).render(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-sticky-panel"
        ]
      },
      children: [
        this._contentPanelPlaceholder,
        this.contentPanelElement
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.checkIfShouldBeSticky(), this.listenTo(ae.document, "scroll", () => {
      this.checkIfShouldBeSticky();
    }, {
      useCapture: !0
    }), this.listenTo(this, "change:isActive", () => {
      this.checkIfShouldBeSticky();
    });
  }
  /**
  * Analyzes the environment to decide whether the panel should be sticky or not.
  * Then handles the positioning of the panel.
  */
  checkIfShouldBeSticky() {
    if (!this.limiterElement || !this.isActive) {
      this._unstick();
      return;
    }
    const e = new je(this.limiterElement);
    let n = e.getVisible();
    if (n) {
      const r = new je(ae.window);
      r.top += this.viewportTopOffset, r.height -= this.viewportTopOffset, n = n.getIntersection(r);
    }
    if (n && e.top < n.top) {
      const r = n.top;
      if (r + this._contentPanelRect.height + this.limiterBottomOffset > n.bottom) {
        const s = Math.max(e.bottom - n.bottom, 0) + this.limiterBottomOffset;
        e.bottom - s > e.top + this._contentPanelRect.height ? this._stickToBottomOfLimiter(s) : this._unstick();
      } else
        this._contentPanelRect.height + this.limiterBottomOffset < e.height ? this._stickToTopOfAncestors(r) : this._unstick();
    } else
      this._unstick();
  }
  /**
  * Sticks the panel at the given CSS `top` offset.
  *
  * @private
  * @param topOffset
  */
  _stickToTopOfAncestors(e) {
    this.isSticky = !0, this._isStickyToTheBottomOfLimiter = !1, this._stickyTopOffset = e, this._stickyBottomOffset = null, this._marginLeft = vo(-ae.window.scrollX);
  }
  /**
  * Sticks the panel at the bottom of the limiter with a given CSS `bottom` offset.
  *
  * @private
  * @param stickyBottomOffset
  */
  _stickToBottomOfLimiter(e) {
    this.isSticky = !0, this._isStickyToTheBottomOfLimiter = !0, this._stickyTopOffset = null, this._stickyBottomOffset = e, this._marginLeft = vo(-ae.window.scrollX);
  }
  /**
  * Unsticks the panel putting it back to its original position.
  *
  * @private
  */
  _unstick() {
    this.isSticky = !1, this._isStickyToTheBottomOfLimiter = !1, this._stickyTopOffset = null, this._stickyBottomOffset = null, this._marginLeft = null;
  }
  /**
  * Returns the bounding rect of the {@link #contentPanelElement}.
  *
  * @private
  */
  get _contentPanelRect() {
    return new je(this.contentPanelElement);
  }
}
const RD = /* @__PURE__ */ vs("px");
class OD extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The toolbar view displayed in the balloon.
    */
    h(this, "toolbarView");
    /**
    * Tracks the focus of the {@link module:ui/editorui/editorui~EditorUI#getEditableElement editable element}
    * and the {@link #toolbarView}. When both are blurred then the toolbar should hide.
    */
    h(this, "focusTracker");
    /**
    * A cached and normalized `config.balloonToolbar` object.
    */
    h(this, "_balloonConfig");
    /**
    * An instance of the resize observer that allows to respond to changes in editable's geometry
    * so the toolbar can stay within its boundaries (and group toolbar items that do not fit).
    *
    * **Note**: Used only when `shouldNotGroupWhenFull` was **not** set in the
    * {@link module:core/editor/editorconfig~EditorConfig#balloonToolbar configuration}.
    *
    * **Note:** Created in {@link #init}.
    */
    h(this, "_resizeObserver", null);
    /**
    * The contextual balloon plugin instance.
    */
    h(this, "_balloon");
    /**
    * Fires `_selectionChangeDebounced` event using `lodash#debounce`.
    *
    * This event is an internal plugin event which is fired 200 ms after model selection last change.
    * This is to makes easy test debounced action without need to use `setTimeout`.
    *
    * This function is stored as a plugin property to make possible to cancel
    * trailing debounced invocation on destroy.
    */
    h(this, "_fireSelectionChangeDebounced");
    this._balloonConfig = Kd(e.config.get("balloonToolbar")), this.toolbarView = this._createToolbarView(), this.focusTracker = new wt(), this._trackFocusableEditableElements(), this.focusTracker.add(this.toolbarView), e.ui.addToolbar(this.toolbarView, {
      beforeFocus: () => this.show(!0),
      afterBlur: () => this.hide(),
      isContextual: !0
    }), this._balloon = e.plugins.get(Qi), this._fireSelectionChangeDebounced = Ci(() => this.fire("_selectionChangeDebounced"), 200), this.decorate("show");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BalloonToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Qi
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.document.selection;
    this.listenTo(this.focusTracker, "change:isFocused", (r, s, o) => {
      const a = this._balloon.visibleView === this.toolbarView;
      !o && a ? this.hide() : o && this.show();
    }), this.listenTo(n, "change:range", (r, s) => {
      (s.directChange || n.isCollapsed) && this.hide(), this._fireSelectionChangeDebounced();
    }), this.listenTo(this, "_selectionChangeDebounced", () => {
      this.editor.editing.view.document.isFocused && this.show();
    }), this._balloonConfig.shouldNotGroupWhenFull || this.listenTo(e, "ready", () => {
      const r = e.ui.view.editable.element;
      this._resizeObserver = new qs(r, (s) => {
        this.toolbarView.maxWidth = RD(s.contentRect.width * 0.9);
      });
    }), this.listenTo(this.toolbarView, "groupedItemsUpdate", () => {
      this._updatePosition();
    }), e.ui.once("ready", () => {
      this.toolbarView.fillFromConfig(this._balloonConfig, this.editor.ui.componentFactory);
    });
  }
  /**
  * Creates the toolbar view instance.
  */
  _createToolbarView() {
    const e = this.editor.locale.t, n = !this._balloonConfig.shouldNotGroupWhenFull, r = new fs(this.editor.locale, {
      shouldGroupWhenFull: n,
      isFloating: !0
    });
    return r.ariaLabel = e("Editor contextual toolbar"), r.render(), r;
  }
  /**
  * Shows the toolbar and attaches it to the selection.
  *
  * Fires {@link #event:show} event which can be stopped to prevent the toolbar from showing up.
  *
  * @param showForCollapsedSelection When set `true`, the toolbar will show despite collapsed selection in the
  * editing view.
  */
  show(e = !1) {
    const n = this.editor, r = n.model.document.selection, s = n.model.schema;
    this._balloon.hasView(this.toolbarView) || r.isCollapsed && !e || VD(r, s) || Array.from(this.toolbarView.items).every((o) => o.isEnabled !== void 0 && !o.isEnabled) || (this.listenTo(this.editor.ui, "update", () => {
      this._updatePosition();
    }), this._balloon.add({
      view: this.toolbarView,
      position: this._getBalloonPositionData(),
      balloonClassName: "ck-toolbar-container"
    }));
  }
  /**
  * Hides the toolbar.
  */
  hide() {
    this._balloon.hasView(this.toolbarView) && (this.stopListening(this.editor.ui, "update"), this._balloon.remove(this.toolbarView));
  }
  /**
  * Add or remove editable elements to the focus tracker. It watches added and removed roots
  * and adds or removes their editable elements to the focus tracker.
  */
  _trackFocusableEditableElements() {
    const { editor: e, focusTracker: n } = this, { editing: r } = e;
    r.view.addObserver(class extends nr {
      /**
      * @inheritDoc
      */
      observe(o) {
        n.add(o);
      }
      /**
      * @inheritDoc
      */
      stopObserving(o) {
        n.remove(o);
      }
    });
  }
  /**
  * Returns positioning options for the {@link #_balloon}. They control the way balloon is attached
  * to the selection.
  */
  _getBalloonPositionData() {
    const n = this.editor.editing.view, r = n.document, s = r.selection, o = r.selection.isBackward;
    return {
      // Because the target for BalloonPanelView is a Rect (not DOMRange), it's geometry will stay fixed
      // as the window scrolls. To let the BalloonPanelView follow such Rect, is must be continuously
      // computed and hence, the target is defined as a function instead of a static value.
      // https://github.com/ckeditor/ckeditor5-ui/issues/195
      target: () => {
        const a = o ? s.getFirstRange() : s.getLastRange(), l = je.getDomRangeRects(n.domConverter.viewRangeToDom(a));
        return o ? l[0] : (l.length > 1 && l[l.length - 1].width === 0 && l.pop(), l[l.length - 1]);
      },
      positions: this._getBalloonPositions(o)
    };
  }
  /**
  * Updates the position of the {@link #_balloon} to make up for changes:
  *
  * * in the geometry of the selection it is attached to (e.g. the selection moved in the viewport or expanded or shrunk),
  * * or the geometry of the balloon toolbar itself (e.g. the toolbar has grouped or ungrouped some items and it is shorter or longer).
  */
  _updatePosition() {
    this._balloon.updatePosition(this._getBalloonPositionData());
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.stopListening(), this._fireSelectionChangeDebounced.cancel(), this.toolbarView.destroy(), this.focusTracker.destroy(), this._resizeObserver && this._resizeObserver.destroy();
  }
  /**
  * Returns toolbar positions for the given direction of the selection.
  */
  _getBalloonPositions(e) {
    const r = Me.isSafari && Me.isiOS ? fn.generatePositions({
      // 20px when zoomed out. Less then 20px when zoomed in; the "radius" of the native selection handle gets
      // smaller as the user zooms in. No less than the default v-offset, though.
      heightOffset: Math.max(fn.arrowHeightOffset, Math.round(20 / ae.window.visualViewport.scale))
    }) : fn.defaultPositions;
    return e ? [
      r.northWestArrowSouth,
      r.northWestArrowSouthWest,
      r.northWestArrowSouthEast,
      r.northWestArrowSouthMiddleEast,
      r.northWestArrowSouthMiddleWest,
      r.southWestArrowNorth,
      r.southWestArrowNorthWest,
      r.southWestArrowNorthEast,
      r.southWestArrowNorthMiddleWest,
      r.southWestArrowNorthMiddleEast
    ] : [
      r.southEastArrowNorth,
      r.southEastArrowNorthEast,
      r.southEastArrowNorthWest,
      r.southEastArrowNorthMiddleEast,
      r.southEastArrowNorthMiddleWest,
      r.northEastArrowSouth,
      r.northEastArrowSouthEast,
      r.northEastArrowSouthWest,
      r.northEastArrowSouthMiddleEast,
      r.northEastArrowSouthMiddleWest
    ];
  }
}
function VD(i, t) {
  return i.rangeCount === 1 ? !1 : [
    ...i.getRanges()
  ].every((e) => {
    const n = e.getContainedElement();
    return n && t.isSelectable(n);
  });
}
const jv = /* @__PURE__ */ vs("px");
class MD extends Te {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = this.bindTemplate;
    this.isVisible = !1, this.isToggleable = !0, this.set("top", 0), this.set("left", 0), this.extendTemplate({
      attributes: {
        class: "ck-block-toolbar-button",
        style: {
          top: e.to("top", (n) => jv(n)),
          left: e.to("left", (n) => jv(n))
        }
      }
    });
  }
}
const $a = /* @__PURE__ */ vs("px");
class DD extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The toolbar view.
    */
    h(this, "toolbarView");
    /**
    * The balloon panel view, containing the {@link #toolbarView}.
    */
    h(this, "panelView");
    /**
    * The button view that opens the {@link #toolbarView}.
    */
    h(this, "buttonView");
    /**
    * An instance of the resize observer that allows to respond to changes in editable's geometry
    * so the toolbar can stay within its boundaries (and group toolbar items that do not fit).
    *
    * **Note**: Used only when `shouldNotGroupWhenFull` was **not** set in the
    * {@link module:core/editor/editorconfig~EditorConfig#blockToolbar configuration}.
    */
    h(this, "_resizeObserver", null);
    /**
    * A cached and normalized `config.blockToolbar` object.
    */
    h(this, "_blockToolbarConfig");
    this._blockToolbarConfig = Kd(this.editor.config.get("blockToolbar")), this.toolbarView = this._createToolbarView(), this.panelView = this._createPanelView(), this.buttonView = this._createButtonView(), _s({
      emitter: this.panelView,
      contextElements: [
        this.panelView.element,
        this.buttonView.element
      ],
      activator: () => this.panelView.isVisible,
      callback: () => this._hidePanel()
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BlockToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.t, r = n("Click to edit block"), s = n("Drag to move"), o = n("Edit block"), a = e.plugins.has("DragDropBlockToolbar"), l = a ? `${r}
${s}` : o;
    this.buttonView.label = l, a && (this.buttonView.element.dataset.ckeTooltipClass = "ck-tooltip_multi-line"), this.listenTo(e.model.document.selection, "change:range", (c, u) => {
      u.directChange && this._hidePanel();
    }), this.listenTo(e.ui, "update", () => this._updateButton()), this.listenTo(e, "change:isReadOnly", () => this._updateButton(), {
      priority: "low"
    }), this.listenTo(e.ui.focusTracker, "change:isFocused", () => this._updateButton()), this.listenTo(this.buttonView, "change:isVisible", (c, u, d) => {
      d ? this.buttonView.listenTo(window, "resize", () => this._updateButton()) : (this.buttonView.stopListening(window, "resize"), this._hidePanel());
    }), this._repositionButtonOnScroll(), e.ui.addToolbar(this.toolbarView, {
      beforeFocus: () => this._showPanel(),
      afterBlur: () => this._hidePanel()
    }), e.ui.once("ready", () => {
      this.toolbarView.fillFromConfig(this._blockToolbarConfig, this.editor.ui.componentFactory);
      for (const c of this.toolbarView.items)
        c.on("execute", () => this._hidePanel(!0), {
          priority: "high"
        });
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.panelView.destroy(), this.buttonView.destroy(), this.toolbarView.destroy(), this._resizeObserver && this._resizeObserver.destroy();
  }
  /**
  * Creates the {@link #toolbarView}.
  */
  _createToolbarView() {
    const e = this.editor.locale.t, n = !this._blockToolbarConfig.shouldNotGroupWhenFull, r = new fs(this.editor.locale, {
      shouldGroupWhenFull: n,
      isFloating: !0
    });
    return r.ariaLabel = e("Editor block content toolbar"), r;
  }
  /**
  * Creates the {@link #panelView}.
  */
  _createPanelView() {
    const e = this.editor, n = new fn(e.locale);
    return n.content.add(this.toolbarView), n.class = "ck-toolbar-container", e.ui.view.body.add(n), this.toolbarView.keystrokes.set("Esc", (r, s) => {
      this._hidePanel(!0), s();
    }), n;
  }
  /**
  * Creates the {@link #buttonView}.
  */
  _createButtonView() {
    const e = this.editor, n = e.t, r = new MD(e.locale), s = this._blockToolbarConfig.icon, o = bm[s] || s || bm.dragIndicator;
    return r.set({
      label: n("Edit block"),
      icon: o,
      withText: !1
    }), r.bind("isOn").to(this.panelView, "isVisible"), r.bind("tooltip").to(this.panelView, "isVisible", (a) => !a), this.listenTo(r, "execute", () => {
      this.panelView.isVisible ? this._hidePanel(!0) : this._showPanel();
    }), this.listenTo(r, "change:isEnabled", (a, l, c) => {
      !c && this.panelView.isVisible && this._hidePanel(!1);
    }), e.ui.view.body.add(r), r;
  }
  /**
  * Shows or hides the button.
  * When all the conditions for displaying the button are matched, it shows the button. Hides otherwise.
  */
  _updateButton() {
    const e = this.editor, n = e.model, r = e.editing.view;
    if (!e.ui.focusTracker.isFocused) {
      this._hideButton();
      return;
    }
    if (!e.model.canEditAt(e.model.document.selection)) {
      this._hideButton();
      return;
    }
    const s = Array.from(n.document.selection.getSelectedBlocks())[0];
    if (!s || Array.from(this.toolbarView.items).every((a) => !a.isEnabled)) {
      this._hideButton();
      return;
    }
    const o = r.domConverter.mapViewToDom(e.editing.mapper.toViewElement(s));
    this.buttonView.isVisible = !0, this._setupToolbarResize(), this._attachButtonToElement(o), this.panelView.isVisible && this._showPanel();
  }
  /**
  * Hides the button.
  */
  _hideButton() {
    this.buttonView.isVisible = !1;
  }
  /**
  * Shows the {@link #toolbarView} attached to the {@link #buttonView}.
  * If the toolbar is already visible, then it simply repositions it.
  */
  _showPanel() {
    if (!this.buttonView.isVisible)
      return;
    const e = this.panelView.isVisible;
    this.panelView.show();
    const n = this._getSelectedEditableElement();
    this.toolbarView.maxWidth = this._getToolbarMaxWidth(n), this.panelView.pin({
      target: this.buttonView.element,
      limiter: n
    }), e || this.toolbarView.items.get(0).focus();
  }
  /**
  * Returns currently selected editable, based on the model selection.
  */
  _getSelectedEditableElement() {
    const e = this.editor.model.document.selection.getFirstRange().root.rootName;
    return this.editor.ui.getEditableElement(e);
  }
  /**
  * Hides the {@link #toolbarView}.
  *
  * @param focusEditable When `true`, the editable will be focused after hiding the panel.
  */
  _hidePanel(e) {
    this.panelView.isVisible = !1, e && this.editor.editing.view.focus();
  }
  /**
  * Repositions the button on scroll.
  */
  _repositionButtonOnScroll() {
    const { buttonView: e } = this;
    let n = !1;
    const r = (s, o) => {
      if (n)
        return;
      const a = this._getSelectedEditableElement();
      o.target !== ae.document && !w2(a).includes(o.target) || (n = !0, ae.window.requestAnimationFrame(() => {
        this._updateButton(), n = !1;
      }));
    };
    e.on("change:isVisible", (s, o, a) => {
      a ? e.listenTo(ae.document, "scroll", r, {
        useCapture: !0,
        usePassive: !0
      }) : e.stopListening(ae.document, "scroll", r);
    });
  }
  /**
  * Attaches the {@link #buttonView} to the target block of content.
  *
  * @param targetElement Target element.
  */
  _attachButtonToElement(e) {
    const n = this.buttonView.element, r = this._getSelectedEditableElement(), s = window.getComputedStyle(e), o = new je(r), a = parseInt(s.paddingTop, 10), l = parseInt(s.lineHeight, 10) || parseInt(s.fontSize, 10) * 1.2, c = new je(n), u = new je(e);
    let d;
    this.editor.locale.uiLanguageDirection === "ltr" ? d = o.left - c.width : d = o.right;
    const f = u.top + a + (l - c.height) / 2;
    c.moveTo(d, f);
    const m = c.toAbsoluteRect();
    this.buttonView.top = m.top, this.buttonView.left = m.left, this._clipButtonToViewport(this.buttonView, r);
  }
  /**
  * Clips the button element to the viewport of the editable element.
  *
  * 	* If the button overflows the editable viewport, it is clipped to make it look like it's cut off by the editable scrollable region.
  * 	* If the button is fully hidden by the top of the editable, it is not clickable but still visible in the DOM.
  *
  * @param buttonView The button view to clip.
  * @param editableElement The editable element whose viewport is used for clipping.
  */
  _clipButtonToViewport(e, n) {
    const r = new je(e.element), s = new je(n).getVisible(), o = (...l) => {
      e.element.style.clipPath = l.length ? `polygon(${l.join(",")})` : "";
    }, a = (l) => {
      e.isEnabled = !l, e.element.style.pointerEvents = l ? "none" : "";
    };
    if (s && s.bottom < r.bottom) {
      const l = Math.min(r.height, r.bottom - s.bottom);
      a(l >= r.height), o("0 0", "100% 0", `100% calc(100% - ${$a(l)})`, `0 calc(100% - ${$a(l)}`);
    } else if (s && s.top > r.top) {
      const l = Math.min(r.height, s.top - r.top);
      a(l >= r.height), o(`0 ${$a(l)}`, `100% ${$a(l)}`, "100% 100%", "0 100%");
    } else
      a(!1), o();
  }
  /**
  * Creates a resize observer that observes selected editable and resizes the toolbar panel accordingly.
  */
  _setupToolbarResize() {
    const e = this._getSelectedEditableElement();
    this._blockToolbarConfig.shouldNotGroupWhenFull || (this._resizeObserver && this._resizeObserver.element !== e && (this._resizeObserver.destroy(), this._resizeObserver = null), this._resizeObserver || (this._resizeObserver = new qs(e, () => {
      this.toolbarView.maxWidth = this._getToolbarMaxWidth(e);
    })));
  }
  /**
  * Gets the {@link #toolbarView} max-width, based on given `editableElement` width plus the distance between the farthest
  * edge of the {@link #buttonView} and the editable.
  *
  * @returns A maximum width that toolbar can have, in pixels.
  */
  _getToolbarMaxWidth(e) {
    const n = new je(e), r = new je(this.buttonView.element), o = this.editor.locale.uiLanguageDirection === "rtl" ? r.left - n.right + r.width : n.left - r.left;
    return $a(n.width + o);
  }
}
class LD extends na {
  /**
  * Creates an instance of the menu bar button view.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * An icon that displays an arrow to indicate a direction of the menu.
    */
    h(this, "arrowView");
    const n = this.bindTemplate;
    this.set({
      withText: !0,
      role: "menuitem"
    }), this.arrowView = this._createArrowView(), this.extendTemplate({
      attributes: {
        class: [
          "ck-menu-bar__menu__button"
        ],
        "aria-haspopup": !0,
        "aria-expanded": this.bindTemplate.to("isOn", (r) => String(r)),
        "data-cke-tooltip-disabled": n.to("isOn")
      },
      on: {
        mouseenter: n.to("mouseenter")
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.children.add(this.arrowView);
  }
  /**
  * Creates the {@link #arrowView} instance.
  */
  _createArrowView() {
    const e = new pr();
    return e.content = qd, e.extendTemplate({
      attributes: {
        class: "ck-menu-bar__menu__button__arrow"
      }
    }), e;
  }
}
class ND extends de {
  /**
  * Creates an instance of the menu panel view.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views in this panel.
    */
    h(this, "children");
    const n = this.bindTemplate;
    this.set("isVisible", !1), this.set("position", "se"), this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset",
          "ck-menu-bar__menu__panel",
          n.to("position", (r) => `ck-menu-bar__menu__panel_position_${r}`),
          n.if("isVisible", "ck-hidden", (r) => !r)
        ],
        tabindex: "-1"
      },
      children: this.children,
      on: {
        // Drag and drop in the panel should not break the selection in the editor.
        // https://github.com/ckeditor/ckeditor5-ui/issues/228
        selectstart: n.to((r) => {
          r.target.tagName.toLocaleLowerCase() !== "input" && r.preventDefault();
        })
      }
    });
  }
  /**
  * Focuses the first child of the panel (default) or the last one if the `direction` is `-1`.
  */
  focus(e = 1) {
    this.children.length && (e === 1 ? this.children.first.focus() : this.children.last.focus());
  }
}
const Sd = class Sd extends de {
  /**
  * Creates an instance of the menu view.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * Button of the menu view.
    */
    h(this, "buttonView");
    /**
    * Panel of the menu. It hosts children of the menu.
    */
    h(this, "panelView");
    /**
    * Tracks information about the DOM focus in the menu.
    */
    h(this, "focusTracker");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}. It manages
    * keystrokes of the menu.
    */
    h(this, "keystrokes");
    const n = this.bindTemplate;
    this.buttonView = new LD(e), this.buttonView.delegate("mouseenter").to(this), this.buttonView.bind("isOn", "isEnabled").to(this, "isOpen", "isEnabled"), this.panelView = new ND(e), this.panelView.bind("isVisible").to(this, "isOpen"), this.keystrokes = new Ot(), this.focusTracker = new wt(), this.set("isOpen", !1), this.set("isEnabled", !0), this.set("panelPosition", "w"), this.set("class", void 0), this.set("parentMenuView", null), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-menu-bar__menu",
          n.to("class"),
          n.if("isEnabled", "ck-disabled", (r) => !r),
          n.if("parentMenuView", "ck-menu-bar__menu_top-level", (r) => !r)
        ]
      },
      children: [
        this.buttonView,
        this.panelView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.buttonView.element), this.focusTracker.add(this.panelView.element), this.keystrokes.listenTo(this.element), Kr.closeOnEscKey(this), this._repositionPanelOnOpen();
  }
  // For now, this method cannot be called in the render process because the `parentMenuView` may be assigned
  // after the rendering process.
  //
  // TODO: We should reconsider the way we handle this logic.
  /**
  * Attach all keyboard behaviors for the menu bar view.
  *
  * @internal
  */
  _attachBehaviors() {
    this.parentMenuView ? (Kr.openOnButtonClick(this), Kr.openOnArrowRightKey(this), Kr.closeOnArrowLeftKey(this), Kr.openAndFocusOnEnterKeyPress(this), Kr.closeOnParentClose(this)) : (this._propagateArrowKeystrokeEvents(), Kr.openAndFocusPanelOnArrowDownKey(this), Kr.toggleOnButtonClick(this));
  }
  /**
  * Fires `arrowright` and `arrowleft` events when the user pressed corresponding arrow keys.
  */
  _propagateArrowKeystrokeEvents() {
    this.keystrokes.set("arrowright", (e, n) => {
      this.fire("arrowright"), n();
    }), this.keystrokes.set("arrowleft", (e, n) => {
      this.fire("arrowleft"), n();
    });
  }
  /**
  * Sets the position of the panel when the menu opens. The panel is positioned
  * so that it optimally uses the available space in the viewport.
  */
  _repositionPanelOnOpen() {
    this.on("change:isOpen", (e, n, r) => {
      if (!r)
        return;
      const s = Sd._getOptimalPosition({
        element: this.panelView.element,
        target: this.buttonView.element,
        fitInViewport: !0,
        positions: this._panelPositions
      });
      this.panelView.position = s ? s.name : this._panelPositions[0].name;
    });
  }
  /**
  * @inheritDoc
  */
  focus() {
    this.buttonView.focus();
  }
  /**
  * Positioning functions for the {@link #panelView} . They change depending on the role of the menu (top-level vs sub-menu) in
  * the {@link module:ui/menubar/menubarview~MenuBarView menu bar} and the UI language direction.
  */
  get _panelPositions() {
    const { southEast: e, southWest: n, northEast: r, northWest: s, westSouth: o, eastSouth: a, westNorth: l, eastNorth: c } = hD;
    return this.locale.uiLanguageDirection === "ltr" ? this.parentMenuView ? [
      a,
      c,
      o,
      l
    ] : [
      e,
      n,
      r,
      s
    ] : this.parentMenuView ? [
      o,
      l,
      a,
      c
    ] : [
      n,
      e,
      s,
      r
    ];
  }
};
/**
* A function used to calculate the optimal position for the dropdown panel.
*
* Referenced for unit testing purposes.
*/
h(Sd, "_getOptimalPosition", qg);
let Yi = Sd;
class ra extends pp {
  /**
  * Creates an instance of the list view.
  *
  * @param locale The localization services instance.
  */
  constructor(t) {
    super(t), this.role = "menu", this.items.on("change", this._setItemsCheckSpace.bind(this));
  }
  /**
  * This method adds empty space if there is any toggleable item in the list.
  * It makes the list properly aligned.
  */
  _setItemsCheckSpace() {
    const t = Array.from(this.items).some((e) => {
      const n = Wv(e);
      return n && n.isToggleable;
    });
    this.items.forEach((e) => {
      const n = Wv(e);
      n && (n.hasCheckSpace = t);
    });
  }
}
function Wv(i) {
  return i instanceof hs ? i.children.map((t) => BD(t) ? t.buttonView : t).find((t) => t instanceof na) : null;
}
function BD(i) {
  return typeof i == "object" && "buttonView" in i && i.buttonView instanceof Te;
}
class ok extends pM {
  /**
  * Creates an instance of the menu bar list button view.
  *
  * @param locale The localization services instance.
  */
  constructor(t) {
    super(t), this.set({
      withText: !0,
      withKeystroke: !0,
      tooltip: !1,
      role: "menuitem"
    }), this.extendTemplate({
      attributes: {
        class: [
          "ck-menu-bar__menu__item__button"
        ]
      }
    });
  }
}
const Uv = [
  "mouseenter",
  "arrowleft",
  "arrowright",
  "change:isOpen"
];
class FD extends de {
  /**
  * Creates an instance of the menu bar view.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views inside the {@link #element}.
    */
    h(this, "children");
    /**
    * A list of {@link module:ui/menubar/menubarmenuview~MenuBarMenuView} instances registered in the menu bar.
    *
    * @observable
    */
    h(this, "menus", []);
    const n = e.t, r = this.bindTemplate;
    this.set({
      isOpen: !1,
      isFocusBorderEnabled: !1
    }), this._setupIsOpenUpdater(), this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-menu-bar",
          r.if("isFocusBorderEnabled", "ck-menu-bar_focus-border-enabled")
        ],
        "aria-label": n("Editor menu bar"),
        role: "menubar"
      },
      children: this.children
    });
  }
  /**
  * A utility that expands a plain menu bar configuration into a structure of menus (also: sub-menus)
  * and items using a given {@link module:ui/componentfactory~ComponentFactory component factory}.
  *
  * See the {@link module:core/editor/editorconfig~EditorConfig#menuBar menu bar} in the editor
  * configuration reference to learn how to configure the menu bar.
  */
  fillFromConfig(e, n, r = []) {
    const s = this.locale, a = gD({
      normalizedConfig: e,
      locale: s,
      componentFactory: n,
      extraItems: r
    }).items.map((l) => this._createMenu({
      componentFactory: n,
      menuDefinition: l
    }));
    this.children.addMany(a);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), bo.toggleMenusAndFocusItemsOnHover(this), bo.closeMenusWhenTheBarCloses(this), bo.closeMenuWhenAnotherOnTheSameLevelOpens(this), bo.focusCycleMenusOnArrows(this), bo.closeOnClickOutside(this), bo.enableFocusHighlightOnInteraction(this);
  }
  /**
  * Focuses the menu bar.
  */
  focus() {
    this.children.first && this.children.first.focus();
  }
  /**
  * Closes all menus in the bar.
  */
  close() {
    for (const e of this.children)
      e.isOpen = !1;
  }
  /**
  * Registers a menu view in the menu bar. Every {@link module:ui/menubar/menubarmenuview~MenuBarMenuView} instance must be registered
  * in the menu bar to be properly managed.
  */
  registerMenu(e, n = null) {
    n ? (e.delegate(...Uv).to(n), e.parentMenuView = n) : e.delegate(...Uv).to(this, (r) => "menu:" + r), e._attachBehaviors(), this.menus.push(e);
  }
  /**
  * Creates a {@link module:ui/menubar/menubarmenuview~MenuBarMenuView} based on the given definition.
  */
  _createMenu({ componentFactory: e, menuDefinition: n, parentMenuView: r }) {
    const s = this.locale, o = new Yi(s);
    return this.registerMenu(o, r), o.buttonView.set({
      label: n.label
    }), o.once("change:isOpen", () => {
      const a = new ra(s);
      a.ariaLabel = n.label, o.panelView.children.add(a), a.items.addMany(this._createMenuItems({
        menuDefinition: n,
        parentMenuView: o,
        componentFactory: e
      }));
    }), o;
  }
  /**
  * Creates a {@link module:ui/menubar/menubarmenuview~MenuBarMenuView} items based on the given definition.
  */
  _createMenuItems({ menuDefinition: e, parentMenuView: n, componentFactory: r }) {
    const s = this.locale, o = [];
    for (const a of e.groups) {
      for (const l of a.items) {
        const c = new Ks(s, n);
        if (vt(l))
          c.children.add(this._createMenu({
            componentFactory: r,
            menuDefinition: l,
            parentMenuView: n
          }));
        else {
          const u = this._createMenuItemContentFromFactory({
            componentName: l,
            componentFactory: r,
            parentMenuView: n
          });
          if (!u)
            continue;
          c.children.add(u);
        }
        o.push(c);
      }
      a !== e.groups[e.groups.length - 1] && o.push(new Gd(s));
    }
    return o;
  }
  /**
  * Uses the component factory to create a content of the menu item (a button or a sub-menu).
  */
  _createMenuItemContentFromFactory({ componentName: e, parentMenuView: n, componentFactory: r }) {
    const s = r.create(e);
    return s instanceof Yi || s instanceof At || s instanceof ok ? (this._registerMenuTree(s, n), s.on("execute", () => {
      this.close();
    }), s) : (Xe("menu-bar-component-unsupported", {
      componentName: e,
      componentView: s
    }), null);
  }
  /**
  * Checks component and its children recursively and calls {@link #registerMenu}
  * for each item that is {@link module:ui/menubar/menubarmenuview~MenuBarMenuView}.
  *
  * @internal
  */
  _registerMenuTree(e, n) {
    if (!(e instanceof Yi)) {
      e.delegate("mouseenter").to(n);
      return;
    }
    this.registerMenu(e, n);
    const r = e.panelView.children.filter((o) => o instanceof ra)[0];
    if (!r) {
      e.delegate("mouseenter").to(n);
      return;
    }
    const s = r.items.filter((o) => o instanceof hs);
    for (const o of s)
      this._registerMenuTree(o.children.get(0), e);
  }
  /**
  * Manages the state of the {@link #isOpen} property of the menu bar. Because the state is a sum of individual
  * top-level menus' states, it's necessary to listen to their changes and update the state accordingly.
  *
  * Additionally, it prevents from unnecessary changes of `isOpen` when one top-level menu opens and another closes
  * (regardless of in which order), maintaining a stable `isOpen === true` in that situation.
  */
  _setupIsOpenUpdater() {
    let e;
    this.on("menu:change:isOpen", (n, r, s) => {
      clearTimeout(e), s ? this.isOpen = !0 : e = setTimeout(() => {
        this.isOpen = Array.from(this.children).some((o) => o.isOpen);
      }, 0);
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class ak {
  /**
  * Creates a new instance of the change buffer.
  *
  * @param limit The maximum number of atomic changes which can be contained in one batch.
  */
  constructor(t, e = 20) {
    /**
    * The model instance.
    */
    h(this, "model");
    /**
    * The maximum number of atomic changes which can be contained in one batch.
    */
    h(this, "limit");
    /**
    * Whether the buffer is locked. A locked buffer cannot be reset unless it gets unlocked.
    */
    h(this, "_isLocked");
    /**
    * The number of atomic changes in the buffer. Once it exceeds the {@link #limit},
    * the {@link #batch batch} is set to a new one.
    */
    h(this, "_size");
    /**
    * The current batch instance.
    */
    h(this, "_batch", null);
    /**
    * The callback to document the change event which later needs to be removed.
    */
    h(this, "_changeCallback");
    /**
    * The callback to document selection `change:attribute` and `change:range` events which resets the buffer.
    */
    h(this, "_selectionChangeCallback");
    this.model = t, this._size = 0, this.limit = e, this._isLocked = !1, this._changeCallback = (n, r) => {
      r.isLocal && r.isUndoable && r !== this._batch && this._reset(!0);
    }, this._selectionChangeCallback = () => {
      this._reset();
    }, this.model.document.on("change", this._changeCallback), this.model.document.selection.on("change:range", this._selectionChangeCallback), this.model.document.selection.on("change:attribute", this._selectionChangeCallback);
  }
  /**
  * The current batch to which a feature should add its operations. Once the {@link #size}
  * is reached or exceeds the {@link #limit}, the batch is set to a new instance and the size is reset.
  */
  get batch() {
    return this._batch || (this._batch = this.model.createBatch({
      isTyping: !0
    })), this._batch;
  }
  /**
  * The number of atomic changes in the buffer. Once it exceeds the {@link #limit},
  * the {@link #batch batch} is set to a new one.
  */
  get size() {
    return this._size;
  }
  /**
  * The input number of changes into the buffer. Once the {@link #size} is
  * reached or exceeds the {@link #limit}, the batch is set to a new instance and the size is reset.
  *
  * @param changeCount The number of atomic changes to input.
  */
  input(t) {
    this._size += t, this._size >= this.limit && this._reset(!0);
  }
  /**
  * Whether the buffer is locked. A locked buffer cannot be reset unless it gets unlocked.
  */
  get isLocked() {
    return this._isLocked;
  }
  /**
  * Locks the buffer.
  */
  lock() {
    this._isLocked = !0;
  }
  /**
  * Unlocks the buffer.
  */
  unlock() {
    this._isLocked = !1;
  }
  /**
  * Destroys the buffer.
  */
  destroy() {
    this.model.document.off("change", this._changeCallback), this.model.document.selection.off("change:range", this._selectionChangeCallback), this.model.document.selection.off("change:attribute", this._selectionChangeCallback);
  }
  /**
  * Resets the change buffer.
  *
  * @param ignoreLock Whether internal lock {@link #isLocked} should be ignored.
  */
  _reset(t = !1) {
    (!this.isLocked || t) && (this._batch = null, this._size = 0);
  }
}
class $D extends Ve {
  /**
  * Creates an instance of the command.
  *
  * @param undoStepSize The maximum number of atomic changes
  * which can be contained in one batch in the command buffer.
  */
  constructor(e, n) {
    super(e);
    /**
    * Typing's change buffer used to group subsequent changes into batches.
    */
    h(this, "_buffer");
    this._buffer = new ak(e.model, n), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * The current change buffer.
  */
  get buffer() {
    return this._buffer;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._buffer.destroy();
  }
  /**
  * Executes the input command. It replaces the content within the given range with the given text.
  * Replacing is a two step process, first the content within the range is removed and then the new text is inserted
  * at the beginning of the range (which after the removal is a collapsed range).
  *
  * @fires execute
  * @param options The command options.
  */
  execute(e = {}) {
    const n = this.editor.model, r = n.document, s = e.text || "", o = s.length;
    let a = r.selection;
    if (e.selection ? a = e.selection : e.range && (a = n.createSelection(e.range)), !n.canEditAt(a))
      return;
    const l = e.resultRange;
    n.enqueueChange(this._buffer.batch, (c) => {
      this._buffer.lock();
      const u = Array.from(r.selection.getAttributes());
      n.deleteContent(a), s && n.insertContent(c.createText(s, u), a), l ? c.setSelection(l) : a.is("documentSelection") || c.setSelection(a), this._buffer.unlock(), this._buffer.input(o);
    });
  }
}
const lk = [
  // For collapsed range:
  //	- This one is a regular typing (all browsers, all systems).
  //	- This one is used by Chrome when typing accented letter – 2nd step when the user selects the accent (Mac).
  // For non-collapsed range:
  //	- This one is used by Chrome when typing accented letter – when the selection box first appears (Mac).
  //	- This one is used by Safari when accepting spell check suggestions from the context menu (Mac).
  "insertText",
  // This one is used by Safari when typing accented letter (Mac).
  // This one is used by Safari when accepting spell check suggestions from the autocorrection pop-up (Mac).
  "insertReplacementText"
], zD = [
  ...lk,
  "insertCompositionText"
];
class HD extends nr {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Instance of the focus observer. Insert text observer calls
    * {@link module:engine/view/observer/focusobserver~FocusObserver#flush} to mark the latest focus change as complete.
    */
    h(this, "focusObserver");
    this.focusObserver = e.getObserver($u);
    const n = Me.isAndroid ? zD : lk, r = e.document;
    r.on("beforeinput", (s, o) => {
      if (!this.isEnabled)
        return;
      const { data: a, targetRanges: l, inputType: c, domEvent: u } = o;
      if (!n.includes(c))
        return;
      this.focusObserver.flush();
      const d = new Br(r, "insertText");
      r.fire(d, new Qo(e, u, {
        text: a,
        selection: e.createSelection(l)
      })), d.stop.called && s.stop();
    }), Me.isAndroid || r.on("compositionend", (s, { data: o, domEvent: a }) => {
      this.isEnabled && o && r.fire("insertText", new Qo(e, a, {
        text: o
      }));
    }, {
      priority: "lowest"
    });
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
class ck extends V {
  constructor() {
    super(...arguments);
    /**
    * The queue of `insertText` command executions that are waiting for the DOM to get updated after beforeinput event.
    */
    h(this, "_compositionQueue");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Input";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model, r = e.editing.view, s = e.editing.mapper, o = n.document.selection;
    this._compositionQueue = new jD(e), r.addObserver(HD);
    const a = new $D(e, e.config.get("typing.undoStep") || 20);
    e.commands.add("insertText", a), e.commands.add("input", a), this.listenTo(r.document, "insertText", (l, c) => {
      r.document.isComposing || c.preventDefault(), Me.isAndroid && r.document.isComposing && this._compositionQueue.flush("next beforeinput");
      const { text: u, selection: d } = c;
      let f;
      d ? f = Array.from(d.getRanges()).map((p) => s.toModelRange(p)) : f = Array.from(o.getRanges());
      let m = u;
      if (Me.isAndroid) {
        const p = Array.from(f[0].getItems()).reduce((b, v) => b + (v.is("$textProxy") ? v.data : ""), "");
        if (p && (p.length <= m.length ? m.startsWith(p) && (m = m.substring(p.length), f[0].start = f[0].start.getShiftedBy(p.length)) : p.startsWith(m) && (f[0].start = f[0].start.getShiftedBy(m.length), m = "")), m.length == 0 && f[0].isCollapsed)
          return;
      }
      const g = {
        text: m,
        selection: n.createSelection(f)
      };
      Me.isAndroid && r.document.isComposing ? this._compositionQueue.push(g) : (e.execute("insertText", g), r.scrollToTheSelection());
    }), Me.isAndroid ? this.listenTo(r.document, "keydown", (l, c) => {
      o.isCollapsed || c.keyCode != 229 || !r.document.isComposing || qv(n, a);
    }) : this.listenTo(r.document, "compositionstart", () => {
      o.isCollapsed || qv(n, a);
    }), Me.isAndroid ? (this.listenTo(r.document, "mutations", (l, { mutations: c }) => {
      if (r.document.isComposing)
        for (const { node: u } of c) {
          const d = UD(u, s), f = s.toModelElement(d);
          if (this._compositionQueue.isComposedElement(f)) {
            this._compositionQueue.flush("mutations");
            return;
          }
        }
    }), this.listenTo(r.document, "compositionend", () => {
      this._compositionQueue.flush("composition end");
    }), this.listenTo(r.document, "compositionend", () => {
      const l = [];
      for (const c of this._compositionQueue.flushComposedElements()) {
        const u = s.toViewElement(c);
        u && l.push({
          type: "children",
          node: u
        });
      }
      l.length && r.document.fire("mutations", {
        mutations: l
      });
    }, {
      priority: "lowest"
    })) : this.listenTo(r.document, "compositionend", () => {
      r.document.fire("mutations", {
        mutations: []
      });
    }, {
      priority: "lowest"
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._compositionQueue.destroy();
  }
}
class jD {
  /**
  * @inheritDoc
  */
  constructor(t) {
    /**
    * The editor instance.
    */
    h(this, "editor");
    /**
    * Debounced queue flush as a safety mechanism for cases of mutation observer not triggering.
    */
    h(this, "flushDebounced", Ci(() => this.flush("timeout"), 50));
    /**
    * The queue of `insertText` command executions that are waiting for the DOM to get updated after beforeinput event.
    */
    h(this, "_queue", []);
    /**
    * A set of model elements. The composition happened in those elements. It's used for mutations check.
    */
    h(this, "_compositionElements", /* @__PURE__ */ new Set());
    this.editor = t;
  }
  /**
  * Destroys the helper object.
  */
  destroy() {
    for (this.flushDebounced.cancel(), this._compositionElements.clear(); this._queue.length; )
      this.shift();
  }
  /**
  * Returns the size of the queue.
  */
  get length() {
    return this._queue.length;
  }
  /**
  * Push next insertText command data to the queue.
  */
  push(t) {
    const e = {
      text: t.text
    };
    if (t.selection) {
      e.selectionRanges = [];
      for (const n of t.selection.getRanges())
        e.selectionRanges.push(Kn.fromRange(n)), this._compositionElements.add(n.start.parent);
    }
    this._queue.push(e), this.flushDebounced();
  }
  /**
  * Shift the first item from the insertText command data queue.
  */
  shift() {
    const t = this._queue.shift(), e = {
      text: t.text
    };
    if (t.selectionRanges) {
      const n = t.selectionRanges.map((r) => WD(r)).filter((r) => !!r);
      n.length && (e.selection = this.editor.model.createSelection(n));
    }
    return e;
  }
  /**
  * Applies all queued insertText command executions.
  *
  * @param reason Used only for debugging.
  */
  flush(t) {
    const e = this.editor, n = e.model, r = e.editing.view;
    if (this.flushDebounced.cancel(), !this._queue.length)
      return;
    const o = e.commands.get("insertText").buffer;
    n.enqueueChange(o.batch, () => {
      for (o.lock(); this._queue.length; ) {
        const a = this.shift();
        e.execute("insertText", a);
      }
      o.unlock();
    }), r.scrollToTheSelection();
  }
  /**
  * Returns `true` if the given model element is related to recent composition.
  */
  isComposedElement(t) {
    return this._compositionElements.has(t);
  }
  /**
  * Returns an array of composition-related elements and clears the internal list.
  */
  flushComposedElements() {
    const t = Array.from(this._compositionElements);
    return this._compositionElements.clear(), t;
  }
}
function qv(i, t) {
  if (!t.isEnabled)
    return;
  const e = t.buffer;
  e.lock(), i.enqueueChange(e.batch, () => {
    i.deleteContent(i.document.selection);
  }), e.unlock();
}
function WD(i) {
  const t = i.toRange();
  return i.detach(), t.root.rootName == "$graveyard" ? null : t;
}
function UD(i, t) {
  let e = i.is("$text") ? i.parent : i;
  for (; !t.toModelElement(e); )
    e = e.parent;
  return e;
}
class Gv extends Ve {
  /**
  * Creates an instance of the command.
  *
  * @param direction The directionality of the delete describing in what direction it
  * should consume the content when the selection is collapsed.
  */
  constructor(e, n) {
    super(e);
    /**
    * The directionality of the delete describing in what direction it should
    * consume the content when the selection is collapsed.
    */
    h(this, "direction");
    /**
    * Delete's change buffer used to group subsequent changes into batches.
    */
    h(this, "_buffer");
    this.direction = n, this._buffer = new ak(e.model, e.config.get("typing.undoStep")), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * The current change buffer.
  */
  get buffer() {
    return this._buffer;
  }
  /**
  * Executes the delete command. Depending on whether the selection is collapsed or not, deletes its content
  * or a piece of content in the {@link #direction defined direction}.
  *
  * @fires execute
  * @param options The command options.
  * @param options.unit See {@link module:engine/model/utils/modifyselection~modifySelection}'s options.
  * @param options.sequence A number describing which subsequent delete event it is without the key being released.
  * See the {@link module:engine/view/document~Document#event:delete} event data.
  * @param options.selection Selection to remove. If not set, current model selection will be used.
  */
  execute(e = {}) {
    const n = this.editor.model, r = n.document;
    n.enqueueChange(this._buffer.batch, (s) => {
      this._buffer.lock();
      const o = s.createSelection(e.selection || r.selection);
      if (!n.canEditAt(o))
        return;
      const a = e.sequence || 1, l = o.isCollapsed;
      if (o.isCollapsed && n.modifySelection(o, {
        direction: this.direction,
        unit: e.unit,
        treatEmojiAsSingleUnit: !0
      }), this._shouldEntireContentBeReplacedWithParagraph(a)) {
        this._replaceEntireContentWithParagraph(s);
        return;
      }
      if (this._shouldReplaceFirstBlockWithParagraph(o, a)) {
        this.editor.execute("paragraph", {
          selection: o
        });
        return;
      }
      if (o.isCollapsed)
        return;
      let c = 0;
      o.getFirstRange().getMinimalFlatRanges().forEach((u) => {
        c += em(u.getWalker({
          singleCharacters: !0,
          ignoreElementEnd: !0,
          shallow: !0
        }));
      }), n.deleteContent(o, {
        doNotResetEntireContent: l,
        direction: this.direction
      }), this._buffer.input(c), s.setSelection(o), this._buffer.unlock();
    });
  }
  /**
  * If the user keeps <kbd>Backspace</kbd> or <kbd>Delete</kbd> key pressed, the content of the current
  * editable will be cleared. However, this will not yet lead to resetting the remaining block to a paragraph
  * (which happens e.g. when the user does <kbd>Ctrl</kbd> + <kbd>A</kbd>, <kbd>Backspace</kbd>).
  *
  * But, if the user pressed the key in an empty editable for the first time,
  * we want to replace the entire content with a paragraph if:
  *
  * * the current limit element is empty,
  * * the paragraph is allowed in the limit element,
  * * the limit doesn't already have a paragraph inside.
  *
  * See https://github.com/ckeditor/ckeditor5-typing/issues/61.
  *
  * @param sequence A number describing which subsequent delete event it is without the key being released.
  */
  _shouldEntireContentBeReplacedWithParagraph(e) {
    if (e > 1)
      return !1;
    const n = this.editor.model, s = n.document.selection, o = n.schema.getLimitElement(s);
    if (!(s.isCollapsed && s.containsEntireContent(o)) || !n.schema.checkChild(o, "paragraph"))
      return !1;
    const l = o.getChild(0);
    return !(l && l.is("element", "paragraph"));
  }
  /**
  * The entire content is replaced with the paragraph. Selection is moved inside the paragraph.
  *
  * @param writer The model writer.
  */
  _replaceEntireContentWithParagraph(e) {
    const n = this.editor.model, s = n.document.selection, o = n.schema.getLimitElement(s), a = e.createElement("paragraph");
    e.remove(e.createRangeIn(o)), e.insert(a, o), e.setSelection(a, 0);
  }
  /**
  * Checks if the selection is inside an empty element that is the first child of the limit element
  * and should be replaced with a paragraph.
  *
  * @param selection The selection.
  * @param sequence A number describing which subsequent delete event it is without the key being released.
  */
  _shouldReplaceFirstBlockWithParagraph(e, n) {
    const r = this.editor.model;
    if (n > 1 || this.direction != "backward" || !e.isCollapsed)
      return !1;
    const s = e.getFirstPosition(), o = r.schema.getLimitElement(s), a = o.getChild(0);
    return !(s.parent != a || !e.containsEntireContent(a) || !r.schema.checkChild(o, "paragraph") || a.name == "paragraph");
  }
}
const qD = "character", Kv = "word", GD = "codePoint", ts = "selection", Mo = "backward", tl = "forward", uk = {
  // --------------------------------------- Backward delete types -----------------------------------------------------
  // This happens in Safari on Mac when some content is selected and Ctrl + K is pressed.
  deleteContent: {
    unit: ts,
    // According to the Input Events Level 2 spec, this delete type has no direction
    // but to keep things simple, let's default to backward.
    direction: Mo
  },
  // Chrome and Safari on Mac: Backspace or Ctrl + H
  deleteContentBackward: {
    // This kind of deletions must be done on the code point-level instead of target range provided by the DOM beforeinput event.
    // Take for instance "👨‍👩‍👧‍👧", it equals:
    //
    //	* [ "👨", "ZERO WIDTH JOINER", "👩", "ZERO WIDTH JOINER", "👧", "ZERO WIDTH JOINER", "👧" ]
    //	* or simply "\u{1F468}\u200D\u{1F469}\u200D\u{1F467}\u200D\u{1F467}"
    //
    // The range provided by the browser would cause the entire multi-byte grapheme to disappear while the user
    // intention when deleting backwards ("👨‍👩‍👧‍👧[]", then backspace) is gradual "decomposition" (first to "👨‍👩‍👧‍[]",
    // then to "👨‍👩‍[]", etc.).
    //
    //	* "👨‍👩‍👧‍👧[]" + backward delete (by code point)  -> results in "👨‍👩‍👧[]", removed the last "👧" 👍
    //	* "👨‍👩‍👧‍👧[]" + backward delete (by character)  -> results in "[]", removed the whole grapheme 👎
    //
    // Deleting by code-point is simply a better UX. See "deleteContentForward" to learn more.
    unit: GD,
    direction: Mo
  },
  // On Mac: Option + Backspace.
  // On iOS: Hold the backspace for a while and the whole words will start to disappear.
  deleteWordBackward: {
    unit: Kv,
    direction: Mo
  },
  // Safari on Mac: Cmd + Backspace
  deleteHardLineBackward: {
    unit: ts,
    direction: Mo
  },
  // Chrome on Mac: Cmd + Backspace.
  deleteSoftLineBackward: {
    unit: ts,
    direction: Mo
  },
  // --------------------------------------- Forward delete types -----------------------------------------------------
  // Chrome on Mac: Fn + Backspace or Ctrl + D
  // Safari on Mac: Ctrl + K or Ctrl + D
  deleteContentForward: {
    // Unlike backward delete, this delete must be performed by character instead of by code point, which
    // provides the best UX for working with accented letters.
    // Take, for example "b̂" ("\u0062\u0302", or [ "LATIN SMALL LETTER B", "COMBINING CIRCUMFLEX ACCENT" ]):
    //
    //	* "b̂[]" + backward delete (by code point)  -> results in "b[]", removed the combining mark 👍
    //	* "[]b̂" + forward delete (by code point)   -> results in "[]^", a bare combining mark does that not make sense when alone 👎
    //	* "[]b̂" + forward delete (by character)    -> results in "[]", removed both "b" and the combining mark 👍
    //
    // See: "deleteContentBackward" to learn more.
    unit: qD,
    direction: tl
  },
  // On Mac: Fn + Option + Backspace.
  deleteWordForward: {
    unit: Kv,
    direction: tl
  },
  // Chrome on Mac: Ctrl + K (you have to disable the Link plugin first, though, because it uses the same keystroke)
  // This is weird that it does not work in Safari on Mac despite being listed in the official shortcuts listing
  // on Apple's webpage.
  deleteHardLineForward: {
    unit: ts,
    direction: tl
  },
  // At this moment there is no known way to trigger this event type but let's keep it for the symmetry with
  // deleteSoftLineBackward.
  deleteSoftLineForward: {
    unit: ts,
    direction: tl
  }
};
class KD extends nr {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = t.document;
    let n = 0;
    e.on("keydown", () => {
      n++;
    }), e.on("keyup", () => {
      n = 0;
    }), e.on("beforeinput", (r, s) => {
      if (!this.isEnabled)
        return;
      const { targetRanges: o, domEvent: a, inputType: l } = s, c = uk[l];
      if (!c)
        return;
      const u = {
        direction: c.direction,
        unit: c.unit,
        sequence: n
      };
      u.unit == ts && (u.selectionToRemove = t.createSelection(o[0])), l === "deleteContentBackward" && (Me.isAndroid && (u.sequence = 1), YD(o) && (u.unit = ts, u.selectionToRemove = t.createSelection(o)));
      const d = new ga(e, "delete", o[0]);
      e.fire(d, new Qo(t, a, u)), d.stop.called && r.stop();
    }), Me.isBlink && ZD(this);
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
function ZD(i) {
  const t = i.view, e = t.document;
  let n = null, r = !1;
  e.on("keydown", (a, { keyCode: l }) => {
    n = l, r = !1;
  }), e.on("keyup", (a, { keyCode: l, domEvent: c }) => {
    const u = e.selection, d = i.isEnabled && l == n && s(l) && !u.isCollapsed && !r;
    if (n = null, d) {
      const f = u.getFirstRange(), m = new ga(e, "delete", f), g = {
        unit: ts,
        direction: o(l),
        selectionToRemove: u
      };
      e.fire(m, new Qo(t, c, g));
    }
  }), e.on("beforeinput", (a, { inputType: l }) => {
    const c = uk[l];
    s(n) && c && c.direction == o(n) && (r = !0);
  }, {
    priority: "high"
  }), e.on("beforeinput", (a, { inputType: l, data: c }) => {
    n == Ke.delete && l == "insertText" && c == "" && a.stop();
  }, {
    priority: "high"
  });
  function s(a) {
    return a == Ke.backspace || a == Ke.delete;
  }
  function o(a) {
    return a == Ke.backspace ? Mo : tl;
  }
}
function YD(i) {
  if (i.length != 1 || i[0].isCollapsed)
    return !1;
  const t = i[0].getWalker({
    direction: "backward",
    singleCharacters: !0,
    ignoreElementEnd: !0
  });
  let e = 0;
  for (const { nextPosition: n, item: r } of t) {
    if (n.parent.is("$text")) {
      const s = n.parent.data, o = n.offset;
      if (Jg(s, o) || Xg(s, o) || O2(s, o))
        continue;
      e++;
    } else (r.is("containerElement") || r.is("emptyElement")) && e++;
    if (e > 1)
      return !0;
  }
  return !1;
}
class Fr extends V {
  constructor() {
    super(...arguments);
    /**
    * Whether pressing backspace should trigger undo action
    */
    h(this, "_undoOnBackspace");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Delete";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.editing.view, r = n.document, s = e.model.document;
    n.addObserver(KD), this._undoOnBackspace = !1;
    const o = new Gv(e, "forward");
    e.commands.add("deleteForward", o), e.commands.add("forwardDelete", o), e.commands.add("delete", new Gv(e, "backward")), this.listenTo(r, "delete", (a, l) => {
      r.isComposing || l.preventDefault();
      const { direction: c, sequence: u, selectionToRemove: d, unit: f } = l, m = c === "forward" ? "deleteForward" : "delete", g = {
        sequence: u
      };
      if (f == "selection") {
        const p = Array.from(d.getRanges()).map((b) => e.editing.mapper.toModelRange(b));
        g.selection = e.model.createSelection(p);
      } else
        g.unit = f;
      e.execute(m, g), n.scrollToTheSelection();
    }, {
      priority: "low"
    }), this.editor.plugins.has("UndoEditing") && (this.listenTo(r, "delete", (a, l) => {
      this._undoOnBackspace && l.direction == "backward" && l.sequence == 1 && l.unit == "codePoint" && (this._undoOnBackspace = !1, e.execute("undo"), l.preventDefault(), a.stop());
    }, {
      context: "$capture"
    }), this.listenTo(s, "change", () => {
      this._undoOnBackspace = !1;
    }));
  }
  /**
  * If the next user action after calling this method is pressing backspace, it would undo the last change.
  *
  * Requires {@link module:undo/undoediting~UndoEditing} plugin. If not loaded, does nothing.
  */
  requestUndoOnBackspace() {
    this.editor.plugins.has("UndoEditing") && (this._undoOnBackspace = !0);
  }
}
class JD extends V {
  static get requires() {
    return [
      ck,
      Fr
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Typing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function dk(i, t) {
  let e = i.start;
  return {
    text: Array.from(i.getWalker({
      ignoreElementEnd: !1
    })).reduce((r, { item: s }) => s.is("$text") || s.is("$textProxy") ? r + s.data : (e = t.createPositionAfter(s), ""), ""),
    range: t.createRange(e, i.end)
  };
}
class hk extends (/* @__PURE__ */ st()) {
  /**
  * Creates a text watcher instance.
  *
  * @param testCallback See {@link module:typing/textwatcher~TextWatcher#testCallback}.
  */
  constructor(e, n) {
    super();
    /**
    * The editor's model.
    */
    h(this, "model");
    /**
    * The function used to match the text.
    *
    * The test callback can return 3 values:
    *
    * * `false` if there is no match,
    * * `true` if there is a match,
    * * an object if there is a match and we want to pass some additional information to the {@link #event:matched:data} event.
    */
    h(this, "testCallback");
    /**
    * Whether there is a match currently.
    */
    h(this, "_hasMatch");
    this.model = e, this.testCallback = n, this._hasMatch = !1, this.set("isEnabled", !0), this.on("change:isEnabled", () => {
      this.isEnabled ? this._startListening() : (this.stopListening(e.document.selection), this.stopListening(e.document));
    }), this._startListening();
  }
  /**
  * Flag indicating whether there is a match currently.
  */
  get hasMatch() {
    return this._hasMatch;
  }
  /**
  * Starts listening to the editor for typing and selection events.
  */
  _startListening() {
    const n = this.model.document;
    this.listenTo(n.selection, "change:range", (r, { directChange: s }) => {
      if (s) {
        if (!n.selection.isCollapsed) {
          this.hasMatch && (this.fire("unmatched"), this._hasMatch = !1);
          return;
        }
        this._evaluateTextBeforeSelection("selection");
      }
    }), this.listenTo(n, "change:data", (r, s) => {
      s.isUndo || !s.isLocal || this._evaluateTextBeforeSelection("data", {
        batch: s
      });
    });
  }
  /**
  * Checks the editor content for matched text.
  *
  * @fires matched:data
  * @fires matched:selection
  * @fires unmatched
  *
  * @param suffix A suffix used for generating the event name.
  * @param data Data object for event.
  */
  _evaluateTextBeforeSelection(e, n = {}) {
    const r = this.model, o = r.document.selection, a = r.createRange(r.createPositionAt(o.focus.parent, 0), o.focus), { text: l, range: c } = dk(a, r), u = this.testCallback(l);
    if (!u && this.hasMatch && this.fire("unmatched"), this._hasMatch = !!u, u) {
      const d = Object.assign(n, {
        text: l,
        range: c
      });
      typeof u == "object" && Object.assign(d, u), this.fire(`matched:${e}`, d);
    }
  }
}
class Gu extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A set of attributes to handle.
    */
    h(this, "attributes");
    /**
    * The current UID of the overridden gravity, as returned by
    * {@link module:engine/model/writer~Writer#overrideSelectionGravity}.
    */
    h(this, "_overrideUid");
    /**
    * A flag indicating that the automatic gravity restoration should not happen upon the next
    * gravity restoration.
    * {@link module:engine/model/selection~Selection#event:change:range} event.
    */
    h(this, "_isNextGravityRestorationSkipped", !1);
    this.attributes = /* @__PURE__ */ new Set(), this._overrideUid = null;
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TwoStepCaretMovement";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model, r = e.editing.view, s = e.locale, o = n.document.selection;
    this.listenTo(r.document, "arrowKey", (a, l) => {
      if (!o.isCollapsed || l.shiftKey || l.altKey || l.ctrlKey)
        return;
      const c = l.keyCode == Ke.arrowright, u = l.keyCode == Ke.arrowleft;
      if (!c && !u)
        return;
      const d = s.contentLanguageDirection;
      let f = !1;
      d === "ltr" && c || d === "rtl" && u ? f = this._handleForwardMovement(l) : f = this._handleBackwardMovement(l), f === !0 && a.stop();
    }, {
      context: "$text",
      priority: "highest"
    }), this.listenTo(o, "change:range", (a, l) => {
      if (this._isNextGravityRestorationSkipped) {
        this._isNextGravityRestorationSkipped = !1;
        return;
      }
      this._isGravityOverridden && (!l.directChange && Pi(o.getFirstPosition(), this.attributes) || this._restoreGravity());
    }), this._enableClickingAfterNode(), this._enableInsertContentSelectionAttributesFixer(), this._handleDeleteContentAfterNode();
  }
  /**
  * Registers a given attribute for the two-step caret movement.
  *
  * @param attribute Name of the attribute to handle.
  */
  registerAttribute(e) {
    this.attributes.add(e);
  }
  /**
  * Updates the document selection and the view according to the two–step caret movement state
  * when moving **forwards**. Executed upon `keypress` in the {@link module:engine/view/view~View}.
  *
  * @param data Data of the key press.
  * @returns `true` when the handler prevented caret movement.
  */
  _handleForwardMovement(e) {
    const n = this.attributes, r = this.editor.model, s = r.document.selection, o = s.getFirstPosition();
    return this._isGravityOverridden || o.isAtStart && Er(s, n) ? !1 : Pi(o, n) ? (Ha(e), Er(s, n) && Pi(o, n, !0) ? za(r, n) : this._overrideGravity(), !0) : !1;
  }
  /**
  * Updates the document selection and the view according to the two–step caret movement state
  * when moving **backwards**. Executed upon `keypress` in the {@link module:engine/view/view~View}.
  *
  * @param data Data of the key press.
  * @returns `true` when the handler prevented caret movement
  */
  _handleBackwardMovement(e) {
    const n = this.attributes, r = this.editor.model, s = r.document.selection, o = s.getFirstPosition();
    return this._isGravityOverridden ? (Ha(e), this._restoreGravity(), Pi(o, n, !0) ? za(r, n) : Wc(r, n, o), !0) : o.isAtStart ? Er(s, n) ? (Ha(e), Wc(r, n, o), !0) : !1 : !Er(s, n) && Pi(o, n, !0) ? (Ha(e), Wc(r, n, o), !0) : Zv(o, n) ? o.isAtEnd && !Er(s, n) && Pi(o, n) ? (Ha(e), Wc(r, n, o), !0) : (this._isNextGravityRestorationSkipped = !0, this._overrideGravity(), !1) : !1;
  }
  /**
  * Starts listening to {@link module:engine/view/document~Document#event:mousedown} and
  * {@link module:engine/view/document~Document#event:selectionChange} and puts the selection before/after a 2-step node
  * if clicked at the beginning/ending of the 2-step node.
  *
  * The purpose of this action is to allow typing around the 2-step node directly after a click.
  *
  * See https://github.com/ckeditor/ckeditor5/issues/1016.
  */
  _enableClickingAfterNode() {
    const e = this.editor, n = e.model, r = n.document.selection, s = e.editing.view.document;
    e.editing.view.addObserver(Hd);
    let o = !1;
    this.listenTo(s, "mousedown", () => {
      o = !0;
    }), this.listenTo(s, "selectionChange", () => {
      const a = this.attributes;
      if (!o || (o = !1, !r.isCollapsed) || !Er(r, a))
        return;
      const l = r.getFirstPosition();
      Pi(l, a) && (l.isAtStart || Pi(l, a, !0) ? za(n, a) : this._isGravityOverridden || this._overrideGravity());
    });
  }
  /**
  * Starts listening to {@link module:engine/model/model~Model#event:insertContent} and corrects the model
  * selection attributes if the selection is at the end of a two-step node after inserting the content.
  *
  * The purpose of this action is to improve the overall UX because the user is no longer "trapped" by the
  * two-step attribute of the selection, and they can type a "clean" (`linkHref`–less) text right away.
  *
  * See https://github.com/ckeditor/ckeditor5/issues/6053.
  */
  _enableInsertContentSelectionAttributesFixer() {
    const n = this.editor.model, r = n.document.selection, s = this.attributes;
    this.listenTo(n, "insertContent", () => {
      const o = r.getFirstPosition();
      Er(r, s) && Pi(o, s) && za(n, s);
    }, {
      priority: "low"
    });
  }
  /**
  * Starts listening to {@link module:engine/model/model~Model#deleteContent} and checks whether
  * removing a content right after the tow-step attribute.
  *
  * If so, the selection should not preserve the two-step attribute. However, if
  * the {@link module:typing/twostepcaretmovement~TwoStepCaretMovement} plugin is active and
  * the selection has the two-step attribute due to overridden gravity (at the end), the two-step attribute should stay untouched.
  *
  * The purpose of this action is to allow removing the link text and keep the selection outside the link.
  *
  * See https://github.com/ckeditor/ckeditor5/issues/7521.
  */
  _handleDeleteContentAfterNode() {
    const e = this.editor, n = e.model, r = n.document.selection, s = e.editing.view;
    let o = !1, a = !1;
    this.listenTo(s.document, "delete", (l, c) => {
      o = c.direction === "backward";
    }, {
      priority: "high"
    }), this.listenTo(n, "deleteContent", () => {
      if (!o)
        return;
      const l = r.getFirstPosition();
      a = Er(r, this.attributes) && !Zv(l, this.attributes);
    }, {
      priority: "high"
    }), this.listenTo(n, "deleteContent", () => {
      o && (o = !1, !a && e.model.enqueueChange(() => {
        const l = r.getFirstPosition();
        Er(r, this.attributes) && Pi(l, this.attributes) && (l.isAtStart || Pi(l, this.attributes, !0) ? za(n, this.attributes) : this._isGravityOverridden || this._overrideGravity());
      }));
    }, {
      priority: "low"
    });
  }
  /**
  * `true` when the gravity is overridden for the plugin.
  */
  get _isGravityOverridden() {
    return !!this._overrideUid;
  }
  /**
  * Overrides the gravity using the {@link module:engine/model/writer~Writer model writer}
  * and stores the information about this fact in the {@link #_overrideUid}.
  *
  * A shorthand for {@link module:engine/model/writer~Writer#overrideSelectionGravity}.
  */
  _overrideGravity() {
    this._overrideUid = this.editor.model.change((e) => e.overrideSelectionGravity());
  }
  /**
  * Restores the gravity using the {@link module:engine/model/writer~Writer model writer}.
  *
  * A shorthand for {@link module:engine/model/writer~Writer#restoreSelectionGravity}.
  */
  _restoreGravity() {
    this.editor.model.change((e) => {
      e.restoreSelectionGravity(this._overrideUid), this._overrideUid = null;
    });
  }
}
function Er(i, t) {
  for (const e of t)
    if (i.hasAttribute(e))
      return !0;
  return !1;
}
function Wc(i, t, e) {
  const n = e.nodeBefore;
  i.change((r) => {
    if (n) {
      const s = [], o = i.schema.isObject(n) && i.schema.isInline(n);
      for (const [a, l] of n.getAttributes())
        i.schema.checkAttribute("$text", a) && (!o || i.schema.getAttributeProperties(a).copyFromObject !== !1) && s.push([
          a,
          l
        ]);
      r.setSelectionAttribute(s);
    } else
      r.removeSelectionAttribute(t);
  });
}
function za(i, t) {
  i.change((e) => {
    e.removeSelectionAttribute(t);
  });
}
function Ha(i) {
  i.preventDefault();
}
function Zv(i, t) {
  const e = i.getShiftedBy(-1);
  return Pi(e, t);
}
function Pi(i, t, e = !1) {
  const { nodeBefore: n, nodeAfter: r } = i;
  for (const s of t) {
    const o = n ? n.getAttribute(s) : void 0, a = r ? r.getAttribute(s) : void 0;
    if (!(e && (o === void 0 || a === void 0)) && a !== o)
      return !0;
  }
  return !1;
}
const Yv = {
  // Common symbols:
  copyright: {
    from: "(c)",
    to: "©"
  },
  registeredTrademark: {
    from: "(r)",
    to: "®"
  },
  trademark: {
    from: "(tm)",
    to: "™"
  },
  // Mathematical:
  oneHalf: {
    from: /(^|[^/a-z0-9])(1\/2)([^/a-z0-9])$/i,
    to: [
      null,
      "½",
      null
    ]
  },
  oneThird: {
    from: /(^|[^/a-z0-9])(1\/3)([^/a-z0-9])$/i,
    to: [
      null,
      "⅓",
      null
    ]
  },
  twoThirds: {
    from: /(^|[^/a-z0-9])(2\/3)([^/a-z0-9])$/i,
    to: [
      null,
      "⅔",
      null
    ]
  },
  oneForth: {
    from: /(^|[^/a-z0-9])(1\/4)([^/a-z0-9])$/i,
    to: [
      null,
      "¼",
      null
    ]
  },
  threeQuarters: {
    from: /(^|[^/a-z0-9])(3\/4)([^/a-z0-9])$/i,
    to: [
      null,
      "¾",
      null
    ]
  },
  lessThanOrEqual: {
    from: "<=",
    to: "≤"
  },
  greaterThanOrEqual: {
    from: ">=",
    to: "≥"
  },
  notEqual: {
    from: "!=",
    to: "≠"
  },
  arrowLeft: {
    from: "<-",
    to: "←"
  },
  arrowRight: {
    from: "->",
    to: "→"
  },
  // Typography:
  horizontalEllipsis: {
    from: "...",
    to: "…"
  },
  enDash: {
    from: /(^| )(--)( )$/,
    to: [
      null,
      "–",
      null
    ]
  },
  emDash: {
    from: /(^| )(---)( )$/,
    to: [
      null,
      "—",
      null
    ]
  },
  // Quotations:
  // English, US
  quotesPrimary: {
    from: wo('"'),
    to: [
      null,
      "“",
      null,
      "”"
    ]
  },
  quotesSecondary: {
    from: wo("'"),
    to: [
      null,
      "‘",
      null,
      "’"
    ]
  },
  // English, UK
  quotesPrimaryEnGb: {
    from: wo("'"),
    to: [
      null,
      "‘",
      null,
      "’"
    ]
  },
  quotesSecondaryEnGb: {
    from: wo('"'),
    to: [
      null,
      "“",
      null,
      "”"
    ]
  },
  // Polish
  quotesPrimaryPl: {
    from: wo('"'),
    to: [
      null,
      "„",
      null,
      "”"
    ]
  },
  quotesSecondaryPl: {
    from: wo("'"),
    to: [
      null,
      "‚",
      null,
      "’"
    ]
  }
}, Jv = {
  symbols: [
    "copyright",
    "registeredTrademark",
    "trademark"
  ],
  mathematical: [
    "oneHalf",
    "oneThird",
    "twoThirds",
    "oneForth",
    "threeQuarters",
    "lessThanOrEqual",
    "greaterThanOrEqual",
    "notEqual",
    "arrowLeft",
    "arrowRight"
  ],
  typography: [
    "horizontalEllipsis",
    "enDash",
    "emDash"
  ],
  quotes: [
    "quotesPrimary",
    "quotesSecondary"
  ]
}, XD = [
  "symbols",
  "mathematical",
  "typography",
  "quotes"
];
class QD extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      "Delete",
      "Input"
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TextTransformation";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("typing", {
      transformations: {
        include: XD
      }
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor.model.document.selection;
    e.on("change:range", () => {
      this.isEnabled = !e.anchor.parent.is("element", "codeBlock");
    }), this._enableTransformationWatchers();
  }
  /**
  * Create new TextWatcher listening to the editor for typing and selection events.
  */
  _enableTransformationWatchers() {
    const t = this.editor, e = t.model, n = t.plugins.get("Delete"), r = iL(t.config.get("typing.transformations")), s = (a) => {
      for (const l of r)
        if (l.from.test(a))
          return {
            normalizedTransformation: l
          };
    }, o = new hk(t.model, s);
    o.on("matched:data", (a, l) => {
      if (!l.batch.isTyping)
        return;
      const { from: c, to: u } = l.normalizedTransformation, d = c.exec(l.text), f = u(d.slice(1)), m = l.range;
      let g = d.index;
      e.enqueueChange((p) => {
        for (let b = 1; b < d.length; b++) {
          const v = d[b], y = f[b - 1];
          if (y == null) {
            g += v.length;
            continue;
          }
          const x = m.start.getShiftedBy(g), S = e.createRange(x, x.getShiftedBy(v.length)), k = nL(x);
          e.insertContent(p.createText(y, k), S), g += y.length;
        }
        e.enqueueChange(() => {
          n.requestUndoOnBackspace();
        });
      });
    }), o.bind("isEnabled").to(this);
  }
}
function eL(i) {
  return typeof i == "string" ? new RegExp(`(${s2(i)})$`) : i;
}
function tL(i) {
  return typeof i == "string" ? () => [
    i
  ] : i instanceof Array ? () => i : i;
}
function nL(i) {
  return (i.textNode ? i.textNode : i.nodeAfter).getAttributes();
}
function wo(i) {
  return new RegExp(`(^|\\s)(${i})([^${i}]*)(${i})$`);
}
function iL(i) {
  const t = i.extra || [], e = i.remove || [], n = (s) => !e.includes(s), r = i.include.concat(t).filter(n);
  return rL(r).filter(n).map((s) => typeof s == "string" && Yv[s] ? Yv[s] : s).filter((s) => typeof s == "object").map((s) => ({
    from: eL(s.from),
    to: tL(s.to)
  }));
}
function rL(i) {
  const t = /* @__PURE__ */ new Set();
  for (const e of i)
    if (typeof e == "string" && Jv[e])
      for (const n of Jv[e])
        t.add(n);
    else
      t.add(e);
  return Array.from(t);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Yd(i, t, e, n) {
  return n.createRange(Xv(i, t, e, !0, n), Xv(i, t, e, !1, n));
}
function Xv(i, t, e, n, r) {
  let s = i.textNode || (n ? i.nodeBefore : i.nodeAfter), o = null;
  for (; s && s.getAttribute(t) == e; )
    o = s, s = n ? s.previousSibling : s.nextSibling;
  return o ? r.createPositionAt(o, n ? "before" : "after") : i;
}
function fk(i, t, e, n) {
  const r = i.editing.view, s = /* @__PURE__ */ new Set();
  r.document.registerPostFixer((o) => {
    const a = i.model.document.selection;
    let l = !1;
    if (a.hasAttribute(t)) {
      const c = Yd(a.getFirstPosition(), t, a.getAttribute(t), i.model), u = i.editing.mapper.toViewRange(c);
      for (const d of u.getItems())
        d.is("element", e) && !d.hasClass(n) && (o.addClass(n, d), s.add(d), l = !0);
    }
    return l;
  }), i.conversion.for("editingDowncast").add((o) => {
    o.on("insert", a, {
      priority: "highest"
    }), o.on("remove", a, {
      priority: "highest"
    }), o.on("attribute", a, {
      priority: "highest"
    }), o.on("selection", a, {
      priority: "highest"
    });
    function a() {
      r.change((l) => {
        for (const c of s.values())
          l.removeClass(n, c), s.delete(c);
      });
    }
  });
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Zr(i, t, e, n) {
  let r, s = null;
  typeof n == "function" ? r = n : (s = i.commands.get(n), r = () => {
    i.execute(n);
  }), i.model.document.on("change:data", (o, a) => {
    if (s && !s.isEnabled || !t.isEnabled)
      return;
    const l = Ft(i.model.document.selection.getRanges());
    if (!l.isCollapsed || a.isUndo || !a.isLocal)
      return;
    const c = Array.from(i.model.document.differ.getChanges()), u = c[0];
    if (c.length != 1 || u.type !== "insert" || u.name != "$text" || u.length != 1)
      return;
    const d = u.position.parent;
    if (d.is("element", "codeBlock") || d.is("element", "listItem") && typeof n != "function" && ![
      "numberedList",
      "bulletedList",
      "todoList"
    ].includes(n) || s && s.value === !0)
      return;
    const f = d.getChild(0), m = i.model.createRangeOn(f);
    if (!m.containsRange(l) && !l.end.isEqual(m.end))
      return;
    const g = e.exec(f.data.substr(0, l.end.offset));
    g && i.model.enqueueChange((p) => {
      const b = p.createPositionAt(d, 0), v = p.createPositionAt(d, g[0].length), y = new Kn(b, v);
      if (r({
        match: g
      }) !== !1) {
        p.remove(y);
        const S = i.model.document.selection.getFirstRange(), k = p.createRangeIn(d);
        d.isEmpty && !k.isEqual(S) && !k.containsRange(S, !0) && p.remove(d);
      }
      y.detach(), i.model.enqueueChange(() => {
        i.plugins.get("Delete").requestUndoOnBackspace();
      });
    });
  });
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function yo(i, t, e, n) {
  let r, s;
  e instanceof RegExp ? r = e : s = e, s = s || ((o) => {
    let a;
    const l = [], c = [];
    for (; (a = r.exec(o)) !== null && !(a && a.length < 4); ) {
      let { index: u, "1": d, "2": f, "3": m } = a;
      const g = d + f + m;
      u += a[0].length - g.length;
      const p = [
        u,
        u + d.length
      ], b = [
        u + d.length + f.length,
        u + d.length + f.length + m.length
      ];
      l.push(p), l.push(b), c.push([
        u + d.length,
        u + d.length + f.length
      ]);
    }
    return {
      remove: l,
      format: c
    };
  }), i.model.document.on("change:data", (o, a) => {
    if (a.isUndo || !a.isLocal || !t.isEnabled)
      return;
    const l = i.model, c = l.document.selection;
    if (!c.isCollapsed)
      return;
    const u = Array.from(l.document.differ.getChanges()), d = u[0];
    if (u.length != 1 || d.type !== "insert" || d.name != "$text" || d.length != 1)
      return;
    const f = c.focus, m = f.parent, { text: g, range: p } = sL(l.createRange(l.createPositionAt(m, 0), f), l), b = s(g), v = Qv(p.start, b.format, l), y = Qv(p.start, b.remove, l);
    v.length && y.length && l.enqueueChange((x) => {
      if (n(x, v) !== !1) {
        for (const k of y.reverse())
          x.remove(k);
        l.enqueueChange(() => {
          i.plugins.get("Delete").requestUndoOnBackspace();
        });
      }
    });
  });
}
function Qv(i, t, e) {
  return t.filter((n) => n[0] !== void 0 && n[1] !== void 0).map((n) => e.createRange(i.getShiftedBy(n[0]), i.getShiftedBy(n[1])));
}
function sL(i, t) {
  let e = i.start;
  return {
    text: Array.from(i.getItems()).reduce((r, s) => !(s.is("$text") || s.is("$textProxy")) || s.getAttribute("code") ? (e = t.createPositionAfter(s), "") : r + s.data, ""),
    range: t.createRange(e, i.end)
  };
}
class oL extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Fr
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Autoformat";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = this.editor.t;
    this._addListAutoformats(), this._addBasicStylesAutoformats(), this._addHeadingAutoformats(), this._addBlockQuoteAutoformats(), this._addCodeBlockAutoformats(), this._addHorizontalLineAutoformats(), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Revert autoformatting action"),
          keystroke: "Backspace"
        }
      ]
    });
  }
  /**
  * Adds autoformatting related to the {@link module:list/list~List}.
  *
  * When typed:
  * - `* ` or `- ` &ndash; A paragraph will be changed into a bulleted list.
  * - `1. ` or `1) ` &ndash; A paragraph will be changed into a numbered list ("1" can be any digit or a list of digits).
  * - `[] ` or `[ ] ` &ndash; A paragraph will be changed into a to-do list.
  * - `[x] ` or `[ x ] ` &ndash; A paragraph will be changed into a checked to-do list.
  */
  _addListAutoformats() {
    const t = this.editor.commands;
    t.get("bulletedList") && Zr(this.editor, this, /^[*-]\s$/, "bulletedList"), t.get("numberedList") && Zr(this.editor, this, /^1[.|)]\s$/, "numberedList"), t.get("todoList") && Zr(this.editor, this, /^\[\s?\]\s$/, "todoList"), t.get("checkTodoList") && Zr(this.editor, this, /^\[\s?x\s?\]\s$/, () => {
      this.editor.execute("todoList"), this.editor.execute("checkTodoList");
    });
  }
  /**
  * Adds autoformatting related to the {@link module:basic-styles/bold~Bold},
  * {@link module:basic-styles/italic~Italic}, {@link module:basic-styles/code~Code}
  * and {@link module:basic-styles/strikethrough~Strikethrough}
  *
  * When typed:
  * - `**foobar**` &ndash; `**` characters are removed and `foobar` is set to bold,
  * - `__foobar__` &ndash; `__` characters are removed and `foobar` is set to bold,
  * - `*foobar*` &ndash; `*` characters are removed and `foobar` is set to italic,
  * - `_foobar_` &ndash; `_` characters are removed and `foobar` is set to italic,
  * - ``` `foobar` &ndash; ``` ` ``` characters are removed and `foobar` is set to code,
  * - `~~foobar~~` &ndash; `~~` characters are removed and `foobar` is set to strikethrough.
  */
  _addBasicStylesAutoformats() {
    const t = this.editor.commands;
    if (t.get("bold")) {
      const e = Uc(this.editor, "bold");
      yo(this.editor, this, /(?:^|\s)(\*\*)([^*]+)(\*\*)$/g, e), yo(this.editor, this, /(?:^|\s)(__)([^_]+)(__)$/g, e);
    }
    if (t.get("italic")) {
      const e = Uc(this.editor, "italic");
      yo(this.editor, this, /(?:^|\s)(\*)([^*_]+)(\*)$/g, e), yo(this.editor, this, /(?:^|\s)(_)([^_]+)(_)$/g, e);
    }
    if (t.get("code")) {
      const e = Uc(this.editor, "code");
      yo(this.editor, this, /(`)([^`]+)(`)$/g, e);
    }
    if (t.get("strikethrough")) {
      const e = Uc(this.editor, "strikethrough");
      yo(this.editor, this, /(~~)([^~]+)(~~)$/g, e);
    }
  }
  /**
  * Adds autoformatting related to {@link module:heading/heading~Heading}.
  *
  * It is using a number at the end of the command name to associate it with the proper trigger:
  *
  * * `heading` with a `heading1` value will be executed when typing `#`,
  * * `heading` with a `heading2` value will be executed when typing `##`,
  * * ... up to `heading6` for `######`.
  */
  _addHeadingAutoformats() {
    const t = this.editor.commands.get("heading");
    t && t.modelElements.filter((e) => e.match(/^heading[1-6]$/)).forEach((e) => {
      const n = e[7], r = new RegExp(`^(#{${n}})\\s$`);
      Zr(this.editor, this, r, () => {
        if (!t.isEnabled || t.value === e)
          return !1;
        this.editor.execute("heading", {
          value: e
        });
      });
    });
  }
  /**
  * Adds autoformatting related to {@link module:block-quote/blockquote~BlockQuote}.
  *
  * When typed:
  * * `> ` &ndash; A paragraph will be changed to a block quote.
  */
  _addBlockQuoteAutoformats() {
    this.editor.commands.get("blockQuote") && Zr(this.editor, this, /^>\s$/, "blockQuote");
  }
  /**
  * Adds autoformatting related to {@link module:code-block/codeblock~CodeBlock}.
  *
  * When typed:
  * - `` ``` `` &ndash; A paragraph will be changed to a code block.
  */
  _addCodeBlockAutoformats() {
    const t = this.editor, e = t.model.document.selection;
    t.commands.get("codeBlock") && Zr(t, this, /^```$/, () => {
      if (e.getFirstPosition().parent.is("element", "listItem"))
        return !1;
      this.editor.execute("codeBlock", {
        usePreviousLanguageChoice: !0
      });
    });
  }
  /**
  * Adds autoformatting related to {@link module:horizontal-line/horizontalline~HorizontalLine}.
  *
  * When typed:
  * - `` --- `` &ndash; Will be replaced with a horizontal line.
  */
  _addHorizontalLineAutoformats() {
    this.editor.commands.get("horizontalLine") && Zr(this.editor, this, /^---$/, "horizontalLine");
  }
}
function Uc(i, t) {
  return (e, n) => {
    if (!i.commands.get(t).isEnabled)
      return !1;
    const s = i.model.schema.getValidRanges(n, t);
    for (const o of s)
      e.setAttribute(t, !0, o);
    e.removeSelectionAttribute(t);
  };
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class aL extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The adapter is an object with a `save()` method. That method will be called whenever
    * the data changes. It might be called some time after the change,
    * since the event is throttled for performance reasons.
    */
    h(this, "adapter");
    /**
    * Debounced save method. The `save()` method is called the specified `waitingTime` after `debouncedSave()` is called,
    * unless a new action happens in the meantime.
    */
    h(this, "_debouncedSave");
    /**
    * The last saved document version.
    */
    h(this, "_lastDocumentVersion");
    /**
    * Promise used for asynchronous save calls.
    *
    * Created to handle the autosave call to an external data source. It resolves when that call is finished. It is re-used if
    * save is called before the promise has been resolved. It is set to `null` if there is no call in progress.
    */
    h(this, "_savePromise");
    /**
    * DOM emitter.
    */
    h(this, "_domEmitter");
    /**
    * The configuration of this plugins.
    */
    h(this, "_config");
    /**
    * Editor's pending actions manager.
    */
    h(this, "_pendingActions");
    /**
    * Informs whether there should be another autosave callback performed, immediately after current autosave callback finishes.
    *
    * This is set to `true` when there is a save request while autosave callback is already being processed
    * and the model has changed since the last save.
    */
    h(this, "_makeImmediateSave");
    /**
    * An action that will be added to the pending action manager for actions happening in that plugin.
    */
    h(this, "_action", null);
    const n = e.config.get("autosave") || {}, r = n.waitingTime || 1e3;
    this.set("state", "synchronized"), this._debouncedSave = Ci(this._save.bind(this), r), this._lastDocumentVersion = e.model.document.version, this._savePromise = null, this._domEmitter = new (Xn())(), this._config = n, this._pendingActions = e.plugins.get(ea), this._makeImmediateSave = !1;
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Autosave";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ea
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.document;
    this.listenTo(e, "ready", () => {
      this.listenTo(n, "change:data", (r, s) => {
        this._saveCallbacks.length && s.isLocal && (this.state === "synchronized" && (this.state = "waiting", this._setPendingAction()), this.state === "waiting" && this._debouncedSave());
      });
    }), this.listenTo(e, "destroy", () => this._flush(), {
      priority: "highest"
    });
    /* istanbul ignore next -- @preserve */
    this._domEmitter.listenTo(window, "beforeunload", (r, s) => {
      this._pendingActions.hasAny && (s.returnValue = this._pendingActions.first.message);
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._domEmitter.stopListening(), super.destroy();
  }
  /**
  * Immediately calls autosave callback. All previously queued (debounced) callbacks are cleared. If there is already an autosave
  * callback in progress, then the requested save will be performed immediately after the current callback finishes.
  *
  * @returns A promise that will be resolved when the autosave callback is finished.
  */
  save() {
    return this._debouncedSave.cancel(), this._save();
  }
  /**
  * Invokes the remaining `_save()` method call.
  */
  _flush() {
    this._debouncedSave.flush();
  }
  /**
  * If the adapter is set and a new document version exists,
  * the `_save()` method creates a pending action and calls the `adapter.save()` method.
  * It waits for the result and then removes the created pending action.
  *
  * @returns A promise that will be resolved when the autosave callback is finished.
  */
  _save() {
    return this._savePromise ? (this._makeImmediateSave = this.editor.model.document.version > this._lastDocumentVersion, this._savePromise) : (this._setPendingAction(), this.state = "saving", this._lastDocumentVersion = this.editor.model.document.version, this._savePromise = Promise.resolve().then(() => Promise.all(this._saveCallbacks.map((e) => e(this.editor)))).finally(() => {
      this._savePromise = null;
    }).then(() => {
      if (this._makeImmediateSave)
        return this._makeImmediateSave = !1, this._save();
      this.editor.model.document.version > this._lastDocumentVersion ? (this.state = "waiting", this._debouncedSave()) : (this.state = "synchronized", this._pendingActions.remove(this._action), this._action = null);
    }).catch((e) => {
      throw this.state = "error", this.state = "saving", this._debouncedSave(), e;
    }), this._savePromise);
  }
  /**
  * Creates a pending action if it is not set already.
  */
  _setPendingAction() {
    const e = this.editor.t;
    this._action || (this._action = this._pendingActions.add(e("Saving changes")));
  }
  /**
  * Saves callbacks.
  */
  get _saveCallbacks() {
    const e = [];
    return this.adapter && this.adapter.save && e.push(this.adapter.save), this._config.save && e.push(this._config.save), e;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class ro extends Ve {
  /**
  * @param attributeKey Attribute that will be set by the command.
  */
  constructor(e, n) {
    super(e);
    /**
    * The attribute that will be set by the command.
    */
    h(this, "attributeKey");
    this.attributeKey = n;
  }
  /**
  * Updates the command's {@link #value} and {@link #isEnabled} based on the current selection.
  */
  refresh() {
    const e = this.editor.model, n = e.document;
    this.value = this._getValueFromFirstAllowedNode(), this.isEnabled = e.schema.checkAttributeInSelection(n.selection, this.attributeKey);
  }
  /**
  * Executes the command &ndash; applies the attribute to the selection or removes it from the selection.
  *
  * If the command is active (`value == true`), it will remove attributes. Otherwise, it will set attributes.
  *
  * The execution result differs, depending on the {@link module:engine/model/document~Document#selection}:
  *
  * * If the selection is on a range, the command applies the attribute to all nodes in that range
  * (if they are allowed to have this attribute by the {@link module:engine/model/schema~Schema schema}).
  * * If the selection is collapsed in a non-empty node, the command applies the attribute to the
  * {@link module:engine/model/document~Document#selection} itself (note that typed characters copy attributes from the selection).
  * * If the selection is collapsed in an empty node, the command applies the attribute to the parent node of the selection (note
  * that the selection inherits all attributes from a node if it is in an empty node).
  *
  * @fires execute
  * @param options Command options.
  * @param options.forceValue If set, it will force the command behavior. If `true`,
  * the command will apply the attribute, otherwise the command will remove the attribute.
  * If not set, the command will look for its current value to decide what it should do.
  */
  execute(e = {}) {
    const n = this.editor.model, s = n.document.selection, o = e.forceValue === void 0 ? !this.value : e.forceValue;
    n.change((a) => {
      if (s.isCollapsed)
        o ? a.setSelectionAttribute(this.attributeKey, !0) : a.removeSelectionAttribute(this.attributeKey);
      else {
        const l = n.schema.getValidRanges(s.getRanges(), this.attributeKey);
        for (const c of l)
          o ? a.setAttribute(this.attributeKey, o, c) : a.removeAttribute(this.attributeKey, c);
      }
    });
  }
  /**
  * Checks the attribute value of the first node in the selection that allows the attribute.
  * For the collapsed selection returns the selection attribute.
  *
  * @returns The attribute value.
  */
  _getValueFromFirstAllowedNode() {
    const e = this.editor.model, n = e.schema, r = e.document.selection;
    if (r.isCollapsed)
      return r.hasAttribute(this.attributeKey);
    for (const s of r.getRanges())
      for (const o of s.getItems())
        if (n.checkAttribute(o, this.attributeKey))
          return o.hasAttribute(this.attributeKey);
    return !1;
  }
}
const _o = "bold";
class lL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BoldEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: _o
    }), t.model.schema.setAttributeProperties(_o, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: _o,
      view: "strong",
      upcastAlso: [
        "b",
        (n) => {
          const r = n.getStyle("font-weight");
          return r && (r == "bold" || Number(r) >= 600) ? {
            name: !0,
            styles: [
              "font-weight"
            ]
          } : null;
        }
      ]
    }), t.commands.add(_o, new ro(t, _o)), t.keystrokes.set("CTRL+B", _o), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Bold text"),
          keystroke: "CTRL+B"
        }
      ]
    });
  }
}
function so({ editor: i, commandName: t, plugin: e, icon: n, label: r, keystroke: s }) {
  return (o) => {
    const a = i.commands.get(t), l = new o(i.locale);
    return l.set({
      label: r,
      icon: n,
      keystroke: s,
      isToggleable: !0
    }), l.bind("isEnabled").to(a, "isEnabled"), l.bind("isOn").to(a, "value"), l instanceof At ? l.set({
      role: "menuitemcheckbox"
    }) : l.set({
      tooltip: !0
    }), e.listenTo(l, "execute", () => {
      i.execute(t), i.editing.view.focus();
    }), l;
  };
}
const rf = "bold";
class cL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BoldUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, n = so({
      editor: t,
      commandName: rf,
      plugin: this,
      icon: ee.bold,
      label: e("Bold"),
      keystroke: "CTRL+B"
    });
    t.ui.componentFactory.add(rf, () => n(Te)), t.ui.componentFactory.add("menuBar:" + rf, () => n(At));
  }
}
class uL extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      lL,
      cL
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Bold";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const As = "code", dL = "ck-code_selected";
class hL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Gu
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: As
    }), t.model.schema.setAttributeProperties(As, {
      isFormatting: !0,
      copyOnEnter: !1
    }), t.conversion.attributeToElement({
      model: As,
      view: "code",
      upcastAlso: {
        styles: {
          "word-wrap": "break-word"
        }
      }
    }), t.commands.add(As, new ro(t, As)), t.plugins.get(Gu).registerAttribute(As), fk(t, As, "code", dL), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Move out of an inline code style"),
          keystroke: [
            [
              "arrowleft",
              "arrowleft"
            ],
            [
              "arrowright",
              "arrowright"
            ]
          ]
        }
      ]
    });
  }
}
var fL = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m12.5 5.7 5.2 3.9v1.3l-5.6 4c-.1.2-.3.2-.5.2-.3-.1-.6-.7-.6-1l.3-.4 4.7-3.5L11.5 7l-.2-.2c-.1-.3-.1-.6 0-.8.2-.2.5-.4.8-.4a.8.8 0 0 1 .4.1zm-5.2 0L2 9.6v1.3l5.6 4c.1.2.3.2.5.2.3-.1.7-.7.6-1 0-.1 0-.3-.2-.4l-5-3.5L8.2 7l.2-.2c.1-.3.1-.6 0-.8-.2-.2-.5-.4-.8-.4a.8.8 0 0 0-.3.1z"/></svg>';
const sf = "code";
class mL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, n = so({
      editor: t,
      commandName: sf,
      plugin: this,
      icon: fL,
      label: e("Code")
    });
    t.ui.componentFactory.add(sf, () => n(Te)), t.ui.componentFactory.add("menuBar:" + sf, () => n(At));
  }
}
class gL extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      hL,
      mL
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Code";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const Co = "italic";
class pL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ItalicEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: Co
    }), t.model.schema.setAttributeProperties(Co, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: Co,
      view: "i",
      upcastAlso: [
        "em",
        {
          styles: {
            "font-style": "italic"
          }
        }
      ]
    }), t.commands.add(Co, new ro(t, Co)), t.keystrokes.set("CTRL+I", Co), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Italic text"),
          keystroke: "CTRL+I"
        }
      ]
    });
  }
}
var bL = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m9.586 14.633.021.004c-.036.335.095.655.393.962.082.083.173.15.274.201h1.474a.6.6 0 1 1 0 1.2H5.304a.6.6 0 0 1 0-1.2h1.15c.474-.07.809-.182 1.005-.334.157-.122.291-.32.404-.597l2.416-9.55a1.053 1.053 0 0 0-.281-.823 1.12 1.12 0 0 0-.442-.296H8.15a.6.6 0 0 1 0-1.2h6.443a.6.6 0 1 1 0 1.2h-1.195c-.376.056-.65.155-.823.296-.215.175-.423.439-.623.79l-2.366 9.347z"/></svg>';
const of = "italic";
class vL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ItalicUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, n = so({
      editor: t,
      commandName: of,
      plugin: this,
      icon: bL,
      keystroke: "CTRL+I",
      label: e("Italic")
    });
    t.ui.componentFactory.add(of, () => n(Te)), t.ui.componentFactory.add("menuBar:" + of, () => n(At));
  }
}
class wL extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pL,
      vL
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Italic";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const ja = "strikethrough";
class yL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StrikethroughEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: ja
    }), t.model.schema.setAttributeProperties(ja, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: ja,
      view: "s",
      upcastAlso: [
        "del",
        "strike",
        {
          styles: {
            "text-decoration": "line-through"
          }
        }
      ]
    }), t.commands.add(ja, new ro(t, ja)), t.keystrokes.set("CTRL+SHIFT+X", "strikethrough"), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Strikethrough text"),
          keystroke: "CTRL+SHIFT+X"
        }
      ]
    });
  }
}
var _L = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M7 16.4c-.8-.4-1.5-.9-2.2-1.5a.6.6 0 0 1-.2-.5l.3-.6h1c1 1.2 2.1 1.7 3.7 1.7 1 0 1.8-.3 2.3-.6.6-.4.6-1.2.6-1.3.2-1.2-.9-2.1-.9-2.1h2.1c.3.7.4 1.2.4 1.7v.8l-.6 1.2c-.6.8-1.1 1-1.6 1.2a6 6 0 0 1-2.4.6c-1 0-1.8-.3-2.5-.6zM6.8 9 6 8.3c-.4-.5-.5-.8-.5-1.6 0-.7.1-1.3.5-1.8.4-.6 1-1 1.6-1.3a6.3 6.3 0 0 1 4.7 0 4 4 0 0 1 1.7 1l.3.7c0 .1.2.4-.2.7-.4.2-.9.1-1 0a3 3 0 0 0-1.2-1c-.4-.2-1-.3-2-.4-.7 0-1.4.2-2 .6-.8.6-1 .8-1 1.5 0 .8.5 1 1.2 1.5.6.4 1.1.7 1.9 1H6.8z"/><path d="M3 10.5V9h14v1.5z"/></svg>';
const af = "strikethrough";
class CL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StrikethroughUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, n = so({
      editor: t,
      commandName: af,
      plugin: this,
      icon: _L,
      keystroke: "CTRL+SHIFT+X",
      label: e("Strikethrough")
    });
    t.ui.componentFactory.add(af, () => n(Te)), t.ui.componentFactory.add("menuBar:" + af, () => n(At));
  }
}
class kL extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      yL,
      CL
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Strikethrough";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const Wa = "subscript";
class xL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SubscriptEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: Wa
    }), t.model.schema.setAttributeProperties(Wa, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: Wa,
      view: "sub",
      upcastAlso: [
        {
          styles: {
            "vertical-align": "sub"
          }
        }
      ]
    }), t.commands.add(Wa, new ro(t, Wa));
  }
}
var EL = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m7.03 10.349 3.818-3.819a.8.8 0 1 1 1.132 1.132L8.16 11.48l3.819 3.818a.8.8 0 1 1-1.132 1.132L7.03 12.61l-3.818 3.82a.8.8 0 1 1-1.132-1.132L5.9 11.48 2.08 7.662A.8.8 0 1 1 3.212 6.53l3.818 3.82zm8.147 7.829h2.549c.254 0 .447.05.58.152a.49.49 0 0 1 .201.413.54.54 0 0 1-.159.393c-.105.108-.266.162-.48.162h-3.594c-.245 0-.435-.066-.572-.197a.621.621 0 0 1-.205-.463c0-.114.044-.265.132-.453a1.62 1.62 0 0 1 .288-.444c.433-.436.824-.81 1.172-1.122.348-.312.597-.517.747-.615.267-.183.49-.368.667-.553.177-.185.312-.375.405-.57.093-.194.139-.384.139-.57a1.008 1.008 0 0 0-.554-.917 1.197 1.197 0 0 0-.56-.133c-.426 0-.761.182-1.005.546a2.332 2.332 0 0 0-.164.39 1.609 1.609 0 0 1-.258.488c-.096.114-.237.17-.423.17a.558.558 0 0 1-.405-.156.568.568 0 0 1-.161-.427c0-.218.05-.446.151-.683.101-.238.252-.453.452-.646s.454-.349.762-.467a2.998 2.998 0 0 1 1.081-.178c.498 0 .923.076 1.274.228a1.916 1.916 0 0 1 1.004 1.032 1.984 1.984 0 0 1-.156 1.794c-.2.32-.405.572-.613.754-.208.182-.558.468-1.048.857-.49.39-.826.691-1.008.906a2.703 2.703 0 0 0-.24.309z"/></svg>';
const lf = "subscript";
class TL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SubscriptUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, n = so({
      editor: t,
      commandName: lf,
      plugin: this,
      icon: EL,
      label: e("Subscript")
    });
    t.ui.componentFactory.add(lf, () => n(Te)), t.ui.componentFactory.add("menuBar:" + lf, () => n(At));
  }
}
class SL extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      xL,
      TL
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Subscript";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const Ua = "superscript";
class AL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SuperscriptEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: Ua
    }), t.model.schema.setAttributeProperties(Ua, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: Ua,
      view: "sup",
      upcastAlso: [
        {
          styles: {
            "vertical-align": "super"
          }
        }
      ]
    }), t.commands.add(Ua, new ro(t, Ua));
  }
}
var PL = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M15.677 8.678h2.549c.254 0 .447.05.58.152a.49.49 0 0 1 .201.413.54.54 0 0 1-.159.393c-.105.108-.266.162-.48.162h-3.594c-.245 0-.435-.066-.572-.197a.621.621 0 0 1-.205-.463c0-.114.044-.265.132-.453a1.62 1.62 0 0 1 .288-.444c.433-.436.824-.81 1.172-1.122.348-.312.597-.517.747-.615.267-.183.49-.368.667-.553.177-.185.312-.375.405-.57.093-.194.139-.384.139-.57a1.008 1.008 0 0 0-.554-.917 1.197 1.197 0 0 0-.56-.133c-.426 0-.761.182-1.005.546a2.332 2.332 0 0 0-.164.39 1.609 1.609 0 0 1-.258.488c-.096.114-.237.17-.423.17a.558.558 0 0 1-.405-.156.568.568 0 0 1-.161-.427c0-.218.05-.446.151-.683.101-.238.252-.453.452-.646s.454-.349.762-.467a2.998 2.998 0 0 1 1.081-.178c.498 0 .923.076 1.274.228a1.916 1.916 0 0 1 1.004 1.032 1.984 1.984 0 0 1-.156 1.794c-.2.32-.405.572-.613.754-.208.182-.558.468-1.048.857-.49.39-.826.691-1.008.906a2.703 2.703 0 0 0-.24.309zM7.03 10.349l3.818-3.819a.8.8 0 1 1 1.132 1.132L8.16 11.48l3.819 3.818a.8.8 0 1 1-1.132 1.132L7.03 12.61l-3.818 3.82a.8.8 0 1 1-1.132-1.132L5.9 11.48 2.08 7.662A.8.8 0 1 1 3.212 6.53l3.818 3.82z"/></svg>';
const cf = "superscript";
class IL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SuperscriptUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, n = so({
      editor: t,
      commandName: cf,
      plugin: this,
      icon: PL,
      label: e("Superscript")
    });
    t.ui.componentFactory.add(cf, () => n(Te)), t.ui.componentFactory.add("menuBar:" + cf, () => n(At));
  }
}
class RL extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      AL,
      IL
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Superscript";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const qa = "underline";
class OL extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "UnderlineEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: qa
    }), t.model.schema.setAttributeProperties(qa, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: qa,
      view: "u",
      upcastAlso: {
        styles: {
          "text-decoration": "underline"
        }
      }
    }), t.commands.add(qa, new ro(t, qa)), t.keystrokes.set("CTRL+U", "underline"), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Underline text"),
          keystroke: "CTRL+U"
        }
      ]
    });
  }
}
var VL = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 18v-1.5h14V18zm2.2-8V3.6c0-.4.4-.6.8-.6.3 0 .7.2.7.6v6.2c0 2 1.3 2.8 3.2 2.8 1.9 0 3.4-.9 3.4-2.9V3.6c0-.3.4-.5.8-.5.3 0 .7.2.7.5V10c0 2.7-2.2 4-4.9 4-2.6 0-4.7-1.2-4.7-4z"/></svg>';
const uf = "underline";
class ML extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "UnderlineUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, n = so({
      editor: t,
      commandName: uf,
      plugin: this,
      icon: VL,
      label: e("Underline"),
      keystroke: "CTRL+U"
    });
    t.ui.componentFactory.add(uf, () => n(Te)), t.ui.componentFactory.add("menuBar:" + uf, () => n(At));
  }
}
class DL extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      OL,
      ML
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Underline";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function* mk(i, t) {
  for (const e of t)
    e && i.getAttributeProperties(e[0]).copyOnEnter && (yield e);
}
class LL extends Ve {
  /**
  * @inheritDoc
  */
  execute() {
    this.editor.model.change((t) => {
      this.enterBlock(t), this.fire("afterExecute", {
        writer: t
      });
    });
  }
  /**
  * Splits a block where the document selection is placed, in the way how the <kbd>Enter</kbd> key is expected to work:
  *
  * ```
  * <p>Foo[]bar</p>   ->   <p>Foo</p><p>[]bar</p>
  * <p>Foobar[]</p>   ->   <p>Foobar</p><p>[]</p>
  * <p>Fo[ob]ar</p>   ->   <p>Fo</p><p>[]ar</p>
  * ```
  *
  * In some cases, the split will not happen:
  *
  * ```
  * // The selection parent is a limit element:
  * <figcaption>A[bc]d</figcaption>   ->   <figcaption>A[]d</figcaption>
  *
  * // The selection spans over multiple elements:
  * <h>x[x</h><p>y]y<p>   ->   <h>x</h><p>[]y</p>
  * ```
  *
  * @param writer Writer to use when performing the enter action.
  * @returns Boolean indicating if the block was split.
  */
  enterBlock(t) {
    const e = this.editor.model, n = e.document.selection, r = e.schema, s = n.isCollapsed, o = n.getFirstRange(), a = o.start.parent, l = o.end.parent;
    if (r.isLimit(a) || r.isLimit(l))
      return !s && a == l && e.deleteContent(n), !1;
    if (s) {
      const c = mk(t.model.schema, n.getAttributes());
      return ew(t, o.start), t.setSelectionAttribute(c), !0;
    } else {
      const c = !(o.start.isAtStart && o.end.isAtEnd), u = a == l;
      if (e.deleteContent(n, {
        leaveUnmerged: c
      }), c) {
        if (u)
          return ew(t, n.focus), !0;
        t.setSelection(l, 0);
      }
    }
    return !1;
  }
}
function ew(i, t) {
  i.split(t), i.setSelection(t.parent.nextSibling, 0);
}
const NL = {
  insertParagraph: {
    isSoft: !1
  },
  insertLineBreak: {
    isSoft: !0
  }
};
class gk extends nr {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = this.document;
    let n = !1;
    e.on("keydown", (r, s) => {
      n = s.shiftKey;
    }), e.on("beforeinput", (r, s) => {
      if (!this.isEnabled)
        return;
      let o = s.inputType;
      Me.isSafari && n && o == "insertParagraph" && (o = "insertLineBreak");
      const a = s.domEvent, l = NL[o];
      if (!l)
        return;
      const c = new ga(e, "enter", s.targetRanges[0]);
      e.fire(c, new Qo(t, a, {
        isSoft: l.isSoft
      })), c.stop.called && r.stop();
    });
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
class Jd extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Enter";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  init() {
    const t = this.editor, e = t.editing.view, n = e.document, r = this.editor.t;
    e.addObserver(gk), t.commands.add("enter", new LL(t)), this.listenTo(n, "enter", (s, o) => {
      n.isComposing || o.preventDefault(), !o.isSoft && (t.execute("enter"), e.scrollToTheSelection());
    }, {
      priority: "low"
    }), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: r("Insert a hard break (a new paragraph)"),
          keystroke: "Enter"
        }
      ]
    });
  }
}
class BL extends Ve {
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = t.document;
    t.change((n) => {
      $L(t, n, e.selection), this.fire("afterExecute", {
        writer: n
      });
    });
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document;
    this.isEnabled = FL(t.schema, e.selection);
  }
}
function FL(i, t) {
  if (t.rangeCount > 1)
    return !1;
  const e = t.anchor;
  if (!e || !i.checkChild(e, "softBreak"))
    return !1;
  const n = t.getFirstRange(), r = n.start.parent, s = n.end.parent;
  return !((wm(r, i) || wm(s, i)) && r !== s);
}
function $L(i, t, e) {
  const n = e.isCollapsed, r = e.getFirstRange(), s = r.start.parent, o = r.end.parent, a = s == o;
  if (n) {
    const l = mk(i.schema, e.getAttributes());
    tw(i, t, r.end), t.removeSelectionAttribute(e.getAttributeKeys()), t.setSelectionAttribute(l);
  } else {
    const l = !(r.start.isAtStart && r.end.isAtEnd);
    i.deleteContent(e, {
      leaveUnmerged: l
    }), a ? tw(i, t, e.focus) : l && t.setSelection(o, 0);
  }
}
function tw(i, t, e) {
  const n = t.createElement("softBreak");
  i.insertContent(n, e), t.setSelection(n, "after");
}
function wm(i, t) {
  return i.is("rootElement") ? !1 : t.isLimit(i) || wm(i.parent, t);
}
class pk extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ShiftEnter";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  init() {
    const t = this.editor, e = t.model.schema, n = t.conversion, r = t.editing.view, s = r.document, o = this.editor.t;
    e.register("softBreak", {
      allowWhere: "$text",
      isInline: !0
    }), n.for("upcast").elementToElement({
      model: "softBreak",
      view: "br"
    }), n.for("downcast").elementToElement({
      model: "softBreak",
      view: (a, { writer: l }) => l.createEmptyElement("br")
    }), r.addObserver(gk), t.commands.add("shiftEnter", new BL(t)), this.listenTo(s, "enter", (a, l) => {
      s.isComposing || l.preventDefault(), l.isSoft && (t.execute("shiftEnter"), r.scrollToTheSelection());
    }, {
      priority: "low"
    }), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: o("Insert a soft break (a <code>&lt;br&gt;</code> element)"),
          keystroke: "Shift+Enter"
        }
      ]
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class zL extends (/* @__PURE__ */ ht()) {
  constructor() {
    super(...arguments);
    h(this, "_stack", []);
  }
  /**
  * Adds highlight descriptor to the stack.
  *
  * @fires change:top
  */
  add(e, n) {
    const r = this._stack, s = r[0];
    this._insertDescriptor(e);
    const o = r[0];
    s !== o && !df(s, o) && this.fire("change:top", {
      oldDescriptor: s,
      newDescriptor: o,
      writer: n
    });
  }
  /**
  * Removes highlight descriptor from the stack.
  *
  * @fires change:top
  * @param id Id of the descriptor to remove.
  */
  remove(e, n) {
    const r = this._stack, s = r[0];
    this._removeDescriptor(e);
    const o = r[0];
    s !== o && !df(s, o) && this.fire("change:top", {
      oldDescriptor: s,
      newDescriptor: o,
      writer: n
    });
  }
  /**
  * Inserts a given descriptor in correct place in the stack. It also takes care about updating information
  * when descriptor with same id is already present.
  */
  _insertDescriptor(e) {
    const n = this._stack, r = n.findIndex((o) => o.id === e.id);
    if (df(e, n[r]))
      return;
    r > -1 && n.splice(r, 1);
    let s = 0;
    for (; n[s] && HL(n[s], e); )
      s++;
    n.splice(s, 0, e);
  }
  /**
  * Removes descriptor with given id from the stack.
  *
  * @param id Descriptor's id.
  */
  _removeDescriptor(e) {
    const n = this._stack, r = n.findIndex((s) => s.id === e);
    r > -1 && n.splice(r, 1);
  }
}
function df(i, t) {
  return i && t && i.priority == t.priority && Ku(i.classes) == Ku(t.classes);
}
function HL(i, t) {
  return i.priority > t.priority ? !0 : i.priority < t.priority ? !1 : Ku(i.classes) > Ku(t.classes);
}
function Ku(i) {
  return Array.isArray(i) ? i.sort().join(",") : i;
}
var jL = '<svg viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg"><path d="M4 0v1H1v3H0V.5A.5.5 0 0 1 .5 0H4zm8 0h3.5a.5.5 0 0 1 .5.5V4h-1V1h-3V0zM4 16H.5a.5.5 0 0 1-.5-.5V12h1v3h3v1zm8 0v-1h3v-3h1v3.5a.5.5 0 0 1-.5.5H12z"/><path fill-opacity=".256" d="M1 1h14v14H1z"/><g class="ck-icon__selected-indicator"><path d="M7 0h2v1H7V0zM0 7h1v2H0V7zm15 0h1v2h-1V7zm-8 8h2v1H7v-1z"/><path fill-opacity=".254" d="M1 1h14v14H1z"/></g></svg>';
const WL = "ck-widget", nw = "ck-widget_selected";
function Gt(i) {
  return i.is("element") ? !!i.getCustomProperty("widget") : !1;
}
function va(i, t, e = {}) {
  if (!i.is("containerElement"))
    throw new T("widget-to-widget-wrong-element-type", null, {
      element: i
    });
  return t.setAttribute("contenteditable", "false", i), t.addClass(WL, i), t.setCustomProperty("widget", !0, i), i.getFillerOffset = ZL, t.setCustomProperty("widgetLabel", [], i), e.label && GL(i, e.label), e.hasSelectionHandle && YL(i, t), bk(i, t), i;
}
function UL(i, t, e) {
  if (t.classes && e.addClass(tt(t.classes), i), t.attributes)
    for (const n in t.attributes)
      e.setAttribute(n, t.attributes[n], i);
}
function qL(i, t, e) {
  if (t.classes && e.removeClass(tt(t.classes), i), t.attributes)
    for (const n in t.attributes)
      e.removeAttribute(n, i);
}
function bk(i, t, e = UL, n = qL) {
  const r = new zL();
  r.on("change:top", (a, l) => {
    l.oldDescriptor && n(i, l.oldDescriptor, l.writer), l.newDescriptor && e(i, l.newDescriptor, l.writer);
  });
  const s = (a, l, c) => r.add(l, c), o = (a, l, c) => r.remove(l, c);
  t.setCustomProperty("addHighlight", s, i), t.setCustomProperty("removeHighlight", o, i);
}
function GL(i, t) {
  i.getCustomProperty("widgetLabel").push(t);
}
function KL(i) {
  return i.getCustomProperty("widgetLabel").reduce((e, n) => typeof n == "function" ? e ? e + ". " + n() : n() : e ? e + ". " + n : n, "");
}
function wp(i, t, e = {}) {
  return t.addClass([
    "ck-editor__editable",
    "ck-editor__nested-editable"
  ], i), t.setAttribute("role", "textbox", i), t.setAttribute("tabindex", "-1", i), e.label && t.setAttribute("aria-label", e.label, i), t.setAttribute("contenteditable", i.isReadOnly ? "false" : "true", i), i.on("change:isReadOnly", (n, r, s) => {
    t.setAttribute("contenteditable", s ? "false" : "true", i);
  }), i.on("change:isFocused", (n, r, s) => {
    s ? t.addClass("ck-editor__nested-editable_focused", i) : t.removeClass("ck-editor__nested-editable_focused", i);
  }), bk(i, t), i;
}
function yp(i, t) {
  const e = i.getSelectedElement();
  if (e) {
    const n = Xr(i);
    if (n)
      return t.createRange(t.createPositionAt(e, n));
  }
  return t.schema.findOptimalInsertionRange(i);
}
function ZL() {
  return null;
}
function YL(i, t) {
  const e = t.createUIElement("div", {
    class: "ck ck-widget__selection-handle"
  }, function(n) {
    const r = this.toDomElement(n), s = new pr();
    return s.set("content", jL), s.render(), r.appendChild(s.element), r;
  });
  t.insert(t.createPositionAt(i, 0), e), t.addClass([
    "ck-widget_with-selection-handle"
  ], i);
}
function _p(i) {
  const t = (a) => {
    const { width: l, paddingLeft: c, paddingRight: u } = a.ownerDocument.defaultView.getComputedStyle(a);
    return parseFloat(l) - (parseFloat(c) || 0) - (parseFloat(u) || 0);
  }, e = i.parentElement;
  if (!e)
    return 0;
  let n = t(e);
  const r = 5;
  let s = 0, o = e;
  for (; isNaN(n); ) {
    if (o = o.parentElement, ++s > r)
      return 0;
    n = t(o);
  }
  return n;
}
function JL(i, t = new je(i)) {
  const e = _p(i);
  return e ? t.width / e * 100 : 0;
}
const Ir = "widget-type-around";
function Ps(i, t, e) {
  return !!i && Gt(i) && !e.isInline(t);
}
function XL(i) {
  return i.closest(".ck-widget__type-around__button");
}
function QL(i) {
  return i.classList.contains("ck-widget__type-around__button_before") ? "before" : "after";
}
function eN(i, t) {
  const e = i.closest(".ck-widget");
  return t.mapDomToView(e);
}
function Xr(i) {
  return i.getAttribute(Ir);
}
var tN = '<svg viewBox="0 0 10 8" xmlns="http://www.w3.org/2000/svg"><path d="M9.055.263v3.972h-6.77M1 4.216l2-2.038m-2 2 2 2.038"/></svg>';
const vk = [
  "before",
  "after"
], nN = new DOMParser().parseFromString(tN, "image/svg+xml").firstChild, iw = "ck-widget__type-around_disabled";
class iN extends V {
  constructor() {
    super(...arguments);
    /**
    * A reference to the model widget element that has the fake caret active
    * on either side of it. It is later used to remove CSS classes associated with the fake caret
    * when the widget no longer needs it.
    */
    h(this, "_currentFakeCaretModelElement", null);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "WidgetTypeAround";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Jd,
      Fr
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.editing.view;
    this.on("change:isEnabled", (r, s, o) => {
      n.change((a) => {
        for (const l of n.document.roots)
          o ? a.removeClass(iw, l) : a.addClass(iw, l);
      }), o || e.model.change((a) => {
        a.removeSelectionAttribute(Ir);
      });
    }), this._enableTypeAroundUIInjection(), this._enableInsertingParagraphsOnButtonClick(), this._enableInsertingParagraphsOnEnterKeypress(), this._enableInsertingParagraphsOnTypingKeystroke(), this._enableTypeAroundFakeCaretActivationUsingKeyboardArrows(), this._enableDeleteIntegration(), this._enableInsertContentIntegration(), this._enableInsertObjectIntegration(), this._enableDeleteContentIntegration();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._currentFakeCaretModelElement = null;
  }
  /**
  * Inserts a new paragraph next to a widget element with the selection anchored in it.
  *
  * **Note**: This method is heavily user-oriented and will both focus the editing view and scroll
  * the viewport to the selection in the inserted paragraph.
  *
  * @param widgetModelElement The model widget element next to which a paragraph is inserted.
  * @param position The position where the paragraph is inserted. Either `'before'` or `'after'` the widget.
  */
  _insertParagraph(e, n) {
    const r = this.editor, s = r.editing.view, o = r.model.schema.getAttributesWithProperty(e, "copyOnReplace", !0);
    r.execute("insertParagraph", {
      position: r.model.createPositionAt(e, n),
      attributes: o
    }), s.focus(), s.scrollToTheSelection();
  }
  /**
  * A wrapper for the {@link module:utils/emittermixin~Emitter#listenTo} method that executes the callbacks only
  * when the plugin {@link #isEnabled is enabled}.
  *
  * @param emitter The object that fires the event.
  * @param event The name of the event.
  * @param callback The function to be called on event.
  * @param options Additional options.
  * @param options.priority The priority of this event callback. The higher the priority value the sooner
  * the callback will be fired. Events having the same priority are called in the order they were added.
  */
  _listenToIfEnabled(e, n, r, s) {
    this.listenTo(e, n, (...o) => {
      this.isEnabled && r(...o);
    }, s);
  }
  /**
  * Similar to {@link #_insertParagraph}, this method inserts a paragraph except that it
  * does not expect a position. Instead, it performs the insertion next to a selected widget
  * according to the `widget-type-around` model selection attribute value (fake caret position).
  *
  * Because this method requires the `widget-type-around` attribute to be set,
  * the insertion can only happen when the widget's fake caret is active (e.g. activated
  * using the keyboard).
  *
  * @returns Returns `true` when the paragraph was inserted (the attribute was present) and `false` otherwise.
  */
  _insertParagraphAccordingToFakeCaretPosition() {
    const r = this.editor.model.document.selection, s = Xr(r);
    if (!s)
      return !1;
    const o = r.getSelectedElement();
    return this._insertParagraph(o, s), !0;
  }
  /**
  * Creates a listener in the editing conversion pipeline that injects the widget type around
  * UI into every single widget instance created in the editor.
  *
  * The UI is delivered as a {@link module:engine/view/uielement~UIElement}
  * wrapper which renders DOM buttons that users can use to insert paragraphs.
  */
  _enableTypeAroundUIInjection() {
    const e = this.editor, n = e.model.schema, r = e.locale.t, s = {
      before: r("Insert paragraph before block"),
      after: r("Insert paragraph after block")
    };
    e.editing.downcastDispatcher.on("insert", (o, a, l) => {
      const c = l.mapper.toViewElement(a.item);
      c && Ps(c, a.item, n) && (rN(l.writer, s, c), c.getCustomProperty("widgetLabel").push(() => this.isEnabled ? r("Press Enter to type after or press Shift + Enter to type before the widget") : ""));
    }, {
      priority: "low"
    });
  }
  /**
  * Brings support for the fake caret that appears when either:
  *
  * * the selection moves to a widget from a position next to it using arrow keys,
  * * the arrow key is pressed when the widget is already selected.
  *
  * The fake caret lets the user know that they can start typing or just press
  * <kbd>Enter</kbd> to insert a paragraph at the position next to a widget as suggested by the fake caret.
  *
  * The fake caret disappears when the user changes the selection or the editor
  * gets blurred.
  *
  * The whole idea is as follows:
  *
  * 1. A user does one of the 2 scenarios described at the beginning.
  * 2. The "keydown" listener is executed and the decision is made whether to show or hide the fake caret.
  * 3. If it should show up, the `widget-type-around` model selection attribute is set indicating
  *    on which side of the widget it should appear.
  * 4. The selection dispatcher reacts to the selection attribute and sets CSS classes responsible for the
  *    fake caret on the view widget.
  * 5. If the fake caret should disappear, the selection attribute is removed and the dispatcher
  *    does the CSS class clean-up in the view.
  * 6. Additionally, `change:range` and `FocusTracker#isFocused` listeners also remove the selection
  *    attribute (the former also removes widget CSS classes).
  */
  _enableTypeAroundFakeCaretActivationUsingKeyboardArrows() {
    const e = this.editor, n = e.model, r = n.document.selection, s = n.schema, o = e.editing.view;
    this._listenToIfEnabled(o.document, "arrowKey", (l, c) => {
      this._handleArrowKeyPress(l, c);
    }, {
      context: [
        Gt,
        "$text"
      ],
      priority: "high"
    }), this._listenToIfEnabled(r, "change:range", (l, c) => {
      c.directChange && e.model.change((u) => {
        u.removeSelectionAttribute(Ir);
      });
    }), this._listenToIfEnabled(n.document, "change:data", () => {
      const l = r.getSelectedElement();
      if (l) {
        const c = e.editing.mapper.toViewElement(l);
        if (Ps(c, l, s))
          return;
      }
      e.model.change((c) => {
        c.removeSelectionAttribute(Ir);
      });
    }), this._listenToIfEnabled(e.editing.downcastDispatcher, "selection", (l, c, u) => {
      const d = u.writer;
      if (this._currentFakeCaretModelElement) {
        const p = u.mapper.toViewElement(this._currentFakeCaretModelElement);
        p && (d.removeClass(vk.map(a), p), this._currentFakeCaretModelElement = null);
      }
      const f = c.selection.getSelectedElement();
      if (!f)
        return;
      const m = u.mapper.toViewElement(f);
      if (!Ps(m, f, s))
        return;
      const g = Xr(c.selection);
      g && (d.addClass(a(g), m), this._currentFakeCaretModelElement = f);
    }), this._listenToIfEnabled(e.ui.focusTracker, "change:isFocused", (l, c, u) => {
      u || e.model.change((d) => {
        d.removeSelectionAttribute(Ir);
      });
    });
    function a(l) {
      return `ck-widget_type-around_show-fake-caret_${l}`;
    }
  }
  /**
  * A listener executed on each "keydown" in the view document, a part of
  * {@link #_enableTypeAroundFakeCaretActivationUsingKeyboardArrows}.
  *
  * It decides whether the arrow keypress should activate the fake caret or not (also whether it should
  * be deactivated).
  *
  * The fake caret activation is done by setting the `widget-type-around` model selection attribute
  * in this listener, and stopping and preventing the event that would normally be handled by the widget
  * plugin that is responsible for the regular keyboard navigation near/across all widgets (that
  * includes inline widgets, which are ignored by the widget type around plugin).
  */
  _handleArrowKeyPress(e, n) {
    const r = this.editor, s = r.model, o = s.document.selection, a = s.schema, l = r.editing.view, c = n.keyCode, u = VR(c, r.locale.contentLanguageDirection), d = l.document.selection.getSelectedElement(), f = r.editing.mapper.toModelElement(d);
    let m;
    Ps(d, f, a) ? m = this._handleArrowKeyPressOnSelectedWidget(u) : o.isCollapsed ? m = this._handleArrowKeyPressWhenSelectionNextToAWidget(u) : n.shiftKey || (m = this._handleArrowKeyPressWhenNonCollapsedSelection(u)), m && (n.preventDefault(), e.stop());
  }
  /**
  * Handles the keyboard navigation on "keydown" when a widget is currently selected and activates or deactivates
  * the fake caret for that widget, depending on the current value of the `widget-type-around` model
  * selection attribute and the direction of the pressed arrow key.
  *
  * @param isForward `true` when the pressed arrow key was responsible for the forward model selection movement
  * as in {@link module:utils/keyboard~isForwardArrowKeyCode}.
  * @returns Returns `true` when the keypress was handled and no other keydown listener of the editor should
  * process the event any further. Returns `false` otherwise.
  */
  _handleArrowKeyPressOnSelectedWidget(e) {
    const r = this.editor.model, s = r.document.selection, o = Xr(s);
    return r.change((a) => {
      if (o) {
        if (!(o === (e ? "after" : "before")))
          return a.removeSelectionAttribute(Ir), !0;
      } else
        return a.setSelectionAttribute(Ir, e ? "after" : "before"), !0;
      return !1;
    });
  }
  /**
  * Handles the keyboard navigation on "keydown" when **no** widget is selected but the selection is **directly** next
  * to one and upon the fake caret should become active for this widget upon arrow keypress
  * (AKA entering/selecting the widget).
  *
  * **Note**: This code mirrors the implementation from the widget plugin but also adds the selection attribute.
  * Unfortunately, there is no safe way to let the widget plugin do the selection part first and then just set the
  * selection attribute here in the widget type around plugin. This is why this code must duplicate some from the widget plugin.
  *
  * @param isForward `true` when the pressed arrow key was responsible for the forward model selection movement
  * as in {@link module:utils/keyboard~isForwardArrowKeyCode}.
  * @returns Returns `true` when the keypress was handled and no other keydown listener of the editor should
  * process the event any further. Returns `false` otherwise.
  */
  _handleArrowKeyPressWhenSelectionNextToAWidget(e) {
    const n = this.editor, r = n.model, s = r.schema, o = n.plugins.get("Widget"), a = o._getObjectElementNextToSelection(e), l = n.editing.mapper.toViewElement(a);
    return Ps(l, a, s) ? (r.change((c) => {
      o._setSelectionOverElement(a), c.setSelectionAttribute(Ir, e ? "before" : "after");
    }), !0) : !1;
  }
  /**
  * Handles the keyboard navigation on "keydown" when a widget is currently selected (together with some other content)
  * and the widget is the first or last element in the selection. It activates or deactivates the fake caret for that widget.
  *
  * @param isForward `true` when the pressed arrow key was responsible for the forward model selection movement
  * as in {@link module:utils/keyboard~isForwardArrowKeyCode}.
  * @returns Returns `true` when the keypress was handled and no other keydown listener of the editor should
  * process the event any further. Returns `false` otherwise.
  */
  _handleArrowKeyPressWhenNonCollapsedSelection(e) {
    const n = this.editor, r = n.model, s = r.schema, o = n.editing.mapper, a = r.document.selection, l = e ? a.getLastPosition().nodeBefore : a.getFirstPosition().nodeAfter, c = o.toViewElement(l);
    return Ps(c, l, s) ? (r.change((u) => {
      u.setSelection(l, "on"), u.setSelectionAttribute(Ir, e ? "after" : "before");
    }), !0) : !1;
  }
  /**
  * Registers a `mousedown` listener for the view document which intercepts events
  * coming from the widget type around UI, which happens when a user clicks one of the buttons
  * that insert a paragraph next to a widget.
  */
  _enableInsertingParagraphsOnButtonClick() {
    const e = this.editor, n = e.editing.view;
    this._listenToIfEnabled(n.document, "mousedown", (r, s) => {
      const o = XL(s.domTarget);
      if (!o)
        return;
      const a = QL(o), l = eN(o, n.domConverter), c = e.editing.mapper.toModelElement(l);
      this._insertParagraph(c, a), s.preventDefault(), r.stop();
    });
  }
  /**
  * Creates the <kbd>Enter</kbd> key listener on the view document that allows the user to insert a paragraph
  * near the widget when either:
  *
  * * The fake caret was first activated using the arrow keys,
  * * The entire widget is selected in the model.
  *
  * In the first case, the new paragraph is inserted according to the `widget-type-around` selection
  * attribute (see {@link #_handleArrowKeyPress}).
  *
  * In the second case, the new paragraph is inserted based on whether a soft (<kbd>Shift</kbd>+<kbd>Enter</kbd>) keystroke
  * was pressed or not.
  */
  _enableInsertingParagraphsOnEnterKeypress() {
    const e = this.editor, n = e.model.document.selection, r = e.editing.view;
    this._listenToIfEnabled(r.document, "enter", (s, o) => {
      if (s.eventPhase != "atTarget")
        return;
      const a = n.getSelectedElement(), l = e.editing.mapper.toViewElement(a), c = e.model.schema;
      let u;
      this._insertParagraphAccordingToFakeCaretPosition() ? u = !0 : Ps(l, a, c) && (this._insertParagraph(a, o.isSoft ? "before" : "after"), u = !0), u && (o.preventDefault(), s.stop());
    }, {
      context: Gt
    });
  }
  /**
  * Similar to the {@link #_enableInsertingParagraphsOnEnterKeypress}, it allows the user
  * to insert a paragraph next to a widget when the fake caret was activated using arrow
  * keys but it responds to typing instead of <kbd>Enter</kbd>.
  *
  * Listener enabled by this method will insert a new paragraph according to the `widget-type-around`
  * model selection attribute as the user simply starts typing, which creates the impression that the fake caret
  * behaves like a real one rendered by the browser (AKA your text appears where the caret was).
  *
  * **Note**: At the moment this listener creates 2 undo steps: one for the `insertParagraph` command
  * and another one for actual typing. It is not a disaster but this may need to be fixed
  * sooner or later.
  */
  _enableInsertingParagraphsOnTypingKeystroke() {
    const n = this.editor.editing.view.document;
    this._listenToIfEnabled(n, "insertText", (r, s) => {
      this._insertParagraphAccordingToFakeCaretPosition() && (s.selection = n.selection);
    }, {
      priority: "high"
    }), Me.isAndroid ? this._listenToIfEnabled(n, "keydown", (r, s) => {
      s.keyCode == 229 && this._insertParagraphAccordingToFakeCaretPosition();
    }) : this._listenToIfEnabled(n, "compositionstart", () => {
      this._insertParagraphAccordingToFakeCaretPosition();
    }, {
      priority: "high"
    });
  }
  /**
  * It creates a "delete" event listener on the view document to handle cases when the <kbd>Delete</kbd> or <kbd>Backspace</kbd>
  * is pressed and the fake caret is currently active.
  *
  * The fake caret should create an illusion of a real browser caret so that when it appears before or after
  * a widget, pressing <kbd>Delete</kbd> or <kbd>Backspace</kbd> should remove a widget or delete the content
  * before or after a widget (depending on the content surrounding the widget).
  */
  _enableDeleteIntegration() {
    const e = this.editor, n = e.editing.view, r = e.model, s = r.schema;
    this._listenToIfEnabled(n.document, "delete", (o, a) => {
      if (o.eventPhase != "atTarget")
        return;
      const l = Xr(r.document.selection);
      if (!l)
        return;
      const c = a.direction, u = r.document.selection.getSelectedElement(), d = l === "before", f = c == "forward";
      if (d === f)
        e.execute("delete", {
          selection: r.createSelection(u, "on")
        });
      else {
        const g = s.getNearestSelectionRange(r.createPositionAt(u, l), c);
        if (g)
          if (!g.isCollapsed)
            r.change((p) => {
              p.setSelection(g), e.execute(f ? "deleteForward" : "delete");
            });
          else {
            const p = r.createSelection(g.start);
            if (r.modifySelection(p, {
              direction: c
            }), !p.focus.isEqual(g.start))
              r.change((b) => {
                b.setSelection(g), e.execute(f ? "deleteForward" : "delete");
              });
            else {
              const b = aN(s, g.start.parent);
              r.deleteContent(r.createSelection(b, "on"), {
                doNotAutoparagraph: !0
              });
            }
          }
      }
      a.preventDefault(), o.stop();
    }, {
      context: Gt
    });
  }
  /**
  * Attaches the {@link module:engine/model/model~Model#event:insertContent} event listener that, for instance, allows the user to paste
  * content near a widget when the fake caret is first activated using the arrow keys.
  *
  * The content is inserted according to the `widget-type-around` selection attribute (see {@link #_handleArrowKeyPress}).
  */
  _enableInsertContentIntegration() {
    const e = this.editor, n = this.editor.model, r = n.document.selection;
    this._listenToIfEnabled(e.model, "insertContent", (s, [o, a]) => {
      if (a && !a.is("documentSelection"))
        return;
      const l = Xr(r);
      if (l)
        return s.stop(), n.change((c) => {
          const u = r.getSelectedElement(), d = n.createPositionAt(u, l), f = c.createSelection(d), m = n.insertContent(o, f);
          return c.setSelection(f), m;
        });
    }, {
      priority: "high"
    });
  }
  /**
  * Attaches the {@link module:engine/model/model~Model#event:insertObject} event listener that modifies the
  * `options.findOptimalPosition`parameter to position of fake caret in relation to selected element
  * to reflect user's intent of desired insertion position.
  *
  * The object is inserted according to the `widget-type-around` selection attribute (see {@link #_handleArrowKeyPress}).
  */
  _enableInsertObjectIntegration() {
    const e = this.editor, r = this.editor.model.document.selection;
    this._listenToIfEnabled(e.model, "insertObject", (s, o) => {
      const [, a, l = {}] = o;
      if (a && !a.is("documentSelection"))
        return;
      const c = Xr(r);
      c && (l.findOptimalPosition = c, o[3] = l);
    }, {
      priority: "high"
    });
  }
  /**
  * Attaches the {@link module:engine/model/model~Model#event:deleteContent} event listener to block the event when the fake
  * caret is active.
  *
  * This is required for cases that trigger {@link module:engine/model/model~Model#deleteContent `model.deleteContent()`}
  * before calling {@link module:engine/model/model~Model#insertContent `model.insertContent()`} like, for instance,
  * plain text pasting.
  */
  _enableDeleteContentIntegration() {
    const e = this.editor, r = this.editor.model.document.selection;
    this._listenToIfEnabled(e.model, "deleteContent", (s, [o]) => {
      if (o && !o.is("documentSelection"))
        return;
      Xr(r) && s.stop();
    }, {
      priority: "high"
    });
  }
}
function rN(i, t, e) {
  const n = i.createUIElement("div", {
    class: "ck ck-reset_all ck-widget__type-around"
  }, function(r) {
    const s = this.toDomElement(r);
    return sN(s, t), oN(s), s;
  });
  i.insert(i.createPositionAt(e, "end"), n);
}
function sN(i, t) {
  for (const e of vk) {
    const n = new li({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-widget__type-around__button",
          `ck-widget__type-around__button_${e}`
        ],
        title: t[e],
        "aria-hidden": "true"
      },
      children: [
        i.ownerDocument.importNode(nN, !0)
      ]
    });
    i.appendChild(n.render());
  }
}
function oN(i) {
  const t = new li({
    tag: "div",
    attributes: {
      class: [
        "ck",
        "ck-widget__type-around__fake-caret"
      ]
    }
  });
  i.appendChild(t.render());
}
function aN(i, t) {
  let e = t;
  for (const n of t.getAncestors({
    parentFirst: !0
  })) {
    if (n.childCount > 1 || i.isLimit(n))
      break;
    e = n;
  }
  return e;
}
function lN(i) {
  const t = i.model;
  return (e, n) => {
    const r = n.keyCode == Ke.arrowup, s = n.keyCode == Ke.arrowdown, o = n.shiftKey, a = t.document.selection;
    if (!r && !s)
      return;
    const l = s;
    if (o && dN(a, l))
      return;
    const c = cN(i, a, l);
    if (c) {
      if (c.isCollapsed) {
        if (a.isCollapsed)
          return;
        if (o)
          return;
      }
      (c.isCollapsed || uN(i, c, l)) && (t.change((u) => {
        const d = l ? c.end : c.start;
        if (o) {
          const f = t.createSelection(a.anchor);
          f.setFocus(d), u.setSelection(f);
        } else
          u.setSelection(d);
      }), e.stop(), n.preventDefault(), n.stopPropagation());
    }
  };
}
function cN(i, t, e) {
  const n = i.model;
  if (e) {
    const r = t.isCollapsed ? t.focus : t.getLastPosition(), s = rw(n, r, "forward");
    if (!s)
      return null;
    const o = n.createRange(r, s), a = sw(n.schema, o, "backward");
    return a ? n.createRange(r, a) : null;
  } else {
    const r = t.isCollapsed ? t.focus : t.getFirstPosition(), s = rw(n, r, "backward");
    if (!s)
      return null;
    const o = n.createRange(s, r), a = sw(n.schema, o, "forward");
    return a ? n.createRange(a, r) : null;
  }
}
function rw(i, t, e) {
  const n = i.schema, r = i.createRangeIn(t.root), s = e == "forward" ? "elementStart" : "elementEnd";
  for (const { previousPosition: o, item: a, type: l } of r.getWalker({
    startPosition: t,
    direction: e
  })) {
    if (n.isLimit(a) && !n.isInline(a))
      return o;
    if (l == s && n.isBlock(a))
      return null;
  }
  return null;
}
function sw(i, t, e) {
  const n = e == "backward" ? t.end : t.start;
  if (i.checkChild(n, "$text"))
    return n;
  for (const { nextPosition: r } of t.getWalker({
    direction: e
  }))
    if (i.checkChild(r, "$text"))
      return r;
  return null;
}
function uN(i, t, e) {
  const n = i.model, r = i.view.domConverter;
  if (e) {
    const c = n.createSelection(t.start);
    n.modifySelection(c), !c.focus.isAtEnd && !t.start.isEqual(c.focus) && (t = n.createRange(c.focus, t.end));
  }
  const s = i.mapper.toViewRange(t), o = r.viewRangeToDom(s), a = je.getDomRangeRects(o);
  let l;
  for (const c of a) {
    if (l === void 0) {
      l = Math.round(c.bottom);
      continue;
    }
    if (Math.round(c.top) >= l)
      return !1;
    l = Math.max(l, Math.round(c.bottom));
  }
  return !0;
}
function dN(i, t) {
  return !i.isCollapsed && i.isBackward == t;
}
class oo extends V {
  constructor() {
    super(...arguments);
    /**
    * Holds previously selected widgets.
    */
    h(this, "_previouslySelected", /* @__PURE__ */ new Set());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Widget";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      iN,
      Fr
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.editing.view, r = n.document, s = e.t;
    this.editor.editing.downcastDispatcher.on("selection", (o, a, l) => {
      const c = l.writer, u = a.selection;
      if (u.isCollapsed)
        return;
      const d = u.getSelectedElement();
      if (!d)
        return;
      const f = e.editing.mapper.toViewElement(d);
      Gt(f) && l.consumable.consume(u, "selection") && c.setSelection(c.createRangeOn(f), {
        fake: !0,
        label: KL(f)
      });
    }), this.editor.editing.downcastDispatcher.on("selection", (o, a, l) => {
      this._clearPreviouslySelectedWidgets(l.writer);
      const c = l.writer, u = c.document.selection;
      let d = null;
      for (const f of u.getRanges())
        for (const m of f) {
          const g = m.item;
          Gt(g) && !mN(g, d) && (c.addClass(nw, g), this._previouslySelected.add(g), d = g);
        }
    }, {
      priority: "low"
    }), n.addObserver(Hd), this.listenTo(r, "mousedown", (...o) => this._onMousedown(...o)), this.listenTo(r, "arrowKey", (...o) => {
      this._handleSelectionChangeOnArrowKeyPress(...o);
    }, {
      context: [
        Gt,
        "$text"
      ]
    }), this.listenTo(r, "arrowKey", (...o) => {
      this._preventDefaultOnArrowKeyPress(...o);
    }, {
      context: "$root"
    }), this.listenTo(r, "arrowKey", lN(this.editor.editing), {
      context: "$text"
    }), this.listenTo(r, "delete", (o, a) => {
      this._handleDelete(a.direction == "forward") && (a.preventDefault(), o.stop());
    }, {
      context: "$root"
    }), this.listenTo(r, "tab", (o, a) => {
      o.eventPhase == "atTarget" && (a.shiftKey || this._selectFirstNestedEditable() && (a.preventDefault(), o.stop()));
    }, {
      context: Gt,
      priority: "low"
    }), this.listenTo(r, "tab", (o, a) => {
      a.shiftKey && this._selectAncestorWidget() && (a.preventDefault(), o.stop());
    }, {
      priority: "low"
    }), this.listenTo(r, "keydown", (o, a) => {
      a.keystroke == Ke.esc && this._selectAncestorWidget() && (a.preventDefault(), o.stop());
    }, {
      priority: "low"
    }), e.accessibility.addKeystrokeInfoGroup({
      id: "widget",
      label: s("Keystrokes that can be used when a widget is selected (for example: image, table, etc.)"),
      keystrokes: [
        {
          label: s("Move focus from an editable area back to the parent widget"),
          keystroke: "Esc"
        },
        {
          label: s("Insert a new paragraph directly after a widget"),
          keystroke: "Enter"
        },
        {
          label: s("Insert a new paragraph directly before a widget"),
          keystroke: "Shift+Enter"
        },
        {
          label: s("Move the caret to allow typing directly before a widget"),
          keystroke: [
            [
              "arrowup"
            ],
            [
              "arrowleft"
            ]
          ]
        },
        {
          label: s("Move the caret to allow typing directly after a widget"),
          keystroke: [
            [
              "arrowdown"
            ],
            [
              "arrowright"
            ]
          ]
        }
      ]
    });
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:mousedown mousedown} events on widget elements.
  */
  _onMousedown(e, n) {
    const r = this.editor, s = r.editing.view, o = s.document;
    let a = n.target;
    if (!a)
      return;
    if (n.domEvent.detail >= 3) {
      this._selectBlockContent(a) && n.preventDefault();
      return;
    }
    if (!Gt(a)) {
      const c = hN(a);
      if (!c)
        return;
      if (Gt(c))
        a = c;
      else {
        const u = fN(s, n);
        if (u && Gt(u))
          a = u;
        else
          return;
      }
    }
    Me.isAndroid && n.preventDefault(), o.isFocused || s.focus();
    const l = r.editing.mapper.toModelElement(a);
    this._setSelectionOverElement(l);
  }
  /**
  * Selects entire block content, e.g. on triple click it selects entire paragraph.
  */
  _selectBlockContent(e) {
    const n = this.editor, r = n.model, s = n.editing.mapper, o = r.schema, a = s.findMappedViewAncestor(this.editor.editing.view.createPositionAt(e, 0)), l = gN(s.toModelElement(a), r.schema);
    return l ? (r.change((c) => {
      const u = o.isLimit(l) ? null : pN(c.createPositionAfter(l), o), d = c.createPositionAt(l, 0), f = u ? c.createPositionAt(u, 0) : c.createPositionAt(l, "end");
      c.setSelection(c.createRange(d, f));
    }), !0) : !1;
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:keydown keydown} events and changes
  * the model selection when:
  *
  * * arrow key is pressed when the widget is selected,
  * * the selection is next to a widget and the widget should become selected upon the arrow key press.
  *
  * See {@link #_preventDefaultOnArrowKeyPress}.
  */
  _handleSelectionChangeOnArrowKeyPress(e, n) {
    const r = n.keyCode, s = this.editor.model, o = s.schema, a = s.document.selection, l = a.getSelectedElement(), c = Zg(r, this.editor.locale.contentLanguageDirection), u = c == "down" || c == "right", d = c == "up" || c == "down";
    if (l && o.isObject(l)) {
      const m = u ? a.getLastPosition() : a.getFirstPosition(), g = o.getNearestSelectionRange(m, u ? "forward" : "backward");
      g && (s.change((p) => {
        p.setSelection(g);
      }), n.preventDefault(), e.stop());
      return;
    }
    if (!a.isCollapsed && !n.shiftKey) {
      const m = a.getFirstPosition(), g = a.getLastPosition(), p = m.nodeAfter, b = g.nodeBefore;
      (p && o.isObject(p) || b && o.isObject(b)) && (s.change((v) => {
        v.setSelection(u ? g : m);
      }), n.preventDefault(), e.stop());
      return;
    }
    if (!a.isCollapsed)
      return;
    const f = this._getObjectElementNextToSelection(u);
    if (f && o.isObject(f)) {
      if (o.isInline(f) && d)
        return;
      this._setSelectionOverElement(f), n.preventDefault(), e.stop();
    }
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:keydown keydown} events and prevents
  * the default browser behavior to make sure the fake selection is not being moved from a fake selection
  * container.
  *
  * See {@link #_handleSelectionChangeOnArrowKeyPress}.
  */
  _preventDefaultOnArrowKeyPress(e, n) {
    const r = this.editor.model, s = r.schema, o = r.document.selection.getSelectedElement();
    o && s.isObject(o) && (n.preventDefault(), e.stop());
  }
  /**
  * Handles delete keys: backspace and delete.
  *
  * @param isForward Set to true if delete was performed in forward direction.
  * @returns Returns `true` if keys were handled correctly.
  */
  _handleDelete(e) {
    const r = this.editor.model.document.selection;
    if (!this.editor.model.canEditAt(r) || !r.isCollapsed)
      return;
    const s = this._getObjectElementNextToSelection(e);
    if (s)
      return this.editor.model.change((o) => {
        let a = r.anchor.parent;
        for (; a.isEmpty; ) {
          const l = a;
          a = l.parent, o.remove(l);
        }
        this._setSelectionOverElement(s);
      }), !0;
  }
  /**
  * Sets {@link module:engine/model/selection~Selection document's selection} over given element.
  *
  * @internal
  */
  _setSelectionOverElement(e) {
    this.editor.model.change((n) => {
      n.setSelection(n.createRangeOn(e));
    });
  }
  /**
  * Checks if {@link module:engine/model/element~Element element} placed next to the current
  * {@link module:engine/model/selection~Selection model selection} exists and is marked in
  * {@link module:engine/model/schema~Schema schema} as `object`.
  *
  * @internal
  * @param forward Direction of checking.
  */
  _getObjectElementNextToSelection(e) {
    const n = this.editor.model, r = n.schema, s = n.document.selection, o = n.createSelection(s);
    if (n.modifySelection(o, {
      direction: e ? "forward" : "backward"
    }), o.isEqual(s))
      return null;
    const a = e ? o.focus.nodeBefore : o.focus.nodeAfter;
    return a && r.isObject(a) ? a : null;
  }
  /**
  * Removes CSS class from previously selected widgets.
  */
  _clearPreviouslySelectedWidgets(e) {
    for (const n of this._previouslySelected)
      e.removeClass(nw, n);
    this._previouslySelected.clear();
  }
  /**
  * Moves the document selection into the first nested editable.
  */
  _selectFirstNestedEditable() {
    const e = this.editor, r = this.editor.editing.view.document;
    for (const s of r.selection.getFirstRange().getItems())
      if (s.is("editableElement")) {
        const o = e.editing.mapper.toModelElement(s);
        /* istanbul ignore next -- @preserve */
        if (!o)
          continue;
        const a = e.model.createPositionAt(o, 0), l = e.model.schema.getNearestSelectionRange(a, "forward");
        return e.model.change((c) => {
          c.setSelection(l);
        }), !0;
      }
    return !1;
  }
  /**
  * Updates the document selection so that it selects first ancestor widget.
  */
  _selectAncestorWidget() {
    const e = this.editor, n = e.editing.mapper, s = e.editing.view.document.selection.getFirstPosition().parent, a = (s.is("$text") ? s.parent : s).findAncestor(Gt);
    if (!a)
      return !1;
    const l = n.toModelElement(a);
    /* istanbul ignore next -- @preserve */
    return l ? (e.model.change((c) => {
      c.setSelection(l, "on");
    }), !0) : !1;
  }
}
function hN(i) {
  let t = i;
  for (; t; ) {
    if (t.is("editableElement") || Gt(t))
      return t;
    t = t.parent;
  }
  return null;
}
function fN(i, t) {
  const e = _2(t.domEvent);
  let n = null;
  if (e ? n = i.domConverter.domRangeToView(e) : n = i.createRange(i.createPositionAt(t.target, 0)), !n)
    return null;
  const r = n.start;
  if (!r.parent)
    return null;
  let s = r.parent;
  return r.parent.is("editableElement") && (r.isAtEnd && r.nodeBefore ? s = r.nodeBefore : r.isAtStart && r.nodeAfter && (s = r.nodeAfter)), s.is("$text") ? s.parent : s;
}
function mN(i, t) {
  return t ? Array.from(i.getAncestors()).includes(t) : !1;
}
function gN(i, t) {
  for (const e of i.getAncestors({
    includeSelf: !0,
    parentFirst: !0
  })) {
    if (t.checkChild(e, "$text"))
      return e;
    if (t.isLimit(e) && !t.isObject(e))
      break;
  }
  return null;
}
function pN(i, t) {
  const e = new Vr({
    startPosition: i
  });
  for (const { item: n } of e) {
    if (t.isLimit(n) || !n.is("element"))
      return null;
    if (t.checkChild(n, "$text"))
      return n;
  }
  return null;
}
class Zu extends V {
  constructor() {
    super(...arguments);
    /**
    * A map of toolbar definitions.
    */
    h(this, "_toolbarDefinitions", /* @__PURE__ */ new Map());
    h(this, "_balloon");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Qi
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "WidgetToolbarRepository";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    if (e.plugins.has("BalloonToolbar")) {
      const n = e.plugins.get("BalloonToolbar");
      this.listenTo(n, "show", (r) => {
        bN(e.editing.view.document.selection) && r.stop();
      }, {
        priority: "high"
      });
    }
    this._balloon = this.editor.plugins.get("ContextualBalloon"), this.on("change:isEnabled", () => {
      this._updateToolbarsVisibility();
    }), this.listenTo(e.ui, "update", () => {
      this._updateToolbarsVisibility();
    }), this.listenTo(e.ui.focusTracker, "change:isFocused", () => {
      this._updateToolbarsVisibility();
    }, {
      priority: "low"
    });
  }
  destroy() {
    super.destroy();
    for (const e of this._toolbarDefinitions.values())
      e.view.destroy();
  }
  /**
  * Registers toolbar in the WidgetToolbarRepository. It renders it in the `ContextualBalloon` based on the value of the invoked
  * `getRelatedElement` function. Toolbar items are gathered from `items` array.
  * The balloon's CSS class is by default `ck-toolbar-container` and may be override with the `balloonClassName` option.
  *
  * Note: This method should be called in the {@link module:core/plugin~PluginInterface#afterInit `Plugin#afterInit()`}
  * callback (or later) to make sure that the given toolbar items were already registered by other plugins.
  *
  * @param toolbarId An id for the toolbar. Used to
  * @param options.ariaLabel Label used by assistive technologies to describe this toolbar element.
  * @param options.items Array of toolbar items.
  * @param options.getRelatedElement Callback which returns an element the toolbar should be attached to.
  * @param options.balloonClassName CSS class for the widget balloon.
  */
  register(e, { ariaLabel: n, items: r, getRelatedElement: s, balloonClassName: o = "ck-toolbar-container" }) {
    if (!r.length) {
      Xe("widget-toolbar-no-items", {
        toolbarId: e
      });
      return;
    }
    const a = this.editor, l = a.t, c = new fs(a.locale);
    if (c.ariaLabel = n || l("Widget toolbar"), this._toolbarDefinitions.has(e))
      throw new T("widget-toolbar-duplicated", this, {
        toolbarId: e
      });
    const u = {
      view: c,
      getRelatedElement: s,
      balloonClassName: o,
      itemsConfig: r,
      initialized: !1
    };
    a.ui.addToolbar(c, {
      isContextual: !0,
      beforeFocus: () => {
        const d = s(a.editing.view.document.selection);
        d && this._showToolbar(u, d);
      },
      afterBlur: () => {
        this._hideToolbar(u);
      }
    }), this._toolbarDefinitions.set(e, u);
  }
  /**
  * Iterates over stored toolbars and makes them visible or hidden.
  */
  _updateToolbarsVisibility() {
    let e = 0, n = null, r = null;
    for (const s of this._toolbarDefinitions.values()) {
      const o = s.getRelatedElement(this.editor.editing.view.document.selection);
      if (!this.isEnabled || !o)
        this._isToolbarInBalloon(s) && this._hideToolbar(s);
      else if (!this.editor.ui.focusTracker.isFocused)
        this._isToolbarVisible(s) && this._hideToolbar(s);
      else {
        const a = o.getAncestors().length;
        a > e && (e = a, n = o, r = s);
      }
    }
    r && this._showToolbar(r, n);
  }
  /**
  * Hides the given toolbar.
  */
  _hideToolbar(e) {
    this._balloon.remove(e.view), this.stopListening(this._balloon, "change:visibleView");
  }
  /**
  * Shows up the toolbar if the toolbar is not visible.
  * Otherwise, repositions the toolbar's balloon when toolbar's view is the most top view in balloon stack.
  *
  * It might happen here that the toolbar's view is under another view. Then do nothing as the other toolbar view
  * should be still visible after the {@link module:ui/editorui/editorui~EditorUI#event:update}.
  */
  _showToolbar(e, n) {
    this._isToolbarVisible(e) ? ow(this.editor, n) : this._isToolbarInBalloon(e) || (e.initialized || (e.initialized = !0, e.view.fillFromConfig(e.itemsConfig, this.editor.ui.componentFactory)), this._balloon.add({
      view: e.view,
      position: wk(this.editor, n),
      balloonClassName: e.balloonClassName
    }), this.listenTo(this._balloon, "change:visibleView", () => {
      for (const r of this._toolbarDefinitions.values())
        if (this._isToolbarVisible(r)) {
          const s = r.getRelatedElement(this.editor.editing.view.document.selection);
          ow(this.editor, s);
        }
    }));
  }
  _isToolbarVisible(e) {
    return this._balloon.visibleView === e.view;
  }
  _isToolbarInBalloon(e) {
    return this._balloon.hasView(e.view);
  }
}
function ow(i, t) {
  const e = i.plugins.get("ContextualBalloon"), n = wk(i, t);
  e.updatePosition(n);
}
function wk(i, t) {
  const e = i.editing.view, n = fn.defaultPositions;
  return {
    target: e.domConverter.mapViewToDom(t),
    positions: [
      n.northArrowSouth,
      n.northArrowSouthWest,
      n.northArrowSouthEast,
      n.southArrowNorth,
      n.southArrowNorthWest,
      n.southArrowNorthEast,
      n.viewportStickyNorth
    ]
  };
}
function bN(i) {
  const t = i.getSelectedElement();
  return !!(t && Gt(t));
}
class vN extends (/* @__PURE__ */ st()) {
  /**
  * @param options Resizer options.
  */
  constructor(e) {
    super();
    /**
    * The reference point of the resizer where the dragging started. It is used to measure the distance the user cursor
    * traveled, so how much the image should be enlarged.
    * This information is only known after the DOM was rendered, so it will be updated later.
    *
    * @internal
    */
    h(this, "_referenceCoordinates");
    /**
    * Resizer options.
    */
    h(this, "_options");
    /**
    * The original width (pixels) of the resized object when the resize process was started.
    *
    * @readonly
    */
    h(this, "_originalWidth");
    /**
    * The original height (pixels) of the resized object when the resize process was started.
    *
    * @readonly
    */
    h(this, "_originalHeight");
    /**
    * The original width (percents) of the resized object when the resize process was started.
    *
    * @readonly
    */
    h(this, "_originalWidthPercents");
    /**
    * A width to height ratio of the resized image.
    *
    * @readonly
    */
    h(this, "_aspectRatio");
    this.set("activeHandlePosition", null), this.set("proposedWidthPercents", null), this.set("proposedWidth", null), this.set("proposedHeight", null), this.set("proposedHandleHostWidth", null), this.set("proposedHandleHostHeight", null), this._options = e, this._referenceCoordinates = null;
  }
  /**
  * The original width (pixels) of the resized object when the resize process was started.
  */
  get originalWidth() {
    return this._originalWidth;
  }
  /**
  * The original height (pixels) of the resized object when the resize process was started.
  */
  get originalHeight() {
    return this._originalHeight;
  }
  /**
  * The original width (percents) of the resized object when the resize process was started.
  */
  get originalWidthPercents() {
    return this._originalWidthPercents;
  }
  /**
  * A width to height ratio of the resized image.
  */
  get aspectRatio() {
    return this._aspectRatio;
  }
  /**
  *
  * @param domResizeHandle The handle used to calculate the reference point.
  */
  begin(e, n, r) {
    const s = new je(n);
    this.activeHandlePosition = _N(e), this._referenceCoordinates = wN(n, CN(this.activeHandlePosition)), this._originalWidth = s.width, this._originalHeight = s.height, this._aspectRatio = s.width / s.height;
    const o = r.style.width;
    o && o.match(/^\d+(\.\d*)?%$/) ? this._originalWidthPercents = parseFloat(o) : this._originalWidthPercents = JL(r, s);
  }
  update(e) {
    this.proposedWidth = e.width, this.proposedHeight = e.height, this.proposedWidthPercents = e.widthPercents, this.proposedHandleHostWidth = e.handleHostWidth, this.proposedHandleHostHeight = e.handleHostHeight;
  }
}
function wN(i, t) {
  const e = new je(i), n = t.split("-"), r = {
    x: n[1] == "right" ? e.right : e.left,
    y: n[0] == "bottom" ? e.bottom : e.top
  };
  return r.x += i.ownerDocument.defaultView.scrollX, r.y += i.ownerDocument.defaultView.scrollY, r;
}
function yN(i) {
  return `ck-widget__resizer__handle-${i}`;
}
function _N(i) {
  const t = [
    "top-left",
    "top-right",
    "bottom-right",
    "bottom-left"
  ];
  for (const e of t)
    if (i.classList.contains(yN(e)))
      return e;
}
function CN(i) {
  const t = i.split("-"), e = {
    top: "bottom",
    bottom: "top",
    left: "right",
    right: "left"
  };
  return `${e[t[0]]}-${e[t[1]]}`;
}
class kN extends de {
  constructor() {
    super();
    const t = this.bindTemplate;
    this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-size-view",
          t.to("_viewPosition", (e) => e ? `ck-orientation-${e}` : "")
        ],
        style: {
          display: t.if("_isVisible", "none", (e) => !e)
        }
      },
      children: [
        {
          text: t.to("_label")
        }
      ]
    });
  }
  /**
  * A method used for binding the `SizeView` instance properties to the `ResizeState` instance observable properties.
  *
  * @internal
  * @param options An object defining the resizer options, used for setting the proper size label.
  * @param resizeState The `ResizeState` class instance, used for keeping the `SizeView` state up to date.
  */
  _bindToState(t, e) {
    this.bind("_isVisible").to(e, "proposedWidth", e, "proposedHeight", (n, r) => n !== null && r !== null), this.bind("_label").to(e, "proposedHandleHostWidth", e, "proposedHandleHostHeight", e, "proposedWidthPercents", (n, r, s) => t.unit === "px" ? `${n}×${r}` : `${s}%`), this.bind("_viewPosition").to(
      e,
      "activeHandlePosition",
      e,
      "proposedHandleHostWidth",
      e,
      "proposedHandleHostHeight",
      // If the widget is too small to contain the size label, display the label above.
      (n, r, s) => r < 50 || s < 50 ? "above-center" : n
    );
  }
  /**
  * A method used for cleaning up. It removes the bindings and hides the view.
  *
  * @internal
  */
  _dismiss() {
    this.unbind(), this._isVisible = !1;
  }
}
class aw extends (/* @__PURE__ */ st()) {
  /**
  * @param options Resizer options.
  */
  constructor(e) {
    super();
    /**
    * Stores the state of the resizable host geometry, such as the original width, the currently proposed height, etc.
    *
    * Note that a new state is created for each resize transaction.
    */
    h(this, "_state");
    /**
    * A view displaying the proposed new element size during the resizing.
    */
    h(this, "_sizeView");
    /**
    * Options passed to the {@link #constructor}.
    */
    h(this, "_options");
    /**
    * A wrapper that is controlled by the resizer. This is usually a widget element.
    */
    h(this, "_viewResizerWrapper", null);
    /**
    * The width of the resized {@link module:widget/widgetresize~ResizerOptions#viewElement viewElement} before the resizing started.
    */
    h(this, "_initialViewWidth");
    this._options = e, this.set("isEnabled", !0), this.set("isSelected", !1), this.bind("isVisible").to(this, "isEnabled", this, "isSelected", (n, r) => n && r), this.decorate("begin"), this.decorate("cancel"), this.decorate("commit"), this.decorate("updateSize"), this.on("commit", (n) => {
      !this.state.proposedWidth && !this.state.proposedWidthPercents && (this._cleanup(), n.stop());
    }, {
      priority: "high"
    });
  }
  /**
  * Stores the state of the resizable host geometry, such as the original width, the currently proposed height, etc.
  *
  * Note that a new state is created for each resize transaction.
  */
  get state() {
    return this._state;
  }
  /**
  * Makes resizer visible in the UI.
  */
  show() {
    this._options.editor.editing.view.change((n) => {
      n.removeClass("ck-hidden", this._viewResizerWrapper);
    });
  }
  /**
  * Hides resizer in the UI.
  */
  hide() {
    this._options.editor.editing.view.change((n) => {
      n.addClass("ck-hidden", this._viewResizerWrapper);
    });
  }
  /**
  * Attaches the resizer to the DOM.
  */
  attach() {
    const e = this, n = this._options.viewElement;
    this._options.editor.editing.view.change((s) => {
      const o = s.createUIElement("div", {
        class: "ck ck-reset_all ck-widget__resizer"
      }, function(a) {
        const l = this.toDomElement(a);
        return e._appendHandles(l), e._appendSizeUI(l), l;
      });
      s.insert(s.createPositionAt(n, "end"), o), s.addClass("ck-widget_with-resizer", n), this._viewResizerWrapper = o, this.isVisible || this.hide();
    }), this.on("change:isVisible", () => {
      this.isVisible ? (this.show(), this.redraw()) : this.hide();
    });
  }
  /**
  * Starts the resizing process.
  *
  * Creates a new {@link #state} for the current process.
  *
  * @fires begin
  * @param domResizeHandle Clicked handle.
  */
  begin(e) {
    this._state = new vN(this._options), this._sizeView._bindToState(this._options, this.state), this._initialViewWidth = this._options.viewElement.getStyle("width"), this.state.begin(e, this._getHandleHost(), this._getResizeHost());
  }
  /**
  * Updates the proposed size based on `domEventData`.
  *
  * @fires updateSize
  */
  updateSize(e) {
    const n = this._proposeNewSize(e);
    this._options.editor.editing.view.change((u) => {
      const d = this._options.unit || "%", f = (d === "%" ? n.widthPercents : n.width) + d;
      u.setStyle("width", f, this._options.viewElement);
    });
    const s = this._getHandleHost(), o = new je(s), a = Math.round(o.width), l = Math.round(o.height), c = new je(s);
    n.width = Math.round(c.width), n.height = Math.round(c.height), this.redraw(o), this.state.update({
      ...n,
      handleHostWidth: a,
      handleHostHeight: l
    });
  }
  /**
  * Applies the geometry proposed with the resizer.
  *
  * @fires commit
  */
  commit() {
    const e = this._options.unit || "%", n = (e === "%" ? this.state.proposedWidthPercents : this.state.proposedWidth) + e;
    this._options.editor.editing.view.change(() => {
      this._cleanup(), this._options.onCommit(n);
    });
  }
  /**
  * Cancels and rejects the proposed resize dimensions, hiding the UI.
  *
  * @fires cancel
  */
  cancel() {
    this._cleanup();
  }
  /**
  * Destroys the resizer.
  */
  destroy() {
    this.cancel();
  }
  /**
  * Redraws the resizer.
  *
  * @param handleHostRect Handle host rectangle might be given to improve performance.
  */
  redraw(e) {
    const n = this._domResizerWrapper;
    if (!TN(n))
      return;
    const r = n.parentElement, s = this._getHandleHost(), o = this._viewResizerWrapper, a = [
      o.getStyle("width"),
      o.getStyle("height"),
      o.getStyle("left"),
      o.getStyle("top")
    ];
    let l;
    if (r.isSameNode(s)) {
      const c = e || new je(s);
      l = [
        c.width + "px",
        c.height + "px",
        void 0,
        void 0
      ];
    } else
      l = [
        s.offsetWidth + "px",
        s.offsetHeight + "px",
        s.offsetLeft + "px",
        s.offsetTop + "px"
      ];
    Fn(a, l) !== "same" && this._options.editor.editing.view.change((c) => {
      c.setStyle({
        width: l[0],
        height: l[1],
        left: l[2],
        top: l[3]
      }, o);
    });
  }
  containsHandle(e) {
    return this._domResizerWrapper.contains(e);
  }
  static isResizeHandle(e) {
    return e.classList.contains("ck-widget__resizer__handle");
  }
  /**
  * Cleans up the context state.
  */
  _cleanup() {
    this._sizeView._dismiss(), this._options.editor.editing.view.change((n) => {
      n.setStyle("width", this._initialViewWidth, this._options.viewElement);
    });
  }
  /**
  * Calculates the proposed size as the resize handles are dragged.
  *
  * @param domEventData Event data that caused the size update request. It should be used to calculate the proposed size.
  */
  _proposeNewSize(e) {
    const n = this.state, r = EN(e), s = this._options.isCentered ? this._options.isCentered(this) : !0, o = {
      x: n._referenceCoordinates.x - (r.x + n.originalWidth),
      y: r.y - n.originalHeight - n._referenceCoordinates.y
    };
    s && n.activeHandlePosition.endsWith("-right") && (o.x = r.x - (n._referenceCoordinates.x + n.originalWidth)), s && (o.x *= 2);
    let a = Math.abs(n.originalWidth + o.x), l = Math.abs(n.originalHeight + o.y);
    return (a / n.aspectRatio > l ? "width" : "height") == "width" ? l = a / n.aspectRatio : a = l * n.aspectRatio, {
      width: Math.round(a),
      height: Math.round(l),
      widthPercents: Math.min(Math.round(n.originalWidthPercents / n.originalWidth * a * 100) / 100, 100)
    };
  }
  /**
  * Obtains the resize host.
  *
  * Resize host is an object that receives dimensions which are the result of resizing.
  */
  _getResizeHost() {
    const e = this._domResizerWrapper.parentElement;
    return this._options.getResizeHost(e);
  }
  /**
  * Obtains the handle host.
  *
  * Handle host is an object that the handles are aligned to.
  *
  * Handle host will not always be an entire widget itself. Take an image as an example. The image widget
  * contains an image and a caption. Only the image should be surrounded with handles.
  */
  _getHandleHost() {
    const e = this._domResizerWrapper.parentElement;
    return this._options.getHandleHost(e);
  }
  /**
  * DOM container of the entire resize UI.
  *
  * Note that this property will have a value only after the element bound with the resizer is rendered
  * (otherwise `null`).
  */
  get _domResizerWrapper() {
    return this._options.editor.editing.view.domConverter.mapViewToDom(this._viewResizerWrapper);
  }
  /**
  * Renders the resize handles in the DOM.
  *
  * @param domElement The resizer wrapper.
  */
  _appendHandles(e) {
    const n = [
      "top-left",
      "top-right",
      "bottom-right",
      "bottom-left"
    ];
    for (const r of n)
      e.appendChild(new li({
        tag: "div",
        attributes: {
          class: `ck-widget__resizer__handle ${xN(r)}`
        }
      }).render());
  }
  /**
  * Sets up the {@link #_sizeView} property and adds it to the passed `domElement`.
  */
  _appendSizeUI(e) {
    this._sizeView = new kN(), this._sizeView.render(), e.appendChild(this._sizeView.element);
  }
}
function xN(i) {
  return `ck-widget__resizer__handle-${i}`;
}
function EN(i) {
  return {
    x: i.pageX,
    y: i.pageY
  };
}
function TN(i) {
  return i && i.ownerDocument && i.ownerDocument.contains(i);
}
class hf extends V {
  constructor() {
    super(...arguments);
    /**
    * A map of resizers created using this plugin instance.
    */
    h(this, "_resizers", /* @__PURE__ */ new Map());
    h(this, "_observer");
    h(this, "_redrawSelectedResizerThrottled");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "WidgetResize";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor.editing, n = ae.window.document;
    this.set("selectedResizer", null), this.set("_activeResizer", null), e.view.addObserver(Hd), this._observer = new (Xn())(), this.listenTo(e.view.document, "mousedown", this._mouseDownListener.bind(this), {
      priority: "high"
    }), this._observer.listenTo(n, "mousemove", this._mouseMoveListener.bind(this)), this._observer.listenTo(n, "mouseup", this._mouseUpListener.bind(this)), this._redrawSelectedResizerThrottled = Jo(() => this.redrawSelectedResizer(), 200), this.editor.ui.on("update", this._redrawSelectedResizerThrottled), this.editor.model.document.on("change", () => {
      for (const [s, o] of this._resizers)
        s.isAttached() || (this._resizers.delete(s), o.destroy());
    }, {
      priority: "lowest"
    }), this._observer.listenTo(ae.window, "resize", this._redrawSelectedResizerThrottled);
    const r = this.editor.editing.view.document.selection;
    r.on("change", () => {
      const s = r.getSelectedElement(), o = this.getResizerByViewElement(s) || null;
      o ? this.select(o) : this.deselect();
    });
  }
  /**
  * Redraws the selected resizer if there is any selected resizer and if it is visible.
  */
  redrawSelectedResizer() {
    this.selectedResizer && this.selectedResizer.isVisible && this.selectedResizer.redraw();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._observer.stopListening();
    for (const e of this._resizers.values())
      e.destroy();
    this._redrawSelectedResizerThrottled.cancel();
  }
  /**
  * Marks resizer as selected.
  */
  select(e) {
    this.deselect(), this.selectedResizer = e, this.selectedResizer.isSelected = !0;
  }
  /**
  * Deselects currently set resizer.
  */
  deselect() {
    this.selectedResizer && (this.selectedResizer.isSelected = !1), this.selectedResizer = null;
  }
  /**
  * @param options Resizer options.
  */
  attachTo(e) {
    const n = new aw(e), r = this.editor.plugins;
    if (n.attach(), r.has("WidgetToolbarRepository")) {
      const a = r.get("WidgetToolbarRepository");
      n.on("begin", () => {
        a.forceDisabled("resize");
      }, {
        priority: "lowest"
      }), n.on("cancel", () => {
        a.clearForceDisabled("resize");
      }, {
        priority: "highest"
      }), n.on("commit", () => {
        a.clearForceDisabled("resize");
      }, {
        priority: "highest"
      });
    }
    this._resizers.set(e.viewElement, n);
    const o = this.editor.editing.view.document.selection.getSelectedElement();
    return this.getResizerByViewElement(o) == n && this.select(n), n;
  }
  /**
  * Returns a resizer created for a given view element (widget element).
  *
  * @param viewElement View element associated with the resizer.
  */
  getResizerByViewElement(e) {
    return this._resizers.get(e);
  }
  /**
  * Returns a resizer that contains a given resize handle.
  */
  _getResizerByHandle(e) {
    for (const n of this._resizers.values())
      if (n.containsHandle(e))
        return n;
  }
  /**
  * @param domEventData Native DOM event.
  */
  _mouseDownListener(e, n) {
    const r = n.domTarget;
    aw.isResizeHandle(r) && (this._activeResizer = this._getResizerByHandle(r) || null, this._activeResizer && (this._activeResizer.begin(r), e.stop(), n.preventDefault()));
  }
  /**
  * @param domEventData Native DOM event.
  */
  _mouseMoveListener(e, n) {
    this._activeResizer && this._activeResizer.updateSize(n);
  }
  _mouseUpListener() {
    this._activeResizer && (this._activeResizer.commit(), this._activeResizer = null);
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class zl extends ws {
  constructor(e) {
    super(e);
    h(this, "domEventType", [
      "paste",
      "copy",
      "cut",
      "drop",
      "dragover",
      "dragstart",
      "dragend",
      "dragenter",
      "dragleave"
    ]);
    const n = this.document;
    this.listenTo(n, "paste", r("clipboardInput"), {
      priority: "low"
    }), this.listenTo(n, "drop", r("clipboardInput"), {
      priority: "low"
    }), this.listenTo(n, "dragover", r("dragging"), {
      priority: "low"
    });
    function r(s) {
      return (o, a) => {
        a.preventDefault();
        const l = a.dropRange ? [
          a.dropRange
        ] : null, c = new Br(n, s);
        n.fire(c, {
          dataTransfer: a.dataTransfer,
          method: o.name,
          targetRanges: l,
          target: a.target,
          domEvent: a.domEvent
        }), c.stop.called && a.stopPropagation();
      };
    }
  }
  onDomEvent(e) {
    const n = "clipboardData" in e ? e.clipboardData : e.dataTransfer, r = e.type == "drop" || e.type == "paste", s = {
      dataTransfer: new W2(n, {
        cacheFiles: r
      })
    };
    if (e.type == "drop" || e.type == "dragover") {
      const o = _2(e);
      s.dropRange = o && this.view.domConverter.domRangeToView(o);
    }
    this.fire(e.type, e, s);
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function SN(i) {
  return i = i.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\r?\n\r?\n/g, "</p><p>").replace(/\r?\n/g, "<br>").replace(/\t/g, "&nbsp;&nbsp;&nbsp;&nbsp;").replace(/^\s/, "&nbsp;").replace(/\s$/, "&nbsp;").replace(/\s\s/g, " &nbsp;"), (i.includes("</p><p>") || i.includes("<br>")) && (i = `<p>${i}</p>`), i;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function AN(i) {
  return i.replace(/<span(?: class="Apple-converted-space"|)>(\s+)<\/span>/g, (t, e) => e.length == 1 ? " " : e).replace(/<!--[\s\S]*?-->/g, "");
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const lw = [
  "figcaption",
  "li"
], cw = [
  "ol",
  "ul"
];
function yk(i) {
  if (i.is("$text") || i.is("$textProxy"))
    return i.data;
  if (i.is("element", "img") && i.hasAttribute("alt"))
    return i.getAttribute("alt");
  if (i.is("element", "br"))
    return `
`;
  let t = "", e = null;
  for (const n of i.getChildren())
    t += PN(n, e) + yk(n), e = n;
  return t;
}
function PN(i, t) {
  return t ? i.is("element", "li") && !i.isEmpty && i.getChild(0).is("containerElement") || cw.includes(i.name) && cw.includes(t.name) ? `

` : !i.is("containerElement") && !t.is("containerElement") ? "" : lw.includes(i.name) || lw.includes(t.name) ? `
` : i.is("element") && i.getCustomProperty("dataPipeline:transparentRendering") || t.is("element") && t.getCustomProperty("dataPipeline:transparentRendering") ? "" : `

` : "";
}
class gl extends V {
  constructor() {
    super(...arguments);
    /**
    * Map of marker names that can be copied.
    *
    * @internal
    */
    h(this, "_markersToCopy", /* @__PURE__ */ new Map());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ClipboardMarkersUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Registers marker name as copyable in clipboard pipeline.
  *
  * @param markerName Name of marker that can be copied.
  * @param config Configuration that describes what can be performed on specified marker.
  * @internal
  */
  _registerMarkerToCopy(e, n) {
    this._markersToCopy.set(e, n);
  }
  /**
  * Performs copy markers on provided selection and paste it to fragment returned from `getCopiedFragment`.
  *
  * 	1. Picks all markers in provided selection.
  * 	2. Inserts fake markers to document.
  * 	3. Gets copied selection fragment from document.
  * 	4. Removes fake elements from fragment and document.
  * 	5. Inserts markers in the place of removed fake markers.
  *
  * Due to selection modification, when inserting items, `getCopiedFragment` must *always* operate on `writer.model.document.selection'.
  * Do not use any other custom selection object within callback, as this will lead to out-of-bounds exceptions in rare scenarios.
  *
  * @param action Type of clipboard action.
  * @param writer An instance of the model writer.
  * @param selection Selection to be checked.
  * @param getCopiedFragment	Callback that performs copy of selection and returns it as fragment.
  * @internal
  */
  _copySelectedFragmentWithMarkers(e, n, r = (s) => s.model.getSelectedContent(s.model.document.selection)) {
    return this.editor.model.change((s) => {
      const o = s.model.document.selection;
      s.setSelection(n);
      const a = this._insertFakeMarkersIntoSelection(s, s.model.document.selection, e), l = r(s), c = this._removeFakeMarkersInsideElement(s, l);
      for (const [u, d] of Object.entries(a)) {
        c[u] || (c[u] = s.createRangeIn(l));
        for (const f of d)
          s.remove(f);
      }
      l.markers.clear();
      for (const [u, d] of Object.entries(c))
        l.markers.set(u, d);
      return s.setSelection(o), l;
    });
  }
  /**
  * Performs paste of markers on already pasted element.
  *
  * 	1. Inserts fake markers that are present in fragment element (such fragment will be processed in `getPastedDocumentElement`).
  * 	2. Calls `getPastedDocumentElement` and gets element that is inserted into root model.
  * 	3. Removes all fake markers present in transformed element.
  * 	4. Inserts new markers with removed fake markers ranges into pasted fragment.
  *
  * There are multiple edge cases that have to be considered before calling this function:
  *
  * 	* `markers` are inserted into the same element that must be later transformed inside `getPastedDocumentElement`.
  * 	* Fake marker elements inside `getPastedDocumentElement` can be cloned, but their ranges cannot overlap.
  * 	* If `duplicateOnPaste` is `true` in marker config then associated marker ID is regenerated before pasting.
  *
  * @param action Type of clipboard action.
  * @param markers Object that maps marker name to corresponding range.
  * @param getPastedDocumentElement Getter used to get target markers element.
  * @internal
  */
  _pasteMarkersIntoTransformedElement(e, n) {
    const r = this._getPasteMarkersFromRangeMap(e);
    return this.editor.model.change((s) => {
      const o = this._insertFakeMarkersElements(s, r), a = n(s), l = this._removeFakeMarkersInsideElement(s, a);
      for (const c of Object.values(o).flat())
        s.remove(c);
      for (const [c, u] of Object.entries(l))
        s.model.markers.has(c) || s.addMarker(c, {
          usingOperation: !0,
          affectsData: !0,
          range: u
        });
      return a;
    });
  }
  /**
  * Pastes document fragment with markers to document.
  * If `duplicateOnPaste` is `true` in marker config then associated markers IDs
  * are regenerated before pasting to avoid markers duplications in content.
  *
  * @param fragment Document fragment that should contain already processed by pipeline markers.
  * @internal
  */
  _pasteFragmentWithMarkers(e) {
    const n = this._getPasteMarkersFromRangeMap(e.markers);
    e.markers.clear();
    for (const r of n)
      e.markers.set(r.name, r.range);
    return this.editor.model.insertContent(e);
  }
  /**
  * In some situations we have to perform copy on selected fragment with certain markers. This function allows to temporarily bypass
  * restrictions on markers that we want to copy.
  *
  * This function executes `executor()` callback. For the duration of the callback, if the clipboard pipeline is used to copy
  * content, markers with the specified name will be copied to the clipboard as well.
  *
  * @param markerName Which markers should be copied.
  * @param executor Callback executed.
  * @param config Optional configuration flags used to copy (such like partial copy flag).
  * @internal
  */
  _forceMarkersCopy(e, n, r = {
    allowedActions: "all",
    copyPartiallySelected: !0,
    duplicateOnPaste: !0
  }) {
    const s = this._markersToCopy.get(e);
    this._markersToCopy.set(e, r), n(), s ? this._markersToCopy.set(e, s) : this._markersToCopy.delete(e);
  }
  /**
  * Checks if marker can be copied.
  *
  * @param markerName Name of checked marker.
  * @param action Type of clipboard action. If null then checks only if marker is registered as copyable.
  * @internal
  */
  _isMarkerCopyable(e, n) {
    const r = this._getMarkerClipboardConfig(e);
    if (!r)
      return !1;
    if (!n)
      return !0;
    const { allowedActions: s } = r;
    return s === "all" || s.includes(n);
  }
  /**
  * Checks if marker has any clipboard copy behavior configuration.
  *
  * @param markerName Name of checked marker.
  */
  _hasMarkerConfiguration(e) {
    return !!this._getMarkerClipboardConfig(e);
  }
  /**
  * Returns marker's configuration flags passed during registration.
  *
  * @param markerName Name of marker that should be returned.
  * @internal
  */
  _getMarkerClipboardConfig(e) {
    const [n] = e.split(":");
    return this._markersToCopy.get(n) || null;
  }
  /**
  * First step of copying markers. It looks for markers intersecting with given selection and inserts `$marker` elements
  * at positions where document markers start or end. This way `$marker` elements can be easily copied together with
  * the rest of the content of the selection.
  *
  * @param writer An instance of the model writer.
  * @param selection Selection to be checked.
  * @param action Type of clipboard action.
  */
  _insertFakeMarkersIntoSelection(e, n, r) {
    const s = this._getCopyableMarkersFromSelection(e, n, r);
    return this._insertFakeMarkersElements(e, s);
  }
  /**
  * Returns array of markers that can be copied in specified selection.
  *
  * If marker cannot be copied partially (according to `copyPartiallySelected` configuration flag) and
  * is not present entirely in any selection range then it will be skipped.
  *
  * @param writer An instance of the model writer.
  * @param selection  Selection which will be checked.
  * @param action Type of clipboard action. If null then checks only if marker is registered as copyable.
  */
  _getCopyableMarkersFromSelection(e, n, r) {
    const s = Array.from(n.getRanges()), o = new Set(s.flatMap((l) => Array.from(e.model.markers.getMarkersIntersectingRange(l)))), a = (l) => {
      if (!this._isMarkerCopyable(l.name, r))
        return !1;
      const { copyPartiallySelected: u } = this._getMarkerClipboardConfig(l.name);
      if (!u) {
        const d = l.getRange();
        return s.some((f) => f.containsRange(d, !0));
      }
      return !0;
    };
    return Array.from(o).filter(a).map((l) => ({
      name: r === "dragstart" ? this._getUniqueMarkerName(l.name) : l.name,
      range: l.getRange()
    }));
  }
  /**
  * Picks all markers from markers map that can be pasted.
  * If `duplicateOnPaste` is `true`, it regenerates their IDs to ensure uniqueness.
  * If marker is not registered, it will be kept in the array anyway.
  *
  * @param markers Object that maps marker name to corresponding range.
  * @param action Type of clipboard action. If null then checks only if marker is registered as copyable.
  */
  _getPasteMarkersFromRangeMap(e, n = null) {
    const { model: r } = this.editor;
    return (e instanceof Map ? Array.from(e.entries()) : Object.entries(e)).flatMap(([o, a]) => {
      if (!this._hasMarkerConfiguration(o))
        return [
          {
            name: o,
            range: a
          }
        ];
      if (this._isMarkerCopyable(o, n)) {
        const l = this._getMarkerClipboardConfig(o), c = r.markers.has(o) && r.markers.get(o).getRange().root.rootName === "$graveyard";
        return (l.duplicateOnPaste || c) && (o = this._getUniqueMarkerName(o)), [
          {
            name: o,
            range: a
          }
        ];
      }
      return [];
    });
  }
  /**
  * Inserts specified array of fake markers elements to document and assigns them `type` and `name` attributes.
  * Fake markers elements are used to calculate position of markers on pasted fragment that were transformed during
  * steps between copy and paste.
  *
  * @param writer An instance of the model writer.
  * @param markers Array of markers that will be inserted.
  */
  _insertFakeMarkersElements(e, n) {
    const r = {}, s = n.flatMap((o) => {
      const { start: a, end: l } = o.range;
      return [
        {
          position: a,
          marker: o,
          type: "start"
        },
        {
          position: l,
          marker: o,
          type: "end"
        }
      ];
    }).sort(({ position: o }, { position: a }) => o.isBefore(a) ? 1 : -1);
    for (const { position: o, marker: a, type: l } of s) {
      const c = e.createElement("$marker", {
        "data-name": a.name,
        "data-type": l
      });
      r[a.name] || (r[a.name] = []), r[a.name].push(c), e.insert(c, o);
    }
    return r;
  }
  /**
  * Removes all `$marker` elements from the given document fragment.
  *
  * Returns an object where keys are marker names, and values are ranges corresponding to positions
  * where `$marker` elements were inserted.
  *
  * If the document fragment had only one `$marker` element for given marker (start or end) the other boundary is set automatically
  * (to the end or start of the document fragment, respectively).
  *
  * @param writer An instance of the model writer.
  * @param rootElement The element to be checked.
  */
  _removeFakeMarkersInsideElement(e, n) {
    const s = this._getAllFakeMarkersFromElement(e, n).reduce((o, a) => {
      const l = a.markerElement && e.createPositionBefore(a.markerElement);
      let c = o[a.name], u = !1;
      return c && c.start && c.end && (this._getMarkerClipboardConfig(a.name).duplicateOnPaste ? o[this._getUniqueMarkerName(a.name)] = o[a.name] : u = !0, c = null), u || (o[a.name] = {
        ...c,
        [a.type]: l
      }), a.markerElement && e.remove(a.markerElement), o;
    }, {});
    return M4(s, (o) => new D(o.start || e.createPositionFromPath(n, [
      0
    ]), o.end || e.createPositionAt(n, "end")));
  }
  /**
  * Returns array that contains list of fake markers with corresponding `$marker` elements.
  *
  * For each marker, there can be two `$marker` elements or only one (if the document fragment contained
  * only the beginning or only the end of a marker).
  *
  * @param writer An instance of the model writer.
  * @param rootElement The element to be checked.
  */
  _getAllFakeMarkersFromElement(e, n) {
    const r = Array.from(e.createRangeIn(n)).flatMap(({ item: a }) => {
      if (!a.is("element", "$marker"))
        return [];
      const l = a.getAttribute("data-name"), c = a.getAttribute("data-type");
      return [
        {
          markerElement: a,
          name: l,
          type: c
        }
      ];
    }), s = [], o = [];
    for (const a of r)
      a.type === "end" && (r.some((c) => c.name === a.name && c.type === "start") || s.push({
        markerElement: null,
        name: a.name,
        type: "start"
      })), a.type === "start" && (r.some((c) => c.name === a.name && c.type === "end") || o.unshift({
        markerElement: null,
        name: a.name,
        type: "end"
      }));
    return [
      ...s,
      ...r,
      ...o
    ];
  }
  /**
  * When copy of markers occurs we have to make sure that pasted markers have different names
  * than source markers. This functions helps with assigning unique part to marker name to
  * prevent duplicated markers error.
  *
  * @param name Name of marker
  */
  _getUniqueMarkerName(e) {
    const n = e.split(":"), r = Jn().substring(1, 6);
    return n.length === 3 ? `${n.slice(0, 2).join(":")}:${r}` : `${n.join(":")}:${r}`;
  }
}
class ui extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ClipboardPipeline";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      gl
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.editing.view.addObserver(zl), this._setupPasteDrop(), this._setupCopyCut();
  }
  /**
  * Fires Clipboard `'outputTransformation'` event for given parameters.
  *
  * @internal
  */
  _fireOutputTransformationEvent(t, e, n) {
    const r = this.editor.plugins.get("ClipboardMarkersUtils");
    this.editor.model.enqueueChange({
      isUndoable: n === "cut"
    }, () => {
      const s = r._copySelectedFragmentWithMarkers(n, e);
      this.fire("outputTransformation", {
        dataTransfer: t,
        content: s,
        method: n
      });
    });
  }
  /**
  * The clipboard paste pipeline.
  */
  _setupPasteDrop() {
    const t = this.editor, e = t.model, n = t.editing.view, r = n.document, s = this.editor.plugins.get("ClipboardMarkersUtils");
    this.listenTo(r, "clipboardInput", (o, a) => {
      a.method == "paste" && !t.model.canEditAt(t.model.document.selection) && o.stop();
    }, {
      priority: "highest"
    }), this.listenTo(r, "clipboardInput", (o, a) => {
      const l = a.dataTransfer;
      let c;
      if (a.content)
        c = a.content;
      else {
        let d = "";
        l.getData("text/html") ? d = AN(l.getData("text/html")) : l.getData("text/plain") && (d = SN(l.getData("text/plain"))), c = this.editor.data.htmlProcessor.toView(d);
      }
      const u = new Br(this, "inputTransformation");
      this.fire(u, {
        content: c,
        dataTransfer: l,
        targetRanges: a.targetRanges,
        method: a.method
      }), u.stop.called && o.stop(), n.scrollToTheSelection();
    }, {
      priority: "low"
    }), this.listenTo(this, "inputTransformation", (o, a) => {
      if (a.content.isEmpty)
        return;
      const c = this.editor.data.toModel(a.content, "$clipboardHolder");
      c.childCount != 0 && (o.stop(), e.change(() => {
        this.fire("contentInsertion", {
          content: c,
          method: a.method,
          dataTransfer: a.dataTransfer,
          targetRanges: a.targetRanges
        });
      }));
    }, {
      priority: "low"
    }), this.listenTo(this, "contentInsertion", (o, a) => {
      a.resultRange = s._pasteFragmentWithMarkers(a.content);
    }, {
      priority: "low"
    });
  }
  /**
  * The clipboard copy/cut pipeline.
  */
  _setupCopyCut() {
    const t = this.editor, e = t.model.document, r = t.editing.view.document, s = (o, a) => {
      const l = a.dataTransfer;
      a.preventDefault(), this._fireOutputTransformationEvent(l, e.selection, o.name);
    };
    this.listenTo(r, "copy", s, {
      priority: "low"
    }), this.listenTo(r, "cut", (o, a) => {
      t.model.canEditAt(t.model.document.selection) ? s(o, a) : a.preventDefault();
    }, {
      priority: "low"
    }), this.listenTo(this, "outputTransformation", (o, a) => {
      const l = t.data.toView(a.content);
      r.fire("clipboardOutput", {
        dataTransfer: a.dataTransfer,
        content: l,
        method: a.method
      });
    }, {
      priority: "low"
    }), this.listenTo(r, "clipboardOutput", (o, a) => {
      a.content.isEmpty || (a.dataTransfer.setData("text/html", this.editor.data.htmlProcessor.toData(a.content)), a.dataTransfer.setData("text/plain", yk(a.content))), a.method == "cut" && t.model.deleteContent(e.selection);
    }, {
      priority: "low"
    });
  }
}
const ff = /* @__PURE__ */ vs("px");
class IN extends de {
  /**
  * @inheritDoc
  */
  constructor() {
    super();
    const t = this.bindTemplate;
    this.set({
      isVisible: !1,
      left: null,
      top: null,
      width: null
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-clipboard-drop-target-line",
          t.if("isVisible", "ck-hidden", (e) => !e)
        ],
        style: {
          left: t.to("left", (e) => ff(e)),
          top: t.to("top", (e) => ff(e)),
          width: t.to("width", (e) => ff(e))
        }
      }
    });
  }
}
class qc extends V {
  constructor() {
    super(...arguments);
    /**
    * A delayed callback removing the drop marker.
    *
    * @internal
    */
    h(this, "removeDropMarkerDelayed", Yg(() => this.removeDropMarker(), 40));
    /**
    * A throttled callback updating the drop marker.
    */
    h(this, "_updateDropMarkerThrottled", Jo((e) => this._updateDropMarker(e), 40));
    /**
    * A throttled callback reconverting the drop parker.
    */
    h(this, "_reconvertMarkerThrottled", Jo(() => {
      this.editor.model.markers.has("drop-target") && this.editor.editing.reconvertMarker("drop-target");
    }, 0));
    /**
    * The horizontal drop target line view.
    */
    h(this, "_dropTargetLineView", new IN());
    /**
    * DOM Emitter.
    */
    h(this, "_domEmitter", new (Xn())());
    /**
    * Map of document scrollable elements.
    */
    h(this, "_scrollables", /* @__PURE__ */ new Map());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DragDropTarget";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this._setupDropMarker();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._domEmitter.stopListening();
    for (const { resizeObserver: e } of this._scrollables.values())
      e.destroy();
    return this._updateDropMarkerThrottled.cancel(), this.removeDropMarkerDelayed.cancel(), this._reconvertMarkerThrottled.cancel(), super.destroy();
  }
  /**
  * Finds the drop target range and updates the drop marker.
  *
  * @internal
  */
  updateDropMarker(e, n, r, s, o, a) {
    this.removeDropMarkerDelayed.cancel();
    const l = uw(this.editor, e, n, r, s, o, a);
    /* istanbul ignore next -- @preserve */
    if (l) {
      if (a && a.containsRange(l))
        return this.removeDropMarker();
      this._updateDropMarkerThrottled(l);
    }
  }
  /**
  * Finds the final drop target range.
  *
  * @internal
  */
  getFinalDropRange(e, n, r, s, o, a) {
    const l = uw(this.editor, e, n, r, s, o, a);
    return this.removeDropMarker(), l;
  }
  /**
  * Removes the drop target marker.
  *
  * @internal
  */
  removeDropMarker() {
    const e = this.editor.model;
    this.removeDropMarkerDelayed.cancel(), this._updateDropMarkerThrottled.cancel(), this._dropTargetLineView.isVisible = !1, e.markers.has("drop-target") && e.change((n) => {
      n.removeMarker("drop-target");
    });
  }
  /**
  * Creates downcast conversion for the drop target marker.
  */
  _setupDropMarker() {
    const e = this.editor;
    e.ui.view.body.add(this._dropTargetLineView), e.conversion.for("editingDowncast").markerToHighlight({
      model: "drop-target",
      view: {
        classes: [
          "ck-clipboard-drop-target-range"
        ]
      }
    }), e.conversion.for("editingDowncast").markerToElement({
      model: "drop-target",
      view: (n, { writer: r }) => {
        if (e.model.schema.checkChild(n.markerRange.start, "$text"))
          return this._dropTargetLineView.isVisible = !1, this._createDropTargetPosition(r);
        n.markerRange.isCollapsed ? this._updateDropTargetLine(n.markerRange) : this._dropTargetLineView.isVisible = !1;
      }
    });
  }
  /**
  * Updates the drop target marker to the provided range.
  *
  * @param targetRange The range to set the marker to.
  */
  _updateDropMarker(e) {
    const n = this.editor, r = n.model.markers;
    n.model.change((s) => {
      r.has("drop-target") ? r.get("drop-target").getRange().isEqual(e) || s.updateMarker("drop-target", {
        range: e
      }) : s.addMarker("drop-target", {
        range: e,
        usingOperation: !1,
        affectsData: !1
      });
    });
  }
  /**
  * Creates the UI element for vertical (in-line) drop target.
  */
  _createDropTargetPosition(e) {
    return e.createUIElement("span", {
      class: "ck ck-clipboard-drop-target-position"
    }, function(n) {
      const r = this.toDomElement(n);
      return r.append("⁠", n.createElement("span"), "⁠"), r;
    });
  }
  /**
  * Updates the horizontal drop target line.
  */
  _updateDropTargetLine(e) {
    const n = this.editor.editing, r = e.start.nodeBefore, s = e.start.nodeAfter, o = e.start.parent, a = r ? n.mapper.toViewElement(r) : null, l = a ? n.view.domConverter.mapViewToDom(a) : null, c = s ? n.mapper.toViewElement(s) : null, u = c ? n.view.domConverter.mapViewToDom(c) : null, d = n.mapper.toViewElement(o);
    if (!d)
      return;
    const f = n.view.domConverter.mapViewToDom(d), m = this._getScrollableRect(d), { scrollX: g, scrollY: p } = ae.window, b = l ? new je(l) : null, v = u ? new je(u) : null, y = new je(f).excludeScrollbarsAndBorders(), x = b ? b.bottom : y.top, S = v ? v.top : y.bottom, k = ae.window.getComputedStyle(f), I = x <= S ? (x + S) / 2 : S;
    if (m.top < I && I < m.bottom) {
      const A = y.left + parseFloat(k.paddingLeft), L = y.right - parseFloat(k.paddingRight), R = Math.max(A + g, m.left), O = Math.min(L + g, m.right);
      this._dropTargetLineView.set({
        isVisible: !0,
        left: R,
        top: I + p,
        width: O - R
      });
    } else
      this._dropTargetLineView.isVisible = !1;
  }
  /**
  * Finds the closest scrollable element rect for the given view element.
  */
  _getScrollableRect(e) {
    const n = e.root.rootName;
    let r;
    if (this._scrollables.has(n))
      r = this._scrollables.get(n).domElement;
    else {
      const s = this.editor.editing.view.domConverter.mapViewToDom(e);
      r = ON(s), this._domEmitter.listenTo(r, "scroll", this._reconvertMarkerThrottled, {
        usePassive: !0
      });
      const o = new qs(r, this._reconvertMarkerThrottled);
      this._scrollables.set(n, {
        domElement: r,
        resizeObserver: o
      });
    }
    return new je(r).excludeScrollbarsAndBorders();
  }
}
function uw(i, t, e, n, r, s, o) {
  const a = i.model, l = i.editing.mapper;
  let u = dw(i, t);
  for (; u; ) {
    if (!s) {
      if (a.schema.checkChild(u, "$text")) {
        if (e) {
          const d = e[0].start, f = l.toModelPosition(d);
          if (!o || Array.from(o.getItems()).every((g) => a.schema.checkChild(f, g))) {
            if (a.schema.checkChild(f, "$text"))
              return a.createRange(f);
            if (d)
              return Gc(i, dw(i, d.parent), n, r);
          }
        }
      } else if (a.schema.isInline(u))
        return Gc(i, u, n, r);
    }
    if (a.schema.isBlock(u))
      return Gc(i, u, n, r);
    if (a.schema.checkChild(u, "$block")) {
      const d = Array.from(u.getChildren()).filter((g) => g.is("element") && !RN(i, g));
      let f = 0, m = d.length;
      if (m == 0)
        return a.createRange(a.createPositionAt(u, "end"));
      for (; f < m - 1; ) {
        const g = Math.floor((f + m) / 2);
        _k(i, d[g], n, r) == "before" ? m = g : f = g;
      }
      return Gc(i, d[f], n, r);
    }
    u = u.parent;
  }
  return null;
}
function RN(i, t) {
  const e = i.editing.mapper, n = i.editing.view.domConverter, r = e.toViewElement(t);
  if (!r)
    return !0;
  const s = n.mapViewToDom(r);
  return ae.window.getComputedStyle(s).float != "none";
}
function Gc(i, t, e, n) {
  const r = i.model;
  return r.createRange(r.createPositionAt(t, _k(i, t, e, n)));
}
function _k(i, t, e, n) {
  const r = i.editing.mapper, s = i.editing.view.domConverter, o = r.toViewElement(t), a = s.mapViewToDom(o), l = new je(a);
  return i.model.schema.isInline(t) ? e < (l.left + l.right) / 2 ? "before" : "after" : n < (l.top + l.bottom) / 2 ? "before" : "after";
}
function dw(i, t) {
  const e = i.editing.mapper, n = i.editing.view, r = e.toModelElement(t);
  if (r)
    return r;
  const s = n.createPositionBefore(t), o = e.findMappedViewAncestor(s);
  return e.toModelElement(o);
}
function ON(i) {
  let t = i;
  do {
    t = t.parentElement;
    const e = ae.window.getComputedStyle(t).overflowY;
    if (e == "auto" || e == "scroll")
      break;
  } while (t.tagName != "BODY");
  return t;
}
class VN extends V {
  constructor() {
    super(...arguments);
    /**
    * Whether current dragging is started by block toolbar button dragging.
    */
    h(this, "_isBlockDragging", !1);
    /**
    * DOM Emitter.
    */
    h(this, "_domEmitter", new (Xn())());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DragDropBlockToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    if (this.listenTo(e, "change:isReadOnly", (n, r, s) => {
      s ? (this.forceDisabled("readOnlyMode"), this._isBlockDragging = !1) : this.clearForceDisabled("readOnlyMode");
    }), Me.isAndroid && this.forceDisabled("noAndroidSupport"), e.plugins.has("BlockToolbar")) {
      const r = e.plugins.get("BlockToolbar").buttonView.element;
      this._domEmitter.listenTo(r, "dragstart", (s, o) => this._handleBlockDragStart(o)), this._domEmitter.listenTo(ae.document, "dragover", (s, o) => this._handleBlockDragging(o)), this._domEmitter.listenTo(ae.document, "drop", (s, o) => this._handleBlockDragging(o)), this._domEmitter.listenTo(ae.document, "dragend", () => this._handleBlockDragEnd(), {
        useCapture: !0
      }), this.isEnabled && r.setAttribute("draggable", "true"), this.on("change:isEnabled", (s, o, a) => {
        r.setAttribute("draggable", a ? "true" : "false");
      });
    }
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this._domEmitter.stopListening(), super.destroy();
  }
  /**
  * The `dragstart` event handler.
  */
  _handleBlockDragStart(e) {
    if (!this.isEnabled)
      return;
    const n = this.editor.model, r = n.document.selection, s = this.editor.editing.view, o = Array.from(r.getSelectedBlocks()), a = n.createRange(n.createPositionBefore(o[0]), n.createPositionAfter(o[o.length - 1]));
    n.change((l) => l.setSelection(a)), this._isBlockDragging = !0, s.focus(), s.getObserver(zl).onDomEvent(e);
  }
  /**
  * The `dragover` and `drop` event handler.
  */
  _handleBlockDragging(e) {
    if (!this.isEnabled || !this._isBlockDragging)
      return;
    const n = e.clientX + (this.editor.locale.contentLanguageDirection == "ltr" ? 100 : -100), r = e.clientY, s = document.elementFromPoint(n, r), o = this.editor.editing.view;
    !s || !s.closest(".ck-editor__editable") || o.getObserver(zl).onDomEvent({
      ...e,
      type: e.type,
      dataTransfer: e.dataTransfer,
      target: s,
      clientX: n,
      clientY: r,
      preventDefault: () => e.preventDefault(),
      stopPropagation: () => e.stopPropagation()
    });
  }
  /**
  * The `dragend` event handler.
  */
  _handleBlockDragEnd() {
    this._isBlockDragging = !1;
  }
}
class MN extends V {
  constructor() {
    super(...arguments);
    /**
    * The live range over the original content that is being dragged.
    */
    h(this, "_draggedRange");
    /**
    * The UID of current dragging that is used to verify if the drop started in the same editor as the drag start.
    *
    * **Note**: This is a workaround for broken 'dragend' events (they are not fired if the source text node got removed).
    */
    h(this, "_draggingUid");
    /**
    * The reference to the model element that currently has a `draggable` attribute set (it is set while dragging).
    */
    h(this, "_draggableElement");
    /**
    * A delayed callback removing draggable attributes.
    */
    h(this, "_clearDraggableAttributesDelayed", Yg(() => this._clearDraggableAttributes(), 40));
    /**
    * Whether the dragged content can be dropped only in block context.
    */
    // TODO handle drag from other editor instance
    // TODO configure to use block, inline or both
    h(this, "_blockMode", !1);
    /**
    * DOM Emitter.
    */
    h(this, "_domEmitter", new (Xn())());
    /**
    * The DOM element used to generate dragged preview image.
    */
    h(this, "_previewContainer");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DragDrop";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ui,
      oo,
      qc,
      VN
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.editing.view;
    this._draggedRange = null, this._draggingUid = "", this._draggableElement = null, n.addObserver(zl), n.addObserver(Hd), this._setupDragging(), this._setupContentInsertionIntegration(), this._setupClipboardInputIntegration(), this._setupDraggableAttributeHandling(), this.listenTo(e, "change:isReadOnly", (r, s, o) => {
      o ? this.forceDisabled("readOnlyMode") : this.clearForceDisabled("readOnlyMode");
    }), this.on("change:isEnabled", (r, s, o) => {
      o || this._finalizeDragging(!1);
    }), Me.isAndroid && this.forceDisabled("noAndroidSupport");
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this._draggedRange && (this._draggedRange.detach(), this._draggedRange = null), this._previewContainer && this._previewContainer.remove(), this._domEmitter.stopListening(), this._clearDraggableAttributesDelayed.cancel(), super.destroy();
  }
  /**
  * Drag and drop events handling.
  */
  _setupDragging() {
    const e = this.editor, n = e.model, r = e.editing.view, s = r.document, o = e.plugins.get(qc);
    this.listenTo(s, "dragstart", (a, l) => {
      if (l.target && l.target.is("editableElement")) {
        l.preventDefault();
        return;
      }
      if (this._prepareDraggedRange(l.target), !this._draggedRange) {
        l.preventDefault();
        return;
      }
      this._draggingUid = Jn(), l.dataTransfer.effectAllowed = this.isEnabled ? "copyMove" : "copy", l.dataTransfer.setData("application/ckeditor5-dragging-uid", this._draggingUid);
      const c = n.createSelection(this._draggedRange.toRange());
      this.editor.plugins.get("ClipboardPipeline")._fireOutputTransformationEvent(l.dataTransfer, c, "dragstart");
      const { dataTransfer: d, domTarget: f, domEvent: m } = l, { clientX: g } = m;
      this._updatePreview({
        dataTransfer: d,
        domTarget: f,
        clientX: g
      }), l.stopPropagation(), this.isEnabled || (this._draggedRange.detach(), this._draggedRange = null, this._draggingUid = "");
    }, {
      priority: "low"
    }), this.listenTo(s, "dragend", (a, l) => {
      this._finalizeDragging(!l.dataTransfer.isCanceled && l.dataTransfer.dropEffect == "move");
    }, {
      priority: "low"
    }), this._domEmitter.listenTo(ae.document, "dragend", () => {
      this._blockMode = !1;
    }, {
      useCapture: !0
    }), this.listenTo(s, "dragenter", () => {
      this.isEnabled && r.focus();
    }), this.listenTo(s, "dragleave", () => {
      o.removeDropMarkerDelayed();
    }), this.listenTo(s, "dragging", (a, l) => {
      if (!this.isEnabled) {
        l.dataTransfer.dropEffect = "none";
        return;
      }
      const { clientX: c, clientY: u } = l.domEvent;
      o.updateDropMarker(l.target, l.targetRanges, c, u, this._blockMode, this._draggedRange), this._draggedRange || (l.dataTransfer.dropEffect = "copy"), Me.isGecko || (l.dataTransfer.effectAllowed == "copy" ? l.dataTransfer.dropEffect = "copy" : [
        "all",
        "copyMove"
      ].includes(l.dataTransfer.effectAllowed) && (l.dataTransfer.dropEffect = "move")), a.stop();
    }, {
      priority: "low"
    });
  }
  /**
  * Integration with the `clipboardInput` event.
  */
  _setupClipboardInputIntegration() {
    const e = this.editor, r = e.editing.view.document, s = e.plugins.get(qc);
    this.listenTo(r, "clipboardInput", (o, a) => {
      if (a.method != "drop")
        return;
      const { clientX: l, clientY: c } = a.domEvent, u = s.getFinalDropRange(a.target, a.targetRanges, l, c, this._blockMode, this._draggedRange);
      if (!u) {
        this._finalizeDragging(!1), o.stop();
        return;
      }
      if (this._draggedRange && this._draggingUid != a.dataTransfer.getData("application/ckeditor5-dragging-uid") && (this._draggedRange.detach(), this._draggedRange = null, this._draggingUid = ""), hw(a.dataTransfer) == "move" && this._draggedRange && this._draggedRange.containsRange(u, !0)) {
        this._finalizeDragging(!1), o.stop();
        return;
      }
      a.targetRanges = [
        e.editing.mapper.toViewRange(u)
      ];
    }, {
      priority: "high"
    });
  }
  /**
  * Integration with the `contentInsertion` event of the clipboard pipeline.
  */
  _setupContentInsertionIntegration() {
    const e = this.editor.plugins.get(ui);
    e.on("contentInsertion", (n, r) => {
      if (!this.isEnabled || r.method !== "drop")
        return;
      const s = r.targetRanges.map((o) => this.editor.editing.mapper.toModelRange(o));
      this.editor.model.change((o) => o.setSelection(s));
    }, {
      priority: "high"
    }), e.on("contentInsertion", (n, r) => {
      if (!this.isEnabled || r.method !== "drop")
        return;
      const s = hw(r.dataTransfer) == "move", o = !r.resultRange || !r.resultRange.isCollapsed;
      this._finalizeDragging(o && s);
    }, {
      priority: "lowest"
    });
  }
  /**
  * Adds listeners that add the `draggable` attribute to the elements while the mouse button is down so the dragging could start.
  */
  _setupDraggableAttributeHandling() {
    const e = this.editor, n = e.editing.view, r = n.document;
    this.listenTo(r, "mousedown", (s, o) => {
      if (Me.isAndroid || !o)
        return;
      this._clearDraggableAttributesDelayed.cancel();
      let a = fw(o.target);
      if (Me.isBlink && !e.isReadOnly && !a && !r.selection.isCollapsed) {
        const l = r.selection.getSelectedElement();
        (!l || !Gt(l)) && (a = r.selection.editableElement);
      }
      a && (n.change((l) => {
        l.setAttribute("draggable", "true", a);
      }), this._draggableElement = e.editing.mapper.toModelElement(a));
    }), this.listenTo(r, "mouseup", () => {
      Me.isAndroid || this._clearDraggableAttributesDelayed();
    });
  }
  /**
  * Removes the `draggable` attribute from the element that was used for dragging.
  */
  _clearDraggableAttributes() {
    const e = this.editor.editing;
    e.view.change((n) => {
      this._draggableElement && this._draggableElement.root.rootName != "$graveyard" && n.removeAttribute("draggable", e.mapper.toViewElement(this._draggableElement)), this._draggableElement = null;
    });
  }
  /**
  * Deletes the dragged content from its original range and clears the dragging state.
  *
  * @param moved Whether the move succeeded.
  */
  _finalizeDragging(e) {
    const n = this.editor, r = n.model;
    n.plugins.get(qc).removeDropMarker(), this._clearDraggableAttributes(), n.plugins.has("WidgetToolbarRepository") && n.plugins.get("WidgetToolbarRepository").clearForceDisabled("dragDrop"), this._draggingUid = "", this._previewContainer && (this._previewContainer.remove(), this._previewContainer = void 0), this._draggedRange && (e && this.isEnabled && r.change((o) => {
      const a = r.createSelection(this._draggedRange);
      r.deleteContent(a, {
        doNotAutoparagraph: !0
      });
      const l = a.getFirstPosition().parent;
      l.isEmpty && !r.schema.checkChild(l, "$text") && r.schema.checkChild(l, "paragraph") && o.insertElement("paragraph", l, 0);
    }), this._draggedRange.detach(), this._draggedRange = null);
  }
  /**
  * Sets the dragged source range based on event target and document selection.
  */
  _prepareDraggedRange(e) {
    const n = this.editor, r = n.model, s = r.document.selection, o = e ? fw(e) : null;
    if (o) {
      const u = n.editing.mapper.toModelElement(o);
      this._draggedRange = Kn.fromRange(r.createRangeOn(u)), this._blockMode = r.schema.isBlock(u), n.plugins.has("WidgetToolbarRepository") && n.plugins.get("WidgetToolbarRepository").forceDisabled("dragDrop");
      return;
    }
    if (s.isCollapsed && !s.getFirstPosition().parent.isEmpty)
      return;
    const a = Array.from(s.getSelectedBlocks()), l = s.getFirstRange();
    if (a.length == 0) {
      this._draggedRange = Kn.fromRange(l);
      return;
    }
    const c = Ck(r, a);
    if (a.length > 1)
      this._draggedRange = Kn.fromRange(c), this._blockMode = !0;
    else if (a.length == 1) {
      const u = l.start.isTouching(c.start) && l.end.isTouching(c.end);
      this._draggedRange = Kn.fromRange(u ? c : l), this._blockMode = u;
    }
    r.change((u) => u.setSelection(this._draggedRange.toRange()));
  }
  /**
  * Updates the dragged preview image.
  */
  _updatePreview({ dataTransfer: e, domTarget: n, clientX: r }) {
    const s = this.editor.editing.view, o = s.document.selection.editableElement, a = s.domConverter.mapViewToDom(o), l = ae.window.getComputedStyle(a);
    this._previewContainer ? this._previewContainer.firstElementChild && this._previewContainer.removeChild(this._previewContainer.firstElementChild) : (this._previewContainer = xn(ae.document, "div", {
      style: "position: fixed; left: -999999px;"
    }), ae.document.body.appendChild(this._previewContainer));
    const c = new je(a);
    if (a.contains(n))
      return;
    const u = parseFloat(l.paddingLeft), d = xn(ae.document, "div");
    d.className = "ck ck-content", d.style.width = l.width, d.style.paddingLeft = `${c.left - r + u}px`, Me.isiOS && (d.style.backgroundColor = "white"), s.domConverter.setContentOf(d, e.getData("text/html")), e.setDragImage(d, 0, 0), this._previewContainer.appendChild(d);
  }
}
function hw(i) {
  return Me.isGecko ? i.dropEffect : [
    "all",
    "copyMove"
  ].includes(i.effectAllowed) ? "move" : "copy";
}
function fw(i) {
  if (i.is("editableElement"))
    return null;
  if (i.hasClass("ck-widget__selection-handle"))
    return i.findAncestor(Gt);
  if (Gt(i))
    return i;
  const t = i.findAncestor((e) => Gt(e) || e.is("editableElement"));
  return Gt(t) ? t : null;
}
function Ck(i, t) {
  const e = t[0], n = t[t.length - 1], r = e.getCommonAncestor(n), s = i.createPositionBefore(e), o = i.createPositionAfter(n);
  if (r && r.is("element") && !i.schema.isLimit(r)) {
    const a = i.createRangeOn(r), l = s.isTouching(a.start), c = o.isTouching(a.end);
    if (l && c)
      return Ck(i, [
        r
      ]);
  }
  return i.createRange(s, o);
}
class DN extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PastePlainText";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ui
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model, n = t.editing.view, r = e.document.selection;
    n.addObserver(zl), t.plugins.get(ui).on("contentInsertion", (s, o) => {
      LN(o.content, e) && e.change((a) => {
        const l = Array.from(r.getAttributes()).filter(([u]) => e.schema.getAttributeProperties(u).isFormatting);
        r.isCollapsed || e.deleteContent(r, {
          doNotAutoparagraph: !0
        }), l.push(...r.getAttributes());
        const c = a.createRangeIn(o.content);
        for (const u of c.getItems())
          for (const d of l)
            e.schema.checkAttribute(u, d[0]) && a.setAttribute(d[0], d[1], u);
      });
    });
  }
}
function LN(i, t) {
  let e = t.createRangeIn(i);
  if (i.childCount == 1) {
    const n = i.getChild(0);
    n.is("element") && t.schema.isBlock(n) && !t.schema.isObject(n) && !t.schema.isLimit(n) && (e = t.createRangeIn(n));
  }
  for (const n of e.getItems())
    if (!t.schema.isInline(n) || Array.from(n.getAttributeKeys()).find((s) => t.schema.getAttributeProperties(s).isFormatting))
      return !1;
  return !0;
}
class Cp extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Clipboard";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      gl,
      ui,
      MN,
      DN
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Copy selected content"),
          keystroke: "CTRL+C"
        },
        {
          label: e("Paste content"),
          keystroke: "CTRL+V"
        },
        {
          label: e("Paste content as plain text"),
          keystroke: "CTRL+SHIFT+V"
        }
      ]
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const NN = {
  autoRefresh: !0
}, mw = 36e5, BN = 5e3;
class kp extends (/* @__PURE__ */ st()) {
  /**
  * Creates `Token` instance.
  * Method `init` should be called after using the constructor or use `create` method instead.
  *
  * @param tokenUrlOrRefreshToken Endpoint address to download the token or a callback that provides the token. If the
  * value is a function it has to match the {@link module:cloud-services/token/token~Token#refreshToken} interface.
  */
  constructor(e, n = {}) {
    super();
    /**
    * Base refreshing function.
    */
    h(this, "_refresh");
    /**
    * Cached token options.
    */
    h(this, "_options");
    /**
    * `setTimeout()` id for a token refresh when {@link module:cloud-services/token/token~TokenOptions auto refresh} is enabled.
    */
    h(this, "_tokenRefreshTimeout");
    if (!e)
      throw new T("token-missing-token-url", this);
    n.initValue && this._validateTokenValue(n.initValue), this.set("value", n.initValue), typeof e == "function" ? this._refresh = e : this._refresh = () => FN(e), this._options = {
      ...NN,
      ...n
    };
  }
  /**
  * Initializes the token.
  */
  init() {
    return new Promise((e, n) => {
      if (!this.value) {
        this.refreshToken().then(e).catch(n);
        return;
      }
      this._options.autoRefresh && this._registerRefreshTokenTimeout(), e(this);
    });
  }
  /**
  * Refresh token method. Useful in a method form as it can be overridden in tests.
  *
  * This method will be invoked periodically based on the token expiry date after first call to keep the token up-to-date
  * (requires {@link module:cloud-services/token/token~TokenOptions auto refresh option} to be set).
  *
  * If the token refresh fails, the method will retry in 5 seconds intervals until success or the token gets
  * {@link #destroy destroyed}.
  */
  refreshToken() {
    const e = this._options.autoRefresh;
    return this._refresh().then((n) => (this._validateTokenValue(n), this.set("value", n), e && this._registerRefreshTokenTimeout(), this)).catch((n) => {
      throw Xe("token-refresh-failed", {
        autoRefresh: e
      }), e && this._registerRefreshTokenTimeout(BN), n;
    });
  }
  /**
  * Destroys token instance. Stops refreshing.
  */
  destroy() {
    clearTimeout(this._tokenRefreshTimeout);
  }
  /**
  * Checks whether the provided token follows the JSON Web Tokens (JWT) format.
  *
  * @param tokenValue The token to validate.
  */
  _validateTokenValue(e) {
    const n = typeof e == "string", r = !/^".*"$/.test(e), s = n && e.split(".").length === 3;
    if (!(r && s))
      throw new T("token-not-in-jwt-format", this);
  }
  /**
  * Registers a refresh token timeout for the time taken from token.
  */
  _registerRefreshTokenTimeout(e) {
    const n = e || this._getTokenRefreshTimeoutTime();
    clearTimeout(this._tokenRefreshTimeout), this._tokenRefreshTimeout = setTimeout(() => {
      this.refreshToken();
    }, n);
  }
  /**
  * Returns token refresh timeout time calculated from expire time in the token payload.
  *
  * If the token parse fails or the token payload doesn't contain, the default DEFAULT_TOKEN_REFRESH_TIMEOUT_TIME is returned.
  */
  _getTokenRefreshTimeoutTime() {
    try {
      const [, e] = this.value.split("."), { exp: n } = JSON.parse(atob(e));
      return n ? Math.floor((n * 1e3 - Date.now()) / 2) : mw;
    } catch {
      return mw;
    }
  }
  /**
  * Creates a initialized {@link module:cloud-services/token/token~Token} instance.
  *
  * @param tokenUrlOrRefreshToken Endpoint address to download the token or a callback that provides the token. If the
  * value is a function it has to match the {@link module:cloud-services/token/token~Token#refreshToken} interface.
  */
  static create(e, n = {}) {
    return new kp(e, n).init();
  }
}
function FN(i) {
  return new Promise((t, e) => {
    const n = new XMLHttpRequest();
    n.open("GET", i), n.addEventListener("load", () => {
      const r = n.status, s = n.response;
      return r < 200 || r > 299 ? e(new T("token-cannot-download-new-token", null)) : t(s);
    }), n.addEventListener("error", () => e(new Error("Network Error"))), n.addEventListener("abort", () => e(new Error("Abort"))), n.send();
  });
}
const ym = /^data:(\S*?);base64,/;
class $N extends (/* @__PURE__ */ ht()) {
  /**
  * Creates `FileUploader` instance.
  *
  * @param fileOrData A blob object or a data string encoded with Base64.
  * @param token Token used for authentication.
  * @param apiAddress API address.
  */
  constructor(e, n, r) {
    super();
    /**
    * A file that is being uploaded.
    */
    h(this, "file");
    h(this, "xhr");
    /**
    * CKEditor Cloud Services access token.
    */
    h(this, "_token");
    /**
    * CKEditor Cloud Services API address.
    */
    h(this, "_apiAddress");
    if (!e)
      throw new T("fileuploader-missing-file", null);
    if (!n)
      throw new T("fileuploader-missing-token", null);
    if (!r)
      throw new T("fileuploader-missing-api-address", null);
    this.file = HN(e) ? zN(e) : e, this._token = n, this._apiAddress = r;
  }
  /**
  * Registers callback on `progress` event.
  */
  onProgress(e) {
    return this.on("progress", (n, r) => e(r)), this;
  }
  /**
  * Registers callback on `error` event. Event is called once when error occurs.
  */
  onError(e) {
    return this.once("error", (n, r) => e(r)), this;
  }
  /**
  * Aborts upload process.
  */
  abort() {
    this.xhr.abort();
  }
  /**
  * Sends XHR request to API.
  */
  send() {
    return this._prepareRequest(), this._attachXHRListeners(), this._sendRequest();
  }
  /**
  * Prepares XHR request.
  */
  _prepareRequest() {
    const e = new XMLHttpRequest();
    e.open("POST", this._apiAddress), e.setRequestHeader("Authorization", this._token.value), e.responseType = "json", this.xhr = e;
  }
  /**
  * Attaches listeners to the XHR.
  */
  _attachXHRListeners() {
    const e = this.xhr, n = (r) => () => this.fire("error", r);
    e.addEventListener("error", n("Network Error")), e.addEventListener("abort", n("Abort"));
    /* istanbul ignore else -- @preserve */
    e.upload && e.upload.addEventListener("progress", (r) => {
      r.lengthComputable && this.fire("progress", {
        total: r.total,
        uploaded: r.loaded
      });
    }), e.addEventListener("load", () => {
      const r = e.status, s = e.response;
      if (r < 200 || r > 299)
        return this.fire("error", s.message || s.error);
    });
  }
  /**
  * Sends XHR request.
  */
  _sendRequest() {
    const e = new FormData(), n = this.xhr;
    return e.append("file", this.file), new Promise((r, s) => {
      n.addEventListener("load", () => {
        const o = n.status, a = n.response;
        return o < 200 || o > 299 ? a.message ? s(new T("fileuploader-uploading-data-failed", this, {
          message: a.message
        })) : s(a.error) : r(a);
      }), n.addEventListener("error", () => s(new Error("Network Error"))), n.addEventListener("abort", () => s(new Error("Abort"))), n.send(e);
    });
  }
}
function zN(i, t = 512) {
  try {
    const e = i.match(ym)[1], n = atob(i.replace(ym, "")), r = [];
    for (let s = 0; s < n.length; s += t) {
      const o = n.slice(s, s + t), a = new Array(o.length);
      for (let l = 0; l < o.length; l++)
        a[l] = o.charCodeAt(l);
      r.push(new Uint8Array(a));
    }
    return new Blob(r, {
      type: e
    });
  } catch {
    throw new T("fileuploader-decoding-image-data-error", null);
  }
}
function HN(i) {
  if (typeof i != "string")
    return !1;
  const t = i.match(ym);
  return !!(t && t.length);
}
class jN {
  /**
  * Creates `UploadGateway` instance.
  *
  * @param token Token used for authentication.
  * @param apiAddress API address.
  */
  constructor(t, e) {
    /**
    * CKEditor Cloud Services access token.
    */
    h(this, "_token");
    /**
    * CKEditor Cloud Services API address.
    */
    h(this, "_apiAddress");
    if (!t)
      throw new T("uploadgateway-missing-token", null);
    if (!e)
      throw new T("uploadgateway-missing-api-address", null);
    this._token = t, this._apiAddress = e;
  }
  /**
  * Creates a {@link module:cloud-services/uploadgateway/fileuploader~FileUploader} instance that wraps
  * file upload process. The file is being sent at a time when the
  * {@link module:cloud-services/uploadgateway/fileuploader~FileUploader#send} method is called.
  *
  * ```ts
  * const token = await Token.create( 'https://token-endpoint' );
  * new UploadGateway( token, 'https://example.org' )
  * 	.upload( 'FILE' )
  * 	.onProgress( ( data ) => console.log( data ) )
  * 	.send()
  * 	.then( ( response ) => console.log( response ) );
  * ```
  *
  * @param {Blob|String} fileOrData A blob object or a data string encoded with Base64.
  * @returns {module:cloud-services/uploadgateway/fileuploader~FileUploader} Returns `FileUploader` instance.
  */
  upload(t) {
    return new $N(t, this._token, this._apiAddress);
  }
}
class WN extends jd {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CloudServicesCore";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Creates the {@link module:cloud-services/token/token~Token} instance.
  *
  * @param tokenUrlOrRefreshToken Endpoint address to download the token or a callback that provides the token. If the
  * value is a function it has to match the {@link module:cloud-services/token/token~Token#refreshToken} interface.
  * @param options.initValue Initial value of the token.
  * @param options.autoRefresh Specifies whether to start the refresh automatically.
  */
  createToken(t, e) {
    return new kp(t, e);
  }
  /**
  * Creates the {@link module:cloud-services/uploadgateway/uploadgateway~UploadGateway} instance.
  *
  * @param token Token used for authentication.
  * @param apiAddress API address.
  */
  createUploadGateway(t, e) {
    return new jN(t, e);
  }
}
class UN extends jd {
  constructor() {
    super(...arguments);
    /**
    * The authentication token URL for CKEditor Cloud Services or a callback to the token value promise. See the
    * {@link module:cloud-services/cloudservicesconfig~CloudServicesConfig#tokenUrl} for more details.
    */
    h(this, "tokenUrl");
    /**
    * The URL to which the files should be uploaded.
    */
    h(this, "uploadUrl");
    /**
    * The URL for web socket communication, used by the `RealTimeCollaborativeEditing` plugin. Every customer (organization in the CKEditor
    * Ecosystem dashboard) has their own, unique URLs to communicate with CKEditor Cloud Services. The URL can be found in the
    * CKEditor Ecosystem customer dashboard.
    *
    * Note: Unlike most plugins, `RealTimeCollaborativeEditing` is not included in any CKEditor 5 build and needs to be installed manually.
    * Check [Collaboration overview](https://ckeditor.com/docs/ckeditor5/latest/features/collaboration/overview.html) for more details.
    */
    h(this, "webSocketUrl");
    /**
    * An optional parameter used for integration with CKEditor Cloud Services when uploading the editor build to cloud services.
    *
    * Whenever the editor build or the configuration changes, this parameter should be set to a new, unique value to differentiate
    * the new bundle (build + configuration) from the old ones.
    */
    h(this, "bundleVersion");
    /**
    * Other plugins use this token for the authorization process. It handles token requesting and refreshing.
    * Its value is `null` when {@link module:cloud-services/cloudservicesconfig~CloudServicesConfig#tokenUrl} is not provided.
    *
    * @readonly
    */
    h(this, "token", null);
    /**
    * A map of token object instances keyed by the token URLs.
    */
    h(this, "_tokens", /* @__PURE__ */ new Map());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CloudServices";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      WN
    ];
  }
  /**
  * @inheritDoc
  */
  async init() {
    const n = this.context.config.get("cloudServices") || {};
    for (const [s, o] of Object.entries(n))
      this[s] = o;
    if (!this.tokenUrl) {
      this.token = null;
      return;
    }
    const r = this.context.plugins.get("CloudServicesCore");
    this.token = await r.createToken(this.tokenUrl).init(), this._tokens.set(this.tokenUrl, this.token);
  }
  /**
  * Registers an additional authentication token URL for CKEditor Cloud Services or a callback to the token value promise. See the
  * {@link module:cloud-services/cloudservicesconfig~CloudServicesConfig#tokenUrl} for more details.
  *
  * @param tokenUrl The authentication token URL for CKEditor Cloud Services or a callback to the token value promise.
  */
  async registerTokenUrl(e) {
    if (this._tokens.has(e))
      return this.getTokenFor(e);
    const r = await this.context.plugins.get("CloudServicesCore").createToken(e).init();
    return this._tokens.set(e, r), r;
  }
  /**
  * Returns an authentication token provider previously registered by {@link #registerTokenUrl}.
  *
  * @param tokenUrl The authentication token URL for CKEditor Cloud Services or a callback to the token value promise.
  */
  getTokenFor(e) {
    const n = this._tokens.get(e);
    if (!n)
      throw new T("cloudservices-token-not-registered", this);
    return n;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy();
    for (const e of this._tokens.values())
      e.destroy();
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Yu(i) {
  const t = i.t, e = i.config.get("codeBlock.languages");
  for (const n of e)
    n.label === "Plain text" && (n.label = t("Plain text")), n.class === void 0 && (n.class = `language-${n.language}`);
  return e;
}
function Ju(i, t, e) {
  const n = {};
  for (const r of i)
    if (t === "class") {
      const s = r[t].split(" ").shift();
      n[s] = r[e];
    } else
      n[r[t]] = r[e];
  return n;
}
function xp(i) {
  return i.data.match(/^(\s*)/)[0];
}
function qN(i, t) {
  const e = i.createDocumentFragment(), n = t.split(`
`), r = n.reduce((s, o, a) => (s.push(o), a < n.length - 1 && s.push(i.createElement("br")), s), []);
  return i.appendChild(r, e), e;
}
function _m(i) {
  const t = i.document.selection, e = [];
  if (t.isCollapsed)
    return [
      t.anchor
    ];
  const n = t.getFirstRange().getWalker({
    ignoreElementEnd: !0,
    direction: "backward"
  });
  for (const { item: r } of n) {
    let s = r.is("$textProxy") ? r.textNode : r;
    const o = s.parent;
    if (!o.is("element", "codeBlock") || s.is("element", "softBreak"))
      continue;
    for (; s.previousSibling && !s.previousSibling.is("element", "softBreak"); )
      s = s.previousSibling;
    const a = s.is("$text") ? s.startOffset + xp(s).length : s.startOffset, l = i.createPositionAt(o, a);
    e.every((c) => !c.isEqual(l)) && e.push(l);
  }
  return e;
}
function kk(i) {
  const t = Ft(i.getSelectedBlocks());
  return !!t && t.is("element", "codeBlock");
}
function gw(i, t) {
  return t.is("rootElement") || i.isLimit(t) ? !1 : i.checkChild(t.parent, "codeBlock");
}
function pw(i, t, e, n) {
  const r = Ju(t, "language", "label"), s = e.getAttribute("language");
  if (s in r) {
    const o = r[s];
    return i(n === "enter" ? "Entering %0 code snippet" : "Leaving %0 code snippet", o);
  }
  return i(n === "enter" ? "Entering code snippet" : "Leaving code snippet");
}
function xk(i, t) {
  for (i.textNode && (i = t.createPositionBefore(i.textNode)); i.nodeBefore && !i.nodeBefore.is("element", "softBreak"); )
    i = t.createPositionBefore(i.nodeBefore);
  const e = i.nodeAfter;
  return e && e.is("$text") ? e : null;
}
class GN extends Ve {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Contains the last used language.
    */
    h(this, "_lastLanguage");
    this._lastLanguage = null;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.value = this._getValue(), this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command. When the command {@link #value is on}, all topmost code blocks within
  * the selection will be removed. If it is off, all selected blocks will be flattened and
  * wrapped by a code block.
  *
  * @fires execute
  * @param options Command options.
  * @param options.language The code block language.
  * @param options.forceValue If set, it will force the command behavior. If `true`, the command will apply a code block,
  * otherwise the command will remove the code block. If not set, the command will act basing on its current value.
  * @param options.usePreviousLanguageChoice If set on `true` and the `options.language` is not specified, the command
  * will apply the previous language (if the command was already executed) when inserting the `codeBlock` element.
  */
  execute(e = {}) {
    const n = this.editor, r = n.model, s = r.document.selection, a = Yu(n)[0], l = Array.from(s.getSelectedBlocks()), c = e.forceValue == null ? !this.value : e.forceValue, u = KN(e, this._lastLanguage, a.language);
    r.change((d) => {
      c ? this._applyCodeBlock(d, l, u) : this._removeCodeBlock(d, l);
    });
  }
  /**
  * Checks the command's {@link #value}.
  *
  * @returns The current value.
  */
  _getValue() {
    const e = this.editor.model.document.selection, n = Ft(e.getSelectedBlocks());
    return !!(n && n.is("element", "codeBlock")) ? n.getAttribute("language") : !1;
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    if (this.value)
      return !0;
    const e = this.editor.model.document.selection, n = this.editor.model.schema, r = Ft(e.getSelectedBlocks());
    return r ? gw(n, r) : !1;
  }
  _applyCodeBlock(e, n, r) {
    this._lastLanguage = r;
    const s = this.editor.model.schema, o = n.filter((a) => gw(s, a));
    for (const a of o)
      e.rename(a, "codeBlock"), e.setAttribute("language", r, a), s.removeDisallowedAttributes([
        a
      ], e), Array.from(a.getChildren()).filter((l) => !s.checkChild(a, l)).forEach((l) => e.remove(l));
    o.reverse().forEach((a, l) => {
      const c = o[l + 1];
      a.previousSibling === c && (e.appendElement("softBreak", c), e.merge(e.createPositionBefore(a)));
    });
  }
  _removeCodeBlock(e, n) {
    const r = n.filter((s) => s.is("element", "codeBlock"));
    for (const s of r) {
      const o = e.createRangeOn(s);
      for (const a of Array.from(o.getItems()).reverse())
        if (a.is("element", "softBreak") && a.parent.is("element", "codeBlock")) {
          const { position: l } = e.split(e.createPositionBefore(a)), c = l.nodeAfter;
          e.rename(c, "paragraph"), e.removeAttribute("language", c), e.remove(a);
        }
      e.rename(s, "paragraph"), e.removeAttribute("language", s);
    }
  }
}
function KN(i, t, e) {
  return i.language ? i.language : i.usePreviousLanguageChoice && t ? t : e;
}
class ZN extends Ve {
  constructor(e) {
    super(e);
    /**
    * A sequence of characters added to the line when the command is executed.
    */
    h(this, "_indentSequence");
    this._indentSequence = e.config.get("codeBlock.indentSequence");
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command. When the command {@link #isEnabled is enabled}, the indentation of the
  * code lines in the selection will be increased.
  *
  * @fires execute
  */
  execute() {
    const n = this.editor.model;
    n.change((r) => {
      const s = _m(n);
      for (const o of s) {
        const a = r.createText(this._indentSequence);
        n.insertContent(a, o);
      }
    });
  }
  /**
  * Checks whether the command can be enabled in the current context.
  */
  _checkEnabled() {
    return this._indentSequence ? kk(this.editor.model.document.selection) : !1;
  }
}
class YN extends Ve {
  constructor(e) {
    super(e);
    /**
    * A sequence of characters removed from the line when the command is executed.
    */
    h(this, "_indentSequence");
    this._indentSequence = e.config.get("codeBlock.indentSequence");
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command. When the command {@link #isEnabled is enabled}, the indentation of the
  * code lines in the selection will be decreased.
  *
  * @fires execute
  */
  execute() {
    const n = this.editor.model;
    n.change(() => {
      const r = _m(n);
      for (const s of r) {
        const o = bw(n, s, this._indentSequence);
        o && n.deleteContent(n.createSelection(o));
      }
    });
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @private
  * @returns {Boolean} Whether the command should be enabled.
  */
  _checkEnabled() {
    if (!this._indentSequence)
      return !1;
    const e = this.editor.model;
    return kk(e.document.selection) ? _m(e).some((n) => bw(e, n, this._indentSequence)) : !1;
  }
}
function bw(i, t, e) {
  const n = xk(t, i);
  if (!n)
    return null;
  const r = xp(n), s = r.lastIndexOf(e);
  if (s + e.length !== r.length || s === -1)
    return null;
  const { parent: o, startOffset: a } = n;
  return i.createRange(i.createPositionAt(o, a + s), i.createPositionAt(o, a + s + e.length));
}
function vw(i, t, e = !1) {
  const n = Ju(t, "language", "class"), r = Ju(t, "language", "label");
  return (s, o, a) => {
    const { writer: l, mapper: c, consumable: u } = a;
    if (!u.consume(o.item, "insert"))
      return;
    const d = o.item.getAttribute("language"), f = c.toViewPosition(i.createPositionBefore(o.item)), m = {};
    e && (m["data-language"] = r[d], m.spellcheck = "false");
    const g = n[d] ? {
      class: n[d]
    } : void 0, p = l.createContainerElement("code", g), b = l.createContainerElement("pre", m, p);
    l.insert(f, b), c.bindElements(o.item, p);
  };
}
function JN(i) {
  return (t, e, n) => {
    if (e.item.parent.name !== "codeBlock")
      return;
    const { writer: r, mapper: s, consumable: o } = n;
    if (!o.consume(e.item, "insert"))
      return;
    const a = s.toViewPosition(i.createPositionBefore(e.item));
    r.insert(a, r.createText(`
`));
  };
}
function XN(i, t) {
  const e = Ju(t, "class", "language"), n = t[0].language;
  return (r, s, o) => {
    const a = s.viewItem, l = a.parent;
    if (!l || !l.is("element", "pre") || s.modelCursor.findAncestor("codeBlock"))
      return;
    const { consumable: c, writer: u } = o;
    if (!c.test(a, {
      name: !0
    }))
      return;
    const d = u.createElement("codeBlock"), f = [
      ...a.getClassNames()
    ];
    f.length || f.push("");
    for (const m of f) {
      const g = e[m];
      if (g) {
        u.setAttribute("language", g, d);
        break;
      }
    }
    d.hasAttribute("language") || u.setAttribute("language", n, d), o.convertChildren(a, d), o.safeInsert(d, s.modelCursor) && (c.consume(a, {
      name: !0
    }), o.updateConversionResult(d, s));
  };
}
function QN() {
  return (i, t, { consumable: e, writer: n }) => {
    let r = t.modelCursor;
    if (!e.test(t.viewItem) || !r.findAncestor("codeBlock"))
      return;
    e.consume(t.viewItem);
    const o = t.viewItem.data.split(`
`).map((l) => n.createText(l)), a = o[o.length - 1];
    for (const l of o)
      if (n.insert(l, r), r = r.getShiftedBy(l.offsetSize), l !== a) {
        const c = n.createElement("softBreak");
        n.insert(c, r), r = n.createPositionAfter(c);
      }
    t.modelRange = n.createRange(t.modelCursor, r), t.modelCursor = r;
  };
}
function eB() {
  return (i, t, { consumable: e }) => {
    const n = t.viewItem;
    if (n.findAncestor("pre"))
      return;
    const r = Array.from(n.getChildren()), s = r.find((o) => o.is("element", "code"));
    if (s)
      for (const o of r)
        o === s || !o.is("$text") || e.consume(o, {
          name: !0
        });
  };
}
const Ek = "paragraph";
class tB extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeBlockEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pk
    ];
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("codeBlock", {
      languages: [
        {
          language: "plaintext",
          label: "Plain text"
        },
        {
          language: "c",
          label: "C"
        },
        {
          language: "cs",
          label: "C#"
        },
        {
          language: "cpp",
          label: "C++"
        },
        {
          language: "css",
          label: "CSS"
        },
        {
          language: "diff",
          label: "Diff"
        },
        {
          language: "html",
          label: "HTML"
        },
        {
          language: "java",
          label: "Java"
        },
        {
          language: "javascript",
          label: "JavaScript"
        },
        {
          language: "php",
          label: "PHP"
        },
        {
          language: "python",
          label: "Python"
        },
        {
          language: "ruby",
          label: "Ruby"
        },
        {
          language: "typescript",
          label: "TypeScript"
        },
        {
          language: "xml",
          label: "XML"
        }
      ],
      // A single tab.
      indentSequence: "	"
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema, n = t.model, r = t.editing.view, s = Yu(t);
    t.commands.add("codeBlock", new GN(t)), t.commands.add("indentCodeBlock", new ZN(t)), t.commands.add("outdentCodeBlock", new YN(t)), this.listenTo(r.document, "tab", (o, a) => {
      const l = a.shiftKey ? "outdentCodeBlock" : "indentCodeBlock";
      t.commands.get(l).isEnabled && (t.execute(l), a.stopPropagation(), a.preventDefault(), o.stop());
    }, {
      context: "pre"
    }), e.register("codeBlock", {
      allowWhere: "$block",
      allowChildren: "$text",
      // Disallow `$inlineObject` and its derivatives like `inlineWidget` inside `codeBlock` to ensure that only text,
      // not other inline elements like inline images, are allowed. This maintains the semantic integrity of code blocks.
      disallowChildren: "$inlineObject",
      allowAttributes: [
        "language"
      ],
      allowAttributesOf: "$listItem",
      isBlock: !0
    }), e.addAttributeCheck((o, a) => {
      const l = o.getItem(o.length - 2);
      if (e.getAttributeProperties(a).isFormatting && l && l.name == "codeBlock")
        return !1;
    }), t.editing.downcastDispatcher.on("insert:codeBlock", vw(n, s, !0)), t.data.downcastDispatcher.on("insert:codeBlock", vw(n, s)), t.data.downcastDispatcher.on("insert:softBreak", JN(n), {
      priority: "high"
    }), t.data.upcastDispatcher.on("element:code", XN(r, s)), t.data.upcastDispatcher.on("text", QN()), t.data.upcastDispatcher.on("element:pre", eB(), {
      priority: "high"
    }), this.listenTo(t.editing.view.document, "clipboardInput", (o, a) => {
      let l = n.createRange(n.document.selection.anchor);
      if (a.targetRanges && (l = t.editing.mapper.toModelRange(a.targetRanges[0])), !l.start.parent.is("element", "codeBlock"))
        return;
      const c = a.dataTransfer.getData("text/plain"), u = new vr(t.editing.view.document);
      a.content = qN(u, c);
    }), t.plugins.has("ClipboardPipeline") && t.plugins.get(ui).on("contentInsertion", (o, a) => {
      const l = t.model, c = l.document.selection;
      c.anchor.parent.is("element", "codeBlock") && l.change((u) => {
        const d = u.createRangeIn(a.content);
        for (const f of [
          ...d.getItems()
        ])
          f.is("node") && !e.checkChild(c.anchor, f) && u.remove(f);
      });
    }), this.listenTo(n, "getSelectedContent", (o, [a]) => {
      const l = a.anchor;
      a.isCollapsed || !l.parent.is("element", "codeBlock") || !l.hasSameParentAs(a.focus) || n.change((c) => {
        const u = o.return;
        if (l.parent.is("element") && (u.childCount > 1 || a.containsEntireContent(l.parent))) {
          const f = c.createElement("codeBlock", l.parent.getAttributes());
          c.append(u, f);
          const m = c.createDocumentFragment();
          c.append(f, m), o.return = m;
          return;
        }
        const d = u.getChild(0);
        e.checkAttribute(d, "code") && c.setAttribute("code", !0, d);
      });
    });
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.commands, n = e.get("indent"), r = e.get("outdent");
    n && n.registerChildCommand(e.get("indentCodeBlock"), {
      priority: "highest"
    }), r && r.registerChildCommand(e.get("outdentCodeBlock")), this.listenTo(t.editing.view.document, "enter", (s, o) => {
      t.model.document.selection.getLastPosition().parent.is("element", "codeBlock") && (!iB(t, o.isSoft) && !rB(t, o.isSoft) && nB(t), o.preventDefault(), s.stop());
    }, {
      context: "pre"
    }), this._initAriaAnnouncements();
  }
  /**
  * Observe when user enters or leaves code block and set proper aria value in global live announcer.
  * This allows screen readers to indicate when the user has entered and left the specified code block.
  *
  * @internal
  */
  _initAriaAnnouncements() {
    const { model: t, ui: e, t: n } = this.editor, r = Yu(this.editor);
    let s = null;
    t.document.selection.on("change:range", () => {
      const o = t.document.selection.focus.parent;
      !e || s === o || !o.is("element") || (s && s.is("element", "codeBlock") && e.ariaLiveAnnouncer.announce(pw(n, r, s, "leave")), o.is("element", "codeBlock") && e.ariaLiveAnnouncer.announce(pw(n, r, o, "enter")), s = o);
    });
  }
}
function nB(i) {
  const t = i.model, e = t.document, n = e.selection.getLastPosition();
  let r;
  const s = xk(n, t);
  s && s.is("$text") && (r = xp(s)), i.model.change((o) => {
    i.execute("shiftEnter"), r && o.insertText(r, e.selection.anchor);
  });
}
function iB(i, t) {
  const n = i.model.document, r = i.editing.view, s = n.selection.getLastPosition(), o = s.nodeAfter;
  return t || !n.selection.isCollapsed || !s.isAtStart || !Os(o) ? !1 : (i.model.change((a) => {
    i.execute("enter");
    const l = n.selection.anchor.parent.previousSibling;
    a.rename(l, Ek), a.setSelection(l, "in"), i.model.schema.removeDisallowedAttributes([
      l
    ], a), a.remove(o);
  }), r.scrollToTheSelection(), !0);
}
function rB(i, t) {
  const e = i.model, n = e.document, r = i.editing.view, s = n.selection.getLastPosition(), o = s.nodeBefore;
  let a;
  if (t || !n.selection.isCollapsed || !s.isAtEnd || !o || !o.previousSibling)
    return !1;
  if (Os(o) && Os(o.previousSibling))
    a = e.createRange(e.createPositionBefore(o.previousSibling), e.createPositionAfter(o));
  else if (mf(o) && Os(o.previousSibling) && Os(o.previousSibling.previousSibling))
    a = e.createRange(e.createPositionBefore(o.previousSibling.previousSibling), e.createPositionAfter(o));
  else if (mf(o) && Os(o.previousSibling) && mf(o.previousSibling.previousSibling) && o.previousSibling.previousSibling && Os(o.previousSibling.previousSibling.previousSibling))
    a = e.createRange(e.createPositionBefore(o.previousSibling.previousSibling.previousSibling), e.createPositionAfter(o));
  else
    return !1;
  return i.model.change((l) => {
    l.remove(a), i.execute("enter");
    const c = n.selection.anchor.parent;
    l.rename(c, Ek), i.model.schema.removeDisallowedAttributes([
      c
    ], l);
  }), r.scrollToTheSelection(), !0;
}
function mf(i) {
  return i && i.is("$text") && !i.data.match(/\S/);
}
function Os(i) {
  return i && i.is("element", "softBreak");
}
class sB extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeBlockUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, n = t.ui.componentFactory, r = Yu(t), s = this._getLanguageListItemDefinitions(r), o = t.commands.get("codeBlock");
    n.add("codeBlock", (a) => {
      const l = Yn(a, io), c = l.buttonView, u = e("Insert code block");
      return c.set({
        label: u,
        tooltip: !0,
        icon: ee.codeBlock,
        isToggleable: !0
      }), c.bind("isOn").to(o, "value", (d) => !!d), c.on("execute", () => {
        t.execute("codeBlock", {
          usePreviousLanguageChoice: !0
        }), t.editing.view.focus();
      }), l.on("execute", (d) => {
        t.execute("codeBlock", {
          language: d.source._codeBlockLanguage,
          forceValue: !0
        }), t.editing.view.focus();
      }), l.class = "ck-code-block-dropdown", l.bind("isEnabled").to(o), pa(l, s, {
        role: "menu",
        ariaLabel: u
      }), l;
    }), n.add("menuBar:codeBlock", (a) => {
      const l = new Yi(a);
      l.buttonView.set({
        role: "menuitem",
        label: e("Code block"),
        icon: ee.codeBlock
      }), l.bind("isEnabled").to(o);
      const c = new ra(a);
      c.set({
        ariaLabel: e("Insert code block")
      });
      for (const u of s) {
        const d = new Ks(a, l), f = new At(a);
        f.bind(...Object.keys(u.model)).to(u.model), f.set({
          isToggleable: !0,
          role: "menuitemcheckbox"
        }), f.delegate("execute").to(l), f.on("execute", () => {
          t.execute("codeBlock", {
            language: u.model._codeBlockLanguage,
            forceValue: o.value != u.model._codeBlockLanguage
          }), t.editing.view.focus();
        }), d.children.add(f), c.items.add(d);
      }
      return l.panelView.children.add(c), l;
    });
  }
  /**
  * A helper returning a collection of the `codeBlock` dropdown items representing languages
  * available for the user to choose from.
  */
  _getLanguageListItemDefinitions(t) {
    const n = this.editor.commands.get("codeBlock"), r = new Pn();
    for (const s of t) {
      const o = {
        type: "button",
        model: new ia({
          _codeBlockLanguage: s.language,
          label: s.label,
          role: "menuitemradio",
          withText: !0
        })
      };
      o.model.bind("isOn").to(n, "value", (a) => a === o.model._codeBlockLanguage), r.add(o);
    }
    return r;
  }
}
class oB extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      tB,
      sB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeBlock";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class aB extends kD {
  /**
  * Creates an instance of the classic editor UI class.
  *
  * @param editor The editor instance.
  * @param view The view of the UI.
  */
  constructor(e, n) {
    super(e);
    /**
    * The main (top–most) view of the editor UI.
    */
    h(this, "view");
    /**
    * A normalized `config.toolbar` object.
    */
    h(this, "_toolbarConfig");
    /**
    * The element replacer instance used to hide the editor's source element.
    */
    h(this, "_elementReplacer");
    this.view = n, this._toolbarConfig = Kd(e.config.get("toolbar")), this._elementReplacer = new g2(), this.listenTo(e.editing.view, "scrollToTheSelection", this._handleScrollToTheSelectionWithStickyPanel.bind(this));
  }
  /**
  * @inheritDoc
  */
  get element() {
    return this.view.element;
  }
  /**
  * Initializes the UI.
  *
  * @param replacementElement The DOM element that will be the source for the created editor.
  */
  init(e) {
    const n = this.editor, r = this.view, s = n.editing.view, o = r.editable, a = s.document.getRoot();
    o.name = a.rootName, r.render();
    const l = o.element;
    this.setEditableElement(o.name, l), r.editable.bind("isFocused").to(this.focusTracker), s.attachDomRoot(l), e && this._elementReplacer.replace(e, this.element), this._initPlaceholder(), this._initToolbar(), r.menuBarView && this._initMenuBar(r.menuBarView), this._initDialogPluginIntegration(), this._initContextualBalloonIntegration(), this.fire("ready");
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy();
    const e = this.view, n = this.editor.editing.view;
    this._elementReplacer.restore(), n.getDomRoot(e.editable.name) && n.detachDomRoot(e.editable.name), e.destroy();
  }
  /**
  * Initializes the editor toolbar.
  */
  _initToolbar() {
    const e = this.view;
    e.stickyPanel.bind("isActive").to(this.focusTracker, "isFocused"), e.stickyPanel.limiterElement = e.element, e.stickyPanel.bind("viewportTopOffset").to(this, "viewportOffset", ({ top: n }) => n || 0), e.toolbar.fillFromConfig(this._toolbarConfig, this.componentFactory), this.addToolbar(e.toolbar);
  }
  /**
  * Enable the placeholder text on the editing root.
  */
  _initPlaceholder() {
    const e = this.editor, n = e.editing.view, r = n.document.getRoot(), s = e.sourceElement;
    let o;
    const a = e.config.get("placeholder");
    a && (o = typeof a == "string" ? a : a[this.view.editable.name]), !o && s && s.tagName.toLowerCase() === "textarea" && (o = s.getAttribute("placeholder")), o && (r.placeholder = o), Qg({
      view: n,
      element: r,
      isDirectHost: !1,
      keepOnFocus: !0
    });
  }
  /**
  * Provides an integration between the sticky toolbar and {@link module:ui/panel/balloon/contextualballoon contextual balloon plugin}.
  * It allows the contextual balloon to consider the height of the
  * {@link module:editor-classic/classiceditoruiview~ClassicEditorUIView#stickyPanel}. It prevents the balloon from overlapping
  * the sticky toolbar by adjusting the balloon's position using viewport offset configuration.
  */
  _initContextualBalloonIntegration() {
    if (!this.editor.plugins.has("ContextualBalloon"))
      return;
    const { stickyPanel: e } = this.view, n = this.editor.plugins.get("ContextualBalloon");
    n.on("getPositionOptions", (s) => {
      const o = s.return;
      if (!o || !e.isSticky || !e.element)
        return;
      const a = new je(e.element).height, l = typeof o.target == "function" ? o.target() : o.target, c = typeof o.limiter == "function" ? o.limiter() : o.limiter;
      if (l && c && new je(l).height >= new je(c).height - a)
        return;
      const u = {
        ...o.viewportOffsetConfig
      }, d = (u.top || 0) + a;
      s.return = {
        ...o,
        viewportOffsetConfig: {
          ...u,
          top: d
        }
      };
    }, {
      priority: "low"
    });
    const r = () => {
      n.visibleView && n.updatePosition();
    };
    this.listenTo(e, "change:isSticky", r), this.listenTo(this.editor.ui, "change:viewportOffset", r);
  }
  /**
  * Provides an integration between the sticky toolbar and {@link module:utils/dom/scroll~scrollViewportToShowTarget}.
  * It allows the UI-agnostic engine method to consider the geometry of the
  * {@link module:editor-classic/classiceditoruiview~ClassicEditorUIView#stickyPanel} that pins to the
  * edge of the viewport and can obscure the user caret after scrolling the window.
  *
  * @param evt The `scrollToTheSelection` event info.
  * @param data The payload carried by the `scrollToTheSelection` event.
  * @param originalArgs The original arguments passed to `scrollViewportToShowTarget()` method (see implementation to learn more).
  */
  _handleScrollToTheSelectionWithStickyPanel(e, n, r) {
    const s = this.view.stickyPanel;
    if (s.isSticky) {
      const o = new je(s.element).height;
      n.viewportOffset.top += o;
    } else {
      const o = () => {
        this.editor.editing.view.scrollToTheSelection(r);
      };
      this.listenTo(s, "change:isSticky", o), setTimeout(() => {
        this.stopListening(s, "change:isSticky", o);
      }, 20);
    }
  }
  /**
  * Provides an integration between the sticky toolbar and {@link module:ui/dialog/dialog the Dialog plugin}.
  *
  * It moves the dialog down to ensure that the
  * {@link module:editor-classic/classiceditoruiview~ClassicEditorUIView#stickyPanel sticky panel}
  * used by the editor UI will not get obscured by the dialog when the dialog uses one of its automatic positions.
  */
  _initDialogPluginIntegration() {
    if (!this.editor.plugins.has("Dialog"))
      return;
    const e = this.view.stickyPanel, n = this.editor.plugins.get("Dialog");
    n.on("show", () => {
      const r = n.view;
      r.on("moveTo", (s, o) => {
        if (!e.isSticky || r.wasMoved)
          return;
        const a = new je(e.contentPanelElement);
        o[1] < a.bottom + $l.defaultOffset && (o[1] = a.bottom + $l.defaultOffset);
      }, {
        priority: "high"
      });
    }, {
      priority: "low"
    });
  }
}
class lB extends ED {
  /**
  * Creates an instance of the classic editor UI view.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param editingView The editing view instance this view is related to.
  * @param options Configuration options for the view instance.
  * @param options.shouldToolbarGroupWhenFull When set `true` enables automatic items grouping
  * in the main {@link module:editor-classic/classiceditoruiview~ClassicEditorUIView#toolbar toolbar}.
  * See {@link module:ui/toolbar/toolbarview~ToolbarOptions#shouldGroupWhenFull} to learn more.
  * @param options.label When set, this value will be used as an accessible `aria-label` of the
  * {@link module:ui/editableui/editableuiview~EditableUIView editable view}.
  */
  constructor(e, n, r = {}) {
    super(e);
    /**
    * Sticky panel view instance. This is a parent view of a {@link #toolbar}
    * that makes toolbar sticky.
    */
    h(this, "stickyPanel");
    /**
    * Toolbar view instance.
    */
    h(this, "toolbar");
    /**
    * Editable UI view.
    */
    h(this, "editable");
    this.stickyPanel = new ID(e), this.toolbar = new fs(e, {
      shouldGroupWhenFull: r.shouldToolbarGroupWhenFull
    }), r.useMenuBar && (this.menuBarView = new FD(e)), this.editable = new SD(e, n, void 0, {
      label: r.label
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.menuBarView ? this.stickyPanel.content.addMany([
      this.menuBarView,
      this.toolbar
    ]) : this.stickyPanel.content.add(this.toolbar), this.top.add(this.stickyPanel), this.main.add(this.editable);
  }
}
class cB extends (/* @__PURE__ */ hm(Oo)) {
  /**
  * Creates an instance of the classic editor.
  *
  * **Note:** do not use the constructor to create editor instances. Use the static
  * {@link module:editor-classic/classiceditor~ClassicEditor.create `ClassicEditor.create()`} method instead.
  *
  * @param sourceElementOrData The DOM element that will be the source for the created editor
  * or the editor's initial data. For more information see
  * {@link module:editor-classic/classiceditor~ClassicEditor.create `ClassicEditor.create()`}.
  * @param config The editor configuration.
  */
  constructor(e, n = {}) {
    if (!ku(e) && n.initialData !== void 0)
      throw new T("editor-create-initial-data", null);
    super(n);
    /**
    * @inheritDoc
    */
    h(this, "ui");
    this.config.define("menuBar.isVisible", !1), this.config.get("initialData") === void 0 && this.config.set("initialData", uB(e)), ku(e) && (this.sourceElement = e), this.model.document.createRoot();
    const r = !this.config.get("toolbar.shouldNotGroupWhenFull"), s = this.config.get("menuBar"), o = new lB(this.locale, this.editing.view, {
      shouldToolbarGroupWhenFull: r,
      useMenuBar: s.isVisible,
      label: this.config.get("label")
    });
    this.ui = new aB(this, o), l9(this);
  }
  /**
  * Destroys the editor instance, releasing all resources used by it.
  *
  * Updates the original editor element with the data if the
  * {@link module:core/editor/editorconfig~EditorConfig#updateSourceElementOnDestroy `updateSourceElementOnDestroy`}
  * configuration option is set to `true`.
  */
  destroy() {
    return this.sourceElement && this.updateSourceElement(), this.ui.destroy(), super.destroy();
  }
  /**
  * Creates a new classic editor instance.
  *
  * There are three ways how the editor can be initialized.
  *
  * # Replacing a DOM element (and loading data from it)
  *
  * You can initialize the editor using an existing DOM element:
  *
  * ```ts
  * ClassicEditor
  * 	.create( document.querySelector( '#editor' ) )
  * 	.then( editor => {
  * 		console.log( 'Editor was initialized', editor );
  * 	} )
  * 	.catch( err => {
  * 		console.error( err.stack );
  * 	} );
  * ```
  *
  * The element's content will be used as the editor data and the element will be replaced by the editor UI.
  *
  * # Creating a detached editor
  *
  * Alternatively, you can initialize the editor by passing the initial data directly as a string.
  * In this case, the editor will render an element that must be inserted into the DOM:
  *
  * ```ts
  * ClassicEditor
  * 	.create( '<p>Hello world!</p>' )
  * 	.then( editor => {
  * 		console.log( 'Editor was initialized', editor );
  *
  * 		// Initial data was provided so the editor UI element needs to be added manually to the DOM.
  * 		document.body.appendChild( editor.ui.element );
  * 	} )
  * 	.catch( err => {
  * 		console.error( err.stack );
  * 	} );
  * ```
  *
  * This lets you dynamically append the editor to your web page whenever it is convenient for you. You may use this method if your
  * web page content is generated on the client side and the DOM structure is not ready at the moment when you initialize the editor.
  *
  * # Replacing a DOM element (and data provided in `config.initialData`)
  *
  * You can also mix these two ways by providing a DOM element to be used and passing the initial data through the configuration:
  *
  * ```ts
  * ClassicEditor
  * 	.create( document.querySelector( '#editor' ), {
  * 		initialData: '<h2>Initial data</h2><p>Foo bar.</p>'
  * 	} )
  * 	.then( editor => {
  * 		console.log( 'Editor was initialized', editor );
  * 	} )
  * 	.catch( err => {
  * 		console.error( err.stack );
  * 	} );
  * ```
  *
  * This method can be used to initialize the editor on an existing element with the specified content in case if your integration
  * makes it difficult to set the content of the source element.
  *
  * Note that an error will be thrown if you pass the initial data both as the first parameter and also in the configuration.
  *
  * # Configuring the editor
  *
  * See the {@link module:core/editor/editorconfig~EditorConfig editor configuration documentation} to learn more about
  * customizing plugins, toolbar and more.
  *
  * @param sourceElementOrData The DOM element that will be the source for the created editor
  * or the editor's initial data.
  *
  * If a DOM element is passed, its content will be automatically loaded to the editor upon initialization
  * and the {@link module:editor-classic/classiceditorui~ClassicEditorUI#element editor element} will replace the passed element
  * in the DOM (the original one will be hidden and the editor will be injected next to it).
  *
  * If the {@link module:core/editor/editorconfig~EditorConfig#updateSourceElementOnDestroy updateSourceElementOnDestroy}
  * option is set to `true`, the editor data will be set back to the original element once the editor is destroyed and when a form,
  * in which this element is contained, is submitted (if the original element is a `<textarea>`). This ensures seamless integration
  * with native web forms.
  *
  * If the initial data is passed, a detached editor will be created. In this case you need to insert it into the DOM manually.
  * It is available under the {@link module:editor-classic/classiceditorui~ClassicEditorUI#element `editor.ui.element`} property.
  *
  * @param config The editor configuration.
  * @returns A promise resolved once the editor is ready. The promise resolves with the created editor instance.
  */
  static create(e, n = {}) {
    return new Promise((r) => {
      const s = new this(e, n);
      r(s.initPlugins().then(() => s.ui.init(ku(e) ? e : null)).then(() => s.data.init(s.config.get("initialData"))).then(() => s.fire("ready")).then(() => s));
    });
  }
}
function uB(i) {
  return ku(i) ? yR(i) : i;
}
function ku(i) {
  return us(i);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class dB extends Ve {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.affectsData = !1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = t.document.selection;
    let n = t.schema.getLimitElement(e);
    if (e.containsEntireContent(n) || !ww(t.schema, n))
      do
        if (n = n.parent, !n)
          return;
      while (!ww(t.schema, n));
    t.change((r) => {
      r.setSelection(n, "in");
    });
  }
}
function ww(i, t) {
  return i.isLimit(t) && (i.checkChild(t, "$text") || i.checkChild(t, "paragraph"));
}
const hB = /* @__PURE__ */ Kg("Ctrl+A");
class fB extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SelectAllEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, r = t.editing.view.document;
    t.commands.add("selectAll", new dB(t)), this.listenTo(r, "keydown", (s, o) => {
      Xo(o) === hB && (t.execute("selectAll"), o.preventDefault());
    }), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Select all"),
          keystroke: "CTRL+A"
        }
      ]
    });
  }
}
var mB = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20"><path d="M.75 15.5a.75.75 0 0 1 .75.75V18l.008.09A.5.5 0 0 0 2 18.5h1.75a.75.75 0 1 1 0 1.5H1.5l-.144-.007a1.5 1.5 0 0 1-1.35-1.349L0 18.5v-2.25a.75.75 0 0 1 .75-.75zm18.5 0a.75.75 0 0 1 .75.75v2.25l-.007.144a1.5 1.5 0 0 1-1.349 1.35L18.5 20h-2.25a.75.75 0 1 1 0-1.5H18a.5.5 0 0 0 .492-.41L18.5 18v-1.75a.75.75 0 0 1 .75-.75zm-10.45 3c.11 0 .2.09.2.2v1.1a.2.2 0 0 1-.2.2H7.2a.2.2 0 0 1-.2-.2v-1.1c0-.11.09-.2.2-.2h1.6zm4 0c.11 0 .2.09.2.2v1.1a.2.2 0 0 1-.2.2h-1.6a.2.2 0 0 1-.2-.2v-1.1c0-.11.09-.2.2-.2h1.6zm.45-5.5a.75.75 0 1 1 0 1.5h-8.5a.75.75 0 1 1 0-1.5h8.5zM1.3 11c.11 0 .2.09.2.2v1.6a.2.2 0 0 1-.2.2H.2a.2.2 0 0 1-.2-.2v-1.6c0-.11.09-.2.2-.2h1.1zm18.5 0c.11 0 .2.09.2.2v1.6a.2.2 0 0 1-.2.2h-1.1a.2.2 0 0 1-.2-.2v-1.6c0-.11.09-.2.2-.2h1.1zm-4.55-2a.75.75 0 1 1 0 1.5H4.75a.75.75 0 1 1 0-1.5h10.5zM1.3 7c.11 0 .2.09.2.2v1.6a.2.2 0 0 1-.2.2H.2a.2.2 0 0 1-.2-.2V7.2c0-.11.09-.2.2-.2h1.1zm18.5 0c.11 0 .2.09.2.2v1.6a.2.2 0 0 1-.2.2h-1.1a.2.2 0 0 1-.2-.2V7.2c0-.11.09-.2.2-.2h1.1zm-4.55-2a.75.75 0 1 1 0 1.5h-2.5a.75.75 0 1 1 0-1.5h2.5zm-5 0a.75.75 0 1 1 0 1.5h-5.5a.75.75 0 0 1 0-1.5h5.5zm-6.5-5a.75.75 0 0 1 0 1.5H2a.5.5 0 0 0-.492.41L1.5 2v1.75a.75.75 0 0 1-1.5 0V1.5l.007-.144A1.5 1.5 0 0 1 1.356.006L1.5 0h2.25zM18.5 0l.144.007a1.5 1.5 0 0 1 1.35 1.349L20 1.5v2.25a.75.75 0 1 1-1.5 0V2l-.008-.09A.5.5 0 0 0 18 1.5h-1.75a.75.75 0 1 1 0-1.5h2.25zM8.8 0c.11 0 .2.09.2.2v1.1a.2.2 0 0 1-.2.2H7.2a.2.2 0 0 1-.2-.2V.2c0-.11.09-.2.2-.2h1.6zm4 0c.11 0 .2.09.2.2v1.1a.2.2 0 0 1-.2.2h-1.6a.2.2 0 0 1-.2-.2V.2c0-.11.09-.2.2-.2h1.6z"/></svg>';
class gB extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SelectAllUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("selectAll", () => {
      const e = this._createButton(Te);
      return e.set({
        tooltip: !0
      }), e;
    }), t.ui.componentFactory.add("menuBar:selectAll", () => this._createButton(At));
  }
  /**
  * Creates a button for select all command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, n = e.locale, r = e.commands.get("selectAll"), s = new t(e.locale), o = n.t;
    return s.set({
      label: o("Select all"),
      icon: mB,
      keystroke: "Ctrl+A"
    }), s.bind("isEnabled").to(r, "isEnabled"), this.listenTo(s, "execute", () => {
      e.execute("selectAll"), e.editing.view.focus();
    }), s;
  }
}
class Tk extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      fB,
      gB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SelectAll";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class Sk extends Ve {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Stack of items stored by the command. These are pairs of:
    *
    * * {@link module:engine/model/batch~Batch batch} saved by the command,
    * * {@link module:engine/model/selection~Selection selection} state at the moment of saving the batch.
    */
    h(this, "_stack", []);
    /**
    * Stores all batches that were created by this command.
    *
    * @internal
    */
    h(this, "_createdBatches", /* @__PURE__ */ new WeakSet());
    this.refresh(), this._isEnabledBasedOnSelection = !1, this.listenTo(e.data, "set", (n, r) => {
      r[1] = {
        ...r[1]
      };
      const s = r[1];
      s.batchType || (s.batchType = {
        isUndoable: !1
      });
    }, {
      priority: "high"
    }), this.listenTo(e.data, "set", (n, r) => {
      r[1].batchType.isUndoable || this.clearStack();
    });
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._stack.length > 0;
  }
  /**
  * Returns all batches created by this command.
  */
  get createdBatches() {
    return this._createdBatches;
  }
  /**
  * Stores a batch in the command, together with the selection state of the {@link module:engine/model/document~Document document}
  * created by the editor which this command is registered to.
  *
  * @param batch The batch to add.
  */
  addBatch(e) {
    const n = this.editor.model.document.selection, r = {
      ranges: n.hasOwnRange ? Array.from(n.getRanges()) : [],
      isBackward: n.isBackward
    };
    this._stack.push({
      batch: e,
      selection: r
    }), this.refresh();
  }
  /**
  * Removes all items from the stack.
  */
  clearStack() {
    this._stack = [], this.refresh();
  }
  /**
  * Restores the {@link module:engine/model/document~Document#selection document selection} state after a batch was undone.
  *
  * @param ranges Ranges to be restored.
  * @param isBackward A flag describing whether the restored range was selected forward or backward.
  * @param operations Operations which has been applied since selection has been stored.
  */
  _restoreSelection(e, n, r) {
    const s = this.editor.model, o = s.document, a = [], l = e.map((u) => u.getTransformedByOperations(r)), c = l.flat();
    for (const u of l) {
      const d = u.filter((f) => f.root != o.graveyard).filter((f) => !bB(f, c));
      d.length && (pB(d), a.push(d[0]));
    }
    a.length && s.change((u) => {
      u.setSelection(a, {
        backward: n
      });
    });
  }
  /**
  * Undoes a batch by reversing that batch, transforming reversed batch and finally applying it.
  * This is a helper method for {@link #execute}.
  *
  * @param batchToUndo The batch to be undone.
  * @param undoingBatch The batch that will contain undoing changes.
  */
  _undo(e, n) {
    const r = this.editor.model, s = r.document;
    this._createdBatches.add(n);
    const o = e.operations.slice().filter((a) => a.isDocumentOperation);
    o.reverse();
    for (const a of o) {
      const l = a.baseVersion + 1, c = Array.from(s.history.getOperations(l)), d = $7([
        a.getReversed()
      ], c, {
        useRelations: !0,
        document: this.editor.model.document,
        padWithNoOps: !1,
        forceWeakRemove: !0
      }).operationsA;
      for (let f of d) {
        const m = f.affectedSelectable;
        m && !r.canEditAt(m) && (f = new gn(f.baseVersion)), n.addOperation(f), r.applyOperation(f), s.history.setOperationAsUndone(a, f);
      }
    }
  }
}
function pB(i) {
  i.sort((t, e) => t.start.isBefore(e.start) ? -1 : 1);
  for (let t = 1; t < i.length; t++) {
    const n = i[t - 1].getJoined(i[t], !0);
    n && (t--, i.splice(t, 2, n));
  }
}
function bB(i, t) {
  return t.some((e) => e !== i && e.containsRange(i, !0));
}
class vB extends Sk {
  /**
  * Executes the command. This method reverts a {@link module:engine/model/batch~Batch batch} added to the command's stack, transforms
  * and applies the reverted version on the {@link module:engine/model/document~Document document} and removes the batch from the stack.
  * Then, it restores the {@link module:engine/model/document~Document#selection document selection}.
  *
  * @fires execute
  * @fires revert
  * @param batch A batch that should be undone. If not set, the last added batch will be undone.
  */
  execute(t = null) {
    const e = t ? this._stack.findIndex((s) => s.batch == t) : this._stack.length - 1, n = this._stack.splice(e, 1)[0], r = this.editor.model.createBatch({
      isUndo: !0
    });
    this.editor.model.enqueueChange(r, () => {
      this._undo(n.batch, r);
      const s = this.editor.model.document.history.getOperations(n.batch.baseVersion);
      this._restoreSelection(n.selection.ranges, n.selection.isBackward, s);
    }), this.fire("revert", n.batch, r), this.refresh();
  }
}
class wB extends Sk {
  /**
  * Executes the command. This method reverts the last {@link module:engine/model/batch~Batch batch} added to
  * the command's stack, applies the reverted and transformed version on the
  * {@link module:engine/model/document~Document document} and removes the batch from the stack.
  * Then, it restores the {@link module:engine/model/document~Document#selection document selection}.
  *
  * @fires execute
  */
  execute() {
    const t = this._stack.pop(), e = this.editor.model.createBatch({
      isUndo: !0
    });
    this.editor.model.enqueueChange(e, () => {
      const r = t.batch.operations[t.batch.operations.length - 1].baseVersion + 1, s = this.editor.model.document.history.getOperations(r);
      this._restoreSelection(t.selection.ranges, t.selection.isBackward, s), this._undo(t.batch, e);
    }), this.refresh();
  }
}
class yB extends V {
  constructor() {
    super(...arguments);
    /**
    * The command that manages the undo {@link module:engine/model/batch~Batch batches} stack (history).
    * Created and registered during the {@link #init feature initialization}.
    */
    h(this, "_undoCommand");
    /**
    * The command that manages the redo {@link module:engine/model/batch~Batch batches} stack (history).
    * Created and registered during the {@link #init feature initialization}.
    */
    h(this, "_redoCommand");
    /**
    * Keeps track of which batches were registered in undo.
    */
    h(this, "_batchRegistry", /* @__PURE__ */ new WeakSet());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "UndoEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.t;
    this._undoCommand = new vB(e), this._redoCommand = new wB(e), e.commands.add("undo", this._undoCommand), e.commands.add("redo", this._redoCommand), this.listenTo(e.model, "applyOperation", (r, s) => {
      const o = s[0];
      if (!o.isDocumentOperation)
        return;
      const a = o.batch, l = this._redoCommand.createdBatches.has(a), c = this._undoCommand.createdBatches.has(a);
      this._batchRegistry.has(a) || (this._batchRegistry.add(a), a.isUndoable && (l ? this._undoCommand.addBatch(a) : c || (this._undoCommand.addBatch(a), this._redoCommand.clearStack())));
    }, {
      priority: "highest"
    }), this.listenTo(this._undoCommand, "revert", (r, s, o) => {
      this._redoCommand.addBatch(o);
    }), e.keystrokes.set("CTRL+Z", "undo"), e.keystrokes.set("CTRL+Y", "redo"), e.keystrokes.set("CTRL+SHIFT+Z", "redo"), e.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: n("Undo"),
          keystroke: "CTRL+Z"
        },
        {
          label: n("Redo"),
          keystroke: [
            [
              "CTRL+Y"
            ],
            [
              "CTRL+SHIFT+Z"
            ]
          ]
        }
      ]
    });
  }
}
class _B extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "UndoUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale, n = t.t, r = e.uiLanguageDirection == "ltr" ? ee.undo : ee.redo, s = e.uiLanguageDirection == "ltr" ? ee.redo : ee.undo;
    this._addButtonsToFactory("undo", n("Undo"), "CTRL+Z", r), this._addButtonsToFactory("redo", n("Redo"), "CTRL+Y", s);
  }
  /**
  * Creates a button for the specified command.
  *
  * @param name Command name.
  * @param label Button label.
  * @param keystroke Command keystroke.
  * @param Icon Source of the icon.
  */
  _addButtonsToFactory(t, e, n, r) {
    const s = this.editor;
    s.ui.componentFactory.add(t, () => {
      const o = this._createButton(Te, t, e, n, r);
      return o.set({
        tooltip: !0
      }), o;
    }), s.ui.componentFactory.add("menuBar:" + t, () => this._createButton(At, t, e, n, r));
  }
  /**
  * TODO
  */
  _createButton(t, e, n, r, s) {
    const o = this.editor, a = o.locale, l = o.commands.get(e), c = new t(a);
    return c.set({
      label: n,
      icon: s,
      keystroke: r
    }), c.bind("isEnabled").to(l, "isEnabled"), this.listenTo(c, "execute", () => {
      o.execute(e), o.editing.view.focus();
    }), c;
  }
}
class Xd extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      yB,
      _B
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Undo";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class CB extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      HC,
      Cp,
      Jd,
      Tk,
      pk,
      JD,
      Xd
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Essentials";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class kB extends de {
  /**
  * Creates a view of find and replace form.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * A collection of child views.
    */
    h(this, "children");
    /**
    * The find in text input view that stores the searched string.
    *
    * @internal
    */
    h(this, "_findInputView");
    /**
    * The replace input view.
    */
    h(this, "_replaceInputView");
    /**
    * The find button view that initializes the search process.
    */
    h(this, "_findButtonView");
    /**
    * The find previous button view.
    */
    h(this, "_findPrevButtonView");
    /**
    * The find next button view.
    */
    h(this, "_findNextButtonView");
    /**
    * A collapsible view aggregating the advanced search options.
    */
    h(this, "_advancedOptionsCollapsibleView");
    /**
    * A switch button view controlling the "Match case" option.
    */
    h(this, "_matchCaseSwitchView");
    /**
    * A switch button view controlling the "Whole words only" option.
    */
    h(this, "_wholeWordsOnlySwitchView");
    /**
    * The replace button view.
    */
    h(this, "_replaceButtonView");
    /**
    * The replace all button view.
    */
    h(this, "_replaceAllButtonView");
    /**
    * The `div` aggregating the inputs.
    */
    h(this, "_inputsDivView");
    /**
    * The `div` aggregating the action buttons.
    */
    h(this, "_actionButtonsDivView");
    /**
    * Tracks information about the DOM focus in the form.
    */
    h(this, "_focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "_keystrokes");
    /**
    * A collection of views that can be focused in the form.
    */
    h(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    h(this, "focusCycler");
    const n = e.t;
    this.children = this.createCollection(), this.set("matchCount", 0), this.set("highlightOffset", 0), this.set("isDirty", !1), this.set("_areCommandsEnabled", {}), this.set("_resultsCounterText", ""), this.set("_matchCase", !1), this.set("_wholeWordsOnly", !1), this.bind("_searchResultsFound").to(this, "matchCount", this, "isDirty", (r, s) => r > 0 && !s), this._findInputView = this._createInputField(n("Find in text…")), this._findPrevButtonView = this._createButton({
      label: n("Previous result"),
      class: "ck-button-prev",
      icon: ee.previousArrow,
      keystroke: "Shift+F3",
      tooltip: !0
    }), this._findNextButtonView = this._createButton({
      label: n("Next result"),
      class: "ck-button-next",
      icon: ee.previousArrow,
      keystroke: "F3",
      tooltip: !0
    }), this._replaceInputView = this._createInputField(n("Replace with…"), "ck-labeled-field-replace"), this._inputsDivView = this._createInputsDiv(), this._matchCaseSwitchView = this._createMatchCaseSwitch(), this._wholeWordsOnlySwitchView = this._createWholeWordsOnlySwitch(), this._advancedOptionsCollapsibleView = this._createAdvancedOptionsCollapsible(), this._replaceAllButtonView = this._createButton({
      label: n("Replace all"),
      class: "ck-button-replaceall",
      withText: !0
    }), this._replaceButtonView = this._createButton({
      label: n("Replace"),
      class: "ck-button-replace",
      withText: !0
    }), this._findButtonView = this._createButton({
      label: n("Find"),
      class: "ck-button-find ck-button-action",
      withText: !0
    }), this._actionButtonsDivView = this._createActionButtonsDiv(), this._focusTracker = new wt(), this._keystrokes = new Ot(), this._focusables = new In(), this.focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this._focusTracker,
      keystrokeHandler: this._keystrokes,
      actions: {
        // Navigate form fields backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the <kbd>Tab</kbd> key.
        focusNext: "tab"
      }
    }), this.children.addMany([
      this._inputsDivView,
      this._advancedOptionsCollapsibleView,
      this._actionButtonsDivView
    ]), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-find-and-replace-form"
        ],
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Cs({
      view: this
    }), this._initFocusCycling(), this._initKeystrokeHandling();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._focusTracker.destroy(), this._keystrokes.destroy();
  }
  /**
  * @inheritDoc
  */
  focus(e) {
    e === -1 ? this.focusCycler.focusLast() : this.focusCycler.focusFirst();
  }
  /**
  * Resets the form before re-appearing.
  *
  * It clears error messages, hides the match counter and disables the replace feature
  * until the next hit of the "Find" button.
  *
  * **Note**: It does not reset inputs and options, though. This way the form works better in editors with
  * disappearing toolbar (e.g. BalloonEditor): hiding the toolbar by accident (together with the find and replace UI)
  * does not require filling the entire form again.
  */
  reset() {
    this._findInputView.errorText = null, this.isDirty = !0;
  }
  /**
  * Returns the value of the find input.
  */
  get _textToFind() {
    return this._findInputView.fieldView.element.value;
  }
  /**
  * Returns the value of the replace input.
  */
  get _textToReplace() {
    return this._replaceInputView.fieldView.element.value;
  }
  /**
  * Configures and returns the `<div>` aggregating all form inputs.
  */
  _createInputsDiv() {
    const e = this.locale, n = e.t, r = new de(e);
    return this._findInputView.fieldView.on("input", () => {
      this.isDirty = !0;
    }), this._findPrevButtonView.delegate("execute").to(this, "findPrevious"), this._findNextButtonView.delegate("execute").to(this, "findNext"), this._findPrevButtonView.bind("isEnabled").to(this, "_areCommandsEnabled", ({ findPrevious: s }) => s), this._findNextButtonView.bind("isEnabled").to(this, "_areCommandsEnabled", ({ findNext: s }) => s), this._injectFindResultsCounter(), this._replaceInputView.bind("isEnabled").to(this, "_areCommandsEnabled", this, "_searchResultsFound", ({ replace: s }, o) => s && o), this._replaceInputView.bind("infoText").to(this._replaceInputView, "isEnabled", this._replaceInputView, "isFocused", (s, o) => s || !o ? "" : n("Tip: Find some text first in order to replace it.")), r.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-find-and-replace-form__inputs"
        ]
      },
      children: [
        this._findInputView,
        this._findPrevButtonView,
        this._findNextButtonView,
        this._replaceInputView
      ]
    }), r;
  }
  /**
  * The action performed when the {@link #_findButtonView} is pressed.
  */
  _onFindButtonExecute() {
    if (!this._textToFind) {
      const e = this.t;
      this._findInputView.errorText = e("Text to find must not be empty.");
      return;
    }
    this.isDirty = !1, this.fire("findNext", {
      searchText: this._textToFind,
      matchCase: this._matchCase,
      wholeWords: this._wholeWordsOnly
    });
  }
  /**
  * Configures an injects the find results counter displaying a "N of M" label of the {@link #_findInputView}.
  */
  _injectFindResultsCounter() {
    const e = this.locale, n = e.t, r = this.bindTemplate, s = new de(this.locale);
    this.bind("_resultsCounterText").to(this, "highlightOffset", this, "matchCount", (a, l) => n("%0 of %1", [
      a,
      l
    ])), s.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-results-counter",
          // The counter only makes sense when the field text corresponds to search results in the editing.
          r.if("isDirty", "ck-hidden")
        ]
      },
      children: [
        {
          text: r.to("_resultsCounterText")
        }
      ]
    });
    const o = () => {
      const a = this._findInputView.fieldView.element;
      if (!a || !mr(a))
        return;
      const l = new je(s.element).width, c = e.uiLanguageDirection === "ltr" ? "paddingRight" : "paddingLeft";
      l ? a.style[c] = `calc( 2 * var(--ck-spacing-standard) + ${l}px )` : a.style[c] = "";
    };
    this.on("change:_resultsCounterText", o, {
      priority: "low"
    }), this.on("change:isDirty", o, {
      priority: "low"
    }), this._findInputView.template.children[0].children.push(s);
  }
  /**
  * Creates the collapsible view aggregating the advanced search options.
  */
  _createAdvancedOptionsCollapsible() {
    const e = this.locale.t, n = new gp(this.locale, [
      this._matchCaseSwitchView,
      this._wholeWordsOnlySwitchView
    ]);
    return n.set({
      label: e("Advanced options"),
      isCollapsed: !0
    }), n;
  }
  /**
  * Configures and returns the `<div>` element aggregating all form action buttons.
  */
  _createActionButtonsDiv() {
    const e = new de(this.locale);
    return this._replaceButtonView.bind("isEnabled").to(this, "_areCommandsEnabled", this, "_searchResultsFound", ({ replace: n }, r) => n && r), this._replaceAllButtonView.bind("isEnabled").to(this, "_areCommandsEnabled", this, "_searchResultsFound", ({ replaceAll: n }, r) => n && r), this._replaceButtonView.on("execute", () => {
      this.fire("replace", {
        searchText: this._textToFind,
        replaceText: this._textToReplace
      });
    }), this._replaceAllButtonView.on("execute", () => {
      this.fire("replaceAll", {
        searchText: this._textToFind,
        replaceText: this._textToReplace
      }), this.focus();
    }), this._findButtonView.on("execute", this._onFindButtonExecute.bind(this)), e.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-find-and-replace-form__actions"
        ]
      },
      children: [
        this._replaceAllButtonView,
        this._replaceButtonView,
        this._findButtonView
      ]
    }), e;
  }
  /**
  * Creates, configures and returns and instance of a dropdown allowing users to narrow
  * the search criteria down. The dropdown has a list with switch buttons for each option.
  */
  _createMatchCaseSwitch() {
    const e = this.locale.t, n = new Gs(this.locale);
    return n.set({
      label: e("Match case"),
      withText: !0
    }), n.bind("isOn").to(this, "_matchCase"), n.on("execute", () => {
      this._matchCase = !this._matchCase, this.isDirty = !0;
    }), n;
  }
  /**
  * Creates, configures and returns and instance of a dropdown allowing users to narrow
  * the search criteria down. The dropdown has a list with switch buttons for each option.
  */
  _createWholeWordsOnlySwitch() {
    const e = this.locale.t, n = new Gs(this.locale);
    return n.set({
      label: e("Whole words only"),
      withText: !0
    }), n.bind("isOn").to(this, "_wholeWordsOnly"), n.on("execute", () => {
      this._wholeWordsOnly = !this._wholeWordsOnly, this.isDirty = !0;
    }), n;
  }
  /**
  * Initializes the {@link #_focusables} and {@link #_focusTracker} to allow navigation
  * using <kbd>Tab</kbd> and <kbd>Shift</kbd>+<kbd>Tab</kbd> keystrokes in the right order.
  */
  _initFocusCycling() {
    [
      this._findInputView,
      this._findPrevButtonView,
      this._findNextButtonView,
      this._replaceInputView,
      this._advancedOptionsCollapsibleView.buttonView,
      this._matchCaseSwitchView,
      this._wholeWordsOnlySwitchView,
      this._replaceAllButtonView,
      this._replaceButtonView,
      this._findButtonView
    ].forEach((n) => {
      this._focusables.add(n), this._focusTracker.add(n.element);
    });
  }
  /**
  * Initializes the keystroke handling in the form.
  */
  _initKeystrokeHandling() {
    const e = (r) => r.stopPropagation(), n = (r) => {
      r.stopPropagation(), r.preventDefault();
    };
    this._keystrokes.listenTo(this.element), this._keystrokes.set("f3", (r) => {
      n(r), this._findNextButtonView.fire("execute");
    }), this._keystrokes.set("shift+f3", (r) => {
      n(r), this._findPrevButtonView.fire("execute");
    }), this._keystrokes.set("enter", (r) => {
      const s = r.target;
      s === this._findInputView.fieldView.element ? (this._areCommandsEnabled.findNext ? this._findNextButtonView.fire("execute") : this._findButtonView.fire("execute"), n(r)) : s === this._replaceInputView.fieldView.element && !this.isDirty && (this._replaceButtonView.fire("execute"), n(r));
    }), this._keystrokes.set("shift+enter", (r) => {
      r.target === this._findInputView.fieldView.element && (this._areCommandsEnabled.findPrevious ? this._findPrevButtonView.fire("execute") : this._findButtonView.fire("execute"), n(r));
    }), this._keystrokes.set("arrowright", e), this._keystrokes.set("arrowleft", e), this._keystrokes.set("arrowup", e), this._keystrokes.set("arrowdown", e);
  }
  /**
  * Creates a button view.
  *
  * @param options The properties of the `ButtonView`.
  * @returns The button view instance.
  */
  _createButton(e) {
    const n = new Te(this.locale);
    return n.set(e), n;
  }
  /**
  * Creates a labeled input view.
  *
  * @param label The input label.
  * @returns The labeled input view instance.
  */
  _createInputField(e, n) {
    const r = new Qt(this.locale, yi);
    return r.label = e, r.class = n, r;
  }
}
var yw = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m12.87 13.786 1.532-1.286 3.857 4.596a1 1 0 1 1-1.532 1.286l-3.857-4.596z"/><path d="M16.004 8.5a6.5 6.5 0 0 1-9.216 5.905c-1.154-.53-.863-1.415-.663-1.615.194-.194.564-.592 1.635-.141a4.5 4.5 0 0 0 5.89-5.904l-.104-.227 1.332-1.331c.045-.046.196-.041.224.007a6.47 6.47 0 0 1 .902 3.306zm-3.4-5.715c.562.305.742 1.106.354 1.494-.388.388-.995.414-1.476.178a4.5 4.5 0 0 0-6.086 5.882l.114.236-1.348 1.349c-.038.037-.17.022-.198-.023a6.5 6.5 0 0 1 5.54-9.9 6.469 6.469 0 0 1 3.1.784z"/><path d="M4.001 11.93.948 8.877a.2.2 0 0 1 .141-.341h6.106a.2.2 0 0 1 .141.341L4.283 11.93a.2.2 0 0 1-.282 0zm11.083-6.789 3.053 3.053a.2.2 0 0 1-.14.342H11.89a.2.2 0 0 1-.14-.342l3.052-3.053a.2.2 0 0 1 .282 0z"/></svg>';
class xB extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A reference to the find and replace form view.
    */
    h(this, "formView");
    e.config.define("findAndReplace.uiType", "dialog"), this.formView = null;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Ud
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FindAndReplaceUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.config.get("findAndReplace.uiType") === "dropdown", r = e.commands.get("find"), s = this.editor.t;
    e.ui.componentFactory.add("findAndReplace", () => {
      let o;
      return n ? (o = this._createDropdown(), o.bind("isEnabled").to(r)) : o = this._createDialogButtonForToolbar(), e.keystrokes.set("Ctrl+F", (a, l) => {
        if (r.isEnabled) {
          if (o instanceof qu) {
            const c = o.buttonView;
            c.isOn || c.fire("execute");
          } else
            o.isOn ? e.plugins.get("Dialog").view.focus() : o.fire("execute");
          l();
        }
      }), o;
    }), n || e.ui.componentFactory.add("menuBar:findAndReplace", () => this._createDialogButtonForMenuBar()), e.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: s("Find in the document"),
          keystroke: "CTRL+F"
        }
      ]
    });
  }
  /**
  * Creates a dropdown containing the find and replace form.
  */
  _createDropdown() {
    const e = this.editor, n = e.locale.t, r = Yn(e.locale);
    return r.once("change:isOpen", () => {
      this.formView = this._createFormView(), this.formView.children.add(new Wd(e.locale, {
        label: n("Find and replace")
      }), 0), r.panelView.children.add(this.formView);
    }), r.on("change:isOpen", (s, o, a) => {
      a ? this._setupFormView() : this.fire("searchReseted");
    }, {
      priority: "low"
    }), r.buttonView.set({
      icon: yw,
      label: n("Find and replace"),
      keystroke: "CTRL+F",
      tooltip: !0
    }), r;
  }
  /**
  * Creates a button that opens a dialog with the find and replace form.
  */
  _createDialogButtonForToolbar() {
    const e = this.editor, n = this._createButton(Te), r = e.plugins.get("Dialog");
    return n.set({
      tooltip: !0
    }), n.bind("isOn").to(r, "id", (s) => s === "findAndReplace"), n.on("execute", () => {
      n.isOn ? r.hide() : this._showDialog();
    }), n;
  }
  /**
  * Creates a button for for menu bar that will show find and replace dialog.
  */
  _createDialogButtonForMenuBar() {
    const e = this._createButton(At), n = this.editor.plugins.get("Dialog"), r = this.editor.plugins.get("Dialog");
    return e.set({
      role: "menuitemcheckbox",
      isToggleable: !0
    }), e.bind("isOn").to(r, "id", (s) => s === "findAndReplace"), e.on("execute", () => {
      if (n.id === "findAndReplace") {
        n.hide();
        return;
      }
      this._showDialog();
    }), e;
  }
  /**
  * Creates a button for find and replace command to use either in toolbar or in menu bar.
  */
  _createButton(e) {
    const n = this.editor, r = n.commands.get("find"), s = new e(n.locale), o = n.locale.t;
    return s.bind("isEnabled").to(r), s.set({
      icon: yw,
      label: o("Find and replace"),
      keystroke: "CTRL+F"
    }), s;
  }
  /**
  * Shows the find and replace dialog.
  */
  _showDialog() {
    const e = this.editor, n = e.plugins.get("Dialog"), r = e.locale.t;
    this.formView || (this.formView = this._createFormView()), n.show({
      id: "findAndReplace",
      title: r("Find and replace"),
      content: this.formView,
      position: Ii.EDITOR_TOP_SIDE,
      onShow: () => {
        this._setupFormView();
      },
      onHide: () => {
        this.fire("searchReseted");
      }
    });
  }
  /**
  * Sets up the form view for the find and replace.
  *
  * @param formView A related form view.
  */
  _createFormView() {
    const e = this.editor, n = new (bc(kB))(e.locale), r = e.commands, o = this.editor.plugins.get("FindAndReplaceEditing").state;
    n.bind("highlightOffset").to(o, "highlightedOffset"), n.listenTo(o.results, "change", () => {
      n.matchCount = o.results.length;
    });
    const a = r.get("findNext"), l = r.get("findPrevious"), c = r.get("replace"), u = r.get("replaceAll");
    return n.bind("_areCommandsEnabled").to(a, "isEnabled", l, "isEnabled", c, "isEnabled", u, "isEnabled", (d, f, m, g) => ({
      findNext: d,
      findPrevious: f,
      replace: m,
      replaceAll: g
    })), n.delegate("findNext", "findPrevious", "replace", "replaceAll").to(this), n.on("change:isDirty", (d, f, m) => {
      m && this.fire("searchReseted");
    }), n;
  }
  /**
  * Clears the find and replace form and focuses the search text field.
  */
  _setupFormView() {
    this.formView.disableCssTransitions(), this.formView.reset(), this.formView._findInputView.fieldView.select(), this.formView.enableCssTransitions();
  }
}
class EB extends Ve {
  /**
  * Creates a new `FindCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param state An object to hold plugin state.
  */
  constructor(e, n) {
    super(e);
    /**
    * The find and replace state object used for command operations.
    */
    h(this, "_state");
    this.isEnabled = !0, this.affectsData = !1, this._state = n;
  }
  /**
  * Executes the command.
  *
  * @param callbackOrText
  * @param options Options object.
  * @param options.matchCase If set to `true`, the letter case will be matched.
  * @param options.wholeWords If set to `true`, only whole words that match `callbackOrText` will be matched.
  *
  * @fires execute
  */
  execute(e, { matchCase: n, wholeWords: r } = {}) {
    const { editor: s } = this, { model: o } = s, a = s.plugins.get("FindAndReplaceUtils");
    let l, c = "";
    typeof e == "string" ? l = (...f) => ({
      results: a.findByTextCallback(e, {
        matchCase: n,
        wholeWords: r
      })(...f),
      searchText: e
    }) : l = e;
    const u = l;
    l = (...f) => {
      const m = u(...f);
      return m && "searchText" in m && (c = m.searchText), m;
    };
    const d = o.document.getRootNames().reduce((f, m) => a.updateFindResultFromRange(o.createRangeIn(o.document.getRoot(m)), o, l, f), null);
    return this._state.clear(o), this._state.results.addMany(d), this._state.highlightedResult = d.get(0), this._state.searchText = c, l && (this._state.lastSearchCallback = l), this._state.matchCase = !!n, this._state.matchWholeWords = !!r, {
      results: d,
      findCallback: l
    };
  }
}
class Ak extends Ve {
  /**
  * Creates a new `ReplaceCommand` instance.
  *
  * @param editor Editor on which this command will be used.
  * @param state An object to hold plugin state.
  */
  constructor(e, n) {
    super(e);
    /**
    * The find and replace state object used for command operations.
    */
    h(this, "_state");
    this.isEnabled = !0, this._state = n, this._isEnabledBasedOnSelection = !1;
  }
  /**
  * Common logic for both `replace` commands.
  * Replace a given find result by a string or a callback.
  *
  * @param result A single result from the find command.
  */
  _replace(e, n) {
    const { model: r } = this.editor, s = n.marker.getRange();
    r.canEditAt(s) && r.change((o) => {
      if (s.root.rootName === "$graveyard") {
        this._state.results.remove(n);
        return;
      }
      let a = {};
      for (const l of s.getItems())
        if (l.is("$text") || l.is("$textProxy")) {
          a = l.getAttributes();
          break;
        }
      r.insertContent(o.createText(e, a), s), this._state.results.has(n) && this._state.results.remove(n);
    });
  }
}
class TB extends Ak {
  /**
  * Replace a given find result by a string or a callback.
  *
  * @param result A single result from the find command.
  *
  * @fires execute
  */
  execute(t, e) {
    this._replace(t, e);
  }
}
class SB extends Ak {
  /**
  * Replaces all the occurrences of `textToReplace` with a given `newText` string.
  *
  * ```ts
  *	replaceAllCommand.execute( 'replaceAll', 'new text replacement', 'text to replace' );
  * ```
  *
  * Alternatively you can call it from editor instance:
  *
  * ```ts
  *	editor.execute( 'replaceAll', 'new text', 'old text' );
  * ```
  *
  * @param newText Text that will be inserted to the editor for each match.
  * @param textToReplace Text to be replaced or a collection of matches
  * as returned by the find command.
  *
  * @fires module:core/command~Command#event:execute
  */
  execute(t, e) {
    const { editor: n } = this, { model: r } = n, s = n.plugins.get("FindAndReplaceUtils"), o = e instanceof Pn ? e : r.document.getRootNames().reduce((a, l) => s.updateFindResultFromRange(r.createRangeIn(r.document.getRoot(l)), r, s.findByTextCallback(e, this._state), a), null);
    o.length && r.change(() => {
      [
        ...o
      ].forEach((a) => {
        this._replace(t, a);
      });
    });
  }
}
class Pk extends Ve {
  /**
  * Creates a new `FindNextCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param state An object to hold plugin state.
  */
  constructor(e, n) {
    super(e);
    /**
    * The find and replace state object used for command operations.
    */
    h(this, "_state");
    this.affectsData = !1, this._state = n, this.isEnabled = !1, this.listenTo(this._state.results, "change", () => {
      this.isEnabled = this._state.results.length > 1;
    });
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._state.results.length > 1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const e = this._state.results, n = e.getIndex(this._state.highlightedResult), r = n + 1 >= e.length ? 0 : n + 1;
    this._state.highlightedResult = this._state.results.get(r);
  }
}
class AB extends Pk {
  /**
  * @inheritDoc
  */
  execute() {
    const e = this._state.results.getIndex(this._state.highlightedResult), n = e - 1 < 0 ? this._state.results.length - 1 : e - 1;
    this._state.highlightedResult = this._state.results.get(n);
  }
}
class PB extends (/* @__PURE__ */ st()) {
  /**
  * Creates an instance of the state.
  */
  constructor(t) {
    super(), this.set("results", new Pn()), this.set("highlightedResult", null), this.set("highlightedOffset", 0), this.set("searchText", ""), this.set("replaceText", ""), this.set("lastSearchCallback", null), this.set("matchCase", !1), this.set("matchWholeWords", !1), this.results.on("change", (e, { removed: n, index: r }) => {
      if (Array.from(n).length) {
        let s = !1;
        if (t.change((o) => {
          for (const a of n)
            this.highlightedResult === a && (s = !0), t.markers.has(a.marker.name) && o.removeMarker(a.marker);
        }), s) {
          const o = r >= this.results.length ? 0 : r;
          this.highlightedResult = this.results.get(o);
        }
      }
    }), this.on("change:highlightedResult", () => {
      this.refreshHighlightOffset();
    });
  }
  /**
  * Cleans the state up and removes markers from the model.
  */
  clear(t) {
    this.searchText = "", t.change((e) => {
      if (this.highlightedResult) {
        const n = this.highlightedResult.marker.name.split(":")[1], r = t.markers.get(`findResultHighlighted:${n}`);
        r && e.removeMarker(r);
      }
      [
        ...this.results
      ].forEach(({ marker: n }) => {
        e.removeMarker(n);
      });
    }), this.results.clear();
  }
  /**
  * Refreshes the highlight result offset based on it's index within the result list.
  */
  refreshHighlightOffset() {
    const { highlightedResult: t, results: e } = this, n = {
      before: -1,
      same: 0,
      after: 1,
      different: 1
    };
    t ? this.highlightedOffset = Array.from(e).sort((r, s) => n[r.marker.getStart().compareWith(s.marker.getStart())]).indexOf(t) + 1 : this.highlightedOffset = 0;
  }
}
class IB extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FindAndReplaceUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Executes findCallback and updates search results list.
  *
  * @param range The model range to scan for matches.
  * @param model The model.
  * @param findCallback The callback that should return `true` if provided text matches the search term.
  * @param startResults An optional collection of find matches that the function should
  * start with. This would be a collection returned by a previous `updateFindResultFromRange()` call.
  * @returns A collection of objects describing find match.
  *
  * An example structure:
  *
  * ```js
  * {
  *	id: resultId,
  *	label: foundItem.label,
  *	marker
  *	}
  * ```
  */
  updateFindResultFromRange(t, e, n, r) {
    const s = r || new Pn(), o = (a) => s.find((l) => {
      const { marker: c } = l, u = c.getRange(), d = a.getRange();
      return u.isEqual(d);
    });
    return e.change((a) => {
      [
        ...t
      ].forEach(({ type: l, item: c }) => {
        if (l === "elementStart" && e.schema.checkChild(c, "$text")) {
          let u = n({
            item: c,
            text: this.rangeToText(e.createRangeIn(c))
          });
          if (!u)
            return;
          "results" in u && (u = u.results), u.forEach((d) => {
            const f = `findResult:${Jn()}`, m = a.addMarker(f, {
              usingOperation: !1,
              affectsData: !1,
              range: a.createRange(a.createPositionAt(c, d.start), a.createPositionAt(c, d.end))
            }), g = RB(s, m);
            o(m) || s.add({
              id: f,
              label: d.label,
              marker: m
            }, g);
          });
        }
      });
    }), s;
  }
  /**
  * Returns text representation of a range. The returned text length should be the same as range length.
  * In order to achieve this, this function will replace inline elements (text-line) as new line character ("\n").
  *
  * @param range The model range.
  * @returns The text content of the provided range.
  */
  rangeToText(t) {
    return Array.from(t.getItems()).reduce((e, n) => n.is("$text") || n.is("$textProxy") ? e + n.data : `${e}
`, "");
  }
  /**
  * Creates a text matching callback for a specified search term and matching options.
  *
  * @param searchTerm The search term.
  * @param options Matching options.
  * 	- options.matchCase=false If set to `true` letter casing will be ignored.
  * 	- options.wholeWords=false If set to `true` only whole words that match `callbackOrText` will be matched.
  */
  findByTextCallback(t, e) {
    let n = "gu";
    e.matchCase || (n += "i");
    let r = `(${s2(t)})`;
    if (e.wholeWords) {
      const a = "[^a-zA-ZÀ-ɏḀ-ỿ]";
      new RegExp("^" + a).test(t) || (r = `(^|${a}|_)${r}`), new RegExp(a + "$").test(t) || (r = `${r}(?=_|${a}|$)`);
    }
    const s = new RegExp(r, n);
    function o({ text: a }) {
      return [
        ...a.matchAll(s)
      ].map(OB);
    }
    return o;
  }
}
function RB(i, t) {
  const e = i.find(({ marker: n }) => t.getStart().isBefore(n.getStart()));
  return e ? i.getIndex(e) : i.length;
}
function OB(i) {
  const t = i.length - 1;
  let e = i.index;
  return i.length === 3 && (e += i[1].length), {
    label: i[t],
    start: e,
    end: e + i[t].length
  };
}
const VB = "ck-find-result_selected";
class MB extends V {
  constructor() {
    super(...arguments);
    /**
    * An object storing the find and replace state within a given editor instance.
    */
    h(this, "state");
    /**
    * Reacts to document changes in order to update search list.
    */
    h(this, "_onDocumentChange", () => {
      const e = /* @__PURE__ */ new Set(), n = /* @__PURE__ */ new Set(), r = this.editor.model, { results: s } = this.state, o = r.document.differ.getChanges(), a = r.document.differ.getChangedMarkers();
      o.forEach((u) => {
        u.position && (u.name === "$text" || u.position.nodeAfter && r.schema.isInline(u.position.nodeAfter) ? (e.add(u.position.parent), [
          ...r.markers.getMarkersAtPosition(u.position)
        ].forEach((d) => {
          n.add(d.name);
        })) : u.type === "insert" && u.position.nodeAfter && e.add(u.position.nodeAfter));
      }), a.forEach(({ name: u, data: { newRange: d } }) => {
        d && d.start.root.rootName === "$graveyard" && n.add(u);
      }), e.forEach((u) => {
        [
          ...r.markers.getMarkersIntersectingRange(r.createRangeIn(u))
        ].forEach((f) => n.add(f.name));
      }), n.forEach((u) => {
        s.has(u) && (s.get(u) === this.state.highlightedResult && (this.state.highlightedResult = null), s.remove(u));
      });
      const l = [], c = this.editor.plugins.get("FindAndReplaceUtils");
      e.forEach((u) => {
        const d = c.updateFindResultFromRange(r.createRangeOn(u), r, this.state.lastSearchCallback, s);
        l.push(...d);
      }), a.forEach((u) => {
        if (u.data.newRange) {
          const d = c.updateFindResultFromRange(u.data.newRange, r, this.state.lastSearchCallback, s);
          l.push(...d);
        }
      }), !this.state.highlightedResult && l.length ? this.state.highlightedResult = l[0] : this.state.refreshHighlightOffset();
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      IB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FindAndReplaceEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.state = new PB(this.editor.model), this.set("_isSearchActive", !1), this._defineConverters(), this._defineCommands(), this.listenTo(this.state, "change:highlightedResult", (r, s, o, a) => {
      const { model: l } = this.editor;
      l.change((c) => {
        if (a) {
          const u = a.marker.name.split(":")[1], d = l.markers.get(`findResultHighlighted:${u}`);
          d && c.removeMarker(d);
        }
        if (o) {
          const u = o.marker.name.split(":")[1];
          c.addMarker(`findResultHighlighted:${u}`, {
            usingOperation: !1,
            affectsData: !1,
            range: o.marker.getRange()
          });
        }
      });
    });
    /* istanbul ignore next -- @preserve */
    const n = Ci(((r, s, o) => {
      if (o) {
        const a = this.editor.editing.view.domConverter, l = this.editor.editing.mapper.toViewRange(o.marker.getRange());
        S2({
          target: a.viewRangeToDom(l),
          viewportOffset: 40
        });
      }
    }).bind(this), 32);
    this.listenTo(this.state, "change:highlightedResult", n, {
      priority: "low"
    }), this.listenTo(this.editor, "destroy", n.cancel), this.on("change:_isSearchActive", (r, s, o) => {
      o ? this.listenTo(this.editor.model.document, "change:data", this._onDocumentChange) : this.stopListening(this.editor.model.document, "change:data", this._onDocumentChange);
    });
  }
  /**
  * Initiate a search.
  */
  find(e, n) {
    return this._isSearchActive = !0, this.editor.execute("find", e, n), this.state.results;
  }
  /**
  * Stops active results from updating, and clears out the results.
  */
  stop() {
    this.state.clear(this.editor.model), this._isSearchActive = !1;
  }
  /**
  * Sets up the commands.
  */
  _defineCommands() {
    this.editor.commands.add("find", new EB(this.editor, this.state)), this.editor.commands.add("findNext", new Pk(this.editor, this.state)), this.editor.commands.add("findPrevious", new AB(this.editor, this.state)), this.editor.commands.add("replace", new TB(this.editor, this.state)), this.editor.commands.add("replaceAll", new SB(this.editor, this.state));
  }
  /**
  * Sets up the marker downcast converters for search results highlighting.
  */
  _defineConverters() {
    const { editor: e } = this;
    e.conversion.for("editingDowncast").markerToHighlight({
      model: "findResult",
      view: ({ markerName: n }) => {
        const [, r] = n.split(":");
        return {
          name: "span",
          classes: [
            "ck-find-result"
          ],
          attributes: {
            // ...however, adding a unique attribute should be future-proof..
            "data-find-result": r
          }
        };
      }
    }), e.conversion.for("editingDowncast").markerToHighlight({
      model: "findResultHighlighted",
      view: ({ markerName: n }) => {
        const [, r] = n.split(":");
        return {
          name: "span",
          classes: [
            VB
          ],
          attributes: {
            // ...however, adding a unique attribute should be future-proof..
            "data-find-result": r
          }
        };
      }
    });
  }
}
class DB extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      MB,
      xB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FindAndReplace";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins.get("FindAndReplaceUI"), e = this.editor.plugins.get("FindAndReplaceEditing"), n = e.state;
    t.on("findNext", (r, s) => {
      s ? (n.searchText = s.searchText, e.find(s.searchText, s)) : this.editor.execute("findNext");
    }), t.on("findPrevious", (r, s) => {
      s && n.searchText !== s.searchText ? e.find(s.searchText) : this.editor.execute("findPrevious");
    }), t.on("replace", (r, s) => {
      n.searchText !== s.searchText && e.find(s.searchText);
      const o = n.highlightedResult;
      o && this.editor.execute("replace", s.replaceText, o);
    }), t.on("replaceAll", (r, s) => {
      n.searchText !== s.searchText && e.find(s.searchText), this.editor.execute("replaceAll", s.replaceText, n.results);
    }), t.on("searchReseted", () => {
      n.clear(this.editor.model), e.stop();
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class LB extends Ve {
  constructor(t) {
    super(t), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document, n = Ft(e.selection.getSelectedBlocks());
    this.value = !!n && n.is("element", "paragraph"), this.isEnabled = !!n && _w(n, t.schema);
  }
  /**
  * Executes the command. All the blocks (see {@link module:engine/model/schema~Schema}) in the selection
  * will be turned to paragraphs.
  *
  * @fires execute
  * @param options Options for the executed command.
  * @param options.selection The selection that the command should be applied to. By default,
  * if not provided, the command is applied to the {@link module:engine/model/document~Document#selection}.
  */
  execute(t = {}) {
    const e = this.editor.model, n = e.document, r = t.selection || n.selection;
    e.canEditAt(r) && e.change((s) => {
      const o = r.getSelectedBlocks();
      for (const a of o)
        !a.is("element", "paragraph") && _w(a, e.schema) && s.rename(a, "paragraph");
    });
  }
}
function _w(i, t) {
  return t.checkChild(i.parent, "paragraph") && !t.isObject(i);
}
class NB extends Ve {
  constructor(t) {
    super(t), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * Executes the command.
  *
  * @param options Options for the executed command.
  * @param options.position The model position at which the new paragraph will be inserted.
  * @param options.attributes Attributes keys and values to set on a inserted paragraph.
  * @fires execute
  */
  execute(t) {
    const e = this.editor.model, n = t.attributes;
    let r = t.position;
    e.canEditAt(r) && e.change((s) => {
      if (r = this._findPositionToInsertParagraph(r, s), !r)
        return;
      const o = s.createElement("paragraph");
      n && e.schema.setAllowedAttributes(o, n, s), e.insertContent(o, r), s.setSelection(o, "in");
    });
  }
  /**
  * Returns the best position to insert a new paragraph.
  */
  _findPositionToInsertParagraph(t, e) {
    const n = this.editor.model;
    if (n.schema.checkChild(t, "paragraph"))
      return t;
    const r = n.schema.findAllowedParent(t, "paragraph");
    if (!r)
      return null;
    const s = t.parent, o = n.schema.checkChild(s, "$text");
    return s.isEmpty || o && t.isAtEnd ? n.createPositionAfter(s) : !s.isEmpty && o && t.isAtStart ? n.createPositionBefore(s) : e.split(t, r).position;
  }
}
const Ad = class Ad extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Paragraph";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model;
    t.commands.add("paragraph", new LB(t)), t.commands.add("insertParagraph", new NB(t)), e.schema.register("paragraph", {
      inheritAllFrom: "$block"
    }), t.conversion.elementToElement({
      model: "paragraph",
      view: "p"
    }), t.conversion.for("upcast").elementToElement({
      model: (n, { writer: r }) => !Ad.paragraphLikeElements.has(n.name) || n.isEmpty ? null : r.createElement("paragraph"),
      view: /.+/,
      converterPriority: "low"
    });
  }
};
/**
* A list of element names which should be treated by the autoparagraphing algorithms as
* paragraph-like. This means that e.g. the following content:
*
* ```html
* <h1>Foo</h1>
* <table>
*   <tr>
*     <td>X</td>
*     <td>
*       <ul>
*         <li>Y</li>
*         <li>Z</li>
*       </ul>
*     </td>
*   </tr>
* </table>
* ```
*
* contains five paragraph-like elements: `<h1>`, two `<td>`s and two `<li>`s.
* Hence, if none of the features is going to convert those elements the above content will be automatically handled
* by the paragraph feature and converted to:
*
* ```html
* <p>Foo</p>
* <p>X</p>
* <p>Y</p>
* <p>Z</p>
* ```
*
* Note: The `<td>` containing two `<li>` elements was ignored as the innermost paragraph-like elements
* have a priority upon conversion.
*/
h(Ad, "paragraphLikeElements", /* @__PURE__ */ new Set([
  "blockquote",
  "dd",
  "div",
  "dt",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "li",
  "p",
  "td",
  "th"
]));
let Xu = Ad;
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class BB extends Ve {
  /**
  * Creates an instance of the command.
  *
  * @param editor Editor instance.
  * @param modelElements Names of the element which this command can apply in the model.
  */
  constructor(e, n) {
    super(e);
    /**
    * Set of defined model's elements names that this command support.
    * See {@link module:heading/headingconfig~HeadingOption}.
    */
    h(this, "modelElements");
    this.modelElements = n;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = Ft(this.editor.model.document.selection.getSelectedBlocks());
    this.value = !!e && this.modelElements.includes(e.name) && e.name, this.isEnabled = !!e && this.modelElements.some((n) => Cw(e, n, this.editor.model.schema));
  }
  /**
  * Executes the command. Applies the heading to the selected blocks or, if the first selected
  * block is a heading already, turns selected headings (of this level only) to paragraphs.
  *
  * @param options.value Name of the element which this command will apply in the model.
  * @fires execute
  */
  execute(e) {
    const n = this.editor.model, r = n.document, s = e.value;
    n.change((o) => {
      const a = Array.from(r.selection.getSelectedBlocks()).filter((l) => Cw(l, s, n.schema));
      for (const l of a)
        l.is("element", s) || o.rename(l, s);
    });
  }
}
function Cw(i, t, e) {
  return e.checkChild(i.parent, t) && !e.isObject(i);
}
const kw = "paragraph";
class FB extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HeadingEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("heading", {
      options: [
        {
          model: "paragraph",
          title: "Paragraph",
          class: "ck-heading_paragraph"
        },
        {
          model: "heading1",
          view: "h2",
          title: "Heading 1",
          class: "ck-heading_heading1"
        },
        {
          model: "heading2",
          view: "h3",
          title: "Heading 2",
          class: "ck-heading_heading2"
        },
        {
          model: "heading3",
          view: "h4",
          title: "Heading 3",
          class: "ck-heading_heading3"
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Xu
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.config.get("heading.options"), n = [];
    for (const r of e)
      r.model !== "paragraph" && (t.model.schema.register(r.model, {
        inheritAllFrom: "$block"
      }), t.conversion.elementToElement(r), n.push(r.model));
    this._addDefaultH1Conversion(t), t.commands.add("heading", new BB(t, n));
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.commands.get("enter"), n = t.config.get("heading.options");
    e && this.listenTo(e, "afterExecute", (r, s) => {
      const o = t.model.document.selection.getFirstPosition().parent;
      n.some((l) => o.is("element", l.model)) && !o.is("element", kw) && o.childCount === 0 && s.writer.rename(o, kw);
    });
  }
  /**
  * Adds default conversion for `h1` -> `heading1` with a low priority.
  *
  * @param editor Editor instance on which to add the `h1` conversion.
  */
  _addDefaultH1Conversion(t) {
    t.conversion.for("upcast").elementToElement({
      model: "heading1",
      view: "h1",
      // With a `low` priority, `paragraph` plugin autoparagraphing mechanism is executed. Make sure
      // this listener is called before it. If not, `h1` will be transformed into a paragraph.
      converterPriority: wi.low + 1
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function $B(i) {
  const t = i.t, e = {
    Paragraph: t("Paragraph"),
    "Heading 1": t("Heading 1"),
    "Heading 2": t("Heading 2"),
    "Heading 3": t("Heading 3"),
    "Heading 4": t("Heading 4"),
    "Heading 5": t("Heading 5"),
    "Heading 6": t("Heading 6")
  };
  return i.config.get("heading.options").map((n) => {
    const r = e[n.title];
    return r && r != n.title && (n.title = r), n;
  });
}
class zB extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HeadingUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, n = $B(t), r = e("Choose heading"), s = e("Heading");
    t.ui.componentFactory.add("heading", (o) => {
      const a = {}, l = new Pn(), c = t.commands.get("heading"), u = t.commands.get("paragraph"), d = [
        c
      ];
      for (const m of n) {
        const g = {
          type: "button",
          model: new ia({
            label: m.title,
            class: m.class,
            role: "menuitemradio",
            withText: !0
          })
        };
        m.model === "paragraph" ? (g.model.bind("isOn").to(u, "value"), g.model.set("commandName", "paragraph"), d.push(u)) : (g.model.bind("isOn").to(c, "value", (p) => p === m.model), g.model.set({
          commandName: "heading",
          commandValue: m.model
        })), l.add(g), a[m.model] = m.title;
      }
      const f = Yn(o);
      return pa(f, l, {
        ariaLabel: s,
        role: "menu"
      }), f.buttonView.set({
        ariaLabel: s,
        ariaLabelledBy: void 0,
        isOn: !1,
        withText: !0,
        tooltip: s
      }), f.extendTemplate({
        attributes: {
          class: [
            "ck-heading-dropdown"
          ]
        }
      }), f.bind("isEnabled").toMany(d, "isEnabled", (...m) => m.some((g) => g)), f.buttonView.bind("label").to(c, "value", u, "value", (m, g) => {
        const p = g ? "paragraph" : m;
        return typeof p == "boolean" || !a[p] ? r : a[p];
      }), f.buttonView.bind("ariaLabel").to(c, "value", u, "value", (m, g) => {
        const p = g ? "paragraph" : m;
        return typeof p == "boolean" || !a[p] ? s : `${a[p]}, ${s}`;
      }), this.listenTo(f, "execute", (m) => {
        const { commandName: g, commandValue: p } = m.source;
        t.execute(g, p ? {
          value: p
        } : void 0), t.editing.view.focus();
      }), f;
    }), t.ui.componentFactory.add("menuBar:heading", (o) => {
      const a = new Yi(o), l = t.commands.get("heading"), c = t.commands.get("paragraph"), u = [
        l
      ], d = new ra(o);
      a.set({
        class: "ck-heading-dropdown"
      }), d.set({
        ariaLabel: e("Heading"),
        role: "menu"
      }), a.buttonView.set({
        label: e("Heading")
      }), a.panelView.children.add(d);
      for (const f of n) {
        const m = new Ks(o, a), g = new At(o);
        m.children.add(g), d.items.add(m), g.set({
          isToggleable: !0,
          label: f.title,
          role: "menuitemradio",
          class: f.class
        }), g.delegate("execute").to(a), g.on("execute", () => {
          const p = f.model === "paragraph" ? "paragraph" : "heading";
          t.execute(p, {
            value: f.model
          }), t.editing.view.focus();
        }), f.model === "paragraph" ? (g.bind("isOn").to(c, "value"), u.push(c)) : g.bind("isOn").to(l, "value", (p) => p === f.model);
      }
      return a.bind("isEnabled").toMany(u, "isEnabled", (...f) => f.some((m) => m)), a;
    });
  }
}
class HB extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      FB,
      zB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Heading";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class jB extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document;
    this.value = e.selection.getAttribute("highlight"), this.isEnabled = t.schema.checkAttributeInSelection(e.selection, "highlight");
  }
  /**
  * Executes the command.
  *
  * @param options Options for the executed command.
  * @param options.value The value to apply.
  *
  * @fires execute
  */
  execute(t = {}) {
    const e = this.editor.model, r = e.document.selection, s = t.value;
    e.change((o) => {
      if (r.isCollapsed) {
        const a = r.getFirstPosition();
        if (r.hasAttribute("highlight")) {
          const l = (f) => f.item.hasAttribute("highlight") && f.item.getAttribute("highlight") === this.value, c = a.getLastMatchingPosition(l, {
            direction: "backward"
          }), u = a.getLastMatchingPosition(l), d = o.createRange(c, u);
          !s || this.value === s ? (a.isEqual(u) || o.removeAttribute("highlight", d), o.removeSelectionAttribute("highlight")) : (a.isEqual(u) || o.setAttribute("highlight", s, d), o.setSelectionAttribute("highlight", s));
        } else s && o.setSelectionAttribute("highlight", s);
      } else {
        const a = e.schema.getValidRanges(r.getRanges(), "highlight");
        for (const l of a)
          s ? o.setAttribute("highlight", s, l) : o.removeAttribute("highlight", l);
      }
    });
  }
}
class WB extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HighlightEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("highlight", {
      options: [
        {
          model: "yellowMarker",
          class: "marker-yellow",
          title: "Yellow marker",
          color: "var(--ck-highlight-marker-yellow)",
          type: "marker"
        },
        {
          model: "greenMarker",
          class: "marker-green",
          title: "Green marker",
          color: "var(--ck-highlight-marker-green)",
          type: "marker"
        },
        {
          model: "pinkMarker",
          class: "marker-pink",
          title: "Pink marker",
          color: "var(--ck-highlight-marker-pink)",
          type: "marker"
        },
        {
          model: "blueMarker",
          class: "marker-blue",
          title: "Blue marker",
          color: "var(--ck-highlight-marker-blue)",
          type: "marker"
        },
        {
          model: "redPen",
          class: "pen-red",
          title: "Red pen",
          color: "var(--ck-highlight-pen-red)",
          type: "pen"
        },
        {
          model: "greenPen",
          class: "pen-green",
          title: "Green pen",
          color: "var(--ck-highlight-pen-green)",
          type: "pen"
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: "highlight"
    });
    const e = t.config.get("highlight.options");
    t.conversion.attributeToElement(UB(e)), t.commands.add("highlight", new jB(t));
  }
}
function UB(i) {
  const t = {
    model: {
      key: "highlight",
      values: []
    },
    view: {}
  };
  for (const e of i)
    t.model.values.push(e.model), t.view[e.model] = {
      name: "mark",
      classes: e.class
    };
  return t;
}
var qB = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path class="ck-icon__fill" d="M10.798 1.59 3.002 12.875l1.895 1.852 2.521 1.402 6.997-12.194z"/><path d="m2.556 16.727.234-.348c-.297-.151-.462-.293-.498-.426-.036-.137.002-.416.115-.837.094-.25.15-.449.169-.595a4.495 4.495 0 0 0 0-.725c-.209-.621-.303-1.041-.284-1.26.02-.218.178-.506.475-.862l6.77-9.414c.539-.91 1.605-.85 3.199.18 1.594 1.032 2.188 1.928 1.784 2.686l-5.877 10.36c-.158.412-.333.673-.526.782-.193.108-.604.179-1.232.21-.362.131-.608.237-.738.318-.13.081-.305.238-.526.47-.293.265-.504.397-.632.397-.096 0-.27-.075-.524-.226l-.31.41-1.6-1.12zm-.279.415 1.575 1.103-.392.515H1.19l1.087-1.618zm8.1-13.656-4.953 6.9L8.75 12.57l4.247-7.574c.175-.25-.188-.647-1.092-1.192-.903-.546-1.412-.652-1.528-.32zM8.244 18.5 9.59 17h9.406v1.5H8.245z"/></svg>', GB = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path class="ck-icon__fill" d="M10.126 2.268 2.002 13.874l1.895 1.852 2.521 1.402L14.47 5.481l-1.543-2.568-2.801-.645z"/><path d="m4.5 18.088-2.645-1.852-.04-2.95-.006-.005.006-.008v-.025l.011.008L8.73 2.97c.165-.233.356-.417.567-.557l-1.212.308L4.604 7.9l-.83-.558 3.694-5.495 2.708-.69 1.65 1.145.046.018.85-1.216 2.16 1.512-.856 1.222c.828.967 1.144 2.141.432 3.158L7.55 17.286l.006.005-3.055.797H4.5zm-.634.166-1.976.516-.026-1.918 2.002 1.402zM9.968 3.817l-.006-.004-6.123 9.184 3.277 2.294 6.108-9.162.005.003c.317-.452-.16-1.332-1.064-1.966-.891-.624-1.865-.776-2.197-.349zM8.245 18.5 9.59 17h9.406v1.5H8.245z"/></svg>';
class KB extends V {
  /**
  * Returns the localized option titles provided by the plugin.
  *
  * The following localized titles corresponding with default
  * {@link module:highlight/highlightconfig~HighlightConfig#options} are available:
  *
  * * `'Yellow marker'`,
  * * `'Green marker'`,
  * * `'Pink marker'`,
  * * `'Blue marker'`,
  * * `'Red pen'`,
  * * `'Green pen'`.
  */
  get localizedOptionTitles() {
    const t = this.editor.t;
    return {
      "Yellow marker": t("Yellow marker"),
      "Green marker": t("Green marker"),
      "Pink marker": t("Pink marker"),
      "Blue marker": t("Blue marker"),
      "Red pen": t("Red pen"),
      "Green pen": t("Green pen")
    };
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HighlightUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.config.get("highlight.options");
    for (const e of t)
      this._addHighlighterButton(e);
    this._addRemoveHighlightButton(), this._addDropdown(t), this._addMenuBarButton(t);
  }
  /**
  * Creates the "Remove highlight" button.
  */
  _addRemoveHighlightButton() {
    const t = this.editor.t, e = this.editor.commands.get("highlight");
    this._addButton("removeHighlight", t("Remove highlight"), ee.eraser, null, (n) => {
      n.bind("isEnabled").to(e, "isEnabled");
    });
  }
  /**
  * Creates a toolbar button from the provided highlight option.
  */
  _addHighlighterButton(t) {
    const e = this.editor.commands.get("highlight");
    this._addButton("highlight:" + t.model, t.title, Kc(t.type), t.model, n);
    function n(r) {
      r.bind("isEnabled").to(e, "isEnabled"), r.bind("isOn").to(e, "value", (s) => s === t.model), r.iconView.fillColor = t.color, r.isToggleable = !0;
    }
  }
  /**
  * Internal method for creating highlight buttons.
  *
  * @param name The name of the button.
  * @param label The label for the button.
  * @param icon The button icon.
  * @param value The `value` property passed to the executed command.
  * @param decorateButton A callback getting ButtonView instance so that it can be further customized.
  */
  _addButton(t, e, n, r, s) {
    const o = this.editor;
    o.ui.componentFactory.add(t, (a) => {
      const l = new Te(a), c = this.localizedOptionTitles[e] ? this.localizedOptionTitles[e] : e;
      return l.set({
        label: c,
        icon: n,
        tooltip: !0
      }), l.on("execute", () => {
        o.execute("highlight", {
          value: r
        }), o.editing.view.focus();
      }), s(l), l;
    });
  }
  /**
  * Creates the split button dropdown UI from the provided highlight options.
  */
  _addDropdown(t) {
    const e = this.editor, n = e.t, r = e.ui.componentFactory, s = t[0], o = t.reduce((a, l) => (a[l.model] = l, a), {});
    r.add("highlight", (a) => {
      const l = e.commands.get("highlight"), c = Yn(a, io), u = c.buttonView;
      u.set({
        label: n("Highlight"),
        tooltip: !0,
        // Holds last executed highlighter.
        lastExecuted: s.model,
        // Holds current highlighter to execute (might be different then last used).
        commandValue: s.model,
        isToggleable: !0
      }), u.bind("icon").to(l, "value", (m) => Kc(f(m, "type"))), u.bind("color").to(l, "value", (m) => f(m, "color")), u.bind("commandValue").to(l, "value", (m) => f(m, "model")), u.bind("isOn").to(l, "value", (m) => !!m), u.delegate("execute").to(c);
      const d = () => {
        const m = t.map((g) => {
          const p = r.create("highlight:" + g.model);
          return this.listenTo(p, "execute", () => {
            c.buttonView.set({
              lastExecuted: g.model
            });
          }), p;
        });
        return m.push(new bp()), m.push(r.create("removeHighlight")), m;
      };
      c.bind("isEnabled").to(l, "isEnabled"), Zd(c, d, {
        enableActiveItemFocusOnDropdownOpen: !0,
        ariaLabel: n("Text highlight toolbar")
      }), ZB(c), u.on("execute", () => {
        e.execute("highlight", {
          value: u.commandValue
        });
      }), this.listenTo(c, "execute", () => {
        e.editing.view.focus();
      });
      function f(m, g) {
        const p = !m || m === u.lastExecuted ? u.lastExecuted : m;
        return o[p][g];
      }
      return c;
    });
  }
  /**
  * Creates the menu bar button for highlight including submenu with available options.
  */
  _addMenuBarButton(t) {
    const e = this.editor, n = e.t, r = e.commands.get("highlight");
    e.ui.componentFactory.add("menuBar:highlight", (s) => {
      const o = new Yi(s);
      o.buttonView.set({
        label: n("Highlight"),
        icon: Kc("marker")
      }), o.bind("isEnabled").to(r), o.buttonView.iconView.fillColor = "transparent";
      const a = new ra(s);
      for (const u of t) {
        const d = new Ks(s, o), f = new At(s);
        f.set({
          label: u.title,
          icon: Kc(u.type),
          role: "menuitemradio",
          isToggleable: !0
        }), f.iconView.fillColor = u.color, f.delegate("execute").to(o), f.bind("isOn").to(r, "value", (m) => m === u.model), f.on("execute", () => {
          e.execute("highlight", {
            value: u.model
          }), e.editing.view.focus();
        }), d.children.add(f), a.items.add(d);
      }
      a.items.add(new Gd(s));
      const l = new Ks(s, o), c = new At(s);
      return c.set({
        label: n("Remove highlight"),
        icon: ee.eraser
      }), c.delegate("execute").to(o), c.on("execute", () => {
        e.execute("highlight", {
          value: null
        }), e.editing.view.focus();
      }), l.children.add(c), a.items.add(l), o.panelView.children.add(a), o;
    });
  }
}
function ZB(i) {
  i.buttonView.actionView.iconView.bind("fillColor").to(i.buttonView, "color");
}
function Kc(i) {
  return i === "marker" ? qB : GB;
}
class YB extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      WB,
      KB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Highlight";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class JB extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.schema, n = t.document.selection, r = xw(n);
    this.isEnabled = XB(n, e, t), this.value = r ? r.getAttribute("value") || "" : null;
  }
  /**
  * Executes the command, which either:
  *
  * * creates and inserts a new HTML embed element if none was selected,
  * * updates the content of the HTML embed if one was selected.
  *
  * @fires execute
  * @param value When passed, the value (content) will be set on a new embed or a selected one.
  */
  execute(t) {
    const e = this.editor.model, n = e.document.selection;
    e.change((r) => {
      let s;
      this.value !== null ? s = xw(n) : (s = r.createElement("rawHtml"), e.insertObject(s, null, null, {
        setSelection: "on"
      })), r.setAttribute("value", t, s);
    });
  }
}
function XB(i, t, e) {
  const n = QB(i, e);
  return t.checkChild(n, "rawHtml");
}
function QB(i, t) {
  const n = yp(i, t).start.parent;
  return n.isEmpty && !n.is("rootElement") ? n.parent : n;
}
function xw(i) {
  const t = i.getSelectedElement();
  return t && t.is("element", "rawHtml") ? t : null;
}
class eF extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Keeps references to {@link module:ui/button/buttonview~ButtonView edit, save, and cancel} button instances created for
    * each widget so they can be destroyed if they are no longer in DOM after the editing view was re-rendered.
    */
    h(this, "_widgetButtonViewReferences", /* @__PURE__ */ new Set());
    e.config.define("htmlEmbed", {
      showPreviews: !1,
      sanitizeHtml: (n) => (Xe("html-embed-provide-sanitize-function"), {
        html: n,
        hasChanged: !1
      })
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HtmlEmbedEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.model.schema.register("rawHtml", {
      inheritAllFrom: "$blockObject",
      allowAttributes: [
        "value"
      ]
    }), e.commands.add("htmlEmbed", new JB(e)), this._setupConversion();
  }
  /**
  * Prepares converters for the feature.
  */
  _setupConversion() {
    const e = this.editor, n = e.t, r = e.editing.view, s = this._widgetButtonViewReferences, o = e.config.get("htmlEmbed");
    this.editor.editing.view.on("render", () => {
      for (const d of s) {
        if (d.element && d.element.isConnected)
          return;
        d.destroy(), s.delete(d);
      }
    }, {
      priority: "lowest"
    }), e.data.registerRawContentMatcher({
      name: "div",
      classes: "raw-html-embed"
    }), e.conversion.for("upcast").elementToElement({
      view: {
        name: "div",
        classes: "raw-html-embed"
      },
      model: (d, { writer: f }) => f.createElement("rawHtml", {
        value: d.getCustomProperty("$rawContent")
      })
    }), e.conversion.for("dataDowncast").elementToElement({
      model: "rawHtml",
      view: (d, { writer: f }) => f.createRawElement("div", {
        class: "raw-html-embed"
      }, function(m) {
        m.innerHTML = d.getAttribute("value") || "";
      })
    }), e.conversion.for("editingDowncast").elementToStructure({
      model: {
        name: "rawHtml",
        attributes: [
          "value"
        ]
      },
      view: (d, { writer: f }) => {
        let m, g, p;
        const b = f.createRawElement("div", {
          class: "raw-html-embed__content-wrapper"
        }, function(x) {
          m = x, a({
            editor: e,
            domElement: x,
            state: g,
            props: p
          }), m.addEventListener("mousedown", () => {
            if (g.isEditable) {
              const S = e.model;
              S.document.selection.getSelectedElement() !== d && S.change((I) => I.setSelection(d, "on"));
            }
          }, !0);
        }), v = {
          makeEditable() {
            g = Object.assign({}, g, {
              isEditable: !0
            }), a({
              domElement: m,
              editor: e,
              state: g,
              props: p
            }), r.change((x) => {
              x.setAttribute("data-cke-ignore-events", "true", b);
            }), m.querySelector("textarea").focus();
          },
          save(x) {
            x !== g.getRawHtmlValue() ? (e.execute("htmlEmbed", x), e.editing.view.focus()) : this.cancel();
          },
          cancel() {
            g = Object.assign({}, g, {
              isEditable: !1
            }), a({
              domElement: m,
              editor: e,
              state: g,
              props: p
            }), e.editing.view.focus(), r.change((x) => {
              x.removeAttribute("data-cke-ignore-events", b);
            });
          }
        };
        g = {
          showPreviews: o.showPreviews,
          isEditable: !1,
          getRawHtmlValue: () => d.getAttribute("value") || ""
        }, p = {
          sanitizeHtml: o.sanitizeHtml,
          textareaPlaceholder: n("Paste raw HTML here..."),
          onEditClick() {
            v.makeEditable();
          },
          onSaveClick(x) {
            v.save(x);
          },
          onCancelClick() {
            v.cancel();
          }
        };
        const y = f.createContainerElement("div", {
          class: "raw-html-embed",
          "data-html-embed-label": n("HTML snippet"),
          dir: e.locale.uiLanguageDirection
        }, b);
        return f.setCustomProperty("rawHtmlApi", v, y), f.setCustomProperty("rawHtml", !0, y), va(y, f, {
          label: n("HTML snippet"),
          hasSelectionHandle: !0
        });
      }
    });
    function a({ editor: d, domElement: f, state: m, props: g }) {
      f.textContent = "";
      const p = f.ownerDocument;
      let b;
      if (m.isEditable) {
        const y = {
          isDisabled: !1,
          placeholder: g.textareaPlaceholder
        };
        b = c({
          domDocument: p,
          state: m,
          props: y
        }), f.append(b);
      } else if (m.showPreviews) {
        const y = {
          sanitizeHtml: g.sanitizeHtml
        };
        f.append(u({
          domDocument: p,
          state: m,
          props: y,
          editor: d
        }));
      } else {
        const y = {
          isDisabled: !0,
          placeholder: g.textareaPlaceholder
        };
        f.append(c({
          domDocument: p,
          state: m,
          props: y
        }));
      }
      const v = {
        onEditClick: g.onEditClick,
        onSaveClick: () => {
          g.onSaveClick(b.value);
        },
        onCancelClick: g.onCancelClick
      };
      f.prepend(l({
        editor: d,
        domDocument: p,
        state: m,
        props: v
      }));
    }
    function l({ editor: d, domDocument: f, state: m, props: g }) {
      const p = xn(f, "div", {
        class: "raw-html-embed__buttons-wrapper"
      });
      if (m.isEditable) {
        const b = gf(d, "save", g.onSaveClick), v = gf(d, "cancel", g.onCancelClick);
        p.append(b.element, v.element), s.add(b).add(v);
      } else {
        const b = gf(d, "edit", g.onEditClick);
        p.append(b.element), s.add(b);
      }
      return p;
    }
    function c({ domDocument: d, state: f, props: m }) {
      const g = xn(d, "textarea", {
        placeholder: m.placeholder,
        class: "ck ck-reset ck-input ck-input-text raw-html-embed__source"
      });
      return g.disabled = m.isDisabled, g.value = f.getRawHtmlValue(), g;
    }
    function u({ editor: d, domDocument: f, state: m, props: g }) {
      const p = g.sanitizeHtml(m.getRawHtmlValue()), b = m.getRawHtmlValue().length > 0 ? n("No preview available") : n("Empty snippet content"), v = xn(f, "div", {
        class: "ck ck-reset_all raw-html-embed__preview-placeholder"
      }, b), y = xn(f, "div", {
        class: "raw-html-embed__preview-content",
        dir: d.locale.contentLanguageDirection
      }), S = f.createRange().createContextualFragment(p.html);
      return y.appendChild(S), xn(f, "div", {
        class: "raw-html-embed__preview"
      }, [
        v,
        y
      ]);
    }
  }
}
function gf(i, t, e) {
  const { t: n } = i.locale, r = new Te(i.locale), s = i.commands.get("htmlEmbed");
  return r.set({
    class: `raw-html-embed__${t}-button`,
    icon: ee.pencil,
    tooltip: !0,
    tooltipPosition: i.locale.uiLanguageDirection === "rtl" ? "e" : "w"
  }), r.render(), t === "edit" ? (r.set({
    icon: ee.pencil,
    label: n("Edit source")
  }), r.bind("isEnabled").to(s)) : t === "save" ? (r.set({
    icon: ee.check,
    label: n("Save changes")
  }), r.bind("isEnabled").to(s)) : r.set({
    icon: ee.cancel,
    label: n("Cancel")
  }), r.on("execute", e), r;
}
class tF extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HtmlEmbedUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, n = t.locale.t;
    t.ui.componentFactory.add("htmlEmbed", () => {
      const r = this._createButton(Te);
      return r.set({
        tooltip: !0,
        label: n("Insert HTML")
      }), r;
    }), t.ui.componentFactory.add("menuBar:htmlEmbed", () => {
      const r = this._createButton(At);
      return r.set({
        label: n("HTML snippet")
      }), r;
    });
  }
  /**
  * Creates a button for html embed command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, n = e.commands.get("htmlEmbed"), r = new t(e.locale);
    return r.set({
      icon: ee.html
    }), r.bind("isEnabled").to(n, "isEnabled"), this.listenTo(r, "execute", () => {
      e.execute("htmlEmbed"), e.editing.view.focus(), e.editing.view.document.selection.getSelectedElement().getCustomProperty("rawHtmlApi").makeEditable();
    }), r;
  }
}
class nF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      eF,
      tF,
      oo
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HtmlEmbed";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Zs(i, t, e, n) {
  t && iF(i, t, n), e && dr(i, e, n);
}
function dr(i, t, e) {
  if (t.attributes)
    for (const [n, r] of Object.entries(t.attributes))
      i.setAttribute(n, r, e);
  t.styles && i.setStyle(t.styles, e), t.classes && i.addClass(t.classes, e);
}
function iF(i, t, e) {
  if (t.attributes)
    for (const [n] of Object.entries(t.attributes))
      i.removeAttribute(n, e);
  if (t.styles)
    for (const n of Object.keys(t.styles))
      i.removeStyle(n, e);
  t.classes && i.removeClass(t.classes, e);
}
function rF(i, t) {
  const e = Fi(i);
  let n = "attributes";
  for (n in t)
    n == "classes" ? e[n] = Array.from(/* @__PURE__ */ new Set([
      ...i[n] || [],
      ...t[n]
    ])) : e[n] = {
      ...i[n],
      ...t[n]
    };
  return e;
}
function ko(i, t, e, n, r) {
  const s = t.getAttribute(e), o = {};
  for (const a of [
    "attributes",
    "styles",
    "classes"
  ]) {
    if (a != n) {
      s && s[a] && (o[a] = s[a]);
      continue;
    }
    if (n == "classes") {
      const c = new Set(s && s.classes || []);
      r(c), c.size && (o[a] = Array.from(c));
      continue;
    }
    const l = new Map(Object.entries(s && s[a] || {}));
    r(l), l.size && (o[a] = Object.fromEntries(l));
  }
  Object.keys(o).length ? t.is("documentSelection") ? i.setSelectionAttribute(e, o) : i.setAttribute(e, o, t) : s && (t.is("documentSelection") ? i.removeSelectionAttribute(e) : i.removeAttribute(e, t));
}
function sF(i) {
  return B4(i).replace(/ /g, "");
}
function Vi(i) {
  return `html${sF(i)}Attributes`;
}
function Ep({ model: i }) {
  return (t, e) => e.writer.createElement(i, {
    htmlContent: t.getCustomProperty("$rawContent")
  });
}
function oF(i, { view: t, isInline: e }) {
  const n = i.t;
  return (r, { writer: s }) => {
    const o = n("HTML object"), a = Qd(t, r, s), l = r.getAttribute(Vi(t));
    s.addClass("html-object-embed__content", a), l && dr(s, l, a);
    const c = s.createContainerElement(e ? "span" : "div", {
      class: "html-object-embed",
      "data-html-object-embed-label": o
    }, a);
    return va(c, s, {
      label: o
    });
  };
}
function Qd(i, t, e) {
  return e.createRawElement(i, null, (n, r) => {
    r.setContentOf(n, t.getAttribute("htmlContent"));
  });
}
function aF({ view: i, model: t, allowEmpty: e }, n) {
  return (s) => {
    s.on(`element:${i}`, (o, a, l) => {
      let c = n.processViewAttributes(a.viewItem, l);
      if (!(!c && !l.consumable.test(a.viewItem, {
        name: !0
      }))) {
        if (c = c || {}, l.consumable.consume(a.viewItem, {
          name: !0
        }), a.modelRange || (a = Object.assign(a, l.convertChildren(a.viewItem, a.modelCursor))), e && a.modelRange.isCollapsed && Object.keys(c).length) {
          const u = l.writer.createElement("htmlEmptyElement");
          if (!l.safeInsert(u, a.modelCursor))
            return;
          const d = l.getSplitParts(u);
          a.modelRange = l.writer.createRange(a.modelRange.start, l.writer.createPositionAfter(d[d.length - 1])), l.updateConversionResult(u, a), r(u, c, l);
          return;
        }
        for (const u of a.modelRange.getItems())
          r(u, c, l);
      }
    }, {
      priority: "low"
    });
  };
  function r(s, o, a) {
    if (a.schema.checkAttribute(s, t)) {
      const l = s.getAttribute(t), c = rF(o, l || {});
      a.writer.setAttribute(t, c, s);
    }
  }
}
function Ew({ model: i, view: t }, e) {
  return (n, { writer: r, consumable: s }) => {
    if (!n.hasAttribute(i))
      return null;
    const o = r.createContainerElement(t), a = n.getAttribute(i);
    return s.consume(n, `attribute:${i}`), dr(r, a, o), o.getFillerOffset = () => null, e ? va(o, r) : o;
  };
}
function lF({ priority: i, view: t }) {
  return (e, n) => {
    if (!e)
      return;
    const { writer: r } = n, s = r.createAttributeElement(t, null, {
      priority: i
    });
    return dr(r, e, s), s;
  };
}
function Hl({ view: i }, t) {
  return (e) => {
    e.on(`element:${i}`, (n, r, s) => {
      if (!r.modelRange || r.modelRange.isCollapsed)
        return;
      const o = t.processViewAttributes(r.viewItem, s);
      o && s.writer.setAttribute(Vi(r.viewItem.name), o, r.modelRange);
    }, {
      priority: "low"
    });
  };
}
function jl({ view: i, model: t }) {
  return (e) => {
    e.on(`attribute:${Vi(i)}:${t}`, (n, r, s) => {
      if (!s.consumable.consume(r.item, n.name))
        return;
      const { attributeOldValue: o, attributeNewValue: a } = r, l = s.writer, c = s.mapper.toViewElement(r.item);
      Zs(l, o, a, c);
    });
  };
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
var Tw = {
  block: [
    // Existing features.
    {
      model: "codeBlock",
      view: "pre"
    },
    {
      model: "paragraph",
      view: "p"
    },
    {
      model: "blockQuote",
      view: "blockquote"
    },
    {
      model: "listItem",
      view: "li"
    },
    {
      model: "pageBreak",
      view: "div"
    },
    {
      model: "rawHtml",
      view: "div"
    },
    {
      model: "table",
      view: "table"
    },
    {
      model: "tableRow",
      view: "tr"
    },
    {
      model: "tableCell",
      view: "td"
    },
    {
      model: "tableCell",
      view: "th"
    },
    {
      model: "tableColumnGroup",
      view: "colgroup"
    },
    {
      model: "tableColumn",
      view: "col"
    },
    {
      model: "caption",
      view: "caption"
    },
    {
      model: "caption",
      view: "figcaption"
    },
    {
      model: "imageBlock",
      view: "img"
    },
    {
      model: "imageInline",
      view: "img"
    },
    // Compatibility features.
    {
      model: "htmlP",
      view: "p",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlBlockquote",
      view: "blockquote",
      modelSchema: {
        inheritAllFrom: "$container"
      }
    },
    {
      model: "htmlTable",
      view: "table",
      modelSchema: {
        allowWhere: "$block",
        isBlock: !0
      }
    },
    {
      model: "htmlTbody",
      view: "tbody",
      modelSchema: {
        allowIn: "htmlTable",
        isBlock: !1
      }
    },
    {
      model: "htmlThead",
      view: "thead",
      modelSchema: {
        allowIn: "htmlTable",
        isBlock: !1
      }
    },
    {
      model: "htmlTfoot",
      view: "tfoot",
      modelSchema: {
        allowIn: "htmlTable",
        isBlock: !1
      }
    },
    {
      model: "htmlCaption",
      view: "caption",
      modelSchema: {
        allowIn: "htmlTable",
        allowChildren: "$text",
        isBlock: !1
      }
    },
    {
      model: "htmlColgroup",
      view: "colgroup",
      modelSchema: {
        allowIn: "htmlTable",
        allowChildren: "col",
        isBlock: !1
      }
    },
    {
      model: "htmlCol",
      view: "col",
      modelSchema: {
        allowIn: "htmlColgroup",
        isBlock: !1
      }
    },
    {
      model: "htmlTr",
      view: "tr",
      modelSchema: {
        allowIn: [
          "htmlTable",
          "htmlThead",
          "htmlTbody"
        ],
        isLimit: !0
      }
    },
    // TODO can also include text.
    {
      model: "htmlTd",
      view: "td",
      modelSchema: {
        allowIn: "htmlTr",
        allowContentOf: "$container",
        isLimit: !0,
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlTh",
      view: "th",
      modelSchema: {
        allowIn: "htmlTr",
        allowContentOf: "$container",
        isLimit: !0,
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlFigure",
      view: "figure",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include other block elements.
    {
      model: "htmlFigcaption",
      view: "figcaption",
      modelSchema: {
        allowIn: "htmlFigure",
        allowChildren: "$text",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlAddress",
      view: "address",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlAside",
      view: "aside",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlMain",
      view: "main",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlDetails",
      view: "details",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    {
      model: "htmlSummary",
      view: "summary",
      modelSchema: {
        allowChildren: [
          "htmlH1",
          "htmlH2",
          "htmlH3",
          "htmlH4",
          "htmlH5",
          "htmlH6",
          "$text"
        ],
        allowIn: "htmlDetails",
        isBlock: !1
      }
    },
    {
      model: "htmlDiv",
      view: "div",
      paragraphLikeModel: "htmlDivParagraph",
      modelSchema: {
        inheritAllFrom: "$container"
      }
    },
    // TODO can also include text.
    {
      model: "htmlFieldset",
      view: "fieldset",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include h1-h6.
    {
      model: "htmlLegend",
      view: "legend",
      modelSchema: {
        allowIn: "htmlFieldset",
        allowChildren: "$text"
      }
    },
    // TODO can also include text.
    {
      model: "htmlHeader",
      view: "header",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlFooter",
      view: "footer",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlForm",
      view: "form",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !0
      }
    },
    {
      model: "htmlHgroup",
      view: "hgroup",
      modelSchema: {
        allowIn: [
          "$root",
          "$container"
        ],
        allowChildren: [
          "paragraph",
          "htmlP",
          "htmlH1",
          "htmlH2",
          "htmlH3",
          "htmlH4",
          "htmlH5",
          "htmlH6"
        ],
        isBlock: !1
      }
    },
    {
      model: "htmlH1",
      view: "h1",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH2",
      view: "h2",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH3",
      view: "h3",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH4",
      view: "h4",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH5",
      view: "h5",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH6",
      view: "h6",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "$htmlList",
      modelSchema: {
        allowWhere: "$container",
        allowChildren: [
          "$htmlList",
          "htmlLi"
        ],
        isBlock: !1
      }
    },
    {
      model: "htmlDir",
      view: "dir",
      modelSchema: {
        inheritAllFrom: "$htmlList"
      }
    },
    {
      model: "htmlMenu",
      view: "menu",
      modelSchema: {
        inheritAllFrom: "$htmlList"
      }
    },
    {
      model: "htmlUl",
      view: "ul",
      modelSchema: {
        inheritAllFrom: "$htmlList"
      }
    },
    {
      model: "htmlOl",
      view: "ol",
      modelSchema: {
        inheritAllFrom: "$htmlList"
      }
    },
    // TODO can also include other block elements.
    {
      model: "htmlLi",
      view: "li",
      modelSchema: {
        allowIn: "$htmlList",
        allowChildren: "$text",
        isBlock: !1
      }
    },
    {
      model: "htmlPre",
      view: "pre",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlArticle",
      view: "article",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    {
      model: "htmlSection",
      view: "section",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlNav",
      view: "nav",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    {
      model: "htmlDivDl",
      view: "div",
      modelSchema: {
        allowChildren: [
          "htmlDt",
          "htmlDd"
        ],
        allowIn: "htmlDl"
      }
    },
    {
      model: "htmlDl",
      view: "dl",
      modelSchema: {
        allowWhere: "$container",
        allowChildren: [
          "htmlDt",
          "htmlDd",
          "htmlDivDl"
        ],
        isBlock: !1
      }
    },
    {
      model: "htmlDt",
      view: "dt",
      modelSchema: {
        allowChildren: "$block",
        isBlock: !1
      }
    },
    {
      model: "htmlDd",
      view: "dd",
      modelSchema: {
        allowChildren: "$block",
        isBlock: !1
      }
    },
    {
      model: "htmlCenter",
      view: "center",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    }
  ],
  inline: [
    // Existing features (attribute set on an existing model element).
    {
      model: "htmlLiAttributes",
      view: "li",
      appliesToBlock: !0,
      coupledAttribute: "listItemId"
    },
    {
      model: "htmlOlAttributes",
      view: "ol",
      appliesToBlock: !0,
      coupledAttribute: "listItemId"
    },
    {
      model: "htmlUlAttributes",
      view: "ul",
      appliesToBlock: !0,
      coupledAttribute: "listItemId"
    },
    {
      model: "htmlFigureAttributes",
      view: "figure",
      appliesToBlock: "table"
    },
    {
      model: "htmlTheadAttributes",
      view: "thead",
      appliesToBlock: "table"
    },
    {
      model: "htmlTbodyAttributes",
      view: "tbody",
      appliesToBlock: "table"
    },
    {
      model: "htmlFigureAttributes",
      view: "figure",
      appliesToBlock: "imageBlock"
    },
    // Compatibility features.
    {
      model: "htmlAcronym",
      view: "acronym",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlTt",
      view: "tt",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlFont",
      view: "font",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlTime",
      view: "time",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlVar",
      view: "var",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlBig",
      view: "big",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSmall",
      view: "small",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSamp",
      view: "samp",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlQ",
      view: "q",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlOutput",
      view: "output",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlKbd",
      view: "kbd",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlBdi",
      view: "bdi",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlBdo",
      view: "bdo",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlAbbr",
      view: "abbr",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlA",
      view: "a",
      priority: 5,
      coupledAttribute: "linkHref"
    },
    {
      model: "htmlStrong",
      view: "strong",
      coupledAttribute: "bold",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlB",
      view: "b",
      coupledAttribute: "bold",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlI",
      view: "i",
      coupledAttribute: "italic",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlEm",
      view: "em",
      coupledAttribute: "italic",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlS",
      view: "s",
      coupledAttribute: "strikethrough",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    // TODO According to HTML-spec can behave as div-like element, although CKE4 only handles it as an inline element.
    {
      model: "htmlDel",
      view: "del",
      coupledAttribute: "strikethrough",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    // TODO According to HTML-spec can behave as div-like element, although CKE4 only handles it as an inline element.
    {
      model: "htmlIns",
      view: "ins",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlU",
      view: "u",
      coupledAttribute: "underline",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSub",
      view: "sub",
      coupledAttribute: "subscript",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSup",
      view: "sup",
      coupledAttribute: "superscript",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlCode",
      view: "code",
      coupledAttribute: "code",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlMark",
      view: "mark",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSpan",
      view: "span",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlCite",
      view: "cite",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlLabel",
      view: "label",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlDfn",
      view: "dfn",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    // Objects.
    {
      model: "htmlObject",
      view: "object",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlIframe",
      view: "iframe",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlInput",
      view: "input",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlButton",
      view: "button",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlTextarea",
      view: "textarea",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlSelect",
      view: "select",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlVideo",
      view: "video",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlEmbed",
      view: "embed",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlOembed",
      view: "oembed",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlAudio",
      view: "audio",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlImg",
      view: "img",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlCanvas",
      view: "canvas",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    // TODO it could be probably represented as non-object element, although it has graphical representation,
    // so probably makes more sense to keep it as an object.
    {
      model: "htmlMeter",
      view: "meter",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    // TODO it could be probably represented as non-object element, although it has graphical representation,
    // so probably makes more sense to keep it as an object.
    {
      model: "htmlProgress",
      view: "progress",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlScript",
      view: "script",
      modelSchema: {
        allowWhere: [
          "$text",
          "$block"
        ],
        isInline: !0
      }
    },
    {
      model: "htmlStyle",
      view: "style",
      modelSchema: {
        allowWhere: [
          "$text",
          "$block"
        ],
        isInline: !0
      }
    },
    {
      model: "htmlCustomElement",
      view: "$customElement",
      modelSchema: {
        allowWhere: [
          "$text",
          "$block"
        ],
        allowAttributesOf: "$inlineObject",
        isInline: !0
      }
    }
  ]
};
class sa extends V {
  constructor() {
    super(...arguments);
    /**
    * A map of registered data schema definitions.
    */
    h(this, "_definitions", []);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DataSchema";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    for (const e of Tw.block)
      this.registerBlockElement(e);
    for (const e of Tw.inline)
      this.registerInlineElement(e);
  }
  /**
  * Add new data schema definition describing block element.
  */
  registerBlockElement(e) {
    this._definitions.push({
      ...e,
      isBlock: !0
    });
  }
  /**
  * Add new data schema definition describing inline element.
  */
  registerInlineElement(e) {
    this._definitions.push({
      ...e,
      isInline: !0
    });
  }
  /**
  * Updates schema definition describing block element with new properties.
  *
  * Creates new scheme if it doesn't exist.
  * Array properties are concatenated with original values.
  *
  * @param definition Definition update.
  */
  extendBlockElement(e) {
    this._extendDefinition({
      ...e,
      isBlock: !0
    });
  }
  /**
  * Updates schema definition describing inline element with new properties.
  *
  * Creates new scheme if it doesn't exist.
  * Array properties are concatenated with original values.
  *
  * @param definition Definition update.
  */
  extendInlineElement(e) {
    this._extendDefinition({
      ...e,
      isInline: !0
    });
  }
  /**
  * Returns all definitions matching the given view name.
  *
  * @param includeReferences Indicates if this method should also include definitions of referenced models.
  */
  getDefinitionsForView(e, n = !1) {
    const r = /* @__PURE__ */ new Set();
    for (const s of this._getMatchingViewDefinitions(e)) {
      if (n)
        for (const o of this._getReferences(s.model))
          r.add(o);
      r.add(s);
    }
    return r;
  }
  /**
  * Returns definitions matching the given model name.
  */
  getDefinitionsForModel(e) {
    return this._definitions.filter((n) => n.model == e);
  }
  /**
  * Returns definitions matching the given view name.
  */
  _getMatchingViewDefinitions(e) {
    return this._definitions.filter((n) => n.view && cF(e, n.view));
  }
  /**
  * Resolves all definition references registered for the given data schema definition.
  *
  * @param modelName Data schema model name.
  */
  *_getReferences(e) {
    const n = [
      "inheritAllFrom",
      "inheritTypesFrom",
      "allowWhere",
      "allowContentOf",
      "allowAttributesOf"
    ], r = this._definitions.filter((s) => s.model == e);
    for (const { modelSchema: s } of r)
      if (s)
        for (const o of n)
          for (const a of tt(s[o] || [])) {
            const l = this._definitions.filter((c) => c.model == a);
            for (const c of l)
              a !== e && (yield* this._getReferences(c.model), yield c);
          }
  }
  /**
  * Updates schema definition with new properties.
  *
  * Creates new scheme if it doesn't exist.
  * Array properties are concatenated with original values.
  *
  * @param definition Definition update.
  */
  _extendDefinition(e) {
    const n = Array.from(this._definitions.entries()).filter(([, r]) => r.model == e.model);
    if (n.length == 0) {
      this._definitions.push(e);
      return;
    }
    for (const [r, s] of n)
      this._definitions[r] = T4({}, s, e, (o, a) => Array.isArray(o) ? o.concat(a) : void 0);
  }
}
function cF(i, t) {
  return typeof i == "string" ? i === t : i instanceof RegExp ? i.test(t) : !1;
}
class nn extends V {
  constructor(e) {
    super(e);
    /**
    * An instance of the {@link module:html-support/dataschema~DataSchema}.
    */
    h(this, "_dataSchema");
    /**
    * {@link module:engine/view/matcher~Matcher Matcher} instance describing rules upon which
    * content attributes should be allowed.
    */
    h(this, "_allowedAttributes");
    /**
    * {@link module:engine/view/matcher~Matcher Matcher} instance describing rules upon which
    * content attributes should be disallowed.
    */
    h(this, "_disallowedAttributes");
    /**
     * Allowed element definitions by {@link module:html-support/datafilter~DataFilter#allowElement} method.
    */
    h(this, "_allowedElements");
    /**
    * Disallowed element names by {@link module:html-support/datafilter~DataFilter#disallowElement} method.
    */
    h(this, "_disallowedElements");
    /**
     * Indicates if {@link module:engine/controller/datacontroller~DataController editor's data controller}
     * data has been already initialized.
    */
    h(this, "_dataInitialized");
    /**
    * Cached map of coupled attributes. Keys are the feature attributes names
    * and values are arrays with coupled GHS attributes names.
    */
    h(this, "_coupledAttributes");
    this._dataSchema = e.plugins.get("DataSchema"), this._allowedAttributes = new xi(), this._disallowedAttributes = new xi(), this._allowedElements = /* @__PURE__ */ new Set(), this._disallowedElements = /* @__PURE__ */ new Set(), this._dataInitialized = !1, this._coupledAttributes = null, this._registerElementsAfterInit(), this._registerElementHandlers(), this._registerCoupledAttributesPostFixer(), this._registerAssociatedHtmlAttributesPostFixer();
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DataFilter";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      sa,
      oo
    ];
  }
  /**
  * Load a configuration of one or many elements, where their attributes should be allowed.
  *
  * **Note**: Rules will be applied just before next data pipeline data init or set.
  *
  * @param config Configuration of elements that should have their attributes accepted in the editor.
  */
  loadAllowedConfig(e) {
    for (const n of e) {
      const r = n.name || /[\s\S]+/, s = Aw(n);
      this.allowElement(r), s.forEach((o) => this.allowAttributes(o));
    }
  }
  /**
  * Load a configuration of one or many elements, where their attributes should be disallowed.
  *
  * **Note**: Rules will be applied just before next data pipeline data init or set.
  *
  * @param config Configuration of elements that should have their attributes rejected from the editor.
  */
  loadDisallowedConfig(e) {
    for (const n of e) {
      const r = n.name || /[\s\S]+/, s = Aw(n);
      s.length == 0 ? this.disallowElement(r) : s.forEach((o) => this.disallowAttributes(o));
    }
  }
  /**
  * Load a configuration of one or many elements, where when empty should be allowed.
  *
  * **Note**: It modifies DataSchema so must be loaded before registering filtering rules.
  *
  * @param config Configuration of elements that should be preserved even if empty.
  */
  loadAllowedEmptyElementsConfig(e) {
    for (const n of e)
      this.allowEmptyElement(n);
  }
  /**
  * Allow the given element in the editor context.
  *
  * This method will only allow elements described by the {@link module:html-support/dataschema~DataSchema} used
  * to create data filter.
  *
  * **Note**: Rules will be applied just before next data pipeline data init or set.
  *
  * @param viewName String or regular expression matching view name.
  */
  allowElement(e) {
    for (const n of this._dataSchema.getDefinitionsForView(e, !0))
      this._addAllowedElement(n), this._coupledAttributes = null;
  }
  /**
  * Disallow the given element in the editor context.
  *
  * This method will only disallow elements described by the {@link module:html-support/dataschema~DataSchema} used
  * to create data filter.
  *
  * @param viewName String or regular expression matching view name.
  */
  disallowElement(e) {
    for (const n of this._dataSchema.getDefinitionsForView(e, !1))
      this._disallowedElements.add(n.view);
  }
  /**
  * Allow the given empty element in the editor context.
  *
  * This method will only allow elements described by the {@link module:html-support/dataschema~DataSchema} used
  * to create data filter.
  *
  * **Note**: It modifies DataSchema so must be called before registering filtering rules.
  *
  * @param viewName String or regular expression matching view name.
  */
  allowEmptyElement(e) {
    for (const n of this._dataSchema.getDefinitionsForView(e, !0))
      n.isInline && this._dataSchema.extendInlineElement({
        ...n,
        allowEmpty: !0
      });
  }
  /**
  * Allow the given attributes for view element allowed by {@link #allowElement} method.
  *
  * @param config Pattern matching all attributes which should be allowed.
  */
  allowAttributes(e) {
    this._allowedAttributes.add(e);
  }
  /**
  * Disallow the given attributes for view element allowed by {@link #allowElement} method.
  *
  * @param config Pattern matching all attributes which should be disallowed.
  */
  disallowAttributes(e) {
    this._disallowedAttributes.add(e);
  }
  /**
  * Processes all allowed and disallowed attributes on the view element by consuming them and returning the allowed ones.
  *
  * This method applies the configuration set up by {@link #allowAttributes `allowAttributes()`}
  * and {@link #disallowAttributes `disallowAttributes()`} over the given view element by consuming relevant attributes.
  * It returns the allowed attributes that were found on the given view element for further processing by integration code.
  *
  * ```ts
  * dispatcher.on( 'element:myElement', ( evt, data, conversionApi ) => {
  * 	// Get rid of disallowed and extract all allowed attributes from a viewElement.
  * 	const viewAttributes = dataFilter.processViewAttributes( data.viewItem, conversionApi );
  * 	// Do something with them, i.e. store inside a model as a dictionary.
  * 	if ( viewAttributes ) {
  * 		conversionApi.writer.setAttribute( 'htmlAttributesOfMyElement', viewAttributes, data.modelRange );
  * 	}
  * } );
  * ```
  *
  * @see module:engine/conversion/viewconsumable~ViewConsumable#consume
  *
  * @returns Object with following properties:
  * - attributes Set with matched attribute names.
  * - styles Set with matched style names.
  * - classes Set with matched class names.
  */
  processViewAttributes(e, n) {
    const { consumable: r } = n;
    return Sw(e, this._disallowedAttributes, r), uF(e, Sw(e, this._allowedAttributes, r));
  }
  /**
  * Adds allowed element definition and fires registration event.
  */
  _addAllowedElement(e) {
    if (!this._allowedElements.has(e)) {
      if (this._allowedElements.add(e), "appliesToBlock" in e && typeof e.appliesToBlock == "string")
        for (const n of this._dataSchema.getDefinitionsForModel(e.appliesToBlock))
          n.isBlock && this._addAllowedElement(n);
      this._dataInitialized && this.editor.data.once("set", () => {
        this._fireRegisterEvent(e);
      }, {
        // With the highest priority listener we are able to register elements right before
        // running data conversion.
        priority: wi.highest + 1
      });
    }
  }
  /**
   * Registers elements allowed by {@link module:html-support/datafilter~DataFilter#allowElement} method
   * once {@link module:engine/controller/datacontroller~DataController editor's data controller} is initialized.
  */
  _registerElementsAfterInit() {
    this.editor.data.on("init", () => {
      this._dataInitialized = !0;
      for (const e of this._allowedElements)
        this._fireRegisterEvent(e);
    }, {
      // With highest priority listener we are able to register elements right before
      // running data conversion. Also:
      // * Make sure that priority is higher than the one used by `RealTimeCollaborationClient`,
      // as RTC is stopping event propagation.
      // * Make sure no other features hook into this event before GHS because otherwise the
      // downcast conversion (for these features) could run before GHS registered its converters
      // (https://github.com/ckeditor/ckeditor5/issues/11356).
      priority: wi.highest + 1
    });
  }
  /**
  * Registers default element handlers.
  */
  _registerElementHandlers() {
    this.on("register", (e, n) => {
      const r = this.editor.model.schema;
      if (n.isObject && !r.isRegistered(n.model))
        this._registerObjectElement(n);
      else if (n.isBlock)
        this._registerBlockElement(n);
      else if (n.isInline)
        this._registerInlineElement(n);
      else
        throw new T("data-filter-invalid-definition", null, n);
      e.stop();
    }, {
      priority: "lowest"
    });
  }
  /**
  * Registers a model post-fixer that is removing coupled GHS attributes of inline elements. Those attributes
  * are removed if a coupled feature attribute is removed.
  *
  * For example, consider following HTML:
  *
  * ```html
  * <a href="foo.html" id="myId">bar</a>
  * ```
  *
  * Which would be upcasted to following text node in the model:
  *
  * ```html
  * <$text linkHref="foo.html" htmlA="{ attributes: { id: 'myId' } }">bar</$text>
  * ```
  *
  * When the user removes the link from that text (using UI), only `linkHref` attribute would be removed:
  *
  * ```html
  * <$text htmlA="{ attributes: { id: 'myId' } }">bar</$text>
  * ```
  *
  * The `htmlA` attribute would stay in the model and would cause GHS to generate an `<a>` element.
  * This is incorrect from UX point of view, as the user wanted to remove the whole link (not only `href`).
  */
  _registerCoupledAttributesPostFixer() {
    const e = this.editor.model, n = e.document.selection;
    e.document.registerPostFixer((r) => {
      const s = e.document.differ.getChanges();
      let o = !1;
      const a = this._getCoupledAttributesMap();
      for (const l of s) {
        if (l.type != "attribute" || l.attributeNewValue !== null)
          continue;
        const c = a.get(l.attributeKey);
        if (c)
          for (const { item: u } of l.range.getWalker())
            for (const d of c)
              u.hasAttribute(d) && (r.removeAttribute(d, u), o = !0);
      }
      return o;
    }), this.listenTo(n, "change:attribute", (r, { attributeKeys: s }) => {
      const o = /* @__PURE__ */ new Set(), a = this._getCoupledAttributesMap();
      for (const l of s) {
        if (n.hasAttribute(l))
          continue;
        const c = a.get(l);
        if (c)
          for (const u of c)
            n.hasAttribute(u) && o.add(u);
      }
      o.size != 0 && e.change((l) => {
        for (const c of o)
          l.removeSelectionAttribute(c);
      });
    });
  }
  /**
  * Removes `html*Attributes` attributes from incompatible elements.
  *
  * For example, consider the following HTML:
  *
  * ```html
  * <heading2 htmlH2Attributes="...">foobar[]</heading2>
  * ```
  *
  * Pressing `enter` creates a new `paragraph` element that inherits
  * the `htmlH2Attributes` attribute from `heading2`.
  *
  * ```html
  * <heading2 htmlH2Attributes="...">foobar</heading2>
  * <paragraph htmlH2Attributes="...">[]</paragraph>
  * ```
  *
  * This postfixer ensures that this doesn't happen, and that elements can
  * only have `html*Attributes` associated with them,
  * e.g.: `htmlPAttributes` for `<p>`, `htmlDivAttributes` for `<div>`, etc.
  *
  * With it enabled, pressing `enter` at the end of `<heading2>` will create
  * a new paragraph without the `htmlH2Attributes` attribute.
  *
  * ```html
  * <heading2 htmlH2Attributes="...">foobar</heading2>
  * <paragraph>[]</paragraph>
  * ```
  */
  _registerAssociatedHtmlAttributesPostFixer() {
    const e = this.editor.model;
    e.document.registerPostFixer((n) => {
      const r = e.document.differ.getChanges();
      let s = !1;
      for (const o of r)
        if (!(o.type !== "insert" || o.name === "$text"))
          for (const a of o.attributes.keys())
            !a.startsWith("html") || !a.endsWith("Attributes") || e.schema.checkAttribute(o.name, a) || (n.removeAttribute(a, o.position.nodeAfter), s = !0);
      return s;
    });
  }
  /**
  * Collects the map of coupled attributes. The returned map is keyed by the feature attribute name
  * and coupled GHS attribute names are stored in the value array.
  */
  _getCoupledAttributesMap() {
    if (this._coupledAttributes)
      return this._coupledAttributes;
    this._coupledAttributes = /* @__PURE__ */ new Map();
    for (const e of this._allowedElements)
      if (e.coupledAttribute && e.model) {
        const n = this._coupledAttributes.get(e.coupledAttribute);
        n ? n.push(e.model) : this._coupledAttributes.set(e.coupledAttribute, [
          e.model
        ]);
      }
    return this._coupledAttributes;
  }
  /**
  * Fires `register` event for the given element definition.
  */
  _fireRegisterEvent(e) {
    e.view && this._disallowedElements.has(e.view) || this.fire(e.view ? `register:${e.view}` : "register", e);
  }
  /**
  * Registers object element and attribute converters for the given data schema definition.
  */
  _registerObjectElement(e) {
    const n = this.editor, r = n.model.schema, s = n.conversion, { view: o, model: a } = e;
    r.register(a, e.modelSchema);
    /* istanbul ignore next: paranoid check -- @preserve */
    o && (r.extend(e.model, {
      allowAttributes: [
        Vi(o),
        "htmlContent"
      ]
    }), n.data.registerRawContentMatcher({
      name: o
    }), s.for("upcast").elementToElement({
      view: o,
      model: Ep(e),
      // With a `low` priority, `paragraph` plugin auto-paragraphing mechanism is executed. Make sure
      // this listener is called before it. If not, some elements will be transformed into a paragraph.
      // `+ 2` is used to take priority over `_addDefaultH1Conversion` in the Heading plugin.
      converterPriority: wi.low + 2
    }), s.for("upcast").add(Hl(e, this)), s.for("editingDowncast").elementToStructure({
      model: {
        name: a,
        attributes: [
          Vi(o)
        ]
      },
      view: oF(n, e)
    }), s.for("dataDowncast").elementToElement({
      model: a,
      view: (l, { writer: c }) => Qd(o, l, c)
    }), s.for("dataDowncast").add(jl(e)));
  }
  /**
  * Registers block element and attribute converters for the given data schema definition.
  */
  _registerBlockElement(e) {
    const n = this.editor, r = n.model.schema, s = n.conversion, { view: o, model: a } = e;
    if (!r.isRegistered(e.model)) {
      if (r.register(e.model, e.modelSchema), !o)
        return;
      s.for("upcast").elementToElement({
        model: a,
        view: o,
        // With a `low` priority, `paragraph` plugin auto-paragraphing mechanism is executed. Make sure
        // this listener is called before it. If not, some elements will be transformed into a paragraph.
        // `+ 2` is used to take priority over `_addDefaultH1Conversion` in the Heading plugin.
        converterPriority: wi.low + 2
      }), s.for("downcast").elementToElement({
        model: a,
        view: o
      });
    }
    o && (r.extend(e.model, {
      allowAttributes: Vi(o)
    }), s.for("upcast").add(Hl(e, this)), s.for("downcast").add(jl(e)));
  }
  /**
  * Registers inline element and attribute converters for the given data schema definition.
  *
  * Extends `$text` model schema to allow the given definition model attribute and its properties.
  */
  _registerInlineElement(e) {
    const n = this.editor, r = n.model.schema, s = n.conversion, o = e.model;
    e.appliesToBlock || (r.extend("$text", {
      allowAttributes: o
    }), e.attributeProperties && r.setAttributeProperties(o, e.attributeProperties), s.for("upcast").add(aF(e, this)), s.for("downcast").attributeToElement({
      model: o,
      view: lF(e)
    }), e.allowEmpty && (r.setAttributeProperties(o, {
      copyFromObject: !1
    }), r.isRegistered("htmlEmptyElement") || r.register("htmlEmptyElement", {
      inheritAllFrom: "$inlineObject"
    }), n.data.htmlProcessor.domConverter.registerInlineObjectMatcher((a) => a.name == e.view && a.isEmpty && Array.from(a.getAttributeKeys()).length ? {
      name: !0
    } : null), s.for("editingDowncast").elementToElement({
      model: "htmlEmptyElement",
      view: Ew(e, !0)
    }), s.for("dataDowncast").elementToElement({
      model: "htmlEmptyElement",
      view: Ew(e)
    })));
  }
}
function Sw(i, t, e) {
  const n = t.matchAll(i) || [], r = i.document.stylesProcessor;
  return n.reduce((s, { match: o }) => {
    for (const a of o.styles || []) {
      const l = r.getRelatedStyles(a).filter((c) => c.split("-").length > a.split("-").length).sort((c, u) => u.split("-").length - c.split("-").length);
      for (const c of l)
        e.consume(i, {
          styles: [
            c
          ]
        }) && s.styles.push(c);
      e.consume(i, {
        styles: [
          a
        ]
      }) && s.styles.push(a);
    }
    for (const a of o.classes || [])
      e.consume(i, {
        classes: [
          a
        ]
      }) && s.classes.push(a);
    for (const a of o.attributes || [])
      e.consume(i, {
        attributes: [
          a
        ]
      }) && s.attributes.push(a);
    return s;
  }, {
    attributes: [],
    classes: [],
    styles: []
  });
}
function uF(i, { attributes: t, classes: e, styles: n }) {
  return !t.length && !e.length && !n.length ? null : {
    ...t.length && {
      attributes: dF(i, t)
    },
    ...n.length && {
      styles: hF(i, n)
    },
    ...e.length && {
      classes: e
    }
  };
}
function dF(i, t) {
  const e = {};
  for (const n of t) {
    const r = i.getAttribute(n);
    r !== void 0 && E2(n) && (e[n] = r);
  }
  return e;
}
function hF(i, t) {
  const e = new Fu(i.document.stylesProcessor);
  for (const n of t) {
    const r = i.getStyle(n);
    r !== void 0 && e.set(n, r);
  }
  return Object.fromEntries(e.getStylesEntries());
}
function pf(i, t) {
  const { name: e } = i, n = i[t];
  return oi(n) ? Object.entries(n).map(([r, s]) => ({
    name: e,
    [t]: {
      [r]: s
    }
  })) : Array.isArray(n) ? n.map((r) => ({
    name: e,
    [t]: [
      r
    ]
  })) : [
    i
  ];
}
function Aw(i) {
  const { name: t, attributes: e, classes: n, styles: r } = i, s = [];
  return e && s.push(...pf({
    name: t,
    attributes: e
  }, "attributes")), n && s.push(...pf({
    name: t,
    classes: n
  }, "classes")), r && s.push(...pf({
    name: t,
    styles: r
  }, "styles")), s;
}
class fF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeBlockElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    if (!this.editor.plugins.has("CodeBlockEditing"))
      return;
    const t = this.editor.plugins.get(nn);
    t.on("register:pre", (e, n) => {
      if (n.model !== "codeBlock")
        return;
      const r = this.editor, s = r.model.schema, o = r.conversion;
      s.extend("codeBlock", {
        allowAttributes: [
          "htmlPreAttributes",
          "htmlContentAttributes"
        ]
      }), o.for("upcast").add(mF(t)), o.for("downcast").add(gF()), e.stop();
    });
  }
}
function mF(i) {
  return (t) => {
    t.on("element:code", (e, n, r) => {
      const s = n.viewItem, o = s.parent;
      if (!o || !o.is("element", "pre"))
        return;
      a(o, "htmlPreAttributes"), a(s, "htmlContentAttributes");
      function a(l, c) {
        const u = i.processViewAttributes(l, r);
        u && r.writer.setAttribute(c, u, n.modelRange);
      }
    }, {
      priority: "low"
    });
  };
}
function gF() {
  return (i) => {
    i.on("attribute:htmlPreAttributes:codeBlock", (t, e, n) => {
      if (!n.consumable.consume(e.item, t.name))
        return;
      const { attributeOldValue: r, attributeNewValue: s } = e, a = n.mapper.toViewElement(e.item).parent;
      Zs(n.writer, r, s, a);
    }), i.on("attribute:htmlContentAttributes:codeBlock", (t, e, n) => {
      if (!n.consumable.consume(e.item, t.name))
        return;
      const { attributeOldValue: r, attributeNewValue: s } = e, o = n.mapper.toViewElement(e.item);
      Zs(n.writer, r, s, o);
    });
  };
}
class pF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DualContentModelElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.plugins.get(nn).on("register", (e, n) => {
      const r = n, s = this.editor, o = s.model.schema, a = s.conversion;
      if (!r.paragraphLikeModel || o.isRegistered(r.model) || o.isRegistered(r.paragraphLikeModel))
        return;
      const l = {
        model: r.paragraphLikeModel,
        view: r.view
      };
      o.register(r.model, r.modelSchema), o.register(l.model, {
        inheritAllFrom: "$block"
      }), a.for("upcast").elementToElement({
        view: r.view,
        model: (c, { writer: u }) => this._hasBlockContent(c) ? u.createElement(r.model) : u.createElement(l.model),
        // With a `low` priority, `paragraph` plugin auto-paragraphing mechanism is executed. Make sure
        // this listener is called before it. If not, some elements will be transformed into a paragraph.
        converterPriority: wi.low + 0.5
      }), a.for("downcast").elementToElement({
        view: r.view,
        model: r.model
      }), this._addAttributeConversion(r), a.for("downcast").elementToElement({
        view: l.view,
        model: l.model
      }), this._addAttributeConversion(l), e.stop();
    });
  }
  /**
  * Checks whether the given view element includes any other block element.
  */
  _hasBlockContent(t) {
    const e = this.editor.editing.view, n = e.domConverter.blockElements;
    for (const r of e.createRangeIn(t).getItems())
      if (r.is("element") && n.includes(r.name))
        return !0;
    return !1;
  }
  /**
  * Adds attribute filtering conversion for the given data schema.
  */
  _addAttributeConversion(t) {
    const e = this.editor, n = e.conversion, r = e.plugins.get(nn);
    e.model.schema.extend(t.model, {
      allowAttributes: Vi(t.view)
    }), n.for("upcast").add(Hl(t, r)), n.for("downcast").add(jl(t));
  }
}
class bF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      sa,
      Jd
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HeadingElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("HeadingEditing"))
      return;
    const e = t.config.get("heading.options");
    this.registerHeadingElements(t, e);
  }
  /**
  * Registers all elements supported by HeadingEditing to enable custom attributes for those elements.
  */
  registerHeadingElements(t, e) {
    const n = t.plugins.get(sa), r = [];
    for (const s of e)
      "model" in s && "view" in s && (n.registerBlockElement({
        view: s.view,
        model: s.model
      }), r.push(s.model));
    n.extendBlockElement({
      model: "htmlHgroup",
      modelSchema: {
        allowChildren: r
      }
    }), n.extendBlockElement({
      model: "htmlSummary",
      modelSchema: {
        allowChildren: r
      }
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Qu(i, t, e) {
  const n = i.createRangeOn(t);
  for (const { item: r } of n.getWalker())
    if (r.is("element", e))
      return r;
}
class vF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("ImageInlineEditing") && !t.plugins.has("ImageBlockEditing"))
      return;
    const e = t.model.schema, n = t.conversion, r = t.plugins.get(nn);
    r.on("register:figure", () => {
      n.for("upcast").add(_F(r));
    }), r.on("register:img", (s, o) => {
      o.model !== "imageBlock" && o.model !== "imageInline" || (e.isRegistered("imageBlock") && e.extend("imageBlock", {
        allowAttributes: [
          "htmlImgAttributes",
          // Figure and Link don't have model counterpart.
          // We will preserve attributes on image model element using these attribute keys.
          "htmlFigureAttributes",
          "htmlLinkAttributes"
        ]
      }), e.isRegistered("imageInline") && e.extend("imageInline", {
        allowAttributes: [
          // `htmlA` is needed for standard GHS link integration.
          "htmlA",
          "htmlImgAttributes"
        ]
      }), n.for("upcast").add(wF(r)), n.for("downcast").add(CF()), t.plugins.has("LinkImage") && n.for("upcast").add(yF(r, t)), s.stop());
    });
  }
}
function wF(i) {
  return (t) => {
    t.on("element:img", (e, n, r) => {
      if (!n.modelRange)
        return;
      const s = n.viewItem, o = i.processViewAttributes(s, r);
      o && r.writer.setAttribute("htmlImgAttributes", o, n.modelRange);
    }, {
      priority: "low"
    });
  };
}
function yF(i, t) {
  const e = t.plugins.get("ImageUtils");
  return (n) => {
    n.on("element:a", (r, s, o) => {
      const a = s.viewItem;
      if (!e.findViewImgElement(a))
        return;
      const c = s.modelCursor.parent;
      if (!c.is("element", "imageBlock"))
        return;
      const u = i.processViewAttributes(a, o);
      u && o.writer.setAttribute("htmlLinkAttributes", u, c);
    }, {
      priority: "low"
    });
  };
}
function _F(i) {
  return (t) => {
    t.on("element:figure", (e, n, r) => {
      const s = n.viewItem;
      if (!n.modelRange || !s.hasClass("image"))
        return;
      const o = i.processViewAttributes(s, r);
      o && r.writer.setAttribute("htmlFigureAttributes", o, n.modelRange);
    }, {
      priority: "low"
    });
  };
}
function CF() {
  return (i) => {
    t("htmlImgAttributes"), e("img", "htmlImgAttributes"), e("figure", "htmlFigureAttributes"), e("a", "htmlLinkAttributes");
    function t(n) {
      i.on(`attribute:${n}:imageInline`, (r, s, o) => {
        if (!o.consumable.consume(s.item, r.name))
          return;
        const { attributeOldValue: a, attributeNewValue: l } = s, c = o.mapper.toViewElement(s.item);
        Zs(o.writer, a, l, c);
      }, {
        priority: "low"
      });
    }
    function e(n, r) {
      i.on(`attribute:${r}:imageBlock`, (s, o, a) => {
        if (!a.consumable.test(o.item, s.name))
          return;
        const { attributeOldValue: l, attributeNewValue: c } = o, u = a.mapper.toViewElement(o.item), d = Qu(a.writer, u, n);
        d && (Zs(a.writer, l, c, d), a.consumable.consume(o.item, s.name));
      }, {
        priority: "low"
      }), n === "a" && i.on("attribute:linkHref:imageBlock", (s, o, a) => {
        if (!a.consumable.consume(o.item, "attribute:htmlLinkAttributes:imageBlock"))
          return;
        const l = a.mapper.toViewElement(o.item), c = Qu(a.writer, l, "a");
        dr(a.writer, o.item.getAttribute("htmlLinkAttributes"), c);
      }, {
        priority: "low"
      });
    }
  };
}
class kF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MediaEmbedElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("MediaEmbed") || t.config.get("mediaEmbed.previewsInData"))
      return;
    const e = t.model.schema, n = t.conversion, r = this.editor.plugins.get(nn), s = this.editor.plugins.get(sa), o = t.config.get("mediaEmbed.elementName");
    s.registerBlockElement({
      model: "media",
      view: o
    }), r.on("register:figure", () => {
      n.for("upcast").add(EF(r));
    }), r.on(`register:${o}`, (a, l) => {
      l.model === "media" && (e.extend("media", {
        allowAttributes: [
          Vi(o),
          "htmlFigureAttributes"
        ]
      }), n.for("upcast").add(xF(r, o)), n.for("dataDowncast").add(TF(o)), a.stop());
    });
  }
}
function xF(i, t) {
  const e = (n, r, s) => {
    const o = r.viewItem;
    a(o, Vi(t));
    function a(l, c) {
      const u = i.processViewAttributes(l, s);
      u && s.writer.setAttribute(c, u, r.modelRange);
    }
  };
  return (n) => {
    n.on(`element:${t}`, e, {
      priority: "low"
    });
  };
}
function EF(i) {
  return (t) => {
    t.on("element:figure", (e, n, r) => {
      const s = n.viewItem;
      if (!n.modelRange || !s.hasClass("media"))
        return;
      const o = i.processViewAttributes(s, r);
      o && r.writer.setAttribute("htmlFigureAttributes", o, n.modelRange);
    }, {
      priority: "low"
    });
  };
}
function TF(i) {
  return (t) => {
    e(i, Vi(i)), e("figure", "htmlFigureAttributes");
    function e(n, r) {
      t.on(`attribute:${r}:media`, (s, o, a) => {
        if (!a.consumable.consume(o.item, s.name))
          return;
        const { attributeOldValue: l, attributeNewValue: c } = o, u = a.mapper.toViewElement(o.item), d = Qu(a.writer, u, n);
        Zs(a.writer, l, c, d);
      });
    }
  };
}
class SF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ScriptElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins.get(nn);
    t.on("register:script", (e, n) => {
      const r = this.editor, s = r.model.schema, o = r.conversion;
      s.register("htmlScript", n.modelSchema), s.extend("htmlScript", {
        allowAttributes: [
          "htmlScriptAttributes",
          "htmlContent"
        ],
        isContent: !0
      }), r.data.registerRawContentMatcher({
        name: "script"
      }), o.for("upcast").elementToElement({
        view: "script",
        model: Ep(n)
      }), o.for("upcast").add(Hl(n, t)), o.for("downcast").elementToElement({
        model: "htmlScript",
        view: (a, { writer: l }) => Qd("script", a, l)
      }), o.for("downcast").add(jl(n)), e.stop();
    });
  }
}
class AF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("TableEditing"))
      return;
    const e = t.model.schema, n = t.conversion, r = t.plugins.get(nn), s = t.plugins.get("TableUtils");
    r.on("register:figure", () => {
      n.for("upcast").add(RF(r));
    }), r.on("register:table", (o, a) => {
      a.model === "table" && (e.extend("table", {
        allowAttributes: [
          "htmlTableAttributes",
          // Figure, thead and tbody elements don't have model counterparts.
          // We will be preserving attributes on table element using these attribute keys.
          "htmlFigureAttributes",
          "htmlTheadAttributes",
          "htmlTbodyAttributes"
        ]
      }), n.for("upcast").add(IF(r)), n.for("downcast").add(OF()), t.model.document.registerPostFixer(PF(t.model, s)), o.stop());
    });
  }
}
function PF(i, t) {
  return (e) => {
    const n = i.document.differ.getChanges();
    let r = !1;
    for (const s of n) {
      if (s.type != "attribute" || s.attributeKey != "headingRows")
        continue;
      const o = s.range.start.nodeAfter, a = o.getAttribute("htmlTheadAttributes"), l = o.getAttribute("htmlTbodyAttributes");
      a && !s.attributeNewValue ? (e.removeAttribute("htmlTheadAttributes", o), r = !0) : l && s.attributeNewValue == t.getRows(o) && (e.removeAttribute("htmlTbodyAttributes", o), r = !0);
    }
    return r;
  };
}
function IF(i) {
  return (t) => {
    t.on("element:table", (e, n, r) => {
      if (!n.modelRange)
        return;
      const s = n.viewItem;
      o(s, "htmlTableAttributes");
      for (const a of s.getChildren())
        a.is("element", "thead") && o(a, "htmlTheadAttributes"), a.is("element", "tbody") && o(a, "htmlTbodyAttributes");
      function o(a, l) {
        const c = i.processViewAttributes(a, r);
        c && r.writer.setAttribute(l, c, n.modelRange);
      }
    }, {
      priority: "low"
    });
  };
}
function RF(i) {
  return (t) => {
    t.on("element:figure", (e, n, r) => {
      const s = n.viewItem;
      if (!n.modelRange || !s.hasClass("table"))
        return;
      const o = i.processViewAttributes(s, r);
      o && r.writer.setAttribute("htmlFigureAttributes", o, n.modelRange);
    }, {
      priority: "low"
    });
  };
}
function OF() {
  return (i) => {
    t("table", "htmlTableAttributes"), t("figure", "htmlFigureAttributes"), t("thead", "htmlTheadAttributes"), t("tbody", "htmlTbodyAttributes");
    function t(e, n) {
      i.on(`attribute:${n}:table`, (r, s, o) => {
        if (!o.consumable.test(s.item, r.name))
          return;
        const a = o.mapper.toViewElement(s.item), l = Qu(o.writer, a, e);
        l && (o.consumable.consume(s.item, r.name), Zs(o.writer, s.attributeOldValue, s.attributeNewValue, l));
      });
    }
  };
}
class VF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StyleElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins.get(nn);
    t.on("register:style", (e, n) => {
      const r = this.editor, s = r.model.schema, o = r.conversion;
      s.register("htmlStyle", n.modelSchema), s.extend("htmlStyle", {
        allowAttributes: [
          "htmlStyleAttributes",
          "htmlContent"
        ],
        isContent: !0
      }), r.data.registerRawContentMatcher({
        name: "style"
      }), o.for("upcast").elementToElement({
        view: "style",
        model: Ep(n)
      }), o.for("upcast").add(Hl(n, t)), o.for("downcast").elementToElement({
        model: "htmlStyle",
        view: (a, { writer: l }) => Qd("style", a, l)
      }), o.for("downcast").add(jl(n)), e.stop();
    });
  }
}
class MF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("ListEditing"))
      return;
    const e = t.model.schema, n = t.conversion, r = t.plugins.get(nn), s = t.plugins.get("ListEditing"), o = t.plugins.get("ListUtils"), a = [
      "ul",
      "ol",
      "li"
    ];
    s.registerDowncastStrategy({
      scope: "item",
      attributeName: "htmlLiAttributes",
      setAttributeOnDowncast: dr
    }), s.registerDowncastStrategy({
      scope: "list",
      attributeName: "htmlUlAttributes",
      setAttributeOnDowncast: dr
    }), s.registerDowncastStrategy({
      scope: "list",
      attributeName: "htmlOlAttributes",
      setAttributeOnDowncast: dr
    }), r.on("register", (l, c) => {
      if (!a.includes(c.view) || (l.stop(), e.checkAttribute("$block", "htmlLiAttributes")))
        return;
      const u = a.map((d) => Vi(d));
      e.extend("$listItem", {
        allowAttributes: u
      }), n.for("upcast").add((d) => {
        d.on("element:ul", bf("htmlUlAttributes", r), {
          priority: "low"
        }), d.on("element:ol", bf("htmlOlAttributes", r), {
          priority: "low"
        }), d.on("element:li", bf("htmlLiAttributes", r), {
          priority: "low"
        });
      });
    }), s.on("postFixer", (l, { listNodes: c, writer: u }) => {
      for (const { node: d, previousNodeInList: f } of c)
        if (f) {
          if (f.getAttribute("listType") == d.getAttribute("listType")) {
            const m = Pw(f.getAttribute("listType")), g = f.getAttribute(m);
            !Yo(d.getAttribute(m), g) && u.model.schema.checkAttribute(d, m) && (u.setAttribute(m, g, d), l.return = !0);
          }
          if (f.getAttribute("listItemId") == d.getAttribute("listItemId")) {
            const m = f.getAttribute("htmlLiAttributes");
            !Yo(d.getAttribute("htmlLiAttributes"), m) && u.model.schema.checkAttribute(d, "htmlLiAttributes") && (u.setAttribute("htmlLiAttributes", m, d), l.return = !0);
          }
        }
    }), s.on("postFixer", (l, { listNodes: c, writer: u }) => {
      for (const { node: d } of c) {
        const f = d.getAttribute("listType");
        !o.isNumberedListType(f) && d.getAttribute("htmlOlAttributes") && (u.removeAttribute("htmlOlAttributes", d), l.return = !0), o.isNumberedListType(f) && d.getAttribute("htmlUlAttributes") && (u.removeAttribute("htmlUlAttributes", d), l.return = !0);
      }
    });
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor;
    if (!t.commands.get("indentList"))
      return;
    const e = t.commands.get("indentList");
    this.listenTo(e, "afterExecute", (n, r) => {
      t.model.change((s) => {
        for (const o of r) {
          const a = Pw(o.getAttribute("listType"));
          t.model.schema.checkAttribute(o, a) && s.setAttribute(a, {}, o);
        }
      });
    });
  }
}
function bf(i, t) {
  return (e, n, r) => {
    const s = n.viewItem;
    n.modelRange || Object.assign(n, r.convertChildren(n.viewItem, n.modelCursor));
    const o = t.processViewAttributes(s, r);
    for (const a of n.modelRange.getItems({
      shallow: !0
    }))
      a.hasAttribute("listItemId") && (a.hasAttribute("htmlUlAttributes") || a.hasAttribute("htmlOlAttributes") || r.writer.model.schema.checkAttribute(a, i) && r.writer.setAttribute(i, o || {}, a));
  };
}
function Pw(i) {
  return i === "numbered" || i == "customNumbered" ? "htmlOlAttributes" : "htmlUlAttributes";
}
class DF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn,
      sa
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CustomElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins.get(nn), e = this.editor.plugins.get(sa);
    t.on("register:$customElement", (n, r) => {
      n.stop();
      const s = this.editor, o = s.model.schema, a = s.conversion, l = s.editing.view.domConverter.unsafeElements, c = s.data.htmlProcessor.domConverter.preElements;
      o.register(r.model, r.modelSchema), o.extend(r.model, {
        allowAttributes: [
          "htmlElementName",
          "htmlCustomElementAttributes",
          "htmlContent"
        ],
        isContent: !0
      }), s.data.htmlProcessor.domConverter.registerRawContentMatcher({
        name: "template"
      }), a.for("upcast").elementToElement({
        view: /.*/,
        model: (u, d) => {
          if (u.name == "$comment" || !LF(u.name) || e.getDefinitionsForView(u.name).size)
            return null;
          l.includes(u.name) || l.push(u.name), c.includes(u.name) || c.push(u.name);
          const f = d.writer.createElement(r.model, {
            htmlElementName: u.name
          }), m = t.processViewAttributes(u, d);
          m && d.writer.setAttribute("htmlCustomElementAttributes", m, f);
          let g;
          if (u.is("element", "template") && u.getCustomProperty("$rawContent"))
            g = u.getCustomProperty("$rawContent");
          else {
            const b = new vr(u.document).createDocumentFragment(u), v = s.data.htmlProcessor.domConverter.viewToDom(b), y = v.firstChild;
            for (; y.firstChild; )
              v.appendChild(y.firstChild);
            y.remove(), g = s.data.htmlProcessor.htmlWriter.getHtml(v);
          }
          d.writer.setAttribute("htmlContent", g, f);
          for (const { item: p } of s.editing.view.createRangeIn(u))
            d.consumable.consume(p, {
              name: !0
            });
          return f;
        },
        converterPriority: "low"
      }), a.for("editingDowncast").elementToElement({
        model: {
          name: r.model,
          attributes: [
            "htmlElementName",
            "htmlCustomElementAttributes",
            "htmlContent"
          ]
        },
        view: (u, { writer: d }) => {
          const f = u.getAttribute("htmlElementName"), m = d.createRawElement(f);
          return u.hasAttribute("htmlCustomElementAttributes") && dr(d, u.getAttribute("htmlCustomElementAttributes"), m), m;
        }
      }), a.for("dataDowncast").elementToElement({
        model: {
          name: r.model,
          attributes: [
            "htmlElementName",
            "htmlCustomElementAttributes",
            "htmlContent"
          ]
        },
        view: (u, { writer: d }) => {
          const f = u.getAttribute("htmlElementName"), m = u.getAttribute("htmlContent"), g = d.createRawElement(f, null, (p, b) => {
            b.setContentOf(p, m);
          });
          return u.hasAttribute("htmlCustomElementAttributes") && dr(d, u.getAttribute("htmlCustomElementAttributes"), g), g;
        }
      });
    });
  }
}
function LF(i) {
  try {
    document.createElement(i);
  } catch {
    return !1;
  }
  return !0;
}
class NF extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "GeneralHtmlSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nn,
      fF,
      pF,
      bF,
      vF,
      kF,
      SF,
      AF,
      VF,
      MF,
      DF
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.plugins.get(nn);
    e.loadAllowedEmptyElementsConfig(t.config.get("htmlSupport.allowEmpty") || []), e.loadAllowedConfig(t.config.get("htmlSupport.allow") || []), e.loadDisallowedConfig(t.config.get("htmlSupport.disallow") || []);
  }
  /**
  * Returns a GHS model attribute name related to a given view element name.
  *
  * @internal
  * @param viewElementName A view element name.
  */
  getGhsAttributeNameForElement(t) {
    const e = this.editor.plugins.get("DataSchema"), n = Array.from(e.getDefinitionsForView(t, !1)), r = n.find((s) => s.isInline && !n[0].isObject);
    return r ? r.model : Vi(t);
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it includes the given class name.
  *
  * @internal
  * @param viewElementName A view element name.
  * @param className The css class to add.
  * @param selectable The selection or element to update.
  */
  addModelHtmlClass(t, e, n) {
    const r = this.editor.model, s = this.getGhsAttributeNameForElement(t);
    r.change((o) => {
      for (const a of xo(r, n, s))
        ko(o, a, s, "classes", (l) => {
          for (const c of tt(e))
            l.add(c);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it does not include the given class name.
  *
  * @internal
  * @param viewElementName A view element name.
  * @param className The css class to remove.
  * @param selectable The selection or element to update.
  */
  removeModelHtmlClass(t, e, n) {
    const r = this.editor.model, s = this.getGhsAttributeNameForElement(t);
    r.change((o) => {
      for (const a of xo(r, n, s))
        ko(o, a, s, "classes", (l) => {
          for (const c of tt(e))
            l.delete(c);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it includes the given attribute.
  *
  * @param viewElementName A view element name.
  * @param attributes The object with attributes to set.
  * @param selectable The selection or element to update.
  */
  setModelHtmlAttributes(t, e, n) {
    const r = this.editor.model, s = this.getGhsAttributeNameForElement(t);
    r.change((o) => {
      for (const a of xo(r, n, s))
        ko(o, a, s, "attributes", (l) => {
          for (const [c, u] of Object.entries(e))
            l.set(c, u);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it does not include the given attribute.
  *
  * @param viewElementName A view element name.
  * @param attributeName The attribute name (or names) to remove.
  * @param selectable The selection or element to update.
  */
  removeModelHtmlAttributes(t, e, n) {
    const r = this.editor.model, s = this.getGhsAttributeNameForElement(t);
    r.change((o) => {
      for (const a of xo(r, n, s))
        ko(o, a, s, "attributes", (l) => {
          for (const c of tt(e))
            l.delete(c);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it includes a given style.
  *
  * @param viewElementName A view element name.
  * @param styles The object with styles to set.
  * @param selectable The selection or element to update.
  */
  setModelHtmlStyles(t, e, n) {
    const r = this.editor.model, s = this.getGhsAttributeNameForElement(t);
    r.change((o) => {
      for (const a of xo(r, n, s))
        ko(o, a, s, "styles", (l) => {
          for (const [c, u] of Object.entries(e))
            l.set(c, u);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it does not include a given style.
  *
  * @param viewElementName A view element name.
  * @param properties The style (or styles list) to remove.
  * @param selectable The selection or element to update.
  */
  removeModelHtmlStyles(t, e, n) {
    const r = this.editor.model, s = this.getGhsAttributeNameForElement(t);
    r.change((o) => {
      for (const a of xo(r, n, s))
        ko(o, a, s, "styles", (l) => {
          for (const c of tt(e))
            l.delete(c);
        });
    });
  }
}
function* xo(i, t, e) {
  if (t)
    if (!(Symbol.iterator in t) && t.is("documentSelection") && t.isCollapsed)
      i.schema.checkAttributeInSelection(t, e) && (yield t);
    else
      for (const n of BF(i, t, e))
        yield* n.getItems({
          shallow: !0
        });
}
function BF(i, t, e) {
  return !(Symbol.iterator in t) && (t.is("node") || t.is("$text") || t.is("$textProxy")) ? i.schema.checkAttribute(t, e) ? [
    i.createRangeOn(t)
  ] : [] : i.schema.getValidRanges(i.createSelection(t).getRanges(), e);
}
class FF extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HtmlComment";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = /* @__PURE__ */ new Map();
    t.data.processor.skipComments = !1, t.model.schema.addAttributeCheck((n, r) => {
      if (n.endsWith("$root") && r.startsWith("$comment"))
        return !0;
    }), t.conversion.for("upcast").elementToMarker({
      view: "$comment",
      model: (n) => {
        const s = `$comment:${Jn()}`, o = n.getCustomProperty("$rawContent");
        return e.set(s, o), s;
      }
    }), t.conversion.for("dataDowncast").markerToElement({
      model: "$comment",
      view: (n, { writer: r }) => {
        let s;
        for (const c of this.editor.model.document.getRootNames())
          if (s = this.editor.model.document.getRoot(c), s.hasAttribute(n.markerName))
            break;
        const o = n.markerName, a = s.getAttribute(o), l = r.createUIElement("$comment");
        return r.setCustomProperty("$rawContent", a, l), l;
      }
    }), t.model.document.registerPostFixer((n) => {
      let r = !1;
      const s = t.model.document.differ.getChangedMarkers().filter((o) => o.name.startsWith("$comment:"));
      for (const o of s) {
        const { oldRange: a, newRange: l } = o.data;
        if (!(a && l && a.root == l.root)) {
          if (a) {
            const c = a.root;
            c.hasAttribute(o.name) && (n.removeAttribute(o.name, c), r = !0);
          }
          if (l) {
            const c = l.root;
            c.rootName == "$graveyard" ? (n.removeMarker(o.name), r = !0) : c.hasAttribute(o.name) || (n.setAttribute(o.name, e.get(o.name) || "", c), r = !0);
          }
        }
      }
      return r;
    }), t.data.on("set", () => {
      for (const n of t.model.markers.getMarkersGroup("$comment"))
        this.removeHtmlComment(n.name);
    }, {
      priority: "high"
    }), t.model.on("deleteContent", (n, [r]) => {
      for (const s of r.getRanges()) {
        const o = t.model.schema.getLimitElement(s), a = t.model.createPositionAt(o, 0), l = t.model.createPositionAt(o, "end");
        let c;
        a.isTouching(s.start) && l.isTouching(s.end) ? c = this.getHtmlCommentsInRange(t.model.createRange(a, l)) : c = this.getHtmlCommentsInRange(s, {
          skipBoundaries: !0
        });
        for (const u of c)
          this.removeHtmlComment(u);
      }
    }, {
      priority: "high"
    });
  }
  /**
  * Creates an HTML comment on the specified position and returns its ID.
  *
  * *Note*: If two comments are created at the same position, the second comment will be inserted before the first one.
  *
  * @returns Comment ID. This ID can be later used to e.g. remove the comment from the content.
  */
  createHtmlComment(t, e) {
    const n = Jn(), s = this.editor.model, o = s.document.getRoot(t.root.rootName), a = `$comment:${n}`;
    return s.change((l) => {
      const c = l.createRange(t);
      return l.addMarker(a, {
        usingOperation: !0,
        affectsData: !0,
        range: c
      }), l.setAttribute(a, e, o), a;
    });
  }
  /**
  * Removes an HTML comment with the given comment ID.
  *
  * It does nothing and returns `false` if the comment with the given ID does not exist.
  * Otherwise it removes the comment and returns `true`.
  *
  * Note that a comment can be removed also by removing the content around the comment.
  *
  * @param commentID The ID of the comment to be removed.
  * @returns `true` when the comment with the given ID was removed, `false` otherwise.
  */
  removeHtmlComment(t) {
    const e = this.editor, n = e.model.markers.get(t);
    return n ? (e.model.change((r) => {
      r.removeMarker(n);
    }), !0) : !1;
  }
  /**
  * Gets the HTML comment data for the comment with a given ID.
  *
  * Returns `null` if the comment does not exist.
  */
  getHtmlCommentData(t) {
    const n = this.editor.model.markers.get(t);
    if (!n)
      return null;
    let r = "";
    for (const s of this.editor.model.document.getRoots())
      if (s.hasAttribute(t)) {
        r = s.getAttribute(t);
        break;
      }
    return {
      content: r,
      position: n.getStart()
    };
  }
  /**
  * Gets all HTML comments in the given range.
  *
  * By default, it includes comments at the range boundaries.
  *
  * @param range
  * @param options.skipBoundaries When set to `true` the range boundaries will be skipped.
  * @returns HTML comment IDs
  */
  getHtmlCommentsInRange(t, { skipBoundaries: e = !1 } = {}) {
    const n = !e;
    return Array.from(this.editor.model.markers.getMarkersGroup("$comment")).filter((s) => r(s, t)).map((s) => s.name);
    function r(s, o) {
      const a = s.getRange().start;
      return (a.isAfter(o.start) || n && a.isEqual(o.start)) && (a.isBefore(o.end) || n && a.isEqual(o.end));
    }
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function $F(i) {
  return i.createContainerElement("span", {
    class: "image-inline"
  }, i.createEmptyElement("img"));
}
function Iw(i) {
  return i.createContainerElement("figure", {
    class: "image"
  }, [
    i.createEmptyElement("img"),
    i.createSlot("children")
  ]);
}
function Ik(i, t) {
  const e = i.plugins.get("ImageUtils"), n = i.plugins.has("ImageInlineEditing") && i.plugins.has("ImageBlockEditing");
  return (s) => e.isInlineImageView(s) ? n && (s.getStyle("display") == "block" || s.findAncestor(e.isBlockImageView) ? "imageBlock" : "imageInline") !== t ? null : r(s) : null;
  function r(s) {
    const o = {
      name: !0
    };
    return s.hasAttribute("src") && (o.attributes = [
      "src"
    ]), o;
  }
}
function Tp(i, t) {
  const e = Ft(t.getSelectedBlocks());
  return !e || i.isObject(e) || e.isEmpty && e.name != "listItem" ? "imageBlock" : "imageInline";
}
function ed(i) {
  return i && i.endsWith("px") ? parseInt(i) : null;
}
function td(i) {
  const t = ed(i.getStyle("width")), e = ed(i.getStyle("height"));
  return !!(t && e);
}
const zF = /^(image|image-inline)$/;
class Hn extends V {
  constructor() {
    super(...arguments);
    /**
    * DOM Emitter.
    */
    h(this, "_domEmitter", new (Xn())());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Checks if the provided model element is an `image` or `imageInline`.
  */
  isImage(e) {
    return this.isInlineImage(e) || this.isBlockImage(e);
  }
  /**
  * Checks if the provided view element represents an inline image.
  *
  * Also, see {@link module:image/imageutils~ImageUtils#isImageWidget}.
  */
  isInlineImageView(e) {
    return !!e && e.is("element", "img");
  }
  /**
  * Checks if the provided view element represents a block image.
  *
  * Also, see {@link module:image/imageutils~ImageUtils#isImageWidget}.
  */
  isBlockImageView(e) {
    return !!e && e.is("element", "figure") && e.hasClass("image");
  }
  /**
  * Handles inserting single file. This method unifies image insertion using {@link module:widget/utils~findOptimalInsertionRange}
  * method.
  *
  * ```ts
  * const imageUtils = editor.plugins.get( 'ImageUtils' );
  *
  * imageUtils.insertImage( { src: 'path/to/image.jpg' } );
  * ```
  *
  * @param attributes Attributes of the inserted image.
  * This method filters out the attributes which are disallowed by the {@link module:engine/model/schema~Schema}.
  * @param selectable Place to insert the image. If not specified,
  * the {@link module:widget/utils~findOptimalInsertionRange} logic will be applied for the block images
  * and `model.document.selection` for the inline images.
  *
  * **Note**: If `selectable` is passed, this helper will not be able to set selection attributes (such as `linkHref`)
  * and apply them to the new image. In this case, make sure all selection attributes are passed in `attributes`.
  *
  * @param imageType Image type of inserted image. If not specified,
  * it will be determined automatically depending of editor config or place of the insertion.
  * @param options.setImageSizes Specifies whether the image `width` and `height` attributes should be set automatically.
  * The default is `true`.
  * @return The inserted model image element.
  */
  insertImage(e = {}, n = null, r = null, s = {}) {
    const o = this.editor, a = o.model, l = a.document.selection, c = Rk(o, n || l, r);
    e = {
      ...Object.fromEntries(l.getAttributes()),
      ...e
    };
    for (const u in e)
      a.schema.checkAttribute(c, u) || delete e[u];
    return a.change((u) => {
      const { setImageSizes: d = !0 } = s, f = u.createElement(c, e);
      return a.insertObject(f, n, null, {
        setSelection: "on",
        // If we want to insert a block image (for whatever reason) then we don't want to split text blocks.
        // This applies only when we don't have the selectable specified (i.e., we insert multiple block images at once).
        findOptimalPosition: !n && c != "imageInline" ? "auto" : void 0
      }), f.parent ? (d && this.setImageNaturalSizeAttributes(f), f) : null;
    });
  }
  /**
  * Reads original image sizes and sets them as `width` and `height`.
  *
  * The `src` attribute may not be available if the user is using an upload adapter. In such a case,
  * this method is called again after the upload process is complete and the `src` attribute is available.
  */
  setImageNaturalSizeAttributes(e) {
    const n = e.getAttribute("src");
    n && (e.getAttribute("width") || e.getAttribute("height") || this.editor.model.change((r) => {
      const s = new ae.window.Image();
      this._domEmitter.listenTo(s, "load", () => {
        !e.getAttribute("width") && !e.getAttribute("height") && this.editor.model.enqueueChange(r.batch, (o) => {
          o.setAttribute("width", s.naturalWidth, e), o.setAttribute("height", s.naturalHeight, e);
        }), this._domEmitter.stopListening(s, "load");
      }), s.src = n;
    }));
  }
  /**
  * Returns an image widget editing view element if one is selected or is among the selection's ancestors.
  */
  getClosestSelectedImageWidget(e) {
    const n = e.getFirstPosition();
    if (!n)
      return null;
    const r = e.getSelectedElement();
    if (r && this.isImageWidget(r))
      return r;
    let s = n.parent;
    for (; s; ) {
      if (s.is("element") && this.isImageWidget(s))
        return s;
      s = s.parent;
    }
    return null;
  }
  /**
  * Returns a image model element if one is selected or is among the selection's ancestors.
  */
  getClosestSelectedImageElement(e) {
    const n = e.getSelectedElement();
    return this.isImage(n) ? n : e.getFirstPosition().findAncestor("imageBlock");
  }
  /**
  * Returns an image widget editing view based on the passed image view.
  */
  getImageWidgetFromImageView(e) {
    return e.findAncestor({
      classes: zF
    });
  }
  /**
  * Checks if image can be inserted at current model selection.
  *
  * @internal
  */
  isImageAllowed() {
    const n = this.editor.model.document.selection;
    return HF(this.editor, n) && jF(n);
  }
  /**
  * Converts a given {@link module:engine/view/element~Element} to an image widget:
  * * Adds a {@link module:engine/view/element~Element#_setCustomProperty custom property} allowing to recognize the image widget
  * element.
  * * Calls the {@link module:widget/utils~toWidget} function with the proper element's label creator.
  *
  * @param writer An instance of the view writer.
  * @param label The element's label. It will be concatenated with the image `alt` attribute if one is present.
  */
  toImageWidget(e, n, r) {
    return n.setCustomProperty("image", !0, e), va(e, n, {
      label: () => {
        const a = this.findViewImgElement(e).getAttribute("alt");
        return a ? `${a} ${r}` : r;
      }
    });
  }
  /**
  * Checks if a given view element is an image widget.
  */
  isImageWidget(e) {
    return !!e.getCustomProperty("image") && Gt(e);
  }
  /**
  * Checks if the provided model element is an `image`.
  */
  isBlockImage(e) {
    return !!e && e.is("element", "imageBlock");
  }
  /**
  * Checks if the provided model element is an `imageInline`.
  */
  isInlineImage(e) {
    return !!e && e.is("element", "imageInline");
  }
  /**
  * Get the view `<img>` from another view element, e.g. a widget (`<figure class="image">`), a link (`<a>`).
  *
  * The `<img>` can be located deep in other elements, so this helper performs a deep tree search.
  */
  findViewImgElement(e) {
    if (this.isInlineImageView(e))
      return e;
    const n = this.editor.editing.view;
    for (const { item: r } of n.createRangeIn(e))
      if (this.isInlineImageView(r))
        return r;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this._domEmitter.stopListening(), super.destroy();
  }
}
function HF(i, t) {
  if (Rk(i, t, null) == "imageBlock") {
    const n = WF(t, i.model);
    if (i.model.schema.checkChild(n, "imageBlock"))
      return !0;
  } else if (i.model.schema.checkChild(t.focus, "imageInline"))
    return !0;
  return !1;
}
function jF(i) {
  return [
    ...i.focus.getAncestors()
  ].every((t) => !t.is("element", "imageBlock"));
}
function WF(i, t) {
  const n = yp(i, t).start.parent;
  return n.isEmpty && !n.is("element", "$root") ? n.parent : n;
}
function Rk(i, t, e) {
  const n = i.model.schema, r = i.config.get("image.insert.type");
  return i.plugins.has("ImageBlockEditing") ? i.plugins.has("ImageInlineEditing") ? e || (r === "inline" ? "imageInline" : r !== "auto" ? "imageBlock" : t.is("selection") ? Tp(n, t) : n.checkChild(t, "imageInline") ? "imageInline" : "imageBlock") : "imageBlock" : "imageInline";
}
const UF = new RegExp(String(/^(http(s)?:\/\/)?[\w-]+\.[\w.~:/[\]@!$&'()*+,;=%-]+/.source + /\.(jpg|jpeg|png|gif|ico|webp|JPG|JPEG|PNG|GIF|ICO|WEBP)/.source + /(\?[\w.~:/[\]@!$&'()*+,;=%-]*)?/.source + /(#[\w.~:/[\]@!$&'()*+,;=%-]*)?$/.source));
class qF extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The paste–to–embed `setTimeout` ID. Stored as a property to allow
    * cleaning of the timeout.
    */
    h(this, "_timeoutId");
    /**
    * The position where the `<imageBlock>` element will be inserted after the timeout,
    * determined each time a new content is pasted into the document.
    */
    h(this, "_positionToInsert");
    this._timeoutId = null, this._positionToInsert = null;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Cp,
      Hn,
      Xd,
      Fr
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AutoImage";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.document, r = e.plugins.get("ClipboardPipeline");
    this.listenTo(r, "inputTransformation", () => {
      const s = n.selection.getFirstRange(), o = ln.fromPosition(s.start);
      o.stickiness = "toPrevious";
      const a = ln.fromPosition(s.end);
      a.stickiness = "toNext", n.once("change:data", () => {
        this._embedImageBetweenPositions(o, a), o.detach(), a.detach();
      }, {
        priority: "high"
      });
    }), e.commands.get("undo").on("execute", () => {
      this._timeoutId && (ae.window.clearTimeout(this._timeoutId), this._positionToInsert.detach(), this._timeoutId = null, this._positionToInsert = null);
    }, {
      priority: "high"
    });
  }
  /**
  * Analyzes the part of the document between provided positions in search for a URL representing an image.
  * When the URL is found, it is automatically converted into an image.
  *
  * @param leftPosition Left position of the selection.
  * @param rightPosition Right position of the selection.
  */
  _embedImageBetweenPositions(e, n) {
    const r = this.editor, s = new Kn(e, n), o = s.getWalker({
      ignoreElementEnd: !0
    }), a = Object.fromEntries(r.model.document.selection.getAttributes()), l = this.editor.plugins.get("ImageUtils");
    let c = "";
    for (const u of o)
      u.item.is("$textProxy") && (c += u.item.data);
    if (c = c.trim(), !c.match(UF)) {
      s.detach();
      return;
    }
    this._positionToInsert = ln.fromPosition(e), this._timeoutId = setTimeout(() => {
      if (!r.commands.get("insertImage").isEnabled) {
        s.detach();
        return;
      }
      r.model.change((f) => {
        this._timeoutId = null, f.remove(s), s.detach();
        let m;
        this._positionToInsert.root.rootName !== "$graveyard" && (m = this._positionToInsert.toPosition()), l.insertImage({
          ...a,
          src: c
        }, m), this._positionToInsert.detach(), this._positionToInsert = null;
      }), r.plugins.get("Delete").requestUndoOnBackspace();
    }, 100);
  }
}
class GF extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const n = this.editor.plugins.get("ImageUtils").getClosestSelectedImageElement(this.editor.model.document.selection);
    this.isEnabled = !!n, this.isEnabled && n.hasAttribute("alt") ? this.value = n.getAttribute("alt") : this.value = !1;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options
  * @param options.newValue The new value of the `alt` attribute to set.
  */
  execute(t) {
    const e = this.editor, n = e.plugins.get("ImageUtils"), r = e.model, s = n.getClosestSelectedImageElement(r.document.selection);
    r.change((o) => {
      o.setAttribute("alt", t.newValue, s);
    });
  }
}
class KF extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageTextAlternativeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.commands.add("imageTextAlternative", new GF(this.editor));
  }
}
class ZF extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Tracks information about the DOM focus in the form.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * An input with a label.
    */
    h(this, "labeledInput");
    /**
    * A button used to submit the form.
    */
    h(this, "saveButtonView");
    /**
    * A button used to cancel the form.
    */
    h(this, "cancelButtonView");
    /**
    * A collection of views which can be focused in the form.
    */
    h(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    h(this, "_focusCycler");
    const n = this.locale.t;
    this.focusTracker = new wt(), this.keystrokes = new Ot(), this.labeledInput = this._createLabeledInputView(), this.saveButtonView = this._createButton(n("Save"), ee.check, "ck-button-save"), this.saveButtonView.type = "submit", this.cancelButtonView = this._createButton(n("Cancel"), ee.cancel, "ck-button-cancel", "cancel"), this._focusables = new In(), this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-text-alternative-form",
          "ck-responsive-form"
        ],
        // https://github.com/ckeditor/ckeditor5-image/issues/40
        tabindex: "-1"
      },
      children: [
        this.labeledInput,
        this.saveButtonView,
        this.cancelButtonView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element), Cs({
      view: this
    }), [
      this.labeledInput,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((e) => {
      this._focusables.add(e), this.focusTracker.add(e.element);
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates the button view.
  *
  * @param label The button label
  * @param icon The button's icon.
  * @param className The additional button CSS class name.
  * @param eventName The event name that the ButtonView#execute event will be delegated to.
  * @returns The button view instance.
  */
  _createButton(e, n, r, s) {
    const o = new Te(this.locale);
    return o.set({
      label: e,
      icon: n,
      tooltip: !0
    }), o.extendTemplate({
      attributes: {
        class: r
      }
    }), s && o.delegate("execute").to(this, s), o;
  }
  /**
  * Creates an input with a label.
  *
  * @returns Labeled field view instance.
  */
  _createLabeledInputView() {
    const e = this.locale.t, n = new Qt(this.locale, yi);
    return n.label = e("Text alternative"), n;
  }
}
function YF(i) {
  const t = i.plugins.get("ContextualBalloon");
  if (i.plugins.get("ImageUtils").getClosestSelectedImageWidget(i.editing.view.document.selection)) {
    const n = Sp(i);
    t.updatePosition(n);
  }
}
function Sp(i) {
  const t = i.editing.view, e = fn.defaultPositions, n = i.plugins.get("ImageUtils");
  return {
    target: t.domConverter.mapViewToDom(n.getClosestSelectedImageWidget(t.document.selection)),
    positions: [
      e.northArrowSouth,
      e.northArrowSouthWest,
      e.northArrowSouthEast,
      e.southArrowNorth,
      e.southArrowNorthWest,
      e.southArrowNorthEast,
      e.viewportStickyNorth
    ]
  };
}
class JF extends V {
  constructor() {
    super(...arguments);
    /**
    * The contextual balloon plugin instance.
    */
    h(this, "_balloon");
    /**
    * A form containing a textarea and buttons, used to change the `alt` text value.
    */
    h(this, "_form");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Qi
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageTextAlternativeUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this._createButton();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._form && this._form.destroy();
  }
  /**
  * Creates a button showing the balloon panel for changing the image text alternative and
  * registers it in the editor {@link module:ui/componentfactory~ComponentFactory ComponentFactory}.
  */
  _createButton() {
    const e = this.editor, n = e.t;
    e.ui.componentFactory.add("imageTextAlternative", (r) => {
      const s = e.commands.get("imageTextAlternative"), o = new Te(r);
      return o.set({
        label: n("Change image text alternative"),
        icon: ee.textAlternative,
        tooltip: !0
      }), o.bind("isEnabled").to(s, "isEnabled"), o.bind("isOn").to(s, "value", (a) => !!a), this.listenTo(o, "execute", () => {
        this._showForm();
      }), o;
    });
  }
  /**
  * Creates the {@link module:image/imagetextalternative/ui/textalternativeformview~TextAlternativeFormView}
  * form.
  */
  _createForm() {
    const e = this.editor, r = e.editing.view.document, s = e.plugins.get("ImageUtils");
    this._balloon = this.editor.plugins.get("ContextualBalloon"), this._form = new (bc(ZF))(e.locale), this._form.render(), this.listenTo(this._form, "submit", () => {
      e.execute("imageTextAlternative", {
        newValue: this._form.labeledInput.fieldView.element.value
      }), this._hideForm(!0);
    }), this.listenTo(this._form, "cancel", () => {
      this._hideForm(!0);
    }), this._form.keystrokes.set("Esc", (o, a) => {
      this._hideForm(!0), a();
    }), this.listenTo(e.ui, "update", () => {
      s.getClosestSelectedImageWidget(r.selection) ? this._isVisible && YF(e) : this._hideForm(!0);
    }), _s({
      emitter: this._form,
      activator: () => this._isVisible,
      contextElements: () => [
        this._balloon.view.element
      ],
      callback: () => this._hideForm()
    });
  }
  /**
  * Shows the {@link #_form} in the {@link #_balloon}.
  */
  _showForm() {
    if (this._isVisible)
      return;
    this._form || this._createForm();
    const e = this.editor, n = e.commands.get("imageTextAlternative"), r = this._form.labeledInput;
    this._form.disableCssTransitions(), this._isInBalloon || this._balloon.add({
      view: this._form,
      position: Sp(e)
    }), r.fieldView.value = r.fieldView.element.value = n.value || "", this._form.labeledInput.fieldView.select(), this._form.enableCssTransitions();
  }
  /**
  * Removes the {@link #_form} from the {@link #_balloon}.
  *
  * @param focusEditable Controls whether the editing view is focused afterwards.
  */
  _hideForm(e = !1) {
    this._isInBalloon && (this._form.focusTracker.isFocused && this._form.saveButtonView.focus(), this._balloon.remove(this._form), e && this.editor.editing.view.focus());
  }
  /**
  * Returns `true` when the {@link #_form} is the visible view in the {@link #_balloon}.
  */
  get _isVisible() {
    return !!this._balloon && this._balloon.visibleView === this._form;
  }
  /**
  * Returns `true` when the {@link #_form} is in the {@link #_balloon}.
  */
  get _isInBalloon() {
    return !!this._balloon && this._balloon.hasView(this._form);
  }
}
class Ap extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      KF,
      JF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageTextAlternative";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function XF(i) {
  const t = (e, n, r) => {
    if (!r.consumable.test(n.viewItem, {
      name: !0,
      classes: "image"
    }))
      return;
    const s = i.findViewImgElement(n.viewItem);
    if (!s || !r.consumable.test(s, {
      name: !0
    }))
      return;
    r.consumable.consume(n.viewItem, {
      name: !0,
      classes: "image"
    });
    const o = r.convertItem(s, n.modelCursor), a = Ft(o.modelRange.getItems());
    if (!a) {
      r.consumable.revert(n.viewItem, {
        name: !0,
        classes: "image"
      });
      return;
    }
    r.convertChildren(n.viewItem, a), r.updateConversionResult(a, n);
  };
  return (e) => {
    e.on("element:figure", t);
  };
}
function Ok(i, t) {
  const e = (n, r, s) => {
    if (!s.consumable.consume(r.item, n.name))
      return;
    const o = s.writer, a = s.mapper.toViewElement(r.item), l = i.findViewImgElement(a);
    r.attributeNewValue === null ? (o.removeAttribute("srcset", l), o.removeAttribute("sizes", l)) : r.attributeNewValue && (o.setAttribute("srcset", r.attributeNewValue, l), o.setAttribute("sizes", "100vw", l));
  };
  return (n) => {
    n.on(`attribute:srcset:${t}`, e);
  };
}
function nd(i, t, e) {
  const n = (r, s, o) => {
    if (!o.consumable.consume(s.item, r.name))
      return;
    const a = o.writer, l = o.mapper.toViewElement(s.item), c = i.findViewImgElement(l);
    a.setAttribute(s.attributeKey, s.attributeNewValue || "", c);
  };
  return (r) => {
    r.on(`attribute:${e}:${t}`, n);
  };
}
class Pp extends nr {
  /**
  * @inheritDoc
  */
  observe(t) {
    this.listenTo(t, "load", (e, n) => {
      const r = n.target;
      this.checkShouldIgnoreEventFromTarget(r) || r.tagName == "IMG" && this._fireEvents(n);
    }, {
      useCapture: !0
    });
  }
  /**
  * @inheritDoc
  */
  stopObserving(t) {
    this.stopListening(t);
  }
  /**
  * Fires {@link module:engine/view/document~Document#event:layoutChanged} and
  * {@link module:engine/view/document~Document#event:imageLoaded}
  * if observer {@link #isEnabled is enabled}.
  *
  * @param domEvent The DOM event.
  */
  _fireEvents(t) {
    this.isEnabled && (this.document.fire("layoutChanged"), this.document.fire("imageLoaded", t));
  }
}
class QF extends Ve {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = t.config.get("image.insert.type");
    t.plugins.has("ImageBlockEditing") || e === "block" && Xe("image-block-plugin-required"), t.plugins.has("ImageInlineEditing") || e === "inline" && Xe("image-inline-plugin-required");
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get("ImageUtils");
    this.isEnabled = t.isImageAllowed();
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options Options for the executed command.
  * @param options.imageType The type of the image to insert. If not specified, the type will be determined automatically.
  * @param options.source The image source or an array of image sources to insert.
  * See the documentation of the command to learn more about accepted formats.
  */
  execute(t) {
    const e = tt(t.source), n = this.editor.model.document.selection, r = this.editor.plugins.get("ImageUtils"), s = Object.fromEntries(n.getAttributes());
    e.forEach((o, a) => {
      const l = n.getSelectedElement();
      if (typeof o == "string" && (o = {
        src: o
      }), a && l && r.isImage(l)) {
        const c = this.editor.model.createPositionAfter(l);
        r.insertImage({
          ...o,
          ...s
        }, c, t.imageType);
      } else
        r.insertImage({
          ...o,
          ...s
        }, null, t.imageType);
    });
  }
}
class e$ extends Ve {
  constructor(t) {
    super(t), this.decorate("cleanupImage");
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.plugins.get("ImageUtils"), n = this.editor.model.document.selection.getSelectedElement();
    this.isEnabled = e.isImage(n), this.value = this.isEnabled ? n.getAttribute("src") : null;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options Options for the executed command.
  * @param options.source The image source to replace.
  */
  execute(t) {
    const e = this.editor.model.document.selection.getSelectedElement(), n = this.editor.plugins.get("ImageUtils");
    this.editor.model.change((r) => {
      r.setAttribute("src", t.source, e), this.cleanupImage(r, e), n.setImageNaturalSizeAttributes(e);
    });
  }
  /**
  * Cleanup image attributes that are not relevant to the new source.
  *
  * Removed attributes are: 'srcset', 'sizes', 'sources', 'width', 'height', 'alt'.
  *
  * This method is decorated, to allow custom cleanup logic.
  * For example, to remove 'myImageId' attribute after 'src' has changed:
  *
  * ```ts
  * replaceImageSourceCommand.on( 'cleanupImage', ( eventInfo, [ writer, image ] ) => {
  * 	writer.removeAttribute( 'myImageId', image );
  * } );
  * ```
  */
  cleanupImage(t, e) {
    t.removeAttribute("srcset", e), t.removeAttribute("sizes", e), t.removeAttribute("sources", e), t.removeAttribute("width", e), t.removeAttribute("height", e), t.removeAttribute("alt", e);
  }
}
class Vk extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.conversion;
    t.editing.view.addObserver(Pp), e.for("upcast").attributeToAttribute({
      view: {
        name: "img",
        key: "alt"
      },
      model: "alt"
    }).attributeToAttribute({
      view: {
        name: "img",
        key: "srcset"
      },
      model: "srcset"
    });
    const n = new QF(t), r = new e$(t);
    t.commands.add("insertImage", n), t.commands.add("replaceImageSource", r), t.commands.add("imageInsert", n);
  }
}
class Mk extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageSizeAttributes";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._registerSchema(), this._registerConverters("imageBlock"), this._registerConverters("imageInline");
  }
  /**
  * Registers the `width` and `height` attributes for inline and block images.
  */
  _registerSchema() {
    this.editor.plugins.has("ImageBlockEditing") && this.editor.model.schema.extend("imageBlock", {
      allowAttributes: [
        "width",
        "height"
      ]
    }), this.editor.plugins.has("ImageInlineEditing") && this.editor.model.schema.extend("imageInline", {
      allowAttributes: [
        "width",
        "height"
      ]
    });
  }
  /**
  * Registers converters for `width` and `height` attributes.
  */
  _registerConverters(t) {
    const e = this.editor, n = e.plugins.get("ImageUtils"), r = t === "imageBlock" ? "figure" : "img";
    e.conversion.for("upcast").attributeToAttribute({
      view: {
        name: r,
        styles: {
          width: /.+/
        }
      },
      model: {
        key: "width",
        value: (o) => td(o) ? ed(o.getStyle("width")) : null
      }
    }).attributeToAttribute({
      view: {
        name: r,
        key: "width"
      },
      model: "width"
    }).attributeToAttribute({
      view: {
        name: r,
        styles: {
          height: /.+/
        }
      },
      model: {
        key: "height",
        value: (o) => td(o) ? ed(o.getStyle("height")) : null
      }
    }).attributeToAttribute({
      view: {
        name: r,
        key: "height"
      },
      model: "height"
    }), e.conversion.for("editingDowncast").add((o) => {
      s(o, "width", "width", !0), s(o, "height", "height", !0);
    }), e.conversion.for("dataDowncast").add((o) => {
      s(o, "width", "width", !1), s(o, "height", "height", !1);
    });
    function s(o, a, l, c) {
      o.on(`attribute:${a}:${t}`, (u, d, f) => {
        if (!f.consumable.consume(d.item, u.name))
          return;
        const m = f.writer, g = f.mapper.toViewElement(d.item), p = n.findViewImgElement(g);
        if (d.attributeNewValue !== null ? m.setAttribute(l, d.attributeNewValue, p) : m.removeAttribute(l, p), d.item.hasAttribute("sources"))
          return;
        const b = d.item.hasAttribute("resizedWidth");
        if (t === "imageInline" && !b && !c)
          return;
        const v = d.item.getAttribute("width"), y = d.item.getAttribute("height");
        v && y && m.setStyle("aspect-ratio", `${v}/${y}`, p);
      });
    }
  }
}
class Dk extends Ve {
  /**
  * @inheritDoc
  *
  * @param modelElementName Model element name the command converts to.
  */
  constructor(e, n) {
    super(e);
    /**
    * Model element name the command converts to.
    */
    h(this, "_modelElementName");
    this._modelElementName = n;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const n = this.editor.plugins.get("ImageUtils"), r = n.getClosestSelectedImageElement(this.editor.model.document.selection);
    this._modelElementName === "imageBlock" ? this.isEnabled = n.isInlineImage(r) : this.isEnabled = n.isBlockImage(r);
  }
  /**
  * Executes the command and changes the type of a selected image.
  *
  * @fires execute
  * @param options.setImageSizes Specifies whether the image `width` and `height` attributes should be set automatically.
  * The default is `true`.
  * @returns An object containing references to old and new model image elements
  * (for before and after the change) so external integrations can hook into the decorated
  * `execute` event and handle this change. `null` if the type change failed.
  */
  execute(e = {}) {
    const n = this.editor, r = this.editor.model, s = n.plugins.get("ImageUtils"), o = s.getClosestSelectedImageElement(r.document.selection), a = Object.fromEntries(o.getAttributes());
    return !a.src && !a.uploadId ? null : r.change((l) => {
      const { setImageSizes: c = !0 } = e, u = Array.from(r.markers).filter((m) => m.getRange().containsItem(o)), d = s.insertImage(a, r.createSelection(o, "on"), this._modelElementName, {
        setImageSizes: c
      });
      if (!d)
        return null;
      const f = l.createRangeOn(d);
      for (const m of u) {
        const g = m.getRange(), p = g.root.rootName != "$graveyard" ? g.getJoined(f, !0) : f;
        l.updateMarker(m, {
          range: p
        });
      }
      return {
        oldElement: o,
        newElement: d
      };
    });
  }
}
class Lk extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImagePlaceholder";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._setupSchema(), this._setupConversion(), this._setupLoadListener();
  }
  /**
  * Extends model schema.
  */
  _setupSchema() {
    const t = this.editor.model.schema;
    t.isRegistered("imageBlock") && t.extend("imageBlock", {
      allowAttributes: [
        "placeholder"
      ]
    }), t.isRegistered("imageInline") && t.extend("imageInline", {
      allowAttributes: [
        "placeholder"
      ]
    });
  }
  /**
  * Registers converters.
  */
  _setupConversion() {
    const t = this.editor, e = t.conversion, n = t.plugins.get("ImageUtils");
    e.for("editingDowncast").add((r) => {
      r.on("attribute:placeholder", (s, o, a) => {
        if (!a.consumable.test(o.item, s.name) || !o.item.is("element", "imageBlock") && !o.item.is("element", "imageInline"))
          return;
        a.consumable.consume(o.item, s.name);
        const l = a.writer, c = a.mapper.toViewElement(o.item), u = n.findViewImgElement(c);
        o.attributeNewValue ? (l.addClass("image_placeholder", u), l.setStyle("background-image", `url(${o.attributeNewValue})`, u), l.setCustomProperty("editingPipeline:doNotReuseOnce", !0, u)) : (l.removeClass("image_placeholder", u), l.removeStyle("background-image", u));
      });
    });
  }
  /**
  * Prepares listener for image load.
  */
  _setupLoadListener() {
    const t = this.editor, e = t.model, n = t.editing, r = n.view, s = t.plugins.get("ImageUtils");
    r.addObserver(Pp), this.listenTo(r.document, "imageLoaded", (o, a) => {
      const l = r.domConverter.mapDomToView(a.target);
      if (!l)
        return;
      const c = s.getImageWidgetFromImageView(l);
      if (!c)
        return;
      const u = n.mapper.toModelElement(c);
      !u || !u.hasAttribute("placeholder") || e.enqueueChange({
        isUndoable: !1
      }, (d) => {
        d.removeAttribute("placeholder", u);
      });
    });
  }
}
class Nk extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Vk,
      Mk,
      Hn,
      Lk,
      ui
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageBlockEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.register("imageBlock", {
      inheritAllFrom: "$blockObject",
      allowAttributes: [
        "alt",
        "src",
        "srcset"
      ]
    }), this._setupConversion(), t.plugins.has("ImageInlineEditing") && (t.commands.add("imageTypeBlock", new Dk(this.editor, "imageBlock")), this._setupClipboardIntegration());
  }
  /**
  * Configures conversion pipelines to support upcasting and downcasting
  * block images (block image widgets) and their attributes.
  */
  _setupConversion() {
    const t = this.editor, e = t.t, n = t.conversion, r = t.plugins.get("ImageUtils");
    n.for("dataDowncast").elementToStructure({
      model: "imageBlock",
      view: (s, { writer: o }) => Iw(o)
    }), n.for("editingDowncast").elementToStructure({
      model: "imageBlock",
      view: (s, { writer: o }) => r.toImageWidget(Iw(o), o, e("image widget"))
    }), n.for("downcast").add(nd(r, "imageBlock", "src")).add(nd(r, "imageBlock", "alt")).add(Ok(r, "imageBlock")), n.for("upcast").elementToElement({
      view: Ik(t, "imageBlock"),
      model: (s, { writer: o }) => o.createElement("imageBlock", s.hasAttribute("src") ? {
        src: s.getAttribute("src")
      } : void 0)
    }).add(XF(r));
  }
  /**
  * Integrates the plugin with the clipboard pipeline.
  *
  * Idea is that the feature should recognize the user's intent when an **inline** image is
  * pasted or dropped. If such an image is pasted/dropped:
  *
  * * into an empty block (e.g. an empty paragraph),
  * * on another object (e.g. some block widget).
  *
  * it gets converted into a block image on the fly. We assume this is the user's intent
  * if they decided to put their image there.
  *
  * See the `ImageInlineEditing` for the similar integration that works in the opposite direction.
  *
  * The feature also sets image `width` and `height` attributes on paste.
  */
  _setupClipboardIntegration() {
    const t = this.editor, e = t.model, n = t.editing.view, r = t.plugins.get("ImageUtils"), s = t.plugins.get("ClipboardPipeline");
    this.listenTo(s, "inputTransformation", (o, a) => {
      const l = Array.from(a.content.getChildren());
      let c;
      if (!l.every(r.isInlineImageView))
        return;
      a.targetRanges ? c = t.editing.mapper.toModelRange(a.targetRanges[0]) : c = e.document.selection.getFirstRange();
      const u = e.createSelection(c);
      if (Tp(e.schema, u) === "imageBlock") {
        const d = new vr(n.document), f = l.map((m) => d.createElement("figure", {
          class: "image"
        }, m));
        a.content = d.createDocumentFragment(f);
      }
    }), this.listenTo(s, "contentInsertion", (o, a) => {
      a.method === "paste" && e.change((l) => {
        const c = l.createRangeIn(a.content);
        for (const u of c.getItems())
          u.is("element", "imageBlock") && r.setImageNaturalSizeAttributes(u);
      });
    });
  }
}
class t$ extends de {
  /**
  * Creates a view for the dropdown panel of {@link module:image/imageinsert/imageinsertui~ImageInsertUI}.
  *
  * @param locale The localization services instance.
  * @param integrations An integrations object that contains components (or tokens for components) to be shown in the panel view.
  */
  constructor(e, n = []) {
    super(e);
    /**
    * Tracks information about DOM focus in the form.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * A collection of views that can be focused in the form.
    */
    h(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    h(this, "_focusCycler");
    /**
    * A collection of the defined integrations for inserting the images.
    */
    h(this, "children");
    this.focusTracker = new wt(), this.keystrokes = new Ot(), this._focusables = new In(), this.children = this.createCollection(), this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    });
    for (const r of n)
      this.children.add(r), this._focusables.add(r), r instanceof gp && this._focusables.addMany(r.children);
    this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-image-insert-form"
        ],
        tabindex: -1
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Cs({
      view: this
    });
    for (const n of this._focusables)
      this.focusTracker.add(n.element);
    this.keystrokes.listenTo(this.element);
    const e = (n) => n.stopPropagation();
    this.keystrokes.set("arrowright", e), this.keystrokes.set("arrowleft", e), this.keystrokes.set("arrowup", e), this.keystrokes.set("arrowdown", e);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the first {@link #_focusables focusable} in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
}
class eh extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The dropdown view responsible for displaying the image insert UI.
    */
    h(this, "dropdownView");
    /**
    * Registered integrations map.
    */
    h(this, "_integrations", /* @__PURE__ */ new Map());
    e.config.define("image.insert.integrations", [
      "upload",
      "assetManager",
      "url"
    ]);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInsertUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.document.selection, r = e.plugins.get("ImageUtils");
    this.set("isImageSelected", !1), this.listenTo(e.model.document, "change", () => {
      this.isImageSelected = r.isImage(n.getSelectedElement());
    });
    const s = (a) => this._createToolbarComponent(a), o = (a) => this._createMenuBarComponent(a);
    e.ui.componentFactory.add("insertImage", s), e.ui.componentFactory.add("imageInsert", s), e.ui.componentFactory.add("menuBar:insertImage", o);
  }
  /**
  * Registers the insert image dropdown integration.
  */
  registerIntegration({ name: e, observable: n, buttonViewCreator: r, formViewCreator: s, menuBarButtonViewCreator: o, requiresForm: a = !1 }) {
    this._integrations.has(e) && Xe("image-insert-integration-exists", {
      name: e
    }), this._integrations.set(e, {
      observable: n,
      buttonViewCreator: r,
      menuBarButtonViewCreator: o,
      formViewCreator: s,
      requiresForm: a
    });
  }
  /**
  * Creates the toolbar component.
  */
  _createToolbarComponent(e) {
    const n = this.editor, r = e.t, s = this._prepareIntegrations();
    if (!s.length)
      return null;
    let o;
    const a = s[0];
    if (s.length == 1) {
      if (!a.requiresForm)
        return a.buttonViewCreator(!0);
      o = a.buttonViewCreator(!0);
    } else {
      const u = a.buttonViewCreator(!1);
      o = new io(e, u), o.tooltip = !0, o.bind("label").to(this, "isImageSelected", (d) => r(d ? "Replace image" : "Insert image"));
    }
    const l = this.dropdownView = Yn(e, o), c = s.map(({ observable: u }) => typeof u == "function" ? u() : u);
    return l.bind("isEnabled").toMany(c, "isEnabled", (...u) => u.some((d) => d)), l.once("change:isOpen", () => {
      const u = s.map(({ formViewCreator: f }) => f(s.length == 1)), d = new t$(n.locale, u);
      l.panelView.children.add(d);
    }), l;
  }
  /**
  * Creates the menu bar component.
  */
  _createMenuBarComponent(e) {
    const n = e.t, r = this._prepareIntegrations();
    if (!r.length)
      return null;
    let s;
    const o = r[0];
    if (r.length == 1)
      s = o.menuBarButtonViewCreator(!0);
    else {
      s = new Yi(e);
      const a = new ra(e);
      s.panelView.children.add(a), s.buttonView.set({
        icon: ee.image,
        label: n("Image")
      });
      for (const l of r) {
        const c = new Ks(e, s), u = l.menuBarButtonViewCreator(!1);
        c.children.add(u), a.items.add(c);
      }
    }
    return s;
  }
  /**
  * Validates the integrations list.
  */
  _prepareIntegrations() {
    const n = this.editor.config.get("image.insert.integrations"), r = [];
    if (!n.length)
      return Xe("image-insert-integrations-not-specified"), r;
    for (const s of n) {
      if (!this._integrations.has(s)) {
        [
          "upload",
          "assetManager",
          "url"
        ].includes(s) || Xe("image-insert-unknown-integration", {
          item: s
        });
        continue;
      }
      r.push(this._integrations.get(s));
    }
    return r.length || Xe("image-insert-integrations-not-registered"), r;
  }
}
class n$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Nk,
      oo,
      Ap,
      eh
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageBlock";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class i$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Vk,
      Mk,
      Hn,
      Lk,
      ui
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInlineEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.register("imageInline", {
      inheritAllFrom: "$inlineObject",
      allowAttributes: [
        "alt",
        "src",
        "srcset"
      ],
      // Disallow inline images in captions (at least for now).
      // This is the best spot to do that because independent packages can introduce captions (ImageCaption, TableCaption, etc.).
      disallowIn: [
        "caption"
      ]
    }), this._setupConversion(), t.plugins.has("ImageBlockEditing") && (t.commands.add("imageTypeInline", new Dk(this.editor, "imageInline")), this._setupClipboardIntegration());
  }
  /**
  * Configures conversion pipelines to support upcasting and downcasting
  * inline images (inline image widgets) and their attributes.
  */
  _setupConversion() {
    const t = this.editor, e = t.t, n = t.conversion, r = t.plugins.get("ImageUtils");
    n.for("dataDowncast").elementToElement({
      model: "imageInline",
      view: (s, { writer: o }) => o.createEmptyElement("img")
    }), n.for("editingDowncast").elementToStructure({
      model: "imageInline",
      view: (s, { writer: o }) => r.toImageWidget($F(o), o, e("image widget"))
    }), n.for("downcast").add(nd(r, "imageInline", "src")).add(nd(r, "imageInline", "alt")).add(Ok(r, "imageInline")), n.for("upcast").elementToElement({
      view: Ik(t, "imageInline"),
      model: (s, { writer: o }) => o.createElement("imageInline", s.hasAttribute("src") ? {
        src: s.getAttribute("src")
      } : void 0)
    });
  }
  /**
  * Integrates the plugin with the clipboard pipeline.
  *
  * Idea is that the feature should recognize the user's intent when an **block** image is
  * pasted or dropped. If such an image is pasted/dropped into a non-empty block
  * (e.g. a paragraph with some text) it gets converted into an inline image on the fly.
  *
  * We assume this is the user's intent if they decided to put their image there.
  *
  * **Note**: If a block image has a caption, it will not be converted to an inline image
  * to avoid the confusion. Captions are added on purpose and they should never be lost
  * in the clipboard pipeline.
  *
  * See the `ImageBlockEditing` for the similar integration that works in the opposite direction.
  *
  * The feature also sets image `width` and `height` attributes when pasting.
  */
  _setupClipboardIntegration() {
    const t = this.editor, e = t.model, n = t.editing.view, r = t.plugins.get("ImageUtils"), s = t.plugins.get("ClipboardPipeline");
    this.listenTo(s, "inputTransformation", (o, a) => {
      const l = Array.from(a.content.getChildren());
      let c;
      if (!l.every(r.isBlockImageView))
        return;
      a.targetRanges ? c = t.editing.mapper.toModelRange(a.targetRanges[0]) : c = e.document.selection.getFirstRange();
      const u = e.createSelection(c);
      if (Tp(e.schema, u) === "imageInline") {
        const d = new vr(n.document), f = l.map((m) => m.childCount === 1 ? (Array.from(m.getAttributes()).forEach((g) => d.setAttribute(...g, r.findViewImgElement(m))), m.getChild(0)) : m);
        a.content = d.createDocumentFragment(f);
      }
    }), this.listenTo(s, "contentInsertion", (o, a) => {
      a.method === "paste" && e.change((l) => {
        const c = l.createRangeIn(a.content);
        for (const u of c.getItems())
          u.is("element", "imageInline") && r.setImageNaturalSizeAttributes(u);
      });
    });
  }
}
class r$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      i$,
      oo,
      Ap,
      eh
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInline";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class Bk extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCaptionUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn
    ];
  }
  /**
  * Returns the caption model element from a given image element. Returns `null` if no caption is found.
  */
  getCaptionFromImageModelElement(t) {
    for (const e of t.getChildren())
      if (e && e.is("element", "caption"))
        return e;
    return null;
  }
  /**
  * Returns the caption model element for a model selection. Returns `null` if the selection has no caption element ancestor.
  */
  getCaptionFromModelSelection(t) {
    const e = this.editor.plugins.get("ImageUtils"), n = t.getFirstPosition().findAncestor("caption");
    return n && e.isBlockImage(n.parent) ? n : null;
  }
  /**
  * {@link module:engine/view/matcher~Matcher} pattern. Checks if a given element is a `<figcaption>` element that is placed
  * inside the image `<figure>` element.
  * @returns Returns the object accepted by {@link module:engine/view/matcher~Matcher} or `null` if the element
  * cannot be matched.
  */
  matchImageCaptionViewElement(t) {
    const e = this.editor.plugins.get("ImageUtils");
    return t.name == "figcaption" && e.isBlockImageView(t.parent) ? {
      name: !0
    } : null;
  }
}
class s$ extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor, e = t.plugins.get("ImageCaptionUtils"), n = t.plugins.get("ImageUtils");
    if (!t.plugins.has(Nk)) {
      this.isEnabled = !1, this.value = !1;
      return;
    }
    const r = t.model.document.selection, s = r.getSelectedElement();
    if (!s) {
      const o = e.getCaptionFromModelSelection(r);
      this.isEnabled = !!o, this.value = !!o;
      return;
    }
    this.isEnabled = n.isImage(s), this.isEnabled ? this.value = !!e.getCaptionFromImageModelElement(s) : this.value = !1;
  }
  /**
  * Executes the command.
  *
  * ```ts
  * editor.execute( 'toggleImageCaption' );
  * ```
  *
  * @param options Options for the executed command.
  * @param options.focusCaptionOnShow When true and the caption shows up, the selection will be moved into it straight away.
  * @fires execute
  */
  execute(t = {}) {
    const { focusCaptionOnShow: e } = t;
    this.editor.model.change((n) => {
      this.value ? this._hideImageCaption(n) : this._showImageCaption(n, e);
    });
  }
  /**
  * Shows the caption of the `<imageBlock>` or `<imageInline>`. Also:
  *
  * * it converts `<imageInline>` to `<imageBlock>` to show the caption,
  * * it attempts to restore the caption content from the `ImageCaptionEditing` caption registry,
  * * it moves the selection to the caption right away, it the `focusCaptionOnShow` option was set.
  */
  _showImageCaption(t, e) {
    const r = this.editor.model.document.selection, s = this.editor.plugins.get("ImageCaptionEditing"), o = this.editor.plugins.get("ImageUtils");
    let a = r.getSelectedElement();
    const l = s._getSavedCaption(a);
    o.isInlineImage(a) && (this.editor.execute("imageTypeBlock"), a = r.getSelectedElement());
    const c = l || t.createElement("caption");
    t.append(c, a), e && t.setSelection(c, "in");
  }
  /**
  * Hides the caption of a selected image (or an image caption the selection is anchored to).
  *
  * The content of the caption is stored in the `ImageCaptionEditing` caption registry to make this
  * a reversible action.
  */
  _hideImageCaption(t) {
    const e = this.editor, n = e.model.document.selection, r = e.plugins.get("ImageCaptionEditing"), s = e.plugins.get("ImageCaptionUtils");
    let o = n.getSelectedElement(), a;
    o ? a = s.getCaptionFromImageModelElement(o) : (a = s.getCaptionFromModelSelection(n), o = a.parent), r._saveCaption(o, a), t.setSelection(o, "on"), t.remove(a);
  }
}
class o$ extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A map that keeps saved JSONified image captions and image model elements they are
    * associated with.
    *
    * To learn more about this system, see {@link #_saveCaption}.
    */
    h(this, "_savedCaptionsMap");
    this._savedCaptionsMap = /* @__PURE__ */ new WeakMap();
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn,
      Bk
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCaptionEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.schema;
    n.isRegistered("caption") ? n.extend("caption", {
      allowIn: "imageBlock"
    }) : n.register("caption", {
      allowIn: "imageBlock",
      allowContentOf: "$block",
      isLimit: !0
    }), e.commands.add("toggleImageCaption", new s$(this.editor)), this._setupConversion(), this._setupImageTypeCommandsIntegration(), this._registerCaptionReconversion();
  }
  /**
  * Configures conversion pipelines to support upcasting and downcasting
  * image captions.
  */
  _setupConversion() {
    const e = this.editor, n = e.editing.view, r = e.plugins.get("ImageUtils"), s = e.plugins.get("ImageCaptionUtils"), o = e.t;
    e.conversion.for("upcast").elementToElement({
      view: (a) => s.matchImageCaptionViewElement(a),
      model: "caption"
    }), e.conversion.for("dataDowncast").elementToElement({
      model: "caption",
      view: (a, { writer: l }) => r.isBlockImage(a.parent) ? l.createContainerElement("figcaption") : null
    }), e.conversion.for("editingDowncast").elementToElement({
      model: "caption",
      view: (a, { writer: l }) => {
        if (!r.isBlockImage(a.parent))
          return null;
        const c = l.createEditableElement("figcaption");
        l.setCustomProperty("imageCaption", !0, c), c.placeholder = o("Enter image caption"), Qg({
          view: n,
          element: c,
          keepOnFocus: !0
        });
        const u = a.parent.getAttribute("alt"), d = u ? o("Caption for image: %0", [
          u
        ]) : o("Caption for the image");
        return wp(c, l, {
          label: d
        });
      }
    });
  }
  /**
  * Integrates with {@link module:image/image/imagetypecommand~ImageTypeCommand image type commands}
  * to make sure the caption is preserved when the type of an image changes so it can be restored
  * in the future if the user decides they want their caption back.
  */
  _setupImageTypeCommandsIntegration() {
    const e = this.editor, n = e.plugins.get("ImageUtils"), r = e.plugins.get("ImageCaptionUtils"), s = e.commands.get("imageTypeInline"), o = e.commands.get("imageTypeBlock"), a = (l) => {
      if (!l.return)
        return;
      const { oldElement: c, newElement: u } = l.return;
      /* istanbul ignore if: paranoid check -- @preserve */
      if (!c)
        return;
      if (n.isBlockImage(c)) {
        const f = r.getCaptionFromImageModelElement(c);
        if (f) {
          this._saveCaption(u, f);
          return;
        }
      }
      const d = this._getSavedCaption(c);
      d && this._saveCaption(u, d);
    };
    s && this.listenTo(s, "execute", a, {
      priority: "low"
    }), o && this.listenTo(o, "execute", a, {
      priority: "low"
    });
  }
  /**
  * Returns the saved {@link module:engine/model/element~Element#toJSON JSONified} caption
  * of an image model element.
  *
  * See {@link #_saveCaption}.
  *
  * @internal
  * @param imageModelElement The model element the caption should be returned for.
  * @returns The model caption element or `null` if there is none.
  */
  _getSavedCaption(e) {
    const n = this._savedCaptionsMap.get(e);
    return n ? xt.fromJSON(n) : null;
  }
  /**
  * Saves a {@link module:engine/model/element~Element#toJSON JSONified} caption for
  * an image element to allow restoring it in the future.
  *
  * A caption is saved every time it gets hidden and/or the type of an image changes. The
  * user should be able to restore it on demand.
  *
  * **Note**: The caption cannot be stored in the image model element attribute because,
  * for instance, when the model state propagates to collaborators, the attribute would get
  * lost (mainly because it does not convert to anything when the caption is hidden) and
  * the states of collaborators' models would de-synchronize causing numerous issues.
  *
  * See {@link #_getSavedCaption}.
  *
  * @internal
  * @param imageModelElement The model element the caption is saved for.
  * @param caption The caption model element to be saved.
  */
  _saveCaption(e, n) {
    this._savedCaptionsMap.set(e, n.toJSON());
  }
  /**
  * Reconverts image caption when image alt attribute changes.
  * The change of alt attribute is reflected in caption's aria-label attribute.
  */
  _registerCaptionReconversion() {
    const e = this.editor, n = e.model, r = e.plugins.get("ImageUtils"), s = e.plugins.get("ImageCaptionUtils");
    n.document.on("change:data", () => {
      const o = n.document.differ.getChanges();
      for (const a of o) {
        if (a.attributeKey !== "alt")
          continue;
        const l = a.range.start.nodeAfter;
        if (r.isBlockImage(l)) {
          const c = s.getCaptionFromImageModelElement(l);
          if (!c)
            return;
          e.editing.reconvertItem(c);
        }
      }
    });
  }
}
class a$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Bk
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCaptionUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.editing.view, n = t.plugins.get("ImageCaptionUtils"), r = t.t;
    t.ui.componentFactory.add("toggleImageCaption", (s) => {
      const o = t.commands.get("toggleImageCaption"), a = new Te(s);
      return a.set({
        icon: ee.caption,
        tooltip: !0,
        isToggleable: !0
      }), a.bind("isOn", "isEnabled").to(o, "value", "isEnabled"), a.bind("label").to(o, "value", (l) => r(l ? "Toggle caption off" : "Toggle caption on")), this.listenTo(a, "execute", () => {
        t.execute("toggleImageCaption", {
          focusCaptionOnShow: !0
        });
        const l = n.getCaptionFromModelSelection(t.model.document.selection);
        if (l) {
          const c = t.editing.mapper.toViewElement(l);
          e.scrollToTheSelection(), e.change((u) => {
            u.addClass("image__caption_highlighted", c);
          });
        }
        t.editing.view.focus();
      }), a;
    });
  }
}
class l$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      o$,
      a$
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCaption";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function Fk(i) {
  const t = i.map((e) => e.replace("+", "\\+"));
  return new RegExp(`^image\\/(${t.join("|")})$`);
}
function c$(i) {
  return new Promise((t, e) => {
    const n = i.getAttribute("src");
    fetch(n).then((r) => r.blob()).then((r) => {
      const s = $k(r, n), a = `image.${s.replace("image/", "")}`, l = new File([
        r
      ], a, {
        type: s
      });
      t(l);
    }).catch((r) => r && r.name === "TypeError" ? d$(n).then(t).catch(e) : e(r));
  });
}
function u$(i, t) {
  return !i.isInlineImageView(t) || !t.getAttribute("src") ? !1 : !!t.getAttribute("src").match(/^data:image\/\w+;base64,/g) || !!t.getAttribute("src").match(/^blob:/g);
}
function $k(i, t) {
  return i.type ? i.type : t.match(/data:(image\/\w+);base64/) ? t.match(/data:(image\/\w+);base64/)[1].toLowerCase() : "image/jpeg";
}
function d$(i) {
  return h$(i).then((t) => {
    const e = $k(t, i), r = `image.${e.replace("image/", "")}`;
    return new File([
      t
    ], r, {
      type: e
    });
  });
}
function h$(i) {
  return new Promise((t, e) => {
    const n = ae.document.createElement("img");
    n.addEventListener("load", () => {
      const r = ae.document.createElement("canvas");
      r.width = n.width, r.height = n.height, r.getContext("2d").drawImage(n, 0, 0), r.toBlob((o) => o ? t(o) : e());
    }), n.addEventListener("error", () => e()), n.src = i;
  });
}
class f$ extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageUploadUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("uploadImage", () => this._createToolbarButton()), t.ui.componentFactory.add("imageUpload", () => this._createToolbarButton()), t.ui.componentFactory.add("menuBar:uploadImage", () => this._createMenuBarButton("standalone")), t.plugins.has("ImageInsertUI") && t.plugins.get("ImageInsertUI").registerIntegration({
      name: "upload",
      observable: () => t.commands.get("uploadImage"),
      buttonViewCreator: () => this._createToolbarButton(),
      formViewCreator: () => this._createDropdownButton(),
      menuBarButtonViewCreator: (e) => this._createMenuBarButton(e ? "insertOnly" : "insertNested")
    });
  }
  /**
  * Creates the base for various kinds of the button component provided by this feature.
  */
  _createButton(t) {
    const e = this.editor, n = e.locale, r = e.commands.get("uploadImage"), s = e.config.get("image.upload.types"), o = Fk(s), a = new t(e.locale), l = n.t;
    return a.set({
      acceptedType: s.map((c) => `image/${c}`).join(","),
      allowMultipleFiles: !0,
      label: l("Upload from computer"),
      icon: ee.imageUpload
    }), a.bind("isEnabled").to(r), a.on("done", (c, u) => {
      const d = Array.from(u).filter((f) => o.test(f.type));
      d.length && (e.execute("uploadImage", {
        file: d
      }), e.editing.view.focus());
    }), a;
  }
  /**
  * Creates a simple toolbar button, with an icon and a tooltip.
  */
  _createToolbarButton() {
    const t = this.editor.locale.t, e = this.editor.plugins.get("ImageInsertUI"), n = this.editor.commands.get("uploadImage"), r = this._createButton(Ov);
    return r.tooltip = !0, r.bind("label").to(e, "isImageSelected", n, "isAccessAllowed", (s, o) => t(o ? s ? "Replace image from computer" : "Upload image from computer" : "You have no image upload permissions.")), r;
  }
  /**
  * Creates a button for the dropdown view, with an icon, text and no tooltip.
  */
  _createDropdownButton() {
    const t = this.editor.locale.t, e = this.editor.plugins.get("ImageInsertUI"), n = this._createButton(Ov);
    return n.withText = !0, n.bind("label").to(e, "isImageSelected", (r) => t(r ? "Replace from computer" : "Upload from computer")), n.on("execute", () => {
      e.dropdownView.isOpen = !1;
    }), n;
  }
  /**
  * Creates a button for the menu bar.
  */
  _createMenuBarButton(t) {
    const e = this.editor.locale.t, n = this._createButton(ok);
    switch (n.withText = !0, t) {
      case "standalone":
        n.label = e("Image from computer");
        break;
      case "insertOnly":
        n.label = e("Image");
        break;
      case "insertNested":
        n.label = e("From computer");
        break;
    }
    return n;
  }
}
class m$ extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The image placeholder that is displayed before real image data can be accessed.
    *
    * For the record, this image is a 1x1 px GIF with an aspect ratio set by CSS.
    */
    h(this, "placeholder");
    /**
    * This method is called each time the image `uploadStatus` attribute is changed.
    *
    * @param evt An object containing information about the fired event.
    * @param data Additional information about the change.
    */
    h(this, "uploadStatusChange", (e, n, r) => {
      const s = this.editor, o = n.item, a = o.getAttribute("uploadId");
      if (!r.consumable.consume(n.item, e.name))
        return;
      const l = s.plugins.get("ImageUtils"), c = s.plugins.get(ml), u = a ? n.attributeNewValue : null, d = this.placeholder, f = s.editing.mapper.toViewElement(o), m = r.writer;
      if (u == "reading") {
        Rw(f, m), Ow(l, d, f, m);
        return;
      }
      if (u == "uploading") {
        const g = c.loaders.get(a);
        Rw(f, m), g ? (Vw(f, m), p$(f, m, g, s.editing.view), _$(l, f, m, g)) : Ow(l, d, f, m);
        return;
      }
      u == "complete" && c.loaders.get(a) && v$(f, m, s.editing.view), b$(f, m), Vw(f, m), g$(f, m);
    });
    this.placeholder = "data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==";
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageUploadProgress";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.plugins.has("ImageBlockEditing") && e.editing.downcastDispatcher.on("attribute:uploadStatus:imageBlock", this.uploadStatusChange), e.plugins.has("ImageInlineEditing") && e.editing.downcastDispatcher.on("attribute:uploadStatus:imageInline", this.uploadStatusChange);
  }
}
function Rw(i, t) {
  i.hasClass("ck-appear") || t.addClass("ck-appear", i);
}
function g$(i, t) {
  t.removeClass("ck-appear", i);
}
function Ow(i, t, e, n) {
  e.hasClass("ck-image-upload-placeholder") || n.addClass("ck-image-upload-placeholder", e);
  const r = i.findViewImgElement(e);
  r.getAttribute("src") !== t && n.setAttribute("src", t, r), zk(e, "placeholder") || n.insert(n.createPositionAfter(r), y$(n));
}
function Vw(i, t) {
  i.hasClass("ck-image-upload-placeholder") && t.removeClass("ck-image-upload-placeholder", i), Hk(i, t, "placeholder");
}
function p$(i, t, e, n) {
  const r = w$(t);
  t.insert(t.createPositionAt(i, "end"), r), e.on("change:uploadedPercent", (s, o, a) => {
    n.change((l) => {
      l.setStyle("width", a + "%", r);
    });
  });
}
function b$(i, t) {
  Hk(i, t, "progressBar");
}
function v$(i, t, e) {
  const n = t.createUIElement("div", {
    class: "ck-image-upload-complete-icon"
  });
  t.insert(t.createPositionAt(i, "end"), n), setTimeout(() => {
    e.change((r) => r.remove(r.createRangeOn(n)));
  }, 3e3);
}
function w$(i) {
  const t = i.createUIElement("div", {
    class: "ck-progress-bar"
  });
  return i.setCustomProperty("progressBar", !0, t), t;
}
function y$(i) {
  const t = i.createUIElement("div", {
    class: "ck-upload-placeholder-loader"
  });
  return i.setCustomProperty("placeholder", !0, t), t;
}
function zk(i, t) {
  for (const e of i.getChildren())
    if (e.getCustomProperty(t))
      return e;
}
function Hk(i, t, e) {
  const n = zk(i, e);
  n && t.remove(t.createRangeOn(n));
}
function _$(i, t, e, n) {
  if (n.data) {
    const r = i.findViewImgElement(t);
    e.setAttribute("src", n.data, r);
  }
}
class C$ extends Ve {
  /**
  * Creates an instance of the `imageUlpoad` command. When executed, the command upload one of
  * the currently selected image from computer.
  *
  * @param editor The editor instance.
  */
  constructor(t) {
    super(t), this.set("isAccessAllowed", !0);
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor, e = t.plugins.get("ImageUtils"), n = t.model.document.selection.getSelectedElement();
    this.isEnabled = e.isImageAllowed() || e.isImage(n);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options Options for the executed command.
  * @param options.file The image file or an array of image files to upload.
  */
  execute(t) {
    const e = tt(t.file), n = this.editor.model.document.selection, r = this.editor.plugins.get("ImageUtils"), s = Object.fromEntries(n.getAttributes());
    e.forEach((o, a) => {
      const l = n.getSelectedElement();
      if (a && l && r.isImage(l)) {
        const c = this.editor.model.createPositionAfter(l);
        this._uploadImage(o, s, c);
      } else
        this._uploadImage(o, s);
    });
  }
  /**
  * Handles uploading single file.
  */
  _uploadImage(t, e, n) {
    const r = this.editor, o = r.plugins.get(ml).createLoader(t), a = r.plugins.get("ImageUtils");
    o && a.insertImage({
      ...e,
      uploadId: o.id
    }, n);
  }
}
class k$ extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * An internal mapping of {@link module:upload/filerepository~FileLoader#id file loader UIDs} and
    * model elements during the upload.
    *
    * Model element of the uploaded image can change, for instance, when {@link module:image/image/imagetypecommand~ImageTypeCommand}
    * is executed as a result of adding caption or changing image style. As a result, the upload logic must keep track of the model
    * element (reference) and resolve the upload for the correct model element (instead of the one that landed in the `$graveyard`
    * after image type changed).
    */
    h(this, "_uploadImageElements");
    e.config.define("image", {
      upload: {
        types: [
          "jpeg",
          "png",
          "gif",
          "bmp",
          "webp",
          "tiff"
        ]
      }
    }), this._uploadImageElements = /* @__PURE__ */ new Map();
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ml,
      Hv,
      ui,
      Hn
    ];
  }
  static get pluginName() {
    return "ImageUploadEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.document, r = e.conversion, s = e.plugins.get(ml), o = e.plugins.get("ImageUtils"), a = e.plugins.get("ClipboardPipeline"), l = Fk(e.config.get("image.upload.types")), c = new C$(e);
    e.commands.add("uploadImage", c), e.commands.add("imageUpload", c), r.for("upcast").attributeToAttribute({
      view: {
        name: "img",
        key: "uploadId"
      },
      model: "uploadId"
    }), this.listenTo(e.editing.view.document, "clipboardInput", (u, d) => {
      if (x$(d.dataTransfer))
        return;
      const f = Array.from(d.dataTransfer.files).filter((g) => g ? l.test(g.type) : !1);
      if (!f.length)
        return;
      if (u.stop(), e.model.change((g) => {
        d.targetRanges && g.setSelection(d.targetRanges.map((p) => e.editing.mapper.toModelRange(p))), e.execute("uploadImage", {
          file: f
        });
      }), !e.commands.get("uploadImage").isAccessAllowed) {
        const g = e.plugins.get("Notification"), p = e.locale.t;
        g.showWarning(p("You have no image upload permissions."), {
          namespace: "image"
        });
      }
    }), this.listenTo(a, "inputTransformation", (u, d) => {
      const f = Array.from(e.editing.view.createRangeIn(d.content)).map((g) => g.item).filter((g) => u$(o, g) && !g.getAttribute("uploadProcessed")).map((g) => ({
        promise: c$(g),
        imageElement: g
      }));
      if (!f.length)
        return;
      const m = new vr(e.editing.view.document);
      for (const g of f) {
        m.setAttribute("uploadProcessed", !0, g.imageElement);
        const p = s.createLoader(g.promise);
        p && (m.setAttribute("src", "", g.imageElement), m.setAttribute("uploadId", p.id, g.imageElement));
      }
    }), e.editing.view.document.on("dragover", (u, d) => {
      d.preventDefault();
    }), n.on("change", () => {
      const u = n.differ.getChanges({
        includeChangesInGraveyard: !0
      }).reverse(), d = /* @__PURE__ */ new Set();
      for (const f of u)
        if (f.type == "insert" && f.name != "$text") {
          const m = f.position.nodeAfter, g = f.position.root.rootName == "$graveyard";
          for (const p of E$(e, m)) {
            const b = p.getAttribute("uploadId");
            if (!b)
              continue;
            const v = s.loaders.get(b);
            v && (g ? d.has(b) || v.abort() : (d.add(b), this._uploadImageElements.set(b, p), v.status == "idle" && this._readAndUpload(v)));
          }
        }
    }), this.on("uploadComplete", (u, { imageElement: d, data: f }) => {
      const m = f.urls ? f.urls : f;
      this.editor.model.change((g) => {
        g.setAttribute("src", m.default, d), this._parseAndSetSrcsetAttributeOnImage(m, d, g), o.setImageNaturalSizeAttributes(d);
      });
    }, {
      priority: "low"
    });
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const e = this.editor.model.schema;
    this.editor.plugins.has("ImageBlockEditing") && e.extend("imageBlock", {
      allowAttributes: [
        "uploadId",
        "uploadStatus"
      ]
    }), this.editor.plugins.has("ImageInlineEditing") && e.extend("imageInline", {
      allowAttributes: [
        "uploadId",
        "uploadStatus"
      ]
    });
  }
  /**
  * Reads and uploads an image.
  *
  * The image is read from the disk and as a Base64-encoded string it is set temporarily to
  * `image[src]`. When the image is successfully uploaded, the temporary data is replaced with the target
  * image's URL (the URL to the uploaded image on the server).
  */
  _readAndUpload(e) {
    const n = this.editor, r = n.model, s = n.locale.t, o = n.plugins.get(ml), a = n.plugins.get(Hv), l = n.plugins.get("ImageUtils"), c = this._uploadImageElements;
    return r.enqueueChange({
      isUndoable: !1
    }, (d) => {
      d.setAttribute("uploadStatus", "reading", c.get(e.id));
    }), e.read().then(() => {
      const d = e.upload(), f = c.get(e.id);
      /* istanbul ignore next -- @preserve */
      if (Me.isSafari) {
        const m = n.editing.mapper.toViewElement(f), g = l.findViewImgElement(m);
        n.editing.view.once("render", () => {
          if (!g.parent)
            return;
          const p = n.editing.view.domConverter.mapViewToDom(g.parent);
          if (!p)
            return;
          const b = p.style.display;
          p.style.display = "none", p._ckHack = p.offsetHeight, p.style.display = b;
        });
      }
      return n.ui && n.ui.ariaLiveAnnouncer.announce(s("Uploading image")), r.enqueueChange({
        isUndoable: !1
      }, (m) => {
        m.setAttribute("uploadStatus", "uploading", f);
      }), d;
    }).then((d) => {
      r.enqueueChange({
        isUndoable: !1
      }, (f) => {
        const m = c.get(e.id);
        f.setAttribute("uploadStatus", "complete", m), n.ui && n.ui.ariaLiveAnnouncer.announce(s("Image upload complete")), this.fire("uploadComplete", {
          data: d,
          imageElement: m
        });
      }), u();
    }).catch((d) => {
      if (n.ui && n.ui.ariaLiveAnnouncer.announce(s("Error during image upload")), e.status !== "error" && e.status !== "aborted")
        throw d;
      e.status == "error" && d && a.showWarning(d, {
        title: s("Upload failed"),
        namespace: "upload"
      }), r.enqueueChange({
        isUndoable: !1
      }, (f) => {
        f.remove(c.get(e.id));
      }), u();
    });
    function u() {
      r.enqueueChange({
        isUndoable: !1
      }, (d) => {
        const f = c.get(e.id);
        d.removeAttribute("uploadId", f), d.removeAttribute("uploadStatus", f), c.delete(e.id);
      }), o.destroyLoader(e);
    }
  }
  /**
  * Creates the `srcset` attribute based on a given file upload response and sets it as an attribute to a specific image element.
  *
  * @param data Data object from which `srcset` will be created.
  * @param image The image element on which the `srcset` attribute will be set.
  */
  _parseAndSetSrcsetAttributeOnImage(e, n, r) {
    let s = 0;
    const o = Object.keys(e).filter((a) => {
      const l = parseInt(a, 10);
      if (!isNaN(l))
        return s = Math.max(s, l), !0;
    }).map((a) => `${e[a]} ${a}w`).join(", ");
    if (o != "") {
      const a = {
        srcset: o
      };
      !n.hasAttribute("width") && !n.hasAttribute("height") && (a.width = s), r.setAttributes(a, n);
    }
  }
}
function x$(i) {
  return Array.from(i.types).includes("text/html") && i.getData("text/html") !== "";
}
function E$(i, t) {
  const e = i.plugins.get("ImageUtils");
  return Array.from(i.model.createRangeOn(t)).filter((n) => e.isImage(n.item)).map((n) => n.item);
}
class T$ extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageUpload";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      k$,
      f$,
      m$
    ];
  }
}
class S$ extends de {
  /**
  * Creates a view for the dropdown panel of {@link module:image/imageinsert/imageinsertui~ImageInsertUI}.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * The URL input field view.
    */
    h(this, "urlInputView");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    this.set("imageURLInputValue", ""), this.set("isImageSelected", !1), this.set("isEnabled", !0), this.keystrokes = new Ot(), this.urlInputView = this._createUrlInputView(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-image-insert-url"
        ]
      },
      children: [
        this.urlInputView,
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-image-insert-url__action-row"
            ]
          }
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates the {@link #urlInputView}.
  */
  _createUrlInputView() {
    const e = this.locale, n = e.t, r = new Qt(e, yi);
    return r.bind("label").to(this, "isImageSelected", (s) => n(s ? "Update image URL" : "Insert image via URL")), r.bind("isEnabled").to(this), r.fieldView.inputMode = "url", r.fieldView.placeholder = "https://example.com/image.png", r.fieldView.bind("value").to(this, "imageURLInputValue", (s) => s || ""), r.fieldView.on("input", () => {
      this.imageURLInputValue = r.fieldView.element.value.trim();
    }), r;
  }
  /**
  * Focuses the view.
  */
  focus() {
    this.urlInputView.focus();
  }
}
class A$ extends V {
  constructor() {
    super(...arguments);
    h(this, "_imageInsertUI");
    h(this, "_formView");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInsertViaUrlUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      eh,
      Ud
    ];
  }
  init() {
    this.editor.ui.componentFactory.add("insertImageViaUrl", () => this._createToolbarButton()), this.editor.ui.componentFactory.add("menuBar:insertImageViaUrl", () => this._createMenuBarButton("standalone"));
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._imageInsertUI = this.editor.plugins.get("ImageInsertUI"), this._imageInsertUI.registerIntegration({
      name: "url",
      observable: () => this.editor.commands.get("insertImage"),
      buttonViewCreator: () => this._createToolbarButton(),
      formViewCreator: () => this._createDropdownButton(),
      menuBarButtonViewCreator: (e) => this._createMenuBarButton(e ? "insertOnly" : "insertNested")
    });
  }
  /**
  * Creates the base for various kinds of the button component provided by this feature.
  */
  _createInsertUrlButton(e) {
    const n = new e(this.editor.locale);
    return n.icon = ee.imageUrl, n.on("execute", () => {
      this._showModal();
    }), n;
  }
  /**
  * Creates a simple toolbar button, with an icon and a tooltip.
  */
  _createToolbarButton() {
    const e = this.editor.locale.t, n = this._createInsertUrlButton(Te);
    return n.tooltip = !0, n.bind("label").to(this._imageInsertUI, "isImageSelected", (r) => e(r ? "Update image URL" : "Insert image via URL")), n;
  }
  /**
  * Creates a button for the dropdown view, with an icon, text and no tooltip.
  */
  _createDropdownButton() {
    const e = this.editor.locale.t, n = this._createInsertUrlButton(Te);
    return n.withText = !0, n.bind("label").to(this._imageInsertUI, "isImageSelected", (r) => e(r ? "Update image URL" : "Insert via URL")), n;
  }
  /**
  * Creates a button for the menu bar.
  */
  _createMenuBarButton(e) {
    const n = this.editor.locale.t, r = this._createInsertUrlButton(At);
    switch (r.withText = !0, e) {
      case "standalone":
        r.label = n("Image via URL");
        break;
      case "insertOnly":
        r.label = n("Image");
        break;
      case "insertNested":
        r.label = n("Via URL");
        break;
    }
    return r;
  }
  /**
  * Creates the form view used to submit the image URL.
  */
  _createInsertUrlView() {
    const e = this.editor, n = e.locale, r = e.commands.get("replaceImageSource"), s = e.commands.get("insertImage"), o = new S$(n);
    return o.bind("isImageSelected").to(this._imageInsertUI), o.bind("isEnabled").toMany([
      s,
      r
    ], "isEnabled", (...a) => a.some((l) => l)), o;
  }
  /**
  * Shows the insert image via URL form view in a modal.
  */
  _showModal() {
    const e = this.editor, r = e.locale.t, s = e.plugins.get("Dialog");
    this._formView || (this._formView = this._createInsertUrlView(), this._formView.on("submit", () => this._handleSave()));
    const o = e.commands.get("replaceImageSource");
    this._formView.imageURLInputValue = o.value || "", s.show({
      id: "insertImageViaUrl",
      title: this._imageInsertUI.isImageSelected ? r("Update image URL") : r("Insert image via URL"),
      isModal: !0,
      content: this._formView,
      actionButtons: [
        {
          label: r("Cancel"),
          withText: !0,
          onExecute: () => s.hide()
        },
        {
          label: r("Accept"),
          class: "ck-button-action",
          withText: !0,
          onExecute: () => this._handleSave()
        }
      ]
    });
  }
  /**
  * Executes appropriate command depending on selection and form value.
  */
  _handleSave() {
    this.editor.commands.get("replaceImageSource").isEnabled ? this.editor.execute("replaceImageSource", {
      source: this._formView.imageURLInputValue
    }) : this.editor.execute("insertImage", {
      source: this._formView.imageURLInputValue
    }), this.editor.plugins.get("Dialog").hide();
  }
}
class P$ extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInsertViaUrl";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      A$,
      eh
    ];
  }
}
class I$ extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor, n = t.plugins.get("ImageUtils").getClosestSelectedImageElement(t.model.document.selection);
    this.isEnabled = !!n, !n || !n.hasAttribute("resizedWidth") ? this.value = null : this.value = {
      width: n.getAttribute("resizedWidth"),
      height: null
    };
  }
  /**
  * Executes the command.
  *
  * ```ts
  * // Sets the width to 50%:
  * editor.execute( 'resizeImage', { width: '50%' } );
  *
  * // Removes the width attribute:
  * editor.execute( 'resizeImage', { width: null } );
  * ```
  *
  * @param options
  * @param options.width The new width of the image.
  * @fires execute
  */
  execute(t) {
    const e = this.editor, n = e.model, r = e.plugins.get("ImageUtils"), s = r.getClosestSelectedImageElement(n.document.selection);
    this.value = {
      width: t.width,
      height: null
    }, s && n.change((o) => {
      o.setAttribute("resizedWidth", t.width, s), o.removeAttribute("resizedHeight", s), r.setImageNaturalSizeAttributes(s);
    });
  }
}
class jk extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageResizeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("image", {
      resizeUnit: "%",
      resizeOptions: [
        {
          name: "resizeImage:original",
          value: null,
          icon: "original"
        },
        {
          name: "resizeImage:custom",
          value: "custom",
          icon: "custom"
        },
        {
          name: "resizeImage:25",
          value: "25",
          icon: "small"
        },
        {
          name: "resizeImage:50",
          value: "50",
          icon: "medium"
        },
        {
          name: "resizeImage:75",
          value: "75",
          icon: "large"
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = new I$(t);
    this._registerConverters("imageBlock"), this._registerConverters("imageInline"), t.commands.add("resizeImage", e), t.commands.add("imageResize", e);
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._registerSchema();
  }
  _registerSchema() {
    this.editor.plugins.has("ImageBlockEditing") && this.editor.model.schema.extend("imageBlock", {
      allowAttributes: [
        "resizedWidth",
        "resizedHeight"
      ]
    }), this.editor.plugins.has("ImageInlineEditing") && this.editor.model.schema.extend("imageInline", {
      allowAttributes: [
        "resizedWidth",
        "resizedHeight"
      ]
    });
  }
  /**
  * Registers image resize converters.
  *
  * @param imageType The type of the image.
  */
  _registerConverters(t) {
    const e = this.editor, n = e.plugins.get("ImageUtils");
    e.conversion.for("downcast").add((r) => r.on(`attribute:resizedWidth:${t}`, (s, o, a) => {
      if (!a.consumable.consume(o.item, s.name))
        return;
      const l = a.writer, c = a.mapper.toViewElement(o.item);
      o.attributeNewValue !== null ? (l.setStyle("width", o.attributeNewValue, c), l.addClass("image_resized", c)) : (l.removeStyle("width", c), l.removeClass("image_resized", c));
    })), e.conversion.for("dataDowncast").attributeToAttribute({
      model: {
        name: t,
        key: "resizedHeight"
      },
      view: (r) => ({
        key: "style",
        value: {
          height: r
        }
      })
    }), e.conversion.for("editingDowncast").add((r) => r.on(`attribute:resizedHeight:${t}`, (s, o, a) => {
      if (!a.consumable.consume(o.item, s.name))
        return;
      const l = a.writer, c = a.mapper.toViewElement(o.item), u = t === "imageInline" ? n.findViewImgElement(c) : c;
      o.attributeNewValue !== null ? l.setStyle("height", o.attributeNewValue, u) : l.removeStyle("height", u);
    })), e.conversion.for("upcast").attributeToAttribute({
      view: {
        name: t === "imageBlock" ? "figure" : "img",
        styles: {
          width: /.+/
        }
      },
      model: {
        key: "resizedWidth",
        value: (r) => td(r) ? null : r.getStyle("width")
      }
    }), e.conversion.for("upcast").attributeToAttribute({
      view: {
        name: t === "imageBlock" ? "figure" : "img",
        styles: {
          height: /.+/
        }
      },
      model: {
        key: "resizedHeight",
        value: (r) => td(r) ? null : r.getStyle("height")
      }
    });
  }
}
const vf = {
  small: ee.objectSizeSmall,
  medium: ee.objectSizeMedium,
  large: ee.objectSizeLarge,
  custom: ee.objectSizeCustom,
  original: ee.objectSizeFull
};
class R$ extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The resize unit.
    * @default '%'
    */
    h(this, "_resizeUnit");
    this._resizeUnit = e.config.get("image.resizeUnit");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jk
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageResizeButtons";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.config.get("image.resizeOptions"), r = e.commands.get("resizeImage");
    this.bind("isEnabled").to(r);
    for (const s of n)
      this._registerImageResizeButton(s);
    this._registerImageResizeDropdown(n);
  }
  /**
  * A helper function that creates a standalone button component for the plugin.
  *
  * @param resizeOption A model of the resize option.
  */
  _registerImageResizeButton(e) {
    const n = this.editor, { name: r, value: s, icon: o } = e;
    n.ui.componentFactory.add(r, (a) => {
      const l = new Te(a), c = n.commands.get("resizeImage"), u = this._getOptionLabelValue(e, !0);
      if (!vf[o])
        throw new T("imageresizebuttons-missing-icon", n, e);
      if (l.set({
        // Use the `label` property for a verbose description (because of ARIA).
        label: u,
        icon: vf[o],
        tooltip: u,
        isToggleable: !0
      }), l.bind("isEnabled").to(this), n.plugins.has("ImageCustomResizeUI") && Ga(e)) {
        const d = n.plugins.get("ImageCustomResizeUI");
        this.listenTo(l, "execute", () => {
          d._showForm(this._resizeUnit);
        });
      } else {
        const d = s ? s + this._resizeUnit : null;
        l.bind("isOn").to(c, "value", Cm(d)), this.listenTo(l, "execute", () => {
          n.execute("resizeImage", {
            width: d
          });
        });
      }
      return l;
    });
  }
  /**
  * A helper function that creates a dropdown component for the plugin containing all the resize options defined in
  * the editor configuration.
  *
  * @param options An array of configured options.
  */
  _registerImageResizeDropdown(e) {
    const n = this.editor, r = n.t, s = e.find((a) => !a.value), o = (a) => {
      const l = n.commands.get("resizeImage"), c = Yn(a, ZC), u = c.buttonView, d = r("Resize image");
      return u.set({
        tooltip: d,
        commandValue: s.value,
        icon: vf.medium,
        isToggleable: !0,
        label: this._getOptionLabelValue(s),
        withText: !0,
        class: "ck-resize-image-button",
        ariaLabel: d,
        ariaLabelledBy: void 0
      }), u.bind("label").to(l, "value", (f) => f && f.width ? f.width : this._getOptionLabelValue(s)), c.bind("isEnabled").to(this), pa(c, () => this._getResizeDropdownListItemDefinitions(e, l), {
        ariaLabel: r("Image resize list"),
        role: "menu"
      }), this.listenTo(c, "execute", (f) => {
        "onClick" in f.source ? f.source.onClick() : (n.execute(f.source.commandName, {
          width: f.source.commandValue
        }), n.editing.view.focus());
      }), c;
    };
    n.ui.componentFactory.add("resizeImage", o), n.ui.componentFactory.add("imageResize", o);
  }
  /**
  * A helper function for creating an option label value string.
  *
  * @param option A resize option object.
  * @param forTooltip An optional flag for creating a tooltip label.
  * @returns A user-defined label combined from the numeric value and the resize unit or the default label
  * for reset options (`Original`).
  */
  _getOptionLabelValue(e, n = !1) {
    const r = this.editor.t;
    return e.label ? e.label : n ? Ga(e) ? r("Custom image size") : e.value ? r("Resize image to %0", e.value + this._resizeUnit) : r("Resize image to the original size") : Ga(e) ? r("Custom") : e.value ? e.value + this._resizeUnit : r("Original");
  }
  /**
  * A helper function that parses the resize options and returns list item definitions ready for use in the dropdown.
  *
  * @param options The resize options.
  * @param command The resize image command.
  * @returns Dropdown item definitions.
  */
  _getResizeDropdownListItemDefinitions(e, n) {
    const { editor: r } = this, s = new Pn(), o = e.map((a) => Ga(a) ? {
      ...a,
      valueWithUnits: "custom"
    } : a.value ? {
      ...a,
      valueWithUnits: `${a.value}${this._resizeUnit}`
    } : {
      ...a,
      valueWithUnits: null
    });
    for (const a of o) {
      let l = null;
      if (r.plugins.has("ImageCustomResizeUI") && Ga(a)) {
        const c = r.plugins.get("ImageCustomResizeUI");
        l = {
          type: "button",
          model: new ia({
            label: this._getOptionLabelValue(a),
            role: "menuitemradio",
            withText: !0,
            icon: null,
            onClick: () => {
              c._showForm(this._resizeUnit);
            }
          })
        };
        const u = I4(o, "valueWithUnits");
        l.model.bind("isOn").to(n, "value", O$(u));
      } else
        l = {
          type: "button",
          model: new ia({
            commandName: "resizeImage",
            commandValue: a.valueWithUnits,
            label: this._getOptionLabelValue(a),
            role: "menuitemradio",
            withText: !0,
            icon: null
          })
        }, l.model.bind("isOn").to(n, "value", Cm(a.valueWithUnits));
      l.model.bind("isEnabled").to(n, "isEnabled"), s.add(l);
    }
    return s;
  }
}
function Ga(i) {
  return i.value === "custom";
}
function Cm(i) {
  return (t) => {
    const e = t;
    return i === null && e === i ? !0 : e !== null && e.width === i;
  };
}
function O$(i) {
  return (t) => !i.some((e) => Cm(e)(t));
}
const V$ = "figure.image.ck-widget > img,figure.image.ck-widget > picture > img,figure.image.ck-widget > a > img,figure.image.ck-widget > a > picture > img,span.image-inline.ck-widget > img,span.image-inline.ck-widget > picture > img", wf = "image_resized";
class M$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      hf,
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageResizeHandles";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.commands.get("resizeImage");
    this.bind("isEnabled").to(t), this._setupResizerCreator();
  }
  /**
  * Attaches the listeners responsible for creating a resizer for each image, except for images inside the HTML embed preview.
  */
  _setupResizerCreator() {
    const t = this.editor, e = t.editing.view, n = t.plugins.get("ImageUtils");
    e.addObserver(Pp), this.listenTo(e.document, "imageLoaded", (r, s) => {
      if (!s.target.matches(V$))
        return;
      const o = t.editing.view.domConverter, a = o.domToView(s.target), l = n.getImageWidgetFromImageView(a);
      let c = this.editor.plugins.get(hf).getResizerByViewElement(l);
      if (c) {
        c.redraw();
        return;
      }
      const u = t.editing.mapper, d = u.toModelElement(l);
      c = t.plugins.get(hf).attachTo({
        unit: t.config.get("image.resizeUnit"),
        modelElement: d,
        viewElement: l,
        editor: t,
        getHandleHost(f) {
          return f.querySelector("img");
        },
        getResizeHost() {
          return o.mapViewToDom(u.toViewElement(d));
        },
        isCentered() {
          return d.getAttribute("imageStyle") == "alignCenter";
        },
        onCommit(f) {
          e.change((m) => {
            m.removeClass(wf, l);
          }), t.execute("resizeImage", {
            width: f
          });
        }
      }), c.on("updateSize", () => {
        l.hasClass(wf) || e.change((m) => {
          m.addClass(wf, l);
        });
        const f = d.name === "imageInline" ? a : l;
        f.getStyle("height") && e.change((m) => {
          m.removeStyle("height", f);
        });
      }), c.bind("isEnabled").to(this);
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Wk(i) {
  if (!i)
    return null;
  const [, t, e] = i.trim().match(/([.,\d]+)(%|px)$/) || [], n = Number.parseFloat(t);
  return Number.isNaN(n) ? null : {
    value: n,
    unit: e
  };
}
function Uk(i, t, e) {
  return e === "px" ? {
    value: t.value,
    unit: "px"
  } : {
    value: t.value / i * 100,
    unit: "%"
  };
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function qk(i) {
  const { editing: t } = i, n = i.plugins.get("ImageUtils").getClosestSelectedImageElement(i.model.document.selection);
  if (!n)
    return null;
  const r = t.mapper.toViewElement(n), s = t.view.domConverter.mapViewToDom(r);
  return {
    model: n,
    view: r,
    dom: s
  };
}
function D$(i, t) {
  const e = qk(i);
  if (!e)
    return null;
  const n = Wk(e.model.getAttribute("resizedWidth") || null);
  if (!n)
    return null;
  if (n.unit === t)
    return n;
  const r = _p(e.dom), s = {
    unit: "px",
    value: new je(e.dom).width
  };
  return Uk(r, s, t);
}
class L$ extends de {
  /**
  * @inheritDoc
  */
  constructor(e, n, r) {
    super(e);
    /**
    * Tracks information about the DOM focus in the form.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * Resize unit shortcut.
    */
    h(this, "unit");
    /**
    * An input with a label.
    */
    h(this, "labeledInput");
    /**
    * A button used to submit the form.
    */
    h(this, "saveButtonView");
    /**
    * A button used to cancel the form.
    */
    h(this, "cancelButtonView");
    /**
    * A collection of views which can be focused in the form.
    */
    h(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    h(this, "_focusCycler");
    /**
    * An array of form validators used by {@link #isValid}.
    */
    h(this, "_validators");
    const s = this.locale.t;
    this.focusTracker = new wt(), this.keystrokes = new Ot(), this.unit = n, this.labeledInput = this._createLabeledInputView(), this.saveButtonView = this._createButton(s("Save"), ee.check, "ck-button-save"), this.saveButtonView.type = "submit", this.cancelButtonView = this._createButton(s("Cancel"), ee.cancel, "ck-button-cancel", "cancel"), this._focusables = new In(), this._validators = r, this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-image-custom-resize-form",
          "ck-responsive-form"
        ],
        // https://github.com/ckeditor/ckeditor5-image/issues/40
        tabindex: "-1"
      },
      children: [
        this.labeledInput,
        this.saveButtonView,
        this.cancelButtonView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element), Cs({
      view: this
    }), [
      this.labeledInput,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((e) => {
      this._focusables.add(e), this.focusTracker.add(e.element);
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates the button view.
  *
  * @param label The button label
  * @param icon The button's icon.
  * @param className The additional button CSS class name.
  * @param eventName The event name that the ButtonView#execute event will be delegated to.
  * @returns The button view instance.
  */
  _createButton(e, n, r, s) {
    const o = new Te(this.locale);
    return o.set({
      label: e,
      icon: n,
      tooltip: !0
    }), o.extendTemplate({
      attributes: {
        class: r
      }
    }), s && o.delegate("execute").to(this, s), o;
  }
  /**
  * Creates an input with a label.
  *
  * @returns Labeled field view instance.
  */
  _createLabeledInputView() {
    const e = this.locale.t, n = new Qt(this.locale, XC);
    return n.label = e("Resize image (in %0)", this.unit), n.fieldView.set({
      step: 0.1
    }), n;
  }
  /**
  * Validates the form and returns `false` when some fields are invalid.
  */
  isValid() {
    this.resetFormStatus();
    for (const e of this._validators) {
      const n = e(this);
      if (n)
        return this.labeledInput.errorText = n, !1;
    }
    return !0;
  }
  /**
  * Cleans up the supplementary error and information text of the {@link #labeledInput}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetFormStatus() {
    this.labeledInput.errorText = null;
  }
  /**
  * The native DOM `value` of the input element of {@link #labeledInput}.
  */
  get rawSize() {
    const { element: e } = this.labeledInput.fieldView;
    return e ? e.value : null;
  }
  /**
  * Get numeric value of size. Returns `null` if value of size input element in {@link #labeledInput}.is not a number.
  */
  get parsedSize() {
    const { rawSize: e } = this;
    if (e === null)
      return null;
    const n = Number.parseFloat(e);
    return Number.isNaN(n) ? null : n;
  }
  /**
  * Returns serialized image input size with unit.
  * Returns `null` if value of size input element in {@link #labeledInput}.is not a number.
  */
  get sizeWithUnits() {
    const { parsedSize: e, unit: n } = this;
    return e === null ? null : `${e}${n}`;
  }
}
function N$(i, t) {
  const e = qk(i);
  if (!e)
    return null;
  const n = _p(e.dom), r = Wk(window.getComputedStyle(e.dom).minWidth) || {
    value: 1,
    unit: "px"
  }, s = Math.max(0.1, Uk(n, r, t).value);
  return {
    unit: t,
    lower: s,
    upper: t === "px" ? n : 100
  };
}
class B$ extends V {
  constructor() {
    super(...arguments);
    /**
    * The contextual balloon plugin instance.
    */
    h(this, "_balloon");
    /**
    * A form containing a textarea and buttons, used to change the `alt` text value.
    */
    h(this, "_form");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Qi
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCustomResizeUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._form && this._form.destroy();
  }
  /**
  * Creates the {@link module:image/imageresize/ui/imagecustomresizeformview~ImageCustomResizeFormView}
  * form.
  */
  _createForm(e) {
    const n = this.editor;
    this._balloon = this.editor.plugins.get("ContextualBalloon"), this._form = new (bc(L$))(n.locale, e, F$(n)), this._form.render(), this.listenTo(this._form, "submit", () => {
      this._form.isValid() && (n.execute("resizeImage", {
        width: this._form.sizeWithUnits
      }), this._hideForm(!0));
    }), this.listenTo(this._form.labeledInput, "change:errorText", () => {
      n.ui.update();
    }), this.listenTo(this._form, "cancel", () => {
      this._hideForm(!0);
    }), this._form.keystrokes.set("Esc", (r, s) => {
      this._hideForm(!0), s();
    }), _s({
      emitter: this._form,
      activator: () => this._isVisible,
      contextElements: () => [
        this._balloon.view.element
      ],
      callback: () => this._hideForm()
    });
  }
  /**
  * Shows the {@link #_form} in the {@link #_balloon}.
  *
  * @internal
  */
  _showForm(e) {
    if (this._isVisible)
      return;
    this._form || this._createForm(e);
    const n = this.editor, r = this._form.labeledInput;
    this._form.disableCssTransitions(), this._form.resetFormStatus(), this._isInBalloon || this._balloon.add({
      view: this._form,
      position: Sp(n)
    });
    const s = D$(n, e), o = s ? s.value.toFixed(1) : "", a = N$(n, e);
    r.fieldView.value = r.fieldView.element.value = o, a && Object.assign(r.fieldView, {
      min: a.lower.toFixed(1),
      max: Math.ceil(a.upper).toFixed(1)
    }), this._form.labeledInput.fieldView.select(), this._form.enableCssTransitions();
  }
  /**
  * Removes the {@link #_form} from the {@link #_balloon}.
  *
  * @param focusEditable Controls whether the editing view is focused afterwards.
  */
  _hideForm(e = !1) {
    this._isInBalloon && (this._form.focusTracker.isFocused && this._form.saveButtonView.focus(), this._balloon.remove(this._form), e && this.editor.editing.view.focus());
  }
  /**
  * Returns `true` when the {@link #_form} is the visible view in the {@link #_balloon}.
  */
  get _isVisible() {
    return !!this._balloon && this._balloon.visibleView === this._form;
  }
  /**
  * Returns `true` when the {@link #_form} is in the {@link #_balloon}.
  */
  get _isInBalloon() {
    return !!this._balloon && this._balloon.hasView(this._form);
  }
}
function F$(i) {
  const t = i.t;
  return [
    (e) => {
      if (e.rawSize.trim() === "")
        return t("The value must not be empty.");
      if (e.parsedSize === null)
        return t("The value should be a plain number.");
    }
  ];
}
class $$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jk,
      M$,
      B$,
      R$
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageResize";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class z$ extends Ve {
  /**
  * Creates an instance of the image style command. When executed, the command applies one of
  * {@link module:image/imageconfig~ImageStyleConfig#options style options} to the currently selected image.
  *
  * @param editor The editor instance.
  * @param styles The style options that this command supports.
  */
  constructor(e, n) {
    super(e);
    /**
    * An object containing names of default style options for the inline and block images.
    * If there is no default style option for the given image type in the configuration,
    * the name will be `false`.
    */
    h(this, "_defaultStyles");
    /**
    * The styles handled by this command.
    */
    h(this, "_styles");
    this._defaultStyles = {
      imageBlock: !1,
      imageInline: !1
    }, this._styles = new Map(n.map((r) => {
      if (r.isDefault)
        for (const s of r.modelElements)
          this._defaultStyles[s] = r.name;
      return [
        r.name,
        r
      ];
    }));
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const r = this.editor.plugins.get("ImageUtils").getClosestSelectedImageElement(this.editor.model.document.selection);
    this.isEnabled = !!r, this.isEnabled ? r.hasAttribute("imageStyle") ? this.value = r.getAttribute("imageStyle") : this.value = this._defaultStyles[r.name] : this.value = !1;
  }
  /**
  * Executes the command and applies the style to the currently selected image:
  *
  * ```ts
  * editor.execute( 'imageStyle', { value: 'side' } );
  * ```
  *
  * **Note**: Executing this command may change the image model element if the desired style requires an image
  * of a different type. Learn more about {@link module:image/imageconfig~ImageStyleOptionDefinition#modelElements model element}
  * configuration for the style option.
  *
  * @param options.value The name of the style (as configured in {@link module:image/imageconfig~ImageStyleConfig#options}).
  * @param options.setImageSizes Specifies whether the image `width` and `height` attributes should be set automatically.
  * The default is `true`.
  * @fires execute
  */
  execute(e = {}) {
    const n = this.editor, r = n.model, s = n.plugins.get("ImageUtils");
    r.change((o) => {
      const a = e.value, { setImageSizes: l = !0 } = e;
      let c = s.getClosestSelectedImageElement(r.document.selection);
      a && this.shouldConvertImageType(a, c) && (this.editor.execute(s.isBlockImage(c) ? "imageTypeInline" : "imageTypeBlock", {
        setImageSizes: l
      }), c = s.getClosestSelectedImageElement(r.document.selection)), !a || this._styles.get(a).isDefault ? o.removeAttribute("imageStyle", c) : o.setAttribute("imageStyle", a, c), l && s.setImageNaturalSizeAttributes(c);
    });
  }
  /**
  * Returns `true` if requested style change would trigger the image type change.
  *
  * @param requestedStyle The name of the style (as configured in {@link module:image/imageconfig~ImageStyleConfig#options}).
  * @param imageElement The image model element.
  */
  shouldConvertImageType(e, n) {
    return !this._styles.get(e).modelElements.includes(n.name);
  }
}
const xu = {
  // This style represents an image placed in the line of text.
  get inline() {
    return {
      name: "inline",
      title: "In line",
      icon: ee.objectInline,
      modelElements: [
        "imageInline"
      ],
      isDefault: !0
    };
  },
  // This style represents an image aligned to the left and wrapped with text.
  get alignLeft() {
    return {
      name: "alignLeft",
      title: "Left aligned image",
      icon: ee.objectLeft,
      modelElements: [
        "imageBlock",
        "imageInline"
      ],
      className: "image-style-align-left"
    };
  },
  // This style represents an image aligned to the left.
  get alignBlockLeft() {
    return {
      name: "alignBlockLeft",
      title: "Left aligned image",
      icon: ee.objectBlockLeft,
      modelElements: [
        "imageBlock"
      ],
      className: "image-style-block-align-left"
    };
  },
  // This style represents a centered image.
  get alignCenter() {
    return {
      name: "alignCenter",
      title: "Centered image",
      icon: ee.objectCenter,
      modelElements: [
        "imageBlock"
      ],
      className: "image-style-align-center"
    };
  },
  // This style represents an image aligned to the right and wrapped with text.
  get alignRight() {
    return {
      name: "alignRight",
      title: "Right aligned image",
      icon: ee.objectRight,
      modelElements: [
        "imageBlock",
        "imageInline"
      ],
      className: "image-style-align-right"
    };
  },
  // This style represents an image aligned to the right.
  get alignBlockRight() {
    return {
      name: "alignBlockRight",
      title: "Right aligned image",
      icon: ee.objectBlockRight,
      modelElements: [
        "imageBlock"
      ],
      className: "image-style-block-align-right"
    };
  },
  // This option is equal to the situation when no style is applied.
  get block() {
    return {
      name: "block",
      title: "Centered image",
      icon: ee.objectCenter,
      modelElements: [
        "imageBlock"
      ],
      isDefault: !0
    };
  },
  // This represents a side image.
  get side() {
    return {
      name: "side",
      title: "Side image",
      icon: ee.objectRight,
      modelElements: [
        "imageBlock"
      ],
      className: "image-style-side"
    };
  }
}, Gk = {
  full: ee.objectFullWidth,
  left: ee.objectBlockLeft,
  right: ee.objectBlockRight,
  center: ee.objectCenter,
  inlineLeft: ee.objectLeft,
  inlineRight: ee.objectRight,
  inline: ee.objectInline
}, Kk = [
  {
    name: "imageStyle:wrapText",
    title: "Wrap text",
    defaultItem: "imageStyle:alignLeft",
    items: [
      "imageStyle:alignLeft",
      "imageStyle:alignRight"
    ]
  },
  {
    name: "imageStyle:breakText",
    title: "Break text",
    defaultItem: "imageStyle:block",
    items: [
      "imageStyle:alignBlockLeft",
      "imageStyle:block",
      "imageStyle:alignBlockRight"
    ]
  }
];
function H$(i) {
  return (i.configuredStyles.options || []).map((n) => U$(n)).filter((n) => q$(n, i));
}
function j$(i, t) {
  return i && t ? {
    options: [
      "inline",
      "alignLeft",
      "alignRight",
      "alignCenter",
      "alignBlockLeft",
      "alignBlockRight",
      "block",
      "side"
    ]
  } : i ? {
    options: [
      "block",
      "side"
    ]
  } : t ? {
    options: [
      "inline",
      "alignLeft",
      "alignRight"
    ]
  } : {};
}
function W$(i) {
  return i.has("ImageBlockEditing") && i.has("ImageInlineEditing") ? [
    ...Kk
  ] : [];
}
function U$(i) {
  return typeof i == "string" ? xu[i] ? i = {
    ...xu[i]
  } : i = {
    name: i
  } : i = G$(xu[i.name], i), typeof i.icon == "string" && (i.icon = Gk[i.icon] || i.icon), i;
}
function q$(i, { isBlockPluginLoaded: t, isInlinePluginLoaded: e }) {
  const { modelElements: n, name: r } = i;
  if (!n || !n.length || !r)
    return Zk({
      style: i
    }), !1;
  {
    const s = [
      t ? "imageBlock" : null,
      e ? "imageInline" : null
    ];
    if (!n.some((o) => s.includes(o)))
      return Xe("image-style-missing-dependency", {
        style: i,
        missingPlugins: n.map((o) => o === "imageBlock" ? "ImageBlockEditing" : "ImageInlineEditing")
      }), !1;
  }
  return !0;
}
function G$(i, t) {
  const e = {
    ...t
  };
  for (const n in i)
    Object.prototype.hasOwnProperty.call(t, n) || (e[n] = i[n]);
  return e;
}
function Zk(i) {
  Xe("image-style-configuration-definition-invalid", i);
}
var km = {
  normalizeStyles: H$,
  getDefaultStylesConfiguration: j$,
  getDefaultDropdownDefinitions: W$,
  warnInvalidStyle: Zk,
  DEFAULT_OPTIONS: xu,
  DEFAULT_ICONS: Gk,
  DEFAULT_DROPDOWN_DEFINITIONS: Kk
};
function K$(i) {
  return (t, e, n) => {
    if (!n.consumable.consume(e.item, t.name))
      return;
    const r = Mw(e.attributeNewValue, i), s = Mw(e.attributeOldValue, i), o = n.mapper.toViewElement(e.item), a = n.writer;
    s && a.removeClass(s.className, o), r && a.addClass(r.className, o);
  };
}
function Z$(i) {
  const t = {
    imageInline: i.filter((e) => !e.isDefault && e.modelElements.includes("imageInline")),
    imageBlock: i.filter((e) => !e.isDefault && e.modelElements.includes("imageBlock"))
  };
  return (e, n, r) => {
    if (!n.modelRange)
      return;
    const s = n.viewItem, o = Ft(n.modelRange.getItems());
    if (o && r.schema.checkAttribute(o, "imageStyle"))
      for (const a of t[o.name])
        r.consumable.consume(s, {
          classes: a.className
        }) && r.writer.setAttribute("imageStyle", a.name, o);
  };
}
function Mw(i, t) {
  for (const e of t)
    if (e.name === i)
      return e;
}
class Yk extends V {
  constructor() {
    super(...arguments);
    /**
    * It contains a list of the normalized and validated style options.
    *
    * * Each option contains a complete icon markup.
    * * The style options not supported by any of the loaded image editing plugins (
    * {@link module:image/image/imageinlineediting~ImageInlineEditing `ImageInlineEditing`} or
    * {@link module:image/image/imageblockediting~ImageBlockEditing `ImageBlockEditing`}) are filtered out.
    *
    * @internal
    * @readonly
    */
    h(this, "normalizedStyles");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageStyleEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const { normalizeStyles: e, getDefaultStylesConfiguration: n } = km, r = this.editor, s = r.plugins.has("ImageBlockEditing"), o = r.plugins.has("ImageInlineEditing");
    r.config.define("image.styles", n(s, o)), this.normalizedStyles = e({
      configuredStyles: r.config.get("image.styles"),
      isBlockPluginLoaded: s,
      isInlinePluginLoaded: o
    }), this._setupConversion(s, o), this._setupPostFixer(), r.commands.add("imageStyle", new z$(r, this.normalizedStyles));
  }
  /**
  * Sets the editor conversion taking the presence of
  * {@link module:image/image/imageinlineediting~ImageInlineEditing `ImageInlineEditing`}
  * and {@link module:image/image/imageblockediting~ImageBlockEditing `ImageBlockEditing`} plugins into consideration.
  */
  _setupConversion(e, n) {
    const r = this.editor, s = r.model.schema, o = K$(this.normalizedStyles), a = Z$(this.normalizedStyles);
    r.editing.downcastDispatcher.on("attribute:imageStyle", o), r.data.downcastDispatcher.on("attribute:imageStyle", o), e && (s.extend("imageBlock", {
      allowAttributes: "imageStyle"
    }), r.data.upcastDispatcher.on("element:figure", a, {
      priority: "low"
    })), n && (s.extend("imageInline", {
      allowAttributes: "imageStyle"
    }), r.data.upcastDispatcher.on("element:img", a, {
      priority: "low"
    }));
  }
  /**
  * Registers a post-fixer that will make sure that the style attribute value is correct for a specific image type (block vs inline).
  */
  _setupPostFixer() {
    const e = this.editor, n = e.model.document, r = e.plugins.get(Hn), s = new Map(this.normalizedStyles.map((o) => [
      o.name,
      o
    ]));
    n.registerPostFixer((o) => {
      let a = !1;
      for (const l of n.differ.getChanges())
        if (l.type == "insert" || l.type == "attribute" && l.attributeKey == "imageStyle") {
          let c = l.type == "insert" ? l.position.nodeAfter : l.range.start.nodeAfter;
          if (c && c.is("element", "paragraph") && c.childCount > 0 && (c = c.getChild(0)), !r.isImage(c))
            continue;
          const u = c.getAttribute("imageStyle");
          if (!u)
            continue;
          const d = s.get(u);
          (!d || !d.modelElements.includes(c.name)) && (o.removeAttribute("imageStyle", c), a = !0);
        }
      return a;
    });
  }
}
class Y$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Yk
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageStyleUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Returns the default localized style titles provided by the plugin.
  *
  * The following localized titles corresponding with
  * {@link module:image/imagestyle/utils#DEFAULT_OPTIONS} are available:
  *
  * * `'Wrap text'`,
  * * `'Break text'`,
  * * `'In line'`,
  * * `'Full size image'`,
  * * `'Side image'`,
  * * `'Left aligned image'`,
  * * `'Centered image'`,
  * * `'Right aligned image'`
  */
  get localizedDefaultStylesTitles() {
    const t = this.editor.t;
    return {
      "Wrap text": t("Wrap text"),
      "Break text": t("Break text"),
      "In line": t("In line"),
      "Full size image": t("Full size image"),
      "Side image": t("Side image"),
      "Left aligned image": t("Left aligned image"),
      "Centered image": t("Centered image"),
      "Right aligned image": t("Right aligned image")
    };
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins, e = this.editor.config.get("image.toolbar") || [], n = t.get("ImageStyleEditing"), r = Dw(n.normalizedStyles, this.localizedDefaultStylesTitles);
    for (const o of r)
      this._createButton(o);
    const s = Dw([
      ...e.filter(vt),
      ...km.getDefaultDropdownDefinitions(t)
    ], this.localizedDefaultStylesTitles);
    for (const o of s)
      this._createDropdown(o, r);
  }
  /**
  * Creates a dropdown and stores it in the editor {@link module:ui/componentfactory~ComponentFactory}.
  */
  _createDropdown(t, e) {
    const n = this.editor.ui.componentFactory;
    n.add(t.name, (r) => {
      let s;
      const { defaultItem: o, items: a, title: l } = t, c = a.filter((m) => e.find(({ name: g }) => Lw(g) === m)).map((m) => {
        const g = n.create(m);
        return m === o && (s = g), g;
      });
      a.length !== c.length && km.warnInvalidStyle({
        dropdown: t
      });
      const u = Yn(r, io), d = u.buttonView, f = d.arrowView;
      return Zd(u, c, {
        enableActiveItemFocusOnDropdownOpen: !0
      }), d.set({
        label: Nw(l, s.label),
        class: null,
        tooltip: !0
      }), f.unbind("label"), f.set({
        label: l
      }), d.bind("icon").toMany(c, "isOn", (...m) => {
        const g = m.findIndex(es);
        return g < 0 ? s.icon : c[g].icon;
      }), d.bind("label").toMany(c, "isOn", (...m) => {
        const g = m.findIndex(es);
        return Nw(l, g < 0 ? s.label : c[g].label);
      }), d.bind("isOn").toMany(c, "isOn", (...m) => m.some(es)), d.bind("class").toMany(c, "isOn", (...m) => m.some(es) ? "ck-splitbutton_flatten" : void 0), d.on("execute", () => {
        c.some(({ isOn: m }) => m) ? u.isOpen = !u.isOpen : s.fire("execute");
      }), u.bind("isEnabled").toMany(c, "isEnabled", (...m) => m.some(es)), this.listenTo(u, "execute", () => {
        this.editor.editing.view.focus();
      }), u;
    });
  }
  /**
  * Creates a button and stores it in the editor {@link module:ui/componentfactory~ComponentFactory}.
  */
  _createButton(t) {
    const e = t.name;
    this.editor.ui.componentFactory.add(Lw(e), (n) => {
      const r = this.editor.commands.get("imageStyle"), s = new Te(n);
      return s.set({
        label: t.title,
        icon: t.icon,
        tooltip: !0,
        isToggleable: !0
      }), s.bind("isEnabled").to(r, "isEnabled"), s.bind("isOn").to(r, "value", (o) => o === e), s.on("execute", this._executeCommand.bind(this, e)), s;
    });
  }
  _executeCommand(t) {
    this.editor.execute("imageStyle", {
      value: t
    }), this.editor.editing.view.focus();
  }
}
function Dw(i, t) {
  for (const e of i)
    t[e.title] && (e.title = t[e.title]);
  return i;
}
function Lw(i) {
  return `imageStyle:${i}`;
}
function Nw(i, t) {
  return (i ? i + ": " : "") + t;
}
class J$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Yk,
      Y$
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageStyle";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class X$ extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Zu,
      Hn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.t, n = t.plugins.get(Zu), r = t.plugins.get("ImageUtils");
    n.register("image", {
      ariaLabel: e("Image toolbar"),
      items: Q$(t.config.get("image.toolbar") || []),
      getRelatedElement: (s) => r.getClosestSelectedImageWidget(s)
    });
  }
}
function Q$(i) {
  return i.map((t) => vt(t) ? t.name : t);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class ez {
  constructor() {
    /**
    * Stores the definition of {@link module:link/linkconfig~LinkDecoratorAutomaticDefinition automatic decorators}.
    * This data is used as a source for a downcast dispatcher to create a proper conversion to output data.
    */
    h(this, "_definitions", /* @__PURE__ */ new Set());
  }
  /**
  * Gives information about the number of decorators stored in the {@link module:link/utils/automaticdecorators~AutomaticDecorators}
  * instance.
  */
  get length() {
    return this._definitions.size;
  }
  /**
  * Adds automatic decorator objects or an array with them to be used during downcasting.
  *
  * @param item A configuration object of automatic rules for decorating links. It might also be an array of such objects.
  */
  add(t) {
    Array.isArray(t) ? t.forEach((e) => this._definitions.add(e)) : this._definitions.add(t);
  }
  /**
  * Provides the conversion helper used in the {@link module:engine/conversion/downcasthelpers~DowncastHelpers#add} method.
  *
  * @returns A dispatcher function used as conversion helper in {@link module:engine/conversion/downcasthelpers~DowncastHelpers#add}.
  */
  getDispatcher() {
    return (t) => {
      t.on("attribute:linkHref", (e, n, r) => {
        if (!r.consumable.test(n.item, "attribute:linkHref") || !(n.item.is("selection") || r.schema.isInline(n.item)))
          return;
        const s = r.writer, o = s.document.selection;
        for (const a of this._definitions) {
          const l = s.createAttributeElement("a", a.attributes, {
            priority: 5
          });
          a.classes && s.addClass(a.classes, l);
          for (const c in a.styles)
            s.setStyle(c, a.styles[c], l);
          s.setCustomProperty("link", !0, l), a.callback(n.attributeNewValue) ? n.item.is("selection") ? s.wrap(o.getFirstRange(), l) : s.wrap(r.mapper.toViewRange(n.range), l) : s.unwrap(r.mapper.toViewRange(n.range), l);
        }
      }, {
        priority: "high"
      });
    };
  }
  /**
  * Provides the conversion helper used in the {@link module:engine/conversion/downcasthelpers~DowncastHelpers#add} method
  * when linking images.
  *
  * @returns A dispatcher function used as conversion helper in {@link module:engine/conversion/downcasthelpers~DowncastHelpers#add}.
  */
  getDispatcherForLinkedImage() {
    return (t) => {
      t.on("attribute:linkHref:imageBlock", (e, n, { writer: r, mapper: s }) => {
        const o = s.toViewElement(n.item), a = Array.from(o.getChildren()).find((l) => l.is("element", "a"));
        if (a)
          for (const l of this._definitions) {
            const c = gr(l.attributes);
            if (l.callback(n.attributeNewValue)) {
              for (const [u, d] of c)
                u === "class" ? r.addClass(d, a) : r.setAttribute(u, d, a);
              l.classes && r.addClass(l.classes, a);
              for (const u in l.styles)
                r.setStyle(u, l.styles[u], a);
            } else {
              for (const [u, d] of c)
                u === "class" ? r.removeClass(d, a) : r.removeAttribute(u, a);
              l.classes && r.removeClass(l.classes, a);
              for (const u in l.styles)
                r.removeStyle(u, a);
            }
          }
      });
    };
  }
}
const tz = /[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205f\u3000]/g, nz = "^(?:(?:<protocols>):|[^a-z]|[a-z+.-]+(?:[^a-z+.:-]|$))", iz = /^[\S]+@((?![-_])(?:[-\w\u00a1-\uffff]{0,63}[^-_]\.))+(?:[a-z\u00a1-\uffff]{2,})$/i, rz = /^((\w+:(\/{2,})?)|(\W))/i, sz = [
  "https?",
  "ftps?",
  "mailto"
], nl = "Ctrl+K";
function oz(i) {
  return i.is("attributeElement") && !!i.getCustomProperty("link");
}
function Bw(i, { writer: t }) {
  const e = t.createAttributeElement("a", {
    href: i
  }, {
    priority: 5
  });
  return t.setCustomProperty("link", !0, e), e;
}
function Jk(i, t = sz) {
  const e = String(i), n = t.join("|"), r = new RegExp(`${nz.replace("<protocols>", n)}`, "i");
  return az(e, r) ? e : "#";
}
function az(i, t) {
  return !!i.replace(tz, "").match(t);
}
function lz(i, t) {
  const e = {
    "Open in a new tab": i("Open in a new tab"),
    Downloadable: i("Downloadable")
  };
  return t.forEach((n) => ("label" in n && e[n.label] && (n.label = e[n.label]), n)), t;
}
function cz(i) {
  const t = [];
  if (i)
    for (const [e, n] of Object.entries(i)) {
      const r = Object.assign({}, n, {
        id: `link${S_(e)}`
      });
      t.push(r);
    }
  return t;
}
function xm(i, t) {
  return i ? t.checkAttribute(i.name, "linkHref") : !1;
}
function uz(i) {
  return iz.test(i);
}
function Ip(i, t) {
  const e = uz(i) ? "mailto:" : t, n = !!e && !Xk(i);
  return i && n ? e + i : i;
}
function Xk(i) {
  return rz.test(i);
}
function Fw(i) {
  window.open(i, "_blank", "noopener");
}
class dz extends Ve {
  constructor() {
    super(...arguments);
    /**
    * A collection of {@link module:link/utils/manualdecorator~ManualDecorator manual decorators}
    * corresponding to the {@link module:link/linkconfig~LinkConfig#decorators decorator configuration}.
    *
    * You can consider it a model with states of manual decorators added to the currently selected link.
    */
    h(this, "manualDecorators", new Pn());
    /**
    * An instance of the helper that ties together all {@link module:link/linkconfig~LinkDecoratorAutomaticDefinition}
    * that are used by the {@glink features/link link} and the {@glink features/images/images-linking linking images} features.
    */
    h(this, "automaticDecorators", new ez());
  }
  /**
  * Synchronizes the state of {@link #manualDecorators} with the currently present elements in the model.
  */
  restoreManualDecoratorStates() {
    for (const e of this.manualDecorators)
      e.value = this._getDecoratorStateFromModel(e.id);
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model, n = e.document.selection, r = n.getSelectedElement() || Ft(n.getSelectedBlocks());
    xm(r, e.schema) ? (this.value = r.getAttribute("linkHref"), this.isEnabled = e.schema.checkAttribute(r, "linkHref")) : (this.value = n.getAttribute("linkHref"), this.isEnabled = e.schema.checkAttributeInSelection(n, "linkHref"));
    for (const s of this.manualDecorators)
      s.value = this._getDecoratorStateFromModel(s.id);
  }
  /**
  * Executes the command.
  *
  * When the selection is non-collapsed, the `linkHref` attribute will be applied to nodes inside the selection, but only to
  * those nodes where the `linkHref` attribute is allowed (disallowed nodes will be omitted).
  *
  * When the selection is collapsed and is not inside the text with the `linkHref` attribute, a
  * new {@link module:engine/model/text~Text text node} with the `linkHref` attribute will be inserted in place of the caret, but
  * only if such element is allowed in this place. The `_data` of the inserted text will equal the `href` parameter.
  * The selection will be updated to wrap the just inserted text node.
  *
  * When the selection is collapsed and inside the text with the `linkHref` attribute, the attribute value will be updated.
  *
  * # Decorators and model attribute management
  *
  * There is an optional argument to this command that applies or removes model
  * {@glink framework/architecture/editing-engine#text-attributes text attributes} brought by
  * {@link module:link/utils/manualdecorator~ManualDecorator manual link decorators}.
  *
  * Text attribute names in the model correspond to the entries in the {@link module:link/linkconfig~LinkConfig#decorators
  * configuration}.
  * For every decorator configured, a model text attribute exists with the "link" prefix. For example, a `'linkMyDecorator'` attribute
  * corresponds to `'myDecorator'` in the configuration.
  *
  * To learn more about link decorators, check out the {@link module:link/linkconfig~LinkConfig#decorators `config.link.decorators`}
  * documentation.
  *
  * Here is how to manage decorator attributes with the link command:
  *
  * ```ts
  * const linkCommand = editor.commands.get( 'link' );
  *
  * // Adding a new decorator attribute.
  * linkCommand.execute( 'http://example.com', {
  * 	linkIsExternal: true
  * } );
  *
  * // Removing a decorator attribute from the selection.
  * linkCommand.execute( 'http://example.com', {
  * 	linkIsExternal: false
  * } );
  *
  * // Adding multiple decorator attributes at the same time.
  * linkCommand.execute( 'http://example.com', {
  * 	linkIsExternal: true,
  * 	linkIsDownloadable: true,
  * } );
  *
  * // Removing and adding decorator attributes at the same time.
  * linkCommand.execute( 'http://example.com', {
  * 	linkIsExternal: false,
  * 	linkFoo: true,
  * 	linkIsDownloadable: false,
  * } );
  * ```
  *
  * **Note**: If the decorator attribute name is not specified, its state remains untouched.
  *
  * **Note**: {@link module:link/unlinkcommand~UnlinkCommand#execute `UnlinkCommand#execute()`} removes all
  * decorator attributes.
  *
  * @fires execute
  * @param href Link destination.
  * @param manualDecoratorIds The information about manual decorator attributes to be applied or removed upon execution.
  */
  execute(e, n = {}) {
    const r = this.editor.model, s = r.document.selection, o = [], a = [];
    for (const l in n)
      n[l] ? o.push(l) : a.push(l);
    r.change((l) => {
      if (s.isCollapsed) {
        const c = s.getFirstPosition();
        if (s.hasAttribute("linkHref")) {
          const u = $w(s);
          let d = Yd(c, "linkHref", s.getAttribute("linkHref"), r);
          s.getAttribute("linkHref") === u && (d = this._updateLinkContent(r, l, d, e)), l.setAttribute("linkHref", e, d), o.forEach((f) => {
            l.setAttribute(f, !0, d);
          }), a.forEach((f) => {
            l.removeAttribute(f, d);
          }), l.setSelection(l.createPositionAfter(d.end.nodeBefore));
        } else if (e !== "") {
          const u = gr(s.getAttributes());
          u.set("linkHref", e), o.forEach((f) => {
            u.set(f, !0);
          });
          const { end: d } = r.insertContent(l.createText(e, u), c);
          l.setSelection(d);
        }
        [
          "linkHref",
          ...o,
          ...a
        ].forEach((u) => {
          l.removeSelectionAttribute(u);
        });
      } else {
        const c = r.schema.getValidRanges(s.getRanges(), "linkHref"), u = [];
        for (const f of s.getSelectedBlocks())
          r.schema.checkAttribute(f, "linkHref") && u.push(l.createRangeOn(f));
        const d = u.slice();
        for (const f of c)
          this._isRangeToUpdate(f, u) && d.push(f);
        for (const f of d) {
          let m = f;
          if (d.length === 1) {
            const g = $w(s);
            s.getAttribute("linkHref") === g && (m = this._updateLinkContent(r, l, f, e), l.setSelection(l.createSelection(m)));
          }
          l.setAttribute("linkHref", e, m), o.forEach((g) => {
            l.setAttribute(g, !0, m);
          }), a.forEach((g) => {
            l.removeAttribute(g, m);
          });
        }
      }
    });
  }
  /**
  * Provides information whether a decorator with a given name is present in the currently processed selection.
  *
  * @param decoratorName The name of the manual decorator used in the model
  * @returns The information whether a given decorator is currently present in the selection.
  */
  _getDecoratorStateFromModel(e) {
    const n = this.editor.model, r = n.document.selection, s = r.getSelectedElement();
    return xm(s, n.schema) ? s.getAttribute(e) : r.getAttribute(e);
  }
  /**
  * Checks whether specified `range` is inside an element that accepts the `linkHref` attribute.
  *
  * @param range A range to check.
  * @param allowedRanges An array of ranges created on elements where the attribute is accepted.
  */
  _isRangeToUpdate(e, n) {
    for (const r of n)
      if (r.containsRange(e))
        return !1;
    return !0;
  }
  /**
  * Updates selected link with a new value as its content and as its href attribute.
  *
  * @param model Model is need to insert content.
  * @param writer Writer is need to create text element in model.
  * @param range A range where should be inserted content.
  * @param href A link value which should be in the href attribute and in the content.
  */
  _updateLinkContent(e, n, r, s) {
    const o = n.createText(s, {
      linkHref: s
    });
    return e.insertContent(o, r);
  }
}
function $w(i) {
  if (i.isCollapsed) {
    const t = i.getFirstPosition();
    return t.textNode && t.textNode.data;
  } else {
    const t = Array.from(i.getFirstRange().getItems());
    if (t.length > 1)
      return null;
    const e = t[0];
    return e.is("$text") || e.is("$textProxy") ? e.data : null;
  }
}
class hz extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document.selection, n = e.getSelectedElement();
    xm(n, t.schema) ? this.isEnabled = t.schema.checkAttribute(n, "linkHref") : this.isEnabled = t.schema.checkAttributeInSelection(e, "linkHref");
  }
  /**
  * Executes the command.
  *
  * When the selection is collapsed, it removes the `linkHref` attribute from each node with the same `linkHref` attribute value.
  * When the selection is non-collapsed, it removes the `linkHref` attribute from each node in selected ranges.
  *
  * # Decorators
  *
  * If {@link module:link/linkconfig~LinkConfig#decorators `config.link.decorators`} is specified,
  * all configured decorators are removed together with the `linkHref` attribute.
  *
  * @fires execute
  */
  execute() {
    const t = this.editor, e = this.editor.model, n = e.document.selection, r = t.commands.get("link");
    e.change((s) => {
      const o = n.isCollapsed ? [
        Yd(n.getFirstPosition(), "linkHref", n.getAttribute("linkHref"), e)
      ] : e.schema.getValidRanges(n.getRanges(), "linkHref");
      for (const a of o)
        if (s.removeAttribute("linkHref", a), r)
          for (const l of r.manualDecorators)
            s.removeAttribute(l.id, a);
    });
  }
}
class fz extends (/* @__PURE__ */ st()) {
  /**
  * Creates a new instance of {@link module:link/utils/manualdecorator~ManualDecorator}.
  *
  * @param config.id The name of the attribute used in the model that represents a given manual decorator.
  * For example: `'linkIsExternal'`.
  * @param config.label The label used in the user interface to toggle the manual decorator.
  * @param config.attributes A set of attributes added to output data when the decorator is active for a specific link.
  * Attributes should keep the format of attributes defined in {@link module:engine/view/elementdefinition~ElementDefinition}.
  * @param [config.defaultValue] Controls whether the decorator is "on" by default.
  */
  constructor({ id: e, label: n, attributes: r, classes: s, styles: o, defaultValue: a }) {
    super();
    /**
    * An ID of a manual decorator which is the name of the attribute in the model, for example: 'linkManualDecorator0'.
    */
    h(this, "id");
    /**
    * The default value of manual decorator.
    */
    h(this, "defaultValue");
    /**
    * The label used in the user interface to toggle the manual decorator.
    */
    h(this, "label");
    /**
    * A set of attributes added to downcasted data when the decorator is activated for a specific link.
    * Attributes should be added in a form of attributes defined in {@link module:engine/view/elementdefinition~ElementDefinition}.
    */
    h(this, "attributes");
    /**
    * A set of classes added to downcasted data when the decorator is activated for a specific link.
    * Classes should be added in a form of classes defined in {@link module:engine/view/elementdefinition~ElementDefinition}.
    */
    h(this, "classes");
    /**
    * A set of styles added to downcasted data when the decorator is activated for a specific link.
    * Styles should be added in a form of styles defined in {@link module:engine/view/elementdefinition~ElementDefinition}.
    */
    h(this, "styles");
    this.id = e, this.set("value", void 0), this.defaultValue = a, this.label = n, this.attributes = r, this.classes = s, this.styles = o;
  }
  /**
  * Returns {@link module:engine/view/matcher~MatcherPattern} with decorator attributes.
  *
  * @internal
  */
  _createPattern() {
    return {
      attributes: this.attributes,
      classes: this.classes,
      styles: this.styles
    };
  }
}
const mz = "ck-link_selected", zw = "automatic", gz = "manual", pz = /^(https?:)?\/\//;
class th extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Gu,
      ck,
      ui
    ];
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("link", {
      allowCreatingEmptyLinks: !1,
      addTargetToExternalLinks: !1
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.config.get("link.allowedProtocols");
    t.model.schema.extend("$text", {
      allowAttributes: "linkHref"
    }), t.conversion.for("dataDowncast").attributeToElement({
      model: "linkHref",
      view: Bw
    }), t.conversion.for("editingDowncast").attributeToElement({
      model: "linkHref",
      view: (s, o) => Bw(Jk(s, e), o)
    }), t.conversion.for("upcast").elementToAttribute({
      view: {
        name: "a",
        attributes: {
          href: !0
        }
      },
      model: {
        key: "linkHref",
        value: (s) => s.getAttribute("href")
      }
    }), t.commands.add("link", new dz(t)), t.commands.add("unlink", new hz(t));
    const n = lz(t.t, cz(t.config.get("link.decorators")));
    this._enableAutomaticDecorators(n.filter((s) => s.mode === zw)), this._enableManualDecorators(n.filter((s) => s.mode === gz)), t.plugins.get(Gu).registerAttribute("linkHref"), fk(t, "linkHref", "a", mz), this._enableLinkOpen(), this._enableSelectionAttributesFixer(), this._enableClipboardIntegration();
  }
  /**
  * Processes an array of configured {@link module:link/linkconfig~LinkDecoratorAutomaticDefinition automatic decorators}
  * and registers a {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher downcast dispatcher}
  * for each one of them. Downcast dispatchers are obtained using the
  * {@link module:link/utils/automaticdecorators~AutomaticDecorators#getDispatcher} method.
  *
  * **Note**: This method also activates the automatic external link decorator if enabled with
  * {@link module:link/linkconfig~LinkConfig#addTargetToExternalLinks `config.link.addTargetToExternalLinks`}.
  */
  _enableAutomaticDecorators(t) {
    const e = this.editor, r = e.commands.get("link").automaticDecorators;
    e.config.get("link.addTargetToExternalLinks") && r.add({
      id: "linkIsExternal",
      mode: zw,
      callback: (s) => !!s && pz.test(s),
      attributes: {
        target: "_blank",
        rel: "noopener noreferrer"
      }
    }), r.add(t), r.length && e.conversion.for("downcast").add(r.getDispatcher());
  }
  /**
  * Processes an array of configured {@link module:link/linkconfig~LinkDecoratorManualDefinition manual decorators},
  * transforms them into {@link module:link/utils/manualdecorator~ManualDecorator} instances and stores them in the
  * {@link module:link/linkcommand~LinkCommand#manualDecorators} collection (a model for manual decorators state).
  *
  * Also registers an {@link module:engine/conversion/downcasthelpers~DowncastHelpers#attributeToElement attribute-to-element}
  * converter for each manual decorator and extends the {@link module:engine/model/schema~Schema model's schema}
  * with adequate model attributes.
  */
  _enableManualDecorators(t) {
    if (!t.length)
      return;
    const e = this.editor, r = e.commands.get("link").manualDecorators;
    t.forEach((s) => {
      e.model.schema.extend("$text", {
        allowAttributes: s.id
      });
      const o = new fz(s);
      r.add(o), e.conversion.for("downcast").attributeToElement({
        model: o.id,
        view: (a, { writer: l, schema: c }, { item: u }) => {
          if ((u.is("selection") || c.isInline(u)) && a) {
            const d = l.createAttributeElement("a", o.attributes, {
              priority: 5
            });
            o.classes && l.addClass(o.classes, d);
            for (const f in o.styles)
              l.setStyle(f, o.styles[f], d);
            return l.setCustomProperty("link", !0, d), d;
          }
        }
      }), e.conversion.for("upcast").elementToAttribute({
        view: {
          name: "a",
          ...o._createPattern()
        },
        model: {
          key: o.id
        }
      });
    });
  }
  /**
  * Attaches handlers for {@link module:engine/view/document~Document#event:enter} and
  * {@link module:engine/view/document~Document#event:click} to enable link following.
  */
  _enableLinkOpen() {
    const t = this.editor, n = t.editing.view.document;
    this.listenTo(n, "click", (r, s) => {
      if (!(Me.isMac ? s.domEvent.metaKey : s.domEvent.ctrlKey))
        return;
      let a = s.domTarget;
      if (a.tagName.toLowerCase() != "a" && (a = a.closest("a")), !a)
        return;
      const l = a.getAttribute("href");
      l && (r.stop(), s.preventDefault(), Fw(l));
    }, {
      context: "$capture"
    }), this.listenTo(n, "keydown", (r, s) => {
      const a = t.commands.get("link").value;
      a && s.keyCode === Ke.enter && s.altKey && (r.stop(), Fw(a));
    });
  }
  /**
  * Watches the DocumentSelection attribute changes and removes link decorator attributes when the linkHref attribute is removed.
  *
  * This is to ensure that there is no left-over link decorator attributes on the document selection that is no longer in a link.
  */
  _enableSelectionAttributesFixer() {
    const e = this.editor.model, n = e.document.selection;
    this.listenTo(n, "change:attribute", (r, { attributeKeys: s }) => {
      !s.includes("linkHref") || n.hasAttribute("linkHref") || e.change((o) => {
        bz(o, vz(e.schema));
      });
    });
  }
  /**
  * Enables URL fixing on pasting.
  */
  _enableClipboardIntegration() {
    const t = this.editor, e = t.model, n = this.editor.config.get("link.defaultProtocol");
    n && this.listenTo(t.plugins.get("ClipboardPipeline"), "contentInsertion", (r, s) => {
      e.change((o) => {
        const a = o.createRangeIn(s.content);
        for (const l of a.getItems())
          if (l.hasAttribute("linkHref")) {
            const c = Ip(l.getAttribute("linkHref"), n);
            o.setAttribute("linkHref", c, l);
          }
      });
    });
  }
}
function bz(i, t) {
  i.removeSelectionAttribute("linkHref");
  for (const e of t)
    i.removeSelectionAttribute(e);
}
function vz(i) {
  return i.getDefinition("$text").allowAttributes.filter((e) => e.startsWith("link"));
}
class wz extends de {
  /**
  * Creates an instance of the {@link module:link/ui/linkformview~LinkFormView} class.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  * @param linkCommand Reference to {@link module:link/linkcommand~LinkCommand}.
  * @param validators  Form validators used by {@link #isValid}.
  */
  constructor(e, n, r) {
    super(e);
    /**
    * Tracks information about DOM focus in the form.
    */
    h(this, "focusTracker", new wt());
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes", new Ot());
    /**
    * The URL input view.
    */
    h(this, "urlInputView");
    /**
    * The Save button view.
    */
    h(this, "saveButtonView");
    /**
    * The Cancel button view.
    */
    h(this, "cancelButtonView");
    /**
    * A collection of {@link module:ui/button/switchbuttonview~SwitchButtonView},
    * which corresponds to {@link module:link/linkcommand~LinkCommand#manualDecorators manual decorators}
    * configured in the editor.
    */
    h(this, "_manualDecoratorSwitches");
    /**
    * A collection of child views in the form.
    */
    h(this, "children");
    /**
    * An array of form validators used by {@link #isValid}.
    */
    h(this, "_validators");
    /**
    * A collection of views that can be focused in the form.
    */
    h(this, "_focusables", new In());
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    h(this, "_focusCycler");
    const s = e.t;
    this._validators = r, this.urlInputView = this._createUrlInput(), this.saveButtonView = this._createButton(s("Save"), ee.check, "ck-button-save"), this.saveButtonView.type = "submit", this.cancelButtonView = this._createButton(s("Cancel"), ee.cancel, "ck-button-cancel", "cancel"), this._manualDecoratorSwitches = this._createManualDecoratorSwitches(n), this.children = this._createFormChildren(n.manualDecorators), this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    });
    const o = [
      "ck",
      "ck-link-form",
      "ck-responsive-form"
    ];
    n.manualDecorators.length && o.push("ck-link-form_layout-vertical", "ck-vertical-form"), this.setTemplate({
      tag: "form",
      attributes: {
        class: o,
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * Obtains the state of the {@link module:ui/button/switchbuttonview~SwitchButtonView switch buttons} representing
  * {@link module:link/linkcommand~LinkCommand#manualDecorators manual link decorators}
  * in the {@link module:link/ui/linkformview~LinkFormView}.
  *
  * @returns Key-value pairs, where the key is the name of the decorator and the value is its state.
  */
  getDecoratorSwitchesState() {
    return Array.from(this._manualDecoratorSwitches).reduce((e, n) => (e[n.name] = n.isOn, e), {});
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Cs({
      view: this
    }), [
      this.urlInputView,
      ...this._manualDecoratorSwitches,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((n) => {
      this._focusables.add(n), this.focusTracker.add(n.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist {@link #_focusables} in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Validates the form and returns `false` when some fields are invalid.
  */
  isValid() {
    this.resetFormStatus();
    for (const e of this._validators) {
      const n = e(this);
      if (n)
        return this.urlInputView.errorText = n, !1;
    }
    return !0;
  }
  /**
  * Cleans up the supplementary error and information text of the {@link #urlInputView}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetFormStatus() {
    this.urlInputView.errorText = null;
  }
  /**
  * Creates a labeled input view.
  *
  * @returns Labeled field view instance.
  */
  _createUrlInput() {
    const e = this.locale.t, n = new Qt(this.locale, yi);
    return n.fieldView.inputMode = "url", n.label = e("Link URL"), n;
  }
  /**
  * Creates a button view.
  *
  * @param label The button label.
  * @param icon The button icon.
  * @param className The additional button CSS class name.
  * @param eventName An event name that the `ButtonView#execute` event will be delegated to.
  * @returns The button view instance.
  */
  _createButton(e, n, r, s) {
    const o = new Te(this.locale);
    return o.set({
      label: e,
      icon: n,
      tooltip: !0
    }), o.extendTemplate({
      attributes: {
        class: r
      }
    }), s && o.delegate("execute").to(this, s), o;
  }
  /**
  * Populates {@link module:ui/viewcollection~ViewCollection} of {@link module:ui/button/switchbuttonview~SwitchButtonView}
  * made based on {@link module:link/linkcommand~LinkCommand#manualDecorators}.
  *
  * @param linkCommand A reference to the link command.
  * @returns ViewCollection of switch buttons.
  */
  _createManualDecoratorSwitches(e) {
    const n = this.createCollection();
    for (const r of e.manualDecorators) {
      const s = new Gs(this.locale);
      s.set({
        name: r.id,
        label: r.label,
        withText: !0
      }), s.bind("isOn").toMany([
        r,
        e
      ], "value", (o, a) => a === void 0 && o === void 0 ? !!r.defaultValue : !!o), s.on("execute", () => {
        r.set("value", !s.isOn);
      }), n.add(s);
    }
    return n;
  }
  /**
  * Populates the {@link #children} collection of the form.
  *
  * If {@link module:link/linkcommand~LinkCommand#manualDecorators manual decorators} are configured in the editor, it creates an
  * additional `View` wrapping all {@link #_manualDecoratorSwitches} switch buttons corresponding
  * to these decorators.
  *
  * @param manualDecorators A reference to
  * the collection of manual decorators stored in the link command.
  * @returns The children of link form view.
  */
  _createFormChildren(e) {
    const n = this.createCollection();
    if (n.add(this.urlInputView), e.length) {
      const r = new de();
      r.setTemplate({
        tag: "ul",
        children: this._manualDecoratorSwitches.map((s) => ({
          tag: "li",
          children: [
            s
          ],
          attributes: {
            class: [
              "ck",
              "ck-list__item"
            ]
          }
        })),
        attributes: {
          class: [
            "ck",
            "ck-reset",
            "ck-list"
          ]
        }
      }), n.add(r);
    }
    return n.add(this.saveButtonView), n.add(this.cancelButtonView), n;
  }
  /**
  * The native DOM `value` of the {@link #urlInputView} element.
  *
  * **Note**: Do not confuse it with the {@link module:ui/inputtext/inputtextview~InputTextView#value}
  * which works one way only and may not represent the actual state of the component in the DOM.
  */
  get url() {
    const { element: e } = this.urlInputView.fieldView;
    return e ? e.value.trim() : null;
  }
}
var yz = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.077 15 .991-1.416a.75.75 0 1 1 1.229.86l-1.148 1.64a.748.748 0 0 1-.217.206 5.251 5.251 0 0 1-8.503-5.955.741.741 0 0 1 .12-.274l1.147-1.639a.75.75 0 1 1 1.228.86L4.933 10.7l.006.003a3.75 3.75 0 0 0 6.132 4.294l.006.004zm5.494-5.335a.748.748 0 0 1-.12.274l-1.147 1.639a.75.75 0 1 1-1.228-.86l.86-1.23a3.75 3.75 0 0 0-6.144-4.301l-.86 1.229a.75.75 0 0 1-1.229-.86l1.148-1.64a.748.748 0 0 1 .217-.206 5.251 5.251 0 0 1 8.503 5.955zm-4.563-2.532a.75.75 0 0 1 .184 1.045l-3.155 4.505a.75.75 0 1 1-1.229-.86l3.155-4.506a.75.75 0 0 1 1.045-.184zm4.919 10.562-1.414 1.414a.75.75 0 1 1-1.06-1.06l1.414-1.415-1.415-1.414a.75.75 0 0 1 1.061-1.06l1.414 1.414 1.414-1.415a.75.75 0 0 1 1.061 1.061l-1.414 1.414 1.414 1.415a.75.75 0 0 1-1.06 1.06l-1.415-1.414z"/></svg>';
class _z extends de {
  /**
  * @inheritDoc
  */
  constructor(e, n = {}) {
    super(e);
    /**
    * Tracks information about DOM focus in the actions.
    */
    h(this, "focusTracker", new wt());
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes", new Ot());
    /**
    * The href preview view.
    */
    h(this, "previewButtonView");
    /**
    * The unlink button view.
    */
    h(this, "unlinkButtonView");
    /**
    * The edit link button view.
    */
    h(this, "editButtonView");
    /**
    * A collection of views that can be focused in the view.
    */
    h(this, "_focusables", new In());
    /**
    * Helps cycling over {@link #_focusables} in the view.
    */
    h(this, "_focusCycler");
    h(this, "_linkConfig");
    const r = e.t;
    this.previewButtonView = this._createPreviewButton(), this.unlinkButtonView = this._createButton(r("Unlink"), yz, "unlink"), this.editButtonView = this._createButton(r("Edit link"), ee.pencil, "edit"), this.set("href", void 0), this._linkConfig = n, this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-link-actions",
          "ck-responsive-form"
        ],
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: [
        this.previewButtonView,
        this.editButtonView,
        this.unlinkButtonView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), [
      this.previewButtonView,
      this.editButtonView,
      this.unlinkButtonView
    ].forEach((n) => {
      this._focusables.add(n), this.focusTracker.add(n.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist {@link #_focusables} in the actions.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Creates a button view.
  *
  * @param label The button label.
  * @param icon The button icon.
  * @param eventName An event name that the `ButtonView#execute` event will be delegated to.
  * @returns The button view instance.
  */
  _createButton(e, n, r) {
    const s = new Te(this.locale);
    return s.set({
      label: e,
      icon: n,
      tooltip: !0
    }), s.delegate("execute").to(this, r), s;
  }
  /**
  * Creates a link href preview button.
  *
  * @returns The button view instance.
  */
  _createPreviewButton() {
    const e = new Te(this.locale), n = this.bindTemplate, r = this.t;
    return e.set({
      withText: !0,
      tooltip: r("Open link in new tab")
    }), e.extendTemplate({
      attributes: {
        class: [
          "ck",
          "ck-link-actions__preview"
        ],
        href: n.to("href", (s) => s && Jk(s, this._linkConfig.allowedProtocols)),
        target: "_blank",
        rel: "noopener noreferrer"
      }
    }), e.bind("label").to(this, "href", (s) => s || r("This link has no URL")), e.bind("isEnabled").to(this, "href", (s) => !!s), e.template.tag = "a", e.template.eventListeners = {}, e;
  }
}
var Qk = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.077 15 .991-1.416a.75.75 0 1 1 1.229.86l-1.148 1.64a.748.748 0 0 1-.217.206 5.251 5.251 0 0 1-8.503-5.955.741.741 0 0 1 .12-.274l1.147-1.639a.75.75 0 1 1 1.228.86L4.933 10.7l.006.003a3.75 3.75 0 0 0 6.132 4.294l.006.004zm5.494-5.335a.748.748 0 0 1-.12.274l-1.147 1.639a.75.75 0 1 1-1.228-.86l.86-1.23a3.75 3.75 0 0 0-6.144-4.301l-.86 1.229a.75.75 0 0 1-1.229-.86l1.148-1.64a.748.748 0 0 1 .217-.206 5.251 5.251 0 0 1 8.503 5.955zm-4.563-2.532a.75.75 0 0 1 .184 1.045l-3.155 4.505a.75.75 0 1 1-1.229-.86l3.155-4.506a.75.75 0 0 1 1.045-.184z"/></svg>';
const lr = "link-ui";
class ex extends V {
  constructor() {
    super(...arguments);
    /**
    * The actions view displayed inside of the balloon.
    */
    h(this, "actionsView", null);
    /**
    * The form view displayed inside the balloon.
    */
    h(this, "formView", null);
    /**
    * The contextual balloon plugin instance.
    */
    h(this, "_balloon");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Qi
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = this.editor.t;
    e.editing.view.addObserver(x8), this._balloon = e.plugins.get(Qi), this._createToolbarLinkButton(), this._enableBalloonActivators(), e.conversion.for("editingDowncast").markerToHighlight({
      model: lr,
      view: {
        classes: [
          "ck-fake-link-selection"
        ]
      }
    }), e.conversion.for("editingDowncast").markerToElement({
      model: lr,
      view: (r, { writer: s }) => {
        if (!r.markerRange.isCollapsed)
          return null;
        const o = s.createUIElement("span");
        return s.addClass([
          "ck-fake-link-selection",
          "ck-fake-link-selection_collapsed"
        ], o), o;
      }
    }), e.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: n("Create link"),
          keystroke: nl
        },
        {
          label: n("Move out of a link"),
          keystroke: [
            [
              "arrowleft",
              "arrowleft"
            ],
            [
              "arrowright",
              "arrowright"
            ]
          ]
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.formView && this.formView.destroy(), this.actionsView && this.actionsView.destroy();
  }
  /**
  * Creates views.
  */
  _createViews() {
    this.actionsView = this._createActionsView(), this.formView = this._createFormView(), this._enableUserBalloonInteractions();
  }
  /**
  * Creates the {@link module:link/ui/linkactionsview~LinkActionsView} instance.
  */
  _createActionsView() {
    const e = this.editor, n = new _z(e.locale, e.config.get("link")), r = e.commands.get("link"), s = e.commands.get("unlink");
    return n.bind("href").to(r, "value"), n.editButtonView.bind("isEnabled").to(r), n.unlinkButtonView.bind("isEnabled").to(s), this.listenTo(n, "edit", () => {
      this._addFormView();
    }), this.listenTo(n, "unlink", () => {
      e.execute("unlink"), this._hideUI();
    }), n.keystrokes.set("Esc", (o, a) => {
      this._hideUI(), a();
    }), n.keystrokes.set(nl, (o, a) => {
      this._addFormView(), a();
    }), n;
  }
  /**
  * Creates the {@link module:link/ui/linkformview~LinkFormView} instance.
  */
  _createFormView() {
    const e = this.editor, n = e.commands.get("link"), r = e.config.get("link.defaultProtocol"), s = new (bc(wz))(e.locale, n, Cz(e));
    return s.urlInputView.fieldView.bind("value").to(n, "value"), s.urlInputView.bind("isEnabled").to(n, "isEnabled"), s.saveButtonView.bind("isEnabled").to(n, "isEnabled"), this.listenTo(s, "submit", () => {
      if (s.isValid()) {
        const { value: o } = s.urlInputView.fieldView.element, a = Ip(o, r);
        e.execute("link", a, s.getDecoratorSwitchesState()), this._closeFormView();
      }
    }), this.listenTo(s.urlInputView, "change:errorText", () => {
      e.ui.update();
    }), this.listenTo(s, "cancel", () => {
      this._closeFormView();
    }), s.keystrokes.set("Esc", (o, a) => {
      this._closeFormView(), a();
    }), s;
  }
  /**
  * Creates a toolbar Link button. Clicking this button will show
  * a {@link #_balloon} attached to the selection.
  */
  _createToolbarLinkButton() {
    const e = this.editor;
    e.ui.componentFactory.add("link", () => {
      const n = this._createButton(Te);
      return n.set({
        tooltip: !0
      }), n;
    }), e.ui.componentFactory.add("menuBar:link", () => {
      const n = this._createButton(At);
      return n.set({
        role: "menuitemcheckbox"
      }), n;
    });
  }
  /**
  * Creates a button for link command to use either in toolbar or in menu bar.
  */
  _createButton(e) {
    const n = this.editor, r = n.locale, s = n.commands.get("link"), o = new e(n.locale), a = r.t;
    return o.set({
      label: a("Link"),
      icon: Qk,
      keystroke: nl,
      isToggleable: !0
    }), o.bind("isEnabled").to(s, "isEnabled"), o.bind("isOn").to(s, "value", (l) => !!l), this.listenTo(o, "execute", () => this._showUI(!0)), o;
  }
  /**
  * Attaches actions that control whether the balloon panel containing the
  * {@link #formView} should be displayed.
  */
  _enableBalloonActivators() {
    const e = this.editor, n = e.editing.view.document;
    this.listenTo(n, "click", () => {
      this._getSelectedLinkElement() && this._showUI();
    }), e.keystrokes.set(nl, (r, s) => {
      s(), e.commands.get("link").isEnabled && this._showUI(!0);
    });
  }
  /**
  * Attaches actions that control whether the balloon panel containing the
  * {@link #formView} is visible or not.
  */
  _enableUserBalloonInteractions() {
    this.editor.keystrokes.set("Tab", (e, n) => {
      this._areActionsVisible && !this.actionsView.focusTracker.isFocused && (this.actionsView.focus(), n());
    }, {
      // Use the high priority because the link UI navigation is more important
      // than other feature's actions, e.g. list indentation.
      // https://github.com/ckeditor/ckeditor5-link/issues/146
      priority: "high"
    }), this.editor.keystrokes.set("Esc", (e, n) => {
      this._isUIVisible && (this._hideUI(), n());
    }), _s({
      emitter: this.formView,
      activator: () => this._isUIInPanel,
      contextElements: () => [
        this._balloon.view.element
      ],
      callback: () => this._hideUI()
    });
  }
  /**
  * Adds the {@link #actionsView} to the {@link #_balloon}.
  *
  * @internal
  */
  _addActionsView() {
    this.actionsView || this._createViews(), !this._areActionsInPanel && this._balloon.add({
      view: this.actionsView,
      position: this._getBalloonPositionData()
    });
  }
  /**
  * Adds the {@link #formView} to the {@link #_balloon}.
  */
  _addFormView() {
    if (this.formView || this._createViews(), this._isFormInPanel)
      return;
    const n = this.editor.commands.get("link");
    this.formView.disableCssTransitions(), this.formView.resetFormStatus(), this._balloon.add({
      view: this.formView,
      position: this._getBalloonPositionData()
    }), this.formView.urlInputView.fieldView.value = n.value || "", this._balloon.visibleView === this.formView && this.formView.urlInputView.fieldView.select(), this.formView.enableCssTransitions();
  }
  /**
  * Closes the form view. Decides whether the balloon should be hidden completely or if the action view should be shown. This is
  * decided upon the link command value (which has a value if the document selection is in the link).
  *
  * Additionally, if any {@link module:link/linkconfig~LinkConfig#decorators} are defined in the editor configuration, the state of
  * switch buttons responsible for manual decorator handling is restored.
  */
  _closeFormView() {
    const e = this.editor.commands.get("link");
    e.restoreManualDecoratorStates(), e.value !== void 0 ? this._removeFormView() : this._hideUI();
  }
  /**
  * Removes the {@link #formView} from the {@link #_balloon}.
  */
  _removeFormView() {
    this._isFormInPanel && (this.formView.saveButtonView.focus(), this.formView.urlInputView.fieldView.reset(), this._balloon.remove(this.formView), this.editor.editing.view.focus(), this._hideFakeVisualSelection());
  }
  /**
  * Shows the correct UI type. It is either {@link #formView} or {@link #actionsView}.
  *
  * @internal
  */
  _showUI(e = !1) {
    this.formView || this._createViews(), this._getSelectedLinkElement() ? (this._areActionsVisible ? this._addFormView() : this._addActionsView(), e && this._balloon.showStack("main")) : (this._showFakeVisualSelection(), this._addActionsView(), e && this._balloon.showStack("main"), this._addFormView()), this._startUpdatingUI();
  }
  /**
  * Removes the {@link #formView} from the {@link #_balloon}.
  *
  * See {@link #_addFormView}, {@link #_addActionsView}.
  */
  _hideUI() {
    if (!this._isUIInPanel)
      return;
    const e = this.editor;
    this.stopListening(e.ui, "update"), this.stopListening(this._balloon, "change:visibleView"), e.editing.view.focus(), this._removeFormView(), this._balloon.remove(this.actionsView), this._hideFakeVisualSelection();
  }
  /**
  * Makes the UI react to the {@link module:ui/editorui/editorui~EditorUI#event:update} event to
  * reposition itself when the editor UI should be refreshed.
  *
  * See: {@link #_hideUI} to learn when the UI stops reacting to the `update` event.
  */
  _startUpdatingUI() {
    const e = this.editor, n = e.editing.view.document;
    let r = this._getSelectedLinkElement(), s = a();
    const o = () => {
      const l = this._getSelectedLinkElement(), c = a();
      r && !l || !r && c !== s ? this._hideUI() : this._isUIVisible && this._balloon.updatePosition(this._getBalloonPositionData()), r = l, s = c;
    };
    function a() {
      return n.selection.focus.getAncestors().reverse().find((l) => l.is("element"));
    }
    this.listenTo(e.ui, "update", o), this.listenTo(this._balloon, "change:visibleView", o);
  }
  /**
  * Returns `true` when {@link #formView} is in the {@link #_balloon}.
  */
  get _isFormInPanel() {
    return !!this.formView && this._balloon.hasView(this.formView);
  }
  /**
  * Returns `true` when {@link #actionsView} is in the {@link #_balloon}.
  */
  get _areActionsInPanel() {
    return !!this.actionsView && this._balloon.hasView(this.actionsView);
  }
  /**
  * Returns `true` when {@link #actionsView} is in the {@link #_balloon} and it is
  * currently visible.
  */
  get _areActionsVisible() {
    return !!this.actionsView && this._balloon.visibleView === this.actionsView;
  }
  /**
  * Returns `true` when {@link #actionsView} or {@link #formView} is in the {@link #_balloon}.
  */
  get _isUIInPanel() {
    return this._isFormInPanel || this._areActionsInPanel;
  }
  /**
  * Returns `true` when {@link #actionsView} or {@link #formView} is in the {@link #_balloon} and it is
  * currently visible.
  */
  get _isUIVisible() {
    const e = this._balloon.visibleView;
    return !!this.formView && e == this.formView || this._areActionsVisible;
  }
  /**
  * Returns positioning options for the {@link #_balloon}. They control the way the balloon is attached
  * to the target element or selection.
  *
  * If the selection is collapsed and inside a link element, the panel will be attached to the
  * entire link element. Otherwise, it will be attached to the selection.
  */
  _getBalloonPositionData() {
    const e = this.editor.editing.view, n = this.editor.model, r = e.document;
    let s;
    if (n.markers.has(lr)) {
      const o = Array.from(this.editor.editing.mapper.markerNameToElements(lr)), a = e.createRange(e.createPositionBefore(o[0]), e.createPositionAfter(o[o.length - 1]));
      s = e.domConverter.viewRangeToDom(a);
    } else
      s = () => {
        const o = this._getSelectedLinkElement();
        return o ? (
          // When selection is inside link element, then attach panel to this element.
          e.domConverter.mapViewToDom(o)
        ) : (
          // Otherwise attach panel to the selection.
          e.domConverter.viewRangeToDom(r.selection.getFirstRange())
        );
      };
    return {
      target: s
    };
  }
  /**
  * Returns the link {@link module:engine/view/attributeelement~AttributeElement} under
  * the {@link module:engine/view/document~Document editing view's} selection or `null`
  * if there is none.
  *
  * **Note**: For a non–collapsed selection, the link element is returned when **fully**
  * selected and the **only** element within the selection boundaries, or when
  * a linked widget is selected.
  */
  _getSelectedLinkElement() {
    const e = this.editor.editing.view, n = e.document.selection, r = n.getSelectedElement();
    if (n.isCollapsed || r && Gt(r))
      return yf(n.getFirstPosition());
    {
      const s = n.getFirstRange().getTrimmed(), o = yf(s.start), a = yf(s.end);
      return !o || o != a ? null : e.createRangeIn(o).getTrimmed().isEqual(s) ? o : null;
    }
  }
  /**
  * Displays a fake visual selection when the contextual balloon is displayed.
  *
  * This adds a 'link-ui' marker into the document that is rendered as a highlight on selected text fragment.
  */
  _showFakeVisualSelection() {
    const e = this.editor.model;
    e.change((n) => {
      const r = e.document.selection.getFirstRange();
      if (e.markers.has(lr))
        n.updateMarker(lr, {
          range: r
        });
      else if (r.start.isAtEnd) {
        const s = r.start.getLastMatchingPosition(({ item: o }) => !e.schema.isContent(o), {
          boundaries: r
        });
        n.addMarker(lr, {
          usingOperation: !1,
          affectsData: !1,
          range: n.createRange(s, r.end)
        });
      } else
        n.addMarker(lr, {
          usingOperation: !1,
          affectsData: !1,
          range: r
        });
    });
  }
  /**
  * Hides the fake visual selection created in {@link #_showFakeVisualSelection}.
  */
  _hideFakeVisualSelection() {
    const e = this.editor.model;
    e.markers.has(lr) && e.change((n) => {
      n.removeMarker(lr);
    });
  }
}
function yf(i) {
  return i.getAncestors().find((t) => oz(t)) || null;
}
function Cz(i) {
  const t = i.t, e = i.config.get("link.allowCreatingEmptyLinks");
  return [
    (n) => {
      if (!e && !n.url.length)
        return t("Link URL must not be empty.");
    }
  ];
}
const kz = 4, tx = new RegExp(
  // Group 1: Line start or after a space.
  "(^|\\s)(((?:(?:(?:https?|ftp):)?\\/\\/)(?:\\S+(?::\\S*)?@)?(?:(?:[1-9]\\d?|1\\d\\d|2[01]\\d|22[0-3])(?:\\.(?:1?\\d{1,2}|2[0-4]\\d|25[0-5])){2}(?:\\.(?:[1-9]\\d?|1\\d\\d|2[0-4]\\d|25[0-4]))|(((?!www\\.)|(www\\.))(?![-_])(?:[-_a-z0-9\\u00a1-\\uffff]{1,63}\\.)+(?:[a-z\\u00a1-\\uffff]{2,63})))(?::\\d{2,5})?(?:[/?#]\\S*)?)|((www.|(\\S+@))((?![-_])(?:[-_a-z0-9\\u00a1-\\uffff]{1,63}\\.))+(?:[a-z\\u00a1-\\uffff]{2,63})))$",
  "i"
), xz = 2;
class nx extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Fr,
      th
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AutoLink";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor.model.document.selection;
    e.on("change:range", () => {
      this.isEnabled = !e.anchor.parent.is("element", "codeBlock");
    }), this._enableTypingHandling();
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._enableEnterHandling(), this._enableShiftEnterHandling(), this._enablePasteLinking();
  }
  /**
  * For given position, returns a range that includes the whole link that contains the position.
  *
  * If position is not inside a link, returns `null`.
  */
  _expandLinkRange(t, e) {
    return e.textNode && e.textNode.hasAttribute("linkHref") ? Yd(e, "linkHref", e.textNode.getAttribute("linkHref"), t) : null;
  }
  /**
  * Extends the document selection to includes all links that intersects with given `selectedRange`.
  */
  _selectEntireLinks(t, e) {
    const r = this.editor.model, s = r.document.selection, o = s.getFirstPosition(), a = s.getLastPosition();
    let l = e.getJoined(this._expandLinkRange(r, o) || e);
    l && (l = l.getJoined(this._expandLinkRange(r, a) || e)), l && (l.start.isBefore(o) || l.end.isAfter(a)) && t.setSelection(l);
  }
  /**
  * Enables autolinking on pasting a URL when some content is selected.
  */
  _enablePasteLinking() {
    const t = this.editor, e = t.model, n = e.document.selection, r = t.plugins.get("ClipboardPipeline"), s = t.commands.get("link");
    r.on("inputTransformation", (o, a) => {
      if (!this.isEnabled || !s.isEnabled || n.isCollapsed || a.method !== "paste" || n.rangeCount > 1)
        return;
      const l = n.getFirstRange(), c = a.dataTransfer.getData("text/plain");
      if (!c)
        return;
      const u = c.match(tx);
      u && u[2] === c && (e.change((d) => {
        this._selectEntireLinks(d, l), s.execute(c);
      }), o.stop());
    }, {
      priority: "high"
    });
  }
  /**
  * Enables autolinking on typing.
  */
  _enableTypingHandling() {
    const t = this.editor, e = new hk(t.model, (n) => {
      if (!Ez(n))
        return;
      const r = Hw(n.substr(0, n.length - 1));
      if (r)
        return {
          url: r
        };
    });
    e.on("matched:data", (n, r) => {
      const { batch: s, range: o, url: a } = r;
      if (!s.isTyping)
        return;
      const l = o.end.getShiftedBy(-1), c = l.getShiftedBy(-a.length), u = t.model.createRange(c, l);
      this._applyAutoLink(a, u);
    }), e.bind("isEnabled").to(this);
  }
  /**
  * Enables autolinking on the <kbd>Enter</kbd> key.
  */
  _enableEnterHandling() {
    const t = this.editor, e = t.model, n = t.commands.get("enter");
    n && n.on("execute", () => {
      const r = e.document.selection.getFirstPosition();
      if (!r.parent.previousSibling)
        return;
      const s = e.createRangeIn(r.parent.previousSibling);
      this._checkAndApplyAutoLinkOnRange(s);
    });
  }
  /**
  * Enables autolinking on the <kbd>Shift</kbd>+<kbd>Enter</kbd> keyboard shortcut.
  */
  _enableShiftEnterHandling() {
    const t = this.editor, e = t.model, n = t.commands.get("shiftEnter");
    n && n.on("execute", () => {
      const r = e.document.selection.getFirstPosition(), s = e.createRange(e.createPositionAt(r.parent, 0), r.getShiftedBy(-1));
      this._checkAndApplyAutoLinkOnRange(s);
    });
  }
  /**
  * Checks if the passed range contains a linkable text.
  */
  _checkAndApplyAutoLinkOnRange(t) {
    const e = this.editor.model, { text: n, range: r } = dk(t, e), s = Hw(n);
    if (s) {
      const o = e.createRange(r.end.getShiftedBy(-s.length), r.end);
      this._applyAutoLink(s, o);
    }
  }
  /**
  * Applies a link on a given range if the link should be applied.
  *
  * @param url The URL to link.
  * @param range The text range to apply the link attribute to.
  */
  _applyAutoLink(t, e) {
    const n = this.editor.model, r = this.editor.config.get("link.defaultProtocol"), s = Ip(t, r);
    !this.isEnabled || !Tz(e, n) || !Xk(s) || Sz(e) || this._persistAutoLink(s, e);
  }
  /**
  * Enqueues autolink changes in the model.
  *
  * @param url The URL to link.
  * @param range The text range to apply the link attribute to.
  */
  _persistAutoLink(t, e) {
    const n = this.editor.model, r = this.editor.plugins.get("Delete");
    n.enqueueChange((s) => {
      s.setAttribute("linkHref", t, e), n.enqueueChange(() => {
        r.requestUndoOnBackspace();
      });
    });
  }
}
function Ez(i) {
  return i.length > kz && i[i.length - 1] === " " && i[i.length - 2] !== " ";
}
function Hw(i) {
  const t = tx.exec(i);
  return t ? t[xz] : null;
}
function Tz(i, t) {
  return t.schema.checkAttributeInSelection(t.createSelection(i), "linkHref");
}
function Sz(i) {
  const t = i.start.nodeAfter;
  return !!t && t.hasAttribute("linkHref");
}
class Az extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      th,
      ex,
      nx
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Link";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class Pz extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      "ImageEditing",
      "ImageUtils",
      th
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkImageEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.model.schema;
    t.plugins.has("ImageBlockEditing") && e.extend("imageBlock", {
      allowAttributes: [
        "linkHref"
      ]
    }), t.conversion.for("upcast").add(Iz(t)), t.conversion.for("downcast").add(Rz(t)), this._enableAutomaticDecorators(), this._enableManualDecorators();
  }
  /**
  * Processes {@link module:link/linkconfig~LinkDecoratorAutomaticDefinition automatic decorators} definitions and
  * attaches proper converters that will work when linking an image.`
  */
  _enableAutomaticDecorators() {
    const t = this.editor, n = t.commands.get("link").automaticDecorators;
    n.length && t.conversion.for("downcast").add(n.getDispatcherForLinkedImage());
  }
  /**
  * Processes transformed {@link module:link/utils/manualdecorator~ManualDecorator} instances and attaches proper converters
  * that will work when linking an image.
  */
  _enableManualDecorators() {
    const t = this.editor, e = t.commands.get("link");
    for (const n of e.manualDecorators)
      t.plugins.has("ImageBlockEditing") && t.model.schema.extend("imageBlock", {
        allowAttributes: n.id
      }), t.plugins.has("ImageInlineEditing") && t.model.schema.extend("imageInline", {
        allowAttributes: n.id
      }), t.conversion.for("downcast").add(Oz(n)), t.conversion.for("upcast").add(Vz(t, n));
  }
}
function Iz(i) {
  const t = i.plugins.has("ImageInlineEditing"), e = i.plugins.get("ImageUtils");
  return (n) => {
    n.on("element:a", (r, s, o) => {
      const a = s.viewItem, l = e.findViewImgElement(a);
      if (!l)
        return;
      const c = l.findAncestor((m) => e.isBlockImageView(m));
      if (t && !c)
        return;
      const u = {
        attributes: [
          "href"
        ]
      };
      if (!o.consumable.consume(a, u))
        return;
      const d = a.getAttribute("href");
      if (!d)
        return;
      let f = s.modelCursor.parent;
      if (!f.is("element", "imageBlock")) {
        const m = o.convertItem(l, s.modelCursor);
        s.modelRange = m.modelRange, s.modelCursor = m.modelCursor, f = s.modelCursor.nodeBefore;
      }
      f && f.is("element", "imageBlock") && o.writer.setAttribute("linkHref", d, f);
    }, {
      priority: "high"
    });
  };
}
function Rz(i) {
  const t = i.plugins.get("ImageUtils");
  return (e) => {
    e.on("attribute:linkHref:imageBlock", (n, r, s) => {
      if (!s.consumable.consume(r.item, n.name))
        return;
      const o = s.mapper.toViewElement(r.item), a = s.writer, l = Array.from(o.getChildren()).find((d) => d.is("element", "a")), c = t.findViewImgElement(o), u = c.parent.is("element", "picture") ? c.parent : c;
      if (l)
        r.attributeNewValue ? a.setAttribute("href", r.attributeNewValue, l) : (a.move(a.createRangeOn(u), a.createPositionAt(o, 0)), a.remove(l));
      else {
        const d = a.createContainerElement("a", {
          href: r.attributeNewValue
        });
        a.insert(a.createPositionAt(o, 0), d), a.move(a.createRangeOn(u), a.createPositionAt(d, 0));
      }
    }, {
      priority: "high"
    });
  };
}
function Oz(i) {
  return (t) => {
    t.on(`attribute:${i.id}:imageBlock`, (e, n, r) => {
      const s = r.mapper.toViewElement(n.item), o = Array.from(s.getChildren()).find((a) => a.is("element", "a"));
      if (o) {
        for (const [a, l] of gr(i.attributes))
          r.writer.setAttribute(a, l, o);
        i.classes && r.writer.addClass(i.classes, o);
        for (const a in i.styles)
          r.writer.setStyle(a, i.styles[a], o);
      }
    });
  };
}
function Vz(i, t) {
  const e = i.plugins.has("ImageInlineEditing"), n = i.plugins.get("ImageUtils");
  return (r) => {
    r.on("element:a", (s, o, a) => {
      const l = o.viewItem, c = n.findViewImgElement(l);
      if (!c)
        return;
      const u = c.findAncestor((g) => n.isBlockImageView(g));
      if (e && !u)
        return;
      const f = new xi(t._createPattern()).match(l);
      if (!f || !a.consumable.consume(l, f.match))
        return;
      const m = o.modelCursor.nodeBefore || o.modelCursor.parent;
      a.writer.setAttribute(t.id, !0, m);
    }, {
      priority: "high"
    });
  };
}
class Mz extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      th,
      ex,
      "ImageBlockEditing"
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkImageUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.editing.view.document;
    this.listenTo(e, "click", (n, r) => {
      this._isSelectedLinkedImage(t.model.document.selection) && (r.preventDefault(), n.stop());
    }, {
      priority: "high"
    }), this._createToolbarLinkImageButton();
  }
  /**
  * Creates a `LinkImageUI` button view.
  *
  * Clicking this button shows a {@link module:link/linkui~LinkUI#_balloon} attached to the selection.
  * When an image is already linked, the view shows {@link module:link/linkui~LinkUI#actionsView} or
  * {@link module:link/linkui~LinkUI#formView} if it is not.
  */
  _createToolbarLinkImageButton() {
    const t = this.editor, e = t.t;
    t.ui.componentFactory.add("linkImage", (n) => {
      const r = new Te(n), s = t.plugins.get("LinkUI"), o = t.commands.get("link");
      return r.set({
        isEnabled: !0,
        label: e("Link image"),
        icon: Qk,
        keystroke: nl,
        tooltip: !0,
        isToggleable: !0
      }), r.bind("isEnabled").to(o, "isEnabled"), r.bind("isOn").to(o, "value", (a) => !!a), this.listenTo(r, "execute", () => {
        this._isSelectedLinkedImage(t.model.document.selection) ? s._addActionsView() : s._showUI(!0);
      }), r;
    });
  }
  /**
  * Returns true if a linked image (either block or inline) is the only selected element
  * in the model document.
  */
  _isSelectedLinkedImage(t) {
    const e = t.getSelectedElement();
    return this.editor.plugins.get("ImageUtils").isImage(e) && e.hasAttribute("linkHref");
  }
}
class Dz extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Pz,
      Mz
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkImage";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class Li {
  /**
  * Creates a document list iterator.
  *
  * @param startElement The start list item block element.
  * @param options.direction The iterating direction.
  * @param options.includeSelf Whether start block should be included in the result (if it's matching other criteria).
  * @param options.sameAttributes Additional attributes that must be the same for each block.
  * @param options.sameIndent Whether blocks with the same indent level as the start block should be included
  * in the result.
  * @param options.lowerIndent Whether blocks with a lower indent level than the start block should be included
  * in the result.
  * @param options.higherIndent Whether blocks with a higher indent level than the start block should be included
  * in the result.
  */
  constructor(t, e) {
    /**
    * The start list item block element.
    */
    h(this, "_startElement");
    /**
    * The reference indent. Initialized by the indent of the start block.
    */
    h(this, "_referenceIndent");
    /**
    * The iterating direction.
    */
    h(this, "_isForward");
    /**
    * Whether start block should be included in the result (if it's matching other criteria).
    */
    h(this, "_includeSelf");
    /**
    * Additional attributes that must be the same for each block.
    */
    h(this, "_sameAttributes");
    /**
    * Whether blocks with the same indent level as the start block should be included in the result.
    */
    h(this, "_sameIndent");
    /**
    * Whether blocks with a lower indent level than the start block should be included in the result.
    */
    h(this, "_lowerIndent");
    /**
    * Whether blocks with a higher indent level than the start block should be included in the result.
    */
    h(this, "_higherIndent");
    this._startElement = t, this._referenceIndent = t.getAttribute("listIndent"), this._isForward = e.direction == "forward", this._includeSelf = !!e.includeSelf, this._sameAttributes = tt(e.sameAttributes || []), this._sameIndent = !!e.sameIndent, this._lowerIndent = !!e.lowerIndent, this._higherIndent = !!e.higherIndent;
  }
  /**
  * Performs only first step of iteration and returns the result.
  *
  * @param startElement The start list item block element.
  * @param options.direction The iterating direction.
  * @param options.includeSelf Whether start block should be included in the result (if it's matching other criteria).
  * @param options.sameAttributes Additional attributes that must be the same for each block.
  * @param options.sameIndent Whether blocks with the same indent level as the start block should be included
  * in the result.
  * @param options.lowerIndent Whether blocks with a lower indent level than the start block should be included
  * in the result.
  * @param options.higherIndent Whether blocks with a higher indent level than the start block should be included
  * in the result.
  */
  static first(t, e) {
    const r = new this(t, e)[Symbol.iterator]();
    return Ft(r);
  }
  /**
  * Iterable interface.
  */
  *[Symbol.iterator]() {
    const t = [];
    for (const { node: e } of wc(this._getStartNode(), this._isForward ? "forward" : "backward")) {
      const n = e.getAttribute("listIndent");
      if (n < this._referenceIndent) {
        if (!this._lowerIndent)
          break;
        this._referenceIndent = n;
      } else if (n > this._referenceIndent) {
        if (!this._higherIndent)
          continue;
        if (!this._isForward) {
          t.push(e);
          continue;
        }
      } else {
        if (!this._sameIndent) {
          if (this._higherIndent) {
            t.length && (yield* t, t.length = 0);
            break;
          }
          continue;
        }
        if (this._sameAttributes.some((r) => e.getAttribute(r) !== this._startElement.getAttribute(r)))
          break;
      }
      t.length && (yield* t, t.length = 0), yield e;
    }
  }
  /**
  * Returns the model element to start iterating.
  */
  _getStartNode() {
    return this._includeSelf ? this._startElement : this._isForward ? this._startElement.nextSibling : this._startElement.previousSibling;
  }
}
function* wc(i, t = "forward") {
  const e = t == "forward", n = [];
  let r = null;
  for (; Rt(i); ) {
    let s = null;
    if (r) {
      const o = i.getAttribute("listIndent"), a = r.getAttribute("listIndent");
      o > a ? n[a] = r : o < a ? (s = n[o], n.length = o) : s = r;
    }
    yield {
      node: i,
      previous: r,
      previousNodeInList: s
    }, r = i, i = e ? i.nextSibling : i.previousSibling;
  }
}
class Lz {
  /**
  * @param listHead The head element of a list.
  */
  constructor(t) {
    h(this, "_listHead");
    this._listHead = t;
  }
  /**
  * List blocks iterator.
  *
  * Iterates over all blocks of a list.
  */
  [Symbol.iterator]() {
    return wc(this._listHead, "forward");
  }
}
class oa {
  /**
  * Returns the next ID.
  *
  * @internal
  */
  /* istanbul ignore next: static function definition -- @preserve */
  static next() {
    return Jn();
  }
}
function Rt(i) {
  return !!i && i.is("element") && i.hasAttribute("listItemId");
}
function Rp(i, t = {}) {
  return [
    ...ms(i, {
      ...t,
      direction: "backward"
    }),
    ...ms(i, {
      ...t,
      direction: "forward"
    })
  ];
}
function ms(i, t = {}) {
  const e = t.direction == "forward", n = Array.from(new Li(i, {
    ...t,
    includeSelf: e,
    sameIndent: !0,
    sameAttributes: "listItemId"
  }));
  return e ? n : n.reverse();
}
function Nz(i) {
  return Array.from(new Li(i, {
    direction: "forward",
    higherIndent: !0
  }));
}
function ix(i, t) {
  const e = new Li(i, {
    sameIndent: !0,
    sameAttributes: "listType",
    ...t
  }), n = new Li(i, {
    sameIndent: !0,
    sameAttributes: "listType",
    includeSelf: !0,
    direction: "forward",
    ...t
  });
  return [
    ...Array.from(e).reverse(),
    ...n
  ];
}
function Ys(i) {
  return !Li.first(i, {
    sameIndent: !0,
    sameAttributes: "listItemId"
  });
}
function jw(i) {
  return !Li.first(i, {
    direction: "forward",
    sameIndent: !0,
    sameAttributes: "listItemId"
  });
}
function yc(i, t = {}) {
  i = tt(i);
  const e = t.withNested !== !1, n = /* @__PURE__ */ new Set();
  for (const r of i)
    for (const s of Rp(r, {
      higherIndent: e
    }))
      n.add(s);
  return ao(n);
}
function nh(i) {
  i = tt(i);
  const t = /* @__PURE__ */ new Set();
  for (const e of i)
    for (const n of ix(e))
      t.add(n);
  return ao(t);
}
function Op(i, t) {
  const e = ms(i, {
    direction: "forward"
  }), n = oa.next();
  for (const r of e)
    t.setAttribute("listItemId", n, r);
  return e;
}
function Em(i, t, e) {
  const n = {};
  for (const [s, o] of t.getAttributes())
    s.startsWith("list") && (n[s] = o);
  const r = ms(i, {
    direction: "forward"
  });
  for (const s of r)
    e.setAttributes(n, s);
  return r;
}
function Tm(i, t, { expand: e, indentBy: n = 1 } = {}) {
  i = tt(i);
  const r = e ? yc(i) : i;
  for (const s of r) {
    const o = s.getAttribute("listIndent") + n;
    o < 0 ? ih(s, t) : t.setAttribute("listIndent", o, s);
  }
  return r;
}
function Bz(i, t) {
  i = tt(i);
  const e = yc(i), n = /* @__PURE__ */ new Set(), r = Math.min(...e.map((o) => o.getAttribute("listIndent"))), s = /* @__PURE__ */ new Map();
  for (const o of e)
    s.set(o, Li.first(o, {
      lowerIndent: !0
    }));
  for (const o of e) {
    if (n.has(o))
      continue;
    n.add(o);
    const a = o.getAttribute("listIndent") - 1;
    if (a < 0) {
      ih(o, t);
      continue;
    }
    if (o.getAttribute("listIndent") == r) {
      const l = $z(o, s.get(o), t);
      for (const c of l)
        n.add(c);
      if (l.length)
        continue;
    }
    t.setAttribute("listIndent", a, o);
  }
  return ao(n);
}
function ih(i, t) {
  i = tt(i);
  for (const e of i)
    e.is("element", "listItem") && t.rename(e, "paragraph");
  for (const e of i)
    for (const n of e.getAttributeKeys())
      n.startsWith("list") && t.removeAttribute(n, e);
  return i;
}
function Wl(i) {
  if (!i.length)
    return !1;
  const t = i[0].getAttribute("listItemId");
  return t ? !i.some((e) => e.getAttribute("listItemId") != t) : !1;
}
function Fz(i, t) {
  const e = [];
  let n = Number.POSITIVE_INFINITY;
  for (const { node: r } of wc(i.nextSibling, "forward")) {
    const s = r.getAttribute("listIndent");
    if (s == 0)
      break;
    s < n && (n = s);
    const o = s - n;
    t.setAttribute("listIndent", o, r), e.push(r);
  }
  return e;
}
function ao(i) {
  return Array.from(i).filter((t) => t.root.rootName !== "$graveyard").sort((t, e) => t.index - e.index);
}
function Ul(i) {
  const t = i.document.selection.getSelectedElement();
  return t && i.schema.isObject(t) && i.schema.isBlock(t) ? t : null;
}
function _f(i, t) {
  return t.checkChild(i.parent, "listItem") && t.checkChild(i, "$text") && !t.isObject(i);
}
function id(i) {
  return i == "numbered" || i == "customNumbered";
}
function $z(i, t, e) {
  return ms(t, {
    direction: "forward"
  }).pop().index > i.index ? Em(i, t, e) : [];
}
class Ww extends Ve {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param indentDirection The direction of indent. If it is equal to `backward`, the command
  * will outdent a list item.
  */
  constructor(e, n) {
    super(e);
    /**
    * Determines by how much the command will change the list item's indent attribute.
    */
    h(this, "_direction");
    this._direction = n;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Indents or outdents (depending on the {@link #constructor}'s `indentDirection` parameter) selected list items.
  *
  * @fires execute
  * @fires afterExecute
  */
  execute() {
    const e = this.editor.model, n = Uw(e.document.selection);
    e.change((r) => {
      const s = [];
      Wl(n) && !Ys(n[0]) ? (this._direction == "forward" && s.push(...Tm(n, r)), s.push(...Op(n[0], r))) : this._direction == "forward" ? s.push(...Tm(n, r, {
        expand: !0
      })) : s.push(...Bz(n, r));
      for (const o of s) {
        if (!o.hasAttribute("listType"))
          continue;
        const a = Li.first(o, {
          sameIndent: !0
        });
        a && r.setAttribute("listType", a.getAttribute("listType"), o);
      }
      this._fireAfterExecute(s);
    });
  }
  /**
  * Fires the `afterExecute` event.
  *
  * @param changedBlocks The changed list elements.
  */
  _fireAfterExecute(e) {
    this.fire("afterExecute", ao(new Set(e)));
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    let e = Uw(this.editor.model.document.selection), n = e[0];
    if (!n)
      return !1;
    if (this._direction == "backward" || Wl(e) && !Ys(e[0]))
      return !0;
    e = yc(e), n = e[0];
    const r = Li.first(n, {
      sameIndent: !0
    });
    return r ? r.getAttribute("listType") == n.getAttribute("listType") : !1;
  }
}
function Uw(i) {
  const t = Array.from(i.getSelectedBlocks()), e = t.findIndex((n) => !Rt(n));
  return e != -1 && (t.length = e), t;
}
class Zc extends Ve {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param type List type that will be handled by this command.
  */
  constructor(e, n, r = {}) {
    super(e);
    /**
    * The type of the list created by the command.
    */
    h(this, "type");
    /**
    * List Walker options that change the range of the list items to be changed when the selection is collapsed within a list item.
    *
    * In a multi-level list, when the selection is collapsed within a list item, instead of changing only the list items of the same list
    * type and current indent level, the entire list structure is changed (all list items at all indent levels of any list type).
    */
    h(this, "_listWalkerOptions");
    this.type = n, this._listWalkerOptions = r.multiLevel ? {
      higherIndent: !0,
      lowerIndent: !0,
      sameAttributes: []
    } : void 0;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.value = this._getValue(), this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the list command.
  *
  * @fires execute
  * @fires afterExecute
  * @param options Command options.
  * @param options.forceValue If set, it will force the command behavior. If `true`, the command will try to convert the
  * selected items and potentially the neighbor elements to the proper list items. If set to `false` it will convert selected elements
  * to paragraphs. If not set, the command will toggle selected elements to list items or paragraphs, depending on the selection.
  * @param options.additionalAttributes Additional attributes that are set for list items when the command is executed.
  */
  execute(e = {}) {
    const n = this.editor.model, r = n.document, s = Ul(n), o = Array.from(r.selection.getSelectedBlocks()).filter((l) => n.schema.checkAttribute(l, "listType") || _f(l, n.schema)), a = e.forceValue !== void 0 ? !e.forceValue : this.value;
    n.change((l) => {
      if (a) {
        const c = o[o.length - 1], u = ms(c, {
          direction: "forward"
        }), d = [];
        u.length > 1 && d.push(...Op(u[1], l)), d.push(...ih(o, l)), d.push(...Fz(c, l)), this._fireAfterExecute(d);
      } else if ((s || r.selection.isCollapsed) && Rt(o[0])) {
        const c = ix(s || o[0], this._listWalkerOptions);
        for (const u of c)
          l.setAttributes({
            ...e.additionalAttributes,
            listType: this.type
          }, u);
        this._fireAfterExecute(c);
      } else {
        const c = [];
        for (const u of o)
          if (!u.hasAttribute("listType"))
            !u.is("element", "listItem") && _f(u, n.schema) && l.rename(u, "listItem"), l.setAttributes({
              ...e.additionalAttributes,
              listIndent: 0,
              listItemId: oa.next(),
              listType: this.type
            }, u), c.push(u);
          else
            for (const d of yc(u, {
              withNested: !1
            }))
              d.getAttribute("listType") != this.type && (l.setAttributes({
                ...e.additionalAttributes,
                listType: this.type
              }, d), c.push(d));
        this._fireAfterExecute(c);
      }
    });
  }
  /**
  * Fires the `afterExecute` event.
  *
  * @param changedBlocks The changed list elements.
  */
  _fireAfterExecute(e) {
    this.fire("afterExecute", ao(new Set(e)));
  }
  /**
  * Checks the command's {@link #value}.
  *
  * @returns The current value.
  */
  _getValue() {
    const e = this.editor.model.document.selection, n = Array.from(e.getSelectedBlocks());
    if (!n.length)
      return !1;
    for (const r of n)
      if (r.getAttribute("listType") != this.type)
        return !1;
    return !0;
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    const e = this.editor.model, n = e.schema, r = e.document.selection, s = Array.from(r.getSelectedBlocks());
    if (!s.length)
      return !1;
    if (this.value)
      return !0;
    for (const o of s)
      if (n.checkAttribute(o, "listType") || _f(o, n))
        return !0;
    return !1;
  }
}
class qw extends Ve {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param direction Whether list item should be merged before or after the selected block.
  */
  constructor(e, n) {
    super(e);
    /**
    * Whether list item should be merged before or after the selected block.
    */
    h(this, "_direction");
    this._direction = n;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Merges list blocks together (depending on the {@link #constructor}'s `direction` parameter).
  *
  * @fires execute
  * @fires afterExecute
  * @param options Command options.
  * @param options.shouldMergeOnBlocksContentLevel When set `true`, merging will be performed together
  * with {@link module:engine/model/model~Model#deleteContent} to get rid of the inline content in the selection or take advantage
  * of the heuristics in `deleteContent()` that helps convert lists into paragraphs in certain cases.
  */
  execute({ shouldMergeOnBlocksContentLevel: e = !1 } = {}) {
    const n = this.editor.model, r = n.document.selection, s = [];
    n.change((o) => {
      const { firstElement: a, lastElement: l } = this._getMergeSubjectElements(r, e), c = a.getAttribute("listIndent") || 0, u = l.getAttribute("listIndent"), d = l.getAttribute("listItemId");
      if (c != u) {
        const f = Nz(l);
        s.push(...Tm([
          l,
          ...f
        ], o, {
          indentBy: c - u,
          // If outdenting, the entire sub-tree that follows must be included.
          expand: c < u
        }));
      }
      if (e) {
        let f = r;
        r.isCollapsed && (f = o.createSelection(o.createRange(o.createPositionAt(a, "end"), o.createPositionAt(l, 0)))), n.deleteContent(f, {
          doNotResetEntireContent: r.isCollapsed
        });
        const m = f.getLastPosition().parent, g = m.nextSibling;
        s.push(m), g && g !== l && g.getAttribute("listItemId") == d && s.push(...Em(g, m, o));
      } else
        s.push(...Em(l, a, o));
      this._fireAfterExecute(s);
    });
  }
  /**
  * Fires the `afterExecute` event.
  *
  * @param changedBlocks The changed list elements.
  */
  _fireAfterExecute(e) {
    this.fire("afterExecute", ao(new Set(e)));
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    const e = this.editor.model, n = e.document.selection, r = Ul(e);
    if (n.isCollapsed || r) {
      const s = r || n.getFirstPosition().parent;
      if (!Rt(s))
        return !1;
      const o = this._direction == "backward" ? s.previousSibling : s.nextSibling;
      if (!o || Wl([
        s,
        o
      ]))
        return !1;
    } else {
      const s = n.getLastPosition(), o = n.getFirstPosition();
      if (s.parent === o.parent || !Rt(s.parent))
        return !1;
    }
    return !0;
  }
  /**
  * Returns the boundary elements the merge should be executed for. These are not necessarily selection's first
  * and last position parents but sometimes sibling or even further blocks depending on the context.
  *
  * @param selection The selection the merge is executed for.
  * @param shouldMergeOnBlocksContentLevel When `true`, merge is performed together with
  * {@link module:engine/model/model~Model#deleteContent} to remove the inline content within the selection.
  */
  _getMergeSubjectElements(e, n) {
    const r = this.editor.model, s = Ul(r);
    let o, a;
    if (e.isCollapsed || s) {
      const l = s || e.getFirstPosition().parent, c = Ys(l);
      this._direction == "backward" ? (a = l, c && !n ? o = Li.first(l, {
        sameIndent: !0,
        lowerIndent: !0
      }) : o = l.previousSibling) : (o = l, a = l.nextSibling);
    } else
      o = e.getFirstPosition().parent, a = e.getLastPosition().parent;
    return {
      firstElement: o,
      lastElement: a
    };
  }
}
class Gw extends Ve {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param direction Whether list item should be split before or after the selected block.
  */
  constructor(e, n) {
    super(e);
    /**
    * Whether list item should be split before or after the selected block.
    */
    h(this, "_direction");
    this._direction = n;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Splits the list item at the selection.
  *
  * @fires execute
  * @fires afterExecute
  */
  execute() {
    this.editor.model.change((n) => {
      const r = Op(this._getStartBlock(), n);
      this._fireAfterExecute(r);
    });
  }
  /**
  * Fires the `afterExecute` event.
  *
  * @param changedBlocks The changed list elements.
  */
  _fireAfterExecute(e) {
    this.fire("afterExecute", ao(new Set(e)));
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    const e = this.editor.model.document.selection, n = this._getStartBlock();
    return e.isCollapsed && Rt(n) && !Ys(n);
  }
  /**
  * Returns the model element that is the main focus of the command (according to the current selection and command direction).
  */
  _getStartBlock() {
    const n = this.editor.model.document.selection.getFirstPosition().parent;
    return this._direction == "before" ? n : n.nextSibling;
  }
}
class zz extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Expands the given list of selected blocks to include all the items of the lists they're in.
  *
  * @param blocks The list of selected blocks.
  */
  expandListBlocksToCompleteList(t) {
    return nh(t);
  }
  /**
  * Check if the given block is the first in the list item.
  *
  * @param listBlock The list block element.
  */
  isFirstBlockOfListItem(t) {
    return Ys(t);
  }
  /**
  * Returns true if the given model node is a list item block.
  *
  * @param node A model node.
  */
  isListItemBlock(t) {
    return Rt(t);
  }
  /**
  * Expands the given list of selected blocks to include the leading and tailing blocks of partially selected list items.
  *
  * @param blocks The list of selected blocks.
  * @param options.withNested Whether should include nested list items.
  */
  expandListBlocksToCompleteItems(t, e = {}) {
    return yc(t, e);
  }
  /**
  * Returns true if listType is of type `numbered` or `customNumbered`.
  */
  isNumberedListType(t) {
    return id(t);
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Hz(i) {
  return i.is("element", "ol") || i.is("element", "ul");
}
function Sm(i) {
  return i.is("element", "li");
}
function jz(i) {
  let t = 0, e = i.parent;
  for (; e; ) {
    if (Sm(e))
      t++;
    else {
      const n = e.previousSibling;
      n && Sm(n) && t++;
    }
    e = e.parent;
  }
  return t;
}
function Wz(i, t, e, n = sx(e, t)) {
  return i.createAttributeElement(rx(e), null, {
    priority: 2 * t / 100 - 100,
    id: n
  });
}
function Uz(i, t, e) {
  return i.createAttributeElement("li", null, {
    priority: (2 * t + 1) / 100 - 100,
    id: e
  });
}
function rx(i) {
  return i == "numbered" || i == "customNumbered" ? "ol" : "ul";
}
function sx(i, t) {
  return `list-${i}-${t}`;
}
function Ui(i, t) {
  const e = i.nodeBefore;
  if (Rt(e)) {
    let n = e;
    for (const { node: r } of wc(n, "backward"))
      if (n = r, t.has(n))
        return;
    t.set(e, n);
  } else {
    const n = i.nodeAfter;
    Rt(n) && t.set(n, n);
  }
}
function qz(i, t) {
  let e = 0, n = -1, r = null, s = !1;
  for (const { node: o } of i) {
    const a = o.getAttribute("listIndent");
    if (a > e) {
      let l;
      r === null ? (r = a - e, l = e) : (r > a && (r = a), l = a - r), l > n + 1 && (l = n + 1), t.setAttribute("listIndent", l, o), s = !0, n = l;
    } else
      r = null, e = a + 1, n = a;
  }
  return s;
}
function Gz(i, t, e) {
  const n = /* @__PURE__ */ new Set();
  let r = !1;
  for (const { node: s } of i) {
    if (n.has(s))
      continue;
    let o = s.getAttribute("listType"), a = s.getAttribute("listItemId");
    if (t.has(a) && (a = oa.next()), t.add(a), s.is("element", "listItem")) {
      s.getAttribute("listItemId") != a && (e.setAttribute("listItemId", a, s), r = !0);
      continue;
    }
    for (const l of ms(s, {
      direction: "forward"
    }))
      n.add(l), l.getAttribute("listType") != o && (a = oa.next(), o = l.getAttribute("listType")), l.getAttribute("listItemId") != a && (e.setAttribute("listItemId", a, l), r = !0);
  }
  return r;
}
function Kz() {
  return (i, t, e) => {
    const { writer: n, schema: r } = e;
    if (!t.modelRange)
      return;
    const s = Array.from(t.modelRange.getItems({
      shallow: !0
    })).filter((d) => r.checkAttribute(d, "listItemId"));
    if (!s.length)
      return;
    const o = oa.next(), a = jz(t.viewItem);
    let l = t.viewItem.parent && t.viewItem.parent.is("element", "ol") ? "numbered" : "bulleted";
    const c = s[0].getAttribute("listType");
    c && (l = c);
    const u = {
      listItemId: o,
      listIndent: a,
      listType: l
    };
    for (const d of s)
      d.hasAttribute("listItemId") || n.setAttributes(u, d);
    s.length > 1 && s[1].getAttribute("listItemId") != u.listItemId && e.keepEmptyElement(s[0]);
  };
}
function Zz(i, t, e, n) {
  return () => {
    const a = i.document.differ.getChanges(), l = [], c = /* @__PURE__ */ new Map(), u = /* @__PURE__ */ new Set();
    for (const d of a)
      if (d.type == "insert" && d.name != "$text")
        Ui(d.position, c), d.attributes.has("listItemId") ? u.add(d.position.nodeAfter) : Ui(d.position.getShiftedBy(d.length), c);
      else if (d.type == "remove" && d.attributes.has("listItemId"))
        Ui(d.position, c);
      else if (d.type == "attribute") {
        const f = d.range.start.nodeAfter;
        e.includes(d.attributeKey) ? (Ui(d.range.start, c), d.attributeNewValue === null ? (Ui(d.range.start.getShiftedBy(1), c), s(f) && l.push(f)) : u.add(f)) : Rt(f) && s(f) && l.push(f);
      }
    for (const d of c.values())
      l.push(...r(d, u));
    for (const d of new Set(l))
      t.reconvertItem(d);
  };
  function r(a, l) {
    const c = [], u = /* @__PURE__ */ new Set(), d = [];
    for (const { node: f, previous: m } of wc(a, "forward")) {
      if (u.has(f))
        continue;
      const g = f.getAttribute("listIndent");
      m && g < m.getAttribute("listIndent") && (d.length = g + 1), d[g] = Object.fromEntries(Array.from(f.getAttributes()).filter(([b]) => e.includes(b)));
      const p = ms(f, {
        direction: "forward"
      });
      for (const b of p)
        u.add(b), (s(b, p) || o(b, d, l)) && c.push(b);
    }
    return c;
  }
  function s(a, l) {
    const c = t.mapper.toViewElement(a);
    if (!c)
      return !1;
    if (n.fire("checkElement", {
      modelElement: a,
      viewElement: c
    }))
      return !0;
    if (!a.is("element", "paragraph") && !a.is("element", "listItem"))
      return !1;
    const d = ax(a, e, l);
    return d && c.is("element", "p") ? !0 : !!(!d && c.is("element", "span"));
  }
  function o(a, l, c) {
    if (c.has(a))
      return !1;
    const u = t.mapper.toViewElement(a);
    let d = l.length - 1;
    for (let f = u.parent; !f.is("editableElement"); f = f.parent) {
      const m = Sm(f), g = Hz(f);
      if (!g && !m)
        continue;
      const p = `checkAttributes:${m ? "item" : "list"}`;
      if (n.fire(p, {
        viewElement: f,
        modelAttributes: l[d]
      }))
        break;
      if (g && (d--, d < 0))
        return !1;
    }
    return !0;
  }
}
function Kw(i, t, e, { dataPipeline: n } = {}) {
  const r = nH(i);
  return (s, o, a) => {
    const { writer: l, mapper: c, consumable: u } = a, d = o.item;
    if (!i.includes(o.attributeKey) || !r(d, u))
      return;
    const f = Jz(d, c, e);
    ox(f, l, c), eH(f, l);
    const m = Qz(d, f, t, l, {
      dataPipeline: n
    });
    tH(d, m, t, l);
  };
}
function Yz(i) {
  return (t, e, n) => {
    const { writer: r, mapper: s } = n, o = t.name.split(":")[1];
    if (!i.checkAttribute(o, "listItemId"))
      return;
    const a = s.toViewPosition(e.position), l = e.position.getShiftedBy(e.length), c = s.toViewPosition(l, {
      isPhantom: !0
    }), d = r.createRange(a, c).getTrimmed().end.nodeBefore;
    /* istanbul ignore next -- @preserve */
    d && ox(d, r, s);
  };
}
function Zw(i, { dataPipeline: t } = {}) {
  return (e, { writer: n }) => {
    if (!ax(e, i))
      return null;
    if (!t)
      return n.createContainerElement("span", {
        class: "ck-list-bogus-paragraph"
      });
    const r = n.createContainerElement("p");
    return n.setCustomProperty("dataPipeline:transparentRendering", !0, r), r;
  };
}
function Jz(i, t, e) {
  const n = e.createRangeOn(i);
  return t.toViewRange(n).getTrimmed().end.nodeBefore;
}
function Xz(i, t) {
  return (e, n) => {
    if (n.modelPosition.offset > 0)
      return;
    const r = n.modelPosition.parent;
    if (!Rt(r) || !i.some((c) => c.scope == "itemMarker" && c.canInjectMarkerIntoElement && c.canInjectMarkerIntoElement(r)))
      return;
    const s = n.mapper.toViewElement(r), o = t.createRangeIn(s), a = o.getWalker();
    let l = o.start;
    for (const { item: c } of a) {
      if (c.is("element") && n.mapper.toModelElement(c) || c.is("$textProxy"))
        break;
      c.is("element") && c.getCustomProperty("listItemMarker") && (l = t.createPositionAfter(c), a.skip(({ previousPosition: u }) => !u.isEqual(l)));
    }
    n.viewPosition = l;
  };
}
function ox(i, t, e) {
  for (; i.parent.is("attributeElement") && i.parent.getCustomProperty("listItemWrapper"); )
    t.unwrap(t.createRangeOn(i), i.parent);
  const n = [];
  r(t.createPositionBefore(i).getWalker({
    direction: "backward"
  })), r(t.createRangeIn(i).getWalker());
  for (const s of n)
    t.remove(s);
  function r(s) {
    for (const { item: o } of s) {
      if (o.is("element") && e.toModelElement(o))
        break;
      o.is("element") && o.getCustomProperty("listItemMarker") && n.push(o);
    }
  }
}
function Qz(i, t, e, n, { dataPipeline: r }) {
  let s = n.createRangeOn(t);
  if (!Ys(i))
    return s;
  for (const o of e) {
    if (o.scope != "itemMarker")
      continue;
    const a = o.createElement(n, i, {
      dataPipeline: r
    });
    if (!a || (n.setCustomProperty("listItemMarker", !0, a), o.canInjectMarkerIntoElement && o.canInjectMarkerIntoElement(i) ? n.insert(n.createPositionAt(t, 0), a) : (n.insert(s.start, a), s = n.createRange(n.createPositionBefore(a), n.createPositionAfter(t))), !o.createWrapperElement || !o.canWrapElement))
      continue;
    const l = o.createWrapperElement(n, i, {
      dataPipeline: r
    });
    n.setCustomProperty("listItemWrapper", !0, l), o.canWrapElement(i) ? s = n.wrap(s, l) : (s = n.wrap(n.createRangeOn(a), l), s = n.createRange(s.start, n.createPositionAfter(t)));
  }
  return s;
}
function eH(i, t) {
  let e = i.parent;
  for (; e.is("attributeElement") && [
    "ul",
    "ol",
    "li"
  ].includes(e.name); ) {
    const n = e.parent;
    t.unwrap(t.createRangeOn(i), e), e = n;
  }
}
function tH(i, t, e, n) {
  if (!i.hasAttribute("listIndent"))
    return;
  const r = i.getAttribute("listIndent");
  let s = i;
  for (let o = r; o >= 0; o--) {
    const a = Uz(n, o, s.getAttribute("listItemId")), l = Wz(n, o, s.getAttribute("listType"));
    for (const c of e)
      (c.scope == "list" || c.scope == "item") && s.hasAttribute(c.attributeName) && c.setAttributeOnDowncast(n, s.getAttribute(c.attributeName), c.scope == "list" ? l : a);
    if (t = n.wrap(t, a), t = n.wrap(t, l), o == 0 || (s = Li.first(s, {
      lowerIndent: !0
    }), !s))
      break;
  }
}
function nH(i) {
  return (t, e) => {
    const n = [];
    for (const r of i)
      t.hasAttribute(r) && n.push(`attribute:${r}`);
    return n.every((r) => e.test(t, r) !== !1) ? (n.forEach((r) => e.consume(t, r)), !0) : !1;
  };
}
function ax(i, t, e = Rp(i)) {
  if (!Rt(i))
    return !1;
  for (const n of i.getAttributeKeys())
    if (!n.startsWith("selection:") && !t.includes(n))
      return !1;
  return e.length < 2;
}
const Eu = [
  "listType",
  "listIndent",
  "listItemId"
];
class Am extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The list of registered downcast strategies.
    */
    h(this, "_downcastStrategies", []);
    e.config.define("list.multiBlock", !0);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Jd,
      Fr,
      zz,
      ui
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model, r = e.config.get("list.multiBlock");
    if (e.plugins.has("LegacyListEditing"))
      throw new T("list-feature-conflict", this, {
        conflictPlugin: "LegacyListEditing"
      });
    n.schema.register("$listItem", {
      allowAttributes: Eu
    }), r ? (n.schema.extend("$container", {
      allowAttributesOf: "$listItem"
    }), n.schema.extend("$block", {
      allowAttributesOf: "$listItem"
    }), n.schema.extend("$blockObject", {
      allowAttributesOf: "$listItem"
    })) : n.schema.register("listItem", {
      inheritAllFrom: "$block",
      allowAttributesOf: "$listItem"
    });
    for (const s of Eu)
      n.schema.setAttributeProperties(s, {
        copyOnReplace: !0
      });
    e.commands.add("numberedList", new Zc(e, "numbered")), e.commands.add("bulletedList", new Zc(e, "bulleted")), e.commands.add("customNumberedList", new Zc(e, "customNumbered", {
      multiLevel: !0
    })), e.commands.add("customBulletedList", new Zc(e, "customBulleted", {
      multiLevel: !0
    })), e.commands.add("indentList", new Ww(e, "forward")), e.commands.add("outdentList", new Ww(e, "backward")), e.commands.add("splitListItemBefore", new Gw(e, "before")), e.commands.add("splitListItemAfter", new Gw(e, "after")), r && (e.commands.add("mergeListItemBackward", new qw(e, "backward")), e.commands.add("mergeListItemForward", new qw(e, "forward"))), this._setupDeleteIntegration(), this._setupEnterIntegration(), this._setupTabIntegration(), this._setupClipboardIntegration(), this._setupAccessibilityIntegration();
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const n = this.editor.commands, r = n.get("indent"), s = n.get("outdent");
    r && r.registerChildCommand(n.get("indentList"), {
      priority: "high"
    }), s && s.registerChildCommand(n.get("outdentList"), {
      priority: "lowest"
    }), this._setupModelPostFixing(), this._setupConversion();
  }
  /**
  * Registers a downcast strategy.
  *
  * **Note**: Strategies must be registered in the `Plugin#init()` phase so that it can be applied
  * in the `ListEditing#afterInit()`.
  *
  * @param strategy The downcast strategy to register.
  */
  registerDowncastStrategy(e) {
    this._downcastStrategies.push(e);
  }
  /**
  * Returns list of model attribute names that should affect downcast conversion.
  */
  getListAttributeNames() {
    return [
      ...Eu,
      ...this._downcastStrategies.map((e) => e.attributeName)
    ];
  }
  /**
  * Attaches the listener to the {@link module:engine/view/document~Document#event:delete} event and handles backspace/delete
  * keys in and around document lists.
  */
  _setupDeleteIntegration() {
    const e = this.editor, n = e.commands.get("mergeListItemBackward"), r = e.commands.get("mergeListItemForward");
    this.listenTo(e.editing.view.document, "delete", (s, o) => {
      const a = e.model.document.selection;
      Ul(e.model) || e.model.change(() => {
        const l = a.getFirstPosition();
        if (a.isCollapsed && o.direction == "backward") {
          if (!l.isAtStart)
            return;
          const c = l.parent;
          if (!Rt(c))
            return;
          if (!Li.first(c, {
            sameAttributes: "listType",
            sameIndent: !0
          }) && c.getAttribute("listIndent") === 0)
            jw(c) || e.execute("splitListItemAfter"), e.execute("outdentList");
          else {
            if (!n || !n.isEnabled)
              return;
            n.execute({
              shouldMergeOnBlocksContentLevel: Yw(e.model, "backward")
            });
          }
          o.preventDefault(), s.stop();
        } else {
          if (a.isCollapsed && !a.getLastPosition().isAtEnd || !r || !r.isEnabled)
            return;
          r.execute({
            shouldMergeOnBlocksContentLevel: Yw(e.model, "forward")
          }), o.preventDefault(), s.stop();
        }
      });
    }, {
      context: "li"
    });
  }
  /**
  * Attaches a listener to the {@link module:engine/view/document~Document#event:enter} event and handles enter key press
  * in document lists.
  */
  _setupEnterIntegration() {
    const e = this.editor, n = e.model, r = e.commands, s = r.get("enter");
    this.listenTo(e.editing.view.document, "enter", (o, a) => {
      const l = n.document, c = l.selection.getFirstPosition().parent;
      if (l.selection.isCollapsed && Rt(c) && c.isEmpty && !a.isSoft) {
        const u = Ys(c), d = jw(c);
        u && d ? (e.execute("outdentList"), a.preventDefault(), o.stop()) : u && !d ? (e.execute("splitListItemAfter"), a.preventDefault(), o.stop()) : d && (e.execute("splitListItemBefore"), a.preventDefault(), o.stop());
      }
    }, {
      context: "li"
    }), this.listenTo(s, "afterExecute", () => {
      const o = r.get("splitListItemBefore");
      if (o.refresh(), !o.isEnabled)
        return;
      const l = e.model.document.selection.getLastPosition().parent;
      Rp(l).length === 2 && o.execute();
    });
  }
  /**
  * Attaches a listener to the {@link module:engine/view/document~Document#event:tab} event and handles tab key and tab+shift keys
  * presses in document lists.
  */
  _setupTabIntegration() {
    const e = this.editor;
    this.listenTo(e.editing.view.document, "tab", (n, r) => {
      const s = r.shiftKey ? "outdentList" : "indentList";
      this.editor.commands.get(s).isEnabled && (e.execute(s), r.stopPropagation(), r.preventDefault(), n.stop());
    }, {
      context: "li"
    });
  }
  /**
  * Registers the conversion helpers for the document-list feature.
  */
  _setupConversion() {
    const e = this.editor, n = e.model, r = this.getListAttributeNames(), s = e.config.get("list.multiBlock"), o = s ? "paragraph" : "listItem";
    e.conversion.for("upcast").elementToElement({
      view: "li",
      model: (l, { writer: c }) => c.createElement(o, {
        listType: ""
      })
    }).elementToElement({
      view: "p",
      model: (l, { writer: c }) => l.parent && l.parent.is("element", "li") ? c.createElement(o, {
        listType: ""
      }) : null,
      converterPriority: "high"
    }).add((l) => {
      l.on("element:li", Kz());
    }), s || e.conversion.for("downcast").elementToElement({
      model: "listItem",
      view: "p"
    }), e.conversion.for("editingDowncast").elementToElement({
      model: o,
      view: Zw(r),
      converterPriority: "high"
    }).add((l) => {
      l.on("attribute", Kw(r, this._downcastStrategies, n)), l.on("remove", Yz(n.schema));
    }), e.conversion.for("dataDowncast").elementToElement({
      model: o,
      view: Zw(r, {
        dataPipeline: !0
      }),
      converterPriority: "high"
    }).add((l) => {
      l.on("attribute", Kw(r, this._downcastStrategies, n, {
        dataPipeline: !0
      }));
    });
    const a = Xz(this._downcastStrategies, e.editing.view);
    e.editing.mapper.on("modelToViewPosition", a), e.data.mapper.on("modelToViewPosition", a), this.listenTo(n.document, "change:data", Zz(n, e.editing, r, this), {
      priority: "high"
    }), this.on("checkAttributes:item", (l, { viewElement: c, modelAttributes: u }) => {
      c.id != u.listItemId && (l.return = !0, l.stop());
    }), this.on("checkAttributes:list", (l, { viewElement: c, modelAttributes: u }) => {
      (c.name != rx(u.listType) || c.id != sx(u.listType, u.listIndent)) && (l.return = !0, l.stop());
    });
  }
  /**
  * Registers model post-fixers.
  */
  _setupModelPostFixing() {
    const e = this.editor.model, n = this.getListAttributeNames();
    e.document.registerPostFixer((r) => iH(e, r, n, this)), this.on("postFixer", (r, { listNodes: s, writer: o }) => {
      r.return = qz(s, o) || r.return;
    }, {
      priority: "high"
    }), this.on("postFixer", (r, { listNodes: s, writer: o, seenIds: a }) => {
      r.return = Gz(s, a, o) || r.return;
    }, {
      priority: "high"
    });
  }
  /**
  * Integrates the feature with the clipboard via {@link module:engine/model/model~Model#insertContent} and
  * {@link module:engine/model/model~Model#getSelectedContent}.
  */
  _setupClipboardIntegration() {
    const e = this.editor.model, n = this.editor.plugins.get("ClipboardPipeline");
    this.listenTo(e, "insertContent", rH(e), {
      priority: "high"
    }), this.listenTo(n, "outputTransformation", (r, s) => {
      e.change((o) => {
        const a = Array.from(s.content.getChildren()), l = a[a.length - 1];
        if (a.length > 1 && l.is("element") && l.isEmpty && a.slice(0, -1).every(Rt) && o.remove(l), s.method == "copy" || s.method == "cut") {
          const c = Array.from(s.content.getChildren());
          Wl(c) && ih(c, o);
        }
      });
    });
  }
  /**
  * Informs editor accessibility features about keystrokes brought by the plugin.
  */
  _setupAccessibilityIntegration() {
    const e = this.editor, n = e.t;
    e.accessibility.addKeystrokeInfoGroup({
      id: "list",
      label: n("Keystrokes that can be used in a list"),
      keystrokes: [
        {
          label: n("Increase list item indent"),
          keystroke: "Tab"
        },
        {
          label: n("Decrease list item indent"),
          keystroke: "Shift+Tab"
        }
      ]
    });
  }
}
function iH(i, t, e, n) {
  const r = i.document.differ.getChanges(), s = /* @__PURE__ */ new Map(), o = n.editor.config.get("list.multiBlock");
  let a = !1;
  for (const c of r) {
    if (c.type == "insert" && c.name != "$text") {
      const u = c.position.nodeAfter;
      if (!i.schema.checkAttribute(u, "listItemId"))
        for (const d of Array.from(u.getAttributeKeys()))
          e.includes(d) && (t.removeAttribute(d, u), a = !0);
      Ui(c.position, s), c.attributes.has("listItemId") || Ui(c.position.getShiftedBy(c.length), s);
      for (const { item: d, previousPosition: f } of i.createRangeIn(u))
        Rt(d) && Ui(f, s);
    } else c.type == "remove" ? Ui(c.position, s) : c.type == "attribute" && e.includes(c.attributeKey) && (Ui(c.range.start, s), c.attributeNewValue === null && Ui(c.range.start.getShiftedBy(1), s));
    if (!o && c.type == "attribute" && Eu.includes(c.attributeKey)) {
      const u = c.range.start.nodeAfter;
      c.attributeNewValue === null && u && u.is("element", "listItem") ? (t.rename(u, "paragraph"), a = !0) : c.attributeOldValue === null && u && u.is("element") && u.name != "listItem" && (t.rename(u, "listItem"), a = !0);
    }
  }
  const l = /* @__PURE__ */ new Set();
  for (const c of s.values())
    a = n.fire("postFixer", {
      listNodes: new Lz(c),
      listHead: c,
      writer: t,
      seenIds: l
    }) || a;
  return a;
}
function rH(i) {
  return (t, [e, n]) => {
    const r = e.is("documentFragment") ? Array.from(e.getChildren()) : [
      e
    ];
    if (!r.length)
      return;
    const o = (n ? i.createSelection(n) : i.document.selection).getFirstPosition();
    let a;
    if (Rt(o.parent))
      a = o.parent;
    else if (Rt(o.nodeBefore))
      a = o.nodeBefore;
    else
      return;
    i.change((l) => {
      const c = a.getAttribute("listType"), u = a.getAttribute("listIndent"), d = r[0].getAttribute("listIndent") || 0, f = Math.max(u - d, 0);
      for (const m of r) {
        const g = Rt(m);
        a.is("element", "listItem") && m.is("element", "paragraph") && l.rename(m, "listItem"), l.setAttributes({
          listIndent: (g ? m.getAttribute("listIndent") : 0) + f,
          listItemId: g ? m.getAttribute("listItemId") : oa.next(),
          listType: c
        }, m);
      }
    });
  };
}
function Yw(i, t) {
  const e = i.document.selection;
  if (!e.isCollapsed)
    return !Ul(i);
  if (t === "forward")
    return !0;
  const r = e.getFirstPosition().parent, s = r.previousSibling;
  return i.schema.isObject(s) ? !1 : s.isEmpty ? !0 : Wl([
    r,
    s
  ]);
}
function Jw(i, t, e, n) {
  i.ui.componentFactory.add(t, () => {
    const r = Xw(Te, i, t, e, n);
    return r.set({
      tooltip: !0,
      isToggleable: !0
    }), r;
  }), i.ui.componentFactory.add(`menuBar:${t}`, () => {
    const r = Xw(At, i, t, e, n);
    return r.set({
      role: "menuitemcheckbox",
      isToggleable: !0
    }), r;
  });
}
function Xw(i, t, e, n, r) {
  const s = t.commands.get(e), o = new i(t.locale);
  return o.set({
    label: n,
    icon: r
  }), o.bind("isOn", "isEnabled").to(s, "value", "isEnabled"), o.on("execute", () => {
    t.execute(e), t.editing.view.focus();
  }), o;
}
class sH extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.t;
    this.editor.ui.componentFactory.has("numberedList") || Jw(this.editor, "numberedList", t("Numbered List"), ee.numberedList), this.editor.ui.componentFactory.has("bulletedList") || Jw(this.editor, "bulletedList", t("Bulleted List"), ee.bulletedList);
  }
}
class oH extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Am,
      sH
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "List";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class aH extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this._getValue();
    this.value = t, this.isEnabled = t != null;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.startIndex The list start index.
  */
  execute({ startIndex: t = 1 } = {}) {
    const e = this.editor.model, n = e.document;
    let r = Array.from(n.selection.getSelectedBlocks()).filter((s) => Rt(s) && id(s.getAttribute("listType")));
    r = nh(r), e.change((s) => {
      for (const o of r)
        s.setAttribute("listStart", t >= 0 ? t : 1, o);
    });
  }
  /**
  * Checks the command's {@link #value}.
  *
  * @returns The current value.
  */
  _getValue() {
    const e = this.editor.model.document, n = Ft(e.selection.getSelectedBlocks());
    return n && Rt(n) && id(n.getAttribute("listType")) ? n.getAttribute("listStart") : null;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const lx = {}, cx = {}, ux = {}, dx = [
  {
    listStyle: "disc",
    typeAttribute: "disc",
    listType: "bulleted"
  },
  {
    listStyle: "circle",
    typeAttribute: "circle",
    listType: "bulleted"
  },
  {
    listStyle: "square",
    typeAttribute: "square",
    listType: "bulleted"
  },
  {
    listStyle: "decimal",
    typeAttribute: "1",
    listType: "numbered"
  },
  {
    listStyle: "decimal-leading-zero",
    typeAttribute: null,
    listType: "numbered"
  },
  {
    listStyle: "lower-roman",
    typeAttribute: "i",
    listType: "numbered"
  },
  {
    listStyle: "upper-roman",
    typeAttribute: "I",
    listType: "numbered"
  },
  {
    listStyle: "lower-alpha",
    typeAttribute: "a",
    listType: "numbered"
  },
  {
    listStyle: "upper-alpha",
    typeAttribute: "A",
    listType: "numbered"
  },
  {
    listStyle: "lower-latin",
    typeAttribute: "a",
    listType: "numbered"
  },
  {
    listStyle: "upper-latin",
    typeAttribute: "A",
    listType: "numbered"
  }
];
for (const { listStyle: i, typeAttribute: t, listType: e } of dx)
  lx[i] = e, cx[i] = t, t && (ux[t] = i);
function hx() {
  return dx.map((i) => i.listStyle);
}
function Vp(i) {
  return lx[i] || null;
}
function fx(i) {
  return ux[i] || null;
}
function Pm(i) {
  return cx[i] || null;
}
class lH extends Ve {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param defaultType The list type that will be used by default if the value was not specified during
  * the command execution.
  * @param supportedTypes The list of supported style types by this command.
  */
  constructor(e, n, r) {
    super(e);
    /**
    * The default type of the list style.
    */
    h(this, "defaultType");
    /**
    * The list of supported style types by this command.
    */
    h(this, "_supportedTypes");
    this.defaultType = n, this._supportedTypes = r;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.value = this._getValue(), this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.type The type of the list style, e.g. `'disc'` or `'square'`. If `null` is specified, the default
  * style will be applied.
  */
  execute(e = {}) {
    const n = this.editor.model, r = n.document;
    n.change((s) => {
      this._tryToConvertItemsToList(e);
      let o = Array.from(r.selection.getSelectedBlocks()).filter((a) => a.hasAttribute("listType"));
      if (o.length) {
        o = nh(o);
        for (const a of o)
          s.setAttribute("listStyle", e.type || this.defaultType, a);
      }
    });
  }
  /**
  * Checks if the given style type is supported by this plugin.
  */
  isStyleTypeSupported(e) {
    return this._supportedTypes ? this._supportedTypes.includes(e) : !0;
  }
  /**
  * Checks the command's {@link #value}.
  *
  * @returns The current value.
  */
  _getValue() {
    const e = Ft(this.editor.model.document.selection.getSelectedBlocks());
    return Rt(e) ? e.getAttribute("listStyle") : null;
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    const e = this.editor, n = e.commands.get("numberedList"), r = e.commands.get("bulletedList");
    return n.isEnabled || r.isEnabled;
  }
  /**
   * Check if the provided list style is valid. Also change the selection to a list if it's not set yet.
   *
   * @param options.type The type of the list style. If `null` is specified, the function does nothing.
  */
  _tryToConvertItemsToList(e) {
    if (!e.type)
      return;
    const n = Vp(e.type);
    if (!n)
      return;
    const r = this.editor, s = `${n}List`;
    r.commands.get(s).value || r.execute(s);
  }
}
class cH extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this._getValue();
    this.value = t, this.isEnabled = t != null;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.reversed Whether the list should be reversed.
  */
  execute(t = {}) {
    const e = this.editor.model, n = e.document;
    let r = Array.from(n.selection.getSelectedBlocks()).filter((s) => Rt(s) && s.getAttribute("listType") == "numbered");
    r = nh(r), e.change((s) => {
      for (const o of r)
        s.setAttribute("listReversed", !!t.reversed, o);
    });
  }
  /**
  * Checks the command's {@link #value}.
  */
  _getValue() {
    const e = this.editor.model.document, n = Ft(e.selection.getSelectedBlocks());
    return Rt(n) && n.getAttribute("listType") == "numbered" ? n.getAttribute("listReversed") : null;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function Qw(i) {
  return (t, e, n) => {
    const { writer: r, schema: s, consumable: o } = n;
    if (o.test(e.viewItem, i.viewConsumables) === !1)
      return;
    e.modelRange || Object.assign(e, n.convertChildren(e.viewItem, e.modelCursor));
    let a = !1;
    for (const l of e.modelRange.getItems({
      shallow: !0
    }))
      s.checkAttribute(l, i.attributeName) && i.appliesToListItem(l) && (l.hasAttribute(i.attributeName) || (r.setAttribute(i.attributeName, i.getAttributeOnUpcast(e.viewItem), l), a = !0));
    a && o.consume(e.viewItem, i.viewConsumables);
  };
}
class uH extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListPropertiesUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Gets all the style types supported by given list type.
  */
  getAllSupportedStyleTypes() {
    return hx();
  }
  /**
  * Checks whether the given list-style-type is supported by numbered or bulleted list.
  */
  getListTypeFromListStyleType(t) {
    return Vp(t);
  }
  /**
  * Converts `type` attribute of `<ul>` or `<ol>` elements to `list-style-type` equivalent.
  */
  getListStyleTypeFromTypeAttribute(t) {
    return fx(t);
  }
  /**
  * Converts `list-style-type` style to `type` attribute of `<ul>` or `<ol>` elements.
  */
  getTypeAttributeFromListStyleType(t) {
    return Pm(t);
  }
}
function mx(i) {
  const { startIndex: t, reversed: e, styles: n } = i;
  return {
    styles: dH(n),
    startIndex: t || !1,
    reversed: e || !1
  };
}
function dH(i) {
  const t = {
    listTypes: [
      "bulleted",
      "numbered"
    ],
    useAttribute: !1
  };
  return i === !0 || (i ? Array.isArray(i) || typeof i == "string" ? t.listTypes = tt(i) : (t.listTypes = i.listTypes ? tt(i.listTypes) : t.listTypes, t.useAttribute = !!i.useAttribute) : t.listTypes = []), t;
}
const Ka = "default";
class hH extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Am,
      uH
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListPropertiesEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("list.properties", {
      styles: !0,
      startIndex: !1,
      reversed: !1
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model, n = t.plugins.get(Am), r = t.config.get("list.properties"), s = fH(r);
    for (const o of s)
      o.addCommand(t), e.schema.extend("$listItem", {
        allowAttributes: o.attributeName
      }), n.registerDowncastStrategy({
        scope: "list",
        attributeName: o.attributeName,
        setAttributeOnDowncast(a, l, c) {
          o.setAttributeOnDowncast(a, l, c);
        }
      });
    t.conversion.for("upcast").add((o) => {
      for (const a of s)
        o.on("element:ol", Qw(a)), o.on("element:ul", Qw(a));
    }), n.on("checkAttributes:list", (o, { viewElement: a, modelAttributes: l }) => {
      for (const c of s)
        c.getAttributeOnUpcast(a) != l[c.attributeName] && (o.return = !0, o.stop());
    }), this.listenTo(t.commands.get("indentList"), "afterExecute", (o, a) => {
      e.change((l) => {
        for (const c of a)
          for (const u of s)
            u.appliesToListItem(c) && l.setAttribute(u.attributeName, u.defaultValue, c);
      });
    }), n.on("postFixer", (o, { listNodes: a, writer: l }) => {
      for (const { node: c } of a)
        for (const u of s)
          u.hasValidAttribute(c) || (u.appliesToListItem(c) ? l.setAttribute(u.attributeName, u.defaultValue, c) : l.removeAttribute(u.attributeName, c), o.return = !0);
    }), n.on("postFixer", (o, { listNodes: a, writer: l }) => {
      for (const { node: c, previousNodeInList: u } of a)
        if (u && u.getAttribute("listType") == c.getAttribute("listType"))
          for (const d of s) {
            const { attributeName: f } = d;
            if (!d.appliesToListItem(c))
              continue;
            const m = u.getAttribute(f);
            c.getAttribute(f) != m && (l.setAttribute(f, m, c), o.return = !0);
          }
    });
  }
}
function fH(i) {
  const t = [], e = mx(i);
  if (i.styles) {
    const n = e.styles.useAttribute;
    t.push({
      attributeName: "listStyle",
      defaultValue: Ka,
      viewConsumables: {
        styles: "list-style-type"
      },
      addCommand(r) {
        let s = hx();
        n && (s = s.filter((o) => !!Pm(o))), r.commands.add("listStyle", new lH(r, Ka, s));
      },
      appliesToListItem(r) {
        return r.getAttribute("listType") == "numbered" || r.getAttribute("listType") == "bulleted";
      },
      hasValidAttribute(r) {
        if (!this.appliesToListItem(r))
          return !r.hasAttribute("listStyle");
        if (!r.hasAttribute("listStyle"))
          return !1;
        const s = r.getAttribute("listStyle");
        return s == Ka ? !0 : Vp(s) == r.getAttribute("listType");
      },
      setAttributeOnDowncast(r, s, o) {
        if (s && s !== Ka)
          if (n) {
            const a = Pm(s);
            if (a) {
              r.setAttribute("type", a, o);
              return;
            }
          } else {
            r.setStyle("list-style-type", s, o);
            return;
          }
        r.removeStyle("list-style-type", o), r.removeAttribute("type", o);
      },
      getAttributeOnUpcast(r) {
        const s = r.getStyle("list-style-type");
        if (s)
          return s;
        const o = r.getAttribute("type");
        return o ? fx(o) : Ka;
      }
    });
  }
  return i.reversed && t.push({
    attributeName: "listReversed",
    defaultValue: !1,
    viewConsumables: {
      attributes: "reversed"
    },
    addCommand(n) {
      n.commands.add("listReversed", new cH(n));
    },
    appliesToListItem(n) {
      return n.getAttribute("listType") == "numbered";
    },
    hasValidAttribute(n) {
      return this.appliesToListItem(n) == n.hasAttribute("listReversed");
    },
    setAttributeOnDowncast(n, r, s) {
      r ? n.setAttribute("reversed", "reversed", s) : n.removeAttribute("reversed", s);
    },
    getAttributeOnUpcast(n) {
      return n.hasAttribute("reversed");
    }
  }), i.startIndex && t.push({
    attributeName: "listStart",
    defaultValue: 1,
    viewConsumables: {
      attributes: "start"
    },
    addCommand(n) {
      n.commands.add("listStart", new aH(n));
    },
    appliesToListItem(n) {
      return id(n.getAttribute("listType"));
    },
    hasValidAttribute(n) {
      return this.appliesToListItem(n) == n.hasAttribute("listStart");
    },
    setAttributeOnDowncast(n, r, s) {
      r == 0 || r > 1 ? n.setAttribute("start", r, s) : n.removeAttribute("start", s);
    },
    getAttributeOnUpcast(n) {
      const r = n.getAttribute("start");
      return r >= 0 ? r : 1;
    }
  }), t;
}
class gx extends de {
  /**
  * Creates an instance of the list properties view.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param options Options of the view.
  * @param options.enabledProperties An object containing the configuration of enabled list property names.
  * Allows conditional rendering the sub-components of the properties view.
  * @param options.styleButtonViews A list of style buttons to be rendered
  * inside the styles grid. The grid will not be rendered when `enabledProperties` does not include the `'styles'` key.
  * @param options.styleGridAriaLabel An assistive technologies label set on the grid of styles (if the grid is rendered).
  */
  constructor(e, { enabledProperties: n, styleButtonViews: r, styleGridAriaLabel: s }) {
    super(e);
    /**
    * A collection of the child views.
    */
    h(this, "children");
    /**
    * A view that renders the grid of list styles.
    */
    h(this, "stylesView", null);
    /**
    * A collapsible view that hosts additional list property fields ({@link #startIndexFieldView} and
    * {@link #reversedSwitchButtonView}) to visually separate them from the {@link #stylesView grid of styles}.
    *
    * **Note**: Only present when:
    * * the view represents **numbered** list properties,
    * * and the {@link #stylesView} is rendered,
    * * and either {@link #startIndexFieldView} or {@link #reversedSwitchButtonView} is rendered.
    *
    * @readonly
    */
    h(this, "additionalPropertiesCollapsibleView", null);
    /**
    * A labeled number field allowing the user to set the start index of the list.
    *
    * **Note**: Only present when the view represents **numbered** list properties.
    *
    * @readonly
    */
    h(this, "startIndexFieldView", null);
    /**
    * A switch button allowing the user to make the edited list reversed.
    *
    * **Note**: Only present when the view represents **numbered** list properties.
    *
    * @readonly
    */
    h(this, "reversedSwitchButtonView", null);
    /**
    * Tracks information about the DOM focus in the view.
    */
    h(this, "focusTracker", new wt());
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes", new Ot());
    /**
    * A collection of views that can be focused in the properties view.
    */
    h(this, "focusables", new In());
    /**
    * Helps cycling over {@link #focusables} in the view.
    */
    h(this, "focusCycler");
    const o = [
      "ck",
      "ck-list-properties"
    ];
    this.children = this.createCollection(), this.focusCycler = new jn({
      focusables: this.focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate #children backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: "shift + tab",
        // Navigate #children forwards using the <kbd>Tab</kbd> key.
        focusNext: "tab"
      }
    }), r && r.length ? (this.stylesView = this._createStylesView(r, s), this.children.add(this.stylesView)) : o.push("ck-list-properties_without-styles"), (n.startIndex || n.reversed) && (this._addNumberedListPropertyViews(n), o.push("ck-list-properties_with-numbered-properties")), this.setTemplate({
      tag: "div",
      attributes: {
        class: o
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    if (super.render(), this.stylesView) {
      this.focusables.add(this.stylesView), this.focusTracker.add(this.stylesView.element), (this.startIndexFieldView || this.reversedSwitchButtonView) && (this.focusables.add(this.children.last.buttonView), this.focusTracker.add(this.children.last.buttonView.element));
      for (const e of this.stylesView.children)
        this.stylesView.focusTracker.add(e.element);
      mp({
        keystrokeHandler: this.stylesView.keystrokes,
        focusTracker: this.stylesView.focusTracker,
        gridItems: this.stylesView.children,
        // Note: The styles view has a different number of columns depending on whether the other properties
        // are enabled in the dropdown or not (https://github.com/ckeditor/ckeditor5/issues/12340)
        numberOfColumns: () => ae.window.getComputedStyle(this.stylesView.element).getPropertyValue("grid-template-columns").split(" ").length,
        uiLanguageDirection: this.locale && this.locale.uiLanguageDirection
      });
    }
    if (this.startIndexFieldView) {
      this.focusables.add(this.startIndexFieldView), this.focusTracker.add(this.startIndexFieldView.element);
      const e = (n) => n.stopPropagation();
      this.keystrokes.set("arrowright", e), this.keystrokes.set("arrowleft", e), this.keystrokes.set("arrowup", e), this.keystrokes.set("arrowdown", e);
    }
    this.reversedSwitchButtonView && (this.focusables.add(this.reversedSwitchButtonView), this.focusTracker.add(this.reversedSwitchButtonView.element)), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  focus() {
    this.focusCycler.focusFirst();
  }
  /**
  * @inheritDoc
  */
  focusLast() {
    this.focusCycler.focusLast();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates the list styles grid.
  *
  * @param styleButtons Buttons to be placed in the grid.
  * @param styleGridAriaLabel The assistive technology label of the grid.
  */
  _createStylesView(e, n) {
    const r = new de(this.locale);
    return r.children = r.createCollection(), r.children.addMany(e), r.setTemplate({
      tag: "div",
      attributes: {
        "aria-label": n,
        class: [
          "ck",
          "ck-list-styles-list"
        ]
      },
      children: r.children
    }), r.children.delegate("execute").to(this), r.focus = function() {
      this.children.first.focus();
    }, r.focusTracker = new wt(), r.keystrokes = new Ot(), r.render(), r.keystrokes.listenTo(r.element), r;
  }
  /**
  * Renders {@link #startIndexFieldView} and/or {@link #reversedSwitchButtonView} depending on the configuration of the properties view.
  *
  * @param enabledProperties An object containing the configuration of enabled list property names
  * (see {@link #constructor}).
  */
  _addNumberedListPropertyViews(e) {
    const n = this.locale.t, r = [];
    e.startIndex && (this.startIndexFieldView = this._createStartIndexField(), r.push(this.startIndexFieldView)), e.reversed && (this.reversedSwitchButtonView = this._createReversedSwitchButton(), r.push(this.reversedSwitchButtonView)), this.stylesView ? (this.additionalPropertiesCollapsibleView = new gp(this.locale, r), this.additionalPropertiesCollapsibleView.set({
      label: n("List properties"),
      isCollapsed: !0
    }), this.additionalPropertiesCollapsibleView.buttonView.bind("isEnabled").toMany(r, "isEnabled", (...s) => s.some((o) => o)), this.additionalPropertiesCollapsibleView.buttonView.on("change:isEnabled", (s, o, a) => {
      a || (this.additionalPropertiesCollapsibleView.isCollapsed = !0);
    }), this.children.add(this.additionalPropertiesCollapsibleView)) : this.children.addMany(r);
  }
  /**
  * Creates the list start index labeled field.
  */
  _createStartIndexField() {
    const e = this.locale.t, n = new Qt(this.locale, XC);
    return n.set({
      label: e("Start at"),
      class: "ck-numbered-list-properties__start-index"
    }), n.fieldView.set({
      min: 0,
      step: 1,
      value: 1,
      inputMode: "numeric"
    }), n.fieldView.on("input", () => {
      const r = n.fieldView.element, s = r.valueAsNumber;
      if (Number.isNaN(s)) {
        n.errorText = e("Invalid start index value.");
        return;
      }
      r.checkValidity() ? this.fire("listStart", {
        startIndex: s
      }) : n.errorText = e("Start index must be greater than 0.");
    }), n;
  }
  /**
  * Creates the reversed list switch button.
  */
  _createReversedSwitchButton() {
    const e = this.locale.t, n = new Gs(this.locale);
    return n.set({
      withText: !0,
      label: e("Reversed order"),
      class: "ck-numbered-list-properties__reversed-order"
    }), n.delegate("execute").to(this, "listReversed"), n;
  }
}
var mH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M11 27a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0-9a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0-9a3 3 0 1 1 0 6 3 3 0 0 1 0-6z"/></svg>', gH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M11 27a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0 1a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm0-10a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0 1a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm0-10a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0 1a2 2 0 1 0 0 4 2 2 0 0 0 0-4z"/></svg>', pH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M14 27v6H8v-6h6zm0-9v6H8v-6h6zm0-9v6H8V9h6z"/></svg>', bH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M10.29 15V8.531H9.286c-.14.393-.4.736-.778 1.03-.378.295-.728.495-1.05.6v1.121a4.257 4.257 0 0 0 1.595-.936V15h1.235zm3.343 0v-1.235h-1.235V15h1.235zM11.3 24v-1.147H8.848c.064-.111.148-.226.252-.343.104-.117.351-.354.74-.712.39-.357.66-.631.81-.821.225-.288.39-.562.494-.824.104-.263.156-.539.156-.829 0-.51-.182-.936-.545-1.279-.363-.342-.863-.514-1.499-.514-.58 0-1.063.148-1.45.444-.387.296-.617.784-.69 1.463l1.23.124c.024-.36.112-.619.264-.774.153-.155.358-.233.616-.233.26 0 .465.074.613.222.148.148.222.36.222.635 0 .25-.085.501-.255.756-.126.185-.468.536-1.024 1.055-.692.641-1.155 1.156-1.389 1.544-.234.389-.375.8-.422 1.233H11.3zm2.333 0v-1.235h-1.235V24h1.235zM9.204 34.11c.615 0 1.129-.2 1.542-.598.413-.398.62-.88.62-1.446 0-.39-.11-.722-.332-.997a1.5 1.5 0 0 0-.886-.532c.619-.337.928-.788.928-1.353 0-.399-.151-.756-.453-1.073-.366-.386-.852-.58-1.459-.58a2.25 2.25 0 0 0-.96.2 1.617 1.617 0 0 0-.668.55c-.16.232-.28.544-.358.933l1.138.194c.032-.282.123-.495.272-.642.15-.146.33-.22.54-.22.215 0 .386.065.515.194s.193.302.193.518c0 .255-.087.46-.263.613-.176.154-.43.227-.765.218l-.136 1.006c.22-.061.409-.092.567-.092.24 0 .444.09.61.272.168.182.251.428.251.739 0 .328-.087.589-.261.782a.833.833 0 0 1-.644.29.841.841 0 0 1-.607-.242c-.167-.16-.27-.394-.307-.698l-1.196.145c.062.542.285.98.668 1.316.384.335.868.503 1.45.503zm4.43-.11v-1.235h-1.236V34h1.235z"/></svg>', vH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M5.714 15.11c.624 0 1.11-.22 1.46-.66.421-.533.632-1.408.632-2.627 0-1.222-.21-2.096-.629-2.624-.351-.445-.839-.668-1.463-.668-.624 0-1.11.22-1.459.66-.422.533-.633 1.406-.633 2.619 0 1.236.192 2.095.576 2.577.384.482.89.723 1.516.723zm0-1.024a.614.614 0 0 1-.398-.14c-.115-.094-.211-.283-.287-.565-.077-.283-.115-.802-.115-1.558s.043-1.294.128-1.613c.064-.246.155-.417.272-.512a.617.617 0 0 1 .4-.143.61.61 0 0 1 .398.143c.116.095.211.284.288.567.076.283.114.802.114 1.558s-.043 1.292-.128 1.608c-.064.246-.155.417-.272.512a.617.617 0 0 1-.4.143zm6.078.914V8.531H10.79c-.14.393-.4.736-.778 1.03-.378.295-.728.495-1.05.6v1.121a4.257 4.257 0 0 0 1.595-.936V15h1.235zm3.344 0v-1.235h-1.235V15h1.235zm-9.422 9.11c.624 0 1.11-.22 1.46-.66.421-.533.632-1.408.632-2.627 0-1.222-.21-2.096-.629-2.624-.351-.445-.839-.668-1.463-.668-.624 0-1.11.22-1.459.66-.422.533-.633 1.406-.633 2.619 0 1.236.192 2.095.576 2.577.384.482.89.723 1.516.723zm0-1.024a.614.614 0 0 1-.398-.14c-.115-.094-.211-.283-.287-.565-.077-.283-.115-.802-.115-1.558s.043-1.294.128-1.613c.064-.246.155-.417.272-.512a.617.617 0 0 1 .4-.143.61.61 0 0 1 .398.143c.116.095.211.284.288.567.076.283.114.802.114 1.558s-.043 1.292-.128 1.608c-.064.246-.155.417-.272.512a.617.617 0 0 1-.4.143zm7.088.914v-1.147H10.35c.065-.111.149-.226.253-.343.104-.117.35-.354.74-.712.39-.357.66-.631.81-.821.225-.288.39-.562.493-.824.104-.263.156-.539.156-.829 0-.51-.181-.936-.544-1.279-.364-.342-.863-.514-1.499-.514-.58 0-1.063.148-1.45.444-.387.296-.617.784-.69 1.463l1.23.124c.024-.36.112-.619.264-.774.152-.155.357-.233.615-.233.261 0 .465.074.613.222.148.148.222.36.222.635 0 .25-.085.501-.255.756-.126.185-.467.536-1.024 1.055-.691.641-1.154 1.156-1.388 1.544-.235.389-.375.8-.422 1.233h4.328zm2.334 0v-1.235h-1.235V24h1.235zM5.714 34.11c.624 0 1.11-.22 1.46-.66.421-.533.632-1.408.632-2.627 0-1.222-.21-2.096-.629-2.624-.351-.445-.839-.668-1.463-.668-.624 0-1.11.22-1.459.66-.422.533-.633 1.406-.633 2.619 0 1.236.192 2.095.576 2.577.384.482.89.723 1.516.723zm0-1.024a.614.614 0 0 1-.398-.14c-.115-.094-.211-.283-.287-.565-.077-.283-.115-.802-.115-1.558s.043-1.294.128-1.613c.064-.246.155-.417.272-.512a.617.617 0 0 1 .4-.143.61.61 0 0 1 .398.143c.116.095.211.284.288.567.076.283.114.802.114 1.558s-.043 1.292-.128 1.608c-.064.246-.155.417-.272.512a.617.617 0 0 1-.4.143zm4.992 1.024c.616 0 1.13-.2 1.543-.598.413-.398.62-.88.62-1.446 0-.39-.111-.722-.332-.997a1.5 1.5 0 0 0-.886-.532c.618-.337.927-.788.927-1.353 0-.399-.15-.756-.452-1.073-.366-.386-.853-.58-1.46-.58a2.25 2.25 0 0 0-.96.2 1.617 1.617 0 0 0-.667.55c-.16.232-.28.544-.359.933l1.139.194c.032-.282.123-.495.272-.642.15-.146.33-.22.54-.22.214 0 .386.065.515.194s.193.302.193.518c0 .255-.088.46-.264.613-.175.154-.43.227-.764.218l-.136 1.006c.22-.061.408-.092.566-.092.24 0 .444.09.611.272.167.182.25.428.25.739 0 .328-.086.589-.26.782a.833.833 0 0 1-.644.29.841.841 0 0 1-.607-.242c-.167-.16-.27-.394-.308-.698l-1.195.145c.062.542.284.98.668 1.316.384.335.867.503 1.45.503zm4.43-.11v-1.235h-1.235V34h1.235z"/></svg>', wH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M11.88 8.7V7.558h-1.234V8.7h1.234zm0 5.3V9.333h-1.234V14h1.234zm2.5 0v-1.235h-1.234V14h1.235zm-4.75 4.7v-1.142H8.395V18.7H9.63zm0 5.3v-4.667H8.395V24H9.63zm2.5-5.3v-1.142h-1.234V18.7h1.235zm0 5.3v-4.667h-1.234V24h1.235zm2.501 0v-1.235h-1.235V24h1.235zM7.38 28.7v-1.142H6.145V28.7H7.38zm0 5.3v-4.667H6.145V34H7.38zm2.5-5.3v-1.142H8.646V28.7H9.88zm0 5.3v-4.667H8.646V34H9.88zm2.5-5.3v-1.142h-1.234V28.7h1.235zm0 5.3v-4.667h-1.234V34h1.235zm2.501 0v-1.235h-1.235V34h1.235z"/></svg>', yH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M11.916 15V8.558h-1.301V15h1.3zm2.465 0v-1.235h-1.235V15h1.235zM9.665 25v-6.442h-1.3V25h1.3zm2.5 0v-6.442h-1.3V25h1.3zm2.466 0v-1.235h-1.235V25h1.235zm-7.216 9v-6.442h-1.3V34h1.3zm2.5 0v-6.442h-1.3V34h1.3zm2.501 0v-6.442h-1.3V34h1.3zm2.465 0v-1.235h-1.235V34h1.235z"/></svg>', _H = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M9.62 14.105c.272 0 .528-.05.768-.153s.466-.257.677-.462c.009.024.023.072.044.145.047.161.086.283.119.365h1.221a2.649 2.649 0 0 1-.222-.626c-.04-.195-.059-.498-.059-.908l.013-1.441c0-.536-.055-.905-.165-1.105-.11-.201-.3-.367-.569-.497-.27-.13-.68-.195-1.23-.195-.607 0-1.064.108-1.371.325-.308.217-.525.55-.65 1.002l1.12.202c.076-.217.176-.369.299-.455.123-.086.294-.13.514-.13.325 0 .546.05.663.152.118.101.176.27.176.508v.123c-.222.093-.622.194-1.2.303-.427.082-.755.178-.982.288-.227.11-.403.268-.53.474a1.327 1.327 0 0 0-.188.706c0 .398.138.728.415.988.277.261.656.391 1.136.391zm.368-.87a.675.675 0 0 1-.492-.189.606.606 0 0 1-.193-.448c0-.176.08-.32.241-.435.106-.07.33-.142.673-.215a7.19 7.19 0 0 0 .751-.19v.247c0 .296-.016.496-.048.602a.773.773 0 0 1-.295.409 1.07 1.07 0 0 1-.637.22zm4.645.765v-1.235h-1.235V14h1.235zM10.2 25.105c.542 0 1.003-.215 1.382-.646.38-.43.57-1.044.57-1.84 0-.771-.187-1.362-.559-1.774a1.82 1.82 0 0 0-1.41-.617c-.522 0-.973.216-1.354.65v-2.32H7.594V25h1.147v-.686a1.9 1.9 0 0 0 .67.592c.26.133.523.2.79.2zm-.299-.975c-.354 0-.638-.164-.852-.492-.153-.232-.229-.59-.229-1.073 0-.468.098-.818.295-1.048a.93.93 0 0 1 .738-.345c.302 0 .55.118.743.354.193.236.29.62.29 1.154 0 .5-.096.868-.288 1.1-.192.233-.424.35-.697.35zm4.478.87v-1.235h-1.234V25h1.234zm-4.017 9.105c.6 0 1.08-.142 1.437-.426.357-.284.599-.704.725-1.261l-1.213-.207c-.061.326-.167.555-.316.688a.832.832 0 0 1-.576.2.916.916 0 0 1-.75-.343c-.185-.228-.278-.62-.278-1.173 0-.498.091-.853.274-1.066.183-.212.429-.318.736-.318.232 0 .42.061.565.184.145.123.238.306.28.55l1.216-.22c-.146-.501-.387-.874-.722-1.119-.336-.244-.788-.366-1.356-.366-.695 0-1.245.214-1.653.643-.407.43-.61 1.03-.61 1.8 0 .762.202 1.358.608 1.788.406.431.95.646 1.633.646zM14.633 34v-1.235h-1.235V34h1.235z"/></svg>', CH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="m7.88 15 .532-1.463h2.575L11.549 15h1.415l-2.58-6.442H9.01L6.5 15h1.38zm2.69-2.549H8.811l.87-2.39.887 2.39zM14.88 15v-1.235h-1.234V15h1.234zM9.352 25c.83-.006 1.352-.02 1.569-.044.346-.038.636-.14.872-.305.236-.166.422-.387.558-.664.137-.277.205-.562.205-.855 0-.372-.106-.695-.317-.97-.21-.276-.512-.471-.905-.585a1.51 1.51 0 0 0 .661-.567 1.5 1.5 0 0 0 .244-.83c0-.28-.066-.53-.197-.754a1.654 1.654 0 0 0-.495-.539 1.676 1.676 0 0 0-.672-.266c-.25-.042-.63-.063-1.14-.063H7.158V25h2.193zm.142-3.88H8.46v-1.49h.747c.612 0 .983.007 1.112.022.217.026.38.102.49.226.11.125.165.287.165.486a.68.68 0 0 1-.192.503.86.86 0 0 1-.525.23 11.47 11.47 0 0 1-.944.023h.18zm.17 2.795H8.46v-1.723h1.05c.592 0 .977.03 1.154.092.177.062.313.16.406.295a.84.84 0 0 1 .14.492c0 .228-.06.41-.181.547a.806.806 0 0 1-.473.257c-.126.026-.423.04-.892.04zM14.88 25v-1.235h-1.234V25h1.234zm-5.018 9.11c.691 0 1.262-.17 1.711-.512.45-.341.772-.864.965-1.567l-1.261-.4c-.109.472-.287.818-.536 1.037-.25.22-.547.33-.892.33-.47 0-.85-.173-1.143-.519-.293-.345-.44-.925-.44-1.74 0-.767.15-1.322.447-1.665.297-.343.684-.514 1.162-.514.346 0 .64.096.881.29.242.193.4.457.477.79l1.288-.307c-.147-.516-.367-.911-.66-1.187-.492-.465-1.132-.698-1.92-.698-.902 0-1.63.296-2.184.89-.554.593-.83 1.426-.83 2.498 0 1.014.275 1.813.825 2.397.551.585 1.254.877 2.11.877zM14.88 34v-1.235h-1.234V34h1.234z"/></svg>';
class kH extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListPropertiesUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  init() {
    const t = this.editor, e = t.locale.t, n = t.config.get("list.properties"), r = mx(n), s = r.styles.listTypes;
    if (s.includes("bulleted")) {
      const o = [
        {
          label: e("Toggle the disc list style"),
          tooltip: e("Disc"),
          type: "disc",
          icon: mH
        },
        {
          label: e("Toggle the circle list style"),
          tooltip: e("Circle"),
          type: "circle",
          icon: gH
        },
        {
          label: e("Toggle the square list style"),
          tooltip: e("Square"),
          type: "square",
          icon: pH
        }
      ], a = e("Bulleted List"), l = e("Bulleted list styles toolbar"), c = "bulletedList";
      t.ui.componentFactory.add(c, e0({
        editor: t,
        normalizedConfig: r,
        parentCommandName: c,
        buttonLabel: a,
        buttonIcon: ee.bulletedList,
        styleGridAriaLabel: l,
        styleDefinitions: o
      })), t.ui.componentFactory.add(`menuBar:${c}`, t0({
        editor: t,
        normalizedConfig: r,
        parentCommandName: c,
        buttonLabel: a,
        styleGridAriaLabel: l,
        styleDefinitions: o
      }));
    }
    if (s.includes("numbered") || n.startIndex || n.reversed) {
      const o = [
        {
          label: e("Toggle the decimal list style"),
          tooltip: e("Decimal"),
          type: "decimal",
          icon: bH
        },
        {
          label: e("Toggle the decimal with leading zero list style"),
          tooltip: e("Decimal with leading zero"),
          type: "decimal-leading-zero",
          icon: vH
        },
        {
          label: e("Toggle the lower–roman list style"),
          tooltip: e("Lower–roman"),
          type: "lower-roman",
          icon: wH
        },
        {
          label: e("Toggle the upper–roman list style"),
          tooltip: e("Upper-roman"),
          type: "upper-roman",
          icon: yH
        },
        {
          label: e("Toggle the lower–latin list style"),
          tooltip: e("Lower-latin"),
          type: "lower-latin",
          icon: _H
        },
        {
          label: e("Toggle the upper–latin list style"),
          tooltip: e("Upper-latin"),
          type: "upper-latin",
          icon: CH
        }
      ], a = e("Numbered List"), l = e("Numbered list styles toolbar"), c = "numberedList";
      t.ui.componentFactory.add(c, e0({
        editor: t,
        normalizedConfig: r,
        parentCommandName: c,
        buttonLabel: a,
        buttonIcon: ee.numberedList,
        styleGridAriaLabel: l,
        styleDefinitions: o
      })), s.includes("numbered") && t.ui.componentFactory.add(`menuBar:${c}`, t0({
        editor: t,
        normalizedConfig: r,
        parentCommandName: c,
        buttonLabel: a,
        styleGridAriaLabel: l,
        styleDefinitions: o
      }));
    }
  }
}
function e0({ editor: i, normalizedConfig: t, parentCommandName: e, buttonLabel: n, buttonIcon: r, styleGridAriaLabel: s, styleDefinitions: o }) {
  const a = i.commands.get(e);
  return (l) => {
    const c = Yn(l, io), u = c.buttonView;
    return c.bind("isEnabled").to(a), c.class = "ck-list-styles-dropdown", u.on("execute", () => {
      i.execute(e), i.editing.view.focus();
    }), u.set({
      label: n,
      icon: r,
      tooltip: !0,
      isToggleable: !0
    }), u.bind("isOn").to(a, "value", (d) => !!d), c.once("change:isOpen", () => {
      const d = xH({
        editor: i,
        normalizedConfig: t,
        dropdownView: c,
        parentCommandName: e,
        styleGridAriaLabel: s,
        styleDefinitions: o
      });
      c.panelView.children.add(d);
    }), c.on("execute", () => {
      i.editing.view.focus();
    }), c;
  };
}
function px({ editor: i, listStyleCommand: t, parentCommandName: e }) {
  const n = i.locale, r = i.commands.get(e);
  return ({ label: s, type: o, icon: a, tooltip: l }) => {
    const c = new Te(n);
    return c.set({
      label: s,
      icon: a,
      tooltip: l
    }), t.on("change:value", () => {
      c.isOn = t.value === o;
    }), c.on("execute", () => {
      r.value ? t.value === o ? i.execute(e) : t.value !== o && i.execute("listStyle", {
        type: o
      }) : i.model.change(() => {
        i.execute("listStyle", {
          type: o
        });
      });
    }), c;
  };
}
function xH({ editor: i, normalizedConfig: t, dropdownView: e, parentCommandName: n, styleDefinitions: r, styleGridAriaLabel: s }) {
  const o = i.locale, a = {
    ...t,
    ...n != "numberedList" ? {
      startIndex: !1,
      reversed: !1
    } : null
  }, l = n.replace("List", "");
  let c = null;
  if (t.styles.listTypes.includes(l)) {
    const d = i.commands.get("listStyle"), f = px({
      editor: i,
      parentCommandName: n,
      listStyleCommand: d
    }), m = bx(d);
    c = r.filter(m).map(f);
  }
  const u = new gx(o, {
    styleGridAriaLabel: s,
    enabledProperties: a,
    styleButtonViews: c
  });
  if (t.styles.listTypes.includes(l) && vp(e, () => u.stylesView.children.find((d) => d.isOn)), a.startIndex) {
    const d = i.commands.get("listStart");
    u.startIndexFieldView.bind("isEnabled").to(d), u.startIndexFieldView.fieldView.bind("value").to(d), u.on("listStart", (f, m) => i.execute("listStart", m));
  }
  if (a.reversed) {
    const d = i.commands.get("listReversed");
    u.reversedSwitchButtonView.bind("isEnabled").to(d), u.reversedSwitchButtonView.bind("isOn").to(d, "value", (f) => !!f), u.on("listReversed", () => {
      const f = d.value;
      i.execute("listReversed", {
        reversed: !f
      });
    });
  }
  return u.delegate("execute").to(e), u;
}
function t0({ editor: i, normalizedConfig: t, parentCommandName: e, buttonLabel: n, styleGridAriaLabel: r, styleDefinitions: s }) {
  return (o) => {
    const a = new Yi(o), l = i.commands.get(e), c = i.commands.get("listStyle"), u = bx(c), d = px({
      editor: i,
      parentCommandName: e,
      listStyleCommand: c
    }), f = s.filter(u).map(d), m = new gx(o, {
      styleGridAriaLabel: r,
      enabledProperties: {
        ...t,
        // Disable list start index and reversed in the menu bar.
        startIndex: !1,
        reversed: !1
      },
      styleButtonViews: f
    });
    return m.delegate("execute").to(a), a.buttonView.set({
      label: n,
      icon: ee[e]
    }), a.panelView.children.add(m), a.bind("isEnabled").to(l, "isEnabled"), a.on("execute", () => {
      i.editing.view.focus();
    }), a;
  };
}
function bx(i) {
  return typeof i.isStyleTypeSupported == "function" ? (t) => i.isStyleTypeSupported(t.type) : () => !0;
}
class EH extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      hH,
      kH
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListProperties";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function vx() {
  return {
    baseUrl: null,
    breaks: !1,
    extensions: null,
    gfm: !0,
    headerIds: !0,
    headerPrefix: "",
    highlight: null,
    langPrefix: "language-",
    mangle: !0,
    pedantic: !1,
    renderer: null,
    sanitize: !1,
    sanitizer: null,
    silent: !1,
    smartLists: !1,
    smartypants: !1,
    tokenizer: null,
    walkTokens: null,
    xhtml: !1
  };
}
let wa = vx();
function TH(i) {
  wa = i;
}
const SH = /[&<>"']/, AH = /[&<>"']/g, PH = /[<>"']|&(?!#?\w+;)/, IH = /[<>"']|&(?!#?\w+;)/g, RH = {
  "&": "&amp;",
  "<": "&lt;",
  ">": "&gt;",
  '"': "&quot;",
  "'": "&#39;"
}, n0 = (i) => RH[i];
function kn(i, t) {
  if (t) {
    if (SH.test(i))
      return i.replace(AH, n0);
  } else if (PH.test(i))
    return i.replace(IH, n0);
  return i;
}
const OH = /&(#(?:\d+)|(?:#x[0-9A-Fa-f]+)|(?:\w+));?/ig;
function wx(i) {
  return i.replace(OH, (t, e) => (e = e.toLowerCase(), e === "colon" ? ":" : e.charAt(0) === "#" ? e.charAt(1) === "x" ? String.fromCharCode(parseInt(e.substring(2), 16)) : String.fromCharCode(+e.substring(1)) : ""));
}
const VH = /(^|[^\[])\^/g;
function yt(i, t) {
  i = i.source || i, t = t || "";
  const e = {
    replace: (n, r) => (r = r.source || r, r = r.replace(VH, "$1"), i = i.replace(n, r), e),
    getRegex: () => new RegExp(i, t)
  };
  return e;
}
const MH = /[^\w:]/g, DH = /^$|^[a-z][a-z0-9+.-]*:|^[?#]/i;
function i0(i, t, e) {
  if (i) {
    let n;
    try {
      n = decodeURIComponent(wx(e)).replace(MH, "").toLowerCase();
    } catch {
      return null;
    }
    if (n.indexOf("javascript:") === 0 || n.indexOf("vbscript:") === 0 || n.indexOf("data:") === 0)
      return null;
  }
  t && !DH.test(e) && (e = FH(t, e));
  try {
    e = encodeURI(e).replace(/%25/g, "%");
  } catch {
    return null;
  }
  return e;
}
const Yc = {}, LH = /^[^:]+:\/*[^/]*$/, NH = /^([^:]+:)[\s\S]*$/, BH = /^([^:]+:\/*[^/]*)[\s\S]*$/;
function FH(i, t) {
  Yc[" " + i] || (LH.test(i) ? Yc[" " + i] = i + "/" : Yc[" " + i] = Tu(i, "/", !0)), i = Yc[" " + i];
  const e = i.indexOf(":") === -1;
  return t.substring(0, 2) === "//" ? e ? t : i.replace(NH, "$1") + t : t.charAt(0) === "/" ? e ? t : i.replace(BH, "$1") + t : i + t;
}
const rd = { exec: function() {
} };
function ir(i) {
  let t = 1, e, n;
  for (; t < arguments.length; t++) {
    e = arguments[t];
    for (n in e)
      Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
  }
  return i;
}
function r0(i, t) {
  const e = i.replace(/\|/g, (s, o, a) => {
    let l = !1, c = o;
    for (; --c >= 0 && a[c] === "\\"; ) l = !l;
    return l ? "|" : " |";
  }), n = e.split(/ \|/);
  let r = 0;
  if (n[0].trim() || n.shift(), n.length > 0 && !n[n.length - 1].trim() && n.pop(), n.length > t)
    n.splice(t);
  else
    for (; n.length < t; ) n.push("");
  for (; r < n.length; r++)
    n[r] = n[r].trim().replace(/\\\|/g, "|");
  return n;
}
function Tu(i, t, e) {
  const n = i.length;
  if (n === 0)
    return "";
  let r = 0;
  for (; r < n; ) {
    const s = i.charAt(n - r - 1);
    if (s === t && !e)
      r++;
    else if (s !== t && e)
      r++;
    else
      break;
  }
  return i.substr(0, n - r);
}
function $H(i, t) {
  if (i.indexOf(t[1]) === -1)
    return -1;
  const e = i.length;
  let n = 0, r = 0;
  for (; r < e; r++)
    if (i[r] === "\\")
      r++;
    else if (i[r] === t[0])
      n++;
    else if (i[r] === t[1] && (n--, n < 0))
      return r;
  return -1;
}
function yx(i) {
  i && i.sanitize && !i.silent && console.warn("marked(): sanitize and sanitizer parameters are deprecated since version 0.7.0, should not be used and will be removed in the future. Read more here: https://marked.js.org/#/USING_ADVANCED.md#options");
}
function s0(i, t) {
  if (t < 1)
    return "";
  let e = "";
  for (; t > 1; )
    t & 1 && (e += i), t >>= 1, i += i;
  return e + i;
}
function o0(i, t, e, n) {
  const r = t.href, s = t.title ? kn(t.title) : null, o = i[1].replace(/\\([\[\]])/g, "$1");
  if (i[0].charAt(0) !== "!") {
    n.state.inLink = !0;
    const a = {
      type: "link",
      raw: e,
      href: r,
      title: s,
      text: o,
      tokens: n.inlineTokens(o, [])
    };
    return n.state.inLink = !1, a;
  } else
    return {
      type: "image",
      raw: e,
      href: r,
      title: s,
      text: kn(o)
    };
}
function zH(i, t) {
  const e = i.match(/^(\s+)(?:```)/);
  if (e === null)
    return t;
  const n = e[1];
  return t.split(`
`).map((r) => {
    const s = r.match(/^\s+/);
    if (s === null)
      return r;
    const [o] = s;
    return o.length >= n.length ? r.slice(n.length) : r;
  }).join(`
`);
}
class Mp {
  constructor(t) {
    this.options = t || wa;
  }
  space(t) {
    const e = this.rules.block.newline.exec(t);
    if (e && e[0].length > 0)
      return {
        type: "space",
        raw: e[0]
      };
  }
  code(t) {
    const e = this.rules.block.code.exec(t);
    if (e) {
      const n = e[0].replace(/^ {1,4}/gm, "");
      return {
        type: "code",
        raw: e[0],
        codeBlockStyle: "indented",
        text: this.options.pedantic ? n : Tu(n, `
`)
      };
    }
  }
  fences(t) {
    const e = this.rules.block.fences.exec(t);
    if (e) {
      const n = e[0], r = zH(n, e[3] || "");
      return {
        type: "code",
        raw: n,
        lang: e[2] ? e[2].trim() : e[2],
        text: r
      };
    }
  }
  heading(t) {
    const e = this.rules.block.heading.exec(t);
    if (e) {
      let n = e[2].trim();
      if (/#$/.test(n)) {
        const s = Tu(n, "#");
        (this.options.pedantic || !s || / $/.test(s)) && (n = s.trim());
      }
      const r = {
        type: "heading",
        raw: e[0],
        depth: e[1].length,
        text: n,
        tokens: []
      };
      return this.lexer.inline(r.text, r.tokens), r;
    }
  }
  hr(t) {
    const e = this.rules.block.hr.exec(t);
    if (e)
      return {
        type: "hr",
        raw: e[0]
      };
  }
  blockquote(t) {
    const e = this.rules.block.blockquote.exec(t);
    if (e) {
      const n = e[0].replace(/^ *> ?/gm, "");
      return {
        type: "blockquote",
        raw: e[0],
        tokens: this.lexer.blockTokens(n, []),
        text: n
      };
    }
  }
  list(t) {
    let e = this.rules.block.list.exec(t);
    if (e) {
      let n, r, s, o, a, l, c, u, d, f, m, g, p = e[1].trim();
      const b = p.length > 1, v = {
        type: "list",
        raw: "",
        ordered: b,
        start: b ? +p.slice(0, -1) : "",
        loose: !1,
        items: []
      };
      p = b ? `\\d{1,9}\\${p.slice(-1)}` : `\\${p}`, this.options.pedantic && (p = b ? p : "[*+-]");
      const y = new RegExp(`^( {0,3}${p})((?: [^\\n]*)?(?:\\n|$))`);
      for (; t && (g = !1, !(!(e = y.exec(t)) || this.rules.block.hr.test(t))); ) {
        if (n = e[0], t = t.substring(n.length), u = e[2].split(`
`, 1)[0], d = t.split(`
`, 1)[0], this.options.pedantic ? (o = 2, m = u.trimLeft()) : (o = e[2].search(/[^ ]/), o = o > 4 ? 1 : o, m = u.slice(o), o += e[1].length), l = !1, !u && /^ *$/.test(d) && (n += d + `
`, t = t.substring(d.length + 1), g = !0), !g) {
          const S = new RegExp(`^ {0,${Math.min(3, o - 1)}}(?:[*+-]|\\d{1,9}[.)])`);
          for (; t && (f = t.split(`
`, 1)[0], u = f, this.options.pedantic && (u = u.replace(/^ {1,4}(?=( {4})*[^ ])/g, "  ")), !S.test(u)); ) {
            if (u.search(/[^ ]/) >= o || !u.trim())
              m += `
` + u.slice(o);
            else if (!l)
              m += `
` + u;
            else
              break;
            !l && !u.trim() && (l = !0), n += f + `
`, t = t.substring(f.length + 1);
          }
        }
        v.loose || (c ? v.loose = !0 : /\n *\n *$/.test(n) && (c = !0)), this.options.gfm && (r = /^\[[ xX]\] /.exec(m), r && (s = r[0] !== "[ ] ", m = m.replace(/^\[[ xX]\] +/, ""))), v.items.push({
          type: "list_item",
          raw: n,
          task: !!r,
          checked: s,
          loose: !1,
          text: m
        }), v.raw += n;
      }
      v.items[v.items.length - 1].raw = n.trimRight(), v.items[v.items.length - 1].text = m.trimRight(), v.raw = v.raw.trimRight();
      const x = v.items.length;
      for (a = 0; a < x; a++) {
        this.lexer.state.top = !1, v.items[a].tokens = this.lexer.blockTokens(v.items[a].text, []);
        const S = v.items[a].tokens.filter((I) => I.type === "space"), k = S.every((I) => {
          const A = I.raw.split("");
          let L = 0;
          for (const R of A)
            if (R === `
` && (L += 1), L > 1)
              return !0;
          return !1;
        });
        !v.loose && S.length && k && (v.loose = !0, v.items[a].loose = !0);
      }
      return v;
    }
  }
  html(t) {
    const e = this.rules.block.html.exec(t);
    if (e) {
      const n = {
        type: "html",
        raw: e[0],
        pre: !this.options.sanitizer && (e[1] === "pre" || e[1] === "script" || e[1] === "style"),
        text: e[0]
      };
      return this.options.sanitize && (n.type = "paragraph", n.text = this.options.sanitizer ? this.options.sanitizer(e[0]) : kn(e[0]), n.tokens = [], this.lexer.inline(n.text, n.tokens)), n;
    }
  }
  def(t) {
    const e = this.rules.block.def.exec(t);
    if (e)
      return e[3] && (e[3] = e[3].substring(1, e[3].length - 1)), {
        type: "def",
        tag: e[1].toLowerCase().replace(/\s+/g, " "),
        raw: e[0],
        href: e[2],
        title: e[3]
      };
  }
  table(t) {
    const e = this.rules.block.table.exec(t);
    if (e) {
      const n = {
        type: "table",
        header: r0(e[1]).map((r) => ({ text: r })),
        align: e[2].replace(/^ *|\| *$/g, "").split(/ *\| */),
        rows: e[3] && e[3].trim() ? e[3].replace(/\n[ \t]*$/, "").split(`
`) : []
      };
      if (n.header.length === n.align.length) {
        n.raw = e[0];
        let r = n.align.length, s, o, a, l;
        for (s = 0; s < r; s++)
          /^ *-+: *$/.test(n.align[s]) ? n.align[s] = "right" : /^ *:-+: *$/.test(n.align[s]) ? n.align[s] = "center" : /^ *:-+ *$/.test(n.align[s]) ? n.align[s] = "left" : n.align[s] = null;
        for (r = n.rows.length, s = 0; s < r; s++)
          n.rows[s] = r0(n.rows[s], n.header.length).map((c) => ({ text: c }));
        for (r = n.header.length, o = 0; o < r; o++)
          n.header[o].tokens = [], this.lexer.inlineTokens(n.header[o].text, n.header[o].tokens);
        for (r = n.rows.length, o = 0; o < r; o++)
          for (l = n.rows[o], a = 0; a < l.length; a++)
            l[a].tokens = [], this.lexer.inlineTokens(l[a].text, l[a].tokens);
        return n;
      }
    }
  }
  lheading(t) {
    const e = this.rules.block.lheading.exec(t);
    if (e) {
      const n = {
        type: "heading",
        raw: e[0],
        depth: e[2].charAt(0) === "=" ? 1 : 2,
        text: e[1],
        tokens: []
      };
      return this.lexer.inline(n.text, n.tokens), n;
    }
  }
  paragraph(t) {
    const e = this.rules.block.paragraph.exec(t);
    if (e) {
      const n = {
        type: "paragraph",
        raw: e[0],
        text: e[1].charAt(e[1].length - 1) === `
` ? e[1].slice(0, -1) : e[1],
        tokens: []
      };
      return this.lexer.inline(n.text, n.tokens), n;
    }
  }
  text(t) {
    const e = this.rules.block.text.exec(t);
    if (e) {
      const n = {
        type: "text",
        raw: e[0],
        text: e[0],
        tokens: []
      };
      return this.lexer.inline(n.text, n.tokens), n;
    }
  }
  escape(t) {
    const e = this.rules.inline.escape.exec(t);
    if (e)
      return {
        type: "escape",
        raw: e[0],
        text: kn(e[1])
      };
  }
  tag(t) {
    const e = this.rules.inline.tag.exec(t);
    if (e)
      return !this.lexer.state.inLink && /^<a /i.test(e[0]) ? this.lexer.state.inLink = !0 : this.lexer.state.inLink && /^<\/a>/i.test(e[0]) && (this.lexer.state.inLink = !1), !this.lexer.state.inRawBlock && /^<(pre|code|kbd|script)(\s|>)/i.test(e[0]) ? this.lexer.state.inRawBlock = !0 : this.lexer.state.inRawBlock && /^<\/(pre|code|kbd|script)(\s|>)/i.test(e[0]) && (this.lexer.state.inRawBlock = !1), {
        type: this.options.sanitize ? "text" : "html",
        raw: e[0],
        inLink: this.lexer.state.inLink,
        inRawBlock: this.lexer.state.inRawBlock,
        text: this.options.sanitize ? this.options.sanitizer ? this.options.sanitizer(e[0]) : kn(e[0]) : e[0]
      };
  }
  link(t) {
    const e = this.rules.inline.link.exec(t);
    if (e) {
      const n = e[2].trim();
      if (!this.options.pedantic && /^</.test(n)) {
        if (!/>$/.test(n))
          return;
        const o = Tu(n.slice(0, -1), "\\");
        if ((n.length - o.length) % 2 === 0)
          return;
      } else {
        const o = $H(e[2], "()");
        if (o > -1) {
          const l = (e[0].indexOf("!") === 0 ? 5 : 4) + e[1].length + o;
          e[2] = e[2].substring(0, o), e[0] = e[0].substring(0, l).trim(), e[3] = "";
        }
      }
      let r = e[2], s = "";
      if (this.options.pedantic) {
        const o = /^([^'"]*[^\s])\s+(['"])(.*)\2/.exec(r);
        o && (r = o[1], s = o[3]);
      } else
        s = e[3] ? e[3].slice(1, -1) : "";
      return r = r.trim(), /^</.test(r) && (this.options.pedantic && !/>$/.test(n) ? r = r.slice(1) : r = r.slice(1, -1)), o0(e, {
        href: r && r.replace(this.rules.inline._escapes, "$1"),
        title: s && s.replace(this.rules.inline._escapes, "$1")
      }, e[0], this.lexer);
    }
  }
  reflink(t, e) {
    let n;
    if ((n = this.rules.inline.reflink.exec(t)) || (n = this.rules.inline.nolink.exec(t))) {
      let r = (n[2] || n[1]).replace(/\s+/g, " ");
      if (r = e[r.toLowerCase()], !r || !r.href) {
        const s = n[0].charAt(0);
        return {
          type: "text",
          raw: s,
          text: s
        };
      }
      return o0(n, r, n[0], this.lexer);
    }
  }
  emStrong(t, e, n = "") {
    let r = this.rules.inline.emStrong.lDelim.exec(t);
    if (!r || r[3] && n.match(/[\p{L}\p{N}]/u)) return;
    const s = r[1] || r[2] || "";
    if (!s || s && (n === "" || this.rules.inline.punctuation.exec(n))) {
      const o = r[0].length - 1;
      let a, l, c = o, u = 0;
      const d = r[0][0] === "*" ? this.rules.inline.emStrong.rDelimAst : this.rules.inline.emStrong.rDelimUnd;
      for (d.lastIndex = 0, e = e.slice(-1 * t.length + o); (r = d.exec(e)) != null; ) {
        if (a = r[1] || r[2] || r[3] || r[4] || r[5] || r[6], !a) continue;
        if (l = a.length, r[3] || r[4]) {
          c += l;
          continue;
        } else if ((r[5] || r[6]) && o % 3 && !((o + l) % 3)) {
          u += l;
          continue;
        }
        if (c -= l, c > 0) continue;
        if (l = Math.min(l, l + c + u), Math.min(o, l) % 2) {
          const m = t.slice(1, o + r.index + l);
          return {
            type: "em",
            raw: t.slice(0, o + r.index + l + 1),
            text: m,
            tokens: this.lexer.inlineTokens(m, [])
          };
        }
        const f = t.slice(2, o + r.index + l - 1);
        return {
          type: "strong",
          raw: t.slice(0, o + r.index + l + 1),
          text: f,
          tokens: this.lexer.inlineTokens(f, [])
        };
      }
    }
  }
  codespan(t) {
    const e = this.rules.inline.code.exec(t);
    if (e) {
      let n = e[2].replace(/\n/g, " ");
      const r = /[^ ]/.test(n), s = /^ /.test(n) && / $/.test(n);
      return r && s && (n = n.substring(1, n.length - 1)), n = kn(n, !0), {
        type: "codespan",
        raw: e[0],
        text: n
      };
    }
  }
  br(t) {
    const e = this.rules.inline.br.exec(t);
    if (e)
      return {
        type: "br",
        raw: e[0]
      };
  }
  del(t) {
    const e = this.rules.inline.del.exec(t);
    if (e)
      return {
        type: "del",
        raw: e[0],
        text: e[2],
        tokens: this.lexer.inlineTokens(e[2], [])
      };
  }
  autolink(t, e) {
    const n = this.rules.inline.autolink.exec(t);
    if (n) {
      let r, s;
      return n[2] === "@" ? (r = kn(this.options.mangle ? e(n[1]) : n[1]), s = "mailto:" + r) : (r = kn(n[1]), s = r), {
        type: "link",
        raw: n[0],
        text: r,
        href: s,
        tokens: [
          {
            type: "text",
            raw: r,
            text: r
          }
        ]
      };
    }
  }
  url(t, e) {
    let n;
    if (n = this.rules.inline.url.exec(t)) {
      let r, s;
      if (n[2] === "@")
        r = kn(this.options.mangle ? e(n[0]) : n[0]), s = "mailto:" + r;
      else {
        let o;
        do
          o = n[0], n[0] = this.rules.inline._backpedal.exec(n[0])[0];
        while (o !== n[0]);
        r = kn(n[0]), n[1] === "www." ? s = "http://" + r : s = r;
      }
      return {
        type: "link",
        raw: n[0],
        text: r,
        href: s,
        tokens: [
          {
            type: "text",
            raw: r,
            text: r
          }
        ]
      };
    }
  }
  inlineText(t, e) {
    const n = this.rules.inline.text.exec(t);
    if (n) {
      let r;
      return this.lexer.state.inRawBlock ? r = this.options.sanitize ? this.options.sanitizer ? this.options.sanitizer(n[0]) : kn(n[0]) : n[0] : r = kn(this.options.smartypants ? e(n[0]) : n[0]), {
        type: "text",
        raw: n[0],
        text: r
      };
    }
  }
}
const De = {
  newline: /^(?: *(?:\n|$))+/,
  code: /^( {4}[^\n]+(?:\n(?: *(?:\n|$))*)?)+/,
  fences: /^ {0,3}(`{3,}(?=[^`\n]*\n)|~{3,})([^\n]*)\n(?:|([\s\S]*?)\n)(?: {0,3}\1[~`]* *(?=\n|$)|$)/,
  hr: /^ {0,3}((?:- *){3,}|(?:_ *){3,}|(?:\* *){3,})(?:\n+|$)/,
  heading: /^ {0,3}(#{1,6})(?=\s|$)(.*)(?:\n+|$)/,
  blockquote: /^( {0,3}> ?(paragraph|[^\n]*)(?:\n|$))+/,
  list: /^( {0,3}bull)( [^\n]+?)?(?:\n|$)/,
  html: "^ {0,3}(?:<(script|pre|style|textarea)[\\s>][\\s\\S]*?(?:</\\1>[^\\n]*\\n+|$)|comment[^\\n]*(\\n+|$)|<\\?[\\s\\S]*?(?:\\?>\\n*|$)|<![A-Z][\\s\\S]*?(?:>\\n*|$)|<!\\[CDATA\\[[\\s\\S]*?(?:\\]\\]>\\n*|$)|</?(tag)(?: +|\\n|/?>)[\\s\\S]*?(?:(?:\\n *)+\\n|$)|<(?!script|pre|style|textarea)([a-z][\\w-]*)(?:attribute)*? */?>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$)|</(?!script|pre|style|textarea)[a-z][\\w-]*\\s*>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$))",
  def: /^ {0,3}\[(label)\]: *(?:\n *)?<?([^\s>]+)>?(?:(?: +(?:\n *)?| *\n *)(title))? *(?:\n+|$)/,
  table: rd,
  lheading: /^([^\n]+)\n {0,3}(=+|-+) *(?:\n+|$)/,
  // regex template, placeholders will be replaced according to different paragraph
  // interruption rules of commonmark and the original markdown spec:
  _paragraph: /^([^\n]+(?:\n(?!hr|heading|lheading|blockquote|fences|list|html|table| +\n)[^\n]+)*)/,
  text: /^[^\n]+/
};
De._label = /(?!\s*\])(?:\\.|[^\[\]\\])+/;
De._title = /(?:"(?:\\"?|[^"\\])*"|'[^'\n]*(?:\n[^'\n]+)*\n?'|\([^()]*\))/;
De.def = yt(De.def).replace("label", De._label).replace("title", De._title).getRegex();
De.bullet = /(?:[*+-]|\d{1,9}[.)])/;
De.listItemStart = yt(/^( *)(bull) */).replace("bull", De.bullet).getRegex();
De.list = yt(De.list).replace(/bull/g, De.bullet).replace("hr", "\\n+(?=\\1?(?:(?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$))").replace("def", "\\n+(?=" + De.def.source + ")").getRegex();
De._tag = "address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|section|source|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul";
De._comment = /<!--(?!-?>)[\s\S]*?(?:-->|$)/;
De.html = yt(De.html, "i").replace("comment", De._comment).replace("tag", De._tag).replace("attribute", / +[a-zA-Z:_][\w.:-]*(?: *= *"[^"\n]*"| *= *'[^'\n]*'| *= *[^\s"'=<>`]+)?/).getRegex();
De.paragraph = yt(De._paragraph).replace("hr", De.hr).replace("heading", " {0,3}#{1,6} ").replace("|lheading", "").replace("|table", "").replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", De._tag).getRegex();
De.blockquote = yt(De.blockquote).replace("paragraph", De.paragraph).getRegex();
De.normal = ir({}, De);
De.gfm = ir({}, De.normal, {
  table: "^ *([^\\n ].*\\|.*)\\n {0,3}(?:\\| *)?(:?-+:? *(?:\\| *:?-+:? *)*)(?:\\| *)?(?:\\n((?:(?! *\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\n|$))*)\\n*|$)"
  // Cells
});
De.gfm.table = yt(De.gfm.table).replace("hr", De.hr).replace("heading", " {0,3}#{1,6} ").replace("blockquote", " {0,3}>").replace("code", " {4}[^\\n]").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", De._tag).getRegex();
De.gfm.paragraph = yt(De._paragraph).replace("hr", De.hr).replace("heading", " {0,3}#{1,6} ").replace("|lheading", "").replace("table", De.gfm.table).replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", De._tag).getRegex();
De.pedantic = ir({}, De.normal, {
  html: yt(
    `^ *(?:comment *(?:\\n|\\s*$)|<(tag)[\\s\\S]+?</\\1> *(?:\\n{2,}|\\s*$)|<tag(?:"[^"]*"|'[^']*'|\\s[^'"/>\\s]*)*?/?> *(?:\\n{2,}|\\s*$))`
  ).replace("comment", De._comment).replace(/tag/g, "(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\b)\\w+(?!:|[^\\w\\s@]*@)\\b").getRegex(),
  def: /^ *\[([^\]]+)\]: *<?([^\s>]+)>?(?: +(["(][^\n]+[")]))? *(?:\n+|$)/,
  heading: /^(#{1,6})(.*)(?:\n+|$)/,
  fences: rd,
  // fences not supported
  paragraph: yt(De.normal._paragraph).replace("hr", De.hr).replace("heading", ` *#{1,6} *[^
]`).replace("lheading", De.lheading).replace("blockquote", " {0,3}>").replace("|fences", "").replace("|list", "").replace("|html", "").getRegex()
});
const ke = {
  escape: /^\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/,
  autolink: /^<(scheme:[^\s\x00-\x1f<>]*|email)>/,
  url: rd,
  tag: "^comment|^</[a-zA-Z][\\w:-]*\\s*>|^<[a-zA-Z][\\w-]*(?:attribute)*?\\s*/?>|^<\\?[\\s\\S]*?\\?>|^<![a-zA-Z]+\\s[\\s\\S]*?>|^<!\\[CDATA\\[[\\s\\S]*?\\]\\]>",
  // CDATA section
  link: /^!?\[(label)\]\(\s*(href)(?:\s+(title))?\s*\)/,
  reflink: /^!?\[(label)\]\[(ref)\]/,
  nolink: /^!?\[(ref)\](?:\[\])?/,
  reflinkSearch: "reflink|nolink(?!\\()",
  emStrong: {
    lDelim: /^(?:\*+(?:([punct_])|[^\s*]))|^_+(?:([punct*])|([^\s_]))/,
    //        (1) and (2) can only be a Right Delimiter. (3) and (4) can only be Left.  (5) and (6) can be either Left or Right.
    //        () Skip orphan delim inside strong    (1) #***                (2) a***#, a***                   (3) #***a, ***a                 (4) ***#              (5) #***#                 (6) a***a
    rDelimAst: /^[^_*]*?\_\_[^_*]*?\*[^_*]*?(?=\_\_)|[punct_](\*+)(?=[\s]|$)|[^punct*_\s](\*+)(?=[punct_\s]|$)|[punct_\s](\*+)(?=[^punct*_\s])|[\s](\*+)(?=[punct_])|[punct_](\*+)(?=[punct_])|[^punct*_\s](\*+)(?=[^punct*_\s])/,
    rDelimUnd: /^[^_*]*?\*\*[^_*]*?\_[^_*]*?(?=\*\*)|[punct*](\_+)(?=[\s]|$)|[^punct*_\s](\_+)(?=[punct*\s]|$)|[punct*\s](\_+)(?=[^punct*_\s])|[\s](\_+)(?=[punct*])|[punct*](\_+)(?=[punct*])/
    // ^- Not allowed for _
  },
  code: /^(`+)([^`]|[^`][\s\S]*?[^`])\1(?!`)/,
  br: /^( {2,}|\\)\n(?!\s*$)/,
  del: rd,
  text: /^(`+|[^`])(?:(?= {2,}\n)|[\s\S]*?(?:(?=[\\<!\[`*_]|\b_|$)|[^ ](?= {2,}\n)))/,
  punctuation: /^([\spunctuation])/
};
ke._punctuation = "!\"#$%&'()+\\-.,/:;<=>?@\\[\\]`^{|}~";
ke.punctuation = yt(ke.punctuation).replace(/punctuation/g, ke._punctuation).getRegex();
ke.blockSkip = /\[[^\]]*?\]\([^\)]*?\)|`[^`]*?`|<[^>]*?>/g;
ke.escapedEmSt = /\\\*|\\_/g;
ke._comment = yt(De._comment).replace("(?:-->|$)", "-->").getRegex();
ke.emStrong.lDelim = yt(ke.emStrong.lDelim).replace(/punct/g, ke._punctuation).getRegex();
ke.emStrong.rDelimAst = yt(ke.emStrong.rDelimAst, "g").replace(/punct/g, ke._punctuation).getRegex();
ke.emStrong.rDelimUnd = yt(ke.emStrong.rDelimUnd, "g").replace(/punct/g, ke._punctuation).getRegex();
ke._escapes = /\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/g;
ke._scheme = /[a-zA-Z][a-zA-Z0-9+.-]{1,31}/;
ke._email = /[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/;
ke.autolink = yt(ke.autolink).replace("scheme", ke._scheme).replace("email", ke._email).getRegex();
ke._attribute = /\s+[a-zA-Z:_][\w.:-]*(?:\s*=\s*"[^"]*"|\s*=\s*'[^']*'|\s*=\s*[^\s"'=<>`]+)?/;
ke.tag = yt(ke.tag).replace("comment", ke._comment).replace("attribute", ke._attribute).getRegex();
ke._label = /(?:\[(?:\\.|[^\[\]\\])*\]|\\.|`[^`]*`|[^\[\]\\`])*?/;
ke._href = /<(?:\\.|[^\n<>\\])+>|[^\s\x00-\x1f]*/;
ke._title = /"(?:\\"?|[^"\\])*"|'(?:\\'?|[^'\\])*'|\((?:\\\)?|[^)\\])*\)/;
ke.link = yt(ke.link).replace("label", ke._label).replace("href", ke._href).replace("title", ke._title).getRegex();
ke.reflink = yt(ke.reflink).replace("label", ke._label).replace("ref", De._label).getRegex();
ke.nolink = yt(ke.nolink).replace("ref", De._label).getRegex();
ke.reflinkSearch = yt(ke.reflinkSearch, "g").replace("reflink", ke.reflink).replace("nolink", ke.nolink).getRegex();
ke.normal = ir({}, ke);
ke.pedantic = ir({}, ke.normal, {
  strong: {
    start: /^__|\*\*/,
    middle: /^__(?=\S)([\s\S]*?\S)__(?!_)|^\*\*(?=\S)([\s\S]*?\S)\*\*(?!\*)/,
    endAst: /\*\*(?!\*)/g,
    endUnd: /__(?!_)/g
  },
  em: {
    start: /^_|\*/,
    middle: /^()\*(?=\S)([\s\S]*?\S)\*(?!\*)|^_(?=\S)([\s\S]*?\S)_(?!_)/,
    endAst: /\*(?!\*)/g,
    endUnd: /_(?!_)/g
  },
  link: yt(/^!?\[(label)\]\((.*?)\)/).replace("label", ke._label).getRegex(),
  reflink: yt(/^!?\[(label)\]\s*\[([^\]]*)\]/).replace("label", ke._label).getRegex()
});
ke.gfm = ir({}, ke.normal, {
  escape: yt(ke.escape).replace("])", "~|])").getRegex(),
  _extended_email: /[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/,
  url: /^((?:ftp|https?):\/\/|www\.)(?:[a-zA-Z0-9\-]+\.?)+[^\s<]*|^email/,
  _backpedal: /(?:[^?!.,:;*_~()&]+|\([^)]*\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_~)]+(?!$))+/,
  del: /^(~~?)(?=[^\s~])([\s\S]*?[^\s~])\1(?=[^~]|$)/,
  text: /^([`~]+|[^`~])(?:(?= {2,}\n)|(?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)|[\s\S]*?(?:(?=[\\<!\[`*~_]|\b_|https?:\/\/|ftp:\/\/|www\.|$)|[^ ](?= {2,}\n)|[^a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-](?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)))/
});
ke.gfm.url = yt(ke.gfm.url, "i").replace("email", ke.gfm._extended_email).getRegex();
ke.breaks = ir({}, ke.gfm, {
  br: yt(ke.br).replace("{2,}", "*").getRegex(),
  text: yt(ke.gfm.text).replace("\\b_", "\\b_| {2,}\\n").replace(/\{2,\}/g, "*").getRegex()
});
function HH(i) {
  return i.replace(/---/g, "—").replace(/--/g, "–").replace(/(^|[-\u2014/(\[{"\s])'/g, "$1‘").replace(/'/g, "’").replace(/(^|[-\u2014/(\[{\u2018\s])"/g, "$1“").replace(/"/g, "”").replace(/\.{3}/g, "…");
}
function a0(i) {
  let t = "", e, n;
  const r = i.length;
  for (e = 0; e < r; e++)
    n = i.charCodeAt(e), Math.random() > 0.5 && (n = "x" + n.toString(16)), t += "&#" + n + ";";
  return t;
}
class $r {
  constructor(t) {
    this.tokens = [], this.tokens.links = /* @__PURE__ */ Object.create(null), this.options = t || wa, this.options.tokenizer = this.options.tokenizer || new Mp(), this.tokenizer = this.options.tokenizer, this.tokenizer.options = this.options, this.tokenizer.lexer = this, this.inlineQueue = [], this.state = {
      inLink: !1,
      inRawBlock: !1,
      top: !0
    };
    const e = {
      block: De.normal,
      inline: ke.normal
    };
    this.options.pedantic ? (e.block = De.pedantic, e.inline = ke.pedantic) : this.options.gfm && (e.block = De.gfm, this.options.breaks ? e.inline = ke.breaks : e.inline = ke.gfm), this.tokenizer.rules = e;
  }
  /**
   * Expose Rules
   */
  static get rules() {
    return {
      block: De,
      inline: ke
    };
  }
  /**
   * Static Lex Method
   */
  static lex(t, e) {
    return new $r(e).lex(t);
  }
  /**
   * Static Lex Inline Method
   */
  static lexInline(t, e) {
    return new $r(e).inlineTokens(t);
  }
  /**
   * Preprocessing
   */
  lex(t) {
    t = t.replace(/\r\n|\r/g, `
`).replace(/\t/g, "    "), this.blockTokens(t, this.tokens);
    let e;
    for (; e = this.inlineQueue.shift(); )
      this.inlineTokens(e.src, e.tokens);
    return this.tokens;
  }
  /**
   * Lexing
   */
  blockTokens(t, e = []) {
    this.options.pedantic && (t = t.replace(/^ +$/gm, ""));
    let n, r, s, o;
    for (; t; )
      if (!(this.options.extensions && this.options.extensions.block && this.options.extensions.block.some((a) => (n = a.call({ lexer: this }, t, e)) ? (t = t.substring(n.raw.length), e.push(n), !0) : !1))) {
        if (n = this.tokenizer.space(t)) {
          t = t.substring(n.raw.length), n.raw.length === 1 && e.length > 0 ? e[e.length - 1].raw += `
` : e.push(n);
          continue;
        }
        if (n = this.tokenizer.code(t)) {
          t = t.substring(n.raw.length), r = e[e.length - 1], r && (r.type === "paragraph" || r.type === "text") ? (r.raw += `
` + n.raw, r.text += `
` + n.text, this.inlineQueue[this.inlineQueue.length - 1].src = r.text) : e.push(n);
          continue;
        }
        if (n = this.tokenizer.fences(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.heading(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.hr(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.blockquote(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.list(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.html(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.def(t)) {
          t = t.substring(n.raw.length), r = e[e.length - 1], r && (r.type === "paragraph" || r.type === "text") ? (r.raw += `
` + n.raw, r.text += `
` + n.raw, this.inlineQueue[this.inlineQueue.length - 1].src = r.text) : this.tokens.links[n.tag] || (this.tokens.links[n.tag] = {
            href: n.href,
            title: n.title
          });
          continue;
        }
        if (n = this.tokenizer.table(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.lheading(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (s = t, this.options.extensions && this.options.extensions.startBlock) {
          let a = 1 / 0;
          const l = t.slice(1);
          let c;
          this.options.extensions.startBlock.forEach(function(u) {
            c = u.call({ lexer: this }, l), typeof c == "number" && c >= 0 && (a = Math.min(a, c));
          }), a < 1 / 0 && a >= 0 && (s = t.substring(0, a + 1));
        }
        if (this.state.top && (n = this.tokenizer.paragraph(s))) {
          r = e[e.length - 1], o && r.type === "paragraph" ? (r.raw += `
` + n.raw, r.text += `
` + n.text, this.inlineQueue.pop(), this.inlineQueue[this.inlineQueue.length - 1].src = r.text) : e.push(n), o = s.length !== t.length, t = t.substring(n.raw.length);
          continue;
        }
        if (n = this.tokenizer.text(t)) {
          t = t.substring(n.raw.length), r = e[e.length - 1], r && r.type === "text" ? (r.raw += `
` + n.raw, r.text += `
` + n.text, this.inlineQueue.pop(), this.inlineQueue[this.inlineQueue.length - 1].src = r.text) : e.push(n);
          continue;
        }
        if (t) {
          const a = "Infinite loop on byte: " + t.charCodeAt(0);
          if (this.options.silent) {
            console.error(a);
            break;
          } else
            throw new Error(a);
        }
      }
    return this.state.top = !0, e;
  }
  inline(t, e) {
    this.inlineQueue.push({ src: t, tokens: e });
  }
  /**
   * Lexing/Compiling
   */
  inlineTokens(t, e = []) {
    let n, r, s, o = t, a, l, c;
    if (this.tokens.links) {
      const u = Object.keys(this.tokens.links);
      if (u.length > 0)
        for (; (a = this.tokenizer.rules.inline.reflinkSearch.exec(o)) != null; )
          u.includes(a[0].slice(a[0].lastIndexOf("[") + 1, -1)) && (o = o.slice(0, a.index) + "[" + s0("a", a[0].length - 2) + "]" + o.slice(this.tokenizer.rules.inline.reflinkSearch.lastIndex));
    }
    for (; (a = this.tokenizer.rules.inline.blockSkip.exec(o)) != null; )
      o = o.slice(0, a.index) + "[" + s0("a", a[0].length - 2) + "]" + o.slice(this.tokenizer.rules.inline.blockSkip.lastIndex);
    for (; (a = this.tokenizer.rules.inline.escapedEmSt.exec(o)) != null; )
      o = o.slice(0, a.index) + "++" + o.slice(this.tokenizer.rules.inline.escapedEmSt.lastIndex);
    for (; t; )
      if (l || (c = ""), l = !1, !(this.options.extensions && this.options.extensions.inline && this.options.extensions.inline.some((u) => (n = u.call({ lexer: this }, t, e)) ? (t = t.substring(n.raw.length), e.push(n), !0) : !1))) {
        if (n = this.tokenizer.escape(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.tag(t)) {
          t = t.substring(n.raw.length), r = e[e.length - 1], r && n.type === "text" && r.type === "text" ? (r.raw += n.raw, r.text += n.text) : e.push(n);
          continue;
        }
        if (n = this.tokenizer.link(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.reflink(t, this.tokens.links)) {
          t = t.substring(n.raw.length), r = e[e.length - 1], r && n.type === "text" && r.type === "text" ? (r.raw += n.raw, r.text += n.text) : e.push(n);
          continue;
        }
        if (n = this.tokenizer.emStrong(t, o, c)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.codespan(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.br(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.del(t)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (n = this.tokenizer.autolink(t, a0)) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (!this.state.inLink && (n = this.tokenizer.url(t, a0))) {
          t = t.substring(n.raw.length), e.push(n);
          continue;
        }
        if (s = t, this.options.extensions && this.options.extensions.startInline) {
          let u = 1 / 0;
          const d = t.slice(1);
          let f;
          this.options.extensions.startInline.forEach(function(m) {
            f = m.call({ lexer: this }, d), typeof f == "number" && f >= 0 && (u = Math.min(u, f));
          }), u < 1 / 0 && u >= 0 && (s = t.substring(0, u + 1));
        }
        if (n = this.tokenizer.inlineText(s, HH)) {
          t = t.substring(n.raw.length), n.raw.slice(-1) !== "_" && (c = n.raw.slice(-1)), l = !0, r = e[e.length - 1], r && r.type === "text" ? (r.raw += n.raw, r.text += n.text) : e.push(n);
          continue;
        }
        if (t) {
          const u = "Infinite loop on byte: " + t.charCodeAt(0);
          if (this.options.silent) {
            console.error(u);
            break;
          } else
            throw new Error(u);
        }
      }
    return e;
  }
}
class Dp {
  constructor(t) {
    this.options = t || wa;
  }
  code(t, e, n) {
    const r = (e || "").match(/\S*/)[0];
    if (this.options.highlight) {
      const s = this.options.highlight(t, r);
      s != null && s !== t && (n = !0, t = s);
    }
    return t = t.replace(/\n$/, "") + `
`, r ? '<pre><code class="' + this.options.langPrefix + kn(r, !0) + '">' + (n ? t : kn(t, !0)) + `</code></pre>
` : "<pre><code>" + (n ? t : kn(t, !0)) + `</code></pre>
`;
  }
  blockquote(t) {
    return `<blockquote>
` + t + `</blockquote>
`;
  }
  html(t) {
    return t;
  }
  heading(t, e, n, r) {
    return this.options.headerIds ? "<h" + e + ' id="' + this.options.headerPrefix + r.slug(n) + '">' + t + "</h" + e + `>
` : "<h" + e + ">" + t + "</h" + e + `>
`;
  }
  hr() {
    return this.options.xhtml ? `<hr/>
` : `<hr>
`;
  }
  list(t, e, n) {
    const r = e ? "ol" : "ul", s = e && n !== 1 ? ' start="' + n + '"' : "";
    return "<" + r + s + `>
` + t + "</" + r + `>
`;
  }
  listitem(t) {
    return "<li>" + t + `</li>
`;
  }
  checkbox(t) {
    return "<input " + (t ? 'checked="" ' : "") + 'disabled="" type="checkbox"' + (this.options.xhtml ? " /" : "") + "> ";
  }
  paragraph(t) {
    return "<p>" + t + `</p>
`;
  }
  table(t, e) {
    return e && (e = "<tbody>" + e + "</tbody>"), `<table>
<thead>
` + t + `</thead>
` + e + `</table>
`;
  }
  tablerow(t) {
    return `<tr>
` + t + `</tr>
`;
  }
  tablecell(t, e) {
    const n = e.header ? "th" : "td";
    return (e.align ? "<" + n + ' align="' + e.align + '">' : "<" + n + ">") + t + "</" + n + `>
`;
  }
  // span level renderer
  strong(t) {
    return "<strong>" + t + "</strong>";
  }
  em(t) {
    return "<em>" + t + "</em>";
  }
  codespan(t) {
    return "<code>" + t + "</code>";
  }
  br() {
    return this.options.xhtml ? "<br/>" : "<br>";
  }
  del(t) {
    return "<del>" + t + "</del>";
  }
  link(t, e, n) {
    if (t = i0(this.options.sanitize, this.options.baseUrl, t), t === null)
      return n;
    let r = '<a href="' + kn(t) + '"';
    return e && (r += ' title="' + e + '"'), r += ">" + n + "</a>", r;
  }
  image(t, e, n) {
    if (t = i0(this.options.sanitize, this.options.baseUrl, t), t === null)
      return n;
    let r = '<img src="' + t + '" alt="' + n + '"';
    return e && (r += ' title="' + e + '"'), r += this.options.xhtml ? "/>" : ">", r;
  }
  text(t) {
    return t;
  }
}
class _x {
  // no need for block level renderers
  strong(t) {
    return t;
  }
  em(t) {
    return t;
  }
  codespan(t) {
    return t;
  }
  del(t) {
    return t;
  }
  html(t) {
    return t;
  }
  text(t) {
    return t;
  }
  link(t, e, n) {
    return "" + n;
  }
  image(t, e, n) {
    return "" + n;
  }
  br() {
    return "";
  }
}
class Cx {
  constructor() {
    this.seen = {};
  }
  serialize(t) {
    return t.toLowerCase().trim().replace(/<[!\/a-z].*?>/ig, "").replace(/[\u2000-\u206F\u2E00-\u2E7F\\'!"#$%&()*+,./:;<=>?@[\]^`{|}~]/g, "").replace(/\s/g, "-");
  }
  /**
   * Finds the next safe (unique) slug to use
   */
  getNextSafeSlug(t, e) {
    let n = t, r = 0;
    if (this.seen.hasOwnProperty(n)) {
      r = this.seen[t];
      do
        r++, n = t + "-" + r;
      while (this.seen.hasOwnProperty(n));
    }
    return e || (this.seen[t] = r, this.seen[n] = 0), n;
  }
  /**
   * Convert string to unique id
   * @param {object} options
   * @param {boolean} options.dryrun Generates the next unique slug without updating the internal accumulator.
   */
  slug(t, e = {}) {
    const n = this.serialize(t);
    return this.getNextSafeSlug(n, e.dryrun);
  }
}
class zr {
  constructor(t) {
    this.options = t || wa, this.options.renderer = this.options.renderer || new Dp(), this.renderer = this.options.renderer, this.renderer.options = this.options, this.textRenderer = new _x(), this.slugger = new Cx();
  }
  /**
   * Static Parse Method
   */
  static parse(t, e) {
    return new zr(e).parse(t);
  }
  /**
   * Static Parse Inline Method
   */
  static parseInline(t, e) {
    return new zr(e).parseInline(t);
  }
  /**
   * Parse Loop
   */
  parse(t, e = !0) {
    let n = "", r, s, o, a, l, c, u, d, f, m, g, p, b, v, y, x, S, k, I;
    const A = t.length;
    for (r = 0; r < A; r++) {
      if (m = t[r], this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[m.type] && (I = this.options.extensions.renderers[m.type].call({ parser: this }, m), I !== !1 || !["space", "hr", "heading", "code", "table", "blockquote", "list", "html", "paragraph", "text"].includes(m.type))) {
        n += I || "";
        continue;
      }
      switch (m.type) {
        case "space":
          continue;
        case "hr": {
          n += this.renderer.hr();
          continue;
        }
        case "heading": {
          n += this.renderer.heading(
            this.parseInline(m.tokens),
            m.depth,
            wx(this.parseInline(m.tokens, this.textRenderer)),
            this.slugger
          );
          continue;
        }
        case "code": {
          n += this.renderer.code(
            m.text,
            m.lang,
            m.escaped
          );
          continue;
        }
        case "table": {
          for (d = "", u = "", a = m.header.length, s = 0; s < a; s++)
            u += this.renderer.tablecell(
              this.parseInline(m.header[s].tokens),
              { header: !0, align: m.align[s] }
            );
          for (d += this.renderer.tablerow(u), f = "", a = m.rows.length, s = 0; s < a; s++) {
            for (c = m.rows[s], u = "", l = c.length, o = 0; o < l; o++)
              u += this.renderer.tablecell(
                this.parseInline(c[o].tokens),
                { header: !1, align: m.align[o] }
              );
            f += this.renderer.tablerow(u);
          }
          n += this.renderer.table(d, f);
          continue;
        }
        case "blockquote": {
          f = this.parse(m.tokens), n += this.renderer.blockquote(f);
          continue;
        }
        case "list": {
          for (g = m.ordered, p = m.start, b = m.loose, a = m.items.length, f = "", s = 0; s < a; s++)
            y = m.items[s], x = y.checked, S = y.task, v = "", y.task && (k = this.renderer.checkbox(x), b ? y.tokens.length > 0 && y.tokens[0].type === "paragraph" ? (y.tokens[0].text = k + " " + y.tokens[0].text, y.tokens[0].tokens && y.tokens[0].tokens.length > 0 && y.tokens[0].tokens[0].type === "text" && (y.tokens[0].tokens[0].text = k + " " + y.tokens[0].tokens[0].text)) : y.tokens.unshift({
              type: "text",
              text: k
            }) : v += k), v += this.parse(y.tokens, b), f += this.renderer.listitem(v, S, x);
          n += this.renderer.list(f, g, p);
          continue;
        }
        case "html": {
          n += this.renderer.html(m.text);
          continue;
        }
        case "paragraph": {
          n += this.renderer.paragraph(this.parseInline(m.tokens));
          continue;
        }
        case "text": {
          for (f = m.tokens ? this.parseInline(m.tokens) : m.text; r + 1 < A && t[r + 1].type === "text"; )
            m = t[++r], f += `
` + (m.tokens ? this.parseInline(m.tokens) : m.text);
          n += e ? this.renderer.paragraph(f) : f;
          continue;
        }
        default: {
          const L = 'Token with "' + m.type + '" type was not found.';
          if (this.options.silent) {
            console.error(L);
            return;
          } else
            throw new Error(L);
        }
      }
    }
    return n;
  }
  /**
   * Parse Inline Tokens
   */
  parseInline(t, e) {
    e = e || this.renderer;
    let n = "", r, s, o;
    const a = t.length;
    for (r = 0; r < a; r++) {
      if (s = t[r], this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[s.type] && (o = this.options.extensions.renderers[s.type].call({ parser: this }, s), o !== !1 || !["escape", "html", "link", "image", "strong", "em", "codespan", "br", "del", "text"].includes(s.type))) {
        n += o || "";
        continue;
      }
      switch (s.type) {
        case "escape": {
          n += e.text(s.text);
          break;
        }
        case "html": {
          n += e.html(s.text);
          break;
        }
        case "link": {
          n += e.link(s.href, s.title, this.parseInline(s.tokens, e));
          break;
        }
        case "image": {
          n += e.image(s.href, s.title, s.text);
          break;
        }
        case "strong": {
          n += e.strong(this.parseInline(s.tokens, e));
          break;
        }
        case "em": {
          n += e.em(this.parseInline(s.tokens, e));
          break;
        }
        case "codespan": {
          n += e.codespan(s.text);
          break;
        }
        case "br": {
          n += e.br();
          break;
        }
        case "del": {
          n += e.del(this.parseInline(s.tokens, e));
          break;
        }
        case "text": {
          n += e.text(s.text);
          break;
        }
        default: {
          const l = 'Token with "' + s.type + '" type was not found.';
          if (this.options.silent) {
            console.error(l);
            return;
          } else
            throw new Error(l);
        }
      }
    }
    return n;
  }
}
function He(i, t, e) {
  if (typeof i > "u" || i === null)
    throw new Error("marked(): input parameter is undefined or null");
  if (typeof i != "string")
    throw new Error("marked(): input parameter is of type " + Object.prototype.toString.call(i) + ", string expected");
  if (typeof t == "function" && (e = t, t = null), t = ir({}, He.defaults, t || {}), yx(t), e) {
    const n = t.highlight;
    let r;
    try {
      r = $r.lex(i, t);
    } catch (a) {
      return e(a);
    }
    const s = function(a) {
      let l;
      if (!a)
        try {
          t.walkTokens && He.walkTokens(r, t.walkTokens), l = zr.parse(r, t);
        } catch (c) {
          a = c;
        }
      return t.highlight = n, a ? e(a) : e(null, l);
    };
    if (!n || n.length < 3 || (delete t.highlight, !r.length)) return s();
    let o = 0;
    He.walkTokens(r, function(a) {
      a.type === "code" && (o++, setTimeout(() => {
        n(a.text, a.lang, function(l, c) {
          if (l)
            return s(l);
          c != null && c !== a.text && (a.text = c, a.escaped = !0), o--, o === 0 && s();
        });
      }, 0));
    }), o === 0 && s();
    return;
  }
  try {
    const n = $r.lex(i, t);
    return t.walkTokens && He.walkTokens(n, t.walkTokens), zr.parse(n, t);
  } catch (n) {
    if (n.message += `
Please report this to https://github.com/markedjs/marked.`, t.silent)
      return "<p>An error occurred:</p><pre>" + kn(n.message + "", !0) + "</pre>";
    throw n;
  }
}
He.options = He.setOptions = function(i) {
  return ir(He.defaults, i), TH(He.defaults), He;
};
He.getDefaults = vx;
He.defaults = wa;
He.use = function(...i) {
  const t = ir({}, ...i), e = He.defaults.extensions || { renderers: {}, childTokens: {} };
  let n;
  i.forEach((r) => {
    if (r.extensions && (n = !0, r.extensions.forEach((s) => {
      if (!s.name)
        throw new Error("extension name required");
      if (s.renderer) {
        const o = e.renderers ? e.renderers[s.name] : null;
        o ? e.renderers[s.name] = function(...a) {
          let l = s.renderer.apply(this, a);
          return l === !1 && (l = o.apply(this, a)), l;
        } : e.renderers[s.name] = s.renderer;
      }
      if (s.tokenizer) {
        if (!s.level || s.level !== "block" && s.level !== "inline")
          throw new Error("extension level must be 'block' or 'inline'");
        e[s.level] ? e[s.level].unshift(s.tokenizer) : e[s.level] = [s.tokenizer], s.start && (s.level === "block" ? e.startBlock ? e.startBlock.push(s.start) : e.startBlock = [s.start] : s.level === "inline" && (e.startInline ? e.startInline.push(s.start) : e.startInline = [s.start]));
      }
      s.childTokens && (e.childTokens[s.name] = s.childTokens);
    })), r.renderer) {
      const s = He.defaults.renderer || new Dp();
      for (const o in r.renderer) {
        const a = s[o];
        s[o] = (...l) => {
          let c = r.renderer[o].apply(s, l);
          return c === !1 && (c = a.apply(s, l)), c;
        };
      }
      t.renderer = s;
    }
    if (r.tokenizer) {
      const s = He.defaults.tokenizer || new Mp();
      for (const o in r.tokenizer) {
        const a = s[o];
        s[o] = (...l) => {
          let c = r.tokenizer[o].apply(s, l);
          return c === !1 && (c = a.apply(s, l)), c;
        };
      }
      t.tokenizer = s;
    }
    if (r.walkTokens) {
      const s = He.defaults.walkTokens;
      t.walkTokens = function(o) {
        r.walkTokens.call(this, o), s && s.call(this, o);
      };
    }
    n && (t.extensions = e), He.setOptions(t);
  });
};
He.walkTokens = function(i, t) {
  for (const e of i)
    switch (t.call(He, e), e.type) {
      case "table": {
        for (const n of e.header)
          He.walkTokens(n.tokens, t);
        for (const n of e.rows)
          for (const r of n)
            He.walkTokens(r.tokens, t);
        break;
      }
      case "list": {
        He.walkTokens(e.items, t);
        break;
      }
      default:
        He.defaults.extensions && He.defaults.extensions.childTokens && He.defaults.extensions.childTokens[e.type] ? He.defaults.extensions.childTokens[e.type].forEach(function(n) {
          He.walkTokens(e[n], t);
        }) : e.tokens && He.walkTokens(e.tokens, t);
    }
};
He.parseInline = function(i, t) {
  if (typeof i > "u" || i === null)
    throw new Error("marked.parseInline(): input parameter is undefined or null");
  if (typeof i != "string")
    throw new Error("marked.parseInline(): input parameter is of type " + Object.prototype.toString.call(i) + ", string expected");
  t = ir({}, He.defaults, t || {}), yx(t);
  try {
    const e = $r.lexInline(i, t);
    return t.walkTokens && He.walkTokens(e, t.walkTokens), zr.parseInline(e, t);
  } catch (e) {
    if (e.message += `
Please report this to https://github.com/markedjs/marked.`, t.silent)
      return "<p>An error occurred:</p><pre>" + kn(e.message + "", !0) + "</pre>";
    throw e;
  }
};
He.Parser = zr;
He.parser = zr.parse;
He.Renderer = Dp;
He.TextRenderer = _x;
He.Lexer = $r;
He.lexer = $r.lex;
He.Tokenizer = Mp;
He.Slugger = Cx;
He.parse = He;
He.options;
He.setOptions;
He.use;
He.walkTokens;
He.parseInline;
zr.parse;
$r.lex;
function jH(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t];
    for (var n in e)
      e.hasOwnProperty(n) && (i[n] = e[n]);
  }
  return i;
}
function Im(i, t) {
  return Array(t + 1).join(i);
}
function WH(i) {
  return i.replace(/^\n*/, "");
}
function UH(i) {
  for (var t = i.length; t > 0 && i[t - 1] === `
`; ) t--;
  return i.substring(0, t);
}
var qH = [
  "ADDRESS",
  "ARTICLE",
  "ASIDE",
  "AUDIO",
  "BLOCKQUOTE",
  "BODY",
  "CANVAS",
  "CENTER",
  "DD",
  "DIR",
  "DIV",
  "DL",
  "DT",
  "FIELDSET",
  "FIGCAPTION",
  "FIGURE",
  "FOOTER",
  "FORM",
  "FRAMESET",
  "H1",
  "H2",
  "H3",
  "H4",
  "H5",
  "H6",
  "HEADER",
  "HGROUP",
  "HR",
  "HTML",
  "ISINDEX",
  "LI",
  "MAIN",
  "MENU",
  "NAV",
  "NOFRAMES",
  "NOSCRIPT",
  "OL",
  "OUTPUT",
  "P",
  "PRE",
  "SECTION",
  "TABLE",
  "TBODY",
  "TD",
  "TFOOT",
  "TH",
  "THEAD",
  "TR",
  "UL"
];
function Lp(i) {
  return Np(i, qH);
}
var kx = [
  "AREA",
  "BASE",
  "BR",
  "COL",
  "COMMAND",
  "EMBED",
  "HR",
  "IMG",
  "INPUT",
  "KEYGEN",
  "LINK",
  "META",
  "PARAM",
  "SOURCE",
  "TRACK",
  "WBR"
];
function xx(i) {
  return Np(i, kx);
}
function GH(i) {
  return Tx(i, kx);
}
var Ex = [
  "A",
  "TABLE",
  "THEAD",
  "TBODY",
  "TFOOT",
  "TH",
  "TD",
  "IFRAME",
  "SCRIPT",
  "AUDIO",
  "VIDEO"
];
function KH(i) {
  return Np(i, Ex);
}
function ZH(i) {
  return Tx(i, Ex);
}
function Np(i, t) {
  return t.indexOf(i.nodeName) >= 0;
}
function Tx(i, t) {
  return i.getElementsByTagName && t.some(function(e) {
    return i.getElementsByTagName(e).length;
  });
}
var Wn = {};
Wn.paragraph = {
  filter: "p",
  replacement: function(i) {
    return `

` + i + `

`;
  }
};
Wn.lineBreak = {
  filter: "br",
  replacement: function(i, t, e) {
    return e.br + `
`;
  }
};
Wn.heading = {
  filter: ["h1", "h2", "h3", "h4", "h5", "h6"],
  replacement: function(i, t, e) {
    var n = Number(t.nodeName.charAt(1));
    if (e.headingStyle === "setext" && n < 3) {
      var r = Im(n === 1 ? "=" : "-", i.length);
      return `

` + i + `
` + r + `

`;
    } else
      return `

` + Im("#", n) + " " + i + `

`;
  }
};
Wn.blockquote = {
  filter: "blockquote",
  replacement: function(i) {
    return i = i.replace(/^\n+|\n+$/g, ""), i = i.replace(/^/gm, "> "), `

` + i + `

`;
  }
};
Wn.list = {
  filter: ["ul", "ol"],
  replacement: function(i, t) {
    var e = t.parentNode;
    return e.nodeName === "LI" && e.lastElementChild === t ? `
` + i : `

` + i + `

`;
  }
};
Wn.listItem = {
  filter: "li",
  replacement: function(i, t, e) {
    i = i.replace(/^\n+/, "").replace(/\n+$/, `
`).replace(/\n/gm, `
    `);
    var n = e.bulletListMarker + "   ", r = t.parentNode;
    if (r.nodeName === "OL") {
      var s = r.getAttribute("start"), o = Array.prototype.indexOf.call(r.children, t);
      n = (s ? Number(s) + o : o + 1) + ".  ";
    }
    return n + i + (t.nextSibling && !/\n$/.test(i) ? `
` : "");
  }
};
Wn.indentedCodeBlock = {
  filter: function(i, t) {
    return t.codeBlockStyle === "indented" && i.nodeName === "PRE" && i.firstChild && i.firstChild.nodeName === "CODE";
  },
  replacement: function(i, t, e) {
    return `

    ` + t.firstChild.textContent.replace(/\n/g, `
    `) + `

`;
  }
};
Wn.fencedCodeBlock = {
  filter: function(i, t) {
    return t.codeBlockStyle === "fenced" && i.nodeName === "PRE" && i.firstChild && i.firstChild.nodeName === "CODE";
  },
  replacement: function(i, t, e) {
    for (var n = t.firstChild.getAttribute("class") || "", r = (n.match(/language-(\S+)/) || [null, ""])[1], s = t.firstChild.textContent, o = e.fence.charAt(0), a = 3, l = new RegExp("^" + o + "{3,}", "gm"), c; c = l.exec(s); )
      c[0].length >= a && (a = c[0].length + 1);
    var u = Im(o, a);
    return `

` + u + r + `
` + s.replace(/\n$/, "") + `
` + u + `

`;
  }
};
Wn.horizontalRule = {
  filter: "hr",
  replacement: function(i, t, e) {
    return `

` + e.hr + `

`;
  }
};
Wn.inlineLink = {
  filter: function(i, t) {
    return t.linkStyle === "inlined" && i.nodeName === "A" && i.getAttribute("href");
  },
  replacement: function(i, t) {
    var e = t.getAttribute("href");
    e && (e = e.replace(/([()])/g, "\\$1"));
    var n = sd(t.getAttribute("title"));
    return n && (n = ' "' + n.replace(/"/g, '\\"') + '"'), "[" + i + "](" + e + n + ")";
  }
};
Wn.referenceLink = {
  filter: function(i, t) {
    return t.linkStyle === "referenced" && i.nodeName === "A" && i.getAttribute("href");
  },
  replacement: function(i, t, e) {
    var n = t.getAttribute("href"), r = sd(t.getAttribute("title"));
    r && (r = ' "' + r + '"');
    var s, o;
    switch (e.linkReferenceStyle) {
      case "collapsed":
        s = "[" + i + "][]", o = "[" + i + "]: " + n + r;
        break;
      case "shortcut":
        s = "[" + i + "]", o = "[" + i + "]: " + n + r;
        break;
      default:
        var a = this.references.length + 1;
        s = "[" + i + "][" + a + "]", o = "[" + a + "]: " + n + r;
    }
    return this.references.push(o), s;
  },
  references: [],
  append: function(i) {
    var t = "";
    return this.references.length && (t = `

` + this.references.join(`
`) + `

`, this.references = []), t;
  }
};
Wn.emphasis = {
  filter: ["em", "i"],
  replacement: function(i, t, e) {
    return i.trim() ? e.emDelimiter + i + e.emDelimiter : "";
  }
};
Wn.strong = {
  filter: ["strong", "b"],
  replacement: function(i, t, e) {
    return i.trim() ? e.strongDelimiter + i + e.strongDelimiter : "";
  }
};
Wn.code = {
  filter: function(i) {
    var t = i.previousSibling || i.nextSibling, e = i.parentNode.nodeName === "PRE" && !t;
    return i.nodeName === "CODE" && !e;
  },
  replacement: function(i) {
    if (!i) return "";
    i = i.replace(/\r?\n|\r/g, " ");
    for (var t = /^`|^ .*?[^ ].* $|`$/.test(i) ? " " : "", e = "`", n = i.match(/`+/gm) || []; n.indexOf(e) !== -1; ) e = e + "`";
    return e + t + i + t + e;
  }
};
Wn.image = {
  filter: "img",
  replacement: function(i, t) {
    var e = sd(t.getAttribute("alt")), n = t.getAttribute("src") || "", r = sd(t.getAttribute("title")), s = r ? ' "' + r + '"' : "";
    return n ? "![" + e + "](" + n + s + ")" : "";
  }
};
function sd(i) {
  return i ? i.replace(/(\n+\s*)+/g, `
`) : "";
}
function Sx(i) {
  this.options = i, this._keep = [], this._remove = [], this.blankRule = {
    replacement: i.blankReplacement
  }, this.keepReplacement = i.keepReplacement, this.defaultRule = {
    replacement: i.defaultReplacement
  }, this.array = [];
  for (var t in i.rules) this.array.push(i.rules[t]);
}
Sx.prototype = {
  add: function(i, t) {
    this.array.unshift(t);
  },
  keep: function(i) {
    this._keep.unshift({
      filter: i,
      replacement: this.keepReplacement
    });
  },
  remove: function(i) {
    this._remove.unshift({
      filter: i,
      replacement: function() {
        return "";
      }
    });
  },
  forNode: function(i) {
    if (i.isBlank) return this.blankRule;
    var t;
    return (t = Cf(this.array, i, this.options)) || (t = Cf(this._keep, i, this.options)) || (t = Cf(this._remove, i, this.options)) ? t : this.defaultRule;
  },
  forEach: function(i) {
    for (var t = 0; t < this.array.length; t++) i(this.array[t], t);
  }
};
function Cf(i, t, e) {
  for (var n = 0; n < i.length; n++) {
    var r = i[n];
    if (YH(r, t, e)) return r;
  }
}
function YH(i, t, e) {
  var n = i.filter;
  if (typeof n == "string") {
    if (n === t.nodeName.toLowerCase()) return !0;
  } else if (Array.isArray(n)) {
    if (n.indexOf(t.nodeName.toLowerCase()) > -1) return !0;
  } else if (typeof n == "function") {
    if (n.call(i, t, e)) return !0;
  } else
    throw new TypeError("`filter` needs to be a string, array, or function");
}
function JH(i) {
  var t = i.element, e = i.isBlock, n = i.isVoid, r = i.isPre || function(d) {
    return d.nodeName === "PRE";
  };
  if (!(!t.firstChild || r(t))) {
    for (var s = null, o = !1, a = null, l = l0(a, t, r); l !== t; ) {
      if (l.nodeType === 3 || l.nodeType === 4) {
        var c = l.data.replace(/[ \r\n\t]+/g, " ");
        if ((!s || / $/.test(s.data)) && !o && c[0] === " " && (c = c.substr(1)), !c) {
          l = kf(l);
          continue;
        }
        l.data = c, s = l;
      } else if (l.nodeType === 1)
        e(l) || l.nodeName === "BR" ? (s && (s.data = s.data.replace(/ $/, "")), s = null, o = !1) : n(l) || r(l) ? (s = null, o = !0) : s && (o = !1);
      else {
        l = kf(l);
        continue;
      }
      var u = l0(a, l, r);
      a = l, l = u;
    }
    s && (s.data = s.data.replace(/ $/, ""), s.data || kf(s));
  }
}
function kf(i) {
  var t = i.nextSibling || i.parentNode;
  return i.parentNode.removeChild(i), t;
}
function l0(i, t, e) {
  return i && i.parentNode === t || e(t) ? t.nextSibling || t.parentNode : t.firstChild || t.nextSibling || t.parentNode;
}
var Bp = typeof window < "u" ? window : {};
function XH() {
  var i = Bp.DOMParser, t = !1;
  try {
    new i().parseFromString("", "text/html") && (t = !0);
  } catch {
  }
  return t;
}
function QH() {
  var i = function() {
  };
  return ej() ? i.prototype.parseFromString = function(t) {
    var e = new window.ActiveXObject("htmlfile");
    return e.designMode = "on", e.open(), e.write(t), e.close(), e;
  } : i.prototype.parseFromString = function(t) {
    var e = document.implementation.createHTMLDocument("");
    return e.open(), e.write(t), e.close(), e;
  }, i;
}
function ej() {
  var i = !1;
  try {
    document.implementation.createHTMLDocument("").open();
  } catch {
    Bp.ActiveXObject && (i = !0);
  }
  return i;
}
var tj = XH() ? Bp.DOMParser : QH();
function nj(i, t) {
  var e;
  if (typeof i == "string") {
    var n = ij().parseFromString(
      // DOM parsers arrange elements in the <head> and <body>.
      // Wrapping in a custom element ensures elements are reliably arranged in
      // a single element.
      '<x-turndown id="turndown-root">' + i + "</x-turndown>",
      "text/html"
    );
    e = n.getElementById("turndown-root");
  } else
    e = i.cloneNode(!0);
  return JH({
    element: e,
    isBlock: Lp,
    isVoid: xx,
    isPre: t.preformattedCode ? rj : null
  }), e;
}
var xf;
function ij() {
  return xf = xf || new tj(), xf;
}
function rj(i) {
  return i.nodeName === "PRE" || i.nodeName === "CODE";
}
function sj(i, t) {
  return i.isBlock = Lp(i), i.isCode = i.nodeName === "CODE" || i.parentNode.isCode, i.isBlank = oj(i), i.flankingWhitespace = aj(i, t), i;
}
function oj(i) {
  return !xx(i) && !KH(i) && /^\s*$/i.test(i.textContent) && !GH(i) && !ZH(i);
}
function aj(i, t) {
  if (i.isBlock || t.preformattedCode && i.isCode)
    return { leading: "", trailing: "" };
  var e = lj(i.textContent);
  return e.leadingAscii && c0("left", i, t) && (e.leading = e.leadingNonAscii), e.trailingAscii && c0("right", i, t) && (e.trailing = e.trailingNonAscii), { leading: e.leading, trailing: e.trailing };
}
function lj(i) {
  var t = i.match(/^(([ \t\r\n]*)(\s*))(?:(?=\S)[\s\S]*\S)?((\s*?)([ \t\r\n]*))$/);
  return {
    leading: t[1],
    // whole string for whitespace-only strings
    leadingAscii: t[2],
    leadingNonAscii: t[3],
    trailing: t[4],
    // empty for whitespace-only strings
    trailingNonAscii: t[5],
    trailingAscii: t[6]
  };
}
function c0(i, t, e) {
  var n, r, s;
  return i === "left" ? (n = t.previousSibling, r = / $/) : (n = t.nextSibling, r = /^ /), n && (n.nodeType === 3 ? s = r.test(n.nodeValue) : e.preformattedCode && n.nodeName === "CODE" ? s = !1 : n.nodeType === 1 && !Lp(n) && (s = r.test(n.textContent))), s;
}
var cj = Array.prototype.reduce, uj = [
  [/\\/g, "\\\\"],
  [/\*/g, "\\*"],
  [/^-/g, "\\-"],
  [/^\+ /g, "\\+ "],
  [/^(=+)/g, "\\$1"],
  [/^(#{1,6}) /g, "\\$1 "],
  [/`/g, "\\`"],
  [/^~~~/g, "\\~~~"],
  [/\[/g, "\\["],
  [/\]/g, "\\]"],
  [/^>/g, "\\>"],
  [/_/g, "\\_"],
  [/^(\d+)\. /g, "$1\\. "]
];
function od(i) {
  if (!(this instanceof od)) return new od(i);
  var t = {
    rules: Wn,
    headingStyle: "setext",
    hr: "* * *",
    bulletListMarker: "*",
    codeBlockStyle: "indented",
    fence: "```",
    emDelimiter: "_",
    strongDelimiter: "**",
    linkStyle: "inlined",
    linkReferenceStyle: "full",
    br: "  ",
    preformattedCode: !1,
    blankReplacement: function(e, n) {
      return n.isBlock ? `

` : "";
    },
    keepReplacement: function(e, n) {
      return n.isBlock ? `

` + n.outerHTML + `

` : n.outerHTML;
    },
    defaultReplacement: function(e, n) {
      return n.isBlock ? `

` + e + `

` : e;
    }
  };
  this.options = jH({}, t, i), this.rules = new Sx(this.options);
}
od.prototype = {
  /**
   * The entry point for converting a string or DOM node to Markdown
   * @public
   * @param {String|HTMLElement} input The string or DOM node to convert
   * @returns A Markdown representation of the input
   * @type String
   */
  turndown: function(i) {
    if (!fj(i))
      throw new TypeError(
        i + " is not a string, or an element/document/fragment node."
      );
    if (i === "") return "";
    var t = Ax.call(this, new nj(i, this.options));
    return dj.call(this, t);
  },
  /**
   * Add one or more plugins
   * @public
   * @param {Function|Array} plugin The plugin or array of plugins to add
   * @returns The Turndown instance for chaining
   * @type Object
   */
  use: function(i) {
    if (Array.isArray(i))
      for (var t = 0; t < i.length; t++) this.use(i[t]);
    else if (typeof i == "function")
      i(this);
    else
      throw new TypeError("plugin must be a Function or an Array of Functions");
    return this;
  },
  /**
   * Adds a rule
   * @public
   * @param {String} key The unique key of the rule
   * @param {Object} rule The rule
   * @returns The Turndown instance for chaining
   * @type Object
   */
  addRule: function(i, t) {
    return this.rules.add(i, t), this;
  },
  /**
   * Keep a node (as HTML) that matches the filter
   * @public
   * @param {String|Array|Function} filter The unique key of the rule
   * @returns The Turndown instance for chaining
   * @type Object
   */
  keep: function(i) {
    return this.rules.keep(i), this;
  },
  /**
   * Remove a node that matches the filter
   * @public
   * @param {String|Array|Function} filter The unique key of the rule
   * @returns The Turndown instance for chaining
   * @type Object
   */
  remove: function(i) {
    return this.rules.remove(i), this;
  },
  /**
   * Escapes Markdown syntax
   * @public
   * @param {String} string The string to escape
   * @returns A string with Markdown syntax escaped
   * @type String
   */
  escape: function(i) {
    return uj.reduce(function(t, e) {
      return t.replace(e[0], e[1]);
    }, i);
  }
};
function Ax(i) {
  var t = this;
  return cj.call(i.childNodes, function(e, n) {
    n = new sj(n, t.options);
    var r = "";
    return n.nodeType === 3 ? r = n.isCode ? n.nodeValue : t.escape(n.nodeValue) : n.nodeType === 1 && (r = hj.call(t, n)), Px(e, r);
  }, "");
}
function dj(i) {
  var t = this;
  return this.rules.forEach(function(e) {
    typeof e.append == "function" && (i = Px(i, e.append(t.options)));
  }), i.replace(/^[\t\r\n]+/, "").replace(/[\t\r\n\s]+$/, "");
}
function hj(i) {
  var t = this.rules.forNode(i), e = Ax.call(this, i), n = i.flankingWhitespace;
  return (n.leading || n.trailing) && (e = e.trim()), n.leading + t.replacement(e, i, this.options) + n.trailing;
}
function Px(i, t) {
  var e = UH(i), n = WH(t), r = Math.max(i.length - e.length, t.length - n.length), s = `

`.substring(0, r);
  return e + s + n;
}
function fj(i) {
  return i != null && (typeof i == "string" || i.nodeType && (i.nodeType === 1 || i.nodeType === 9 || i.nodeType === 11));
}
var u0 = /highlight-(?:text|source)-([a-z0-9]+)/;
function mj(i) {
  i.addRule("highlightedCodeBlock", {
    filter: function(t) {
      var e = t.firstChild;
      return t.nodeName === "DIV" && u0.test(t.className) && e && e.nodeName === "PRE";
    },
    replacement: function(t, e, n) {
      var r = e.className || "", s = (r.match(u0) || [null, ""])[1];
      return `

` + n.fence + s + `
` + e.firstChild.textContent + `
` + n.fence + `

`;
    }
  });
}
function gj(i) {
  i.addRule("strikethrough", {
    filter: ["del", "s", "strike"],
    replacement: function(t) {
      return "~" + t + "~";
    }
  });
}
var pj = Array.prototype.indexOf, bj = Array.prototype.every, aa = {};
aa.tableCell = {
  filter: ["th", "td"],
  replacement: function(i, t) {
    return Ix(i, t);
  }
};
aa.tableRow = {
  filter: "tr",
  replacement: function(i, t) {
    var e = "", n = { left: ":--", right: "--:", center: ":-:" };
    if (Fp(t))
      for (var r = 0; r < t.childNodes.length; r++) {
        var s = "---", o = (t.childNodes[r].getAttribute("align") || "").toLowerCase();
        o && (s = n[o] || s), e += Ix(s, t.childNodes[r]);
      }
    return `
` + i + (e ? `
` + e : "");
  }
};
aa.table = {
  // Only convert tables with a heading row.
  // Tables with no heading row are kept using `keep` (see below).
  filter: function(i) {
    return i.nodeName === "TABLE" && Fp(i.rows[0]);
  },
  replacement: function(i) {
    return i = i.replace(`

`, `
`), `

` + i + `

`;
  }
};
aa.tableSection = {
  filter: ["thead", "tbody", "tfoot"],
  replacement: function(i) {
    return i;
  }
};
function Fp(i) {
  var t = i.parentNode;
  return t.nodeName === "THEAD" || t.firstChild === i && (t.nodeName === "TABLE" || vj(t)) && bj.call(i.childNodes, function(e) {
    return e.nodeName === "TH";
  });
}
function vj(i) {
  var t = i.previousSibling;
  return i.nodeName === "TBODY" && (!t || t.nodeName === "THEAD" && /^\s*$/i.test(t.textContent));
}
function Ix(i, t) {
  var e = pj.call(t.parentNode.childNodes, t), n = " ";
  return e === 0 && (n = "| "), n + i + " |";
}
function wj(i) {
  i.keep(function(e) {
    return e.nodeName === "TABLE" && !Fp(e.rows[0]);
  });
  for (var t in aa) i.addRule(t, aa[t]);
}
function yj(i) {
  i.addRule("taskListItems", {
    filter: function(t) {
      return t.type === "checkbox" && t.parentNode.nodeName === "LI";
    },
    replacement: function(t, e) {
      return (e.checked ? "[x]" : "[ ]") + " ";
    }
  });
}
function _j(i) {
  i.use([
    mj,
    gj,
    wj,
    yj
  ]);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class Cj {
  constructor() {
    h(this, "_parser");
    h(this, "_options", {
      gfm: !0,
      breaks: !0,
      tables: !0,
      xhtml: !0,
      headerIds: !1
    });
    He.use({
      tokenizer: {
        // Disable the autolink rule in the lexer.
        autolink: () => null,
        url: () => null
      },
      renderer: {
        checkbox(...t) {
          return Object.getPrototypeOf(this).checkbox.call(this, ...t).trimRight();
        },
        code(...t) {
          return Object.getPrototypeOf(this).code.call(this, ...t).replace(`
</code>`, "</code>");
        }
      }
    }), this._parser = He;
  }
  parse(t) {
    return this._parser.parse(t, this._options);
  }
}
const kj = /* @__PURE__ */ new RegExp(
  // Prefix.
  /\b(?:(?:https?|ftp):\/\/|www\.)/.source + // Domain name.
  /(?![-_])(?:[-_a-z0-9\u00a1-\uffff]{1,63}\.)+(?:[a-z\u00a1-\uffff]{2,63})/.source + // The rest.
  /(?:[^\s<>]*)/.source,
  "gi"
);
class xj extends od {
  escape(t) {
    const e = super.escape;
    function n(o) {
      return o = e(o), o = o.replace(/</g, "\\<"), o;
    }
    let r = "", s = 0;
    for (const o of this._matchAutolink(t)) {
      const a = o.index;
      a > s && (r += n(t.substring(s, a)));
      const l = o[0];
      r += l, s = a + l.length;
    }
    return s < t.length && (r += n(t.substring(s, t.length))), r;
  }
  /**
  * Trimming end of link.
  * https://github.github.com/gfm/#autolinks-extension-
  */
  *_matchAutolink(t) {
    for (const e of t.matchAll(kj)) {
      const n = e[0], r = this._autolinkFindEnd(n);
      yield Object.assign([
        n.substring(0, r)
      ], {
        index: e.index
      });
    }
  }
  /**
  * Returns the new length of the link (after it would trim trailing characters).
  */
  _autolinkFindEnd(t) {
    let e = t.length;
    for (; e > 0; ) {
      const n = t[e - 1];
      if (`?!.,:*_~'"`.includes(n))
        e--;
      else if (n == ")") {
        let r = 0;
        for (let s = 0; s < e; s++)
          t[s] == "(" ? r++ : t[s] == ")" && r--;
        if (r < 0)
          e--;
        else
          break;
      } else
        break;
    }
    return e;
  }
}
class Ej {
  constructor() {
    h(this, "_parser");
    this._parser = this._createParser();
  }
  parse(t) {
    return this._parser.turndown(t);
  }
  keep(t) {
    this._parser.keep(t);
  }
  _createParser() {
    const t = new xj({
      codeBlockStyle: "fenced",
      hr: "---",
      headingStyle: "atx"
    });
    return t.use([
      _j,
      this._todoList
    ]), t;
  }
  // This is a copy of the original taskListItems rule from turndown-plugin-gfm, with minor changes.
  _todoList(t) {
    t.addRule("taskListItems", {
      filter(e) {
        return e.type === "checkbox" && // Changes here as CKEditor outputs a deeper structure.
        (e.parentNode.nodeName === "LI" || e.parentNode.parentNode.nodeName === "LI");
      },
      replacement(e, n) {
        return (n.checked ? "[x]" : "[ ]") + " ";
      }
    });
  }
}
class Tj {
  /**
  * Creates a new instance of the Markdown data processor class.
  */
  constructor(t) {
    /**
    * HTML data processor used to process HTML produced by the Markdown-to-HTML converter and the other way.
    */
    h(this, "_htmlDP");
    /**
    * Helper for converting Markdown to HTML.
    */
    h(this, "_markdown2html");
    /**
    * Helper for converting HTML to Markdown.
    */
    h(this, "_html2markdown");
    this._htmlDP = new hC(t), this._markdown2html = new Cj(), this._html2markdown = new Ej();
  }
  /**
  * Keeps the specified element in the output as HTML. This is useful if the editor contains
  * features producing HTML that is not a part of the Markdown standard.
  *
  * By default, all HTML tags are removed.
  *
  * @param element The element name to be kept.
  */
  keepHtml(t) {
    this._html2markdown.keep([
      t
    ]);
  }
  /**
  * Converts the provided Markdown string to a view tree.
  *
  * @param data A Markdown string.
  * @returns The converted view element.
  */
  toView(t) {
    const e = this._markdown2html.parse(t);
    return this._htmlDP.toView(e);
  }
  /**
  * Converts the provided {@link module:engine/view/documentfragment~DocumentFragment} to data format &ndash; in this
  * case to a Markdown string.
  *
  * @returns Markdown string.
  */
  toData(t) {
    const e = this._htmlDP.toData(t);
    return this._html2markdown.parse(e);
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} for view elements whose content should be treated as raw data
  * and not processed during the conversion from Markdown to view elements.
  *
  * The raw data can be later accessed by a
  * {@link module:engine/view/element~Element#getCustomProperty custom property of a view element} called `"$rawContent"`.
  *
  * @param pattern The pattern matching all view elements whose content should
  * be treated as raw data.
  */
  registerRawContentMatcher(t) {
    this._htmlDP.registerRawContentMatcher(t);
  }
  /**
  * This method does not have any effect on the data processor result. It exists for compatibility with the
  * {@link module:engine/dataprocessor/dataprocessor~DataProcessor `DataProcessor` interface}.
  */
  useFillerType() {
  }
}
const Sj = [
  "SPAN",
  "BR",
  "PRE",
  "CODE"
];
class Aj extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * @internal
    */
    h(this, "_gfmDataProcessor");
    this._gfmDataProcessor = new Tj(e.data.viewDocument);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PasteFromMarkdownExperimental";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ui
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, r = e.editing.view.document, s = e.plugins.get("ClipboardPipeline");
    let o = !1;
    this.listenTo(r, "keydown", (a, l) => {
      o = l.shiftKey;
    }), this.listenTo(s, "inputTransformation", (a, l) => {
      if (o)
        return;
      const c = l.dataTransfer.getData("text/html");
      if (!c) {
        const d = l.dataTransfer.getData("text/plain");
        l.content = this._gfmDataProcessor.toView(d);
        return;
      }
      const u = this._parseMarkdownFromHtml(c);
      u && (l.content = this._gfmDataProcessor.toView(u));
    });
  }
  /**
  * Determines if the code copied from a website in the `text/html` type can be parsed as Markdown.
  * It removes any OS-specific HTML tags, for example, <meta> on macOS and <!--StartFragment--> on Windows.
  * Then removes a single wrapper HTML tag or wrappers for sibling tags, and if there are no more tags left,
  * returns the remaining text. Returns null if there are any remaining HTML tags detected.
  *
  * @param htmlString Clipboard content in the `text/html` type format.
  */
  _parseMarkdownFromHtml(e) {
    const n = this._removeOsSpecificTags(e);
    if (!this._containsOnlyAllowedFirstLevelTags(n))
      return null;
    const r = this._removeFirstLevelWrapperTagsAndBrs(n);
    return this._containsAnyRemainingHtmlTags(r) ? null : this._replaceHtmlReservedEntitiesWithCharacters(r);
  }
  /**
  * Removes OS-specific tags.
  *
  * @param htmlString Clipboard content in the `text/html` type format.
  */
  _removeOsSpecificTags(e) {
    return e.replace(/^<meta\b[^>]*>/, "").trim().replace(/^<html>/, "").replace(/<\/html>$/, "").trim().replace(/^<body>/, "").replace(/<\/body>$/, "").trim().replace(/^<!--StartFragment-->/, "").replace(/<!--EndFragment-->$/, "").trim();
  }
  /**
  * If the input HTML string contains any first-level formatting tags
  * like <b>, <strong>, or <i>, we should not treat it as Markdown.
  *
  * @param htmlString Clipboard content.
  */
  _containsOnlyAllowedFirstLevelTags(e) {
    const n = new DOMParser(), { body: r } = n.parseFromString(e, "text/html");
    return Array.from(r.children).map((o) => o.tagName).every((o) => Sj.includes(o));
  }
  /**
  * Removes multiple HTML wrapper tags from a list of sibling HTML tags.
  *
  * @param htmlString Clipboard content without any OS-specific tags.
  */
  _removeFirstLevelWrapperTagsAndBrs(e) {
    const n = new DOMParser(), { body: r } = n.parseFromString(e, "text/html"), s = r.querySelectorAll("br");
    for (const a of s)
      a.replaceWith(`
`);
    const o = r.querySelectorAll(":scope > *");
    for (const a of o) {
      const l = a.cloneNode(!0);
      a.replaceWith(...l.childNodes);
    }
    return r.innerHTML;
  }
  /**
  * Determines if a string contains any HTML tags.
  */
  _containsAnyRemainingHtmlTags(e) {
    return e.includes("<");
  }
  /**
  * Replaces the reserved HTML entities with the actual characters.
  *
  * @param htmlString Clipboard content without any tags.
  */
  _replaceHtmlReservedEntitiesWithCharacters(e) {
    return e.replace(/&gt;/g, ">").replace(/&lt;/g, "<").replace(/&nbsp;/g, " ");
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function d0(i, t) {
  const e = (n, r, s) => {
    if (!s.consumable.consume(r.item, n.name))
      return;
    const o = r.attributeNewValue, a = s.writer, l = s.mapper.toViewElement(r.item), c = [
      ...l.getChildren()
    ].find((d) => d.getCustomProperty("media-content"));
    a.remove(c);
    const u = i.getMediaViewElement(a, o, t);
    a.insert(a.createPositionAt(l, 0), u);
  };
  return (n) => {
    n.on("attribute:url:media", e);
  };
}
function Pj(i, t, e) {
  return t.setCustomProperty("media", !0, i), va(i, t, {
    label: e
  });
}
function h0(i, t, e, n) {
  return i.createContainerElement("figure", {
    class: "media"
  }, [
    t.getMediaViewElement(i, e, n),
    i.createSlot()
  ]);
}
function f0(i) {
  const t = i.getSelectedElement();
  return t && t.is("element", "media") ? t : null;
}
function Rx(i, t, e, n) {
  i.change((r) => {
    const s = r.createElement("media", {
      url: t
    });
    i.insertObject(s, e, null, {
      setSelection: "on",
      findOptimalPosition: n ? "auto" : void 0
    });
  });
}
class Ij extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document.selection, n = f0(e);
    this.value = n ? n.getAttribute("url") : void 0, this.isEnabled = Oj(e) || Rj(e, t);
  }
  /**
  * Executes the command, which either:
  *
  * * updates the URL of the selected media,
  * * inserts the new media into the editor and puts the selection around it.
  *
  * @fires execute
  * @param url The URL of the media.
  */
  execute(t) {
    const e = this.editor.model, n = e.document.selection, r = f0(n);
    r ? e.change((s) => {
      s.setAttribute("url", t, r);
    }) : Rx(e, t, n, !0);
  }
}
function Rj(i, t) {
  let n = yp(i, t).start.parent;
  return n.isEmpty && !t.schema.isLimit(n) && (n = n.parent), t.schema.checkChild(n, "media");
}
function Oj(i) {
  const t = i.getSelectedElement();
  return !!t && t.name === "media";
}
var Vj = '<svg viewBox="0 0 64 42" xmlns="http://www.w3.org/2000/svg"><path d="M47.426 17V3.713L63.102 0v19.389h-.001l.001.272c0 1.595-2.032 3.43-4.538 4.098-2.506.668-4.538-.083-4.538-1.678 0-1.594 2.032-3.43 4.538-4.098.914-.244 2.032-.565 2.888-.603V4.516L49.076 7.447v9.556A1.014 1.014 0 0 0 49 17h-1.574zM29.5 17h-8.343a7.073 7.073 0 1 0-4.657 4.06v3.781H3.3a2.803 2.803 0 0 1-2.8-2.804V8.63a2.803 2.803 0 0 1 2.8-2.805h4.082L8.58 2.768A1.994 1.994 0 0 1 10.435 1.5h8.985c.773 0 1.477.448 1.805 1.149l1.488 3.177H26.7c1.546 0 2.8 1.256 2.8 2.805V17zm-11.637 0H17.5a1 1 0 0 0-1 1v.05A4.244 4.244 0 1 1 17.863 17zm29.684 2c.97 0 .953-.048.953.889v20.743c0 .953.016.905-.953.905H19.453c-.97 0-.953.048-.953-.905V19.89c0-.937-.016-.889.97-.889h28.077zm-4.701 19.338V22.183H24.154v16.155h18.692zM20.6 21.375v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616V37.53H20.6zm24.233-16.155v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615V37.53h-1.615zM29.485 25.283a.4.4 0 0 1 .593-.35l9.05 4.977a.4.4 0 0 1 0 .701l-9.05 4.978a.4.4 0 0 1-.593-.35v-9.956z"/></svg>';
const Mj = "0 0 64 42";
class Dj {
  /**
  * Creates an instance of the {@link module:media-embed/mediaregistry~MediaRegistry} class.
  *
  * @param locale The localization services instance.
  * @param config The configuration of the media embed feature.
  */
  constructor(t, e) {
    /**
    * The {@link module:utils/locale~Locale} instance.
    */
    h(this, "locale");
    /**
    * The media provider definitions available for the registry. Usually corresponding with the
    * {@link module:media-embed/mediaembedconfig~MediaEmbedConfig media configuration}.
    */
    h(this, "providerDefinitions");
    const n = e.providers, r = e.extraProviders || [], s = new Set(e.removeProviders), o = n.concat(r).filter((a) => {
      const l = a.name;
      return l ? !s.has(l) : (Xe("media-embed-no-provider-name", {
        provider: a
      }), !1);
    });
    this.locale = t, this.providerDefinitions = o;
  }
  /**
  * Checks whether the passed URL is representing a certain media type allowed in the editor.
  *
  * @param url The URL to be checked
  */
  hasMedia(t) {
    return !!this._getMedia(t);
  }
  /**
  * For the given media URL string and options, it returns the {@link module:engine/view/element~Element view element}
  * representing that media.
  *
  * **Note:** If no URL is specified, an empty view element is returned.
  *
  * @param writer The view writer used to produce a view element.
  * @param url The URL to be translated into a view element.
  */
  getMediaViewElement(t, e, n) {
    return this._getMedia(e).getViewElement(t, n);
  }
  /**
  * Returns a `Media` instance for the given URL.
  *
  * @param url The URL of the media.
  * @returns The `Media` instance or `null` when there is none.
  */
  _getMedia(t) {
    if (!t)
      return new m0(this.locale);
    t = t.trim();
    for (const e of this.providerDefinitions) {
      const n = e.html, r = tt(e.url);
      for (const s of r) {
        const o = this._getUrlMatches(t, s);
        if (o)
          return new m0(this.locale, t, o, n);
      }
    }
    return null;
  }
  /**
  * Tries to match `url` to `pattern`.
  *
  * @param url The URL of the media.
  * @param pattern The pattern that should accept the media URL.
  */
  _getUrlMatches(t, e) {
    let n = t.match(e);
    if (n)
      return n;
    let r = t.replace(/^https?:\/\//, "");
    return n = r.match(e), n || (r = r.replace(/^www\./, ""), n = r.match(e), n) ? n : null;
  }
}
class m0 {
  constructor(t, e, n, r) {
    /**
    * The URL this Media instance represents.
    */
    h(this, "url");
    /**
    * Shorthand for {@link module:utils/locale~Locale#t}.
    *
    * @see module:utils/locale~Locale#t
    */
    h(this, "_locale");
    /**
    * The output of the `RegExp.match` which validated the {@link #url} of this media.
    */
    h(this, "_match");
    /**
    * The function returning the HTML string preview of this media.
    */
    h(this, "_previewRenderer");
    this.url = this._getValidUrl(e), this._locale = t, this._match = n, this._previewRenderer = r;
  }
  /**
  * Returns the view element representation of the media.
  *
  * @param writer The view writer used to produce a view element.
  */
  getViewElement(t, e) {
    const n = {};
    let r;
    if (e.renderForEditingView || e.renderMediaPreview && this.url && this._previewRenderer) {
      this.url && (n["data-oembed-url"] = this.url), e.renderForEditingView && (n.class = "ck-media__wrapper");
      const s = this._getPreviewHtml(e);
      r = t.createRawElement("div", n, (o, a) => {
        a.setContentOf(o, s);
      });
    } else
      this.url && (n.url = this.url), r = t.createEmptyElement(e.elementName, n);
    return t.setCustomProperty("media-content", !0, r), r;
  }
  /**
  * Returns the HTML string of the media content preview.
  */
  _getPreviewHtml(t) {
    return this._previewRenderer ? this._previewRenderer(this._match) : this.url && t.renderForEditingView ? this._getPlaceholderHtml() : "";
  }
  /**
  * Returns the placeholder HTML when the media has no content preview.
  */
  _getPlaceholderHtml() {
    const t = new pr(), e = this._locale.t;
    return t.content = Vj, t.viewBox = Mj, new li({
      tag: "div",
      attributes: {
        class: "ck ck-reset_all ck-media__placeholder"
      },
      children: [
        {
          tag: "div",
          attributes: {
            class: "ck-media__placeholder__icon"
          },
          children: [
            t
          ]
        },
        {
          tag: "a",
          attributes: {
            class: "ck-media__placeholder__url",
            target: "_blank",
            rel: "noopener noreferrer",
            href: this.url,
            "data-cke-tooltip-text": e("Open media in new tab")
          },
          children: [
            {
              tag: "span",
              attributes: {
                class: "ck-media__placeholder__url__text"
              },
              children: [
                this.url
              ]
            }
          ]
        }
      ]
    }).render().outerHTML;
  }
  /**
  * Returns the full URL to the specified media.
  *
  * @param url The URL of the media.
  */
  _getValidUrl(t) {
    return t ? t.match(/^https?/) ? t : "https://" + t : null;
  }
}
class ad extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The media registry managing the media providers in the editor.
    */
    h(this, "registry");
    e.config.define("mediaEmbed", {
      elementName: "oembed",
      providers: [
        {
          name: "dailymotion",
          url: [
            /^dailymotion\.com\/video\/(\w+)/,
            /^dai.ly\/(\w+)/
          ],
          html: (n) => `<div style="position: relative; padding-bottom: 100%; height: 0; "><iframe src="https://www.dailymotion.com/embed/video/${n[1]}" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0;" frameborder="0" width="480" height="270" allowfullscreen allow="autoplay"></iframe></div>`
        },
        {
          name: "spotify",
          url: [
            /^open\.spotify\.com\/(artist\/\w+)/,
            /^open\.spotify\.com\/(album\/\w+)/,
            /^open\.spotify\.com\/(track\/\w+)/
          ],
          html: (n) => `<div style="position: relative; padding-bottom: 100%; height: 0; padding-bottom: 126%;"><iframe src="https://open.spotify.com/embed/${n[1]}" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0;" frameborder="0" allowtransparency="true" allow="encrypted-media"></iframe></div>`
        },
        {
          name: "youtube",
          url: [
            /^(?:m\.)?youtube\.com\/watch\?v=([\w-]+)(?:&t=(\d+))?/,
            /^(?:m\.)?youtube\.com\/shorts\/([\w-]+)(?:\?t=(\d+))?/,
            /^(?:m\.)?youtube\.com\/v\/([\w-]+)(?:\?t=(\d+))?/,
            /^youtube\.com\/embed\/([\w-]+)(?:\?start=(\d+))?/,
            /^youtu\.be\/([\w-]+)(?:\?t=(\d+))?/
          ],
          html: (n) => {
            const r = n[1], s = n[2];
            return `<div style="position: relative; padding-bottom: 100%; height: 0; padding-bottom: 56.2493%;"><iframe src="https://www.youtube.com/embed/${r}${s ? `?start=${s}` : ""}" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0;" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe></div>`;
          }
        },
        {
          name: "vimeo",
          url: [
            /^vimeo\.com\/(\d+)/,
            /^vimeo\.com\/[^/]+\/[^/]+\/video\/(\d+)/,
            /^vimeo\.com\/album\/[^/]+\/video\/(\d+)/,
            /^vimeo\.com\/channels\/[^/]+\/(\d+)/,
            /^vimeo\.com\/groups\/[^/]+\/videos\/(\d+)/,
            /^vimeo\.com\/ondemand\/[^/]+\/(\d+)/,
            /^player\.vimeo\.com\/video\/(\d+)/
          ],
          html: (n) => `<div style="position: relative; padding-bottom: 100%; height: 0; padding-bottom: 56.2493%;"><iframe src="https://player.vimeo.com/video/${n[1]}" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0;" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe></div>`
        },
        {
          name: "instagram",
          url: [
            /^instagram\.com\/p\/(\w+)/,
            /^instagram\.com\/reel\/(\w+)/
          ]
        },
        {
          name: "twitter",
          url: [
            /^twitter\.com/,
            /^x\.com/
          ]
        },
        {
          name: "googleMaps",
          url: [
            /^google\.com\/maps/,
            /^goo\.gl\/maps/,
            /^maps\.google\.com/,
            /^maps\.app\.goo\.gl/
          ]
        },
        {
          name: "flickr",
          url: /^flickr\.com/
        },
        {
          name: "facebook",
          url: /^facebook\.com/
        }
      ]
    }), this.registry = new Dj(e.locale, e.config.get("mediaEmbed"));
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MediaEmbedEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.schema, r = e.t, s = e.conversion, o = e.config.get("mediaEmbed.previewsInData"), a = e.config.get("mediaEmbed.elementName"), l = this.registry;
    e.commands.add("mediaEmbed", new Ij(e)), n.register("media", {
      inheritAllFrom: "$blockObject",
      allowAttributes: [
        "url"
      ]
    }), s.for("dataDowncast").elementToStructure({
      model: "media",
      view: (c, { writer: u }) => {
        const d = c.getAttribute("url");
        return h0(u, l, d, {
          elementName: a,
          renderMediaPreview: !!d && o
        });
      }
    }), s.for("dataDowncast").add(d0(l, {
      elementName: a,
      renderMediaPreview: o
    })), s.for("editingDowncast").elementToStructure({
      model: "media",
      view: (c, { writer: u }) => {
        const d = c.getAttribute("url"), f = h0(u, l, d, {
          elementName: a,
          renderForEditingView: !0
        });
        return Pj(f, u, r("media widget"));
      }
    }), s.for("editingDowncast").add(d0(l, {
      elementName: a,
      renderForEditingView: !0
    })), s.for("upcast").elementToElement({
      view: (c) => [
        "oembed",
        a
      ].includes(c.name) && c.getAttribute("url") ? {
        name: !0
      } : null,
      model: (c, { writer: u }) => {
        const d = c.getAttribute("url");
        return l.hasMedia(d) ? u.createElement("media", {
          url: d
        }) : null;
      }
    }).elementToElement({
      view: {
        name: "div",
        attributes: {
          "data-oembed-url": !0
        }
      },
      model: (c, { writer: u }) => {
        const d = c.getAttribute("data-oembed-url");
        return l.hasMedia(d) ? u.createElement("media", {
          url: d
        }) : null;
      }
    }).add((c) => {
      const u = (d, f, m) => {
        if (!m.consumable.consume(f.viewItem, {
          name: !0,
          classes: "media"
        }))
          return;
        const { modelRange: g, modelCursor: p } = m.convertChildren(f.viewItem, f.modelCursor);
        f.modelRange = g, f.modelCursor = p, Ft(g.getItems()) || m.consumable.revert(f.viewItem, {
          name: !0,
          classes: "media"
        });
      };
      c.on("element:figure", u);
    });
  }
}
const Lj = /^(?:http(s)?:\/\/)?[\w-]+\.[\w-.~:/?#[\]@!$&'()*+,;=%]+$/;
class Nj extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The paste–to–embed `setTimeout` ID. Stored as a property to allow
    * cleaning of the timeout.
    */
    h(this, "_timeoutId");
    /**
    * The position where the `<media>` element will be inserted after the timeout,
    * determined each time the new content is pasted into the document.
    */
    h(this, "_positionToInsert");
    this._timeoutId = null, this._positionToInsert = null;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Cp,
      Fr,
      Xd
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AutoMediaEmbed";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.document, r = e.plugins.get("ClipboardPipeline");
    this.listenTo(r, "inputTransformation", () => {
      const o = n.selection.getFirstRange(), a = ln.fromPosition(o.start);
      a.stickiness = "toPrevious";
      const l = ln.fromPosition(o.end);
      l.stickiness = "toNext", n.once("change:data", () => {
        this._embedMediaBetweenPositions(a, l), a.detach(), l.detach();
      }, {
        priority: "high"
      });
    }), e.commands.get("undo").on("execute", () => {
      this._timeoutId && (ae.window.clearTimeout(this._timeoutId), this._positionToInsert.detach(), this._timeoutId = null, this._positionToInsert = null);
    }, {
      priority: "high"
    });
  }
  /**
  * Analyzes the part of the document between provided positions in search for a URL representing media.
  * When the URL is found, it is automatically converted into media.
  *
  * @param leftPosition Left position of the selection.
  * @param rightPosition Right position of the selection.
  */
  _embedMediaBetweenPositions(e, n) {
    const r = this.editor, s = r.plugins.get(ad).registry, o = new Kn(e, n), a = o.getWalker({
      ignoreElementEnd: !0
    });
    let l = "";
    for (const u of a)
      u.item.is("$textProxy") && (l += u.item.data);
    if (l = l.trim(), !l.match(Lj)) {
      o.detach();
      return;
    }
    if (!s.hasMedia(l)) {
      o.detach();
      return;
    }
    if (!r.commands.get("mediaEmbed").isEnabled) {
      o.detach();
      return;
    }
    this._positionToInsert = ln.fromPosition(e), this._timeoutId = ae.window.setTimeout(() => {
      r.model.change((u) => {
        this._timeoutId = null, u.remove(o), o.detach();
        let d = null;
        this._positionToInsert.root.rootName !== "$graveyard" && (d = this._positionToInsert), Rx(r.model, l, d, !1), this._positionToInsert.detach(), this._positionToInsert = null;
      }), r.plugins.get(Fr).requestUndoOnBackspace();
    }, 100);
  }
}
class Bj extends de {
  /**
  * @param validators Form validators used by {@link #isValid}.
  * @param locale The localization services instance.
  */
  constructor(e, n) {
    super(n);
    /**
    * Tracks information about the DOM focus in the form.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * The URL input view.
    */
    h(this, "urlInputView");
    /**
    * An array of form validators used by {@link #isValid}.
    */
    h(this, "_validators");
    /**
    * The default info text for the {@link #urlInputView}.
    */
    h(this, "_urlInputViewInfoDefault");
    /**
    * The info text with an additional tip for the {@link #urlInputView},
    * displayed when the input has some value.
    */
    h(this, "_urlInputViewInfoTip");
    this.focusTracker = new wt(), this.keystrokes = new Ot(), this.set("mediaURLInputValue", ""), this.urlInputView = this._createUrlInput(), this._validators = e, this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-media-form",
          "ck-responsive-form"
        ],
        tabindex: "-1"
      },
      children: [
        this.urlInputView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Cs({
      view: this
    }), this.focusTracker.add(this.urlInputView.element), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the {@link #urlInputView}.
  */
  focus() {
    this.urlInputView.focus();
  }
  /**
  * The native DOM `value` of the {@link #urlInputView} element.
  *
  * **Note**: Do not confuse it with the {@link module:ui/inputtext/inputtextview~InputTextView#value}
  * which works one way only and may not represent the actual state of the component in the DOM.
  */
  get url() {
    return this.urlInputView.fieldView.element.value.trim();
  }
  set url(e) {
    this.urlInputView.fieldView.value = e.trim();
  }
  /**
  * Validates the form and returns `false` when some fields are invalid.
  */
  isValid() {
    this.resetFormStatus();
    for (const e of this._validators) {
      const n = e(this);
      if (n)
        return this.urlInputView.errorText = n, !1;
    }
    return !0;
  }
  /**
  * Cleans up the supplementary error and information text of the {@link #urlInputView}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetFormStatus() {
    this.urlInputView.errorText = null, this.urlInputView.infoText = this._urlInputViewInfoDefault;
  }
  /**
  * Creates a labeled input view.
  *
  * @returns Labeled input view instance.
  */
  _createUrlInput() {
    const e = this.locale.t, n = new Qt(this.locale, yi), r = n.fieldView;
    return this._urlInputViewInfoDefault = e("Paste the media URL in the input."), this._urlInputViewInfoTip = e("Tip: Paste the URL into the content to embed faster."), n.label = e("Media URL"), n.infoText = this._urlInputViewInfoDefault, r.inputMode = "url", r.on("input", () => {
      n.infoText = r.element.value ? this._urlInputViewInfoTip : this._urlInputViewInfoDefault, this.mediaURLInputValue = r.element.value.trim();
    }), n;
  }
}
var Fj = '<svg viewBox="0 0 22 20" xmlns="http://www.w3.org/2000/svg"><path d="M1.587 1.5c-.612 0-.601-.029-.601.551v14.84c0 .59-.01.559.591.559h18.846c.602 0 .591.03.591-.56V2.052c0-.58.01-.55-.591-.55H1.587Zm.701.971h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-14.24 1h13.008v12H4.467l.029-12Zm-2.208 1h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h1.003l-.029 1h-.974v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h.974v1h-.974v-1Zm16.448 0h1.003v1h-1.003v-1Z"/><path d="M8.374 6.648a.399.399 0 0 1 .395-.4.402.402 0 0 1 .2.049l5.148 2.824a.4.4 0 0 1 0 .7l-5.148 2.824a.403.403 0 0 1-.595-.35V6.648Z"/></svg>';
class $j extends V {
  constructor() {
    super(...arguments);
    h(this, "_formView");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ad,
      Ud
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MediaEmbedUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.ui.componentFactory.add("mediaEmbed", () => {
      const n = this.editor.locale.t, r = this._createDialogButton(Te);
      return r.tooltip = !0, r.label = n("Insert media"), r;
    }), e.ui.componentFactory.add("menuBar:mediaEmbed", () => {
      const n = this.editor.locale.t, r = this._createDialogButton(At);
      return r.label = n("Media"), r;
    });
  }
  /**
  * Creates a button for menu bar that will show media embed dialog.
  */
  _createDialogButton(e) {
    const n = this.editor, r = new e(n.locale), s = n.commands.get("mediaEmbed"), o = this.editor.plugins.get("Dialog");
    return r.icon = Fj, r.bind("isEnabled").to(s, "isEnabled"), r.on("execute", () => {
      o.id === "mediaEmbed" ? o.hide() : this._showDialog();
    }), r;
  }
  _showDialog() {
    const e = this.editor, n = e.plugins.get("Dialog"), r = e.commands.get("mediaEmbed"), s = e.locale.t;
    if (!this._formView) {
      const o = e.plugins.get(ad).registry;
      this._formView = new (bc(Bj))(zj(e.t, o), e.locale), this._formView.on("submit", () => this._handleSubmitForm());
    }
    n.show({
      id: "mediaEmbed",
      title: s("Insert media"),
      content: this._formView,
      isModal: !0,
      onShow: () => {
        this._formView.url = r.value || "", this._formView.resetFormStatus(), this._formView.urlInputView.fieldView.select();
      },
      actionButtons: [
        {
          label: s("Cancel"),
          withText: !0,
          onExecute: () => n.hide()
        },
        {
          label: s("Accept"),
          class: "ck-button-action",
          withText: !0,
          onExecute: () => this._handleSubmitForm()
        }
      ]
    });
  }
  _handleSubmitForm() {
    const e = this.editor, n = e.plugins.get("Dialog");
    this._formView.isValid() && (e.execute("mediaEmbed", this._formView.url), n.hide(), e.editing.view.focus());
  }
}
function zj(i, t) {
  return [
    (e) => {
      if (!e.url.length)
        return i("The URL must not be empty.");
    },
    (e) => {
      if (!t.hasMedia(e.url))
        return i("This media URL is not supported.");
    }
  ];
}
class Hj extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ad,
      $j,
      Nj,
      oo
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MediaEmbed";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
Ke.arrowup, Ke.arrowdown, Ke.esc;
Ke.enter, Ke.tab;
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function jj(i) {
  const t = parseFloat(i);
  return i.endsWith("pt") ? Do(t * 96 / 72) : i.endsWith("pc") ? Do(t * 12 * 96 / 72) : i.endsWith("in") ? Do(t * 96) : i.endsWith("cm") ? Do(t * 96 / 2.54) : i.endsWith("mm") ? Do(t / 10 * 96 / 2.54) : i;
}
function g0(i) {
  return i !== void 0 && i.endsWith("px");
}
function Do(i) {
  return i.toFixed(2).replace(/\.?0+$/, "") + "px";
}
function Wj(i, t, e) {
  if (!i.childCount)
    return;
  const n = new vr(i.document), r = qj(i, n);
  if (!r.length)
    return;
  const s = {}, o = [];
  for (const a of r)
    if (a.indent !== void 0) {
      Gj(a) || (o.length = 0);
      const l = `${a.id}:${a.indent}`, c = Math.min(a.indent - 1, o.length);
      if (c < o.length && o[c].id !== a.id && (o.length = c), c < o.length - 1)
        o.length = c + 1;
      else {
        const d = Kj(a, t);
        if (c > o.length - 1 || o[c].listElement.name != d.type) {
          c == 0 && d.type == "ol" && a.id !== void 0 && s[l] && (d.startIndex = s[l]);
          const f = Xj(d, n, e);
          if (g0(a.marginLeft) && (c == 0 || g0(o[c - 1].marginLeft))) {
            let m = a.marginLeft;
            c > 0 && (m = Do(parseFloat(m) - parseFloat(o[c - 1].marginLeft))), n.setStyle("padding-left", m, f);
          }
          if (o.length == 0) {
            const m = a.element.parent, g = m.getChildIndex(a.element) + 1;
            n.insertChild(g, f, m);
          } else {
            const m = o[c - 1].listItemElements;
            n.appendChild(f, m[m.length - 1]);
          }
          o[c] = {
            ...a,
            listElement: f,
            listItemElements: []
          }, c == 0 && a.id !== void 0 && (s[l] = d.startIndex || 1);
        }
      }
      const u = a.element.name == "li" ? a.element : n.createElement("li");
      n.appendChild(u, o[c].listElement), o[c].listItemElements.push(u), c == 0 && a.id !== void 0 && s[l]++, a.element != u && n.appendChild(a.element, u), eW(a.element, n), n.removeStyle("text-indent", a.element), n.removeStyle("margin-left", a.element);
    } else {
      const l = o.find((c) => c.marginLeft == a.marginLeft);
      if (l) {
        const c = l.listItemElements;
        n.appendChild(a.element, c[c.length - 1]), n.removeStyle("margin-left", a.element);
      } else
        o.length = 0;
    }
}
function Uj(i, t) {
  for (const e of t.createRangeIn(i)) {
    const n = e.item;
    if (n.is("element", "li")) {
      const r = n.getChild(0);
      r && r.is("element", "p") && t.unwrapElement(r);
    }
  }
}
function qj(i, t) {
  const e = t.createRangeIn(i), n = [], r = /* @__PURE__ */ new Set();
  for (const s of e.getItems()) {
    if (!s.is("element") || !s.name.match(/^(p|h\d+|li|div)$/))
      continue;
    let o = tW(s);
    if (o !== void 0 && parseFloat(o) == 0 && !Array.from(s.getClassNames()).find((a) => a.startsWith("MsoList")) && (o = void 0), s.hasStyle("mso-list") || o !== void 0 && r.has(o)) {
      const a = Qj(s);
      n.push({
        element: s,
        id: a.id,
        order: a.order,
        indent: a.indent,
        marginLeft: o
      }), o !== void 0 && r.add(o);
    } else
      r.clear();
  }
  return n;
}
function Gj(i) {
  const t = i.element.previousSibling;
  return p0(t || i.element.parent);
}
function p0(i) {
  return i.is("element", "ol") || i.is("element", "ul");
}
function Kj(i, t) {
  const e = new RegExp(`@list l${i.id}:level${i.indent}\\s*({[^}]*)`, "gi"), n = /mso-level-number-format:([^;]{0,100});/gi, r = /mso-level-start-at:\s{0,100}([0-9]{0,10})\s{0,100};/gi, s = new RegExp(`@list\\s+l${i.id}:level\\d\\s*{[^{]*mso-level-text:"%\\d\\\\.`, "gi"), o = new RegExp(`@list l${i.id}:level\\d\\s*{[^{]*mso-level-number-format:`, "gi"), a = s.exec(t), l = o.exec(t), c = a && !l, u = e.exec(t);
  let d = "decimal", f = "ol", m = null;
  if (u && u[1]) {
    const g = n.exec(u[1]);
    if (g && g[1] && (d = g[1].trim(), f = d !== "bullet" && d !== "image" ? "ol" : "ul"), d === "bullet") {
      const p = Zj(i.element);
      p && (d = p);
    } else {
      const p = r.exec(u[1]);
      p && p[1] && (m = parseInt(p[1]));
    }
    c && (f = "ol");
  }
  return {
    type: f,
    startIndex: m,
    style: Jj(d),
    isLegalStyleList: c
  };
}
function Zj(i) {
  if (i.name == "li" && i.parent.name == "ul" && i.parent.hasAttribute("type"))
    return i.parent.getAttribute("type");
  const t = Yj(i);
  if (!t)
    return null;
  const e = t._data;
  return e === "o" ? "circle" : e === "·" ? "disc" : e === "§" ? "square" : null;
}
function Yj(i) {
  if (i.getChild(0).is("$text"))
    return null;
  for (const t of i.getChildren()) {
    if (!t.is("element", "span"))
      continue;
    const e = t.getChild(0);
    if (e)
      return e.is("$text") ? e : e.getChild(0);
  }
  /* istanbul ignore next -- @preserve */
  return null;
}
function Jj(i) {
  if (i.startsWith("arabic-leading-zero"))
    return "decimal-leading-zero";
  switch (i) {
    case "alpha-upper":
      return "upper-alpha";
    case "alpha-lower":
      return "lower-alpha";
    case "roman-upper":
      return "upper-roman";
    case "roman-lower":
      return "lower-roman";
    case "circle":
    case "disc":
    case "square":
      return i;
    default:
      return null;
  }
}
function Xj(i, t, e) {
  const n = t.createElement(i.type);
  return i.style && t.setStyle("list-style-type", i.style, n), i.startIndex && i.startIndex > 1 && t.setAttribute("start", i.startIndex, n), i.isLegalStyleList && e && t.addClass("legal-list", n), n;
}
function Qj(i) {
  const t = i.getStyle("mso-list");
  if (t === void 0)
    return {};
  const e = t.match(/(^|\s{1,100})l(\d+)/i), n = t.match(/\s{0,100}lfo(\d+)/i), r = t.match(/\s{0,100}level(\d+)/i);
  return e && n && r ? {
    id: e[2],
    order: n[1],
    indent: parseInt(r[1])
  } : {
    indent: 1
    // Handle empty mso-list style as a marked for default list item.
  };
}
function eW(i, t) {
  const e = new xi({
    name: "span",
    styles: {
      "mso-list": "Ignore"
    }
  }), n = t.createRangeIn(i);
  for (const r of n)
    r.type === "elementStart" && e.match(r.item) && t.remove(r.item);
}
function tW(i) {
  const t = i.getStyle("margin-left");
  return t === void 0 || t.endsWith("px") ? t : jj(t);
}
function nW(i, t) {
  if (!i.childCount)
    return;
  const e = new vr(i.document), n = rW(i, e);
  sW(n, i, e), aW(n, i, e), oW(i, e);
  const r = lW(i, e);
  r.length && uW(r, cW(t), e);
}
function iW(i) {
  return btoa(i.match(/\w{2}/g).map((t) => String.fromCharCode(parseInt(t, 16))).join(""));
}
function rW(i, t) {
  const e = t.createRangeIn(i), n = new xi({
    name: /v:(.+)/
  }), r = [];
  for (const s of e) {
    if (s.type != "elementStart")
      continue;
    const o = s.item, a = o.previousSibling, l = a && a.is("element") ? a.name : null, c = [
      "Chart"
    ], u = n.match(o), d = o.getAttribute("o:gfxdata"), f = l === "v:shapetype", m = d && c.some((g) => o.getAttribute("id").includes(g));
    u && d && !f && !m && r.push(s.item.getAttribute("id"));
  }
  return r;
}
function sW(i, t, e) {
  const n = e.createRangeIn(t), r = new xi({
    name: "img"
  }), s = [];
  for (const o of n)
    if (o.item.is("element") && r.match(o.item)) {
      const a = o.item, l = a.getAttribute("v:shapes") ? a.getAttribute("v:shapes").split(" ") : [];
      l.length && l.every((c) => i.indexOf(c) > -1) ? s.push(a) : a.getAttribute("src") || s.push(a);
    }
  for (const o of s)
    e.remove(o);
}
function oW(i, t) {
  const e = t.createRangeIn(i), n = new xi({
    name: /v:(.+)/
  }), r = [];
  for (const s of e)
    s.type == "elementStart" && n.match(s.item) && r.push(s.item);
  for (const s of r)
    t.remove(s);
}
function aW(i, t, e) {
  const n = e.createRangeIn(t), r = [];
  for (const a of n)
    if (a.type == "elementStart" && a.item.is("element", "v:shape")) {
      const l = a.item.getAttribute("id");
      if (i.includes(l))
        continue;
      s(a.item.parent.getChildren(), l) || r.push(a.item);
    }
  for (const a of r) {
    const l = {
      src: o(a)
    };
    a.hasAttribute("alt") && (l.alt = a.getAttribute("alt"));
    const c = e.createElement("img", l);
    e.insertChild(a.index + 1, c, a.parent);
  }
  function s(a, l) {
    for (const c of a) {
      /* istanbul ignore else -- @preserve */
      if (c.is("element") && (c.name == "img" && c.getAttribute("v:shapes") == l || s(c.getChildren(), l)))
        return !0;
    }
    return !1;
  }
  function o(a) {
    for (const l of a.getChildren()) {
      /* istanbul ignore else -- @preserve */
      if (l.is("element") && l.getAttribute("src"))
        return l.getAttribute("src");
    }
  }
}
function lW(i, t) {
  const e = t.createRangeIn(i), n = new xi({
    name: "img"
  }), r = [];
  for (const s of e)
    s.item.is("element") && n.match(s.item) && s.item.getAttribute("src").startsWith("file://") && r.push(s.item);
  return r;
}
function cW(i) {
  if (!i)
    return [];
  const t = /{\\pict[\s\S]+?\\bliptag-?\d+(\\blipupi-?\d+)?({\\\*\\blipuid\s?[\da-fA-F]+)?[\s}]*?/, e = new RegExp("(?:(" + t.source + "))([\\da-fA-F\\s]+)\\}", "g"), n = i.match(e), r = [];
  if (n)
    for (const s of n) {
      let o = !1;
      s.includes("\\pngblip") ? o = "image/png" : s.includes("\\jpegblip") && (o = "image/jpeg"), o && r.push({
        hex: s.replace(t, "").replace(/[^\da-fA-F]/g, ""),
        type: o
      });
    }
  return r;
}
function uW(i, t, e) {
  if (i.length === t.length)
    for (let n = 0; n < i.length; n++) {
      const r = `data:${t[n].type};base64,${iW(t[n].hex)}`;
      e.setAttribute("src", r, i[n]);
    }
}
function dW(i) {
  const t = [], e = new vr(i.document);
  for (const { item: n } of e.createRangeIn(i))
    if (n.is("element")) {
      for (const r of n.getClassNames())
        /\bmso/gi.exec(r) && e.removeClass(r, n);
      for (const r of n.getStyleNames())
        /\bmso/gi.exec(r) && e.removeStyle(r, n);
      (n.is("element", "w:sdt") || n.is("element", "w:sdtpr") && n.isEmpty || n.is("element", "o:p") && n.isEmpty) && t.push(n);
    }
  for (const n of t) {
    const r = n.parent, s = r.getChildIndex(n);
    e.insertChild(s, n.getChildren(), r), e.remove(n);
  }
}
const hW = /<meta\s*name="?generator"?\s*content="?microsoft\s*word\s*\d+"?\/?>/i, fW = /xmlns:o="urn:schemas-microsoft-com/i;
class mW {
  /**
  * Creates a new `MSWordNormalizer` instance.
  *
  * @param document View document.
  */
  constructor(t, e = !1) {
    h(this, "document");
    h(this, "hasMultiLevelListPlugin");
    this.document = t, this.hasMultiLevelListPlugin = e;
  }
  /**
  * @inheritDoc
  */
  isActive(t) {
    return hW.test(t) || fW.test(t);
  }
  /**
  * @inheritDoc
  */
  execute(t) {
    const { body: e, stylesString: n } = t._parsedData;
    Wj(e, n, this.hasMultiLevelListPlugin), nW(e, t.dataTransfer.getData("text/rtf")), dW(e), t.content = e;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function gW(i, t) {
  for (const e of i.getChildren())
    if (e.is("element", "b") && e.getStyle("font-weight") === "normal") {
      const n = i.getChildIndex(e);
      t.remove(e), t.insertChild(n, e.getChildren(), i);
    }
}
function pW(i, t) {
  const e = new Fd(t.document.stylesProcessor), n = new zd(e, {
    renderingMode: "data"
  }), r = n.blockElements, s = n.inlineObjectElements, o = [];
  for (const a of t.createRangeIn(i)) {
    const l = a.item;
    if (l.is("element", "br")) {
      const c = b0(l, "forward", t, {
        blockElements: r,
        inlineObjectElements: s
      }), u = b0(l, "backward", t, {
        blockElements: r,
        inlineObjectElements: s
      }), d = v0(c, r);
      (v0(u, r) || d) && o.push(l);
    }
  }
  for (const a of o)
    a.hasClass("Apple-interchange-newline") ? t.remove(a) : t.replace(a, t.createElement("p"));
}
function b0(i, t, e, { blockElements: n, inlineObjectElements: r }) {
  let s = e.createPositionAt(i, t == "forward" ? "after" : "before");
  return s = s.getLastMatchingPosition(({ item: o }) => o.is("element") && !n.includes(o.name) && !r.includes(o.name), {
    direction: t
  }), t == "forward" ? s.nodeAfter : s.nodeBefore;
}
function v0(i, t) {
  return !!i && i.is("element") && t.includes(i.name);
}
const bW = /id=("|')docs-internal-guid-[-0-9a-f]+("|')/i;
class vW {
  /**
  * Creates a new `GoogleDocsNormalizer` instance.
  *
  * @param document View document.
  */
  constructor(t) {
    h(this, "document");
    this.document = t;
  }
  /**
  * @inheritDoc
  */
  isActive(t) {
    return bW.test(t);
  }
  /**
  * @inheritDoc
  */
  execute(t) {
    const e = new vr(this.document), { body: n } = t._parsedData;
    gW(n, e), Uj(n, e), pW(n, e), t.content = n;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function wW(i, t) {
  for (const e of i.getChildren())
    e.is("element", "table") && e.hasAttribute("xmlns") && t.removeAttribute("xmlns", e);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function yW(i, t) {
  for (const e of i.getChildren())
    if (e.is("element", "google-sheets-html-origin")) {
      const n = i.getChildIndex(e);
      t.remove(e), t.insertChild(n, e.getChildren(), i);
    }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function _W(i, t) {
  for (const e of i.getChildren())
    e.is("element", "table") && e.getStyle("width") === "0px" && t.removeStyle("width", e);
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function CW(i, t) {
  for (const e of Array.from(i.getChildren()))
    e.is("element", "style") && t.remove(e);
}
const kW = /<google-sheets-html-origin/i;
class xW {
  /**
  * Creates a new `GoogleSheetsNormalizer` instance.
  *
  * @param document View document.
  */
  constructor(t) {
    h(this, "document");
    this.document = t;
  }
  /**
  * @inheritDoc
  */
  isActive(t) {
    return kW.test(t);
  }
  /**
  * @inheritDoc
  */
  execute(t) {
    const e = new vr(this.document), { body: n } = t._parsedData;
    yW(n, e), wW(n, e), _W(n, e), CW(n, e), t.content = n;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function EW(i) {
  return w0(w0(i)).replace(/(<span\s+style=['"]mso-spacerun:yes['"]>[^\S\r\n]*?)[\r\n]+([^\S\r\n]*<\/span>)/g, "$1$2").replace(/<span\s+style=['"]mso-spacerun:yes['"]><\/span>/g, "").replace(/(<span\s+style=['"]letter-spacing:[^'"]+?['"]>)[\r\n]+(<\/span>)/g, "$1 $2").replace(/ <\//g, " </").replace(/ <o:p><\/o:p>/g, " <o:p></o:p>").replace(/<o:p>(&nbsp;|\u00A0)<\/o:p>/g, "").replace(/>([^\S\r\n]*[\r\n]\s*)</g, "><");
}
function TW(i) {
  i.querySelectorAll("span[style*=spacerun]").forEach((t) => {
    const e = t, n = e.innerText.length || 0;
    e.innerText = Array(n + 1).join("  ").substr(0, n);
  });
}
function w0(i) {
  return i.replace(/<span(?: class="Apple-converted-space"|)>(\s+)<\/span>/g, (t, e) => e.length === 1 ? " " : Array(e.length + 1).join("  ").substr(0, e.length));
}
function SW(i, t) {
  const e = new DOMParser();
  i = i.replace(/<!--\[if gte vml 1]>/g, ""), i = i.replace(/<o:SmartTagType(?:\s+[^\s>=]+(?:="[^"]*")?)*\s*\/?>/gi, "");
  const n = EW(IW(i)), r = e.parseFromString(n, "text/html");
  TW(r);
  const s = r.body.innerHTML, o = AW(r, t), a = PW(r);
  return {
    body: o,
    bodyString: s,
    styles: a.styles,
    stylesString: a.stylesString
  };
}
function AW(i, t) {
  const e = new Fd(t), n = new zd(e, {
    renderingMode: "data"
  }), r = i.createDocumentFragment(), s = i.body.childNodes;
  for (; s.length > 0; )
    r.appendChild(s[0]);
  return n.domToView(r, {
    skipComments: !0
  });
}
function PW(i) {
  const t = [], e = [], n = Array.from(i.getElementsByTagName("style"));
  for (const r of n)
    r.sheet && r.sheet.cssRules && r.sheet.cssRules.length && (t.push(r.sheet), e.push(r.innerHTML));
  return {
    styles: t,
    stylesString: e.join(" ")
  };
}
function IW(i) {
  const t = "</body>", e = "</html>", n = i.indexOf(t);
  if (n < 0)
    return i;
  const r = i.indexOf(e, n + t.length);
  return i.substring(0, n + t.length) + (r >= 0 ? i.substring(r) : "");
}
class RW extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PasteFromOffice";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ui
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.plugins.get("ClipboardPipeline"), n = t.editing.view.document, r = [], s = this.editor.plugins.has("MultiLevelList");
    r.push(new mW(n, s)), r.push(new vW(n)), r.push(new xW(n)), e.on("inputTransformation", (o, a) => {
      if (a._isTransformedWithPasteFromOffice || t.model.document.selection.getFirstPosition().parent.is("element", "codeBlock"))
        return;
      const c = a.dataTransfer.getData("text/html"), u = r.find((d) => d.isActive(c));
      u && (a._parsedData || (a._parsedData = SW(c, n.stylesProcessor)), u.execute(a), a._isTransformedWithPasteFromOffice = !0);
    }, {
      priority: "high"
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
var OW = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M8.69 14.915c.053.052.173.083.36.093a.366.366 0 0 1 .345.485l-.003.01a.738.738 0 0 1-.697.497h-2.67a.374.374 0 0 1-.353-.496l.013-.038a.681.681 0 0 1 .644-.458c.197-.012.325-.043.386-.093a.28.28 0 0 0 .072-.11L9.592 4.5H6.269c-.359-.017-.609.013-.75.09-.142.078-.289.265-.442.563-.192.29-.516.464-.864.464H4.17a.43.43 0 0 1-.407-.569L4.46 3h13.08l-.62 2.043a.81.81 0 0 1-.775.574h-.114a.486.486 0 0 1-.486-.486c.001-.284-.054-.464-.167-.54-.112-.076-.367-.106-.766-.091h-3.28l-2.68 10.257c-.006.074.007.127.038.158zM3 17h8a.5.5 0 1 1 0 1H3a.5.5 0 1 1 0-1zm11.299 1.17a.75.75 0 1 1-1.06-1.06l1.414-1.415-1.415-1.414a.75.75 0 0 1 1.06-1.06l1.415 1.414 1.414-1.415a.75.75 0 1 1 1.06 1.06l-1.413 1.415 1.414 1.415a.75.75 0 0 1-1.06 1.06l-1.415-1.414-1.414 1.414z"/></svg>';
const Jc = "removeFormat";
class VW extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "RemoveFormatUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add(Jc, () => {
      const e = this._createButton(Te);
      return e.set({
        tooltip: !0
      }), e;
    }), t.ui.componentFactory.add(`menuBar:${Jc}`, () => this._createButton(At));
  }
  /**
  * Creates a button for remove format command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, n = e.locale, r = e.commands.get(Jc), s = new t(e.locale), o = n.t;
    return s.set({
      label: o("Remove Format"),
      icon: OW
    }), s.bind("isEnabled").to(r, "isEnabled"), this.listenTo(s, "execute", () => {
      e.execute(Jc), e.editing.view.focus();
    }), s;
  }
}
class MW extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model;
    this.isEnabled = !!Ft(this._getFormattingItems(t.document.selection, t.schema));
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = t.schema;
    t.change((n) => {
      for (const r of this._getFormattingItems(t.document.selection, e))
        if (r.is("selection"))
          for (const s of this._getFormattingAttributes(r, e))
            n.removeSelectionAttribute(s);
        else {
          const s = n.createRangeOn(r);
          for (const o of this._getFormattingAttributes(r, e))
            n.removeAttribute(o, s);
        }
    });
  }
  /**
  * Returns an iterable of items in a selection (including the selection itself) that have formatting model
  * attributes to be removed by the feature.
  *
  * @param schema The schema describing the item.
  */
  *_getFormattingItems(t, e) {
    const n = (r) => !!Ft(this._getFormattingAttributes(r, e));
    for (const r of t.getRanges())
      for (const s of r.getItems())
        !e.isBlock(s) && n(s) && (yield s);
    for (const r of t.getSelectedBlocks())
      n(r) && (yield r);
    n(t) && (yield t);
  }
  /**
  * Returns an iterable of formatting attributes of a given model item.
  *
  * **Note:** Formatting items have the `isFormatting` property set to `true`.
  *
  * @param schema The schema describing the item.
  * @returns The names of formatting attributes found in a given item.
  */
  *_getFormattingAttributes(t, e) {
    for (const [n] of t.getAttributes()) {
      const r = e.getAttributeProperties(n);
      r && r.isFormatting && (yield n);
    }
  }
}
class DW extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "RemoveFormatEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.commands.add("removeFormat", new MW(t));
  }
}
class LW extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      DW,
      VW
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "RemoveFormat";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class NW extends Ve {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.affectsData = !1, this.value = !1;
  }
  /**
  * Toggles the visibility of content blocks.
  */
  execute() {
    const t = "ck-show-blocks", e = this.editor.editing.view;
    e.change((n) => {
      for (const r of e.document.roots)
        r.hasClass(t) ? (n.removeClass(t, r), this.value = !1) : (n.addClass(t, r), this.value = !0);
    });
  }
}
class BW extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ShowBlocksEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const { editor: t } = this;
    t.commands.add("showBlocks", new NW(t));
  }
}
var FW = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m6.395 9.196 2.545-.007V6.498a.598.598 0 0 1 .598-.598h.299a.598.598 0 0 1 .598.598v6.877a.598.598 0 0 1-.598.598h-.299a.598.598 0 0 1-.598-.598v-2.691l-2.545.007v2.691a.598.598 0 0 1-.598.598h-.299a.598.598 0 0 1-.598-.598V6.505a.598.598 0 0 1 .598-.598h.299a.598.598 0 0 1 .598.598v2.691Z"/><path d="M15.094 13.417V6.462a.562.562 0 0 0-.562-.562h-.782a1 1 0 0 0-.39.08l-1.017.43a.562.562 0 0 0-.343.517v.197c0 .4.406.67.775.519l.819-.337v6.111c0 .31.251.562.561.562h.377c.31 0 .562-.251.562-.562Z"/><path d="M0 15.417v1.5h1.5v-1.5H0Z"/><path d="M18.5 15.417v1.5H20v-1.5h-1.5Z"/><path d="M18.5 12.333v1.5H20v-1.5h-1.5Z"/><path d="M18.5 9.25v1.5H20v-1.5h-1.5Z"/><path d="M18.5 6.167v1.5H20v-1.5h-1.5Z"/><path d="M0 18.5v.5a1 1 0 0 0 1 1h.5v-1.5H0Z"/><path d="M3.083 18.5V20h1.5v-1.5h-1.5Z"/><path d="M6.167 18.5V20h1.5v-1.5h-1.5Z"/><path d="M9.25 18.5V20h1.5v-1.5h-1.5Z"/><path d="M12.333 18.5V20h1.5v-1.5h-1.5Z"/><path d="M15.417 18.5V20h1.5v-1.5h-1.5Z"/><path d="M18.5 18.5V20h.5a1 1 0 0 0 1-1v-.5h-1.5Z"/><path clip-rule="evenodd" d="M0 1a1 1 0 0 1 1-1h18a1 1 0 0 1 1 1v3.583h-1.5V1.5h-17v12.333H0V1Z"/></svg>';
class $W extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ShowBlocksUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("showBlocks", () => {
      const e = this._createButton(Te);
      return e.set({
        tooltip: !0,
        icon: FW
      }), e;
    }), t.ui.componentFactory.add("menuBar:showBlocks", () => this._createButton(At));
  }
  /**
  * Creates a button for show blocks command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, n = e.locale, r = e.commands.get("showBlocks"), s = new t(n), o = n.t;
    return s.set({
      label: o("Show blocks"),
      isToggleable: !0,
      role: "menuitemcheckbox"
    }), s.bind("isEnabled").to(r), s.bind("isOn").to(r, "value", r, "isEnabled", (a, l) => a && l), this.listenTo(s, "execute", () => {
      e.execute("showBlocks"), e.editing.view.focus();
    }), s;
  }
}
class zW extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ShowBlocks";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      BW,
      $W
    ];
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function HW(i) {
  const t = [
    {
      name: "address",
      isVoid: !1
    },
    {
      name: "article",
      isVoid: !1
    },
    {
      name: "aside",
      isVoid: !1
    },
    {
      name: "blockquote",
      isVoid: !1
    },
    {
      name: "details",
      isVoid: !1
    },
    {
      name: "dialog",
      isVoid: !1
    },
    {
      name: "dd",
      isVoid: !1
    },
    {
      name: "div",
      isVoid: !1
    },
    {
      name: "dl",
      isVoid: !1
    },
    {
      name: "dt",
      isVoid: !1
    },
    {
      name: "fieldset",
      isVoid: !1
    },
    {
      name: "figcaption",
      isVoid: !1
    },
    {
      name: "figure",
      isVoid: !1
    },
    {
      name: "footer",
      isVoid: !1
    },
    {
      name: "form",
      isVoid: !1
    },
    {
      name: "h1",
      isVoid: !1
    },
    {
      name: "h2",
      isVoid: !1
    },
    {
      name: "h3",
      isVoid: !1
    },
    {
      name: "h4",
      isVoid: !1
    },
    {
      name: "h5",
      isVoid: !1
    },
    {
      name: "h6",
      isVoid: !1
    },
    {
      name: "header",
      isVoid: !1
    },
    {
      name: "hgroup",
      isVoid: !1
    },
    {
      name: "hr",
      isVoid: !0
    },
    {
      name: "li",
      isVoid: !1
    },
    {
      name: "main",
      isVoid: !1
    },
    {
      name: "nav",
      isVoid: !1
    },
    {
      name: "ol",
      isVoid: !1
    },
    {
      name: "p",
      isVoid: !1
    },
    {
      name: "section",
      isVoid: !1
    },
    {
      name: "table",
      isVoid: !1
    },
    {
      name: "tbody",
      isVoid: !1
    },
    {
      name: "td",
      isVoid: !1
    },
    {
      name: "th",
      isVoid: !1
    },
    {
      name: "thead",
      isVoid: !1
    },
    {
      name: "tr",
      isVoid: !1
    },
    {
      name: "ul",
      isVoid: !1
    }
  ], e = t.map((o) => o.name).join("|"), n = i.replace(new RegExp(`</?(${e})( .*?)?>`, "g"), `
$&
`).replace(/<br[^>]*>/g, `$&
`).split(`
`);
  let r = 0, s = !1;
  return n.filter((o) => o.length).map((o) => (s = UW(o, s), jW(o, t) ? Ef(o, r++) : WW(o, t) ? Ef(o, --r) : s === "middle" || s === "last" ? o : Ef(o, r))).join(`
`);
}
function jW(i, t) {
  return t.some((e) => !(e.isVoid || !new RegExp(`<${e.name}( .*?)?>`).test(i)));
}
function WW(i, t) {
  return t.some((e) => new RegExp(`</${e.name}>`).test(i));
}
function Ef(i, t, e = "    ") {
  return `${e.repeat(Math.max(0, t))}${i}`;
}
function UW(i, t) {
  return new RegExp("<pre( .*?)?>").test(i) ? "first" : new RegExp("</pre>").test(i) ? "last" : t === "first" || t === "middle" ? "middle" : !1;
}
var qW = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m12.5 0 5 4.5v15.003h-16V0h11zM3 1.5v3.25l-1.497 1-.003 8 1.5 1v3.254L7.685 18l-.001 1.504H17.5V8.002L16 9.428l-.004-4.22-4.222-3.692L3 1.5z"/><path d="M4.06 6.64a.75.75 0 0 1 .958 1.15l-.085.07L2.29 9.75l2.646 1.89c.302.216.4.62.232.951l-.058.095a.75.75 0 0 1-.951.232l-.095-.058-3.5-2.5V9.14l3.496-2.5zm4.194 6.22a.75.75 0 0 1-.958-1.149l.085-.07 2.643-1.89-2.646-1.89a.75.75 0 0 1-.232-.952l.058-.095a.75.75 0 0 1 .95-.232l.096.058 3.5 2.5v1.22l-3.496 2.5zm7.644-.836 2.122 2.122-5.825 5.809-2.125-.005.003-2.116zm2.539-1.847 1.414 1.414a.5.5 0 0 1 0 .707l-1.06 1.06-2.122-2.12 1.061-1.061a.5.5 0 0 1 .707 0z"/></svg>';
const Xc = "SourceEditingMode";
class GW extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The element replacer instance used to replace the editing roots with the wrapper elements containing the document source.
    */
    h(this, "_elementReplacer");
    /**
    * Maps all root names to wrapper elements containing the document source.
    */
    h(this, "_replacedRoots");
    /**
    * Maps all root names to their document data.
    */
    h(this, "_dataFromRoots");
    this.set("isSourceEditingMode", !1), this._elementReplacer = new g2(), this._replacedRoots = /* @__PURE__ */ new Map(), this._dataFromRoots = /* @__PURE__ */ new Map(), e.config.define("sourceEditing.allowCollaborationFeatures", !1);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SourceEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ea
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    this._checkCompatibility();
    const e = this.editor, n = e.locale.t;
    e.ui.componentFactory.add("sourceEditing", () => {
      const r = this._createButton(Te);
      return r.set({
        label: n("Source"),
        icon: qW,
        tooltip: !0,
        class: "ck-source-editing-button"
      }), r;
    }), e.ui.componentFactory.add("menuBar:sourceEditing", () => {
      const r = this._createButton(At);
      return r.set({
        label: n("Show source"),
        role: "menuitemcheckbox"
      }), r;
    }), this._isAllowedToHandleSourceEditingMode() && (this.on("change:isSourceEditingMode", (r, s, o) => {
      o ? (this._hideVisibleDialog(), this._showSourceEditing(), this._disableCommands()) : (this._hideSourceEditing(), this._enableCommands());
    }), this.on("change:isEnabled", (r, s, o) => this._handleReadOnlyMode(!o)), this.listenTo(e, "change:isReadOnly", (r, s, o) => this._handleReadOnlyMode(o))), e.data.on("get", () => {
      this.isSourceEditingMode && this.updateEditorData();
    }, {
      priority: "high"
    });
  }
  /**
  * Updates the source data in all hidden editing roots.
  */
  updateEditorData() {
    const e = this.editor, n = {};
    for (const [r, s] of this._replacedRoots) {
      const o = this._dataFromRoots.get(r), a = s.dataset.value;
      o !== a && (n[r] = a, this._dataFromRoots.set(r, a));
    }
    Object.keys(n).length && e.data.set(n, {
      batchType: {
        isUndoable: !0
      },
      suppressErrorInCollaboration: !0
    });
  }
  _checkCompatibility() {
    const e = this.editor, n = e.config.get("sourceEditing.allowCollaborationFeatures");
    if (!n && e.plugins.has("RealTimeCollaborativeEditing"))
      throw new T("source-editing-incompatible-with-real-time-collaboration", null);
    !n && [
      "CommentsEditing",
      "TrackChangesEditing",
      "RevisionHistory"
    ].some((s) => e.plugins.has(s)) && console.warn("You initialized the editor with the source editing feature and at least one of the collaboration features. Please be advised that the source editing feature may not work, and be careful when editing document source that contains markers created by the collaboration features."), e.plugins.has("RestrictedEditingModeEditing") && console.warn("You initialized the editor with the source editing feature and restricted editing feature. Please be advised that the source editing feature may not work, and be careful when editing document source that contains markers created by the restricted editing feature.");
  }
  /**
  * Creates source editing wrappers that replace each editing root. Each wrapper contains the document source from the corresponding
  * root.
  *
  * The wrapper element contains a textarea and it solves the problem, that the textarea element cannot auto expand its height based on
  * the content it contains. The solution is to make the textarea more like a plain div element, which expands in height as much as it
  * needs to, in order to display the whole document source without scrolling. The wrapper element is a parent for the textarea and for
  * the pseudo-element `::after`, that replicates the look, content, and position of the textarea. The pseudo-element replica is hidden,
  * but it is styled to be an identical visual copy of the textarea with the same content. Then, the wrapper is a grid container and both
  * of its children (the textarea and the `::after` pseudo-element) are positioned within a CSS grid to occupy the same grid cell. The
  * content in the pseudo-element `::after` is set in CSS and it stretches the grid to the appropriate size based on the textarea value.
  * Since both children occupy the same grid cell, both have always the same height.
  */
  _showSourceEditing() {
    const e = this.editor, n = e.editing.view, r = e.model;
    r.change((s) => {
      s.setSelection(null), s.removeSelectionAttribute(r.document.selection.getAttributeKeys());
    });
    for (const [s, o] of n.domRoots) {
      const a = KW(e.data.get({
        rootName: s
      })), l = xn(o.ownerDocument, "textarea", {
        rows: "1",
        "aria-label": "Source code editing area"
      }), c = xn(o.ownerDocument, "div", {
        class: "ck-source-editing-area",
        "data-value": a
      }, [
        l
      ]);
      l.value = a, l.setSelectionRange(0, 0), l.addEventListener("input", () => {
        c.dataset.value = l.value, e.ui.update();
      }), n.change((u) => {
        const d = n.document.getRoot(s);
        u.addClass("ck-hidden", d);
      }), e.ui.setEditableElement("sourceEditing:" + s, l), this._replacedRoots.set(s, c), this._elementReplacer.replace(o, c), this._dataFromRoots.set(s, a);
    }
    this._focusSourceEditing();
  }
  /**
  * Restores all hidden editing roots and sets the source data in them.
  */
  _hideSourceEditing() {
    const n = this.editor.editing.view;
    this.updateEditorData(), n.change((r) => {
      for (const [s] of this._replacedRoots)
        r.removeClass("ck-hidden", n.document.getRoot(s));
    }), this._elementReplacer.restore(), this._replacedRoots.clear(), this._dataFromRoots.clear(), n.focus();
  }
  /**
  * Focuses the textarea containing document source from the first editing root.
  */
  _focusSourceEditing() {
    const e = this.editor, [n] = this._replacedRoots.values(), r = n.querySelector("textarea");
    e.editing.view.document.isFocused = !1, r.focus();
  }
  /**
  * Disables all commands.
  */
  _disableCommands() {
    const e = this.editor;
    for (const n of e.commands.commands())
      n.forceDisabled(Xc);
    e.plugins.has("CommentsArchiveUI") && e.plugins.get("CommentsArchiveUI").forceDisabled(Xc);
  }
  /**
  * Clears forced disable for all commands, that was previously set through {@link #_disableCommands}.
  */
  _enableCommands() {
    const e = this.editor;
    for (const n of e.commands.commands())
      n.clearForceDisabled(Xc);
    e.plugins.has("CommentsArchiveUI") && e.plugins.get("CommentsArchiveUI").clearForceDisabled(Xc);
  }
  /**
  * Adds or removes the `readonly` attribute from the textarea from all roots, if document source mode is active.
  *
  * @param isReadOnly Indicates whether all textarea elements should be read-only.
  */
  _handleReadOnlyMode(e) {
    if (this.isSourceEditingMode)
      for (const [, n] of this._replacedRoots)
        n.querySelector("textarea").readOnly = e;
  }
  /**
  * Checks, if the plugin is allowed to handle the source editing mode by itself. Currently, the source editing mode is supported only
  * for the {@link module:editor-classic/classiceditor~ClassicEditor classic editor}.
  */
  _isAllowedToHandleSourceEditingMode() {
    const n = this.editor.ui.view.editable;
    return n && !n.hasExternalElement;
  }
  /**
  * If any {@link module:ui/dialog/dialogview~DialogView editor dialog} is currently visible, hide it.
  */
  _hideVisibleDialog() {
    if (this.editor.plugins.has("Dialog")) {
      const e = this.editor.plugins.get("Dialog");
      e.isOpen && e.hide();
    }
  }
  _createButton(e) {
    const n = this.editor, r = new e(n.locale);
    return r.set({
      withText: !0,
      isToggleable: !0
    }), r.bind("isOn").to(this, "isSourceEditingMode"), r.bind("isEnabled").to(this, "isEnabled", n, "isReadOnly", n.plugins.get(ea), "hasAny", (s, o, a) => !(!s || o || a)), this.listenTo(r, "execute", () => {
      this.isSourceEditingMode = !this.isSourceEditingMode;
    }), r;
  }
}
function KW(i) {
  return ZW(i) ? HW(i) : i;
}
function ZW(i) {
  return i.startsWith("<");
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function $p(i, t) {
  const { modelAttribute: e, styleName: n, viewElement: r, defaultValue: s, reduceBoxSides: o = !1, shouldUpcast: a = () => !0 } = t;
  i.for("upcast").attributeToAttribute({
    view: {
      name: r,
      styles: {
        [n]: /[\s\S]+/
      }
    },
    model: {
      key: e,
      value: (l) => {
        if (!a(l))
          return;
        const c = l.getNormalizedStyle(n), u = o ? Au(c) : c;
        if (s !== u)
          return u;
      }
    }
  });
}
function Rm(i, t, e, n) {
  i.for("upcast").add((r) => r.on("element:" + t, (s, o, a) => {
    if (!o.modelRange)
      return;
    const l = [
      "border-top-width",
      "border-top-color",
      "border-top-style",
      "border-bottom-width",
      "border-bottom-color",
      "border-bottom-style",
      "border-right-width",
      "border-right-color",
      "border-right-style",
      "border-left-width",
      "border-left-color",
      "border-left-style"
    ].filter((m) => o.viewItem.hasStyle(m));
    if (!l.length)
      return;
    const c = {
      styles: l
    };
    if (!a.consumable.test(o.viewItem, c))
      return;
    const u = [
      ...o.modelRange.getItems({
        shallow: !0
      })
    ].pop();
    a.consumable.consume(o.viewItem, c);
    const d = {
      style: o.viewItem.getNormalizedStyle("border-style"),
      color: o.viewItem.getNormalizedStyle("border-color"),
      width: o.viewItem.getNormalizedStyle("border-width")
    }, f = {
      style: Au(d.style),
      color: Au(d.color),
      width: Au(d.width)
    };
    f.style !== n.style && a.writer.setAttribute(e.style, f.style, u), f.color !== n.color && a.writer.setAttribute(e.color, f.color, u), f.width !== n.width && a.writer.setAttribute(e.width, f.width, u);
  }));
}
function pl(i, t) {
  const { modelElement: e, modelAttribute: n, styleName: r } = t;
  i.for("downcast").attributeToAttribute({
    model: {
      name: e,
      key: n
    },
    view: (s) => ({
      key: "style",
      value: {
        [r]: s
      }
    })
  });
}
function Su(i, t) {
  const { modelAttribute: e, styleName: n } = t;
  i.for("downcast").add((r) => r.on(`attribute:${e}:table`, (s, o, a) => {
    const { item: l, attributeNewValue: c } = o, { mapper: u, writer: d } = a;
    if (!a.consumable.consume(o.item, s.name))
      return;
    const f = [
      ...u.toViewElement(l).getChildren()
    ].find((m) => m.is("element", "table"));
    c ? d.setStyle(n, c, f) : d.removeStyle(n, f);
  }));
}
function Au(i) {
  if (!i)
    return;
  const t = [
    "top",
    "right",
    "bottom",
    "left"
  ];
  if (!t.every((s) => i[s]))
    return i;
  const n = i.top;
  return t.every((s) => i[s] === n) ? n : i;
}
function qt(i, t, e, n, r = 1) {
  t != null && r !== void 0 && r !== null && t > r ? n.setAttribute(i, t, e) : n.removeAttribute(i, e);
}
function lo(i, t, e = {}) {
  const n = i.createElement("tableCell", e);
  return i.insertElement("paragraph", n), i.insert(n, t), n;
}
function Om(i, t) {
  const e = t.parent.parent, n = parseInt(e.getAttribute("headingColumns") || "0"), { column: r } = i.getCellLocation(t);
  return !!n && r < n;
}
function Pu(i, t, e) {
  const { modelAttribute: n } = e;
  i.extend("tableCell", {
    allowAttributes: [
      n
    ]
  }), $p(t, {
    viewElement: /^(td|th)$/,
    ...e
  }), pl(t, {
    modelElement: "tableCell",
    ...e
  });
}
function Fs(i) {
  const t = i.getSelectedElement();
  return t && t.is("element", "table") ? t : i.getFirstPosition().findAncestor("table");
}
function YW() {
  return (i) => {
    i.on("element:figure", (t, e, n) => {
      if (!n.consumable.test(e.viewItem, {
        name: !0,
        classes: "table"
      }))
        return;
      const r = QW(e.viewItem);
      if (!r || !n.consumable.test(r, {
        name: !0
      }))
        return;
      n.consumable.consume(e.viewItem, {
        name: !0,
        classes: "table"
      });
      const s = n.convertItem(r, e.modelCursor), o = Ft(s.modelRange.getItems());
      if (!o) {
        n.consumable.revert(e.viewItem, {
          name: !0,
          classes: "table"
        });
        return;
      }
      n.convertChildren(e.viewItem, n.writer.createPositionAt(o, "end")), n.updateConversionResult(o, e);
    });
  };
}
function JW() {
  return (i) => {
    i.on("element:table", (t, e, n) => {
      const r = e.viewItem;
      if (!n.consumable.test(r, {
        name: !0
      }))
        return;
      const { rows: s, headingRows: o, headingColumns: a } = eU(r), l = {};
      a && (l.headingColumns = a), o && (l.headingRows = o);
      const c = n.writer.createElement("table", l);
      if (n.safeInsert(c, e.modelCursor)) {
        if (n.consumable.consume(r, {
          name: !0
        }), s.forEach((u) => n.convertItem(u, n.writer.createPositionAt(c, "end"))), n.convertChildren(r, n.writer.createPositionAt(c, "end")), c.isEmpty) {
          const u = n.writer.createElement("tableRow");
          n.writer.insert(u, n.writer.createPositionAt(c, "end")), lo(n.writer, n.writer.createPositionAt(u, "end"));
        }
        n.updateConversionResult(c, e);
      }
    });
  };
}
function XW() {
  return (i) => {
    i.on("element:tr", (t, e) => {
      e.viewItem.isEmpty && e.modelCursor.index == 0 && t.stop();
    }, {
      priority: "high"
    });
  };
}
function y0(i) {
  return (t) => {
    t.on(`element:${i}`, (e, n, { writer: r }) => {
      if (!n.modelRange)
        return;
      const s = n.modelRange.start.nodeAfter, o = r.createPositionAt(s, 0);
      if (n.viewItem.isEmpty) {
        r.insertElement("paragraph", o);
        return;
      }
      const a = Array.from(s.getChildren());
      if (a.every((l) => l.is("element", "$marker"))) {
        const l = r.createElement("paragraph");
        r.insert(l, r.createPositionAt(s, 0));
        for (const c of a)
          r.move(r.createRangeOn(c), r.createPositionAt(l, "end"));
      }
    }, {
      priority: "low"
    });
  };
}
function QW(i) {
  for (const t of i.getChildren())
    if (t.is("element", "table"))
      return t;
}
function eU(i) {
  let t = 0, e;
  const n = [], r = [];
  let s;
  for (const o of Array.from(i.getChildren())) {
    if (o.name !== "tbody" && o.name !== "thead" && o.name !== "tfoot")
      continue;
    o.name === "thead" && !s && (s = o);
    const a = Array.from(o.getChildren()).filter((l) => l.is("element", "tr"));
    for (const l of a)
      if (s && o === s || o.name === "tbody" && Array.from(l.getChildren()).length && Array.from(l.getChildren()).every((c) => c.is("element", "th")))
        t++, n.push(l);
      else {
        r.push(l);
        const c = tU(l);
        (!e || c < e) && (e = c);
      }
  }
  return {
    headingRows: t,
    headingColumns: e || 0,
    rows: [
      ...n,
      ...r
    ]
  };
}
function tU(i) {
  let t = 0, e = 0;
  const n = Array.from(i.getChildren()).filter((r) => r.name === "th" || r.name === "td");
  for (; e < n.length && n[e].name === "th"; ) {
    const r = n[e], s = parseInt(r.getAttribute("colspan") || "1");
    t = t + s, e++;
  }
  return t;
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
class ut {
  /**
  * Creates an instance of the table walker.
  *
  * The table walker iterates internally by traversing the table from row index = 0 and column index = 0.
  * It walks row by row and column by column in order to output values defined in the constructor.
  * By default it will output only the locations that are occupied by a cell. To include also spanned rows and columns,
  * pass the `includeAllSlots` option to the constructor.
  *
  * The most important values of the iterator are column and row indexes of a cell.
  *
  * See {@link module:table/tablewalker~TableSlot} what values are returned by the table walker.
  *
  * To iterate over a given row:
  *
  * ```ts
  * const tableWalker = new TableWalker( table, { startRow: 1, endRow: 2 } );
  *
  * for ( const tableSlot of tableWalker ) {
  *   console.log( 'A cell at row', tableSlot.row, 'and column', tableSlot.column );
  * }
  * ```
  *
  * For instance the code above for the following table:
  *
  *  +----+----+----+----+----+----+
  *  | 00      | 02 | 03 | 04 | 05 |
  *  |         +----+----+----+----+
  *  |         | 12      | 14 | 15 |
  *  |         +----+----+----+    +
  *  |         | 22           |    |
  *  |----+----+----+----+----+    +
  *  | 30 | 31 | 32 | 33 | 34 |    |
  *  +----+----+----+----+----+----+
  *
  * will log in the console:
  *
  *  'A cell at row 1 and column 2'
  *  'A cell at row 1 and column 4'
  *  'A cell at row 1 and column 5'
  *  'A cell at row 2 and column 2'
  *
  * To also iterate over spanned cells:
  *
  * ```ts
  * const tableWalker = new TableWalker( table, { row: 1, includeAllSlots: true } );
  *
  * for ( const tableSlot of tableWalker ) {
  *   console.log( 'Slot at', tableSlot.row, 'x', tableSlot.column, ':', tableSlot.isAnchor ? 'is anchored' : 'is spanned' );
  * }
  * ```
  *
  * will log in the console for the table from the previous example:
  *
  *  'Cell at 1 x 0 : is spanned'
  *  'Cell at 1 x 1 : is spanned'
  *  'Cell at 1 x 2 : is anchored'
  *  'Cell at 1 x 3 : is spanned'
  *  'Cell at 1 x 4 : is anchored'
  *  'Cell at 1 x 5 : is anchored'
  *
  * **Note**: Option `row` is a shortcut that sets both `startRow` and `endRow` to the same row.
  * (Use either `row` or `startRow` and `endRow` but never together). Similarly the `column` option sets both `startColumn`
  * and `endColumn` to the same column (Use either `column` or `startColumn` and `endColumn` but never together).
  *
  * @param table A table over which the walker iterates.
  * @param options An object with configuration.
  * @param options.row A row index for which this iterator will output cells. Can't be used together with `startRow` and `endRow`.
  * @param options.startRow A row index from which this iterator should start. Can't be used together with `row`. Default value is 0.
  * @param options.endRow A row index at which this iterator should end. Can't be used together with `row`.
  * @param options.column A column index for which this iterator will output cells.
  * Can't be used together with `startColumn` and `endColumn`.
  * @param options.startColumn A column index from which this iterator should start.
  * Can't be used together with `column`. Default value is 0.
  * @param options.endColumn A column index at which this iterator should end. Can't be used together with `column`.
  * @param options.includeAllSlots Also return values for spanned cells. Default value is "false".
  */
  constructor(t, e = {}) {
    /**
    * The walker's table element.
    *
    * @internal
    */
    h(this, "_table");
    /**
    * A row index from which this iterator will start.
    */
    h(this, "_startRow");
    /**
    * A row index at which this iterator will end.
    */
    h(this, "_endRow");
    /**
    * If set, the table walker will only output cells from a given column and following ones or cells that overlap them.
    */
    h(this, "_startColumn");
    /**
    * If set, the table walker will only output cells up to a given column.
    */
    h(this, "_endColumn");
    /**
    * Enables output of spanned cells that are normally not yielded.
    */
    h(this, "_includeAllSlots");
    /**
    * Row indexes to skip from the iteration.
    */
    h(this, "_skipRows");
    /**
    * The current row index.
    *
    * @internal
    */
    h(this, "_row");
    /**
    * The index of the current row element in the table.
    *
    * @internal
    */
    h(this, "_rowIndex");
    /**
    * The current column index.
    *
    * @internal
    */
    h(this, "_column");
    /**
    * The cell index in a parent row. For spanned cells when {@link #_includeAllSlots} is set to `true`,
    * this represents the index of the next table cell.
    *
    * @internal
    */
    h(this, "_cellIndex");
    /**
    * Holds a map of spanned cells in a table.
    */
    h(this, "_spannedCells");
    /**
    * Index of the next column where a cell is anchored.
    */
    h(this, "_nextCellAtColumn");
    /**
    * Indicates whether the iterator jumped to (or close to) the start row, ignoring rows that don't need to be traversed.
    */
    h(this, "_jumpedToStartRow", !1);
    this._table = t, this._startRow = e.row !== void 0 ? e.row : e.startRow || 0, this._endRow = e.row !== void 0 ? e.row : e.endRow, this._startColumn = e.column !== void 0 ? e.column : e.startColumn || 0, this._endColumn = e.column !== void 0 ? e.column : e.endColumn, this._includeAllSlots = !!e.includeAllSlots, this._skipRows = /* @__PURE__ */ new Set(), this._row = 0, this._rowIndex = 0, this._column = 0, this._cellIndex = 0, this._spannedCells = /* @__PURE__ */ new Map(), this._nextCellAtColumn = -1;
  }
  /**
  * Iterable interface.
  */
  [Symbol.iterator]() {
    return this;
  }
  /**
  * Gets the next table walker's value.
  *
  * @returns The next table walker's value.
  */
  next() {
    this._canJumpToStartRow() && this._jumpToNonSpannedRowClosestToStartRow();
    const t = this._table.getChild(this._rowIndex);
    if (!t || this._isOverEndRow())
      return {
        done: !0,
        value: void 0
      };
    if (!t.is("element", "tableRow"))
      return this._rowIndex++, this.next();
    if (this._isOverEndColumn())
      return this._advanceToNextRow();
    let e = null;
    const n = this._getSpanned();
    if (n)
      this._includeAllSlots && !this._shouldSkipSlot() && (e = this._formatOutValue(n.cell, n.row, n.column));
    else {
      const r = t.getChild(this._cellIndex);
      if (!r)
        return this._advanceToNextRow();
      const s = parseInt(r.getAttribute("colspan") || "1"), o = parseInt(r.getAttribute("rowspan") || "1");
      (s > 1 || o > 1) && this._recordSpans(r, o, s), this._shouldSkipSlot() || (e = this._formatOutValue(r)), this._nextCellAtColumn = this._column + s;
    }
    return this._column++, this._column == this._nextCellAtColumn && this._cellIndex++, e || this.next();
  }
  /**
  * Marks a row to skip in the next iteration. It will also skip cells from the current row if there are any cells from the current row
  * to output.
  *
  * @param row The row index to skip.
  */
  skipRow(t) {
    this._skipRows.add(t);
  }
  /**
  * Advances internal cursor to the next row.
  */
  _advanceToNextRow() {
    return this._row++, this._rowIndex++, this._column = 0, this._cellIndex = 0, this._nextCellAtColumn = -1, this.next();
  }
  /**
  * Checks if the current row is over {@link #_endRow}.
  */
  _isOverEndRow() {
    return this._endRow !== void 0 && this._row > this._endRow;
  }
  /**
  * Checks if the current cell is over {@link #_endColumn}
  */
  _isOverEndColumn() {
    return this._endColumn !== void 0 && this._column > this._endColumn;
  }
  /**
  * A common method for formatting the iterator's output value.
  *
  * @param cell The table cell to output.
  * @param anchorRow The row index of a cell anchor slot.
  * @param anchorColumn The column index of a cell anchor slot.
  */
  _formatOutValue(t, e = this._row, n = this._column) {
    return {
      done: !1,
      value: new nU(this, t, e, n)
    };
  }
  /**
  * Checks if the current slot should be skipped.
  */
  _shouldSkipSlot() {
    const t = this._skipRows.has(this._row), e = this._row < this._startRow, n = this._column < this._startColumn, r = this._endColumn !== void 0 && this._column > this._endColumn;
    return t || e || n || r;
  }
  /**
  * Returns the cell element that is spanned over the current cell location.
  */
  _getSpanned() {
    const t = this._spannedCells.get(this._row);
    return t && t.get(this._column) || null;
  }
  /**
  * Updates spanned cells map relative to the current cell location and its span dimensions.
  *
  * @param cell A cell that is spanned.
  * @param rowspan Cell height.
  * @param colspan Cell width.
  */
  _recordSpans(t, e, n) {
    const r = {
      cell: t,
      row: this._row,
      column: this._column
    };
    for (let s = this._row; s < this._row + e; s++)
      for (let o = this._column; o < this._column + n; o++)
        (s != this._row || o != this._column) && this._markSpannedCell(s, o, r);
  }
  /**
  * Marks the cell location as spanned by another cell.
  *
  * @param row The row index of the cell location.
  * @param column The column index of the cell location.
  * @param data A spanned cell details (cell element, anchor row and column).
  */
  _markSpannedCell(t, e, n) {
    this._spannedCells.has(t) || this._spannedCells.set(t, /* @__PURE__ */ new Map()), this._spannedCells.get(t).set(e, n);
  }
  /**
  * Checks if part of the table can be skipped.
  */
  _canJumpToStartRow() {
    return !!this._startRow && this._startRow > 0 && !this._jumpedToStartRow;
  }
  /**
  * Sets the current row to `this._startRow` or the first row before it that has the number of cells
  * equal to the number of columns in the table.
  *
  * Example:
  * 	+----+----+----+
  *  | 00 | 01 | 02 |
  *  |----+----+----+
  *  | 10      | 12 |
  *  |         +----+
  *  |         | 22 |
  *  |         +----+
  *  |         | 32 | <--- Start row
  *  +----+----+----+
  *  | 40 | 41 | 42 |
  *  +----+----+----+
  *
  * If the 4th row is a `this._startRow`, this method will:
  * 1.) Count the number of columns this table has based on the first row (3 columns in this case).
  * 2.) Check if the 4th row contains 3 cells. It doesn't, so go to the row before it.
  * 3.) Check if the 3rd row contains 3 cells. It doesn't, so go to the row before it.
  * 4.) Check if the 2nd row contains 3 cells. It does, so set the current row to that row.
  *
  * Setting the current row this way is necessary to let the `next()`  method loop over the cells
  * spanning multiple rows or columns and update the `this._spannedCells` property.
  */
  _jumpToNonSpannedRowClosestToStartRow() {
    const t = this._getRowLength(0);
    for (let e = this._startRow; !this._jumpedToStartRow; e--)
      t === this._getRowLength(e) && (this._row = e, this._rowIndex = e, this._jumpedToStartRow = !0);
  }
  /**
  * Returns a number of columns in a row taking `colspan` into consideration.
  */
  _getRowLength(t) {
    return [
      ...this._table.getChild(t).getChildren()
    ].reduce((n, r) => n + parseInt(r.getAttribute("colspan") || "1"), 0);
  }
}
class nU {
  /**
  * Creates an instance of the table walker value.
  *
  * @param tableWalker The table walker instance.
  * @param cell The current table cell.
  * @param anchorRow The row index of a cell anchor slot.
  * @param anchorColumn The column index of a cell anchor slot.
  */
  constructor(t, e, n, r) {
    /**
    * The current table cell.
    */
    h(this, "cell");
    /**
    * The row index of a table slot.
    */
    h(this, "row");
    /**
    * The column index of a table slot.
    */
    h(this, "column");
    /**
    * The row index of a cell anchor slot.
    */
    h(this, "cellAnchorRow");
    /**
    * The column index of a cell anchor slot.
    */
    h(this, "cellAnchorColumn");
    /**
    * The index of the current cell in the parent row.
    */
    h(this, "_cellIndex");
    /**
    * The index of the current row element in the table.
    */
    h(this, "_rowIndex");
    /**
    * The table element.
    */
    h(this, "_table");
    this.cell = e, this.row = t._row, this.column = t._column, this.cellAnchorRow = n, this.cellAnchorColumn = r, this._cellIndex = t._cellIndex, this._rowIndex = t._rowIndex, this._table = t._table;
  }
  // @if CK_DEBUG // public get isSpanned(): unknown { return throwMissingGetterError( 'isSpanned' ); }
  // @if CK_DEBUG // public get colspan(): unknown { return throwMissingGetterError( 'colspan' ); }
  // @if CK_DEBUG // public get rowspan(): unknown { return throwMissingGetterError( 'rowspan' ); }
  // @if CK_DEBUG // public get cellIndex(): unknown { return throwMissingGetterError( 'cellIndex' ); }
  /**
  * Whether the cell is anchored in the current slot.
  */
  get isAnchor() {
    return this.row === this.cellAnchorRow && this.column === this.cellAnchorColumn;
  }
  /**
  * The width of a cell defined by a `colspan` attribute. If the model attribute is not present, it is set to `1`.
  */
  get cellWidth() {
    return parseInt(this.cell.getAttribute("colspan") || "1");
  }
  /**
  * The height of a cell defined by a `rowspan` attribute. If the model attribute is not present, it is set to `1`.
  */
  get cellHeight() {
    return parseInt(this.cell.getAttribute("rowspan") || "1");
  }
  /**
  * The index of the current row element in the table.
  */
  get rowIndex() {
    return this._rowIndex;
  }
  /**
  * Returns the {@link module:engine/model/position~Position} before the table slot.
  */
  getPositionBefore() {
    return this._table.root.document.model.createPositionAt(this._table.getChild(this.row), this._cellIndex);
  }
}
function _0(i, t) {
  return (e, { writer: n }) => {
    const r = e.getAttribute("headingRows") || 0, s = n.createContainerElement("table", null, []), o = n.createContainerElement("figure", {
      class: "table"
    }, s);
    r > 0 && n.insert(n.createPositionAt(s, "end"), n.createContainerElement("thead", null, n.createSlot((a) => a.is("element", "tableRow") && a.index < r))), r < i.getRows(e) && n.insert(n.createPositionAt(s, "end"), n.createContainerElement("tbody", null, n.createSlot((a) => a.is("element", "tableRow") && a.index >= r)));
    for (const { positionOffset: a, filter: l } of t.additionalSlots)
      n.insert(n.createPositionAt(s, a), n.createSlot(l));
    return n.insert(n.createPositionAt(s, "after"), n.createSlot((a) => a.is("element", "tableRow") ? !1 : !t.additionalSlots.some(({ filter: l }) => l(a)))), t.asWidget ? rU(o, n) : o;
  };
}
function iU() {
  return (i, { writer: t }) => i.isEmpty ? t.createEmptyElement("tr") : t.createContainerElement("tr");
}
function C0(i = {}) {
  return (t, { writer: e }) => {
    const n = t.parent, r = n.parent, s = r.getChildIndex(n), o = new ut(r, {
      row: s
    }), a = r.getAttribute("headingRows") || 0, l = r.getAttribute("headingColumns") || 0;
    let c = null;
    for (const u of o)
      if (u.cell == t) {
        const f = u.row < a || u.column < l ? "th" : "td";
        c = i.asWidget ? wp(e.createEditableElement(f), e) : e.createContainerElement(f);
        break;
      }
    return c;
  };
}
function k0(i = {}) {
  return (t, { writer: e }) => {
    if (!t.parent.is("element", "tableCell") || !Ox(t))
      return null;
    if (i.asWidget)
      return e.createContainerElement("span", {
        class: "ck-table-bogus-paragraph"
      });
    {
      const n = e.createContainerElement("p");
      return e.setCustomProperty("dataPipeline:transparentRendering", !0, n), n;
    }
  };
}
function Ox(i) {
  return i.parent.childCount == 1 && !sU(i);
}
function rU(i, t) {
  return t.setCustomProperty("table", !0, i), va(i, t, {
    hasSelectionHandle: !0
  });
}
function sU(i) {
  return !i.getAttributeKeys().next().done;
}
class oU extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document.selection, n = t.schema;
    this.isEnabled = aU(e, n);
  }
  /**
  * Executes the command.
  *
  * Inserts a table with the given number of rows and columns into the editor.
  *
  * @param options.rows The number of rows to create in the inserted table. Default value is 2.
  * @param options.columns The number of columns to create in the inserted table. Default value is 2.
  * @param options.headingRows The number of heading rows. If not provided it will default to
  * {@link module:table/tableconfig~TableConfig#defaultHeadings `config.table.defaultHeadings.rows`} table config.
  * @param options.headingColumns The number of heading columns. If not provided it will default to
  * {@link module:table/tableconfig~TableConfig#defaultHeadings `config.table.defaultHeadings.columns`} table config.
  * @fires execute
  */
  execute(t = {}) {
    const e = this.editor, n = e.model, r = e.plugins.get("TableUtils"), s = e.config.get("table.defaultHeadings.rows"), o = e.config.get("table.defaultHeadings.columns");
    t.headingRows === void 0 && s && (t.headingRows = s), t.headingColumns === void 0 && o && (t.headingColumns = o), n.change((a) => {
      const l = r.createTable(a, t);
      n.insertObject(l, null, null, {
        findOptimalPosition: "auto"
      }), a.setSelection(a.createPositionAt(l.getNodeByPath([
        0,
        0,
        0
      ]), 0));
    });
  }
}
function aU(i, t) {
  const e = i.getFirstPosition().parent, n = e === e.root ? e : e.parent;
  return t.checkChild(n, "table");
}
class x0 extends Ve {
  /**
  * Creates a new `InsertRowCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param options.order The order of insertion relative to the row in which the caret is located.
  * Possible values: `"above"` and `"below"`. Default value is "below"
  */
  constructor(e, n = {}) {
    super(e);
    /**
    * The order of insertion relative to the row in which the caret is located.
    */
    h(this, "order");
    this.order = n.order || "below";
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model.document.selection, r = !!this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(e).length;
    this.isEnabled = r;
  }
  /**
  * Executes the command.
  *
  * Depending on the command's {@link #order} value, it inserts a row `'below'` or `'above'` the row in which selection is set.
  *
  * @fires execute
  */
  execute() {
    const e = this.editor, n = e.model.document.selection, r = e.plugins.get("TableUtils"), s = this.order === "above", o = r.getSelectionAffectedTableCells(n), a = r.getRowIndexes(o), l = s ? a.first : a.last, c = o[0].findAncestor("table");
    r.insertRows(c, {
      at: s ? l : l + 1,
      copyStructureFromAbove: !s
    });
  }
}
class E0 extends Ve {
  /**
  * Creates a new `InsertColumnCommand` instance.
  *
  * @param editor An editor on which this command will be used.
  * @param options.order The order of insertion relative to the column in which the caret is located.
  * Possible values: `"left"` and `"right"`. Default value is "right".
  */
  constructor(e, n = {}) {
    super(e);
    /**
    * The order of insertion relative to the column in which the caret is located.
    */
    h(this, "order");
    this.order = n.order || "right";
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model.document.selection, r = !!this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(e).length;
    this.isEnabled = r;
  }
  /**
  * Executes the command.
  *
  * Depending on the command's {@link #order} value, it inserts a column to the `'left'` or `'right'` of the column
  * in which the selection is set.
  *
  * @fires execute
  */
  execute() {
    const e = this.editor, n = e.model.document.selection, r = e.plugins.get("TableUtils"), s = this.order === "left", o = r.getSelectionAffectedTableCells(n), a = r.getColumnIndexes(o), l = s ? a.first : a.last, c = o[0].findAncestor("table");
    r.insertColumns(c, {
      columns: 1,
      at: s ? l : l + 1
    });
  }
}
class T0 extends Ve {
  /**
  * Creates a new `SplitCellCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param options.direction Indicates whether the command should split cells `'horizontally'` or `'vertically'`.
  */
  constructor(e, n = {}) {
    super(e);
    /**
    * The direction that indicates which cell will be split.
    */
    h(this, "direction");
    this.direction = n.direction || "horizontally";
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const n = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(this.editor.model.document.selection);
    this.isEnabled = n.length === 1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const e = this.editor.plugins.get("TableUtils"), n = e.getSelectionAffectedTableCells(this.editor.model.document.selection)[0];
    this.direction === "horizontally" ? e.splitCellHorizontally(n, 2) : e.splitCellVertically(n, 2);
  }
}
function Vx(i, t, e) {
  const { startRow: n, startColumn: r, endRow: s, endColumn: o } = t, a = e.createElement("table"), l = s - n + 1;
  for (let u = 0; u < l; u++)
    e.insertElement("tableRow", a, "end");
  const c = [
    ...new ut(i, {
      startRow: n,
      endRow: s,
      startColumn: r,
      endColumn: o,
      includeAllSlots: !0
    })
  ];
  for (const { row: u, column: d, cell: f, isAnchor: m, cellAnchorRow: g, cellAnchorColumn: p } of c) {
    const b = u - n, v = a.getChild(b);
    if (!m)
      (g < n || p < r) && lo(e, e.createPositionAt(v, "end"));
    else {
      const y = e.cloneElement(f);
      e.append(y, v), Bx(y, u, d, s, o, e);
    }
  }
  return lU(a, i, n, r, e), a;
}
function Mx(i, t, e = 0) {
  const n = [], r = new ut(i, {
    startRow: e,
    endRow: t - 1
  });
  for (const s of r) {
    const { row: o, cellHeight: a } = s, l = o + a - 1;
    o < t && t <= l && n.push(s);
  }
  return n;
}
function Dx(i, t, e) {
  const n = i.parent, r = n.parent, s = n.index, o = parseInt(i.getAttribute("rowspan")), a = t - s, l = {}, c = o - a;
  c > 1 && (l.rowspan = c);
  const u = parseInt(i.getAttribute("colspan") || "1");
  u > 1 && (l.colspan = u);
  const d = s, f = d + a, m = [
    ...new ut(r, {
      startRow: d,
      endRow: f,
      includeAllSlots: !0
    })
  ];
  let g = null, p;
  for (const b of m) {
    const { row: v, column: y, cell: x } = b;
    x === i && p === void 0 && (p = y), p !== void 0 && p === y && v === f && (g = lo(e, b.getPositionBefore(), l));
  }
  return qt("rowspan", a, i, e), g;
}
function Lx(i, t) {
  const e = [], n = new ut(i);
  for (const r of n) {
    const { column: s, cellWidth: o } = r, a = s + o - 1;
    s < t && t <= a && e.push(r);
  }
  return e;
}
function Nx(i, t, e, n) {
  const r = parseInt(i.getAttribute("colspan")), s = e - t, o = {}, a = r - s;
  a > 1 && (o.colspan = a);
  const l = parseInt(i.getAttribute("rowspan") || "1");
  l > 1 && (o.rowspan = l);
  const c = lo(n, n.createPositionAfter(i), o);
  return qt("colspan", s, i, n), c;
}
function Bx(i, t, e, n, r, s) {
  const o = parseInt(i.getAttribute("colspan") || "1"), a = parseInt(i.getAttribute("rowspan") || "1");
  if (e + o - 1 > r) {
    const u = r - e + 1;
    qt("colspan", u, i, s, 1);
  }
  if (t + a - 1 > n) {
    const u = n - t + 1;
    qt("rowspan", u, i, s, 1);
  }
}
function lU(i, t, e, n, r) {
  const s = parseInt(t.getAttribute("headingRows") || "0");
  if (s > 0) {
    const a = s - e;
    qt("headingRows", a, i, r, 0);
  }
  const o = parseInt(t.getAttribute("headingColumns") || "0");
  if (o > 0) {
    const a = o - n;
    qt("headingColumns", a, i, r, 0);
  }
}
function Vm(i, t) {
  const e = t.getColumns(i), n = new Array(e).fill(0);
  for (const { column: s } of new ut(i))
    n[s]++;
  const r = n.reduce((s, o, a) => o ? s : [
    ...s,
    a
  ], []);
  if (r.length > 0) {
    const s = r[r.length - 1];
    return t.removeColumns(i, {
      at: s
    }), !0;
  }
  return !1;
}
function Mm(i, t) {
  const e = [], n = t.getRows(i);
  for (let r = 0; r < n; r++)
    i.getChild(r).isEmpty && e.push(r);
  if (e.length > 0) {
    const r = e[e.length - 1];
    return t.removeRows(i, {
      at: r
    }), !0;
  }
  return !1;
}
function zp(i, t) {
  Vm(i, t) || Mm(i, t);
}
function Fx(i, t) {
  const e = Array.from(new ut(i, {
    startColumn: t.firstColumn,
    endColumn: t.lastColumn,
    row: t.lastRow
  }));
  if (e.every(({ cellHeight: s }) => s === 1))
    return t.lastRow;
  const r = e[0].cellHeight - 1;
  return t.lastRow + r;
}
function $x(i, t) {
  const e = Array.from(new ut(i, {
    startRow: t.firstRow,
    endRow: t.lastRow,
    column: t.lastColumn
  }));
  if (e.every(({ cellWidth: s }) => s === 1))
    return t.lastColumn;
  const r = e[0].cellWidth - 1;
  return t.lastColumn + r;
}
class Qc extends Ve {
  /**
  * Creates a new `MergeCellCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param options.direction Indicates which cell to merge with the currently selected one.
  * Possible values are: `'left'`, `'right'`, `'up'` and `'down'`.
  */
  constructor(e, n) {
    super(e);
    /**
    * The direction that indicates which cell will be merged with the currently selected one.
    */
    h(this, "direction");
    /**
    * Whether the merge is horizontal (left/right) or vertical (up/down).
    */
    h(this, "isHorizontal");
    this.direction = n.direction, this.isHorizontal = this.direction == "right" || this.direction == "left";
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this._getMergeableCell();
    this.value = e, this.isEnabled = !!e;
  }
  /**
  * Executes the command.
  *
  * Depending on the command's {@link #direction} value, it will merge the cell that is to the `'left'`, `'right'`, `'up'` or `'down'`.
  *
  * @fires execute
  */
  execute() {
    const e = this.editor.model, n = e.document, s = this.editor.plugins.get("TableUtils").getTableCellsContainingSelection(n.selection)[0], o = this.value, a = this.direction;
    e.change((l) => {
      const c = a == "right" || a == "down", u = c ? s : o, d = c ? o : s, f = d.parent;
      dU(d, u, l);
      const m = this.isHorizontal ? "colspan" : "rowspan", g = parseInt(s.getAttribute(m) || "1"), p = parseInt(o.getAttribute(m) || "1");
      l.setAttribute(m, g + p, u), l.setSelection(l.createRangeIn(u));
      const b = this.editor.plugins.get("TableUtils"), v = f.findAncestor("table");
      zp(v, b);
    });
  }
  /**
  * Returns a cell that can be merged with the current cell depending on the command's direction.
  */
  _getMergeableCell() {
    const n = this.editor.model.document, r = this.editor.plugins.get("TableUtils"), s = r.getTableCellsContainingSelection(n.selection)[0];
    if (!s)
      return;
    const o = this.isHorizontal ? cU(s, this.direction, r) : uU(s, this.direction, r);
    if (!o)
      return;
    const a = this.isHorizontal ? "rowspan" : "colspan", l = parseInt(s.getAttribute(a) || "1");
    if (parseInt(o.getAttribute(a) || "1") === l)
      return o;
  }
}
function cU(i, t, e) {
  const r = i.parent.parent, s = t == "right" ? i.nextSibling : i.previousSibling, o = (r.getAttribute("headingColumns") || 0) > 0;
  if (!s)
    return;
  const a = t == "right" ? i : s, l = t == "right" ? s : i, { column: c } = e.getCellLocation(a), { column: u } = e.getCellLocation(l), d = parseInt(a.getAttribute("colspan") || "1"), f = Om(e, a), m = Om(e, l);
  return o && f != m ? void 0 : c + d === u ? s : void 0;
}
function uU(i, t, e) {
  const n = i.parent, r = n.parent, s = r.getChildIndex(n);
  if (t == "down" && s === e.getRows(r) - 1 || t == "up" && s === 0)
    return null;
  const o = parseInt(i.getAttribute("rowspan") || "1"), a = r.getAttribute("headingRows") || 0, l = t == "down" && s + o === a, c = t == "up" && s === a;
  if (a && (l || c))
    return null;
  const u = parseInt(i.getAttribute("rowspan") || "1"), d = t == "down" ? s + u : s, f = [
    ...new ut(r, {
      endRow: d
    })
  ], g = f.find((b) => b.cell === i).column, p = f.find(({ row: b, cellHeight: v, column: y }) => y !== g ? !1 : t == "down" ? b === d : d === b + v);
  return p && p.cell ? p.cell : null;
}
function dU(i, t, e) {
  S0(i) || (S0(t) && e.remove(e.createRangeIn(t)), e.move(e.createRangeIn(i), e.createPositionAt(t, "end"))), e.remove(i);
}
function S0(i) {
  const t = i.getChild(0);
  return i.childCount == 1 && t.is("element", "paragraph") && t.isEmpty;
}
class hU extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get("TableUtils"), e = t.getSelectionAffectedTableCells(this.editor.model.document.selection), n = e[0];
    if (n) {
      const r = n.findAncestor("table"), o = t.getRows(r) - 1, a = t.getRowIndexes(e), l = a.first === 0 && a.last === o;
      this.isEnabled = !l;
    } else
      this.isEnabled = !1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = this.editor.plugins.get("TableUtils"), n = e.getSelectionAffectedTableCells(t.document.selection), r = e.getRowIndexes(n), s = n[0], o = s.findAncestor("table"), a = e.getCellLocation(s).column;
    t.change((l) => {
      const c = r.last - r.first + 1;
      e.removeRows(o, {
        at: r.first,
        rows: c
      });
      const u = fU(o, r.first, a, e.getRows(o));
      l.setSelection(l.createPositionAt(u, 0));
    });
  }
}
function fU(i, t, e, n) {
  const r = i.getChild(Math.min(t, n - 1));
  let s = r.getChild(0), o = 0;
  for (const a of r.getChildren()) {
    if (o > e)
      return s;
    s = a, o += parseInt(a.getAttribute("colspan") || "1");
  }
  return s;
}
class mU extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get("TableUtils"), e = t.getSelectionAffectedTableCells(this.editor.model.document.selection), n = e[0];
    if (n) {
      const r = n.findAncestor("table"), s = t.getColumns(r), { first: o, last: a } = t.getColumnIndexes(e);
      this.isEnabled = a - o < s - 1;
    } else
      this.isEnabled = !1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.plugins.get("TableUtils"), [e, n] = pU(this.editor.model.document.selection, t), r = e.parent.parent, s = [
      ...new ut(r)
    ], o = {
      first: s.find((l) => l.cell === e).column,
      last: s.find((l) => l.cell === n).column
    }, a = gU(s, e, n, o);
    this.editor.model.change((l) => {
      const c = o.last - o.first + 1;
      t.removeColumns(r, {
        at: o.first,
        columns: c
      }), l.setSelection(l.createPositionAt(a, 0));
    });
  }
}
function gU(i, t, e, n) {
  return parseInt(e.getAttribute("colspan") || "1") > 1 ? e : t.previousSibling || e.nextSibling ? e.nextSibling || t.previousSibling : n.first ? i.reverse().find(({ column: s }) => s < n.first).cell : i.reverse().find(({ column: s }) => s > n.last).cell;
}
function pU(i, t) {
  const e = t.getSelectionAffectedTableCells(i), n = e[0], r = e.pop(), s = [
    n,
    r
  ];
  return n.isBefore(r) ? s : s.reverse();
}
class bU extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get("TableUtils"), e = this.editor.model, n = t.getSelectionAffectedTableCells(e.document.selection), r = n.length > 0;
    this.isEnabled = r, this.value = r && n.every((s) => this._isInHeading(s, s.parent.parent));
  }
  /**
  * Executes the command.
  *
  * When the selection is in a non-header row, the command will set the `headingRows` table attribute to cover that row.
  *
  * When the selection is already in a header row, it will set `headingRows` so the heading section will end before that row.
  *
  * @fires execute
  * @param options.forceValue If set, the command will set (`true`) or unset (`false`) the header rows according to
  * the `forceValue` parameter instead of the current model state.
  */
  execute(t = {}) {
    if (t.forceValue === this.value)
      return;
    const e = this.editor.plugins.get("TableUtils"), n = this.editor.model, r = e.getSelectionAffectedTableCells(n.document.selection), s = r[0].findAncestor("table"), { first: o, last: a } = e.getRowIndexes(r), l = this.value ? o : a + 1, c = s.getAttribute("headingRows") || 0;
    n.change((u) => {
      if (l) {
        const d = l > c ? c : 0, f = Mx(s, l, d);
        for (const { cell: m } of f)
          Dx(m, l, u);
      }
      qt("headingRows", l, s, u, 0);
    });
  }
  /**
  * Checks if a table cell is in the heading section.
  */
  _isInHeading(t, e) {
    const n = parseInt(e.getAttribute("headingRows") || "0");
    return !!n && t.parent.index < n;
  }
}
class vU extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = this.editor.plugins.get("TableUtils"), n = e.getSelectionAffectedTableCells(t.document.selection), r = n.length > 0;
    this.isEnabled = r, this.value = r && n.every((s) => Om(e, s));
  }
  /**
  * Executes the command.
  *
  * When the selection is in a non-header column, the command will set the `headingColumns` table attribute to cover that column.
  *
  * When the selection is already in a header column, it will set `headingColumns` so the heading section will end before that column.
  *
  * @fires execute
  * @param options.forceValue If set, the command will set (`true`) or unset (`false`) the header columns according to
  * the `forceValue` parameter instead of the current model state.
  */
  execute(t = {}) {
    if (t.forceValue === this.value)
      return;
    const e = this.editor.plugins.get("TableUtils"), n = this.editor.model, r = e.getSelectionAffectedTableCells(n.document.selection), s = r[0].findAncestor("table"), { first: o, last: a } = e.getColumnIndexes(r), l = this.value ? o : a + 1;
    n.change((c) => {
      if (l) {
        const u = Lx(s, l);
        for (const { cell: d, column: f } of u)
          Nx(d, f, l, c);
      }
      qt("headingColumns", l, s, c, 0);
    });
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
const wU = 5, Dm = 40, yU = 2;
function _U(i) {
  const t = /* @__PURE__ */ new Set();
  for (const e of i.document.differ.getChanges()) {
    let n = null;
    switch (e.type) {
      case "insert":
        n = [
          "table",
          "tableRow",
          "tableCell"
        ].includes(e.name) ? e.position : null;
        break;
      case "remove":
        n = [
          "tableRow",
          "tableCell"
        ].includes(e.name) ? e.position : null;
        break;
      case "attribute":
        e.range.start.nodeAfter && (n = [
          "table",
          "tableRow",
          "tableCell"
        ].includes(e.range.start.nodeAfter.name) ? e.range.start : null);
        break;
    }
    if (!n)
      continue;
    const r = n.nodeAfter && n.nodeAfter.is("element", "table") ? n.nodeAfter : n.findAncestor("table");
    for (const s of i.createRangeOn(r).getItems())
      s.is("element", "table") && _c(s) && t.add(s);
  }
  return t;
}
function CU(i, t) {
  return Dm * 100 / zx(i, t);
}
function zx(i, t) {
  const e = A0(i, "tbody", t) || A0(i, "thead", t), n = t.editing.view.domConverter.mapViewToDom(e);
  return Lm(n);
}
function A0(i, t, e) {
  return [
    ...[
      ...e.editing.mapper.toViewElement(i).getChildren()
    ].find((s) => s.is("element", "table")).getChildren()
  ].find((s) => s.is("element", t));
}
function Lm(i) {
  const t = ae.window.getComputedStyle(i);
  return t.boxSizing === "border-box" ? parseFloat(t.width) - parseFloat(t.paddingLeft) - parseFloat(t.paddingRight) - parseFloat(t.borderLeftWidth) - parseFloat(t.borderRightWidth) : parseFloat(t.width);
}
function kU(i, t) {
  const e = t.getCellLocation(i).column, n = i.getAttribute("colspan") || 1;
  return {
    leftEdge: e,
    rightEdge: e + n - 1
  };
}
function si(i) {
  const t = Math.pow(10, yU), e = typeof i == "number" ? i : parseFloat(i);
  return Math.round(e * t) / t;
}
function xU(i, t, e) {
  return i <= t ? si(t) : i >= e ? si(e) : si(i);
}
function EU(i, t) {
  return Array(i).fill(t);
}
function ql(i) {
  return i.map((t) => typeof t == "number" ? t : parseFloat(t)).filter((t) => !Number.isNaN(t)).reduce((t, e) => t + e, 0);
}
function Hp(i) {
  const t = i.map((r) => r === "auto" ? r : parseFloat(r.replace("%", "")));
  let e = TU(t);
  const n = ql(e);
  return n !== 100 && (e = e.map((r) => si(r * 100 / n)).map((r, s, o) => {
    if (!(s === o.length - 1))
      return r;
    const l = ql(o);
    return si(r + 100 - l);
  })), e.map((r) => r + "%");
}
function TU(i) {
  const t = i.filter((r) => r === "auto").length;
  if (t === 0)
    return i.map((r) => si(r));
  const e = ql(i), n = Math.max((100 - e) / t, wU);
  return i.map((r) => r === "auto" ? n : r).map((r) => si(r));
}
function SU(i) {
  const t = ae.window.getComputedStyle(i);
  return t.boxSizing === "border-box" ? parseInt(t.width) : parseFloat(t.width) + parseFloat(t.paddingLeft) + parseFloat(t.paddingRight) + parseFloat(t.borderWidth);
}
function Hx(i, t, e, n) {
  for (let r = 0; r < Math.max(e.length, i.length); r++) {
    const s = i[r], o = e[r];
    o ? s ? n.setAttribute("columnWidth", o, s) : n.appendElement("tableColumn", {
      columnWidth: o
    }, t) : n.remove(s);
  }
}
function _c(i) {
  if (i.is("element", "tableColumnGroup"))
    return i;
  const t = i.getChildren();
  return Array.from(t).find((e) => e.is("element", "tableColumnGroup"));
}
function Cc(i) {
  const t = _c(i);
  return t ? Array.from(t.getChildren()) : [];
}
function AU(i) {
  return Cc(i).map((t) => t.getAttribute("columnWidth"));
}
function PU(i, t) {
  return Cc(i).reduce((n, r) => {
    const s = r.getAttribute("columnWidth"), o = r.getAttribute("colSpan");
    if (!o)
      return n.push(s), n;
    for (let a = 0; a < o; a++)
      n.push(s);
    return t.removeAttribute("colSpan", r), n;
  }, []);
}
class Sn extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.decorate("insertColumns"), this.decorate("insertRows");
  }
  /**
  * Returns the table cell location as an object with table row and table column indexes.
  *
  * For instance, in the table below:
  *
  *      0   1   2   3
  *    +---+---+---+---+
  *  0 | a     | b | c |
  *    +       +   +---+
  *  1 |       |   | d |
  *    +---+---+   +---+
  *  2 | e     |   | f |
  *    +---+---+---+---+
  *
  * the method will return:
  *
  * ```ts
  * const cellA = table.getNodeByPath( [ 0, 0 ] );
  * editor.plugins.get( 'TableUtils' ).getCellLocation( cellA );
  * // will return { row: 0, column: 0 }
  *
  * const cellD = table.getNodeByPath( [ 1, 0 ] );
  * editor.plugins.get( 'TableUtils' ).getCellLocation( cellD );
  * // will return { row: 1, column: 3 }
  * ```
  *
  * @returns Returns a `{row, column}` object.
  */
  getCellLocation(t) {
    const e = t.parent, n = e.parent, r = n.getChildIndex(e), s = new ut(n, {
      row: r
    });
    for (const { cell: o, row: a, column: l } of s)
      if (o === t)
        return {
          row: a,
          column: l
        };
    /* istanbul ignore next -- @preserve */
  }
  /**
  * Creates an empty table with a proper structure. The table needs to be inserted into the model,
  * for example, by using the {@link module:engine/model/model~Model#insertContent} function.
  *
  * ```ts
  * model.change( ( writer ) => {
  *   // Create a table of 2 rows and 7 columns:
  *   const table = tableUtils.createTable( writer, { rows: 2, columns: 7 } );
  *
  *   // Insert a table to the model at the best position taking the current selection:
  *   model.insertContent( table );
  * }
  * ```
  *
  * @param writer The model writer.
  * @param options.rows The number of rows to create. Default value is 2.
  * @param options.columns The number of columns to create. Default value is 2.
  * @param options.headingRows The number of heading rows. Default value is 0.
  * @param options.headingColumns The number of heading columns. Default value is 0.
  * @returns The created table element.
  */
  createTable(t, e) {
    const n = t.createElement("table"), r = e.rows || 2, s = e.columns || 2;
    return Tf(t, n, 0, r, s), e.headingRows && qt("headingRows", Math.min(e.headingRows, r), n, t, 0), e.headingColumns && qt("headingColumns", Math.min(e.headingColumns, s), n, t, 0), n;
  }
  /**
  * Inserts rows into a table.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).insertRows( table, { at: 1, rows: 2 } );
  * ```
  *
  * Assuming the table on the left, the above code will transform it to the table on the right:
  *
  *  row index
  *    0 +---+---+---+       `at` = 1,      +---+---+---+ 0
  *      | a | b | c |       `rows` = 2,    | a | b | c |
  *    1 +   +---+---+   <-- insert here    +   +---+---+ 1
  *      |   | d | e |                      |   |   |   |
  *    2 +   +---+---+       will give:     +   +---+---+ 2
  *      |   | f | g |                      |   |   |   |
  *    3 +---+---+---+                      +   +---+---+ 3
  *                                         |   | d | e |
  *                                         +   +---+---+ 4
  *                                         +   + f | g |
  *                                         +---+---+---+ 5
  *
  * @param table The table model element where the rows will be inserted.
  * @param options.at The row index at which the rows will be inserted.  Default value is 0.
  * @param options.rows The number of rows to insert.  Default value is 1.
  * @param options.copyStructureFromAbove The flag for copying row structure. Note that
  * the row structure will not be copied if this option is not provided.
  */
  insertRows(t, e = {}) {
    const n = this.editor.model, r = e.at || 0, s = e.rows || 1, o = e.copyStructureFromAbove !== void 0, a = e.copyStructureFromAbove ? r - 1 : r, l = this.getRows(t), c = this.getColumns(t);
    if (r > l)
      throw new T("tableutils-insertrows-insert-out-of-range", this, {
        options: e
      });
    n.change((u) => {
      const d = t.getAttribute("headingRows") || 0;
      if (d > r && qt("headingRows", d + s, t, u, 0), !o && (r === 0 || r === l)) {
        Tf(u, t, r, s, c);
        return;
      }
      const f = o ? Math.max(r, a) : r, m = new ut(t, {
        endRow: f
      }), g = new Array(c).fill(1);
      for (const { row: p, column: b, cellHeight: v, cellWidth: y, cell: x } of m) {
        const S = p + v - 1, k = p < r && r <= S, I = p <= a && a <= S;
        k ? (u.setAttribute("rowspan", v + s, x), g[b] = -y) : o && I && (g[b] = y);
      }
      for (let p = 0; p < s; p++) {
        const b = u.createElement("tableRow");
        u.insert(b, t, r);
        for (let v = 0; v < g.length; v++) {
          const y = g[v], x = u.createPositionAt(b, "end");
          y > 0 && lo(u, x, y > 1 ? {
            colspan: y
          } : void 0), v += Math.abs(y) - 1;
        }
      }
    });
  }
  /**
  * Inserts columns into a table.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).insertColumns( table, { at: 1, columns: 2 } );
  * ```
  *
  * Assuming the table on the left, the above code will transform it to the table on the right:
  *
  *  0   1   2   3                   0   1   2   3   4   5
  *  +---+---+---+                   +---+---+---+---+---+
  *  | a     | b |                   | a             | b |
  *  +       +---+                   +               +---+
  *  |       | c |                   |               | c |
  *  +---+---+---+     will give:    +---+---+---+---+---+
  *  | d | e | f |                   | d |   |   | e | f |
  *  +---+   +---+                   +---+---+---+   +---+
  *  | g |   | h |                   | g |   |   |   | h |
  *  +---+---+---+                   +---+---+---+---+---+
  *  | i         |                   | i                 |
  *  +---+---+---+                   +---+---+---+---+---+
  *      ^---- insert here, `at` = 1, `columns` = 2
  *
  * @param table The table model element where the columns will be inserted.
  * @param options.at The column index at which the columns will be inserted. Default value is 0.
  * @param options.columns The number of columns to insert. Default value is 1.
  */
  insertColumns(t, e = {}) {
    const n = this.editor.model, r = e.at || 0, s = e.columns || 1;
    n.change((o) => {
      const a = t.getAttribute("headingColumns");
      r < a && o.setAttribute("headingColumns", a + s, t);
      const l = this.getColumns(t);
      if (r === 0 || l === r) {
        for (const u of t.getChildren())
          u.is("element", "tableRow") && Lo(s, o, o.createPositionAt(u, r ? "end" : 0));
        return;
      }
      const c = new ut(t, {
        column: r,
        includeAllSlots: !0
      });
      for (const u of c) {
        const { row: d, cell: f, cellAnchorColumn: m, cellAnchorRow: g, cellWidth: p, cellHeight: b } = u;
        if (m < r) {
          o.setAttribute("colspan", p + s, f);
          const v = g + b - 1;
          for (let y = d; y <= v; y++)
            c.skipRow(y);
        } else
          Lo(s, o, u.getPositionBefore());
      }
    });
  }
  /**
  * Removes rows from the given `table`.
  *
  * This method re-calculates the table geometry including `rowspan` attribute of table cells overlapping removed rows
  * and table headings values.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).removeRows( table, { at: 1, rows: 2 } );
  * ```
  *
  * Executing the above code in the context of the table on the left will transform its structure as presented on the right:
  *
  *  row index
  *      ┌───┬───┬───┐        `at` = 1        ┌───┬───┬───┐
  *    0 │ a │ b │ c │        `rows` = 2      │ a │ b │ c │ 0
  *      │   ├───┼───┤                        │   ├───┼───┤
  *    1 │   │ d │ e │  <-- remove from here  │   │ d │ g │ 1
  *      │   │   ├───┤        will give:      ├───┼───┼───┤
  *    2 │   │   │ f │                        │ h │ i │ j │ 2
  *      │   │   ├───┤                        └───┴───┴───┘
  *    3 │   │   │ g │
  *      ├───┼───┼───┤
  *    4 │ h │ i │ j │
  *      └───┴───┴───┘
  *
  * @param options.at The row index at which the removing rows will start.
  * @param options.rows The number of rows to remove. Default value is 1.
  */
  removeRows(t, e) {
    const n = this.editor.model, r = e.rows || 1, s = this.getRows(t), o = e.at, a = o + r - 1;
    if (a > s - 1)
      throw new T("tableutils-removerows-row-index-out-of-range", this, {
        table: t,
        options: e
      });
    n.change((l) => {
      const c = {
        first: o,
        last: a
      }, { cellsToMove: u, cellsToTrim: d } = OU(t, c);
      if (u.size) {
        const f = a + 1;
        VU(t, f, u, l);
      }
      for (let f = a; f >= o; f--)
        l.remove(t.getChild(f));
      for (const { rowspan: f, cell: m } of d)
        qt("rowspan", f, m, l);
      RU(t, c, l), Vm(t, this) || Mm(t, this);
    });
  }
  /**
  * Removes columns from the given `table`.
  *
  * This method re-calculates the table geometry including the `colspan` attribute of table cells overlapping removed columns
  * and table headings values.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).removeColumns( table, { at: 1, columns: 2 } );
  * ```
  *
  * Executing the above code in the context of the table on the left will transform its structure as presented on the right:
  *
  *    0   1   2   3   4                       0   1   2
  *  ┌───────────────┬───┐                   ┌───────┬───┐
  *  │ a             │ b │                   │ a     │ b │
  *  │               ├───┤                   │       ├───┤
  *  │               │ c │                   │       │ c │
  *  ├───┬───┬───┬───┼───┤     will give:    ├───┬───┼───┤
  *  │ d │ e │ f │ g │ h │                   │ d │ g │ h │
  *  ├───┼───┼───┤   ├───┤                   ├───┤   ├───┤
  *  │ i │ j │ k │   │ l │                   │ i │   │ l │
  *  ├───┴───┴───┴───┴───┤                   ├───┴───┴───┤
  *  │ m                 │                   │ m         │
  *  └───────────────────┘                   └───────────┘
  *        ^---- remove from here, `at` = 1, `columns` = 2
  *
  * @param options.at The row index at which the removing columns will start.
  * @param options.columns The number of columns to remove.
  */
  removeColumns(t, e) {
    const n = this.editor.model, r = e.at, s = e.columns || 1, o = e.at + s - 1;
    n.change((a) => {
      IU(t, {
        first: r,
        last: o
      }, a);
      const l = Cc(t);
      for (let c = o; c >= r; c--) {
        for (const { cell: u, column: d, cellWidth: f } of [
          ...new ut(t)
        ])
          d <= c && f > 1 && d + f > c ? qt("colspan", f - 1, u, a) : d === c && a.remove(u);
        if (l[c]) {
          const u = c === 0 ? l[1] : l[c - 1], d = parseFloat(l[c].getAttribute("columnWidth")), f = parseFloat(u.getAttribute("columnWidth"));
          a.remove(l[c]), a.setAttribute("columnWidth", d + f + "%", u);
        }
      }
      Mm(t, this) || Vm(t, this);
    });
  }
  /**
  * Divides a table cell vertically into several ones.
  *
  * The cell will be visually split into more cells by updating colspans of other cells in a column
  * and inserting cells (columns) after that cell.
  *
  * In the table below, if cell "a" is split into 3 cells:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +---+---+---+
  *  | d | e | f |
  *  +---+---+---+
  *
  * it will result in the table below:
  *
  *  +---+---+---+---+---+
  *  | a |   |   | b | c |
  *  +---+---+---+---+---+
  *  | d         | e | f |
  *  +---+---+---+---+---+
  *
  * So cell "d" will get its `colspan` updated to `3` and 2 cells will be added (2 columns will be created).
  *
  * Splitting a cell that already has a `colspan` attribute set will distribute the cell `colspan` evenly and the remainder
  * will be left to the original cell:
  *
  *  +---+---+---+
  *  | a         |
  *  +---+---+---+
  *  | b | c | d |
  *  +---+---+---+
  *
  * Splitting cell "a" with `colspan=3` into 2 cells will create 1 cell with a `colspan=a` and cell "a" that will have `colspan=2`:
  *
  *  +---+---+---+
  *  | a     |   |
  *  +---+---+---+
  *  | b | c | d |
  *  +---+---+---+
  */
  splitCellVertically(t, e = 2) {
    const n = this.editor.model, s = t.parent.parent, o = parseInt(t.getAttribute("rowspan") || "1"), a = parseInt(t.getAttribute("colspan") || "1");
    n.change((l) => {
      if (a > 1) {
        const { newCellsSpan: c, updatedSpan: u } = P0(a, e);
        qt("colspan", u, t, l);
        const d = {};
        c > 1 && (d.colspan = c), o > 1 && (d.rowspan = o);
        const f = a > e ? e - 1 : a - 1;
        Lo(f, l, l.createPositionAfter(t), d);
      }
      if (a < e) {
        const c = e - a, u = [
          ...new ut(s)
        ], { column: d } = u.find(({ cell: p }) => p === t), f = u.filter(({ cell: p, cellWidth: b, column: v }) => {
          const y = p !== t && v === d, x = v < d && v + b > d;
          return y || x;
        });
        for (const { cell: p, cellWidth: b } of f)
          l.setAttribute("colspan", b + c, p);
        const m = {};
        o > 1 && (m.rowspan = o), Lo(c, l, l.createPositionAfter(t), m);
        const g = s.getAttribute("headingColumns") || 0;
        g > d && qt("headingColumns", g + c, s, l);
      }
    });
  }
  /**
  * Divides a table cell horizontally into several ones.
  *
  * The cell will be visually split into more cells by updating rowspans of other cells in the row and inserting rows with a single cell
  * below.
  *
  * If in the table below cell "b" is split into 3 cells:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +---+---+---+
  *  | d | e | f |
  *  +---+---+---+
  *
  * It will result in the table below:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +   +---+   +
  *  |   |   |   |
  *  +   +---+   +
  *  |   |   |   |
  *  +---+---+---+
  *  | d | e | f |
  *  +---+---+---+
  *
  * So cells "a" and "b" will get their `rowspan` updated to `3` and 2 rows with a single cell will be added.
  *
  * Splitting a cell that already has a `rowspan` attribute set will distribute the cell `rowspan` evenly and the remainder
  * will be left to the original cell:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +   +---+---+
  *  |   | d | e |
  *  +   +---+---+
  *  |   | f | g |
  *  +   +---+---+
  *  |   | h | i |
  *  +---+---+---+
  *
  * Splitting cell "a" with `rowspan=4` into 3 cells will create 2 cells with a `rowspan=1` and cell "a" will have `rowspan=2`:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +   +---+---+
  *  |   | d | e |
  *  +---+---+---+
  *  |   | f | g |
  *  +---+---+---+
  *  |   | h | i |
  *  +---+---+---+
  */
  splitCellHorizontally(t, e = 2) {
    const n = this.editor.model, r = t.parent, s = r.parent, o = s.getChildIndex(r), a = parseInt(t.getAttribute("rowspan") || "1"), l = parseInt(t.getAttribute("colspan") || "1");
    n.change((c) => {
      if (a > 1) {
        const u = [
          ...new ut(s, {
            startRow: o,
            endRow: o + a - 1,
            includeAllSlots: !0
          })
        ], { newCellsSpan: d, updatedSpan: f } = P0(a, e);
        qt("rowspan", f, t, c);
        const { column: m } = u.find(({ cell: b }) => b === t), g = {};
        d > 1 && (g.rowspan = d), l > 1 && (g.colspan = l);
        let p = 0;
        for (const b of u) {
          const { column: v, row: y } = b, x = y >= o + f, S = v === m;
          p >= d && S && (p = 0), x && S && (p || Lo(1, c, b.getPositionBefore(), g), p++);
        }
      }
      if (a < e) {
        const u = e - a, d = [
          ...new ut(s, {
            startRow: 0,
            endRow: o
          })
        ];
        for (const { cell: g, cellHeight: p, row: b } of d)
          if (g !== t && b + p > o) {
            const v = p + u;
            c.setAttribute("rowspan", v, g);
          }
        const f = {};
        l > 1 && (f.colspan = l), Tf(c, s, o + 1, u, 1, f);
        const m = s.getAttribute("headingRows") || 0;
        m > o && qt("headingRows", m + u, s, c);
      }
    });
  }
  /**
  * Returns the number of columns for a given table.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).getColumns( table );
  * ```
  *
  * @param table The table to analyze.
  */
  getColumns(t) {
    return [
      ...t.getChild(0).getChildren()
    ].filter((n) => n.is("element", "tableCell")).reduce((n, r) => {
      const s = parseInt(r.getAttribute("colspan") || "1");
      return n + s;
    }, 0);
  }
  /**
  * Returns the number of rows for a given table. Any other element present in the table model is omitted.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).getRows( table );
  * ```
  *
  * @param table The table to analyze.
  */
  getRows(t) {
    return Array.from(t.getChildren()).reduce((e, n) => n.is("element", "tableRow") ? e + 1 : e, 0);
  }
  /**
  * Creates an instance of the table walker.
  *
  * The table walker iterates internally by traversing the table from row index = 0 and column index = 0.
  * It walks row by row and column by column in order to output values defined in the options.
  * By default it will output only the locations that are occupied by a cell. To include also spanned rows and columns,
  * pass the `includeAllSlots` option.
  *
  * @internal
  * @param table A table over which the walker iterates.
  * @param options An object with configuration.
  */
  createTableWalker(t, e = {}) {
    return new ut(t, e);
  }
  /**
  * Returns all model table cells that are fully selected (from the outside)
  * within the provided model selection's ranges.
  *
  * To obtain the cells selected from the inside, use
  * {@link #getTableCellsContainingSelection}.
  */
  getSelectedTableCells(t) {
    const e = [];
    for (const n of this.sortRanges(t.getRanges())) {
      const r = n.getContainedElement();
      r && r.is("element", "tableCell") && e.push(r);
    }
    return e;
  }
  /**
  * Returns all model table cells that the provided model selection's ranges
  * {@link module:engine/model/range~Range#start} inside.
  *
  * To obtain the cells selected from the outside, use
  * {@link #getSelectedTableCells}.
  */
  getTableCellsContainingSelection(t) {
    const e = [];
    for (const n of t.getRanges()) {
      const r = n.start.findAncestor("tableCell");
      r && e.push(r);
    }
    return e;
  }
  /**
  * Returns all model table cells that are either completely selected
  * by selection ranges or host selection range
  * {@link module:engine/model/range~Range#start start positions} inside them.
  *
  * Combines {@link #getTableCellsContainingSelection} and
  * {@link #getSelectedTableCells}.
  */
  getSelectionAffectedTableCells(t) {
    const e = this.getSelectedTableCells(t);
    return e.length ? e : this.getTableCellsContainingSelection(t);
  }
  /**
  * Returns an object with the `first` and `last` row index contained in the given `tableCells`.
  *
  * ```ts
  * const selectedTableCells = getSelectedTableCells( editor.model.document.selection );
  *
  * const { first, last } = getRowIndexes( selectedTableCells );
  *
  * console.log( `Selected rows: ${ first } to ${ last }` );
  * ```
  *
  * @returns Returns an object with the `first` and `last` table row indexes.
  */
  getRowIndexes(t) {
    const e = t.map((n) => n.parent.index);
    return this._getFirstLastIndexesObject(e);
  }
  /**
  * Returns an object with the `first` and `last` column index contained in the given `tableCells`.
  *
  * ```ts
  * const selectedTableCells = getSelectedTableCells( editor.model.document.selection );
  *
  * const { first, last } = getColumnIndexes( selectedTableCells );
  *
  * console.log( `Selected columns: ${ first } to ${ last }` );
  * ```
  *
  * @returns Returns an object with the `first` and `last` table column indexes.
  */
  getColumnIndexes(t) {
    const e = t[0].findAncestor("table"), r = [
      ...new ut(e)
    ].filter((s) => t.includes(s.cell)).map((s) => s.column);
    return this._getFirstLastIndexesObject(r);
  }
  /**
  * Checks if the selection contains cells that do not exceed rectangular selection.
  *
  * In a table below:
  *
  *  ┌───┬───┬───┬───┐
  *  │ a │ b │ c │ d │
  *  ├───┴───┼───┤   │
  *  │ e     │ f │   │
  *  │       ├───┼───┤
  *  │       │ g │ h │
  *  └───────┴───┴───┘
  *
  * Valid selections are these which create a solid rectangle (without gaps), such as:
  *   - a, b (two horizontal cells)
  *   - c, f (two vertical cells)
  *   - a, b, e (cell "e" spans over four cells)
  *   - c, d, f (cell d spans over a cell in the row below)
  *
  * While an invalid selection would be:
  *   - a, c (the unselected cell "b" creates a gap)
  *   - f, g, h (cell "d" spans over a cell from the row of "f" cell - thus creates a gap)
  */
  isSelectionRectangular(t) {
    if (t.length < 2 || !this._areCellInTheSameTableSection(t))
      return !1;
    const e = /* @__PURE__ */ new Set(), n = /* @__PURE__ */ new Set();
    let r = 0;
    for (const o of t) {
      const { row: a, column: l } = this.getCellLocation(o), c = parseInt(o.getAttribute("rowspan")) || 1, u = parseInt(o.getAttribute("colspan")) || 1;
      e.add(a), n.add(l), c > 1 && e.add(a + c - 1), u > 1 && n.add(l + u - 1), r += c * u;
    }
    return DU(e, n) == r;
  }
  /**
  * Returns array of sorted ranges.
  */
  sortRanges(t) {
    return Array.from(t).sort(MU);
  }
  /**
  * Helper method to get an object with `first` and `last` indexes from an unsorted array of indexes.
  */
  _getFirstLastIndexesObject(t) {
    const e = t.sort((s, o) => s - o), n = e[0], r = e[e.length - 1];
    return {
      first: n,
      last: r
    };
  }
  /**
  * Checks if the selection does not mix a header (column or row) with other cells.
  *
  * For instance, in the table below valid selections consist of cells with the same letter only.
  * So, a-a (same heading row and column) or d-d (body cells) are valid while c-d or a-b are not.
  *
  * header columns
  *    ↓   ↓
  *  ┌───┬───┬───┬───┐
  *  │ a │ a │ b │ b │  ← header row
  *  ├───┼───┼───┼───┤
  *  │ c │ c │ d │ d │
  *  ├───┼───┼───┼───┤
  *  │ c │ c │ d │ d │
  *  └───┴───┴───┴───┘
  */
  _areCellInTheSameTableSection(t) {
    const e = t[0].findAncestor("table"), n = this.getRowIndexes(t), r = parseInt(e.getAttribute("headingRows")) || 0;
    if (!this._areIndexesInSameSection(n, r))
      return !1;
    const s = this.getColumnIndexes(t), o = parseInt(e.getAttribute("headingColumns")) || 0;
    return this._areIndexesInSameSection(s, o);
  }
  /**
  * Unified check if table rows/columns indexes are in the same heading/body section.
  */
  _areIndexesInSameSection({ first: t, last: e }, n) {
    const r = t < n, s = e < n;
    return r === s;
  }
}
function Tf(i, t, e, n, r, s = {}) {
  for (let o = 0; o < n; o++) {
    const a = i.createElement("tableRow");
    i.insert(a, t, e), Lo(r, i, i.createPositionAt(a, "end"), s);
  }
}
function Lo(i, t, e, n = {}) {
  for (let r = 0; r < i; r++)
    lo(t, e, n);
}
function P0(i, t) {
  if (i < t)
    return {
      newCellsSpan: 1,
      updatedSpan: 1
    };
  const e = Math.floor(i / t), n = i - e * t + e;
  return {
    newCellsSpan: e,
    updatedSpan: n
  };
}
function IU(i, t, e) {
  const n = i.getAttribute("headingColumns") || 0;
  if (n && t.first < n) {
    const r = Math.min(n - 1, t.last) - t.first + 1;
    e.setAttribute("headingColumns", n - r, i);
  }
}
function RU(i, { first: t, last: e }, n) {
  const r = i.getAttribute("headingRows") || 0;
  if (t < r) {
    const s = e < r ? r - (e - t + 1) : t;
    qt("headingRows", s, i, n, 0);
  }
}
function OU(i, { first: t, last: e }) {
  const n = /* @__PURE__ */ new Map(), r = [];
  for (const { row: s, column: o, cellHeight: a, cell: l } of new ut(i, {
    endRow: e
  })) {
    const c = s + a - 1;
    if (s >= t && s <= e && c > e) {
      const f = e - s + 1, m = a - f;
      n.set(o, {
        cell: l,
        rowspan: m
      });
    }
    if (s < t && c >= t) {
      let f;
      c >= e ? f = e - t + 1 : f = c - t + 1, r.push({
        cell: l,
        rowspan: a - f
      });
    }
  }
  return {
    cellsToMove: n,
    cellsToTrim: r
  };
}
function VU(i, t, e, n) {
  const s = [
    ...new ut(i, {
      includeAllSlots: !0,
      row: t
    })
  ], o = i.getChild(t);
  let a;
  for (const { column: l, cell: c, isAnchor: u } of s)
    if (e.has(l)) {
      const { cell: d, rowspan: f } = e.get(l), m = a ? n.createPositionAfter(a) : n.createPositionAt(o, 0);
      n.move(n.createRangeOn(d), m), qt("rowspan", f, d, n), a = d;
    } else u && (a = c);
}
function MU(i, t) {
  const e = i.start, n = t.start;
  return e.isBefore(n) ? -1 : 1;
}
function DU(i, t) {
  const e = Array.from(i.values()), n = Array.from(t.values()), r = Math.max(...e), s = Math.min(...e), o = Math.max(...n), a = Math.min(...n);
  return (r - s + 1) * (o - a + 1);
}
class LU extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get(Sn), e = t.getSelectedTableCells(this.editor.model.document.selection);
    this.isEnabled = t.isSelectionRectangular(e);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  */
  execute() {
    const t = this.editor.model, e = this.editor.plugins.get(Sn);
    t.change((n) => {
      const r = e.getSelectedTableCells(t.document.selection), s = r.shift(), { mergeWidth: o, mergeHeight: a } = BU(s, r, e);
      qt("colspan", o, s, n), qt("rowspan", a, s, n);
      for (const c of r)
        NU(c, s, n);
      const l = s.findAncestor("table");
      zp(l, e), n.setSelection(s, "in");
    });
  }
}
function NU(i, t, e) {
  I0(i) || (I0(t) && e.remove(e.createRangeIn(t)), e.move(e.createRangeIn(i), e.createPositionAt(t, "end"))), e.remove(i);
}
function I0(i) {
  const t = i.getChild(0);
  return i.childCount == 1 && t.is("element", "paragraph") && t.isEmpty;
}
function BU(i, t, e) {
  let n = 0, r = 0;
  for (const c of t) {
    const { row: u, column: d } = e.getCellLocation(c);
    n = R0(c, d, n, "colspan"), r = R0(c, u, r, "rowspan");
  }
  const { row: s, column: o } = e.getCellLocation(i), a = n - o, l = r - s;
  return {
    mergeWidth: a,
    mergeHeight: l
  };
}
function R0(i, t, e, n) {
  const r = parseInt(i.getAttribute(n) || "1");
  return Math.max(e, t + r);
}
class FU extends Ve {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.affectsData = !1;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(this.editor.model.document.selection);
    this.isEnabled = e.length > 0;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = this.editor.plugins.get("TableUtils"), n = e.getSelectionAffectedTableCells(t.document.selection), r = e.getRowIndexes(n), s = n[0].findAncestor("table"), o = [];
    for (let a = r.first; a <= r.last; a++)
      for (const l of s.getChild(a).getChildren())
        o.push(t.createRangeOn(l));
    t.change((a) => {
      a.setSelection(o);
    });
  }
}
class $U extends Ve {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.affectsData = !1;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(this.editor.model.document.selection);
    this.isEnabled = e.length > 0;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.plugins.get("TableUtils"), e = this.editor.model, n = t.getSelectionAffectedTableCells(e.document.selection), r = n[0], s = n.pop(), o = r.findAncestor("table"), a = t.getCellLocation(r), l = t.getCellLocation(s), c = Math.min(a.column, l.column), u = Math.max(a.column, l.column), d = [];
    for (const f of new ut(o, {
      startColumn: c,
      endColumn: u
    }))
      d.push(e.createRangeOn(f.cell));
    e.change((f) => {
      f.setSelection(d);
    });
  }
}
function zU(i) {
  i.document.registerPostFixer((t) => HU(t, i));
}
function HU(i, t) {
  const e = t.document.differ.getChanges();
  let n = !1;
  const r = /* @__PURE__ */ new Set();
  for (const s of e) {
    let o = null;
    s.type == "insert" && s.name == "table" && (o = s.position.nodeAfter), (s.type == "insert" || s.type == "remove") && (s.name == "tableRow" || s.name == "tableCell") && (o = s.position.findAncestor("table")), GU(s) && (o = s.range.start.findAncestor("table")), o && !r.has(o) && (n = jU(o, i) || n, n = WU(o, i) || n, r.add(o));
  }
  return n;
}
function jU(i, t) {
  let e = !1;
  const n = UU(i);
  if (n.length) {
    e = !0;
    for (const r of n)
      qt("rowspan", r.rowspan, r.cell, t, 1);
  }
  return e;
}
function WU(i, t) {
  let e = !1;
  const n = qU(i), r = [];
  for (const [l, c] of n.entries())
    !c && i.getChild(l).is("element", "tableRow") && r.push(l);
  if (r.length) {
    e = !0;
    for (const l of r.reverse())
      t.remove(i.getChild(l)), n.splice(l, 1);
  }
  const s = n.filter((l, c) => i.getChild(c).is("element", "tableRow")), o = s[0];
  if (!s.every((l) => l === o)) {
    const l = s.reduce((c, u) => u > c ? u : c, 0);
    for (const [c, u] of s.entries()) {
      const d = l - u;
      if (d) {
        for (let f = 0; f < d; f++)
          lo(t, t.createPositionAt(i.getChild(c), "end"));
        e = !0;
      }
    }
  }
  return e;
}
function UU(i) {
  const t = parseInt(i.getAttribute("headingRows") || "0"), e = Array.from(i.getChildren()).reduce((r, s) => s.is("element", "tableRow") ? r + 1 : r, 0), n = [];
  for (const { row: r, cell: s, cellHeight: o } of new ut(i)) {
    if (o < 2)
      continue;
    const l = r < t ? t : e;
    if (r + o > l) {
      const c = l - r;
      n.push({
        cell: s,
        rowspan: c
      });
    }
  }
  return n;
}
function qU(i) {
  const t = new Array(i.childCount).fill(0);
  for (const { rowIndex: e } of new ut(i, {
    includeAllSlots: !0
  }))
    t[e]++;
  return t;
}
function GU(i) {
  if (i.type !== "attribute")
    return !1;
  const t = i.attributeKey;
  return t === "headingRows" || t === "colspan" || t === "rowspan";
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function KU(i) {
  i.document.registerPostFixer((t) => ZU(t, i));
}
function ZU(i, t) {
  const e = t.document.differ.getChanges();
  let n = !1;
  for (const r of e)
    r.type == "insert" && r.name == "table" && (n = YU(r.position.nodeAfter, i) || n), r.type == "insert" && r.name == "tableRow" && (n = jx(r.position.nodeAfter, i) || n), r.type == "insert" && r.name == "tableCell" && (n = Nm(r.position.nodeAfter, i) || n), (r.type == "remove" || r.type == "insert") && JU(r) && (n = Nm(r.position.parent, i) || n);
  return n;
}
function YU(i, t) {
  let e = !1;
  for (const n of i.getChildren())
    n.is("element", "tableRow") && (e = jx(n, t) || e);
  return e;
}
function jx(i, t) {
  let e = !1;
  for (const n of i.getChildren())
    e = Nm(n, t) || e;
  return e;
}
function Nm(i, t) {
  if (i.childCount == 0)
    return t.insertElement("paragraph", i), !0;
  const e = Array.from(i.getChildren()).filter((n) => n.is("$text"));
  for (const n of e)
    t.wrap(t.createRangeOn(n), "paragraph");
  return !!e.length;
}
function JU(i) {
  return i.position.parent.is("element", "tableCell") ? i.type == "insert" && i.name == "$text" || i.type == "remove" : !1;
}
function XU(i, t) {
  const e = i.document.differ;
  for (const n of e.getChanges()) {
    let r, s = !1;
    if (n.type == "attribute") {
      const c = n.range.start.nodeAfter;
      if (!c || !c.is("element", "table") || n.attributeKey != "headingRows" && n.attributeKey != "headingColumns")
        continue;
      r = c, s = n.attributeKey == "headingRows";
    } else (n.name == "tableRow" || n.name == "tableCell") && (r = n.position.findAncestor("table"), s = n.name == "tableRow");
    if (!r)
      continue;
    const o = r.getAttribute("headingRows") || 0, a = r.getAttribute("headingColumns") || 0, l = new ut(r);
    for (const c of l) {
      const d = c.row < o || c.column < a ? "th" : "td", f = t.mapper.toViewElement(c.cell);
      f && f.is("element") && f.name != d && t.reconvertItem(s ? c.cell.parent : c.cell);
    }
  }
}
function QU(i, t) {
  const e = i.document.differ, n = /* @__PURE__ */ new Set();
  for (const r of e.getChanges()) {
    const s = r.type == "attribute" ? r.range.start.parent : r.position.parent;
    s.is("element", "tableCell") && n.add(s);
  }
  for (const r of n.values()) {
    const s = Array.from(r.getChildren()).filter((o) => eq(o, t.mapper));
    for (const o of s)
      t.reconvertItem(o);
  }
}
function eq(i, t) {
  if (!i.is("element", "paragraph"))
    return !1;
  const e = t.toViewElement(i);
  return e ? Ox(i) !== e.is("element", "span") : !1;
}
class kc extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Handlers for creating additional slots in the table.
    */
    h(this, "_additionalSlots");
    this._additionalSlots = [];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model, r = n.schema, s = e.conversion, o = e.plugins.get(Sn);
    r.register("table", {
      inheritAllFrom: "$blockObject",
      allowAttributes: [
        "headingRows",
        "headingColumns"
      ]
    }), r.register("tableRow", {
      allowIn: "table",
      isLimit: !0
    }), r.register("tableCell", {
      allowContentOf: "$container",
      allowIn: "tableRow",
      allowAttributes: [
        "colspan",
        "rowspan"
      ],
      isLimit: !0,
      isSelectable: !0
    }), s.for("upcast").add(YW()), s.for("upcast").add(JW()), s.for("editingDowncast").elementToStructure({
      model: {
        name: "table",
        attributes: [
          "headingRows"
        ]
      },
      view: _0(o, {
        asWidget: !0,
        additionalSlots: this._additionalSlots
      })
    }), s.for("dataDowncast").elementToStructure({
      model: {
        name: "table",
        attributes: [
          "headingRows"
        ]
      },
      view: _0(o, {
        additionalSlots: this._additionalSlots
      })
    }), s.for("upcast").elementToElement({
      model: "tableRow",
      view: "tr"
    }), s.for("upcast").add(XW()), s.for("downcast").elementToElement({
      model: "tableRow",
      view: iU()
    }), s.for("upcast").elementToElement({
      model: "tableCell",
      view: "td"
    }), s.for("upcast").elementToElement({
      model: "tableCell",
      view: "th"
    }), s.for("upcast").add(y0("td")), s.for("upcast").add(y0("th")), s.for("editingDowncast").elementToElement({
      model: "tableCell",
      view: C0({
        asWidget: !0
      })
    }), s.for("dataDowncast").elementToElement({
      model: "tableCell",
      view: C0()
    }), s.for("editingDowncast").elementToElement({
      model: "paragraph",
      view: k0({
        asWidget: !0
      }),
      converterPriority: "high"
    }), s.for("dataDowncast").elementToElement({
      model: "paragraph",
      view: k0(),
      converterPriority: "high"
    }), s.for("downcast").attributeToAttribute({
      model: "colspan",
      view: "colspan"
    }), s.for("upcast").attributeToAttribute({
      model: {
        key: "colspan",
        value: O0("colspan")
      },
      view: "colspan"
    }), s.for("downcast").attributeToAttribute({
      model: "rowspan",
      view: "rowspan"
    }), s.for("upcast").attributeToAttribute({
      model: {
        key: "rowspan",
        value: O0("rowspan")
      },
      view: "rowspan"
    }), e.config.define("table.defaultHeadings.rows", 0), e.config.define("table.defaultHeadings.columns", 0), e.commands.add("insertTable", new oU(e)), e.commands.add("insertTableRowAbove", new x0(e, {
      order: "above"
    })), e.commands.add("insertTableRowBelow", new x0(e, {
      order: "below"
    })), e.commands.add("insertTableColumnLeft", new E0(e, {
      order: "left"
    })), e.commands.add("insertTableColumnRight", new E0(e, {
      order: "right"
    })), e.commands.add("removeTableRow", new hU(e)), e.commands.add("removeTableColumn", new mU(e)), e.commands.add("splitTableCellVertically", new T0(e, {
      direction: "vertically"
    })), e.commands.add("splitTableCellHorizontally", new T0(e, {
      direction: "horizontally"
    })), e.commands.add("mergeTableCells", new LU(e)), e.commands.add("mergeTableCellRight", new Qc(e, {
      direction: "right"
    })), e.commands.add("mergeTableCellLeft", new Qc(e, {
      direction: "left"
    })), e.commands.add("mergeTableCellDown", new Qc(e, {
      direction: "down"
    })), e.commands.add("mergeTableCellUp", new Qc(e, {
      direction: "up"
    })), e.commands.add("setTableColumnHeader", new vU(e)), e.commands.add("setTableRowHeader", new bU(e)), e.commands.add("selectTableRow", new FU(e)), e.commands.add("selectTableColumn", new $U(e)), zU(n), KU(n), this.listenTo(n.document, "change:data", () => {
      XU(n, e.editing), QU(n, e.editing);
    });
  }
  /**
  * Registers downcast handler for the additional table slot.
  */
  registerAdditionalSlot(e) {
    this._additionalSlots.push(e);
  }
}
function O0(i) {
  return (t) => {
    const e = parseInt(t.getAttribute(i));
    return Number.isNaN(e) || e <= 0 ? null : e;
  };
}
class V0 extends de {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of table size box items.
    */
    h(this, "items");
    /**
    * Listen to `keydown` events fired in this view's main element.
    */
    h(this, "keystrokes");
    /**
    * Tracks information about the DOM focus in the grid.
    */
    h(this, "focusTracker");
    const n = this.bindTemplate;
    this.items = this._createGridCollection(), this.keystrokes = new Ot(), this.focusTracker = new wt(), this.set("rows", 0), this.set("columns", 0), this.bind("label").to(this, "columns", this, "rows", (r, s) => `${s} × ${r}`), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck"
        ]
      },
      children: [
        {
          tag: "div",
          attributes: {
            class: [
              "ck-insert-table-dropdown__grid"
            ]
          },
          on: {
            "mouseover@.ck-insert-table-dropdown-grid-box": n.to("boxover")
          },
          children: this.items
        },
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-insert-table-dropdown__label"
            ],
            "aria-hidden": !0
          },
          children: [
            {
              text: n.to("label")
            }
          ]
        }
      ],
      on: {
        mousedown: n.to((r) => {
          r.preventDefault();
        }),
        click: n.to(() => {
          this.fire("execute");
        })
      }
    }), this.on("boxover", (r, s) => {
      const { row: o, column: a } = s.target.dataset;
      this.items.get((parseInt(o, 10) - 1) * 10 + (parseInt(a, 10) - 1)).focus();
    }), this.focusTracker.on("change:focusedElement", (r, s, o) => {
      if (!o)
        return;
      const { row: a, column: l } = o.dataset;
      this.set({
        rows: parseInt(a),
        columns: parseInt(l)
      });
    }), this.on("change:columns", () => this._highlightGridBoxes()), this.on("change:rows", () => this._highlightGridBoxes());
  }
  render() {
    super.render(), mp({
      keystrokeHandler: this.keystrokes,
      focusTracker: this.focusTracker,
      gridItems: this.items,
      numberOfColumns: 10,
      uiLanguageDirection: this.locale && this.locale.uiLanguageDirection
    });
    for (const e of this.items)
      this.focusTracker.add(e.element);
    this.keystrokes.listenTo(this.element);
  }
  /**
  * Resets the rows and columns selection.
  */
  reset() {
    this.set({
      rows: 1,
      columns: 1
    });
  }
  /**
  * @inheritDoc
  */
  focus() {
    this.items.get(0).focus();
  }
  /**
  * @inheritDoc
  */
  focusLast() {
    this.items.get(0).focus();
  }
  /**
  * Highlights grid boxes depending on rows and columns selected.
  */
  _highlightGridBoxes() {
    const e = this.rows, n = this.columns;
    this.items.map((r, s) => {
      const o = Math.floor(s / 10), a = s % 10, l = o < e && a < n;
      r.set("isOn", l);
    });
  }
  /**
  * Creates a new Button for the grid.
  *
  * @param locale The locale instance.
  * @param row Row number.
  * @param column Column number.
  * @param label The grid button label.
  */
  _createGridButton(e, n, r, s) {
    const o = new Te(e);
    return o.set({
      label: s,
      class: "ck-insert-table-dropdown-grid-box"
    }), o.extendTemplate({
      attributes: {
        "data-row": n,
        "data-column": r
      }
    }), o;
  }
  /**
  * @returns A view collection containing boxes to be placed in a table grid.
  */
  _createGridCollection() {
    const e = [];
    for (let n = 0; n < 100; n++) {
      const r = Math.floor(n / 10), s = n % 10, o = `${r + 1} × ${s + 1}`;
      e.push(this._createGridButton(this.locale, r + 1, s + 1, o));
    }
    return this.createCollection(e);
  }
}
var tq = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 1h15A1.5 1.5 0 0 1 19 2.5v15a1.5 1.5 0 0 1-1.5 1.5h-15A1.5 1.5 0 0 1 1 17.5v-15A1.5 1.5 0 0 1 2.5 1zM2 2v16h16V2H2z" opacity=".6"/><path d="M18 7v1H2V7h16zm0 5v1H2v-1h16z" opacity=".6"/><path d="M14 1v18a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1V1a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1zm-2 1H8v4h4V2zm0 6H8v4h4V8zm0 6H8v4h4v-4z"/></svg>', nq = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 1h15A1.5 1.5 0 0 1 19 2.5v15a1.5 1.5 0 0 1-1.5 1.5h-15A1.5 1.5 0 0 1 1 17.5v-15A1.5 1.5 0 0 1 2.5 1zM2 2v16h16V2H2z" opacity=".6"/><path d="M7 2h1v16H7V2zm5 0h1v16h-1V2z" opacity=".6"/><path d="M1 6h18a1 1 0 0 1 1 1v6a1 1 0 0 1-1 1H1a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1zm1 2v4h4V8H2zm6 0v4h4V8H8zm6 0v4h4V8h-4z"/></svg>', iq = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 1h15A1.5 1.5 0 0 1 19 2.5v15a1.5 1.5 0 0 1-1.5 1.5h-15A1.5 1.5 0 0 1 1 17.5v-15A1.5 1.5 0 0 1 2.5 1zM2 2v16h16V2H2z" opacity=".6"/><path d="M7 2h1v16H7V2zm5 0h1v7h-1V2zm6 5v1H2V7h16zM8 12v1H2v-1h6z" opacity=".6"/><path d="M7 7h12a1 1 0 0 1 1 1v11a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1V8a1 1 0 0 1 1-1zm1 2v9h10V9H8z"/></svg>';
class rq extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t, r = t.locale.contentLanguageDirection === "ltr";
    t.ui.componentFactory.add("insertTable", (s) => {
      const o = t.commands.get("insertTable"), a = Yn(s);
      a.bind("isEnabled").to(o), a.buttonView.set({
        icon: ee.table,
        label: e("Insert table"),
        tooltip: !0
      });
      let l;
      return a.on("change:isOpen", () => {
        l || (l = new V0(s), a.panelView.children.add(l), l.delegate("execute").to(a), a.on("execute", () => {
          t.execute("insertTable", {
            rows: l.rows,
            columns: l.columns
          }), t.editing.view.focus();
        }));
      }), a;
    }), t.ui.componentFactory.add("menuBar:insertTable", (s) => {
      const o = t.commands.get("insertTable"), a = new Yi(s), l = new V0(s);
      return l.delegate("execute").to(a), a.on("change:isOpen", (c, u, d) => {
        d || l.reset();
      }), l.on("execute", () => {
        t.execute("insertTable", {
          rows: l.rows,
          columns: l.columns
        }), t.editing.view.focus();
      }), a.buttonView.set({
        label: e("Table"),
        icon: ee.table
      }), a.panelView.children.add(l), a.bind("isEnabled").to(o), a;
    }), t.ui.componentFactory.add("tableColumn", (s) => {
      const o = [
        {
          type: "switchbutton",
          model: {
            commandName: "setTableColumnHeader",
            label: e("Header column"),
            bindIsOn: !0
          }
        },
        {
          type: "separator"
        },
        {
          type: "button",
          model: {
            commandName: r ? "insertTableColumnLeft" : "insertTableColumnRight",
            label: e("Insert column left")
          }
        },
        {
          type: "button",
          model: {
            commandName: r ? "insertTableColumnRight" : "insertTableColumnLeft",
            label: e("Insert column right")
          }
        },
        {
          type: "button",
          model: {
            commandName: "removeTableColumn",
            label: e("Delete column")
          }
        },
        {
          type: "button",
          model: {
            commandName: "selectTableColumn",
            label: e("Select column")
          }
        }
      ];
      return this._prepareDropdown(e("Column"), tq, o, s);
    }), t.ui.componentFactory.add("tableRow", (s) => {
      const o = [
        {
          type: "switchbutton",
          model: {
            commandName: "setTableRowHeader",
            label: e("Header row"),
            bindIsOn: !0
          }
        },
        {
          type: "separator"
        },
        {
          type: "button",
          model: {
            commandName: "insertTableRowAbove",
            label: e("Insert row above")
          }
        },
        {
          type: "button",
          model: {
            commandName: "insertTableRowBelow",
            label: e("Insert row below")
          }
        },
        {
          type: "button",
          model: {
            commandName: "removeTableRow",
            label: e("Delete row")
          }
        },
        {
          type: "button",
          model: {
            commandName: "selectTableRow",
            label: e("Select row")
          }
        }
      ];
      return this._prepareDropdown(e("Row"), nq, o, s);
    }), t.ui.componentFactory.add("mergeTableCells", (s) => {
      const o = [
        {
          type: "button",
          model: {
            commandName: "mergeTableCellUp",
            label: e("Merge cell up")
          }
        },
        {
          type: "button",
          model: {
            commandName: r ? "mergeTableCellRight" : "mergeTableCellLeft",
            label: e("Merge cell right")
          }
        },
        {
          type: "button",
          model: {
            commandName: "mergeTableCellDown",
            label: e("Merge cell down")
          }
        },
        {
          type: "button",
          model: {
            commandName: r ? "mergeTableCellLeft" : "mergeTableCellRight",
            label: e("Merge cell left")
          }
        },
        {
          type: "separator"
        },
        {
          type: "button",
          model: {
            commandName: "splitTableCellVertically",
            label: e("Split cell vertically")
          }
        },
        {
          type: "button",
          model: {
            commandName: "splitTableCellHorizontally",
            label: e("Split cell horizontally")
          }
        }
      ];
      return this._prepareMergeSplitButtonDropdown(e("Merge cells"), iq, o, s);
    });
  }
  /**
  * Creates a dropdown view from a set of options.
  *
  * @param label The dropdown button label.
  * @param icon An icon for the dropdown button.
  * @param options The list of options for the dropdown.
  */
  _prepareDropdown(t, e, n, r) {
    const s = this.editor, o = Yn(r), a = this._fillDropdownWithListOptions(o, n);
    return o.buttonView.set({
      label: t,
      icon: e,
      tooltip: !0
    }), o.bind("isEnabled").toMany(a, "isEnabled", (...l) => l.some((c) => c)), this.listenTo(o, "execute", (l) => {
      s.execute(l.source.commandName), l.source instanceof Gs || s.editing.view.focus();
    }), o;
  }
  /**
  * Creates a dropdown view with a {@link module:ui/dropdown/button/splitbuttonview~SplitButtonView} for
  * merge (and split)–related commands.
  *
  * @param label The dropdown button label.
  * @param icon An icon for the dropdown button.
  * @param options The list of options for the dropdown.
  */
  _prepareMergeSplitButtonDropdown(t, e, n, r) {
    const s = this.editor, o = Yn(r, io), a = "mergeTableCells", l = s.commands.get(a), c = this._fillDropdownWithListOptions(o, n);
    return o.buttonView.set({
      label: t,
      icon: e,
      tooltip: !0,
      isEnabled: !0
    }), o.bind("isEnabled").toMany([
      l,
      ...c
    ], "isEnabled", (...u) => u.some((d) => d)), this.listenTo(o.buttonView, "execute", () => {
      s.execute(a), s.editing.view.focus();
    }), this.listenTo(o, "execute", (u) => {
      s.execute(u.source.commandName), s.editing.view.focus();
    }), o;
  }
  /**
  * Injects a {@link module:ui/list/listview~ListView} into the passed dropdown with buttons
  * which execute editor commands as configured in passed options.
  *
  * @param options The list of options for the dropdown.
  * @returns Commands the list options are interacting with.
  */
  _fillDropdownWithListOptions(t, e) {
    const n = this.editor, r = [], s = new Pn();
    for (const o of e)
      sq(o, n, r, s);
    return pa(t, s), r;
  }
}
function sq(i, t, e, n) {
  if (i.type === "button" || i.type === "switchbutton") {
    const r = i.model = new ia(i.model), { commandName: s, bindIsOn: o } = i.model, a = t.commands.get(s);
    e.push(a), r.set({
      commandName: s
    }), r.bind("isEnabled").to(a), o && r.bind("isOn").to(a, "value"), r.set({
      withText: !0
    });
  }
  n.add(i);
}
class $s extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableSelection";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Sn,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model, n = t.editing.view;
    this.listenTo(e, "deleteContent", (r, s) => this._handleDeleteContent(r, s), {
      priority: "high"
    }), this.listenTo(n.document, "insertText", (r, s) => this._handleInsertTextEvent(r, s), {
      priority: "high"
    }), this._defineSelectionConverter(), this._enablePluginDisabling();
  }
  /**
  * Returns the currently selected table cells or `null` if it is not a table cells selection.
  */
  getSelectedTableCells() {
    const t = this.editor.plugins.get(Sn), e = this.editor.model.document.selection, n = t.getSelectedTableCells(e);
    return n.length == 0 ? null : n;
  }
  /**
  * Returns the selected table fragment as a document fragment.
  */
  getSelectionAsFragment() {
    const t = this.editor.plugins.get(Sn), e = this.getSelectedTableCells();
    return e ? this.editor.model.change((n) => {
      const r = n.createDocumentFragment(), { first: s, last: o } = t.getColumnIndexes(e), { first: a, last: l } = t.getRowIndexes(e), c = e[0].findAncestor("table");
      let u = l, d = o;
      if (t.isSelectionRectangular(e)) {
        const g = {
          firstColumn: s,
          lastColumn: o,
          firstRow: a,
          lastRow: l
        };
        u = Fx(c, g), d = $x(c, g);
      }
      const m = Vx(c, {
        startRow: a,
        startColumn: s,
        endRow: u,
        endColumn: d
      }, n);
      return n.insert(m, r, 0), r;
    }) : null;
  }
  /**
  * Sets the model selection based on given anchor and target cells (can be the same cell).
  * Takes care of setting the backward flag.
  *
  * ```ts
  * const modelRoot = editor.model.document.getRoot();
  * const firstCell = modelRoot.getNodeByPath( [ 0, 0, 0 ] );
  * const lastCell = modelRoot.getNodeByPath( [ 0, 0, 1 ] );
  *
  * const tableSelection = editor.plugins.get( 'TableSelection' );
  * tableSelection.setCellSelection( firstCell, lastCell );
  * ```
  */
  setCellSelection(t, e) {
    const n = this._getCellsToSelect(t, e);
    this.editor.model.change((r) => {
      r.setSelection(n.cells.map((s) => r.createRangeOn(s)), {
        backward: n.backward
      });
    });
  }
  /**
  * Returns the focus cell from the current selection.
  */
  getFocusCell() {
    const n = [
      ...this.editor.model.document.selection.getRanges()
    ].pop().getContainedElement();
    return n && n.is("element", "tableCell") ? n : null;
  }
  /**
  * Returns the anchor cell from the current selection.
  */
  getAnchorCell() {
    const t = this.editor.model.document.selection, n = Ft(t.getRanges()).getContainedElement();
    return n && n.is("element", "tableCell") ? n : null;
  }
  /**
  * Defines a selection converter which marks the selected cells with a specific class.
  *
  * The real DOM selection is put in the last cell. Since the order of ranges is dependent on whether the
  * selection is backward or not, the last cell will usually be close to the "focus" end of the selection
  * (a selection has anchor and focus).
  *
  * The real DOM selection is then hidden with CSS.
  */
  _defineSelectionConverter() {
    const t = this.editor, e = /* @__PURE__ */ new Set();
    t.conversion.for("editingDowncast").add((r) => r.on("selection", (s, o, a) => {
      const l = a.writer;
      n(l);
      const c = this.getSelectedTableCells();
      if (!c)
        return;
      for (const d of c) {
        const f = a.mapper.toViewElement(d);
        l.addClass("ck-editor__editable_selected", f), e.add(f);
      }
      const u = a.mapper.toViewElement(c[c.length - 1]);
      l.setSelection(u, 0);
    }, {
      priority: "lowest"
    }));
    function n(r) {
      for (const s of e)
        r.removeClass("ck-editor__editable_selected", s);
      e.clear();
    }
  }
  /**
  * Creates a listener that reacts to changes in {@link #isEnabled} and, if the plugin was disabled,
  * it collapses the multi-cell selection to a regular selection placed inside a table cell.
  *
  * This listener helps features that disable the table selection plugin bring the selection
  * to a clear state they can work with (for instance, because they don't support multiple cell selection).
  */
  _enablePluginDisabling() {
    const t = this.editor;
    this.on("change:isEnabled", () => {
      if (!this.isEnabled) {
        const e = this.getSelectedTableCells();
        if (!e)
          return;
        t.model.change((n) => {
          const r = n.createPositionAt(e[0], 0), s = t.model.schema.getNearestSelectionRange(r);
          n.setSelection(s);
        });
      }
    });
  }
  /**
  * Overrides the default `model.deleteContent()` behavior over a selected table fragment.
  *
  * @param args Delete content method arguments.
  */
  _handleDeleteContent(t, e) {
    const n = this.editor.plugins.get(Sn), r = e[0], s = e[1], o = this.editor.model, a = !s || s.direction == "backward", l = n.getSelectedTableCells(r);
    l.length && (t.stop(), o.change((c) => {
      const u = l[a ? l.length - 1 : 0];
      o.change((f) => {
        for (const m of l)
          o.deleteContent(f.createSelection(m, "in"));
      });
      const d = o.schema.getNearestSelectionRange(c.createPositionAt(u, 0));
      r.is("documentSelection") ? c.setSelection(d) : r.setTo(d);
    }));
  }
  /**
  * This handler makes it possible to remove the content of all selected cells by starting to type.
  * If you take a look at {@link #_defineSelectionConverter} you will find out that despite the multi-cell selection being set
  * in the model, the view selection is collapsed in the last cell (because most browsers are unable to render multi-cell selections;
  * yes, it's a hack).
  *
  * When multiple cells are selected in the model and the user starts to type, the
  * {@link module:engine/view/document~Document#event:insertText} event carries information provided by the
  * beforeinput DOM  event, that in turn only knows about this collapsed DOM selection in the last cell.
  *
  * As a result, the selected cells have no chance to be cleaned up. To fix this, this listener intercepts
  * the event and injects the custom view selection in the data that translates correctly to the actual state
  * of the multi-cell selection in the model.
  *
  * @param data Insert text event data.
  */
  _handleInsertTextEvent(t, e) {
    const n = this.editor, r = this.getSelectedTableCells();
    if (!r)
      return;
    const s = n.editing.view, o = n.editing.mapper, a = r.map((l) => s.createRangeOn(o.toViewElement(l)));
    e.selection = s.createSelection(a);
  }
  /**
  * Returns an array of table cells that should be selected based on the
  * given anchor cell and target (focus) cell.
  *
  * The cells are returned in a reverse direction if the selection is backward.
  */
  _getCellsToSelect(t, e) {
    const n = this.editor.plugins.get("TableUtils"), r = n.getCellLocation(t), s = n.getCellLocation(e), o = Math.min(r.row, s.row), a = Math.max(r.row, s.row), l = Math.min(r.column, s.column), c = Math.max(r.column, s.column), u = new Array(a - o + 1).fill(null).map(() => []), d = {
      startRow: o,
      endRow: a,
      startColumn: l,
      endColumn: c
    };
    for (const { row: g, cell: p } of new ut(t.findAncestor("table"), d))
      u[g - o].push(p);
    const f = s.row < r.row, m = s.column < r.column;
    return f && u.reverse(), m && u.forEach((g) => g.reverse()), {
      cells: u.flat(),
      backward: f || m
    };
  }
}
class oq extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableClipboard";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      gl,
      ui,
      $s,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.editing.view.document;
    this.listenTo(e, "copy", (n, r) => this._onCopyCut(n, r)), this.listenTo(e, "cut", (n, r) => this._onCopyCut(n, r)), this.listenTo(t.model, "insertContent", (n, [r, s]) => this._onInsertContent(n, r, s), {
      priority: "high"
    }), this.decorate("_replaceTableSlotCell");
  }
  /**
  * Copies table content to a clipboard on "copy" & "cut" events.
  *
  * @param evt An object containing information about the handled event.
  * @param data Clipboard event data.
  */
  _onCopyCut(t, e) {
    const n = this.editor.editing.view, r = this.editor.plugins.get($s), s = this.editor.plugins.get(gl);
    r.getSelectedTableCells() && (t.name == "cut" && !this.editor.model.canEditAt(this.editor.model.document.selection) || (e.preventDefault(), t.stop(), this.editor.model.enqueueChange({
      isUndoable: t.name === "cut"
    }, () => {
      const o = s._copySelectedFragmentWithMarkers(t.name, this.editor.model.document.selection, () => r.getSelectionAsFragment());
      n.document.fire("clipboardOutput", {
        dataTransfer: e.dataTransfer,
        content: this.editor.data.toView(o),
        method: t.name
      });
    })));
  }
  /**
  * Overrides default {@link module:engine/model/model~Model#insertContent `model.insertContent()`} method to handle pasting table inside
  * selected table fragment.
  *
  * Depending on selected table fragment:
  * - If a selected table fragment is smaller than paste table it will crop pasted table to match dimensions.
  * - If dimensions are equal it will replace selected table fragment with a pasted table contents.
  *
  * @param content The content to insert.
  * @param selectable The selection into which the content should be inserted.
  * If not provided the current model document selection will be used.
  */
  _onInsertContent(t, e, n) {
    if (n && !n.is("documentSelection"))
      return;
    const r = this.editor.model, s = this.editor.plugins.get(Sn), o = this.editor.plugins.get(gl), a = this.getTableIfOnlyTableInContent(e, r);
    if (!a)
      return;
    const l = s.getSelectionAffectedTableCells(r.document.selection);
    if (!l.length) {
      zp(a, s);
      return;
    }
    t.stop(), e.is("documentFragment") ? o._pasteMarkersIntoTransformedElement(e.markers, (c) => this._replaceSelectedCells(a, l, c)) : this.editor.model.change((c) => {
      this._replaceSelectedCells(a, l, c);
    });
  }
  /**
  * Inserts provided `selectedTableCells` into `pastedTable`.
  */
  _replaceSelectedCells(t, e, n) {
    const r = this.editor.plugins.get(Sn), s = {
      width: r.getColumns(t),
      height: r.getRows(t)
    }, o = aq(e, s, n, r), a = o.lastRow - o.firstRow + 1, l = o.lastColumn - o.firstColumn + 1, c = {
      startRow: 0,
      startColumn: 0,
      endRow: Math.min(a, s.height) - 1,
      endColumn: Math.min(l, s.width) - 1
    };
    t = Vx(t, c, n);
    const u = e[0].findAncestor("table"), d = this._replaceSelectedCellsWithPasted(t, s, u, o, n);
    if (this.editor.plugins.get("TableSelection").isEnabled) {
      const f = r.sortRanges(d.map((m) => n.createRangeOn(m)));
      n.setSelection(f);
    } else
      n.setSelection(d[0], 0);
    return u;
  }
  /**
  * Replaces the part of selectedTable with pastedTable.
  */
  _replaceSelectedCellsWithPasted(t, e, n, r, s) {
    const { width: o, height: a } = e, l = cq(t, o, a), c = [
      ...new ut(n, {
        startRow: r.firstRow,
        endRow: r.lastRow,
        startColumn: r.firstColumn,
        endColumn: r.lastColumn,
        includeAllSlots: !0
      })
    ], u = [];
    let d;
    for (const b of c) {
      const { row: v, column: y } = b;
      y === r.firstColumn && (d = b.getPositionBefore());
      const x = v - r.firstRow, S = y - r.firstColumn, k = l[x % a][S % o], I = k ? s.cloneElement(k) : null, A = this._replaceTableSlotCell(b, I, d, s);
      A && (Bx(A, v, y, r.lastRow, r.lastColumn, s), u.push(A), d = s.createPositionAfter(A));
    }
    const f = parseInt(n.getAttribute("headingRows") || "0"), m = parseInt(n.getAttribute("headingColumns") || "0"), g = r.firstRow < f && f <= r.lastRow, p = r.firstColumn < m && m <= r.lastColumn;
    if (g) {
      const b = {
        first: r.firstColumn,
        last: r.lastColumn
      }, v = Bm(n, f, b, s, r.firstRow);
      u.push(...v);
    }
    if (p) {
      const b = {
        first: r.firstRow,
        last: r.lastRow
      }, v = Fm(n, m, b, s);
      u.push(...v);
    }
    return u;
  }
  /**
  * Replaces a single table slot.
  *
  * @returns Inserted table cell or null if slot should remain empty.
  * @private
  */
  _replaceTableSlotCell(t, e, n, r) {
    const { cell: s, isAnchor: o } = t;
    return o && r.remove(s), e ? (r.insert(e, n), e) : null;
  }
  /**
  * Extracts the table for pasting into a table.
  *
  * @param content The content to insert.
  * @param model The editor model.
  */
  getTableIfOnlyTableInContent(t, e) {
    if (!t.is("documentFragment") && !t.is("element"))
      return null;
    if (t.is("element", "table"))
      return t;
    if (t.childCount == 1 && t.getChild(0).is("element", "table"))
      return t.getChild(0);
    const n = e.createRangeIn(t);
    for (const r of n.getItems())
      if (r.is("element", "table")) {
        const s = e.createRange(n.start, e.createPositionBefore(r));
        if (e.hasContent(s, {
          ignoreWhitespaces: !0
        }))
          return null;
        const o = e.createRange(e.createPositionAfter(r), n.end);
        return e.hasContent(o, {
          ignoreWhitespaces: !0
        }) ? null : r;
      }
    return null;
  }
}
function aq(i, t, e, n) {
  const r = i[0].findAncestor("table"), s = n.getColumnIndexes(i), o = n.getRowIndexes(i), a = {
    firstColumn: s.first,
    lastColumn: s.last,
    firstRow: o.first,
    lastRow: o.last
  }, l = i.length === 1;
  return l && (a.lastRow += t.height - 1, a.lastColumn += t.width - 1, lq(r, a.lastRow + 1, a.lastColumn + 1, n)), l || !n.isSelectionRectangular(i) ? uq(r, a, e) : (a.lastRow = Fx(r, a), a.lastColumn = $x(r, a)), a;
}
function lq(i, t, e, n) {
  const r = n.getColumns(i), s = n.getRows(i);
  e > r && n.insertColumns(i, {
    at: r,
    columns: e - r
  }), t > s && n.insertRows(i, {
    at: s,
    rows: t - s
  });
}
function cq(i, t, e) {
  const n = new Array(e).fill(null).map(() => new Array(t).fill(null));
  for (const { column: r, row: s, cell: o } of new ut(i))
    n[s][r] = o;
  return n;
}
function uq(i, t, e) {
  const { firstRow: n, lastRow: r, firstColumn: s, lastColumn: o } = t, a = {
    first: n,
    last: r
  }, l = {
    first: s,
    last: o
  };
  Fm(i, s, a, e), Fm(i, o + 1, a, e), Bm(i, n, l, e), Bm(i, r + 1, l, e, n);
}
function Bm(i, t, e, n, r = 0) {
  return t < 1 ? void 0 : Mx(i, t, r).filter(({ column: a, cellWidth: l }) => Wx(a, l, e)).map(({ cell: a }) => Dx(a, t, n));
}
function Fm(i, t, e, n) {
  return t < 1 ? void 0 : Lx(i, t).filter(({ row: o, cellHeight: a }) => Wx(o, a, e)).map(({ cell: o, column: a }) => Nx(o, a, t, n));
}
function Wx(i, t, e) {
  const n = i + t - 1, { first: r, last: s } = e, o = i >= r && i <= s, a = i < r && n >= r;
  return o || a;
}
class dq extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableKeyboard";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $s,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, n = t.editing.view.document, r = t.t;
    this.listenTo(n, "arrowKey", (...s) => this._onArrowKey(...s), {
      context: "table"
    }), this.listenTo(n, "tab", (...s) => this._handleTabOnSelectedTable(...s), {
      context: "figure"
    }), this.listenTo(n, "tab", (...s) => this._handleTab(...s), {
      context: [
        "th",
        "td"
      ]
    }), t.accessibility.addKeystrokeInfoGroup({
      id: "table",
      label: r("Keystrokes that can be used in a table cell"),
      keystrokes: [
        {
          label: r("Move the selection to the next cell"),
          keystroke: "Tab"
        },
        {
          label: r("Move the selection to the previous cell"),
          keystroke: "Shift+Tab"
        },
        {
          label: r("Insert a new table row (when in the last cell of a table)"),
          keystroke: "Tab"
        },
        {
          label: r("Navigate through the table"),
          keystroke: [
            [
              "arrowup"
            ],
            [
              "arrowright"
            ],
            [
              "arrowdown"
            ],
            [
              "arrowleft"
            ]
          ]
        }
      ]
    });
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:tab tab} events for the <kbd>Tab</kbd> key executed
  * when the table widget is selected.
  */
  _handleTabOnSelectedTable(t, e) {
    const n = this.editor, s = n.model.document.selection.getSelectedElement();
    !s || !s.is("element", "table") || (e.preventDefault(), e.stopPropagation(), t.stop(), n.model.change((o) => {
      o.setSelection(o.createRangeIn(s.getChild(0).getChild(0)));
    }));
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:tab tab} events for the <kbd>Tab</kbd> key executed
  * inside table cells.
  */
  _handleTab(t, e) {
    const n = this.editor, r = this.editor.plugins.get(Sn), s = this.editor.plugins.get("TableSelection"), o = n.model.document.selection, a = !e.shiftKey;
    let l = r.getTableCellsContainingSelection(o)[0];
    if (l || (l = s.getFocusCell()), !l)
      return;
    e.preventDefault(), e.stopPropagation(), t.stop();
    const c = l.parent, u = c.parent, d = u.getChildIndex(c), f = c.getChildIndex(l), m = f === 0;
    if (!a && m && d === 0) {
      n.model.change((v) => {
        v.setSelection(v.createRangeOn(u));
      });
      return;
    }
    const g = f === c.childCount - 1, p = d === r.getRows(u) - 1;
    if (a && p && g && (n.execute("insertTableRowBelow"), d === r.getRows(u) - 1)) {
      n.model.change((v) => {
        v.setSelection(v.createRangeOn(u));
      });
      return;
    }
    let b;
    if (a && g)
      b = u.getChild(d + 1).getChild(0);
    else if (!a && m) {
      const v = u.getChild(d - 1);
      b = v.getChild(v.childCount - 1);
    } else
      b = c.getChild(f + (a ? 1 : -1));
    n.model.change((v) => {
      v.setSelection(v.createRangeIn(b));
    });
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:keydown keydown} events.
  */
  _onArrowKey(t, e) {
    const n = this.editor, r = e.keyCode, s = Zg(r, n.locale.contentLanguageDirection);
    this._handleArrowKeys(s, e.shiftKey) && (e.preventDefault(), e.stopPropagation(), t.stop());
  }
  /**
  * Handles arrow keys to move the selection around the table.
  *
  * @param direction The direction of the arrow key.
  * @param expandSelection If the current selection should be expanded.
  * @returns Returns `true` if key was handled.
  */
  _handleArrowKeys(t, e) {
    const n = this.editor.plugins.get(Sn), r = this.editor.plugins.get("TableSelection"), s = this.editor.model, o = s.document.selection, a = [
      "right",
      "down"
    ].includes(t), l = n.getSelectedTableCells(o);
    if (l.length) {
      let u;
      return e ? u = r.getFocusCell() : u = a ? l[l.length - 1] : l[0], this._navigateFromCellInDirection(u, t, e), !0;
    }
    const c = o.focus.findAncestor("tableCell");
    /* istanbul ignore if: paranoid check -- @preserve */
    if (!c)
      return !1;
    if (!o.isCollapsed)
      if (e) {
        if (o.isBackward == a && !o.containsEntireContent(c))
          return !1;
      } else {
        const u = o.getSelectedElement();
        if (!u || !s.schema.isObject(u))
          return !1;
      }
    return this._isSelectionAtCellEdge(o, c, a) ? (this._navigateFromCellInDirection(c, t, e), !0) : !1;
  }
  /**
  * Returns `true` if the selection is at the boundary of a table cell according to the navigation direction.
  *
  * @param selection The current selection.
  * @param tableCell The current table cell element.
  * @param isForward The expected navigation direction.
  */
  _isSelectionAtCellEdge(t, e, n) {
    const r = this.editor.model, s = this.editor.model.schema, o = n ? t.getLastPosition() : t.getFirstPosition();
    if (!s.getLimitElement(o).is("element", "tableCell"))
      return r.createPositionAt(e, n ? "end" : 0).isTouching(o);
    const a = r.createSelection(o);
    return r.modifySelection(a, {
      direction: n ? "forward" : "backward"
    }), o.isEqual(a.focus);
  }
  /**
  * Moves the selection from the given table cell in the specified direction.
  *
  * @param focusCell The table cell that is current multi-cell selection focus.
  * @param direction Direction in which selection should move.
  * @param expandSelection If the current selection should be expanded. Default value is false.
  */
  _navigateFromCellInDirection(t, e, n = !1) {
    const r = this.editor.model, s = t.findAncestor("table"), o = [
      ...new ut(s, {
        includeAllSlots: !0
      })
    ], { row: a, column: l } = o[o.length - 1], c = o.find(({ cell: y }) => y == t);
    let { row: u, column: d } = c;
    switch (e) {
      case "left":
        d--;
        break;
      case "up":
        u--;
        break;
      case "right":
        d += c.cellWidth;
        break;
      case "down":
        u += c.cellHeight;
        break;
    }
    const f = u < 0 || u > a, m = d < 0 && u <= 0, g = d > l && u >= a;
    if (f || m || g) {
      r.change((y) => {
        y.setSelection(y.createRangeOn(s));
      });
      return;
    }
    d < 0 ? (d = n ? 0 : l, u--) : d > l && (d = n ? l : 0, u++);
    const p = o.find((y) => y.row == u && y.column == d).cell, b = [
      "right",
      "down"
    ].includes(e), v = this.editor.plugins.get("TableSelection");
    if (n && v.isEnabled) {
      const y = v.getAnchorCell() || t;
      v.setCellSelection(y, p);
    } else {
      const y = r.createPositionAt(p, b ? 0 : "end");
      r.change((x) => {
        x.setSelection(y);
      });
    }
  }
}
class Ux extends ws {
  constructor() {
    super(...arguments);
    h(this, "domEventType", [
      "mousemove",
      "mouseleave"
    ]);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
}
class hq extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableMouse";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $s,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.editing.view.addObserver(Ux), this._enableShiftClickSelection(), this._enableMouseDragSelection();
  }
  /**
  * Enables making cells selection by <kbd>Shift</kbd>+click. Creates a selection from the cell which previously held
  * the selection to the cell which was clicked. It can be the same cell, in which case it selects a single cell.
  */
  _enableShiftClickSelection() {
    const t = this.editor, e = t.plugins.get(Sn);
    let n = !1;
    const r = t.plugins.get($s);
    this.listenTo(t.editing.view.document, "mousedown", (s, o) => {
      const a = t.model.document.selection;
      if (!this.isEnabled || !r.isEnabled || !o.domEvent.shiftKey)
        return;
      const l = r.getAnchorCell() || e.getTableCellsContainingSelection(a)[0];
      if (!l)
        return;
      const c = this._getModelTableCellFromDomEvent(o);
      c && M0(l, c) && (n = !0, r.setCellSelection(l, c), o.preventDefault());
    }), this.listenTo(t.editing.view.document, "mouseup", () => {
      n = !1;
    }), this.listenTo(t.editing.view.document, "selectionChange", (s) => {
      n && s.stop();
    }, {
      priority: "highest"
    });
  }
  /**
  * Enables making cells selection by dragging.
  *
  * The selection is made only on mousemove. Mouse tracking is started on mousedown.
  * However, the cells selection is enabled only after the mouse cursor left the anchor cell.
  * Thanks to that normal text selection within one cell works just fine. However, you can still select
  * just one cell by leaving the anchor cell and moving back to it.
  */
  _enableMouseDragSelection() {
    const t = this.editor;
    let e, n, r = !1, s = !1;
    const o = t.plugins.get($s);
    this.listenTo(t.editing.view.document, "mousedown", (a, l) => {
      !this.isEnabled || !o.isEnabled || l.domEvent.shiftKey || l.domEvent.ctrlKey || l.domEvent.altKey || (e = this._getModelTableCellFromDomEvent(l));
    }), this.listenTo(t.editing.view.document, "mousemove", (a, l) => {
      if (!l.domEvent.buttons || !e)
        return;
      const c = this._getModelTableCellFromDomEvent(l);
      c && M0(e, c) && (n = c, !r && n != e && (r = !0)), r && (s = !0, o.setCellSelection(e, n), l.preventDefault());
    }), this.listenTo(t.editing.view.document, "mouseup", () => {
      r = !1, s = !1, e = null, n = null;
    }), this.listenTo(t.editing.view.document, "selectionChange", (a) => {
      s && a.stop();
    }, {
      priority: "highest"
    });
  }
  /**
  * Returns the model table cell element based on the target element of the passed DOM event.
  *
  * @returns Returns the table cell or `undefined`.
  */
  _getModelTableCellFromDomEvent(t) {
    const e = t.target, n = this.editor.editing.view.createPositionAt(e, 0);
    return this.editor.editing.mapper.toModelPosition(n).parent.findAncestor("tableCell", {
      includeSelf: !0
    });
  }
}
function M0(i, t) {
  return i.parent.parent == t.parent.parent;
}
class fq extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      kc,
      rq,
      $s,
      hq,
      dq,
      oq,
      oo
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Table";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function qx(i) {
  const t = Gx(i);
  return t || rh(i);
}
function Gx(i) {
  const t = i.getSelectedElement();
  return t && Kx(t) ? t : null;
}
function rh(i) {
  const t = i.getFirstPosition();
  if (!t)
    return null;
  let e = t.parent;
  for (; e; ) {
    if (e.is("element") && Kx(e))
      return e;
    e = e.parent;
  }
  return null;
}
function Kx(i) {
  return !!i.getCustomProperty("table") && Gt(i);
}
class mq extends V {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Zu
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.t, n = t.plugins.get(Zu), r = t.config.get("table.contentToolbar"), s = t.config.get("table.tableToolbar");
    r && n.register("tableContent", {
      ariaLabel: e("Table toolbar"),
      items: r,
      getRelatedElement: rh
    }), s && n.register("table", {
      ariaLabel: e("Table toolbar"),
      items: s,
      getRelatedElement: Gx
    });
  }
}
class gq extends de {
  /**
  * Creates an instance of the color input view.
  *
  * @param locale The locale instance.
  * @param options The input options.
  * @param options.colorDefinitions The colors to be displayed in the palette inside the input's dropdown.
  * @param options.columns The number of columns in which the colors will be displayed.
  * @param options.defaultColorValue If specified, the color input view will replace the "Remove color" button with
  * the "Restore default" button. Instead of clearing the input field, the default color value will be set.
  */
  constructor(e, n) {
    super(e);
    /**
    * A cached reference to the options passed to the constructor.
    */
    h(this, "options");
    /**
    * Tracks information about the DOM focus in the view.
    */
    h(this, "focusTracker");
    /**
    * Helps cycling over focusable children in the input view.
    */
    h(this, "focusCycler");
    /**
    * A collection of views that can be focused in the view.
    */
    h(this, "_focusables");
    /**
    * An instance of the dropdown allowing to select a color from a grid.
    */
    h(this, "dropdownView");
    /**
    * An instance of the input allowing the user to type a color value.
    */
    h(this, "inputView");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * The flag that indicates whether the user is still typing.
    * If set to true, it means that the text input field ({@link #inputView}) still has the focus.
    * So, we should interrupt the user by replacing the input's value.
    */
    h(this, "_stillTyping");
    this.set("value", ""), this.set("isReadOnly", !1), this.set("isFocused", !1), this.set("isEmpty", !0), this.options = n, this.focusTracker = new wt(), this._focusables = new In(), this.dropdownView = this._createDropdownView(), this.inputView = this._createInputTextView(), this.keystrokes = new Ot(), this._stillTyping = !1, this.focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate items backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: "shift + tab",
        // Navigate items forwards using the <kbd>Tab</kbd> key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-input-color"
        ]
      },
      children: [
        this.dropdownView,
        this.inputView
      ]
    }), this.on("change:value", (r, s, o) => this._setInputValue(o));
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), [
      this.inputView,
      this.dropdownView.buttonView
    ].forEach((e) => {
      this.focusTracker.add(e.element), this._focusables.add(e);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * Focuses the view.
  */
  focus(e) {
    e === -1 ? this.focusCycler.focusLast() : this.focusCycler.focusFirst();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates and configures the {@link #dropdownView}.
  */
  _createDropdownView() {
    const e = this.locale, n = e.t, r = this.bindTemplate, s = this._createColorSelector(e), o = Yn(e), a = new de();
    return a.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-input-color__button__preview"
        ],
        style: {
          backgroundColor: r.to("value")
        }
      },
      children: [
        {
          tag: "span",
          attributes: {
            class: [
              "ck",
              "ck-input-color__button__preview__no-color-indicator",
              r.if("value", "ck-hidden", (l) => l != "")
            ]
          }
        }
      ]
    }), o.buttonView.extendTemplate({
      attributes: {
        class: "ck-input-color__button"
      }
    }), o.buttonView.children.add(a), o.buttonView.label = n("Color picker"), o.buttonView.tooltip = !0, o.panelPosition = e.uiLanguageDirection === "rtl" ? "se" : "sw", o.panelView.children.add(s), o.bind("isEnabled").to(this, "isReadOnly", (l) => !l), o.on("change:isOpen", (l, c, u) => {
      u && (s.updateSelectedColors(), s.showColorGridsFragment());
    }), o;
  }
  /**
  * Creates and configures an instance of {@link module:ui/inputtext/inputtextview~InputTextView}.
  *
  * @returns A configured instance to be set as {@link #inputView}.
  */
  _createInputTextView() {
    const e = this.locale, n = new KC(e);
    return n.extendTemplate({
      on: {
        blur: n.bindTemplate.to("blur")
      }
    }), n.value = this.value, n.bind("isReadOnly", "hasError").to(this), this.bind("isFocused", "isEmpty").to(n), n.on("input", () => {
      const r = n.element.value, s = this.options.colorDefinitions.find((o) => r === o.label);
      this._stillTyping = !0, this.value = s && s.color || r;
    }), n.on("blur", () => {
      this._stillTyping = !1, this._setInputValue(n.element.value);
    }), n.delegate("input").to(this), n;
  }
  /**
  * Creates and configures the panel with "color grid" and "color picker" inside the {@link #dropdownView}.
  */
  _createColorSelector(e) {
    const n = e.t, r = this.options.defaultColorValue || "", s = n(r ? "Restore default" : "Remove color"), o = new ZM(e, {
      colors: this.options.colorDefinitions,
      columns: this.options.columns,
      removeButtonLabel: s,
      colorPickerLabel: n("Color picker"),
      colorPickerViewConfig: this.options.colorPickerConfig === !1 ? !1 : {
        ...this.options.colorPickerConfig,
        hideInput: !0
      }
    });
    o.appendUI(), o.on("execute", (l, c) => {
      if (c.source === "colorPickerSaveButton") {
        this.dropdownView.isOpen = !1;
        return;
      }
      this.value = c.value || r, this.fire("input"), c.source !== "colorPicker" && (this.dropdownView.isOpen = !1);
    });
    let a = this.value;
    return o.on("colorPicker:cancel", () => {
      this.value = a, this.fire("input"), this.dropdownView.isOpen = !1;
    }), o.colorGridsFragmentView.colorPickerButtonView.on("execute", () => {
      a = this.value;
    }), o.bind("selectedColor").to(this, "value"), o;
  }
  /**
  * Sets {@link #inputView}'s value property to the color value or color label,
  * if there is one and the user is not typing.
  *
  * Handles cases like:
  *
  * * Someone picks the color in the grid.
  * * The color is set from the plugin level.
  *
  * @param inputValue Color value to be set.
  */
  _setInputValue(e) {
    if (!this._stillTyping) {
      const n = D0(e), r = this.options.colorDefinitions.find((s) => n === D0(s.color));
      r ? this.inputView.value = r.label : this.inputView.value = e || "";
    }
  }
}
function D0(i) {
  return i.replace(/([(,])\s+/g, "$1").replace(/^\s+|\s+(?=[),\s]|$)/g, "").replace(/,|\s/g, " ");
}
const jp = (i) => i === "";
function Wp(i) {
  return {
    none: i("None"),
    solid: i("Solid"),
    dotted: i("Dotted"),
    dashed: i("Dashed"),
    double: i("Double"),
    groove: i("Groove"),
    ridge: i("Ridge"),
    inset: i("Inset"),
    outset: i("Outset")
  };
}
function Zx(i) {
  return i('The color is invalid. Try "#FF0000" or "rgb(255,0,0)" or "red".');
}
function Yx(i) {
  return i('The value is invalid. Try "10px" or "2em" or simply "2".');
}
function ld(i) {
  return i = i.trim().toLowerCase(), jp(i) || _C(i);
}
function bl(i) {
  return i = i.trim(), jp(i) || Qx(i) || cp(i) || L8(i);
}
function Jx(i) {
  return i = i.trim(), jp(i) || Qx(i) || cp(i);
}
function Xx(i, t) {
  const e = new Pn(), n = Wp(i.t);
  for (const r in n) {
    const s = {
      type: "button",
      model: new ia({
        _borderStyleValue: r,
        label: n[r],
        role: "menuitemradio",
        withText: !0
      })
    };
    r === "none" ? s.model.bind("isOn").to(i, "borderStyle", (o) => t === "none" ? !o : o === r) : s.model.bind("isOn").to(i, "borderStyle", (o) => o === r), e.add(s);
  }
  return e;
}
function $m(i) {
  const { view: t, icons: e, toolbar: n, labels: r, propertyName: s, nameToValue: o, defaultValue: a } = i;
  for (const l in r) {
    const c = new Te(t.locale);
    c.set({
      label: r[l],
      icon: e[l],
      tooltip: r[l]
    });
    const u = o ? o(l) : l;
    c.bind("isOn").to(t, s, (d) => {
      let f = d;
      return d === "" && a && (f = a), u === f;
    }), c.on("execute", () => {
      t[s] = u;
    }), n.items.add(c);
  }
}
const cd = [
  {
    color: "hsl(0, 0%, 0%)",
    label: "Black"
  },
  {
    color: "hsl(0, 0%, 30%)",
    label: "Dim grey"
  },
  {
    color: "hsl(0, 0%, 60%)",
    label: "Grey"
  },
  {
    color: "hsl(0, 0%, 90%)",
    label: "Light grey"
  },
  {
    color: "hsl(0, 0%, 100%)",
    label: "White",
    hasBorder: !0
  },
  {
    color: "hsl(0, 75%, 60%)",
    label: "Red"
  },
  {
    color: "hsl(30, 75%, 60%)",
    label: "Orange"
  },
  {
    color: "hsl(60, 75%, 60%)",
    label: "Yellow"
  },
  {
    color: "hsl(90, 75%, 60%)",
    label: "Light green"
  },
  {
    color: "hsl(120, 75%, 60%)",
    label: "Green"
  },
  {
    color: "hsl(150, 75%, 60%)",
    label: "Aquamarine"
  },
  {
    color: "hsl(180, 75%, 60%)",
    label: "Turquoise"
  },
  {
    color: "hsl(210, 75%, 60%)",
    label: "Light blue"
  },
  {
    color: "hsl(240, 75%, 60%)",
    label: "Blue"
  },
  {
    color: "hsl(270, 75%, 60%)",
    label: "Purple"
  }
];
function ud(i) {
  return (t, e, n) => {
    const r = new gq(t.locale, {
      colorDefinitions: pq(i.colorConfig),
      columns: i.columns,
      defaultColorValue: i.defaultColorValue,
      colorPickerConfig: i.colorPickerConfig
    });
    return r.inputView.set({
      id: e,
      ariaDescribedById: n
    }), r.bind("isReadOnly").to(t, "isEnabled", (s) => !s), r.bind("hasError").to(t, "errorText", (s) => !!s), r.on("input", () => {
      t.errorText = null;
    }), t.bind("isEmpty", "isFocused").to(r), r;
  };
}
function Qx(i) {
  const t = parseFloat(i);
  return !Number.isNaN(t) && i === String(t);
}
function pq(i) {
  return i.map((t) => ({
    color: t.model,
    label: t.label,
    options: {
      hasBorder: t.hasBorder
    }
  }));
}
class gi extends de {
  /**
  * Creates an instance of the form row class.
  *
  * @param locale The locale instance.
  * @param options.labelView When passed, the row gets the `group` and `aria-labelledby`
  * DOM attributes and gets described by the label.
  */
  constructor(e, n = {}) {
    super(e);
    /**
    * A collection of row items (buttons, dropdowns, etc.).
    */
    h(this, "children");
    const r = this.bindTemplate;
    this.set("class", n.class || null), this.children = this.createCollection(), n.children && n.children.forEach((s) => this.children.add(s)), this.set("_role", null), this.set("_ariaLabelledBy", null), n.labelView && this.set({
      _role: "group",
      _ariaLabelledBy: n.labelView.id
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-form__row",
          r.to("class")
        ],
        role: r.to("_role"),
        "aria-labelledby": r.to("_ariaLabelledBy")
      },
      children: this.children
    });
  }
}
class bq extends de {
  /**
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param options Additional configuration of the view.
  * @param options.borderColors A configuration of the border color palette used by the
  * {@link module:table/tablecellproperties/ui/tablecellpropertiesview~TableCellPropertiesView#borderColorInput}.
  * @param options.backgroundColors A configuration of the background color palette used by the
  * {@link module:table/tablecellproperties/ui/tablecellpropertiesview~TableCellPropertiesView#backgroundInput}.
  * @param options.defaultTableCellProperties The default table cell properties.
  */
  constructor(e, n) {
    super(e);
    /**
    * Options passed to the view. See {@link #constructor} to learn more.
    */
    h(this, "options");
    /**
    * Tracks information about the DOM focus in the form.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * A collection of child views in the form.
    */
    h(this, "children");
    /**
    * A dropdown that allows selecting the style of the table cell border.
    */
    h(this, "borderStyleDropdown");
    /**
    * An input that allows specifying the width of the table cell border.
    */
    h(this, "borderWidthInput");
    /**
    * An input that allows specifying the color of the table cell border.
    */
    h(this, "borderColorInput");
    /**
    * An input that allows specifying the table cell background color.
    */
    h(this, "backgroundInput");
    /**
    * An input that allows specifying the table cell padding.
    */
    h(this, "paddingInput");
    /**
    * An input that allows specifying the table cell width.
    */
    h(this, "widthInput");
    /**
    * An input that allows specifying the table cell height.
    */
    h(this, "heightInput");
    /**
    * A toolbar with buttons that allow changing the horizontal text alignment in a table cell.
    */
    h(this, "horizontalAlignmentToolbar");
    /**
    * A toolbar with buttons that allow changing the vertical text alignment in a table cell.
    */
    h(this, "verticalAlignmentToolbar");
    /**
    * The "Save" button view.
    */
    h(this, "saveButtonView");
    /**
    * The "Cancel" button view.
    */
    h(this, "cancelButtonView");
    /**
    * A collection of views that can be focused in the form.
    */
    h(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    h(this, "_focusCycler");
    this.set({
      borderStyle: "",
      borderWidth: "",
      borderColor: "",
      padding: "",
      backgroundColor: "",
      width: "",
      height: "",
      horizontalAlignment: "",
      verticalAlignment: ""
    }), this.options = n;
    const { borderStyleDropdown: r, borderWidthInput: s, borderColorInput: o, borderRowLabel: a } = this._createBorderFields(), { backgroundRowLabel: l, backgroundInput: c } = this._createBackgroundFields(), { widthInput: u, operatorLabel: d, heightInput: f, dimensionsLabel: m } = this._createDimensionFields(), { horizontalAlignmentToolbar: g, verticalAlignmentToolbar: p, alignmentLabel: b } = this._createAlignmentFields();
    this.focusTracker = new wt(), this.keystrokes = new Ot(), this.children = this.createCollection(), this.borderStyleDropdown = r, this.borderWidthInput = s, this.borderColorInput = o, this.backgroundInput = c, this.paddingInput = this._createPaddingField(), this.widthInput = u, this.heightInput = f, this.horizontalAlignmentToolbar = g, this.verticalAlignmentToolbar = p;
    const { saveButtonView: v, cancelButtonView: y } = this._createActionButtons();
    this.saveButtonView = v, this.cancelButtonView = y, this._focusables = new In(), this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.children.add(new Wd(e, {
      label: this.t("Cell properties")
    })), this.children.add(new gi(e, {
      labelView: a,
      children: [
        a,
        r,
        o,
        s
      ],
      class: "ck-table-form__border-row"
    })), this.children.add(new gi(e, {
      labelView: l,
      children: [
        l,
        c
      ],
      class: "ck-table-form__background-row"
    })), this.children.add(new gi(e, {
      children: [
        // Dimensions row.
        new gi(e, {
          labelView: m,
          children: [
            m,
            u,
            d,
            f
          ],
          class: "ck-table-form__dimensions-row"
        }),
        // Padding row.
        new gi(e, {
          children: [
            this.paddingInput
          ],
          class: "ck-table-cell-properties-form__padding-row"
        })
      ]
    })), this.children.add(new gi(e, {
      labelView: b,
      children: [
        b,
        g,
        p
      ],
      class: "ck-table-cell-properties-form__alignment-row"
    })), this.children.add(new gi(e, {
      children: [
        this.saveButtonView,
        this.cancelButtonView
      ],
      class: "ck-table-form__action-row"
    })), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-form",
          "ck-table-form",
          "ck-table-cell-properties-form"
        ],
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Cs({
      view: this
    }), [
      this.borderColorInput,
      this.backgroundInput
    ].forEach((e) => {
      this._focusCycler.chain(e.fieldView.focusCycler);
    }), [
      this.borderStyleDropdown,
      this.borderColorInput,
      this.borderWidthInput,
      this.backgroundInput,
      this.widthInput,
      this.heightInput,
      this.paddingInput,
      this.horizontalAlignmentToolbar,
      this.verticalAlignmentToolbar,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((e) => {
      this._focusables.add(e), this.focusTracker.add(e.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist focusable field in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #borderStyleDropdown},
  * * {@link #borderWidthInput},
  * * {@link #borderColorInput}.
  */
  _createBorderFields() {
    const e = this.options.defaultTableCellProperties, n = {
      style: e.borderStyle,
      width: e.borderWidth,
      color: e.borderColor
    }, r = ud({
      colorConfig: this.options.borderColors,
      columns: 5,
      defaultColorValue: n.color,
      colorPickerConfig: this.options.colorPickerConfig
    }), s = this.locale, o = this.t, a = o("Style"), l = new Oi(s);
    l.text = o("Border");
    const c = Wp(o), u = new Qt(s, QC);
    u.set({
      label: a,
      class: "ck-table-form__border-style"
    }), u.fieldView.buttonView.set({
      ariaLabel: a,
      ariaLabelledBy: void 0,
      isOn: !1,
      withText: !0,
      tooltip: a
    }), u.fieldView.buttonView.bind("label").to(this, "borderStyle", (m) => c[m || "none"]), u.fieldView.on("execute", (m) => {
      this.borderStyle = m.source._borderStyleValue;
    }), u.bind("isEmpty").to(this, "borderStyle", (m) => !m), pa(u.fieldView, Xx(this, n.style), {
      role: "menu",
      ariaLabel: a
    });
    const d = new Qt(s, yi);
    d.set({
      label: o("Width"),
      class: "ck-table-form__border-width"
    }), d.fieldView.bind("value").to(this, "borderWidth"), d.bind("isEnabled").to(this, "borderStyle", eu), d.fieldView.on("input", () => {
      this.borderWidth = d.fieldView.element.value;
    });
    const f = new Qt(s, r);
    return f.set({
      label: o("Color"),
      class: "ck-table-form__border-color"
    }), f.fieldView.bind("value").to(this, "borderColor"), f.bind("isEnabled").to(this, "borderStyle", eu), f.fieldView.on("input", () => {
      this.borderColor = f.fieldView.value;
    }), this.on("change:borderStyle", (m, g, p, b) => {
      eu(p) || (this.borderColor = "", this.borderWidth = ""), eu(b) || (this.borderColor = n.color, this.borderWidth = n.width);
    }), {
      borderRowLabel: l,
      borderStyleDropdown: u,
      borderColorInput: f,
      borderWidthInput: d
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #backgroundInput}.
  */
  _createBackgroundFields() {
    const e = this.locale, n = this.t, r = new Oi(e);
    r.text = n("Background");
    const s = ud({
      colorConfig: this.options.backgroundColors,
      columns: 5,
      defaultColorValue: this.options.defaultTableCellProperties.backgroundColor,
      colorPickerConfig: this.options.colorPickerConfig
    }), o = new Qt(e, s);
    return o.set({
      label: n("Color"),
      class: "ck-table-cell-properties-form__background"
    }), o.fieldView.bind("value").to(this, "backgroundColor"), o.fieldView.on("input", () => {
      this.backgroundColor = o.fieldView.value;
    }), {
      backgroundRowLabel: r,
      backgroundInput: o
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #widthInput}.
  * * {@link #heightInput}.
  */
  _createDimensionFields() {
    const e = this.locale, n = this.t, r = new Oi(e);
    r.text = n("Dimensions");
    const s = new Qt(e, yi);
    s.set({
      label: n("Width"),
      class: "ck-table-form__dimensions-row__width"
    }), s.fieldView.bind("value").to(this, "width"), s.fieldView.on("input", () => {
      this.width = s.fieldView.element.value;
    });
    const o = new de(e);
    o.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck-table-form__dimension-operator"
        ]
      },
      children: [
        {
          text: "×"
        }
      ]
    });
    const a = new Qt(e, yi);
    return a.set({
      label: n("Height"),
      class: "ck-table-form__dimensions-row__height"
    }), a.fieldView.bind("value").to(this, "height"), a.fieldView.on("input", () => {
      this.height = a.fieldView.element.value;
    }), {
      dimensionsLabel: r,
      widthInput: s,
      operatorLabel: o,
      heightInput: a
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #paddingInput}.
  */
  _createPaddingField() {
    const e = this.locale, n = this.t, r = new Qt(e, yi);
    return r.set({
      label: n("Padding"),
      class: "ck-table-cell-properties-form__padding"
    }), r.fieldView.bind("value").to(this, "padding"), r.fieldView.on("input", () => {
      this.padding = r.fieldView.element.value;
    }), r;
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #horizontalAlignmentToolbar},
  * * {@link #verticalAlignmentToolbar}.
  */
  _createAlignmentFields() {
    const e = this.locale, n = this.t, r = new Oi(e), s = {
      left: ee.alignLeft,
      center: ee.alignCenter,
      right: ee.alignRight,
      justify: ee.alignJustify,
      top: ee.alignTop,
      middle: ee.alignMiddle,
      bottom: ee.alignBottom
    };
    r.text = n("Table cell text alignment");
    const o = new fs(e), a = e.contentLanguageDirection === "rtl";
    o.set({
      isCompact: !0,
      ariaLabel: n("Horizontal text alignment toolbar")
    }), $m({
      view: this,
      icons: s,
      toolbar: o,
      labels: this._horizontalAlignmentLabels,
      propertyName: "horizontalAlignment",
      nameToValue: (c) => {
        if (a) {
          if (c === "left")
            return "right";
          if (c === "right")
            return "left";
        }
        return c;
      },
      defaultValue: this.options.defaultTableCellProperties.horizontalAlignment
    });
    const l = new fs(e);
    return l.set({
      isCompact: !0,
      ariaLabel: n("Vertical text alignment toolbar")
    }), $m({
      view: this,
      icons: s,
      toolbar: l,
      labels: this._verticalAlignmentLabels,
      propertyName: "verticalAlignment",
      defaultValue: this.options.defaultTableCellProperties.verticalAlignment
    }), {
      horizontalAlignmentToolbar: o,
      verticalAlignmentToolbar: l,
      alignmentLabel: r
    };
  }
  /**
  * Creates the following form controls:
  *
  * * {@link #saveButtonView},
  * * {@link #cancelButtonView}.
  */
  _createActionButtons() {
    const e = this.locale, n = this.t, r = new Te(e), s = new Te(e), o = [
      this.borderWidthInput,
      this.borderColorInput,
      this.backgroundInput,
      this.paddingInput
    ];
    return r.set({
      label: n("Save"),
      icon: ee.check,
      class: "ck-button-save",
      type: "submit",
      withText: !0
    }), r.bind("isEnabled").toMany(o, "errorText", (...a) => a.every((l) => !l)), s.set({
      label: n("Cancel"),
      icon: ee.cancel,
      class: "ck-button-cancel",
      withText: !0
    }), s.delegate("execute").to(this, "cancel"), {
      saveButtonView: r,
      cancelButtonView: s
    };
  }
  /**
  * Provides localized labels for {@link #horizontalAlignmentToolbar} buttons.
  */
  get _horizontalAlignmentLabels() {
    const e = this.locale, n = this.t, r = n("Align cell text to the left"), s = n("Align cell text to the center"), o = n("Align cell text to the right"), a = n("Justify cell text");
    return e.uiLanguageDirection === "rtl" ? {
      right: o,
      center: s,
      left: r,
      justify: a
    } : {
      left: r,
      center: s,
      right: o,
      justify: a
    };
  }
  /**
  * Provides localized labels for {@link #verticalAlignmentToolbar} buttons.
  */
  get _verticalAlignmentLabels() {
    const e = this.t;
    return {
      top: e("Align cell text to the top"),
      middle: e("Align cell text to the middle"),
      bottom: e("Align cell text to the bottom")
    };
  }
}
function eu(i) {
  return i !== "none";
}
const zm = [
  fn.defaultPositions.northArrowSouth,
  fn.defaultPositions.northArrowSouthWest,
  fn.defaultPositions.northArrowSouthEast,
  fn.defaultPositions.southArrowNorth,
  fn.defaultPositions.southArrowNorthWest,
  fn.defaultPositions.southArrowNorthEast,
  fn.defaultPositions.viewportStickyNorth
];
function eE(i, t) {
  const e = i.plugins.get("ContextualBalloon"), n = i.editing.view.document.selection;
  let r;
  t === "cell" ? rh(n) && (r = nE(i)) : qx(n) && (r = tE(i)), r && e.updatePosition(r);
}
function tE(i) {
  const t = i.model.document.selection, e = Fs(t), n = i.editing.mapper.toViewElement(e);
  return {
    target: i.editing.view.domConverter.mapViewToDom(n),
    positions: zm
  };
}
function nE(i) {
  const t = i.editing.mapper, e = i.editing.view.domConverter, n = i.model.document.selection;
  if (n.rangeCount > 1)
    return {
      target: () => vq(n.getRanges(), i),
      positions: zm
    };
  const r = iE(n.getFirstPosition()), s = t.toViewElement(r);
  return {
    target: e.mapViewToDom(s),
    positions: zm
  };
}
function iE(i) {
  return i.nodeAfter && i.nodeAfter.is("element", "tableCell") ? i.nodeAfter : i.findAncestor("tableCell");
}
function vq(i, t) {
  const e = t.editing.mapper, n = t.editing.view.domConverter, r = Array.from(i).map((s) => {
    const o = iE(s.start), a = e.toViewElement(o);
    return new je(n.mapViewToDom(a));
  });
  return je.getBoundingRect(r);
}
var wq = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.105 18-.17 1H2.5A1.5 1.5 0 0 1 1 17.5v-15A1.5 1.5 0 0 1 2.5 1h15A1.5 1.5 0 0 1 19 2.5v9.975l-.85-.124-.15-.302V8h-5v4h.021l-.172.351-1.916.28-.151.027c-.287.063-.54.182-.755.341L8 13v5h3.105zM2 12h5V8H2v4zm10-4H8v4h4V8zM2 2v5h5V2H2zm0 16h5v-5H2v5zM13 7h5V2h-5v5zM8 2v5h4V2H8z" opacity=".6"/><path d="m15.5 11.5 1.323 2.68 2.957.43-2.14 2.085.505 2.946L15.5 18.25l-2.645 1.39.505-2.945-2.14-2.086 2.957-.43L15.5 11.5zM13 6a1 1 0 0 1 1 1v3.172a2.047 2.047 0 0 0-.293.443l-.858 1.736-1.916.28-.151.027A1.976 1.976 0 0 0 9.315 14H7a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h6zm-1 2H8v4h4V8z"/></svg>';
function co(i) {
  if (!i || !vt(i))
    return i;
  const { top: t, right: e, bottom: n, left: r } = i;
  if (t == e && e == n && n == r)
    return t;
}
function uo(i, t) {
  const e = parseFloat(i);
  return Number.isNaN(e) || String(e) !== String(i) ? i : `${e}${t}`;
}
function rE(i, t = {}) {
  const e = {
    borderStyle: "none",
    borderWidth: "",
    borderColor: "",
    backgroundColor: "",
    width: "",
    height: "",
    ...i
  };
  return t.includeAlignmentProperty && !e.alignment && (e.alignment = "center"), t.includePaddingProperty && !e.padding && (e.padding = ""), t.includeVerticalAlignmentProperty && !e.verticalAlignment && (e.verticalAlignment = "middle"), t.includeHorizontalAlignmentProperty && !e.horizontalAlignment && (e.horizontalAlignment = t.isRightToLeftContent ? "right" : "left"), e;
}
function sE(i, t) {
  return rE({
    // It adds support for border none in the table element, keep it in sync with the content styles
    // See more: https://github.com/ckeditor/ckeditor5/issues/6841#issuecomment-1959195608
    borderStyle: "double",
    borderColor: "hsl(0, 0%, 70%)",
    borderWidth: "1px",
    ...i
  }, t);
}
function Up(i, t) {
  return rE({
    // It adds support for border none in the table element, keep it in sync with the content styles
    // See more: https://github.com/ckeditor/ckeditor5/issues/6841#issuecomment-1959195608
    borderStyle: "solid",
    borderColor: "hsl(0, 0%, 75%)",
    borderWidth: "1px",
    ...i
  }, t);
}
const yq = 500, L0 = {
  borderStyle: "tableCellBorderStyle",
  borderColor: "tableCellBorderColor",
  borderWidth: "tableCellBorderWidth",
  height: "tableCellHeight",
  width: "tableCellWidth",
  padding: "tableCellPadding",
  backgroundColor: "tableCellBackgroundColor",
  horizontalAlignment: "tableCellHorizontalAlignment",
  verticalAlignment: "tableCellVerticalAlignment"
};
class _q extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The default table cell properties.
    */
    h(this, "_defaultTableCellProperties");
    /**
    * The contextual balloon plugin instance.
    */
    h(this, "_balloon");
    /**
    * The cell properties form view displayed inside the balloon.
    */
    h(this, "view");
    /**
    * The batch used to undo all changes made by the form (which are live, as the user types)
    * when "Cancel" was pressed. Each time the view is shown, a new batch is created.
    */
    h(this, "_undoStepBatch");
    /**
    * Flag used to indicate whether view is ready to execute update commands
    * (it finished loading initial data).
    */
    h(this, "_isReady");
    e.config.define("table.tableCellProperties", {
      borderColors: cd,
      backgroundColors: cd
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Qi
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCellPropertiesUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.t;
    this._defaultTableCellProperties = Up(e.config.get("table.tableCellProperties.defaultProperties"), {
      includeVerticalAlignmentProperty: !0,
      includeHorizontalAlignmentProperty: !0,
      includePaddingProperty: !0,
      isRightToLeftContent: e.locale.contentLanguageDirection === "rtl"
    }), this._balloon = e.plugins.get(Qi), this.view = null, this._isReady = !1, e.ui.componentFactory.add("tableCellProperties", (r) => {
      const s = new Te(r);
      s.set({
        label: n("Cell properties"),
        icon: wq,
        tooltip: !0
      }), this.listenTo(s, "execute", () => this._showView());
      const o = Object.values(L0).map((a) => e.commands.get(a));
      return s.bind("isEnabled").toMany(o, "isEnabled", (...a) => a.some((l) => l)), s;
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.view && this.view.destroy();
  }
  /**
  * Creates the {@link module:table/tablecellproperties/ui/tablecellpropertiesview~TableCellPropertiesView} instance.
  *
  * @returns The cell properties form view instance.
  */
  _createPropertiesView() {
    const e = this.editor, n = e.config.get("table.tableCellProperties"), r = Uu(n.borderColors), s = Wu(e.locale, r), o = Uu(n.backgroundColors), a = Wu(e.locale, o), l = n.colorPicker !== !1, c = new bq(e.locale, {
      borderColors: s,
      backgroundColors: a,
      defaultTableCellProperties: this._defaultTableCellProperties,
      colorPickerConfig: l ? n.colorPicker || {} : !1
    }), u = e.t;
    c.render(), this.listenTo(c, "submit", () => {
      this._hideView();
    }), this.listenTo(c, "cancel", () => {
      this._undoStepBatch.operations.length && e.execute("undo", this._undoStepBatch), this._hideView();
    }), c.keystrokes.set("Esc", (m, g) => {
      this._hideView(), g();
    }), _s({
      emitter: c,
      activator: () => this._isViewInBalloon,
      contextElements: [
        this._balloon.view.element
      ],
      callback: () => this._hideView()
    });
    const d = Zx(u), f = Yx(u);
    return c.on("change:borderStyle", this._getPropertyChangeCallback("tableCellBorderStyle")), c.on("change:borderColor", this._getValidatedPropertyChangeCallback({
      viewField: c.borderColorInput,
      commandName: "tableCellBorderColor",
      errorText: d,
      validator: ld
    })), c.on("change:borderWidth", this._getValidatedPropertyChangeCallback({
      viewField: c.borderWidthInput,
      commandName: "tableCellBorderWidth",
      errorText: f,
      validator: Jx
    })), c.on("change:padding", this._getValidatedPropertyChangeCallback({
      viewField: c.paddingInput,
      commandName: "tableCellPadding",
      errorText: f,
      validator: bl
    })), c.on("change:width", this._getValidatedPropertyChangeCallback({
      viewField: c.widthInput,
      commandName: "tableCellWidth",
      errorText: f,
      validator: bl
    })), c.on("change:height", this._getValidatedPropertyChangeCallback({
      viewField: c.heightInput,
      commandName: "tableCellHeight",
      errorText: f,
      validator: bl
    })), c.on("change:backgroundColor", this._getValidatedPropertyChangeCallback({
      viewField: c.backgroundInput,
      commandName: "tableCellBackgroundColor",
      errorText: d,
      validator: ld
    })), c.on("change:horizontalAlignment", this._getPropertyChangeCallback("tableCellHorizontalAlignment")), c.on("change:verticalAlignment", this._getPropertyChangeCallback("tableCellVerticalAlignment")), c;
  }
  /**
  * In this method the "editor data -> UI" binding is happening.
  *
  * When executed, this method obtains selected cell property values from various table commands
  * and passes them to the {@link #view}.
  *
  * This way, the UI stays up–to–date with the editor data.
  */
  _fillViewFormFromCommandValues() {
    const e = this.editor.commands, n = e.get("tableCellBorderStyle");
    Object.entries(L0).map(([r, s]) => {
      const o = this._defaultTableCellProperties[r] || "";
      return [
        r,
        e.get(s).value || o
      ];
    }).forEach(([r, s]) => {
      (r === "borderColor" || r === "borderWidth") && n.value === "none" || this.view.set(r, s);
    }), this._isReady = !0;
  }
  /**
  * Shows the {@link #view} in the {@link #_balloon}.
  *
  * **Note**: Each time a view is shown, a new {@link #_undoStepBatch} is created. It contains
  * all changes made to the document when the view is visible, allowing a single undo step
  * for all of them.
  */
  _showView() {
    const e = this.editor;
    this.view || (this.view = this._createPropertiesView()), this.listenTo(e.ui, "update", () => {
      this._updateView();
    }), this._fillViewFormFromCommandValues(), this._balloon.add({
      view: this.view,
      position: nE(e)
    }), this._undoStepBatch = e.model.createBatch(), this.view.focus();
  }
  /**
  * Removes the {@link #view} from the {@link #_balloon}.
  */
  _hideView() {
    const e = this.editor;
    this.stopListening(e.ui, "update"), this._isReady = !1, this.view.saveButtonView.focus(), this._balloon.remove(this.view), this.editor.editing.view.focus();
  }
  /**
  * Repositions the {@link #_balloon} or hides the {@link #view} if a table cell is no longer selected.
  */
  _updateView() {
    const e = this.editor, n = e.editing.view.document;
    rh(n.selection) ? this._isViewVisible && eE(e, "cell") : this._hideView();
  }
  /**
  * Returns `true` when the {@link #view} is visible in the {@link #_balloon}.
  */
  get _isViewVisible() {
    return !!this.view && this._balloon.visibleView === this.view;
  }
  /**
  * Returns `true` when the {@link #view} is in the {@link #_balloon}.
  */
  get _isViewInBalloon() {
    return !!this.view && this._balloon.hasView(this.view);
  }
  /**
  * Creates a callback that when executed upon the {@link #view view's} property change
  * executes a related editor command with the new property value.
  *
  * @param defaultValue The default value of the command.
  */
  _getPropertyChangeCallback(e) {
    return (n, r, s) => {
      this._isReady && this.editor.execute(e, {
        value: s,
        batch: this._undoStepBatch
      });
    };
  }
  /**
  * Creates a callback that when executed upon the {@link #view view's} property change:
  * * Executes a related editor command with the new property value if the value is valid,
  * * Or sets the error text next to the invalid field, if the value did not pass the validation.
  */
  _getValidatedPropertyChangeCallback(e) {
    const { commandName: n, viewField: r, validator: s, errorText: o } = e, a = Ci(() => {
      r.errorText = o;
    }, yq);
    return (l, c, u) => {
      a.cancel(), this._isReady && (s(u) ? (this.editor.execute(n, {
        value: u,
        batch: this._undoStepBatch
      }), r.errorText = null) : a());
    };
  }
}
class Ur extends Ve {
  /**
  * Creates a new `TableCellPropertyCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param attributeName Table cell attribute name.
  * @param defaultValue The default value of the attribute.
  */
  constructor(e, n, r) {
    super(e);
    /**
    * The attribute that will be set by the command.
    */
    h(this, "attributeName");
    /**
    * The default value for the attribute.
    */
    h(this, "_defaultValue");
    this.attributeName = n, this._defaultValue = r;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor, r = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(e.model.document.selection);
    this.isEnabled = !!r.length, this.value = this._getSingleValue(r);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.value If set, the command will set the attribute on selected table cells.
  * If it is not set, the command will remove the attribute from the selected table cells.
  * @param options.batch Pass the model batch instance to the command to aggregate changes,
  * for example to allow a single undo step for multiple executions.
  */
  execute(e = {}) {
    const { value: n, batch: r } = e, s = this.editor.model, a = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(s.document.selection), l = this._getValueToSet(n);
    s.enqueueChange(r, (c) => {
      l ? a.forEach((u) => c.setAttribute(this.attributeName, l, u)) : a.forEach((u) => c.removeAttribute(this.attributeName, u));
    });
  }
  /**
  * Returns the attribute value for a table cell.
  */
  _getAttribute(e) {
    if (!e)
      return;
    const n = e.getAttribute(this.attributeName);
    if (n !== this._defaultValue)
      return n;
  }
  /**
  * Returns the proper model value. It can be used to add a default unit to numeric values.
  */
  _getValueToSet(e) {
    if (e !== this._defaultValue)
      return e;
  }
  /**
  * Returns a single value for all selected table cells. If the value is the same for all cells,
  * it will be returned (`undefined` otherwise).
  */
  _getSingleValue(e) {
    const n = this._getAttribute(e[0]);
    return e.every((s) => this._getAttribute(s) === n) ? n : void 0;
  }
}
class Cq extends Ur {
  /**
  * Creates a new `TableCellWidthCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellWidth", e);
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    if (t = uo(t, "px"), t !== this._defaultValue)
      return t;
  }
}
class oE extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCellWidthEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      kc
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = Up(t.config.get("table.tableCellProperties.defaultProperties"));
    Pu(t.model.schema, t.conversion, {
      modelAttribute: "tableCellWidth",
      styleName: "width",
      defaultValue: e.width
    }), t.commands.add("tableCellWidth", new Cq(t, e.width));
  }
}
class kq extends Ur {
  /**
  * Creates a new `TableCellPaddingCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellPadding", e);
  }
  /**
  * @inheritDoc
  */
  _getAttribute(t) {
    if (!t)
      return;
    const e = co(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    const e = uo(t, "px");
    if (e !== this._defaultValue)
      return e;
  }
}
class xq extends Ur {
  /**
  * Creates a new `TableCellHeightCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellHeight", e);
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    const e = uo(t, "px");
    if (e !== this._defaultValue)
      return e;
  }
}
class Eq extends Ur {
  /**
  * Creates a new `TableCellBackgroundColorCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellBackgroundColor", e);
  }
}
class Tq extends Ur {
  /**
  * Creates a new `TableCellVerticalAlignmentCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value for the "alignment" attribute.
  */
  constructor(t, e) {
    super(t, "tableCellVerticalAlignment", e);
  }
}
class Sq extends Ur {
  /**
  * Creates a new `TableCellHorizontalAlignmentCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value for the "alignment" attribute.
  */
  constructor(t, e) {
    super(t, "tableCellHorizontalAlignment", e);
  }
}
class Aq extends Ur {
  /**
  * Creates a new `TableCellBorderStyleCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellBorderStyle", e);
  }
  /**
  * @inheritDoc
  */
  _getAttribute(t) {
    if (!t)
      return;
    const e = co(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
}
class Pq extends Ur {
  /**
  * Creates a new `TableCellBorderColorCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellBorderColor", e);
  }
  /**
  * @inheritDoc
  */
  _getAttribute(t) {
    if (!t)
      return;
    const e = co(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
}
class Iq extends Ur {
  /**
  * Creates a new `TableCellBorderWidthCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellBorderWidth", e);
  }
  /**
  * @inheritDoc
  */
  _getAttribute(t) {
    if (!t)
      return;
    const e = co(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    const e = uo(t, "px");
    if (e !== this._defaultValue)
      return e;
  }
}
const N0 = /^(top|middle|bottom)$/, B0 = /^(left|center|right|justify)$/;
class Rq extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCellPropertiesEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      kc,
      oE
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema, n = t.conversion;
    t.config.define("table.tableCellProperties.defaultProperties", {});
    const r = Up(t.config.get("table.tableCellProperties.defaultProperties"), {
      includeVerticalAlignmentProperty: !0,
      includeHorizontalAlignmentProperty: !0,
      includePaddingProperty: !0,
      isRightToLeftContent: t.locale.contentLanguageDirection === "rtl"
    });
    t.data.addStyleProcessorRules(kC), Oq(e, n, {
      color: r.borderColor,
      style: r.borderStyle,
      width: r.borderWidth
    }), t.commands.add("tableCellBorderStyle", new Aq(t, r.borderStyle)), t.commands.add("tableCellBorderColor", new Pq(t, r.borderColor)), t.commands.add("tableCellBorderWidth", new Iq(t, r.borderWidth)), Pu(e, n, {
      modelAttribute: "tableCellHeight",
      styleName: "height",
      defaultValue: r.height
    }), t.commands.add("tableCellHeight", new xq(t, r.height)), t.data.addStyleProcessorRules(Q8), Pu(e, n, {
      modelAttribute: "tableCellPadding",
      styleName: "padding",
      reduceBoxSides: !0,
      defaultValue: r.padding
    }), t.commands.add("tableCellPadding", new kq(t, r.padding)), t.data.addStyleProcessorRules(CC), Pu(e, n, {
      modelAttribute: "tableCellBackgroundColor",
      styleName: "background-color",
      defaultValue: r.backgroundColor
    }), t.commands.add("tableCellBackgroundColor", new Eq(t, r.backgroundColor)), Vq(e, n, r.horizontalAlignment), t.commands.add("tableCellHorizontalAlignment", new Sq(t, r.horizontalAlignment)), Mq(e, n, r.verticalAlignment), t.commands.add("tableCellVerticalAlignment", new Tq(t, r.verticalAlignment));
  }
}
function Oq(i, t, e) {
  const n = {
    width: "tableCellBorderWidth",
    color: "tableCellBorderColor",
    style: "tableCellBorderStyle"
  };
  i.extend("tableCell", {
    allowAttributes: Object.values(n)
  }), Rm(t, "td", n, e), Rm(t, "th", n, e), pl(t, {
    modelElement: "tableCell",
    modelAttribute: n.style,
    styleName: "border-style"
  }), pl(t, {
    modelElement: "tableCell",
    modelAttribute: n.color,
    styleName: "border-color"
  }), pl(t, {
    modelElement: "tableCell",
    modelAttribute: n.width,
    styleName: "border-width"
  });
}
function Vq(i, t, e) {
  i.extend("tableCell", {
    allowAttributes: [
      "tableCellHorizontalAlignment"
    ]
  }), t.for("downcast").attributeToAttribute({
    model: {
      name: "tableCell",
      key: "tableCellHorizontalAlignment"
    },
    view: (n) => ({
      key: "style",
      value: {
        "text-align": n
      }
    })
  }), t.for("upcast").attributeToAttribute({
    view: {
      name: /^(td|th)$/,
      styles: {
        "text-align": B0
      }
    },
    model: {
      key: "tableCellHorizontalAlignment",
      value: (n) => {
        const r = n.getStyle("text-align");
        return r === e ? null : r;
      }
    }
  }).attributeToAttribute({
    view: {
      name: /^(td|th)$/,
      attributes: {
        align: B0
      }
    },
    model: {
      key: "tableCellHorizontalAlignment",
      value: (n) => {
        const r = n.getAttribute("align");
        return r === e ? null : r;
      }
    }
  });
}
function Mq(i, t, e) {
  i.extend("tableCell", {
    allowAttributes: [
      "tableCellVerticalAlignment"
    ]
  }), t.for("downcast").attributeToAttribute({
    model: {
      name: "tableCell",
      key: "tableCellVerticalAlignment"
    },
    view: (n) => ({
      key: "style",
      value: {
        "vertical-align": n
      }
    })
  }), t.for("upcast").attributeToAttribute({
    view: {
      name: /^(td|th)$/,
      styles: {
        "vertical-align": N0
      }
    },
    model: {
      key: "tableCellVerticalAlignment",
      value: (n) => {
        const r = n.getStyle("vertical-align");
        return r === e ? null : r;
      }
    }
  }).attributeToAttribute({
    view: {
      name: /^(td|th)$/,
      attributes: {
        valign: N0
      }
    },
    model: {
      key: "tableCellVerticalAlignment",
      value: (n) => {
        const r = n.getAttribute("valign");
        return r === e ? null : r;
      }
    }
  });
}
class Dq extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCellProperties";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Rq,
      _q
    ];
  }
}
class ho extends Ve {
  /**
  * Creates a new `TablePropertyCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param attributeName Table cell attribute name.
  * @param defaultValue The default value of the attribute.
  */
  constructor(e, n, r) {
    super(e);
    /**
    * The attribute that will be set by the command.
    */
    h(this, "attributeName");
    /**
    * The default value for the attribute.
    */
    h(this, "_defaultValue");
    this.attributeName = n, this._defaultValue = r;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const n = this.editor.model.document.selection, r = Fs(n);
    this.isEnabled = !!r, this.value = this._getValue(r);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.value If set, the command will set the attribute on the selected table.
  * If not set, the command will remove the attribute from the selected table.
  * @param options.batch Pass the model batch instance to the command to aggregate changes,
  * for example, to allow a single undo step for multiple executions.
  */
  execute(e = {}) {
    const n = this.editor.model, r = n.document.selection, { value: s, batch: o } = e, a = Fs(r), l = this._getValueToSet(s);
    n.enqueueChange(o, (c) => {
      l ? c.setAttribute(this.attributeName, l, a) : c.removeAttribute(this.attributeName, a);
    });
  }
  /**
  * Returns the attribute value for a table.
  */
  _getValue(e) {
    if (!e)
      return;
    const n = e.getAttribute(this.attributeName);
    if (n !== this._defaultValue)
      return n;
  }
  /**
  * Returns the proper model value. It can be used to add a default unit to numeric values.
  */
  _getValueToSet(e) {
    if (e !== this._defaultValue)
      return e;
  }
}
class Lq extends ho {
  /**
  * Creates a new `TableBackgroundColorCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableBackgroundColor", e);
  }
}
class Nq extends ho {
  /**
  * Creates a new `TableBorderColorCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableBorderColor", e);
  }
  /**
  * @inheritDoc
  */
  _getValue(t) {
    if (!t)
      return;
    const e = co(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
}
class Bq extends ho {
  /**
  * Creates a new `TableBorderStyleCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableBorderStyle", e);
  }
  /**
  * @inheritDoc
  */
  _getValue(t) {
    if (!t)
      return;
    const e = co(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
}
class Fq extends ho {
  /**
  * Creates a new `TableBorderWidthCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableBorderWidth", e);
  }
  /**
  * @inheritDoc
  */
  _getValue(t) {
    if (!t)
      return;
    const e = co(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    const e = uo(t, "px");
    if (e !== this._defaultValue)
      return e;
  }
}
class $q extends ho {
  /**
  * Creates a new `TableWidthCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableWidth", e);
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    if (t = uo(t, "px"), t !== this._defaultValue)
      return t;
  }
}
class zq extends ho {
  /**
  * Creates a new `TableHeightCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableHeight", e);
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    if (t = uo(t, "px"), t !== this._defaultValue)
      return t;
  }
}
class Hq extends ho {
  /**
  * Creates a new `TableAlignmentCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value for the "alignment" attribute.
  */
  constructor(t, e) {
    super(t, "tableAlignment", e);
  }
}
const jq = /^(left|center|right)$/, Wq = /^(left|none|right)$/;
class Uq extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TablePropertiesEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      kc
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema, n = t.conversion;
    t.config.define("table.tableProperties.defaultProperties", {});
    const r = sE(t.config.get("table.tableProperties.defaultProperties"), {
      includeAlignmentProperty: !0
    });
    t.data.addStyleProcessorRules(kC), qq(e, n, {
      color: r.borderColor,
      style: r.borderStyle,
      width: r.borderWidth
    }), t.commands.add("tableBorderColor", new Nq(t, r.borderColor)), t.commands.add("tableBorderStyle", new Bq(t, r.borderStyle)), t.commands.add("tableBorderWidth", new Fq(t, r.borderWidth)), Gq(e, n, r.alignment), t.commands.add("tableAlignment", new Hq(t, r.alignment)), F0(e, n, {
      modelAttribute: "tableWidth",
      styleName: "width",
      defaultValue: r.width
    }), t.commands.add("tableWidth", new $q(t, r.width)), F0(e, n, {
      modelAttribute: "tableHeight",
      styleName: "height",
      defaultValue: r.height
    }), t.commands.add("tableHeight", new zq(t, r.height)), t.data.addStyleProcessorRules(CC), Kq(e, n, {
      modelAttribute: "tableBackgroundColor",
      styleName: "background-color",
      defaultValue: r.backgroundColor
    }), t.commands.add("tableBackgroundColor", new Lq(t, r.backgroundColor));
  }
}
function qq(i, t, e) {
  const n = {
    width: "tableBorderWidth",
    color: "tableBorderColor",
    style: "tableBorderStyle"
  };
  i.extend("table", {
    allowAttributes: Object.values(n)
  }), Rm(t, "table", n, e), Su(t, {
    modelAttribute: n.color,
    styleName: "border-color"
  }), Su(t, {
    modelAttribute: n.style,
    styleName: "border-style"
  }), Su(t, {
    modelAttribute: n.width,
    styleName: "border-width"
  });
}
function Gq(i, t, e) {
  i.extend("table", {
    allowAttributes: [
      "tableAlignment"
    ]
  }), t.for("downcast").attributeToAttribute({
    model: {
      name: "table",
      key: "tableAlignment"
    },
    view: (n) => ({
      key: "style",
      value: {
        // Model: `alignment:center` => CSS: `float:none`.
        float: n === "center" ? "none" : n
      }
    }),
    converterPriority: "high"
  }), t.for("upcast").attributeToAttribute({
    view: {
      name: /^(table|figure)$/,
      styles: {
        float: Wq
      }
    },
    model: {
      key: "tableAlignment",
      value: (n) => {
        let r = n.getStyle("float");
        return r === "none" && (r = "center"), r === e ? null : r;
      }
    }
  }).attributeToAttribute({
    view: {
      attributes: {
        align: jq
      }
    },
    model: {
      name: "table",
      key: "tableAlignment",
      value: (n) => {
        const r = n.getAttribute("align");
        return r === e ? null : r;
      }
    }
  });
}
function Kq(i, t, e) {
  const { modelAttribute: n } = e;
  i.extend("table", {
    allowAttributes: [
      n
    ]
  }), $p(t, {
    viewElement: "table",
    ...e
  }), Su(t, e);
}
function F0(i, t, e) {
  const { modelAttribute: n } = e;
  i.extend("table", {
    allowAttributes: [
      n
    ]
  }), $p(t, {
    viewElement: /^(table|figure)$/,
    shouldUpcast: (r) => !(r.name == "table" && r.parent.name == "figure"),
    ...e
  }), pl(t, {
    modelElement: "table",
    ...e
  });
}
class Zq extends de {
  /**
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param options Additional configuration of the view.
  */
  constructor(e, n) {
    super(e);
    /**
    * Options passed to the view. See {@link #constructor} to learn more.
    */
    h(this, "options");
    /**
    * Tracks information about the DOM focus in the form.
    */
    h(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    h(this, "keystrokes");
    /**
    * A collection of child views in the form.
    */
    h(this, "children");
    /**
    * A dropdown that allows selecting the style of the table border.
    */
    h(this, "borderStyleDropdown");
    /**
    * An input that allows specifying the width of the table border.
    */
    h(this, "borderWidthInput");
    /**
    * An input that allows specifying the color of the table border.
    */
    h(this, "borderColorInput");
    /**
    * An input that allows specifying the table background color.
    */
    h(this, "backgroundInput");
    /**
    * An input that allows specifying the table width.
    */
    h(this, "widthInput");
    /**
    * An input that allows specifying the table height.
    */
    h(this, "heightInput");
    /**
    * A toolbar with buttons that allow changing the alignment of an entire table.
    */
    h(this, "alignmentToolbar");
    /**
    * The "Save" button view.
    */
    h(this, "saveButtonView");
    /**
    * The "Cancel" button view.
    */
    h(this, "cancelButtonView");
    /**
    * A collection of views that can be focused in the form.
    */
    h(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    h(this, "_focusCycler");
    this.set({
      borderStyle: "",
      borderWidth: "",
      borderColor: "",
      backgroundColor: "",
      width: "",
      height: "",
      alignment: ""
    }), this.options = n;
    const { borderStyleDropdown: r, borderWidthInput: s, borderColorInput: o, borderRowLabel: a } = this._createBorderFields(), { backgroundRowLabel: l, backgroundInput: c } = this._createBackgroundFields(), { widthInput: u, operatorLabel: d, heightInput: f, dimensionsLabel: m } = this._createDimensionFields(), { alignmentToolbar: g, alignmentLabel: p } = this._createAlignmentFields();
    this.focusTracker = new wt(), this.keystrokes = new Ot(), this.children = this.createCollection(), this.borderStyleDropdown = r, this.borderWidthInput = s, this.borderColorInput = o, this.backgroundInput = c, this.widthInput = u, this.heightInput = f, this.alignmentToolbar = g;
    const { saveButtonView: b, cancelButtonView: v } = this._createActionButtons();
    this.saveButtonView = b, this.cancelButtonView = v, this._focusables = new In(), this._focusCycler = new jn({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.children.add(new Wd(e, {
      label: this.t("Table properties")
    })), this.children.add(new gi(e, {
      labelView: a,
      children: [
        a,
        r,
        o,
        s
      ],
      class: "ck-table-form__border-row"
    })), this.children.add(new gi(e, {
      labelView: l,
      children: [
        l,
        c
      ],
      class: "ck-table-form__background-row"
    })), this.children.add(new gi(e, {
      children: [
        // Dimensions row.
        new gi(e, {
          labelView: m,
          children: [
            m,
            u,
            d,
            f
          ],
          class: "ck-table-form__dimensions-row"
        }),
        // Alignment row.
        new gi(e, {
          labelView: p,
          children: [
            p,
            g
          ],
          class: "ck-table-properties-form__alignment-row"
        })
      ]
    })), this.children.add(new gi(e, {
      children: [
        this.saveButtonView,
        this.cancelButtonView
      ],
      class: "ck-table-form__action-row"
    })), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-form",
          "ck-table-form",
          "ck-table-properties-form"
        ],
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Cs({
      view: this
    }), [
      this.borderColorInput,
      this.backgroundInput
    ].forEach((e) => {
      this._focusCycler.chain(e.fieldView.focusCycler);
    }), [
      this.borderStyleDropdown,
      this.borderColorInput,
      this.borderWidthInput,
      this.backgroundInput,
      this.widthInput,
      this.heightInput,
      this.alignmentToolbar,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((e) => {
      this._focusables.add(e), this.focusTracker.add(e.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist focusable field in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #borderStyleDropdown},
  * * {@link #borderWidthInput},
  * * {@link #borderColorInput}.
  */
  _createBorderFields() {
    const e = this.options.defaultTableProperties, n = {
      style: e.borderStyle,
      width: e.borderWidth,
      color: e.borderColor
    }, r = ud({
      colorConfig: this.options.borderColors,
      columns: 5,
      defaultColorValue: n.color,
      colorPickerConfig: this.options.colorPickerConfig
    }), s = this.locale, o = this.t, a = o("Style"), l = new Oi(s);
    l.text = o("Border");
    const c = Wp(o), u = new Qt(s, QC);
    u.set({
      label: a,
      class: "ck-table-form__border-style"
    }), u.fieldView.buttonView.set({
      ariaLabel: a,
      ariaLabelledBy: void 0,
      isOn: !1,
      withText: !0,
      tooltip: a
    }), u.fieldView.buttonView.bind("label").to(this, "borderStyle", (m) => c[m || "none"]), u.fieldView.on("execute", (m) => {
      this.borderStyle = m.source._borderStyleValue;
    }), u.bind("isEmpty").to(this, "borderStyle", (m) => !m), pa(u.fieldView, Xx(this, n.style), {
      role: "menu",
      ariaLabel: a
    });
    const d = new Qt(s, yi);
    d.set({
      label: o("Width"),
      class: "ck-table-form__border-width"
    }), d.fieldView.bind("value").to(this, "borderWidth"), d.bind("isEnabled").to(this, "borderStyle", tu), d.fieldView.on("input", () => {
      this.borderWidth = d.fieldView.element.value;
    });
    const f = new Qt(s, r);
    return f.set({
      label: o("Color"),
      class: "ck-table-form__border-color"
    }), f.fieldView.bind("value").to(this, "borderColor"), f.bind("isEnabled").to(this, "borderStyle", tu), f.fieldView.on("input", () => {
      this.borderColor = f.fieldView.value;
    }), this.on("change:borderStyle", (m, g, p, b) => {
      tu(p) || (this.borderColor = "", this.borderWidth = ""), tu(b) || (this.borderColor = n.color, this.borderWidth = n.width);
    }), {
      borderRowLabel: l,
      borderStyleDropdown: u,
      borderColorInput: f,
      borderWidthInput: d
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #backgroundInput}.
  */
  _createBackgroundFields() {
    const e = this.locale, n = this.t, r = new Oi(e);
    r.text = n("Background");
    const s = ud({
      colorConfig: this.options.backgroundColors,
      columns: 5,
      defaultColorValue: this.options.defaultTableProperties.backgroundColor,
      colorPickerConfig: this.options.colorPickerConfig
    }), o = new Qt(e, s);
    return o.set({
      label: n("Color"),
      class: "ck-table-properties-form__background"
    }), o.fieldView.bind("value").to(this, "backgroundColor"), o.fieldView.on("input", () => {
      this.backgroundColor = o.fieldView.value;
    }), {
      backgroundRowLabel: r,
      backgroundInput: o
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #widthInput},
  * * {@link #heightInput}.
  */
  _createDimensionFields() {
    const e = this.locale, n = this.t, r = new Oi(e);
    r.text = n("Dimensions");
    const s = new Qt(e, yi);
    s.set({
      label: n("Width"),
      class: "ck-table-form__dimensions-row__width"
    }), s.fieldView.bind("value").to(this, "width"), s.fieldView.on("input", () => {
      this.width = s.fieldView.element.value;
    });
    const o = new de(e);
    o.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck-table-form__dimension-operator"
        ]
      },
      children: [
        {
          text: "×"
        }
      ]
    });
    const a = new Qt(e, yi);
    return a.set({
      label: n("Height"),
      class: "ck-table-form__dimensions-row__height"
    }), a.fieldView.bind("value").to(this, "height"), a.fieldView.on("input", () => {
      this.height = a.fieldView.element.value;
    }), {
      dimensionsLabel: r,
      widthInput: s,
      operatorLabel: o,
      heightInput: a
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #alignmentToolbar}.
  */
  _createAlignmentFields() {
    const e = this.locale, n = this.t, r = new Oi(e);
    r.text = n("Alignment");
    const s = new fs(e);
    return s.set({
      isCompact: !0,
      ariaLabel: n("Table alignment toolbar")
    }), $m({
      view: this,
      icons: {
        left: ee.objectLeft,
        center: ee.objectCenter,
        right: ee.objectRight
      },
      toolbar: s,
      labels: this._alignmentLabels,
      propertyName: "alignment",
      defaultValue: this.options.defaultTableProperties.alignment
    }), {
      alignmentLabel: r,
      alignmentToolbar: s
    };
  }
  /**
  * Creates the following form controls:
  *
  * * {@link #saveButtonView},
  * * {@link #cancelButtonView}.
  */
  _createActionButtons() {
    const e = this.locale, n = this.t, r = new Te(e), s = new Te(e), o = [
      this.borderWidthInput,
      this.borderColorInput,
      this.backgroundInput,
      this.widthInput,
      this.heightInput
    ];
    return r.set({
      label: n("Save"),
      icon: ee.check,
      class: "ck-button-save",
      type: "submit",
      withText: !0
    }), r.bind("isEnabled").toMany(o, "errorText", (...a) => a.every((l) => !l)), s.set({
      label: n("Cancel"),
      icon: ee.cancel,
      class: "ck-button-cancel",
      withText: !0
    }), s.delegate("execute").to(this, "cancel"), {
      saveButtonView: r,
      cancelButtonView: s
    };
  }
  /**
  * Provides localized labels for {@link #alignmentToolbar} buttons.
  */
  get _alignmentLabels() {
    const e = this.locale, n = this.t, r = n("Align table to the left"), s = n("Center table"), o = n("Align table to the right");
    return e.uiLanguageDirection === "rtl" ? {
      right: o,
      center: s,
      left: r
    } : {
      left: r,
      center: s,
      right: o
    };
  }
}
function tu(i) {
  return i !== "none";
}
var Yq = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M8 2v5h4V2h1v5h5v1h-5v4h.021l-.172.351-1.916.28-.151.027c-.287.063-.54.182-.755.341L8 13v5H7v-5H2v-1h5V8H2V7h5V2h1zm4 6H8v4h4V8z" opacity=".6"/><path d="m15.5 11.5 1.323 2.68 2.957.43-2.14 2.085.505 2.946L15.5 18.25l-2.645 1.39.505-2.945-2.14-2.086 2.957-.43L15.5 11.5zM17 1a2 2 0 0 1 2 2v9.475l-.85-.124-.857-1.736a2.048 2.048 0 0 0-.292-.44L17 3H3v14h7.808l.402.392L10.935 19H3a2 2 0 0 1-2-2V3a2 2 0 0 1 2-2h14z"/></svg>';
const Jq = 500, $0 = {
  borderStyle: "tableBorderStyle",
  borderColor: "tableBorderColor",
  borderWidth: "tableBorderWidth",
  backgroundColor: "tableBackgroundColor",
  width: "tableWidth",
  height: "tableHeight",
  alignment: "tableAlignment"
};
class Xq extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The default table properties.
    */
    h(this, "_defaultTableProperties");
    /**
    * The contextual balloon plugin instance.
    */
    h(this, "_balloon");
    /**
    * The properties form view displayed inside the balloon.
    */
    h(this, "view", null);
    /**
    * The batch used to undo all changes made by the form (which are live, as the user types)
    * when "Cancel" was pressed. Each time the view is shown, a new batch is created.
    */
    h(this, "_undoStepBatch");
    /**
    * Flag used to indicate whether view is ready to execute update commands
    * (it finished loading initial data).
    */
    h(this, "_isReady");
    e.config.define("table.tableProperties", {
      borderColors: cd,
      backgroundColors: cd
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Qi
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TablePropertiesUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.t;
    this._defaultTableProperties = sE(e.config.get("table.tableProperties.defaultProperties"), {
      includeAlignmentProperty: !0
    }), this._balloon = e.plugins.get(Qi), e.ui.componentFactory.add("tableProperties", (r) => {
      const s = new Te(r);
      s.set({
        label: n("Table properties"),
        icon: Yq,
        tooltip: !0
      }), this.listenTo(s, "execute", () => this._showView());
      const o = Object.values($0).map((a) => e.commands.get(a));
      return s.bind("isEnabled").toMany(o, "isEnabled", (...a) => a.some((l) => l)), s;
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.view && this.view.destroy();
  }
  /**
  * Creates the {@link module:table/tableproperties/ui/tablepropertiesview~TablePropertiesView} instance.
  *
  * @returns The table properties form view instance.
  */
  _createPropertiesView() {
    const e = this.editor, n = e.config.get("table.tableProperties"), r = Uu(n.borderColors), s = Wu(e.locale, r), o = Uu(n.backgroundColors), a = Wu(e.locale, o), l = n.colorPicker !== !1, c = new Zq(e.locale, {
      borderColors: s,
      backgroundColors: a,
      defaultTableProperties: this._defaultTableProperties,
      colorPickerConfig: l ? n.colorPicker || {} : !1
    }), u = e.t;
    c.render(), this.listenTo(c, "submit", () => {
      this._hideView();
    }), this.listenTo(c, "cancel", () => {
      this._undoStepBatch.operations.length && e.execute("undo", this._undoStepBatch), this._hideView();
    }), c.keystrokes.set("Esc", (m, g) => {
      this._hideView(), g();
    }), _s({
      emitter: c,
      activator: () => this._isViewInBalloon,
      contextElements: [
        this._balloon.view.element
      ],
      callback: () => this._hideView()
    });
    const d = Zx(u), f = Yx(u);
    return c.on("change:borderStyle", this._getPropertyChangeCallback("tableBorderStyle")), c.on("change:borderColor", this._getValidatedPropertyChangeCallback({
      viewField: c.borderColorInput,
      commandName: "tableBorderColor",
      errorText: d,
      validator: ld
    })), c.on("change:borderWidth", this._getValidatedPropertyChangeCallback({
      viewField: c.borderWidthInput,
      commandName: "tableBorderWidth",
      errorText: f,
      validator: Jx
    })), c.on("change:backgroundColor", this._getValidatedPropertyChangeCallback({
      viewField: c.backgroundInput,
      commandName: "tableBackgroundColor",
      errorText: d,
      validator: ld
    })), c.on("change:width", this._getValidatedPropertyChangeCallback({
      viewField: c.widthInput,
      commandName: "tableWidth",
      errorText: f,
      validator: bl
    })), c.on("change:height", this._getValidatedPropertyChangeCallback({
      viewField: c.heightInput,
      commandName: "tableHeight",
      errorText: f,
      validator: bl
    })), c.on("change:alignment", this._getPropertyChangeCallback("tableAlignment")), c;
  }
  /**
  * In this method the "editor data -> UI" binding is happening.
  *
  * When executed, this method obtains selected table property values from various table commands
  * and passes them to the {@link #view}.
  *
  * This way, the UI stays up–to–date with the editor data.
  */
  _fillViewFormFromCommandValues() {
    const e = this.editor.commands, n = e.get("tableBorderStyle");
    Object.entries($0).map(([r, s]) => {
      const o = r, a = this._defaultTableProperties[o] || "";
      return [
        o,
        e.get(s).value || a
      ];
    }).forEach(([r, s]) => {
      (r === "borderColor" || r === "borderWidth") && n.value === "none" || this.view.set(r, s);
    }), this._isReady = !0;
  }
  /**
  * Shows the {@link #view} in the {@link #_balloon}.
  *
  * **Note**: Each time a view is shown, the new {@link #_undoStepBatch} is created that contains
  * all changes made to the document when the view is visible, allowing a single undo step
  * for all of them.
  */
  _showView() {
    const e = this.editor;
    this.view || (this.view = this._createPropertiesView()), this.listenTo(e.ui, "update", () => {
      this._updateView();
    }), this._fillViewFormFromCommandValues(), this._balloon.add({
      view: this.view,
      position: tE(e)
    }), this._undoStepBatch = e.model.createBatch(), this.view.focus();
  }
  /**
  * Removes the {@link #view} from the {@link #_balloon}.
  */
  _hideView() {
    const e = this.editor;
    this.stopListening(e.ui, "update"), this._isReady = !1, this.view.saveButtonView.focus(), this._balloon.remove(this.view), this.editor.editing.view.focus();
  }
  /**
  * Repositions the {@link #_balloon} or hides the {@link #view} if a table is no longer selected.
  */
  _updateView() {
    const e = this.editor, n = e.editing.view.document;
    qx(n.selection) ? this._isViewVisible && eE(e, "table") : this._hideView();
  }
  /**
  * Returns `true` when the {@link #view} is the visible in the {@link #_balloon}.
  */
  get _isViewVisible() {
    return !!this.view && this._balloon.visibleView === this.view;
  }
  /**
  * Returns `true` when the {@link #view} is in the {@link #_balloon}.
  */
  get _isViewInBalloon() {
    return !!this.view && this._balloon.hasView(this.view);
  }
  /**
  * Creates a callback that when executed upon {@link #view view's} property change
  * executes a related editor command with the new property value.
  *
  * If new value will be set to the default value, the command will not be executed.
  *
  * @param commandName The command that will be executed.
  */
  _getPropertyChangeCallback(e) {
    return (n, r, s) => {
      this._isReady && this.editor.execute(e, {
        value: s,
        batch: this._undoStepBatch
      });
    };
  }
  /**
  * Creates a callback that when executed upon {@link #view view's} property change:
  * * executes a related editor command with the new property value if the value is valid,
  * * or sets the error text next to the invalid field, if the value did not pass the validation.
  */
  _getValidatedPropertyChangeCallback(e) {
    const { commandName: n, viewField: r, validator: s, errorText: o } = e, a = Ci(() => {
      r.errorText = o;
    }, Jq);
    return (l, c, u) => {
      a.cancel(), this._isReady && (s(u) ? (this.editor.execute(n, {
        value: u,
        batch: this._undoStepBatch
      }), r.errorText = null) : a());
    };
  }
}
class Qq extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableProperties";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Uq,
      Xq
    ];
  }
}
/**
 * @license Copyright (c) 2003-2024, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-oss-license
 */
function eG(i) {
  i.document.registerPostFixer((t) => tG(t, i));
}
function tG(i, t) {
  const e = t.document.differ.getChanges();
  let n = !1;
  for (const r of e) {
    if (r.type != "insert")
      continue;
    const s = r.position.parent;
    if (s.is("element", "table") || r.name == "table") {
      const o = r.name == "table" ? r.position.nodeAfter : s, a = Array.from(o.getChildren()).filter((c) => c.is("element", "caption")), l = a.shift();
      if (!l)
        continue;
      for (const c of a)
        i.move(i.createRangeIn(c), l, "end"), i.remove(c);
      l.nextSibling && (i.move(i.createRangeOn(l), o, "end"), n = !0), n = !!a.length || n;
    }
  }
  return n;
}
function z0(i) {
  return !!i && i.is("element", "table");
}
function Hm(i) {
  for (const t of i.getChildren())
    if (t.is("element", "caption"))
      return t;
  return null;
}
function nG(i) {
  const t = Fs(i);
  return t ? Hm(t) : null;
}
function iG(i) {
  const t = i.parent;
  return i.name == "figcaption" && t && t.is("element", "figure") && t.hasClass("table") ? {
    name: !0
  } : i.name == "caption" && t && t.is("element", "table") ? {
    name: !0
  } : null;
}
class rG extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor, e = Fs(t.model.document.selection);
    this.isEnabled = !!e, this.isEnabled ? this.value = !!Hm(e) : this.value = !1;
  }
  /**
  * Executes the command.
  *
  * ```ts
  * editor.execute( 'toggleTableCaption' );
  * ```
  *
  * @param options Options for the executed command.
  * @param options.focusCaptionOnShow When true and the caption shows up, the selection will be moved into it straight away.
  * @fires execute
  */
  execute({ focusCaptionOnShow: t = !1 } = {}) {
    this.editor.model.change((e) => {
      this.value ? this._hideTableCaption(e) : this._showTableCaption(e, t);
    });
  }
  /**
  * Shows the table caption. Also:
  *
  * * it attempts to restore the caption content from the `TableCaptionEditing` caption registry,
  * * it moves the selection to the caption right away, it the `focusCaptionOnShow` option was set.
  *
  * @param focusCaptionOnShow Default focus behavior when showing the caption.
  */
  _showTableCaption(t, e) {
    const n = this.editor.model, r = Fs(n.document.selection), a = this.editor.plugins.get("TableCaptionEditing")._getSavedCaption(r) || t.createElement("caption");
    n.insertContent(a, r, "end"), e && t.setSelection(a, "in");
  }
  /**
  * Hides the caption of a selected table (or an table caption the selection is anchored to).
  *
  * The content of the caption is stored in the `TableCaptionEditing` caption registry to make this
  * a reversible action.
  */
  _hideTableCaption(t) {
    const e = this.editor.model, n = Fs(e.document.selection), r = this.editor.plugins.get("TableCaptionEditing"), s = Hm(n);
    r._saveCaption(n, s), e.deleteContent(t.createSelection(s, "on"));
  }
}
class sG extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A map that keeps saved JSONified table captions and table model elements they are
    * associated with.
    *
    * To learn more about this system, see {@link #_saveCaption}.
    */
    h(this, "_savedCaptionsMap");
    this._savedCaptionsMap = /* @__PURE__ */ new WeakMap();
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCaptionEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, n = e.model.schema, r = e.editing.view, s = e.t;
    n.isRegistered("caption") ? n.extend("caption", {
      allowIn: "table"
    }) : n.register("caption", {
      allowIn: "table",
      allowContentOf: "$block",
      isLimit: !0
    }), e.commands.add("toggleTableCaption", new rG(this.editor)), e.conversion.for("upcast").elementToElement({
      view: iG,
      model: "caption"
    }), e.conversion.for("dataDowncast").elementToElement({
      model: "caption",
      view: (o, { writer: a }) => z0(o.parent) ? a.createContainerElement("figcaption") : null
    }), e.conversion.for("editingDowncast").elementToElement({
      model: "caption",
      view: (o, { writer: a }) => {
        if (!z0(o.parent))
          return null;
        const l = a.createEditableElement("figcaption");
        return a.setCustomProperty("tableCaption", !0, l), l.placeholder = s("Enter table caption"), Qg({
          view: r,
          element: l,
          keepOnFocus: !0
        }), wp(l, a);
      }
    }), eG(e.model);
  }
  /**
  * Returns the saved {@link module:engine/model/element~Element#toJSON JSONified} caption
  * of a table model element.
  *
  * See {@link #_saveCaption}.
  *
  * @internal
  * @param tableModelElement The model element the caption should be returned for.
  * @returns The model caption element or `null` if there is none.
  */
  _getSavedCaption(e) {
    const n = this._savedCaptionsMap.get(e);
    return n ? xt.fromJSON(n) : null;
  }
  /**
  * Saves a {@link module:engine/model/element~Element#toJSON JSONified} caption for
  * a table element to allow restoring it in the future.
  *
  * A caption is saved every time it gets hidden. The
  * user should be able to restore it on demand.
  *
  * **Note**: The caption cannot be stored in the table model element attribute because,
  * for instance, when the model state propagates to collaborators, the attribute would get
  * lost (mainly because it does not convert to anything when the caption is hidden) and
  * the states of collaborators' models would de-synchronize causing numerous issues.
  *
  * See {@link #_getSavedCaption}.
  *
  * @internal
  * @param tableModelElement The model element the caption is saved for.
  * @param caption The caption model element to be saved.
  */
  _saveCaption(e, n) {
    this._savedCaptionsMap.set(e, n.toJSON());
  }
}
class oG extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCaptionUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.editing.view, n = t.t;
    t.ui.componentFactory.add("toggleTableCaption", (r) => {
      const s = t.commands.get("toggleTableCaption"), o = new Te(r);
      return o.set({
        icon: ee.caption,
        tooltip: !0,
        isToggleable: !0
      }), o.bind("isOn", "isEnabled").to(s, "value", "isEnabled"), o.bind("label").to(s, "value", (a) => n(a ? "Toggle caption off" : "Toggle caption on")), this.listenTo(o, "execute", () => {
        if (t.execute("toggleTableCaption", {
          focusCaptionOnShow: !0
        }), s.value) {
          const a = nG(t.model.document.selection), l = t.editing.mapper.toViewElement(a);
          if (!l)
            return;
          e.scrollToTheSelection(), e.change((c) => {
            c.addClass("table__caption_highlighted", l);
          });
        }
        t.editing.view.focus();
      }), o;
    });
  }
}
class aG extends V {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCaption";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      sG,
      oG
    ];
  }
}
class lG extends Ve {
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = !0;
  }
  /**
  * Updated the `tableWidth` attribute of the table and the `columnWidth` attribute of the columns of that table.
  */
  execute(t = {}) {
    const { model: e, plugins: n } = this.editor;
    let { table: r = e.document.selection.getSelectedElement(), columnWidths: s, tableWidth: o } = t;
    s && (s = Array.isArray(s) ? s : s.split(",")), e.change((a) => {
      o ? a.setAttribute("tableWidth", o, r) : a.removeAttribute("tableWidth", r);
      const l = n.get("TableColumnResizeEditing").getColumnGroupElement(r);
      if (!s && !l)
        return;
      if (!s)
        return a.remove(l);
      const c = Hp(s);
      if (l)
        Array.from(l.getChildren()).forEach((u, d) => a.setAttribute("columnWidth", c[d], u));
      else {
        const u = a.createElement("tableColumnGroup");
        c.forEach((d) => a.appendElement("tableColumn", {
          columnWidth: d
        }, u)), a.append(u, r);
      }
    });
  }
}
function cG(i) {
  return (t) => t.on("element:colgroup", (e, n, r) => {
    const s = n.modelCursor.findAncestor("table"), o = _c(s);
    if (!o)
      return;
    const a = Cc(o), l = i.getColumns(s);
    let c = PU(o, r.writer);
    c = Array.from({
      length: l
    }, (u, d) => c[d] || "auto"), (c.length != a.length || c.includes("auto")) && Hx(a, o, Hp(c), r.writer);
  }, {
    priority: "low"
  });
}
function uG() {
  return (i) => i.on("insert:table", (t, e, n) => {
    const r = n.writer, s = e.item, o = n.mapper.toViewElement(s), a = o.is("element", "table") ? o : Array.from(o.getChildren()).find((c) => c.is("element", "table"));
    _c(s) ? r.addClass("ck-table-resized", a) : r.removeClass("ck-table-resized", a);
  }, {
    priority: "low"
  });
}
class dG extends V {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A flag indicating if the column resizing is in progress.
    */
    h(this, "_isResizingActive");
    /**
    * A temporary storage for the required data needed to correctly calculate the widths of the resized columns. This storage is
    * initialized when column resizing begins, and is purged upon completion.
    */
    h(this, "_resizingData");
    /**
    * DOM emitter.
    */
    h(this, "_domEmitter");
    /**
    * A local reference to the {@link module:table/tableutils~TableUtils} plugin.
    */
    h(this, "_tableUtilsPlugin");
    this._isResizingActive = !1, this.set("_isResizingAllowed", !0), this._resizingData = null, this._domEmitter = new (Xn())(), this._tableUtilsPlugin = e.plugins.get("TableUtils"), this.on("change:_isResizingAllowed", (n, r, s) => {
      const o = s ? "removeClass" : "addClass";
      e.editing.view.change((a) => {
        for (const l of e.editing.view.document.roots)
          a[o]("ck-column-resize_disabled", e.editing.view.document.getRoot(l.rootName));
      });
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      kc,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableColumnResizeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this._extendSchema(), this._registerPostFixer(), this._registerConverters(), this._registerResizingListeners(), this._registerResizerInserter();
    const e = this.editor, n = e.plugins.get("TableColumnResize");
    e.plugins.get("TableEditing").registerAdditionalSlot({
      filter: (o) => o.is("element", "tableColumnGroup"),
      positionOffset: 0
    });
    const s = new lG(e);
    e.commands.add("resizeTableWidth", s), e.commands.add("resizeColumnWidths", s), this.bind("_isResizingAllowed").to(e, "isReadOnly", n, "isEnabled", s, "isEnabled", (o, a, l) => !o && a && l);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._domEmitter.stopListening(), super.destroy();
  }
  /**
  * Returns a 'tableColumnGroup' element from the 'table'.
  *
  * @param element A 'table' or 'tableColumnGroup' element.
  * @returns A 'tableColumnGroup' element.
  */
  getColumnGroupElement(e) {
    return _c(e);
  }
  /**
  * Returns an array of 'tableColumn' elements.
  *
  * @param element A 'table' or 'tableColumnGroup' element.
  * @returns An array of 'tableColumn' elements.
  */
  getTableColumnElements(e) {
    return Cc(e);
  }
  /**
  * Returns an array of table column widths.
  *
  * @param element A 'table' or 'tableColumnGroup' element.
  * @returns An array of table column widths.
  */
  getTableColumnsWidths(e) {
    return AU(e);
  }
  /**
  * Registers new attributes for a table model element.
  */
  _extendSchema() {
    this.editor.model.schema.extend("table", {
      allowAttributes: [
        "tableWidth"
      ]
    }), this.editor.model.schema.register("tableColumnGroup", {
      allowIn: "table",
      isLimit: !0
    }), this.editor.model.schema.register("tableColumn", {
      allowIn: "tableColumnGroup",
      allowAttributes: [
        "columnWidth",
        "colSpan"
      ],
      isLimit: !0
    });
  }
  /**
  * Registers table column resize post-fixer.
  *
  * It checks if the change from the differ concerns a table-related element or attribute. For detected changes it:
  *  * Adjusts the `columnWidths` attribute to guarantee that the sum of the widths from all columns is 100%.
  *  * Checks if the `columnWidths` attribute gets updated accordingly after columns have been added or removed.
  */
  _registerPostFixer() {
    const n = this.editor.model;
    n.document.registerPostFixer((o) => {
      let a = !1;
      for (const l of _U(n)) {
        const c = this.getColumnGroupElement(l), u = this.getTableColumnElements(c), d = this.getTableColumnsWidths(c);
        let f = Hp(d);
        f = r(f, l, this), !Yo(d, f) && (Hx(u, c, f, o), a = !0);
      }
      return a;
    });
    function r(o, a, l) {
      const c = l._tableUtilsPlugin.getColumns(a);
      if (c - o.length === 0)
        return o;
      const d = o.map((m) => Number(m.replace("%", ""))), f = s(l.editor.model.document.differ, a);
      for (const m of f) {
        const g = c - d.length;
        if (g === 0)
          continue;
        const p = g > 0, b = l._tableUtilsPlugin.getCellLocation(m).column;
        if (p) {
          const v = CU(a, l.editor), y = EU(g, v);
          d.splice(b, 0, ...y);
        } else {
          const v = d.splice(b, Math.abs(g));
          d[b] += ql(v);
        }
      }
      return d.map((m) => m + "%");
    }
    function s(o, a) {
      const l = /* @__PURE__ */ new Set();
      for (const c of o.getChanges())
        if (c.type == "insert" && c.position.nodeAfter && c.position.nodeAfter.name == "tableCell" && c.position.nodeAfter.getAncestors().includes(a))
          l.add(c.position.nodeAfter);
        else if (c.type == "remove") {
          const u = c.position.nodeBefore || c.position.nodeAfter;
          u.name == "tableCell" && u.getAncestors().includes(a) && l.add(u);
        }
      return l;
    }
  }
  /**
  * Registers table column resize converters.
  */
  _registerConverters() {
    const n = this.editor.conversion;
    n.for("upcast").attributeToAttribute({
      view: {
        name: "figure",
        key: "style",
        value: {
          width: /[\s\S]+/
        }
      },
      model: {
        name: "table",
        key: "tableWidth",
        value: (r) => r.getStyle("width")
      }
    }), n.for("downcast").attributeToAttribute({
      model: {
        name: "table",
        key: "tableWidth"
      },
      view: (r) => ({
        name: "figure",
        key: "style",
        value: {
          width: r
        }
      })
    }), n.elementToElement({
      model: "tableColumnGroup",
      view: "colgroup"
    }), n.elementToElement({
      model: "tableColumn",
      view: "col"
    }), n.for("downcast").add(uG()), n.for("upcast").add(cG(this._tableUtilsPlugin)), n.for("upcast").attributeToAttribute({
      view: {
        name: "col",
        styles: {
          width: /.*/
        }
      },
      model: {
        key: "columnWidth",
        value: (r) => {
          const s = r.getStyle("width");
          return !s || !s.endsWith("%") && !s.endsWith("pt") ? "auto" : s;
        }
      }
    }), n.for("upcast").attributeToAttribute({
      view: {
        name: "col",
        key: "span"
      },
      model: "colSpan"
    }), n.for("downcast").attributeToAttribute({
      model: {
        name: "tableColumn",
        key: "columnWidth"
      },
      view: (r) => ({
        key: "style",
        value: {
          width: r
        }
      })
    });
  }
  /**
  * Registers listeners to handle resizing process.
  */
  _registerResizingListeners() {
    const e = this.editor.editing.view;
    e.addObserver(Ux), e.document.on("mousedown", this._onMouseDownHandler.bind(this), {
      priority: "high"
    }), this._domEmitter.listenTo(ae.window.document, "mousemove", Jo(this._onMouseMoveHandler.bind(this), 50)), this._domEmitter.listenTo(ae.window.document, "mouseup", this._onMouseUpHandler.bind(this));
  }
  /**
  * Handles the `mousedown` event on column resizer element:
  *  * calculates the initial column pixel widths,
  *  * inserts the `<colgroup>` element if it is not present in the `<table>`,
  *  * puts the necessary data in the temporary storage,
  *  * applies the attributes to the `<table>` view element.
  *
  * @param eventInfo An object containing information about the fired event.
  * @param domEventData The data related to the DOM event.
  */
  _onMouseDownHandler(e, n) {
    const r = n.target;
    if (!r.hasClass("ck-table-column-resizer") || !this._isResizingAllowed)
      return;
    const s = this.editor, o = s.editing.mapper.toModelElement(r.findAncestor("figure"));
    if (!s.model.canEditAt(o))
      return;
    n.preventDefault(), e.stop();
    const a = u(o, this._tableUtilsPlugin, s), l = r.findAncestor("table"), c = s.editing.view;
    Array.from(l.getChildren()).find((m) => m.is("element", "colgroup")) || c.change((m) => {
      d(m, a, l);
    }), this._isResizingActive = !0, this._resizingData = this._getResizingData(n, a), c.change((m) => f(m, l, this._resizingData));
    function u(m, g, p) {
      const b = Array(g.getColumns(m)), v = new ut(m);
      for (const y of v) {
        const x = p.editing.mapper.toViewElement(y.cell), S = p.editing.view.domConverter.mapViewToDom(x), k = SU(S);
        (!b[y.column] || k < b[y.column]) && (b[y.column] = si(k));
      }
      return b;
    }
    function d(m, g, p) {
      const b = m.createContainerElement("colgroup");
      for (let v = 0; v < g.length; v++) {
        const y = m.createEmptyElement("col"), x = `${si(g[v] / ql(g) * 100)}%`;
        m.setStyle("width", x, y), m.insert(m.createPositionAt(b, "end"), y);
      }
      m.insert(m.createPositionAt(p, 0), b);
    }
    function f(m, g, p) {
      const b = p.widths.viewFigureWidth / p.widths.viewFigureParentWidth;
      m.addClass("ck-table-resized", g), m.addClass("ck-table-column-resizer__active", p.elements.viewResizer), m.setStyle("width", `${si(b * 100)}%`, g.findAncestor("figure"));
    }
  }
  /**
  * Handles the `mousemove` event.
  *  * If resizing process is not in progress, it does nothing.
  *  * If resizing is active but not allowed, it stops the resizing process instantly calling the `mousedown` event handler.
  *  * Otherwise it dynamically updates the widths of the resized columns.
  *
  * @param eventInfo An object containing information about the fired event.
  * @param mouseEventData The native DOM event.
  */
  _onMouseMoveHandler(e, n) {
    if (!this._isResizingActive)
      return;
    if (!this._isResizingAllowed) {
      this._onMouseUpHandler();
      return;
    }
    const { columnPosition: r, flags: { isRightEdge: s, isTableCentered: o, isLtrContent: a }, elements: { viewFigure: l, viewLeftColumn: c, viewRightColumn: u }, widths: { viewFigureParentWidth: d, tableWidth: f, leftColumnWidth: m, rightColumnWidth: g } } = this._resizingData, p = -m + Dm, b = s ? d - f : g - Dm, v = (a ? 1 : -1) * (s && o ? 2 : 1), y = xU((n.clientX - r) * v, Math.min(p, 0), Math.max(b, 0));
    y !== 0 && this.editor.editing.view.change((x) => {
      const S = si((m + y) * 100 / f);
      if (x.setStyle("width", `${S}%`, c), s) {
        const k = si((f + y) * 100 / d);
        x.setStyle("width", `${k}%`, l);
      } else {
        const k = si((g - y) * 100 / f);
        x.setStyle("width", `${k}%`, u);
      }
    });
  }
  /**
  * Handles the `mouseup` event.
  *  * If resizing process is not in progress, it does nothing.
  *  * If resizing is active but not allowed, it cancels the resizing process restoring the original widths.
  *  * Otherwise it propagates the changes from view to the model by executing the adequate commands.
  */
  _onMouseUpHandler() {
    if (!this._isResizingActive)
      return;
    const { viewResizer: e, modelTable: n, viewFigure: r, viewColgroup: s } = this._resizingData.elements, o = this.editor, a = o.editing.view, l = this.getColumnGroupElement(n), c = Array.from(s.getChildren()).filter((b) => b.is("view:element")), u = l ? this.getTableColumnsWidths(l) : null, d = c.map((b) => b.getStyle("width")), f = !Yo(u, d), m = n.getAttribute("tableWidth"), g = r.getStyle("width"), p = m !== g;
    (f || p) && (this._isResizingAllowed ? o.execute("resizeTableWidth", {
      table: n,
      tableWidth: `${si(g)}%`,
      columnWidths: d
    }) : a.change((b) => {
      if (u)
        for (const v of c)
          b.setStyle("width", u.shift(), v);
      else
        b.remove(s);
      p && (m ? b.setStyle("width", m, r) : b.removeStyle("width", r)), !u && !m && b.removeClass("ck-table-resized", [
        ...r.getChildren()
      ].find((v) => v.name === "table"));
    })), a.change((b) => {
      b.removeClass("ck-table-column-resizer__active", e);
    }), this._isResizingActive = !1, this._resizingData = null;
  }
  /**
  * Retrieves and returns required data needed for the resizing process.
  *
  * @param domEventData The data of the `mousedown` event.
  * @param columnWidths The current widths of the columns.
  * @returns The data needed for the resizing process.
  */
  _getResizingData(e, n) {
    const r = this.editor, s = e.domEvent.clientX, o = e.target, a = o.findAncestor("td") || o.findAncestor("th"), l = r.editing.mapper.toModelElement(a), c = l.findAncestor("table"), u = kU(l, this._tableUtilsPlugin).rightEdge, d = this._tableUtilsPlugin.getColumns(c) - 1, f = u === d, m = !c.hasAttribute("tableAlignment"), g = r.locale.contentLanguageDirection !== "rtl", p = a.findAncestor("table"), b = p.findAncestor("figure"), v = [
      ...p.getChildren()
    ].find((R) => R.is("element", "colgroup")), y = v.getChild(u), x = f ? void 0 : v.getChild(u + 1), S = Lm(r.editing.view.domConverter.mapViewToDom(b.parent)), k = Lm(r.editing.view.domConverter.mapViewToDom(b)), I = zx(c, r), A = n[u], L = f ? void 0 : n[u + 1];
    return {
      columnPosition: s,
      flags: {
        isRightEdge: f,
        isTableCentered: m,
        isLtrContent: g
      },
      elements: {
        viewResizer: o,
        modelTable: c,
        viewFigure: b,
        viewColgroup: v,
        viewLeftColumn: y,
        viewRightColumn: x
      },
      widths: {
        viewFigureParentWidth: S,
        viewFigureWidth: k,
        tableWidth: I,
        leftColumnWidth: A,
        rightColumnWidth: L
      }
    };
  }
  /**
  * Registers a listener ensuring that each resizable cell have a resizer handle.
  */
  _registerResizerInserter() {
    this.editor.conversion.for("editingDowncast").add((e) => {
      e.on("insert:tableCell", (n, r, s) => {
        const o = r.item, a = s.mapper.toViewElement(o), l = s.writer;
        l.insert(l.createPositionAt(a, "end"), l.createUIElement("div", {
          class: "ck-table-column-resizer"
        }));
      }, {
        priority: "lowest"
      });
    });
  }
}
class hG extends V {
  /**
  * @inheritDoc
   */
  static get requires() {
    return [
      dG,
      oE
    ];
  }
  /**
  * @inheritDoc
   */
  static get pluginName() {
    return "TableColumnResize";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function fG(i) {
  if (Array.isArray(i)) return i;
}
function mG(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function jm(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function aE(i, t) {
  if (i) {
    if (typeof i == "string") return jm(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return jm(i, t);
  }
}
function gG() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function Iu(i, t) {
  return fG(i) || mG(i, t) || aE(i, t) || gG();
}
function Tn(i) {
  "@babel/helpers - typeof";
  return Tn = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Tn(i);
}
function Re() {
  for (var i = arguments.length, t = new Array(i), e = 0; e < i; e++)
    t[e] = arguments[e];
  if (t) {
    for (var n = [], r = 0; r < t.length; r++) {
      var s = t[r];
      if (s) {
        var o = Tn(s);
        if (o === "string" || o === "number")
          n.push(s);
        else if (o === "object") {
          var a = Array.isArray(s) ? s : Object.entries(s).map(function(l) {
            var c = Iu(l, 2), u = c[0], d = c[1];
            return d ? u : null;
          });
          n = a.length ? n.concat(a.filter(function(l) {
            return !!l;
          })) : n;
        }
      }
    }
    return n.join(" ").trim();
  }
}
function pG(i) {
  if (Array.isArray(i)) return jm(i);
}
function bG(i) {
  if (typeof Symbol < "u" && i[Symbol.iterator] != null || i["@@iterator"] != null) return Array.from(i);
}
function vG() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function dd(i) {
  return pG(i) || bG(i) || aE(i) || vG();
}
function qp(i, t) {
  if (!(i instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
function wG(i, t) {
  if (Tn(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (Tn(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function lE(i) {
  var t = wG(i, "string");
  return Tn(t) === "symbol" ? t : String(t);
}
function yG(i, t) {
  for (var e = 0; e < t.length; e++) {
    var n = t[e];
    n.enumerable = n.enumerable || !1, n.configurable = !0, "value" in n && (n.writable = !0), Object.defineProperty(i, lE(n.key), n);
  }
}
function Gp(i, t, e) {
  return e && yG(i, e), Object.defineProperty(i, "prototype", {
    writable: !1
  }), i;
}
function sh(i, t, e) {
  return t = lE(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function Sf(i, t) {
  var e = typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (!e) {
    if (Array.isArray(i) || (e = _G(i)) || t) {
      e && (i = e);
      var n = 0, r = function() {
      };
      return { s: r, n: function() {
        return n >= i.length ? { done: !0 } : { done: !1, value: i[n++] };
      }, e: function(c) {
        throw c;
      }, f: r };
    }
    throw new TypeError(`Invalid attempt to iterate non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
  }
  var s = !0, o = !1, a;
  return { s: function() {
    e = e.call(i);
  }, n: function() {
    var c = e.next();
    return s = c.done, c;
  }, e: function(c) {
    o = !0, a = c;
  }, f: function() {
    try {
      !s && e.return != null && e.return();
    } finally {
      if (o) throw a;
    }
  } };
}
function _G(i, t) {
  if (i) {
    if (typeof i == "string") return H0(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return H0(i, t);
  }
}
function H0(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
var j = /* @__PURE__ */ function() {
  function i() {
    qp(this, i);
  }
  return Gp(i, null, [{
    key: "innerWidth",
    value: function(e) {
      if (e) {
        var n = e.offsetWidth, r = getComputedStyle(e);
        return n = n + (parseFloat(r.paddingLeft) + parseFloat(r.paddingRight)), n;
      }
      return 0;
    }
  }, {
    key: "width",
    value: function(e) {
      if (e) {
        var n = e.offsetWidth, r = getComputedStyle(e);
        return n = n - (parseFloat(r.paddingLeft) + parseFloat(r.paddingRight)), n;
      }
      return 0;
    }
  }, {
    key: "getBrowserLanguage",
    value: function() {
      return navigator.userLanguage || navigator.languages && navigator.languages.length && navigator.languages[0] || navigator.language || navigator.browserLanguage || navigator.systemLanguage || "en";
    }
  }, {
    key: "getWindowScrollTop",
    value: function() {
      var e = document.documentElement;
      return (window.pageYOffset || e.scrollTop) - (e.clientTop || 0);
    }
  }, {
    key: "getWindowScrollLeft",
    value: function() {
      var e = document.documentElement;
      return (window.pageXOffset || e.scrollLeft) - (e.clientLeft || 0);
    }
  }, {
    key: "getOuterWidth",
    value: function(e, n) {
      if (e) {
        var r = e.getBoundingClientRect().width || e.offsetWidth;
        if (n) {
          var s = getComputedStyle(e);
          r = r + (parseFloat(s.marginLeft) + parseFloat(s.marginRight));
        }
        return r;
      }
      return 0;
    }
  }, {
    key: "getOuterHeight",
    value: function(e, n) {
      if (e) {
        var r = e.getBoundingClientRect().height || e.offsetHeight;
        if (n) {
          var s = getComputedStyle(e);
          r = r + (parseFloat(s.marginTop) + parseFloat(s.marginBottom));
        }
        return r;
      }
      return 0;
    }
  }, {
    key: "getClientHeight",
    value: function(e, n) {
      if (e) {
        var r = e.clientHeight;
        if (n) {
          var s = getComputedStyle(e);
          r = r + (parseFloat(s.marginTop) + parseFloat(s.marginBottom));
        }
        return r;
      }
      return 0;
    }
  }, {
    key: "getClientWidth",
    value: function(e, n) {
      if (e) {
        var r = e.clientWidth;
        if (n) {
          var s = getComputedStyle(e);
          r = r + (parseFloat(s.marginLeft) + parseFloat(s.marginRight));
        }
        return r;
      }
      return 0;
    }
  }, {
    key: "getViewport",
    value: function() {
      var e = window, n = document, r = n.documentElement, s = n.getElementsByTagName("body")[0], o = e.innerWidth || r.clientWidth || s.clientWidth, a = e.innerHeight || r.clientHeight || s.clientHeight;
      return {
        width: o,
        height: a
      };
    }
  }, {
    key: "getOffset",
    value: function(e) {
      if (e) {
        var n = e.getBoundingClientRect();
        return {
          top: n.top + (window.pageYOffset || document.documentElement.scrollTop || document.body.scrollTop || 0),
          left: n.left + (window.pageXOffset || document.documentElement.scrollLeft || document.body.scrollLeft || 0)
        };
      }
      return {
        top: "auto",
        left: "auto"
      };
    }
  }, {
    key: "index",
    value: function(e) {
      if (e)
        for (var n = e.parentNode.childNodes, r = 0, s = 0; s < n.length; s++) {
          if (n[s] === e)
            return r;
          n[s].nodeType === 1 && r++;
        }
      return -1;
    }
  }, {
    key: "addMultipleClasses",
    value: function(e, n) {
      if (e && n)
        if (e.classList)
          for (var r = n.split(" "), s = 0; s < r.length; s++)
            e.classList.add(r[s]);
        else
          for (var o = n.split(" "), a = 0; a < o.length; a++)
            e.className = e.className + (" " + o[a]);
    }
  }, {
    key: "removeMultipleClasses",
    value: function(e, n) {
      if (e && n)
        if (e.classList)
          for (var r = n.split(" "), s = 0; s < r.length; s++)
            e.classList.remove(r[s]);
        else
          for (var o = n.split(" "), a = 0; a < o.length; a++)
            e.className = e.className.replace(new RegExp("(^|\\b)" + o[a].split(" ").join("|") + "(\\b|$)", "gi"), " ");
    }
  }, {
    key: "addClass",
    value: function(e, n) {
      e && n && (e.classList ? e.classList.add(n) : e.className = e.className + (" " + n));
    }
  }, {
    key: "removeClass",
    value: function(e, n) {
      e && n && (e.classList ? e.classList.remove(n) : e.className = e.className.replace(new RegExp("(^|\\b)" + n.split(" ").join("|") + "(\\b|$)", "gi"), " "));
    }
  }, {
    key: "hasClass",
    value: function(e, n) {
      return e ? e.classList ? e.classList.contains(n) : new RegExp("(^| )" + n + "( |$)", "gi").test(e.className) : !1;
    }
  }, {
    key: "addStyles",
    value: function(e) {
      var n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
      e && Object.entries(n).forEach(function(r) {
        var s = Iu(r, 2), o = s[0], a = s[1];
        return e.style[o] = a;
      });
    }
  }, {
    key: "find",
    value: function(e, n) {
      return e ? Array.from(e.querySelectorAll(n)) : [];
    }
  }, {
    key: "findSingle",
    value: function(e, n) {
      return e ? e.querySelector(n) : null;
    }
  }, {
    key: "setAttributes",
    value: function(e) {
      var n = this, r = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
      if (e) {
        var s = function o(a, l) {
          var c, u, d = e != null && (c = e.$attrs) !== null && c !== void 0 && c[a] ? [e == null || (u = e.$attrs) === null || u === void 0 ? void 0 : u[a]] : [];
          return [l].flat().reduce(function(f, m) {
            if (m != null) {
              var g = Tn(m);
              if (g === "string" || g === "number")
                f.push(m);
              else if (g === "object") {
                var p = Array.isArray(m) ? o(a, m) : Object.entries(m).map(function(b) {
                  var v = Iu(b, 2), y = v[0], x = v[1];
                  return a === "style" && (x || x === 0) ? "".concat(y.replace(/([a-z])([A-Z])/g, "$1-$2").toLowerCase(), ":").concat(x) : x ? y : void 0;
                });
                f = p.length ? f.concat(p.filter(function(b) {
                  return !!b;
                })) : f;
              }
            }
            return f;
          }, d);
        };
        Object.entries(r).forEach(function(o) {
          var a = Iu(o, 2), l = a[0], c = a[1];
          if (c != null) {
            var u = l.match(/^on(.+)/);
            u ? e.addEventListener(u[1].toLowerCase(), c) : l === "p-bind" ? n.setAttributes(e, c) : (c = l === "class" ? dd(new Set(s("class", c))).join(" ").trim() : l === "style" ? s("style", c).join(";").trim() : c, (e.$attrs = e.$attrs || {}) && (e.$attrs[l] = c), e.setAttribute(l, c));
          }
        });
      }
    }
  }, {
    key: "getAttribute",
    value: function(e, n) {
      if (e) {
        var r = e.getAttribute(n);
        return isNaN(r) ? r === "true" || r === "false" ? r === "true" : r : +r;
      }
    }
  }, {
    key: "isAttributeEquals",
    value: function(e, n, r) {
      return e ? this.getAttribute(e, n) === r : !1;
    }
  }, {
    key: "isAttributeNotEquals",
    value: function(e, n, r) {
      return !this.isAttributeEquals(e, n, r);
    }
  }, {
    key: "getHeight",
    value: function(e) {
      if (e) {
        var n = e.offsetHeight, r = getComputedStyle(e);
        return n = n - (parseFloat(r.paddingTop) + parseFloat(r.paddingBottom) + parseFloat(r.borderTopWidth) + parseFloat(r.borderBottomWidth)), n;
      }
      return 0;
    }
  }, {
    key: "getWidth",
    value: function(e) {
      if (e) {
        var n = e.offsetWidth, r = getComputedStyle(e);
        return n = n - (parseFloat(r.paddingLeft) + parseFloat(r.paddingRight) + parseFloat(r.borderLeftWidth) + parseFloat(r.borderRightWidth)), n;
      }
      return 0;
    }
  }, {
    key: "alignOverlay",
    value: function(e, n, r) {
      var s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !0;
      e && n && (r === "self" ? this.relativePosition(e, n) : (s && (e.style.minWidth = i.getOuterWidth(n) + "px"), this.absolutePosition(e, n)));
    }
  }, {
    key: "absolutePosition",
    value: function(e, n) {
      var r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "left";
      if (e && n) {
        var s = e.offsetParent ? {
          width: e.offsetWidth,
          height: e.offsetHeight
        } : this.getHiddenElementDimensions(e), o = s.height, a = s.width, l = n.offsetHeight, c = n.offsetWidth, u = n.getBoundingClientRect(), d = this.getWindowScrollTop(), f = this.getWindowScrollLeft(), m = this.getViewport(), g, p;
        u.top + l + o > m.height ? (g = u.top + d - o, g < 0 && (g = d), e.style.transformOrigin = "bottom") : (g = l + u.top + d, e.style.transformOrigin = "top");
        var b = u.left, v = r === "left" ? 0 : a - c;
        b + c + a > m.width ? p = Math.max(0, b + f + c - a) : p = b - v + f, e.style.top = g + "px", e.style.left = p + "px";
      }
    }
  }, {
    key: "relativePosition",
    value: function(e, n) {
      if (e && n) {
        var r = e.offsetParent ? {
          width: e.offsetWidth,
          height: e.offsetHeight
        } : this.getHiddenElementDimensions(e), s = n.offsetHeight, o = n.getBoundingClientRect(), a = this.getViewport(), l, c;
        o.top + s + r.height > a.height ? (l = -1 * r.height, o.top + l < 0 && (l = -1 * o.top), e.style.transformOrigin = "bottom") : (l = s, e.style.transformOrigin = "top"), r.width > a.width ? c = o.left * -1 : o.left + r.width > a.width ? c = (o.left + r.width - a.width) * -1 : c = 0, e.style.top = l + "px", e.style.left = c + "px";
      }
    }
  }, {
    key: "flipfitCollision",
    value: function(e, n) {
      var r = this, s = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "left top", o = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "left bottom", a = arguments.length > 4 ? arguments[4] : void 0;
      if (e && n) {
        var l = n.getBoundingClientRect(), c = this.getViewport(), u = s.split(" "), d = o.split(" "), f = function(v, y) {
          return y ? +v.substring(v.search(/(\+|-)/g)) || 0 : v.substring(0, v.search(/(\+|-)/g)) || v;
        }, m = {
          my: {
            x: f(u[0]),
            y: f(u[1] || u[0]),
            offsetX: f(u[0], !0),
            offsetY: f(u[1] || u[0], !0)
          },
          at: {
            x: f(d[0]),
            y: f(d[1] || d[0]),
            offsetX: f(d[0], !0),
            offsetY: f(d[1] || d[0], !0)
          }
        }, g = {
          left: function() {
            var v = m.my.offsetX + m.at.offsetX;
            return v + l.left + (m.my.x === "left" ? 0 : -1 * (m.my.x === "center" ? r.getOuterWidth(e) / 2 : r.getOuterWidth(e)));
          },
          top: function() {
            var v = m.my.offsetY + m.at.offsetY;
            return v + l.top + (m.my.y === "top" ? 0 : -1 * (m.my.y === "center" ? r.getOuterHeight(e) / 2 : r.getOuterHeight(e)));
          }
        }, p = {
          count: {
            x: 0,
            y: 0
          },
          left: function() {
            var v = g.left(), y = i.getWindowScrollLeft();
            e.style.left = v + y + "px", this.count.x === 2 ? (e.style.left = y + "px", this.count.x = 0) : v < 0 && (this.count.x++, m.my.x = "left", m.at.x = "right", m.my.offsetX *= -1, m.at.offsetX *= -1, this.right());
          },
          right: function() {
            var v = g.left() + i.getOuterWidth(n), y = i.getWindowScrollLeft();
            e.style.left = v + y + "px", this.count.x === 2 ? (e.style.left = c.width - i.getOuterWidth(e) + y + "px", this.count.x = 0) : v + i.getOuterWidth(e) > c.width && (this.count.x++, m.my.x = "right", m.at.x = "left", m.my.offsetX *= -1, m.at.offsetX *= -1, this.left());
          },
          top: function() {
            var v = g.top(), y = i.getWindowScrollTop();
            e.style.top = v + y + "px", this.count.y === 2 ? (e.style.left = y + "px", this.count.y = 0) : v < 0 && (this.count.y++, m.my.y = "top", m.at.y = "bottom", m.my.offsetY *= -1, m.at.offsetY *= -1, this.bottom());
          },
          bottom: function() {
            var v = g.top() + i.getOuterHeight(n), y = i.getWindowScrollTop();
            e.style.top = v + y + "px", this.count.y === 2 ? (e.style.left = c.height - i.getOuterHeight(e) + y + "px", this.count.y = 0) : v + i.getOuterHeight(n) > c.height && (this.count.y++, m.my.y = "bottom", m.at.y = "top", m.my.offsetY *= -1, m.at.offsetY *= -1, this.top());
          },
          center: function(v) {
            if (v === "y") {
              var y = g.top() + i.getOuterHeight(n) / 2;
              e.style.top = y + i.getWindowScrollTop() + "px", y < 0 ? this.bottom() : y + i.getOuterHeight(n) > c.height && this.top();
            } else {
              var x = g.left() + i.getOuterWidth(n) / 2;
              e.style.left = x + i.getWindowScrollLeft() + "px", x < 0 ? this.left() : x + i.getOuterWidth(e) > c.width && this.right();
            }
          }
        };
        p[m.at.x]("x"), p[m.at.y]("y"), this.isFunction(a) && a(m);
      }
    }
  }, {
    key: "findCollisionPosition",
    value: function(e) {
      if (e) {
        var n = e === "top" || e === "bottom", r = e === "left" ? "right" : "left", s = e === "top" ? "bottom" : "top";
        return n ? {
          axis: "y",
          my: "center ".concat(s),
          at: "center ".concat(e)
        } : {
          axis: "x",
          my: "".concat(r, " center"),
          at: "".concat(e, " center")
        };
      }
    }
  }, {
    key: "getParents",
    value: function(e) {
      var n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
      return e.parentNode === null ? n : this.getParents(e.parentNode, n.concat([e.parentNode]));
    }
  }, {
    key: "getScrollableParents",
    value: function(e) {
      var n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, r = [];
      if (e) {
        var s = this.getParents(e), o = /(auto|scroll)/, a = function(x) {
          var S = x ? getComputedStyle(x) : null;
          return S && (o.test(S.getPropertyValue("overflow")) || o.test(S.getPropertyValue("overflow-x")) || o.test(S.getPropertyValue("overflow-y")));
        }, l = function(x) {
          n ? r.push(x.nodeName === "BODY" || x.nodeName === "HTML" || x.nodeType === 9 ? window : x) : r.push(x);
        }, c = Sf(s), u;
        try {
          for (c.s(); !(u = c.n()).done; ) {
            var d = u.value, f = d.nodeType === 1 && d.dataset.scrollselectors;
            if (f) {
              var m = f.split(","), g = Sf(m), p;
              try {
                for (g.s(); !(p = g.n()).done; ) {
                  var b = p.value, v = this.findSingle(d, b);
                  v && a(v) && l(v);
                }
              } catch (y) {
                g.e(y);
              } finally {
                g.f();
              }
            }
            d.nodeType === 1 && a(d) && l(d);
          }
        } catch (y) {
          c.e(y);
        } finally {
          c.f();
        }
      }
      return r.some(function(y) {
        return y === document.body || y === window;
      }) || r.push(window), r;
    }
  }, {
    key: "getHiddenElementOuterHeight",
    value: function(e) {
      if (e) {
        e.style.visibility = "hidden", e.style.display = "block";
        var n = e.offsetHeight;
        return e.style.display = "none", e.style.visibility = "visible", n;
      }
      return 0;
    }
  }, {
    key: "getHiddenElementOuterWidth",
    value: function(e) {
      if (e) {
        e.style.visibility = "hidden", e.style.display = "block";
        var n = e.offsetWidth;
        return e.style.display = "none", e.style.visibility = "visible", n;
      }
      return 0;
    }
  }, {
    key: "getHiddenElementDimensions",
    value: function(e) {
      var n = {};
      return e && (e.style.visibility = "hidden", e.style.display = "block", n.width = e.offsetWidth, n.height = e.offsetHeight, e.style.display = "none", e.style.visibility = "visible"), n;
    }
  }, {
    key: "fadeIn",
    value: function(e, n) {
      if (e) {
        e.style.opacity = 0;
        var r = +/* @__PURE__ */ new Date(), s = 0, o = function a() {
          s = +e.style.opacity + ((/* @__PURE__ */ new Date()).getTime() - r) / n, e.style.opacity = s, r = +/* @__PURE__ */ new Date(), +s < 1 && (window.requestAnimationFrame && requestAnimationFrame(a) || setTimeout(a, 16));
        };
        o();
      }
    }
  }, {
    key: "fadeOut",
    value: function(e, n) {
      if (e)
        var r = 1, s = 50, o = s / n, a = setInterval(function() {
          r = r - o, r <= 0 && (r = 0, clearInterval(a)), e.style.opacity = r;
        }, s);
    }
  }, {
    key: "getUserAgent",
    value: function() {
      return navigator.userAgent;
    }
  }, {
    key: "isIOS",
    value: function() {
      return /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;
    }
  }, {
    key: "isAndroid",
    value: function() {
      return /(android)/i.test(navigator.userAgent);
    }
  }, {
    key: "isChrome",
    value: function() {
      return /(chrome)/i.test(navigator.userAgent);
    }
  }, {
    key: "isClient",
    value: function() {
      return !!(typeof window < "u" && window.document && window.document.createElement);
    }
  }, {
    key: "isTouchDevice",
    value: function() {
      return "ontouchstart" in window || navigator.maxTouchPoints > 0 || navigator.msMaxTouchPoints > 0;
    }
  }, {
    key: "isFunction",
    value: function(e) {
      return !!(e && e.constructor && e.call && e.apply);
    }
  }, {
    key: "appendChild",
    value: function(e, n) {
      if (this.isElement(n))
        n.appendChild(e);
      else if (n.el && n.el.nativeElement)
        n.el.nativeElement.appendChild(e);
      else
        throw new Error("Cannot append " + n + " to " + e);
    }
  }, {
    key: "removeChild",
    value: function(e, n) {
      if (this.isElement(n))
        n.removeChild(e);
      else if (n.el && n.el.nativeElement)
        n.el.nativeElement.removeChild(e);
      else
        throw new Error("Cannot remove " + e + " from " + n);
    }
  }, {
    key: "isElement",
    value: function(e) {
      return (typeof HTMLElement > "u" ? "undefined" : Tn(HTMLElement)) === "object" ? e instanceof HTMLElement : e && Tn(e) === "object" && e !== null && e.nodeType === 1 && typeof e.nodeName == "string";
    }
  }, {
    key: "scrollInView",
    value: function(e, n) {
      var r = getComputedStyle(e).getPropertyValue("border-top-width"), s = r ? parseFloat(r) : 0, o = getComputedStyle(e).getPropertyValue("padding-top"), a = o ? parseFloat(o) : 0, l = e.getBoundingClientRect(), c = n.getBoundingClientRect(), u = c.top + document.body.scrollTop - (l.top + document.body.scrollTop) - s - a, d = e.scrollTop, f = e.clientHeight, m = this.getOuterHeight(n);
      u < 0 ? e.scrollTop = d + u : u + m > f && (e.scrollTop = d + u - f + m);
    }
  }, {
    key: "clearSelection",
    value: function() {
      if (window.getSelection)
        window.getSelection().empty ? window.getSelection().empty() : window.getSelection().removeAllRanges && window.getSelection().rangeCount > 0 && window.getSelection().getRangeAt(0).getClientRects().length > 0 && window.getSelection().removeAllRanges();
      else if (document.selection && document.selection.empty)
        try {
          document.selection.empty();
        } catch {
        }
    }
  }, {
    key: "calculateScrollbarWidth",
    value: function(e) {
      if (e) {
        var n = getComputedStyle(e);
        return e.offsetWidth - e.clientWidth - parseFloat(n.borderLeftWidth) - parseFloat(n.borderRightWidth);
      }
      if (this.calculatedScrollbarWidth != null)
        return this.calculatedScrollbarWidth;
      var r = document.createElement("div");
      r.className = "p-scrollbar-measure", document.body.appendChild(r);
      var s = r.offsetWidth - r.clientWidth;
      return document.body.removeChild(r), this.calculatedScrollbarWidth = s, s;
    }
  }, {
    key: "calculateBodyScrollbarWidth",
    value: function() {
      return window.innerWidth - document.documentElement.offsetWidth;
    }
  }, {
    key: "getBrowser",
    value: function() {
      if (!this.browser) {
        var e = this.resolveUserAgent();
        this.browser = {}, e.browser && (this.browser[e.browser] = !0, this.browser.version = e.version), this.browser.chrome ? this.browser.webkit = !0 : this.browser.webkit && (this.browser.safari = !0);
      }
      return this.browser;
    }
  }, {
    key: "resolveUserAgent",
    value: function() {
      var e = navigator.userAgent.toLowerCase(), n = /(chrome)[ ]([\w.]+)/.exec(e) || /(webkit)[ ]([\w.]+)/.exec(e) || /(opera)(?:.*version|)[ ]([\w.]+)/.exec(e) || /(msie) ([\w.]+)/.exec(e) || e.indexOf("compatible") < 0 && /(mozilla)(?:.*? rv:([\w.]+)|)/.exec(e) || [];
      return {
        browser: n[1] || "",
        version: n[2] || "0"
      };
    }
  }, {
    key: "blockBodyScroll",
    value: function() {
      var e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "p-overflow-hidden", n = !!document.body.style.getPropertyValue("--scrollbar-width");
      !n && document.body.style.setProperty("--scrollbar-width", this.calculateBodyScrollbarWidth() + "px"), this.addClass(document.body, e);
    }
  }, {
    key: "unblockBodyScroll",
    value: function() {
      var e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "p-overflow-hidden";
      document.body.style.removeProperty("--scrollbar-width"), this.removeClass(document.body, e);
    }
  }, {
    key: "isVisible",
    value: function(e) {
      return e && (e.clientHeight !== 0 || e.getClientRects().length !== 0 || getComputedStyle(e).display !== "none");
    }
  }, {
    key: "isExist",
    value: function(e) {
      return !!(e !== null && typeof e < "u" && e.nodeName && e.parentNode);
    }
  }, {
    key: "getFocusableElements",
    value: function(e) {
      var n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "", r = i.find(e, 'button:not([tabindex = "-1"]):not([disabled]):not([style*="display:none"]):not([hidden])'.concat(n, `,
                [href][clientHeight][clientWidth]:not([tabindex = "-1"]):not([disabled]):not([style*="display:none"]):not([hidden])`).concat(n, `,
                input:not([tabindex = "-1"]):not([disabled]):not([style*="display:none"]):not([hidden])`).concat(n, `,
                select:not([tabindex = "-1"]):not([disabled]):not([style*="display:none"]):not([hidden])`).concat(n, `,
                textarea:not([tabindex = "-1"]):not([disabled]):not([style*="display:none"]):not([hidden])`).concat(n, `,
                [tabIndex]:not([tabIndex = "-1"]):not([disabled]):not([style*="display:none"]):not([hidden])`).concat(n, `,
                [contenteditable]:not([tabIndex = "-1"]):not([disabled]):not([style*="display:none"]):not([hidden])`).concat(n)), s = [], o = Sf(r), a;
      try {
        for (o.s(); !(a = o.n()).done; ) {
          var l = a.value;
          getComputedStyle(l).display !== "none" && getComputedStyle(l).visibility !== "hidden" && s.push(l);
        }
      } catch (c) {
        o.e(c);
      } finally {
        o.f();
      }
      return s;
    }
  }, {
    key: "getFirstFocusableElement",
    value: function(e, n) {
      var r = i.getFocusableElements(e, n);
      return r.length > 0 ? r[0] : null;
    }
  }, {
    key: "getLastFocusableElement",
    value: function(e, n) {
      var r = i.getFocusableElements(e, n);
      return r.length > 0 ? r[r.length - 1] : null;
    }
    /**
     * Focus an input element if it does not already have focus.
     *
     * @param {HTMLElement} el a HTML element
     * @param {boolean} scrollTo flag to control whether to scroll to the element, false by default
     */
  }, {
    key: "focus",
    value: function(e, n) {
      var r = n === void 0 ? !0 : !n;
      e && document.activeElement !== e && e.focus({
        preventScroll: r
      });
    }
    /**
     * Focus the first focusable element if it does not already have focus.
     *
     * @param {HTMLElement} el a HTML element
     * @param {boolean} scrollTo flag to control whether to scroll to the element, false by default
     * @return {HTMLElement | undefined} the first focusable HTML element found
     */
  }, {
    key: "focusFirstElement",
    value: function(e, n) {
      if (e) {
        var r = i.getFirstFocusableElement(e);
        return r && i.focus(r, n), r;
      }
    }
  }, {
    key: "getCursorOffset",
    value: function(e, n, r, s) {
      if (e) {
        var o = getComputedStyle(e), a = document.createElement("div");
        a.style.position = "absolute", a.style.top = "0px", a.style.left = "0px", a.style.visibility = "hidden", a.style.pointerEvents = "none", a.style.overflow = o.overflow, a.style.width = o.width, a.style.height = o.height, a.style.padding = o.padding, a.style.border = o.border, a.style.overflowWrap = o.overflowWrap, a.style.whiteSpace = o.whiteSpace, a.style.lineHeight = o.lineHeight, a.innerHTML = n.replace(/\r\n|\r|\n/g, "<br />");
        var l = document.createElement("span");
        l.textContent = s, a.appendChild(l);
        var c = document.createTextNode(r);
        a.appendChild(c), document.body.appendChild(a);
        var u = l.offsetLeft, d = l.offsetTop, f = l.clientHeight;
        return document.body.removeChild(a), {
          left: Math.abs(u - e.scrollLeft),
          top: Math.abs(d - e.scrollTop) + f
        };
      }
      return {
        top: "auto",
        left: "auto"
      };
    }
  }, {
    key: "invokeElementMethod",
    value: function(e, n, r) {
      e[n].apply(e, r);
    }
  }, {
    key: "isClickable",
    value: function(e) {
      var n = e.nodeName, r = e.parentElement && e.parentElement.nodeName;
      return n === "INPUT" || n === "TEXTAREA" || n === "BUTTON" || n === "A" || r === "INPUT" || r === "TEXTAREA" || r === "BUTTON" || r === "A" || this.hasClass(e, "p-button") || this.hasClass(e.parentElement, "p-button") || this.hasClass(e.parentElement, "p-checkbox") || this.hasClass(e.parentElement, "p-radiobutton");
    }
  }, {
    key: "applyStyle",
    value: function(e, n) {
      if (typeof n == "string")
        e.style.cssText = this.style;
      else
        for (var r in this.style)
          e.style[r] = n[r];
    }
  }, {
    key: "exportCSV",
    value: function(e, n) {
      var r = new Blob([e], {
        type: "application/csv;charset=utf-8;"
      });
      if (window.navigator.msSaveOrOpenBlob)
        navigator.msSaveOrOpenBlob(r, n + ".csv");
      else {
        var s = i.saveAs({
          name: n + ".csv",
          src: URL.createObjectURL(r)
        });
        s || (e = "data:text/csv;charset=utf-8," + e, window.open(encodeURI(e)));
      }
    }
  }, {
    key: "saveAs",
    value: function(e) {
      if (e) {
        var n = document.createElement("a");
        if (n.download !== void 0) {
          var r = e.name, s = e.src;
          return n.setAttribute("href", s), n.setAttribute("download", r), n.style.display = "none", document.body.appendChild(n), n.click(), document.body.removeChild(n), !0;
        }
      }
      return !1;
    }
  }, {
    key: "createInlineStyle",
    value: function(e, n) {
      var r = document.createElement("style");
      return i.addNonce(r, e), n || (n = document.head), n.appendChild(r), r;
    }
  }, {
    key: "removeInlineStyle",
    value: function(e) {
      if (this.isExist(e)) {
        try {
          e.parentNode.removeChild(e);
        } catch {
        }
        e = null;
      }
      return e;
    }
  }, {
    key: "addNonce",
    value: function(e, n) {
      try {
        n || (n = process.env.REACT_APP_CSS_NONCE);
      } catch {
      }
      n && e.setAttribute("nonce", n);
    }
  }, {
    key: "getTargetElement",
    value: function(e) {
      if (!e)
        return null;
      if (e === "document")
        return document;
      if (e === "window")
        return window;
      if (Tn(e) === "object" && e.hasOwnProperty("current"))
        return this.isExist(e.current) ? e.current : null;
      var n = function(o) {
        return !!(o && o.constructor && o.call && o.apply);
      }, r = n(e) ? e() : e;
      return r && r.nodeType === 9 || this.isExist(r) ? r : null;
    }
    /**
     * Get the attribute names for an element and sorts them alpha for comparison
     */
  }, {
    key: "getAttributeNames",
    value: function(e) {
      var n, r, s;
      for (r = [], s = e.attributes, n = 0; n < s.length; ++n)
        r.push(s[n].nodeName);
      return r.sort(), r;
    }
    /**
     * Compare two elements for equality.  Even will compare if the style element
     * is out of order for example:
     *
     * elem1 = style="color: red; font-size: 28px"
     * elem2 = style="font-size: 28px; color: red"
     */
  }, {
    key: "isEqualElement",
    value: function(e, n) {
      var r, s, o, a, l;
      if (r = i.getAttributeNames(e), s = i.getAttributeNames(n), r.join(",") !== s.join(","))
        return !1;
      for (var c = 0; c < r.length; ++c)
        if (o = r[c], o === "style")
          for (var u = e.style, d = n.style, f = /^\d+$/, m = 0, g = Object.keys(u); m < g.length; m++) {
            var p = g[m];
            if (!f.test(p) && u[p] !== d[p])
              return !1;
          }
        else if (e.getAttribute(o) !== n.getAttribute(o))
          return !1;
      for (a = e.firstChild, l = n.firstChild; a && l; a = a.nextSibling, l = l.nextSibling) {
        if (a.nodeType !== l.nodeType)
          return !1;
        if (a.nodeType === 1) {
          if (!i.isEqualElement(a, l))
            return !1;
        } else if (a.nodeValue !== l.nodeValue)
          return !1;
      }
      return !(a || l);
    }
  }, {
    key: "hasCSSAnimation",
    value: function(e) {
      if (e) {
        var n = getComputedStyle(e), r = parseFloat(n.getPropertyValue("animation-duration") || "0");
        return r > 0;
      }
      return !1;
    }
  }, {
    key: "hasCSSTransition",
    value: function(e) {
      if (e) {
        var n = getComputedStyle(e), r = parseFloat(n.getPropertyValue("transition-duration") || "0");
        return r > 0;
      }
      return !1;
    }
  }]);
}();
sh(j, "DATA_PROPS", ["data-"]);
sh(j, "ARIA_PROPS", ["aria", "focus-target"]);
function CG() {
  var i = /* @__PURE__ */ new Map();
  return {
    on: function(e, n) {
      var r = i.get(e);
      r ? r.push(n) : r = [n], i.set(e, r);
    },
    off: function(e, n) {
      var r = i.get(e);
      r && r.splice(r.indexOf(n) >>> 0, 1);
    },
    emit: function(e, n) {
      var r = i.get(e);
      r && r.slice().forEach(function(s) {
        return s(n);
      });
    }
  };
}
function Wm() {
  return Wm = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, Wm.apply(this, arguments);
}
function kG(i, t) {
  var e = typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (!e) {
    if (Array.isArray(i) || (e = xG(i)) || t) {
      e && (i = e);
      var n = 0, r = function() {
      };
      return { s: r, n: function() {
        return n >= i.length ? { done: !0 } : { done: !1, value: i[n++] };
      }, e: function(c) {
        throw c;
      }, f: r };
    }
    throw new TypeError(`Invalid attempt to iterate non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
  }
  var s = !0, o = !1, a;
  return { s: function() {
    e = e.call(i);
  }, n: function() {
    var c = e.next();
    return s = c.done, c;
  }, e: function(c) {
    o = !0, a = c;
  }, f: function() {
    try {
      !s && e.return != null && e.return();
    } finally {
      if (o) throw a;
    }
  } };
}
function xG(i, t) {
  if (i) {
    if (typeof i == "string") return j0(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return j0(i, t);
  }
}
function j0(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
var z = /* @__PURE__ */ function() {
  function i() {
    qp(this, i);
  }
  return Gp(i, null, [{
    key: "equals",
    value: function(e, n, r) {
      return r && e && Tn(e) === "object" && n && Tn(n) === "object" ? this.deepEquals(this.resolveFieldData(e, r), this.resolveFieldData(n, r)) : this.deepEquals(e, n);
    }
    /**
     * Compares two JSON objects for deep equality recursively comparing both objects.
     * @param {*} a the first JSON object
     * @param {*} b the second JSON object
     * @returns true if equals, false it not
     */
  }, {
    key: "deepEquals",
    value: function(e, n) {
      if (e === n)
        return !0;
      if (e && n && Tn(e) === "object" && Tn(n) === "object") {
        var r = Array.isArray(e), s = Array.isArray(n), o, a, l;
        if (r && s) {
          if (a = e.length, a !== n.length)
            return !1;
          for (o = a; o-- !== 0; )
            if (!this.deepEquals(e[o], n[o]))
              return !1;
          return !0;
        }
        if (r !== s)
          return !1;
        var c = e instanceof Date, u = n instanceof Date;
        if (c !== u)
          return !1;
        if (c && u)
          return e.getTime() === n.getTime();
        var d = e instanceof RegExp, f = n instanceof RegExp;
        if (d !== f)
          return !1;
        if (d && f)
          return e.toString() === n.toString();
        var m = Object.keys(e);
        if (a = m.length, a !== Object.keys(n).length)
          return !1;
        for (o = a; o-- !== 0; )
          if (!Object.prototype.hasOwnProperty.call(n, m[o]))
            return !1;
        for (o = a; o-- !== 0; )
          if (l = m[o], !this.deepEquals(e[l], n[l]))
            return !1;
        return !0;
      }
      return e !== e && n !== n;
    }
  }, {
    key: "resolveFieldData",
    value: function(e, n) {
      if (!e || !n)
        return null;
      try {
        var r = e[n];
        if (this.isNotEmpty(r))
          return r;
      } catch {
      }
      if (Object.keys(e).length) {
        if (this.isFunction(n))
          return n(e);
        if (this.isNotEmpty(e[n]))
          return e[n];
        if (n.indexOf(".") === -1)
          return e[n];
        for (var s = n.split("."), o = e, a = 0, l = s.length; a < l; ++a) {
          if (o == null)
            return null;
          o = o[s[a]];
        }
        return o;
      }
      return null;
    }
  }, {
    key: "findDiffKeys",
    value: function(e, n) {
      return !e || !n ? {} : Object.keys(e).filter(function(r) {
        return !n.hasOwnProperty(r);
      }).reduce(function(r, s) {
        return r[s] = e[s], r;
      }, {});
    }
    /**
     * Removes keys from a JSON object that start with a string such as "data" to get all "data-id" type properties.
     *
     * @param {any} obj the JSON object to reduce
     * @param {string[]} startsWiths the string(s) to check if the property starts with this key
     * @returns the JSON object containing only the key/values that match the startsWith string
     */
  }, {
    key: "reduceKeys",
    value: function(e, n) {
      var r = {};
      return !e || !n || n.length === 0 || Object.keys(e).filter(function(s) {
        return n.some(function(o) {
          return s.startsWith(o);
        });
      }).forEach(function(s) {
        r[s] = e[s], delete e[s];
      }), r;
    }
  }, {
    key: "reorderArray",
    value: function(e, n, r) {
      e && n !== r && (r >= e.length && (r = r % e.length, n = n % e.length), e.splice(r, 0, e.splice(n, 1)[0]));
    }
  }, {
    key: "findIndexInList",
    value: function(e, n, r) {
      var s = this;
      return n ? r ? n.findIndex(function(o) {
        return s.equals(o, e, r);
      }) : n.findIndex(function(o) {
        return o === e;
      }) : -1;
    }
  }, {
    key: "getJSXElement",
    value: function(e) {
      for (var n = arguments.length, r = new Array(n > 1 ? n - 1 : 0), s = 1; s < n; s++)
        r[s - 1] = arguments[s];
      return this.isFunction(e) ? e.apply(void 0, r) : e;
    }
  }, {
    key: "getItemValue",
    value: function(e) {
      for (var n = arguments.length, r = new Array(n > 1 ? n - 1 : 0), s = 1; s < n; s++)
        r[s - 1] = arguments[s];
      return this.isFunction(e) ? e.apply(void 0, r) : e;
    }
  }, {
    key: "getProp",
    value: function(e) {
      var n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "", r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, s = e ? e[n] : void 0;
      return s === void 0 ? r[n] : s;
    }
  }, {
    key: "getPropCaseInsensitive",
    value: function(e, n) {
      var r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, s = this.toFlatCase(n);
      for (var o in e)
        if (e.hasOwnProperty(o) && this.toFlatCase(o) === s)
          return e[o];
      for (var a in r)
        if (r.hasOwnProperty(a) && this.toFlatCase(a) === s)
          return r[a];
    }
  }, {
    key: "getMergedProps",
    value: function(e, n) {
      return Object.assign({}, n, e);
    }
  }, {
    key: "getDiffProps",
    value: function(e, n) {
      return this.findDiffKeys(e, n);
    }
  }, {
    key: "getPropValue",
    value: function(e) {
      for (var n = arguments.length, r = new Array(n > 1 ? n - 1 : 0), s = 1; s < n; s++)
        r[s - 1] = arguments[s];
      return this.isFunction(e) ? e.apply(void 0, r) : e;
    }
  }, {
    key: "getComponentProp",
    value: function(e) {
      var n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "", r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
      return this.isNotEmpty(e) ? this.getProp(e.props, n, r) : void 0;
    }
  }, {
    key: "getComponentProps",
    value: function(e, n) {
      return this.isNotEmpty(e) ? this.getMergedProps(e.props, n) : void 0;
    }
  }, {
    key: "getComponentDiffProps",
    value: function(e, n) {
      return this.isNotEmpty(e) ? this.getDiffProps(e.props, n) : void 0;
    }
  }, {
    key: "isValidChild",
    value: function(e, n, r) {
      if (e) {
        var s, o = this.getComponentProp(e, "__TYPE") || (e.type ? e.type.displayName : void 0);
        !o && e !== null && e !== void 0 && (s = e.type) !== null && s !== void 0 && (s = s._payload) !== null && s !== void 0 && s.value && (o = e.type._payload.value.find(function(c) {
          return c === n;
        }));
        var a = o === n;
        try {
          var l;
        } catch {
        }
        return a;
      }
      return !1;
    }
  }, {
    key: "getRefElement",
    value: function(e) {
      return e ? Tn(e) === "object" && e.hasOwnProperty("current") ? e.current : e : null;
    }
  }, {
    key: "combinedRefs",
    value: function(e, n) {
      e && n && (typeof n == "function" ? n(e.current) : n.current = e.current);
    }
  }, {
    key: "removeAccents",
    value: function(e) {
      return e && e.search(/[\xC0-\xFF]/g) > -1 && (e = e.replace(/[\xC0-\xC5]/g, "A").replace(/[\xC6]/g, "AE").replace(/[\xC7]/g, "C").replace(/[\xC8-\xCB]/g, "E").replace(/[\xCC-\xCF]/g, "I").replace(/[\xD0]/g, "D").replace(/[\xD1]/g, "N").replace(/[\xD2-\xD6\xD8]/g, "O").replace(/[\xD9-\xDC]/g, "U").replace(/[\xDD]/g, "Y").replace(/[\xDE]/g, "P").replace(/[\xE0-\xE5]/g, "a").replace(/[\xE6]/g, "ae").replace(/[\xE7]/g, "c").replace(/[\xE8-\xEB]/g, "e").replace(/[\xEC-\xEF]/g, "i").replace(/[\xF1]/g, "n").replace(/[\xF2-\xF6\xF8]/g, "o").replace(/[\xF9-\xFC]/g, "u").replace(/[\xFE]/g, "p").replace(/[\xFD\xFF]/g, "y")), e;
    }
  }, {
    key: "toFlatCase",
    value: function(e) {
      return this.isNotEmpty(e) && this.isString(e) ? e.replace(/(-|_)/g, "").toLowerCase() : e;
    }
  }, {
    key: "toCapitalCase",
    value: function(e) {
      return this.isNotEmpty(e) && this.isString(e) ? e[0].toUpperCase() + e.slice(1) : e;
    }
  }, {
    key: "trim",
    value: function(e) {
      return this.isNotEmpty(e) && this.isString(e) ? e.trim() : e;
    }
  }, {
    key: "isEmpty",
    value: function(e) {
      return e == null || e === "" || Array.isArray(e) && e.length === 0 || !(e instanceof Date) && Tn(e) === "object" && Object.keys(e).length === 0;
    }
  }, {
    key: "isNotEmpty",
    value: function(e) {
      return !this.isEmpty(e);
    }
  }, {
    key: "isFunction",
    value: function(e) {
      return !!(e && e.constructor && e.call && e.apply);
    }
  }, {
    key: "isObject",
    value: function(e) {
      return e !== null && e instanceof Object && e.constructor === Object;
    }
  }, {
    key: "isDate",
    value: function(e) {
      return e !== null && e instanceof Date && e.constructor === Date;
    }
  }, {
    key: "isArray",
    value: function(e) {
      return e !== null && Array.isArray(e);
    }
  }, {
    key: "isString",
    value: function(e) {
      return e !== null && typeof e == "string";
    }
  }, {
    key: "isPrintableCharacter",
    value: function() {
      var e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "";
      return this.isNotEmpty(e) && e.length === 1 && e.match(/\S| /);
    }
  }, {
    key: "isLetter",
    value: function(e) {
      return /^[a-zA-Z\u00C0-\u017F]$/.test(e);
    }
  }, {
    key: "isScalar",
    value: function(e) {
      return e != null && (typeof e == "string" || typeof e == "number" || typeof e == "bigint" || typeof e == "boolean");
    }
    /**
     * Firefox-v103 does not currently support the "findLast" method. It is stated that this method will be supported with Firefox-v104.
     * https://caniuse.com/mdn-javascript_builtins_array_findlast
     */
  }, {
    key: "findLast",
    value: function(e, n) {
      var r;
      if (this.isNotEmpty(e))
        try {
          r = e.findLast(n);
        } catch {
          r = dd(e).reverse().find(n);
        }
      return r;
    }
    /**
     * Firefox-v103 does not currently support the "findLastIndex" method. It is stated that this method will be supported with Firefox-v104.
     * https://caniuse.com/mdn-javascript_builtins_array_findlastindex
     */
  }, {
    key: "findLastIndex",
    value: function(e, n) {
      var r = -1;
      if (this.isNotEmpty(e))
        try {
          r = e.findLastIndex(n);
        } catch {
          r = e.lastIndexOf(dd(e).reverse().find(n));
        }
      return r;
    }
  }, {
    key: "sort",
    value: function(e, n) {
      var r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1, s = arguments.length > 3 ? arguments[3] : void 0, o = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : 1, a = this.compare(e, n, s, r), l = r;
      return (this.isEmpty(e) || this.isEmpty(n)) && (l = o === 1 ? r : o), l * a;
    }
  }, {
    key: "compare",
    value: function(e, n, r) {
      var s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 1, o = -1, a = this.isEmpty(e), l = this.isEmpty(n);
      return a && l ? o = 0 : a ? o = s : l ? o = -s : typeof e == "string" && typeof n == "string" ? o = r(e, n) : o = e < n ? -1 : e > n ? 1 : 0, o;
    }
  }, {
    key: "localeComparator",
    value: function(e) {
      return new Intl.Collator(e, {
        numeric: !0
      }).compare;
    }
  }, {
    key: "findChildrenByKey",
    value: function(e, n) {
      var r = kG(e), s;
      try {
        for (r.s(); !(s = r.n()).done; ) {
          var o = s.value;
          if (o.key === n)
            return o.children || [];
          if (o.children) {
            var a = this.findChildrenByKey(o.children, n);
            if (a.length > 0)
              return a;
          }
        }
      } catch (l) {
        r.e(l);
      } finally {
        r.f();
      }
      return [];
    }
    /**
     * This function takes mutates and object with a new value given
     * a specific field. This will handle deeply nested fields that
     * need to be modified or created.
     *
     * e.g:
     * data = {
     *  nested: {
     *      foo: "bar"
     *  }
     * }
     *
     * field = "nested.foo"
     * value = "baz"
     *
     * The function will mutate data to be
     * e.g:
     * data = {
     *  nested: {
     *      foo: "baz"
     *  }
     * }
     *
     * @param {object} data the object to be modified
     * @param {string} field the field in the object to replace
     * @param {any} value the value to have replaced in the field
     */
  }, {
    key: "mutateFieldData",
    value: function(e, n, r) {
      if (!(Tn(e) !== "object" || typeof n != "string"))
        for (var s = n.split("."), o = e, a = 0, l = s.length; a < l; ++a) {
          if (a + 1 - l === 0) {
            o[s[a]] = r;
            break;
          }
          o[s[a]] || (o[s[a]] = {}), o = o[s[a]];
        }
    }
  }]);
}();
function W0(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function EG(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? W0(Object(e), !0).forEach(function(n) {
      sh(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : W0(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var Mi = /* @__PURE__ */ function() {
  function i() {
    qp(this, i);
  }
  return Gp(i, null, [{
    key: "getJSXIcon",
    value: function(e) {
      var n = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, r = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, s = null;
      if (e !== null) {
        var o = Tn(e), a = Re(n.className, o === "string" && e);
        if (s = /* @__PURE__ */ w.createElement("span", Wm({}, n, {
          className: a
        })), o !== "string") {
          var l = EG({
            iconProps: n,
            element: s
          }, r);
          return z.getJSXElement(e, l);
        }
      }
      return s;
    }
  }]);
}();
function U0(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function q0(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? U0(Object(e), !0).forEach(function(n) {
      sh(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : U0(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
function hd(i) {
  var t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  if (i) {
    var e = function(o) {
      return typeof o == "function";
    }, n = t.classNameMergeFunction, r = e(n);
    return i.reduce(function(s, o) {
      if (!o)
        return s;
      var a = function() {
        var u = o[l];
        if (l === "style")
          s.style = q0(q0({}, s.style), o.style);
        else if (l === "className") {
          var d = "";
          r ? d = n(s.className, o.className) : d = [s.className, o.className].join(" ").trim(), s.className = d || void 0;
        } else if (e(u)) {
          var f = s[l];
          s[l] = f ? function() {
            f.apply(void 0, arguments), u.apply(void 0, arguments);
          } : u;
        } else
          s[l] = u;
      };
      for (var l in o)
        a();
      return s;
    }, {});
  }
}
var G0 = 0;
function Kp() {
  var i = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "pr_id_";
  return G0++, "".concat(i).concat(G0);
}
function TG() {
  var i = [], t = function(a, l) {
    var c = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 999, u = r(a, l, c), d = u.value + (u.key === a ? 0 : c) + 1;
    return i.push({
      key: a,
      value: d
    }), d;
  }, e = function(a) {
    i = i.filter(function(l) {
      return l.value !== a;
    });
  }, n = function(a, l) {
    return r(a, l).value;
  }, r = function(a, l) {
    var c = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
    return dd(i).reverse().find(function(u) {
      return l ? !0 : u.key === a;
    }) || {
      key: a,
      value: c
    };
  }, s = function(a) {
    return a && parseInt(a.style.zIndex, 10) || 0;
  };
  return {
    get: s,
    set: function(a, l, c, u) {
      l && (l.style.zIndex = String(t(a, c, u)));
    },
    clear: function(a) {
      a && (e(Ki.get(a)), a.style.zIndex = "");
    },
    getCurrent: function(a, l) {
      return n(a, l);
    }
  };
}
var Ki = TG(), Bn = Object.freeze({
  STARTS_WITH: "startsWith",
  CONTAINS: "contains",
  NOT_CONTAINS: "notContains",
  ENDS_WITH: "endsWith",
  EQUALS: "equals",
  NOT_EQUALS: "notEquals",
  IN: "in",
  LESS_THAN: "lt",
  LESS_THAN_OR_EQUAL_TO: "lte",
  GREATER_THAN: "gt",
  GREATER_THAN_OR_EQUAL_TO: "gte",
  BETWEEN: "between",
  DATE_IS: "dateIs",
  DATE_IS_NOT: "dateIsNot",
  DATE_BEFORE: "dateBefore",
  DATE_AFTER: "dateAfter",
  CUSTOM: "custom"
});
function K0(i, t) {
  var e = typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (!e) {
    if (Array.isArray(i) || (e = SG(i)) || t) {
      e && (i = e);
      var n = 0, r = function() {
      };
      return { s: r, n: function() {
        return n >= i.length ? { done: !0 } : { done: !1, value: i[n++] };
      }, e: function(c) {
        throw c;
      }, f: r };
    }
    throw new TypeError(`Invalid attempt to iterate non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
  }
  var s = !0, o = !1, a;
  return { s: function() {
    e = e.call(i);
  }, n: function() {
    var c = e.next();
    return s = c.done, c;
  }, e: function(c) {
    o = !0, a = c;
  }, f: function() {
    try {
      !s && e.return != null && e.return();
    } finally {
      if (o) throw a;
    }
  } };
}
function SG(i, t) {
  if (i) {
    if (typeof i == "string") return Z0(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return Z0(i, t);
  }
}
function Z0(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
var Y0 = {
  filter: function(t, e, n, r, s) {
    var o = [];
    if (!t)
      return o;
    var a = K0(t), l;
    try {
      for (a.s(); !(l = a.n()).done; ) {
        var c = l.value;
        if (typeof c == "string") {
          if (this.filters[r](c, n, s)) {
            o.push(c);
            continue;
          }
        } else {
          var u = K0(e), d;
          try {
            for (u.s(); !(d = u.n()).done; ) {
              var f = d.value, m = z.resolveFieldData(c, f);
              if (this.filters[r](m, n, s)) {
                o.push(c);
                break;
              }
            }
          } catch (g) {
            u.e(g);
          } finally {
            u.f();
          }
        }
      }
    } catch (g) {
      a.e(g);
    } finally {
      a.f();
    }
    return o;
  },
  filters: {
    startsWith: function(t, e, n) {
      if (e == null || e.trim() === "")
        return !0;
      if (t == null)
        return !1;
      var r = z.removeAccents(e.toString()).toLocaleLowerCase(n), s = z.removeAccents(t.toString()).toLocaleLowerCase(n);
      return s.slice(0, r.length) === r;
    },
    contains: function(t, e, n) {
      if (e == null || typeof e == "string" && e.trim() === "")
        return !0;
      if (t == null)
        return !1;
      var r = z.removeAccents(e.toString()).toLocaleLowerCase(n), s = z.removeAccents(t.toString()).toLocaleLowerCase(n);
      return s.indexOf(r) !== -1;
    },
    notContains: function(t, e, n) {
      if (e == null || typeof e == "string" && e.trim() === "")
        return !0;
      if (t == null)
        return !1;
      var r = z.removeAccents(e.toString()).toLocaleLowerCase(n), s = z.removeAccents(t.toString()).toLocaleLowerCase(n);
      return s.indexOf(r) === -1;
    },
    endsWith: function(t, e, n) {
      if (e == null || e.trim() === "")
        return !0;
      if (t == null)
        return !1;
      var r = z.removeAccents(e.toString()).toLocaleLowerCase(n), s = z.removeAccents(t.toString()).toLocaleLowerCase(n);
      return s.indexOf(r, s.length - r.length) !== -1;
    },
    equals: function(t, e, n) {
      return e == null || typeof e == "string" && e.trim() === "" ? !0 : t == null ? !1 : t.getTime && e.getTime ? t.getTime() === e.getTime() : z.removeAccents(t.toString()).toLocaleLowerCase(n) === z.removeAccents(e.toString()).toLocaleLowerCase(n);
    },
    notEquals: function(t, e, n) {
      return e == null || typeof e == "string" && e.trim() === "" || t == null ? !0 : t.getTime && e.getTime ? t.getTime() !== e.getTime() : z.removeAccents(t.toString()).toLocaleLowerCase(n) !== z.removeAccents(e.toString()).toLocaleLowerCase(n);
    },
    in: function(t, e) {
      if (e == null || e.length === 0)
        return !0;
      for (var n = 0; n < e.length; n++)
        if (z.equals(t, e[n]))
          return !0;
      return !1;
    },
    notIn: function(t, e) {
      if (e == null || e.length === 0)
        return !0;
      for (var n = 0; n < e.length; n++)
        if (z.equals(t, e[n]))
          return !1;
      return !0;
    },
    between: function(t, e) {
      return e == null || e[0] == null || e[1] == null ? !0 : t == null ? !1 : t.getTime ? e[0].getTime() <= t.getTime() && t.getTime() <= e[1].getTime() : e[0] <= t && t <= e[1];
    },
    lt: function(t, e) {
      return e == null ? !0 : t == null ? !1 : t.getTime && e.getTime ? t.getTime() < e.getTime() : t < e;
    },
    lte: function(t, e) {
      return e == null ? !0 : t == null ? !1 : t.getTime && e.getTime ? t.getTime() <= e.getTime() : t <= e;
    },
    gt: function(t, e) {
      return e == null ? !0 : t == null ? !1 : t.getTime && e.getTime ? t.getTime() > e.getTime() : t > e;
    },
    gte: function(t, e) {
      return e == null ? !0 : t == null ? !1 : t.getTime && e.getTime ? t.getTime() >= e.getTime() : t >= e;
    },
    dateIs: function(t, e) {
      return e == null ? !0 : t == null ? !1 : t.toDateString() === e.toDateString();
    },
    dateIsNot: function(t, e) {
      return e == null ? !0 : t == null ? !1 : t.toDateString() !== e.toDateString();
    },
    dateBefore: function(t, e) {
      return e == null ? !0 : t == null ? !1 : t.getTime() < e.getTime();
    },
    dateAfter: function(t, e) {
      return e == null ? !0 : t == null ? !1 : t.getTime() > e.getTime();
    }
  },
  register: function(t, e) {
    this.filters[t] = e;
  }
};
function Gl(i) {
  "@babel/helpers - typeof";
  return Gl = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Gl(i);
}
function AG(i, t) {
  if (Gl(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (Gl(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function PG(i) {
  var t = AG(i, "string");
  return Gl(t) === "symbol" ? t : String(t);
}
function Ti(i, t, e) {
  return t = PG(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function IG(i, t, e) {
  return Object.defineProperty(i, "prototype", {
    writable: !1
  }), i;
}
function RG(i, t) {
  if (!(i instanceof t))
    throw new TypeError("Cannot call a class as a function");
}
var Un = /* @__PURE__ */ IG(function i() {
  RG(this, i);
});
Ti(Un, "ripple", !1);
Ti(Un, "inputStyle", "outlined");
Ti(Un, "locale", "en");
Ti(Un, "appendTo", null);
Ti(Un, "cssTransition", !0);
Ti(Un, "autoZIndex", !0);
Ti(Un, "hideOverlaysOnDocumentScrolling", !1);
Ti(Un, "nonce", null);
Ti(Un, "nullSortOrder", 1);
Ti(Un, "zIndex", {
  modal: 1100,
  overlay: 1e3,
  menu: 1e3,
  tooltip: 1100,
  toast: 1200
});
Ti(Un, "pt", void 0);
Ti(Un, "filterMatchModeOptions", {
  text: [Bn.STARTS_WITH, Bn.CONTAINS, Bn.NOT_CONTAINS, Bn.ENDS_WITH, Bn.EQUALS, Bn.NOT_EQUALS],
  numeric: [Bn.EQUALS, Bn.NOT_EQUALS, Bn.LESS_THAN, Bn.LESS_THAN_OR_EQUAL_TO, Bn.GREATER_THAN, Bn.GREATER_THAN_OR_EQUAL_TO],
  date: [Bn.DATE_IS, Bn.DATE_IS_NOT, Bn.DATE_BEFORE, Bn.DATE_AFTER]
});
Ti(Un, "changeTheme", function(i, t, e, n) {
  var r, s = document.getElementById(e);
  if (!s)
    throw Error("Element with id ".concat(e, " not found."));
  var o = s.getAttribute("href").replace(i, t), a = document.createElement("link");
  a.setAttribute("rel", "stylesheet"), a.setAttribute("id", e), a.setAttribute("href", o), a.addEventListener("load", function() {
    n && n();
  }), (r = s.parentNode) === null || r === void 0 || r.replaceChild(a, s);
});
var OG = {
  en: {
    accept: "Yes",
    addRule: "Add Rule",
    am: "AM",
    apply: "Apply",
    cancel: "Cancel",
    choose: "Choose",
    chooseDate: "Choose Date",
    chooseMonth: "Choose Month",
    chooseYear: "Choose Year",
    clear: "Clear",
    completed: "Completed",
    contains: "Contains",
    custom: "Custom",
    dateAfter: "Date is after",
    dateBefore: "Date is before",
    dateFormat: "mm/dd/yy",
    dateIs: "Date is",
    dateIsNot: "Date is not",
    dayNames: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
    dayNamesMin: ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"],
    dayNamesShort: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
    emptyFilterMessage: "No results found",
    emptyMessage: "No available options",
    emptySearchMessage: "No results found",
    emptySelectionMessage: "No selected item",
    endsWith: "Ends with",
    equals: "Equals",
    fileSizeTypes: ["B", "KB", "MB", "GB", "TB", "PB", "EB", "ZB", "YB"],
    filter: "Filter",
    firstDayOfWeek: 0,
    gt: "Greater than",
    gte: "Greater than or equal to",
    lt: "Less than",
    lte: "Less than or equal to",
    matchAll: "Match All",
    matchAny: "Match Any",
    medium: "Medium",
    monthNames: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
    monthNamesShort: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],
    nextDecade: "Next Decade",
    nextHour: "Next Hour",
    nextMinute: "Next Minute",
    nextMonth: "Next Month",
    nextSecond: "Next Second",
    nextYear: "Next Year",
    noFilter: "No Filter",
    notContains: "Not contains",
    notEquals: "Not equals",
    now: "Now",
    passwordPrompt: "Enter a password",
    pending: "Pending",
    pm: "PM",
    prevDecade: "Previous Decade",
    prevHour: "Previous Hour",
    prevMinute: "Previous Minute",
    prevMonth: "Previous Month",
    prevSecond: "Previous Second",
    prevYear: "Previous Year",
    reject: "No",
    removeRule: "Remove Rule",
    searchMessage: "{0} results are available",
    selectionMessage: "{0} items selected",
    showMonthAfterYear: !1,
    startsWith: "Starts with",
    strong: "Strong",
    today: "Today",
    upload: "Upload",
    weak: "Weak",
    weekHeader: "Wk",
    aria: {
      cancelEdit: "Cancel Edit",
      close: "Close",
      collapseRow: "Row Collapsed",
      editRow: "Edit Row",
      expandRow: "Row Expanded",
      falseLabel: "False",
      filterConstraint: "Filter Constraint",
      filterOperator: "Filter Operator",
      firstPageLabel: "First Page",
      gridView: "Grid View",
      hideFilterMenu: "Hide Filter Menu",
      jumpToPageDropdownLabel: "Jump to Page Dropdown",
      jumpToPageInputLabel: "Jump to Page Input",
      lastPageLabel: "Last Page",
      listView: "List View",
      moveAllToSource: "Move All to Source",
      moveAllToTarget: "Move All to Target",
      moveBottom: "Move Bottom",
      moveDown: "Move Down",
      moveToSource: "Move to Source",
      moveToTarget: "Move to Target",
      moveTop: "Move Top",
      moveUp: "Move Up",
      navigation: "Navigation",
      next: "Next",
      nextPageLabel: "Next Page",
      nullLabel: "Not Selected",
      pageLabel: "Page {page}",
      otpLabel: "Please enter one time password character {0}",
      passwordHide: "Hide Password",
      passwordShow: "Show Password",
      previous: "Previous",
      previousPageLabel: "Previous Page",
      rotateLeft: "Rotate Left",
      rotateRight: "Rotate Right",
      rowsPerPageLabel: "Rows per page",
      saveEdit: "Save Edit",
      scrollTop: "Scroll Top",
      selectAll: "All items selected",
      selectRow: "Row Selected",
      showFilterMenu: "Show Filter Menu",
      slide: "Slide",
      slideNumber: "{slideNumber}",
      star: "1 star",
      stars: "{star} stars",
      trueLabel: "True",
      unselectAll: "All items unselected",
      unselectRow: "Row Unselected",
      zoomImage: "Zoom Image",
      zoomIn: "Zoom In",
      zoomOut: "Zoom Out"
    }
  }
};
function la(i, t) {
  if (i.includes("__proto__") || i.includes("prototype"))
    throw new Error("Unsafe key detected");
  var e = Un.locale;
  try {
    return VG(e)[i];
  } catch {
    throw new Error("The ".concat(i, " option is not found in the current locale('").concat(e, "')."));
  }
}
function VG(i) {
  var t = i || Un.locale;
  if (t.includes("__proto__") || t.includes("prototype"))
    throw new Error("Unsafe locale detected");
  return OG[t];
}
var cn = /* @__PURE__ */ Se.createContext(), tn = Un;
function MG(i) {
  if (Array.isArray(i)) return i;
}
function DG(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t === 0) {
        if (Object(e) !== e) return;
        l = !1;
      } else for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function Um(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function cE(i, t) {
  if (i) {
    if (typeof i == "string") return Um(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return Um(i, t);
  }
}
function LG() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function ns(i, t) {
  return MG(i) || DG(i, t) || cE(i, t) || LG();
}
var Kl = function(t) {
  var e = w.useRef(null);
  return w.useEffect(function() {
    return e.current = t, function() {
      e.current = null;
    };
  }, [t]), e.current;
}, wr = function(t) {
  return w.useEffect(function() {
    return t;
  }, []);
}, is = function(t) {
  var e = t.target, n = e === void 0 ? "document" : e, r = t.type, s = t.listener, o = t.options, a = t.when, l = a === void 0 ? !0 : a, c = w.useRef(null), u = w.useRef(null), d = Kl(s), f = Kl(o), m = function() {
    var y = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, x = y.target;
    z.isNotEmpty(x) && (g(), (y.when || l) && (c.current = j.getTargetElement(x))), !u.current && c.current && (u.current = function(S) {
      return s && s(S);
    }, c.current.addEventListener(r, u.current, o));
  }, g = function() {
    u.current && (c.current.removeEventListener(r, u.current, o), u.current = null);
  }, p = function() {
    g(), d = null, f = null;
  }, b = w.useCallback(function() {
    l ? c.current = j.getTargetElement(n) : (g(), c.current = null);
  }, [n, l]);
  return w.useEffect(function() {
    b();
  }, [b]), w.useEffect(function() {
    var v = "".concat(d) !== "".concat(s), y = f !== o, x = u.current;
    x && (v || y) ? (g(), l && m()) : x || p();
  }, [s, o, l]), wr(function() {
    p();
  }), [m, g];
}, Is = {}, NG = function(t) {
  var e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0, n = w.useState(function() {
    return Kp();
  }), r = ns(n, 1), s = r[0], o = w.useState(0), a = ns(o, 2), l = a[0], c = a[1];
  return w.useEffect(function() {
    if (e) {
      Is[t] || (Is[t] = []);
      var u = Is[t].push(s);
      return c(u), function() {
        delete Is[t][u - 1];
        var d = Is[t].length - 1, f = z.findLastIndex(Is[t], function(m) {
          return m !== void 0;
        });
        f !== d && Is[t].splice(f + 1), c(void 0);
      };
    }
  }, [t, s, e]), l;
};
function BG(i) {
  if (Array.isArray(i)) return Um(i);
}
function FG(i) {
  if (typeof Symbol < "u" && i[Symbol.iterator] != null || i["@@iterator"] != null) return Array.from(i);
}
function $G() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function J0(i) {
  return BG(i) || FG(i) || cE(i) || $G();
}
var uE = {
  SIDEBAR: 100,
  SLIDE_MENU: 200,
  DIALOG: 300,
  IMAGE: 400,
  MENU: 500,
  OVERLAY_PANEL: 600,
  PASSWORD: 700,
  CASCADE_SELECT: 800,
  SPLIT_BUTTON: 900,
  SPEED_DIAL: 1e3,
  TOOLTIP: 1200
}, dE = {
  /**
   * Mapping from ESC_KEY_HANDLING_PRIORITY to array of related listeners, grouped by priority
   * @example
   * Map<{
   *     [ESC_KEY_HANDLING_PRIORITIES.SIDEBAR]: Map<{
   *         1: () => {...},
   *         2: () => {...}
   *     }>,
   *     [ESC_KEY_HANDLING_PRIORITIES.DIALOG]: Map<{
   *         1: () => {...},
   *         2: () => {...}
   *     }>
   * }>;
   */
  escKeyListeners: /* @__PURE__ */ new Map(),
  /**
   * Keydown handler (attached to any keydown)
   */
  onGlobalKeyDown: function(t) {
    if (t.code === "Escape") {
      var e = dE.escKeyListeners, n = Math.max.apply(Math, J0(e.keys())), r = e.get(n), s = Math.max.apply(Math, J0(r.keys())), o = r.get(s);
      o(t);
    }
  },
  /**
   * Attach global keydown listener if there are any "esc" key handlers assigned,
   * otherwise detach.
   */
  refreshGlobalKeyDownListener: function() {
    var t = j.getTargetElement("document");
    this.escKeyListeners.size > 0 ? t.addEventListener("keydown", this.onGlobalKeyDown) : t.removeEventListener("keydown", this.onGlobalKeyDown);
  },
  /**
   * Add "Esc" key handler
   */
  addListener: function(t, e) {
    var n = this, r = ns(e, 2), s = r[0], o = r[1], a = this.escKeyListeners;
    a.has(s) || a.set(s, /* @__PURE__ */ new Map());
    var l = a.get(s);
    if (l.has(o))
      throw new Error("Unexpected: global esc key listener with priority [".concat(s, ", ").concat(o, "] already exists."));
    return l.set(o, t), this.refreshGlobalKeyDownListener(), function() {
      l.delete(o), l.size === 0 && a.delete(s), n.refreshGlobalKeyDownListener();
    };
  }
}, hE = function(t) {
  var e = t.callback, n = t.when, r = t.priority;
  Ee(function() {
    if (n)
      return dE.addListener(e, r);
  }, [e, n, r]);
}, Qn = function() {
  var t = hr(cn);
  return function() {
    for (var e = arguments.length, n = new Array(e), r = 0; r < e; r++)
      n[r] = arguments[r];
    return hd(n, t == null ? void 0 : t.ptOptions);
  };
}, rr = function(t) {
  var e = w.useRef(!1);
  return w.useEffect(function() {
    if (!e.current)
      return e.current = !0, t && t();
  }, []);
}, fE = function(t) {
  var e = t.target, n = t.listener, r = t.options, s = t.when, o = s === void 0 ? !0 : s, a = w.useContext(cn), l = w.useRef(null), c = w.useRef(null), u = w.useRef([]), d = Kl(n), f = Kl(r), m = function() {
    var y = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    if (z.isNotEmpty(y.target) && (g(), (y.when || o) && (l.current = j.getTargetElement(y.target))), !c.current && l.current) {
      var x = a ? a.hideOverlaysOnDocumentScrolling : tn.hideOverlaysOnDocumentScrolling, S = u.current = j.getScrollableParents(l.current, x);
      c.current = function(k) {
        return n && n(k);
      }, S.forEach(function(k) {
        return k.addEventListener("scroll", c.current, r);
      });
    }
  }, g = function() {
    if (c.current) {
      var y = u.current;
      y.forEach(function(x) {
        return x.removeEventListener("scroll", c.current, r);
      }), c.current = null;
    }
  }, p = function() {
    g(), u.current = null, d = null, f = null;
  }, b = w.useCallback(function() {
    o ? l.current = j.getTargetElement(e) : (g(), l.current = null);
  }, [e, o]);
  return w.useEffect(function() {
    b();
  }, [b]), w.useEffect(function() {
    var v = "".concat(d) !== "".concat(n), y = f !== r, x = c.current;
    x && (v || y) ? (g(), o && m()) : x || p();
  }, [n, r, o]), wr(function() {
    p();
  }), [m, g];
}, Zp = function(t) {
  var e = t.listener, n = t.when, r = n === void 0 ? !0 : n;
  return is({
    target: "window",
    type: "resize",
    listener: e,
    when: r
  });
}, zG = function(t) {
  var e = t.target, n = t.overlay, r = t.listener, s = t.when, o = s === void 0 ? !0 : s, a = t.type, l = a === void 0 ? "click" : a, c = w.useRef(null), u = w.useRef(null), d = is({
    target: "window",
    type: l,
    listener: function(ce) {
      r && r(ce, {
        type: "outside",
        valid: ce.which !== 3 && N(ce)
      });
    }
  }), f = ns(d, 2), m = f[0], g = f[1], p = Zp({
    target: "window",
    listener: function(ce) {
      r && r(ce, {
        type: "resize",
        valid: !j.isTouchDevice()
      });
    }
  }), b = ns(p, 2), v = b[0], y = b[1], x = is({
    target: "window",
    type: "orientationchange",
    listener: function(ce) {
      r && r(ce, {
        type: "orientationchange",
        valid: !0
      });
    }
  }), S = ns(x, 2), k = S[0], I = S[1], A = fE({
    target: e,
    listener: function(ce) {
      r && r(ce, {
        type: "scroll",
        valid: !0
      });
    }
  }), L = ns(A, 2), R = L[0], O = L[1], N = function(ce) {
    return c.current && !(c.current.isSameNode(ce.target) || c.current.contains(ce.target) || u.current && u.current.contains(ce.target));
  }, le = function() {
    m(), v(), k(), R();
  }, te = function() {
    g(), y(), I(), O();
  };
  return w.useEffect(function() {
    o ? (c.current = j.getTargetElement(e), u.current = j.getTargetElement(n)) : (te(), c.current = u.current = null);
  }, [e, n, o]), w.useEffect(function() {
    te();
  }, [o]), wr(function() {
    te();
  }), [le, te];
}, HG = 0, Ls = function(t) {
  var e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, n = Ne(!1), r = ns(n, 2), s = r[0], o = r[1], a = at(null), l = hr(cn), c = j.isClient() ? window.document : void 0, u = e.document, d = u === void 0 ? c : u, f = e.manual, m = f === void 0 ? !1 : f, g = e.name, p = g === void 0 ? "style_".concat(++HG) : g, b = e.id, v = b === void 0 ? void 0 : b, y = e.media, x = y === void 0 ? void 0 : y, S = function(R) {
    var O = R.querySelector('style[data-primereact-style-id="'.concat(p, '"]'));
    if (O)
      return O;
    if (v !== void 0) {
      var N = d.getElementById(v);
      if (N)
        return N;
    }
    return d.createElement("style");
  }, k = function(R) {
    s && t !== R && (a.current.textContent = R);
  }, I = function() {
    if (!(!d || s)) {
      var R = (l == null ? void 0 : l.styleContainer) || d.head;
      a.current = S(R), a.current.isConnected || (a.current.type = "text/css", v && (a.current.id = v), x && (a.current.media = x), j.addNonce(a.current, l && l.nonce || tn.nonce), R.appendChild(a.current), p && a.current.setAttribute("data-primereact-style-id", p)), a.current.textContent = t, o(!0);
    }
  }, A = function() {
    !d || !a.current || (j.removeInlineStyle(a.current), o(!1));
  };
  return Ee(function() {
    m || I();
  }, [m]), {
    id: v,
    name: p,
    update: k,
    unload: A,
    load: I,
    isLoaded: s
  };
}, Zt = function(t, e) {
  var n = w.useRef(!1);
  return w.useEffect(function() {
    if (!n.current) {
      n.current = !0;
      return;
    }
    return t && t();
  }, e);
};
function qm(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function jG(i) {
  if (Array.isArray(i)) return qm(i);
}
function WG(i) {
  if (typeof Symbol < "u" && i[Symbol.iterator] != null || i["@@iterator"] != null) return Array.from(i);
}
function UG(i, t) {
  if (i) {
    if (typeof i == "string") return qm(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return qm(i, t);
  }
}
function qG() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function X0(i) {
  return jG(i) || WG(i) || UG(i) || qG();
}
function Zl(i) {
  "@babel/helpers - typeof";
  return Zl = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Zl(i);
}
function GG(i, t) {
  if (Zl(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (Zl(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function KG(i) {
  var t = GG(i, "string");
  return Zl(t) === "symbol" ? t : String(t);
}
function Gm(i, t, e) {
  return t = KG(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function Q0(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function _n(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Q0(Object(e), !0).forEach(function(n) {
      Gm(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : Q0(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var ZG = `
.p-hidden-accessible {
    border: 0;
    padding: 0;
    margin: -1px;
    position: absolute;
    height: 1px;
    width: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    clip-path: inset(50%);
    white-space: nowrap;
}

.p-hidden-accessible input,
.p-hidden-accessible select {
    transform: scale(0);
}

.p-overflow-hidden {
    overflow: hidden;
    padding-right: var(--scrollbar-width);
}
`, YG = `
.p-button {
    margin: 0;
    display: inline-flex;
    cursor: pointer;
    user-select: none;
    align-items: center;
    vertical-align: bottom;
    text-align: center;
    overflow: hidden;
    position: relative;
}

.p-button-label {
    flex: 1 1 auto;
}

.p-button-icon-right {
    order: 1;
}

.p-button:disabled {
    cursor: default;
}

.p-button-icon-only {
    justify-content: center;
}

.p-button-icon-only .p-button-label {
    visibility: hidden;
    width: 0;
    flex: 0 0 auto;
}

.p-button-vertical {
    flex-direction: column;
}

.p-button-icon-bottom {
    order: 2;
}

.p-button-group .p-button {
    margin: 0;
}

.p-button-group .p-button:not(:last-child) {
    border-right: 0 none;
}

.p-button-group .p-button:not(:first-of-type):not(:last-of-type) {
    border-radius: 0;
}

.p-button-group .p-button:first-of-type {
    border-top-right-radius: 0;
    border-bottom-right-radius: 0;
}

.p-button-group .p-button:last-of-type {
    border-top-left-radius: 0;
    border-bottom-left-radius: 0;
}

.p-button-group .p-button:focus {
    position: relative;
    z-index: 1;
}
`, JG = `
.p-inputtext {
    margin: 0;
}

.p-fluid .p-inputtext {
    width: 100%;
}

/* InputGroup */
.p-inputgroup {
    display: flex;
    align-items: stretch;
    width: 100%;
}

.p-inputgroup-addon {
    display: flex;
    align-items: center;
    justify-content: center;
}

.p-inputgroup .p-float-label {
    display: flex;
    align-items: stretch;
    width: 100%;
}

.p-inputgroup .p-inputtext,
.p-fluid .p-inputgroup .p-inputtext,
.p-inputgroup .p-inputwrapper,
.p-fluid .p-inputgroup .p-input {
    flex: 1 1 auto;
    width: 1%;
}

/* Floating Label */
.p-float-label {
    display: block;
    position: relative;
}

.p-float-label label {
    position: absolute;
    pointer-events: none;
    top: 50%;
    margin-top: -0.5rem;
    transition-property: all;
    transition-timing-function: ease;
    line-height: 1;
}

.p-float-label textarea ~ label,
.p-float-label .p-mention ~ label {
    top: 1rem;
}

.p-float-label input:focus ~ label,
.p-float-label input:-webkit-autofill ~ label,
.p-float-label input.p-filled ~ label,
.p-float-label textarea:focus ~ label,
.p-float-label textarea.p-filled ~ label,
.p-float-label .p-inputwrapper-focus ~ label,
.p-float-label .p-inputwrapper-filled ~ label,
.p-float-label .p-tooltip-target-wrapper ~ label {
    top: -0.75rem;
    font-size: 12px;
}

.p-float-label .p-placeholder,
.p-float-label input::placeholder,
.p-float-label .p-inputtext::placeholder {
    opacity: 0;
    transition-property: all;
    transition-timing-function: ease;
}

.p-float-label .p-focus .p-placeholder,
.p-float-label input:focus::placeholder,
.p-float-label .p-inputtext:focus::placeholder {
    opacity: 1;
    transition-property: all;
    transition-timing-function: ease;
}

.p-input-icon-left,
.p-input-icon-right {
    position: relative;
    display: inline-block;
}

.p-input-icon-left > i,
.p-input-icon-right > i,
.p-input-icon-left > svg,
.p-input-icon-right > svg,
.p-input-icon-left > .p-input-prefix,
.p-input-icon-right > .p-input-suffix {
    position: absolute;
    top: 50%;
    margin-top: -0.5rem;
}

.p-fluid .p-input-icon-left,
.p-fluid .p-input-icon-right {
    display: block;
    width: 100%;
}
`, XG = `
.p-icon {
    display: inline-block;
}

.p-icon-spin {
    -webkit-animation: p-icon-spin 2s infinite linear;
    animation: p-icon-spin 2s infinite linear;
}

svg.p-icon {
    pointer-events: auto;
}

svg.p-icon g,
.p-disabled svg.p-icon {
    pointer-events: none;
}

@-webkit-keyframes p-icon-spin {
    0% {
        -webkit-transform: rotate(0deg);
        transform: rotate(0deg);
    }
    100% {
        -webkit-transform: rotate(359deg);
        transform: rotate(359deg);
    }
}

@keyframes p-icon-spin {
    0% {
        -webkit-transform: rotate(0deg);
        transform: rotate(0deg);
    }
    100% {
        -webkit-transform: rotate(359deg);
        transform: rotate(359deg);
    }
}
`, QG = `
@layer primereact {
    .p-component, .p-component * {
        box-sizing: border-box;
    }

    .p-hidden {
        display: none;
    }

    .p-hidden-space {
        visibility: hidden;
    }

    .p-reset {
        margin: 0;
        padding: 0;
        border: 0;
        outline: 0;
        text-decoration: none;
        font-size: 100%;
        list-style: none;
    }

    .p-disabled, .p-disabled * {
        cursor: default;
        pointer-events: none;
        user-select: none;
    }

    .p-component-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }

    .p-unselectable-text {
        user-select: none;
    }

    .p-scrollbar-measure {
        width: 100px;
        height: 100px;
        overflow: scroll;
        position: absolute;
        top: -9999px;
    }

    @-webkit-keyframes p-fadein {
      0%   { opacity: 0; }
      100% { opacity: 1; }
    }
    @keyframes p-fadein {
      0%   { opacity: 0; }
      100% { opacity: 1; }
    }

    .p-link {
        text-align: left;
        background-color: transparent;
        margin: 0;
        padding: 0;
        border: none;
        cursor: pointer;
        user-select: none;
    }

    .p-link:disabled {
        cursor: default;
    }

    /* Non react overlay animations */
    .p-connected-overlay {
        opacity: 0;
        transform: scaleY(0.8);
        transition: transform .12s cubic-bezier(0, 0, 0.2, 1), opacity .12s cubic-bezier(0, 0, 0.2, 1);
    }

    .p-connected-overlay-visible {
        opacity: 1;
        transform: scaleY(1);
    }

    .p-connected-overlay-hidden {
        opacity: 0;
        transform: scaleY(1);
        transition: opacity .1s linear;
    }

    /* React based overlay animations */
    .p-connected-overlay-enter {
        opacity: 0;
        transform: scaleY(0.8);
    }

    .p-connected-overlay-enter-active {
        opacity: 1;
        transform: scaleY(1);
        transition: transform .12s cubic-bezier(0, 0, 0.2, 1), opacity .12s cubic-bezier(0, 0, 0.2, 1);
    }

    .p-connected-overlay-enter-done {
        transform: none;
    }

    .p-connected-overlay-exit {
        opacity: 1;
    }

    .p-connected-overlay-exit-active {
        opacity: 0;
        transition: opacity .1s linear;
    }

    /* Toggleable Content */
    .p-toggleable-content-enter {
        max-height: 0;
    }

    .p-toggleable-content-enter-active {
        overflow: hidden;
        max-height: 1000px;
        transition: max-height 1s ease-in-out;
    }

    .p-toggleable-content-enter-done {
        transform: none;
    }

    .p-toggleable-content-exit {
        max-height: 1000px;
    }

    .p-toggleable-content-exit-active {
        overflow: hidden;
        max-height: 0;
        transition: max-height 0.45s cubic-bezier(0, 1, 0, 1);
    }

    .p-sr-only {
        border: 0;
        clip: rect(1px, 1px, 1px, 1px);
        clip-path: inset(50%);
        height: 1px;
        margin: -1px;
        overflow: hidden;
        padding: 0;
        position: absolute;
        width: 1px;
        word-wrap: normal;
    }

    /* @todo Refactor */
    .p-menu .p-menuitem-link {
        cursor: pointer;
        display: flex;
        align-items: center;
        text-decoration: none;
        overflow: hidden;
        position: relative;
    }

    `.concat(YG, `
    `).concat(JG, `
    `).concat(XG, `
}
`), et = {
  cProps: void 0,
  cParams: void 0,
  cName: void 0,
  defaultProps: {
    pt: void 0,
    ptOptions: void 0,
    unstyled: !1
  },
  context: {},
  globalCSS: void 0,
  classes: {},
  styles: "",
  extend: function() {
    var t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, e = t.css, n = _n(_n({}, t.defaultProps), et.defaultProps), r = {}, s = function(u) {
      var d = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
      return et.context = d, et.cProps = u, z.getMergedProps(u, n);
    }, o = function(u) {
      return z.getDiffProps(u, n);
    }, a = function() {
      var u, d = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, f = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "", m = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, g = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !0;
      d.hasOwnProperty("pt") && d.pt !== void 0 && (d = d.pt);
      var p = f, b = /./g.test(p) && !!m[p.split(".")[0]], v = b ? z.toFlatCase(p.split(".")[1]) : z.toFlatCase(p), y = m.hostName && z.toFlatCase(m.hostName), x = y || m.props && m.props.__TYPE && z.toFlatCase(m.props.__TYPE) || "", S = v === "transition", k = "data-pc-", I = function Fe(be) {
        return be != null && be.props ? be.hostName ? be.props.__TYPE === be.hostName ? be.props : Fe(be.parent) : be.parent : void 0;
      }, A = function(be) {
        var re, B;
        return ((re = m.props) === null || re === void 0 ? void 0 : re[be]) || ((B = I(m)) === null || B === void 0 ? void 0 : B[be]);
      };
      et.cParams = m, et.cName = x;
      var L = A("ptOptions") || et.context.ptOptions || {}, R = L.mergeSections, O = R === void 0 ? !0 : R, N = L.mergeProps, le = N === void 0 ? !1 : N, te = function() {
        var be = rs.apply(void 0, arguments);
        return Array.isArray(be) ? {
          className: Re.apply(void 0, X0(be))
        } : z.isString(be) ? {
          className: be
        } : be != null && be.hasOwnProperty("className") && Array.isArray(be.className) ? {
          className: Re.apply(void 0, X0(be.className))
        } : be;
      }, me = g ? b ? mE(te, p, m) : gE(te, p, m) : void 0, ce = b ? void 0 : ah(oh(d, x), te, p, m), Ae = !S && _n(_n({}, v === "root" && Gm({}, "".concat(k, "name"), m.props && m.props.__parentMetadata ? z.toFlatCase(m.props.__TYPE) : x)), {}, Gm({}, "".concat(k, "section"), v));
      return O || !O && ce ? le ? hd([me, ce, Object.keys(Ae).length ? Ae : {}], {
        classNameMergeFunction: (u = et.context.ptOptions) === null || u === void 0 ? void 0 : u.classNameMergeFunction
      }) : _n(_n(_n({}, me), ce), Object.keys(Ae).length ? Ae : {}) : _n(_n({}, ce), Object.keys(Ae).length ? Ae : {});
    }, l = function() {
      var u = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, d = u.props, f = u.state, m = function() {
        var x = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "", S = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        return a((d || {}).pt, x, _n(_n({}, u), S));
      }, g = function() {
        var x = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, S = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "", k = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
        return a(x, S, k, !1);
      }, p = function() {
        return et.context.unstyled || tn.unstyled || d.unstyled;
      }, b = function() {
        var x = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "", S = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        return p() ? void 0 : rs(e && e.classes, x, _n({
          props: d,
          state: f
        }, S));
      }, v = function() {
        var x = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "", S = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, k = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
        if (k) {
          var I, A = rs(e && e.inlineStyles, x, _n({
            props: d,
            state: f
          }, S)), L = rs(r, x, _n({
            props: d,
            state: f
          }, S));
          return hd([L, A], {
            classNameMergeFunction: (I = et.context.ptOptions) === null || I === void 0 ? void 0 : I.classNameMergeFunction
          });
        }
      };
      return {
        ptm: m,
        ptmo: g,
        sx: v,
        cx: b,
        isUnstyled: p
      };
    };
    return _n(_n({
      getProps: s,
      getOtherProps: o,
      setMetaData: l
    }, t), {}, {
      defaultProps: n
    });
  }
}, rs = function i(t) {
  var e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "", n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, r = String(z.toFlatCase(e)).split("."), s = r.shift(), o = z.isNotEmpty(t) ? Object.keys(t).find(function(a) {
    return z.toFlatCase(a) === s;
  }) : "";
  return s ? z.isObject(t) ? i(z.getItemValue(t[o], n), r.join("."), n) : void 0 : z.getItemValue(t, n);
}, oh = function(t) {
  var e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "", n = arguments.length > 2 ? arguments[2] : void 0, r = t == null ? void 0 : t._usept, s = function(a) {
    var l, c = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, u = n ? n(a) : a, d = z.toFlatCase(e);
    return (l = c ? d !== et.cName ? u == null ? void 0 : u[d] : void 0 : u == null ? void 0 : u[d]) !== null && l !== void 0 ? l : u;
  };
  return z.isNotEmpty(r) ? {
    _usept: r,
    originalValue: s(t.originalValue),
    value: s(t.value)
  } : s(t, !0);
}, ah = function(t, e, n, r) {
  var s = function(p) {
    return e(p, n, r);
  };
  if (t != null && t.hasOwnProperty("_usept")) {
    var o = t._usept || et.context.ptOptions || {}, a = o.mergeSections, l = a === void 0 ? !0 : a, c = o.mergeProps, u = c === void 0 ? !1 : c, d = o.classNameMergeFunction, f = s(t.originalValue), m = s(t.value);
    return f === void 0 && m === void 0 ? void 0 : z.isString(m) ? m : z.isString(f) ? f : l || !l && m ? u ? hd([f, m], {
      classNameMergeFunction: d
    }) : _n(_n({}, f), m) : m;
  }
  return s(t);
}, eK = function() {
  return oh(et.context.pt || tn.pt, void 0, function(t) {
    return z.getItemValue(t, et.cParams);
  });
}, tK = function() {
  return oh(et.context.pt || tn.pt, void 0, function(t) {
    return rs(t, et.cName, et.cParams) || z.getItemValue(t, et.cParams);
  });
}, mE = function(t, e, n) {
  return ah(eK(), t, e, n);
}, gE = function(t, e, n) {
  return ah(tK(), t, e, n);
}, sr = function(t) {
  var e = arguments.length > 2 ? arguments[2] : void 0, n = e.name, r = e.styled, s = r === void 0 ? !1 : r, o = e.hostName, a = o === void 0 ? "" : o, l = mE(rs, "global.css", et.cParams), c = z.toFlatCase(n), u = Ls(ZG, {
    name: "base",
    manual: !0
  }), d = u.load, f = Ls(QG, {
    name: "common",
    manual: !0
  }), m = f.load, g = Ls(l, {
    name: "global",
    manual: !0
  }), p = g.load, b = Ls(t, {
    name: n,
    manual: !0
  }), v = b.load, y = function(S) {
    if (!a) {
      var k = ah(oh((et.cProps || {}).pt, c), rs, "hooks.".concat(S)), I = gE(rs, "hooks.".concat(S));
      k == null || k(), I == null || I();
    }
  };
  y("useMountEffect"), rr(function() {
    d(), p(), m(), s || v();
  }), Zt(function() {
    y("useUpdateEffect");
  }), wr(function() {
    y("useUnmountEffect");
  });
};
function fd() {
  return fd = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, fd.apply(null, arguments);
}
function pE(i, t) {
  if (i == null) return {};
  var e = {};
  for (var n in i) if ({}.hasOwnProperty.call(i, n)) {
    if (t.includes(n)) continue;
    e[n] = i[n];
  }
  return e;
}
function Km(i, t) {
  return Km = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(e, n) {
    return e.__proto__ = n, e;
  }, Km(i, t);
}
function bE(i, t) {
  i.prototype = Object.create(t.prototype), i.prototype.constructor = i, Km(i, t);
}
function nK(i, t) {
  return i.classList ? !!t && i.classList.contains(t) : (" " + (i.className.baseVal || i.className) + " ").indexOf(" " + t + " ") !== -1;
}
function iK(i, t) {
  i.classList ? i.classList.add(t) : nK(i, t) || (typeof i.className == "string" ? i.className = i.className + " " + t : i.setAttribute("class", (i.className && i.className.baseVal || "") + " " + t));
}
function ey(i, t) {
  return i.replace(new RegExp("(^|\\s)" + t + "(?:\\s|$)", "g"), "$1").replace(/\s+/g, " ").replace(/^\s*|\s*$/g, "");
}
function rK(i, t) {
  i.classList ? i.classList.remove(t) : typeof i.className == "string" ? i.className = ey(i.className, t) : i.setAttribute("class", ey(i.className && i.className.baseVal || "", t));
}
const ty = {
  disabled: !1
};
var sK = process.env.NODE_ENV !== "production" ? Ie.oneOfType([Ie.number, Ie.shape({
  enter: Ie.number,
  exit: Ie.number,
  appear: Ie.number
}).isRequired]) : null, oK = process.env.NODE_ENV !== "production" ? Ie.oneOfType([Ie.string, Ie.shape({
  enter: Ie.string,
  exit: Ie.string,
  active: Ie.string
}), Ie.shape({
  enter: Ie.string,
  enterDone: Ie.string,
  enterActive: Ie.string,
  exit: Ie.string,
  exitDone: Ie.string,
  exitActive: Ie.string
})]) : null;
const vE = Se.createContext(null);
var wE = function(t) {
  return t.scrollTop;
}, il = "unmounted", Vs = "exited", Ms = "entering", No = "entered", Zm = "exiting", yr = /* @__PURE__ */ function(i) {
  bE(t, i);
  function t(n, r) {
    var s;
    s = i.call(this, n, r) || this;
    var o = r, a = o && !o.isMounting ? n.enter : n.appear, l;
    return s.appearStatus = null, n.in ? a ? (l = Vs, s.appearStatus = Ms) : l = No : n.unmountOnExit || n.mountOnEnter ? l = il : l = Vs, s.state = {
      status: l
    }, s.nextCallback = null, s;
  }
  t.getDerivedStateFromProps = function(r, s) {
    var o = r.in;
    return o && s.status === il ? {
      status: Vs
    } : null;
  };
  var e = t.prototype;
  return e.componentDidMount = function() {
    this.updateStatus(!0, this.appearStatus);
  }, e.componentDidUpdate = function(r) {
    var s = null;
    if (r !== this.props) {
      var o = this.state.status;
      this.props.in ? o !== Ms && o !== No && (s = Ms) : (o === Ms || o === No) && (s = Zm);
    }
    this.updateStatus(!1, s);
  }, e.componentWillUnmount = function() {
    this.cancelNextCallback();
  }, e.getTimeouts = function() {
    var r = this.props.timeout, s, o, a;
    return s = o = a = r, r != null && typeof r != "number" && (s = r.exit, o = r.enter, a = r.appear !== void 0 ? r.appear : o), {
      exit: s,
      enter: o,
      appear: a
    };
  }, e.updateStatus = function(r, s) {
    if (r === void 0 && (r = !1), s !== null)
      if (this.cancelNextCallback(), s === Ms) {
        if (this.props.unmountOnExit || this.props.mountOnEnter) {
          var o = this.props.nodeRef ? this.props.nodeRef.current : Xa.findDOMNode(this);
          o && wE(o);
        }
        this.performEnter(r);
      } else
        this.performExit();
    else this.props.unmountOnExit && this.state.status === Vs && this.setState({
      status: il
    });
  }, e.performEnter = function(r) {
    var s = this, o = this.props.enter, a = this.context ? this.context.isMounting : r, l = this.props.nodeRef ? [a] : [Xa.findDOMNode(this), a], c = l[0], u = l[1], d = this.getTimeouts(), f = a ? d.appear : d.enter;
    if (!r && !o || ty.disabled) {
      this.safeSetState({
        status: No
      }, function() {
        s.props.onEntered(c);
      });
      return;
    }
    this.props.onEnter(c, u), this.safeSetState({
      status: Ms
    }, function() {
      s.props.onEntering(c, u), s.onTransitionEnd(f, function() {
        s.safeSetState({
          status: No
        }, function() {
          s.props.onEntered(c, u);
        });
      });
    });
  }, e.performExit = function() {
    var r = this, s = this.props.exit, o = this.getTimeouts(), a = this.props.nodeRef ? void 0 : Xa.findDOMNode(this);
    if (!s || ty.disabled) {
      this.safeSetState({
        status: Vs
      }, function() {
        r.props.onExited(a);
      });
      return;
    }
    this.props.onExit(a), this.safeSetState({
      status: Zm
    }, function() {
      r.props.onExiting(a), r.onTransitionEnd(o.exit, function() {
        r.safeSetState({
          status: Vs
        }, function() {
          r.props.onExited(a);
        });
      });
    });
  }, e.cancelNextCallback = function() {
    this.nextCallback !== null && (this.nextCallback.cancel(), this.nextCallback = null);
  }, e.safeSetState = function(r, s) {
    s = this.setNextCallback(s), this.setState(r, s);
  }, e.setNextCallback = function(r) {
    var s = this, o = !0;
    return this.nextCallback = function(a) {
      o && (o = !1, s.nextCallback = null, r(a));
    }, this.nextCallback.cancel = function() {
      o = !1;
    }, this.nextCallback;
  }, e.onTransitionEnd = function(r, s) {
    this.setNextCallback(s);
    var o = this.props.nodeRef ? this.props.nodeRef.current : Xa.findDOMNode(this), a = r == null && !this.props.addEndListener;
    if (!o || a) {
      setTimeout(this.nextCallback, 0);
      return;
    }
    if (this.props.addEndListener) {
      var l = this.props.nodeRef ? [this.nextCallback] : [o, this.nextCallback], c = l[0], u = l[1];
      this.props.addEndListener(c, u);
    }
    r != null && setTimeout(this.nextCallback, r);
  }, e.render = function() {
    var r = this.state.status;
    if (r === il)
      return null;
    var s = this.props, o = s.children;
    s.in, s.mountOnEnter, s.unmountOnExit, s.appear, s.enter, s.exit, s.timeout, s.addEndListener, s.onEnter, s.onEntering, s.onEntered, s.onExit, s.onExiting, s.onExited, s.nodeRef;
    var a = pE(s, ["children", "in", "mountOnEnter", "unmountOnExit", "appear", "enter", "exit", "timeout", "addEndListener", "onEnter", "onEntering", "onEntered", "onExit", "onExiting", "onExited", "nodeRef"]);
    return (
      // allows for nested Transitions
      /* @__PURE__ */ Se.createElement(vE.Provider, {
        value: null
      }, typeof o == "function" ? o(r, a) : Se.cloneElement(Se.Children.only(o), a))
    );
  }, t;
}(Se.Component);
yr.contextType = vE;
yr.propTypes = process.env.NODE_ENV !== "production" ? {
  /**
   * A React reference to DOM element that need to transition:
   * https://stackoverflow.com/a/51127130/4671932
   *
   *   - When `nodeRef` prop is used, `node` is not passed to callback functions
   *      (e.g. `onEnter`) because user already has direct access to the node.
   *   - When changing `key` prop of `Transition` in a `TransitionGroup` a new
   *     `nodeRef` need to be provided to `Transition` with changed `key` prop
   *     (see
   *     [test/CSSTransition-test.js](https://github.com/reactjs/react-transition-group/blob/13435f897b3ab71f6e19d724f145596f5910581c/test/CSSTransition-test.js#L362-L437)).
   */
  nodeRef: Ie.shape({
    current: typeof Element > "u" ? Ie.any : function(i, t, e, n, r, s) {
      var o = i[t];
      return Ie.instanceOf(o && "ownerDocument" in o ? o.ownerDocument.defaultView.Element : Element)(i, t, e, n, r, s);
    }
  }),
  /**
   * A `function` child can be used instead of a React element. This function is
   * called with the current transition status (`'entering'`, `'entered'`,
   * `'exiting'`, `'exited'`), which can be used to apply context
   * specific props to a component.
   *
   * ```jsx
   * <Transition in={this.state.in} timeout={150}>
   *   {state => (
   *     <MyComponent className={`fade fade-${state}`} />
   *   )}
   * </Transition>
   * ```
   */
  children: Ie.oneOfType([Ie.func.isRequired, Ie.element.isRequired]).isRequired,
  /**
   * Show the component; triggers the enter or exit states
   */
  in: Ie.bool,
  /**
   * By default the child component is mounted immediately along with
   * the parent `Transition` component. If you want to "lazy mount" the component on the
   * first `in={true}` you can set `mountOnEnter`. After the first enter transition the component will stay
   * mounted, even on "exited", unless you also specify `unmountOnExit`.
   */
  mountOnEnter: Ie.bool,
  /**
   * By default the child component stays mounted after it reaches the `'exited'` state.
   * Set `unmountOnExit` if you'd prefer to unmount the component after it finishes exiting.
   */
  unmountOnExit: Ie.bool,
  /**
   * By default the child component does not perform the enter transition when
   * it first mounts, regardless of the value of `in`. If you want this
   * behavior, set both `appear` and `in` to `true`.
   *
   * > **Note**: there are no special appear states like `appearing`/`appeared`, this prop
   * > only adds an additional enter transition. However, in the
   * > `<CSSTransition>` component that first enter transition does result in
   * > additional `.appear-*` classes, that way you can choose to style it
   * > differently.
   */
  appear: Ie.bool,
  /**
   * Enable or disable enter transitions.
   */
  enter: Ie.bool,
  /**
   * Enable or disable exit transitions.
   */
  exit: Ie.bool,
  /**
   * The duration of the transition, in milliseconds.
   * Required unless `addEndListener` is provided.
   *
   * You may specify a single timeout for all transitions:
   *
   * ```jsx
   * timeout={500}
   * ```
   *
   * or individually:
   *
   * ```jsx
   * timeout={{
   *  appear: 500,
   *  enter: 300,
   *  exit: 500,
   * }}
   * ```
   *
   * - `appear` defaults to the value of `enter`
   * - `enter` defaults to `0`
   * - `exit` defaults to `0`
   *
   * @type {number | { enter?: number, exit?: number, appear?: number }}
   */
  timeout: function(t) {
    var e = sK;
    t.addEndListener || (e = e.isRequired);
    for (var n = arguments.length, r = new Array(n > 1 ? n - 1 : 0), s = 1; s < n; s++)
      r[s - 1] = arguments[s];
    return e.apply(void 0, [t].concat(r));
  },
  /**
   * Add a custom transition end trigger. Called with the transitioning
   * DOM node and a `done` callback. Allows for more fine grained transition end
   * logic. Timeouts are still used as a fallback if provided.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * ```jsx
   * addEndListener={(node, done) => {
   *   // use the css transitionend event to mark the finish of a transition
   *   node.addEventListener('transitionend', done, false);
   * }}
   * ```
   */
  addEndListener: Ie.func,
  /**
   * Callback fired before the "entering" status is applied. An extra parameter
   * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool) -> void
   */
  onEnter: Ie.func,
  /**
   * Callback fired after the "entering" status is applied. An extra parameter
   * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool)
   */
  onEntering: Ie.func,
  /**
   * Callback fired after the "entered" status is applied. An extra parameter
   * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool) -> void
   */
  onEntered: Ie.func,
  /**
   * Callback fired before the "exiting" status is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement) -> void
   */
  onExit: Ie.func,
  /**
   * Callback fired after the "exiting" status is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement) -> void
   */
  onExiting: Ie.func,
  /**
   * Callback fired after the "exited" status is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed
   *
   * @type Function(node: HtmlElement) -> void
   */
  onExited: Ie.func
} : {};
function Eo() {
}
yr.defaultProps = {
  in: !1,
  mountOnEnter: !1,
  unmountOnExit: !1,
  appear: !1,
  enter: !0,
  exit: !0,
  onEnter: Eo,
  onEntering: Eo,
  onEntered: Eo,
  onExit: Eo,
  onExiting: Eo,
  onExited: Eo
};
yr.UNMOUNTED = il;
yr.EXITED = Vs;
yr.ENTERING = Ms;
yr.ENTERED = No;
yr.EXITING = Zm;
var aK = function(t, e) {
  return t && e && e.split(" ").forEach(function(n) {
    return iK(t, n);
  });
}, Af = function(t, e) {
  return t && e && e.split(" ").forEach(function(n) {
    return rK(t, n);
  });
}, Yp = /* @__PURE__ */ function(i) {
  bE(t, i);
  function t() {
    for (var n, r = arguments.length, s = new Array(r), o = 0; o < r; o++)
      s[o] = arguments[o];
    return n = i.call.apply(i, [this].concat(s)) || this, n.appliedClasses = {
      appear: {},
      enter: {},
      exit: {}
    }, n.onEnter = function(a, l) {
      var c = n.resolveArguments(a, l), u = c[0], d = c[1];
      n.removeClasses(u, "exit"), n.addClass(u, d ? "appear" : "enter", "base"), n.props.onEnter && n.props.onEnter(a, l);
    }, n.onEntering = function(a, l) {
      var c = n.resolveArguments(a, l), u = c[0], d = c[1], f = d ? "appear" : "enter";
      n.addClass(u, f, "active"), n.props.onEntering && n.props.onEntering(a, l);
    }, n.onEntered = function(a, l) {
      var c = n.resolveArguments(a, l), u = c[0], d = c[1], f = d ? "appear" : "enter";
      n.removeClasses(u, f), n.addClass(u, f, "done"), n.props.onEntered && n.props.onEntered(a, l);
    }, n.onExit = function(a) {
      var l = n.resolveArguments(a), c = l[0];
      n.removeClasses(c, "appear"), n.removeClasses(c, "enter"), n.addClass(c, "exit", "base"), n.props.onExit && n.props.onExit(a);
    }, n.onExiting = function(a) {
      var l = n.resolveArguments(a), c = l[0];
      n.addClass(c, "exit", "active"), n.props.onExiting && n.props.onExiting(a);
    }, n.onExited = function(a) {
      var l = n.resolveArguments(a), c = l[0];
      n.removeClasses(c, "exit"), n.addClass(c, "exit", "done"), n.props.onExited && n.props.onExited(a);
    }, n.resolveArguments = function(a, l) {
      return n.props.nodeRef ? [n.props.nodeRef.current, a] : [a, l];
    }, n.getClassNames = function(a) {
      var l = n.props.classNames, c = typeof l == "string", u = c && l ? l + "-" : "", d = c ? "" + u + a : l[a], f = c ? d + "-active" : l[a + "Active"], m = c ? d + "-done" : l[a + "Done"];
      return {
        baseClassName: d,
        activeClassName: f,
        doneClassName: m
      };
    }, n;
  }
  var e = t.prototype;
  return e.addClass = function(r, s, o) {
    var a = this.getClassNames(s)[o + "ClassName"], l = this.getClassNames("enter"), c = l.doneClassName;
    s === "appear" && o === "done" && c && (a += " " + c), o === "active" && r && wE(r), a && (this.appliedClasses[s][o] = a, aK(r, a));
  }, e.removeClasses = function(r, s) {
    var o = this.appliedClasses[s], a = o.base, l = o.active, c = o.done;
    this.appliedClasses[s] = {}, a && Af(r, a), l && Af(r, l), c && Af(r, c);
  }, e.render = function() {
    var r = this.props;
    r.classNames;
    var s = pE(r, ["classNames"]);
    return /* @__PURE__ */ Se.createElement(yr, fd({}, s, {
      onEnter: this.onEnter,
      onEntered: this.onEntered,
      onEntering: this.onEntering,
      onExit: this.onExit,
      onExiting: this.onExiting,
      onExited: this.onExited
    }));
  }, t;
}(Se.Component);
Yp.defaultProps = {
  classNames: ""
};
Yp.propTypes = process.env.NODE_ENV !== "production" ? fd({}, yr.propTypes, {
  /**
   * The animation classNames applied to the component as it appears, enters,
   * exits or has finished the transition. A single name can be provided, which
   * will be suffixed for each stage, e.g. `classNames="fade"` applies:
   *
   * - `fade-appear`, `fade-appear-active`, `fade-appear-done`
   * - `fade-enter`, `fade-enter-active`, `fade-enter-done`
   * - `fade-exit`, `fade-exit-active`, `fade-exit-done`
   *
   * A few details to note about how these classes are applied:
   *
   * 1. They are _joined_ with the ones that are already defined on the child
   *    component, so if you want to add some base styles, you can use
   *    `className` without worrying that it will be overridden.
   *
   * 2. If the transition component mounts with `in={false}`, no classes are
   *    applied yet. You might be expecting `*-exit-done`, but if you think
   *    about it, a component cannot finish exiting if it hasn't entered yet.
   *
   * 2. `fade-appear-done` and `fade-enter-done` will _both_ be applied. This
   *    allows you to define different behavior for when appearing is done and
   *    when regular entering is done, using selectors like
   *    `.fade-enter-done:not(.fade-appear-done)`. For example, you could apply
   *    an epic entrance animation when element first appears in the DOM using
   *    [Animate.css](https://daneden.github.io/animate.css/). Otherwise you can
   *    simply use `fade-enter-done` for defining both cases.
   *
   * Each individual classNames can also be specified independently like:
   *
   * ```js
   * classNames={{
   *  appear: 'my-appear',
   *  appearActive: 'my-active-appear',
   *  appearDone: 'my-done-appear',
   *  enter: 'my-enter',
   *  enterActive: 'my-active-enter',
   *  enterDone: 'my-done-enter',
   *  exit: 'my-exit',
   *  exitActive: 'my-active-exit',
   *  exitDone: 'my-done-exit',
   * }}
   * ```
   *
   * If you want to set these classes using CSS Modules:
   *
   * ```js
   * import styles from './styles.css';
   * ```
   *
   * you might want to use camelCase in your CSS file, that way could simply
   * spread them instead of listing them one by one:
   *
   * ```js
   * classNames={{ ...styles }}
   * ```
   *
   * @type {string | {
   *  appear?: string,
   *  appearActive?: string,
   *  appearDone?: string,
   *  enter?: string,
   *  enterActive?: string,
   *  enterDone?: string,
   *  exit?: string,
   *  exitActive?: string,
   *  exitDone?: string,
   * }}
   */
  classNames: oK,
  /**
   * A `<Transition>` callback fired immediately after the 'enter' or 'appear' class is
   * applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool)
   */
  onEnter: Ie.func,
  /**
   * A `<Transition>` callback fired immediately after the 'enter-active' or
   * 'appear-active' class is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool)
   */
  onEntering: Ie.func,
  /**
   * A `<Transition>` callback fired immediately after the 'enter' or
   * 'appear' classes are **removed** and the `done` class is added to the DOM node.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool)
   */
  onEntered: Ie.func,
  /**
   * A `<Transition>` callback fired immediately after the 'exit' class is
   * applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed
   *
   * @type Function(node: HtmlElement)
   */
  onExit: Ie.func,
  /**
   * A `<Transition>` callback fired immediately after the 'exit-active' is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed
   *
   * @type Function(node: HtmlElement)
   */
  onExiting: Ie.func,
  /**
   * A `<Transition>` callback fired immediately after the 'exit' classes
   * are **removed** and the `exit-done` class is added to the DOM node.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed
   *
   * @type Function(node: HtmlElement)
   */
  onExited: Ie.func
}) : {};
function Yl(i) {
  "@babel/helpers - typeof";
  return Yl = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Yl(i);
}
function lK(i, t) {
  if (Yl(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (Yl(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function cK(i) {
  var t = lK(i, "string");
  return Yl(t) === "symbol" ? t : String(t);
}
function uK(i, t, e) {
  return t = cK(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
var Ym = {
  defaultProps: {
    __TYPE: "CSSTransition",
    children: void 0
  },
  getProps: function(t) {
    return z.getMergedProps(t, Ym.defaultProps);
  },
  getOtherProps: function(t) {
    return z.getDiffProps(t, Ym.defaultProps);
  }
};
function ny(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function Pf(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? ny(Object(e), !0).forEach(function(n) {
      uK(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : ny(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var lh = /* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Ym.getProps(i), n = w.useContext(cn), r = e.disabled || e.options && e.options.disabled || n && !n.cssTransition || !tn.cssTransition, s = function(p, b) {
    e.onEnter && e.onEnter(p, b), e.options && e.options.onEnter && e.options.onEnter(p, b);
  }, o = function(p, b) {
    e.onEntering && e.onEntering(p, b), e.options && e.options.onEntering && e.options.onEntering(p, b);
  }, a = function(p, b) {
    e.onEntered && e.onEntered(p, b), e.options && e.options.onEntered && e.options.onEntered(p, b);
  }, l = function(p) {
    e.onExit && e.onExit(p), e.options && e.options.onExit && e.options.onExit(p);
  }, c = function(p) {
    e.onExiting && e.onExiting(p), e.options && e.options.onExiting && e.options.onExiting(p);
  }, u = function(p) {
    e.onExited && e.onExited(p), e.options && e.options.onExited && e.options.onExited(p);
  };
  if (Zt(function() {
    if (r) {
      var g = z.getRefElement(e.nodeRef);
      e.in ? (s(g, !0), o(g, !0), a(g, !0)) : (l(g), c(g), u(g));
    }
  }, [e.in]), r)
    return e.in ? e.children : null;
  var d = {
    nodeRef: e.nodeRef,
    in: e.in,
    onEnter: s,
    onEntering: o,
    onEntered: a,
    onExit: l,
    onExiting: c,
    onExited: u
  }, f = {
    classNames: e.classNames,
    timeout: e.timeout,
    unmountOnExit: e.unmountOnExit
  }, m = Pf(Pf(Pf({}, f), e.options || {}), d);
  return /* @__PURE__ */ w.createElement(Yp, m, e.children);
});
lh.displayName = "CSSTransition";
var _i = {
  defaultProps: {
    __TYPE: "IconBase",
    className: null,
    label: null,
    spin: !1
  },
  getProps: function(t) {
    return z.getMergedProps(t, _i.defaultProps);
  },
  getOtherProps: function(t) {
    return z.getDiffProps(t, _i.defaultProps);
  },
  getPTI: function(t) {
    var e = z.isEmpty(t.label), n = _i.getOtherProps(t), r = {
      className: Re("p-icon", {
        "p-icon-spin": t.spin
      }, t.className),
      role: e ? void 0 : "img",
      "aria-label": e ? void 0 : t.label,
      "aria-hidden": e
    };
    return z.getMergedProps(n, r);
  }
};
function Jm() {
  return Jm = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, Jm.apply(this, arguments);
}
var Jp = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", Jm({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    d: "M7.01744 10.398C6.91269 10.3985 6.8089 10.378 6.71215 10.3379C6.61541 10.2977 6.52766 10.2386 6.45405 10.1641L1.13907 4.84913C1.03306 4.69404 0.985221 4.5065 1.00399 4.31958C1.02276 4.13266 1.10693 3.95838 1.24166 3.82747C1.37639 3.69655 1.55301 3.61742 1.74039 3.60402C1.92777 3.59062 2.11386 3.64382 2.26584 3.75424L7.01744 8.47394L11.769 3.75424C11.9189 3.65709 12.097 3.61306 12.2748 3.62921C12.4527 3.64535 12.6199 3.72073 12.7498 3.84328C12.8797 3.96582 12.9647 4.12842 12.9912 4.30502C13.0177 4.48162 12.9841 4.662 12.8958 4.81724L7.58083 10.1322C7.50996 10.2125 7.42344 10.2775 7.32656 10.3232C7.22968 10.3689 7.12449 10.3944 7.01744 10.398Z",
    fill: "currentColor"
  }));
}));
Jp.displayName = "ChevronDownIcon";
function Xm() {
  return Xm = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, Xm.apply(this, arguments);
}
var yE = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", Xm({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    d: "M4.38708 13C4.28408 13.0005 4.18203 12.9804 4.08691 12.9409C3.99178 12.9014 3.9055 12.8433 3.83313 12.7701C3.68634 12.6231 3.60388 12.4238 3.60388 12.2161C3.60388 12.0084 3.68634 11.8091 3.83313 11.6622L8.50507 6.99022L3.83313 2.31827C3.69467 2.16968 3.61928 1.97313 3.62287 1.77005C3.62645 1.56698 3.70872 1.37322 3.85234 1.22959C3.99596 1.08597 4.18972 1.00371 4.3928 1.00012C4.59588 0.996539 4.79242 1.07192 4.94102 1.21039L10.1669 6.43628C10.3137 6.58325 10.3962 6.78249 10.3962 6.99022C10.3962 7.19795 10.3137 7.39718 10.1669 7.54416L4.94102 12.7701C4.86865 12.8433 4.78237 12.9014 4.68724 12.9409C4.59212 12.9804 4.49007 13.0005 4.38708 13Z",
    fill: "currentColor"
  }));
}));
yE.displayName = "ChevronRightIcon";
function md() {
  return md = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, md.apply(this, arguments);
}
function Qm(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function dK(i) {
  if (Array.isArray(i)) return Qm(i);
}
function hK(i) {
  if (typeof Symbol < "u" && i[Symbol.iterator] != null || i["@@iterator"] != null) return Array.from(i);
}
function _E(i, t) {
  if (i) {
    if (typeof i == "string") return Qm(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return Qm(i, t);
  }
}
function fK() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function mK(i) {
  return dK(i) || hK(i) || _E(i) || fK();
}
function Jl(i) {
  "@babel/helpers - typeof";
  return Jl = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Jl(i);
}
function gK(i, t) {
  if (Jl(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (Jl(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function pK(i) {
  var t = gK(i, "string");
  return Jl(t) === "symbol" ? t : String(t);
}
function bK(i, t, e) {
  return t = pK(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function vK(i) {
  if (Array.isArray(i)) return i;
}
function wK(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function yK() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function iy(i, t) {
  return vK(i) || wK(i, t) || _E(i, t) || yK();
}
var _K = {
  root: "p-accordion p-component",
  accordiontab: {
    root: function(t) {
      var e = t.selected;
      return Re("p-accordion-tab", {
        "p-accordion-tab-active": e
      });
    },
    content: "p-accordion-content",
    header: function(t) {
      var e = t.selected, n = t.getTabProp, r = t.tab;
      return Re("p-accordion-header", {
        "p-highlight": e,
        "p-disabled": n(r, "disabled")
      });
    },
    headeraction: "p-accordion-header-link",
    headericon: "p-accordion-toggle-icon",
    headertitle: "p-accordion-header-text",
    toggleablecontent: "p-toggleable-content",
    transition: "p-toggleable-content"
  }
}, CK = `
@layer primereact {
    .p-accordion-header-link {
        cursor: pointer;
        display: flex;
        align-items: center;
        user-select: none;
        position: relative;
        text-decoration: none;
    }
    
    .p-accordion-header-link:focus {
        z-index: 1;
    }
    
    .p-accordion-header-text {
        line-height: 1;
        width: 100%;
    }
}
`, nu = et.extend({
  defaultProps: {
    __TYPE: "Accordion",
    id: null,
    activeIndex: null,
    className: null,
    style: null,
    multiple: !1,
    expandIcon: null,
    collapseIcon: null,
    transitionOptions: null,
    onTabOpen: null,
    onTabClose: null,
    onTabChange: null,
    children: void 0
  },
  css: {
    classes: _K,
    styles: CK
  }
}), qo = et.extend({
  defaultProps: {
    __TYPE: "AccordionTab",
    className: null,
    contentClassName: null,
    contentStyle: null,
    disabled: !1,
    header: null,
    headerClassName: null,
    headerStyle: null,
    headerTemplate: null,
    style: null,
    tabIndex: 0,
    children: void 0
  },
  getCProp: function(t, e) {
    return z.getComponentProp(t, e, qo.defaultProps);
  },
  getCProps: function(t) {
    return z.getComponentProps(t, qo.defaultProps);
  },
  getCOtherProps: function(t) {
    return z.getComponentDiffProps(t, qo.defaultProps);
  }
});
function ry(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function To(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? ry(Object(e), !0).forEach(function(n) {
      bK(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : ry(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var di = function() {
}, or = /* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = nu.getProps(i, n), s = w.useState(r.id), o = iy(s, 2), a = o[0], l = o[1], c = w.useState(r.activeIndex), u = iy(c, 2), d = u[0], f = u[1], m = w.useRef(null), g = r.onTabChange ? r.activeIndex : d, p = w.Children.count(r.children), b = {
    props: r,
    state: {
      id: a,
      activeIndex: d
    }
  }, v = nu.setMetaData(To({}, b)), y = v.ptm, x = v.ptmo, S = v.cx, k = v.isUnstyled;
  sr(nu.css.styles, k, {
    name: "accordion"
  });
  var I = function(F, H) {
    return qo.getCProp(F, H);
  }, A = function(F, H, pe) {
    var W = {
      // props: atProps, /* @todo */
      parent: b,
      context: {
        index: pe,
        count: p,
        first: pe === 0,
        last: pe === p - 1,
        selected: X(pe),
        disabled: I(F, "disabled")
      }
    };
    return e(y("tab.".concat(H), {
      tab: W
    }), y("accordiontab.".concat(H), {
      accordiontab: W
    }), y("accordiontab.".concat(H), W), x(I(F, "pt"), H, W));
  }, L = function(F, H, pe) {
    R(F, H, pe);
  }, R = function(F, H, pe) {
    if (!I(H, "disabled")) {
      var W = X(pe), We = null;
      if (r.multiple) {
        var Ye = g || [];
        We = W ? Ye.filter(function(Je) {
          return Je !== pe;
        }) : [].concat(mK(Ye), [pe]);
      } else
        We = W ? null : pe;
      var Ze = W ? r.onTabClose : r.onTabOpen;
      Ze && Ze({
        originalEvent: F,
        index: pe
      }), r.onTabChange ? r.onTabChange({
        originalEvent: F,
        index: We
      }) : f(We);
    }
    F.preventDefault();
  }, O = function(F, H, pe) {
    switch (F.code) {
      case "ArrowDown":
        N(F);
        break;
      case "ArrowUp":
        le(F);
        break;
      case "Home":
        te(F);
        break;
      case "End":
        me(F);
        break;
      case "Enter":
      case "NumpadEnter":
      case "Space":
        ce(F, H, pe);
        break;
    }
  }, N = function(F) {
    var H = Ae(F.target.parentElement.parentElement);
    H ? B(H) : te(F), F.preventDefault();
  }, le = function(F) {
    var H = Fe(F.target.parentElement.parentElement);
    H ? B(H) : me(F), F.preventDefault();
  }, te = function(F) {
    var H = be();
    B(H), F.preventDefault();
  }, me = function(F) {
    var H = re();
    B(H), F.preventDefault();
  }, ce = function(F, H, pe) {
    R(F, H, pe), F.preventDefault();
  }, Ae = function J(F) {
    var H = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, pe = H ? F : F.nextElementSibling, W = j.findSingle(pe, '[data-pc-section="header"]');
    return W ? j.getAttribute(W, "data-p-disabled") ? J(W.parentElement) : j.findSingle(W, '[data-pc-section="headeraction"]') : null;
  }, Fe = function J(F) {
    var H = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1, pe = H ? F : F.previousElementSibling, W = j.findSingle(pe, '[data-pc-section="header"]');
    return W ? j.getAttribute(W, "data-p-disabled") ? J(W.parentElement) : j.findSingle(W, '[data-pc-section="headeraction"]') : null;
  }, be = function() {
    return Ae(m.current.firstElementChild, !0);
  }, re = function() {
    return Fe(m.current.lastElementChild, !0);
  }, B = function(F) {
    F && j.focus(F);
  }, X = function(F) {
    return r.multiple && Array.isArray(g) ? g && g.some(function(H) {
      return H === F;
    }) : g === F;
  };
  if (w.useImperativeHandle(t, function() {
    return {
      props: r,
      getElement: function() {
        return m.current;
      }
    };
  }), rr(function() {
    a || l(Kp());
  }), !a)
    return null;
  var he = function(F, H, pe) {
    var W = To(To({}, I(F, "style") || {}), I(F, "headerStyle") || {}), We = a + "_header_" + pe, Ye = a + "_content_" + pe, Ze = I(F, "disabled") ? -1 : I(F, "tabIndex"), Je = e({
      className: S("accordiontab.headertitle")
    }, A(F, "headertitle", pe)), pt = qo.getCProps(F), Rn = I(F, "headerTemplate") ? z.getJSXElement(I(F, "headerTemplate"), pt) : /* @__PURE__ */ w.createElement("span", Je, z.getJSXElement(I(F, "header"), pt)), rn = e({
      "aria-hidden": "true",
      className: S("accordiontab.headericon")
    }, A(F, "headericon", pe)), $t = H ? r.collapseIcon || /* @__PURE__ */ w.createElement(Jp, rn) : r.expandIcon || /* @__PURE__ */ w.createElement(yE, rn), zt = Mi.getJSXIcon($t, To({}, rn), {
      props: r,
      selected: H
    }), bn = e({
      className: Re(I(F, "headerClassName"), I(F, "className"), S("accordiontab.header", {
        selected: H,
        getTabProp: I,
        tab: F
      })),
      style: W,
      "data-p-highlight": H,
      "data-p-disabled": I(F, "disabled")
    }, A(F, "header", pe)), On = e({
      id: We,
      href: "#" + Ye,
      className: S("accordiontab.headeraction"),
      role: "button",
      tabIndex: Ze,
      onClick: function(Ht) {
        return L(Ht, F, pe);
      },
      onKeyDown: function(Ht) {
        return O(Ht, F, pe);
      },
      "aria-disabled": I(F, "disabled"),
      "aria-controls": Ye,
      "aria-expanded": H
    }, A(F, "headeraction", pe));
    return /* @__PURE__ */ w.createElement("div", bn, /* @__PURE__ */ w.createElement("a", On, zt, Rn));
  }, Q = function(F, H, pe) {
    var W = To(To({}, I(F, "style") || {}), I(F, "contentStyle") || {}), We = a + "_content_" + pe, Ye = a + "_header_" + pe, Ze = /* @__PURE__ */ w.createRef(), Je = e({
      id: We,
      ref: Ze,
      className: Re(I(F, "contentClassName"), I(F, "className"), S("accordiontab.toggleablecontent")),
      style: W,
      role: "region",
      "aria-labelledby": Ye
    }, A(F, "toggleablecontent", pe)), pt = e({
      className: S("accordiontab.content")
    }, A(F, "content", pe)), Rn = e({
      classNames: S("accordiontab.transition"),
      timeout: {
        enter: 1e3,
        exit: 450
      },
      in: H,
      unmountOnExit: !0,
      options: r.transitionOptions
    }, A(F, "transition", pe));
    return /* @__PURE__ */ w.createElement(lh, md({
      nodeRef: Ze
    }, Rn), /* @__PURE__ */ w.createElement("div", Je, /* @__PURE__ */ w.createElement("div", pt, I(F, "children"))));
  }, Y = function(F, H) {
    if (z.isValidChild(F, "AccordionTab")) {
      var pe = a + "_" + H, W = X(H), We = he(F, W, H), Ye = Q(F, W, H), Ze = e({
        key: pe,
        className: S("accordiontab.root", {
          selected: W
        })
      }, qo.getCOtherProps(F), A(F, "root", H));
      return /* @__PURE__ */ w.createElement("div", Ze, We, Ye);
    }
    return null;
  }, we = function() {
    return w.Children.map(r.children, Y);
  }, ye = we(), Ce = e({
    className: Re(r.className, S("root")),
    style: r.style
  }, nu.getOtherProps(r), y("root"));
  return /* @__PURE__ */ w.createElement("div", md({
    id: a,
    ref: m
  }, Ce), ye);
});
di.displayName = "AccordionTab";
or.displayName = "Accordion";
function eg(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function kK(i) {
  if (Array.isArray(i)) return eg(i);
}
function xK(i) {
  if (typeof Symbol < "u" && i[Symbol.iterator] != null || i["@@iterator"] != null) return Array.from(i);
}
function EK(i, t) {
  if (i) {
    if (typeof i == "string") return eg(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return eg(i, t);
  }
}
function TK() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function SK(i) {
  return kK(i) || xK(i) || EK(i) || TK();
}
var zo = {
  /* eslint-disable */
  DEFAULT_MASKS: {
    pint: /[\d]/,
    int: /[\d\-]/,
    pnum: /[\d\.]/,
    money: /[\d\.\s,]/,
    num: /[\d\-\.]/,
    hex: /[0-9a-f]/i,
    email: /[a-z0-9_\.\-@]/i,
    alpha: /[a-z_]/i,
    alphanum: /[a-z0-9_]/i
  },
  /* eslint-enable */
  getRegex: function(t) {
    return zo.DEFAULT_MASKS[t] ? zo.DEFAULT_MASKS[t] : t;
  },
  onBeforeInput: function(t, e, n) {
    n || !j.isAndroid() || this.validateKey(t, t.data, e);
  },
  onKeyPress: function(t, e, n) {
    n || j.isAndroid() || t.ctrlKey || t.altKey || t.metaKey || this.validateKey(t, t.key, e);
  },
  onPaste: function(t, e, n) {
    if (!n) {
      var r = this.getRegex(e), s = t.clipboardData.getData("text");
      SK(s).forEach(function(o) {
        if (!r.test(o))
          return t.preventDefault(), !1;
      });
    }
  },
  validateKey: function(t, e, n) {
    if (e != null) {
      var r = e.length <= 2;
      if (r) {
        var s = this.getRegex(n);
        s.test(e) || t.preventDefault();
      }
    }
  },
  validate: function(t, e) {
    var n = t.target.value, r = !0, s = this.getRegex(e);
    return n && !s.test(n) && (r = !1), r;
  }
};
function AK(i) {
  if (Array.isArray(i)) return i;
}
function PK(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function sy(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function IK(i, t) {
  if (i) {
    if (typeof i == "string") return sy(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return sy(i, t);
  }
}
function RK() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function OK(i, t) {
  return AK(i) || PK(i, t) || IK(i, t) || RK();
}
var tg = {
  defaultProps: {
    __TYPE: "Portal",
    element: null,
    appendTo: null,
    visible: !1,
    onMounted: null,
    onUnmounted: null,
    children: void 0
  },
  getProps: function(t) {
    return z.getMergedProps(t, tg.defaultProps);
  },
  getOtherProps: function(t) {
    return z.getDiffProps(t, tg.defaultProps);
  }
}, xc = /* @__PURE__ */ w.memo(function(i) {
  var t = tg.getProps(i), e = w.useContext(cn), n = w.useState(t.visible && j.isClient()), r = OK(n, 2), s = r[0], o = r[1];
  rr(function() {
    j.isClient() && !s && (o(!0), t.onMounted && t.onMounted());
  }), Zt(function() {
    t.onMounted && t.onMounted();
  }, [s]), wr(function() {
    t.onUnmounted && t.onUnmounted();
  });
  var a = t.element || t.children;
  if (a && s) {
    var l = t.appendTo || e && e.appendTo || tn.appendTo;
    return z.isFunction(l) && (l = l()), l || (l = document.body), l === "self" ? a : /* @__PURE__ */ Xa.createPortal(a, l);
  }
  return null;
});
xc.displayName = "Portal";
function gd() {
  return gd = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, gd.apply(this, arguments);
}
function Xl(i) {
  "@babel/helpers - typeof";
  return Xl = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Xl(i);
}
function VK(i, t) {
  if (Xl(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (Xl(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function MK(i) {
  var t = VK(i, "string");
  return Xl(t) === "symbol" ? t : String(t);
}
function CE(i, t, e) {
  return t = MK(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function ng(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function DK(i) {
  if (Array.isArray(i)) return ng(i);
}
function LK(i) {
  if (typeof Symbol < "u" && i[Symbol.iterator] != null || i["@@iterator"] != null) return Array.from(i);
}
function kE(i, t) {
  if (i) {
    if (typeof i == "string") return ng(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return ng(i, t);
  }
}
function NK() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function BK(i) {
  return DK(i) || LK(i) || kE(i) || NK();
}
function FK(i) {
  if (Array.isArray(i)) return i;
}
function $K(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function zK() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function Za(i, t) {
  return FK(i) || $K(i, t) || kE(i, t) || zK();
}
var HK = {
  root: function(t) {
    var e = t.positionState, n = t.classNameState;
    return Re("p-tooltip p-component", CE({}, "p-tooltip-".concat(e), !0), n);
  },
  arrow: "p-tooltip-arrow",
  text: "p-tooltip-text"
}, jK = {
  arrow: function(t) {
    var e = t.context;
    return {
      top: e.bottom ? "0" : e.right || e.left || !e.right && !e.left && !e.top && !e.bottom ? "50%" : null,
      bottom: e.top ? "0" : null,
      left: e.right || !e.right && !e.left && !e.top && !e.bottom ? "0" : e.top || e.bottom ? "50%" : null,
      right: e.left ? "0" : null
    };
  }
}, WK = `
@layer primereact {
    .p-tooltip {
        position: absolute;
        padding: .25em .5rem;
        /* #3687: Tooltip prevent scrollbar flickering */
        top: -9999px;
        left: -9999px;
    }
    
    .p-tooltip.p-tooltip-right,
    .p-tooltip.p-tooltip-left {
        padding: 0 .25rem;
    }
    
    .p-tooltip.p-tooltip-top,
    .p-tooltip.p-tooltip-bottom {
        padding:.25em 0;
    }
    
    .p-tooltip .p-tooltip-text {
       white-space: pre-line;
       word-break: break-word;
    }
    
    .p-tooltip-arrow {
        position: absolute;
        width: 0;
        height: 0;
        border-color: transparent;
        border-style: solid;
    }
    
    .p-tooltip-right .p-tooltip-arrow {
        top: 50%;
        left: 0;
        margin-top: -.25rem;
        border-width: .25em .25em .25em 0;
    }
    
    .p-tooltip-left .p-tooltip-arrow {
        top: 50%;
        right: 0;
        margin-top: -.25rem;
        border-width: .25em 0 .25em .25rem;
    }
    
    .p-tooltip.p-tooltip-top {
        padding: .25em 0;
    }
    
    .p-tooltip-top .p-tooltip-arrow {
        bottom: 0;
        left: 50%;
        margin-left: -.25rem;
        border-width: .25em .25em 0;
    }
    
    .p-tooltip-bottom .p-tooltip-arrow {
        top: 0;
        left: 50%;
        margin-left: -.25rem;
        border-width: 0 .25em .25rem;
    }

    .p-tooltip-target-wrapper {
        display: inline-flex;
    }
}
`, iu = et.extend({
  defaultProps: {
    __TYPE: "Tooltip",
    appendTo: null,
    at: null,
    autoHide: !0,
    autoZIndex: !0,
    baseZIndex: 0,
    className: null,
    closeOnEscape: !1,
    content: null,
    disabled: !1,
    event: null,
    hideDelay: 0,
    hideEvent: "mouseleave",
    id: null,
    mouseTrack: !1,
    mouseTrackLeft: 5,
    mouseTrackTop: 5,
    my: null,
    onBeforeHide: null,
    onBeforeShow: null,
    onHide: null,
    onShow: null,
    position: "right",
    showDelay: 0,
    showEvent: "mouseenter",
    showOnDisabled: !1,
    style: null,
    target: null,
    updateDelay: 0,
    children: void 0
  },
  css: {
    classes: HK,
    styles: WK,
    inlineStyles: jK
  }
});
function oy(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function UK(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? oy(Object(e), !0).forEach(function(n) {
      CE(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : oy(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var ya = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = iu.getProps(i, n), s = w.useState(!1), o = Za(s, 2), a = o[0], l = o[1], c = w.useState(r.position || "right"), u = Za(c, 2), d = u[0], f = u[1], m = w.useState(""), g = Za(m, 2), p = g[0], b = g[1], v = {
    props: r,
    state: {
      visible: a,
      position: d,
      className: p
    },
    context: {
      right: d === "right",
      left: d === "left",
      top: d === "top",
      bottom: d === "bottom"
    }
  }, y = iu.setMetaData(v), x = y.ptm, S = y.cx, k = y.sx, I = y.isUnstyled;
  sr(iu.css.styles, I, {
    name: "tooltip"
  }), hE({
    callback: function() {
      Je();
    },
    when: r.closeOnEscape,
    priority: [uE.TOOLTIP, 0]
  });
  var A = w.useRef(null), L = w.useRef(null), R = w.useRef(null), O = w.useRef(null), N = w.useRef(!0), le = w.useRef({}), te = w.useRef(null), me = Zp({
    listener: function(U) {
      !j.isTouchDevice() && Je(U);
    }
  }), ce = Za(me, 2), Ae = ce[0], Fe = ce[1], be = fE({
    target: R.current,
    listener: function(U) {
      Je(U);
    },
    when: a
  }), re = Za(be, 2), B = re[0], X = re[1], he = function(U) {
    return !(r.content || J(U, "tooltip"));
  }, Q = function(U) {
    return !(r.content || J(U, "tooltip") || r.children);
  }, Y = function(U) {
    return J(U, "mousetrack") || r.mouseTrack;
  }, we = function(U) {
    return J(U, "disabled") === "true" || F(U, "disabled") || r.disabled;
  }, ye = function(U) {
    return J(U, "showondisabled") || r.showOnDisabled;
  }, Ce = function() {
    return J(R.current, "autohide") || r.autoHide;
  }, J = function(U, _e) {
    return F(U, "data-pr-".concat(_e)) ? U.getAttribute("data-pr-".concat(_e)) : null;
  }, F = function(U, _e) {
    return U && U.hasAttribute(_e);
  }, H = function(U) {
    var _e = [J(U, "showevent") || r.showEvent], Ue = [J(U, "hideevent") || r.hideEvent];
    if (Y(U))
      _e = ["mousemove"], Ue = ["mouseleave"];
    else {
      var $e = J(U, "event") || r.event;
      $e === "focus" && (_e = ["focus"], Ue = ["blur"]), $e === "both" && (_e = ["focus", "mouseenter"], Ue = ["blur", "mouseleave"]);
    }
    return {
      showEvents: _e,
      hideEvents: Ue
    };
  }, pe = function(U) {
    return J(U, "position") || d;
  }, W = function(U) {
    var _e = J(U, "mousetracktop") || r.mouseTrackTop, Ue = J(U, "mousetrackleft") || r.mouseTrackLeft;
    return {
      top: _e,
      left: Ue
    };
  }, We = function(U, _e) {
    if (L.current) {
      var Ue = J(U, "tooltip") || r.content;
      Ue ? (L.current.innerHTML = "", L.current.appendChild(document.createTextNode(Ue)), _e()) : r.children && _e();
    }
  }, Ye = function(U) {
    We(R.current, function() {
      var _e = te.current, Ue = _e.pageX, $e = _e.pageY;
      r.autoZIndex && !Ki.get(A.current) && Ki.set("tooltip", A.current, n && n.autoZIndex || tn.autoZIndex, r.baseZIndex || n && n.zIndex.tooltip || tn.zIndex.tooltip), A.current.style.left = "", A.current.style.top = "", Ce() && (A.current.style.pointerEvents = "none");
      var ze = Y(R.current) || U === "mouse";
      (ze && !O.current || ze) && (O.current = {
        width: j.getOuterWidth(A.current),
        height: j.getOuterHeight(A.current)
      }), pt(R.current, {
        x: Ue,
        y: $e
      }, U);
    });
  }, Ze = function(U) {
    R.current = U.currentTarget;
    var _e = we(R.current), Ue = Q(ye(R.current) && _e ? R.current.firstChild : R.current);
    if (!(Ue || _e))
      if (te.current = U, a)
        On("updateDelay", Ye);
      else {
        var $e = Vt(r.onBeforeShow, {
          originalEvent: U,
          target: R.current
        });
        $e && On("showDelay", function() {
          l(!0), Vt(r.onShow, {
            originalEvent: U,
            target: R.current
          });
        });
      }
  }, Je = function(U) {
    if (Ht(), a) {
      var _e = Vt(r.onBeforeHide, {
        originalEvent: U,
        target: R.current
      });
      _e && On("hideDelay", function() {
        !Ce() && N.current === !1 || (Ki.clear(A.current), j.removeClass(A.current, "p-tooltip-active"), l(!1), Vt(r.onHide, {
          originalEvent: U,
          target: R.current
        }));
      });
    }
  }, pt = function(U, _e, Ue) {
    var $e = 0, ze = 0, ft = Ue || d;
    if ((Y(U) || ft == "mouse") && _e) {
      var Jt = {
        width: j.getOuterWidth(A.current),
        height: j.getOuterHeight(A.current)
      };
      $e = _e.x, ze = _e.y;
      var it = W(U), rt = it.top, dt = it.left;
      switch (ft) {
        case "left":
          $e = $e - (Jt.width + dt), ze = ze - (Jt.height / 2 - rt);
          break;
        case "right":
        case "mouse":
          $e = $e + dt, ze = ze - (Jt.height / 2 - rt);
          break;
        case "top":
          $e = $e - (Jt.width / 2 - dt), ze = ze - (Jt.height + rt);
          break;
        case "bottom":
          $e = $e - (Jt.width / 2 - dt), ze = ze + rt;
          break;
      }
      $e <= 0 || O.current.width > Jt.width ? (A.current.style.left = "0px", A.current.style.right = window.innerWidth - Jt.width - $e + "px") : (A.current.style.right = "", A.current.style.left = $e + "px"), A.current.style.top = ze + "px", j.addClass(A.current, "p-tooltip-active");
    } else {
      var Mt = j.findCollisionPosition(ft), Dt = J(U, "my") || r.my || Mt.my, Lt = J(U, "at") || r.at || Mt.at;
      A.current.style.padding = "0px", j.flipfitCollision(A.current, U, Dt, Lt, function(Nt) {
        var on = Nt.at, Pt = on.x, vn = on.y, wn = Nt.my.x, yn = r.at ? Pt !== "center" && Pt !== wn ? Pt : vn : Nt.at["".concat(Mt.axis)];
        A.current.style.padding = "", f(yn), Rn(yn), j.addClass(A.current, "p-tooltip-active");
      });
    }
  }, Rn = function(U) {
    if (A.current) {
      var _e = getComputedStyle(A.current);
      U === "left" ? A.current.style.left = parseFloat(_e.left) - parseFloat(_e.paddingLeft) * 2 + "px" : U === "top" && (A.current.style.top = parseFloat(_e.top) - parseFloat(_e.paddingTop) * 2 + "px");
    }
  }, rn = function() {
    Ce() || (N.current = !1);
  }, $t = function(U) {
    Ce() || (N.current = !0, Je(U));
  }, zt = function(U) {
    if (U) {
      var _e = H(U), Ue = _e.showEvents, $e = _e.hideEvents, ze = hi(U);
      Ue.forEach(function(ft) {
        return ze == null ? void 0 : ze.addEventListener(ft, Ze);
      }), $e.forEach(function(ft) {
        return ze == null ? void 0 : ze.addEventListener(ft, Je);
      });
    }
  }, bn = function(U) {
    if (U) {
      var _e = H(U), Ue = _e.showEvents, $e = _e.hideEvents, ze = hi(U);
      Ue.forEach(function(ft) {
        return ze == null ? void 0 : ze.removeEventListener(ft, Ze);
      }), $e.forEach(function(ft) {
        return ze == null ? void 0 : ze.removeEventListener(ft, Je);
      });
    }
  }, On = function(U, _e) {
    Ht();
    var Ue = J(R.current, U.toLowerCase()) || r[U];
    Ue ? le.current["".concat(U)] = setTimeout(function() {
      return _e();
    }, Ue) : _e();
  }, Vt = function(U) {
    if (U) {
      for (var _e = arguments.length, Ue = new Array(_e > 1 ? _e - 1 : 0), $e = 1; $e < _e; $e++)
        Ue[$e - 1] = arguments[$e];
      var ze = U.apply(void 0, Ue);
      return ze === void 0 && (ze = !0), ze;
    }
    return !0;
  }, Ht = function() {
    Object.values(le.current).forEach(function(U) {
      return clearTimeout(U);
    });
  }, hi = function(U) {
    if (U) {
      if (ye(U)) {
        if (!U.hasWrapper) {
          var _e = document.createElement("div"), Ue = U.nodeName === "INPUT";
          return Ue ? j.addMultipleClasses(_e, "p-tooltip-target-wrapper p-inputwrapper") : j.addClass(_e, "p-tooltip-target-wrapper"), U.parentNode.insertBefore(_e, U), _e.appendChild(U), U.hasWrapper = !0, _e;
        }
        return U.parentElement;
      } else if (U.hasWrapper) {
        var $e;
        ($e = U.parentElement).replaceWith.apply($e, BK(U.parentElement.childNodes)), delete U.hasWrapper;
      }
      return U;
    }
    return null;
  }, jt = function(U) {
    Yt(U), Wt(U);
  }, Wt = function(U) {
    ti(U || r.target, zt);
  }, Yt = function(U) {
    ti(U || r.target, bn);
  }, ti = function(U, _e) {
    if (U = z.getRefElement(U), U)
      if (j.isElement(U))
        _e(U);
      else {
        var Ue = function(ze) {
          var ft = j.find(document, ze);
          ft.forEach(function(Jt) {
            _e(Jt);
          });
        };
        U instanceof Array ? U.forEach(function($e) {
          Ue($e);
        }) : Ue(U);
      }
  };
  rr(function() {
    a && R.current && we(R.current) && Je();
  }), Zt(function() {
    return Wt(), function() {
      Yt();
    };
  }, [Ze, Je, r.target]), Zt(function() {
    if (a) {
      var Le = pe(R.current), U = J(R.current, "classname");
      f(Le), b(U), Ye(Le), Ae(), B();
    } else
      f(r.position || "right"), b(""), R.current = null, O.current = null, N.current = !0;
    return function() {
      Fe(), X();
    };
  }, [a]), Zt(function() {
    var Le = pe(R.current);
    a && Le !== "mouse" && On("updateDelay", function() {
      We(R.current, function() {
        pt(R.current);
      });
    });
  }, [r.content]), wr(function() {
    Je(), Ki.clear(A.current);
  }), w.useImperativeHandle(t, function() {
    return {
      props: r,
      updateTargetEvents: jt,
      loadTargetEvents: Wt,
      unloadTargetEvents: Yt,
      show: Ze,
      hide: Je,
      getElement: function() {
        return A.current;
      },
      getTarget: function() {
        return R.current;
      }
    };
  });
  var sn = function() {
    var U = he(R.current), _e = e({
      id: r.id,
      className: Re(r.className, S("root", {
        positionState: d,
        classNameState: p
      })),
      style: r.style,
      role: "tooltip",
      "aria-hidden": a,
      onMouseEnter: function(ft) {
        return rn();
      },
      onMouseLeave: function(ft) {
        return $t(ft);
      }
    }, iu.getOtherProps(r), x("root")), Ue = e({
      className: S("arrow"),
      style: k("arrow", UK({}, v))
    }, x("arrow")), $e = e({
      className: S("text")
    }, x("text"));
    return /* @__PURE__ */ w.createElement("div", gd({
      ref: A
    }, _e), /* @__PURE__ */ w.createElement("div", Ue), /* @__PURE__ */ w.createElement("div", gd({
      ref: L
    }, $e), U && r.children));
  };
  if (a) {
    var un = sn();
    return /* @__PURE__ */ w.createElement(xc, {
      element: un,
      appendTo: r.appendTo,
      visible: !0
    });
  }
  return null;
}));
ya.displayName = "Tooltip";
function pd() {
  return pd = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, pd.apply(this, arguments);
}
function Ql(i) {
  "@babel/helpers - typeof";
  return Ql = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, Ql(i);
}
function qK(i, t) {
  if (Ql(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (Ql(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function GK(i) {
  var t = qK(i, "string");
  return Ql(t) === "symbol" ? t : String(t);
}
function KK(i, t, e) {
  return t = GK(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
var ZK = {
  root: function(t) {
    var e = t.props, n = t.isFilled, r = t.context;
    return Re("p-inputtext p-component", {
      "p-disabled": e.disabled,
      "p-filled": n,
      "p-invalid": e.invalid,
      "p-variant-filled": e.variant ? e.variant === "filled" : r && r.inputStyle === "filled"
    });
  }
}, ru = et.extend({
  defaultProps: {
    __TYPE: "InputText",
    __parentMetadata: null,
    children: void 0,
    className: null,
    invalid: !1,
    variant: null,
    keyfilter: null,
    onBeforeInput: null,
    onInput: null,
    onKeyDown: null,
    onPaste: null,
    tooltip: null,
    tooltipOptions: null,
    validateOnly: !1,
    iconPosition: null
  },
  css: {
    classes: ZK
  }
});
function ay(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function ly(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? ay(Object(e), !0).forEach(function(n) {
      KK(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : ay(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var _t = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = ru.getProps(i, n), s = ru.setMetaData(ly(ly({
    props: r
  }, r.__parentMetadata), {}, {
    context: {
      disabled: r.disabled,
      iconPosition: r.iconPosition
    }
  })), o = s.ptm, a = s.cx, l = s.isUnstyled;
  sr(ru.css.styles, l, {
    name: "inputtext",
    styled: !0
  });
  var c = w.useRef(t), u = function(y) {
    r.onKeyDown && r.onKeyDown(y), r.keyfilter && zo.onKeyPress(y, r.keyfilter, r.validateOnly);
  }, d = function(y) {
    r.onBeforeInput && r.onBeforeInput(y), r.keyfilter && zo.onBeforeInput(y, r.keyfilter, r.validateOnly);
  }, f = function(y) {
    var x = y.target, S = !0;
    r.keyfilter && r.validateOnly && (S = zo.validate(y, r.keyfilter)), r.onInput && r.onInput(y, S), z.isNotEmpty(x.value) ? j.addClass(x, "p-filled") : j.removeClass(x, "p-filled");
  }, m = function(y) {
    r.onPaste && r.onPaste(y), r.keyfilter && zo.onPaste(y, r.keyfilter, r.validateOnly);
  };
  w.useEffect(function() {
    z.combinedRefs(c, t);
  }, [c, t]);
  var g = w.useMemo(function() {
    return z.isNotEmpty(r.value) || z.isNotEmpty(r.defaultValue);
  }, [r.value, r.defaultValue]), p = z.isNotEmpty(r.tooltip), b = e({
    className: Re(r.className, a("root", {
      context: n,
      isFilled: g
    })),
    onBeforeInput: d,
    onInput: f,
    onKeyDown: u,
    onPaste: m
  }, ru.getOtherProps(r), o("root"));
  return /* @__PURE__ */ w.createElement(w.Fragment, null, /* @__PURE__ */ w.createElement("input", pd({
    ref: c
  }, b)), p && /* @__PURE__ */ w.createElement(ya, pd({
    target: c,
    content: r.tooltip,
    pt: o("tooltip")
  }, r.tooltipOptions)));
}));
_t.displayName = "InputText";
const YK = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...Ko, ...i }), r = (d) => {
    const f = { ...e, [d.target.name]: d.target.value };
    n(f), t(f);
  }, s = (d) => {
    const f = { ...e, value: d };
    n(f), t(f);
  };
  Ee(() => {
    n({ ...Ko, ...i });
  }, [i]);
  const o = at(null), a = at(null), [l, c] = Ne(!1);
  Ee(() => (c(!0), () => c(!1)), []);
  const u = {
    toolbar: {
      items: [
        "undo",
        "redo",
        "|",
        "sourceEditing",
        "showBlocks",
        "findAndReplace",
        "|",
        "heading",
        "|",
        "bold",
        "italic",
        "underline",
        "strikethrough",
        "subscript",
        "superscript",
        "code",
        "removeFormat",
        "|",
        "link",
        "mediaEmbed",
        "insertTable",
        "highlight",
        "codeBlock",
        "htmlEmbed",
        "|",
        "bulletedList",
        "numberedList"
      ],
      shouldNotGroupWhenFull: !1
    },
    plugins: [
      HC,
      oL,
      qF,
      nx,
      aL,
      OD,
      DD,
      uL,
      UN,
      gL,
      oB,
      CB,
      DB,
      NF,
      HB,
      YB,
      FF,
      nF,
      n$,
      l$,
      r$,
      P$,
      $$,
      J$,
      Ap,
      X$,
      T$,
      wL,
      Az,
      Dz,
      oH,
      EH,
      Hj,
      Xu,
      Aj,
      RW,
      LW,
      Tk,
      zW,
      GW,
      kL,
      SL,
      RL,
      fq,
      aG,
      Dq,
      hG,
      Qq,
      mq,
      QD,
      DL,
      Xd
    ],
    balloonToolbar: ["bold", "italic", "|", "link", "|", "bulletedList", "numberedList"],
    blockToolbar: ["bold", "italic", "|", "link", "insertTable", "|", "bulletedList", "numberedList"],
    htmlSupport: {
      allow: [
        {
          name: /^.*$/,
          styles: !0,
          attributes: !0,
          classes: !0
        }
      ]
    },
    image: {
      toolbar: [
        "toggleImageCaption",
        "imageTextAlternative",
        "|",
        "imageStyle:inline",
        "imageStyle:wrapText",
        "imageStyle:breakText",
        "|",
        "resizeImage"
      ]
    },
    initialData: e.value,
    link: {
      addTargetToExternalLinks: !0,
      defaultProtocol: "https://",
      decorators: {
        toggleDownloadable: {
          mode: "manual",
          label: "Downloadable",
          attributes: {
            download: "file"
          }
        }
      }
    },
    list: {
      properties: {
        styles: !0,
        startIndex: !0,
        reversed: !0
      }
    },
    placeholder: "Type or paste your content here!",
    table: {
      contentToolbar: ["tableColumn", "tableRow", "mergeTableCells", "tableProperties", "tableCellProperties"]
    }
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx(or, { activeIndex: 0, children: /* @__PURE__ */ _.jsx(di, { header: "General", children: /* @__PURE__ */ _.jsxs("div", { className: "form-item flex flex-column gap-4", children: [
    /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
      /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Label" }),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: e.label || "",
          name: "label",
          onChange: r,
          className: "w-full"
        }
      )
    ] }),
    /* @__PURE__ */ _.jsx("div", { className: "main-container", children: /* @__PURE__ */ _.jsx(
      "div",
      {
        className: "editor-container editor-container_classic-editor editor-container_include-block-toolbar",
        ref: o,
        children: /* @__PURE__ */ _.jsx("div", { className: "editor-container__editor", children: /* @__PURE__ */ _.jsx("div", { ref: a, children: l && /* @__PURE__ */ _.jsx(
          Ig,
          {
            editor: cB,
            config: u,
            onChange: (d, f) => {
              s(f.getData());
            }
          }
        ) }) })
      }
    ) })
  ] }) }, "general") }) });
}, JK = (i) => ({}), XK = new ps({
  type: Ko.type,
  form: new zn({
    render: N5,
    validation: B5
  }),
  settings: new zn({
    render: YK,
    validation: JK
  }),
  heading: "HTML Content",
  description: "WYSIWYG editor for adding in content to the form",
  hidden: !1,
  icon: F5,
  data: Ko
}), xE = new Id(new Pd({ label: "Content" }));
xE.registerItem(XK);
const zs = {
  id: br(),
  type: "input-number",
  label: "",
  value: "",
  placeholder: ""
}, QK = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...zs, ...i });
  Ee(() => {
    n({ ...zs, ...i });
  }, [i]);
  const r = (o) => {
    const a = { ...zs, ...i, value: o.target.value };
    t(a);
  }, s = Math.random().toString(36).substring(2, 15);
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { htmlFor: s, children: e.label }),
    /* @__PURE__ */ _.jsx(
      _t,
      {
        type: "number",
        value: e.value,
        onChange: r,
        placeholder: e.placeholder || "",
        className: "w-full",
        id: s
      }
    )
  ] }) }) });
}, eZ = (i) => ({});
var cy;
function ig() {
  return ig = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, ig.apply(null, arguments);
}
var tZ = function(t) {
  return /* @__PURE__ */ w.createElement("svg", ig({
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 320 512"
  }, t), cy || (cy = /* @__PURE__ */ w.createElement("path", {
    d: "M0 64c0-17.7 14.3-32 32-32h256c11.5 0 22 6.1 27.7 16.1s5.7 22.2-.1 32.1l-224 384c-8.9 15.3-28.5 20.4-43.8 11.5s-20.4-28.5-11.5-43.8L232.3 96H32C14.3 96 0 81.7 0 64"
  })));
};
const nZ = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...zs, ...i }), r = (s) => {
    const o = { ...e, [s.target.name]: s.target.value };
    n(o), t(o);
  };
  return Ee(() => {
    n({ ...zs, ...i });
  }, [i]), /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs(or, { activeIndex: 0, children: [
    " ",
    /* @__PURE__ */ _.jsx(di, { header: "General", children: /* @__PURE__ */ _.jsxs("div", { className: "form-item flex flex-column gap-4", children: [
      /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
        /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Label" }),
        /* @__PURE__ */ _.jsx(
          _t,
          {
            id: "label",
            value: e.label || "",
            name: "label",
            onChange: r,
            className: "w-full"
          }
        )
      ] }),
      /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
        /* @__PURE__ */ _.jsx("label", { htmlFor: "placeholder", children: "Placeholder" }),
        /* @__PURE__ */ _.jsx(
          _t,
          {
            id: "placeholder",
            value: e.placeholder || "",
            name: "placeholder",
            onChange: r,
            className: "w-full"
          }
        )
      ] })
    ] }) }, "general"),
    " "
  ] }) });
}, iZ = (i) => ({}), rZ = new ps({
  type: zs.type,
  form: new zn({
    render: QK,
    validation: eZ
  }),
  settings: new zn({
    render: nZ,
    validation: iZ
  }),
  heading: "Number input",
  description: "a simple box to insert a number value into",
  hidden: !1,
  icon: tZ,
  data: zs
}), os = {
  id: br(),
  type: "input-select",
  label: "",
  value: "",
  source: "local",
  options: [],
  placeholder: ""
}, If = (i) => i instanceof Date, sZ = (i) => Object.keys(i).length === 0, rg = (i) => i != null && typeof i == "object", uy = (i, ...t) => Object.prototype.hasOwnProperty.call(i, ...t), Rf = (i) => rg(i) && sZ(i), oZ = () => /* @__PURE__ */ Object.create(null), EE = (i, t) => {
  if (i === t) return {};
  if (!rg(i) || !rg(t)) return t;
  const e = Object.keys(i).reduce((n, r) => (uy(t, r) || (n[r] = void 0), n), oZ());
  return If(i) || If(t) ? i.valueOf() == t.valueOf() ? {} : t : Object.keys(t).reduce((n, r) => {
    if (!uy(i, r))
      return n[r] = t[r], n;
    const s = EE(i[r], t[r]);
    return Rf(s) && !If(s) && (Rf(i[r]) || !Rf(t[r])) || (n[r] = s), n;
  }, e);
};
function pn(i) {
  return JSON.parse(JSON.stringify(i));
}
function TE(i, t) {
  const e = EE(i, t);
  return Object.keys(e).length !== 0;
}
const dy = (i) => {
  let t;
  const e = /* @__PURE__ */ new Set(), n = (c, u) => {
    const d = typeof c == "function" ? c(t) : c;
    if (!Object.is(d, t)) {
      const f = t;
      t = u ?? (typeof d != "object" || d === null) ? d : Object.assign({}, t, d), e.forEach((m) => m(t, f));
    }
  }, r = () => t, a = { setState: n, getState: r, getInitialState: () => l, subscribe: (c) => (e.add(c), () => e.delete(c)) }, l = t = i(n, r, a);
  return a;
}, aZ = (i) => i ? dy(i) : dy, lZ = (i) => i;
function cZ(i, t = lZ) {
  const e = Se.useSyncExternalStore(
    i.subscribe,
    () => t(i.getState()),
    () => t(i.getInitialState())
  );
  return Se.useDebugValue(e), e;
}
const hy = (i) => {
  const t = aZ(i), e = (n) => cZ(t, n);
  return Object.assign(e, t), e;
}, SE = (i) => i ? hy(i) : hy, _r = SE()((i, t) => ({
  config: new D5(),
  setConfig: (e) => {
    TE(t().config, e) && i(() => ({ config: e }));
  }
})), uZ = {
  id: null,
  label: null,
  created_at: null,
  items: []
}, dZ = (i) => (i.items.map((t) => {
  let e = ks.getByItem(t);
  return e ? { ...e.data, ...t } : t;
}), i), Hr = SE()((i, t) => ({
  form: uZ,
  setForm: (e) => {
    TE(t().form, e) && i(() => ({ form: dZ(e) }));
  }
}));
function sg() {
  return sg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, sg.apply(this, arguments);
}
var AE = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", sg({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    d: "M12.2097 10.4113C12.1057 10.4118 12.0027 10.3915 11.9067 10.3516C11.8107 10.3118 11.7237 10.2532 11.6506 10.1792L6.93602 5.46461L2.22139 10.1476C2.07272 10.244 1.89599 10.2877 1.71953 10.2717C1.54307 10.2556 1.3771 10.1808 1.24822 10.0593C1.11933 9.93766 1.035 9.77633 1.00874 9.6011C0.982477 9.42587 1.0158 9.2469 1.10338 9.09287L6.37701 3.81923C6.52533 3.6711 6.72639 3.58789 6.93602 3.58789C7.14565 3.58789 7.3467 3.6711 7.49502 3.81923L12.7687 9.09287C12.9168 9.24119 13 9.44225 13 9.65187C13 9.8615 12.9168 10.0626 12.7687 10.2109C12.616 10.3487 12.4151 10.4207 12.2097 10.4113Z",
    fill: "currentColor"
  }));
}));
AE.displayName = "ChevronUpIcon";
function og() {
  return og = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, og.apply(this, arguments);
}
var ch = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", og({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    d: "M6.99701 14C5.85441 13.999 4.72939 13.7186 3.72012 13.1832C2.71084 12.6478 1.84795 11.8737 1.20673 10.9284C0.565504 9.98305 0.165424 8.89526 0.041387 7.75989C-0.0826496 6.62453 0.073125 5.47607 0.495122 4.4147C0.917119 3.35333 1.59252 2.4113 2.46241 1.67077C3.33229 0.930247 4.37024 0.413729 5.4857 0.166275C6.60117 -0.0811796 7.76026 -0.0520535 8.86188 0.251112C9.9635 0.554278 10.9742 1.12227 11.8057 1.90555C11.915 2.01493 11.9764 2.16319 11.9764 2.31778C11.9764 2.47236 11.915 2.62062 11.8057 2.73C11.7521 2.78503 11.688 2.82877 11.6171 2.85864C11.5463 2.8885 11.4702 2.90389 11.3933 2.90389C11.3165 2.90389 11.2404 2.8885 11.1695 2.85864C11.0987 2.82877 11.0346 2.78503 10.9809 2.73C9.9998 1.81273 8.73246 1.26138 7.39226 1.16876C6.05206 1.07615 4.72086 1.44794 3.62279 2.22152C2.52471 2.99511 1.72683 4.12325 1.36345 5.41602C1.00008 6.70879 1.09342 8.08723 1.62775 9.31926C2.16209 10.5513 3.10478 11.5617 4.29713 12.1803C5.48947 12.7989 6.85865 12.988 8.17414 12.7157C9.48963 12.4435 10.6711 11.7264 11.5196 10.6854C12.3681 9.64432 12.8319 8.34282 12.8328 7C12.8328 6.84529 12.8943 6.69692 13.0038 6.58752C13.1132 6.47812 13.2616 6.41667 13.4164 6.41667C13.5712 6.41667 13.7196 6.47812 13.8291 6.58752C13.9385 6.69692 14 6.84529 14 7C14 8.85651 13.2622 10.637 11.9489 11.9497C10.6356 13.2625 8.85432 14 6.99701 14Z",
    fill: "currentColor"
  }));
}));
ch.displayName = "SpinnerIcon";
function ag() {
  return ag = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, ag.apply(this, arguments);
}
var uh = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", ag({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    d: "M8.01186 7.00933L12.27 2.75116C12.341 2.68501 12.398 2.60524 12.4375 2.51661C12.4769 2.42798 12.4982 2.3323 12.4999 2.23529C12.5016 2.13827 12.4838 2.0419 12.4474 1.95194C12.4111 1.86197 12.357 1.78024 12.2884 1.71163C12.2198 1.64302 12.138 1.58893 12.0481 1.55259C11.9581 1.51625 11.8617 1.4984 11.7647 1.50011C11.6677 1.50182 11.572 1.52306 11.4834 1.56255C11.3948 1.60204 11.315 1.65898 11.2488 1.72997L6.99067 5.98814L2.7325 1.72997C2.59553 1.60234 2.41437 1.53286 2.22718 1.53616C2.03999 1.53946 1.8614 1.61529 1.72901 1.74767C1.59663 1.88006 1.5208 2.05865 1.5175 2.24584C1.5142 2.43303 1.58368 2.61419 1.71131 2.75116L5.96948 7.00933L1.71131 11.2675C1.576 11.403 1.5 11.5866 1.5 11.7781C1.5 11.9696 1.576 12.1532 1.71131 12.2887C1.84679 12.424 2.03043 12.5 2.2219 12.5C2.41338 12.5 2.59702 12.424 2.7325 12.2887L6.99067 8.03052L11.2488 12.2887C11.3843 12.424 11.568 12.5 11.7594 12.5C11.9509 12.5 12.1346 12.424 12.27 12.2887C12.4053 12.1532 12.4813 11.9696 12.4813 11.7781C12.4813 11.5866 12.4053 11.403 12.27 11.2675L8.01186 7.00933Z",
    fill: "currentColor"
  }));
}));
uh.displayName = "TimesIcon";
var Hs = CG();
function lg() {
  return lg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, lg.apply(this, arguments);
}
var PE = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", lg({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    fillRule: "evenodd",
    clipRule: "evenodd",
    d: "M2.67602 11.0265C3.6661 11.688 4.83011 12.0411 6.02086 12.0411C6.81149 12.0411 7.59438 11.8854 8.32483 11.5828C8.87005 11.357 9.37808 11.0526 9.83317 10.6803L12.9769 13.8241C13.0323 13.8801 13.0983 13.9245 13.171 13.9548C13.2438 13.985 13.3219 14.0003 13.4007 14C13.4795 14.0003 13.5575 13.985 13.6303 13.9548C13.7031 13.9245 13.7691 13.8801 13.8244 13.8241C13.9367 13.7116 13.9998 13.5592 13.9998 13.4003C13.9998 13.2414 13.9367 13.089 13.8244 12.9765L10.6807 9.8328C11.053 9.37773 11.3573 8.86972 11.5831 8.32452C11.8857 7.59408 12.0414 6.81119 12.0414 6.02056C12.0414 4.8298 11.6883 3.66579 11.0268 2.67572C10.3652 1.68564 9.42494 0.913972 8.32483 0.45829C7.22472 0.00260857 6.01418 -0.116618 4.84631 0.115686C3.67844 0.34799 2.60568 0.921393 1.76369 1.76338C0.921698 2.60537 0.348296 3.67813 0.115991 4.84601C-0.116313 6.01388 0.00291375 7.22441 0.458595 8.32452C0.914277 9.42464 1.68595 10.3649 2.67602 11.0265ZM3.35565 2.0158C4.14456 1.48867 5.07206 1.20731 6.02086 1.20731C7.29317 1.20731 8.51338 1.71274 9.41304 2.6124C10.3127 3.51206 10.8181 4.73226 10.8181 6.00457C10.8181 6.95337 10.5368 7.88088 10.0096 8.66978C9.48251 9.45868 8.73328 10.0736 7.85669 10.4367C6.98011 10.7997 6.01554 10.8947 5.08496 10.7096C4.15439 10.5245 3.2996 10.0676 2.62869 9.39674C1.95778 8.72583 1.50089 7.87104 1.31579 6.94046C1.13068 6.00989 1.22568 5.04532 1.58878 4.16874C1.95187 3.29215 2.56675 2.54292 3.35565 2.0158Z",
    fill: "currentColor"
  }));
}));
PE.displayName = "SearchIcon";
function cg() {
  return cg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, cg.apply(this, arguments);
}
function ec(i) {
  "@babel/helpers - typeof";
  return ec = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, ec(i);
}
function hZ(i, t) {
  if (ec(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (ec(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function fZ(i) {
  var t = hZ(i, "string");
  return ec(t) === "symbol" ? t : String(t);
}
function IE(i, t, e) {
  return t = fZ(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function mZ(i) {
  if (Array.isArray(i)) return i;
}
function gZ(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t === 0) {
        if (Object(e) !== e) return;
        l = !1;
      } else for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function fy(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function pZ(i, t) {
  if (i) {
    if (typeof i == "string") return fy(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return fy(i, t);
  }
}
function bZ() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function Tr(i, t) {
  return mZ(i) || gZ(i, t) || pZ(i, t) || bZ();
}
var vZ = `
.p-virtualscroller {
    position: relative;
    overflow: auto;
    contain: strict;
    transform: translateZ(0);
    will-change: scroll-position;
    outline: 0 none;
}

.p-virtualscroller-content {
    position: absolute;
    top: 0;
    left: 0;
    /*contain: content;*/
    min-height: 100%;
    min-width: 100%;
    will-change: transform;
}

.p-virtualscroller-spacer {
    position: absolute;
    top: 0;
    left: 0;
    height: 1px;
    width: 1px;
    transform-origin: 0 0;
    pointer-events: none;
}

.p-virtualscroller-loader {
    position: sticky;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}

.p-virtualscroller-loader.p-component-overlay {
    display: flex;
    align-items: center;
    justify-content: center;
}

.p-virtualscroller-loading-icon {
    font-size: 2rem;
}

.p-virtualscroller-horizontal > .p-virtualscroller-content {
    display: flex;
}

/* Inline */
.p-virtualscroller-inline .p-virtualscroller-content {
    position: static;
}
`, su = et.extend({
  defaultProps: {
    __TYPE: "VirtualScroller",
    __parentMetadata: null,
    id: null,
    style: null,
    className: null,
    tabIndex: 0,
    items: null,
    itemSize: 0,
    scrollHeight: null,
    scrollWidth: null,
    orientation: "vertical",
    step: 0,
    numToleratedItems: null,
    delay: 0,
    resizeDelay: 10,
    appendOnly: !1,
    inline: !1,
    lazy: !1,
    disabled: !1,
    loaderDisabled: !1,
    loadingIcon: null,
    columns: null,
    loading: void 0,
    autoSize: !1,
    showSpacer: !0,
    showLoader: !1,
    loadingTemplate: null,
    loaderIconTemplate: null,
    itemTemplate: null,
    contentTemplate: null,
    onScroll: null,
    onScrollIndexChange: null,
    onLazyLoad: null,
    children: void 0
  },
  css: {
    styles: vZ
  }
});
function my(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function So(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? my(Object(e), !0).forEach(function(n) {
      IE(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : my(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var RE = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = su.getProps(i, n), s = Kl(i) || {}, o = r.orientation === "vertical", a = r.orientation === "horizontal", l = r.orientation === "both", c = w.useState(l ? {
    rows: 0,
    cols: 0
  } : 0), u = Tr(c, 2), d = u[0], f = u[1], m = w.useState(l ? {
    rows: 0,
    cols: 0
  } : 0), g = Tr(m, 2), p = g[0], b = g[1], v = w.useState(0), y = Tr(v, 2), x = y[0], S = y[1], k = w.useState(l ? {
    rows: 0,
    cols: 0
  } : 0), I = Tr(k, 2), A = I[0], L = I[1], R = w.useState(r.numToleratedItems), O = Tr(R, 2), N = O[0], le = O[1], te = w.useState(r.loading || !1), me = Tr(te, 2), ce = me[0], Ae = me[1], Fe = w.useState([]), be = Tr(Fe, 2), re = be[0], B = be[1], X = su.setMetaData({
    props: r,
    state: {
      first: d,
      last: p,
      page: x,
      numItemsInViewport: A,
      numToleratedItems: N,
      loading: ce,
      loaderArr: re
    }
  }), he = X.ptm;
  Ls(su.css.styles, {
    name: "virtualscroller"
  });
  var Q = w.useRef(null), Y = w.useRef(null), we = w.useRef(null), ye = w.useRef(null), Ce = w.useRef(l ? {
    top: 0,
    left: 0
  } : 0), J = w.useRef(null), F = w.useRef(null), H = w.useRef({}), pe = w.useRef({}), W = w.useRef(null), We = w.useRef(null), Ye = w.useRef(null), Ze = w.useRef(null), Je = w.useRef(!1), pt = w.useRef(null), Rn = Zp({
    listener: function(M) {
      return Dt();
    },
    when: !r.disabled
  }), rn = Tr(Rn, 1), $t = rn[0], zt = is({
    target: "window",
    type: "orientationchange",
    listener: function(M) {
      return Dt();
    },
    when: !r.disabled
  }), bn = Tr(zt, 1), On = bn[0], Vt = function() {
    return Q;
  }, Ht = function(M) {
    return Math.floor((M + N * 4) / (r.step || 1));
  }, hi = function(M) {
    Y.current = M || Y.current || j.findSingle(Q.current, ".p-virtualscroller-content");
  }, jt = function(M) {
    return r.step ? x !== Ht(M) : !0;
  }, Wt = function(M) {
    Ce.current = l ? {
      top: 0,
      left: 0
    } : 0, Q.current && Q.current.scrollTo(M);
  }, Yt = function(M) {
    var $ = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "auto", Z = U(), ne = Z.numToleratedItems, oe = ze(), se = function() {
      var Ut = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0, fi = arguments.length > 1 ? arguments[1] : void 0;
      return Ut <= fi ? 0 : Ut;
    }, ve = function(Ut, fi, Cr) {
      return Ut * fi + Cr;
    }, Ge = function() {
      var Ut = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0, fi = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
      return Wt({
        left: Ut,
        top: fi,
        behavior: $
      });
    }, Be = l ? {
      rows: 0,
      cols: 0
    } : 0, Et = !1;
    l ? (Be = {
      rows: se(M[0], ne[0]),
      cols: se(M[1], ne[1])
    }, Ge(ve(Be.cols, r.itemSize[1], oe.left), ve(Be.rows, r.itemSize[0], oe.top)), Et = d.rows !== Be.rows || d.cols !== Be.cols) : (Be = se(M, ne), a ? Ge(ve(Be, r.itemSize, oe.left), 0) : Ge(0, ve(Be, r.itemSize, oe.top)), Et = d !== Be), Je.current = Et, f(Be);
  }, ti = function(M, $) {
    var Z = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "auto";
    if ($) {
      var ne = Le(), oe = ne.first, se = ne.viewport, ve = function() {
        var fi = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0, Cr = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
        return Wt({
          left: fi,
          top: Cr,
          behavior: Z
        });
      }, Ge = $ === "to-start", Be = $ === "to-end";
      if (Ge) {
        if (l)
          se.first.rows - oe.rows > M[0] ? ve(se.first.cols * r.itemSize[1], (se.first.rows - 1) * r.itemSize[0]) : se.first.cols - oe.cols > M[1] && ve((se.first.cols - 1) * r.itemSize[1], se.first.rows * r.itemSize[0]);
        else if (se.first - oe > M) {
          var Et = (se.first - 1) * r.itemSize;
          a ? ve(Et, 0) : ve(0, Et);
        }
      } else if (Be) {
        if (l)
          se.last.rows - oe.rows <= M[0] + 1 ? ve(se.first.cols * r.itemSize[1], (se.first.rows + 1) * r.itemSize[0]) : se.last.cols - oe.cols <= M[1] + 1 && ve((se.first.cols + 1) * r.itemSize[1], se.first.rows * r.itemSize[0]);
        else if (se.last - oe <= M + 1) {
          var dn = (se.first + 1) * r.itemSize;
          a ? ve(dn, 0) : ve(0, dn);
        }
      }
    } else
      Yt(M, Z);
  }, sn = function() {
    return ce ? r.loaderDisabled ? re : [] : on();
  }, un = function() {
    return r.columns && l || a ? ce && r.loaderDisabled ? l ? re[0] : re : r.columns.slice(l ? d.cols : d, l ? p.cols : p) : r.columns;
  }, Le = function() {
    var M = function(Be, Et) {
      return Math.floor(Be / (Et || Be));
    }, $ = d, Z = 0;
    if (Q.current) {
      var ne = Q.current, oe = ne.scrollTop, se = ne.scrollLeft;
      if (l)
        $ = {
          rows: M(oe, r.itemSize[0]),
          cols: M(se, r.itemSize[1])
        }, Z = {
          rows: $.rows + A.rows,
          cols: $.cols + A.cols
        };
      else {
        var ve = a ? se : oe;
        $ = M(ve, r.itemSize), Z = $ + A;
      }
    }
    return {
      first: d,
      last: p,
      viewport: {
        first: $,
        last: Z
      }
    };
  }, U = function() {
    var M = ze(), $ = Q.current ? Q.current.offsetWidth - M.left : 0, Z = Q.current ? Q.current.offsetHeight - M.top : 0, ne = function(Be, Et) {
      return Math.ceil(Be / (Et || Be));
    }, oe = function(Be) {
      return Math.ceil(Be / 2);
    }, se = l ? {
      rows: ne(Z, r.itemSize[0]),
      cols: ne($, r.itemSize[1])
    } : ne(a ? $ : Z, r.itemSize), ve = N || (l ? [oe(se.rows), oe(se.cols)] : oe(se));
    return {
      numItemsInViewport: se,
      numToleratedItems: ve
    };
  }, _e = function() {
    var M = U(), $ = M.numItemsInViewport, Z = M.numToleratedItems, ne = function(ve, Ge, Be) {
      var Et = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
      return $e(ve + Ge + (ve < Be ? 2 : 3) * Be, Et);
    }, oe = l ? {
      rows: ne(d.rows, $.rows, Z[0]),
      cols: ne(d.cols, $.cols, Z[1], !0)
    } : ne(d, $, Z);
    L($), le(Z), b(oe), r.showLoader && B(l ? Array.from({
      length: $.rows
    }).map(function() {
      return Array.from({
        length: $.cols
      });
    }) : Array.from({
      length: $
    })), r.lazy && Promise.resolve().then(function() {
      pt.current = {
        first: r.step ? l ? {
          rows: 0,
          cols: d.cols
        } : 0 : d,
        last: Math.min(r.step ? r.step : oe, (r.items || []).length)
      }, r.onLazyLoad && r.onLazyLoad(pt.current);
    });
  }, Ue = function(M) {
    r.autoSize && !M && Promise.resolve().then(function() {
      if (Y.current) {
        Y.current.style.minHeight = Y.current.style.minWidth = "auto", Y.current.style.position = "relative", Q.current.style.contain = "none";
        var $ = [j.getWidth(Q.current), j.getHeight(Q.current)], Z = $[0], ne = $[1];
        (l || a) && (Q.current.style.width = (Z < W.current ? Z : r.scrollWidth || W.current) + "px"), (l || o) && (Q.current.style.height = (ne < We.current ? ne : r.scrollHeight || We.current) + "px"), Y.current.style.minHeight = Y.current.style.minWidth = "", Y.current.style.position = "", Q.current.style.contain = "";
      }
    });
  }, $e = function() {
    var M, $ = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0, Z = arguments.length > 1 ? arguments[1] : void 0;
    return r.items ? Math.min(Z ? ((M = r.columns || r.items[0]) === null || M === void 0 ? void 0 : M.length) || 0 : (r.items || []).length, $) : 0;
  }, ze = function() {
    if (Y.current) {
      var M = getComputedStyle(Y.current), $ = parseFloat(M.paddingLeft) + Math.max(parseFloat(M.left) || 0, 0), Z = parseFloat(M.paddingRight) + Math.max(parseFloat(M.right) || 0, 0), ne = parseFloat(M.paddingTop) + Math.max(parseFloat(M.top) || 0, 0), oe = parseFloat(M.paddingBottom) + Math.max(parseFloat(M.bottom) || 0, 0);
      return {
        left: $,
        right: Z,
        top: ne,
        bottom: oe,
        x: $ + Z,
        y: ne + oe
      };
    }
    return {
      left: 0,
      right: 0,
      top: 0,
      bottom: 0,
      x: 0,
      y: 0
    };
  }, ft = function() {
    if (Q.current) {
      var M = Q.current.parentElement, $ = r.scrollWidth || "".concat(Q.current.offsetWidth || M.offsetWidth, "px"), Z = r.scrollHeight || "".concat(Q.current.offsetHeight || M.offsetHeight, "px"), ne = function(se, ve) {
        return Q.current.style[se] = ve;
      };
      l || a ? (ne("height", Z), ne("width", $)) : ne("height", Z);
    }
  }, Jt = function() {
    var M = r.items;
    if (M) {
      var $ = ze(), Z = function(oe, se, ve) {
        var Ge = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0;
        return pe.current = So(So({}, pe.current), IE({}, "".concat(oe), (se || []).length * ve + Ge + "px"));
      };
      l ? (Z("height", M, r.itemSize[0], $.y), Z("width", r.columns || M[1], r.itemSize[1], $.x)) : a ? Z("width", r.columns || M, r.itemSize, $.x) : Z("height", M, r.itemSize, $.y);
    }
  }, it = function(M) {
    if (Y.current && !r.appendOnly) {
      var $ = M ? M.first : d, Z = function(ve, Ge) {
        return ve * Ge;
      }, ne = function() {
        var ve = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0, Ge = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
        ye.current && (ye.current.style.top = "-".concat(Ge, "px")), H.current = So(So({}, H.current), {
          transform: "translate3d(".concat(ve, "px, ").concat(Ge, "px, 0)")
        });
      };
      if (l)
        ne(Z($.cols, r.itemSize[1]), Z($.rows, r.itemSize[0]));
      else {
        var oe = Z($, r.itemSize);
        a ? ne(oe, 0) : ne(0, oe);
      }
    }
  }, rt = function(M) {
    var $ = M.target, Z = ze(), ne = function(hn, ri) {
      return hn ? hn > ri ? hn - ri : hn : 0;
    }, oe = function(hn, ri) {
      return Math.floor(hn / (ri || hn));
    }, se = function(hn, ri, Gr, fo, Hi, kr) {
      return hn <= Hi ? Hi : kr ? Gr - fo - Hi : ri + Hi - 1;
    }, ve = function(hn, ri, Gr, fo, Hi, kr, xs) {
      return hn <= kr ? 0 : Math.max(0, xs ? hn < ri ? Gr : hn - kr : hn > ri ? Gr : hn - 2 * kr);
    }, Ge = function(hn, ri, Gr, fo, Hi, kr) {
      var xs = ri + fo + 2 * Hi;
      return hn >= Hi && (xs = xs + (Hi + 1)), $e(xs, kr);
    }, Be = ne($.scrollTop, Z.top), Et = ne($.scrollLeft, Z.left), dn = l ? {
      rows: 0,
      cols: 0
    } : 0, Ut = p, fi = !1, Cr = Ce.current;
    if (l) {
      var Sa = Ce.current.top <= Be, Aa = Ce.current.left <= Et;
      if (!r.appendOnly || r.appendOnly && (Sa || Aa)) {
        var qr = {
          rows: oe(Be, r.itemSize[0]),
          cols: oe(Et, r.itemSize[1])
        }, Pc = {
          rows: se(qr.rows, d.rows, p.rows, A.rows, N[0], Sa),
          cols: se(qr.cols, d.cols, p.cols, A.cols, N[1], Aa)
        };
        dn = {
          rows: ve(qr.rows, Pc.rows, d.rows, p.rows, A.rows, N[0], Sa),
          cols: ve(qr.cols, Pc.cols, d.cols, p.cols, A.cols, N[1], Aa)
        }, Ut = {
          rows: Ge(qr.rows, dn.rows, p.rows, A.rows, N[0]),
          cols: Ge(qr.cols, dn.cols, p.cols, A.cols, N[1], !0)
        }, fi = dn.rows !== d.rows || Ut.rows !== p.rows || dn.cols !== d.cols || Ut.cols !== p.cols || Je.current, Cr = {
          top: Be,
          left: Et
        };
      }
    } else {
      var Pa = a ? Et : Be, Ia = Ce.current <= Pa;
      if (!r.appendOnly || r.appendOnly && Ia) {
        var Ra = oe(Pa, r.itemSize), an = se(Ra, d, p, A, N, Ia);
        dn = ve(Ra, an, d, p, A, N, Ia), Ut = Ge(Ra, dn, p, A, N), fi = dn !== d || Ut !== p || Je.current, Cr = Pa;
      }
    }
    return {
      first: dn,
      last: Ut,
      isRangeChanged: fi,
      scrollPos: Cr
    };
  }, dt = function(M) {
    var $ = rt(M), Z = $.first, ne = $.last, oe = $.isRangeChanged, se = $.scrollPos;
    if (oe) {
      var ve = {
        first: Z,
        last: ne
      };
      if (it(ve), f(Z), b(ne), Ce.current = se, r.onScrollIndexChange && r.onScrollIndexChange(ve), r.lazy && jt(Z)) {
        var Ge = {
          first: r.step ? Math.min(Ht(Z) * r.step, (r.items || []).length - r.step) : Z,
          last: Math.min(r.step ? (Ht(Z) + 1) * r.step : ne, (r.items || []).length)
        }, Be = !pt.current || pt.current.first !== Ge.first || pt.current.last !== Ge.last;
        Be && r.onLazyLoad && r.onLazyLoad(Ge), pt.current = Ge;
      }
    }
  }, Mt = function(M) {
    if (r.onScroll && r.onScroll(M), r.delay) {
      if (J.current && clearTimeout(J.current), jt(d)) {
        if (!ce && r.showLoader) {
          var $ = rt(M), Z = $.isRangeChanged, ne = Z || (r.step ? jt(d) : !1);
          ne && Ae(!0);
        }
        J.current = setTimeout(function() {
          dt(M), ce && r.showLoader && (!r.lazy || r.loading === void 0) && (Ae(!1), S(Ht(d)));
        }, r.delay);
      }
    } else
      dt(M);
  }, Dt = function() {
    F.current && clearTimeout(F.current), F.current = setTimeout(function() {
      if (Q.current) {
        var M = [j.getWidth(Q.current), j.getHeight(Q.current)], $ = M[0], Z = M[1], ne = $ !== W.current, oe = Z !== We.current, se = l ? ne || oe : a ? ne : o ? oe : !1;
        se && (le(r.numToleratedItems), W.current = $, We.current = Z, Ye.current = j.getWidth(Y.current), Ze.current = j.getHeight(Y.current));
      }
    }, r.resizeDelay);
  }, Lt = function(M) {
    var $ = (r.items || []).length, Z = l ? d.rows + M : d + M;
    return {
      index: Z,
      count: $,
      first: Z === 0,
      last: Z === $ - 1,
      even: Z % 2 === 0,
      odd: Z % 2 !== 0,
      props: r
    };
  }, Nt = function(M, $) {
    var Z = re.length || 0;
    return So({
      index: M,
      count: Z,
      first: M === 0,
      last: M === Z - 1,
      even: M % 2 === 0,
      odd: M % 2 !== 0,
      props: r
    }, $);
  }, on = function() {
    var M = r.items;
    return M && !ce ? l ? M.slice(r.appendOnly ? 0 : d.rows, p.rows).map(function($) {
      return r.columns ? $ : $.slice(r.appendOnly ? 0 : d.cols, p.cols);
    }) : a && r.columns ? M : M.slice(r.appendOnly ? 0 : d, p) : [];
  }, Pt = function() {
    Q.current && j.isVisible(Q.current) && (hi(Y.current), vn(), $t(), On(), W.current = j.getWidth(Q.current), We.current = j.getHeight(Q.current), Ye.current = j.getWidth(Y.current), Ze.current = j.getHeight(Y.current));
  }, vn = function() {
    r.disabled || (ft(), _e(), Jt());
  };
  rr(function() {
    Pt();
  }), Zt(function() {
    vn();
  }, [r.itemSize, r.scrollHeight, r.scrollWidth]), Zt(function() {
    r.numToleratedItems !== N && le(r.numToleratedItems);
  }, [r.numToleratedItems]), Zt(function() {
    r.numToleratedItems === N && vn();
  }, [N]), Zt(function() {
    var ue = s.items !== void 0 && s.items !== null, M = r.items !== void 0 && r.items !== null, $ = ue ? s.items.length : 0, Z = M ? r.items.length : 0, ne = $ !== Z;
    if (l && !ne) {
      var oe = ue && s.items.length > 0 ? s.items[0].length : 0, se = M && r.items.length > 0 ? r.items[0].length : 0;
      ne = oe !== se;
    }
    (!ue || ne) && vn();
    var ve = ce;
    r.lazy && s.loading !== r.loading && r.loading !== ce && (Ae(r.loading), ve = r.loading), Ue(ve);
  }), Zt(function() {
    Ce.current = l ? {
      top: 0,
      left: 0
    } : 0;
  }, [r.orientation]), w.useImperativeHandle(t, function() {
    return {
      props: r,
      getElementRef: Vt,
      scrollTo: Wt,
      scrollToIndex: Yt,
      scrollInView: ti,
      getRenderedRange: Le
    };
  });
  var wn = function(M) {
    var $ = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, Z = Nt(M, $), ne = z.getJSXElement(r.loadingTemplate, Z);
    return /* @__PURE__ */ w.createElement(w.Fragment, {
      key: M
    }, ne);
  }, yn = function() {
    var M = "p-virtualscroller-loading-icon", $ = e({
      className: M
    }, he("loadingIcon")), Z = r.loadingIcon || /* @__PURE__ */ w.createElement(ch, cg({}, $, {
      spin: !0
    })), ne = Mi.getJSXIcon(Z, So({}, $), {
      props: r
    });
    if (!r.loaderDisabled && r.showLoader && ce) {
      var oe = Re("p-virtualscroller-loader", {
        "p-component-overlay": !r.loadingTemplate
      }), se = ne;
      if (r.loadingTemplate)
        se = re.map(function(Be, Et) {
          return wn(Et, l && {
            numCols: A.cols
          });
        });
      else if (r.loaderIconTemplate) {
        var ve = {
          iconClassName: M,
          element: se,
          props: r
        };
        se = z.getJSXElement(r.loaderIconTemplate, ve);
      }
      var Ge = e({
        className: oe
      }, he("loader"));
      return /* @__PURE__ */ w.createElement("div", Ge, se);
    }
    return null;
  }, ni = function() {
    if (r.showSpacer) {
      var M = e({
        ref: we,
        style: pe.current,
        className: "p-virtualscroller-spacer"
      }, he("spacer"));
      return /* @__PURE__ */ w.createElement("div", M);
    }
    return null;
  }, zi = function(M, $) {
    var Z = Lt($), ne = z.getJSXElement(r.itemTemplate, M, Z);
    return /* @__PURE__ */ w.createElement(w.Fragment, {
      key: Z.index
    }, ne);
  }, ii = function() {
    var M = on();
    return M.map(zi);
  }, qn = function() {
    var M = ii(), $ = Re("p-virtualscroller-content", {
      "p-virtualscroller-loading": ce
    }), Z = e({
      ref: Y,
      style: H.current,
      className: $
    }, he("content")), ne = /* @__PURE__ */ w.createElement("div", Z, M);
    if (r.contentTemplate) {
      var oe = {
        style: H.current,
        className: $,
        spacerStyle: pe.current,
        contentRef: function(ve) {
          return Y.current = z.getRefElement(ve);
        },
        spacerRef: function(ve) {
          return we.current = z.getRefElement(ve);
        },
        stickyRef: function(ve) {
          return ye.current = z.getRefElement(ve);
        },
        items: on(),
        getItemOptions: function(ve) {
          return Lt(ve);
        },
        children: M,
        element: ne,
        props: r,
        loading: ce,
        getLoaderOptions: function(ve, Ge) {
          return Nt(ve, Ge);
        },
        loadingTemplate: r.loadingTemplate,
        itemSize: r.itemSize,
        rows: sn(),
        columns: un(),
        vertical: o,
        horizontal: a,
        both: l
      };
      return z.getJSXElement(r.contentTemplate, oe);
    }
    return ne;
  };
  if (r.disabled) {
    var Si = z.getJSXElement(r.contentTemplate, {
      items: r.items,
      rows: r.items,
      columns: r.columns
    });
    return /* @__PURE__ */ w.createElement(w.Fragment, null, r.children, Si);
  }
  var C = Re("p-virtualscroller", {
    "p-virtualscroller-inline": r.inline,
    "p-virtualscroller-both p-both-scroll": l,
    "p-virtualscroller-horizontal p-horizontal-scroll": a
  }, r.className), P = yn(), q = qn(), ge = ni(), Oe = e({
    ref: Q,
    className: C,
    tabIndex: r.tabIndex,
    style: r.style,
    onScroll: function(M) {
      return Mt(M);
    }
  }, su.getOtherProps(r), he("root"));
  return /* @__PURE__ */ w.createElement("div", Oe, q, ge, P);
}));
RE.displayName = "VirtualScroller";
function ug() {
  return ug = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, ug.apply(this, arguments);
}
function tc(i) {
  "@babel/helpers - typeof";
  return tc = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, tc(i);
}
function wZ(i, t) {
  if (tc(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (tc(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function yZ(i) {
  var t = wZ(i, "string");
  return tc(t) === "symbol" ? t : String(t);
}
function _Z(i, t, e) {
  return t = yZ(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function CZ(i) {
  if (Array.isArray(i)) return i;
}
function kZ(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function gy(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function xZ(i, t) {
  if (i) {
    if (typeof i == "string") return gy(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return gy(i, t);
  }
}
function EZ() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function TZ(i, t) {
  return CZ(i) || kZ(i, t) || xZ(i, t) || EZ();
}
var SZ = `
@layer primereact {
    .p-ripple {
        overflow: hidden;
        position: relative;
    }
    
    .p-ink {
        display: block;
        position: absolute;
        background: rgba(255, 255, 255, 0.5);
        border-radius: 100%;
        transform: scale(0);
    }
    
    .p-ink-active {
        animation: ripple 0.4s linear;
    }
    
    .p-ripple-disabled .p-ink {
        display: none;
    }
}

@keyframes ripple {
    100% {
        opacity: 0;
        transform: scale(2.5);
    }
}

`, AZ = {
  root: "p-ink"
}, Ho = et.extend({
  defaultProps: {
    __TYPE: "Ripple",
    children: void 0
  },
  css: {
    styles: SZ,
    classes: AZ
  },
  getProps: function(t) {
    return z.getMergedProps(t, Ho.defaultProps);
  },
  getOtherProps: function(t) {
    return z.getDiffProps(t, Ho.defaultProps);
  }
});
function py(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function PZ(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? py(Object(e), !0).forEach(function(n) {
      _Z(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : py(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var nc = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = w.useState(!1), n = TZ(e, 2), r = n[0], s = n[1], o = w.useRef(null), a = w.useRef(null), l = Qn(), c = w.useContext(cn), u = Ho.getProps(i, c), d = c && c.ripple || tn.ripple, f = {
    props: u
  };
  Ls(Ho.css.styles, {
    name: "ripple",
    manual: !d
  });
  var m = Ho.setMetaData(PZ({}, f)), g = m.ptm, p = m.cx, b = function() {
    return o.current && o.current.parentElement;
  }, v = function() {
    a.current && a.current.addEventListener("pointerdown", x);
  }, y = function() {
    a.current && a.current.removeEventListener("pointerdown", x);
  }, x = function(R) {
    var O = j.getOffset(a.current), N = R.pageX - O.left + document.body.scrollTop - j.getWidth(o.current) / 2, le = R.pageY - O.top + document.body.scrollLeft - j.getHeight(o.current) / 2;
    S(N, le);
  }, S = function(R, O) {
    !o.current || getComputedStyle(o.current, null).display === "none" || (j.removeClass(o.current, "p-ink-active"), I(), o.current.style.top = O + "px", o.current.style.left = R + "px", j.addClass(o.current, "p-ink-active"));
  }, k = function(R) {
    j.removeClass(R.currentTarget, "p-ink-active");
  }, I = function() {
    if (o.current && !j.getHeight(o.current) && !j.getWidth(o.current)) {
      var R = Math.max(j.getOuterWidth(a.current), j.getOuterHeight(a.current));
      o.current.style.height = R + "px", o.current.style.width = R + "px";
    }
  };
  if (w.useImperativeHandle(t, function() {
    return {
      props: u,
      getInk: function() {
        return o.current;
      },
      getTarget: function() {
        return a.current;
      }
    };
  }), rr(function() {
    s(!0);
  }), Zt(function() {
    r && o.current && (a.current = b(), I(), v());
  }, [r]), Zt(function() {
    o.current && !a.current && (a.current = b(), I(), v());
  }), wr(function() {
    o.current && (a.current = null, y());
  }), !d)
    return null;
  var A = l({
    "aria-hidden": !0,
    className: Re(p("root"))
  }, Ho.getOtherProps(u), g("root"));
  return /* @__PURE__ */ w.createElement("span", ug({
    role: "presentation",
    ref: o
  }, A, {
    onAnimationEnd: k
  }));
}));
nc.displayName = "Ripple";
function dg() {
  return dg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, dg.apply(this, arguments);
}
var Xp = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", dg({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    d: "M4.86199 11.5948C4.78717 11.5923 4.71366 11.5745 4.64596 11.5426C4.57826 11.5107 4.51779 11.4652 4.46827 11.4091L0.753985 7.69483C0.683167 7.64891 0.623706 7.58751 0.580092 7.51525C0.536478 7.44299 0.509851 7.36177 0.502221 7.27771C0.49459 7.19366 0.506156 7.10897 0.536046 7.03004C0.565935 6.95111 0.613367 6.88 0.674759 6.82208C0.736151 6.76416 0.8099 6.72095 0.890436 6.69571C0.970973 6.67046 1.05619 6.66385 1.13966 6.67635C1.22313 6.68886 1.30266 6.72017 1.37226 6.76792C1.44186 6.81567 1.4997 6.8786 1.54141 6.95197L4.86199 10.2503L12.6397 2.49483C12.7444 2.42694 12.8689 2.39617 12.9932 2.40745C13.1174 2.41873 13.2343 2.47141 13.3251 2.55705C13.4159 2.64268 13.4753 2.75632 13.4938 2.87973C13.5123 3.00315 13.4888 3.1292 13.4271 3.23768L5.2557 11.4091C5.20618 11.4652 5.14571 11.5107 5.07801 11.5426C5.01031 11.5745 4.9368 11.5923 4.86199 11.5948Z",
    fill: "currentColor"
  }));
}));
Xp.displayName = "CheckIcon";
function Mr() {
  return Mr = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, Mr.apply(this, arguments);
}
function ca(i) {
  "@babel/helpers - typeof";
  return ca = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, ca(i);
}
function IZ(i, t) {
  if (ca(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (ca(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function RZ(i) {
  var t = IZ(i, "string");
  return ca(t) === "symbol" ? t : String(t);
}
function dh(i, t, e) {
  return t = RZ(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function OZ(i) {
  if (Array.isArray(i)) return i;
}
function VZ(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function by(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function MZ(i, t) {
  if (i) {
    if (typeof i == "string") return by(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return by(i, t);
  }
}
function DZ() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function Ya(i, t) {
  return OZ(i) || VZ(i, t) || MZ(i, t) || DZ();
}
function vy(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function LZ(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? vy(Object(e), !0).forEach(function(n) {
      dh(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : vy(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var NZ = {
  root: function(t) {
    var e = t.props, n = t.focusedState, r = t.overlayVisibleState, s = t.context;
    return Re("p-dropdown p-component p-inputwrapper", {
      "p-disabled": e.disabled,
      "p-invalid": e.invalid,
      "p-focus": n,
      "p-variant-filled": e.variant ? e.variant === "filled" : s && s.inputStyle === "filled",
      "p-dropdown-clearable": e.showClear && !e.disabled,
      "p-inputwrapper-filled": z.isNotEmpty(e.value),
      "p-inputwrapper-focus": n || r
    });
  },
  input: function(t) {
    var e = t.props, n = t.label;
    return e.editable ? "p-dropdown-label p-inputtext" : Re("p-dropdown-label p-inputtext", {
      "p-placeholder": n === null && e.placeholder,
      "p-dropdown-label-empty": n === null && !e.placeholder
    });
  },
  trigger: "p-dropdown-trigger",
  emptyMessage: "p-dropdown-empty-message",
  itemGroup: function(t) {
    var e = t.optionGroupLabel;
    return Re("p-dropdown-item-group", {
      "p-dropdown-item-empty": !e || e.length === 0
    });
  },
  itemGroupLabel: "p-dropdown-item-group-label",
  dropdownIcon: "p-dropdown-trigger-icon p-clickable",
  loadingIcon: "p-dropdown-trigger-icon p-clickable",
  clearIcon: "p-dropdown-clear-icon p-clickable",
  filterIcon: "p-dropdown-filter-icon",
  filterClearIcon: "p-dropdown-filter-clear-icon",
  filterContainer: function(t) {
    var e = t.clearIcon;
    return Re("p-dropdown-filter-container", {
      "p-dropdown-clearable-filter": !!e
    });
  },
  filterInput: function(t) {
    var e = t.props, n = t.context;
    return Re("p-dropdown-filter p-inputtext p-component", {
      "p-variant-filled": e.variant ? e.variant === "filled" : n && n.inputStyle === "filled"
    });
  },
  list: function(t) {
    var e = t.virtualScrollerOptions;
    return "p-dropdown-items";
  },
  panel: function(t) {
    var e = t.context;
    return Re("p-dropdown-panel p-component", {
      "p-input-filled": e && e.inputStyle === "filled" || tn.inputStyle === "filled",
      "p-ripple-disabled": e && e.ripple === !1 || tn.ripple === !1
    });
  },
  item: function(t) {
    var e = t.selected, n = t.disabled, r = t.label, s = t.index, o = t.focusedOptionIndex, a = t.highlightOnSelect;
    return Re("p-dropdown-item", {
      "p-highlight": e && a,
      "p-disabled": n,
      "p-focus": s === o,
      "p-dropdown-item-empty": !r || r.length === 0
    });
  },
  itemLabel: "p-dropdown-item-label",
  checkIcon: "p-dropdown-check-icon",
  blankIcon: "p-dropdown-blank-icon",
  wrapper: "p-dropdown-items-wrapper",
  header: "p-dropdown-header",
  footer: "p-dropdown-footer",
  transition: "p-connected-overlay"
}, BZ = `
@layer primereact {
    .p-dropdown {
        display: inline-flex;
        cursor: pointer;
        position: relative;
        user-select: none;
    }
    
    .p-dropdown-trigger {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }
    
    .p-dropdown-label {
        display: block;
        white-space: nowrap;
        overflow: hidden;
        flex: 1 1 auto;
        width: 1%;
        text-overflow: ellipsis;
        cursor: pointer;
    }
    
    .p-dropdown-label-empty {
        overflow: hidden;
        visibility: hidden;
    }
    
    input.p-dropdown-label  {
        cursor: default;
    }
    
    .p-dropdown .p-dropdown-panel {
        min-width: 100%;
    }
    
    .p-dropdown-panel {
        position: absolute;
        top: 0;
        left: 0;
    }
    
    .p-dropdown-items-wrapper {
        overflow: auto;
    }
    
    .p-dropdown-item {
        cursor: pointer;
        font-weight: normal;
        white-space: nowrap;
        position: relative;
        overflow: hidden;
    }
    
    .p-dropdown-items {
        margin: 0;
        padding: 0;
        list-style-type: none;
    }
    
    .p-dropdown-filter {
        width: 100%;
    }
    
    .p-dropdown-filter-container {
        position: relative;
    }
    
    .p-dropdown-clear-icon,
    .p-dropdown-filter-icon,
    .p-dropdown-filter-clear-icon {
        position: absolute;
        top: 50%;
        margin-top: -.5rem;
        right: 2rem;
    }
    
    .p-fluid .p-dropdown {
        display: flex;
    }
    
    .p-fluid .p-dropdown .p-dropdown-label {
        width: 1%;
    }
}
`, FZ = {
  wrapper: function(t) {
    var e = t.props;
    return {
      maxHeight: e.scrollHeight || "auto"
    };
  },
  panel: function(t) {
    var e = t.props;
    return LZ({}, e.panelStyle);
  }
}, ou = et.extend({
  defaultProps: {
    __TYPE: "Dropdown",
    __parentMetadata: null,
    appendTo: null,
    ariaLabel: null,
    ariaLabelledBy: null,
    autoFocus: !1,
    children: void 0,
    className: null,
    clearIcon: null,
    dataKey: null,
    disabled: !1,
    dropdownIcon: null,
    collapseIcon: null,
    editable: !1,
    emptyFilterMessage: null,
    highlightOnSelect: !0,
    checkmark: !1,
    emptyMessage: null,
    filter: !1,
    filterBy: null,
    filterClearIcon: null,
    filterIcon: null,
    filterInputAutoFocus: !1,
    filterLocale: void 0,
    filterMatchMode: "contains",
    filterPlaceholder: null,
    filterTemplate: null,
    focusInputRef: null,
    id: null,
    inputId: null,
    inputRef: null,
    invalid: !1,
    variant: null,
    itemTemplate: null,
    loading: !1,
    loadingIcon: null,
    maxLength: null,
    name: null,
    onBlur: null,
    onChange: null,
    onContextMenu: null,
    onFilter: null,
    onFocus: null,
    onHide: null,
    onMouseDown: null,
    onShow: null,
    optionDisabled: null,
    optionGroupChildren: "items",
    selectOnFocus: !1,
    focusOnHover: !0,
    autoOptionFocus: !1,
    optionGroupLabel: null,
    optionGroupTemplate: null,
    optionLabel: null,
    optionValue: null,
    options: null,
    panelClassName: null,
    panelFooterTemplate: null,
    panelStyle: null,
    placeholder: null,
    required: !1,
    resetFilterOnHide: !1,
    scrollHeight: "200px",
    showClear: !1,
    showFilterClear: !1,
    showOnFocus: !1,
    style: null,
    tabIndex: null,
    tooltip: null,
    tooltipOptions: null,
    transitionOptions: null,
    value: null,
    valueTemplate: null,
    virtualScrollerOptions: null
  },
  css: {
    classes: NZ,
    styles: BZ,
    inlineStyles: FZ
  }
}), OE = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", Mr({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("rect", {
    width: "1",
    height: "1",
    fill: "currentColor",
    fillOpacity: "0"
  }));
}));
OE.displayName = "BlankIcon";
var VE = /* @__PURE__ */ w.memo(function(i) {
  var t = Qn(), e = i.ptm, n = i.cx, r = i.selected, s = i.disabled, o = i.option, a = i.label, l = i.index, c = i.focusedOptionIndex, u = i.ariaSetSize, d = i.checkmark, f = i.highlightOnSelect, m = i.onInputKeyDown, g = function(k) {
    return e(k, {
      context: {
        selected: r,
        disabled: s,
        focused: l === c
      }
    });
  }, p = function(k, I) {
    i.onClick && i.onClick({
      originalEvent: k,
      option: o
    });
  }, b = i.template ? z.getJSXElement(i.template, i.option) : i.label, v = t({
    id: "dropdownItem_".concat(l),
    role: "option",
    key: i.label,
    className: Re(o.className, n("item", {
      selected: r,
      disabled: s,
      label: a,
      index: l,
      focusedOptionIndex: c,
      highlightOnSelect: f
    })),
    style: i.style,
    tabIndex: 0,
    onClick: function(k) {
      return p(k);
    },
    onKeyDown: function(k) {
      return m(k);
    },
    onMouseMove: function(k) {
      return i == null ? void 0 : i.onMouseMove(k, l);
    },
    "aria-setsize": u,
    "aria-posinset": l + 1,
    "aria-label": a,
    "aria-selected": r,
    "data-p-highlight": r,
    "data-p-focused": c === l,
    "data-p-disabled": s
  }, g("item")), y = t({
    className: n("itemLabel")
  }, g("itemLabel")), x = function() {
    if (r) {
      var k = t({
        className: n("checkIcon")
      }, g("checkIcon"));
      return /* @__PURE__ */ w.createElement(Xp, k);
    }
    var I = t({
      className: n("blankIcon")
    }, g("blankIcon"));
    return /* @__PURE__ */ w.createElement(OE, I);
  };
  return /* @__PURE__ */ w.createElement("li", v, d && x(), /* @__PURE__ */ w.createElement("span", y, b), /* @__PURE__ */ w.createElement(nc, null));
});
VE.displayName = "DropdownItem";
function wy(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function ji(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? wy(Object(e), !0).forEach(function(n) {
      dh(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : wy(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var ME = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = i.ptm, r = i.cx, s = i.sx, o = w.useContext(cn), a = w.useRef(null), l = !(i.visibleOptions && i.visibleOptions.length) && i.hasFilter, c = i.visibleOptions ? i.visibleOptions.length : 0, u = {
    filter: function(O) {
      return g(O);
    },
    reset: function() {
      return i.resetFilter();
    }
  }, d = function(O, N) {
    return n(O, ji({
      hostName: i.hostName
    }, N));
  }, f = function() {
    i.onEnter(function() {
      if (i.virtualScrollerRef.current) {
        var O = i.getSelectedOptionIndex();
        O !== -1 && setTimeout(function() {
          return i.virtualScrollerRef.current.scrollToIndex(O);
        }, 0);
      }
    });
  }, m = function() {
    i.onEntered(function() {
      i.filter && i.filterInputAutoFocus && j.focus(a.current, !1);
    });
  }, g = function(O) {
    i.onFilterInputChange && i.onFilterInputChange(O);
  }, p = function() {
    if (i.panelFooterTemplate) {
      var O = z.getJSXElement(i.panelFooterTemplate, i, i.onOverlayHide), N = e({
        className: r("footer")
      }, d("footer"));
      return /* @__PURE__ */ w.createElement("div", N, O);
    }
    return null;
  }, b = function(O, N) {
    if (i.focusOnHover) {
      var le;
      i == null || (le = i.changeFocusedOptionIndex) === null || le === void 0 || le.call(i, O, N);
    }
  }, v = function(O, N) {
    var le = z.getJSXElement(O, i) || la(N ? "emptyFilterMessage" : "emptyMessage"), te = e({
      className: r("emptyMessage")
    }, d("emptyMessage"));
    return /* @__PURE__ */ w.createElement("li", te, le);
  }, y = function(O, N) {
    var le = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, te = {
      height: le.props ? le.props.itemSize : void 0
    };
    if (te = ji(ji({}, te), O.style), O.group && O.optionGroup && i.optionGroupLabel) {
      var me = i.optionGroupLabel, ce = i.optionGroupTemplate ? z.getJSXElement(i.optionGroupTemplate, O, N) : i.getOptionGroupLabel(O), Ae = N + "_" + i.getOptionGroupRenderKey(O), Fe = e({
        className: r("itemGroup", {
          optionGroupLabel: me
        }),
        style: te,
        "data-p-highlight": i.selected
      }, d("itemGroup")), be = e({
        className: r("itemGroupLabel")
      }, d("itemGroupLabel"));
      return /* @__PURE__ */ w.createElement("li", Mr({
        key: Ae
      }, Fe), /* @__PURE__ */ w.createElement("span", be, ce));
    }
    var re = i.getOptionRenderKey(O) + "_" + N, B = i.getOptionLabel(O), X = i.isOptionDisabled(O);
    return /* @__PURE__ */ w.createElement(VE, {
      key: re,
      label: B,
      index: N,
      focusedOptionIndex: i.focusedOptionIndex,
      option: O,
      ariaSetSize: c,
      onInputKeyDown: i.onInputKeyDown,
      style: te,
      template: i.itemTemplate,
      selected: i.isSelected(O),
      highlightOnSelect: i.highlightOnSelect,
      disabled: X,
      onClick: i.onOptionClick,
      onMouseMove: b,
      ptm: n,
      cx: r,
      checkmark: i.checkmark
    });
  }, x = function() {
    return z.isNotEmpty(i.visibleOptions) ? i.visibleOptions.map(y) : i.hasFilter ? v(i.emptyFilterMessage, !0) : v(i.emptyMessage);
  }, S = function() {
    if (i.showFilterClear && i.filterValue) {
      var O = la("clear"), N = e({
        className: r("filterClearIcon"),
        "aria-label": O,
        onClick: function() {
          return i.onFilterClearIconClick(function() {
            return j.focus(a.current);
          });
        }
      }, d("filterClearIcon")), le = i.filterClearIcon || /* @__PURE__ */ w.createElement(uh, N), te = Mi.getJSXIcon(le, ji({}, N), {
        props: i
      });
      return te;
    }
    return null;
  }, k = function() {
    if (i.filter) {
      var O = S(), N = e({
        className: r("filterIcon")
      }, d("filterIcon")), le = i.filterIcon || /* @__PURE__ */ w.createElement(PE, N), te = Mi.getJSXIcon(le, ji({}, N), {
        props: i
      }), me = e({
        className: r("filterContainer", {
          clearIcon: O
        })
      }, d("filterContainer")), ce = e({
        ref: a,
        type: "text",
        autoComplete: "off",
        className: r("filterInput", {
          context: o
        }),
        placeholder: i.filterPlaceholder,
        onKeyDown: i.onFilterInputKeyDown,
        onChange: function(B) {
          return g(B);
        },
        value: i.filterValue
      }, d("filterInput")), Ae = /* @__PURE__ */ w.createElement("div", me, /* @__PURE__ */ w.createElement("input", ce), O, te);
      if (i.filterTemplate) {
        var Fe = {
          className: Re("p-dropdown-filter-container", {
            "p-dropdown-clearable-filter": !!O
          }),
          element: Ae,
          filterOptions: u,
          filterInputKeyDown: i.onFilterInputKeyDown,
          filterInputChange: g,
          filterIconClassName: "p-dropdown-filter-icon",
          clearIcon: O,
          props: i
        };
        Ae = z.getJSXElement(i.filterTemplate, Fe);
      }
      var be = e({
        className: r("header")
      }, d("header"));
      return /* @__PURE__ */ w.createElement("div", be, Ae);
    }
    return null;
  }, I = function() {
    if (i.virtualScrollerOptions) {
      var O = ji(ji({}, i.virtualScrollerOptions), {
        style: ji(ji({}, i.virtualScrollerOptions.style), {
          height: i.scrollHeight
        }),
        className: Re("p-dropdown-items-wrapper", i.virtualScrollerOptions.className),
        items: i.visibleOptions,
        autoSize: !0,
        onLazyLoad: function(ce) {
          return i.virtualScrollerOptions.onLazyLoad(ji(ji({}, ce), {
            filter: i.filterValue
          }));
        },
        itemTemplate: function(ce, Ae) {
          return ce && y(ce, Ae.index, Ae);
        },
        contentTemplate: function(ce) {
          var Ae = i.hasFilter ? i.emptyFilterMessage : i.emptyMessage, Fe = l ? v(Ae) : ce.children, be = e({
            ref: ce.contentRef,
            style: ce.style,
            className: Re(ce.className, r("list", {
              virtualScrollerProps: i.virtualScrollerOptions
            })),
            role: "listbox"
          }, d("list"));
          return /* @__PURE__ */ w.createElement("ul", be, Fe);
        }
      });
      return /* @__PURE__ */ w.createElement(RE, Mr({
        ref: i.virtualScrollerRef
      }, O, {
        pt: n("virtualScroller")
      }));
    }
    var N = x(), le = e({
      className: r("wrapper"),
      style: s("wrapper")
    }, d("wrapper")), te = e({
      className: r("list"),
      role: "listbox"
    }, d("list"));
    return /* @__PURE__ */ w.createElement("div", le, /* @__PURE__ */ w.createElement("ul", te, N));
  }, A = function() {
    var O = k(), N = I(), le = p(), te = e({
      className: Re(i.panelClassName, r("panel", {
        context: o
      })),
      style: s("panel"),
      onClick: i.onClick
    }, d("panel")), me = e({
      classNames: r("transition"),
      in: i.in,
      timeout: {
        enter: 120,
        exit: 100
      },
      options: i.transitionOptions,
      unmountOnExit: !0,
      onEnter: f,
      onEntered: m,
      onExit: i.onExit,
      onExited: i.onExited
    }, d("transition"));
    return /* @__PURE__ */ w.createElement(lh, Mr({
      nodeRef: t
    }, me), /* @__PURE__ */ w.createElement("div", Mr({
      ref: t
    }, te), i.firstFocusableElement, O, N, le, i.lastFocusableElement));
  }, L = A();
  return /* @__PURE__ */ w.createElement(xc, {
    element: L,
    appendTo: i.appendTo
  });
}));
ME.displayName = "DropdownPanel";
function $Z(i, t) {
  var e = typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (!e) {
    if (Array.isArray(i) || (e = zZ(i)) || t) {
      e && (i = e);
      var n = 0, r = function() {
      };
      return { s: r, n: function() {
        return n >= i.length ? { done: !0 } : { done: !1, value: i[n++] };
      }, e: function(c) {
        throw c;
      }, f: r };
    }
    throw new TypeError(`Invalid attempt to iterate non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
  }
  var s = !0, o = !1, a;
  return { s: function() {
    e = e.call(i);
  }, n: function() {
    var c = e.next();
    return s = c.done, c;
  }, e: function(c) {
    o = !0, a = c;
  }, f: function() {
    try {
      !s && e.return != null && e.return();
    } finally {
      if (o) throw a;
    }
  } };
}
function zZ(i, t) {
  if (i) {
    if (typeof i == "string") return yy(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return yy(i, t);
  }
}
function yy(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function _y(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function Sr(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? _y(Object(e), !0).forEach(function(n) {
      dh(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : _y(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var Qp = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = ou.getProps(i, n), s = w.useState(""), o = Ya(s, 2), a = o[0], l = o[1], c = w.useState(!1), u = Ya(c, 2), d = u[0], f = u[1], m = w.useState(-1), g = Ya(m, 2), p = g[0], b = g[1], v = w.useState(!1), y = Ya(v, 2), x = y[0], S = y[1], k = w.useRef(!1), I = w.useRef(null), A = w.useRef(null), L = w.useRef(null), R = w.useRef(null), O = w.useRef(r.inputRef), N = w.useRef(r.focusInputRef), le = w.useRef(null), te = w.useRef(null), me = w.useRef(null), ce = r.virtualScrollerOptions && r.virtualScrollerOptions.lazy, Ae = z.isNotEmpty(a), Fe = r.appendTo || n && n.appendTo || tn.appendTo, be = ou.setMetaData(Sr(Sr({
    props: r
  }, r.__parentMetadata), {}, {
    state: {
      filter: a,
      focused: d,
      overlayVisible: x
    }
  })), re = be.ptm, B = be.cx, X = be.sx, he = be.isUnstyled;
  sr(ou.css.styles, he, {
    name: "dropdown"
  });
  var Q = zG({
    target: I,
    overlay: A,
    listener: function(E, K) {
      var Pe = K.type, Ct = K.valid;
      Ct && (Pe === "outside" ? !pe(E) && P() : P());
    },
    when: x
  }), Y = Ya(Q, 2), we = Y[0], ye = Y[1], Ce = function(E) {
    return (E || []).reduce(function(K, Pe, Ct) {
      K.push({
        optionGroup: Pe,
        group: !0,
        index: Ct,
        code: Pe.code,
        label: Pe.label
      });
      var Xt = Ut(Pe);
      return Xt && Xt.forEach(function(mo) {
        return K.push(mo);
      }), K;
    }, []);
  }, J = function() {
    var E = r.optionGroupLabel ? Ce(r.options) : r.options;
    if (Ae && !ce) {
      var K = a.trim().toLocaleLowerCase(r.filterLocale), Pe = r.filterBy ? r.filterBy.split(",") : [r.optionLabel || "label"];
      if (r.optionGroupLabel) {
        var Ct = [], Xt = $Z(r.options), mo;
        try {
          for (Xt.s(); !(mo = Xt.n()).done; ) {
            var ub = mo.value, xh = Y0.filter(Ut(ub), Pe, K, r.filterMatchMode, r.filterLocale);
            xh && xh.length && Ct.push(Sr(Sr({}, ub), dh({}, "".concat(r.optionGroupChildren), xh)));
          }
        } catch (k5) {
          Xt.e(k5);
        } finally {
          Xt.f();
        }
        return Ce(Ct);
      }
      return Y0.filter(E, Pe, K, r.filterMatchMode, r.filterLocale);
    }
    return E;
  }, F = function(E) {
    var K = E.relatedTarget === N.current ? j.getFirstFocusableElement(A.current, ':not([data-p-hidden-focusable="true"])') : N.current;
    j.focus(K);
  }, H = function(E) {
    var K = E.relatedTarget === N.current ? j.getLastFocusableElement(A.current, ':not([data-p-hidden-focusable="true"])') : N.current;
    j.focus(K);
  }, pe = function(E) {
    return j.isAttributeEquals(E.target, "data-pc-section", "clearicon") || j.isAttributeEquals(E.target.parentElement || E.target, "data-pc-section", "filterclearicon");
  }, W = function(E) {
    r.disabled || r.loading || (r.onClick && r.onClick(E), !E.defaultPrevented && (pe(E) || E.target.tagName === "INPUT" || ((!A.current || !(A.current && A.current.contains(E.target))) && (j.focus(N.current), x ? P() : C()), k.current = !0)));
  }, We = function(E) {
    r.showOnFocus && !x && C(), f(!0), r.onFocus && r.onFocus(E);
  }, Ye = function(E) {
    f(!1), r.onBlur && setTimeout(function() {
      var K = O.current ? O.current.value : void 0;
      r.onBlur({
        originalEvent: E.originalEvent,
        value: K,
        stopPropagation: function() {
          E.originalEvent.stopPropagation();
        },
        preventDefault: function() {
          E.originalEvent.preventDefault();
        },
        target: {
          name: r.name,
          id: r.id,
          value: K
        }
      });
    }, 200);
  }, Ze = function(E, K) {
    var Pe = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0, Ct = se(K);
    ni({
      originalEvent: E,
      option: Ct
    }), Pe && P();
  }, Je = function(E) {
    Hs.emit("overlay-click", {
      originalEvent: E,
      target: I.current
    });
  }, pt = function(E) {
    if (r.disabled) {
      E.preventDefault();
      return;
    }
    var K = E.metaKey || E.ctrlKey, Pe = j.isAndroid() ? E.key : E.code;
    switch (Pe) {
      case "ArrowDown":
        U(E);
        break;
      case "ArrowUp":
        _e(E);
        break;
      case "ArrowLeft":
      case "ArrowRight":
        Ue(E, r.editable);
        break;
      case "Home":
        $e(E);
        break;
      case "End":
        ze(E);
        break;
      case "PageDown":
        Jt(E);
        break;
      case "PageUp":
        ft(E);
        break;
      case "Space":
        it(E, r.editable);
        break;
      case "NumpadEnter":
      case "Enter":
        rt(E);
        break;
      case "Escape":
        dt(E);
        break;
      case "Tab":
        Mt(E);
        break;
      case "Backspace":
        Dt(E, r.editable);
        break;
      case "ShiftLeft":
      case "ShiftRight":
        break;
      default:
        !K && z.isPrintableCharacter(E.key) && (!x && !r.editable && C(), !r.editable && hi(E, E.key));
        break;
    }
    k.current = !1;
  }, Rn = function(E) {
    switch (E.code) {
      case "ArrowDown":
        U(E);
        break;
      case "ArrowUp":
        _e(E);
        break;
      case "ArrowLeft":
      case "ArrowRight":
        Ue(E, !0);
        break;
      case "Escape":
      case "Enter":
      case "NumpadEnter":
        rt(E), E.preventDefault();
        break;
    }
  }, rn = function() {
    return j.getFocusableElements(A.current, ':not([data-p-hidden-focusable="true"])').length > 0;
  }, $t = function(E) {
    var K;
    return zt(E) && ((K = oe(E)) === null || K === void 0 ? void 0 : K.toLocaleLowerCase(r.filterLocale).startsWith(me.current.toLocaleLowerCase(r.filterLocale)));
  }, zt = function(E) {
    return z.isNotEmpty(E) && !(Be(E) || Ge(E));
  }, bn = function() {
    return z.isNotEmpty(r.value);
  }, On = function(E) {
    return zt(E) && Si(E);
  }, Vt = function() {
    return bn ? an.findIndex(function(E) {
      return On(E);
    }) : -1;
  }, Ht = function() {
    var E = Vt();
    return E < 0 ? Wt() : E;
  }, hi = function(E, K) {
    me.current = (me.current || "") + K;
    var Pe = -1, Ct = !1;
    return z.isNotEmpty(me.current) && (p !== -1 ? (Pe = an.slice(p).findIndex(function(Xt) {
      return $t(Xt);
    }), Pe = Pe === -1 ? an.slice(0, p).findIndex(function(Xt) {
      return $t(Xt);
    }) : Pe + p) : Pe = an.findIndex(function(Xt) {
      return $t(Xt);
    }), Pe !== -1 && (Ct = !0), Pe === -1 && p === -1 && (Pe = Ht()), Pe !== -1 && un(E, Pe)), te.current && clearTimeout(te.current), te.current = setTimeout(function() {
      me.current = "", te.current = null;
    }, 500), Ct;
  }, jt = function() {
    var E = Vt();
    return E < 0 ? Yt() : E;
  }, Wt = function() {
    return an.findIndex(function(E) {
      return zt(E);
    });
  }, Yt = function() {
    return z.findLastIndex(an, function(E) {
      return zt(E);
    });
  }, ti = function(E) {
    var K = E < an.length - 1 ? an.slice(E + 1).findIndex(function(Pe) {
      return zt(Pe);
    }) : -1;
    return K > -1 ? K + E + 1 : E;
  }, sn = function(E) {
    var K = E > 0 ? z.findLastIndex(an.slice(0, E), function(Pe) {
      return zt(Pe);
    }) : -1;
    return K > -1 ? K : E;
  }, un = function(E, K) {
    p !== K && (b(K), Le(K), r.selectOnFocus && Ze(E, an[K], !1));
  }, Le = function(E) {
    var K = j.findSingle(A.current, 'li[id="dropdownItem_'.concat(E, '"]'));
    K && K.focus();
  }, U = function(E) {
    if (!x)
      C(), r.editable && un(E, Vt());
    else {
      var K = p !== -1 ? ti(p) : k.current ? Wt() : Ht();
      un(E, K);
    }
    E.preventDefault();
  }, _e = function(E) {
    var K = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    if (E.altKey && !K)
      p !== -1 && Ze(E, an[p]), state.overlayVisible && P(), E.preventDefault();
    else {
      var Pe = p !== -1 ? sn(p) : k.current ? Yt() : jt();
      un(E, Pe), !x && C(), E.preventDefault();
    }
  }, Ue = function(E) {
    var K = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    K && b(-1);
  }, $e = function(E) {
    var K = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    K ? (E.currentTarget.setSelectionRange(0, 0), b(-1)) : (un(E, Wt()), !x && C()), E.preventDefault();
  }, ze = function(E) {
    var K = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    if (K) {
      var Pe = E.currentTarget, Ct = Pe.value.length;
      Pe.setSelectionRange(Ct, Ct), b(-1);
    } else
      un(E, Yt()), !x && C();
    E.preventDefault();
  }, ft = function(E) {
    E.preventDefault();
  }, Jt = function(E) {
    E.preventDefault();
  }, it = function(E) {
    var K = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    !K && rt(E);
  }, rt = function(E) {
    x ? (p !== -1 && Ze(E, an[p]), P()) : (b(-1), U(E)), E.preventDefault();
  }, dt = function(E) {
    x && P(), E.preventDefault();
  }, Mt = function(E) {
    var K = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    K || (x && !rn() ? (j.focus(L.current), E.preventDefault()) : (p !== -1 && Ze(E, an[p]), x && P()));
  }, Dt = function(E) {
    var K = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
    E && K && !x && C();
  }, Lt = function(E) {
    !x && C();
    var K = null;
    E.target.value && an && (K = an.findIndex(function(Pe) {
      return oe(Pe).toLocaleLowerCase().startsWith(E.target.value.toLocaleLowerCase());
    })), b(K), r.onChange && r.onChange({
      originalEvent: E.originalEvent,
      value: E.target.value,
      stopPropagation: function() {
        E.originalEvent.stopPropagation();
      },
      preventDefault: function() {
        E.originalEvent.preventDefault();
      },
      target: {
        name: r.name,
        id: r.id,
        value: E.target.value
      }
    });
  }, Nt = function(E) {
    f(!0), P(), r.onFocus && r.onFocus(E);
  }, on = function(E) {
    var K = E.option;
    K.disabled || (ni(E), j.focus(N.current)), P();
  }, Pt = function(E) {
    var K = E.target.value;
    l(K), r.onFilter && r.onFilter({
      originalEvent: E,
      filter: K
    });
  }, vn = function(E) {
    wn(E);
  }, wn = function(E) {
    l(""), r.onFilter && r.onFilter({
      filter: ""
    }), E && E();
  }, yn = function(E) {
    r.onChange && r.onChange({
      originalEvent: E,
      value: void 0,
      stopPropagation: function() {
        E == null || E.stopPropagation();
      },
      preventDefault: function() {
        E == null || E.preventDefault();
      },
      target: {
        name: r.name,
        id: r.id,
        value: void 0
      }
    }), r.filter && wn(), ne();
  }, ni = function(E) {
    if (Dn !== E.option) {
      ne(E.option), b(-1);
      var K = se(E.option), Pe = qn(E.option, an);
      r.onChange && r.onChange({
        originalEvent: E.originalEvent,
        value: K,
        stopPropagation: function() {
          E.originalEvent.stopPropagation();
        },
        preventDefault: function() {
          E.originalEvent.preventDefault();
        },
        target: {
          name: r.name,
          id: r.id,
          value: K
        }
      }), un(E.originalEvent, Pe);
    }
  }, zi = function(E) {
    if (E = E || an, r.value != null && E)
      if (r.optionGroupLabel)
        for (var K = 0; K < E.length; K++) {
          var Pe = qn(r.value, Ut(E[K]));
          if (Pe !== -1)
            return {
              group: K,
              option: Pe
            };
        }
      else
        return qn(r.value, E);
    return -1;
  }, ii = function() {
    return r.optionValue ? null : r.dataKey;
  }, qn = function(E, K) {
    var Pe = ii();
    return K.findIndex(function(Ct) {
      return z.equals(E, se(Ct), Pe);
    });
  }, Si = function(E) {
    return z.equals(r.value, se(E), ii());
  }, C = function() {
    b(p !== -1 ? p : r.autoOptionFocus ? Ht() : r.editable ? -1 : Vt()), S(!0);
  }, P = function() {
    S(!1), k.current = !1;
  }, q = function() {
    r.editable && !x && k.current === !1 && j.focus(O.current);
  }, ge = function(E) {
    Ki.set("overlay", A.current, n && n.autoZIndex || tn.autoZIndex, n && n.zIndex.overlay || tn.zIndex.overlay), j.addStyles(A.current, {
      position: "absolute",
      top: "0",
      left: "0"
    }), $(), E && E();
  }, Oe = function(E) {
    E && E(), we(), r.onShow && r.onShow();
  }, ue = function() {
    ye();
  }, M = function() {
    r.filter && r.resetFilterOnHide && wn(), Ki.clear(A.current), r.onHide && r.onHide();
  }, $ = function() {
    j.alignOverlay(A.current, O.current.parentElement, r.appendTo || n && n.appendTo || tn.appendTo);
  }, Z = function() {
    var E = j.findSingle(A.current, 'li[data-p-focused="true"]');
    if (E && E.scrollIntoView)
      E.scrollIntoView({
        block: "nearest",
        inline: "nearest"
      });
    else {
      var K = j.findSingle(A.current, 'li[data-p-highlight="true"]');
      K && K.scrollIntoView && K.scrollIntoView({
        block: "nearest",
        inline: "nearest"
      });
    }
  }, ne = function(E) {
    O.current && (O.current.value = E ? oe(E) : r.value || "", N.current && (N.current.value = O.current.value));
  }, oe = function(E) {
    if (z.isScalar(E))
      return "".concat(E);
    var K = r.optionLabel ? z.resolveFieldData(E, r.optionLabel) : E.label;
    return "".concat(K);
  }, se = function(E) {
    if (r.useOptionAsValue)
      return E;
    var K = r.optionValue ? z.resolveFieldData(E, r.optionValue) : E ? E.value : z.resolveFieldData(E, "value");
    return r.optionValue || z.isNotEmpty(K) ? K : E;
  }, ve = function(E) {
    return r.dataKey ? z.resolveFieldData(E, r.dataKey) : oe(E);
  }, Ge = function(E) {
    return r.optionGroupLabel && E.optionGroup && E.group;
  }, Be = function(E) {
    return r.optionDisabled ? z.isFunction(r.optionDisabled) ? r.optionDisabled(E) : z.resolveFieldData(E, r.optionDisabled) : E && E.disabled !== void 0 ? E.disabled : !1;
  }, Et = function(E) {
    return z.resolveFieldData(E, r.optionGroupLabel);
  }, dn = function(E) {
    return z.resolveFieldData(E, r.optionGroupLabel);
  }, Ut = function(E) {
    return z.resolveFieldData(E, r.optionGroupChildren);
  }, fi = function() {
    if (r.editable && O.current) {
      var E = Dn ? oe(Dn) : null, K = E || r.value || "";
      O.current.value = K, N.current && (N.current.value = K);
    }
  }, Cr = function() {
    var E = zi(r.options);
    return E !== -1 ? r.optionGroupLabel ? Ut(r.options[E.group])[E.option] : r.options[E] : null;
  };
  w.useImperativeHandle(t, function() {
    return {
      props: r,
      show: C,
      hide: P,
      clear: yn,
      focus: function() {
        return j.focus(N.current);
      },
      getElement: function() {
        return I.current;
      },
      getOverlay: function() {
        return A.current;
      },
      getInput: function() {
        return O.current;
      },
      getFocusInput: function() {
        return N.current;
      },
      getVirtualScroller: function() {
        return le.current;
      }
    };
  }), w.useEffect(function() {
    z.combinedRefs(O, r.inputRef), z.combinedRefs(N, r.focusInputRef);
  }, [O, r.inputRef, N, r.focusInputRef]), rr(function() {
    r.autoFocus && j.focus(N.current, r.autoFocus), $();
  }), Zt(function() {
    x && (r.value || p >= 0) && Z();
  }, [x, r.value, p]), Zt(function() {
    x && a && r.filter && $();
  }, [x, a, r.filter]), Zt(function() {
    le.current && le.current.scrollInView(0);
  }, [a]), Zt(function() {
    a && (!r.options || r.options.length === 0) && l(""), fi(), O.current && (O.current.selectedIndex = 1);
  }), wr(function() {
    Ki.clear(A.current);
  });
  var Sa = function() {
    var E = {
      value: "",
      label: r.placeholder
    };
    if (Dn) {
      var K = se(Dn);
      E = {
        value: ca(K) === "object" ? r.options.findIndex(function(mo) {
          return mo === K;
        }) : K,
        label: oe(Dn)
      };
    }
    var Pe = e({
      className: "p-hidden-accessible p-dropdown-hidden-select"
    }, re("hiddenSelectedMessage")), Ct = e({
      ref: O,
      required: r.required,
      defaultValue: E.value,
      name: r.name,
      tabIndex: -1,
      "aria-hidden": "true"
    }, re("select")), Xt = e({
      value: E.value
    }, re("option"));
    return /* @__PURE__ */ w.createElement("div", Pe, /* @__PURE__ */ w.createElement("select", Ct, /* @__PURE__ */ w.createElement("option", Xt, E.label)));
  }, Aa = function() {
    var E = z.isNotEmpty(Dn) ? oe(Dn) : null;
    r.editable && (E = E || r.value || "");
    var K = e({
      className: "p-hidden-accessible"
    }, re("hiddenSelectedMessage")), Pe = e(Sr({
      ref: N,
      id: r.inputId,
      defaultValue: E,
      type: "text",
      readOnly: !0,
      "aria-haspopup": "listbox",
      onFocus: We,
      onBlur: Ye,
      onKeyDown: pt,
      disabled: r.disabled,
      tabIndex: r.disabled ? -1 : r.tabIndex || 0
    }, Gr), re("input"));
    return /* @__PURE__ */ w.createElement("div", K, /* @__PURE__ */ w.createElement("input", Pe));
  }, qr = function() {
    var E = z.isNotEmpty(Dn) ? oe(Dn) : null;
    if (r.editable) {
      var K = E || r.value || "", Pe = e(Sr({
        ref: O,
        type: "text",
        defaultValue: K,
        className: B("input", {
          label: E
        }),
        disabled: r.disabled,
        placeholder: r.placeholder,
        maxLength: r.maxLength,
        onInput: Lt,
        onFocus: Nt,
        onKeyDown: pt,
        onBlur: Ye,
        tabIndex: r.disabled ? -1 : r.tabIndex || 0,
        "aria-haspopup": "listbox"
      }, Gr), re("input"));
      return /* @__PURE__ */ w.createElement("input", Pe);
    }
    var Ct = r.valueTemplate ? z.getJSXElement(r.valueTemplate, Dn, r) : E || r.placeholder || r.emptyMessage || /* @__PURE__ */ w.createElement(w.Fragment, null, " "), Xt = e({
      ref: O,
      className: B("input", {
        label: E
      }),
      tabIndex: "-1"
    }, re("input"));
    return /* @__PURE__ */ w.createElement("span", Xt, Ct);
  }, Pc = function(E) {
    (E.key === "Enter" || E.code === "Space") && (yn(E), E.preventDefault());
  }, Pa = function() {
    if (r.value != null && r.showClear && !r.disabled && !z.isEmpty(r.options)) {
      var E = e({
        className: B("clearIcon"),
        onPointerUp: yn,
        tabIndex: r.tabIndex || "0",
        onKeyDown: Pc,
        "aria-label": la("clear")
      }, re("clearIcon")), K = r.clearIcon || /* @__PURE__ */ w.createElement(uh, E);
      return Mi.getJSXIcon(K, Sr({}, E), {
        props: r
      });
    }
    return null;
  }, Ia = function() {
    var E = e({
      className: B("loadingIcon"),
      "data-pr-overlay-visible": x
    }, re("loadingIcon")), K = r.loadingIcon || /* @__PURE__ */ w.createElement(ch, {
      spin: !0
    }), Pe = Mi.getJSXIcon(K, Sr({}, E), {
      props: r
    }), Ct = r.placeholder || r.ariaLabel, Xt = e({
      className: B("trigger"),
      role: "button",
      "aria-haspopup": "listbox",
      "aria-expanded": x,
      "aria-label": Ct
    }, re("trigger"));
    return /* @__PURE__ */ w.createElement("div", Xt, Pe);
  }, Ra = function() {
    var E = e({
      className: B("dropdownIcon"),
      "data-pr-overlay-visible": x
    }, re("dropdownIcon")), K = x ? r.collapseIcon || /* @__PURE__ */ w.createElement(AE, E) : r.dropdownIcon || /* @__PURE__ */ w.createElement(Jp, E), Pe = Mi.getJSXIcon(K, Sr({}, E), {
      props: r
    }), Ct = r.placeholder || r.ariaLabel, Xt = e({
      className: B("trigger"),
      role: "button",
      "aria-haspopup": "listbox",
      "aria-expanded": x,
      "aria-label": Ct
    }, re("trigger"));
    return /* @__PURE__ */ w.createElement("div", Xt, Pe);
  }, an = J(), Dn = Cr(), hn = z.isNotEmpty(r.tooltip), ri = ou.getOtherProps(r), Gr = z.reduceKeys(ri, j.ARIA_PROPS), fo = Sa(), Hi = Aa(), kr = qr(), xs = r.loading ? Ia() : Ra(), w5 = Pa(), y5 = e({
    id: r.id,
    ref: I,
    className: Re(r.className, B("root", {
      context: n,
      focusedState: d,
      overlayVisibleState: x
    })),
    style: r.style,
    onClick: function(E) {
      return W(E);
    },
    onMouseDown: r.onMouseDown,
    onContextMenu: r.onContextMenu,
    onFocus: q,
    "data-p-disabled": r.disabled,
    "data-p-focus": d,
    "aria-activedescendant": d ? "dropdownItem_".concat(p) : void 0
  }, ri, re("root")), _5 = e({
    ref: L,
    role: "presentation",
    "aria-hidden": "true",
    className: "p-hidden-accessible p-hidden-focusable",
    tabIndex: "0",
    onFocus: F,
    "data-p-hidden-accessible": !0,
    "data-p-hidden-focusable": !0
  }, re("hiddenFirstFocusableEl")), C5 = e({
    ref: R,
    role: "presentation",
    "aria-hidden": "true",
    className: "p-hidden-accessible p-hidden-focusable",
    tabIndex: "0",
    onFocus: H,
    "data-p-hidden-accessible": !0,
    "data-p-hidden-focusable": !0
  }, re("hiddenLastFocusableEl"));
  return /* @__PURE__ */ w.createElement(w.Fragment, null, /* @__PURE__ */ w.createElement("div", y5, Hi, fo, kr, w5, xs, /* @__PURE__ */ w.createElement(ME, Mr({
    hostName: "Dropdown",
    ref: A,
    visibleOptions: an,
    virtualScrollerRef: le
  }, r, {
    appendTo: Fe,
    cx: B,
    filterValue: a,
    focusedOptionIndex: p,
    getOptionGroupChildren: Ut,
    getOptionGroupLabel: dn,
    getOptionGroupRenderKey: Et,
    getOptionLabel: oe,
    getOptionRenderKey: ve,
    getSelectedOptionIndex: zi,
    hasFilter: Ae,
    in: x,
    isOptionDisabled: Be,
    isSelected: Si,
    onClick: Je,
    onEnter: ge,
    onEntered: Oe,
    onExit: ue,
    onExited: M,
    onFilterClearIconClick: vn,
    onFilterInputChange: Pt,
    onFilterInputKeyDown: Rn,
    onOptionClick: on,
    onInputKeyDown: pt,
    ptm: re,
    resetFilter: wn,
    changeFocusedOptionIndex: un,
    firstFocusableElement: /* @__PURE__ */ w.createElement("span", _5),
    lastFocusableElement: /* @__PURE__ */ w.createElement("span", C5),
    sx: X
  }))), hn && /* @__PURE__ */ w.createElement(ya, Mr({
    target: I,
    content: r.tooltip,
    pt: re("tooltip")
  }, r.tooltipOptions)));
}));
Qp.displayName = "Dropdown";
const HZ = ({ item: i, onChange: t }) => {
  const { config: e } = _r(), [n, r] = Ne({ ...os, ...i }), [s, o] = Ne(n.options);
  Ee(() => {
    r({ ...os, ...i });
  }, [i]), Ee(() => {
    if (o(n.options), n.source != "local") {
      const c = e.external_data.find((u) => u.key == n.source);
      c && (c.options && o(c.options), c.options_func && c.options_func().then((u) => {
        o(u);
      }));
    }
  }, [n.source, n.options]);
  const a = (c) => {
    const u = { ...os, ...i, value: c.target.value };
    t(u);
  }, l = Math.random().toString(36).substring(2, 15);
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { htmlFor: l, children: n.label }),
    /* @__PURE__ */ _.jsx(
      Qp,
      {
        id: l,
        value: n.value || "",
        onChange: a,
        options: s,
        showClear: !0,
        placeholder: n.placeholder || "",
        className: "w-full",
        children: " "
      }
    )
  ] }) }) });
}, jZ = (i) => ({});
var Cy;
function hg() {
  return hg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, hg.apply(null, arguments);
}
var WZ = function(t) {
  return /* @__PURE__ */ w.createElement("svg", hg({
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 448 512"
  }, t), Cy || (Cy = /* @__PURE__ */ w.createElement("path", {
    d: "M384 432c8.8 0 16-7.2 16-16V96c0-8.8-7.2-16-16-16H64c-8.8 0-16 7.2-16 16v320c0 8.8 7.2 16 16 16zm64-16c0 35.3-28.7 64-64 64H64c-35.3 0-64-28.7-64-64V96c0-35.3 28.7-64 64-64h320c35.3 0 64 28.7 64 64zm-224-64c-6.7 0-13-2.8-17.6-7.7l-104-112c-6.5-7-8.2-17.2-4.4-25.9s12.5-14.4 22-14.4h208c9.5 0 18.2 5.7 22 14.4s2.1 18.9-4.4 25.9l-104 112c-4.5 4.9-10.9 7.7-17.6 7.7"
  })));
};
function UZ() {
  for (var i = arguments.length, t = new Array(i), e = 0; e < i; e++)
    t[e] = arguments[e];
  return bt(
    () => (n) => {
      t.forEach((r) => r(n));
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    t
  );
}
const hh = typeof window < "u" && typeof window.document < "u" && typeof window.document.createElement < "u";
function _a(i) {
  const t = Object.prototype.toString.call(i);
  return t === "[object Window]" || // In Electron context the Window object serializes to [object global]
  t === "[object global]";
}
function eb(i) {
  return "nodeType" in i;
}
function ei(i) {
  var t, e;
  return i ? _a(i) ? i : eb(i) && (t = (e = i.ownerDocument) == null ? void 0 : e.defaultView) != null ? t : window : window;
}
function tb(i) {
  const {
    Document: t
  } = ei(i);
  return i instanceof t;
}
function Ec(i) {
  return _a(i) ? !1 : i instanceof ei(i).HTMLElement;
}
function DE(i) {
  return i instanceof ei(i).SVGElement;
}
function Ca(i) {
  return i ? _a(i) ? i.document : eb(i) ? tb(i) ? i : Ec(i) || DE(i) ? i.ownerDocument : document : document : document;
}
const Ni = hh ? T5 : Ee;
function fh(i) {
  const t = at(i);
  return Ni(() => {
    t.current = i;
  }), $n(function() {
    for (var e = arguments.length, n = new Array(e), r = 0; r < e; r++)
      n[r] = arguments[r];
    return t.current == null ? void 0 : t.current(...n);
  }, []);
}
function qZ() {
  const i = at(null), t = $n((n, r) => {
    i.current = setInterval(n, r);
  }, []), e = $n(() => {
    i.current !== null && (clearInterval(i.current), i.current = null);
  }, []);
  return [t, e];
}
function ic(i, t) {
  t === void 0 && (t = [i]);
  const e = at(i);
  return Ni(() => {
    e.current !== i && (e.current = i);
  }, t), e;
}
function Tc(i, t) {
  const e = at();
  return bt(
    () => {
      const n = i(e.current);
      return e.current = n, n;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [...t]
  );
}
function bd(i) {
  const t = fh(i), e = at(null), n = $n(
    (r) => {
      r !== e.current && (t == null || t(r, e.current)), e.current = r;
    },
    //eslint-disable-next-line
    []
  );
  return [e, n];
}
function vd(i) {
  const t = at();
  return Ee(() => {
    t.current = i;
  }, [i]), t.current;
}
let Of = {};
function Sc(i, t) {
  return bt(() => {
    if (t)
      return t;
    const e = Of[i] == null ? 0 : Of[i] + 1;
    return Of[i] = e, i + "-" + e;
  }, [i, t]);
}
function LE(i) {
  return function(t) {
    for (var e = arguments.length, n = new Array(e > 1 ? e - 1 : 0), r = 1; r < e; r++)
      n[r - 1] = arguments[r];
    return n.reduce((s, o) => {
      const a = Object.entries(o);
      for (const [l, c] of a) {
        const u = s[l];
        u != null && (s[l] = u + i * c);
      }
      return s;
    }, {
      ...t
    });
  };
}
const Go = /* @__PURE__ */ LE(1), rc = /* @__PURE__ */ LE(-1);
function GZ(i) {
  return "clientX" in i && "clientY" in i;
}
function mh(i) {
  if (!i)
    return !1;
  const {
    KeyboardEvent: t
  } = ei(i.target);
  return t && i instanceof t;
}
function KZ(i) {
  if (!i)
    return !1;
  const {
    TouchEvent: t
  } = ei(i.target);
  return t && i instanceof t;
}
function wd(i) {
  if (KZ(i)) {
    if (i.touches && i.touches.length) {
      const {
        clientX: t,
        clientY: e
      } = i.touches[0];
      return {
        x: t,
        y: e
      };
    } else if (i.changedTouches && i.changedTouches.length) {
      const {
        clientX: t,
        clientY: e
      } = i.changedTouches[0];
      return {
        x: t,
        y: e
      };
    }
  }
  return GZ(i) ? {
    x: i.clientX,
    y: i.clientY
  } : null;
}
const Bi = /* @__PURE__ */ Object.freeze({
  Translate: {
    toString(i) {
      if (!i)
        return;
      const {
        x: t,
        y: e
      } = i;
      return "translate3d(" + (t ? Math.round(t) : 0) + "px, " + (e ? Math.round(e) : 0) + "px, 0)";
    }
  },
  Scale: {
    toString(i) {
      if (!i)
        return;
      const {
        scaleX: t,
        scaleY: e
      } = i;
      return "scaleX(" + t + ") scaleY(" + e + ")";
    }
  },
  Transform: {
    toString(i) {
      if (i)
        return [Bi.Translate.toString(i), Bi.Scale.toString(i)].join(" ");
    }
  },
  Transition: {
    toString(i) {
      let {
        property: t,
        duration: e,
        easing: n
      } = i;
      return t + " " + e + "ms " + n;
    }
  }
}), ky = "a,frame,iframe,input:not([type=hidden]):not(:disabled),select:not(:disabled),textarea:not(:disabled),button:not(:disabled),*[tabindex]";
function ZZ(i) {
  return i.matches(ky) ? i : i.querySelector(ky);
}
const YZ = {
  display: "none"
};
function JZ(i) {
  let {
    id: t,
    value: e
  } = i;
  return Se.createElement("div", {
    id: t,
    style: YZ
  }, e);
}
function XZ(i) {
  let {
    id: t,
    announcement: e,
    ariaLiveType: n = "assertive"
  } = i;
  const r = {
    position: "fixed",
    width: 1,
    height: 1,
    margin: -1,
    border: 0,
    padding: 0,
    overflow: "hidden",
    clip: "rect(0 0 0 0)",
    clipPath: "inset(100%)",
    whiteSpace: "nowrap"
  };
  return Se.createElement("div", {
    id: t,
    style: r,
    role: "status",
    "aria-live": n,
    "aria-atomic": !0
  }, e);
}
function QZ() {
  const [i, t] = Ne("");
  return {
    announce: $n((n) => {
      n != null && t(n);
    }, []),
    announcement: i
  };
}
const NE = /* @__PURE__ */ dc(null);
function eY(i) {
  const t = hr(NE);
  Ee(() => {
    if (!t)
      throw new Error("useDndMonitor must be used within a children of <DndContext>");
    return t(i);
  }, [i, t]);
}
function tY() {
  const [i] = Ne(() => /* @__PURE__ */ new Set()), t = $n((n) => (i.add(n), () => i.delete(n)), [i]);
  return [$n((n) => {
    let {
      type: r,
      event: s
    } = n;
    i.forEach((o) => {
      var a;
      return (a = o[r]) == null ? void 0 : a.call(o, s);
    });
  }, [i]), t];
}
const nY = {
  draggable: `
    To pick up a draggable item, press the space bar.
    While dragging, use the arrow keys to move the item.
    Press space again to drop the item in its new position, or press escape to cancel.
  `
}, iY = {
  onDragStart(i) {
    let {
      active: t
    } = i;
    return "Picked up draggable item " + t.id + ".";
  },
  onDragOver(i) {
    let {
      active: t,
      over: e
    } = i;
    return e ? "Draggable item " + t.id + " was moved over droppable area " + e.id + "." : "Draggable item " + t.id + " is no longer over a droppable area.";
  },
  onDragEnd(i) {
    let {
      active: t,
      over: e
    } = i;
    return e ? "Draggable item " + t.id + " was dropped over droppable area " + e.id : "Draggable item " + t.id + " was dropped.";
  },
  onDragCancel(i) {
    let {
      active: t
    } = i;
    return "Dragging was cancelled. Draggable item " + t.id + " was dropped.";
  }
};
function rY(i) {
  let {
    announcements: t = iY,
    container: e,
    hiddenTextDescribedById: n,
    screenReaderInstructions: r = nY
  } = i;
  const {
    announce: s,
    announcement: o
  } = QZ(), a = Sc("DndLiveRegion"), [l, c] = Ne(!1);
  if (Ee(() => {
    c(!0);
  }, []), eY(bt(() => ({
    onDragStart(d) {
      let {
        active: f
      } = d;
      s(t.onDragStart({
        active: f
      }));
    },
    onDragMove(d) {
      let {
        active: f,
        over: m
      } = d;
      t.onDragMove && s(t.onDragMove({
        active: f,
        over: m
      }));
    },
    onDragOver(d) {
      let {
        active: f,
        over: m
      } = d;
      s(t.onDragOver({
        active: f,
        over: m
      }));
    },
    onDragEnd(d) {
      let {
        active: f,
        over: m
      } = d;
      s(t.onDragEnd({
        active: f,
        over: m
      }));
    },
    onDragCancel(d) {
      let {
        active: f,
        over: m
      } = d;
      s(t.onDragCancel({
        active: f,
        over: m
      }));
    }
  }), [s, t])), !l)
    return null;
  const u = Se.createElement(Se.Fragment, null, Se.createElement(JZ, {
    id: n,
    value: r.draggable
  }), Se.createElement(XZ, {
    id: a,
    announcement: o
  }));
  return e ? A5(u, e) : u;
}
var mn;
(function(i) {
  i.DragStart = "dragStart", i.DragMove = "dragMove", i.DragEnd = "dragEnd", i.DragCancel = "dragCancel", i.DragOver = "dragOver", i.RegisterDroppable = "registerDroppable", i.SetDroppableDisabled = "setDroppableDisabled", i.UnregisterDroppable = "unregisterDroppable";
})(mn || (mn = {}));
function yd() {
}
function gs(i, t) {
  return bt(
    () => ({
      sensor: i,
      options: t ?? {}
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [i, t]
  );
}
function gh() {
  for (var i = arguments.length, t = new Array(i), e = 0; e < i; e++)
    t[e] = arguments[e];
  return bt(
    () => [...t].filter((n) => n != null),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [...t]
  );
}
const er = /* @__PURE__ */ Object.freeze({
  x: 0,
  y: 0
});
function BE(i, t) {
  return Math.sqrt(Math.pow(i.x - t.x, 2) + Math.pow(i.y - t.y, 2));
}
function sY(i, t) {
  const e = wd(i);
  if (!e)
    return "0 0";
  const n = {
    x: (e.x - t.left) / t.width * 100,
    y: (e.y - t.top) / t.height * 100
  };
  return n.x + "% " + n.y + "%";
}
function FE(i, t) {
  let {
    data: {
      value: e
    }
  } = i, {
    data: {
      value: n
    }
  } = t;
  return e - n;
}
function oY(i, t) {
  let {
    data: {
      value: e
    }
  } = i, {
    data: {
      value: n
    }
  } = t;
  return n - e;
}
function xy(i) {
  let {
    left: t,
    top: e,
    height: n,
    width: r
  } = i;
  return [{
    x: t,
    y: e
  }, {
    x: t + r,
    y: e
  }, {
    x: t,
    y: e + n
  }, {
    x: t + r,
    y: e + n
  }];
}
function $E(i, t) {
  if (!i || i.length === 0)
    return null;
  const [e] = i;
  return e[t];
}
function Ey(i, t, e) {
  return t === void 0 && (t = i.left), e === void 0 && (e = i.top), {
    x: t + i.width * 0.5,
    y: e + i.height * 0.5
  };
}
const ph = (i) => {
  let {
    collisionRect: t,
    droppableRects: e,
    droppableContainers: n
  } = i;
  const r = Ey(t, t.left, t.top), s = [];
  for (const o of n) {
    const {
      id: a
    } = o, l = e.get(a);
    if (l) {
      const c = BE(Ey(l), r);
      s.push({
        id: a,
        data: {
          droppableContainer: o,
          value: c
        }
      });
    }
  }
  return s.sort(FE);
}, aY = (i) => {
  let {
    collisionRect: t,
    droppableRects: e,
    droppableContainers: n
  } = i;
  const r = xy(t), s = [];
  for (const o of n) {
    const {
      id: a
    } = o, l = e.get(a);
    if (l) {
      const c = xy(l), u = r.reduce((f, m, g) => f + BE(c[g], m), 0), d = Number((u / 4).toFixed(4));
      s.push({
        id: a,
        data: {
          droppableContainer: o,
          value: d
        }
      });
    }
  }
  return s.sort(FE);
};
function lY(i, t) {
  const e = Math.max(t.top, i.top), n = Math.max(t.left, i.left), r = Math.min(t.left + t.width, i.left + i.width), s = Math.min(t.top + t.height, i.top + i.height), o = r - n, a = s - e;
  if (n < r && e < s) {
    const l = t.width * t.height, c = i.width * i.height, u = o * a, d = u / (l + c - u);
    return Number(d.toFixed(4));
  }
  return 0;
}
const cY = (i) => {
  let {
    collisionRect: t,
    droppableRects: e,
    droppableContainers: n
  } = i;
  const r = [];
  for (const s of n) {
    const {
      id: o
    } = s, a = e.get(o);
    if (a) {
      const l = lY(a, t);
      l > 0 && r.push({
        id: o,
        data: {
          droppableContainer: s,
          value: l
        }
      });
    }
  }
  return r.sort(oY);
};
function uY(i, t, e) {
  return {
    ...i,
    scaleX: t && e ? t.width / e.width : 1,
    scaleY: t && e ? t.height / e.height : 1
  };
}
function zE(i, t) {
  return i && t ? {
    x: i.left - t.left,
    y: i.top - t.top
  } : er;
}
function dY(i) {
  return function(e) {
    for (var n = arguments.length, r = new Array(n > 1 ? n - 1 : 0), s = 1; s < n; s++)
      r[s - 1] = arguments[s];
    return r.reduce((o, a) => ({
      ...o,
      top: o.top + i * a.y,
      bottom: o.bottom + i * a.y,
      left: o.left + i * a.x,
      right: o.right + i * a.x
    }), {
      ...e
    });
  };
}
const hY = /* @__PURE__ */ dY(1);
function HE(i) {
  if (i.startsWith("matrix3d(")) {
    const t = i.slice(9, -1).split(/, /);
    return {
      x: +t[12],
      y: +t[13],
      scaleX: +t[0],
      scaleY: +t[5]
    };
  } else if (i.startsWith("matrix(")) {
    const t = i.slice(7, -1).split(/, /);
    return {
      x: +t[4],
      y: +t[5],
      scaleX: +t[0],
      scaleY: +t[3]
    };
  }
  return null;
}
function fY(i, t, e) {
  const n = HE(t);
  if (!n)
    return i;
  const {
    scaleX: r,
    scaleY: s,
    x: o,
    y: a
  } = n, l = i.left - o - (1 - r) * parseFloat(e), c = i.top - a - (1 - s) * parseFloat(e.slice(e.indexOf(" ") + 1)), u = r ? i.width / r : i.width, d = s ? i.height / s : i.height;
  return {
    width: u,
    height: d,
    top: c,
    right: l + u,
    bottom: c + d,
    left: l
  };
}
const mY = {
  ignoreTransform: !1
};
function ka(i, t) {
  t === void 0 && (t = mY);
  let e = i.getBoundingClientRect();
  if (t.ignoreTransform) {
    const {
      transform: c,
      transformOrigin: u
    } = ei(i).getComputedStyle(i);
    c && (e = fY(e, c, u));
  }
  const {
    top: n,
    left: r,
    width: s,
    height: o,
    bottom: a,
    right: l
  } = e;
  return {
    top: n,
    left: r,
    width: s,
    height: o,
    bottom: a,
    right: l
  };
}
function Ty(i) {
  return ka(i, {
    ignoreTransform: !0
  });
}
function gY(i) {
  const t = i.innerWidth, e = i.innerHeight;
  return {
    top: 0,
    left: 0,
    right: t,
    bottom: e,
    width: t,
    height: e
  };
}
function pY(i, t) {
  return t === void 0 && (t = ei(i).getComputedStyle(i)), t.position === "fixed";
}
function bY(i, t) {
  t === void 0 && (t = ei(i).getComputedStyle(i));
  const e = /(auto|scroll|overlay)/;
  return ["overflow", "overflowX", "overflowY"].some((r) => {
    const s = t[r];
    return typeof s == "string" ? e.test(s) : !1;
  });
}
function bh(i, t) {
  const e = [];
  function n(r) {
    if (t != null && e.length >= t || !r)
      return e;
    if (tb(r) && r.scrollingElement != null && !e.includes(r.scrollingElement))
      return e.push(r.scrollingElement), e;
    if (!Ec(r) || DE(r) || e.includes(r))
      return e;
    const s = ei(i).getComputedStyle(r);
    return r !== i && bY(r, s) && e.push(r), pY(r, s) ? e : n(r.parentNode);
  }
  return i ? n(i) : e;
}
function jE(i) {
  const [t] = bh(i, 1);
  return t ?? null;
}
function Vf(i) {
  return !hh || !i ? null : _a(i) ? i : eb(i) ? tb(i) || i === Ca(i).scrollingElement ? window : Ec(i) ? i : null : null;
}
function WE(i) {
  return _a(i) ? i.scrollX : i.scrollLeft;
}
function UE(i) {
  return _a(i) ? i.scrollY : i.scrollTop;
}
function fg(i) {
  return {
    x: WE(i),
    y: UE(i)
  };
}
var En;
(function(i) {
  i[i.Forward = 1] = "Forward", i[i.Backward = -1] = "Backward";
})(En || (En = {}));
function qE(i) {
  return !hh || !i ? !1 : i === document.scrollingElement;
}
function GE(i) {
  const t = {
    x: 0,
    y: 0
  }, e = qE(i) ? {
    height: window.innerHeight,
    width: window.innerWidth
  } : {
    height: i.clientHeight,
    width: i.clientWidth
  }, n = {
    x: i.scrollWidth - e.width,
    y: i.scrollHeight - e.height
  }, r = i.scrollTop <= t.y, s = i.scrollLeft <= t.x, o = i.scrollTop >= n.y, a = i.scrollLeft >= n.x;
  return {
    isTop: r,
    isLeft: s,
    isBottom: o,
    isRight: a,
    maxScroll: n,
    minScroll: t
  };
}
const vY = {
  x: 0.2,
  y: 0.2
};
function wY(i, t, e, n, r) {
  let {
    top: s,
    left: o,
    right: a,
    bottom: l
  } = e;
  n === void 0 && (n = 10), r === void 0 && (r = vY);
  const {
    isTop: c,
    isBottom: u,
    isLeft: d,
    isRight: f
  } = GE(i), m = {
    x: 0,
    y: 0
  }, g = {
    x: 0,
    y: 0
  }, p = {
    height: t.height * r.y,
    width: t.width * r.x
  };
  return !c && s <= t.top + p.height ? (m.y = En.Backward, g.y = n * Math.abs((t.top + p.height - s) / p.height)) : !u && l >= t.bottom - p.height && (m.y = En.Forward, g.y = n * Math.abs((t.bottom - p.height - l) / p.height)), !f && a >= t.right - p.width ? (m.x = En.Forward, g.x = n * Math.abs((t.right - p.width - a) / p.width)) : !d && o <= t.left + p.width && (m.x = En.Backward, g.x = n * Math.abs((t.left + p.width - o) / p.width)), {
    direction: m,
    speed: g
  };
}
function yY(i) {
  if (i === document.scrollingElement) {
    const {
      innerWidth: s,
      innerHeight: o
    } = window;
    return {
      top: 0,
      left: 0,
      right: s,
      bottom: o,
      width: s,
      height: o
    };
  }
  const {
    top: t,
    left: e,
    right: n,
    bottom: r
  } = i.getBoundingClientRect();
  return {
    top: t,
    left: e,
    right: n,
    bottom: r,
    width: i.clientWidth,
    height: i.clientHeight
  };
}
function KE(i) {
  return i.reduce((t, e) => Go(t, fg(e)), er);
}
function _Y(i) {
  return i.reduce((t, e) => t + WE(e), 0);
}
function CY(i) {
  return i.reduce((t, e) => t + UE(e), 0);
}
function ZE(i, t) {
  if (t === void 0 && (t = ka), !i)
    return;
  const {
    top: e,
    left: n,
    bottom: r,
    right: s
  } = t(i);
  jE(i) && (r <= 0 || s <= 0 || e >= window.innerHeight || n >= window.innerWidth) && i.scrollIntoView({
    block: "center",
    inline: "center"
  });
}
const kY = [["x", ["left", "right"], _Y], ["y", ["top", "bottom"], CY]];
class nb {
  constructor(t, e) {
    this.rect = void 0, this.width = void 0, this.height = void 0, this.top = void 0, this.bottom = void 0, this.right = void 0, this.left = void 0;
    const n = bh(e), r = KE(n);
    this.rect = {
      ...t
    }, this.width = t.width, this.height = t.height;
    for (const [s, o, a] of kY)
      for (const l of o)
        Object.defineProperty(this, l, {
          get: () => {
            const c = a(n), u = r[s] - c;
            return this.rect[l] + u;
          },
          enumerable: !0
        });
    Object.defineProperty(this, "rect", {
      enumerable: !1
    });
  }
}
class vl {
  constructor(t) {
    this.target = void 0, this.listeners = [], this.removeAll = () => {
      this.listeners.forEach((e) => {
        var n;
        return (n = this.target) == null ? void 0 : n.removeEventListener(...e);
      });
    }, this.target = t;
  }
  add(t, e, n) {
    var r;
    (r = this.target) == null || r.addEventListener(t, e, n), this.listeners.push([t, e, n]);
  }
}
function xY(i) {
  const {
    EventTarget: t
  } = ei(i);
  return i instanceof t ? i : Ca(i);
}
function Mf(i, t) {
  const e = Math.abs(i.x), n = Math.abs(i.y);
  return typeof t == "number" ? Math.sqrt(e ** 2 + n ** 2) > t : "x" in t && "y" in t ? e > t.x && n > t.y : "x" in t ? e > t.x : "y" in t ? n > t.y : !1;
}
var Ri;
(function(i) {
  i.Click = "click", i.DragStart = "dragstart", i.Keydown = "keydown", i.ContextMenu = "contextmenu", i.Resize = "resize", i.SelectionChange = "selectionchange", i.VisibilityChange = "visibilitychange";
})(Ri || (Ri = {}));
function Sy(i) {
  i.preventDefault();
}
function EY(i) {
  i.stopPropagation();
}
var Qe;
(function(i) {
  i.Space = "Space", i.Down = "ArrowDown", i.Right = "ArrowRight", i.Left = "ArrowLeft", i.Up = "ArrowUp", i.Esc = "Escape", i.Enter = "Enter";
})(Qe || (Qe = {}));
const YE = {
  start: [Qe.Space, Qe.Enter],
  cancel: [Qe.Esc],
  end: [Qe.Space, Qe.Enter]
}, TY = (i, t) => {
  let {
    currentCoordinates: e
  } = t;
  switch (i.code) {
    case Qe.Right:
      return {
        ...e,
        x: e.x + 25
      };
    case Qe.Left:
      return {
        ...e,
        x: e.x - 25
      };
    case Qe.Down:
      return {
        ...e,
        y: e.y + 25
      };
    case Qe.Up:
      return {
        ...e,
        y: e.y - 25
      };
  }
};
class xa {
  constructor(t) {
    this.props = void 0, this.autoScrollEnabled = !1, this.referenceCoordinates = void 0, this.listeners = void 0, this.windowListeners = void 0, this.props = t;
    const {
      event: {
        target: e
      }
    } = t;
    this.props = t, this.listeners = new vl(Ca(e)), this.windowListeners = new vl(ei(e)), this.handleKeyDown = this.handleKeyDown.bind(this), this.handleCancel = this.handleCancel.bind(this), this.attach();
  }
  attach() {
    this.handleStart(), this.windowListeners.add(Ri.Resize, this.handleCancel), this.windowListeners.add(Ri.VisibilityChange, this.handleCancel), setTimeout(() => this.listeners.add(Ri.Keydown, this.handleKeyDown));
  }
  handleStart() {
    const {
      activeNode: t,
      onStart: e
    } = this.props, n = t.node.current;
    n && ZE(n), e(er);
  }
  handleKeyDown(t) {
    if (mh(t)) {
      const {
        active: e,
        context: n,
        options: r
      } = this.props, {
        keyboardCodes: s = YE,
        coordinateGetter: o = TY,
        scrollBehavior: a = "smooth"
      } = r, {
        code: l
      } = t;
      if (s.end.includes(l)) {
        this.handleEnd(t);
        return;
      }
      if (s.cancel.includes(l)) {
        this.handleCancel(t);
        return;
      }
      const {
        collisionRect: c
      } = n.current, u = c ? {
        x: c.left,
        y: c.top
      } : er;
      this.referenceCoordinates || (this.referenceCoordinates = u);
      const d = o(t, {
        active: e,
        context: n.current,
        currentCoordinates: u
      });
      if (d) {
        const f = rc(d, u), m = {
          x: 0,
          y: 0
        }, {
          scrollableAncestors: g
        } = n.current;
        for (const p of g) {
          const b = t.code, {
            isTop: v,
            isRight: y,
            isLeft: x,
            isBottom: S,
            maxScroll: k,
            minScroll: I
          } = GE(p), A = yY(p), L = {
            x: Math.min(b === Qe.Right ? A.right - A.width / 2 : A.right, Math.max(b === Qe.Right ? A.left : A.left + A.width / 2, d.x)),
            y: Math.min(b === Qe.Down ? A.bottom - A.height / 2 : A.bottom, Math.max(b === Qe.Down ? A.top : A.top + A.height / 2, d.y))
          }, R = b === Qe.Right && !y || b === Qe.Left && !x, O = b === Qe.Down && !S || b === Qe.Up && !v;
          if (R && L.x !== d.x) {
            const N = p.scrollLeft + f.x, le = b === Qe.Right && N <= k.x || b === Qe.Left && N >= I.x;
            if (le && !f.y) {
              p.scrollTo({
                left: N,
                behavior: a
              });
              return;
            }
            le ? m.x = p.scrollLeft - N : m.x = b === Qe.Right ? p.scrollLeft - k.x : p.scrollLeft - I.x, m.x && p.scrollBy({
              left: -m.x,
              behavior: a
            });
            break;
          } else if (O && L.y !== d.y) {
            const N = p.scrollTop + f.y, le = b === Qe.Down && N <= k.y || b === Qe.Up && N >= I.y;
            if (le && !f.x) {
              p.scrollTo({
                top: N,
                behavior: a
              });
              return;
            }
            le ? m.y = p.scrollTop - N : m.y = b === Qe.Down ? p.scrollTop - k.y : p.scrollTop - I.y, m.y && p.scrollBy({
              top: -m.y,
              behavior: a
            });
            break;
          }
        }
        this.handleMove(t, Go(rc(d, this.referenceCoordinates), m));
      }
    }
  }
  handleMove(t, e) {
    const {
      onMove: n
    } = this.props;
    t.preventDefault(), n(e);
  }
  handleEnd(t) {
    const {
      onEnd: e
    } = this.props;
    t.preventDefault(), this.detach(), e();
  }
  handleCancel(t) {
    const {
      onCancel: e
    } = this.props;
    t.preventDefault(), this.detach(), e();
  }
  detach() {
    this.listeners.removeAll(), this.windowListeners.removeAll();
  }
}
xa.activators = [{
  eventName: "onKeyDown",
  handler: (i, t, e) => {
    let {
      keyboardCodes: n = YE,
      onActivation: r
    } = t, {
      active: s
    } = e;
    const {
      code: o
    } = i.nativeEvent;
    if (n.start.includes(o)) {
      const a = s.activatorNode.current;
      return a && i.target !== a ? !1 : (i.preventDefault(), r == null || r({
        event: i.nativeEvent
      }), !0);
    }
    return !1;
  }
}];
function Ay(i) {
  return !!(i && "distance" in i);
}
function Py(i) {
  return !!(i && "delay" in i);
}
class ib {
  constructor(t, e, n) {
    var r;
    n === void 0 && (n = xY(t.event.target)), this.props = void 0, this.events = void 0, this.autoScrollEnabled = !0, this.document = void 0, this.activated = !1, this.initialCoordinates = void 0, this.timeoutId = null, this.listeners = void 0, this.documentListeners = void 0, this.windowListeners = void 0, this.props = t, this.events = e;
    const {
      event: s
    } = t, {
      target: o
    } = s;
    this.props = t, this.events = e, this.document = Ca(o), this.documentListeners = new vl(this.document), this.listeners = new vl(n), this.windowListeners = new vl(ei(o)), this.initialCoordinates = (r = wd(s)) != null ? r : er, this.handleStart = this.handleStart.bind(this), this.handleMove = this.handleMove.bind(this), this.handleEnd = this.handleEnd.bind(this), this.handleCancel = this.handleCancel.bind(this), this.handleKeydown = this.handleKeydown.bind(this), this.removeTextSelection = this.removeTextSelection.bind(this), this.attach();
  }
  attach() {
    const {
      events: t,
      props: {
        options: {
          activationConstraint: e,
          bypassActivationConstraint: n
        }
      }
    } = this;
    if (this.listeners.add(t.move.name, this.handleMove, {
      passive: !1
    }), this.listeners.add(t.end.name, this.handleEnd), this.windowListeners.add(Ri.Resize, this.handleCancel), this.windowListeners.add(Ri.DragStart, Sy), this.windowListeners.add(Ri.VisibilityChange, this.handleCancel), this.windowListeners.add(Ri.ContextMenu, Sy), this.documentListeners.add(Ri.Keydown, this.handleKeydown), e) {
      if (n != null && n({
        event: this.props.event,
        activeNode: this.props.activeNode,
        options: this.props.options
      }))
        return this.handleStart();
      if (Py(e)) {
        this.timeoutId = setTimeout(this.handleStart, e.delay);
        return;
      }
      if (Ay(e))
        return;
    }
    this.handleStart();
  }
  detach() {
    this.listeners.removeAll(), this.windowListeners.removeAll(), setTimeout(this.documentListeners.removeAll, 50), this.timeoutId !== null && (clearTimeout(this.timeoutId), this.timeoutId = null);
  }
  handleStart() {
    const {
      initialCoordinates: t
    } = this, {
      onStart: e
    } = this.props;
    t && (this.activated = !0, this.documentListeners.add(Ri.Click, EY, {
      capture: !0
    }), this.removeTextSelection(), this.documentListeners.add(Ri.SelectionChange, this.removeTextSelection), e(t));
  }
  handleMove(t) {
    var e;
    const {
      activated: n,
      initialCoordinates: r,
      props: s
    } = this, {
      onMove: o,
      options: {
        activationConstraint: a
      }
    } = s;
    if (!r)
      return;
    const l = (e = wd(t)) != null ? e : er, c = rc(r, l);
    if (!n && a) {
      if (Ay(a)) {
        if (a.tolerance != null && Mf(c, a.tolerance))
          return this.handleCancel();
        if (Mf(c, a.distance))
          return this.handleStart();
      }
      return Py(a) && Mf(c, a.tolerance) ? this.handleCancel() : void 0;
    }
    t.cancelable && t.preventDefault(), o(l);
  }
  handleEnd() {
    const {
      onEnd: t
    } = this.props;
    this.detach(), t();
  }
  handleCancel() {
    const {
      onCancel: t
    } = this.props;
    this.detach(), t();
  }
  handleKeydown(t) {
    t.code === Qe.Esc && this.handleCancel();
  }
  removeTextSelection() {
    var t;
    (t = this.document.getSelection()) == null || t.removeAllRanges();
  }
}
const SY = {
  move: {
    name: "pointermove"
  },
  end: {
    name: "pointerup"
  }
};
class Ea extends ib {
  constructor(t) {
    const {
      event: e
    } = t, n = Ca(e.target);
    super(t, SY, n);
  }
}
Ea.activators = [{
  eventName: "onPointerDown",
  handler: (i, t) => {
    let {
      nativeEvent: e
    } = i, {
      onActivation: n
    } = t;
    return !e.isPrimary || e.button !== 0 ? !1 : (n == null || n({
      event: e
    }), !0);
  }
}];
const AY = {
  move: {
    name: "mousemove"
  },
  end: {
    name: "mouseup"
  }
};
var mg;
(function(i) {
  i[i.RightClick = 2] = "RightClick";
})(mg || (mg = {}));
class PY extends ib {
  constructor(t) {
    super(t, AY, Ca(t.event.target));
  }
}
PY.activators = [{
  eventName: "onMouseDown",
  handler: (i, t) => {
    let {
      nativeEvent: e
    } = i, {
      onActivation: n
    } = t;
    return e.button === mg.RightClick ? !1 : (n == null || n({
      event: e
    }), !0);
  }
}];
const Df = {
  move: {
    name: "touchmove"
  },
  end: {
    name: "touchend"
  }
};
class IY extends ib {
  constructor(t) {
    super(t, Df);
  }
  static setup() {
    return window.addEventListener(Df.move.name, t, {
      capture: !1,
      passive: !1
    }), function() {
      window.removeEventListener(Df.move.name, t);
    };
    function t() {
    }
  }
}
IY.activators = [{
  eventName: "onTouchStart",
  handler: (i, t) => {
    let {
      nativeEvent: e
    } = i, {
      onActivation: n
    } = t;
    const {
      touches: r
    } = e;
    return r.length > 1 ? !1 : (n == null || n({
      event: e
    }), !0);
  }
}];
var wl;
(function(i) {
  i[i.Pointer = 0] = "Pointer", i[i.DraggableRect = 1] = "DraggableRect";
})(wl || (wl = {}));
var _d;
(function(i) {
  i[i.TreeOrder = 0] = "TreeOrder", i[i.ReversedTreeOrder = 1] = "ReversedTreeOrder";
})(_d || (_d = {}));
function RY(i) {
  let {
    acceleration: t,
    activator: e = wl.Pointer,
    canScroll: n,
    draggingRect: r,
    enabled: s,
    interval: o = 5,
    order: a = _d.TreeOrder,
    pointerCoordinates: l,
    scrollableAncestors: c,
    scrollableAncestorRects: u,
    delta: d,
    threshold: f
  } = i;
  const m = VY({
    delta: d,
    disabled: !s
  }), [g, p] = qZ(), b = at({
    x: 0,
    y: 0
  }), v = at({
    x: 0,
    y: 0
  }), y = bt(() => {
    switch (e) {
      case wl.Pointer:
        return l ? {
          top: l.y,
          bottom: l.y,
          left: l.x,
          right: l.x
        } : null;
      case wl.DraggableRect:
        return r;
    }
  }, [e, r, l]), x = at(null), S = $n(() => {
    const I = x.current;
    if (!I)
      return;
    const A = b.current.x * v.current.x, L = b.current.y * v.current.y;
    I.scrollBy(A, L);
  }, []), k = bt(() => a === _d.TreeOrder ? [...c].reverse() : c, [a, c]);
  Ee(
    () => {
      if (!s || !c.length || !y) {
        p();
        return;
      }
      for (const I of k) {
        if ((n == null ? void 0 : n(I)) === !1)
          continue;
        const A = c.indexOf(I), L = u[A];
        if (!L)
          continue;
        const {
          direction: R,
          speed: O
        } = wY(I, L, y, t, f);
        for (const N of ["x", "y"])
          m[N][R[N]] || (O[N] = 0, R[N] = 0);
        if (O.x > 0 || O.y > 0) {
          p(), x.current = I, g(S, o), b.current = O, v.current = R;
          return;
        }
      }
      b.current = {
        x: 0,
        y: 0
      }, v.current = {
        x: 0,
        y: 0
      }, p();
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      t,
      S,
      n,
      p,
      s,
      o,
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(y),
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(m),
      g,
      c,
      k,
      u,
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(f)
    ]
  );
}
const OY = {
  x: {
    [En.Backward]: !1,
    [En.Forward]: !1
  },
  y: {
    [En.Backward]: !1,
    [En.Forward]: !1
  }
};
function VY(i) {
  let {
    delta: t,
    disabled: e
  } = i;
  const n = vd(t);
  return Tc((r) => {
    if (e || !n || !r)
      return OY;
    const s = {
      x: Math.sign(t.x - n.x),
      y: Math.sign(t.y - n.y)
    };
    return {
      x: {
        [En.Backward]: r.x[En.Backward] || s.x === -1,
        [En.Forward]: r.x[En.Forward] || s.x === 1
      },
      y: {
        [En.Backward]: r.y[En.Backward] || s.y === -1,
        [En.Forward]: r.y[En.Forward] || s.y === 1
      }
    };
  }, [e, t, n]);
}
function MY(i, t) {
  const e = t !== null ? i.get(t) : void 0, n = e ? e.node.current : null;
  return Tc((r) => {
    var s;
    return t === null ? null : (s = n ?? r) != null ? s : null;
  }, [n, t]);
}
function DY(i, t) {
  return bt(() => i.reduce((e, n) => {
    const {
      sensor: r
    } = n, s = r.activators.map((o) => ({
      eventName: o.eventName,
      handler: t(o.handler, n)
    }));
    return [...e, ...s];
  }, []), [i, t]);
}
var sc;
(function(i) {
  i[i.Always = 0] = "Always", i[i.BeforeDragging = 1] = "BeforeDragging", i[i.WhileDragging = 2] = "WhileDragging";
})(sc || (sc = {}));
var gg;
(function(i) {
  i.Optimized = "optimized";
})(gg || (gg = {}));
const Iy = /* @__PURE__ */ new Map();
function LY(i, t) {
  let {
    dragging: e,
    dependencies: n,
    config: r
  } = t;
  const [s, o] = Ne(null), {
    frequency: a,
    measure: l,
    strategy: c
  } = r, u = at(i), d = b(), f = ic(d), m = $n(function(v) {
    v === void 0 && (v = []), !f.current && o((y) => y === null ? v : y.concat(v.filter((x) => !y.includes(x))));
  }, [f]), g = at(null), p = Tc((v) => {
    if (d && !e)
      return Iy;
    if (!v || v === Iy || u.current !== i || s != null) {
      const y = /* @__PURE__ */ new Map();
      for (let x of i) {
        if (!x)
          continue;
        if (s && s.length > 0 && !s.includes(x.id) && x.rect.current) {
          y.set(x.id, x.rect.current);
          continue;
        }
        const S = x.node.current, k = S ? new nb(l(S), S) : null;
        x.rect.current = k, k && y.set(x.id, k);
      }
      return y;
    }
    return v;
  }, [i, s, e, d, l]);
  return Ee(() => {
    u.current = i;
  }, [i]), Ee(
    () => {
      d || m();
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [e, d]
  ), Ee(
    () => {
      s && s.length > 0 && o(null);
    },
    //eslint-disable-next-line react-hooks/exhaustive-deps
    [JSON.stringify(s)]
  ), Ee(
    () => {
      d || typeof a != "number" || g.current !== null || (g.current = setTimeout(() => {
        m(), g.current = null;
      }, a));
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [a, d, m, ...n]
  ), {
    droppableRects: p,
    measureDroppableContainers: m,
    measuringScheduled: s != null
  };
  function b() {
    switch (c) {
      case sc.Always:
        return !1;
      case sc.BeforeDragging:
        return e;
      default:
        return !e;
    }
  }
}
function rb(i, t) {
  return Tc((e) => i ? e || (typeof t == "function" ? t(i) : i) : null, [t, i]);
}
function NY(i, t) {
  return rb(i, t);
}
function BY(i) {
  let {
    callback: t,
    disabled: e
  } = i;
  const n = fh(t), r = bt(() => {
    if (e || typeof window > "u" || typeof window.MutationObserver > "u")
      return;
    const {
      MutationObserver: s
    } = window;
    return new s(n);
  }, [n, e]);
  return Ee(() => () => r == null ? void 0 : r.disconnect(), [r]), r;
}
function vh(i) {
  let {
    callback: t,
    disabled: e
  } = i;
  const n = fh(t), r = bt(
    () => {
      if (e || typeof window > "u" || typeof window.ResizeObserver > "u")
        return;
      const {
        ResizeObserver: s
      } = window;
      return new s(n);
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [e]
  );
  return Ee(() => () => r == null ? void 0 : r.disconnect(), [r]), r;
}
function FY(i) {
  return new nb(ka(i), i);
}
function Ry(i, t, e) {
  t === void 0 && (t = FY);
  const [n, r] = Sg(a, null), s = BY({
    callback(l) {
      if (i)
        for (const c of l) {
          const {
            type: u,
            target: d
          } = c;
          if (u === "childList" && d instanceof HTMLElement && d.contains(i)) {
            r();
            break;
          }
        }
    }
  }), o = vh({
    callback: r
  });
  return Ni(() => {
    r(), i ? (o == null || o.observe(i), s == null || s.observe(document.body, {
      childList: !0,
      subtree: !0
    })) : (o == null || o.disconnect(), s == null || s.disconnect());
  }, [i]), n;
  function a(l) {
    if (!i)
      return null;
    if (i.isConnected === !1) {
      var c;
      return (c = l ?? e) != null ? c : null;
    }
    const u = t(i);
    return JSON.stringify(l) === JSON.stringify(u) ? l : u;
  }
}
function $Y(i) {
  const t = rb(i);
  return zE(i, t);
}
const Oy = [];
function zY(i) {
  const t = at(i), e = Tc((n) => i ? n && n !== Oy && i && t.current && i.parentNode === t.current.parentNode ? n : bh(i) : Oy, [i]);
  return Ee(() => {
    t.current = i;
  }, [i]), e;
}
function HY(i) {
  const [t, e] = Ne(null), n = at(i), r = $n((s) => {
    const o = Vf(s.target);
    o && e((a) => a ? (a.set(o, fg(o)), new Map(a)) : null);
  }, []);
  return Ee(() => {
    const s = n.current;
    if (i !== s) {
      o(s);
      const a = i.map((l) => {
        const c = Vf(l);
        return c ? (c.addEventListener("scroll", r, {
          passive: !0
        }), [c, fg(c)]) : null;
      }).filter((l) => l != null);
      e(a.length ? new Map(a) : null), n.current = i;
    }
    return () => {
      o(i), o(s);
    };
    function o(a) {
      a.forEach((l) => {
        const c = Vf(l);
        c == null || c.removeEventListener("scroll", r);
      });
    }
  }, [r, i]), bt(() => i.length ? t ? Array.from(t.values()).reduce((s, o) => Go(s, o), er) : KE(i) : er, [i, t]);
}
function Vy(i, t) {
  t === void 0 && (t = []);
  const e = at(null);
  return Ee(
    () => {
      e.current = null;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    t
  ), Ee(() => {
    const n = i !== er;
    n && !e.current && (e.current = i), !n && e.current && (e.current = null);
  }, [i]), e.current ? rc(i, e.current) : er;
}
function jY(i) {
  Ee(
    () => {
      if (!hh)
        return;
      const t = i.map((e) => {
        let {
          sensor: n
        } = e;
        return n.setup == null ? void 0 : n.setup();
      });
      return () => {
        for (const e of t)
          e == null || e();
      };
    },
    // TO-DO: Sensors length could theoretically change which would not be a valid dependency
    // eslint-disable-next-line react-hooks/exhaustive-deps
    i.map((t) => {
      let {
        sensor: e
      } = t;
      return e;
    })
  );
}
function WY(i, t) {
  return bt(() => i.reduce((e, n) => {
    let {
      eventName: r,
      handler: s
    } = n;
    return e[r] = (o) => {
      s(o, t);
    }, e;
  }, {}), [i, t]);
}
function JE(i) {
  return bt(() => i ? gY(i) : null, [i]);
}
const Lf = [];
function UY(i, t) {
  t === void 0 && (t = ka);
  const [e] = i, n = JE(e ? ei(e) : null), [r, s] = Sg(a, Lf), o = vh({
    callback: s
  });
  return i.length > 0 && r === Lf && s(), Ni(() => {
    i.length ? i.forEach((l) => o == null ? void 0 : o.observe(l)) : (o == null || o.disconnect(), s());
  }, [i]), r;
  function a() {
    return i.length ? i.map((l) => qE(l) ? n : new nb(t(l), l)) : Lf;
  }
}
function XE(i) {
  if (!i)
    return null;
  if (i.children.length > 1)
    return i;
  const t = i.children[0];
  return Ec(t) ? t : i;
}
function qY(i) {
  let {
    measure: t
  } = i;
  const [e, n] = Ne(null), r = $n((c) => {
    for (const {
      target: u
    } of c)
      if (Ec(u)) {
        n((d) => {
          const f = t(u);
          return d ? {
            ...d,
            width: f.width,
            height: f.height
          } : f;
        });
        break;
      }
  }, [t]), s = vh({
    callback: r
  }), o = $n((c) => {
    const u = XE(c);
    s == null || s.disconnect(), u && (s == null || s.observe(u)), n(u ? t(u) : null);
  }, [t, s]), [a, l] = bd(o);
  return bt(() => ({
    nodeRef: a,
    rect: e,
    setRef: l
  }), [e, a, l]);
}
const GY = [{
  sensor: Ea,
  options: {}
}, {
  sensor: xa,
  options: {}
}], KY = {
  current: {}
}, Ru = {
  draggable: {
    measure: Ty
  },
  droppable: {
    measure: Ty,
    strategy: sc.WhileDragging,
    frequency: gg.Optimized
  },
  dragOverlay: {
    measure: ka
  }
};
class yl extends Map {
  get(t) {
    var e;
    return t != null && (e = super.get(t)) != null ? e : void 0;
  }
  toArray() {
    return Array.from(this.values());
  }
  getEnabled() {
    return this.toArray().filter((t) => {
      let {
        disabled: e
      } = t;
      return !e;
    });
  }
  getNodeFor(t) {
    var e, n;
    return (e = (n = this.get(t)) == null ? void 0 : n.node.current) != null ? e : void 0;
  }
}
const ZY = {
  activatorEvent: null,
  active: null,
  activeNode: null,
  activeNodeRect: null,
  collisions: null,
  containerNodeRect: null,
  draggableNodes: /* @__PURE__ */ new Map(),
  droppableRects: /* @__PURE__ */ new Map(),
  droppableContainers: /* @__PURE__ */ new yl(),
  over: null,
  dragOverlay: {
    nodeRef: {
      current: null
    },
    rect: null,
    setRef: yd
  },
  scrollableAncestors: [],
  scrollableAncestorRects: [],
  measuringConfiguration: Ru,
  measureDroppableContainers: yd,
  windowRect: null,
  measuringScheduled: !1
}, QE = {
  activatorEvent: null,
  activators: [],
  active: null,
  activeNodeRect: null,
  ariaDescribedById: {
    draggable: ""
  },
  dispatch: yd,
  draggableNodes: /* @__PURE__ */ new Map(),
  over: null,
  measureDroppableContainers: yd
}, Ac = /* @__PURE__ */ dc(QE), e5 = /* @__PURE__ */ dc(ZY);
function YY() {
  return {
    draggable: {
      active: null,
      initialCoordinates: {
        x: 0,
        y: 0
      },
      nodes: /* @__PURE__ */ new Map(),
      translate: {
        x: 0,
        y: 0
      }
    },
    droppable: {
      containers: new yl()
    }
  };
}
function JY(i, t) {
  switch (t.type) {
    case mn.DragStart:
      return {
        ...i,
        draggable: {
          ...i.draggable,
          initialCoordinates: t.initialCoordinates,
          active: t.active
        }
      };
    case mn.DragMove:
      return i.draggable.active ? {
        ...i,
        draggable: {
          ...i.draggable,
          translate: {
            x: t.coordinates.x - i.draggable.initialCoordinates.x,
            y: t.coordinates.y - i.draggable.initialCoordinates.y
          }
        }
      } : i;
    case mn.DragEnd:
    case mn.DragCancel:
      return {
        ...i,
        draggable: {
          ...i.draggable,
          active: null,
          initialCoordinates: {
            x: 0,
            y: 0
          },
          translate: {
            x: 0,
            y: 0
          }
        }
      };
    case mn.RegisterDroppable: {
      const {
        element: e
      } = t, {
        id: n
      } = e, r = new yl(i.droppable.containers);
      return r.set(n, e), {
        ...i,
        droppable: {
          ...i.droppable,
          containers: r
        }
      };
    }
    case mn.SetDroppableDisabled: {
      const {
        id: e,
        key: n,
        disabled: r
      } = t, s = i.droppable.containers.get(e);
      if (!s || n !== s.key)
        return i;
      const o = new yl(i.droppable.containers);
      return o.set(e, {
        ...s,
        disabled: r
      }), {
        ...i,
        droppable: {
          ...i.droppable,
          containers: o
        }
      };
    }
    case mn.UnregisterDroppable: {
      const {
        id: e,
        key: n
      } = t, r = i.droppable.containers.get(e);
      if (!r || n !== r.key)
        return i;
      const s = new yl(i.droppable.containers);
      return s.delete(e), {
        ...i,
        droppable: {
          ...i.droppable,
          containers: s
        }
      };
    }
    default:
      return i;
  }
}
function XY(i) {
  let {
    disabled: t
  } = i;
  const {
    active: e,
    activatorEvent: n,
    draggableNodes: r
  } = hr(Ac), s = vd(n), o = vd(e == null ? void 0 : e.id);
  return Ee(() => {
    if (!t && !n && s && o != null) {
      if (!mh(s) || document.activeElement === s.target)
        return;
      const a = r.get(o);
      if (!a)
        return;
      const {
        activatorNode: l,
        node: c
      } = a;
      if (!l.current && !c.current)
        return;
      requestAnimationFrame(() => {
        for (const u of [l.current, c.current]) {
          if (!u)
            continue;
          const d = ZZ(u);
          if (d) {
            d.focus();
            break;
          }
        }
      });
    }
  }, [n, t, r, o, s]), null;
}
function t5(i, t) {
  let {
    transform: e,
    ...n
  } = t;
  return i != null && i.length ? i.reduce((r, s) => s({
    transform: r,
    ...n
  }), e) : e;
}
function QY(i) {
  return bt(
    () => ({
      draggable: {
        ...Ru.draggable,
        ...i == null ? void 0 : i.draggable
      },
      droppable: {
        ...Ru.droppable,
        ...i == null ? void 0 : i.droppable
      },
      dragOverlay: {
        ...Ru.dragOverlay,
        ...i == null ? void 0 : i.dragOverlay
      }
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [i == null ? void 0 : i.draggable, i == null ? void 0 : i.droppable, i == null ? void 0 : i.dragOverlay]
  );
}
function eJ(i) {
  let {
    activeNode: t,
    measure: e,
    initialRect: n,
    config: r = !0
  } = i;
  const s = at(!1), {
    x: o,
    y: a
  } = typeof r == "boolean" ? {
    x: r,
    y: r
  } : r;
  Ni(() => {
    if (!o && !a || !t) {
      s.current = !1;
      return;
    }
    if (s.current || !n)
      return;
    const c = t == null ? void 0 : t.node.current;
    if (!c || c.isConnected === !1)
      return;
    const u = e(c), d = zE(u, n);
    if (o || (d.x = 0), a || (d.y = 0), s.current = !0, Math.abs(d.x) > 0 || Math.abs(d.y) > 0) {
      const f = jE(c);
      f && f.scrollBy({
        top: d.y,
        left: d.x
      });
    }
  }, [t, o, a, n, e]);
}
const wh = /* @__PURE__ */ dc({
  ...er,
  scaleX: 1,
  scaleY: 1
});
var Qr;
(function(i) {
  i[i.Uninitialized = 0] = "Uninitialized", i[i.Initializing = 1] = "Initializing", i[i.Initialized = 2] = "Initialized";
})(Qr || (Qr = {}));
const yh = /* @__PURE__ */ Qy(function(t) {
  var e, n, r, s;
  let {
    id: o,
    accessibility: a,
    autoScroll: l = !0,
    children: c,
    sensors: u = GY,
    collisionDetection: d = cY,
    measuring: f,
    modifiers: m,
    ...g
  } = t;
  const p = Sg(JY, void 0, YY), [b, v] = p, [y, x] = tY(), [S, k] = Ne(Qr.Uninitialized), I = S === Qr.Initialized, {
    draggable: {
      active: A,
      nodes: L,
      translate: R
    },
    droppable: {
      containers: O
    }
  } = b, N = A ? L.get(A) : null, le = at({
    initial: null,
    translated: null
  }), te = bt(() => {
    var it;
    return A != null ? {
      id: A,
      // It's possible for the active node to unmount while dragging
      data: (it = N == null ? void 0 : N.data) != null ? it : KY,
      rect: le
    } : null;
  }, [A, N]), me = at(null), [ce, Ae] = Ne(null), [Fe, be] = Ne(null), re = ic(g, Object.values(g)), B = Sc("DndDescribedBy", o), X = bt(() => O.getEnabled(), [O]), he = QY(f), {
    droppableRects: Q,
    measureDroppableContainers: Y,
    measuringScheduled: we
  } = LY(X, {
    dragging: I,
    dependencies: [R.x, R.y],
    config: he.droppable
  }), ye = MY(L, A), Ce = bt(() => Fe ? wd(Fe) : null, [Fe]), J = Jt(), F = NY(ye, he.draggable.measure);
  eJ({
    activeNode: A ? L.get(A) : null,
    config: J.layoutShiftCompensation,
    initialRect: F,
    measure: he.draggable.measure
  });
  const H = Ry(ye, he.draggable.measure, F), pe = Ry(ye ? ye.parentElement : null), W = at({
    activatorEvent: null,
    active: null,
    activeNode: ye,
    collisionRect: null,
    collisions: null,
    droppableRects: Q,
    draggableNodes: L,
    draggingNode: null,
    draggingNodeRect: null,
    droppableContainers: O,
    over: null,
    scrollableAncestors: [],
    scrollAdjustedTranslate: null
  }), We = O.getNodeFor((e = W.current.over) == null ? void 0 : e.id), Ye = qY({
    measure: he.dragOverlay.measure
  }), Ze = (n = Ye.nodeRef.current) != null ? n : ye, Je = I ? (r = Ye.rect) != null ? r : H : null, pt = !!(Ye.nodeRef.current && Ye.rect), Rn = $Y(pt ? null : H), rn = JE(Ze ? ei(Ze) : null), $t = zY(I ? We ?? ye : null), zt = UY($t), bn = t5(m, {
    transform: {
      x: R.x - Rn.x,
      y: R.y - Rn.y,
      scaleX: 1,
      scaleY: 1
    },
    activatorEvent: Fe,
    active: te,
    activeNodeRect: H,
    containerNodeRect: pe,
    draggingNodeRect: Je,
    over: W.current.over,
    overlayNodeRect: Ye.rect,
    scrollableAncestors: $t,
    scrollableAncestorRects: zt,
    windowRect: rn
  }), On = Ce ? Go(Ce, R) : null, Vt = HY($t), Ht = Vy(Vt), hi = Vy(Vt, [H]), jt = Go(bn, Ht), Wt = Je ? hY(Je, bn) : null, Yt = te && Wt ? d({
    active: te,
    collisionRect: Wt,
    droppableRects: Q,
    droppableContainers: X,
    pointerCoordinates: On
  }) : null, ti = $E(Yt, "id"), [sn, un] = Ne(null), Le = pt ? bn : Go(bn, hi), U = uY(Le, (s = sn == null ? void 0 : sn.rect) != null ? s : null, H), _e = $n(
    (it, rt) => {
      let {
        sensor: dt,
        options: Mt
      } = rt;
      if (me.current == null)
        return;
      const Dt = L.get(me.current);
      if (!Dt)
        return;
      const Lt = it.nativeEvent, Nt = new dt({
        active: me.current,
        activeNode: Dt,
        event: Lt,
        options: Mt,
        // Sensors need to be instantiated with refs for arguments that change over time
        // otherwise they are frozen in time with the stale arguments
        context: W,
        onStart(Pt) {
          const vn = me.current;
          if (vn == null)
            return;
          const wn = L.get(vn);
          if (!wn)
            return;
          const {
            onDragStart: yn
          } = re.current, ni = {
            active: {
              id: vn,
              data: wn.data,
              rect: le
            }
          };
          Oa(() => {
            yn == null || yn(ni), k(Qr.Initializing), v({
              type: mn.DragStart,
              initialCoordinates: Pt,
              active: vn
            }), y({
              type: "onDragStart",
              event: ni
            });
          });
        },
        onMove(Pt) {
          v({
            type: mn.DragMove,
            coordinates: Pt
          });
        },
        onEnd: on(mn.DragEnd),
        onCancel: on(mn.DragCancel)
      });
      Oa(() => {
        Ae(Nt), be(it.nativeEvent);
      });
      function on(Pt) {
        return async function() {
          const {
            active: wn,
            collisions: yn,
            over: ni,
            scrollAdjustedTranslate: zi
          } = W.current;
          let ii = null;
          if (wn && zi) {
            const {
              cancelDrop: qn
            } = re.current;
            ii = {
              activatorEvent: Lt,
              active: wn,
              collisions: yn,
              delta: zi,
              over: ni
            }, Pt === mn.DragEnd && typeof qn == "function" && await Promise.resolve(qn(ii)) && (Pt = mn.DragCancel);
          }
          me.current = null, Oa(() => {
            v({
              type: Pt
            }), k(Qr.Uninitialized), un(null), Ae(null), be(null);
            const qn = Pt === mn.DragEnd ? "onDragEnd" : "onDragCancel";
            if (ii) {
              const Si = re.current[qn];
              Si == null || Si(ii), y({
                type: qn,
                event: ii
              });
            }
          });
        };
      }
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [L]
  ), Ue = $n((it, rt) => (dt, Mt) => {
    const Dt = dt.nativeEvent, Lt = L.get(Mt);
    if (
      // Another sensor is already instantiating
      me.current !== null || // No active draggable
      !Lt || // Event has already been captured
      Dt.dndKit || Dt.defaultPrevented
    )
      return;
    const Nt = {
      active: Lt
    };
    it(dt, rt.options, Nt) === !0 && (Dt.dndKit = {
      capturedBy: rt.sensor
    }, me.current = Mt, _e(dt, rt));
  }, [L, _e]), $e = DY(u, Ue);
  jY(u), Ni(() => {
    H && S === Qr.Initializing && k(Qr.Initialized);
  }, [H, S]), Ee(
    () => {
      const {
        onDragMove: it
      } = re.current, {
        active: rt,
        activatorEvent: dt,
        collisions: Mt,
        over: Dt
      } = W.current;
      if (!rt || !dt)
        return;
      const Lt = {
        active: rt,
        activatorEvent: dt,
        collisions: Mt,
        delta: {
          x: jt.x,
          y: jt.y
        },
        over: Dt
      };
      Oa(() => {
        it == null || it(Lt), y({
          type: "onDragMove",
          event: Lt
        });
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [jt.x, jt.y]
  ), Ee(
    () => {
      const {
        active: it,
        activatorEvent: rt,
        collisions: dt,
        droppableContainers: Mt,
        scrollAdjustedTranslate: Dt
      } = W.current;
      if (!it || me.current == null || !rt || !Dt)
        return;
      const {
        onDragOver: Lt
      } = re.current, Nt = Mt.get(ti), on = Nt && Nt.rect.current ? {
        id: Nt.id,
        rect: Nt.rect.current,
        data: Nt.data,
        disabled: Nt.disabled
      } : null, Pt = {
        active: it,
        activatorEvent: rt,
        collisions: dt,
        delta: {
          x: Dt.x,
          y: Dt.y
        },
        over: on
      };
      Oa(() => {
        un(on), Lt == null || Lt(Pt), y({
          type: "onDragOver",
          event: Pt
        });
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [ti]
  ), Ni(() => {
    W.current = {
      activatorEvent: Fe,
      active: te,
      activeNode: ye,
      collisionRect: Wt,
      collisions: Yt,
      droppableRects: Q,
      draggableNodes: L,
      draggingNode: Ze,
      draggingNodeRect: Je,
      droppableContainers: O,
      over: sn,
      scrollableAncestors: $t,
      scrollAdjustedTranslate: jt
    }, le.current = {
      initial: Je,
      translated: Wt
    };
  }, [te, ye, Yt, Wt, L, Ze, Je, Q, O, sn, $t, jt]), RY({
    ...J,
    delta: R,
    draggingRect: Wt,
    pointerCoordinates: On,
    scrollableAncestors: $t,
    scrollableAncestorRects: zt
  });
  const ze = bt(() => ({
    active: te,
    activeNode: ye,
    activeNodeRect: H,
    activatorEvent: Fe,
    collisions: Yt,
    containerNodeRect: pe,
    dragOverlay: Ye,
    draggableNodes: L,
    droppableContainers: O,
    droppableRects: Q,
    over: sn,
    measureDroppableContainers: Y,
    scrollableAncestors: $t,
    scrollableAncestorRects: zt,
    measuringConfiguration: he,
    measuringScheduled: we,
    windowRect: rn
  }), [te, ye, H, Fe, Yt, pe, Ye, L, O, Q, sn, Y, $t, zt, he, we, rn]), ft = bt(() => ({
    activatorEvent: Fe,
    activators: $e,
    active: te,
    activeNodeRect: H,
    ariaDescribedById: {
      draggable: B
    },
    dispatch: v,
    draggableNodes: L,
    over: sn,
    measureDroppableContainers: Y
  }), [Fe, $e, te, H, v, B, L, sn, Y]);
  return Se.createElement(NE.Provider, {
    value: x
  }, Se.createElement(Ac.Provider, {
    value: ft
  }, Se.createElement(e5.Provider, {
    value: ze
  }, Se.createElement(wh.Provider, {
    value: U
  }, c)), Se.createElement(XY, {
    disabled: (a == null ? void 0 : a.restoreFocus) === !1
  })), Se.createElement(rY, {
    ...a,
    hiddenTextDescribedById: B
  }));
  function Jt() {
    const it = (ce == null ? void 0 : ce.autoScrollEnabled) === !1, rt = typeof l == "object" ? l.enabled === !1 : l === !1, dt = I && !it && !rt;
    return typeof l == "object" ? {
      ...l,
      enabled: dt
    } : {
      enabled: dt
    };
  }
}), tJ = /* @__PURE__ */ dc(null), My = "button", nJ = "Droppable";
function n5(i) {
  let {
    id: t,
    data: e,
    disabled: n = !1,
    attributes: r
  } = i;
  const s = Sc(nJ), {
    activators: o,
    activatorEvent: a,
    active: l,
    activeNodeRect: c,
    ariaDescribedById: u,
    draggableNodes: d,
    over: f
  } = hr(Ac), {
    role: m = My,
    roleDescription: g = "draggable",
    tabIndex: p = 0
  } = r ?? {}, b = (l == null ? void 0 : l.id) === t, v = hr(b ? wh : tJ), [y, x] = bd(), [S, k] = bd(), I = WY(o, t), A = ic(e);
  Ni(
    () => (d.set(t, {
      id: t,
      key: s,
      node: y,
      activatorNode: S,
      data: A
    }), () => {
      const R = d.get(t);
      R && R.key === s && d.delete(t);
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [d, t]
  );
  const L = bt(() => ({
    role: m,
    tabIndex: p,
    "aria-disabled": n,
    "aria-pressed": b && m === My ? !0 : void 0,
    "aria-roledescription": g,
    "aria-describedby": u.draggable
  }), [n, m, p, b, g, u.draggable]);
  return {
    active: l,
    activatorEvent: a,
    activeNodeRect: c,
    attributes: L,
    isDragging: b,
    listeners: n ? void 0 : I,
    node: y,
    over: f,
    setNodeRef: x,
    setActivatorNodeRef: k,
    transform: v
  };
}
function i5() {
  return hr(e5);
}
const iJ = "Droppable", rJ = {
  timeout: 25
};
function r5(i) {
  let {
    data: t,
    disabled: e = !1,
    id: n,
    resizeObserverConfig: r
  } = i;
  const s = Sc(iJ), {
    active: o,
    dispatch: a,
    over: l,
    measureDroppableContainers: c
  } = hr(Ac), u = at({
    disabled: e
  }), d = at(!1), f = at(null), m = at(null), {
    disabled: g,
    updateMeasurementsFor: p,
    timeout: b
  } = {
    ...rJ,
    ...r
  }, v = ic(p ?? n), y = $n(
    () => {
      if (!d.current) {
        d.current = !0;
        return;
      }
      m.current != null && clearTimeout(m.current), m.current = setTimeout(() => {
        c(Array.isArray(v.current) ? v.current : [v.current]), m.current = null;
      }, b);
    },
    //eslint-disable-next-line react-hooks/exhaustive-deps
    [b]
  ), x = vh({
    callback: y,
    disabled: g || !o
  }), S = $n((L, R) => {
    x && (R && (x.unobserve(R), d.current = !1), L && x.observe(L));
  }, [x]), [k, I] = bd(S), A = ic(t);
  return Ee(() => {
    !x || !k.current || (x.disconnect(), d.current = !1, x.observe(k.current));
  }, [k, x]), Ni(
    () => (a({
      type: mn.RegisterDroppable,
      element: {
        id: n,
        key: s,
        disabled: e,
        node: k,
        rect: f,
        data: A
      }
    }), () => a({
      type: mn.UnregisterDroppable,
      key: s,
      id: n
    })),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [n]
  ), Ee(() => {
    e !== u.current.disabled && (a({
      type: mn.SetDroppableDisabled,
      id: n,
      key: s,
      disabled: e
    }), u.current.disabled = e);
  }, [n, s, e, a]), {
    active: o,
    rect: f,
    isOver: (l == null ? void 0 : l.id) === n,
    node: k,
    over: l,
    setNodeRef: I
  };
}
function sJ(i) {
  let {
    animation: t,
    children: e
  } = i;
  const [n, r] = Ne(null), [s, o] = Ne(null), a = vd(e);
  return !e && !n && a && r(a), Ni(() => {
    if (!s)
      return;
    const l = n == null ? void 0 : n.key, c = n == null ? void 0 : n.props.id;
    if (l == null || c == null) {
      r(null);
      return;
    }
    Promise.resolve(t(c, s)).then(() => {
      r(null);
    });
  }, [t, n, s]), Se.createElement(Se.Fragment, null, e, n ? S5(n, {
    ref: o
  }) : null);
}
const oJ = {
  x: 0,
  y: 0,
  scaleX: 1,
  scaleY: 1
};
function aJ(i) {
  let {
    children: t
  } = i;
  return Se.createElement(Ac.Provider, {
    value: QE
  }, Se.createElement(wh.Provider, {
    value: oJ
  }, t));
}
const lJ = {
  position: "fixed",
  touchAction: "none"
}, cJ = (i) => mh(i) ? "transform 250ms ease" : void 0, uJ = /* @__PURE__ */ Tg((i, t) => {
  let {
    as: e,
    activatorEvent: n,
    adjustScale: r,
    children: s,
    className: o,
    rect: a,
    style: l,
    transform: c,
    transition: u = cJ
  } = i;
  if (!a)
    return null;
  const d = r ? c : {
    ...c,
    scaleX: 1,
    scaleY: 1
  }, f = {
    ...lJ,
    width: a.width,
    height: a.height,
    top: a.top,
    left: a.left,
    transform: Bi.Transform.toString(d),
    transformOrigin: r && n ? sY(n, a) : void 0,
    transition: typeof u == "function" ? u(n) : u,
    ...l
  };
  return Se.createElement(e, {
    className: o,
    style: f,
    ref: t
  }, s);
}), dJ = (i) => (t) => {
  let {
    active: e,
    dragOverlay: n
  } = t;
  const r = {}, {
    styles: s,
    className: o
  } = i;
  if (s != null && s.active)
    for (const [a, l] of Object.entries(s.active))
      l !== void 0 && (r[a] = e.node.style.getPropertyValue(a), e.node.style.setProperty(a, l));
  if (s != null && s.dragOverlay)
    for (const [a, l] of Object.entries(s.dragOverlay))
      l !== void 0 && n.node.style.setProperty(a, l);
  return o != null && o.active && e.node.classList.add(o.active), o != null && o.dragOverlay && n.node.classList.add(o.dragOverlay), function() {
    for (const [l, c] of Object.entries(r))
      e.node.style.setProperty(l, c);
    o != null && o.active && e.node.classList.remove(o.active);
  };
}, hJ = (i) => {
  let {
    transform: {
      initial: t,
      final: e
    }
  } = i;
  return [{
    transform: Bi.Transform.toString(t)
  }, {
    transform: Bi.Transform.toString(e)
  }];
}, fJ = {
  duration: 250,
  easing: "ease",
  keyframes: hJ,
  sideEffects: /* @__PURE__ */ dJ({
    styles: {
      active: {
        opacity: "0"
      }
    }
  })
};
function mJ(i) {
  let {
    config: t,
    draggableNodes: e,
    droppableContainers: n,
    measuringConfiguration: r
  } = i;
  return fh((s, o) => {
    if (t === null)
      return;
    const a = e.get(s);
    if (!a)
      return;
    const l = a.node.current;
    if (!l)
      return;
    const c = XE(o);
    if (!c)
      return;
    const {
      transform: u
    } = ei(o).getComputedStyle(o), d = HE(u);
    if (!d)
      return;
    const f = typeof t == "function" ? t : gJ(t);
    return ZE(l, r.draggable.measure), f({
      active: {
        id: s,
        data: a.data,
        node: l,
        rect: r.draggable.measure(l)
      },
      draggableNodes: e,
      dragOverlay: {
        node: o,
        rect: r.dragOverlay.measure(c)
      },
      droppableContainers: n,
      measuringConfiguration: r,
      transform: d
    });
  });
}
function gJ(i) {
  const {
    duration: t,
    easing: e,
    sideEffects: n,
    keyframes: r
  } = {
    ...fJ,
    ...i
  };
  return (s) => {
    let {
      active: o,
      dragOverlay: a,
      transform: l,
      ...c
    } = s;
    if (!t)
      return;
    const u = {
      x: a.rect.left - o.rect.left,
      y: a.rect.top - o.rect.top
    }, d = {
      scaleX: l.scaleX !== 1 ? o.rect.width * l.scaleX / a.rect.width : 1,
      scaleY: l.scaleY !== 1 ? o.rect.height * l.scaleY / a.rect.height : 1
    }, f = {
      x: l.x - u.x,
      y: l.y - u.y,
      ...d
    }, m = r({
      ...c,
      active: o,
      dragOverlay: a,
      transform: {
        initial: l,
        final: f
      }
    }), [g] = m, p = m[m.length - 1];
    if (JSON.stringify(g) === JSON.stringify(p))
      return;
    const b = n == null ? void 0 : n({
      active: o,
      dragOverlay: a,
      ...c
    }), v = a.node.animate(m, {
      duration: t,
      easing: e,
      fill: "forwards"
    });
    return new Promise((y) => {
      v.onfinish = () => {
        b == null || b(), y();
      };
    });
  };
}
let Dy = 0;
function pJ(i) {
  return bt(() => {
    if (i != null)
      return Dy++, Dy;
  }, [i]);
}
const bJ = /* @__PURE__ */ Se.memo((i) => {
  let {
    adjustScale: t = !1,
    children: e,
    dropAnimation: n,
    style: r,
    transition: s,
    modifiers: o,
    wrapperElement: a = "div",
    className: l,
    zIndex: c = 999
  } = i;
  const {
    activatorEvent: u,
    active: d,
    activeNodeRect: f,
    containerNodeRect: m,
    draggableNodes: g,
    droppableContainers: p,
    dragOverlay: b,
    over: v,
    measuringConfiguration: y,
    scrollableAncestors: x,
    scrollableAncestorRects: S,
    windowRect: k
  } = i5(), I = hr(wh), A = pJ(d == null ? void 0 : d.id), L = t5(o, {
    activatorEvent: u,
    active: d,
    activeNodeRect: f,
    containerNodeRect: m,
    draggingNodeRect: b.rect,
    over: v,
    overlayNodeRect: b.rect,
    scrollableAncestors: x,
    scrollableAncestorRects: S,
    transform: I,
    windowRect: k
  }), R = rb(f), O = mJ({
    config: n,
    draggableNodes: g,
    droppableContainers: p,
    measuringConfiguration: y
  }), N = R ? b.setRef : void 0;
  return Se.createElement(aJ, null, Se.createElement(sJ, {
    animation: O
  }, d && A ? Se.createElement(uJ, {
    key: A,
    id: d.id,
    ref: N,
    as: a,
    activatorEvent: u,
    adjustScale: t,
    className: l,
    transition: s,
    rect: R,
    style: {
      zIndex: c,
      ...r
    },
    transform: L
  }, e) : null));
});
function as(i, t, e) {
  const n = i.slice();
  return n.splice(e < 0 ? n.length + e : e, 0, n.splice(t, 1)[0]), n;
}
function vJ(i, t) {
  return i.reduce((e, n, r) => {
    const s = t.get(n);
    return s && (e[r] = s), e;
  }, Array(i.length));
}
function au(i) {
  return i !== null && i >= 0;
}
function wJ(i, t) {
  if (i === t)
    return !0;
  if (i.length !== t.length)
    return !1;
  for (let e = 0; e < i.length; e++)
    if (i[e] !== t[e])
      return !1;
  return !0;
}
function yJ(i) {
  return typeof i == "boolean" ? {
    draggable: i,
    droppable: i
  } : i;
}
const s5 = (i) => {
  let {
    rects: t,
    activeIndex: e,
    overIndex: n,
    index: r
  } = i;
  const s = as(t, n, e), o = t[r], a = s[r];
  return !a || !o ? null : {
    x: a.left - o.left,
    y: a.top - o.top,
    scaleX: a.width / o.width,
    scaleY: a.height / o.height
  };
}, _J = (i) => {
  let {
    activeIndex: t,
    index: e,
    rects: n,
    overIndex: r
  } = i, s, o;
  return e === t && (s = n[e], o = n[r]), e === r && (s = n[e], o = n[t]), !o || !s ? null : {
    x: o.left - s.left,
    y: o.top - s.top,
    scaleX: o.width / s.width,
    scaleY: o.height / s.height
  };
}, lu = {
  scaleX: 1,
  scaleY: 1
}, sb = (i) => {
  var t;
  let {
    activeIndex: e,
    activeNodeRect: n,
    index: r,
    rects: s,
    overIndex: o
  } = i;
  const a = (t = s[e]) != null ? t : n;
  if (!a)
    return null;
  if (r === e) {
    const c = s[o];
    return c ? {
      x: 0,
      y: e < o ? c.top + c.height - (a.top + a.height) : c.top - a.top,
      ...lu
    } : null;
  }
  const l = CJ(s, r, e);
  return r > e && r <= o ? {
    x: 0,
    y: -a.height - l,
    ...lu
  } : r < e && r >= o ? {
    x: 0,
    y: a.height + l,
    ...lu
  } : {
    x: 0,
    y: 0,
    ...lu
  };
};
function CJ(i, t, e) {
  const n = i[t], r = i[t - 1], s = i[t + 1];
  return n ? e < t ? r ? n.top - (r.top + r.height) : s ? s.top - (n.top + n.height) : 0 : s ? s.top - (n.top + n.height) : r ? n.top - (r.top + r.height) : 0 : 0;
}
const o5 = "Sortable", a5 = /* @__PURE__ */ Se.createContext({
  activeIndex: -1,
  containerId: o5,
  disableTransforms: !1,
  items: [],
  overIndex: -1,
  useDragOverlay: !1,
  sortedRects: [],
  strategy: s5,
  disabled: {
    draggable: !1,
    droppable: !1
  }
});
function _h(i) {
  let {
    children: t,
    id: e,
    items: n,
    strategy: r = s5,
    disabled: s = !1
  } = i;
  const {
    active: o,
    dragOverlay: a,
    droppableRects: l,
    over: c,
    measureDroppableContainers: u
  } = i5(), d = Sc(o5, e), f = a.rect !== null, m = bt(() => n.map((I) => typeof I == "object" && "id" in I ? I.id : I), [n]), g = o != null, p = o ? m.indexOf(o.id) : -1, b = c ? m.indexOf(c.id) : -1, v = at(m), y = !wJ(m, v.current), x = b !== -1 && p === -1 || y, S = yJ(s);
  Ni(() => {
    y && g && u(m);
  }, [y, m, g, u]), Ee(() => {
    v.current = m;
  }, [m]);
  const k = bt(
    () => ({
      activeIndex: p,
      containerId: d,
      disabled: S,
      disableTransforms: x,
      items: m,
      overIndex: b,
      useDragOverlay: f,
      sortedRects: vJ(m, l),
      strategy: r
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [p, d, S.draggable, S.droppable, x, m, b, l, f, r]
  );
  return Se.createElement(a5.Provider, {
    value: k
  }, t);
}
const kJ = (i) => {
  let {
    id: t,
    items: e,
    activeIndex: n,
    overIndex: r
  } = i;
  return as(e, n, r).indexOf(t);
}, xJ = (i) => {
  let {
    containerId: t,
    isSorting: e,
    wasDragging: n,
    index: r,
    items: s,
    newIndex: o,
    previousItems: a,
    previousContainerId: l,
    transition: c
  } = i;
  return !c || !n || a !== s && r === o ? !1 : e ? !0 : o !== r && t === l;
}, EJ = {
  duration: 200,
  easing: "ease"
}, l5 = "transform", TJ = /* @__PURE__ */ Bi.Transition.toString({
  property: l5,
  duration: 0,
  easing: "linear"
}), SJ = {
  roleDescription: "sortable"
};
function AJ(i) {
  let {
    disabled: t,
    index: e,
    node: n,
    rect: r
  } = i;
  const [s, o] = Ne(null), a = at(e);
  return Ni(() => {
    if (!t && e !== a.current && n.current) {
      const l = r.current;
      if (l) {
        const c = ka(n.current, {
          ignoreTransform: !0
        }), u = {
          x: l.left - c.left,
          y: l.top - c.top,
          scaleX: l.width / c.width,
          scaleY: l.height / c.height
        };
        (u.x || u.y) && o(u);
      }
    }
    e !== a.current && (a.current = e);
  }, [t, e, n, r]), Ee(() => {
    s && o(null);
  }, [s]), s;
}
function Ch(i) {
  let {
    animateLayoutChanges: t = xJ,
    attributes: e,
    disabled: n,
    data: r,
    getNewIndex: s = kJ,
    id: o,
    strategy: a,
    resizeObserverConfig: l,
    transition: c = EJ
  } = i;
  const {
    items: u,
    containerId: d,
    activeIndex: f,
    disabled: m,
    disableTransforms: g,
    sortedRects: p,
    overIndex: b,
    useDragOverlay: v,
    strategy: y
  } = hr(a5), x = PJ(n, m), S = u.indexOf(o), k = bt(() => ({
    sortable: {
      containerId: d,
      index: S,
      items: u
    },
    ...r
  }), [d, r, S, u]), I = bt(() => u.slice(u.indexOf(o)), [u, o]), {
    rect: A,
    node: L,
    isOver: R,
    setNodeRef: O
  } = r5({
    id: o,
    data: k,
    disabled: x.droppable,
    resizeObserverConfig: {
      updateMeasurementsFor: I,
      ...l
    }
  }), {
    active: N,
    activatorEvent: le,
    activeNodeRect: te,
    attributes: me,
    setNodeRef: ce,
    listeners: Ae,
    isDragging: Fe,
    over: be,
    setActivatorNodeRef: re,
    transform: B
  } = n5({
    id: o,
    data: k,
    attributes: {
      ...SJ,
      ...e
    },
    disabled: x.draggable
  }), X = UZ(O, ce), he = !!N, Q = he && !g && au(f) && au(b), Y = !v && Fe, we = Y && Q ? B : null, Ce = Q ? we ?? (a ?? y)({
    rects: p,
    activeNodeRect: te,
    activeIndex: f,
    overIndex: b,
    index: S
  }) : null, J = au(f) && au(b) ? s({
    id: o,
    items: u,
    activeIndex: f,
    overIndex: b
  }) : S, F = N == null ? void 0 : N.id, H = at({
    activeId: F,
    items: u,
    newIndex: J,
    containerId: d
  }), pe = u !== H.current.items, W = t({
    active: N,
    containerId: d,
    isDragging: Fe,
    isSorting: he,
    id: o,
    index: S,
    items: u,
    newIndex: H.current.newIndex,
    previousItems: H.current.items,
    previousContainerId: H.current.containerId,
    transition: c,
    wasDragging: H.current.activeId != null
  }), We = AJ({
    disabled: !W,
    index: S,
    node: L,
    rect: A
  });
  return Ee(() => {
    he && H.current.newIndex !== J && (H.current.newIndex = J), d !== H.current.containerId && (H.current.containerId = d), u !== H.current.items && (H.current.items = u);
  }, [he, J, d, u]), Ee(() => {
    if (F === H.current.activeId)
      return;
    if (F && !H.current.activeId) {
      H.current.activeId = F;
      return;
    }
    const Ze = setTimeout(() => {
      H.current.activeId = F;
    }, 50);
    return () => clearTimeout(Ze);
  }, [F]), {
    active: N,
    activeIndex: f,
    attributes: me,
    data: k,
    rect: A,
    index: S,
    newIndex: J,
    items: u,
    isOver: R,
    isSorting: he,
    isDragging: Fe,
    listeners: Ae,
    node: L,
    overIndex: b,
    over: be,
    setNodeRef: X,
    setActivatorNodeRef: re,
    setDroppableNodeRef: O,
    setDraggableNodeRef: ce,
    transform: We ?? Ce,
    transition: Ye()
  };
  function Ye() {
    if (
      // Temporarily disable transitions for a single frame to set up derived transforms
      We || // Or to prevent items jumping to back to their "new" position when items change
      pe && H.current.newIndex === S
    )
      return TJ;
    if (!(Y && !mh(le) || !c) && (he || W))
      return Bi.Transition.toString({
        ...c,
        property: l5
      });
  }
}
function PJ(i, t) {
  var e, n;
  return typeof i == "boolean" ? {
    draggable: i,
    // Backwards compatibility
    droppable: !1
  } : {
    draggable: (e = i == null ? void 0 : i.draggable) != null ? e : t.draggable,
    droppable: (n = i == null ? void 0 : i.droppable) != null ? n : t.droppable
  };
}
function Cd(i) {
  if (!i)
    return !1;
  const t = i.data.current;
  return !!(t && "sortable" in t && typeof t.sortable == "object" && "containerId" in t.sortable && "items" in t.sortable && "index" in t.sortable);
}
const IJ = [Qe.Down, Qe.Right, Qe.Up, Qe.Left], kh = (i, t) => {
  let {
    context: {
      active: e,
      collisionRect: n,
      droppableRects: r,
      droppableContainers: s,
      over: o,
      scrollableAncestors: a
    }
  } = t;
  if (IJ.includes(i.code)) {
    if (i.preventDefault(), !e || !n)
      return;
    const l = [];
    s.getEnabled().forEach((d) => {
      if (!d || d != null && d.disabled)
        return;
      const f = r.get(d.id);
      if (f)
        switch (i.code) {
          case Qe.Down:
            n.top < f.top && l.push(d);
            break;
          case Qe.Up:
            n.top > f.top && l.push(d);
            break;
          case Qe.Left:
            n.left > f.left && l.push(d);
            break;
          case Qe.Right:
            n.left < f.left && l.push(d);
            break;
        }
    });
    const c = aY({
      active: e,
      collisionRect: n,
      droppableRects: r,
      droppableContainers: l,
      pointerCoordinates: null
    });
    let u = $E(c, "id");
    if (u === (o == null ? void 0 : o.id) && c.length > 1 && (u = c[1].id), u != null) {
      const d = s.get(e.id), f = s.get(u), m = f ? r.get(f.id) : null, g = f == null ? void 0 : f.node.current;
      if (g && m && d && f) {
        const b = bh(g).some((I, A) => a[A] !== I), v = c5(d, f), y = RJ(d, f), x = b || !v ? {
          x: 0,
          y: 0
        } : {
          x: y ? n.width - m.width : 0,
          y: y ? n.height - m.height : 0
        }, S = {
          x: m.left,
          y: m.top
        };
        return x.x && x.y ? S : rc(S, x);
      }
    }
  }
};
function c5(i, t) {
  return !Cd(i) || !Cd(t) ? !1 : i.data.current.sortable.containerId === t.data.current.sortable.containerId;
}
function RJ(i, t) {
  return !Cd(i) || !Cd(t) || !c5(i, t) ? !1 : i.data.current.sortable.index < t.data.current.sortable.index;
}
function _l() {
  return _l = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, _l.apply(this, arguments);
}
function oc(i) {
  "@babel/helpers - typeof";
  return oc = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, oc(i);
}
function OJ(i, t) {
  if (oc(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (oc(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function VJ(i) {
  var t = OJ(i, "string");
  return oc(t) === "symbol" ? t : String(t);
}
function Or(i, t, e) {
  return t = VJ(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
var MJ = {
  root: function(t) {
    var e = t.props;
    return Re("p-badge p-component", Or({
      "p-badge-no-gutter": z.isNotEmpty(e.value) && String(e.value).length === 1,
      "p-badge-dot": z.isEmpty(e.value),
      "p-badge-lg": e.size === "large",
      "p-badge-xl": e.size === "xlarge"
    }, "p-badge-".concat(e.severity), e.severity !== null));
  }
}, DJ = `
@layer primereact {
    .p-badge {
        display: inline-block;
        border-radius: 10px;
        text-align: center;
        padding: 0 .5rem;
    }
    
    .p-overlay-badge {
        position: relative;
    }
    
    .p-overlay-badge .p-badge {
        position: absolute;
        top: 0;
        right: 0;
        transform: translate(50%,-50%);
        transform-origin: 100% 0;
        margin: 0;
    }
    
    .p-badge-dot {
        width: .5rem;
        min-width: .5rem;
        height: .5rem;
        border-radius: 50%;
        padding: 0;
    }
    
    .p-badge-no-gutter {
        padding: 0;
        border-radius: 50%;
    }
}
`, cu = et.extend({
  defaultProps: {
    __TYPE: "Badge",
    __parentMetadata: null,
    value: null,
    severity: null,
    size: null,
    style: null,
    className: null,
    children: void 0
  },
  css: {
    classes: MJ,
    styles: DJ
  }
});
function Ly(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function LJ(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Ly(Object(e), !0).forEach(function(n) {
      Or(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : Ly(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var u5 = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = cu.getProps(i, n), s = cu.setMetaData(LJ({
    props: r
  }, r.__parentMetadata)), o = s.ptm, a = s.cx, l = s.isUnstyled;
  sr(cu.css.styles, l, {
    name: "badge"
  });
  var c = w.useRef(null);
  w.useImperativeHandle(t, function() {
    return {
      props: r,
      getElement: function() {
        return c.current;
      }
    };
  });
  var u = e({
    ref: c,
    style: r.style,
    className: Re(r.className, a("root"))
  }, cu.getOtherProps(r), o("root"));
  return /* @__PURE__ */ w.createElement("span", u, r.value);
}));
u5.displayName = "Badge";
var NJ = {
  icon: function(t) {
    var e = t.props;
    return Re("p-button-icon p-c", Or({}, "p-button-icon-".concat(e.iconPos), e.label));
  },
  loadingIcon: function(t) {
    var e = t.props, n = t.className;
    return Re(n, {
      "p-button-loading-icon": e.loading
    });
  },
  label: "p-button-label p-c",
  root: function(t) {
    var e = t.props, n = t.size, r = t.disabled;
    return Re("p-button p-component", Or(Or(Or(Or({
      "p-button-icon-only": (e.icon || e.loading) && !e.label && !e.children,
      "p-button-vertical": (e.iconPos === "top" || e.iconPos === "bottom") && e.label,
      "p-disabled": r,
      "p-button-loading": e.loading,
      "p-button-outlined": e.outlined,
      "p-button-raised": e.raised,
      "p-button-link": e.link,
      "p-button-text": e.text,
      "p-button-rounded": e.rounded,
      "p-button-loading-label-only": e.loading && !e.icon && e.label
    }, "p-button-loading-".concat(e.iconPos), e.loading && e.label), "p-button-".concat(n), n), "p-button-".concat(e.severity), e.severity), "p-button-plain", e.plain));
  }
}, uu = et.extend({
  defaultProps: {
    __TYPE: "Button",
    __parentMetadata: null,
    badge: null,
    badgeClassName: null,
    className: null,
    children: void 0,
    disabled: !1,
    icon: null,
    iconPos: "left",
    label: null,
    link: !1,
    loading: !1,
    loadingIcon: null,
    outlined: !1,
    plain: !1,
    raised: !1,
    rounded: !1,
    severity: null,
    size: null,
    text: !1,
    tooltip: null,
    tooltipOptions: null,
    visible: !0
  },
  css: {
    classes: NJ
  }
});
function Ny(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function Nf(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Ny(Object(e), !0).forEach(function(n) {
      Or(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : Ny(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var Mn = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = uu.getProps(i, n), s = r.disabled || r.loading, o = Nf(Nf({
    props: r
  }, r.__parentMetadata), {}, {
    context: {
      disabled: s
    }
  }), a = uu.setMetaData(o), l = a.ptm, c = a.cx, u = a.isUnstyled;
  sr(uu.css.styles, u, {
    name: "button",
    styled: !0
  });
  var d = w.useRef(t);
  if (w.useEffect(function() {
    z.combinedRefs(d, t);
  }, [d, t]), r.visible === !1)
    return null;
  var f = function() {
    var R = Re("p-button-icon p-c", Or({}, "p-button-icon-".concat(r.iconPos), r.label)), O = e({
      className: c("icon")
    }, l("icon"));
    R = Re(R, {
      "p-button-loading-icon": r.loading
    });
    var N = e({
      className: c("loadingIcon", {
        className: R
      })
    }, l("loadingIcon")), le = r.loading ? r.loadingIcon || /* @__PURE__ */ w.createElement(ch, _l({}, N, {
      spin: !0
    })) : r.icon;
    return Mi.getJSXIcon(le, Nf({}, O), {
      props: r
    });
  }, m = function() {
    var R = e({
      className: c("label")
    }, l("label"));
    return r.label ? /* @__PURE__ */ w.createElement("span", R, r.label) : !r.children && !r.label && /* @__PURE__ */ w.createElement("span", _l({}, R, {
      dangerouslySetInnerHTML: {
        __html: "&nbsp;"
      }
    }));
  }, g = function() {
    if (r.badge) {
      var R = e({
        className: Re(r.badgeClassName),
        value: r.badge,
        unstyled: r.unstyled,
        __parentMetadata: {
          parent: o
        }
      }, l("badge"));
      return /* @__PURE__ */ w.createElement(u5, R, r.badge);
    }
    return null;
  }, p = !s || r.tooltipOptions && r.tooltipOptions.showOnDisabled, b = z.isNotEmpty(r.tooltip) && p, v = {
    large: "lg",
    small: "sm"
  }, y = v[r.size], x = f(), S = m(), k = g(), I = r.label ? r.label + (r.badge ? " " + r.badge : "") : r["aria-label"], A = e({
    ref: d,
    "aria-label": I,
    "data-pc-autofocus": r.autoFocus,
    className: Re(r.className, c("root", {
      size: y,
      disabled: s
    })),
    disabled: s
  }, uu.getOtherProps(r), l("root"));
  return /* @__PURE__ */ w.createElement(w.Fragment, null, /* @__PURE__ */ w.createElement("button", A, x, S, r.children, k, /* @__PURE__ */ w.createElement(nc, null)), b && /* @__PURE__ */ w.createElement(ya, _l({
    target: d,
    content: r.tooltip,
    pt: l("tooltip")
  }, r.tooltipOptions)));
}));
Mn.displayName = "Button";
function Cl() {
  return Cl = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, Cl.apply(this, arguments);
}
function ac(i) {
  "@babel/helpers - typeof";
  return ac = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, ac(i);
}
function BJ(i, t) {
  if (ac(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (ac(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function FJ(i) {
  var t = BJ(i, "string");
  return ac(t) === "symbol" ? t : String(t);
}
function $J(i, t, e) {
  return t = FJ(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
var zJ = {
  root: function(t) {
    var e = t.props, n = t.context;
    return Re("p-radiobutton p-component", {
      "p-highlight": e.checked,
      "p-disabled": e.disabled,
      "p-invalid": e.invalid,
      "p-variant-filled": e.variant ? e.variant === "filled" : n && n.inputStyle === "filled"
    });
  },
  box: "p-radiobutton-box",
  input: "p-radiobutton-input",
  icon: "p-radiobutton-icon"
}, du = et.extend({
  defaultProps: {
    __TYPE: "RadioButton",
    autoFocus: !1,
    checked: !1,
    className: null,
    disabled: !1,
    id: null,
    inputId: null,
    inputRef: null,
    invalid: !1,
    variant: null,
    name: null,
    onChange: null,
    onClick: null,
    required: !1,
    style: null,
    tabIndex: null,
    tooltip: null,
    tooltipOptions: null,
    value: null,
    children: void 0
  },
  css: {
    classes: zJ
  }
});
function By(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function HJ(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? By(Object(e), !0).forEach(function(n) {
      $J(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : By(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var Nr = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = du.getProps(i, n), s = w.useRef(null), o = w.useRef(r.inputRef), a = du.setMetaData({
    props: r
  }), l = a.ptm, c = a.cx, u = a.isUnstyled;
  sr(du.css.styles, u, {
    name: "radiobutton"
  });
  var d = function(k) {
    f(k);
  }, f = function(k) {
    if (!(r.disabled || r.readonly) && r.onChange) {
      var I = r.checked, A = k.target instanceof HTMLDivElement, L = k.target === o.current, R = L && k.target.checked !== I, O = A && (j.hasClass(s.current, "p-radiobutton-checked") === I ? !I : !1), N = !I, le = {
        originalEvent: k,
        value: r.value,
        checked: N,
        stopPropagation: function() {
          k == null || k.stopPropagation();
        },
        preventDefault: function() {
          k == null || k.preventDefault();
        },
        target: {
          type: "radio",
          name: r.name,
          id: r.id,
          value: r.value,
          checked: N
        }
      };
      if (R || O) {
        var te;
        if (r == null || (te = r.onChange) === null || te === void 0 || te.call(r, le), k.defaultPrevented)
          return;
        O && (o.current.checked = N);
      }
      j.focus(o.current);
    }
  }, m = function(k) {
    var I;
    r == null || (I = r.onFocus) === null || I === void 0 || I.call(r, k);
  }, g = function(k) {
    var I;
    r == null || (I = r.onBlur) === null || I === void 0 || I.call(r, k);
  };
  w.useImperativeHandle(t, function() {
    return {
      props: r,
      select: d,
      focus: function() {
        return j.focus(o.current);
      },
      getElement: function() {
        return s.current;
      },
      getInput: function() {
        return o.current;
      }
    };
  }), w.useEffect(function() {
    o.current && (o.current.checked = r.checked);
  }, [r.checked]), w.useEffect(function() {
    z.combinedRefs(o, r.inputRef);
  }, [o, r.inputRef]), rr(function() {
    r.autoFocus && j.focus(o.current, r.autoFocus);
  });
  var p = z.isNotEmpty(r.tooltip), b = du.getOtherProps(r), v = e({
    id: r.id,
    className: Re(r.className, c("root", {
      context: n
    })),
    style: r.style,
    "data-p-checked": r.checked
  }, b, l("root"));
  delete v.input, delete v.box, delete v.icon;
  var y = function() {
    var k = z.reduceKeys(b, j.ARIA_PROPS), I = e(HJ({
      id: r.inputId,
      type: "radio",
      name: r.name,
      defaultChecked: r.checked,
      onFocus: m,
      onBlur: g,
      onChange: f,
      disabled: r.disabled,
      readOnly: r.readonly,
      required: r.required,
      tabIndex: r.tabIndex,
      className: c("input")
    }, k), i.input, l("input"));
    return /* @__PURE__ */ w.createElement("input", Cl({
      ref: o
    }, I));
  }, x = function() {
    var k = e({
      className: c("box")
    }, i.box, l("box")), I = e({
      className: c("icon")
    }, i.icon, l("icon"));
    return /* @__PURE__ */ w.createElement("div", k, /* @__PURE__ */ w.createElement("div", I));
  };
  return /* @__PURE__ */ w.createElement(w.Fragment, null, /* @__PURE__ */ w.createElement("div", Cl({
    ref: s
  }, v), y(), x()), p && /* @__PURE__ */ w.createElement(ya, Cl({
    target: s,
    content: r.tooltip,
    pt: l("tooltip")
  }, r.tooltipOptions)));
}));
Nr.displayName = "RadioButton";
const jJ = ({ option: i, onChange: t, index: e }) => {
  const [n, r] = Ne(i), s = (f) => {
    const m = {
      value: n.value,
      label: n.label,
      [f.target.name]: f.target.value
    };
    r(m), t(m);
  };
  Ee(() => {
    r(i);
  }, [i]);
  const {
    attributes: o,
    listeners: a,
    setNodeRef: l,
    setActivatorNodeRef: c,
    transform: u
  } = Ch({ id: e }), d = {
    transform: Bi.Transform.toString(u)
    // transition,
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-row gap-2", ref: l, style: d, ...o, children: [
    /* @__PURE__ */ _.jsx(
      Mn,
      {
        icon: "pi pi-sort",
        className: "px-0 border-noround border-0",
        style: { width: "1rem", color: "#E1E1E1", background: "transparent" },
        severity: "secondary",
        size: "small",
        ref: c,
        ...a
      }
    ),
    /* @__PURE__ */ _.jsxs("div", { className: "p-inputgroup flex-1", children: [
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "value",
          value: n.value,
          name: "value",
          onChange: s,
          className: "flex-1",
          placeholder: "Value"
        }
      ),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: n.label,
          name: "label",
          onChange: s,
          className: "flex-1",
          placeholder: "Label"
        }
      )
    ] }),
    /* @__PURE__ */ _.jsx(Mn, { icon: "pi pi-trash", onClick: () => t(null) })
  ] }) });
}, WJ = ({ onInsert: i }) => {
  const t = {
    value: "",
    label: ""
  }, [e, n] = Ne(t), r = (o) => {
    const a = {
      value: e.value,
      label: e.label,
      [o.target.name]: o.target.value
    };
    n(a);
  }, s = () => {
    i({ value: e.value, label: e.label }), n(t);
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-row gap-2", children: [
    /* @__PURE__ */ _.jsxs("div", { className: "p-inputgroup flex-1", children: [
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "value",
          value: e.value,
          name: "value",
          onChange: r,
          className: "",
          placeholder: "Value"
        }
      ),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: e.label,
          name: "label",
          onChange: r,
          className: "",
          placeholder: "Label"
        }
      )
    ] }),
    /* @__PURE__ */ _.jsx(
      Mn,
      {
        icon: "pi pi-plus",
        onClick: s
      }
    )
  ] }) });
}, UJ = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...os, ...i }), r = (l, c) => {
    const u = pn(e.options);
    l ? u[c] = l : u.splice(c, 1);
    const d = { ...e, options: u };
    n(d), t(d);
  }, s = (l) => {
    const c = pn(e.options);
    c.push(l);
    const u = { ...e, options: c };
    n(u), t(u);
  }, o = gh(
    gs(Ea),
    gs(xa, {
      coordinateGetter: kh
    })
  );
  function a(l) {
    const { active: c, over: u } = l;
    if (u && c.id !== u.id) {
      let d = function(g) {
        return parseInt(g.replace("option-", ""));
      };
      const f = as(e.options, d(u.id.toString()), d(c.id.toString())), m = { ...e, options: f };
      n(m), t(m);
    }
  }
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "form-item flex flex-column gap-4", children: [
    /* @__PURE__ */ _.jsx(
      yh,
      {
        sensors: o,
        collisionDetection: ph,
        onDragEnd: a,
        children: /* @__PURE__ */ _.jsx(
          _h,
          {
            items: e.options.map((l, c) => `option-${c}`),
            strategy: sb,
            children: e.options.map((l, c) => /* @__PURE__ */ _.jsx(
              jJ,
              {
                option: l,
                index: `option-${c}`,
                onChange: (u) => {
                  r(u, c);
                }
              },
              `option-${c}`
            ))
          }
        )
      }
    ),
    /* @__PURE__ */ _.jsx(
      WJ,
      {
        onInsert: s
      },
      "item-new"
    )
  ] }) });
}, qJ = ({ item: i, onChange: t }) => {
  const { config: e } = _r(), [n, r] = Ne({ ...os, ...i }), s = (o) => {
    const a = { ...n, [o.target.name]: o.target.value };
    r(a), t(a);
  };
  return Ee(() => {
    r({ ...os, ...i });
  }, [i]), /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs(or, { activeIndex: 0, children: [
    /* @__PURE__ */ _.jsx(di, { header: "General", children: /* @__PURE__ */ _.jsxs("div", { className: "form-item flex flex-column gap-4", children: [
      /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
        /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Label" }),
        /* @__PURE__ */ _.jsx(
          _t,
          {
            id: "label",
            value: n.label || "",
            name: "label",
            onChange: s,
            className: "w-full"
          }
        )
      ] }),
      /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
        /* @__PURE__ */ _.jsx("label", { htmlFor: "placeholder", children: "Placeholder" }),
        /* @__PURE__ */ _.jsx(
          _t,
          {
            id: "placeholder",
            value: n.placeholder || "",
            name: "placeholder",
            onChange: s,
            className: "w-full"
          }
        )
      ] })
    ] }) }, "general"),
    /* @__PURE__ */ _.jsxs(di, { header: "Options", children: [
      e.external_data.map((o) => /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
        /* @__PURE__ */ _.jsx(
          Nr,
          {
            inputId: o.key,
            name: "source",
            value: o.key,
            onChange: s,
            checked: n.source === o.key
          }
        ),
        /* @__PURE__ */ _.jsx("label", { htmlFor: o.key, className: "ml-2", children: o.label })
      ] }, o.key)),
      /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
        /* @__PURE__ */ _.jsx(
          Nr,
          {
            inputId: "local",
            name: "source",
            value: "local",
            onChange: s,
            checked: n.source === "local"
          }
        ),
        /* @__PURE__ */ _.jsx("label", { htmlFor: "local", className: "ml-2", children: "Custom" })
      ] }, "local"),
      /* @__PURE__ */ _.jsx("div", { className: "mt-4", children: n.source === "local" && /* @__PURE__ */ _.jsx(UJ, { item: n, onChange: t }) })
    ] }, "options")
  ] }) });
}, GJ = (i) => ({}), KJ = new ps({
  type: os.type,
  form: new zn({
    render: HZ,
    validation: jZ
  }),
  settings: new zn({
    render: qJ,
    validation: GJ
  }),
  heading: "Select box",
  description: "select from a list of items in a drop down",
  hidden: !1,
  icon: WZ,
  data: os
}), js = {
  id: br(),
  type: "input-text",
  label: "",
  value: "",
  placeholder: ""
}, ZJ = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...js, ...i });
  Ee(() => {
    n({ ...js, ...i });
  }, [i]);
  const r = (o) => {
    const a = { ...js, ...i, value: o.target.value };
    t(a);
  }, s = Math.random().toString(36).substring(2, 15);
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { htmlFor: s, children: e.label }),
    /* @__PURE__ */ _.jsx(
      _t,
      {
        value: e.value,
        onChange: r,
        placeholder: e.placeholder,
        className: "w-full",
        id: s
      }
    )
  ] }) }) });
}, YJ = (i) => ({});
var Fy;
function pg() {
  return pg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, pg.apply(null, arguments);
}
var JJ = function(t) {
  return /* @__PURE__ */ w.createElement("svg", pg({
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 448 512"
  }, t), Fy || (Fy = /* @__PURE__ */ w.createElement("path", {
    d: "M254 52.8C249.3 40.3 237.3 32 224 32s-25.3 8.3-30 20.8L57.8 416H32c-17.7 0-32 14.3-32 32s14.3 32 32 32h96c17.7 0 32-14.3 32-32s-14.3-32-32-32h-1.8l18-48h159.6l18 48H320c-17.7 0-32 14.3-32 32s14.3 32 32 32h96c17.7 0 32-14.3 32-32s-14.3-32-32-32h-25.8zM279.8 304H168.2L224 155.1z"
  })));
};
const XJ = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...js, ...i }), r = (s) => {
    const o = { ...e, [s.target.name]: s.target.value };
    n(o), t(o);
  };
  return Ee(() => {
    n({ ...js, ...i });
  }, [i]), /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx(or, { activeIndex: 0, children: /* @__PURE__ */ _.jsx(di, { header: "General", children: /* @__PURE__ */ _.jsxs("div", { className: "form-item flex flex-column gap-4", children: [
    /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
      /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Label" }),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: e.label || "",
          name: "label",
          onChange: r,
          className: "w-full"
        }
      )
    ] }),
    /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
      /* @__PURE__ */ _.jsx("label", { htmlFor: "placeholder", children: "Placeholder" }),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "placeholder",
          value: e.placeholder || "",
          name: "placeholder",
          onChange: r,
          className: "w-full"
        }
      )
    ] })
  ] }) }, "general") }) });
}, QJ = (i) => ({}), eX = new ps({
  type: js.type,
  form: new zn({
    render: ZJ,
    validation: YJ
  }),
  settings: new zn({
    render: XJ,
    validation: QJ
  }),
  heading: "Text input",
  description: "a simple box to insert a value into",
  hidden: !1,
  icon: JJ,
  data: js
}), ls = {
  id: br(),
  type: "input-checkbox",
  label: "",
  value: [],
  source: "local",
  display: "normal",
  options: []
};
function kl() {
  return kl = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, kl.apply(this, arguments);
}
function lc(i) {
  "@babel/helpers - typeof";
  return lc = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, lc(i);
}
function tX(i, t) {
  if (lc(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (lc(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function nX(i) {
  var t = tX(i, "string");
  return lc(t) === "symbol" ? t : String(t);
}
function iX(i, t, e) {
  return t = nX(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function rX(i) {
  if (Array.isArray(i)) return i;
}
function sX(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function $y(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function oX(i, t) {
  if (i) {
    if (typeof i == "string") return $y(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return $y(i, t);
  }
}
function aX() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function lX(i, t) {
  return rX(i) || sX(i, t) || oX(i, t) || aX();
}
var cX = {
  box: "p-checkbox-box",
  input: "p-checkbox-input",
  icon: "p-checkbox-icon",
  root: function(t) {
    var e = t.props, n = t.checked, r = t.context;
    return Re("p-checkbox p-component", {
      "p-highlight": n,
      "p-disabled": e.disabled,
      "p-invalid": e.invalid,
      "p-variant-filled": e.variant ? e.variant === "filled" : r && r.inputStyle === "filled"
    });
  }
}, hu = et.extend({
  defaultProps: {
    __TYPE: "Checkbox",
    autoFocus: !1,
    checked: !1,
    className: null,
    disabled: !1,
    falseValue: !1,
    icon: null,
    id: null,
    inputId: null,
    inputRef: null,
    invalid: !1,
    variant: null,
    name: null,
    onChange: null,
    onContextMenu: null,
    onMouseDown: null,
    readOnly: !1,
    required: !1,
    style: null,
    tabIndex: null,
    tooltip: null,
    tooltipOptions: null,
    trueValue: !0,
    value: null,
    children: void 0
  },
  css: {
    classes: cX
  }
});
function zy(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function Hy(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? zy(Object(e), !0).forEach(function(n) {
      iX(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : zy(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var d5 = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = hu.getProps(i, n), s = w.useState(!1), o = lX(s, 2), a = o[0], l = o[1], c = hu.setMetaData({
    props: r,
    state: {
      focused: a
    },
    context: {
      checked: r.checked === r.trueValue,
      disabled: r.disabled
    }
  }), u = c.ptm, d = c.cx, f = c.isUnstyled;
  sr(hu.css.styles, f, {
    name: "checkbox"
  });
  var m = w.useRef(null), g = w.useRef(r.inputRef), p = function() {
    return r.checked === r.trueValue;
  }, b = function(O) {
    if (!(r.disabled || r.readonly) && r.onChange) {
      var N, le = p(), te = le ? r.falseValue : r.trueValue, me = {
        originalEvent: O,
        value: r.value,
        checked: te,
        stopPropagation: function() {
          O == null || O.stopPropagation();
        },
        preventDefault: function() {
          O == null || O.preventDefault();
        },
        target: {
          type: "checkbox",
          name: r.name,
          id: r.id,
          value: r.value,
          checked: te
        }
      };
      if (r == null || (N = r.onChange) === null || N === void 0 || N.call(r, me), O.defaultPrevented)
        return;
      j.focus(g.current);
    }
  }, v = function() {
    var O;
    l(!0), r == null || (O = r.onFocus) === null || O === void 0 || O.call(r);
  }, y = function() {
    var O;
    l(!1), r == null || (O = r.onBlur) === null || O === void 0 || O.call(r);
  };
  w.useImperativeHandle(t, function() {
    return {
      props: r,
      focus: function() {
        return j.focus(g.current);
      },
      getElement: function() {
        return m.current;
      },
      getInput: function() {
        return g.current;
      }
    };
  }), w.useEffect(function() {
    z.combinedRefs(g, r.inputRef);
  }, [g, r.inputRef]), Zt(function() {
    g.current.checked = p();
  }, [r.checked, r.trueValue]), rr(function() {
    r.autoFocus && j.focus(g.current, r.autoFocus);
  });
  var x = p(), S = z.isNotEmpty(r.tooltip), k = hu.getOtherProps(r), I = e({
    id: r.id,
    className: Re(r.className, d("root", {
      checked: x,
      context: n
    })),
    style: r.style,
    "data-p-highlight": x,
    "data-p-disabled": r.disabled,
    onContextMenu: r.onContextMenu,
    onMouseDown: r.onMouseDown
  }, k, u("root")), A = function() {
    var O = z.reduceKeys(k, j.ARIA_PROPS), N = e(Hy({
      id: r.inputId,
      type: "checkbox",
      className: d("input"),
      name: r.name,
      tabIndex: r.tabIndex,
      onFocus: function(te) {
        return v();
      },
      onBlur: function(te) {
        return y();
      },
      onChange: function(te) {
        return b(te);
      },
      disabled: r.disabled,
      readOnly: r.readOnly,
      required: r.required,
      "aria-invalid": r.invalid,
      checked: x
    }, O), u("input"));
    return /* @__PURE__ */ w.createElement("input", kl({
      ref: g
    }, N));
  }, L = function() {
    var O = e({
      className: d("icon")
    }, u("icon")), N = e({
      className: d("box", {
        checked: x
      }),
      "data-p-highlight": x,
      "data-p-disabled": r.disabled
    }, u("box")), le = x ? r.icon || /* @__PURE__ */ w.createElement(Xp, O) : null, te = Mi.getJSXIcon(le, Hy({}, O), {
      props: r,
      checked: x
    });
    return /* @__PURE__ */ w.createElement("div", N, te);
  };
  return /* @__PURE__ */ w.createElement(w.Fragment, null, /* @__PURE__ */ w.createElement("div", kl({
    ref: m
  }, I), A(), L()), S && /* @__PURE__ */ w.createElement(ya, kl({
    target: m,
    content: r.tooltip,
    pt: u("tooltip")
  }, r.tooltipOptions)));
}));
d5.displayName = "Checkbox";
const uX = ({ item: i, onChange: t }) => {
  const { config: e } = _r(), [n, r] = Ne({ ...ls, ...i }), [s, o] = Ne(n.options);
  Ee(() => {
    r({ ...ls, ...i });
  }, [i]), Ee(() => {
    if (o(n.options), n.source != "local") {
      const c = e.external_data.find((u) => u.key == n.source);
      c && (c.options && o(c.options), c.options_func && c.options_func().then((u) => {
        o(u);
      }));
    }
  }, [n.source, n.options]);
  const a = (c) => {
    const u = pn({ ...ls, ...i }), d = c.target;
    d.checked ? u.value.push(d.value) : u.value = u.value.filter((f) => f != d.value), t(u);
  }, l = Math.random().toString(36).substring(2, 15);
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { htmlFor: l, children: n.label }),
    s.map((c) => /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
      /* @__PURE__ */ _.jsx(
        d5,
        {
          inputId: c.value,
          name: "value",
          value: c.value,
          onChange: a,
          checked: n.value.includes(c.value)
        }
      ),
      /* @__PURE__ */ _.jsx("label", { htmlFor: c.value, className: "ml-2", children: c.label })
    ] }, `${i.id}-${c.value}`))
  ] }) }) });
}, dX = (i) => ({});
var jy;
function bg() {
  return bg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, bg.apply(null, arguments);
}
var hX = function(t) {
  return /* @__PURE__ */ w.createElement("svg", bg({
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 448 512"
  }, t), jy || (jy = /* @__PURE__ */ w.createElement("path", {
    d: "M64 80c-8.8 0-16 7.2-16 16v320c0 8.8 7.2 16 16 16h320c8.8 0 16-7.2 16-16V96c0-8.8-7.2-16-16-16zM0 96c0-35.3 28.7-64 64-64h320c35.3 0 64 28.7 64 64v320c0 35.3-28.7 64-64 64H64c-35.3 0-64-28.7-64-64zm337 113L209 337c-9.4 9.4-24.6 9.4-33.9 0l-64-64c-9.4-9.4-9.4-24.6 0-33.9s24.6-9.4 33.9 0l47 47L303 175c9.4-9.4 24.6-9.4 33.9 0s9.4 24.6 0 33.9z"
  })));
};
const fX = ({ option: i, onChange: t, index: e }) => {
  const [n, r] = Ne(i), s = (f) => {
    const m = {
      value: n.value,
      label: n.label,
      [f.target.name]: f.target.value
    };
    r(m), t(m);
  };
  Ee(() => {
    r(i);
  }, [i]);
  const {
    attributes: o,
    listeners: a,
    setNodeRef: l,
    setActivatorNodeRef: c,
    transform: u
  } = Ch({ id: e }), d = {
    transform: Bi.Transform.toString(u)
    // transition,
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-row gap-2", ref: l, style: d, ...o, children: [
    /* @__PURE__ */ _.jsx(
      Mn,
      {
        icon: "pi pi-sort",
        className: "px-0 border-noround border-0",
        style: { width: "1rem", color: "#E1E1E1", background: "transparent" },
        severity: "secondary",
        size: "small",
        ref: c,
        ...a
      }
    ),
    /* @__PURE__ */ _.jsxs("div", { className: "p-inputgroup flex-1", children: [
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "value",
          value: n.value,
          name: "value",
          onChange: s,
          className: "flex-1",
          placeholder: "Value"
        }
      ),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: n.label,
          name: "label",
          onChange: s,
          className: "flex-1",
          placeholder: "Label"
        }
      )
    ] }),
    /* @__PURE__ */ _.jsx(Mn, { icon: "pi pi-trash", onClick: () => t(null) })
  ] }) });
}, mX = ({ onInsert: i }) => {
  const t = {
    value: "",
    label: ""
  }, [e, n] = Ne(t), r = (o) => {
    const a = {
      value: e.value,
      label: e.label,
      [o.target.name]: o.target.value
    };
    n(a);
  }, s = () => {
    i({ value: e.value, label: e.label }), n(t);
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-row gap-2", children: [
    /* @__PURE__ */ _.jsxs("div", { className: "p-inputgroup flex-1", children: [
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "value",
          value: e.value,
          name: "value",
          onChange: r,
          className: "",
          placeholder: "Value"
        }
      ),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: e.label,
          name: "label",
          onChange: r,
          className: "",
          placeholder: "Label"
        }
      )
    ] }),
    /* @__PURE__ */ _.jsx(
      Mn,
      {
        icon: "pi pi-plus",
        onClick: s
      }
    )
  ] }) });
}, gX = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...ls, ...i }), r = (l, c) => {
    const u = pn(e.options);
    l ? u[c] = l : u.splice(c, 1);
    const d = { ...e, options: u };
    n(d), t(d);
  }, s = (l) => {
    const c = pn(e.options);
    c.push(l);
    const u = { ...e, options: c };
    n(u), t(u);
  }, o = gh(
    gs(Ea),
    gs(xa, {
      coordinateGetter: kh
    })
  );
  function a(l) {
    const { active: c, over: u } = l;
    if (u && c.id !== u.id) {
      let d = function(g) {
        return parseInt(g.replace("option-", ""));
      };
      const f = as(e.options, d(u.id.toString()), d(c.id.toString())), m = { ...e, options: f };
      n(m), t(m);
    }
  }
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "form-item flex flex-column gap-4", children: [
    /* @__PURE__ */ _.jsx(
      yh,
      {
        sensors: o,
        collisionDetection: ph,
        onDragEnd: a,
        children: /* @__PURE__ */ _.jsx(
          _h,
          {
            items: e.options.map((l, c) => `option-${c}`),
            strategy: sb,
            children: e.options.map((l, c) => /* @__PURE__ */ _.jsx(
              fX,
              {
                option: l,
                index: `option-${c}`,
                onChange: (u) => {
                  r(u, c);
                }
              },
              `option-${c}`
            ))
          }
        )
      }
    ),
    /* @__PURE__ */ _.jsx(
      mX,
      {
        onInsert: s
      },
      "item-new"
    )
  ] }) });
}, pX = ({ item: i, onChange: t }) => {
  const { config: e } = _r(), [n, r] = Ne({ ...ls, ...i }), s = (a) => {
    const l = { ...n, [a.target.name]: a.target.value };
    r(l), t(l);
  };
  Ee(() => {
    r({ ...ls, ...i });
  }, [i]);
  const o = [];
  return o.push({
    key: "normal",
    label: "Normal"
  }), o.push({
    key: "button-block",
    label: "Button block"
  }), /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs(or, { activeIndex: 0, children: [
    /* @__PURE__ */ _.jsx(di, { header: "General", children: /* @__PURE__ */ _.jsx("div", { className: "form-item flex flex-column gap-4", children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
      /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Label" }),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: n.label || "",
          name: "label",
          onChange: s,
          className: "w-full"
        }
      )
    ] }) }) }, "general"),
    /* @__PURE__ */ _.jsxs(di, { header: "Options", children: [
      e.external_data.map((a) => /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
        /* @__PURE__ */ _.jsx(
          Nr,
          {
            inputId: a.key,
            name: "source",
            value: a.key,
            onChange: s,
            checked: n.source === a.key
          }
        ),
        /* @__PURE__ */ _.jsx("label", { htmlFor: a.key, className: "ml-2", children: a.label })
      ] }, a.key)),
      /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
        /* @__PURE__ */ _.jsx(
          Nr,
          {
            inputId: "local",
            name: "source",
            value: "local",
            onChange: s,
            checked: n.source === "local"
          }
        ),
        /* @__PURE__ */ _.jsx("label", { htmlFor: "local", className: "ml-2", children: "Custom" })
      ] }, "local"),
      /* @__PURE__ */ _.jsx("div", { className: "mt-4", children: n.source === "local" && /* @__PURE__ */ _.jsx(gX, { item: n, onChange: t }) }),
      /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
        /* @__PURE__ */ _.jsx("label", { htmlFor: "display", children: "Display as:" }),
        o.map((a) => /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
          /* @__PURE__ */ _.jsx(
            Nr,
            {
              inputId: a.key,
              name: "display",
              value: a.key,
              onChange: s,
              checked: n.display == a.key
            }
          ),
          /* @__PURE__ */ _.jsx("label", { htmlFor: a.key, className: "ml-2", children: a.label })
        ] }, a.key))
      ] })
    ] }, "options")
  ] }) });
}, bX = (i) => ({}), vX = new ps({
  type: ls.type,
  form: new zn({
    render: uX,
    validation: dX
  }),
  settings: new zn({
    render: pX,
    validation: bX
  }),
  heading: "Checkbox input",
  description: "select one or more items from a checkbox",
  hidden: !1,
  icon: hX,
  data: ls
}), cs = {
  id: br(),
  type: "input-radio",
  label: "",
  value: "",
  source: "local",
  options: []
}, wX = ({ item: i, onChange: t }) => {
  const { config: e } = _r(), [n, r] = Ne({ ...cs, ...i }), [s, o] = Ne(n.options);
  Ee(() => {
    r({ ...cs, ...i });
  }, [i]), Ee(() => {
    if (o(n.options), n.source != "local") {
      const c = e.external_data.find((u) => u.key == n.source);
      c && (c.options && o(c.options), c.options_func && c.options_func().then((u) => {
        o(u);
      }));
    }
  }, [n.source, n.options]);
  const a = (c) => {
    const u = { ...cs, ...i, value: c.target.value };
    t(u);
  }, l = Math.random().toString(36).substring(2, 15);
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { htmlFor: l, children: n.label }),
    s.map((c) => /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
      /* @__PURE__ */ _.jsx(
        Nr,
        {
          inputId: c.value,
          name: "value",
          value: c.value,
          onChange: a,
          checked: n.value == c.value
        }
      ),
      /* @__PURE__ */ _.jsx("label", { htmlFor: c.value, className: "ml-2", children: c.label })
    ] }, `${i.id}-${c.value}`))
  ] }) }) });
}, yX = (i) => ({});
var Wy;
function vg() {
  return vg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, vg.apply(null, arguments);
}
var _X = function(t) {
  return /* @__PURE__ */ w.createElement("svg", vg({
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 512 512"
  }, t), Wy || (Wy = /* @__PURE__ */ w.createElement("path", {
    d: "M464 256a208 208 0 1 0-416 0 208 208 0 1 0 416 0M0 256a256 256 0 1 1 512 0 256 256 0 1 1-512 0m256-96a96 96 0 1 1 0 192 96 96 0 1 1 0-192"
  })));
};
const CX = ({ option: i, onChange: t, index: e }) => {
  const [n, r] = Ne(i), s = (f) => {
    const m = {
      value: n.value,
      label: n.label,
      [f.target.name]: f.target.value
    };
    r(m), t(m);
  };
  Ee(() => {
    r(i);
  }, [i]);
  const {
    attributes: o,
    listeners: a,
    setNodeRef: l,
    setActivatorNodeRef: c,
    transform: u
  } = Ch({ id: e }), d = {
    transform: Bi.Transform.toString(u)
    // transition,
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-row gap-2", ref: l, style: d, ...o, children: [
    /* @__PURE__ */ _.jsx(
      Mn,
      {
        icon: "pi pi-sort",
        className: "px-0 border-noround border-0",
        style: { width: "1rem", color: "#E1E1E1", background: "transparent" },
        severity: "secondary",
        size: "small",
        ref: c,
        ...a
      }
    ),
    /* @__PURE__ */ _.jsxs("div", { className: "p-inputgroup flex-1", children: [
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "value",
          value: n.value,
          name: "value",
          onChange: s,
          className: "flex-1",
          placeholder: "Value"
        }
      ),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: n.label,
          name: "label",
          onChange: s,
          className: "flex-1",
          placeholder: "Label"
        }
      )
    ] }),
    /* @__PURE__ */ _.jsx(Mn, { icon: "pi pi-trash", onClick: () => t(null) })
  ] }) });
}, kX = ({ onInsert: i }) => {
  const t = {
    value: "",
    label: ""
  }, [e, n] = Ne(t), r = (o) => {
    const a = {
      value: e.value,
      label: e.label,
      [o.target.name]: o.target.value
    };
    n(a);
  }, s = () => {
    i({ value: e.value, label: e.label }), n(t);
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-row gap-2", children: [
    /* @__PURE__ */ _.jsxs("div", { className: "p-inputgroup flex-1", children: [
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "value",
          value: e.value,
          name: "value",
          onChange: r,
          className: "",
          placeholder: "Value"
        }
      ),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: e.label,
          name: "label",
          onChange: r,
          className: "",
          placeholder: "Label"
        }
      )
    ] }),
    /* @__PURE__ */ _.jsx(
      Mn,
      {
        icon: "pi pi-plus",
        onClick: s
      }
    )
  ] }) });
}, xX = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...cs, ...i }), r = (l, c) => {
    const u = pn(e.options);
    l ? u[c] = l : u.splice(c, 1);
    const d = { ...e, options: u };
    n(d), t(d);
  }, s = (l) => {
    const c = pn(e.options);
    c.push(l);
    const u = { ...e, options: c };
    n(u), t(u);
  }, o = gh(
    gs(Ea),
    gs(xa, {
      coordinateGetter: kh
    })
  );
  function a(l) {
    const { active: c, over: u } = l;
    if (u && c.id !== u.id) {
      let d = function(g) {
        return parseInt(g.replace("option-", ""));
      };
      const f = as(e.options, d(u.id.toString()), d(c.id.toString())), m = { ...e, options: f };
      n(m), t(m);
    }
  }
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "form-item flex flex-column gap-4", children: [
    /* @__PURE__ */ _.jsx(
      yh,
      {
        sensors: o,
        collisionDetection: ph,
        onDragEnd: a,
        children: /* @__PURE__ */ _.jsx(
          _h,
          {
            items: e.options.map((l, c) => `option-${c}`),
            strategy: sb,
            children: e.options.map((l, c) => /* @__PURE__ */ _.jsx(
              CX,
              {
                option: l,
                index: `option-${c}`,
                onChange: (u) => {
                  r(u, c);
                }
              },
              `option-${c}`
            ))
          }
        )
      }
    ),
    /* @__PURE__ */ _.jsx(
      kX,
      {
        onInsert: s
      },
      "item-new"
    )
  ] }) });
}, EX = ({ item: i, onChange: t }) => {
  const { config: e } = _r(), [n, r] = Ne({ ...cs, ...i }), s = (o) => {
    const a = { ...n, [o.target.name]: o.target.value };
    r(a), t(a);
  };
  return Ee(() => {
    r({ ...cs, ...i });
  }, [i]), /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs(or, { activeIndex: 0, children: [
    /* @__PURE__ */ _.jsx(di, { header: "General", children: /* @__PURE__ */ _.jsx("div", { className: "form-item flex flex-column gap-4", children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
      /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Label" }),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: n.label || "",
          name: "label",
          onChange: s,
          className: "w-full"
        }
      )
    ] }) }) }, "general"),
    /* @__PURE__ */ _.jsxs(di, { header: "Options", children: [
      e.external_data.map((o) => /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
        /* @__PURE__ */ _.jsx(
          Nr,
          {
            inputId: o.key,
            name: "source",
            value: o.key,
            onChange: s,
            checked: n.source === o.key
          }
        ),
        /* @__PURE__ */ _.jsx("label", { htmlFor: o.key, className: "ml-2", children: o.label })
      ] }, o.key)),
      /* @__PURE__ */ _.jsxs("div", { className: "flex align-items-center", children: [
        /* @__PURE__ */ _.jsx(
          Nr,
          {
            inputId: "local",
            name: "source",
            value: "local",
            onChange: s,
            checked: n.source === "local"
          }
        ),
        /* @__PURE__ */ _.jsx("label", { htmlFor: "local", className: "ml-2", children: "Custom" })
      ] }, "local"),
      /* @__PURE__ */ _.jsx("div", { className: "mt-4", children: n.source === "local" && /* @__PURE__ */ _.jsx(xX, { item: n, onChange: t }) })
    ] }, "options")
  ] }) });
}, TX = (i) => ({}), SX = new ps({
  type: cs.type,
  form: new zn({
    render: wX,
    validation: yX
  }),
  settings: new zn({
    render: EX,
    validation: TX
  }),
  heading: "Radio input",
  description: "select an item from a radio group",
  hidden: !1,
  icon: _X,
  data: cs
}), Ta = new Id(new Pd({ label: "Inputs" }));
Ta.registerItem(eX);
Ta.registerItem(rZ);
Ta.registerItem(KJ);
Ta.registerItem(vX);
Ta.registerItem(SX);
const ua = {
  id: br(),
  type: "special-signature",
  label: "",
  value: [],
  color: "#000000"
};
/*!
 * Signature Pad v5.0.3 | https://github.com/szimek/signature_pad
 * (c) 2024 Szymon Nowak | Released under the MIT license
 */
class kd {
  constructor(t, e, n, r) {
    if (isNaN(t) || isNaN(e))
      throw new Error(`Point is invalid: (${t}, ${e})`);
    this.x = +t, this.y = +e, this.pressure = n || 0, this.time = r || Date.now();
  }
  distanceTo(t) {
    return Math.sqrt(Math.pow(this.x - t.x, 2) + Math.pow(this.y - t.y, 2));
  }
  equals(t) {
    return this.x === t.x && this.y === t.y && this.pressure === t.pressure && this.time === t.time;
  }
  velocityFrom(t) {
    return this.time !== t.time ? this.distanceTo(t) / (this.time - t.time) : 0;
  }
}
class ob {
  static fromPoints(t, e) {
    const n = this.calculateControlPoints(t[0], t[1], t[2]).c2, r = this.calculateControlPoints(t[1], t[2], t[3]).c1;
    return new ob(t[1], n, r, t[2], e.start, e.end);
  }
  static calculateControlPoints(t, e, n) {
    const r = t.x - e.x, s = t.y - e.y, o = e.x - n.x, a = e.y - n.y, l = { x: (t.x + e.x) / 2, y: (t.y + e.y) / 2 }, c = { x: (e.x + n.x) / 2, y: (e.y + n.y) / 2 }, u = Math.sqrt(r * r + s * s), d = Math.sqrt(o * o + a * a), f = l.x - c.x, m = l.y - c.y, g = u + d == 0 ? 0 : d / (u + d), p = { x: c.x + f * g, y: c.y + m * g }, b = e.x - p.x, v = e.y - p.y;
    return {
      c1: new kd(l.x + b, l.y + v),
      c2: new kd(c.x + b, c.y + v)
    };
  }
  constructor(t, e, n, r, s, o) {
    this.startPoint = t, this.control2 = e, this.control1 = n, this.endPoint = r, this.startWidth = s, this.endWidth = o;
  }
  length() {
    let e = 0, n, r;
    for (let s = 0; s <= 10; s += 1) {
      const o = s / 10, a = this.point(o, this.startPoint.x, this.control1.x, this.control2.x, this.endPoint.x), l = this.point(o, this.startPoint.y, this.control1.y, this.control2.y, this.endPoint.y);
      if (s > 0) {
        const c = a - n, u = l - r;
        e += Math.sqrt(c * c + u * u);
      }
      n = a, r = l;
    }
    return e;
  }
  point(t, e, n, r, s) {
    return e * (1 - t) * (1 - t) * (1 - t) + 3 * n * (1 - t) * (1 - t) * t + 3 * r * (1 - t) * t * t + s * t * t * t;
  }
}
class AX {
  constructor() {
    try {
      this._et = new EventTarget();
    } catch {
      this._et = document;
    }
  }
  addEventListener(t, e, n) {
    this._et.addEventListener(t, e, n);
  }
  dispatchEvent(t) {
    return this._et.dispatchEvent(t);
  }
  removeEventListener(t, e, n) {
    this._et.removeEventListener(t, e, n);
  }
}
function PX(i, t = 250) {
  let e = 0, n = null, r, s, o;
  const a = () => {
    e = Date.now(), n = null, r = i.apply(s, o), n || (s = null, o = []);
  };
  return function(...c) {
    const u = Date.now(), d = t - (u - e);
    return s = this, o = c, d <= 0 || d > t ? (n && (clearTimeout(n), n = null), e = u, r = i.apply(s, o), n || (s = null, o = [])) : n || (n = window.setTimeout(a, d)), r;
  };
}
class xd extends AX {
  constructor(t, e = {}) {
    var n, r, s;
    super(), this.canvas = t, this._drawingStroke = !1, this._isEmpty = !0, this._lastPoints = [], this._data = [], this._lastVelocity = 0, this._lastWidth = 0, this._handleMouseDown = (o) => {
      !this._isLeftButtonPressed(o, !0) || this._drawingStroke || this._strokeBegin(this._pointerEventToSignatureEvent(o));
    }, this._handleMouseMove = (o) => {
      if (!this._isLeftButtonPressed(o, !0) || !this._drawingStroke) {
        this._strokeEnd(this._pointerEventToSignatureEvent(o), !1);
        return;
      }
      this._strokeMoveUpdate(this._pointerEventToSignatureEvent(o));
    }, this._handleMouseUp = (o) => {
      this._isLeftButtonPressed(o) || this._strokeEnd(this._pointerEventToSignatureEvent(o));
    }, this._handleTouchStart = (o) => {
      o.targetTouches.length !== 1 || this._drawingStroke || (o.cancelable && o.preventDefault(), this._strokeBegin(this._touchEventToSignatureEvent(o)));
    }, this._handleTouchMove = (o) => {
      if (o.targetTouches.length === 1) {
        if (o.cancelable && o.preventDefault(), !this._drawingStroke) {
          this._strokeEnd(this._touchEventToSignatureEvent(o), !1);
          return;
        }
        this._strokeMoveUpdate(this._touchEventToSignatureEvent(o));
      }
    }, this._handleTouchEnd = (o) => {
      o.targetTouches.length === 0 && (o.cancelable && o.preventDefault(), this.canvas.removeEventListener("touchmove", this._handleTouchMove), this._strokeEnd(this._touchEventToSignatureEvent(o)));
    }, this._handlePointerDown = (o) => {
      !this._isLeftButtonPressed(o) || this._drawingStroke || (o.preventDefault(), this._strokeBegin(this._pointerEventToSignatureEvent(o)));
    }, this._handlePointerMove = (o) => {
      if (!this._isLeftButtonPressed(o, !0) || !this._drawingStroke) {
        this._strokeEnd(this._pointerEventToSignatureEvent(o), !1);
        return;
      }
      o.preventDefault(), this._strokeMoveUpdate(this._pointerEventToSignatureEvent(o));
    }, this._handlePointerUp = (o) => {
      this._isLeftButtonPressed(o) || (o.preventDefault(), this._strokeEnd(this._pointerEventToSignatureEvent(o)));
    }, this.velocityFilterWeight = e.velocityFilterWeight || 0.7, this.minWidth = e.minWidth || 0.5, this.maxWidth = e.maxWidth || 2.5, this.throttle = (n = e.throttle) !== null && n !== void 0 ? n : 16, this.minDistance = (r = e.minDistance) !== null && r !== void 0 ? r : 5, this.dotSize = e.dotSize || 0, this.penColor = e.penColor || "black", this.backgroundColor = e.backgroundColor || "rgba(0,0,0,0)", this.compositeOperation = e.compositeOperation || "source-over", this.canvasContextOptions = (s = e.canvasContextOptions) !== null && s !== void 0 ? s : {}, this._strokeMoveUpdate = this.throttle ? PX(xd.prototype._strokeUpdate, this.throttle) : xd.prototype._strokeUpdate, this._ctx = t.getContext("2d", this.canvasContextOptions), this.clear(), this.on();
  }
  clear() {
    const { _ctx: t, canvas: e } = this;
    t.fillStyle = this.backgroundColor, t.clearRect(0, 0, e.width, e.height), t.fillRect(0, 0, e.width, e.height), this._data = [], this._reset(this._getPointGroupOptions()), this._isEmpty = !0;
  }
  fromDataURL(t, e = {}) {
    return new Promise((n, r) => {
      const s = new Image(), o = e.ratio || window.devicePixelRatio || 1, a = e.width || this.canvas.width / o, l = e.height || this.canvas.height / o, c = e.xOffset || 0, u = e.yOffset || 0;
      this._reset(this._getPointGroupOptions()), s.onload = () => {
        this._ctx.drawImage(s, c, u, a, l), n();
      }, s.onerror = (d) => {
        r(d);
      }, s.crossOrigin = "anonymous", s.src = t, this._isEmpty = !1;
    });
  }
  toDataURL(t = "image/png", e) {
    switch (t) {
      case "image/svg+xml":
        return typeof e != "object" && (e = void 0), `data:image/svg+xml;base64,${btoa(this.toSVG(e))}`;
      default:
        return typeof e != "number" && (e = void 0), this.canvas.toDataURL(t, e);
    }
  }
  on() {
    this.canvas.style.touchAction = "none", this.canvas.style.msTouchAction = "none", this.canvas.style.userSelect = "none";
    const t = /Macintosh/.test(navigator.userAgent) && "ontouchstart" in document;
    window.PointerEvent && !t ? this._handlePointerEvents() : (this._handleMouseEvents(), "ontouchstart" in window && this._handleTouchEvents());
  }
  off() {
    this.canvas.style.touchAction = "auto", this.canvas.style.msTouchAction = "auto", this.canvas.style.userSelect = "auto", this.canvas.removeEventListener("pointerdown", this._handlePointerDown), this.canvas.removeEventListener("mousedown", this._handleMouseDown), this.canvas.removeEventListener("touchstart", this._handleTouchStart), this._removeMoveUpEventListeners();
  }
  _getListenerFunctions() {
    var t;
    const e = window.document === this.canvas.ownerDocument ? window : (t = this.canvas.ownerDocument.defaultView) !== null && t !== void 0 ? t : this.canvas.ownerDocument;
    return {
      addEventListener: e.addEventListener.bind(e),
      removeEventListener: e.removeEventListener.bind(e)
    };
  }
  _removeMoveUpEventListeners() {
    const { removeEventListener: t } = this._getListenerFunctions();
    t("pointermove", this._handlePointerMove), t("pointerup", this._handlePointerUp), t("mousemove", this._handleMouseMove), t("mouseup", this._handleMouseUp), t("touchmove", this._handleTouchMove), t("touchend", this._handleTouchEnd);
  }
  isEmpty() {
    return this._isEmpty;
  }
  fromData(t, { clear: e = !0 } = {}) {
    e && this.clear(), this._fromData(t, this._drawCurve.bind(this), this._drawDot.bind(this)), this._data = this._data.concat(t);
  }
  toData() {
    return this._data;
  }
  _isLeftButtonPressed(t, e) {
    return e ? t.buttons === 1 : (t.buttons & 1) === 1;
  }
  _pointerEventToSignatureEvent(t) {
    return {
      event: t,
      type: t.type,
      x: t.clientX,
      y: t.clientY,
      pressure: "pressure" in t ? t.pressure : 0
    };
  }
  _touchEventToSignatureEvent(t) {
    const e = t.changedTouches[0];
    return {
      event: t,
      type: t.type,
      x: e.clientX,
      y: e.clientY,
      pressure: e.force
    };
  }
  _getPointGroupOptions(t) {
    return {
      penColor: t && "penColor" in t ? t.penColor : this.penColor,
      dotSize: t && "dotSize" in t ? t.dotSize : this.dotSize,
      minWidth: t && "minWidth" in t ? t.minWidth : this.minWidth,
      maxWidth: t && "maxWidth" in t ? t.maxWidth : this.maxWidth,
      velocityFilterWeight: t && "velocityFilterWeight" in t ? t.velocityFilterWeight : this.velocityFilterWeight,
      compositeOperation: t && "compositeOperation" in t ? t.compositeOperation : this.compositeOperation
    };
  }
  _strokeBegin(t) {
    if (!this.dispatchEvent(new CustomEvent("beginStroke", { detail: t, cancelable: !0 })))
      return;
    const { addEventListener: n } = this._getListenerFunctions();
    switch (t.event.type) {
      case "mousedown":
        n("mousemove", this._handleMouseMove), n("mouseup", this._handleMouseUp);
        break;
      case "touchstart":
        n("touchmove", this._handleTouchMove), n("touchend", this._handleTouchEnd);
        break;
      case "pointerdown":
        n("pointermove", this._handlePointerMove), n("pointerup", this._handlePointerUp);
        break;
    }
    this._drawingStroke = !0;
    const r = this._getPointGroupOptions(), s = Object.assign(Object.assign({}, r), { points: [] });
    this._data.push(s), this._reset(r), this._strokeUpdate(t);
  }
  _strokeUpdate(t) {
    if (!this._drawingStroke)
      return;
    if (this._data.length === 0) {
      this._strokeBegin(t);
      return;
    }
    this.dispatchEvent(new CustomEvent("beforeUpdateStroke", { detail: t }));
    const e = this._createPoint(t.x, t.y, t.pressure), n = this._data[this._data.length - 1], r = n.points, s = r.length > 0 && r[r.length - 1], o = s ? e.distanceTo(s) <= this.minDistance : !1, a = this._getPointGroupOptions(n);
    if (!s || !(s && o)) {
      const l = this._addPoint(e, a);
      s ? l && this._drawCurve(l, a) : this._drawDot(e, a), r.push({
        time: e.time,
        x: e.x,
        y: e.y,
        pressure: e.pressure
      });
    }
    this.dispatchEvent(new CustomEvent("afterUpdateStroke", { detail: t }));
  }
  _strokeEnd(t, e = !0) {
    this._removeMoveUpEventListeners(), this._drawingStroke && (e && this._strokeUpdate(t), this._drawingStroke = !1, this.dispatchEvent(new CustomEvent("endStroke", { detail: t })));
  }
  _handlePointerEvents() {
    this._drawingStroke = !1, this.canvas.addEventListener("pointerdown", this._handlePointerDown);
  }
  _handleMouseEvents() {
    this._drawingStroke = !1, this.canvas.addEventListener("mousedown", this._handleMouseDown);
  }
  _handleTouchEvents() {
    this.canvas.addEventListener("touchstart", this._handleTouchStart);
  }
  _reset(t) {
    this._lastPoints = [], this._lastVelocity = 0, this._lastWidth = (t.minWidth + t.maxWidth) / 2, this._ctx.fillStyle = t.penColor, this._ctx.globalCompositeOperation = t.compositeOperation;
  }
  _createPoint(t, e, n) {
    const r = this.canvas.getBoundingClientRect();
    return new kd(t - r.left, e - r.top, n, (/* @__PURE__ */ new Date()).getTime());
  }
  _addPoint(t, e) {
    const { _lastPoints: n } = this;
    if (n.push(t), n.length > 2) {
      n.length === 3 && n.unshift(n[0]);
      const r = this._calculateCurveWidths(n[1], n[2], e), s = ob.fromPoints(n, r);
      return n.shift(), s;
    }
    return null;
  }
  _calculateCurveWidths(t, e, n) {
    const r = n.velocityFilterWeight * e.velocityFrom(t) + (1 - n.velocityFilterWeight) * this._lastVelocity, s = this._strokeWidth(r, n), o = {
      end: s,
      start: this._lastWidth
    };
    return this._lastVelocity = r, this._lastWidth = s, o;
  }
  _strokeWidth(t, e) {
    return Math.max(e.maxWidth / (t + 1), e.minWidth);
  }
  _drawCurveSegment(t, e, n) {
    const r = this._ctx;
    r.moveTo(t, e), r.arc(t, e, n, 0, 2 * Math.PI, !1), this._isEmpty = !1;
  }
  _drawCurve(t, e) {
    const n = this._ctx, r = t.endWidth - t.startWidth, s = Math.ceil(t.length()) * 2;
    n.beginPath(), n.fillStyle = e.penColor;
    for (let o = 0; o < s; o += 1) {
      const a = o / s, l = a * a, c = l * a, u = 1 - a, d = u * u, f = d * u;
      let m = f * t.startPoint.x;
      m += 3 * d * a * t.control1.x, m += 3 * u * l * t.control2.x, m += c * t.endPoint.x;
      let g = f * t.startPoint.y;
      g += 3 * d * a * t.control1.y, g += 3 * u * l * t.control2.y, g += c * t.endPoint.y;
      const p = Math.min(t.startWidth + c * r, e.maxWidth);
      this._drawCurveSegment(m, g, p);
    }
    n.closePath(), n.fill();
  }
  _drawDot(t, e) {
    const n = this._ctx, r = e.dotSize > 0 ? e.dotSize : (e.minWidth + e.maxWidth) / 2;
    n.beginPath(), this._drawCurveSegment(t.x, t.y, r), n.closePath(), n.fillStyle = e.penColor, n.fill();
  }
  _fromData(t, e, n) {
    for (const r of t) {
      const { points: s } = r, o = this._getPointGroupOptions(r);
      if (s.length > 1)
        for (let a = 0; a < s.length; a += 1) {
          const l = s[a], c = new kd(l.x, l.y, l.pressure, l.time);
          a === 0 && this._reset(o);
          const u = this._addPoint(c, o);
          u && e(u, o);
        }
      else
        this._reset(o), n(s[0], o);
    }
  }
  toSVG({ includeBackgroundColor: t = !1 } = {}) {
    const e = this._data, n = Math.max(window.devicePixelRatio || 1, 1), r = 0, s = 0, o = this.canvas.width / n, a = this.canvas.height / n, l = document.createElementNS("http://www.w3.org/2000/svg", "svg");
    if (l.setAttribute("xmlns", "http://www.w3.org/2000/svg"), l.setAttribute("xmlns:xlink", "http://www.w3.org/1999/xlink"), l.setAttribute("viewBox", `${r} ${s} ${o} ${a}`), l.setAttribute("width", o.toString()), l.setAttribute("height", a.toString()), t && this.backgroundColor) {
      const c = document.createElement("rect");
      c.setAttribute("width", "100%"), c.setAttribute("height", "100%"), c.setAttribute("fill", this.backgroundColor), l.appendChild(c);
    }
    return this._fromData(e, (c, { penColor: u }) => {
      const d = document.createElement("path");
      if (!isNaN(c.control1.x) && !isNaN(c.control1.y) && !isNaN(c.control2.x) && !isNaN(c.control2.y)) {
        const f = `M ${c.startPoint.x.toFixed(3)},${c.startPoint.y.toFixed(3)} C ${c.control1.x.toFixed(3)},${c.control1.y.toFixed(3)} ${c.control2.x.toFixed(3)},${c.control2.y.toFixed(3)} ${c.endPoint.x.toFixed(3)},${c.endPoint.y.toFixed(3)}`;
        d.setAttribute("d", f), d.setAttribute("stroke-width", (c.endWidth * 2.25).toFixed(3)), d.setAttribute("stroke", u), d.setAttribute("fill", "none"), d.setAttribute("stroke-linecap", "round"), l.appendChild(d);
      }
    }, (c, { penColor: u, dotSize: d, minWidth: f, maxWidth: m }) => {
      const g = document.createElement("circle"), p = d > 0 ? d : (f + m) / 2;
      g.setAttribute("r", p.toString()), g.setAttribute("cx", c.x.toString()), g.setAttribute("cy", c.y.toString()), g.setAttribute("fill", u), l.appendChild(g);
    }), l.outerHTML;
  }
}
function IX(i, t, e) {
  var n = e || {}, r = n.noTrailing, s = r === void 0 ? !1 : r, o = n.noLeading, a = o === void 0 ? !1 : o, l = n.debounceMode, c = l === void 0 ? void 0 : l, u, d = !1, f = 0;
  function m() {
    u && clearTimeout(u);
  }
  function g(b) {
    var v = b || {}, y = v.upcomingOnly, x = y === void 0 ? !1 : y;
    m(), d = !x;
  }
  function p() {
    for (var b = arguments.length, v = new Array(b), y = 0; y < b; y++)
      v[y] = arguments[y];
    var x = this, S = Date.now() - f;
    if (d)
      return;
    function k() {
      f = Date.now(), t.apply(x, v);
    }
    function I() {
      u = void 0;
    }
    !a && c && !u && k(), m(), c === void 0 && S > i ? a ? (f = Date.now(), s || (u = setTimeout(c ? I : k, i))) : k() : s !== !0 && (u = setTimeout(c ? I : k, c === void 0 ? i - S : i));
  }
  return p.cancel = g, p;
}
function RX(i, t, e) {
  var n = {}, r = n.atBegin, s = r === void 0 ? !1 : r;
  return IX(i, t, {
    debounceMode: s !== !1
  });
}
class Ou extends w.PureComponent {
  constructor(t) {
    super(t), Object.defineProperty(this, "canvasRef", { enumerable: !0, configurable: !0, writable: !0, value: w.createRef() }), Object.defineProperty(this, "signaturePad", { enumerable: !0, configurable: !0, writable: !0, value: void 0 }), Object.defineProperty(this, "callResizeHandler", { enumerable: !0, configurable: !0, writable: !0, value: void 0 }), this.state = { canvasWidth: 0, canvasHeight: 0 }, this.callResizeHandler = RX(this.props.debounceInterval, this.handleResize.bind(this));
  }
  componentDidMount() {
    const t = this.canvasRef.current;
    t && (this.props.width && this.props.height || (t.style.width = "100%", window.addEventListener("resize", this.callResizeHandler)), this.signaturePad = new xd(t, this.props.options), this.scaleCanvas(t));
  }
  componentWillUnmount() {
    this.props.width && this.props.height || window.removeEventListener("resize", this.callResizeHandler), this.signaturePad.off();
  }
  get instance() {
    return this.signaturePad;
  }
  get canvas() {
    return this.canvasRef;
  }
  set dotSize(t) {
    this.signaturePad.dotSize = t;
  }
  get dotSize() {
    return this.signaturePad.dotSize;
  }
  set minWidth(t) {
    this.signaturePad.minWidth = t;
  }
  get minWidth() {
    return this.signaturePad.minWidth;
  }
  set maxWidth(t) {
    this.signaturePad.maxWidth = t;
  }
  get maxWidth() {
    return this.signaturePad.maxWidth;
  }
  set throttle(t) {
    this.signaturePad.throttle = t;
  }
  get throttle() {
    return this.signaturePad.throttle;
  }
  set backgroundColor(t) {
    this.signaturePad.backgroundColor = t;
  }
  get backgroundColor() {
    return this.signaturePad.backgroundColor;
  }
  set penColor(t) {
    this.signaturePad.penColor = t;
  }
  get penColor() {
    return this.signaturePad.penColor;
  }
  set velocityFilterWeight(t) {
    this.signaturePad.velocityFilterWeight = t;
  }
  get velocityFilterWeight() {
    return this.signaturePad.velocityFilterWeight;
  }
  isEmpty() {
    return this.signaturePad.isEmpty();
  }
  clear() {
    this.signaturePad.clear();
  }
  fromDataURL(t, e = {}) {
    this.signaturePad.fromDataURL(t, e);
  }
  toDataURL(t, e) {
    return this.signaturePad.toDataURL(t, e);
  }
  toSVG(t) {
    return this.signaturePad.toSVG(t);
  }
  fromData(t) {
    this.signaturePad.fromData(t);
  }
  toData() {
    return this.signaturePad.toData();
  }
  off() {
    this.signaturePad.off();
  }
  on() {
    this.signaturePad.on();
  }
  handleResize() {
    const t = this.canvasRef.current;
    t && this.scaleCanvas(t);
  }
  scaleCanvas(t) {
    const e = Math.max(window.devicePixelRatio || 1, 1), n = (this.props.width || t.offsetWidth) * e, r = (this.props.height || t.offsetHeight) * e, { canvasWidth: s, canvasHeight: o } = this.state;
    if (n === s && r === o) return;
    let a;
    this.props.redrawOnResize && this.signaturePad && !this.signaturePad.isEmpty() && (a = this.signaturePad.toDataURL()), t.width = n, t.height = r, this.setState({ canvasWidth: n, canvasHeight: r });
    const l = t.getContext("2d");
    l && l.scale(e, e), a ? this.signaturePad.fromDataURL(a) : this.signaturePad && this.signaturePad.clear();
  }
  render() {
    const { canvasProps: t } = this.props;
    return w.createElement("canvas", Object.assign({ "data-testid": "canvas-element", ref: this.canvasRef }, t));
  }
}
Object.defineProperty(Ou, "displayName", { enumerable: !0, configurable: !0, writable: !0, value: "react-signature-pad-wrapper" }), Object.defineProperty(Ou, "propTypes", { enumerable: !0, configurable: !0, writable: !0, value: { width: Rs.number, height: Rs.number, options: Rs.object, canvasProps: Rs.object, redrawOnResize: Rs.bool.isRequired, debounceInterval: Rs.number.isRequired } }), Object.defineProperty(Ou, "defaultProps", { enumerable: !0, configurable: !0, writable: !0, value: { redrawOnResize: !1, debounceInterval: 150 } });
const OX = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...ua, ...i });
  Ee(() => {
    n({ ...ua, ...i });
  }, [i]);
  const r = Math.random().toString(36).substring(2, 15), s = Se.createRef(), o = () => {
    const c = s.current;
    c && c.instance.clear();
    const u = pn(e);
    u.value = [], n(u), t(u);
  }, a = () => {
    const c = s.current;
    if (!c)
      return;
    const u = pn(e);
    c.isEmpty() ? u.value = [] : u.value = c.toData(), n(u), t(u);
  };
  Ee(() => {
    e.value && s.current && s.current.fromData(e.value);
  }, [e]);
  const l = {
    height: "10rem",
    width: "100%",
    border: "1px solid #cccccc",
    boxShadow: "0 0 0.32rem #ccc inset"
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { htmlFor: r, children: e.label }),
    /* @__PURE__ */ _.jsx("div", { children: /* @__PURE__ */ _.jsx(
      Ou,
      {
        ref: s,
        options: { penColor: "rgb(0, 0, 0)" },
        redrawOnResize: !0,
        canvasProps: { style: l }
      }
    ) }),
    /* @__PURE__ */ _.jsx("button", { onClick: o, children: "clear" }),
    /* @__PURE__ */ _.jsx("button", { onClick: a, children: "save" })
  ] }) }) });
}, VX = (i) => ({});
var Uy;
function wg() {
  return wg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, wg.apply(null, arguments);
}
var MX = function(t) {
  return /* @__PURE__ */ w.createElement("svg", wg({
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 576 512"
  }, t), Uy || (Uy = /* @__PURE__ */ w.createElement("path", {
    d: "M64 0C28.7 0 0 28.7 0 64v384c0 35.3 28.7 64 64 64h256c35.3 0 64-28.7 64-64v-19.3c-2.7 1.1-5.4 2-8.2 2.7l-60.1 15c-3 .7-6 1.2-9 1.4-.9.1-1.8.2-2.7.2h-64c-6.1 0-11.6-3.4-14.3-8.8l-8.8-17.7c-1.7-3.4-5.1-5.5-8.8-5.5s-7.2 2.1-8.8 5.5l-8.8 17.7c-2.9 5.9-9.2 9.4-15.7 8.8s-12.1-5.1-13.9-11.3L144 381l-9.8 32.8c-6.1 20.3-24.8 34.2-46 34.2H80c-8.8 0-16-7.2-16-16s7.2-16 16-16h8.2c7.1 0 13.3-4.6 15.3-11.4l14.9-49.5c3.4-11.3 13.8-19.1 25.6-19.1s22.2 7.8 25.6 19.1l11.6 38.6c7.4-6.2 16.8-9.7 26.8-9.7 15.9 0 30.4 9 37.5 23.2l4.4 8.8h8.9c-3.1-8.8-3.7-18.4-1.4-27.8l15-60.1c2.8-11.3 8.6-21.5 16.8-29.7l94.8-94.8V160H256c-17.7 0-32-14.3-32-32V0zm192 0v128h128zm293.8 139.7c-15.6-15.6-40.9-15.6-56.6 0l-29.4 29.4 71 71 29.4-29.4c15.6-15.6 15.6-40.9 0-56.6zM311.9 321c-4.1 4.1-7 9.2-8.4 14.9l-15 60.1c-1.4 5.5.2 11.2 4.2 15.2s9.7 5.6 15.2 4.2l60.1-15c5.6-1.4 10.8-4.3 14.9-8.4l129.2-129.3-71-71z"
  })));
};
const DX = ({ item: i, onChange: t }) => {
  const [e, n] = Ne({ ...ua, ...i }), r = (s) => {
    const o = { ...e, [s.target.name]: s.target.value };
    n(o), t(o);
  };
  return Ee(() => {
    n({ ...ua, ...i });
  }, [i]), /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx(or, { activeIndex: 0, children: /* @__PURE__ */ _.jsx(di, { header: "General", children: /* @__PURE__ */ _.jsx("div", { className: "form-item flex flex-column gap-4", children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Label" }),
    /* @__PURE__ */ _.jsx(
      _t,
      {
        id: "label",
        value: e.label || "",
        name: "label",
        onChange: r,
        className: "w-full"
      }
    )
  ] }) }) }, "general") }) });
}, LX = (i) => ({}), NX = new ps({
  type: ua.type,
  form: new zn({
    render: OX,
    validation: VX
  }),
  settings: new zn({
    render: DX,
    validation: LX
  }),
  heading: "Signature",
  description: "Signature capture block",
  hidden: !1,
  icon: MX,
  data: ua
}), Js = {
  id: br(),
  type: "embedded-form",
  label: "",
  form_id: "",
  items: []
}, BX = ({ item: i, onChange: t }) => {
  const { config: e } = _r(), [n, r] = Ne({ ...Js, ...i }), [s, o] = Ne();
  Ee(() => {
    r({ ...Js, ...i }), n.form_id && n.form_id != (s == null ? void 0 : s.id) && o(e.forms.find((l) => l.id === n.form_id));
  }, [i]);
  const a = (l) => {
    const c = pn(n);
    c.items = l.items, r(c), t(c);
  };
  return s ? /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
    /* @__PURE__ */ _.jsx("label", { children: n.label }),
    /* @__PURE__ */ _.jsx(
      kQ,
      {
        onChange: a,
        form: s,
        config: e
      }
    )
  ] }) }) }) : /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("div", { children: "No child form found" }) });
};
var qy;
function yg() {
  return yg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e) ({}).hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, yg.apply(null, arguments);
}
var FX = function(t) {
  return /* @__PURE__ */ w.createElement("svg", yg({
    xmlns: "http://www.w3.org/2000/svg",
    viewBox: "0 0 512 512"
  }, t), qy || (qy = /* @__PURE__ */ w.createElement("path", {
    d: "M448 416v-64H64v64zm0 64H64c-35.3 0-64-28.7-64-64v-64c0-35.3 28.7-64 64-64h384c35.3 0 64 28.7 64 64v64c0 35.3-28.7 64-64 64M288 160c0 35.3-28.7 64-64 64H64c-35.3 0-64-28.7-64-64V96c0-35.3 28.7-64 64-64h304c44.2 0 80 35.8 80 80v16h38.1c21.4 0 32.1 25.9 17 41L433 239c-9.4 9.4-24.6 9.4-33.9 0L329 169c-15.1-15.1-4.4-41 17-41h38.1v-16c0-8.8-7.2-16-16-16h-80v64z"
  })));
};
const $X = ({ item: i, onChange: t }) => {
  const { form: e } = Hr(), { config: n } = _r(), [r, s] = Ne({ ...Js, ...i }), o = (l) => {
    const c = { ...r, [l.target.name]: l.target.value };
    s(c), t(c);
  };
  Ee(() => {
    s({ ...Js, ...i });
  }, [i]);
  const a = n.forms.map((l) => ({ value: l.id, label: l.label })).filter((l) => l.value != e.id);
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx(or, { activeIndex: 0, children: /* @__PURE__ */ _.jsxs(di, { header: "General", children: [
    /* @__PURE__ */ _.jsx("div", { className: "form-item flex flex-column gap-4", children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
      /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Label" }),
      /* @__PURE__ */ _.jsx(
        _t,
        {
          id: "label",
          value: r.label || "",
          name: "label",
          onChange: o,
          className: "w-full"
        }
      )
    ] }) }),
    /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
      /* @__PURE__ */ _.jsx("label", { htmlFor: "form_id", children: "Form" }),
      /* @__PURE__ */ _.jsx(
        Qp,
        {
          id: "form_id",
          value: r.form_id || "",
          onChange: o,
          options: a,
          name: "form_id",
          showClear: !0,
          placeholder: "Select a form",
          className: "w-full",
          children: " "
        }
      )
    ] })
  ] }, "general") }) });
}, zX = (i) => ({}), HX = new ps({
  type: Js.type,
  form: new zn({
    render: BX,
    validation: null
  }),
  settings: new zn({
    render: $X,
    validation: zX
  }),
  heading: "Embedded form",
  description: "Embed a form",
  hidden: !1,
  icon: FX,
  data: Js
}), ab = new Id(new Pd({ label: "Special" }));
ab.registerItem(NX);
ab.registerItem(HX);
const ks = new Id();
ks.addRegistry(Ta);
ks.addRegistry(xE);
ks.addRegistry(ab);
const lb = ({ item: i, onChange: t }) => {
  const e = ks.getByItem(i);
  return i && e ? Se.createElement(e.form.render, {
    item: i,
    onChange: t
  }) : Se.createElement(
    () => /* @__PURE__ */ _.jsxs("div", { children: [
      "The component ",
      i.type,
      " was not found."
    ] })
  );
}, jX = ({ item: i, onChange: t }) => {
  const e = ks.getByItem(i);
  return i && e ? Se.createElement(e.settings.render, {
    item: i,
    onChange: t
  }) : Se.createElement(
    () => /* @__PURE__ */ _.jsxs("div", { children: [
      "The component ",
      i.type,
      " was not found."
    ] })
  );
};
var WX = {
  root: "p-button-group p-component"
}, fu = et.extend({
  defaultProps: {
    __TYPE: "ButtonGroup",
    children: void 0
  },
  css: {
    classes: WX
  }
}), h5 = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = fu.getProps(i, n), s = w.useRef(t), o = fu.setMetaData({
    props: r
  }), a = o.ptm, l = o.cx, c = o.isUnstyled;
  sr(fu.css.styles, c, {
    name: "buttongroup"
  }), w.useEffect(function() {
    z.combinedRefs(s, t);
  }, [s, t]);
  var u = e({
    ref: s,
    className: Re(l("root")),
    role: "group"
  }, fu.getOtherProps(r), a("root"));
  return /* @__PURE__ */ w.createElement("span", u, r.children);
}));
h5.displayName = "ButtonGroup";
function _g() {
  return _g = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, _g.apply(this, arguments);
}
var f5 = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", _g({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    fillRule: "evenodd",
    clipRule: "evenodd",
    d: "M7 14H11.8C12.3835 14 12.9431 13.7682 13.3556 13.3556C13.7682 12.9431 14 12.3835 14 11.8V2.2C14 1.61652 13.7682 1.05694 13.3556 0.644365C12.9431 0.231785 12.3835 0 11.8 0H2.2C1.61652 0 1.05694 0.231785 0.644365 0.644365C0.231785 1.05694 0 1.61652 0 2.2V7C0 7.15913 0.063214 7.31174 0.175736 7.42426C0.288258 7.53679 0.44087 7.6 0.6 7.6C0.75913 7.6 0.911742 7.53679 1.02426 7.42426C1.13679 7.31174 1.2 7.15913 1.2 7V2.2C1.2 1.93478 1.30536 1.68043 1.49289 1.49289C1.68043 1.30536 1.93478 1.2 2.2 1.2H11.8C12.0652 1.2 12.3196 1.30536 12.5071 1.49289C12.6946 1.68043 12.8 1.93478 12.8 2.2V11.8C12.8 12.0652 12.6946 12.3196 12.5071 12.5071C12.3196 12.6946 12.0652 12.8 11.8 12.8H7C6.84087 12.8 6.68826 12.8632 6.57574 12.9757C6.46321 13.0883 6.4 13.2409 6.4 13.4C6.4 13.5591 6.46321 13.7117 6.57574 13.8243C6.68826 13.9368 6.84087 14 7 14ZM9.77805 7.42192C9.89013 7.534 10.0415 7.59788 10.2 7.59995C10.3585 7.59788 10.5099 7.534 10.622 7.42192C10.7341 7.30985 10.798 7.15844 10.8 6.99995V3.94242C10.8066 3.90505 10.8096 3.86689 10.8089 3.82843C10.8079 3.77159 10.7988 3.7157 10.7824 3.6623C10.756 3.55552 10.701 3.45698 10.622 3.37798C10.5099 3.2659 10.3585 3.20202 10.2 3.19995H7.00002C6.84089 3.19995 6.68828 3.26317 6.57576 3.37569C6.46324 3.48821 6.40002 3.64082 6.40002 3.79995C6.40002 3.95908 6.46324 4.11169 6.57576 4.22422C6.68828 4.33674 6.84089 4.39995 7.00002 4.39995H8.80006L6.19997 7.00005C6.10158 7.11005 6.04718 7.25246 6.04718 7.40005C6.04718 7.54763 6.10158 7.69004 6.19997 7.80005C6.30202 7.91645 6.44561 7.98824 6.59997 8.00005C6.75432 7.98824 6.89791 7.91645 6.99997 7.80005L9.60002 5.26841V6.99995C9.6021 7.15844 9.66598 7.30985 9.77805 7.42192ZM1.4 14H3.8C4.17066 13.9979 4.52553 13.8498 4.78763 13.5877C5.04973 13.3256 5.1979 12.9707 5.2 12.6V10.2C5.1979 9.82939 5.04973 9.47452 4.78763 9.21242C4.52553 8.95032 4.17066 8.80215 3.8 8.80005H1.4C1.02934 8.80215 0.674468 8.95032 0.412371 9.21242C0.150274 9.47452 0.00210008 9.82939 0 10.2V12.6C0.00210008 12.9707 0.150274 13.3256 0.412371 13.5877C0.674468 13.8498 1.02934 13.9979 1.4 14ZM1.25858 10.0586C1.29609 10.0211 1.34696 10 1.4 10H3.8C3.85304 10 3.90391 10.0211 3.94142 10.0586C3.97893 10.0961 4 10.147 4 10.2V12.6C4 12.6531 3.97893 12.704 3.94142 12.7415C3.90391 12.779 3.85304 12.8 3.8 12.8H1.4C1.34696 12.8 1.29609 12.779 1.25858 12.7415C1.22107 12.704 1.2 12.6531 1.2 12.6V10.2C1.2 10.147 1.22107 10.0961 1.25858 10.0586Z",
    fill: "currentColor"
  }));
}));
f5.displayName = "WindowMaximizeIcon";
function Cg() {
  return Cg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, Cg.apply(this, arguments);
}
var m5 = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = _i.getPTI(i);
  return /* @__PURE__ */ w.createElement("svg", Cg({
    ref: t,
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, e), /* @__PURE__ */ w.createElement("path", {
    fillRule: "evenodd",
    clipRule: "evenodd",
    d: "M11.8 0H2.2C1.61652 0 1.05694 0.231785 0.644365 0.644365C0.231785 1.05694 0 1.61652 0 2.2V7C0 7.15913 0.063214 7.31174 0.175736 7.42426C0.288258 7.53679 0.44087 7.6 0.6 7.6C0.75913 7.6 0.911742 7.53679 1.02426 7.42426C1.13679 7.31174 1.2 7.15913 1.2 7V2.2C1.2 1.93478 1.30536 1.68043 1.49289 1.49289C1.68043 1.30536 1.93478 1.2 2.2 1.2H11.8C12.0652 1.2 12.3196 1.30536 12.5071 1.49289C12.6946 1.68043 12.8 1.93478 12.8 2.2V11.8C12.8 12.0652 12.6946 12.3196 12.5071 12.5071C12.3196 12.6946 12.0652 12.8 11.8 12.8H7C6.84087 12.8 6.68826 12.8632 6.57574 12.9757C6.46321 13.0883 6.4 13.2409 6.4 13.4C6.4 13.5591 6.46321 13.7117 6.57574 13.8243C6.68826 13.9368 6.84087 14 7 14H11.8C12.3835 14 12.9431 13.7682 13.3556 13.3556C13.7682 12.9431 14 12.3835 14 11.8V2.2C14 1.61652 13.7682 1.05694 13.3556 0.644365C12.9431 0.231785 12.3835 0 11.8 0ZM6.368 7.952C6.44137 7.98326 6.52025 7.99958 6.6 8H9.8C9.95913 8 10.1117 7.93678 10.2243 7.82426C10.3368 7.71174 10.4 7.55913 10.4 7.4C10.4 7.24087 10.3368 7.08826 10.2243 6.97574C10.1117 6.86321 9.95913 6.8 9.8 6.8H8.048L10.624 4.224C10.73 4.11026 10.7877 3.95982 10.7849 3.80438C10.7822 3.64894 10.7192 3.50063 10.6093 3.3907C10.4994 3.28077 10.3511 3.2178 10.1956 3.21506C10.0402 3.21232 9.88974 3.27002 9.776 3.376L7.2 5.952V4.2C7.2 4.04087 7.13679 3.88826 7.02426 3.77574C6.91174 3.66321 6.75913 3.6 6.6 3.6C6.44087 3.6 6.28826 3.66321 6.17574 3.77574C6.06321 3.88826 6 4.04087 6 4.2V7.4C6.00042 7.47975 6.01674 7.55862 6.048 7.632C6.07656 7.70442 6.11971 7.7702 6.17475 7.82524C6.2298 7.88029 6.29558 7.92344 6.368 7.952ZM1.4 8.80005H3.8C4.17066 8.80215 4.52553 8.95032 4.78763 9.21242C5.04973 9.47452 5.1979 9.82939 5.2 10.2V12.6C5.1979 12.9707 5.04973 13.3256 4.78763 13.5877C4.52553 13.8498 4.17066 13.9979 3.8 14H1.4C1.02934 13.9979 0.674468 13.8498 0.412371 13.5877C0.150274 13.3256 0.00210008 12.9707 0 12.6V10.2C0.00210008 9.82939 0.150274 9.47452 0.412371 9.21242C0.674468 8.95032 1.02934 8.80215 1.4 8.80005ZM3.94142 12.7415C3.97893 12.704 4 12.6531 4 12.6V10.2C4 10.147 3.97893 10.0961 3.94142 10.0586C3.90391 10.0211 3.85304 10 3.8 10H1.4C1.34696 10 1.29609 10.0211 1.25858 10.0586C1.22107 10.0961 1.2 10.147 1.2 10.2V12.6C1.2 12.6531 1.22107 12.704 1.25858 12.7415C1.29609 12.779 1.34696 12.8 1.4 12.8H3.8C3.85304 12.8 3.90391 12.779 3.94142 12.7415Z",
    fill: "currentColor"
  }));
}));
m5.displayName = "WindowMinimizeIcon";
function kg() {
  return kg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, kg.apply(this, arguments);
}
function cc(i) {
  "@babel/helpers - typeof";
  return cc = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, cc(i);
}
function xg(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function UX(i) {
  if (Array.isArray(i)) return xg(i);
}
function qX(i) {
  if (typeof Symbol < "u" && i[Symbol.iterator] != null || i["@@iterator"] != null) return Array.from(i);
}
function g5(i, t) {
  if (i) {
    if (typeof i == "string") return xg(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return xg(i, t);
  }
}
function GX() {
  throw new TypeError(`Invalid attempt to spread non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function KX(i) {
  return UX(i) || qX(i) || g5(i) || GX();
}
function ZX(i, t) {
  if (cc(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (cc(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function YX(i) {
  var t = ZX(i, "string");
  return cc(t) === "symbol" ? t : String(t);
}
function cb(i, t, e) {
  return t = YX(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
function JX(i) {
  if (Array.isArray(i)) return i;
}
function XX(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function QX() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function Yr(i, t) {
  return JX(i) || XX(i, t) || g5(i, t) || QX();
}
var eQ = "", xl = et.extend({
  defaultProps: {
    __TYPE: "FocusTrap",
    children: void 0
  },
  css: {
    styles: eQ
  },
  getProps: function(t) {
    return z.getMergedProps(t, xl.defaultProps);
  },
  getOtherProps: function(t) {
    return z.getDiffProps(t, xl.defaultProps);
  }
});
function Gy(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function tQ(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Gy(Object(e), !0).forEach(function(n) {
      cb(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : Gy(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var nQ = /* @__PURE__ */ Se.memo(/* @__PURE__ */ Se.forwardRef(function(i, t) {
  var e = Se.useRef(null), n = Se.useRef(null), r = Se.useRef(null), s = Se.useContext(cn), o = xl.getProps(i, s), a = {
    props: o
  };
  Ls(xl.css.styles, {
    name: "focustrap"
  });
  var l = xl.setMetaData(tQ({}, a));
  l.ptm, Se.useImperativeHandle(t, function() {
    return {
      props: o,
      getInk: function() {
        return n.current;
      },
      getTarget: function() {
        return e.current;
      }
    };
  }), rr(function() {
    o.disabled || (e.current = c(), u(e.current));
  });
  var c = function() {
    return n.current && n.current.parentElement;
  }, u = function(b) {
    var v = o || {}, y = v.autoFocusSelector, x = y === void 0 ? "" : y, S = v.firstFocusableSelector, k = S === void 0 ? "" : S, I = v.autoFocus, A = I === void 0 ? !1 : I, L = "".concat(d(x)), R = "[autofocus]".concat(L, ", [data-pc-autofocus='true']").concat(L), O = j.getFirstFocusableElement(b, R);
    A && !O && (O = j.getFirstFocusableElement(b, d(k))), j.focus(O);
  }, d = function(b) {
    return ':not(.p-hidden-focusable):not([data-p-hidden-focusable="true"])'.concat(b ?? "");
  }, f = function(b) {
    var v, y = b.currentTarget, x = b.relatedTarget, S = x === y.$_pfocustrap_lasthiddenfocusableelement || !((v = e.current) !== null && v !== void 0 && v.contains(x)) ? j.getFirstFocusableElement(y.parentElement, d(y.$_pfocustrap_focusableselector)) : y.$_pfocustrap_lasthiddenfocusableelement;
    j.focus(S);
  }, m = function(b) {
    var v, y = b.currentTarget, x = b.relatedTarget, S = x === y.$_pfocustrap_firsthiddenfocusableelement || !((v = e.current) !== null && v !== void 0 && v.contains(x)) ? j.getLastFocusableElement(y.parentElement, d(y.$_pfocustrap_focusableselector)) : y.$_pfocustrap_firsthiddenfocusableelement;
    j.focus(S);
  }, g = function() {
    var b = o || {}, v = b.tabIndex, y = v === void 0 ? 0 : v, x = function(A, L) {
      return /* @__PURE__ */ Se.createElement("span", {
        ref: L === "firstfocusableelement" ? n : r,
        className: "p-hidden-accessible p-hidden-focusable",
        tabIndex: y,
        role: "presentation",
        "aria-hidden": !0,
        "data-p-hidden-accessible": !0,
        "data-p-hidden-focusable": !0,
        onFocus: A,
        "data-pc-section": L
      });
    }, S = x(f, "firstfocusableelement"), k = x(m, "lastfocusableelement");
    return S.ref.current && k.ref.current && (S.ref.current.$_pfocustrap_lasthiddenfocusableelement = k.ref.current, k.ref.current.$_pfocustrap_firsthiddenfocusableelement = S.ref.current), /* @__PURE__ */ Se.createElement(Se.Fragment, null, S, o.children, k);
  };
  return g();
})), iQ = nQ;
function Ky(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function rQ(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Ky(Object(e), !0).forEach(function(n) {
      cb(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : Ky(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var sQ = {
  closeButtonIcon: "p-dialog-header-close-icon",
  closeButton: "p-dialog-header-icon p-dialog-header-close p-link",
  maximizableIcon: "p-dialog-header-maximize-icon",
  maximizableButton: "p-dialog-header-icon p-dialog-header-maximize p-link",
  header: function(t) {
    var e = t.props;
    return Re("p-dialog-header", e.headerClassName);
  },
  headerTitle: "p-dialog-title",
  headerIcons: "p-dialog-header-icons",
  content: function(t) {
    var e = t.props;
    return Re("p-dialog-content", e.contentClassName);
  },
  footer: function(t) {
    var e = t.props;
    return Re("p-dialog-footer", e.footerClassName);
  },
  mask: function(t) {
    var e = t.props, n = t.maskVisibleState, r = ["center", "left", "right", "top", "top-left", "top-right", "bottom", "bottom-left", "bottom-right"], s = r.find(function(o) {
      return o === e.position || o.replace("-", "") === e.position;
    });
    return Re("p-dialog-mask", s ? "p-dialog-".concat(s) : "", {
      "p-component-overlay p-component-overlay-enter": e.modal,
      "p-dialog-visible": n,
      "p-dialog-draggable": e.draggable,
      "p-dialog-resizable": e.resizable
    }, e.maskClassName);
  },
  root: function(t) {
    var e = t.props, n = t.maximized, r = t.context;
    return Re("p-dialog p-component", {
      "p-dialog-rtl": e.rtl,
      "p-dialog-maximized": n,
      "p-dialog-default": !n,
      "p-input-filled": r && r.inputStyle === "filled" || tn.inputStyle === "filled",
      "p-ripple-disabled": r && r.ripple === !1 || tn.ripple === !1
    });
  },
  transition: "p-dialog"
}, oQ = `
@layer primereact {
    .p-dialog-mask {
        background-color: transparent;
        transition-property: background-color;
    }

    .p-dialog-visible {
        display: flex;
    }

    .p-dialog-mask.p-component-overlay {
        pointer-events: auto;
    }

    .p-dialog {
        display: flex;
        flex-direction: column;
        pointer-events: auto;
        max-height: 90%;
        transform: scale(1);
        position: relative;
    }

    .p-dialog-content {
        overflow-y: auto;
        flex-grow: 1;
    }

    .p-dialog-header {
        display: flex;
        align-items: center;
        flex-shrink: 0;
    }

    .p-dialog-footer {
        flex-shrink: 0;
    }

    .p-dialog .p-dialog-header-icons {
        display: flex;
        align-items: center;
        align-self: flex-start;
        flex-shrink: 0;
    }

    .p-dialog .p-dialog-header-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        position: relative;
    }

    .p-dialog .p-dialog-title {
        flex-grow: 1;
    }

    /* Fluid */
    .p-fluid .p-dialog-footer .p-button {
        width: auto;
    }

    /* Animation */
    /* Center */
    .p-dialog-enter {
        opacity: 0;
        transform: scale(0.7);
    }

    .p-dialog-enter-active {
        opacity: 1;
        transform: scale(1);
        transition: all 150ms cubic-bezier(0, 0, 0.2, 1);
    }

    .p-dialog-enter-done {
        transform: none;
    }

    .p-dialog-exit-active {
        opacity: 0;
        transform: scale(0.7);
        transition: all 150ms cubic-bezier(0.4, 0, 0.2, 1);
    }

    /* Top, Bottom, Left, Right, Top* and Bottom* */
    .p-dialog-top .p-dialog,
    .p-dialog-bottom .p-dialog,
    .p-dialog-left .p-dialog,
    .p-dialog-right .p-dialog,
    .p-dialog-top-left .p-dialog,
    .p-dialog-top-right .p-dialog,
    .p-dialog-bottom-left .p-dialog,
    .p-dialog-bottom-right .p-dialog {
        margin: 0.75em;
    }

    .p-dialog-top .p-dialog-enter,
    .p-dialog-top .p-dialog-exit-active {
        transform: translate3d(0px, -100%, 0px);
    }

    .p-dialog-bottom .p-dialog-enter,
    .p-dialog-bottom .p-dialog-exit-active {
        transform: translate3d(0px, 100%, 0px);
    }

    .p-dialog-left .p-dialog-enter,
    .p-dialog-left .p-dialog-exit-active,
    .p-dialog-top-left .p-dialog-enter,
    .p-dialog-top-left .p-dialog-exit-active,
    .p-dialog-bottom-left .p-dialog-enter,
    .p-dialog-bottom-left .p-dialog-exit-active {
        transform: translate3d(-100%, 0px, 0px);
    }

    .p-dialog-right .p-dialog-enter,
    .p-dialog-right .p-dialog-exit-active,
    .p-dialog-top-right .p-dialog-enter,
    .p-dialog-top-right .p-dialog-exit-active,
    .p-dialog-bottom-right .p-dialog-enter,
    .p-dialog-bottom-right .p-dialog-exit-active {
        transform: translate3d(100%, 0px, 0px);
    }

    .p-dialog-top .p-dialog-enter-active,
    .p-dialog-bottom .p-dialog-enter-active,
    .p-dialog-left .p-dialog-enter-active,
    .p-dialog-top-left .p-dialog-enter-active,
    .p-dialog-bottom-left .p-dialog-enter-active,
    .p-dialog-right .p-dialog-enter-active,
    .p-dialog-top-right .p-dialog-enter-active,
    .p-dialog-bottom-right .p-dialog-enter-active {
        transform: translate3d(0px, 0px, 0px);
        transition: all 0.3s ease-out;
    }

    .p-dialog-top .p-dialog-exit-active,
    .p-dialog-bottom .p-dialog-exit-active,
    .p-dialog-left .p-dialog-exit-active,
    .p-dialog-top-left .p-dialog-exit-active,
    .p-dialog-bottom-left .p-dialog-exit-active,
    .p-dialog-right .p-dialog-exit-active,
    .p-dialog-top-right .p-dialog-exit-active,
    .p-dialog-bottom-right .p-dialog-exit-active {
        transition: all 0.3s ease-out;
    }

    /* Maximize */
    .p-dialog-maximized {
        transition: none;
        transform: none;
        margin: 0;
        width: 100vw !important;
        height: 100vh !important;
        max-height: 100%;
        top: 0px !important;
        left: 0px !important;
    }

    .p-dialog-maximized .p-dialog-content {
        flex-grow: 1;
    }

    .p-confirm-dialog .p-dialog-content {
        display: flex;
        align-items: center;
    }

    /* Resizable */
    .p-dialog .p-resizable-handle {
        position: absolute;
        font-size: 0.1px;
        display: block;
        cursor: se-resize;
        width: 12px;
        height: 12px;
        right: 1px;
        bottom: 1px;
    }

    .p-dialog-draggable .p-dialog-header {
        cursor: move;
    }
}
`, aQ = {
  mask: function(t) {
    var e = t.props;
    return rQ({
      position: "fixed",
      height: "100%",
      width: "100%",
      left: 0,
      top: 0,
      display: "flex",
      justifyContent: e.position === "left" || e.position === "top-left" || e.position === "bottom-left" ? "flex-start" : e.position === "right" || e.position === "top-right" || e.position === "bottom-right" ? "flex-end" : "center",
      alignItems: e.position === "top" || e.position === "top-left" || e.position === "top-right" ? "flex-start" : e.position === "bottom" || e.position === "bottom-left" || e.position === "bottom-right" ? "flex-end" : "center",
      pointerEvents: !e.modal && "none"
    }, e.maskStyle);
  }
}, mu = et.extend({
  defaultProps: {
    __TYPE: "Dialog",
    __parentMetadata: null,
    appendTo: null,
    ariaCloseIconLabel: null,
    baseZIndex: 0,
    blockScroll: !1,
    breakpoints: null,
    className: null,
    closable: !0,
    closeIcon: null,
    closeOnEscape: !0,
    content: null,
    contentClassName: null,
    contentStyle: null,
    dismissableMask: !1,
    draggable: !0,
    focusOnShow: !0,
    footer: null,
    footerClassName: null,
    header: null,
    headerClassName: null,
    headerStyle: null,
    icons: null,
    id: null,
    keepInViewport: !0,
    maskClassName: null,
    maskStyle: null,
    maximizable: !1,
    maximizeIcon: null,
    maximized: !1,
    minX: 0,
    minY: 0,
    minimizeIcon: null,
    modal: !0,
    onClick: null,
    onDrag: null,
    onDragEnd: null,
    onDragStart: null,
    onHide: null,
    onMaskClick: null,
    onMaximize: null,
    onResize: null,
    onResizeEnd: null,
    onResizeStart: null,
    onShow: null,
    position: "center",
    resizable: !0,
    rtl: !1,
    showHeader: !0,
    style: null,
    transitionOptions: null,
    visible: !1,
    children: void 0
  },
  css: {
    classes: sQ,
    styles: oQ,
    inlineStyles: aQ
  }
});
function Zy(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function Bf(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Zy(Object(e), !0).forEach(function(n) {
      cb(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : Zy(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var p5 = /* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = mu.getProps(i, n), s = r.id ? r.id : Kp(), o = w.useState(s), a = Yr(o, 2), l = a[0];
  a[1];
  var c = w.useState(!1), u = Yr(c, 2), d = u[0], f = u[1], m = w.useState(!1), g = Yr(m, 2), p = g[0], b = g[1], v = w.useState(r.maximized), y = Yr(v, 2), x = y[0], S = y[1], k = w.useRef(null), I = w.useRef(null), A = w.useRef(null), L = w.useRef(null), R = w.useRef(null), O = w.useRef(null), N = w.useRef(null), le = w.useRef(!1), te = w.useRef(!1), me = w.useRef(null), ce = w.useRef(null), Ae = w.useRef(null), Fe = w.useRef(s), be = w.useRef(null), re = r.onMaximize ? r.maximized : x, B = p && (r.blockScroll || r.maximizable && re), X = r.closable && r.closeOnEscape && p, he = NG("dialog", X), Q = mu.setMetaData(Bf(Bf({
    props: r
  }, r.__parentMetadata), {}, {
    state: {
      id: l,
      maximized: re,
      containerVisible: d
    }
  })), Y = Q.ptm, we = Q.cx, ye = Q.sx, Ce = Q.isUnstyled;
  sr(mu.css.styles, Ce, {
    name: "dialog"
  }), hE({
    callback: function(P) {
      Vt(P);
    },
    when: X && he,
    priority: [uE.DIALOG, he]
  });
  var J = is({
    type: "mousemove",
    target: function() {
      return window.document;
    },
    listener: function(P) {
      return U(P);
    }
  }), F = Yr(J, 2), H = F[0], pe = F[1], W = is({
    type: "mouseup",
    target: function() {
      return window.document;
    },
    listener: function(P) {
      return _e(P);
    }
  }), We = Yr(W, 2), Ye = We[0], Ze = We[1], Je = is({
    type: "mousemove",
    target: function() {
      return window.document;
    },
    listener: function(P) {
      return ti(P);
    }
  }), pt = Yr(Je, 2), Rn = pt[0], rn = pt[1], $t = is({
    type: "mouseup",
    target: function() {
      return window.document;
    },
    listener: function(P) {
      return sn(P);
    }
  }), zt = Yr($t, 2), bn = zt[0], On = zt[1], Vt = function(P) {
    r.onHide(), P.preventDefault();
  }, Ht = function() {
    var P = document.activeElement, q = P && k.current && k.current.contains(P);
    !q && r.closable && r.showHeader && N.current && N.current.focus();
  }, hi = function(P) {
    A.current = P.target, r.onPointerDown && r.onPointerDown(P);
  }, jt = function(P) {
    r.dismissableMask && r.modal && I.current === P.target && !A.current && Vt(P), r.onMaskClick && r.onMaskClick(P), A.current = null;
  }, Wt = function(P) {
    r.onMaximize ? r.onMaximize({
      originalEvent: P,
      maximized: !re
    }) : S(function(q) {
      return !q;
    }), P.preventDefault();
  }, Yt = function(P) {
    j.hasClass(P.target, "p-dialog-header-icon") || j.hasClass(P.target.parentElement, "p-dialog-header-icon") || r.draggable && (le.current = !0, me.current = P.pageX, ce.current = P.pageY, k.current.style.margin = "0", j.addClass(document.body, "p-unselectable-text"), r.onDragStart && r.onDragStart(P));
  }, ti = function(P) {
    if (le.current) {
      var q = j.getOuterWidth(k.current), ge = j.getOuterHeight(k.current), Oe = P.pageX - me.current, ue = P.pageY - ce.current, M = k.current.getBoundingClientRect(), $ = M.left + Oe, Z = M.top + ue, ne = j.getViewport(), oe = getComputedStyle(k.current), se = parseFloat(oe.marginLeft), ve = parseFloat(oe.marginTop);
      k.current.style.position = "fixed", r.keepInViewport ? ($ >= r.minX && $ + q < ne.width && (me.current = P.pageX, k.current.style.left = $ - se + "px"), Z >= r.minY && Z + ge < ne.height && (ce.current = P.pageY, k.current.style.top = Z - ve + "px")) : (me.current = P.pageX, k.current.style.left = $ - se + "px", ce.current = P.pageY, k.current.style.top = Z - ve + "px"), r.onDrag && r.onDrag(P);
    }
  }, sn = function(P) {
    le.current && (le.current = !1, j.removeClass(document.body, "p-unselectable-text"), r.onDragEnd && r.onDragEnd(P));
  }, un = function(P) {
    r.resizable && (te.current = !0, me.current = P.pageX, ce.current = P.pageY, j.addClass(document.body, "p-unselectable-text"), r.onResizeStart && r.onResizeStart(P));
  }, Le = function(P, q, ge) {
    !ge && (ge = j.getViewport());
    var Oe = parseInt(P);
    return /^(\d+|(\.\d+))(\.\d+)?%$/.test(P) ? Oe * (ge[q] / 100) : Oe;
  }, U = function(P) {
    if (te.current) {
      var q = P.pageX - me.current, ge = P.pageY - ce.current, Oe = j.getOuterWidth(k.current), ue = j.getOuterHeight(k.current), M = k.current.getBoundingClientRect(), $ = j.getViewport(), Z = !parseInt(k.current.style.top) || !parseInt(k.current.style.left), ne = Le(k.current.style.minWidth, "width", $), oe = Le(k.current.style.minHeight, "height", $), se = Oe + q, ve = ue + ge;
      Z && (se = se + q, ve = ve + ge), (!ne || se > ne) && M.left + se < $.width && (k.current.style.width = se + "px"), (!oe || ve > oe) && M.top + ve < $.height && (k.current.style.height = ve + "px"), me.current = P.pageX, ce.current = P.pageY, r.onResize && r.onResize(P);
    }
  }, _e = function(P) {
    te.current && (te.current = !1, j.removeClass(document.body, "p-unselectable-text"), r.onResizeEnd && r.onResizeEnd(P));
  }, Ue = function() {
    k.current.style.position = "", k.current.style.left = "", k.current.style.top = "", k.current.style.margin = "";
  }, $e = function() {
    k.current.setAttribute(Fe.current, "");
  }, ze = function() {
    r.onShow && r.onShow(), r.focusOnShow && Ht(), it();
  }, ft = function() {
    r.modal && !Ce() && j.addClass(I.current, "p-component-overlay-leave");
  }, Jt = function() {
    le.current = !1, Ki.clear(I.current), f(!1), rt(), j.focus(be.current), be.current = null;
  }, it = function() {
    Dt();
  }, rt = function() {
    Lt();
  }, dt = function() {
    var P = document.primeDialogParams && document.primeDialogParams.some(function(q) {
      return q.hasBlockScroll;
    });
    P ? j.blockBodyScroll() : j.unblockBodyScroll();
  }, Mt = function(P) {
    if (P && p) {
      var q = {
        id: l,
        hasBlockScroll: B
      };
      document.primeDialogParams || (document.primeDialogParams = []);
      var ge = document.primeDialogParams.findIndex(function(Oe) {
        return Oe.id === l;
      });
      ge === -1 ? document.primeDialogParams = [].concat(KX(document.primeDialogParams), [q]) : document.primeDialogParams = document.primeDialogParams.toSpliced(ge, 1, q);
    } else
      document.primeDialogParams = document.primeDialogParams && document.primeDialogParams.filter(function(Oe) {
        return Oe.id !== l;
      });
    dt();
  }, Dt = function() {
    r.draggable && (Rn(), bn()), r.resizable && (H(), Ye());
  }, Lt = function() {
    rn(), On(), pe(), Ze();
  }, Nt = function() {
    Ae.current = j.createInlineStyle(n && n.nonce || tn.nonce, n && n.styleContainer);
    var P = "";
    for (var q in r.breakpoints)
      P = P + `
                @media screen and (max-width: `.concat(q, `) {
                     [data-pc-name="dialog"][`).concat(Fe.current, `] {
                        width: `).concat(r.breakpoints[q], ` !important;
                    }
                }
            `);
    Ae.current.innerHTML = P;
  }, on = function() {
    Ae.current = j.removeInlineStyle(Ae.current);
  };
  rr(function() {
    Mt(!0), r.visible && f(!0);
  }), w.useEffect(function() {
    return r.breakpoints && Nt(), function() {
      on();
    };
  }, [r.breakpoints]), Zt(function() {
    r.visible && !d && f(!0), r.visible !== p && d && b(r.visible), r.visible && (be.current = document.activeElement);
  }, [r.visible, d]), Zt(function() {
    d && (Ki.set("modal", I.current, n && n.autoZIndex || tn.autoZIndex, r.baseZIndex || n && n.zIndex.modal || tn.zIndex.modal), b(!0));
  }, [d]), Zt(function() {
    Mt(!0);
  }, [B, p]), wr(function() {
    rt(), Mt(!1), j.removeInlineStyle(Ae.current), Ki.clear(I.current);
  }), w.useImperativeHandle(t, function() {
    return {
      props: r,
      resetPosition: Ue,
      getElement: function() {
        return k.current;
      },
      getMask: function() {
        return I.current;
      },
      getContent: function() {
        return L.current;
      },
      getHeader: function() {
        return R.current;
      },
      getFooter: function() {
        return O.current;
      },
      getCloseButton: function() {
        return N.current;
      }
    };
  });
  var Pt = function() {
    if (r.closable) {
      var P = r.ariaCloseIconLabel || la("close"), q = e({
        className: we("closeButtonIcon"),
        "aria-hidden": !0
      }, Y("closeButtonIcon")), ge = r.closeIcon || /* @__PURE__ */ w.createElement(uh, q), Oe = Mi.getJSXIcon(ge, Bf({}, q), {
        props: r
      }), ue = e({
        ref: N,
        type: "button",
        className: we("closeButton"),
        "aria-label": P,
        onClick: Vt,
        onKeyDown: function($) {
          $.key !== "Escape" && $.stopPropagation();
        }
      }, Y("closeButton"));
      return /* @__PURE__ */ w.createElement("button", ue, Oe, /* @__PURE__ */ w.createElement(nc, null));
    }
    return null;
  }, vn = function() {
    var P, q = e({
      className: we("maximizableIcon")
    }, Y("maximizableIcon"));
    re ? P = r.minimizeIcon || /* @__PURE__ */ w.createElement(m5, q) : P = r.maximizeIcon || /* @__PURE__ */ w.createElement(f5, q);
    var ge = Mi.getJSXIcon(P, q, {
      props: r
    });
    if (r.maximizable) {
      var Oe = e({
        type: "button",
        className: we("maximizableButton"),
        onClick: Wt
      }, Y("maximizableButton"));
      return /* @__PURE__ */ w.createElement("button", Oe, ge, /* @__PURE__ */ w.createElement(nc, null));
    }
    return null;
  }, wn = function() {
    if (r.showHeader) {
      var P = Pt(), q = vn(), ge = z.getJSXElement(r.icons, r), Oe = z.getJSXElement(r.header, r), ue = l + "_header", M = e({
        ref: R,
        style: r.headerStyle,
        className: we("header"),
        onMouseDown: Yt
      }, Y("header")), $ = e({
        id: ue,
        className: we("headerTitle")
      }, Y("headerTitle")), Z = e({
        className: we("headerIcons")
      }, Y("headerIcons"));
      return /* @__PURE__ */ w.createElement("div", M, /* @__PURE__ */ w.createElement("div", $, Oe), /* @__PURE__ */ w.createElement("div", Z, ge, q, P));
    }
    return null;
  }, yn = function() {
    var P = l + "_content", q = e({
      id: P,
      ref: L,
      style: r.contentStyle,
      className: we("content")
    }, Y("content"));
    return /* @__PURE__ */ w.createElement("div", q, r.children);
  }, ni = function() {
    var P = z.getJSXElement(r.footer, r), q = e({
      ref: O,
      className: we("footer")
    }, Y("footer"));
    return P && /* @__PURE__ */ w.createElement("div", q, P);
  }, zi = function() {
    return r.resizable ? /* @__PURE__ */ w.createElement("span", {
      className: "p-resizable-handle",
      style: {
        zIndex: 90
      },
      onMouseDown: un
    }) : null;
  }, ii = function() {
    var P, q = {
      header: r.header,
      content: r.message,
      message: r == null || (P = r.children) === null || P === void 0 || (P = P[1]) === null || P === void 0 || (P = P.props) === null || P === void 0 ? void 0 : P.children
    }, ge = {
      headerRef: R,
      contentRef: L,
      footerRef: O,
      closeRef: N,
      hide: Vt,
      message: q
    };
    return z.getJSXElement(i.content, ge);
  }, qn = function() {
    var P = wn(), q = yn(), ge = ni(), Oe = zi();
    return /* @__PURE__ */ w.createElement(w.Fragment, null, P, q, ge, Oe);
  }, Si = function() {
    var P = l + "_header", q = l + "_content", ge = {
      enter: r.position === "center" ? 150 : 300,
      exit: r.position === "center" ? 150 : 300
    }, Oe = e({
      ref: I,
      style: ye("mask"),
      className: we("mask"),
      onPointerUp: jt
    }, Y("mask")), ue = e({
      ref: k,
      id: l,
      className: Re(r.className, we("root", {
        props: r,
        maximized: re,
        context: n
      })),
      style: r.style,
      onClick: r.onClick,
      role: "dialog",
      "aria-labelledby": P,
      "aria-describedby": q,
      "aria-modal": r.modal,
      onPointerDown: hi
    }, mu.getOtherProps(r), Y("root")), M = e({
      classNames: we("transition"),
      timeout: ge,
      in: p,
      options: r.transitionOptions,
      unmountOnExit: !0,
      onEnter: $e,
      onEntered: ze,
      onExiting: ft,
      onExited: Jt
    }, Y("transition")), $ = null;
    i != null && i.content ? $ = ii() : $ = qn();
    var Z = /* @__PURE__ */ w.createElement("div", Oe, /* @__PURE__ */ w.createElement(lh, kg({
      nodeRef: k
    }, M), /* @__PURE__ */ w.createElement("div", ue, /* @__PURE__ */ w.createElement(iQ, {
      autoFocus: r.focusOnShow
    }, $))));
    return /* @__PURE__ */ w.createElement(xc, {
      element: Z,
      appendTo: r.appendTo,
      visible: !0
    });
  };
  return d && Si();
});
p5.displayName = "Dialog";
function Eg() {
  return Eg = Object.assign ? Object.assign.bind() : function(i) {
    for (var t = 1; t < arguments.length; t++) {
      var e = arguments[t];
      for (var n in e)
        Object.prototype.hasOwnProperty.call(e, n) && (i[n] = e[n]);
    }
    return i;
  }, Eg.apply(this, arguments);
}
function lQ(i) {
  if (Array.isArray(i)) return i;
}
function cQ(i, t) {
  var e = i == null ? null : typeof Symbol < "u" && i[Symbol.iterator] || i["@@iterator"];
  if (e != null) {
    var n, r, s, o, a = [], l = !0, c = !1;
    try {
      if (s = (e = e.call(i)).next, t !== 0) for (; !(l = (n = s.call(e)).done) && (a.push(n.value), a.length !== t); l = !0) ;
    } catch (u) {
      c = !0, r = u;
    } finally {
      try {
        if (!l && e.return != null && (o = e.return(), Object(o) !== o)) return;
      } finally {
        if (c) throw r;
      }
    }
    return a;
  }
}
function Yy(i, t) {
  (t == null || t > i.length) && (t = i.length);
  for (var e = 0, n = new Array(t); e < t; e++) n[e] = i[e];
  return n;
}
function uQ(i, t) {
  if (i) {
    if (typeof i == "string") return Yy(i, t);
    var e = Object.prototype.toString.call(i).slice(8, -1);
    if (e === "Object" && i.constructor && (e = i.constructor.name), e === "Map" || e === "Set") return Array.from(i);
    if (e === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(e)) return Yy(i, t);
  }
}
function dQ() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function Jy(i, t) {
  return lQ(i) || cQ(i, t) || uQ(i, t) || dQ();
}
function uc(i) {
  "@babel/helpers - typeof";
  return uc = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(t) {
    return typeof t;
  } : function(t) {
    return t && typeof Symbol == "function" && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
  }, uc(i);
}
function hQ(i, t) {
  if (uc(i) !== "object" || i === null) return i;
  var e = i[Symbol.toPrimitive];
  if (e !== void 0) {
    var n = e.call(i, t || "default");
    if (uc(n) !== "object") return n;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (t === "string" ? String : Number)(i);
}
function fQ(i) {
  var t = hQ(i, "string");
  return uc(t) === "symbol" ? t : String(t);
}
function mQ(i, t, e) {
  return t = fQ(t), t in i ? Object.defineProperty(i, t, {
    value: e,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : i[t] = e, i;
}
var gQ = {
  root: "p-confirm-dialog",
  message: "p-confirm-dialog-message",
  icon: "p-confirm-dialog-icon",
  acceptButton: "p-confirm-dialog-accept",
  rejectButton: function(t) {
    var e = t.getPropValue;
    return Re("p-confirm-dialog-reject", {
      "p-button-text": !e("rejectClassName")
    });
  }
}, gu = et.extend({
  defaultProps: {
    __TYPE: "ConfirmDialog",
    accept: null,
    acceptClassName: null,
    acceptIcon: null,
    acceptLabel: null,
    appendTo: null,
    breakpoints: null,
    children: void 0,
    className: null,
    content: null,
    defaultFocus: "accept",
    footer: null,
    icon: null,
    message: null,
    onHide: null,
    reject: null,
    rejectClassName: null,
    rejectIcon: null,
    rejectLabel: null,
    tagKey: void 0,
    visible: void 0
  },
  css: {
    classes: gQ
  }
});
function Xy(i, t) {
  var e = Object.keys(i);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(i);
    t && (n = n.filter(function(r) {
      return Object.getOwnPropertyDescriptor(i, r).enumerable;
    })), e.push.apply(e, n);
  }
  return e;
}
function Bo(i) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t] != null ? arguments[t] : {};
    t % 2 ? Xy(Object(e), !0).forEach(function(n) {
      mQ(i, n, e[n]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(i, Object.getOwnPropertyDescriptors(e)) : Xy(Object(e)).forEach(function(n) {
      Object.defineProperty(i, n, Object.getOwnPropertyDescriptor(e, n));
    });
  }
  return i;
}
var pQ = function() {
  var t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
  t = Bo(Bo({}, t), {
    visible: t.visible === void 0 ? !0 : t.visible
  }), t.visible && Hs.emit("confirm-dialog", t);
  var e = function() {
    var s = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    Hs.emit("confirm-dialog", Bo(Bo(Bo({}, t), s), {
      visible: !0
    }));
  }, n = function() {
    Hs.emit("confirm-dialog", {
      visible: !1
    });
  };
  return {
    show: e,
    hide: n
  };
}, b5 = /* @__PURE__ */ w.memo(/* @__PURE__ */ w.forwardRef(function(i, t) {
  var e = Qn(), n = w.useContext(cn), r = gu.getProps(i, n), s = w.useState(r.visible), o = Jy(s, 2), a = o[0], l = o[1], c = w.useState(!1), u = Jy(c, 2), d = u[0], f = u[1], m = w.useRef(null), g = w.useRef(!1), p = w.useRef(null), b = function() {
    var re = r.group;
    return m.current && (re = m.current.group), Object.assign({}, r, m.current, {
      group: re
    });
  }, v = function(re) {
    return b()[re];
  }, y = function(re) {
    for (var B = arguments.length, X = new Array(B > 1 ? B - 1 : 0), he = 1; he < B; he++)
      X[he - 1] = arguments[he];
    return z.getPropValue(v(re), X);
  }, x = v("acceptLabel") || la("accept"), S = v("rejectLabel") || la("reject"), k = {
    props: r,
    state: {
      visible: a
    }
  }, I = gu.setMetaData(k), A = I.ptm, L = I.cx, R = I.isUnstyled;
  sr(gu.css.styles, R, {
    name: "confirmdialog"
  });
  var O = function() {
    g.current || (g.current = !0, y("accept"), te("accept"));
  }, N = function() {
    g.current || (g.current = !0, y("reject"), te("reject"));
  }, le = function() {
    var re = b();
    re.group === r.group && (l(!0), g.current = !1, p.current = document.activeElement);
  }, te = function() {
    var re = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "cancel";
    l(!1), y("onHide", {
      result: re
    }), j.focus(p.current), p.current = null;
  }, me = function(re) {
    if (re.tagKey === r.tagKey) {
      var B = a !== re.visible, X = v("target") !== re.target;
      X && !r.target ? (te(), m.current = re, f(!0)) : B && (m.current = re, re.visible ? le() : te());
    }
  };
  w.useEffect(function() {
    r.visible ? le() : te();
  }, [r.visible]), w.useEffect(function() {
    return !r.target && !r.message && Hs.on("confirm-dialog", me), function() {
      Hs.off("confirm-dialog", me);
    };
  }, [r.target]), Zt(function() {
    d && le();
  }, [d]), wr(function() {
    Hs.off("confirm-dialog", me);
  }), w.useImperativeHandle(t, function() {
    return {
      props: r,
      confirm: me
    };
  });
  var ce = function() {
    var re = v("defaultFocus"), B = Re("p-confirm-dialog-accept", v("acceptClassName")), X = Re("p-confirm-dialog-reject", {
      "p-button-text": !v("rejectClassName")
    }, v("rejectClassName")), he = e({
      label: S,
      autoFocus: re === "reject",
      icon: v("rejectIcon"),
      className: Re(v("rejectClassName"), L("rejectButton", {
        getPropValue: v
      })),
      onClick: N,
      pt: A("rejectButton"),
      unstyled: r.unstyled,
      __parentMetadata: {
        parent: k
      }
    }, A("rejectButton")), Q = e({
      label: x,
      autoFocus: re === void 0 || re === "accept",
      icon: v("acceptIcon"),
      className: Re(v("acceptClassName"), L("acceptButton")),
      onClick: O,
      pt: A("acceptButton"),
      unstyled: r.unstyled,
      __parentMetadata: {
        parent: k
      }
    }, A("acceptButton")), Y = /* @__PURE__ */ w.createElement(w.Fragment, null, /* @__PURE__ */ w.createElement(Mn, he), /* @__PURE__ */ w.createElement(Mn, Q));
    if (v("footer")) {
      var we = {
        accept: O,
        reject: N,
        acceptClassName: B,
        rejectClassName: X,
        acceptLabel: x,
        rejectLabel: S,
        element: Y,
        props: b()
      };
      return z.getJSXElement(v("footer"), we);
    }
    return Y;
  }, Ae = function() {
    var re = b(), B = z.getJSXElement(v("message"), re), X = e({
      className: L("icon")
    }, A("icon")), he = Mi.getJSXIcon(v("icon"), Bo({}, X), {
      props: re
    }), Q = ce(), Y = e({
      className: L("message")
    }, A("message")), we = e({
      visible: a,
      className: Re(v("className"), L("root")),
      footer: Q,
      onHide: te,
      breakpoints: v("breakpoints"),
      pt: re.pt,
      unstyled: r.unstyled,
      appendTo: v("appendTo"),
      __parentMetadata: {
        parent: k
      }
    }, gu.getOtherProps(re));
    return /* @__PURE__ */ w.createElement(p5, Eg({}, we, {
      content: i == null ? void 0 : i.content
    }), he, /* @__PURE__ */ w.createElement("span", Y, B));
  }, Fe = Ae();
  return /* @__PURE__ */ w.createElement(xc, {
    element: Fe,
    appendTo: v("appendTo")
  });
}));
b5.displayName = "ConfirmDialog";
function bQ({ id: i, item: t, active: e, setActive: n, onItemChange: r, onItemRemove: s }) {
  const { attributes: o, listeners: a, setNodeRef: l, setActivatorNodeRef: c, transform: u, transition: d } = Ch({
    id: i,
    data: {
      id: i,
      item: t
    }
  }), f = {
    transform: Bi.Transform.toString(u),
    transition: d
  };
  let m = "sortable-item";
  t.id == (e == null ? void 0 : e.id) && (m = m + " active");
  const g = [Js.type].includes(t.type), p = () => {
    pQ({
      message: "Are you sure you want to remove this item?",
      header: "Confirmation",
      icon: "pi pi-exclamation-triangle",
      accept: () => s(i)
      // reject: () => rejectFunc()
    });
  };
  let b = /* @__PURE__ */ _.jsx(Mn, { size: "small", outlined: !0, label: "Edit", icon: "pi pi-pencil", onClick: () => n(t) });
  return e && e.id == t.id && (b = /* @__PURE__ */ _.jsx(Mn, { size: "small", outlined: !0, label: "Done", icon: "pi pi-pencil", onClick: () => n(void 0) })), /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { ref: l, style: f, ...o, className: m, children: [
    /* @__PURE__ */ _.jsx(lb, { item: t, onChange: r }),
    " ",
    " ",
    g ? /* @__PURE__ */ _.jsx("div", { className: "mask" }) : null,
    /* @__PURE__ */ _.jsxs("div", { className: "flex flex-row align-items-center item-footer", children: [
      /* @__PURE__ */ _.jsxs("div", { className: "flex-grow-1 item-type", children: [
        " ",
        t.type
      ] }),
      /* @__PURE__ */ _.jsxs(h5, { children: [
        " ",
        /* @__PURE__ */ _.jsx(
          Mn,
          {
            ...a,
            ref: c,
            size: "small",
            outlined: !0,
            label: "Order",
            icon: "pi pi-sort"
          }
        ),
        b,
        /* @__PURE__ */ _.jsx(Mn, { size: "small", outlined: !0, label: "Remove", icon: "pi pi-trash", onClick: p })
      ] })
    ] })
  ] }) });
}
function vQ({ activeItem: i, setActiveItem: t }) {
  const { form: e, setForm: n } = Hr(), [r, s] = Ne(e.items ?? []);
  Ee(() => Hr.subscribe((m) => {
    s(pn(m.form.items));
  }));
  const o = (m) => {
    const g = pn(e);
    g.items = g.items.filter(
      (p) => p.id != m
    ), n(g), s(g.items), i && i.id == m && t(void 0);
  }, a = (m) => {
    const g = pn(e);
    g.items = r.map((p) => p.id == m.id ? m : p), n(g), s(g.items);
  }, { listeners: l, setNodeRef: c, transform: u, transition: d } = r5({
    id: "canvas_droppable",
    data: {
      parent: null,
      isContainer: !0
    }
  }), f = {
    transform: Bi.Transform.toString(u),
    transition: d
  };
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { ref: c, className: "canvas form-area", style: f, ...l, children: [
    /* @__PURE__ */ _.jsx("h1", { children: e.label }),
    r == null ? void 0 : r.map((m) => /* @__PURE__ */ _.jsx(
      bQ,
      {
        id: m.id,
        item: m,
        active: i,
        setActive: t,
        onItemChange: a,
        onItemRemove: o
      },
      m.id
    ))
  ] }) });
}
function wQ({ ...i }) {
  const { form: t, setForm: e } = Hr(), [n, r] = Ne(i.activeItem), s = (l) => {
    r(l);
    const c = pn(t);
    c.items = c.items.map((u) => u.id == l.id ? l : u), e(c);
  }, o = (l) => {
    const c = { ...t, [l.target.name]: l.target.value };
    e(c);
  }, a = () => {
    i.setActiveItem(void 0);
  };
  if (Ee(() => {
    r(i.activeItem);
  }, [i.activeItem]), n) {
    const l = ks.getByItem(n);
    if (l) {
      const c = l.icon;
      return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "properties active", children: [
        /* @__PURE__ */ _.jsxs("div", { className: "item-panel-item flex align-items-center", children: [
          /* @__PURE__ */ _.jsxs("div", { className: "flex-grow-1 flex align-items-center", children: [
            /* @__PURE__ */ _.jsx("div", { className: "item-icon mr-2", children: /* @__PURE__ */ _.jsx(c, {}) }),
            /* @__PURE__ */ _.jsx("div", { children: /* @__PURE__ */ _.jsx("h2", { children: l.heading }) })
          ] }),
          /* @__PURE__ */ _.jsx(Mn, { onClick: a, size: "small", outlined: !0, icon: "pi pi-times" })
        ] }),
        /* @__PURE__ */ _.jsx(jX, { item: n, onChange: s })
      ] }) });
    }
    return Se.createElement(
      () => /* @__PURE__ */ _.jsxs("div", { children: [
        "The component ",
        n.type,
        " was not found."
      ] })
    );
  } else
    return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "properties", children: [
      /* @__PURE__ */ _.jsx("div", { className: "item-panel-item flex px-3", children: /* @__PURE__ */ _.jsx("div", { children: /* @__PURE__ */ _.jsx("h2", { children: "Form Settings" }) }) }),
      /* @__PURE__ */ _.jsx(or, { activeIndex: 0, children: /* @__PURE__ */ _.jsx(di, { header: "General", children: /* @__PURE__ */ _.jsx("div", { className: "form-item flex flex-column gap-4", children: /* @__PURE__ */ _.jsxs("div", { className: "flex flex-column gap-2", children: [
        /* @__PURE__ */ _.jsx("label", { htmlFor: "label", children: "Form label" }),
        /* @__PURE__ */ _.jsx(
          _t,
          {
            onChange: o,
            name: "label",
            value: t.label ?? "",
            className: "w-full"
          }
        )
      ] }) }) }, "general") })
    ] }) });
}
function v5({ item: i }) {
  const t = i.icon;
  return /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("article", { className: "sidebar-field", children: /* @__PURE__ */ _.jsxs("div", { className: "item-panel-item flex p-2 w-100", children: [
    /* @__PURE__ */ _.jsx("div", { className: "item-icon mr-2", children: /* @__PURE__ */ _.jsx(t, {}) }),
    /* @__PURE__ */ _.jsxs("div", { children: [
      /* @__PURE__ */ _.jsx("div", { className: "item-title", children: i.heading }),
      /* @__PURE__ */ _.jsx("div", { className: "item-text", children: i.description })
    ] })
  ] }) }) });
}
function yQ({ item: i }) {
  const t = at(br()), { attributes: e, listeners: n, setNodeRef: r } = n5({
    id: t.current,
    data: {
      item: i,
      fromSidebar: !0
    }
  });
  return /* @__PURE__ */ _.jsx("div", { ref: r, className: "sidebar-field", ...e, ...n, children: /* @__PURE__ */ _.jsx(v5, { item: i }) });
}
function _Q({ ...i }) {
  const t = {};
  return ks.map((e) => {
    var r, s;
    const n = ((r = e.group) == null ? void 0 : r.label) || "";
    Object.keys(t).includes(n) || (t[n] = {
      label: ((s = e.group) == null ? void 0 : s.label) || "",
      items: []
    }), t[n].items.push(e);
  }), /* @__PURE__ */ _.jsxs(_.Fragment, { children: [
    " ",
    " ",
    " ",
    " ",
    " ",
    " ",
    " ",
    /* @__PURE__ */ _.jsx(or, { activeIndex: 0, children: Object.keys(t).map((e) => /* @__PURE__ */ _.jsx(di, { header: t[e].label, children: t[e].items.map((n) => /* @__PURE__ */ _.jsx(yQ, { item: n }, `sidebar-${n.type}`)) }, e)) })
  ] });
}
function Ja(i) {
  var t;
  return ((t = i == null ? void 0 : i.data) == null ? void 0 : t.current) ?? {};
}
const CQ = ({ children: i, activeItem: t, setActiveItem: e }) => {
  const { form: n, setForm: r } = Hr(), s = gh(
    gs(Ea),
    gs(xa, {
      coordinateGetter: kh
    })
  ), [o, a] = Ne(), [l, c] = Ne(), u = () => {
    a(void 0), c(void 0);
  }, d = (g) => {
    const { active: p } = g, b = Ja(p), { item: v } = b;
    if (b.fromSidebar) {
      const y = v.data;
      y.id = br(), v.id = y.id, a(v), e(y);
    } else
      c(b.item);
  }, f = (g) => {
    const { active: p, over: b } = g, v = Ja(p), y = Ja(b), x = pn(n);
    if (v.fromSidebar)
      if (x.items.findIndex((S) => S.id === v.item.id) == -1) {
        x.items.push(v.item.data);
        const S = x.items.findIndex((I) => I.id === v.item.id), k = x.items.findIndex((I) => I.id === y.id);
        k != -1 && k != S && (x.items = as(x.items, S, k)), r(x);
      } else if (!b)
        x.items = x.items.filter((S) => S.id !== v.item.id), r(x);
      else {
        const S = x.items.findIndex((I) => I.id === v.item.id), k = x.items.findIndex((I) => I.id === y.id);
        n.items = as(x.items, S, k), r(x);
      }
    else
      x.items.findIndex((S) => S.id === v.item.id), x.items.findIndex((S) => S.id === y.id);
  }, m = (g) => {
    const { active: p, over: b } = g, v = pn(n), y = Ja(p), x = Ja(b), S = v.items.findIndex((I) => I.id === y.item.id), k = v.items.findIndex((I) => I.id === x.id);
    S != k && (v.items = as(n.items, S, k)), r(v), u();
  };
  return n ? /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs(
    yh,
    {
      sensors: s,
      onDragStart: d,
      onDragOver: f,
      onDragEnd: m,
      collisionDetection: ph,
      autoScroll: !0,
      children: [
        i,
        /* @__PURE__ */ _.jsxs(bJ, { dropAnimation: null, children: [
          o ? /* @__PURE__ */ _.jsx(v5, { item: o }) : null,
          " ",
          l ? /* @__PURE__ */ _.jsx(
            lb,
            {
              item: l,
              onChange: () => {
              }
            }
          ) : null
        ] })
      ]
    }
  ) }) : /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("div", { children: "No form config loaded" }) });
}, WQ = ({ ...i }) => {
  const [t, e] = Ne(void 0), { form: n, setForm: r } = Hr(), s = _r((o) => o.setConfig);
  return Ee(() => {
    r(i.form);
  }, [i.form]), Ee(() => {
    s(i.config);
  }, [i.config]), Ee(() => Hr.subscribe((o) => {
    i.onChange(pn(o.form));
  })), /* @__PURE__ */ _.jsxs(_.Fragment, { children: [
    /* @__PURE__ */ _.jsx("div", { className: "form-builder", children: /* @__PURE__ */ _.jsxs(
      CQ,
      {
        setActiveItem: e,
        activeItem: t,
        children: [
          /* @__PURE__ */ _.jsx("div", { className: "sidebar-area", children: /* @__PURE__ */ _.jsx(
            _Q,
            {
              setActiveItem: e,
              activeItem: t
            }
          ) }),
          /* @__PURE__ */ _.jsx("div", { className: "canvas-area", children: /* @__PURE__ */ _.jsx(
            _h,
            {
              strategy: _J,
              items: n.items.map((o) => o.id),
              children: /* @__PURE__ */ _.jsx(
                vQ,
                {
                  setActiveItem: e,
                  activeItem: t
                }
              )
            }
          ) }),
          /* @__PURE__ */ _.jsx("div", { className: "properties-area", children: /* @__PURE__ */ _.jsx(
            wQ,
            {
              setActiveItem: e,
              activeItem: t
            }
          ) })
        ]
      }
    ) }),
    /* @__PURE__ */ _.jsx(b5, {})
  ] });
}, kQ = ({ ...i }) => {
  const { form: t, setForm: e } = Hr(), n = _r((a) => a.setConfig), [r, s] = Ne(t.items);
  Ee(() => {
    e(i.form), s(i.form.items);
  }, [i.form]), Ee(() => {
    n(i.config);
  }, [i.config]), Ee(() => Hr.subscribe((a) => {
    i.onChange(pn(a.form)), s(a.form.items);
  }));
  const o = (a) => {
    const l = pn(t);
    l.items = t.items.map((c) => c.id == a.id ? a : c), s(l.items), e(l);
  };
  return t && t.id ? /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsxs("div", { className: "form-area", children: [
    /* @__PURE__ */ _.jsx("h1", { children: t.label }),
    r.map((a) => /* @__PURE__ */ _.jsx(
      lb,
      {
        item: a,
        onChange: o
      },
      a.id
    ))
  ] }) }) : /* @__PURE__ */ _.jsx(_.Fragment, { children: /* @__PURE__ */ _.jsx("div", { children: "No form config loaded" }) });
};
export {
  kQ as Form,
  WQ as FormBuilder,
  lb as Item
};
//# sourceMappingURL=index.js.map
